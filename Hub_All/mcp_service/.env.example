# MCP Service — mẫu cấu hình môi trường
# Copy sang .env và chỉnh theo môi trường triển khai.

# Base URL của API Service (KHÔNG kèm /api). Chỉ chấp nhận scheme http hoặc https.
MCP_API_BASE_URL=http://localhost:8180

# Host và port MCP Service lắng nghe.
MCP_SERVICE_HOST=0.0.0.0
MCP_SERVICE_PORT=8190

# Timeout (giây) cho mỗi request HTTP tới API Service.
MCP_HTTP_TIMEOUT=30

# --- OAuth (Phase 8.3) ---
# URL public HTTPS để Claude web kết nối connector. Prod PHẢI https.
MCP_OAUTH_ISSUER_URL=http://localhost:8190
# File SQLite lưu OAuth state (clients/codes/tokens/pending). Docker: override /app/.oauth/state.db.
MCP_OAUTH_STATE_DB_PATH=.oauth/state.db
# Lifetime token OAuth (giây).
MCP_OAUTH_ACCESS_TOKEN_TTL=3600
MCP_OAUTH_REFRESH_TOKEN_TTL=2592000
