# docker-compose.override.yml
#
# Dev/smoke-only override — auto-merged khi `docker compose up`.
# Map port 8001 ra host để curl smoke trực tiếp từ Windows.
# Production deploy KHÔNG dùng file này (rename hoặc xóa khi build prod).
#
# Tạo bởi smoke verify Phase 2 (2026-04-28). KHÔNG commit nếu không cần dev local.

services:
  docling-pipeline:
    ports:
      - "8001:8001"
