# Local Claude Code config
.claude/

# Local test data
file_test/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# === M2 Python stack (Plan 01) ===

# Python venv & cache
api/.venv/
api/__pycache__/
api/**/__pycache__/
api/.pytest_cache/
api/.mypy_cache/
api/.ruff_cache/
api/*.egg-info/

# Secrets (M2)
api/keys/
api/keys/*.pem
api/.env
mcp_service/.env

# File store (local uploads)
file_store/

# Postgres data volume
medinet_pgdata/

# === Cocoindex 1.0.3 LMDB state (Plan 04-03 Q5) ===
# Cocoindex 1.0.3 lưu memo cache + fingerprint + lineage local LMDB.
# Operator KHÔNG commit state — re-build từ scratch acceptable cho M2.
.cocoindex/
api/.cocoindex/

# === Plan 02-05 FACTOR-04 — Dynamic hub registration override (operator-local) ===
# Template `docker-compose.override.yml.template` PHẢI commit (source-of-truth).
# Override file `docker-compose.override.yml` operator-local (auto-generated bởi
# `make hub-add`) — chứa hub list cá nhân (vd `tmp_test`, `dev_<engineer>`),
# KHÔNG commit để tránh leak operator-local hub list (T-02-05-04 Info Disclosure).
docker-compose.override.yml
