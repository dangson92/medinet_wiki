# Requirements: Medinet Wiki — Milestone v3.0 (Multi-Hub Split)

**Defined:** 2026-05-21
**Milestone:** v3.0 — Multi-Hub Split (subpath routing + multi-DB physical isolation)
**Core Value:** Ingestion tri thức Medinet phải tái hiện trung thực cấu trúc tài liệu nguồn — biến mọi tài liệu y tế / dược / HCNS thành chunk semantic giàu metadata. v3.0 mở rộng theo trục tách hub vật lý (process + DB riêng cùng instance) + cross-hub aggregation tại central, top-3 retrieval vẫn ≥75% trên eval set thật.
**Granularity:** Large (7 phases) · **Mode:** YOLO · **Phase numbering:** Reset về 1 (D-V3-05)

> ⚠️ **REPLACE — M2 requirements (v2.0 Full RAG Rewrite) shipped 2026-05-21.** Toàn bộ 38 REQ-ID của M2 archive vào `.planning/milestones/v2.0-full-rag-rewrite/REQUIREMENTS.md`. v3.0 = bộ REQ-ID mới hoàn toàn (7 category: TOPO/FACTOR/SSO/SYNC/PROXY/SETTINGS/MIGRATE).

---

## v1 Requirements (v3.0 scope — 29 REQ-ID)

### TOPO — Multi-DB Topology + Per-hub Alembic (4 REQ)

- [x] **TOPO-01** ✅ Phase 1: Postgres init script idempotent tạo N+1 logical DB cùng instance — `medinet_central` (đã có từ M2) + `medinet_hub_yte` + `medinet_hub_duoc` + `medinet_hub_hcns`. Mỗi DB có `CREATE EXTENSION vector` + HNSW index 1536-dim verify build được. Khả năng thêm hub động (`medinet_hub_<new>`) qua make target `make hub-init HUB=<name>` mà KHÔNG cần down instance.
- [x] **TOPO-02** ✅ Phase 1: Per-hub Alembic migration set — mỗi DB hub con có `alembic_version` table riêng, head revision khớp giữa các DB. Make target `make migrate-all` apply migrations tuần tự N database + verify version match. CI lint check `alembic current` returns cùng head SHA cho tất cả DB sau migration (R-V3-3 mitigation).
- [x] **TOPO-03** ✅ Phase 1: Cocoindex flow naming per-hub — `medinet_<hub>_ingest` (`medinet_yte_ingest`, `medinet_duoc_ingest`, ...). APP_NAMESPACE per-hub `medinet_<hub>_prod` đảm bảo cocoindex internal tables không đụng nhau giữa các DB. `db_schema_name="cocoindex"` giữ nguyên (R5 carry forward).
- [x] **TOPO-04** ✅ Phase 1: Per-hub Postgres connection pool + isolation env — `HUB_NAME=yte` → kết nối `medinet_hub_yte` (KHÔNG fallback central). Helper `get_engine(hub_name)` resolve URL từ env + connection pool size config-driven. Test integration: `HUB_NAME=yte` deploy KHÔNG truy cập được `medinet_hub_duoc` qua DB connection (R-V3-3 + E-V3-3 enforce).

### FACTOR — Hub-con Codebase Factor (4 REQ)

- [ ] **FACTOR-01**: 1 codebase deploy được nhiều lần với env `HUB_NAME=<name>` (`central` vs `yte` vs `duoc` vs `hcns`). App factory `create_app(hub_name)` đọc env, mount router phù hợp, khởi tạo cocoindex flow đúng tên (TOPO-03). Docker compose mở rộng từ 3 service (M2) sang 1 Postgres + 1 Redis + N+1 process FastAPI.
- [x] **FACTOR-02**: Strip system settings router khi `HUB_NAME != "central"` — hub con KHÔNG mount `/api/rag-config` (GET/PUT), `/api/api-keys` (GET/POST/DELETE), `/api/hubs` (POST/PUT/PATCH), `/api/users` (admin CRUD), `/api/audit-logs` (GET). Test integration: `GET /api/rag-config` ở hub con trả 404 (KHÔNG 403 — endpoint không exist). Central giữ nguyên 100% endpoint M2. — **DONE 2026-05-22** (Plan 02-01 unit-level 9/9 + Plan 02-03 integration-level 10/10 PASS; Starlette HTTPException handler wrap 404 envelope D6 shape — Rule 2 auto-add ở Plan 02-03)
- [x] **FACTOR-03**: Hub con expose endpoint hạn chế (10 endpoint hub-scoped collective / 12 specific) — `POST/GET /api/auth/*` (4: login/refresh/logout/me), `GET/PATCH /api/profile` (2), `POST/GET/DELETE /api/documents/*` (3), `POST /api/search`, `POST /api/ask`, `GET /api/usage`. KHÔNG expose cross-hub search (đó là endpoint central). Smoke test mỗi endpoint trả 200 hoặc lỗi đúng shape envelope. — **DONE 2026-05-22** (Plan 02-03 integration test verify 3 hub × 12 endpoint = 36 assertion non-404 PASS)

  > **NOTE clarify (Phase 2 closeout 2026-05-22 — WRN-05 fix):** Số "10 endpoint hub-scoped" trong label ROADMAP/REQUIREMENTS gốc là cách đếm **collective endpoint group** (gộp `/api/auth/*` 4 verb thành 1 group, `/api/documents` GET/POST/DELETE thành 1 group, `/api/profile` GET/PATCH thành 1 group, etc.). Implementation thực tế tính theo HTTP method × path = **12 specific HTTP method endpoints**:
  >
  > 1. `POST /api/auth/login`
  > 2. `POST /api/auth/refresh`
  > 3. `POST /api/auth/logout`
  > 4. `GET  /api/auth/me`
  > 5. `GET  /api/profile`
  > 6. `PATCH /api/profile`
  > 7. `POST /api/documents`
  > 8. `GET  /api/documents`
  > 9. `DELETE /api/documents/{id}`
  > 10. `POST /api/search`
  > 11. `POST /api/ask`
  > 12. `GET  /api/usage`
  >
  > Xem `.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md` D-V3-Phase2-D matrix để hiểu chi tiết breakdown collective vs specific. Integration test `tests/integration/test_factor_hub_scoped.py` (Plan 02-03) verify đúng 12 specific endpoint × 3 hub con = 36 assertion non-404 PASS. CLAUDE.md section 6 cũng có note traceability tương tự.

  > **NOTE update (Phase 3 Plan 03-04 SSO-02 — 2026-05-22):** Hub con auth endpoint behavior refinement (D-V3-Phase3-G LOCKED):
  >
  > - `POST /api/auth/login` — Phase 2 mount universal handle local. **Phase 3 Plan 03-04: hub con trả 307 Location: central** (browser auto-follow, preserve POST + body RFC 7231). Central giữ M2 path handle local.
  > - `POST /api/auth/refresh` — tương tự, hub con trả 307 Location: central (D-V3-Phase3-C — hub con KHÔNG sinh refresh token, 100% refresh ở central).
  > - `POST /api/auth/logout` — giữ handle LOCAL ở hub con (verify JWT qua JWKSCache Plan 03-02 + Redis blacklist chung Plan 03-03 — central thấy ngay). KHÔNG redirect (giảm latency logout).
  > - `GET /api/auth/me` — giữ handle LOCAL ở hub con (verify JWT + return user).
  >
  > **"12 specific HTTP method endpoints" giữ NGUYÊN số** — login + refresh vẫn mount route (KHÔNG strip — Plan 03-04 thay behavior 200/401/422 → 307). Hub con endpoint summary:
  >
  > - **10 endpoint handle LOCAL:** `/api/auth/logout`, `/api/auth/me`, `/api/profile` (GET/PATCH), `/api/documents` (POST/GET/DELETE), `/api/search`, `/api/ask`, `/api/usage`.
  > - **2 endpoint SSO REDIRECT 307:** `/api/auth/login`, `/api/auth/refresh`.
  >
  > Integration test `tests/integration/test_factor_hub_scoped.py` Phase 3 Plan 03-04 Task 3 update assertion: 2 endpoint redirect assert `== 307` (+ verify Location header trỏ central), 10 endpoint local assert `!= 404`. Frontend redirect wire defer Phase 5 PROXY-02 (D-V3-Phase3-F + D-V3-06 D6 expire). Unit test `tests/unit/test_auth_router_hub_redirect.py` (10 test) verify behavior router-level.
- [ ] **FACTOR-04** (added 2026-05-22 sau Phase 2 closeout discussion — user direction B "generalize 100% dynamic"): **Dynamic hub registration** — operator thêm hub mới (vd `medinet_hub_phap_che`) bằng 1 lệnh `make hub-add HUB=<name> [PORT=<port>]` mà **KHÔNG sửa code Python / docker-compose.yml**. (1) `Settings.hub_name` đổi từ `Literal["central","yte","duoc","hcns"]` → `str` với `@field_validator` regex `^[a-z][a-z0-9_]{1,15}$` + reserved-name blacklist (`postgres`, `cocoindex`, `template0`, `template1`, `public`, `medinet` — collision với Postgres system DB / role medinet). "central" + 4 hub gốc accept regression KHÔNG break. (2) `scripts/hub-add.sh` wrap quanh `hub-init.sh` (DB-level Phase 1 Plan 01-05 carry forward) + auto-append service block từ template vào `docker-compose.override.yml` (docker compose auto-merge với base) + auto-detect port `8180 + N` nếu user không truyền explicit. (3) `docker-compose.override.yml.template` snippet với placeholder `{{HUB}}` + `{{PORT}}` + inherit từ `x-api-template` anchor base. (4) Phase 1 `_enforce_hub_dsn_match` validator dùng `self.hub_name` dynamic — KHÔNG sửa. (5) Smoke checkpoint: `make hub-add HUB=tmp_test PORT=8189` → `docker compose up -d python-api-tmp_test` → `curl http://localhost:8189/api/health` 200 → cleanup. Test: `tests/unit/test_config_hub_name_dynamic.py` accept 4 hub gốc + 3 hub mới + reject invalid pattern (uppercase/hyphen/starting-digit/too-long/reserved name).

### SSO — Auth SSO + hub_ids trong JWT (4 REQ — GA-V3-A chốt ở `/gsd-discuss-phase 3` — SSO-01..04 satisfied 2026-05-22 Plan 03-01..05)

- [x] **SSO-01** ✅ Phase 3: Central expose JWKS endpoint `GET /.well-known/jwks.json` public key RS256 (PKCS#8). Hub con cache JWKS local TTL 1h ở startup + refresh background (R-V3-5 mitigation HA). Hub con verify JWT bằng JWKS đã cache thay vì shared keypair file. Test integration: rotate central keypair → hub con detect mới trong TTL window. — **DONE 2026-05-22** (SSO-01 satisfied 2026-05-22 Plan 03-01 publish layer + Plan 03-02 hub con cache; integration test rotate keypair `test_jwks_cache_rotate_keypair_detects_new_kid` PASS — `JWKSCache` in-process LRU + asyncio refresh 1h fail-quiet + 24h hard limit fail-loud delayed 503 `JWKS_STALE`; D-V3-Phase3-A/B/D LOCKED; RFC 7517 single-key strategy v3.0-a + kid deterministic SHA-256 11-char base64url)
- [x] **SSO-02** ✅ Phase 3: Refresh token blacklist Redis chung — central + hub con cùng kết nối 1 Redis instance, blacklist key `auth:blacklist:<jti>` valid cross-process. Hub con KHÔNG sinh refresh token (login + refresh chỉ ở central qua `POST /api/auth/login` central URL). User login 1 lần ở central → JWT valid trên tất cả hub con qua subpath redirect. — **DONE 2026-05-22** (SSO-02 satisfied 2026-05-22 Plan 03-03 key rename `blacklist:` → `auth:blacklist:` 5 vị trí touch qua mini-module `_blacklist.py` + Plan 03-04 307 redirect hub con login/refresh `Location: central` RFC 7231 preserve POST + body; integration test cross-process `test_sso_blacklist_cross_process` PASS; D-V3-Phase3-C/G/H LOCKED)
- [x] **SSO-03** ✅ Phase 3: JWT claim `hub_ids: list[str]` reflect user's hub assignments — central verify access cross-hub search dùng `hub_ids` (intersection với requested `hub_ids` body). Hub con verify access local hub bằng so sánh `current_user.hub_ids` chứa `HUB_NAME` của process. JWT issuer URL cố định central (KHÔNG đổi per-hub). — **DONE 2026-05-22** (SSO-03 satisfied 2026-05-22 Plan 03-03 hub_ids REQUIRED + aud=`["medinet-wiki"]` REQUIRED + PyJWT strict audience check 2 verify path `verify_token` + `verify_token_with_key`; iss giữ `"medinet-wiki"` RE-CONFIRM D-V3-Phase3-E — URL-based defer Phase 7 MCP MIGRATE-04 split aud)
- [x] **SSO-04** ✅ Phase 3: E4 reinforced — hub con KHÔNG truy cập được data hub khác kể cả khi JWT compromised (DB-level isolation từ TOPO-04 + repository layer vẫn enforce `WHERE hub_id = settings.hub_name`). Test integration: stale JWT chứa `hub_ids=["duoc"]` post tới `medinet_hub_yte` API → 403 (không phải 404 hay data leak). — **DONE 2026-05-22** (SSO-04 satisfied 2026-05-22 Plan 03-03 dependency mới `get_current_user_for_hub_access` — **E4 reinforced 3-layer enforce (Layer 1 Phase 1 DSN validator + Layer 2 M2 repo + Layer 3 Plan 03-03 dependency hub_ids check)** defense-in-depth; integration test `test_stale_jwt_cross_hub_returns_403_envelope` PASS verify 403 CROSS_HUB_ACCESS_DENIED envelope D6 — KHÔNG 404 leak / 500 error / 200 data leak)

  > **NOTE Phase 3 closeout (Plan 03-05 SSO-01..04 — 2026-05-22):** 5 plan ship đầy đủ 4 REQ-ID:
  >
  > 1. Plan 03-01 — JWKS endpoint publish RFC 7517 (D-V3-Phase3-A) — module `api/app/auth/jwks.py` mới với `publish_jwks() + load_public_key_as_jwk()` helpers cryptography stdlib (KHÔNG jwcrypto dep mới). Central mount conditional + Cache-Control 1h + 503 fallback envelope D6. 9 unit test PASS.
  > 2. Plan 03-02 — JWKSCache hub con + lifespan + dependency branch (D-V3-Phase3-B/D) — `JWKSCache` class extend jwks.py + 2 exception JWKSStaleError/JWKSKidNotFoundError + lifespan blocking fetch_initial 5s timeout (boot fail-loud R-V3-5 mitigation) + asyncio refresh task 1h fail-quiet + 24h hard limit fail-loud delayed. `get_current_user` dependency branch (central=local pem / hub con=JWKSCache.get_public_key(kid) → verify_token_with_key). Settings 3 field + 1 model_validator hub con required. JWT header `kid` auto-add deterministic SHA-256. 27 unit + 3 integration test PASS.
  > 3. Plan 03-03 — JWT aud/hub_ids REQUIRED + Redis blacklist key rename + E4 reinforced (D-V3-Phase3-E/H + SSO-04) — `JWT_AUDIENCE` constant + aud/hub_ids REQUIRED + PyJWT strict audience check 2 path. Mini-module `_blacklist.py` (REDIS_BLACKLIST_PREFIX + make_blacklist_key) — 5 vị trí touch. Dependency `get_current_user_for_hub_access` Layer 3 enforce hub_ids check → 403 CROSS_HUB_ACCESS_DENIED. request.state.jwt_claims wire. 19 unit + 3 integration test PASS.
  > 4. Plan 03-04 — 307 redirect hub con login/refresh + Settings central_url (D-V3-Phase3-G) — auth router refactor `RedirectResponse(status_code=307)` preserve POST + body RFC 7231. `_sso_redirect` helper + X-SSO-Redirect-Reason/X-SSO-Original-Hub headers debug. `response_model=None` opt-out. Settings.central_url + model_validator hub con required. Docker-compose 3 hub con + override.yml.template env CENTRAL_URL (FACTOR-04 inherit). Phase 2 integration test split 2 list (10 LOCAL + 2 SSO_REDIRECT). 10 unit test PASS.
  > 5. Plan 03-05 — Closeout docs + smoke checkpoint (file này note) — CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md update. Task 5 smoke compose runtime SKIP pre-resolved per user decision (defer Phase 7 MIGRATE-05 full E2E).
  >
  > **Backward incompat TRIPLE cumulative:** M2 cũ JWT thiếu (1) `kid` header Plan 03-02 + (2) `aud` claim Plan 03-03 + (3) `hub_ids` claim Plan 03-03 → 401 reject sau deploy. Frontend M2 hardcode `/api/auth/login` same-origin sẽ FAIL ở hub con cho tới Phase 5 PROXY-02 wire form action central (acceptable dev v3.0-a). User re-login ~15-30s downtime (Slack/Email broadcast). Xem `Hub_All/README.md` "SSO Backward Incompat (Phase 3 v3.0)" section deploy 7 step + rollback procedure.
  >
  > **Frontend wire:** Defer Phase 5 PROXY-02 (D-V3-Phase3-F honored + D-V3-06 D6 expire formally Phase 5 — frontend rewrite `<form action="https://central/api/auth/login">` redirect flow).
  >
  > **v3.0-a progress:** Phase 1+2+3 DONE (3/3 phase v3.0-a — 15/~32 plan ≈ 47%). 🚦 v3.0-a EXIT GATE TRIGGERED — user accept demo (1 hub con yte + central + JWT SSO + golden path defer Phase 7 MIGRATE-05 full E2E; evidence chain 65+ unit + 6 integration in-process PASS cover semantic) → tiếp tục v3.0-b (Phase 4-7 sync + proxy + settings + migration).

### SYNC — Cross-hub Data Sync (5 REQ — GA-V3-D mechanism chốt ở `/gsd-discuss-phase 4` — SYNC-01..05 satisfied 2026-05-22 Plan 04-01..07)

- [x] **SYNC-01** ✅ Phase 4: Sau khi cocoindex flow hub con ingest xong → push chunks + vector (denormalized) lên `medinet_central.chunks` với cùng `chunk_id` + `document_id` + `hub_id` + `content` + `content_hash` + `vector` (1536-dim). Hub tổng KHÔNG re-embed (D-V3-02). Push trigger: cocoindex post-process hook hoặc outbox worker (chốt ở discuss-phase 4). — **DONE 2026-05-22** (Plan 04-01..04 sync_outbox + trigger + worker + lifespan integration; Plan 04-01 per-hub Alembic 0005 + Postgres function `enqueue_sync_outbox()` + 2 trigger AFTER INSERT/DELETE chunks FOR EACH ROW atomic cùng transaction; Plan 04-02 Settings.hub_id UUID + central_sync_dsn boot fail-fast 3 model_validator; Plan 04-03 `api/app/sync/` module 5 file + sync_worker_loop + ChunkPayload Pydantic hex decode BLOCKER 2 + PUSH_INSERT_CHUNK_SQL ON CONFLICT (id); Plan 04-04 lifespan central_sync_pool init `_init_central_sync_conn` register pgvector codec + verify `SELECT current_database() == 'medinet_central'` fail-fast T-04-04-01 + sync_worker_task spawn hub con + graceful shutdown wait_for 10s).
- [x] **SYNC-02** ✅ Phase 4: Push idempotent on retry — `INSERT ... ON CONFLICT (chunk_id) DO UPDATE SET content_hash, vector, ... WHERE central.content_hash != hub.content_hash`. Multi-retry KHÔNG dup. Test integration: kill push mid-batch → re-run KHÔNG sai chunk_count tổng. — **DONE 2026-05-22** (Plan 04-03 PUSH_INSERT_CHUNK_SQL ON CONFLICT (id) DO UPDATE WHERE chunks.content_hash IS DISTINCT FROM EXCLUDED.content_hash 1 SQL atomic D-V3-Phase4-B1 LOCKED; chunk_id stable UUID5 re-export `stable_chunk_id` rag/flow.py qua sync/keys.py; content_hash filter tránh UPDATE no-op bảo vệ HNSW vector index disk write thừa; 18/18 worker unit test PASS verify idempotent retry KHÔNG dup + concurrent SKIP LOCKED).
- [x] **SYNC-03** ✅ Phase 4: Cross-hub search ở central dùng aggregated `medinet_central.chunks` — query SQL `WHERE hub_id = ANY($1::uuid[])` với `hub_ids` từ JWT claim + intersect với `body.hub_ids`. KHÔNG fan-out HTTP tới hub con (latency × N). Re-rank theo score (carry forward Phase 6 v2.0 cross-hub logic). — **DONE 2026-05-22** (Plan 04-05 SearchService._search_cross_hub_impl refactor 1 SQL aggregated `WHERE c.hub_id = ANY($2::uuid[]) ORDER BY vector <=> $1::vector LIMIT $3` thay fan-out `asyncio.gather(*[_search_one(h) for h in hub_ids])` N task — D-V3-Phase4-D1 LOCKED; re-rank tự nhiên qua SQL ORDER BY KHÔNG Python merge sort; public API `SearchService.search_cross_hub(*, body, user) -> dict[str, Any]` signature KHÔNG đổi backward compat M2 ask_service.py + frontend api.ts crossHubSearch; tách routers/search.py 2 APIRouter (`search_router` universal + `cross_hub_router` central-only) + main.py conditional include block central-only — D-V3-Phase4-D3; CENTRAL_ONLY list 8 → 9 endpoint; JWT hub_ids ∩ body.hub_ids intersect SSO-03 carry forward Phase 3; HNSW `iterative_scan=relaxed_order` + `ef_search=200` + `max_scan_tuples=20000` SET LOCAL session tuning carry forward M2 Phase 6; E-V3-2 p95 < 1.5s defer Phase 7 MIGRATE-05 runtime smoke live measure; 8/8 unit + 15/15 integration (10 baseline + 5 dedicated cross-hub mount/strip) PASS).
- [x] **SYNC-04** ✅ Phase 4: Checksum verify periodic chống drift (R-V3-1 mitigation) — daily cron Prometheus metric `sync_drift_total{hub_id, drift_type}` đếm row count + sample content_hash diff giữa `medinet_hub_<name>.chunks` và `medinet_central.chunks WHERE hub_id = <hub>`. Alert nếu drift > 1% (E-V3-5 trigger nếu sustained 7 ngày). — **DONE 2026-05-22** (Plan 04-06 `api/app/observability/checksum_scheduler.py` central FastAPI lifespan asyncio task — daily 2AM `COUNT(*)` per hub_id vs central → `SYNC_COUNT_DRIFT{hub_name}` gauge symmetric drift_ratio + hourly `TABLESAMPLE BERNOULLI(1)` chunks created last 1h → content_hash diff → `SYNC_HASH_DRIFT{hub_name, drift_type=mismatch|missing}` counter — D-V3-Phase4-C1 LOCKED; naive `asyncio.sleep` cron loop KHÔNG cần APScheduler dep mới — D-V3-Phase4-C3; lazy per-hub asyncpg.Pool init khi lần đầu encounter trong tick (operator append hub mới FACTOR-04 + restart scheduler refresh dict); per-hub error isolation 1 hub fail KHÔNG abort scheduler; `POST /api/sync/replay { hub_id, since }` admin RBAC endpoint reset 4 field dead rows atomic + audit_logs INSERT non-repudiation `action='sync.replay'` defensive inner try/except W8 fix T-04-06-03 reinforced — D-V3-Phase4-C2; require_role("admin") Phase 3 SSO-04 dependency carry forward; 22/22 unit test PASS).
- [x] **SYNC-05** ✅ Phase 4: Sync mechanism chốt ở `/gsd-discuss-phase 4` — 3 option đánh giá: (a) cocoindex target thứ 2 (Postgres target trỏ central — chỉ activate sau ingest local), (b) Postgres logical replication (publication `chunks_hub_yte_pub` ở hub con + subscription `chunks_hub_yte_sub` ở central), (c) outbox + worker (transactional INSERT outbox table local + worker async push central với ack). Trade-off: (a) lock-in cocoindex / (b) Postgres native nhưng schema drift sensitive / (c) flexible nhưng phải tự maintain worker. — **DONE 2026-05-22** (D-V3-Phase4-A1 LOCKED outbox + worker option (c) — REJECT (a) cocoindex target thứ 2 lock-in + KHÔNG isolate failure khi central down → flow stall; REJECT (b) Postgres logical replication schema drift sensitive debug khó; SELECTED outbox = R-V3-1 sync drift HIGH mitigation rõ nhất; Plan 04-01..04 ship sync_outbox table per-hub (D-V3-Phase4-A2) + Postgres trigger AFTER INSERT/DELETE chunks atomic enqueue (D-V3-Phase4-A4) + in-process asyncio worker hub con lifespan KHÔNG separate container KHÔNG central worker SPOF (D-V3-Phase4-A3) + batch 100/5s poll + SELECT FOR UPDATE SKIP LOCKED concurrency-safe + exp backoff [1,5,30,120]s + max 5 attempts → status='dead' + 6 Prometheus metric infrastructure — D-V3-Phase4-A5 LOCKED).

  > **NOTE Phase 4 closeout (Plan 04-07 SYNC-01..05 — 2026-05-22):** 7 plan ship đầy đủ 5 REQ-ID + 9 D-V3-Phase4-A1..D3 LOCKED consumed + 6 Prometheus metric infrastructure + ~36 STRIDE threat addressed:
  >
  > 1. Plan 04-01 — Per-hub Alembic 0005 sync_outbox + Postgres trigger AFTER INSERT/DELETE chunks + documents.sync_status enum (D-V3-Phase4-A2/A4/B2). BLOCKER 1 fix initial 'pending' → 'syncing' idempotent UPDATE guard. BLOCKER 2 fix explicit `jsonb_build_object` (KHÔNG `to_jsonb(NEW)`) + `NEW.vector::float4[]` cast + `encode(content_hash, 'hex')` 64 char. 17/17 unit + 293/293 + 10/10 Phase 1 regression PASS.
  > 2. Plan 04-02 — Settings 7 field + 3 model_validator + 1 length validator + docker-compose env wire (D-V3-Phase4-A5/C3/D2). 17/17 unit + 310/310 regression PASS.
  > 3. Plan 04-03 — `api/app/sync/` module 5 file (keys + models + metrics + worker + __init__) ~816 LOC + sync_worker_loop + 6 Prometheus collector label `hub_name` W7 + ChunkPayload field_validator hex decode BLOCKER 2 (D-V3-Phase4-A1/A5/B1/B3). 43/43 unit + 353/353 regression PASS.
  > 4. Plan 04-04 — Lifespan integration central_sync_pool create_pool(init=`_init_central_sync_conn`) register pgvector codec + verify `current_database()=='medinet_central'` fail-fast + sync_worker_task spawn hub con + graceful shutdown wait_for(10s) + rag/flow.py defensive guard doc_hub_id mismatch (D-V3-Phase4-D2 + E-V3-3 Layer 1 carry forward); shared `api/app/db/dsn.py` `_to_asyncpg_dsn` helper (W3 fix circular import); SYNC_SKIP_CENTRAL_POOL=1 escape hatch. 6 mock integration + 1 skipif live-DB PASS.
  > 5. Plan 04-05 — `SearchService._search_cross_hub_impl` refactor 1 SQL aggregated thay fan-out asyncio.gather (D-V3-Phase4-D1) + tách `routers/search.py` 2 APIRouter `search_router` universal + `cross_hub_router` central-only mount (D-V3-Phase4-D3) + CENTRAL_ONLY list 8 → 9; HNSW iterative_scan + ef_search=200 carry forward M2 Phase 6; public API signature unchanged backward compat. 8/8 unit + 15/15 integration PASS.
  > 6. Plan 04-06 — `api/app/observability/checksum_scheduler.py` central lifespan asyncio task daily 2AM COUNT(*) + hourly TABLESAMPLE BERNOULLI(1) hash sample + lazy per-hub asyncpg.Pool init + per-hub error isolation (D-V3-Phase4-C1/C3); `POST /api/sync/replay` admin endpoint require_role("admin") Phase 3 SSO-04 dependency + reset 4 field dead rows atomic + audit_logs INSERT W8 fix T-04-06-03 (D-V3-Phase4-C2). 22/22 unit + 383/383 regression + 21/21 integration PASS.
  > 7. Plan 04-07 — Closeout docs (CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md) + smoke checkpoint runtime SKIP pre-resolved per user decision (defer Phase 7 MIGRATE-05 full E2E 3 hub + central golden path + JWT SSO live + cross-hub search live data; evidence chain 113 unit + 21 integration in-process PASS cover semantic SYNC-01..05 + `docker compose config --quiet` base PASS Plan 04-02).
  >
  > **Reference:** `.planning/phases/04-cross-hub-data-sync/04-{01..07}-SUMMARY.md` chi tiết deliverable + commit + test count + deviation per plan.
  >
  > **Backward incompat:** Hub con `/api/search/cross-hub` strip → 404 envelope D6 NOT_FOUND (FACTOR-02 extend Plan 04-05 D-V3-Phase4-D3); M2 frontend hardcode same-origin sẽ FAIL ở hub con cho tới Phase 5 PROXY-02 wire base URL detect prefix (D6 expire formally D-V3-06). Acceptable dev v3.0-b. M2 COMPAT stub `routers/sync.py` `/api/sync/{stats,batches,...}` PRESERVE — append `/api/sync/replay` mới (FACTOR-02 central-only carry forward).
  >
  > **R-V3-1 HIGH mitigation chain:** Outbox INSERT atomic cùng transaction chunks INSERT (Plan 04-01 trigger) + ON CONFLICT idempotent retry KHÔNG dup (Plan 04-03 + B1) + exp backoff + max 5 attempts → dead + Prometheus alert (Plan 04-03 + A5) + daily/hourly checksum verify (Plan 04-06 + C1) + admin replay manual recovery (Plan 04-06 + C2).
  >
  > **E-V3-2 cross-hub p95 < 1.5s carry forward:** HNSW iterative_scan + ef_search=200 carry forward M2 Phase 6; 1 SQL aggregated giảm overhead orchestration thay fan-out N task (Plan 04-05 + D1); live measure defer Phase 7 MIGRATE-05 runtime smoke E2E.
  >
  > **v3.0-b progress:** Phase 4 DONE (1/4 phase v3.0-b — 22/~32 plan ≈ 69%). Next: `/gsd-discuss-phase 5` Reverse Proxy + Frontend Subpath (PROXY-01..04 — Caddy subpath routing + frontend prefix detect 1 build + D-V3-06 D6 expire formally + per-hub login branding; GA-V3-C confirm).

### PROXY — Reverse Proxy + Frontend Subpath (4 REQ — GA-V3-C chốt ở `/gsd-discuss-phase 5`, D-V3-06 D6 expire)

- [x] **PROXY-01**: Caddy route `wiki.domain.com/<hub>/api/*` → upstream container hub đúng (`http://hub-<hub>:8180/api/*`) với strip prefix `/<hub>`. Central route `wiki.domain.com/api/*` → `http://central:8180/api/*` (KHÔNG strip). Caddy auto-TLS tiếp tục từ v2.0 Phase 8.3. Test smoke: `curl https://wiki.domain.com/yte/api/health` PASS + `curl https://wiki.domain.com/api/health` PASS (central). _Closed 2026-05-23 Plan 05-01 (Caddyfile wiki block + path_regexp `^/({$HUBS_ALLOWLIST_REGEX:yte|duoc|hcns})/api/(.*)$` + handle_path strip prefix + docker-compose WIKI_PUBLIC_DOMAIN env) + Plan 05-05 (`hub-add.sh` 9-step pipeline extend step 8 atomic .env HUBS_ALLOWLIST/REGEX update + step 9 PRE-validate + zero-downtime reload + smoke curl warn-only). Live smoke `curl` 2 URL defer Phase 7 MIGRATE-05 runtime E2E (v3.0-b precedent)._
- [x] **PROXY-02** ✅ Phase 5: Frontend detect prefix từ URL (1 build dùng chung — option a GA-V3-C khuyến nghị seed) — `frontend/src/services/api.ts` đổi base URL: `const PREFIX = window.location.pathname.split('/').filter(Boolean)[0]; const API_BASE = PREFIX && KNOWN_HUBS.includes(PREFIX) ? '/' + PREFIX + '/api' : '/api';`. Per-hub build (`VITE_HUB_NAME=yte`) bỏ — đỡ build matrix. _Closed 2026-05-23 Plan 05-02 (api.ts module-level PREFIX/API_BASE/APP_BASE/CURRENT_HUB compute + KNOWN_HUBS từ window.__HUB_CONFIG__.allowlist fallback hardcode + Wave 0 vitest@^2 + @testing-library/react@^16 + jsdom@^25 install + 10 vitest PASS) + Plan 05-04 (Login.tsx 4-state machine + T-5-04 strict ?return validation 4-layer + W-05 origin-not-host check + window.location.replace cross-prefix + api.crossHubSearch ABSOLUTE path qua requestAbsolute helper bypass ${API_BASE} prefix D-V3-Phase4-D3 carry forward; 13 vitest PASS). GA-V3-C confirmed 1 build approach 2026-05-23._
- [x] **PROXY-03** ✅ Phase 5: D6 expire formally — `Hub_All/CLAUDE.md` section 3 cập nhật ghi D6 hết hiệu lực ở v3.0-05. Frontend rewrite được phép cho prefix detect (PROXY-02) + per-hub login branding (PROXY-04). Smoke regression 11 trang React M2 COMPAT-01 carry forward (R-V3-2 mitigation). _Closed 2026-05-23 Plan 05-06 Task 1 (CLAUDE.md §3 Constraint M2 "Frontend D6" line REPLACE → strikethrough + "EXPIRED 2026-05-23 PROXY-03 satisfied" explicit note + Phase 5 cho phép FE rewrite + smoke regression 11 trang carry forward + M2 envelope shape locked). Task 5b smoke 4 hub × 11 trang manual checkpoint resume signal `skip smoke` auto-fallback per --auto chain + v3.0-b precedent (Plan 03-05 + 04-07 pre-resolved); evidence chain 37/37 vitest + caddy validate + docker compose config + bash -n PASS cover semantic; manual visual smoke defer Phase 7 MIGRATE-05 full E2E._
- [x] **PROXY-04** ✅ Phase 5: Per-hub login branding — hub con render logo + tiêu đề khác nhau (Hub y_te = phòng y tế Medinet, Hub dược = phòng dược, ...). Tách `frontend/src/branding/<hub>/` config (logo URL + title VN + theme color) thay vì sửa core component. Central giữ branding gốc Medinet Wiki. Login form ở hub con redirect về central `/api/auth/login` (SSO-02). _Closed 2026-05-23 Plan 05-03 (branding registry `frontend/src/branding/index.ts` Vite `import.meta.glob('./*/index.ts', { eager: true })` + BrandingConfig type + getBranding(hub) fallback central + getContrastTextColor WCAG 1.4.11 helper threshold 0.6 hcns amber slate-900 + 4 hub config central indigo `#6366f1` / yte emerald `#10b981` / duoc sky `#0ea5e9` / hcns amber `#f59e0b` + 4 SVG placeholder text-only initial M/Y/D/H + 13 vitest PASS Rule 1 fix threshold) + Plan 05-04 (Login.tsx 4 state machine (A central no-return / B central + valid return chip / C hub direct visit skeleton redirect / D invalid silent fallback) + branding inline CSS var --hub-theme + submit button gradient color-mix() + Layout.tsx sidebar header logo bg + title swap module-level getBranding(CURRENT_HUB) + hover ring themeColor — UI-SPEC §3 LOCKED scope chỉ sidebar header touch R-V3-2 mitigation)._

> **NOTE Phase 5 closeout 2026-05-23 (6-plan list):**
> - **Plan 05-01** Wave 1 BLOCKING: `Hub_All/Caddyfile` ADD `{$WIKI_PUBLIC_DOMAIN}` server block parallel MCP block + `path_regexp hub_api ^/({$HUBS_ALLOWLIST_REGEX:yte|duoc|hcns})/api/(.*)$` + `uri strip_prefix /{re.hub_api.1}` + `reverse_proxy http://python-api-{re.hub_api.1}:8080` (port STATIC :8080) + central `/api/*` no-strip + `/.well-known/*` JWKS pass + static SPA `try_files {path} /index.html`. `docker-compose.yml` caddy service env wire + `./frontend/dist:/srv/wiki/dist:ro` mount + depends_on python-api-central. `.env.example` 3 env document. PROXY-01 partial.
> - **Plan 05-02** Wave 2 parallel: Wave 0 vitest@^2 + @testing-library/react@^16 + @testing-library/jest-dom@^6 + jsdom@^25 install + `frontend/vitest.config.ts` jsdom env + `frontend/src/test-setup.ts` jest-dom matchers + `"test": "vitest run"` npm script. `frontend/src/services/api.ts` module-level export `PREFIX/API_BASE/APP_BASE/CURRENT_HUB` từ `window.location.pathname` + `KNOWN_HUBS` runtime config fallback hardcode `['yte','duoc','hcns']`. `frontend/src/App.tsx` wrap `<BrowserRouter basename={APP_BASE}>`. 10 vitest test PASS (7 api + 3 App). PROXY-02 partial.
> - **Plan 05-03** Wave 2 parallel: `frontend/src/branding/index.ts` Vite `import.meta.glob('./*/index.ts', { eager: true })` + `BrandingConfig` type + `getBranding(hub)` fallback central + `getContrastTextColor(themeColor)` WCAG 1.4.11 mitigation threshold 0.6 hcns amber. 4 hub config `branding/<hub>/index.ts` + 4 SVG placeholder `public/branding/<hub>/logo.svg` (M/Y/D/H text-only). 13 vitest test PASS Rule 1 fix threshold 0.55→0.6. PROXY-04 partial.
> - **Plan 05-04** Wave 3: `Login.tsx` 4 state machine (A/B/C/D per UI-SPEC §2) + branding inline CSS var `--hub-theme` + submit button gradient `color-mix()` + T-5-04 strict ?return 4-layer validation (strip `/` + reject `//` + reject `://` + regex hub format + KNOWN_HUBS allowlist) + W-05 origin-not-host check + window.location.replace cross-prefix. `Layout.tsx` sidebar header logo bg + title swap module-level `getBranding(CURRENT_HUB)` + hover ring themeColor. `api.ts::crossHubSearch` REPLACE → ABSOLUTE `/api/search/cross-hub` qua `requestAbsolute<T>` helper (D-V3-Phase4-D3 carry forward). `tryRefresh()` explicit `redirect: 'follow'` audit B-02 fix (D-V3-Phase5-C4). 37/37 vitest full suite PASS (6 Login + 4 Layout + 11 api + 3 App + 13 branding). 4 atomic commit (54fc315 + ca6f4e4 + ed721fd + 61ac731). PROXY-02 + PROXY-04 satisfied.
> - **Plan 05-05** Wave 4: `api/scripts/hub-add.sh` 7-step → 9-step. Step 8 atomic sed-edit `.env` HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX (tmp file + mv preserve other env, duplicate skip idempotent). Step 9 PRE-validate `caddy validate --config` (Pitfall 7 silent rollback mitigation) + `caddy reload` zero-downtime + smoke `curl -k https://${WIKI_PUBLIC_DOMAIN}/${HUB}/api/health` warn-only + dev pre-up tolerance (caddy chưa running → skip + hint). T-5-05 strict guard Plan 02-05 carry forward (regex + RESERVED blacklist + `$HUB` quoted). 1 atomic commit (6b282b8 137+/10-). PROXY-01 + FACTOR-04 extend.
> - **Plan 05-06** Wave 5 closeout: CLAUDE.md §3 D6 EXPIRED + §6 progress row + Phase 5 pattern subsection (CLAUDE.md). STATE.md frontmatter + Current Position + Phase 5 Planning + Results Summary + Next Action. REQUIREMENTS.md PROXY-01..04 mark [x] + NOTE này. ROADMAP.md Phase 5 row DONE + Progress table. README.md NEW section "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)". Task 5b `checkpoint:human-action gate=blocking` smoke 4 hub × 11 trang React M2 COMPAT-01 (UI-SPEC §7 — 44 checkpoint). Resume signal: `skip smoke` auto-fallback per --auto chain mode + v3.0-b precedent (Plan 03-05 + 04-07 pre-resolved skip pattern). Manual visual smoke runtime defer Phase 7 MIGRATE-05 full E2E (3 hub + central golden path + JWT SSO live + cross-hub search live data + per-hub branding visual diff).
>
> **Backward compat:** Phase 5 KHÔNG break M2 — endpoint URL/envelope shape `{success, data, error, meta}` LOCKED unchanged + 11 trang nội dung React preserve (chỉ Login.tsx + Layout.tsx sidebar header touch — R-V3-2 mitigation D2 scope minimal). M2 frontend hardcode `${window.location.hostname}:8180` REMOVED → relative path qua Caddy gateway. localStorage same-origin SSO carry forward (D-V3-Phase5-C2). 307 backend redirect (Plan 03-04 SSO-02) là failsafe layer 2 cho `api.ts::tryRefresh()` POST same-origin + explicit `redirect: 'follow'` B-02 audit fix.
>
> **STRIDE T-5-01..07 coverage:** T-5-01 Caddy open redirect (mitigate path_regexp anchor `^/(...)/api/(.*)$` unknown hub fall through file_server) + T-5-02 KNOWN_HUBS tampering (mitigate backend Caddy regex authoritative — FE UX-only) + T-5-03 logo asset path traversal (mitigate regex constrain `^\.\/([a-z][a-z0-9_]{0,15})\/index\.ts$`) + T-5-04 Login ?return open redirect (mitigate 4-layer validation strip + reject absolute + regex + allowlist + W-05 origin-not-host) + T-5-05 hub-add.sh shell injection (mitigate Plan 02-05 carry forward regex + RESERVED blacklist + `$HUB` quoted) + T-5-06 XSS theme color (mitigate compile-time TS const + React style escape) + T-5-07 localStorage XSS exfil (accept M2 carry forward — defer v4.0 HARD-V4-05 httpOnly cookie).
>
> **v3.0-b progress:** Phase 4+5 DONE (2/4 phase v3.0-b — 28/~32 plan ≈ 88%). Next: `/gsd-discuss-phase 6` System Settings Sync (SETTINGS-01..04 — rag_config HTTP pull TTL + Redis pub/sub invalidate + api_keys proxy + hub_registry read-only — GA-V3-B chốt).

### SETTINGS — System Settings Sync (4 REQ — GA-V3-B chốt 2026-05-23 — SETTINGS-01..04 satisfied 2026-05-23 Plan 06-01..05)

- [x] **SETTINGS-01** ✅ Phase 6: Hub con đọc `rag_config` (LLM/embedding provider + model + dim) từ central qua HTTP pull on-demand `GET https://central/api/rag-config` (cache Redis local TTL 60s). Cache key `settings:rag_config:<hub>`. Fallback: nếu central down, dùng cached value cho tới TTL expire — sau đó fail-loud nếu vẫn không reach (KHÔNG silent degrade). _Closed 2026-05-23 Plan 06-01 (settings_sync/ module scaffold 3 file `__init__.py` + `keys.py` 4 constant + `make_apikey_cache_key` SHA-256 hex prefix + `metrics.py` 6 Prometheus collector + Settings 5 field + 1 model_validator) + Plan 06-02 (RagConfigClient httpx async fetch_initial blocking 5s + Redis cache TTL 60s key `settings:rag_config:<hub_name>` + SettingsUnavailableError fail-loud + Prometheus emit cache_hit/miss/pull_latency/stale_seconds) + Plan 06-04 (lifespan hub con block init RagConfigClient + blocking fetch_initial 5s + boot fail-loud raise → uvicorn exit 1 D-V3-Phase6-A LOCKED + R-V3-6 LOW pattern song song Phase 3 JWKSCache)._
- [x] **SETTINGS-02** ✅ Phase 6: Push invalidate qua Redis pub/sub channel `settings:invalidate` — khi central `PUT /api/rag-config` → publish `{config_key: "rag_config", timestamp}` → hub con subscriber flush local cache trong < 30s (E-V3-4). Test integration: đổi `rag_config` ở central → hub con phải re-fetch trong window. _Closed 2026-05-23 Plan 06-02 (settings_subscriber_loop asyncio task subscribe single channel `settings:invalidate` + Pydantic InvalidateMessage Literal enum schema `config_key: Literal["rag_config","hub_registry","apikey"] + hub: str + key_id: str|None + timestamp: int` validate reject injection T-06-02-03 + 3 config_key flush branch rag_config per-hub broadcast / hub_registry singleton / apikey SCAN+DEL + outer reconnect retry 5s on ConnectionError + CancelledError graceful shutdown) + Plan 06-03 (update_rag_config PUT extend `request: Request` param + publish best-effort fail-open `redis.publish("settings:invalidate", json({"config_key":"rag_config","hub":"*","timestamp":int(time.time())}))` sau service.update_config success — pattern carry forward search_cache.py M2 publish_invalidate fail-open) + Plan 06-04 (lifespan spawn settings_subscriber_task `asyncio.create_task(settings_subscriber_loop)` guard `redis_ready` T-06-04-04 + shutdown graceful task.cancel + asyncio.wait_for timeout=10s suppress CancelledError/TimeoutError T-06-04-03)._
- [x] **SETTINGS-03** ✅ Phase 6: API key verification proxy — khi hub con nhận `X-API-Key:` header → gọi central `POST /api/api-keys/verify` (cache Redis TTL 60s key `apikey:<hash>`) thay vì lưu plaintext key cục bộ. Trả 401 nếu central reject. Central giữ AES-GCM encrypt-at-rest từ M2 AUX-02. _Closed 2026-05-23 Plan 06-02 (ApiKeyVerifyClient httpx POST `/api/api-keys/verify` body `{api_key}` header `X-Internal-Auth: <settings_proxy_secret>` + Redis cache TTL 60s hash key `apikey:verify:<sha256[:16]>` T-06-04-02 mitigation KHÔNG plaintext + KHÔNG cache negative 401 + Prometheus `APIKEY_VERIFY_TOTAL{result=valid|invalid|cached}`) + Plan 06-03 (require_api_key REFACTOR branch hub_name central=local M2 `ApiKeyService.verify_key` AES-GCM unchanged AUX-02 / hub con=`request.app.state.api_key_verify_client.verify(x_api_key)` HTTP proxy + 503 `APIKEY_VERIFY_CLIENT_UNAVAILABLE` envelope nếu client chưa init boot fail-loud R-V3-6 + require_internal_auth hmac.compare_digest constant-time → 401 `INTERNAL_AUTH_FAIL` envelope T-06-03-01 timing attack mitigation + POST /api/api-keys/verify endpoint MỚI central-only mount FACTOR-02 carry forward Phase 2 — api_keys_router central-only KHÔNG cần thay đổi main.py include + VerifyApiKeyRequest Pydantic schema `api_key: str Field min_length=1 max_length=256` + return raw dict `{valid, principal}` KHÔNG envelope — hub con ApiKyVerifyClient parse raw structure)._
- [x] **SETTINGS-04** ✅ Phase 6: `hub_registry` read-only ở hub con — hub con load `hub_registry` (list hub_id + subpath + active) từ central qua HTTP pull TTL 5 phút (rare-change). Hub con KHÔNG ghi (PUT/POST trả 404 do FACTOR-02 strip). Central giữ nguyên CRUD M2 HUB-01. _Closed 2026-05-23 Plan 06-02 (HubRegistryClient httpx async `GET /api/hubs` M2 endpoint hiện hữu HUB-01 + Redis cache TTL 300s singleton key `settings:hub_registry` KHÔNG per-hub + fetch_initial blocking 5s lifespan) + Plan 06-04 (lifespan hub con init HubRegistryClient + await hub_client.fetch_initial blocking 5s fail-loud). Hub con POST/PUT/PATCH /api/hubs* đã 404 do FACTOR-02 Phase 2 strip hubs_router central-only — KHÔNG cần thay đổi._

> **NOTE Phase 6 closeout (Plan 06-05 SETTINGS-01..04 — 2026-05-23):** 5 plan ship đầy đủ 4 REQ-ID + 4 D-V3-Phase6-A/B/C/D LOCKED consumed + 6 Prometheus metric infrastructure + ~33 STRIDE T-06-01..04 threat addressed:
>
> 1. Plan 06-01 — Wave 1 BLOCKING infra: settings_sync/ module scaffold 3 file (`__init__.py` re-export 11 symbol + `keys.py` 4 constant `SETTINGS_INVALIDATE_CHANNEL/RAG_CONFIG_KEY_PREFIX/HUB_REGISTRY_KEY/APIKEY_VERIFY_KEY_PREFIX` + `make_apikey_cache_key` SHA-256 hex 16 char T-06-04-02 + `metrics.py` 6 Prometheus collector module-level `hub_name` + `key_type` bounded enum W7) + Settings 5 field mới (`settings_proxy_secret` + 3 TTL field + reconnect_seconds) + 1 model_validator `_enforce_settings_proxy_secret` length ≥ 32 char BOTH central + hub con T-06-04-01 + docker-compose 4 service env wire `${VAR:?msg}` fail-loud + override.yml.template FACTOR-04 placeholder + .env.example document 5 env mới + hint `openssl rand -hex 32`. 25/25 unit + 408/408 regression PASS.
> 2. Plan 06-02 — Wave 2 client + subscriber: 3 HTTP client class (RagConfigClient + HubRegistryClient + ApiKeyVerifyClient) + SettingsUnavailableError + `settings_subscriber_loop` asyncio task subscribe single channel + Pydantic `InvalidateMessage` Literal enum schema validate + 3 config_key flush branch + outer reconnect retry + CancelledError graceful + `_process_message`/`_subscribe_and_listen` helper C901 refactor. 22/22 unit test PASS (11 client + 11 subscriber).
> 3. Plan 06-03 — Wave 3 refactor + extend: `require_api_key` REFACTOR branch hub_name + `require_internal_auth` hmac.compare_digest constant-time dep + `update_rag_config` PUT publish best-effort fail-open + POST `/api/api-keys/verify` endpoint mới central-only mount + `VerifyApiKeyRequest` Pydantic schema. 22/22 unit test PASS + 452/452 regression + 50/50 cluster + 19/19 integration. 0 deviation.
> 4. Plan 06-04 — Wave 4 BLOCKING lifespan integration: `main.py::lifespan` thêm block hub con init 3 client + spawn `settings_subscriber_task` guard `redis_ready` (T-06-04-04 Rule 3 fix) + shutdown graceful wait_for(10s) + `SETTINGS_SKIP_FETCH=1` escape hatch (4-flag pattern song song JWKS_SKIP_FETCH + SYNC_SKIP_CENTRAL_POOL + COCOINDEX_SKIP_SETUP) + 6 integration test ASGI LifespanManager. 6/6 integration PASS + 452/452 unit regression + 19/19 Phase 2+5 integration regression. 3 Rule 3 deviation auto-fix inline (main.py redis_ready guard + 2 test infra SETTINGS_SKIP_FETCH=1 env).
> 5. Plan 06-05 — Wave 5 closeout: docs update CLAUDE.md §6 progress row + Phase 6 pattern subsection + STATE.md frontmatter + Phase 6 Planning + Results Summary + REQUIREMENTS.md (file này) SETTINGS-01..04 mark [x] + NOTE Phase 6 closeout + ROADMAP.md Phase 6 row DONE + Decisions block + Progress table 33/~37 ≈ 89% + README.md NEW section "System Settings Sync Deploy Notes (Phase 6 v3.0)". Task 5b smoke 4 hub × PUT central rag_config + verify hub con cache flush + central /api/api-keys/verify proxy 200 SKIP pre-resolved per --auto chain + v3.0-b precedent (Plan 03-05 + 04-07 + 05-06 pre-resolved skip pattern).
>
> **Backward compat (Phase 6 KHÔNG break M2):** `require_api_key` + `update_rag_config` signature mở rộng thêm `request: Request` param (FastAPI Depends auto-inject — consumer endpoint M2 KHÔNG cần update). M2 `ApiKeyService.verify_key` AES-GCM at-rest + `RagConfigService.update_config()` LOCKED unchanged. Hub con M2 cũ thiếu `SETTINGS_PROXY_SECRET` env → Settings validator fail boot → operator phải set TRƯỚC deploy (xem README.md "System Settings Sync Deploy Notes (Phase 6 v3.0)" section deploy 5 step + rollback procedure + `openssl rand -hex 32` generate secret).
>
> **R-V3-6 LOW mitigation chain:** HTTP pull on-demand + Redis cache TTL 60s/300s/60s baseline + pub/sub invalidate broadcast `settings:invalidate` channel — < 1s propagate thực tế (E-V3-4 < 30s threshold thừa margin) + subscriber reconnect retry 5s interval on ConnectionError (TTL natural fallback) + boot fail-loud rag_client + hub_client `fetch_initial()` 5s timeout (R-V3-5 pattern carry forward Plan 03-02 JWKSCache) + idempotent flush (multi pub/sub event same key OK). Subscriber spawn guard `redis_ready` source-of-truth T-06-04-04.
>
> **E-V3-4 propagate < 30s carry forward:** Default TTL 60s (rag_config + apikey) overlap pub/sub natural propagate + Pub/sub broadcast `hub="*"` tới all hub con đồng thời + Live measure defer Phase 7 MIGRATE-05 runtime smoke E2E.
>
> **v3.0-b progress:** Phase 6 hoàn tất 4/4 REQ-ID SETTINGS. v3.0-b 3/4 phase DONE (Phase 4+5+6 — 33/~37 plan ≈ 89%). Next: `/gsd-discuss-phase 7` Migration + Smoke E2E (MIGRATE-01..05 — pg_dump per hub_id + blue/green restore + MCP re-point central + smoke E2E 3 hub con + central + golden path + cross-hub p95 < 1.5s live measure E-V3-2 + hub isolation enforce E-V3-3).

### MIGRATE — Migration + Smoke E2E (5 REQ — GA-V3-D strategy chốt ở `/gsd-discuss-phase 7`)

- [x] **MIGRATE-01** ✅ Phase 7: Snapshot data `medinet_central.{chunks, documents, users, audit_logs, usage_events}` per `hub_id` qua `pg_dump --data-only --table=... --where="hub_id = '<uuid>'"` (option a GA-V3-D khuyến nghị seed; option b "snapshot + replay cocoindex flow rebuild từ file_store" backup). File output `migrate-<hub>-<date>.sql` lưu vào `migrate-snapshots/`. _Closed 2026-05-23 Plan 07-01 (commits e057e5f feat snapshot script + 786586d docs .gitignore + README; script 194 LOC + .gitignore PII exclude + README 30-day retention; bash -n PASS + 18 grep acceptance PASS; D-V3-Phase7-A LOCKED option a + T-07-01-01..04 STRIDE mitigation)._
- [ ] **MIGRATE-02**: Restore snapshot vào `medinet_hub_<name>` qua `psql -d medinet_hub_<name> -f migrate-<hub>.sql` (blue/green per-hub — R-V3-4 mitigation). Test trên DB NEW trước khi switch traffic. Sau verify smoke PASS → switch Caddy upstream `hub-<hub>:8180` từ central proxy sang hub con dedicated.
- [ ] **MIGRATE-03**: Truncate `hub_id` rows khỏi central post-migration — giữ skeleton aggregate. Central `medinet_central.{documents, users, audit_logs, usage_events}` chỉ giữ rows `hub_id IS NULL` (system-level — admin, audit, settings change). `medinet_central.chunks` KHÔNG truncate (D-V3-02 — vẫn nhận sync từ hub con cho cross-hub search).
- [ ] **MIGRATE-04**: MCP service re-point gọi central cho cross-hub aggregate (`mcp_service/config.py` đổi `API_BASE_URL` sang `https://central/api`). MCP tools `search_wiki(hub_id?)` + `ask_wiki(hub_id?)` vẫn hoạt động với `hub_id` parameter optional (cross-hub khi omit). OAuth flow Phase 8.3 carry forward — KHÔNG fan-out N hub con. Smoke MCP qua Claude Inspector PASS sau re-point.
- [ ] **MIGRATE-05**: Smoke E2E sau migration — Docker compose mở rộng up đầy đủ (1 Postgres + 1 Redis + 4 FastAPI process: central + 3 hub con + Caddy + frontend). Golden path mỗi hub: `wiki.domain.com/yte` → login (qua central SSO) → upload DOCX → poll status `completed` → search local hub → search cross-hub (qua central) → ask → citation `[N]` → logout. 3 hub con + central PASS hết. Cross-hub search latency p95 < 1.5s (E-V3-2). Hub con KHÔNG access được data hub khác (E-V3-3).

---

## v2 Requirements (defer sang v4.0 / v4.1)

### v4.0 — Production Hardening + Advanced RAG

- **HARD-V4-01**: OCR Vietnamese revisit (Docling/Tesseract optional sidecar) nếu user feedback regress scanned PDF.
- **HARD-V4-02**: Cross-dim embedding swap (1536 ↔ 3072) cho `text-embedding-3-large` full quality.
- **HARD-V4-03**: Streaming `/api/ask` qua SSE với citation injection mid-stream.
- **HARD-V4-04**: Comprehensive coverage >80% (v2.0 dừng ở 57.75% critical-path).
- **HARD-V4-05**: Khắc phục CONCERNS bảo mật cũ — `.gitignore` root + GCP key audit + AES_KEY rotation + XSS token storage migration httpOnly cookie.
- **HARD-V4-06**: Branch protection rule GitHub repo enforce 2 workflow trước merge main.
- **HARD-V4-07**: Postgres pg17 upgrade.
- **HARD-V4-08**: Email send (M2 user reset password chỉ log console).
- **HARD-V4-09**: Avatar upload S3/GCS.
- **HARD-V4-10**: GDrive file storage backend (M2 chỉ local `file_store/`).
- **HARD-V4-11**: cocoindex augmenter Q&A pair gen.

### v4.1 — Advanced Retrieval

- **RAG-V41-01**: Hybrid retrieval BM25 + reranker (Cohere / cross-encoder).
- **RAG-V41-02**: Local embedding model (sentence-transformers / BGE-M3 cho on-prem) — SEED-001.
- **RAG-V41-03**: Version history & concurrent editing.

---

## Out of Scope (v3.0)

- **OCR Vietnamese revisit** — defer v4.0. v3.0 vẫn dùng whitelist `{.docx, .txt, .md, .pdf text-only}` + `failed_unsupported` (R4 carry forward).
- **Cross-dim embedding swap** — defer v4.0. v3.0 vẫn PIN dim 1536 (R1 + R7 carry forward).
- **Streaming `/api/ask`** — defer v4.0.
- **Coverage >80%** — defer v4.0. v3.0 giữ gate ≥50% critical-path (HARD-03 carry forward).
- **Hybrid BM25 + reranker** — defer v4.1.
- **Local embedding model** — defer v4.1 (SEED-001 dormant).
- **Schema riêng per hub trong cùng DB** — bỏ phương án này (đụng R5/P7 — cocoindex `db_schema_name` cố định). LOCKED D-V3-01.
- **Instance Postgres riêng per hub** — bỏ phương án này (ops × N). LOCKED D-V3-01.
- **Federated HTTP fan-out cho cross-hub search** — bỏ phương án này (latency × N hub). LOCKED D-V3-02 (chunks+vector denormalized).
- **Hub tổng re-embed chunks từ hub con** — KHÔNG re-embed; lưu sẵn vector 1536-dim từ hub con. LOCKED D-V3-02.
- **Per-hub `VITE_HUB_NAME` build matrix** — bỏ option b GA-V3-C; chọn 1 build detect prefix (PROXY-02). Re-confirm ở discuss-phase 5.

---

## Traceability

100% coverage REQ → phase. Mapping 1-to-1 (mỗi REQ map đúng 1 phase, không có REQ orphan).

| REQ-ID | Phase | Confirm |
|---|---|---|
| TOPO-01..04 | Phase 1 | Multi-DB topology + per-hub Alembic |
| FACTOR-01..03 | Phase 2 | Hub-con codebase factor |
| SSO-01..04 | Phase 3 | Auth SSO + hub_ids trong JWT (GA-V3-A chốt) |
| SYNC-01..05 | Phase 4 | Cross-hub data sync (GA-V3-D part 1 chốt) |
| PROXY-01..04 | Phase 5 | Reverse proxy + frontend subpath (GA-V3-C chốt, D-V3-06 D6 expire) |
| SETTINGS-01..04 | Phase 6 | System settings sync (GA-V3-B chốt) |
| MIGRATE-01..05 | Phase 7 | Migration + smoke E2E (GA-V3-D part 2 chốt) |

**Tổng:** 4 + 3 + 4 + 5 + 4 + 4 + 5 = **29 REQ-ID v1**. 100% coverage qua 7 phase. Full mapping chi tiết: `.planning/ROADMAP.md` Traceability section.

---

*Last updated: 2026-05-21 sau `/gsd-new-milestone v3.0`. Pre-roadmap snapshot. REQ-ID có thể refine sau khi `/gsd-discuss-phase 1` chốt gray area Phase 1.*
