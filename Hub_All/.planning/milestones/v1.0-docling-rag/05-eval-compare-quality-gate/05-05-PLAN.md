---
phase: 05-eval-compare-quality-gate
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - Makefile
  - eval/README.md
autonomous: true
requirements: [EVAL-05]
runtime_smoke: deferred
must_haves:
  truths:
    - "Makefile root có target eval-smoke chạy 1 file e2e + assert extractor_used=docling + ≥1 chunk is_table=true"
    - "Makefile có target eval-baseline-docling, eval-compare, eval-all"
    - "eval/README.md update với hướng dẫn chạy đầy đủ Phase 5 (run_docling → run_compare → EVAL.md)"
    - "Hướng dẫn user chạy runtime end-to-end (defer cho user vì cần Docling service real)"
  artifacts:
    - path: "Makefile"
      provides: "EVAL-05: eval-smoke + eval-* targets"
      contains: "eval-smoke"
    - path: "eval/README.md"
      provides: "Hướng dẫn chạy Phase 5 + runtime checklist"
      min_lines: 100
  key_links:
    - from: "Makefile target eval-smoke"
      to: "eval/lib.py APIClient + Docling service"
      via: "Python inline script upload 1 file + search + assert"
      pattern: "is_table"
---

<objective>
Implement EVAL-05 + final M1 wrap: thêm `Makefile` root targets (`eval-smoke`, `eval-baseline-docling`, `eval-compare`, `eval-all`) + update `eval/README.md` với hướng dẫn chạy Phase 5 đầy đủ + runtime checklist cho user.

Purpose: `make eval-smoke` là smoke test cuối cùng đảm bảo Docling thực sự được dùng (không fallback ngầm) — guard quan trọng cho M1 production. README là tài liệu dẫn dắt user chạy end-to-end Phase 5.
Output: `Makefile` (root, có thể đã tồn tại — append target nếu có), `eval/README.md` updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-eval-compare-quality-gate/05-CONTEXT.md
@.planning/phases/05-eval-compare-quality-gate/05-01-PLAN.md
@.planning/phases/05-eval-compare-quality-gate/05-04-PLAN.md
@.planning/REQUIREMENTS.md
@eval/lib.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo/Update Makefile root với 4 target eval-*</name>
  <files>Makefile</files>
  <action>
Kiểm tra `Makefile` root tồn tại không (`backend/Makefile` đã có — nhưng EVAL-05 yêu cầu **root Makefile** để `make eval-smoke` chạy từ root repo theo CONTEXT mục G).

Nếu chưa có `Makefile` root → tạo mới. Nếu đã có → append section `# === Eval (M1 Phase 5) ===`.

Nội dung target (đúng CONTEXT mục G):

```makefile
# === Eval (M1 Phase 5) ===

.PHONY: eval-smoke eval-baseline-docling eval-compare eval-all

eval-smoke: ## Smoke test: upload 1 file qua Docling + assert is_table chunk indexed
	@python -c "import sys; sys.path.insert(0, 'eval'); \
		from lib import APIClient, preflight; \
		import os; \
		preflight(os.getenv('BACKEND_URL', 'http://localhost:8180'), \
		          os.getenv('DB_DSN', 'postgresql://medinet@localhost:5432/medinet_central'), \
		          os.getenv('CHROMA_URL', 'http://localhost:8000'), \
		          'eval'); \
		c = APIClient(os.getenv('BACKEND_URL', 'http://localhost:8180'), \
		              os.getenv('ADMIN_EMAIL', 'admin@medinet.vn'), \
		              os.getenv('ADMIN_PASSWORD', 'Admin@123')); \
		c.login(); \
		hub = c.get_hub_id('eval'); \
		doc = c.upload_and_wait(hub, 'eval/dataset/sources/DMD_T1-01_DinhVi_TrungTam_v1.docx', timeout=300); \
		assert doc.get('extractor_used') == 'docling', f'Expected docling, got {doc.get(\"extractor_used\")}'; \
		results = c.search(hub, 'tóm tắt định vị thương hiệu', top_k=5); \
		has_table = any((r.get('metadata') or {}).get('is_table') for r in results); \
		assert has_table, 'Expected ≥ 1 chunk với is_table=true'; \
		print('SMOKE OK: Docling extractor used + table chunk indexed')"

eval-baseline-docling: ## Chạy ingestion mode docling + snapshot baseline_docling.json
	python eval/run_docling.py

eval-compare: ## Orchestrator: extraction + retrieval compare → EVAL.md
	python eval/run_compare.py

eval-all: eval-baseline-docling eval-compare ## Full pipeline: snapshot Docling → compare → EVAL.md
```

Nếu Makefile root đã tồn tại với content khác:
- Append section trên vào cuối file (preserve content cũ).
- Đảm bảo không xung đột target name.

Tab indent (Makefile yêu cầu TAB, không SPACE).

LƯU Ý: `make eval-smoke` runtime cần Docling service real → defer cho user. Executor chỉ commit Makefile, verify syntax (`make -n eval-smoke` dry-run sẽ in command mà không chạy).
  </action>
  <verify>
    <automated>test -f Makefile && grep -q "eval-smoke" Makefile && grep -q "eval-baseline-docling" Makefile && grep -q "eval-compare" Makefile && grep -q "eval-all" Makefile && echo OK</automated>
  </verify>
  <done>
`Makefile` root có 4 target `eval-smoke`, `eval-baseline-docling`, `eval-compare`, `eval-all`. Tab indent đúng. Smoke target đầy đủ logic: upload file → assert extractor_used=docling → search → assert is_table chunk.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update eval/README.md với hướng dẫn chạy Phase 5 đầy đủ</name>
  <files>eval/README.md</files>
  <action>
Update `eval/README.md` (tồn tại từ Phase 1) — thêm section mới ở cuối:

```markdown
## Phase 5 — Eval Compare & Quality Gate (M1)

Phase 5 so sánh chất lượng RAG giữa 2 mode `RAG_EXTRACTOR=native` (baseline Phase 1) và `=docling` (sản phẩm M1) để gate milestone.

### Tiền điều kiện

1. Phase 1 đã chạy xong: `eval/baseline_native.json` tồn tại (commit `f37cd96`).
2. Phase 2-4 đã ship: service `docling-pipeline` chạy được, `RAG_EXTRACTOR=docling` switch được runtime qua `PUT /api/rag-config`.
3. Backend Go + Postgres + ChromaDB + docling-pipeline đều UP (`docker compose up`).
4. State sạch: `python eval/scripts/cleanup.py` reset eval_hub.

### Chạy end-to-end

```bash
# Bước 1: Snapshot mode Docling (~30-60 phút, 10 file × Docling extract chậm)
make eval-baseline-docling
# → eval/baseline_docling.json

# Bước 2: Compare + sinh EVAL.md (verdict PASS/FAIL)
make eval-compare
# → eval/extraction_compare.json
# → eval/retrieval_eval.json
# → eval/EVAL.md (commit cuối M1)

# Hoặc chạy 2 bước trên một lần:
make eval-all
```

### Smoke test

```bash
# Verify Docling thực sự được dùng (không fallback ngầm)
make eval-smoke
# Expected: SMOKE OK: Docling extractor used + table chunk indexed
```

### Quality gate

`run_compare.py` exit code:
- **0 (PASS):** top-3 hit rate cải thiện ≥ 15 điểm phần trăm so với baseline native, HOẶC top-3 đạt ≥ 75% tuyệt đối.
- **1 (FAIL):** Không đạt cả hai ngưỡng. `EVAL.md` vẫn được sinh để debug.

### File outputs

| File | Mô tả | Commit |
|---|---|---|
| `eval/baseline_native.json` | Phase 1 snapshot (immutable) | Có (f37cd96) |
| `eval/baseline_docling.json` | Phase 5 snapshot Docling mode | Có |
| `eval/extraction_compare.json` | Per-document extraction quality | Optional |
| `eval/retrieval_eval.json` | Retrieval metrics + per-query diff | Optional |
| `eval/EVAL.md` | **Báo cáo cuối M1** — verdict + bảng số liệu | **Có (gate milestone)** |

### Troubleshooting

- **`EMBEDDER LOCK FAIL`:** Embedder config khác baseline native. Kiểm tra `RAG_EMBEDDING_PROVIDER` + `RAG_EMBEDDING_MODEL` trong `backend/.env` khớp với `baseline_native.json`.
- **Docling chậm > 5 phút/file:** Chấp nhận — Docling extract đa cấp + OCR `vie+eng` chậm hơn native 5-10×. Tăng `--upload-timeout 600` nếu cần.
- **Smoke FAIL `Expected docling, got native`:** Docling service down hoặc circuit breaker đang OPEN. Check `GET /api/rag-config` trả `docling_service_status`. Reset breaker qua `PUT /api/rag-config {"extractor_mode": "auto"}`.
- **Verdict FAIL:** Đọc EVAL.md section 7 "Defer for M2/M3" — recommendation rerank / hybrid retrieval / data improvement.
```

KHÔNG xóa content cũ Phase 1 — chỉ append section mới ở cuối.

Tiếng Việt có dấu, tên kỹ thuật giữ tiếng Anh (Makefile target, ENV, file path, REQ-ID).
  </action>
  <verify>
    <automated>test -f eval/README.md && grep -q "Phase 5" eval/README.md && grep -q "make eval-smoke" eval/README.md && grep -q "EMBEDDER LOCK FAIL" eval/README.md && echo OK</automated>
  </verify>
  <done>
`eval/README.md` ≥ 100 dòng, có section "Phase 5 — Eval Compare & Quality Gate" với: tiền điều kiện, hướng dẫn chạy end-to-end (3 bước), smoke test, quality gate exit code, bảng file outputs, troubleshooting 4 mục.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `make eval-smoke` → backend Go API | Smoke test gọi admin endpoint, dùng `.env` credential local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | Smoke test có thể bị bypass nếu user sửa Makefile | accept | Trust boundary local dev — gate cứng đã ở Plan 05-04 |
| T-05-11 | Information Disclosure | Hardcode default credential `Admin@123` trong Makefile | accept | Đã có pattern `.env` override (env var có precedence trong Makefile recipe) |
</threat_model>

<verification>
- `Makefile` root có 4 target eval-*.
- `eval/README.md` có section Phase 5 đầy đủ.
- Smoke test logic đúng CONTEXT mục G (assert `extractor_used=docling` + `is_table=true`).
- 4 success criteria ROADMAP Phase 5 đã có code path:
  1. `python eval/run_extraction_compare.py` chạy hết 2 mode → Plan 05-02 ✅
  2. `python eval/run_retrieval_eval.py` 12 truy vấn vàng → Plan 05-03 ✅
  3. `eval/EVAL.md` commit với bảng cụ thể + gate verdict → Plan 05-04 ✅
  4. `make eval-smoke` upload + search + ≥1 is_table chunk → Plan 05-05 ✅ (this plan)
</verification>

<success_criteria>
- EVAL-05 phần code 100%.
- M1 Phase 5 wrap-up: README + Makefile sẵn sàng cho user chạy end-to-end.
- Runtime EVAL.md + smoke verdict defer cho user (cần Docling service real Phase 2-4 ship xong).
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/05-eval-compare-quality-gate/05-05-SUMMARY.md`.
Đây là plan cuối M1 — sau khi user chạy runtime + verify EVAL.md PASS, milestone M1 hoàn tất.
</output>
