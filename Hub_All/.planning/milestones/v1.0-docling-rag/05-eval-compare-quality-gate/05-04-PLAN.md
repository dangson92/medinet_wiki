---
phase: 05-eval-compare-quality-gate
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - eval/run_compare.py
autonomous: true
requirements: [EVAL-04]
runtime_smoke: deferred
must_haves:
  truths:
    - "Script orchestrator chạy run_extraction_compare.py + run_retrieval_eval.py qua subprocess"
    - "Apply quality gate logic: PASS nếu delta_top3 >= 0.15 OR docling_top3 >= 0.75, else FAIL"
    - "Sinh eval/EVAL.md đúng template CONTEXT mục F (Setup / Retrieval / Per-query / Per-doc / Smoke / Conclusion)"
    - "Exit 0 nếu PASS, exit 1 nếu FAIL (CI-friendly)"
    - "EVAL.md vẫn được sinh dù FAIL — chứa số liệu để debug"
  artifacts:
    - path: "eval/run_compare.py"
      provides: "EVAL-04: orchestrator + quality gate + EVAL.md generator"
      min_lines: 250
  key_links:
    - from: "eval/run_compare.py"
      to: "run_extraction_compare.py + run_retrieval_eval.py"
      via: "subprocess.run + read JSON output"
      pattern: "subprocess"
    - from: "eval/run_compare.py"
      to: "eval/EVAL.md"
      via: "Markdown template + tabulate"
      pattern: "EVAL.md"
---

<objective>
Implement EVAL-04: orchestrator `eval/run_compare.py` chạy Plan 05-02 + 05-03 → tổng hợp → apply quality gate logic → sinh `eval/EVAL.md` (sản phẩm cuối M1) + exit code 0/1 theo gate.

Purpose: Đây là **gate milestone M1**. Output `EVAL.md` là sản phẩm chứng minh M1 success — bảng số liệu cụ thể trước/sau, verdict PASS/FAIL, không dùng từ định tính chung chung.
Output: `eval/run_compare.py`, `eval/EVAL.md` (artifact runtime).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-eval-compare-quality-gate/05-CONTEXT.md
@.planning/phases/05-eval-compare-quality-gate/05-01-PLAN.md
@.planning/phases/05-eval-compare-quality-gate/05-02-PLAN.md
@.planning/phases/05-eval-compare-quality-gate/05-03-PLAN.md
@.planning/REQUIREMENTS.md
@eval/baseline_native.json
</context>

<interfaces>
**Input:**
- `eval/baseline_native.json` (Phase 1, immutable)
- `eval/baseline_docling.json` (Plan 05-01 output)
- `eval/extraction_compare.json` (Plan 05-02 output, hoặc tự chạy `run_extraction_compare.py` qua subprocess)
- `eval/retrieval_eval.json` (Plan 05-03 output, hoặc tự chạy `run_retrieval_eval.py` qua subprocess)

**Output `eval/EVAL.md`** — đúng template CONTEXT mục F:
```markdown
# EVAL — M1 RAG Quality with Docling

**Ngày chạy:** 2026-05-XX
**Verdict:** PASS / FAIL
**Reason:** <1 dòng tóm tắt>

## 1. Setup
- Dataset: 10 file
- Queries: 12 truy vấn vàng
- Embedder: openai/text-embedding-3-large 3072d (locked)
- Native run: 2026-04-28 (commit f37cd96)
- Docling run: 2026-05-XX

## 2. Retrieval Comparison
| Metric | Native | Docling | Delta |
|---|---|---|---|
| top-1 | ... | ... | ... |

## 3. Per-Query Diff
| Q | Expected | Native rank | Docling rank | Verdict |
|---|---|---|---|---|

## 4. Per-Document Extraction Quality
| Doc | Native chunks | Docling chunks | Heading recall | Table preservation |
|---|---|---|---|---|

## 5. Smoke Verification
- `make eval-smoke` last run: <ngày> → status

## 6. Conclusion
<text trình bày verdict + recommendation>

## 7. Defer for M2/M3
- ...
```

**Quality gate function** (CONTEXT mục E):
```python
def evaluate_gate(native: dict, docling: dict) -> tuple[str, str]:
    n3 = native["retrieval"]["top_3_hit_rate"]
    d3 = docling["retrieval"]["top_3_hit_rate"]
    delta = d3 - n3
    if delta >= 0.15:
        return "PASS", f"top-3 cải thiện {delta*100:+.1f}pp ({n3*100:.1f}% → {d3*100:.1f}%)"
    if d3 >= 0.75:
        return "PASS", f"top-3 đạt {d3*100:.1f}% (≥ 75% tuyệt đối, dù delta {delta*100:+.1f}pp)"
    return "FAIL", f"top-3 chỉ {d3*100:.1f}%, delta {delta*100:+.1f}pp — chưa đạt gate ≥ +15pp HOẶC ≥ 75%"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tạo eval/run_compare.py — orchestrator + quality gate + EVAL.md generator</name>
  <files>eval/run_compare.py</files>
  <action>
Tạo `eval/run_compare.py`:

1. **Argparse:** `--skip-extraction` / `--skip-retrieval` (cho phép skip nếu JSON intermediate đã có), `--output eval/EVAL.md`.

2. **Pre-condition check:** `baseline_native.json` + `baseline_docling.json` tồn tại. Thiếu → exit 1 với hướng dẫn rõ.

3. **Orchestrate Plan 05-02 + 05-03 qua subprocess:**
   ```python
   if not args.skip_extraction:
       result = subprocess.run(["python", "eval/run_extraction_compare.py"], check=False)
       if result.returncode != 0:
           logger.error("run_extraction_compare.py FAIL"); sys.exit(1)
   if not args.skip_retrieval:
       result = subprocess.run(["python", "eval/run_retrieval_eval.py"], check=False)
       if result.returncode != 0:
           logger.error("run_retrieval_eval.py FAIL"); sys.exit(1)
   ```

4. **Load 4 JSON:** native, docling, extraction_compare, retrieval_eval.

5. **Apply quality gate** function `evaluate_gate(native, docling)` đúng spec CONTEXT mục E (xem `<interfaces>`).

6. **Sinh `eval/EVAL.md`** dùng `tabulate` (Markdown format) cho 3 bảng:
   - **Bảng Setup** (section 1): bullet list, KHÔNG dùng tabulate.
   - **Bảng Retrieval Comparison** (section 2): tabulate `[[metric, native, docling, delta]]` headers `["Metric", "Native", "Docling", "Delta"]` tablefmt `"github"`.
   - **Bảng Per-Query Diff** (section 3): tabulate per_query từ `retrieval_eval.json` — columns `[Q, Expected, Native rank, Docling rank, Verdict]`.
   - **Bảng Per-Document** (section 4): tabulate documents từ `extraction_compare.json` — columns `[Doc, Native chunks, Docling chunks, Heading recall (N→D), Table preservation (N→D)]`.
   - **Section 5 Smoke:** placeholder `Đang chờ user chạy 'make eval-smoke' (Plan 05-05)` — Plan 05-05 sẽ update sau.
   - **Section 6 Conclusion:** text trình bày verdict + recommendation. Nếu PASS → khen. Nếu FAIL → recommend 3 hướng (reranker, hybrid retrieval, data improvement — đồng bộ CONTEXT Rủi ro 4).
   - **Section 7 Defer for M2/M3:** liệt kê 3 deferred ideas từ CONTEXT mục Deferred (999.13/14/15) + bất kỳ query nào có verdict `REGRESSED` hoặc `both_miss`.

7. **Format số:**
   - Hit rate: `{x*100:.1f}%`
   - Delta pp: `{x:+.1f} pp`
   - MRR: `{x:.3f}`

8. **Verdict header:** dòng `**Verdict:** PASS` (hoặc FAIL) + `**Reason:** <reason>` ngay sau ngày chạy.

9. **Date:** dùng `datetime.now(UTC).strftime("%Y-%m-%d")` cho "Ngày chạy". "Native run" lấy từ `native["run_id"]` parse date. "Docling run" lấy từ `docling["run_id"]`.

10. **Embedder line:** `{provider}/{model} {dim}d (locked)` lấy từ `native` (đã verify match docling).

11. **Exit code:** `sys.exit(0 if verdict == "PASS" else 1)` — CI-friendly.

12. **EVAL.md vẫn được sinh dù FAIL** (CONTEXT mục E: "Nhưng EVAL.md vẫn được sinh dù FAIL — chứa số liệu để debug"). Chỉ exit 1, không skip ghi file.

13. **Logging tóm tắt:** in ra console verdict + reason trước khi exit.

LƯU Ý: Script này dùng `tabulate` (thêm ở Plan 05-01). Runtime cần snapshot Docling từ user → defer.

LƯU Ý quan trọng: `eval/EVAL.md` là sản phẩm M1 — KHÔNG được commit placeholder. File này được sinh runtime, executor chỉ verify code parse. Phần text trong section Conclusion phải tiếng Việt có dấu, không xen tiếng Anh dài.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('eval/run_compare.py', encoding='utf-8').read()); print('parse OK')"</automated>
  </verify>
  <done>
`eval/run_compare.py` ≥ 250 dòng, parse OK, có orchestrator subprocess + quality gate function chính xác CONTEXT mục E + EVAL.md generator template CONTEXT mục F + exit code 0/1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → subprocess (Python child) | Chạy script local cùng repo, full trust |
| Script → file system | Write EVAL.md (commit artifact) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | EVAL.md có thể bị edit thủ công sau commit | accept | Git history audit, gate enforce qua exit code |
| T-05-09 | Repudiation | Verdict PASS/FAIL không có chữ ký | accept | run_id + commit hash trong EVAL.md đủ traceability |
</threat_model>

<verification>
- Script parse OK.
- `evaluate_gate()` đúng spec CONTEXT mục E (3 case PASS-delta / PASS-absolute / FAIL).
- Template EVAL.md đủ 7 section CONTEXT mục F.
- Exit code 0 nếu PASS, 1 nếu FAIL.
- `tabulate` được import + dùng cho 3 bảng.
- EVAL.md vẫn ghi dù FAIL.
</verification>

<success_criteria>
- EVAL-04 phần code 100% — orchestrator + gate + generator.
- Sản phẩm `eval/EVAL.md` (runtime, user chạy) đầy đủ 7 section, verdict rõ ràng, số liệu cụ thể.
- Gate cứng theo CONTEXT mục E.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/05-eval-compare-quality-gate/05-04-SUMMARY.md`.
</output>
