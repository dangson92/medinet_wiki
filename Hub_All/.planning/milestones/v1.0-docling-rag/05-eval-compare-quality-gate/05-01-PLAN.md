---
phase: 05-eval-compare-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/lib.py
  - eval/run_docling.py
  - eval/pyproject.toml
autonomous: true
requirements: []
runtime_smoke: deferred
must_haves:
  truths:
    - "Module eval/lib.py expose APIClient + helper preflight/get_embedder_config/upload_dataset/evaluate_queries"
    - "Script run_docling.py PUT /api/rag-config sang extractor_mode=docling, upload 10 file, snapshot eval/baseline_docling.json"
    - "Script restore /api/rag-config về extractor_mode=auto cuối run (kể cả khi exception)"
    - "Embedder lock verify hard fail nếu provider/model/dim khác baseline_native.json"
  artifacts:
    - path: "eval/lib.py"
      provides: "Shared API client + helper reuse cho Phase 5"
      min_lines: 250
    - path: "eval/run_docling.py"
      provides: "Orchestrator chạy ingestion mode docling + snapshot baseline_docling.json"
      min_lines: 150
    - path: "eval/pyproject.toml"
      provides: "Thêm dependency tabulate>=0.9"
      contains: "tabulate"
  key_links:
    - from: "eval/run_docling.py"
      to: "eval/lib.py"
      via: "import APIClient, preflight, upload_dataset, evaluate_queries"
      pattern: "from lib import"
    - from: "eval/run_docling.py"
      to: "/api/rag-config"
      via: "PUT extractor_mode=docling tại bắt đầu, restore auto cuối"
      pattern: "rag-config"
---

<objective>
Tạo nền tảng Phase 5: extract pattern reuse từ `eval/baseline.py` (Phase 1, immutable) sang module `eval/lib.py` để 4 plan kế tiếp dùng chung; viết `eval/run_docling.py` chạy ingestion với `RAG_EXTRACTOR=docling` runtime và sinh snapshot `eval/baseline_docling.json`.

Purpose: Mọi script eval Phase 5 (extraction compare, retrieval eval, orchestrator, smoke) sẽ import `lib.py` — tránh duplicate 588 dòng `baseline.py`. `run_docling.py` là input chính cho Plan 02–04.
Output: `eval/lib.py`, `eval/run_docling.py`, `eval/baseline_docling.json` (artifact runtime do user chạy), `eval/pyproject.toml` thêm `tabulate>=0.9`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-eval-compare-quality-gate/05-CONTEXT.md
@.planning/phases/01-eval-dataset-baseline-native/01-CONTEXT.md
@eval/baseline.py
@eval/baseline_native.json
@eval/pyproject.toml
@eval/dataset/queries.jsonl
</context>

<interfaces>
Interface contract của `eval/lib.py` (Phase 5 plan kế tiếp sẽ import):

```python
from dataclasses import dataclass

@dataclass
class DocResult:
    doc_id: str
    filename: str
    status: str            # "completed" | "error"
    chunks_count: int
    avg_chunk_tokens: float
    extractor_used: str    # "docling" | "native" | "" (nếu fail trước khi ghi)
    error: str | None = None

@dataclass
class QueryResult:
    query_id: str
    query: str
    expected_doc_id: str
    ranks: list[int]       # [rank_top1_match, ...] -- empty nếu không hit
    top_rank: int | None   # rank đầu tiên match expected_doc_id, None nếu miss

@dataclass
class RetrievalMetrics:
    top_1_hit_rate: float
    top_3_hit_rate: float
    top_5_hit_rate: float
    mrr: float
    per_query: list[QueryResult]

class APIClient:
    def __init__(self, base_url: str, email: str, password: str): ...
    def login(self) -> None: ...
    def refresh_if_expired(self) -> None: ...
    def get_hub_id(self, code: str) -> str: ...
    def upload_and_wait(self, hub_id: str, file_path: str, timeout: int = 300) -> dict: ...
    def search(self, hub_id: str, query: str, top_k: int = 5, min_score: float = 0.0) -> list[dict]: ...
    def get_rag_config(self) -> dict: ...
    def put_rag_config(self, payload: dict) -> dict: ...
    def reindex(self, doc_id: str, extractor: str) -> dict: ...

def preflight(backend_url: str, db_dsn: str, chroma_url: str, hub_code: str) -> None: ...
def get_embedder_config(client: "APIClient") -> dict: ...   # {provider, model, dim}
def assert_embedder_match(current: dict, baseline_native_path: str) -> None: ...   # hard fail
def upload_dataset(client: "APIClient", hub_id: str, dataset_dir: str) -> list[DocResult]: ...
def evaluate_queries(client: "APIClient", hub_id: str, queries_path: str, top_k: int = 5) -> RetrievalMetrics: ...
```

Schema `baseline_docling.json` IDENTICAL với `baseline_native.json` (Phase 1) + thêm field `extractor_used` per-document. Tham chiếu chi tiết: `.planning/phases/05-eval-compare-quality-gate/05-CONTEXT.md` mục B.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tạo module eval/lib.py extract pattern reuse từ baseline.py</name>
  <files>eval/lib.py</files>
  <action>
Tạo `eval/lib.py` (Python 3.11, đồng bộ với `baseline.py` về convention):

1. Copy logic (KHÔNG sửa `baseline.py`) các pattern reuse từ `eval/baseline.py`:
   - Login admin → access_token (handle 15-min TTL, có `refresh_if_expired`).
   - `get_hub_id(code)` qua `GET /api/hubs?code=`.
   - `upload_and_wait(hub_id, file_path, timeout)`: POST `/api/documents/upload` + poll `/api/documents/{id}` đến `status in (completed, error)`.
   - `search(hub_id, query, top_k)`: POST `/api/search`.
   - `preflight(backend_url, db_dsn, chroma_url, hub_code)`: 3 check Phase 1 — backend health, ChromaDB heartbeat, hub seed (psycopg).

2. Bổ sung helper Phase 5 (KHÔNG có ở Phase 1):
   - `get_rag_config()` GET `/api/rag-config` → trả `{extractor_mode, embedder_provider, embedder_model, embedder_dim, ...}`.
   - `put_rag_config(payload)` PUT `/api/rag-config` (admin token bắt buộc).
   - `reindex(doc_id, extractor)` POST `/api/documents/{id}/reindex?extractor=docling|native` (CFG-07, Phase 4).
   - `get_embedder_config(client)` wrapper trả `{provider, model, dim}`.
   - `assert_embedder_match(current, baseline_native_path)`: load `baseline_native.json`, compare 3 field `embedder_provider/embedder_model/embedder_dim`. Mismatch → `raise SystemExit(f"EMBEDDER LOCK FAIL: ...")`.
   - `upload_dataset(client, hub_id, dataset_dir)`: glob 8 file `eval/dataset/sources/*.docx + *.pdf` + 2 file `eval/dataset/scanned/*.pdf`, upload tuần tự + poll, trả `list[DocResult]`. Đọc `extractor_used` từ response `GET /api/documents/{id}` (CFG-06).
   - `evaluate_queries(client, hub_id, queries_path, top_k=5)`: load `queries.jsonl`, mỗi query gọi `search()`, match `expected_doc_id` với `result.category` lowercase (đồng bộ Phase 1 baseline.py REVISION 1 B2), trả `RetrievalMetrics` (top-1/3/5 + MRR + per_query list).

3. Dataclass `DocResult`, `QueryResult`, `RetrievalMetrics` đặt đầu file (xem `<interfaces>`).

4. Logging: `logging.basicConfig(level=INFO, format="%(asctime)s [%(levelname)s] %(message)s")` (đồng bộ Phase 1).

5. Env load: `load_dotenv(REPO_ROOT / "eval" / ".env")` (giữ pattern baseline.py).

6. KHÔNG có `if __name__ == "__main__"` — đây là module library.

7. Style: ruff sạch (pattern theo `pyproject.toml` Phase 1: line-length 100, double quote, target py311).

8. Type hint đầy đủ (đồng bộ baseline.py).
  </action>
  <verify>
    <automated>cd eval && python -c "from lib import APIClient, preflight, get_embedder_config, assert_embedder_match, upload_dataset, evaluate_queries, DocResult, QueryResult, RetrievalMetrics; print('OK')"</automated>
  </verify>
  <done>
File `eval/lib.py` tồn tại, ≥ 250 dòng, import-able, ruff không error, expose đầy đủ symbol theo `<interfaces>`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật eval/pyproject.toml thêm dep tabulate>=0.9</name>
  <files>eval/pyproject.toml</files>
  <action>
Thêm `"tabulate>=0.9",` vào mảng `dependencies` của `eval/pyproject.toml` (giữ alphabet thứ tự nếu có thể, hoặc thêm cuối). KHÔNG thay đổi field nào khác. Lý do: `tabulate` cần cho Plan 05-04 sinh Markdown table trong `EVAL.md`. Đặt ở Plan 05-01 vì các plan sau cần install dep ngay từ wave 2.
  </action>
  <verify>
    <automated>grep -q "tabulate" eval/pyproject.toml && echo OK</automated>
  </verify>
  <done>
`eval/pyproject.toml` chứa `tabulate>=0.9` trong `dependencies`. `pip install -e eval/` install thành công (defer runtime cho user — không block executor).
  </done>
</task>

<task type="auto">
  <name>Task 3: Tạo eval/run_docling.py orchestrator chạy ingestion mode docling + snapshot</name>
  <files>eval/run_docling.py</files>
  <action>
Tạo `eval/run_docling.py` (Python 3.11), pattern song song với `baseline.py` Phase 1 nhưng dùng `lib.py`:

Pipeline:
1. Argparse: `--top-k 5`, `--upload-timeout 300` (cao hơn baseline 180 vì Docling chậm hơn, theo CONTEXT mục D).
2. `preflight()` (lib.py).
3. `client = APIClient(...); client.login()`.
4. `current_cfg = client.get_rag_config()`; `embedder = get_embedder_config(client)`.
5. `assert_embedder_match(embedder, "eval/baseline_native.json")` — hard fail nếu lệch (theo CONTEXT mục B "Embedder lock verify").
6. **Switch mode:** `original_mode = current_cfg["extractor_mode"]`; `client.put_rag_config({"extractor_mode": "docling"})`. Log loud.
7. **Try/finally restore:**
   ```python
   try:
       hub_id = client.get_hub_id(EVAL_HUB_CODE)
       docs: list[DocResult] = upload_dataset(client, hub_id, "eval/dataset")
       # Verify Docling thực sự được dùng (theo CONTEXT mục D step 3):
       docling_used_count = sum(1 for d in docs if d.extractor_used == "docling")
       logger.info(f"extractor_used=docling: {docling_used_count}/{len(docs)} docs")
       metrics: RetrievalMetrics = evaluate_queries(client, hub_id, "eval/dataset/queries.jsonl", top_k=args.top_k)
   finally:
       # Restore mode auto theo CONTEXT mục D "Restore mode sau run"
       client.put_rag_config({"extractor_mode": "auto"})
       logger.info("Restored extractor_mode=auto")
   ```
8. Snapshot `eval/baseline_docling.json` schema IDENTICAL `baseline_native.json` + field `extractor_used` per-document (xem `<interfaces>` + CONTEXT mục B):
   ```json
   {
     "run_id": "2026-05-XXTHH:MM:SSZ",
     "extractor_mode": "docling",
     "embedder_provider": "...",
     "embedder_model": "...",
     "embedder_dim": ...,
     "documents": [
       {"doc_id": "...", "filename": "...", "status": "completed",
        "chunks_count": N, "avg_chunk_tokens": F,
        "extractor_used": "docling"}
     ],
     "retrieval": {"top_1_hit_rate": ..., "top_3_hit_rate": ..., "top_5_hit_rate": ..., "mrr": ..., "per_query": [...]}
   }
   ```
9. Log progress mỗi file (CONTEXT Rủi ro 3: run time có thể 30-60 phút).

10. Exit 0 thành công, exit 1 nếu preflight/embedder lock fail.

LƯU Ý: Phase 5 plan này code 100%, **runtime defer cho user** (cần Docling service real từ Phase 2-4 — `runtime_smoke: deferred` trong frontmatter). Executor chỉ commit code + verify import, KHÔNG chạy thực tế.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('eval/run_docling.py', encoding='utf-8').read()); print('parse OK')" && python -c "import sys; sys.path.insert(0, 'eval'); import run_docling; print('import OK')"</automated>
  </verify>
  <done>
`eval/run_docling.py` tồn tại ≥ 150 dòng, parse + import OK, có try/finally restore mode `auto`, có `assert_embedder_match` hard fail, schema snapshot khớp `<interfaces>`. Runtime chạy thật defer cho user.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval script → backend Go API | Script eval gửi token admin qua HTTPS local, tin cậy backend |
| eval script → Postgres (psycopg) | Read-only check hub seed, dùng credential từ `.env` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `put_rag_config` đổi extractor_mode runtime | mitigate | Try/finally restore về `auto` đảm bảo state không bị corrupt nếu script crash |
| T-05-02 | Information Disclosure | Token admin trong env | accept | `.env` không commit, đồng bộ pattern Phase 1 |
| T-05-03 | Denial of Service | Switch mode khi worker đang process job native | mitigate | CONTEXT Rủi ro 2: cleanup eval_hub trước khi switch + chờ workers idle (Plan 05-04 orchestrator handle) |
</threat_model>

<verification>
- `python -c "from lib import ..."` import OK toàn bộ symbol expose.
- `python -c "import run_docling"` parse + import OK.
- `eval/pyproject.toml` chứa `tabulate>=0.9`.
- KHÔNG sửa `eval/baseline.py` Phase 1 (immutable).
- KHÔNG sửa `eval/baseline_native.json` Phase 1 (immutable).
</verification>

<success_criteria>
- `eval/lib.py` expose APIClient + helper, type hint đầy đủ, ruff sạch.
- `eval/run_docling.py` parse + import OK, restore mode `auto` cuối run, embedder lock verify.
- `eval/pyproject.toml` thêm `tabulate>=0.9`.
- Plan 05-02..05 có thể `from lib import ...` và `subprocess.run([...,"eval/run_docling.py"])`.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/05-eval-compare-quality-gate/05-01-SUMMARY.md`.
</output>
