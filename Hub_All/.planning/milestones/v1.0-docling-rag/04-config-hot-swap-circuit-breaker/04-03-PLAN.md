---
phase: 04-config-hot-swap-circuit-breaker
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - backend/internal/router/router.go
  - backend/cmd/server/main.go
  - backend/internal/rag/extractor/docling.go
autonomous: true
requirements: [CFG-02, CFG-03, CFG-04]
tags: [router, admin, rag-config, http]

must_haves:
  truths:
    - "`GET /api/rag-config` trả thêm 5 field: `extractor_mode`, `docling_service_status`, `docling_version`, `docling_circuit_state`, `last_fallback_at`."
    - "`PUT /api/rag-config` accept body `{ \"extractor_mode\": \"native|docling|auto\" }` — validate enum, gọi `pipeline.SetExtractorMode(mode)` (setter đã có sẵn từ Plan 02), reset Redis circuit state, ghi audit `action=\"rag_config_change\"`."
    - "Khi admin PUT mode khác mode hiện tại, log `slog.Info` ghi from→to + actor user_id."
    - "Validate fail (mode không thuộc enum) → 400 với message rõ ràng tiếng Việt."
    - "Field `last_fallback_at` query từ `audit_logs WHERE action='rag_fallback' ORDER BY timestamp DESC LIMIT 1` — null nếu chưa có fallback."
    - "DoclingExtractor truyền `ocr_options.langs` từ ctx key `WithOCRLangs` qua multipart field `ocr_langs` khi gọi `/v1/process`."
    - "Plan này KHÔNG sửa `pipeline.go` (toàn bộ struct change đã gộp vào Plan 02 theo CHECK round 1 B2)."
  artifacts:
    - path: "backend/internal/router/router.go"
      provides: "GET/PUT /api/rag-config extension + 1 import extractor.DoclingCircuit + extractor.DoclingHealthProbe; signature `Setup` thêm 4 param mới."
      contains: "extractor_mode"
    - path: "backend/cmd/server/main.go"
      provides: "Pass doclingCircuit + doclingHealthProbe + auditRepo + pipeline vào router.Setup(). Verify settingsRepo đã có ở scope router (W4)."
      contains: "doclingHealthProbe"
    - path: "backend/internal/rag/extractor/docling.go"
      provides: "Context key `ctxOCRLangsKey` + `WithOCRLangs(ctx, langs)` + multipart writer thêm field `ocr_langs`."
      contains: "ocr_langs"
  key_links:
    - from: "GET /api/rag-config"
      to: "extractor.DoclingHealthProbe.Status(ctx)"
      via: "healthProbe.Status(c.Request.Context())"
      pattern: "healthProbe\\.Status"
    - from: "PUT /api/rag-config"
      to: "DoclingCircuit.Reset(ctx)"
      via: "doclingCircuit.Reset(c.Request.Context())"
      pattern: "doclingCircuit\\.Reset"
    - from: "PUT /api/rag-config"
      to: "auditRepo.Insert(...action=\"rag_config_change\")"
      via: "audit when extractor_mode changed"
      pattern: "rag_config_change"
    - from: "PUT /api/rag-config"
      to: "pipeline.SetExtractorMode(mode)"
      via: "setter có sẵn từ Plan 02 — Plan này CHỈ gọi"
      pattern: "SetExtractorMode"
---

<objective>
Mở rộng admin endpoint `GET/PUT /api/rag-config` để admin quan sát + force chuyển mode runtime, đồng thời propagate `DOCLING_OCR_LANGS` qua sidecar request.

Purpose: CFG-03 (admin observability) + CFG-04 (reset state khi admin chỉnh) + CFG-02 (OCR langs propagate). Plan 02 đã có circuit + audit + 4 setter (`SetExtractorMode`, `SetDoclingOCRLangs`, ...); Plan này chỉ wire qua HTTP API + thêm context key + multipart field trong `docling.go`.

Output:
- `router.go`: signature `Setup()` thêm 4 param (`doclingCircuit`, `doclingHealthProbe`, `auditRepo`, `pipeline`); GET response thêm 5 field; PUT body accept `extractor_mode` + reset circuit + audit log + gọi `pipeline.SetExtractorMode`.
- `main.go`: tạo `doclingHealthProbe`, pass 4 param mới vào `router.Setup`. Verify `settingsRepo` đã ở scope router.
- `docling.go`: thêm `ctxOCRLangsKey` + `WithOCRLangs()` + multipart field `ocr_langs`.

Plan này KHÔNG đụng `pipeline.go` (CHECK round 1 B2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-config-hot-swap-circuit-breaker/04-CONTEXT.md
@.planning/phases/04-config-hot-swap-circuit-breaker/04-01-PLAN.md
@.planning/phases/04-config-hot-swap-circuit-breaker/04-02-PLAN.md
@backend/internal/router/router.go
@backend/internal/rag/extractor/docling.go
@backend/internal/repository/audit_repo.go

<interfaces>
<!-- API hợp đồng + setter list. -->

Router.Setup hiện tại (Phase 3, sẽ extend):
```go
func Setup(
    cfg *config.Config,
    jwtManager *jwtpkg.Manager,
    rdb *redis.Client,
    authHandler *handler.AuthHandler,
    ...
    swappableEmbedder *embedding.SwappableEmbedder,
) *gin.Engine
```

Cần add 4 param mới (theo thứ tự cuối cùng):
```go
doclingCircuit     *extractor.DoclingCircuit,
doclingHealthProbe *extractor.DoclingHealthProbe,
auditRepo          *repository.AuditRepo,    // (đã có nếu phase trước truyền — kiểm tra)
pipeline           *rag.Pipeline,
```

Setter có sẵn từ Plan 02 (KHÔNG cần thêm gì vào pipeline.go ở plan này):
```go
func (p *Pipeline) SetExtractorMode(mode string)
func (p *Pipeline) SetDoclingCircuit(c *extractor.DoclingCircuit)
func (p *Pipeline) SetAuditInserter(r AuditInserter)
func (p *Pipeline) SetDoclingOCRLangs(langs string)
```

GET response hiện tại (router.go:91-104) trả 12 field. Plan này thêm 5 field mới — KHÔNG xóa field cũ.

PUT request struct hiện tại (router.go:108-128) có 9 field. Plan này thêm 1 field `ExtractorMode string`.

`AuditRepo.List(ctx, dateFrom, dateTo, actorType, action, hubID, page, perPage)`:
```go
// Dùng để query last_fallback_at:
entries, _, err := auditRepo.List(ctx, "", "", "", "rag_fallback", "", 1, 1)
if err == nil && len(entries) > 0 {
    lastFallbackAt = entries[0].Timestamp.Format(time.RFC3339)
}
```

`settingsRepo` (kiểm tra W4): grep `settingsRepo` trong `backend/internal/router/router.go` để xác nhận đã có ở scope handler hiện tại. Nếu CHƯA có → Task 2 main.go phải pass thêm vào `router.Setup` HOẶC fallback chỉ update `cfg.RAG.Extractor` in-memory + audit (KHÔNG persist) và defer persistence sang hardening milestone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `GET /api/rag-config` + `PUT /api/rag-config` trong router.go</name>
  <files>backend/internal/router/router.go</files>
  <action>
1. **Sửa signature `router.Setup()`** — thêm 4 param SAU `swappableEmbedder`:

```go
func Setup(
    cfg *config.Config,
    ...
    swappableEmbedder *embedding.SwappableEmbedder,
    // ─── M1 Phase 4 ───
    doclingCircuit *extractor.DoclingCircuit,
    doclingHealthProbe *extractor.DoclingHealthProbe,
    auditRepo *repository.AuditRepo,
    pipeline *rag.Pipeline,
) *gin.Engine {
```

Import thêm: `"github.com/medinet/hub-all-backend/internal/rag/extractor"` và `"github.com/medinet/hub-all-backend/internal/rag"` (nếu chưa có).

2. **VERIFY `settingsRepo`** (CHECK round 1 W4): trước khi viết PUT handler, grep:
   ```
   grep -n "settingsRepo" backend/internal/router/router.go
   ```
   - Nếu CÓ → dùng trực tiếp như interface đã có sẵn.
   - Nếu KHÔNG → 2 lựa chọn:
     - (a) thêm param `settingsRepo *repository.SettingsRepo` vào `router.Setup` signature + Task 2 pass từ main.go.
     - (b) bỏ persistence — chỉ update `cfg.RAG.Extractor` in-memory + ghi audit, defer persistence sang hardening. Comment rõ trong code: `// TODO(hardening): persist RAG_EXTRACTOR vào settings_repo khi available`.
   - Khuyến nghị (a) nếu `settingsRepo` đã tồn tại ở `main.go` scope (Phase trước đã wire cho LLM/embedding settings). Executor quyết tại chỗ.

3. **Sửa handler `GET /api/rag-config`** (~router.go:78-105) — extend response:

```go
r.GET("/api/rag-config", func(c *gin.Context) {
    geminiMask := maskKey(os.Getenv("GEMINI_API_KEY"))
    openaiMask := maskKey(os.Getenv("OPENAI_API_KEY"))
    llmMode := "auto"
    if llmClient != nil {
        llmMode = string(llmClient.Mode())
    }
    geminiLLMModel := os.Getenv(llm.GeminiModelEnvKey)
    if geminiLLMModel == "" {
        geminiLLMModel = llm.DefaultGeminiModel
    }

    // ─── M1 Phase 4 — Docling fields (CFG-03) ───
    var (
        doclingStatus  string = "unknown"
        doclingVersion string = ""
        circuitState   string = "unknown"
        lastFallback   *string
    )
    ctx := c.Request.Context()
    if doclingHealthProbe != nil {
        doclingStatus = string(doclingHealthProbe.Status(ctx))
        doclingVersion = doclingHealthProbe.Version(ctx)
    }
    if doclingCircuit != nil {
        circuitState = doclingCircuit.State().String()
    }
    if auditRepo != nil {
        entries, _, err := auditRepo.List(ctx, "", "", "", "rag_fallback", "", 1, 1)
        if err == nil && len(entries) > 0 {
            ts := entries[0].Timestamp.UTC().Format(time.RFC3339)
            lastFallback = &ts
        }
    }

    c.JSON(http.StatusOK, gin.H{
        // existing fields ...
        "chunker":            "Semantic Chunker (AI-powered)",
        "chunk_size":         cfg.RAG.ChunkSize,
        "chunk_overlap":      cfg.RAG.ChunkOverlap,
        "embedding_model":    cfg.RAG.EmbeddingModel,
        "embedding_provider": cfg.RAG.EmbeddingProvider,
        "batch_size":         cfg.RAG.BatchSize,
        "gemini_key_mask":    geminiMask,
        "openai_key_mask":    openaiMask,
        "gemini_key_saved":   os.Getenv("GEMINI_API_KEY") != "",
        "openai_key_saved":   os.Getenv("OPENAI_API_KEY") != "",
        "llm_provider":       llmMode,
        "gemini_llm_model":   geminiLLMModel,
        // ─── Phase 4 mới (CFG-03) ───
        "extractor_mode":          cfg.RAG.Extractor,        // native|docling|auto
        "docling_service_status":  doclingStatus,            // healthy|degraded|down|unknown
        "docling_version":         doclingVersion,           // "" nếu probe fail
        "docling_circuit_state":   circuitState,             // closed|half-open|open|unknown
        "last_fallback_at":        lastFallback,             // *string nullable RFC3339
        "docling_fail_threshold":  cfg.RAG.DoclingFailThreshold,
        "docling_cooldown_min":    cfg.RAG.DoclingCooldownMin,
    })
})
```

4. **Sửa handler `PUT /api/rag-config`** (~router.go:107-244) — thêm field `ExtractorMode` vào request struct và xử lý:

Vào struct `req` thêm:
```go
// ExtractorMode: hot-swap pipeline RAG mode (M1 Phase 4 CFG-04).
// Hợp lệ: "native", "docling", "auto". "" = không đổi.
ExtractorMode string `json:"extractor_mode"`
```

Sau block `BatchSize` validation (~ dòng 145), thêm:

```go
// ─── M1 Phase 4 — Hot-swap extractor mode (CFG-03, CFG-04) ───
if req.ExtractorMode != "" {
    validModes := map[string]bool{"native": true, "docling": true, "auto": true}
    if !validModes[req.ExtractorMode] {
        c.JSON(http.StatusBadRequest, gin.H{
            "error": "extractor_mode không hợp lệ — chỉ chấp nhận: native, docling, auto",
        })
        return
    }
    oldMode := cfg.RAG.Extractor
    if oldMode != req.ExtractorMode {
        cfg.RAG.Extractor = req.ExtractorMode

        // Sync vào Pipeline runtime — setter đã có sẵn từ Plan 02 (CHECK round 1 B2).
        if pipeline != nil {
            pipeline.SetExtractorMode(req.ExtractorMode)
        }

        // Reset circuit state Redis (CFG-04).
        if doclingCircuit != nil {
            if err := doclingCircuit.Reset(c.Request.Context()); err != nil {
                slog.Warn("reset circuit failed", "err", err)
            }
        }

        // Persist vào settings_repo để restart giữ giá trị mới.
        // W4: settingsRepo phải nằm trong scope router (verified ở step 2).
        if settingsRepo != nil {
            _ = settingsRepo.Set(c.Request.Context(), "RAG_EXTRACTOR", req.ExtractorMode, false)
        }

        // Audit (CFG-05).
        if auditRepo != nil {
            actorID, _ := middleware.UserIDFromContext(c)
            actorName := "admin"
            payload := map[string]any{
                "from": oldMode,
                "to":   req.ExtractorMode,
                "by":   actorID,
            }
            payloadJSON, _ := json.Marshal(payload)
            _ = auditRepo.Insert(c.Request.Context(), &model.AuditLogEntry{
                ID:        uuid.New(),
                Timestamp: time.Now().UTC(),
                UserName:  &actorName,
                IsAI:      false,
                Action:    "rag_config_change",
                Payload:   payloadJSON,
            })
        }

        slog.Info("extractor_mode hot-swapped",
            "from", oldMode, "to", req.ExtractorMode)
    }
}
```
  </action>
  <verify>
    <automated>cd backend && go build ./...</automated>
  </verify>
  <done>
- `go build ./...` PASS.
- `grep -c "extractor_mode\|docling_service_status\|docling_version\|docling_circuit_state\|last_fallback_at" backend/internal/router/router.go` ≥ 5.
- PUT body với `{"extractor_mode": "invalid"}` trả 400 (test bằng `httptest` hoặc curl manual).
- PUT body với `{"extractor_mode": "auto"}` trả 200, `cfg.RAG.Extractor` đổi, `pipeline.SetExtractorMode("auto")` được gọi.
- KHÔNG sửa `pipeline.go` trong plan này.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update `cmd/server/main.go` — tạo doclingHealthProbe + verify settingsRepo + pass 4 param vào router.Setup</name>
  <files>backend/cmd/server/main.go</files>
  <action>
1. Sau khi tạo `doclingCircuit` (Plan 02 đã có), thêm:

```go
doclingHealthProbe := extractor.NewDoclingHealthProbe(cfg.RAG.DoclingServiceURL, rdb)
```

2. **VERIFY `settingsRepo` đã DI vào router (CHECK round 1 W4)**:
   - Grep `settingsRepo` trong `backend/cmd/server/main.go` và `backend/internal/router/router.go`.
   - Nếu `settingsRepo` đã tồn tại trong main.go scope NHƯNG chưa pass cho `router.Setup` → thêm vào danh sách param `router.Setup` ở Task 1 (theo lựa chọn (a) W4).
   - Nếu không có ở main.go → kế hoạch (b) W4: bỏ persistence, comment TODO trong handler PUT.
   - Quyết định và document trong SUMMARY.

3. Sửa call `router.Setup(...)` thêm 4 param cuối cùng:

```go
engine := router.Setup(
    cfg,
    jwtManager,
    rdb,
    authHandler,
    ...,
    swappableEmbedder,
    // M1 Phase 4
    doclingCircuit,
    doclingHealthProbe,
    auditRepo,
    pipeline,
)
```

(`pipeline` đã tồn tại ở scope main vì được pass cho `docHandler` Phase 3.)

4. Verify import `extractor` package + `rag` package đã có.

5. **KHÔNG sửa `pipeline.go`** — toàn bộ setter (`SetExtractorMode`, `SetDoclingOCRLangs`, ...) đã được Plan 02 tạo. Plan này chỉ wire ở main + router (CHECK round 1 B2).
  </action>
  <verify>
    <automated>cd backend && go build ./...</automated>
  </verify>
  <done>
- `go build ./...` PASS.
- `grep -n "doclingHealthProbe" backend/cmd/server/main.go` trả ≥ 2 match (tạo + pass router.Setup).
- `pipeline.go` KHÔNG bị edit ở plan này (verify `git diff backend/internal/rag/pipeline.go` trống cho commit Plan 03).
- Quyết định settingsRepo (a hoặc b) ghi rõ trong SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Propagate `DOCLING_OCR_LANGS` qua multipart trong `docling.go`</name>
  <files>backend/internal/rag/extractor/docling.go</files>
  <action>
1. Thêm context key + setter (style giống `WithHubCode`):

```go
// Context key cho ocr_langs (Plan 04-03 CFG-02).
type ctxOCRLangsKey struct{}

// WithOCRLangs gắn ocr_langs vào ctx. Pipeline (Plan 04-02) gọi trước khi vào
// ExtractStructured để DoclingExtractor đính multipart field "ocr_langs".
func WithOCRLangs(ctx context.Context, langs string) context.Context {
    return context.WithValue(ctx, ctxOCRLangsKey{}, langs)
}
```

2. Trong `buildReq` closure (sau `WriteField("doc_type", v)` ~ dòng 134), thêm:

```go
if v, _ := ctx.Value(ctxOCRLangsKey{}).(string); v != "" {
    _ = mw.WriteField("ocr_langs", v)
}
```

3. **KHÔNG sửa `pipeline.go`** ở plan này — Plan 02 đã add field `doclingOCRLangs` + setter `SetDoclingOCRLangs` + 2 dòng `extractor.WithOCRLangs(callCtx, p.doclingOCRLangs)` trong cả branch `auto` và `docling` (CHECK round 1 B2).
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./internal/rag/extractor/...</automated>
  </verify>
  <done>
- `go build ./...` PASS.
- `grep "ocr_langs\|WithOCRLangs" backend/internal/rag/extractor/docling.go` trả ≥ 3 match (type, function, WriteField).
- `pipeline.go` KHÔNG bị edit ở plan này.
- Manual smoke (defer verify-work): tcpdump hoặc Docling sidecar log thấy field `ocr_langs=vie+eng` trong request.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend admin → PUT /api/rag-config | Đã có `JWTAuth + RequireRole("admin")` ở Phase 3. |
| GET /api/rag-config | KHÔNG có auth — public. |

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-04-07 | Information Disclosure | GET /api/rag-config public expose `docling_version`, `docling_circuit_state` | accept | Version + circuit state là metadata vận hành, không phải secret. KHÔNG lộ key/credentials. Nếu cần khoá: thêm `RequireRole("admin")` ở Plan 04-03 task 1 — defer hardening. |
| T-04-08 | Tampering | PUT extractor_mode bị admin compromise → force `native` mãi mãi | mitigate | Audit log `rag_config_change` đã ghi from→to + actor; admin khác có thể PUT lại. |
| T-04-09 | DoS | DoclingHealthProbe trong GET /api/rag-config bị spam | mitigate | Cache 30s qua Redis (Plan 01) → tối đa 1 probe/30s/instance. |
</threat_model>

<verification>
- `go build ./...` PASS.
- Test manual: `curl http://localhost:8180/api/rag-config | jq` thấy đủ 5 field mới.
- Test manual với token admin: `curl -X PUT -H "Auth..." -d '{"extractor_mode":"auto"}' /api/rag-config` → 200, sau đó GET thấy `extractor_mode=auto`.
- `curl -X PUT ... -d '{"extractor_mode":"invalid"}'` → 400 với message tiếng Việt.
- Sau PUT, query Postgres: `SELECT action, payload FROM audit_logs WHERE action='rag_config_change' LIMIT 1` → 1 row có `from`/`to`/`by`.
- `git diff backend/internal/rag/pipeline.go` cho commit Plan 03 trống (verify B2).
</verification>

<success_criteria>
1. CFG-03 satisfied — admin GET observability đầy đủ 5 field; PUT force mode runtime hoạt động qua `pipeline.SetExtractorMode`.
2. CFG-04 satisfied — PUT extractor_mode reset Redis circuit state.
3. CFG-02 satisfied — DOCLING_OCR_LANGS propagate sang sidecar qua multipart.
4. Plan này KHÔNG sửa `pipeline.go` (B2).
5. settingsRepo handling rõ ràng (a hoặc b) document trong SUMMARY (W4).
</success_criteria>

<output>
Tạo `.planning/phases/04-config-hot-swap-circuit-breaker/04-03-SUMMARY.md`:
- Liệt kê 5 field mới GET response.
- Curl example PUT thành công + PUT invalid.
- Note: signature `router.Setup` đã tăng 4 param (doclingCircuit, doclingHealthProbe, auditRepo, pipeline).
- Quyết định settingsRepo (W4): chọn (a) hoặc (b), lý do.
- Verify `pipeline.go` KHÔNG bị plan 03 sửa (B2).
</output>
