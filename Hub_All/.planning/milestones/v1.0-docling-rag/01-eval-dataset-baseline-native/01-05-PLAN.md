---
phase: 01-eval-dataset-baseline-native
plan: 05
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - eval/dataset/queries.jsonl
  - eval/dataset/QUERIES_REVIEW.md
autonomous: false  # checkpoint:human-verify ở task cuối
requirements: [EVAL-01]
status: completed
completed_at: 2026-04-28
completed_commit: e13102d
user_approved: true

must_haves:
  truths:
    - "File `eval/dataset/queries.jsonl` có 10–15 dòng JSON, mỗi dòng valid (`json.loads` không lỗi)."
    - "Mỗi query có đủ 5 field: `id`, `query`, `expected_doc_id`, `expected_section`, `notes`."
    - "Phân bố queries: ≥ 1 query trỏ vào MỖI trong 10 file dataset (đảm bảo coverage — Rủi ro 2 trong CONTEXT.md)."
    - "`expected_doc_id` của mỗi query LÀ filename (không phải UUID) — match `result.category` ở Plan 06 (xem `backend/internal/rag/searcher.go:131`)."
    - "`expected_doc_id` của mỗi query khớp filename trong `eval/dataset/sources/` HOẶC `eval/dataset/scanned/`."
    - "`expected_section` của mỗi query là một heading path hợp lệ trong `headings.json[expected_doc_id]` (PDF gốc đã có heading manual ở Plan 04)."
    - "User đã review + approve queries + heading vàng PDF gốc trước khi commit (checkpoint:human-verify)."
  artifacts:
    - path: "eval/dataset/queries.jsonl"
      provides: "10-15 truy vấn vàng tiếng Việt"
      min_lines: 10
    - path: "eval/dataset/QUERIES_REVIEW.md"
      provides: "Bảng review để user check trước khi approve"
      min_lines: 30
  key_links:
    - from: "eval/dataset/queries.jsonl"
      to: "eval/dataset/headings.json"
      via: "expected_section của query phải match heading path trong headings của expected_doc_id"
      pattern: "\"expected_section\":\\s*\"[^\"]+\""
    - from: "eval/dataset/queries.jsonl"
      to: "baseline.py (Plan 06)"
      via: "baseline.py đọc queries.jsonl + gọi /api/search per query, match `expected_doc_id == result.category` lowercase"
      pattern: "queries\\.jsonl"
---

<objective>
Sinh draft 12 truy vấn vàng tiếng Việt cho 10 file dataset, sau đó user review + approve trước khi commit. LLM (Claude) tạo draft theo CONTEXT.md mục A — user là gatekeeper cuối vì 8/10 file thuộc DMD nội bộ user hiểu rõ.

Purpose: 12 queries = ground truth cho metric retrieval `top_k_hit_rate` + `mrr` ở Plan 06. Phase 5 dùng đúng cùng 12 queries để so sánh.
Output: 1 file JSONL + 1 file review markdown.

REVISION 1 (B2): `expected_doc_id` LÀ **filename** (vd `"DMD_T1-01_DinhVi_TrungTam_v1.docx"`), KHÔNG phải UUID. Lý do: backend Go gán document name vào field `Category` của `model.SearchResult` (`backend/internal/rag/searcher.go:131`) — Plan 06 sẽ match `expected_doc_id.lower() == result["category"].lower()`. Đây là field duy nhất stable để match ngược về document gốc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-eval-dataset-baseline-native/01-CONTEXT.md
@eval/dataset/sources/
@eval/dataset/scanned/
@eval/dataset/headings.json

<interfaces>
<!-- Schema queries.jsonl đã chốt ở CONTEXT.md mục A — REVISION 1: expected_doc_id LÀ filename -->
{
  "id": "q01",
  "query": "<câu hỏi tiếng Việt>",
  "expected_doc_id": "<filename>",            // ⚠ FILENAME, không phải UUID
                                              //   ví dụ "DMD_T1-01_DinhVi_TrungTam_v1.docx"
                                              //   match với SearchResult.Category (searcher.go:131)
  "expected_section": "<heading path>",       // ví dụ "Định vị thương hiệu > Thông điệp cốt lõi"
  "notes": "<context tại sao đây là expected — vài chữ ngắn cho user review>"
}

<!-- Phân bố tối thiểu 1 query/file (10 file → 10 query bắt buộc, 2 query bonus cho file phức tạp) -->
File list (10):
- DMD_T1-01_DinhVi_TrungTam_v1.docx (định vị thương hiệu)
- DMD_T1-02_TuDien_ThuongHieu_v1.docx (từ điển thương hiệu)
- DMD_T1-03_Script_Library_v1.docx (script library)
- DMD_T1-04_FAQ_ThuongHieu_v1.docx (FAQ — Q&A có cấu trúc, dễ làm test case dual: text + Q&A)
- DMD_T3-02_PhanCong_NhanVat_v1.docx (phân công nhân vật)
- DMD_T5-01_ContentStrategy_12TuyenND_v1.docx (content strategy 12 tuyến nội dung)
- DMD_T5-02_Playbook_KenhTruyen_v1.docx (playbook kênh truyền)
- tri_thuc_chinh_tri.pdf (PDF gốc — chính trị)
- DMD_T1-01_scanned.pdf (scanned từ DMD_T1-01)
- DMD_T1-04_scanned.pdf (scanned từ DMD_T1-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Đọc 10 file dataset + sinh draft 12 queries.jsonl</name>
  <files>eval/dataset/queries.jsonl</files>
  <action>
**Bước 1**: Đọc nội dung từng file trong `eval/dataset/sources/` để hiểu chủ đề. Cách đọc:
- DOCX: dùng `python-docx` đọc text + heading; HOẶC dùng `extract_headings.py` output để biết cấu trúc.
- PDF gốc `tri_thuc_chinh_tri.pdf`: dùng `pypdf` để extract text → đọc 5–10 trang đầu hiểu chủ đề.
- Scanned PDF: KHÔNG đọc được (chưa OCR) — dùng heading từ DOCX gốc tương ứng (đã copy ở `headings.json`).

**Bước 2**: Đọc `eval/dataset/headings.json` để biết heading nào là valid `expected_section`. Plan 04 đã đảm bảo PDF gốc có ≥ 1 heading manual nên không có file nào empty.

**Bước 3**: Sinh 12 truy vấn theo phân bố:
- 10 query (q01..q10): 1 query / 1 file (đảm bảo coverage tối thiểu — Rủi ro 2 CONTEXT.md).
- 2 query bonus (q11, q12): trỏ vào 2 file phức tạp nhất:
  - 1 query trỏ vào DOCX có table/structure phức tạp (vd `DMD_T5-01_ContentStrategy_12TuyenND_v1.docx`).
  - 1 query trỏ vào scanned PDF (vd `DMD_T1-04_scanned.pdf`) — tạo edge case Phase 5 đo OCR recovery.

**Bước 4**: Mỗi query phải:
- Là **câu hỏi tiếng Việt tự nhiên** (như user thật hỏi), KHÔNG phải copy nguyên heading.
- `expected_doc_id` = **filename** chính xác (case-sensitive, có extension). KHÔNG phải UUID.
  - REVISION 1 (B2): Plan 06 sẽ lowercase compare với `result.category` từ backend SearchResult — filename lowercase chuẩn.
- `expected_section` = heading path từ `headings.json[expected_doc_id]` (chọn 1 heading lvl 2+ specific).
  - PDF gốc đã có heading manual (Plan 04 fix W4) → query trỏ vào PDF gốc CÓ thể có `expected_section` thật.
- `notes` = 1-2 câu giải thích vì sao expected (giúp user review nhanh).
- `id` định dạng `q01`, `q02`, ..., `q12` (zero-padded 2 digit).

**Bước 5**: Format JSONL — 1 query / 1 dòng, không pretty-print:
```jsonl
{"id":"q01","query":"...","expected_doc_id":"DMD_T1-01_DinhVi_TrungTam_v1.docx","expected_section":"...","notes":"..."}
{"id":"q02","query":"...","expected_doc_id":"DMD_T1-02_TuDien_ThuongHieu_v1.docx","expected_section":"...","notes":"..."}
```

LƯU Ý quan trọng:
- TIẾNG VIỆT có dấu cho `query` + `notes` + `expected_section`. JSON dùng `ensure_ascii=False` khi write.
- Truy vấn nên đa dạng kiểu: definition ("X là gì?"), procedure ("Cách làm Y?"), comparison ("So sánh A và B"), specific fact ("Số tiền X là bao nhiêu?").
- KHÔNG bao gồm `expected_answer_span` (defer v2 — CONTEXT.md mục A).

Quy trình thực hiện:
```python
# Pseudo:
import json
from pathlib import Path
from docx import Document
from pypdf import PdfReader

# 1. Đọc text từ 7 DOCX + 1 PDF gốc
# 2. Đọc heading từ headings.json
# 3. Sinh 12 query dict theo phân bố
# 4. Write JSONL
queries = [...]  # 12 dict
with open('eval/dataset/queries.jsonl', 'w', encoding='utf-8') as f:
    for q in queries:
        f.write(json.dumps(q, ensure_ascii=False) + '\n')
```

Sau khi write, sanity check (REVISION 1 W3 — Python syntax hợp lệ):
```python
import json
queries = [json.loads(line) for line in open("eval/dataset/queries.jsonl") if line.strip()]
assert 10 <= len(queries) <= 15, f"Expected 10-15 queries, got {len(queries)}"
required_fields = {"id", "query", "expected_doc_id", "expected_section", "notes"}
for q in queries:
    missing = required_fields - q.keys()
    assert not missing, f"Query {q.get('id')} missing fields: {missing}"
print(f"OK: {len(queries)} queries valid")
```
  </action>
  <verify>
    <automated>test -f eval/dataset/queries.jsonl && python -c "
import json
queries = [json.loads(line) for line in open('eval/dataset/queries.jsonl', encoding='utf-8') if line.strip()]
assert 10 <= len(queries) <= 15, f'Expected 10-15 queries, got {len(queries)}'
required_fields = {'id', 'query', 'expected_doc_id', 'expected_section', 'notes'}
for q in queries:
    missing = required_fields - q.keys()
    assert not missing, f'Query {q.get(\"id\")} missing fields: {missing}'
docs = {q['expected_doc_id'] for q in queries}
assert len(docs) >= 10, f'Coverage chỉ {len(docs)}/10 doc'
print(f'OK: {len(queries)} queries valid, cover {len(docs)} docs')
"</automated>
  </verify>
  <done>
- `queries.jsonl` có 10–15 dòng (target 12).
- Mỗi dòng valid JSON có đủ 5 field bắt buộc.
- ≥ 10 file unique trong `expected_doc_id` (coverage tối thiểu 1 query/file).
- Tất cả `expected_doc_id` LÀ filename (chứa `.docx` hoặc `.pdf` extension), khớp filename thực tế trong `eval/dataset/sources/` hoặc `eval/dataset/scanned/`.
- KHÔNG có `expected_doc_id` là UUID (regex `^[0-9a-f]{8}-` không match dòng nào).
  </done>
</task>

<task type="auto">
  <name>Task 2: Sinh QUERIES_REVIEW.md để user review</name>
  <files>eval/dataset/QUERIES_REVIEW.md</files>
  <action>
Tạo `eval/dataset/QUERIES_REVIEW.md` (≥ 30 dòng) là **bảng tóm tắt 12 query + heading vàng PDF gốc** cho user duyệt trước khi commit.

REVISION 1 (W4): Gộp review heading vàng `tri_thuc_chinh_tri.pdf` vào cùng checkpoint này (1 lần duyệt cả queries + heading PDF).

Format:
```markdown
# Queries Review — Phase 1 Eval Dataset

**Ngày sinh:** <YYYY-MM-DD>
**Tổng query:** 12 (10 cover-each-file + 2 bonus)
**Cách sinh:** Claude (LLM) đọc 10 file dataset + heading vàng → draft.

User check 5 điều:
1. **Câu hỏi có tự nhiên + đúng tiếng Việt không?** (không phải copy heading machine-like)
2. **`expected_doc_id` có đúng file không?** (kiến thức trong file đó thật sự trả lời được câu hỏi)
3. **`expected_section` có là heading specific nhất không?** (không phải heading lvl 1 quá broad)
4. **Phân bố có cover được edge case không?** (ít nhất 1 query / file, có scanned PDF, có table-heavy doc)
5. **Heading vàng `tri_thuc_chinh_tri.pdf` (PDF gốc) có đúng cấu trúc PDF không?** (LLM-draft, user xác nhận trùng TOC thực tế)

## Bảng query

| ID | Query (cắt 80 ký tự) | Expected Doc | Expected Section | Note |
|----|----------------------|--------------|------------------|------|
| q01 | ... | DMD_T1-01_... | Định vị > Thông điệp | ... |
| q02 | ... | DMD_T1-02_... | ... | ... |
... (12 dòng)

## Coverage check

| File | # query trỏ vào | Note |
|------|-----------------|------|
| DMD_T1-01_DinhVi_TrungTam_v1.docx | 1 | ✓ |
| DMD_T1-02_TuDien_ThuongHieu_v1.docx | 1 | ✓ |
| ... | ... | ... |
| DMD_T1-01_scanned.pdf | 1 | ✓ (edge case OCR) |
| DMD_T1-04_scanned.pdf | 1 | ✓ |
| **Tổng** | 12 | 10 file unique |

## Heading vàng PDF gốc — `tri_thuc_chinh_tri.pdf`

(LLM-draft từ Plan 04 Task 2 — user review)

```
1. <heading lvl1>
2. <heading lvl1> > <heading lvl2>
3. <heading lvl1> > <heading lvl2> > <heading lvl3>
... (5-15 entries)
```

User check: mở PDF gốc, đối chiếu với TOC thật (trang đầu PDF) hoặc heading thấy trên page 1-10. Nếu sai → ghi sửa ở phần Comments dưới.

## Cách user approve

```bash
# 1. Mở review
cat eval/dataset/QUERIES_REVIEW.md

# 2. Mở queries gốc
cat eval/dataset/queries.jsonl

# 3. Mở heading vàng PDF gốc
python -c "import json; print(json.dumps(json.load(open('eval/dataset/headings.json'))['tri_thuc_chinh_tri.pdf'], ensure_ascii=False, indent=2))"

# 4. Nếu OK:
git add eval/dataset/queries.jsonl eval/dataset/QUERIES_REVIEW.md eval/scripts/extract_headings.py eval/dataset/headings.json

# 5. Nếu cần sửa: edit trực tiếp queries.jsonl HOẶC MANUAL_PDF_HEADINGS trong extract_headings.py, sau đó:
python -c "import json; [json.loads(l) for l in open('eval/dataset/queries.jsonl', encoding='utf-8')]"
python eval/scripts/extract_headings.py
# (validate JSON + regenerate headings.json)
```

## Disposition

- [ ] User reviewed (queries + heading vàng PDF gốc)
- [ ] User approved
- [ ] User requested changes (xem comment phía dưới)

### Comments user

(để trống — user điền nếu cần sửa)
```

Sinh nội dung bảng tự động từ `queries.jsonl` đã write ở Task 1 + heading vàng PDF gốc từ `headings.json`.
  </action>
  <verify>
    <automated>test -f eval/dataset/QUERIES_REVIEW.md && [ "$(wc -l < eval/dataset/QUERIES_REVIEW.md)" -ge 30 ] && grep -q "Coverage check" eval/dataset/QUERIES_REVIEW.md && grep -q "Disposition" eval/dataset/QUERIES_REVIEW.md && grep -q "tri_thuc_chinh_tri" eval/dataset/QUERIES_REVIEW.md</automated>
  </verify>
  <done>
- `QUERIES_REVIEW.md` ≥ 30 dòng.
- Có bảng 12 query (1 dòng / query).
- Có bảng coverage 10 file.
- Có section heading vàng PDF gốc `tri_thuc_chinh_tri.pdf` để user review.
- Có section "Cách user approve" + "Disposition".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — User review + approve queries + heading vàng PDF gốc</name>
  <what-built>
- 12 truy vấn vàng tiếng Việt sinh bởi LLM (Claude) tại `eval/dataset/queries.jsonl`.
- Heading vàng cho `tri_thuc_chinh_tri.pdf` (PDF gốc) — LLM draft trong `MANUAL_PDF_HEADINGS` của `extract_headings.py`, output ở `eval/dataset/headings.json`.
- Bảng review tóm tắt tại `eval/dataset/QUERIES_REVIEW.md`.
- Mỗi query có doc + section + note để user verify.
  </what-built>
  <how-to-verify>
1. Mở `eval/dataset/QUERIES_REVIEW.md` đọc bảng 12 query + heading vàng PDF gốc.
2. Đối chiếu vài query với file gốc trong `eval/dataset/sources/`:
   - Câu hỏi có "đúng tinh thần" tài liệu không?
   - `expected_doc_id` đúng file chứa kiến thức trả lời?
   - `expected_section` đúng heading specific?
3. Check coverage: 10 file đều có ≥ 1 query trỏ vào (xem bảng "Coverage check").
4. Đặc biệt review:
   - q11/q12 (bonus) có trỏ vào edge case không (table-heavy hoặc scanned)?
   - Query trên scanned PDF có biết là expected_section copy từ DOCX gốc không?
5. **Review heading vàng PDF gốc `tri_thuc_chinh_tri.pdf`** (REVISION 1 W4):
   - Mở PDF gốc, đối chiếu TOC thật / heading trên page 1-10.
   - Nếu LLM draft sai → ghi sửa ở Comments của QUERIES_REVIEW.md.
6. Nếu OK: trả lời "approved" hoặc "OK".
7. Nếu cần sửa: ghi rõ query/heading nào sai, sửa ra sao (vd "q03 nên hỏi cụ thể hơn về X", hoặc "PDF heading thiếu Phần II"). Sau khi user feedback, edit `queries.jsonl` HOẶC `MANUAL_PDF_HEADINGS` trong `extract_headings.py` rồi chạy lại script.
  </how-to-verify>
  <resume-signal>Reply "approved" để commit, hoặc liệt kê các thay đổi cần làm</resume-signal>
</task>

</tasks>

<verification>
1. `wc -l eval/dataset/queries.jsonl` ∈ [10, 15].
2. Mỗi dòng valid JSON với 5 field bắt buộc (verify bằng Python script W3 chuẩn).
3. Coverage ≥ 10 file unique.
4. `expected_doc_id` luôn LÀ filename (kết thúc bằng `.docx` hoặc `.pdf`).
5. User đã reply "approved" trên checkpoint (cả queries + heading vàng PDF gốc).
</verification>

<success_criteria>
- 10-15 queries = ground truth cố định cho cả Phase 1 baseline và Phase 5 compare.
- User là gatekeeper — câu hỏi đúng "tinh thần" tài liệu DMD nội bộ.
- Phân bố cover edge case (scanned + table-heavy) → Phase 5 đo được Docling đóng gap đúng chỗ.
- `expected_doc_id` = filename → Plan 06 match `result.category` lowercase robust.
- Heading vàng PDF gốc đã được user review (W4 fix).
</success_criteria>

<output>
Sau khi user approve, tạo `.planning/phases/01-eval-dataset-baseline-native/01-05-SUMMARY.md` ghi:
- Output 12 query (full content).
- User feedback nếu có (changes requested + applied).
- Coverage table cuối cùng.
- Confirm `expected_doc_id` luôn là filename, KHÔNG phải UUID.
- Confirm heading vàng PDF gốc đã user-approved.
</output>
