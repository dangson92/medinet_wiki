---
phase: 01-eval-dataset-baseline-native
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - eval/baseline.py
  - eval/baseline_native.json
  - eval/dataset/DATASET.md
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "Pre-flight check chạy đầu tiên: backend Go up (`GET /api/health`), ChromaDB up (`GET /api/v2/heartbeat`), hub `eval` đã seed (DB query) — fail loud nếu thiếu bất kỳ điều kiện nào."
    - "Lệnh `python eval/baseline.py` đăng nhập admin → upload toàn bộ 10 file (8 sources + 2 scanned) → poll status → search 12 queries → ghi snapshot."
    - "Output `eval/baseline_native.json` valid JSON, đúng schema CONTEXT.md mục cuối (REVISION 1: KHÔNG có headings_recalled/headings_missed — defer Phase 5 theo REQ EVAL-02)."
    - "Snapshot có `embedder_provider`, `embedder_model`, `embedder_dim` lấy từ `/api/rag-config` + `/api/rag-config/collections` (admin) — fail loud (`SystemExit`) nếu provider rỗng hoặc dim=0."
    - "Snapshot có `documents[]` với 10 entries: 8 file `status='completed'` + 2 scanned PDF có `status='error'` với `error_message` chứa `no text extracted` (Phase 1 SC4)."
    - "Snapshot có `retrieval` với `top_1_hit_rate`, `top_3_hit_rate`, `top_5_hit_rate`, `mrr` (số thực 0-1) + `per_query` chi tiết 12 entry."
    - "Match retrieval: `expected_doc_id` (filename) lowercase compare với `result.category` lowercase — đây là field document name từ `searcher.go:131`. Top-K hit nếu BẤT KỲ result nào trong top-K có `category == expected_doc_id` (case-insensitive)."
    - "Script handle JWT TTL 15 phút — khi 401, tự refresh qua `/api/auth/refresh`, retry request không lỗi."
    - "`eval/dataset/DATASET.md` mô tả full dataset: license, cách tái tạo, structure, version."
    - "Phase 1 SC4 (ROADMAP) thỏa mãn: 2 scanned PDF có error_message chứa `no text extracted` từ `pdf.go:52`."
  artifacts:
    - path: "eval/baseline.py"
      provides: "Script chính: pre-flight → login → upload all → poll → search → snapshot"
      min_lines: 280
    - path: "eval/baseline_native.json"
      provides: "Snapshot baseline native cho M1 quality gate"
      contains: "extractor_mode"
    - path: "eval/dataset/DATASET.md"
      provides: "Mô tả dataset cho reproducibility"
      min_lines: 50
  key_links:
    - from: "eval/baseline.py"
      to: "POST /api/auth/login + POST /api/documents/upload + GET /api/documents/{id}/status + POST /api/search + GET /api/rag-config + GET /api/rag-config/collections"
      via: "httpx async client với JWT bearer + auto-refresh"
      pattern: "Authorization.*Bearer"
    - from: "eval/baseline.py"
      to: "result.category (filename) match expected_doc_id (lowercase)"
      via: "compute_retrieval_metrics dùng `result['category'].lower() == expected_doc_id.lower()`"
      pattern: "category.*lower"
    - from: "eval/baseline_native.json"
      to: "Phase 5 compare script"
      via: "Phase 5 đọc cả native + docling snapshot, diff metric"
      pattern: "top_3_hit_rate"
---

<objective>
Script chính của Phase 1 — đo baseline retrieval với extractor Go native. Output snapshot `baseline_native.json` là **deliverable cuối cùng** của phase, là input cho Phase 5 quality gate (M1 pass khi top-3 tăng ≥ 15pp HOẶC đạt ≥ 75%).

Purpose: Có số liệu cụ thể trước-sau để chứng minh Docling thật sự cải thiện retrieval (không phải hype). Đồng thời log explicit `no text extracted` cho 2 scanned PDF — bằng chứng gap cho Phase 2 đóng.
Output: 1 script Python + 1 snapshot JSON + 1 file mô tả dataset.

REVISION 1 fixes:
- B1: KHÔNG có `headings_recalled` / `headings_missed` trong schema — defer Phase 5 (REQ EVAL-02). Chỉ giữ `headings_gold_count`.
- B2: Match `expected_doc_id` (filename) với `result.category` (= document name, từ `searcher.go:131`), lowercase compare. KHÔNG dùng `result.id` (= chunk_id).
- B3: Task 0 Pre-flight check: backend health + ChromaDB heartbeat + DB query xác nhận hub `eval` đã seed.
- W2: Verify task 3 assert `error_message` chứa `"no text extracted"` (có fallback bypass nếu API không trả error_message — log warning + skip assert).
- W5: `get_embedder_config` raise `SystemExit` nếu provider rỗng hoặc dim=0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-eval-dataset-baseline-native/01-CONTEXT.md
@.planning/phases/01-eval-dataset-baseline-native/01-PATTERNS.md
@backend/internal/handler/auth_handler.go
@backend/internal/handler/document_handler.go
@backend/internal/handler/search_handler.go
@backend/internal/router/router.go
@backend/internal/rag/searcher.go

<interfaces>
<!-- API contracts (PATTERNS.md mục 1+8) -->

POST /api/auth/login (public, rate-limited)
  Request:  {"email": "admin@medinet.vn", "password": "Admin@123"}
  Response: {"success": true, "data": {"access_token": "...", "refresh_token": "...", "expires_at": <unix>, "user": {...}}}

POST /api/auth/refresh (public)
  Request:  {"refresh_token": "..."}
  Response: same shape as login

POST /api/documents/upload (admin role required)
  multipart: file=<binary>, hub_id=<uuid>
  Response: 202 {"success": true, "data": {"id":"...","status":"pending","progress":0,...}}

GET /api/documents/{id}/status (auth required)
  Response: {"status": "pending|processing|completed|error", "progress": 0..100, "error": "...?", "error_message": "...?"}
  ⚠ Field error_message chứa text từ pdf.go:52 (`no text extracted`).
  ⚠ Backend có thể trả `error` HOẶC `error_message` — script check cả hai.

POST /api/search (auth required)
  Request:  {"query": "...", "hub_ids": ["<eval_uuid>"], "top_k": 5, "min_score": 0.0}
  Response: {"success":true, "data":{"results":[{"id","hub_id","title","snippet","score","category","source"}], ...}}
  ⚠ "id" = chunk_id (vector ID trong ChromaDB), KHÔNG phải document_id.
  ⚠ "category" = document name (= filename gốc), từ searcher.go:131 — match với expected_doc_id.

GET /api/rag-config (PUBLIC, no auth)
  Response: {"chunker","chunk_size","chunk_overlap","embedding_model","embedding_provider","batch_size","llm_provider",...}
  ⚠ KHÔNG có embedding_dim trong response.

GET /api/rag-config/collections (admin role required)
  Response: {"current_dimension": <int>, "collections": [...]}
  ⚠ Đây là endpoint duy nhất trả embedding_dim.

GET /api/health (public, không yêu cầu JWT)
  Response: 200 OK với JSON status (hoặc plain text "OK")

GET /api/v2/heartbeat (ChromaDB, public)
  Response: 200 OK với {"nanosecond heartbeat": <int>}

<!-- File path local upload (document_service.go:119-126) -->
backend/uploads/<hub_code>/<doc_uuid>/<filename>

<!-- Default admin credential (seed.sql) -->
admin@medinet.vn / Admin@123

<!-- Token TTL -->
JWT access: 15 phút, refresh: 7 ngày — script phải refresh khi access expired.

<!-- Allowed file types (document_service.go:24-35) -->
pdf, docx, txt, md, xlsx, pptx, jpg, png, csv, html
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight check — backend + ChromaDB + DB hub seed (REVISION 1 B3)</name>
  <files>eval/baseline.py</files>
  <action>
**Pre-flight là phần đầu của `eval/baseline.py`** — chạy TRƯỚC khi login + upload. Nếu fail bất kỳ check nào → exit code != 0 với message rõ ràng.

Implementation (sẽ embed vào baseline.py ở Task 1, nhưng định nghĩa rõ ở đây để tách trách nhiệm):

```python
async def preflight_check() -> None:
    """3 checks bắt buộc trước khi chạy baseline.

    1. Backend Go up: GET /api/health (HTTP 200).
    2. ChromaDB up: GET /api/v2/heartbeat (HTTP 200).
    3. Hub `eval` đã seed: SELECT id FROM hubs WHERE code='eval'.

    Fail loud → SystemExit với hint cụ thể.
    """
    # Check 1: Backend health
    async with httpx.AsyncClient(timeout=10) as client:
        try:
            r = await client.get(f"{BACKEND_URL}/api/health")
            if r.status_code != 200:
                raise SystemExit(
                    f"Pre-flight FAIL: Backend Go health check {BACKEND_URL}/api/health "
                    f"trả {r.status_code}. Khởi động backend trước:\n"
                    f"  cd backend && go run ./cmd/server"
                )
            logger.info("✓ Pre-flight: Backend Go up @ %s", BACKEND_URL)
        except httpx.RequestError as e:
            raise SystemExit(
                f"Pre-flight FAIL: Không kết nối được backend ({BACKEND_URL}/api/health): {e}\n"
                f"Khởi động backend trước: cd backend && go run ./cmd/server"
            ) from e

    # Check 2: ChromaDB heartbeat
    async with httpx.AsyncClient(timeout=10) as client:
        try:
            r = await client.get(f"{CHROMA_URL}/api/v2/heartbeat")
            if r.status_code != 200:
                raise SystemExit(
                    f"Pre-flight FAIL: ChromaDB heartbeat {CHROMA_URL}/api/v2/heartbeat "
                    f"trả {r.status_code}. Khởi động ChromaDB trước (docker-compose up chroma)."
                )
            logger.info("✓ Pre-flight: ChromaDB up @ %s", CHROMA_URL)
        except httpx.RequestError as e:
            raise SystemExit(
                f"Pre-flight FAIL: Không kết nối được ChromaDB ({CHROMA_URL}/api/v2/heartbeat): {e}\n"
                f"Khởi động ChromaDB: docker-compose up -d chroma"
            ) from e

    # Check 3: Hub eval đã seed (DB query qua psycopg)
    dsn = f"host={DB_HOST} port={DB_PORT} dbname={DB_NAME} user={DB_USER} password={DB_PASSWORD}"
    try:
        with psycopg.connect(dsn, connect_timeout=5) as conn, conn.cursor() as cur:
            cur.execute("SELECT id FROM hubs WHERE code = %s", (EVAL_HUB_CODE,))
            row = cur.fetchone()
            if not row:
                raise SystemExit(
                    f"Pre-flight FAIL: Hub code='{EVAL_HUB_CODE}' chưa seed trong DB.\n"
                    f"Chạy seed trước:\n"
                    f"  psql -h {DB_HOST} -U {DB_USER} -d {DB_NAME} -f eval/scripts/seed_hub.sql"
                )
            logger.info("✓ Pre-flight: hub '%s' đã seed (id=%s)", EVAL_HUB_CODE, row[0])
    except psycopg.OperationalError as e:
        raise SystemExit(
            f"Pre-flight FAIL: Không kết nối được Postgres "
            f"({DB_HOST}:{DB_PORT}/{DB_NAME}): {e}\n"
            f"Kiểm tra Postgres đang chạy + credential trong eval/.env."
        ) from e

    logger.info("✓ Pre-flight: tất cả 3 check pass")
```

Gọi `await preflight_check()` ở đầu `run_baseline()` — TRƯỚC `api.login()`. Nếu pre-flight raise SystemExit → script exit không khởi tạo APIClient (giảm side-effect).

LƯU Ý:
- Endpoint `/api/health` có thể chưa tồn tại trong backend Go — fallback dùng `/api/rag-config` (public endpoint, KHÔNG cần JWT, đã verify trong PATTERNS.md mục 1) làm proxy health check. Implement: thử `/api/health` trước, nếu trả 404 thì thử `/api/rag-config`.
- `/api/v2/heartbeat` là path chuẩn của ChromaDB v2 (không cần token).
- DB query dùng connect_timeout=5 để fail nhanh nếu Postgres down.
  </action>
  <verify>
    <automated>cd eval && grep -q "preflight_check" baseline.py && grep -q "GET /api/health\\|api/health" baseline.py && grep -q "api/v2/heartbeat" baseline.py && grep -q "WHERE code = %s" baseline.py && grep -q "Pre-flight FAIL" baseline.py</automated>
  </verify>
  <done>
- `baseline.py` có hàm `preflight_check()` với 3 check.
- Mỗi check fail → `SystemExit` với hint cụ thể (lệnh khởi động backend / ChromaDB / chạy seed_hub.sql).
- Pre-flight gọi TRƯỚC `api.login()`.
  </done>
</task>

<task type="auto">
  <name>Task 1: Viết eval/baseline.py — script chính (orchestration)</name>
  <files>eval/baseline.py</files>
  <action>
Tạo `eval/baseline.py` (≥ 280 dòng), structure theo class `BaselineRunner`. REVISION 1 fixes embedded:
- Pre-flight check (Task 0 logic) là method đầu tiên trong run_baseline.
- `compute_retrieval_metrics` match `expected_doc_id.lower()` với `result["category"].lower()` (B2).
- `compute_heading_metrics` chỉ ghi `headings_gold_count`, KHÔNG ghi `headings_recalled`/`headings_missed` (B1).
- `get_embedder_config` raise SystemExit nếu provider rỗng / dim=0 (W5).

```python
"""Đo baseline retrieval với extractor Go native — Phase 1 Eval Dataset & Baseline Native.

Pipeline:
  0. Pre-flight: backend health + ChromaDB heartbeat + DB hub seed (B3).
  1. Login admin → access_token (handle 15-min TTL).
  2. Lấy embedder config: GET /api/rag-config + GET /api/rag-config/collections.
     Fail loud nếu provider rỗng / dim=0 (W5).
  3. Lấy eval_hub_id qua API list hub.
  4. Upload tất cả 10 file (8 sources + 2 scanned) → poll status đến completed/error.
  5. Search 12 queries qua /api/search → đo top-K hit rate + MRR.
     Match expected_doc_id (filename) với result.category lowercase (B2).
  6. Ghi snapshot eval/baseline_native.json (KHÔNG có headings_recalled/missed — B1).

Chạy: python eval/baseline.py [--top-k 5] [--upload-timeout 300] [--poll-interval 2]

Tiền điều kiện (Task 0 sẽ verify):
  - Backend Go đang chạy ở BACKEND_URL.
  - ChromaDB đang chạy ở CHROMA_URL.
  - Hub `eval` đã seed (chạy seed_hub.sql).
  - State sạch (chạy cleanup.py trước khi chạy script này — khuyến nghị, không verify).
"""
from __future__ import annotations

import argparse
import asyncio
import json
import logging
import os
import sys
import time
from datetime import datetime
from pathlib import Path

import httpx
import psycopg
from dotenv import load_dotenv

logging.basicConfig(level=logging.INFO, format="%(asctime)s [%(levelname)s] %(message)s")
logger = logging.getLogger(__name__)

REPO_ROOT = Path(__file__).resolve().parents[1]
load_dotenv(REPO_ROOT / "eval" / ".env")

BACKEND_URL = os.getenv("BACKEND_URL", "http://localhost:8180")
ADMIN_EMAIL = os.getenv("ADMIN_EMAIL", "admin@medinet.vn")
ADMIN_PASSWORD = os.getenv("ADMIN_PASSWORD", "Admin@123")
EVAL_HUB_CODE = os.getenv("EVAL_HUB_CODE", "eval")
CHROMA_URL = os.getenv("CHROMA_URL", "http://localhost:8000")
DB_HOST = os.getenv("DB_HOST", "localhost")
DB_PORT = int(os.getenv("DB_PORT", "5432"))
DB_NAME = os.getenv("DB_NAME", "medinet_central")
DB_USER = os.getenv("DB_USER", "medinet")
DB_PASSWORD = os.getenv("DB_PASSWORD", "")
UPLOAD_TIMEOUT_SEC = int(os.getenv("EVAL_UPLOAD_TIMEOUT_SEC", "300"))
POLL_INTERVAL_SEC = int(os.getenv("EVAL_POLL_INTERVAL_SEC", "2"))

SOURCES_DIR = REPO_ROOT / "eval" / "dataset" / "sources"
SCANNED_DIR = REPO_ROOT / "eval" / "dataset" / "scanned"
QUERIES_PATH = REPO_ROOT / "eval" / "dataset" / "queries.jsonl"
HEADINGS_PATH = REPO_ROOT / "eval" / "dataset" / "headings.json"
OUTPUT_PATH = REPO_ROOT / "eval" / "baseline_native.json"


# ─── Pre-flight (REVISION 1 B3) ───────────────────────────────────────────
async def preflight_check() -> None:
    """3 checks bắt buộc trước khi chạy baseline.

    1. Backend Go up: GET /api/health (fallback /api/rag-config).
    2. ChromaDB up: GET /api/v2/heartbeat.
    3. Hub `eval` đã seed: DB query SELECT id FROM hubs WHERE code='eval'.
    """
    async with httpx.AsyncClient(timeout=10) as client:
        try:
            r = await client.get(f"{BACKEND_URL}/api/health")
            if r.status_code == 404:
                # Fallback: /api/rag-config (public, KHÔNG cần JWT)
                r = await client.get(f"{BACKEND_URL}/api/rag-config")
            if r.status_code != 200:
                raise SystemExit(
                    f"Pre-flight FAIL: Backend Go health check {BACKEND_URL} "
                    f"trả {r.status_code}. Khởi động backend trước:\n"
                    f"  cd backend && go run ./cmd/server"
                )
            logger.info("✓ Pre-flight: Backend Go up @ %s", BACKEND_URL)
        except httpx.RequestError as e:
            raise SystemExit(
                f"Pre-flight FAIL: Không kết nối được backend ({BACKEND_URL}): {e}\n"
                f"Khởi động backend: cd backend && go run ./cmd/server"
            ) from e

        try:
            r = await client.get(f"{CHROMA_URL}/api/v2/heartbeat")
            if r.status_code != 200:
                raise SystemExit(
                    f"Pre-flight FAIL: ChromaDB heartbeat {CHROMA_URL}/api/v2/heartbeat "
                    f"trả {r.status_code}. Khởi động: docker-compose up -d chroma"
                )
            logger.info("✓ Pre-flight: ChromaDB up @ %s", CHROMA_URL)
        except httpx.RequestError as e:
            raise SystemExit(
                f"Pre-flight FAIL: Không kết nối được ChromaDB ({CHROMA_URL}): {e}\n"
                f"Khởi động: docker-compose up -d chroma"
            ) from e

    dsn = f"host={DB_HOST} port={DB_PORT} dbname={DB_NAME} user={DB_USER} password={DB_PASSWORD}"
    try:
        with psycopg.connect(dsn, connect_timeout=5) as conn, conn.cursor() as cur:
            cur.execute("SELECT id FROM hubs WHERE code = %s", (EVAL_HUB_CODE,))
            row = cur.fetchone()
            if not row:
                raise SystemExit(
                    f"Pre-flight FAIL: Hub code='{EVAL_HUB_CODE}' chưa seed.\n"
                    f"Chạy seed trước:\n"
                    f"  psql -h {DB_HOST} -U {DB_USER} -d {DB_NAME} -f eval/scripts/seed_hub.sql"
                )
            logger.info("✓ Pre-flight: hub '%s' đã seed (id=%s)", EVAL_HUB_CODE, row[0])
    except psycopg.OperationalError as e:
        raise SystemExit(
            f"Pre-flight FAIL: Không kết nối được Postgres ({DB_HOST}:{DB_PORT}/{DB_NAME}): {e}"
        ) from e

    logger.info("✓ Pre-flight: tất cả 3 check pass")


# ─── HTTP client với JWT auto-refresh ─────────────────────────────────────
class APIClient:
    """HTTP client gọi backend Go, auto-refresh JWT khi 401."""

    def __init__(self, base_url: str, email: str, password: str) -> None:
        self.base_url = base_url
        self.email = email
        self.password = password
        self.access_token: str | None = None
        self.refresh_token: str | None = None
        self._client = httpx.AsyncClient(timeout=httpx.Timeout(120.0))

    async def aclose(self) -> None:
        await self._client.aclose()

    async def login(self) -> None:
        r = await self._client.post(
            f"{self.base_url}/api/auth/login",
            json={"email": self.email, "password": self.password},
        )
        r.raise_for_status()
        data = r.json()["data"]
        self.access_token = data["access_token"]
        self.refresh_token = data["refresh_token"]
        logger.info("Login OK: %s (token TTL ~15m)", self.email)

    async def refresh(self) -> None:
        if not self.refresh_token:
            await self.login()
            return
        try:
            r = await self._client.post(
                f"{self.base_url}/api/auth/refresh",
                json={"refresh_token": self.refresh_token},
            )
            r.raise_for_status()
            data = r.json()["data"]
            self.access_token = data["access_token"]
            self.refresh_token = data["refresh_token"]
            logger.info("Refresh token OK")
        except httpx.HTTPStatusError:
            logger.warning("Refresh fail → re-login")
            await self.login()

    def _headers(self) -> dict[str, str]:
        return {"Authorization": f"Bearer {self.access_token}"} if self.access_token else {}

    async def _request_with_retry(self, method: str, url: str, **kw) -> httpx.Response:
        kw.setdefault("headers", {}).update(self._headers())
        r = await self._client.request(method, url, **kw)
        if r.status_code == 401:
            await self.refresh()
            kw["headers"].update(self._headers())
            r = await self._client.request(method, url, **kw)
        return r

    async def get(self, path: str, **kw) -> dict:
        r = await self._request_with_retry("GET", f"{self.base_url}{path}", **kw)
        r.raise_for_status()
        return r.json()

    async def post_json(self, path: str, body: dict) -> dict:
        r = await self._request_with_retry("POST", f"{self.base_url}{path}", json=body)
        r.raise_for_status()
        return r.json()

    async def post_multipart(self, path: str, file_path: Path, fields: dict[str, str]) -> dict:
        with file_path.open("rb") as f:
            files = {"file": (file_path.name, f.read(), "application/octet-stream")}
            r = await self._request_with_retry(
                "POST", f"{self.base_url}{path}", files=files, data=fields,
            )
        r.raise_for_status()
        return r.json()


# ─── Helpers ──────────────────────────────────────────────────────────────
async def get_eval_hub_id(api: APIClient) -> str:
    """Lấy hub_id của hub code='eval' qua API list hubs."""
    resp = await api.get("/api/hubs")
    hubs = resp["data"].get("hubs", resp["data"]) if isinstance(resp["data"], dict) else resp["data"]
    for h in hubs:
        if h.get("code") == EVAL_HUB_CODE:
            return h["id"]
    raise SystemExit(
        f"Hub code='{EVAL_HUB_CODE}' chưa tồn tại qua API. Chạy seed_hub.sql trước:\n"
        f"  psql -f eval/scripts/seed_hub.sql"
    )


async def get_embedder_config(api: APIClient) -> dict:
    """GET /api/rag-config (public) + /api/rag-config/collections (admin) merge.

    REVISION 1 (W5): Fail loud nếu provider rỗng hoặc dim=0.
    """
    rag = await api.get("/api/rag-config")
    if "data" in rag:
        rag = rag["data"]
    collections = await api.get("/api/rag-config/collections")
    if "data" in collections:
        collections = collections["data"]
    config = {
        "embedder_provider": rag.get("embedding_provider", ""),
        "embedder_model": rag.get("embedding_model", ""),
        "embedder_dim": collections.get("current_dimension", 0),
        "chunker": rag.get("chunker", "unknown"),
        "chunk_size": rag.get("chunk_size"),
        "chunk_overlap": rag.get("chunk_overlap"),
    }
    # W5: fail loud
    if not config.get("embedder_provider") or config.get("embedder_dim", 0) == 0:
        raise SystemExit(
            f"Embedding config invalid: provider={config.get('embedder_provider')!r}, "
            f"dim={config.get('embedder_dim')}. "
            f"Check backend /.env và GET {BACKEND_URL}/api/rag-config/collections — "
            f"collection medinet_eval phải đã được create với dim > 0."
        )
    return config


async def upload_and_wait(
    api: APIClient, file_path: Path, hub_id: str, timeout_sec: int, poll_sec: int,
) -> dict:
    """Upload 1 file, poll status đến completed|error. Trả dict status cuối."""
    logger.info("Upload %s (size %.1f KB)", file_path.name, file_path.stat().st_size / 1024)
    upload_resp = await api.post_multipart(
        "/api/documents/upload",
        file_path,
        {"hub_id": hub_id},
    )
    doc = upload_resp["data"]
    doc_id = doc["id"]
    logger.info("  → doc_id=%s status=%s", doc_id, doc["status"])

    deadline = time.time() + timeout_sec
    while time.time() < deadline:
        await asyncio.sleep(poll_sec)
        status_resp = await api.get(f"/api/documents/{doc_id}/status")
        s = status_resp["data"] if "data" in status_resp else status_resp
        st = s.get("status", "")
        progress = s.get("progress", 0)
        logger.info("  poll %s: %s %d%%", file_path.name, st, progress)
        if st in ("completed", "error"):
            return {"id": doc_id, "filename": file_path.name, **s}
    return {"id": doc_id, "filename": file_path.name, "status": "timeout", "progress": 0}


async def search_query(api: APIClient, query: str, hub_id: str, top_k: int) -> list[dict]:
    """Gọi /api/search trả về list results (mỗi result có category = document name)."""
    resp = await api.post_json(
        "/api/search",
        {"query": query, "hub_ids": [hub_id], "top_k": top_k, "min_score": 0.0},
    )
    return resp["data"]["results"]


# ─── Metrics — REVISION 1 B2 ──────────────────────────────────────────────
def compute_retrieval_metrics(
    queries: list[dict], per_query_results: list[dict],
) -> dict:
    """Tính top-1/3/5 hit rate + MRR.

    REVISION 1 (B2): A hit = result.category (= document name = filename gốc, từ
    backend/internal/rag/searcher.go:131) lowercase compare với expected_doc_id lowercase.
    KHÔNG dùng result.id (= chunk_id, không phải document_id).

    Top-K hit rate: hit nếu BẤT KỲ result nào trong top-K có category == expected_doc_id (case-insensitive).
    """
    n = len(queries)
    hits_at_1 = hits_at_3 = hits_at_5 = 0
    rr_sum = 0.0
    per_query: list[dict] = []

    for q, r in zip(queries, per_query_results, strict=True):
        expected_filename = q["expected_doc_id"].lower()
        results = r["results"]

        rank = None
        for idx, res in enumerate(results, 1):
            res_category = (res.get("category") or "").lower()
            if res_category == expected_filename:
                rank = idx
                break

        if rank == 1: hits_at_1 += 1
        if rank and rank <= 3: hits_at_3 += 1
        if rank and rank <= 5: hits_at_5 += 1
        if rank: rr_sum += 1.0 / rank

        per_query.append({
            "id": q["id"],
            "query": q["query"],
            "expected_doc_id": q["expected_doc_id"],
            "expected_section": q["expected_section"],
            "actual_top_5": [
                {
                    "chunk_id": res.get("id"),  # ⚠ rename rõ: id = chunk_id
                    "category": res.get("category"),  # filename gốc — match field
                    "title": res.get("title"),
                    "score": res.get("score"),
                }
                for res in results[:5]
            ],
            "rank": rank,
        })

    return {
        "top_1_hit_rate": hits_at_1 / n if n else 0.0,
        "top_3_hit_rate": hits_at_3 / n if n else 0.0,
        "top_5_hit_rate": hits_at_5 / n if n else 0.0,
        "mrr": rr_sum / n if n else 0.0,
        "per_query": per_query,
    }


# ─── Heading metrics — REVISION 1 B1: defer Phase 5 ───────────────────────
def annotate_heading_gold_count(
    docs: list[dict], headings_gold: dict[str, list[str]],
) -> list[dict]:
    """Chỉ ghi `headings_gold_count` per doc.

    REVISION 1 (B1): KHÔNG ghi `headings_recalled` / `headings_missed` ở Phase 1
    — defer Phase 5 theo REQ EVAL-02. Phase 5 sẽ đọc lại headings.json + chunks
    metadata để compute recall thật.

    heading_recall measured in Phase 5 (REQ EVAL-02)
    """
    for d in docs:
        gold = headings_gold.get(d.get("filename", ""), [])
        d["headings_gold_count"] = len(gold)
    return docs


# ─── Main orchestration ───────────────────────────────────────────────────
async def run_baseline(args: argparse.Namespace) -> int:
    # REVISION 1 B3: pre-flight TRƯỚC khi tạo APIClient
    await preflight_check()

    api = APIClient(BACKEND_URL, ADMIN_EMAIL, ADMIN_PASSWORD)
    try:
        await api.login()
        embedder_cfg = await get_embedder_config(api)  # W5 fail loud bên trong
        logger.info("Embedder: %s/%s dim=%s",
                    embedder_cfg["embedder_provider"],
                    embedder_cfg["embedder_model"],
                    embedder_cfg["embedder_dim"])

        eval_hub_id = await get_eval_hub_id(api)
        logger.info("eval_hub_id=%s", eval_hub_id)

        # Build danh sách file: 8 sources + 2 scanned
        all_files = sorted(SOURCES_DIR.glob("*"))
        all_files += sorted(SCANNED_DIR.glob("*.pdf"))
        if len(all_files) != 10:
            logger.warning("Mong đợi 10 file, có %d", len(all_files))

        # Upload + poll sequential (tránh worker pool quá tải)
        docs: list[dict] = []
        for fp in all_files:
            try:
                d = await upload_and_wait(api, fp, eval_hub_id, args.upload_timeout, args.poll_interval)
            except httpx.HTTPStatusError as e:
                d = {"filename": fp.name, "status": "error",
                     "error_message": f"HTTP {e.response.status_code}: {e.response.text[:200]}"}
            docs.append(d)

        # Load queries + headings
        queries = [json.loads(l) for l in QUERIES_PATH.read_text(encoding="utf-8").splitlines() if l.strip()]
        headings_gold = json.loads(HEADINGS_PATH.read_text(encoding="utf-8"))

        # Search 12 queries — match expected_doc_id LOWERCASE với result.category LOWERCASE (B2)
        per_q: list[dict] = []
        for q in queries:
            results = await search_query(api, q["query"], eval_hub_id, args.top_k)
            per_q.append({"results": results})

        retrieval = compute_retrieval_metrics(queries, per_q)
        docs = annotate_heading_gold_count(docs, headings_gold)

        snapshot = {
            "run_id": datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%SZ"),
            "extractor_mode": "native",
            **embedder_cfg,
            "eval_hub_id": eval_hub_id,
            "documents": docs,  # KHÔNG có headings_recalled/headings_missed (B1)
            "retrieval": retrieval,
            "queries_count": len(queries),
            "files_count": len(all_files),
            # heading_recall measured in Phase 5 (REQ EVAL-02)
            "_note": "headings_recalled/headings_missed: defer Phase 5 per REQ EVAL-02",
        }

        OUTPUT_PATH.write_text(
            json.dumps(snapshot, ensure_ascii=False, indent=2),
            encoding="utf-8",
        )
        logger.info(
            "Baseline DONE → %s (top-1=%.3f top-3=%.3f top-5=%.3f mrr=%.3f)",
            OUTPUT_PATH,
            retrieval["top_1_hit_rate"], retrieval["top_3_hit_rate"],
            retrieval["top_5_hit_rate"], retrieval["mrr"],
        )

        # Phase 1 success criterion 4
        scanned_failed = [
            d for d in docs
            if d.get("filename", "").endswith("_scanned.pdf") and d.get("status") == "error"
        ]
        if scanned_failed:
            logger.info(
                "✓ Phase 1 SC4 met: %d scanned PDF có error log (gap cho Docling)",
                len(scanned_failed),
            )
        else:
            logger.warning(
                "⚠ Phase 1 SC4: scanned PDF không fail như mong đợi — kiểm tra extractor backend"
            )

        return 0
    finally:
        await api.aclose()


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--top-k", type=int, default=5)
    parser.add_argument("--upload-timeout", type=int, default=UPLOAD_TIMEOUT_SEC)
    parser.add_argument("--poll-interval", type=int, default=POLL_INTERVAL_SEC)
    args = parser.parse_args()
    return asyncio.run(run_baseline(args))


if __name__ == "__main__":
    sys.exit(main())
```

LƯU Ý:
- Async với `asyncio` + `httpx.AsyncClient` (PATTERNS.md mục 6 chốt).
- Retry logic 401: tự refresh token rồi retry 1 lần.
- `annotate_heading_gold_count` chỉ ghi count — KHÔNG ghi recalled/missed (B1 defer Phase 5).
- Match retrieval **lowercase**: `expected_doc_id.lower() == result.category.lower()` (B2). KHÔNG dùng `result.id` (chunk_id).
- Snapshot UTF-8 (`ensure_ascii=False`) để query tiếng Việt readable.
- Comment business logic tiếng Việt.

Edge cases handle:
- Upload exception → ghi `status='error'` + `error_message` thay vì raise.
- Poll timeout → ghi `status='timeout'`.
- Search 0 result → `rank=None`, không hit.
- Pre-flight fail → `SystemExit` ngay, không tạo APIClient.
- Embedder config invalid → `SystemExit` ngay sau login (W5).
  </action>
  <verify>
    <automated>cd eval && ruff check baseline.py && python -c "import ast; ast.parse(open('baseline.py').read())" && python baseline.py --help && grep -q "preflight_check" baseline.py && grep -q "category.*lower\\|lower.*category" baseline.py && grep -q "headings_gold_count" baseline.py && ! grep -q '"headings_recalled"\\|"headings_missed"' baseline.py</automated>
  </verify>
  <done>
- `baseline.py` ≥ 280 dòng.
- `ruff check` exit 0.
- `python baseline.py --help` in usage có 3 flag: --top-k, --upload-timeout, --poll-interval.
- Có hàm `preflight_check()` (B3) gọi trước login.
- Có class `APIClient` với refresh logic.
- Có 5 hàm core: get_eval_hub_id, get_embedder_config (fail loud W5), upload_and_wait, search_query, compute_retrieval_metrics (lowercase match B2).
- Snapshot output ĐÚNG schema CONTEXT.md REVISION 1: KHÔNG có `headings_recalled`/`headings_missed`, có `headings_gold_count`.
- `compute_retrieval_metrics` match `expected_doc_id.lower() == result.category.lower()`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Viết eval/dataset/DATASET.md mô tả dataset</name>
  <files>eval/dataset/DATASET.md</files>
  <action>
Tạo `eval/dataset/DATASET.md` (≥ 50 dòng) mô tả dataset cho reproducibility:

```markdown
# Eval Dataset — M1 RAG Quality with Docling

**Phase:** 1 / 5
**Ngày tạo:** 2026-04-28
**Version:** 1.0

## Mục đích

Dataset đo baseline retrieval cho Medinet Wiki RAG, dùng cho cả:
- **Phase 1**: baseline với extractor Go native → snapshot `eval/baseline_native.json`.
- **Phase 5**: compare với extractor Docling → snapshot `eval/baseline_docling.json`.

Gate milestone M1: top-3 hit rate **tăng ≥ 15 điểm phần trăm** HOẶC đạt **≥ 75% tuyệt đối** (xem `ROADMAP.md`).

## Cấu trúc

```
eval/dataset/
├── DATASET.md            (file này)
├── sources/              (8 file gốc copy từ file_test/)
│   ├── DMD_T1-01_DinhVi_TrungTam_v1.docx
│   ├── DMD_T1-02_TuDien_ThuongHieu_v1.docx
│   ├── DMD_T1-03_Script_Library_v1.docx
│   ├── DMD_T1-04_FAQ_ThuongHieu_v1.docx
│   ├── DMD_T3-02_PhanCong_NhanVat_v1.docx
│   ├── DMD_T5-01_ContentStrategy_12TuyenND_v1.docx
│   ├── DMD_T5-02_Playbook_KenhTruyen_v1.docx
│   └── tri_thuc_chinh_tri.pdf
├── scanned/              (2 file PDF image-only sinh bởi build_scanned.py)
│   ├── DMD_T1-01_scanned.pdf
│   └── DMD_T1-04_scanned.pdf
├── queries.jsonl         (10-15 truy vấn vàng tiếng Việt)
├── headings.json         (heading vàng per doc)
└── QUERIES_REVIEW.md     (bảng review user approve)
```

## Phân loại file

| Filename | Loại | Nguồn | Đặc điểm |
|----------|------|-------|----------|
| DMD_T1-01_DinhVi_TrungTam_v1.docx | DOCX | DMD nội bộ | Định vị thương hiệu, text-heavy |
| DMD_T1-02_TuDien_ThuongHieu_v1.docx | DOCX | DMD nội bộ | Từ điển — definition-style |
| DMD_T1-03_Script_Library_v1.docx | DOCX | DMD nội bộ | Script library |
| DMD_T1-04_FAQ_ThuongHieu_v1.docx | DOCX | DMD nội bộ | FAQ Q&A có cấu trúc |
| DMD_T3-02_PhanCong_NhanVat_v1.docx | DOCX | DMD nội bộ | Phân công — table-heavy |
| DMD_T5-01_ContentStrategy_12TuyenND_v1.docx | DOCX | DMD nội bộ | Content strategy 12 tuyến |
| DMD_T5-02_Playbook_KenhTruyen_v1.docx | DOCX | DMD nội bộ | Playbook kênh truyền |
| tri_thuc_chinh_tri.pdf | PDF | Public | PDF gốc có text layer |
| DMD_T1-01_scanned.pdf | PDF (image-only) | Tự sinh | Scanned từ DMD_T1-01 (test OCR) |
| DMD_T1-04_scanned.pdf | PDF (image-only) | Tự sinh | Scanned từ DMD_T1-04 (test OCR) |

## Cách tái tạo

```bash
# Bước 1: Build skeleton
cd eval && pip install -e ".[dev]"

# Bước 2: Build scanned PDF (idempotent)
python scripts/build_scanned.py

# Bước 3: Extract heading vàng từ DOCX (PDF gốc đã LLM-draft trong code)
python scripts/extract_headings.py

# Bước 4: Sinh queries.jsonl (Plan 05 — LLM draft + user review)
# (manual review, đã commit version đã approve)

# Bước 5: Seed eval_hub
psql -h localhost -U medinet -d medinet_central -f scripts/seed_hub.sql

# Bước 6: Cleanup state cũ + run baseline (Pre-flight tự verify)
python scripts/cleanup.py
python ../baseline.py
```

## Schema queries.jsonl

```json
{
  "id": "q01",
  "query": "<câu hỏi tiếng Việt>",
  "expected_doc_id": "<filename>",
  "expected_section": "<heading path nối bằng ' > '>",
  "notes": "<context tại sao expected>"
}
```

⚠ `expected_doc_id` LÀ filename (không phải UUID) — match với `result.category` từ backend SearchResult (xem `backend/internal/rag/searcher.go:131`).

10-15 query phân bố tối thiểu 1 query / file (10 file × 1 + bonus cho edge case).

## Schema headings.json

```json
{
  "<filename>": ["<heading path 1>", "<heading path 2>", ...]
}
```

- 7 DOCX: extract auto từ `pStyle` + `outlineLvl` (port từ Go `docx.go:46-95`).
- 1 PDF gốc: manual entry trong `extract_headings.py:MANUAL_PDF_HEADINGS` (LLM-draft + user review Plan 05).
- 2 scanned PDF: copy heading từ DOCX gốc tương ứng (chính là ground truth — Docling OCR ở Phase 5 phải recover đúng heading này).

## Schema baseline_native.json

```json
{
  "run_id": "...",
  "extractor_mode": "native",
  "embedder_provider": "...",
  "embedder_model": "...",
  "embedder_dim": <int>,
  "documents": [
    {"id": "...", "filename": "...", "status": "completed|error", "error_message": "...?", "chunk_count": <int>, "headings_gold_count": <int>}
  ],
  "retrieval": {
    "top_1_hit_rate": <float>, "top_3_hit_rate": <float>, "top_5_hit_rate": <float>, "mrr": <float>,
    "per_query": [{"id": "q01", "expected_doc_id": "...", "actual_top_5": [...], "rank": <int|null>}]
  }
}
```

⚠ **Phase 1 KHÔNG có `headings_recalled` / `headings_missed`** — defer Phase 5 theo REQ EVAL-02. Phase 1 chỉ ghi `headings_gold_count` (số heading vàng/doc).

## License

- 7 file DMD: nội bộ Medinet, KHÔNG public.
- `tri_thuc_chinh_tri.pdf`: public domain (xem comment trong file gốc).
- 2 scanned PDF: derived từ DOCX nội bộ → cùng license nội bộ.

## Versioning

- v1.0 (2026-04-28): khởi tạo cho M1 Phase 1 baseline native.
- Khi sửa queries hoặc thêm file: bump version + ghi changelog ở cuối file này.

## Reproducibility check

Sau khi sinh dataset:
```bash
# Hash check
sha256sum eval/dataset/sources/*.docx
sha256sum eval/dataset/scanned/*.pdf
sha256sum eval/dataset/queries.jsonl
sha256sum eval/dataset/headings.json
```

Phase 5 chạy script trên cùng input phải ra cùng hash (trừ scanned PDF có thể khác do timestamp metadata trong PDF — chấp nhận diff).
```

Tối thiểu 50 dòng. Tất cả ngôn ngữ tự nhiên TIẾNG VIỆT có dấu.
  </action>
  <verify>
    <automated>test -f eval/dataset/DATASET.md && [ "$(wc -l < eval/dataset/DATASET.md)" -ge 50 ] && grep -q "queries.jsonl" eval/dataset/DATASET.md && grep -q "headings.json" eval/dataset/DATASET.md && grep -q "Reproducibility" eval/dataset/DATASET.md && grep -q "REQ EVAL-02" eval/dataset/DATASET.md</automated>
  </verify>
  <done>
- `eval/dataset/DATASET.md` ≥ 50 dòng.
- Có đủ 9 section: Mục đích, Cấu trúc, Phân loại file, Cách tái tạo, Schema queries, Schema headings, Schema baseline_native, License, Versioning, Reproducibility check.
- Có bảng phân loại 10 file đầy đủ.
- Có note `expected_doc_id` LÀ filename + reference `searcher.go:131`.
- Có note Phase 1 defer `headings_recalled`/`missed` Phase 5 (REQ EVAL-02).
  </done>
</task>

<task type="auto">
  <name>Task 3: Chạy baseline.py end-to-end + verify snapshot output (REVISION 1 W2)</name>
  <files>eval/baseline_native.json</files>
  <action>
Tiền điều kiện (Pre-flight tự verify trong baseline.py):
1. Backend Go đang chạy ở `http://localhost:8180`.
2. Postgres + Redis + ChromaDB đang chạy.
3. Hub `eval` đã seed (Plan 02 task 1).
4. State sạch — chạy `python eval/scripts/cleanup.py` ngay trước.

Chạy:
```bash
cd D:/ChuongNV_Medinet/AI/medinet_wiki/Hub_All
python eval/scripts/cleanup.py
python eval/baseline.py
```

Thời gian dự kiến: 5–15 phút (phụ thuộc embedding API + 10 file).

Verify output (REVISION 1 W2 — assert error_message chứa "no text extracted"):
```bash
python -c "
import json
from pathlib import Path

s = json.loads(Path('eval/baseline_native.json').read_text('utf-8'))
print('Run ID:', s['run_id'])
print('Mode:', s['extractor_mode'])
print('Embedder:', s['embedder_provider'], s['embedder_model'], 'dim=', s['embedder_dim'])
print('Files:', s['files_count'], 'Queries:', s['queries_count'])
print()
print('Documents status:')
for d in s['documents']:
    print(f'  {d[\"filename\"]}: {d[\"status\"]} (chunks={d.get(\"chunk_count\", 0)}, headings_gold={d.get(\"headings_gold_count\", 0)})')
print()
r = s['retrieval']
print(f'Top-1: {r[\"top_1_hit_rate\"]:.3f}')
print(f'Top-3: {r[\"top_3_hit_rate\"]:.3f}')
print(f'Top-5: {r[\"top_5_hit_rate\"]:.3f}')
print(f'MRR:   {r[\"mrr\"]:.3f}')

# Phase 1 success criterion 4 — REVISION 1 W2
scanned_failed = [d for d in s['documents'] if d['filename'].endswith('_scanned.pdf') and d['status'] == 'error']
assert len(scanned_failed) >= 2, f'Mong đợi 2 scanned fail extraction, có {len(scanned_failed)}'

# REVISION 1 W2: check error_message chứa 'no text extracted'
# Backend có thể trả 'error' hoặc 'error_message' — check cả hai.
no_text_count = 0
no_msg_count = 0
for d in scanned_failed:
    err_text = d.get('error_message') or d.get('error') or ''
    if 'no text extracted' in err_text.lower():
        no_text_count += 1
    elif not err_text:
        no_msg_count += 1
        print(f'⚠ W2 bypass: {d[\"filename\"]} status=error nhưng không có error_message từ API')
    else:
        print(f'⚠ {d[\"filename\"]} error message: {err_text[:200]}')

if no_text_count >= 2:
    print(f'✓ W2 met: {no_text_count}/2 scanned PDF có error_message chứa \"no text extracted\"')
elif no_msg_count == len(scanned_failed):
    print(f'⚠ W2 bypassed: API không trả error_message cho scanned PDF — chỉ verify status=error')
    print(f'  Lý do bypass: GET /api/documents/{{id}}/status có thể không expose error_message field.')
    print(f'  Khắc phục Phase 5: thêm field error_message vào API response.')
else:
    raise AssertionError(f'W2 fail: chỉ {no_text_count}/2 scanned có \"no text extracted\", còn lại có error message khác')

# Verify schema B1: KHÔNG có headings_recalled/headings_missed
for d in s['documents']:
    assert 'headings_recalled' not in d, f'B1 fail: {d[\"filename\"]} có headings_recalled (should defer Phase 5)'
    assert 'headings_missed' not in d, f'B1 fail: {d[\"filename\"]} có headings_missed (should defer Phase 5)'
    assert 'headings_gold_count' in d, f'B1 fail: {d[\"filename\"]} thiếu headings_gold_count'
print('✓ B1 schema: documents có headings_gold_count, KHÔNG có headings_recalled/missed (defer Phase 5)')
"
```

NẾU `top_3_hit_rate` quá thấp (vd < 0.20):
- Đây có thể là expected behavior cho native extractor + 2 scanned PDF fail.
- KHÔNG phải bug — chính là baseline cần cải thiện.

NẾU upload bất kỳ file DOCX nào fail (không phải scanned):
- Kiểm tra log backend: `tail -f backend/logs/...` (nếu có log file) hoặc console output.
- Có thể do: ChromaDB collection chưa create, embedding API rate limit, file_type không hỗ trợ.
- Re-run sau khi fix infra issue.

NẾU script hang ở "polling":
- Kiểm tra worker pool backend (`backend/internal/worker/manager.go`) còn alive không.
- Default timeout 5 phút / file → tăng `--upload-timeout 600` nếu file lớn.

NẾU pre-flight fail (B3): script exit ngay với hint cụ thể (backend down / ChromaDB down / hub chưa seed) — fix theo hint rồi chạy lại.

NẾU embedder config invalid (W5): script exit ngay sau login với message yêu cầu check `.env` backend.

**REVISION 1 W2 — bypass case rõ ràng:**
Nếu API `GET /api/documents/{id}/status` không trả field `error_message` (chỉ trả `status='error'` mà không có thêm context), W2 verify sẽ log warning + bypass assertion. Lý do: backend có thể chưa expose error chi tiết qua status endpoint. Trong case này:
- Vẫn pass verify (status='error' là đủ cho SC4 cơ bản).
- Ghi rõ lý do bypass trong SUMMARY: "API không expose error_message field — chỉ verify status='error'."
- Đề xuất Phase 5 fix backend trả error_message qua status endpoint.

Commit `baseline_native.json` vào git để Phase 5 reference được.
  </action>
  <verify>
    <automated>test -f eval/baseline_native.json && python -c "
import json
s = json.loads(open('eval/baseline_native.json', encoding='utf-8').read())
assert s['extractor_mode'] == 'native'
assert s['files_count'] == 10
assert 10 <= s['queries_count'] <= 15
assert 'top_3_hit_rate' in s['retrieval']
assert 'mrr' in s['retrieval']
assert len(s['documents']) == 10

# B1 schema check
for d in s['documents']:
    assert 'headings_recalled' not in d
    assert 'headings_missed' not in d
    assert 'headings_gold_count' in d

# SC4 + W2
scanned_err = [d for d in s['documents'] if d['filename'].endswith('_scanned.pdf') and d['status'] == 'error']
assert len(scanned_err) >= 2, f'Expected 2 scanned errors got {len(scanned_err)}'

# W2: assert error_message chứa 'no text extracted' HOẶC bypass nếu không có field
no_text = sum(1 for d in scanned_err if 'no text extracted' in (d.get('error_message') or d.get('error') or '').lower())
no_field = sum(1 for d in scanned_err if not (d.get('error_message') or d.get('error')))
assert no_text >= 2 or no_field == len(scanned_err), f'W2 fail: only {no_text}/2 scanned have no-text-extracted msg'
print('OK')
"</automated>
  </verify>
  <done>
- `eval/baseline_native.json` valid JSON, đủ schema CONTEXT.md REVISION 1.
- 10 documents trong snapshot.
- 10-15 queries trong `retrieval.per_query`.
- 2 scanned PDF có `status='error'` (Phase 1 SC4).
- W2 fix: hoặc 2 scanned có `error_message` chứa `"no text extracted"`, HOẶC bypass với log rõ lý do (API không trả error_message field).
- B1 schema: documents có `headings_gold_count`, KHÔNG có `headings_recalled`/`headings_missed`.
- 8 DOCX/PDF gốc có `status='completed'` (Phase 1 SC3 phần upload).
- 4 metric retrieval đều là số 0..1.
- File committed vào git.
  </done>
</task>

</tasks>

<verification>
Sau khi 4 task xong (Task 0 + Task 1 + Task 2 + Task 3), Phase 1 đầy đủ thỏa 4 success criteria (ROADMAP.md):

1. ✓ `eval/dataset/` chứa 8 file gốc + 2 scanned PDF tiếng Việt (Plan 01 + 03).
2. ✓ `eval/dataset/queries.jsonl` có 10-15 truy vấn vàng (Plan 05).
3. ✓ Toàn bộ dataset đã upload + snapshot baseline lưu tại `eval/baseline_native.json` (Task 3).
4. ✓ 2 scanned PDF có `status='error'` với error message chứa `no text extracted` từ `pdf.go:52` (Task 3 verify W2 — bypass nếu API không expose).

REVISION 1 fixes verified:
- B1: snapshot KHÔNG có `headings_recalled`/`headings_missed`, chỉ `headings_gold_count`.
- B2: match `expected_doc_id.lower() == result.category.lower()`.
- B3: pre-flight check chạy đầu, fail loud với hint.
- W2: assert `"no text extracted"` trong error_message hoặc bypass có lý do.
- W5: `get_embedder_config` raise SystemExit nếu invalid.
</verification>

<success_criteria>
- Phase 1 hoàn tất, M1 milestone có baseline cụ thể để compare.
- Snapshot reproducible — Phase 5 chạy lại baseline.py với mode docling tạo `baseline_docling.json` để diff.
- Embedder config khóa cứng trong snapshot — Phase 5 fail loud nếu provider/model đổi giữa 2 run.
- 2 scanned PDF fail là **bằng chứng định lượng** cho gap mà Phase 2 (Docling OCR) đóng.
- Heading recall metric defer Phase 5 (REQ EVAL-02) — Phase 1 KHÔNG đo placeholder rỗng.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/01-eval-dataset-baseline-native/01-06-SUMMARY.md` ghi:
- Toàn bộ output snapshot baseline_native.json (4 metric retrieval + 10 doc status).
- Embedder config đã capture (provider/model/dim) — confirm W5 fail-loud không trigger.
- Confirm 2 scanned PDF fail extraction với error message (W2 — pass hay bypass và lý do).
- Confirm pre-flight check pass (B3).
- Confirm B1 schema: KHÔNG có headings_recalled/missed.
- Note runtime tổng (login → upload all → search) để Phase 5 estimate được thời gian.
</output>
