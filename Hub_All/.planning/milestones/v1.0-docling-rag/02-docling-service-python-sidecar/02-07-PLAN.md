---
phase: 02-docling-service-python-sidecar
plan: 07
type: execute
wave: 4
depends_on: [06]
files_modified:
  - docling-pipeline/tests/conftest.py
  - docling-pipeline/tests/test_health.py
  - docling-pipeline/tests/test_extract.py
  - docling-pipeline/tests/test_ocr.py
  - docling-pipeline/tests/test_table_figure.py
  - docling-pipeline/tests/test_limits.py
  - docling-pipeline/tests/test_logging.py
  - docling-pipeline/tests/fixtures/sample_small.pdf
  - docling-pipeline/tests/fixtures/sample_small.docx
  - docling-pipeline/tests/fixtures/sample_scanned_vi.pdf
  - docling-pipeline/tests/fixtures/sample_with_table.pdf
  - docling-pipeline/tests/fixtures/sample_with_figure.pdf
  - docling-pipeline/tests/fixtures/generate_fixtures.py
autonomous: true
requirements:
  - DSVC-01
  - DSVC-02
  - DSVC-04
  - DSVC-06
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
  - EXTRACT-04
must_haves:
  truths:
    - "pytest chạy trong docling-pipeline/ pass tất cả test (≥ 6 file test, ≥ 12 test case)"
    - "test_health.py: /healthz trả 200, /readyz trả 200 (ready) hoặc 503 (not_ready)"
    - "test_extract.py: POST /v1/process với sample_small.pdf + sample_small.docx trả 200 + schema DSVC-02 hợp lệ"
    - "test_ocr.py: POST /v1/process với sample_scanned_vi.pdf trả 200 + có chunk text tiếng Việt"
    - "test_table_figure.py (B3): table HTML preserved (assert <table>/<thead>/<tbody>) + figure caption marker (![<caption>](#fig-)"
    - "test_limits.py (B4): test_413_payload + test_504_when_timeout (monkeypatch slow extract + DOCLING_REQUEST_TIMEOUT_SEC=1)"
    - "test_logging.py (W4): X-Request-Id propagate vào structlog stdout JSON (capsys assert rid in captured.out)"
  artifacts:
    - path: "docling-pipeline/tests/conftest.py"
      provides: "FastAPI TestClient fixture + monkeypatch settings + 5 fixture files"
      exports: ["client", "sample_pdf", "sample_docx", "sample_scanned_vi", "sample_with_table", "sample_with_figure"]
    - path: "docling-pipeline/tests/test_health.py"
      provides: "Test 2 endpoint /healthz + /readyz"
      min_lines: 20
    - path: "docling-pipeline/tests/test_extract.py"
      provides: "Test /v1/process với PDF + DOCX nhỏ + request_id propagate"
      min_lines: 60
    - path: "docling-pipeline/tests/test_ocr.py"
      provides: "Test scanned PDF tiếng Việt qua OCR Tesseract vie+eng"
      min_lines: 30
    - path: "docling-pipeline/tests/test_table_figure.py"
      provides: "B3 — Test table_html preserved + figure caption marker"
      min_lines: 40
    - path: "docling-pipeline/tests/test_limits.py"
      provides: "B4 — Test 413 payload + 504 timeout (monkeypatch slow)"
      min_lines: 35
    - path: "docling-pipeline/tests/test_logging.py"
      provides: "W4 — Test X-Request-Id xuất hiện trong structlog JSON stdout"
      min_lines: 25
    - path: "docling-pipeline/tests/fixtures/generate_fixtures.py"
      provides: "Script Python sinh sample_with_table.pdf + sample_with_figure.pdf bằng reportlab + pillow"
    - path: "docling-pipeline/tests/fixtures/sample_with_table.pdf"
      provides: "PDF chứa 1 table 3x3 với header (B3 test_table_html_preserved)"
    - path: "docling-pipeline/tests/fixtures/sample_with_figure.pdf"
      provides: "PDF chứa 1 figure + caption (B3 test_figure_caption_marker)"
    - path: "docling-pipeline/tests/fixtures/sample_scanned_vi.pdf"
      provides: "Scanned PDF tiếng Việt 1 trang (copy từ eval/dataset/scanned/)"
  key_links:
    - from: "tests/conftest.py"
      to: "main.create_app()"
      via: "fastapi.testclient.TestClient(app)"
      pattern: "TestClient"
    - from: "test_table_figure.py"
      to: "fixtures/sample_with_table.pdf + sample_with_figure.pdf"
      via: "open(fixture_path, 'rb')"
      pattern: "sample_with_"
    - from: "test_limits.py test_504"
      to: "monkeypatch DoclingExtractor.extract"
      via: "monkeypatch.setattr với asyncio.sleep slow"
      pattern: "monkeypatch.setattr"
    - from: "test_logging.py"
      to: "structlog stdout (PrintLoggerFactory)"
      via: "capsys.readouterr()"
      pattern: "capsys"
---

<objective>
Tạo bộ test pytest cho service: 6 file test (health + extract + ocr + table_figure + limits + logging) + 5 fixtures + script generate. Coverage mục tiêu 60-70% (CONTEXT mục F). Sử dụng FastAPI TestClient, fixtures < 100KB, copy 1 scanned PDF từ `eval/dataset/scanned/DMD_T1-01_scanned.pdf` (đã có sẵn từ Phase 1, commit 045d29b) để test OCR.

Purpose: SC2 + SC3 + SC4 + SC5 trong ROADMAP yêu cầu chứng minh schema DSVC-02 đúng + OCR vie+eng chạy + table HTML preserved + figure caption marker + 413/504 limit + request_id in log. Test tự động hoá việc verify này, KHÔNG phụ thuộc smoke manual ở Plan 08.

**Revision B3 + B4 + W4:** Thêm 3 file test mới (`test_table_figure.py`, `test_limits.py`, `test_logging.py`) + 2 fixture mới (`sample_with_table.pdf`, `sample_with_figure.pdf`) + script `generate_fixtures.py` (reportlab + pillow) để fixture deterministic (I2 — cũng giải).

Output: pytest pass; coverage ≥ 60% qua `pytest --cov=docling_pipeline`.

⚠️ **Lưu ý runtime:** Test này CẦN Docling models đã download (lần đầu chạy ~30-60s warm). Trong CI environment chưa có Docker, executor có thể skip live test (`-m "not slow"`) hoặc chạy với marker `@pytest.mark.skipif(not DOCLING_INSTALLED)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docling-service-python-sidecar/02-CONTEXT.md
@.planning/phases/02-docling-service-python-sidecar/02-06-PLAN.md
@eval/dataset/scanned/DMD_T1-01_scanned.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo conftest + 6 test file + 5 fixture + script generate</name>
  <files>docling-pipeline/tests/conftest.py, docling-pipeline/tests/test_health.py, docling-pipeline/tests/test_extract.py, docling-pipeline/tests/test_ocr.py, docling-pipeline/tests/test_table_figure.py, docling-pipeline/tests/test_limits.py, docling-pipeline/tests/test_logging.py, docling-pipeline/tests/fixtures/generate_fixtures.py, docling-pipeline/tests/fixtures/sample_small.pdf, docling-pipeline/tests/fixtures/sample_small.docx, docling-pipeline/tests/fixtures/sample_scanned_vi.pdf, docling-pipeline/tests/fixtures/sample_with_table.pdf, docling-pipeline/tests/fixtures/sample_with_figure.pdf</files>
  <action>
**Bước 1: Copy fixture có sẵn**

```bash
# Copy scanned PDF tiếng Việt từ eval (Phase 1 commit 045d29b — đã có sẵn)
cp eval/dataset/scanned/DMD_T1-01_scanned.pdf docling-pipeline/tests/fixtures/sample_scanned_vi.pdf
```

**Bước 2: Script generate 4 fixture còn lại — `tests/fixtures/generate_fixtures.py`**

```python
"""Sinh fixture PDF/DOCX deterministic cho pytest (I2 — fixtures commit vào git, không sinh runtime).

Chạy 1 lần:
    cd docling-pipeline
    python tests/fixtures/generate_fixtures.py
Sau đó commit 4 file PDF/DOCX vào git.
"""

from __future__ import annotations

from pathlib import Path

from reportlab.lib.pagesizes import letter
from reportlab.platypus import (
    Image,
    Paragraph,
    SimpleDocTemplate,
    Spacer,
    Table,
    TableStyle,
)
from reportlab.lib import colors
from reportlab.lib.styles import getSampleStyleSheet
from PIL import Image as PILImage, ImageDraw

FIX = Path(__file__).parent


def gen_sample_small_pdf() -> None:
    doc = SimpleDocTemplate(str(FIX / "sample_small.pdf"), pagesize=letter)
    styles = getSampleStyleSheet()
    story = [
        Paragraph("Hello World — sample small PDF for Docling tests.", styles["Title"]),
        Spacer(1, 12),
        Paragraph("This is a paragraph for extraction.", styles["Normal"]),
    ]
    doc.build(story)


def gen_sample_with_table_pdf() -> None:
    """B3 — PDF chứa 1 table 3x3 với header để test table_html preserved."""
    doc = SimpleDocTemplate(str(FIX / "sample_with_table.pdf"), pagesize=letter)
    styles = getSampleStyleSheet()
    data = [
        ["Cột A", "Cột B", "Cột C"],
        ["1", "2", "3"],
        ["4", "5", "6"],
    ]
    tbl = Table(data, colWidths=[100, 100, 100])
    tbl.setStyle(
        TableStyle(
            [
                ("BACKGROUND", (0, 0), (-1, 0), colors.grey),
                ("TEXTCOLOR", (0, 0), (-1, 0), colors.whitesmoke),
                ("GRID", (0, 0), (-1, -1), 0.5, colors.black),
                ("FONTNAME", (0, 0), (-1, 0), "Helvetica-Bold"),
            ]
        )
    )
    story = [Paragraph("Bảng test", styles["Title"]), Spacer(1, 12), tbl]
    doc.build(story)


def gen_sample_with_figure_pdf() -> None:
    """B3 — PDF chứa 1 figure + caption để test figure caption marker."""
    # Tạo image PNG nhỏ ngay
    img_path = FIX / "_tmp_figure.png"
    img = PILImage.new("RGB", (200, 100), color=(180, 220, 255))
    draw = ImageDraw.Draw(img)
    draw.rectangle([20, 20, 180, 80], outline=(0, 0, 0), width=2)
    draw.text((40, 40), "FIGURE", fill=(0, 0, 0))
    img.save(img_path)

    doc = SimpleDocTemplate(str(FIX / "sample_with_figure.pdf"), pagesize=letter)
    styles = getSampleStyleSheet()
    story = [
        Paragraph("Tài liệu có hình", styles["Title"]),
        Spacer(1, 12),
        Image(str(img_path), width=200, height=100),
        Paragraph("Hình 1: Sơ đồ minh họa test", styles["Italic"]),
    ]
    doc.build(story)
    img_path.unlink()


def gen_sample_small_docx() -> None:
    from docx import Document

    d = Document()
    d.add_heading("Sample DOCX", 0)
    d.add_paragraph("Đây là đoạn văn test.")
    d.save(str(FIX / "sample_small.docx"))


if __name__ == "__main__":
    gen_sample_small_pdf()
    gen_sample_with_table_pdf()
    gen_sample_with_figure_pdf()
    gen_sample_small_docx()
    print("Generated fixtures in", FIX)
```

**Bổ sung dev deps cho generate** (executor cài tạm trong virtualenv để chạy script, KHÔNG thêm vào pyproject runtime):
```bash
pip install reportlab pillow python-docx
python docling-pipeline/tests/fixtures/generate_fixtures.py
```

Sau khi 4 file PDF/DOCX sinh ra (size < 50KB mỗi file) → commit vào git (I2 — deterministic).

**Bước 3: `tests/conftest.py`**

```python
"""Pytest fixtures cho docling-pipeline service."""

from __future__ import annotations

import os
from collections.abc import Iterator
from pathlib import Path

import pytest
from fastapi.testclient import TestClient

FIXTURES_DIR = Path(__file__).parent / "fixtures"


@pytest.fixture(scope="session", autouse=True)
def _set_env() -> None:
    """Override env trước khi import settings — keep file size limit nhỏ cho test 413."""
    os.environ.setdefault("DOCLING_MAX_FILE_MB", "5")
    os.environ.setdefault("DOCLING_REQUEST_TIMEOUT_SEC", "120")
    os.environ.setdefault("DOCLING_LOG_FORMAT", "json")  # W4 — test_logging cần JSON


@pytest.fixture(scope="session")
def client() -> Iterator[TestClient]:
    """TestClient duy nhất cho toàn session — lifespan warm models 1 lần."""
    from docling_pipeline.main import create_app

    app = create_app()
    with TestClient(app) as c:
        yield c


@pytest.fixture
def sample_pdf() -> bytes:
    return (FIXTURES_DIR / "sample_small.pdf").read_bytes()


@pytest.fixture
def sample_docx() -> bytes:
    return (FIXTURES_DIR / "sample_small.docx").read_bytes()


@pytest.fixture
def sample_scanned_vi() -> bytes:
    return (FIXTURES_DIR / "sample_scanned_vi.pdf").read_bytes()


@pytest.fixture
def sample_with_table() -> bytes:
    return (FIXTURES_DIR / "sample_with_table.pdf").read_bytes()


@pytest.fixture
def sample_with_figure() -> bytes:
    return (FIXTURES_DIR / "sample_with_figure.pdf").read_bytes()
```

**Bước 4: `tests/test_health.py`**

```python
"""Test /healthz + /readyz endpoints (DSVC-04 + B5)."""

from __future__ import annotations

from fastapi.testclient import TestClient


def test_healthz_returns_ok(client: TestClient) -> None:
    resp = client.get("/healthz")
    assert resp.status_code == 200
    assert resp.json() == {"status": "ok"}


def test_readyz_returns_ready_or_not_ready(client: TestClient) -> None:
    resp = client.get("/readyz")
    # B5: 200 ready (lib OK) hoặc 503 not_ready (ImportError docling)
    assert resp.status_code in (200, 503)
    body = resp.json()
    assert body["status"] in {"ready", "not_ready"}
```

**Bước 5: `tests/test_extract.py`**

```python
"""Test POST /v1/process với PDF + DOCX nhỏ + request_id (DSVC-01, DSVC-02, DSVC-04)."""

from __future__ import annotations

import uuid

from fastapi.testclient import TestClient


def _post_process(client: TestClient, file_bytes: bytes, filename: str, **kwargs):
    files = {"file": (filename, file_bytes, "application/octet-stream")}
    data = {
        "hub_code": kwargs.get("hub_code", "test_hub"),
        "doc_type": kwargs.get("doc_type", "test"),
        "request_id": kwargs.get("request_id", ""),
    }
    headers = kwargs.get("headers", {})
    return client.post("/v1/process", files=files, data=data, headers=headers)


def test_process_pdf_returns_schema(client: TestClient, sample_pdf: bytes) -> None:
    resp = _post_process(client, sample_pdf, "sample.pdf")
    assert resp.status_code == 200, resp.text
    body = resp.json()

    # Schema DSVC-02 top-level
    assert "request_id" in body
    assert "doc_meta" in body
    assert "chunks" in body

    # doc_meta fields
    meta = body["doc_meta"]
    assert meta["filename"] == "sample.pdf"
    assert meta["file_type"] == "pdf"
    assert isinstance(meta["page_count"], int)
    assert isinstance(meta["ocr_used"], bool)

    # chunks: ≥ 1 với đầy đủ field
    assert len(body["chunks"]) > 0
    ch0 = body["chunks"][0]
    for field in (
        "chunk_index", "text", "headers", "caption",
        "page_start", "page_end", "is_table", "table_html",
        "bbox", "token_count",
    ):
        assert field in ch0, f"missing field {field}"
    assert isinstance(ch0["chunk_index"], int)
    assert isinstance(ch0["headers"], list)
    assert isinstance(ch0["is_table"], bool)
    assert isinstance(ch0["token_count"], int)
    assert ch0["token_count"] > 0


def test_process_docx_returns_schema(client: TestClient, sample_docx: bytes) -> None:
    resp = _post_process(client, sample_docx, "sample.docx")
    assert resp.status_code == 200, resp.text
    body = resp.json()
    assert body["doc_meta"]["file_type"] == "docx"
    assert len(body["chunks"]) > 0


def test_request_id_propagate(client: TestClient, sample_pdf: bytes) -> None:
    rid = str(uuid.uuid4())
    resp = _post_process(client, sample_pdf, "sample.pdf", headers={"X-Request-Id": rid})
    assert resp.status_code == 200
    # Response phải echo lại request_id trong body + header
    assert resp.json()["request_id"] == rid
    assert resp.headers.get("X-Request-Id") == rid


def test_process_unsupported_extension(client: TestClient) -> None:
    resp = _post_process(client, b"hello", "evil.exe")
    assert resp.status_code == 415
```

**Bước 6: `tests/test_ocr.py`**

```python
"""Test OCR Tesseract vie+eng cho scanned PDF tiếng Việt (DSVC-03 + EXTRACT-02).

Đảo ngược thất bại Phase 1: scanned PDF tiếng Việt phải extract được text VN qua OCR.
"""

from __future__ import annotations

import pytest
from fastapi.testclient import TestClient

EXPECTED_VI_TOKENS = [
    "Đỗ Minh Đường",
    "Định vị",
    "Trung tâm",
    "y học",
    "định vị",
    "đỗ minh",
]


@pytest.mark.slow
def test_ocr_scanned_vi_extracts_vietnamese(
    client: TestClient, sample_scanned_vi: bytes
) -> None:
    files = {"file": ("scanned.pdf", sample_scanned_vi, "application/pdf")}
    data = {"hub_code": "test", "doc_type": "scanned", "request_id": ""}
    resp = client.post("/v1/process", files=files, data=data)

    assert resp.status_code == 200, resp.text
    body = resp.json()
    assert body["doc_meta"]["ocr_used"] is True
    assert len(body["chunks"]) > 0

    full_text = " ".join(ch["text"] for ch in body["chunks"]).lower()
    assert any(
        tok.lower() in full_text for tok in EXPECTED_VI_TOKENS
    ), (
        "Không tìm thấy token tiếng Việt nào trong OCR output — Tesseract vie+eng "
        f"có thể chưa cài trong test env. Tokens checked: {EXPECTED_VI_TOKENS}. "
        f"Sample text: {full_text[:200]!r}"
    )
```

**Bước 7: `tests/test_table_figure.py` (B3 — MỚI)**

```python
"""B3 — Test EXTRACT-03 (table HTML preserved) + EXTRACT-04 (figure caption marker)."""

from __future__ import annotations

import re

import pytest
from fastapi.testclient import TestClient


def _post(client: TestClient, body: bytes, filename: str):
    files = {"file": (filename, body, "application/pdf")}
    data = {"hub_code": "test", "doc_type": "test", "request_id": ""}
    return client.post("/v1/process", files=files, data=data)


@pytest.mark.slow
def test_table_html_preserved(client: TestClient, sample_with_table: bytes) -> None:
    """EXTRACT-03 — chunk có is_table=True phải kèm table_html chứa <table>/<thead>/<tbody>."""
    resp = _post(client, sample_with_table, "table.pdf")
    assert resp.status_code == 200, resp.text
    body = resp.json()

    table_chunks = [c for c in body["chunks"] if c.get("is_table")]
    assert len(table_chunks) >= 1, (
        f"không tìm thấy chunk nào có is_table=True; chunks={body['chunks']}"
    )

    tbl = table_chunks[0]
    html = tbl.get("table_html") or ""
    assert "<table" in html.lower(), f"table_html không chứa <table>: {html[:200]!r}"
    # thead + tbody — Docling 2.91 default emit cả 2 khi có header row
    assert "<thead" in html.lower() or "<tbody" in html.lower(), (
        f"table_html thiếu thead/tbody: {html[:200]!r}"
    )


@pytest.mark.slow
def test_figure_caption_marker(client: TestClient, sample_with_figure: bytes) -> None:
    """EXTRACT-04 — chunk text chứa pattern ![<caption>](#fig-N) cho figure có caption."""
    resp = _post(client, sample_with_figure, "figure.pdf")
    assert resp.status_code == 200, resp.text
    body = resp.json()

    full_text = "\n".join(c["text"] for c in body["chunks"])
    # Pattern: ![bất kỳ caption nào](#fig-<số>)
    pattern = re.compile(r"!\[[^\]]*\]\(#fig-\d+\)")
    assert pattern.search(full_text), (
        f"không tìm thấy figure caption marker '![...](#fig-N)' trong text. "
        f"Text sample: {full_text[:500]!r}"
    )
```

**Bước 8: `tests/test_limits.py` (B4 — MỚI)**

```python
"""B4 — Test 413 payload-too-large + 504 timeout (DSVC-06)."""

from __future__ import annotations

import asyncio

import pytest
from fastapi.testclient import TestClient


def _post_raw(client: TestClient, body: bytes, filename: str):
    files = {"file": (filename, body, "application/pdf")}
    data = {"hub_code": "test", "doc_type": "test", "request_id": ""}
    return client.post("/v1/process", files=files, data=data)


def test_413_payload_too_large(client: TestClient) -> None:
    """DOCLING_MAX_FILE_MB=5 trong conftest → upload 6MB phải trả 413."""
    big = b"x" * (6 * 1024 * 1024)
    resp = _post_raw(client, big, "huge.pdf")
    assert resp.status_code == 413
    assert resp.json()["detail"]["error"] == "payload_too_large"
    assert resp.json()["detail"]["max_mb"] == 5


def test_504_when_timeout(client: TestClient, monkeypatch: pytest.MonkeyPatch) -> None:
    """B4 — Khi extract chậm hơn DOCLING_REQUEST_TIMEOUT_SEC → trả 504 Gateway Timeout.

    Monkeypatch DoclingExtractor.extract bằng hàm sleep dài; ép timeout = 1s.
    """
    # Monkeypatch settings.request_timeout_sec = 1 (ép nhanh)
    from docling_pipeline.config import get_settings

    get_settings.cache_clear()  # type: ignore[attr-defined]
    monkeypatch.setenv("DOCLING_REQUEST_TIMEOUT_SEC", "1")

    # Slow extract — block 999s; asyncio.timeout(1) phải trigger
    def slow_extract(self, file_bytes, filename):  # noqa: ARG001
        import time
        time.sleep(999)
        return None

    from docling_pipeline.core.extractor import DoclingExtractor

    monkeypatch.setattr(DoclingExtractor, "extract", slow_extract)

    files = {"file": ("x.pdf", b"%PDF-1.4\n%dummy", "application/pdf")}
    data = {"hub_code": "test", "doc_type": "test", "request_id": ""}

    resp = client.post("/v1/process", files=files, data=data)
    assert resp.status_code == 504, f"expected 504, got {resp.status_code}: {resp.text}"
    body = resp.json()
    assert body["detail"]["error"] == "request_timeout"
    assert body["detail"]["timeout_sec"] == 1

    # Cleanup cache để các test sau dùng lại settings gốc
    get_settings.cache_clear()  # type: ignore[attr-defined]
```

**Bước 9: `tests/test_logging.py` (W4 — MỚI)**

```python
"""W4 — Test X-Request-Id propagate vào structlog JSON stdout (DSVC-04 evidence end-to-end)."""

from __future__ import annotations

import uuid

import pytest
from fastapi.testclient import TestClient


def test_request_id_propagated_to_log(
    client: TestClient,
    sample_pdf: bytes,
    capsys: pytest.CaptureFixture[str],
) -> None:
    """RequestIdMiddleware bind_contextvars(request_id=rid) → structlog JSON line phải chứa rid."""
    rid = f"test-w4-{uuid.uuid4()}"
    files = {"file": ("sample.pdf", sample_pdf, "application/pdf")}
    data = {"hub_code": "test", "doc_type": "test", "request_id": ""}
    headers = {"X-Request-Id": rid}

    resp = client.post("/v1/process", files=files, data=data, headers=headers)
    assert resp.status_code == 200

    # structlog PrintLoggerFactory ghi ra sys.stdout (configure_logging Plan 06)
    captured = capsys.readouterr()
    # Phải có ít nhất 1 line JSON chứa rid (event "process_done" + middleware bind_contextvars)
    assert rid in captured.out, (
        f"request_id {rid!r} không xuất hiện trong stdout structlog. "
        f"Captured (first 500 chars): {captured.out[:500]!r}"
    )
```

**Marker `slow` cho test_ocr + test_table_figure** — Plan 08 README sẽ document `pytest -m "not slow"` cho fast loop.
  </action>
  <verify>
    <automated>cd docling-pipeline && test -f tests/conftest.py && test -f tests/test_health.py && test -f tests/test_extract.py && test -f tests/test_ocr.py && test -f tests/test_table_figure.py && test -f tests/test_limits.py && test -f tests/test_logging.py && test -f tests/fixtures/sample_scanned_vi.pdf && test -f tests/fixtures/sample_small.pdf && test -f tests/fixtures/sample_small.docx && test -f tests/fixtures/sample_with_table.pdf && test -f tests/fixtures/sample_with_figure.pdf && test -f tests/fixtures/generate_fixtures.py && grep -q "TestClient" tests/conftest.py && grep -q "test_table_html_preserved" tests/test_table_figure.py && grep -q "test_figure_caption_marker" tests/test_table_figure.py && grep -q "test_504_when_timeout" tests/test_limits.py && grep -q "test_request_id_propagated_to_log" tests/test_logging.py && grep -q "EXPECTED_VI_TOKENS" tests/test_ocr.py</automated>
  </verify>
  <done>6 test file + conftest + 5 fixture + script generate exist; pytest collect ≥ 12 test case (chưa cần all pass — runtime verify ở Plan 08).</done>
</task>

</tasks>

<verification>
- `cd docling-pipeline && pytest --collect-only` liệt kê ≥ 12 test.
- Nếu Docling đã pip install + Tesseract có trong env: `pytest -m "not slow"` pass tất cả test fast (≤ 30s) — gồm health, extract schema, request_id propagate (W4), 413 + 504 limits (B4), unsupported extension.
- `pytest -m slow` pass nếu có Tesseract vie+eng — gồm OCR test + table_html test (B3) + figure caption test (B3).
- Coverage `pytest --cov=docling_pipeline --cov-report=term-missing` ≥ 60%.
</verification>

<success_criteria>
- 6 test file + conftest + 5 fixture + script generate commit.
- ≥ 12 test case (2 health + 4 extract + 1 OCR + 2 table/figure (B3) + 2 limits (B4) + 1 logging (W4)).
- B3: test_table_html_preserved assert `<table>` + `<thead>`/`<tbody>` trong table_html; test_figure_caption_marker assert pattern `![<caption>](#fig-N)`.
- B4: test_504_when_timeout dùng monkeypatch slow extract + DOCLING_REQUEST_TIMEOUT_SEC=1, assert 504.
- W4: test_request_id_propagated_to_log dùng capsys assert rid xuất hiện trong stdout JSON structlog.
- Marker `@pytest.mark.slow` cho test OCR + test_table_figure (cho phép skip ở fast loop).
- Fixture deterministic (I2): 4 PDF/DOCX commit binary < 50KB mỗi file, sinh từ generate_fixtures.py 1 lần — KHÔNG sinh runtime.
</success_criteria>

<output>
Tạo `02-07-SUMMARY.md` ghi nhận: test scaffold xong với 6 test file (revision B3 + B4 + W4 + I2 đã đóng). Marker slow tách OCR + table/figure test. 5 fixture commit deterministic. Note: nếu CI environment chưa có Tesseract vie+eng, OCR test sẽ fail — Plan 08 README hướng dẫn install hoặc skip qua marker.
</output>
