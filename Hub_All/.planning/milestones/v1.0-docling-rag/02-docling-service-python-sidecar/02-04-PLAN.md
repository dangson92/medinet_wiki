---
phase: 02-docling-service-python-sidecar
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docling-pipeline/src/docling_pipeline/core/chunker.py
autonomous: true
requirements:
  - CHUNK-01
  - CHUNK-02
  - CHUNK-04
must_haves:
  truths:
    - "Hàm chunk_document(doc, options) -> list[DocChunk] hoạt động với HybridChunker Docling"
    - "Tokenizer mặc định cl100k_base (env DOCLING_TOKENIZER_NAME), override per-request qua chunker_options.tokenizer_name (CHUNK-01)"
    - "HybridChunker khởi tạo qua string overload tokenizer='cl100k_base' (B2 — KHÔNG import OpenAITokenizer manual để tránh API drift Docling 2.91 patch)"
    - "Default params merge_peers=True, repeat_table_header=True, omit_header_on_overflow=False (CHUNK-02)"
    - "KHÔNG augment Q&A / keyword trong service Python — giữ chunks pristine (CHUNK-04)"
    - "Trả về list DocChunk Docling raw (chưa serialize sang response schema — Plan 05 làm)"
    - "Task 0 verify-import chạy đầu Task 1: nếu HybridChunker không accept tokenizer string → fail loud, KHÔNG fallback silent"
  artifacts:
    - path: "docling-pipeline/src/docling_pipeline/core/chunker.py"
      provides: "Wrapper HybridChunker với tokenizer string overload + per-request override"
      exports: ["DoclingChunker", "ChunkerOptions", "get_chunker", "chunk_document"]
      min_lines: 70
  key_links:
    - from: "chunker.py"
      to: "Docling HybridChunker (tiktoken nội bộ qua string overload)"
      via: "HybridChunker(tokenizer='cl100k_base', max_tokens=512)"
      pattern: "HybridChunker"
---

<objective>
Tạo wrapper Python `core/chunker.py` bọc Docling `HybridChunker` — nhận `DoclingDocument` từ Plan 03, trả list `DocChunk` Docling. Tokenizer default `cl100k_base` (cho OpenAI text-embedding-3-*), override per-request qua request body field `chunker_options.tokenizer_name`.

Purpose: CHUNK-01..04. HybridChunker đã làm hết logic merge_peers/header propagation; wrapper chỉ cần (a) build chunker với tokenizer string, (b) per-request rebuild khi options khác default, (c) export `chunk_document(doc, opts)` clean signature.

**Revision B2:** Thay vì import `OpenAITokenizer` từ `docling_core.transforms.chunker.tokenizer.openai` (đường này drift giữa Docling 2.91 patch — đôi khi `.tiktoken`, đôi khi `.openai`), dùng overload đơn giản `HybridChunker(tokenizer="cl100k_base")` — Docling 2.x accept string và resolve tiktoken nội bộ. Stable hơn, không bị break khi Docling tái cấu trúc namespace nội bộ.

Output: `chunker.py` ~70-130 dòng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docling-service-python-sidecar/02-CONTEXT.md
@.planning/phases/02-docling-service-python-sidecar/02-01-PLAN.md

<interfaces>
From docling-pipeline/src/docling_pipeline/config.py:
```python
class Settings(BaseSettings):
    tokenizer_name: str = "cl100k_base"
    max_tokens_per_chunk: int = 512
```

Docling 2.91 chunking API (B2 revision — string overload):
```python
from docling.chunking import HybridChunker
from docling_core.transforms.chunker import DocChunk

# B2: string overload — Docling resolve tiktoken nội bộ
chunker = HybridChunker(
    tokenizer="cl100k_base",
    max_tokens=512,
    merge_peers=True,
)
chunks: Iterable[DocChunk] = chunker.chunk(dl_doc=doc)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Verify Docling chunker import + string overload (B2 fail-loud guard)</name>
  <files>(không tạo file — chỉ chạy lệnh verify)</files>
  <action>
**Mục đích:** Trước khi viết chunker.py, verify rằng Docling 2.91.0 (đã pin exact ở Plan 01) thật sự expose `HybridChunker` qua `docling.chunking` VÀ accept string overload `tokenizer="cl100k_base"`. Nếu API drift giữa các patch của 2.91 → fail loud ngay, KHÔNG silent fallback sang import path khác.

**Lệnh chạy (executor PHẢI chạy trước khi proceed Task 1):**

```bash
cd docling-pipeline
pip install -e .[dev]   # cài docling==2.91.0 + tiktoken (đã pin Plan 01)

# Verify 1: import HybridChunker từ public namespace
python -c "from docling.chunking import HybridChunker; print('import OK:', HybridChunker)"

# Verify 2: string overload tokenizer='cl100k_base' khởi tạo được
python -c "from docling.chunking import HybridChunker; c = HybridChunker(tokenizer='cl100k_base', max_tokens=512); print('string overload OK:', type(c).__name__)"
```

**Expected output:** Cả 2 lệnh in ra OK message + class name (không raise).

**Nếu fail (B2 trigger):**
- ImportError trên Verify 1 → Docling 2.91.0 không expose `docling.chunking.HybridChunker` → STOP plan, escalate planner để chốt lại version.
- TypeError / ValueError trên Verify 2 (string overload không accept) → STOP plan, escalate planner để chuyển sang import object Tokenizer manual (path cụ thể cần verify thêm).

KHÔNG được "câm lặng" sửa import path khác — fail loud để user thấy API drift sớm.
  </action>
  <verify>
    <automated>cd docling-pipeline && pip install -e .[dev] >/dev/null 2>&1 && python -c "from docling.chunking import HybridChunker; HybridChunker(tokenizer='cl100k_base', max_tokens=512)"</automated>
  </verify>
  <done>2 lệnh python verify pass — HybridChunker import OK + string overload accept 'cl100k_base'. Sẵn sàng viết chunker.py.</done>
</task>

<task type="auto">
  <name>Task 1: Tạo core/chunker.py — wrapper HybridChunker với string overload</name>
  <files>docling-pipeline/src/docling_pipeline/core/chunker.py</files>
  <action>
```python
"""Wrapper Docling HybridChunker với tokenizer string overload + per-request override.

Trách nhiệm:
1. Build HybridChunker với tokenizer string ('cl100k_base' default — Docling 2.x resolve
   tiktoken nội bộ; tránh import OpenAITokenizer manual để khỏi drift API).
2. Per-request override qua ChunkerOptions (CHUNK-01 + CHUNK-02).
3. KHÔNG augment Q&A/keyword (CHUNK-04 — giữ pristine, augmenter Go xử sau).

Tham chiếu:
- CONTEXT.md mục C (tokenizer chốt cl100k_base).
- CHUNK-01..04 trong REQUIREMENTS.md.
- Revision B2: dùng string overload thay vì import OpenAITokenizer manual.
"""

from __future__ import annotations

from dataclasses import dataclass
from functools import lru_cache
from typing import Any

import structlog
from docling.chunking import HybridChunker
from docling_core.transforms.chunker import DocChunk
from docling_core.types.doc import DoclingDocument

from docling_pipeline.config import Settings, get_settings

logger = structlog.get_logger(__name__)

# Whitelist tokenizer name — chỉ accept tiktoken encodings Docling resolve được nội bộ.
# CONTEXT.md mục C: cl100k_base default cho OpenAI text-embedding-3-*; cũng dùng cho Gemini
# làm workaround sai số ±10%. HuggingFace tokenizer (Gemini native) defer M3.
_TIKTOKEN_NAMES = {"cl100k_base", "p50k_base", "p50k_edit", "r50k_base", "o200k_base"}


@dataclass(frozen=True)
class ChunkerOptions:
    """Per-request chunker overrides (CHUNK-02 — admin có thể truyền qua body)."""

    tokenizer_name: str | None = None
    max_tokens: int | None = None
    merge_peers: bool = True
    # repeat_table_header + omit_header_on_overflow là default Docling — không expose
    # cho client trừ khi cần tinh chỉnh; CONTEXT mục C giữ default.

    @classmethod
    def from_dict(cls, data: dict[str, Any] | None) -> ChunkerOptions:
        if not data:
            return cls()
        return cls(
            tokenizer_name=data.get("tokenizer_name"),
            max_tokens=data.get("max_tokens"),
            merge_peers=bool(data.get("merge_peers", True)),
        )


def _validate_tokenizer_name(name: str) -> str:
    """Validate tokenizer name — chỉ accept tiktoken whitelist. Fail loud nếu unknown."""
    if name not in _TIKTOKEN_NAMES:
        raise ValueError(
            f"unsupported tokenizer_name {name!r} — currently support: {sorted(_TIKTOKEN_NAMES)}"
        )
    return name


class DoclingChunker:
    """Wrapper HybridChunker — single instance reuse cho default config.

    Per-request override sẽ build chunker mới on-demand (rare path).
    """

    def __init__(self, settings: Settings | None = None) -> None:
        self.settings = settings or get_settings()
        self._default_chunker = self._build_chunker(
            tokenizer_name=self.settings.tokenizer_name,
            max_tokens=self.settings.max_tokens_per_chunk,
            merge_peers=True,
        )

    def _build_chunker(
        self,
        tokenizer_name: str,
        max_tokens: int,
        merge_peers: bool,
    ) -> HybridChunker:
        # B2: string overload — Docling 2.x resolve tiktoken nội bộ.
        # KHÔNG import OpenAITokenizer / BaseTokenizer manual (path drift giữa các patch 2.91.x).
        validated = _validate_tokenizer_name(tokenizer_name)
        return HybridChunker(
            tokenizer=validated,
            max_tokens=max_tokens,
            merge_peers=merge_peers,
        )

    def chunk(
        self, doc: DoclingDocument, options: ChunkerOptions | None = None
    ) -> list[DocChunk]:
        """Chunk DoclingDocument → list DocChunk Docling raw (chưa serialize)."""
        options = options or ChunkerOptions()

        # Hot path: dùng default chunker khi options không override
        if (
            options.tokenizer_name is None
            and options.max_tokens is None
            and options.merge_peers is True
        ):
            chunker = self._default_chunker
            tok_name = self.settings.tokenizer_name
            mt = self.settings.max_tokens_per_chunk
        else:
            tok_name = options.tokenizer_name or self.settings.tokenizer_name
            mt = options.max_tokens or self.settings.max_tokens_per_chunk
            chunker = self._build_chunker(tok_name, mt, options.merge_peers)

        logger.info(
            "chunk_start",
            tokenizer=tok_name,
            max_tokens=mt,
            merge_peers=options.merge_peers,
        )
        chunks = list(chunker.chunk(dl_doc=doc))
        logger.info("chunk_done", chunks=len(chunks))
        return chunks


@lru_cache(maxsize=1)
def get_chunker() -> DoclingChunker:
    return DoclingChunker()


def chunk_document(
    doc: DoclingDocument, options: ChunkerOptions | None = None
) -> list[DocChunk]:
    """Convenience entry."""
    return get_chunker().chunk(doc, options)
```

**Lưu ý B2:** KHÔNG thêm `import OpenAITokenizer` / `BaseTokenizer` / `tiktoken` trực tiếp. Toàn bộ tokenization ủy thác cho Docling qua string. Nếu sau này Docling đổi API string overload → revisit theo CHECK-list.

**Verify cuối Task 1 (B2 guard lần 2):**
```bash
python -c "from docling_pipeline.core.chunker import get_chunker; c = get_chunker(); print('chunker built:', c.settings.tokenizer_name)"
# Expected: "chunker built: cl100k_base"
```
Nếu fail → API drift đã xảy ra giữa Task 0 và Task 1 (rất hiếm); rollback và escalate.
  </action>
  <verify>
    <automated>test -f docling-pipeline/src/docling_pipeline/core/chunker.py && grep -q "class DoclingChunker" docling-pipeline/src/docling_pipeline/core/chunker.py && grep -q "HybridChunker" docling-pipeline/src/docling_pipeline/core/chunker.py && grep -q "cl100k_base" docling-pipeline/src/docling_pipeline/core/chunker.py && grep -q "merge_peers" docling-pipeline/src/docling_pipeline/core/chunker.py && grep -q "ChunkerOptions" docling-pipeline/src/docling_pipeline/core/chunker.py && ! grep -q "OpenAITokenizer" docling-pipeline/src/docling_pipeline/core/chunker.py && python -c "from docling.chunking import HybridChunker; HybridChunker(tokenizer='cl100k_base')"</automated>
  </verify>
  <done>chunker.py có DoclingChunker + ChunkerOptions, dùng HybridChunker string overload (KHÔNG import OpenAITokenizer manual), default cl100k_base, per-request override hoạt động. Verify import chạy clean.</done>
</task>

</tasks>

<verification>
- `python -c "from docling_pipeline.core.chunker import get_chunker, ChunkerOptions; print(get_chunker().settings.tokenizer_name)"` in `cl100k_base`.
- `from_dict` parse JSON dict thành ChunkerOptions không lỗi.
- `! grep -q "from docling_core.transforms.chunker.tokenizer" chunker.py` — xác nhận KHÔNG import path tokenizer nội bộ (B2).
- KHÔNG verify chunk thật (cần DoclingDocument fixture — Plan 07).
</verification>

<success_criteria>
- chunker.py ≥ 70 dòng.
- Default tokenizer cl100k_base, default merge_peers=True, max_tokens 512.
- Per-request override qua ChunkerOptions hoạt động (build chunker mới khi cần).
- KHÔNG import OpenAITokenizer / BaseTokenizer manual (B2 — string overload duy nhất).
- KHÔNG có code augment Q&A / keyword (CHUNK-04 enforcement).
- _validate_tokenizer_name raise ValueError cho name unknown (fail-loud, không silent fallback).
</success_criteria>

<output>
Tạo `02-04-SUMMARY.md` ghi nhận: chunker wrapper xong với string overload (revision B2), Task 0 verify-import pass, hot path dùng singleton default, override path build mới on-demand. Note: HuggingFace tokenizer (Gemini native) defer M3 theo CONTEXT mục C.
</output>
</content>
