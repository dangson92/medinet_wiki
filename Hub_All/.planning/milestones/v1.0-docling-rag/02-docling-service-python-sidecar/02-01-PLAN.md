---
phase: 02-docling-service-python-sidecar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docling-pipeline/Dockerfile
  - docling-pipeline/pyproject.toml
  - docling-pipeline/.gitignore
  - docling-pipeline/.env.example
  - docling-pipeline/src/docling_pipeline/__init__.py
  - docling-pipeline/src/docling_pipeline/config.py
  - docling-pipeline/src/docling_pipeline/api/__init__.py
  - docling-pipeline/src/docling_pipeline/core/__init__.py
  - docling-pipeline/src/docling_pipeline/observability/__init__.py
  - docling-pipeline/tests/__init__.py
  - docling-pipeline/tests/fixtures/.gitkeep
autonomous: true
requirements:
  - DSVC-05
  - DSVC-06
must_haves:
  truths:
    - "Thư mục docling-pipeline/ tồn tại với layout đúng spec CONTEXT mục Cấu trúc thư mục"
    - "Dockerfile build được CPU-only image base python:3.11-slim-bookworm + tesseract vie+eng"
    - "pyproject.toml pin chính xác docling==2.91.0 (exact, KHÔNG wildcard) + fastapi + uvicorn[standard] + python-multipart + structlog + tiktoken + pydantic-settings"
    - "config.py expose Settings class load từ env (DOCLING_OCR_ENGINE, DOCLING_OCR_LANGS, DOCLING_TOKENIZER_NAME, DOCLING_MAX_FILE_MB, DOCLING_REQUEST_TIMEOUT_SEC, DOCLING_LOG_LEVEL)"
    - "Wave 2 dependency gate (W3): core/__init__.py + api/__init__.py + observability/__init__.py phải tồn tại sau Plan 01 để 3 plan core 03/04/05 cùng tạo file con (extractor.py / chunker.py / serializer.py) song song KHÔNG conflict cấu trúc package"
  artifacts:
    - path: "docling-pipeline/Dockerfile"
      provides: "Build instructions service Python sidecar"
      contains: "tesseract-ocr-vie"
    - path: "docling-pipeline/pyproject.toml"
      provides: "Python dependencies pinned (docling exact version)"
      contains: "docling==2.91.0"
    - path: "docling-pipeline/src/docling_pipeline/config.py"
      provides: "Settings load từ env qua pydantic-settings"
      exports: ["Settings", "get_settings"]
    - path: "docling-pipeline/src/docling_pipeline/core/__init__.py"
      provides: "Package marker rỗng — gate cho Wave 2 (3 plan 03/04/05 cùng tạo file con)"
    - path: "docling-pipeline/src/docling_pipeline/api/__init__.py"
      provides: "Package marker rỗng — gate cho Plan 06 (api/health.py + api/process.py)"
    - path: "docling-pipeline/src/docling_pipeline/observability/__init__.py"
      provides: "Package marker rỗng — gate cho Plan 06 (observability/logging.py)"
    - path: "docling-pipeline/.env.example"
      provides: "Mẫu env vars cho dev local"
    - path: "docling-pipeline/.gitignore"
      provides: "Loại trừ __pycache__, .ruff_cache, *.egg-info, docling_models/, .venv"
  key_links:
    - from: "Dockerfile"
      to: "pyproject.toml"
      via: "COPY pyproject.toml + pip install ."
      pattern: "pip install"
    - from: "config.py"
      to: "env vars (.env.example)"
      via: "pydantic_settings.BaseSettings"
      pattern: "BaseSettings"
---

<objective>
Dựng skeleton thư mục `docling-pipeline/` (Python service sidecar) với Dockerfile CPU-only, pyproject.toml deps pinned (docling==2.91.0 exact), config.py load env, và toàn bộ cấu trúc src/tests trống có `__init__.py`. Đây là foundation cho mọi plan sau (Wave 2 trở đi sẽ fill core logic + API).

Purpose: Không có skeleton thì các plan core/API ở Wave 2-3 không có chỗ đặt code. Dockerfile + pyproject phải đúng pin ngay từ đầu để tránh rework toàn bộ image build sau này. Pin docling exact (2.91.0) thay vì wildcard (2.91.*) để khoá patch level — tránh API drift bất ngờ giữa các minor patch (xem B2 trong 02-CHECK).
Output: Folder `docling-pipeline/` đầy đủ skeleton commit được, image build thành công lần đầu (chậm do tải models — có thể skip nếu mạng yếu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-docling-service-python-sidecar/02-CONTEXT.md
@eval/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo Dockerfile CPU-only + pyproject.toml + .gitignore + .env.example</name>
  <files>docling-pipeline/Dockerfile, docling-pipeline/pyproject.toml, docling-pipeline/.gitignore, docling-pipeline/.env.example</files>
  <action>
Tạo 4 file:

**`docling-pipeline/Dockerfile`** (single-stage, CPU-only):
```dockerfile
FROM python:3.11-slim-bookworm

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

# OCR + binary deps cho pillow/pdf2image (CONTEXT mục Rủi ro 3)
RUN apt-get update && apt-get install -y --no-install-recommends \
      tesseract-ocr \
      tesseract-ocr-vie \
      tesseract-ocr-eng \
      libpoppler-cpp-dev \
      poppler-utils \
      libjpeg-dev \
      zlib1g-dev \
      libgl1 \
      libglib2.0-0 \
      curl \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY pyproject.toml ./
COPY src/ ./src/

RUN pip install --upgrade pip setuptools wheel \
    && pip install .

# Volume mount điểm cache HuggingFace models (CONTEXT mục B)
VOLUME ["/root/.cache/docling"]

EXPOSE 8001

# Single uvicorn worker (Docling KHÔNG thread-safe, CONTEXT mục D)
CMD ["uvicorn", "docling_pipeline.main:app", "--host", "0.0.0.0", "--port", "8001", "--workers", "1"]
```

**`docling-pipeline/pyproject.toml`** (PEP 621, pin exact version per CONTEXT mục A + revision B2):
```toml
[project]
name = "docling-pipeline"
version = "0.1.0"
description = "Medinet Docling sidecar — extract + chunk Python service cho RAG ingestion"
requires-python = ">=3.11,<3.12"
dependencies = [
    "docling==2.91.0",
    "fastapi>=0.115,<0.116",
    "uvicorn[standard]>=0.32,<0.33",
    "python-multipart>=0.0.12",
    "structlog>=24.4",
    "tiktoken>=0.8",
    "pydantic-settings>=2.6",
    "pydantic>=2.9,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.7",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
```

**Lưu ý revision B2:** `docling==2.91.0` exact (KHÔNG `2.91.*`) — Docling minor patch đôi khi đổi import path tokenizer giữa 2.91.0 và 2.91.x; pin patch level để executor luôn test trên cùng API surface đã verify ở Plan 04 Task 0.

**`docling-pipeline/.gitignore`**:
```
__pycache__/
*.py[cod]
*.egg-info/
.ruff_cache/
.pytest_cache/
.mypy_cache/
.venv/
venv/
docling_models/
*.log
.coverage
htmlcov/
```

**`docling-pipeline/.env.example`** (toàn bộ env vars CONTEXT chốt):
```env
# ─── OCR ───
DOCLING_OCR_ENGINE=tesseract            # tesseract | rapidocr
DOCLING_OCR_LANGS=vie+eng

# ─── Tokenizer cho HybridChunker ───
DOCLING_TOKENIZER_NAME=cl100k_base      # default OpenAI text-embedding-3-*
DOCLING_MAX_TOKENS_PER_CHUNK=512

# ─── Limits ───
DOCLING_MAX_FILE_MB=50                  # 413 nếu vượt
DOCLING_REQUEST_TIMEOUT_SEC=180         # 504 nếu vượt

# ─── Logging ───
DOCLING_LOG_LEVEL=INFO                  # DEBUG | INFO | WARNING | ERROR
DOCLING_LOG_FORMAT=json                 # json | console

# ─── Service ───
DOCLING_HOST=0.0.0.0
DOCLING_PORT=8001
```
  </action>
  <verify>
    <automated>test -f docling-pipeline/Dockerfile && test -f docling-pipeline/pyproject.toml && grep -q "tesseract-ocr-vie" docling-pipeline/Dockerfile && grep -q "docling==2.91.0" docling-pipeline/pyproject.toml && ! grep -q "docling==2.91\.\*" docling-pipeline/pyproject.toml && grep -q "DOCLING_OCR_LANGS=vie+eng" docling-pipeline/.env.example</automated>
  </verify>
  <done>4 file tồn tại, Dockerfile có tesseract-ocr-vie, pyproject pin docling==2.91.0 exact (không wildcard), .env.example có đủ 4 nhóm env (OCR/tokenizer/limits/logging).</done>
</task>

<task type="auto">
  <name>Task 2: Tạo skeleton src/ + tests/ + config.py load env</name>
  <files>docling-pipeline/src/docling_pipeline/__init__.py, docling-pipeline/src/docling_pipeline/config.py, docling-pipeline/src/docling_pipeline/api/__init__.py, docling-pipeline/src/docling_pipeline/core/__init__.py, docling-pipeline/src/docling_pipeline/observability/__init__.py, docling-pipeline/tests/__init__.py, docling-pipeline/tests/fixtures/.gitkeep</files>
  <action>
Tạo cấu trúc package + config:

**`src/docling_pipeline/__init__.py`**:
```python
"""Medinet Docling sidecar — extract + chunk service."""

__version__ = "0.1.0"
```

**`src/docling_pipeline/api/__init__.py`** + **`core/__init__.py`** + **`observability/__init__.py`**: file rỗng (chỉ marker package).

> **W3 — Wave 2 dependency gate:** 3 file `__init__.py` (api / core / observability) **PHẢI** tồn tại sau Plan 01 trước khi Wave 2 chạy. 3 plan core (02-03 extractor, 02-04 chunker, 02-05 serializer) sẽ cùng ghi file con vào `core/`; gate này đảm bảo Python coi `core/` là package hợp lệ ngay từ commit đầu, tránh ImportError khi Wave 2 plan chạy song song.

**`src/docling_pipeline/config.py`** (pydantic-settings load env):
```python
"""Settings load từ env vars qua pydantic-settings.

Đọc tất cả env DOCLING_* và provide qua get_settings() lru_cache.
Tham chiếu CONTEXT.md mục D + E + Schema response cho semantics từng field.
"""

from __future__ import annotations

from functools import lru_cache
from typing import Literal

from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """Cấu hình runtime — đọc từ env DOCLING_* (case-insensitive)."""

    model_config = SettingsConfigDict(
        env_prefix="DOCLING_",
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=False,
        extra="ignore",
    )

    # ─── OCR ───
    ocr_engine: Literal["tesseract", "rapidocr"] = "tesseract"
    ocr_langs: str = "vie+eng"

    # ─── Tokenizer cho HybridChunker ───
    tokenizer_name: str = "cl100k_base"
    max_tokens_per_chunk: int = Field(default=512, gt=0)

    # ─── Limits ───
    max_file_mb: int = Field(default=50, gt=0)
    request_timeout_sec: int = Field(default=180, gt=0)

    # ─── Logging ───
    log_level: Literal["DEBUG", "INFO", "WARNING", "ERROR"] = "INFO"
    log_format: Literal["json", "console"] = "json"

    # ─── Service ───
    host: str = "0.0.0.0"
    port: int = 8001

    @property
    def max_file_bytes(self) -> int:
        return self.max_file_mb * 1024 * 1024


@lru_cache(maxsize=1)
def get_settings() -> Settings:
    """Singleton settings — cache cho hot path."""
    return Settings()
```

**`tests/__init__.py`**: rỗng.
**`tests/fixtures/.gitkeep`**: rỗng (placeholder để git track folder, fixture file thật sẽ thêm ở Plan 07).
  </action>
  <verify>
    <automated>test -f docling-pipeline/src/docling_pipeline/config.py && test -f docling-pipeline/src/docling_pipeline/api/__init__.py && test -f docling-pipeline/src/docling_pipeline/core/__init__.py && test -f docling-pipeline/src/docling_pipeline/observability/__init__.py && grep -q "class Settings" docling-pipeline/src/docling_pipeline/config.py && grep -q "ocr_langs" docling-pipeline/src/docling_pipeline/config.py</automated>
  </verify>
  <done>Cấu trúc thư mục đầy đủ theo CONTEXT, config.py có class Settings load env, get_settings() cached. 3 package marker (api/core/observability) sẵn sàng cho Wave 2 + Plan 06.</done>
</task>

</tasks>

<verification>
- `tree docling-pipeline/` (hoặc `find docling-pipeline -type f`) hiển thị đủ Dockerfile + pyproject + .env.example + .gitignore + cấu trúc src/docling_pipeline/{api,core,observability}/__init__.py + tests/.
- `docker build -t docling-pipeline:dev docling-pipeline/` thành công (tốn 5-10 phút lần đầu — có thể defer verify này nếu mạng yếu, vì image build sẽ verify lại ở Plan 08 smoke).
- KHÔNG cần chạy container ở plan này.
</verification>

<success_criteria>
- 4 file Dockerfile/pyproject/.gitignore/.env.example commit được, đúng pin exact docling==2.91.0/env.
- 7 file Python skeleton + .gitkeep commit được.
- `python -c "from docling_pipeline.config import get_settings; print(get_settings().ocr_langs)"` (sau khi `pip install -e docling-pipeline[dev]`) in `vie+eng`.
- KHÔNG có code core/API logic ở plan này — chỉ skeleton.
</success_criteria>

<output>
Sau khi xong, tạo `.planning/phases/02-docling-service-python-sidecar/02-01-SUMMARY.md` ghi nhận: skeleton tạo xong, Dockerfile CPU-only đã pin Tesseract vie+eng, pyproject pin docling==2.91.0 exact (revision B2), env vars chốt ở .env.example. 3 package marker sẵn sàng cho Wave 2 (W3 gate). Note rủi ro: image build lần đầu chậm (~5-10 phút) — defer verify build sang Plan 08.
</output>
</content>
</invoke>