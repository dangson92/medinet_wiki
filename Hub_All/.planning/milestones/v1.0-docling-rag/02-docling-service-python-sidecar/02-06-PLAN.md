---
phase: 02-docling-service-python-sidecar
plan: 06
type: execute
wave: 3
depends_on: [01, 03, 04, 05]
files_modified:
  - docling-pipeline/src/docling_pipeline/main.py
  - docling-pipeline/src/docling_pipeline/api/health.py
  - docling-pipeline/src/docling_pipeline/api/process.py
  - docling-pipeline/src/docling_pipeline/observability/logging.py
autonomous: true
requirements:
  - DSVC-01
  - DSVC-04
  - DSVC-06
  - CHUNK-03
must_haves:
  truths:
    - "GET /healthz trả 200 ngay (liveness — không check models warm)"
    - "GET /readyz trả 200 sau khi Docling library import OK + warm dummy doc xong (readiness)"
    - "GET /readyz trả 503 chỉ khi Docling library KHÔNG cài được (ImportError) — semantics: ready=False vĩnh viễn cho đến khi user fix env"
    - "Lifespan B5 phân biệt 3 case: (a) ImportError docling → ready=False; (b) warm dummy fail (transient) → ready=True + log warning; (c) warm OK → ready=True + log info"
    - "POST /v1/process accept multipart/form-data với field file (binary) + metadata fields (hub_code, doc_type, request_id) + chunker_options JSON optional"
    - "POST /v1/process trả JSON {request_id, doc_meta, chunks: [...]} đúng schema DSVC-02"
    - "Vượt DOCLING_MAX_FILE_MB → trả 413 Payload Too Large với JSON {error, max_mb}"
    - "Vượt DOCLING_REQUEST_TIMEOUT_SEC → trả 504 Gateway Timeout (asyncio.timeout wrap)"
    - "X-Request-Id header propagate vào structlog context, xuất hiện trong mọi log line JSON"
  artifacts:
    - path: "docling-pipeline/src/docling_pipeline/main.py"
      provides: "FastAPI app + lifespan warm models (B5 semantics) + register router + middleware logging"
      exports: ["app", "create_app"]
      min_lines: 60
    - path: "docling-pipeline/src/docling_pipeline/api/health.py"
      provides: "/healthz + /readyz endpoints"
      exports: ["router", "set_models_ready"]
    - path: "docling-pipeline/src/docling_pipeline/api/process.py"
      provides: "/v1/process multipart upload endpoint"
      exports: ["router"]
    - path: "docling-pipeline/src/docling_pipeline/observability/logging.py"
      provides: "structlog JSON config + request_id contextvar + middleware"
      exports: ["configure_logging", "RequestIdMiddleware"]
  key_links:
    - from: "process.py"
      to: "extractor.get_extractor() + chunker.chunk_document() + serializer.serialize_chunks()"
      via: "import + sequential call trong handler"
      pattern: "get_extractor\\(\\).extract"
    - from: "main.py lifespan"
      to: "extractor.warm_models() + ImportError guard"
      via: "try import docling → asyncio.to_thread(warm) → set_models_ready(bool)"
      pattern: "ImportError"
    - from: "RequestIdMiddleware"
      to: "structlog contextvars"
      via: "structlog.contextvars.bind_contextvars(request_id=...)"
      pattern: "bind_contextvars"
---

<objective>
Wire core (extractor + chunker + serializer từ Plan 03/04/05) sau lưng API FastAPI: 3 endpoint `GET /healthz`, `GET /readyz`, `POST /v1/process`. Setup structlog JSON + propagate `X-Request-Id` từ Go (DSVC-04). Enforce limits 413/504 (DSVC-06). Lifespan warm Docling models theo **B5 semantics**: phân biệt ImportError (ready=False vĩnh viễn) vs warm transient fail (ready=True, log warning).

Purpose: Đây là plan "wire" — không có core logic mới, chỉ glue 3 module core thành HTTP API đúng contract DSVC-01..06. Sau plan này service đã chạy được local (`uvicorn docling_pipeline.main:app`).

**Revision B5:** Lifespan cũ luôn `set_models_ready(True)` dù warm fail → /readyz mất ý nghĩa readiness. Bản revise phân biệt 3 nhánh rõ ràng — ImportError thì ready=False vĩnh viễn (env hỏng), còn warm dummy fail (transient, ví dụ stub PDF không trigger model load) thì ready=True (service vẫn serve được, request đầu sẽ chậm).

Output: 4 file Python, service start được `uvicorn docling_pipeline.main:app --port 8001`, /healthz trả 200 ngay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docling-service-python-sidecar/02-CONTEXT.md
@.planning/phases/02-docling-service-python-sidecar/02-01-PLAN.md
@.planning/phases/02-docling-service-python-sidecar/02-03-PLAN.md
@.planning/phases/02-docling-service-python-sidecar/02-04-PLAN.md
@.planning/phases/02-docling-service-python-sidecar/02-05-PLAN.md

<interfaces>
From Plan 03 (extractor.py):
  - get_extractor() -> DoclingExtractor
  - DoclingExtractor.extract(file_bytes: bytes, filename: str) -> DoclingDocument
  - DoclingExtractor.warm_models() -> None
  - warm_models() -> None  (module-level convenience)

From Plan 04 (chunker.py):
  - get_chunker() -> DoclingChunker
  - DoclingChunker.chunk(doc, options: ChunkerOptions | None) -> list[DocChunk]
  - ChunkerOptions.from_dict(data: dict | None) -> ChunkerOptions

From Plan 05 (serializer.py):
  - build_doc_meta(doc, filename, ocr_used, language_detected=None) -> DocMetaDict
  - serialize_chunks(doc, chunks, tokenizer_name="cl100k_base") -> list[ChunkDict]

From Plan 01 (config.py):
  - get_settings() -> Settings (max_file_bytes, request_timeout_sec, tokenizer_name, ocr_engine)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: observability/logging.py + main.py (B5 lifespan) + 2 router files</name>
  <files>docling-pipeline/src/docling_pipeline/observability/logging.py, docling-pipeline/src/docling_pipeline/main.py, docling-pipeline/src/docling_pipeline/api/health.py, docling-pipeline/src/docling_pipeline/api/process.py</files>
  <action>
**`observability/logging.py`** (structlog JSON + RequestIdMiddleware):

```python
"""Structlog JSON config + propagate X-Request-Id từ Go (DSVC-04)."""

from __future__ import annotations

import logging
import sys
import uuid
from typing import Any

import structlog
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import Response

from docling_pipeline.config import get_settings


def configure_logging() -> None:
    settings = get_settings()
    level = getattr(logging, settings.log_level, logging.INFO)

    shared_processors: list[Any] = [
        structlog.contextvars.merge_contextvars,
        structlog.processors.add_log_level,
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.processors.StackInfoRenderer(),
        structlog.processors.format_exc_info,
    ]

    if settings.log_format == "json":
        renderer = structlog.processors.JSONRenderer()
    else:
        renderer = structlog.dev.ConsoleRenderer()

    structlog.configure(
        processors=[*shared_processors, renderer],
        wrapper_class=structlog.make_filtering_bound_logger(level),
        context_class=dict,
        logger_factory=structlog.PrintLoggerFactory(file=sys.stdout),
        cache_logger_on_first_use=True,
    )

    # Bridge stdlib logging → structlog format
    logging.basicConfig(format="%(message)s", level=level, handlers=[logging.StreamHandler(sys.stdout)])


class RequestIdMiddleware(BaseHTTPMiddleware):
    """Bind X-Request-Id từ header vào structlog contextvars (DSVC-04)."""

    HEADER = "X-Request-Id"

    async def dispatch(self, request: Request, call_next):
        rid = request.headers.get(self.HEADER) or str(uuid.uuid4())
        structlog.contextvars.clear_contextvars()
        structlog.contextvars.bind_contextvars(request_id=rid, path=request.url.path)
        try:
            response: Response = await call_next(request)
        finally:
            structlog.contextvars.clear_contextvars()
        response.headers[self.HEADER] = rid
        return response
```

**`api/health.py`**:

```python
"""GET /healthz (liveness) + GET /readyz (readiness — Docling library + models warm)."""

from __future__ import annotations

from fastapi import APIRouter, Response

router = APIRouter(tags=["health"])

# Module-level state — set qua set_models_ready() từ lifespan (Plan 06 main.py)
# B5 semantics: False = Docling library KHÔNG cài được (ImportError). User phải fix env.
#               True  = Docling import OK (warm có thể fail soft, vẫn ready=True).
_models_ready: bool = False


def set_models_ready(ready: bool) -> None:
    global _models_ready
    _models_ready = ready


@router.get("/healthz")
def healthz() -> dict[str, str]:
    """Liveness — luôn trả OK nếu process còn sống."""
    return {"status": "ok"}


@router.get("/readyz")
def readyz(response: Response) -> dict[str, str]:
    """Readiness — chỉ OK khi Docling library đã import OK (B5)."""
    if not _models_ready:
        response.status_code = 503
        return {"status": "not_ready", "reason": "docling_library_unavailable"}
    return {"status": "ready"}
```

**`api/process.py`** (DSVC-01 + DSVC-06 + CHUNK-03):

```python
"""POST /v1/process — multipart upload, trả schema DSVC-02."""

from __future__ import annotations

import asyncio
import json
import uuid
from typing import Any

import structlog
from fastapi import APIRouter, Form, HTTPException, Request, UploadFile

from docling_pipeline.config import get_settings
from docling_pipeline.core.chunker import ChunkerOptions, get_chunker
from docling_pipeline.core.extractor import get_extractor
from docling_pipeline.core.serializer import build_doc_meta, serialize_chunks

router = APIRouter(prefix="/v1", tags=["process"])
logger = structlog.get_logger(__name__)


@router.post("/process")
async def process_document(
    request: Request,
    file: UploadFile,
    hub_code: str = Form(default=""),
    doc_type: str = Form(default=""),
    request_id: str = Form(default=""),
    chunker_options: str = Form(default=""),
) -> dict[str, Any]:
    settings = get_settings()
    rid = request_id or request.headers.get("X-Request-Id") or str(uuid.uuid4())

    # ── Limit: payload size (DSVC-06 → 413) ──
    body = await file.read()
    if len(body) > settings.max_file_bytes:
        logger.warning(
            "payload_too_large",
            request_id=rid,
            size_bytes=len(body),
            max_bytes=settings.max_file_bytes,
        )
        raise HTTPException(
            status_code=413,
            detail={
                "error": "payload_too_large",
                "max_mb": settings.max_file_mb,
                "received_mb": round(len(body) / 1024 / 1024, 2),
            },
        )

    if not file.filename:
        raise HTTPException(status_code=400, detail={"error": "missing filename"})

    # Parse chunker_options JSON (CHUNK-02 per-request override)
    chunk_opts = ChunkerOptions()
    if chunker_options:
        try:
            opts_dict = json.loads(chunker_options)
            chunk_opts = ChunkerOptions.from_dict(opts_dict)
        except (json.JSONDecodeError, TypeError) as exc:
            raise HTTPException(
                status_code=400,
                detail={"error": "invalid chunker_options json", "message": str(exc)},
            ) from exc

    extractor = get_extractor()
    chunker = get_chunker()

    # ── Limit: timeout (DSVC-06 → 504) ──
    # Docling extract + chunk là blocking CPU work → wrap to_thread + asyncio.timeout
    async def _run() -> tuple[Any, list[Any]]:
        doc = await asyncio.to_thread(extractor.extract, body, file.filename)
        chunks = await asyncio.to_thread(chunker.chunk, doc, chunk_opts)
        return doc, chunks

    try:
        async with asyncio.timeout(settings.request_timeout_sec):
            doc, chunks = await _run()
    except TimeoutError as exc:
        logger.error(
            "request_timeout",
            request_id=rid,
            timeout_sec=settings.request_timeout_sec,
            filename=file.filename,
        )
        raise HTTPException(
            status_code=504,
            detail={
                "error": "request_timeout",
                "timeout_sec": settings.request_timeout_sec,
            },
        ) from exc
    except ValueError as exc:
        # Unsupported extension từ extractor
        raise HTTPException(status_code=415, detail={"error": str(exc)}) from exc
    except Exception as exc:
        logger.error("process_fail", request_id=rid, error=str(exc), filename=file.filename)
        raise HTTPException(status_code=500, detail={"error": str(exc)}) from exc

    # ocr_used: best-effort detect — file là image hoặc PDF + Docling đã chạy OCR pipeline
    ext = (file.filename.rsplit(".", 1)[-1] if "." in file.filename else "").lower()
    ocr_used = ext in {"png", "jpg", "jpeg"} or ext == "pdf"  # PDF có thể có OCR; conservative

    tokenizer_name = chunk_opts.tokenizer_name or settings.tokenizer_name
    doc_meta = build_doc_meta(doc, file.filename, ocr_used=ocr_used)
    chunks_dict = serialize_chunks(doc, chunks, tokenizer_name=tokenizer_name)

    logger.info(
        "process_done",
        request_id=rid,
        filename=file.filename,
        hub_code=hub_code,
        doc_type=doc_type,
        chunks=len(chunks_dict),
        pages=doc_meta["page_count"],
    )

    return {
        "request_id": rid,
        "doc_meta": doc_meta,
        "chunks": chunks_dict,
    }
```

**`main.py`** (FastAPI app + lifespan B5 semantics):

```python
"""FastAPI app entrypoint — lifespan warm models (B5 semantics), register middleware + routers."""

from __future__ import annotations

import asyncio
from contextlib import asynccontextmanager

import structlog
from fastapi import FastAPI

from docling_pipeline.api import health, process
from docling_pipeline.config import get_settings
from docling_pipeline.observability.logging import (
    RequestIdMiddleware,
    configure_logging,
)


@asynccontextmanager
async def lifespan(app: FastAPI):
    """Lifespan B5 — phân biệt 3 case ImportError vs warm transient fail vs warm OK."""
    configure_logging()
    logger = structlog.get_logger(__name__)
    settings = get_settings()
    logger.info(
        "service_starting",
        ocr_engine=settings.ocr_engine,
        ocr_langs=settings.ocr_langs,
        tokenizer=settings.tokenizer_name,
        max_file_mb=settings.max_file_mb,
        timeout_sec=settings.request_timeout_sec,
    )

    # B5 — readyz semantics đúng:
    # (a) ImportError docling → ready=False vĩnh viễn (env hỏng, /readyz 503)
    # (b) warm dummy fail (transient) → ready=True (service vẫn serve được, request đầu chậm)
    # (c) warm dummy OK → ready=True
    try:
        # Lazy import — guard ImportError ở đây thay vì module top.
        # Nếu docling không cài được, vẫn để FastAPI app start (process còn sống cho /healthz),
        # NHƯNG /readyz trả 503 → orchestrator/k8s biết container không serve được.
        from docling_pipeline.core.extractor import warm_models

        try:
            await asyncio.to_thread(warm_models)
            health.set_models_ready(True)
            logger.info("models_warmed", state="ready")
        except Exception as exc:
            # Case (b) transient warm fail — vẫn ready=True
            logger.warning(
                "warm_dummy_failed_but_library_ok",
                error=str(exc),
                note="service vẫn serve được, request đầu sẽ chậm hơn",
            )
            health.set_models_ready(True)
    except ImportError as exc:
        # Case (a) — fatal, ready=False
        logger.error(
            "docling_library_unavailable",
            error=str(exc),
            critical=True,
            note="/readyz sẽ trả 503 cho đến khi user pip install docling==2.91.0",
        )
        health.set_models_ready(False)

    yield

    logger.info("service_stopping")


def create_app() -> FastAPI:
    app = FastAPI(
        title="Medinet Docling Sidecar",
        description="Extract + chunk service cho RAG ingestion (M1)",
        version="0.1.0",
        lifespan=lifespan,
    )
    app.add_middleware(RequestIdMiddleware)
    app.include_router(health.router)
    app.include_router(process.router)
    return app


app = create_app()
```

**Tóm tắt B5 logic:**

| Trường hợp | `import docling` | `warm_models()` dummy | `_models_ready` | `/readyz` |
|---|---|---|---|---|
| (a) Library không cài | ImportError | (không chạy) | False | 503 not_ready |
| (b) Library OK, warm fail transient | OK | Exception | True | 200 ready |
| (c) Library OK, warm OK | OK | OK | True | 200 ready |

Phân biệt rõ ràng giúp ops biết khi nào restart container (case a) vs để service tự warm lazy (case b).
  </action>
  <verify>
    <automated>test -f docling-pipeline/src/docling_pipeline/main.py && test -f docling-pipeline/src/docling_pipeline/api/health.py && test -f docling-pipeline/src/docling_pipeline/api/process.py && test -f docling-pipeline/src/docling_pipeline/observability/logging.py && grep -q "lifespan" docling-pipeline/src/docling_pipeline/main.py && grep -q "ImportError" docling-pipeline/src/docling_pipeline/main.py && grep -q "set_models_ready(False)" docling-pipeline/src/docling_pipeline/main.py && grep -q "set_models_ready(True)" docling-pipeline/src/docling_pipeline/main.py && grep -q "/healthz" docling-pipeline/src/docling_pipeline/api/health.py && grep -q "/process" docling-pipeline/src/docling_pipeline/api/process.py && grep -q "X-Request-Id" docling-pipeline/src/docling_pipeline/observability/logging.py && grep -q "413" docling-pipeline/src/docling_pipeline/api/process.py && grep -q "504" docling-pipeline/src/docling_pipeline/api/process.py</automated>
  </verify>
  <done>4 file ready; lifespan B5 phân biệt ImportError (ready=False) vs warm transient fail (ready=True); service có thể import (`python -c "from docling_pipeline.main import app"` không lỗi).</done>
</task>

</tasks>

<verification>
- `python -c "from docling_pipeline.main import app; print(len(app.routes))"` ≥ 5 (3 endpoint + lifespan + middleware).
- `uvicorn docling_pipeline.main:app --port 8001` (sau pip install -e .[dev]) start không crash, log JSON ra stdout.
- `curl localhost:8001/healthz` trả `{"status":"ok"}` 200.
- `curl localhost:8001/readyz` trả `{"status":"ready"}` 200 sau khi lifespan xong (~30-60s) hoặc `{"status":"not_ready","reason":"docling_library_unavailable"}` 503 nếu Docling không cài được.
- KHÔNG verify /v1/process với file thật ở plan này (Plan 07 + 08 verify).
</verification>

<success_criteria>
- 3 endpoint /healthz /readyz /v1/process register đúng.
- structlog JSON log ra stdout với key request_id.
- Limits 413 (file > max_file_mb) + 504 (timeout) implement đúng.
- Lifespan B5 semantics: ImportError → ready=False; warm transient fail → ready=True + log warning; warm OK → ready=True + log info.
- Per-request chunker_options parse JSON OK.
</success_criteria>

<output>
Tạo `02-06-SUMMARY.md` ghi nhận: API wire xong, service runnable. Lifespan B5 revised — phân biệt 3 case rõ ràng, /readyz 503 chỉ khi docling library thật sự không cài được. Note: ocr_used detect "best-effort" cho PDF (Docling không expose flag rõ — defer M3 nếu cần precise).
</output>
</content>
