---
phase: 03-go-adapter-pipeline-wiring
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/internal/worker/manager.go
  - backend/internal/config/config.go
  - backend/.env.example
autonomous: true
requirements: [WIRE-04]
must_haves:
  truths:
    - "Job extractor=docling chạy với timeout `JOB_TIMEOUT_DOCLING_SEC` (default 240s — gấp đôi DOCLING_TIMEOUT_SEC để buffer 2 lần retry HTTP)"
    - "Job native (extractor cũ) chạy với timeout `JOB_TIMEOUT_DEFAULT_SEC` (default 120s)"
    - "Worker pool áp `context.WithTimeout` per-job (KHÔNG dùng worker-global ctx) — job timeout không kill worker"
    - "Khi job timeout, document status update thành `error` với message rõ ràng (`job timeout after Xs`); worker pool tiếp nhận job mới ngay"
    - "Config `RAGConfig` có thêm 2 field: `JobTimeoutDefaultSec`, `JobTimeoutDoclingSec`"
  artifacts:
    - path: "backend/internal/worker/manager.go"
      provides: "processJob có per-job ctx timeout dựa trên cfg.RAG.Extractor"
      contains: "context.WithTimeout"
    - path: "backend/internal/config/config.go"
      provides: "RAGConfig fields JobTimeout*Sec"
      contains: "JobTimeoutDoclingSec"
  key_links:
    - from: "backend/internal/worker/manager.go"
      to: "cfg.RAG.JobTimeoutDoclingSec"
      via: "constructor inject + select theo extractor mode"
      pattern: "JobTimeout"
---

<objective>
Bổ sung per-job timeout cho worker pool — phân biệt theo extractor mode. Job Docling chạy lâu hơn (multipart upload + Docling parse + retry) nên cần timeout dài hơn job native (extract Go in-process).

**Wave structure (revision round 1):** Plan 03 đã chuyển từ Wave 2 → Wave 3, depends_on `["03-01", "03-02"]` để TRÁNH file conflict với Plan 02 (cả hai cùng sửa `backend/internal/config/config.go` và `backend/.env.example`). Plan 03 chạy sau Plan 02 để append field/env trên nền Plan 02 đã commit, bảo đảm parallel-safety.

**Purpose:** Khi Plan 04 bật `RAG_EXTRACTOR=docling`, worker pool không bị treo khi Docling chậm; cũng không kill quá sớm khi service Docling đang busy chunk PDF lớn 50MB.

**Output:**
- `worker/manager.go` modify: per-job `context.WithTimeout`.
- `config.go` thêm 2 field timeout.
- `.env.example` document.
</objective>

<execution_context>
@C:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-go-adapter-pipeline-wiring/03-CONTEXT.md
@.planning/phases/03-go-adapter-pipeline-wiring/03-01-SUMMARY.md
@.planning/phases/03-go-adapter-pipeline-wiring/03-02-SUMMARY.md
@backend/internal/worker/manager.go
@backend/internal/config/config.go
@backend/.env.example

<interfaces>
<!-- Hiện tại worker/manager.go KHÔNG có cfg field — read full file để biết constructor pattern -->
<!-- WorkerManager struct (đã có): -->
```go
type WorkerManager struct {
    jobs     chan EmbedJob
    pipeline *rag.Pipeline
    docRepo  *repository.DocumentRepo
    workers  int
    wg       sync.WaitGroup
    cancel   context.CancelFunc
}
func NewWorkerManager(workers int, pipeline *rag.Pipeline, docRepo *repository.DocumentRepo) *WorkerManager
```

<!-- processJob hiện tại dùng ctx từ worker goroutine (không timeout per-job) -->
<!-- Plan 03 phải: -->
<!-- 1. Thêm cfg vào WorkerManager (hoặc 2 field timeout đơn giản hơn). -->
<!-- 2. processJob: timeout = (cfg.Extractor == "docling") ? doclingTimeout : defaultTimeout. -->
<!-- 3. Wrap context.WithTimeout, defer cancel. -->

<!-- Plan 02 đã thêm vào RAGConfig (depends_on, đã commit khi Plan 03 chạy): -->
<!--   Extractor, DoclingServiceURL, DoclingTimeoutSec -->
<!-- Plan 03 append 2 field JobTimeout* SAU các field Plan 02 — không sửa lại các field cũ. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mở rộng RAGConfig + .env.example với 2 field JobTimeout*Sec</name>
  <files>backend/internal/config/config.go, backend/.env.example</files>
  <action>
**1. `backend/internal/config/config.go`** — thêm vào `RAGConfig` (cuối struct, SAU các field `Extractor`/`DoclingServiceURL`/`DoclingTimeoutSec` mà Plan 02 đã commit ở Wave 2):

```go
type RAGConfig struct {
    // ... fields cũ + Extractor/DoclingServiceURL/DoclingTimeoutSec từ Plan 02 ...

    // Worker per-job timeout. Phân biệt mode để job Docling không bị cắt sớm.
    JobTimeoutDefaultSec int // env JOB_TIMEOUT_DEFAULT_SEC, default 120s — extract Go in-process.
    JobTimeoutDoclingSec int // env JOB_TIMEOUT_DOCLING_SEC, default 240s — gấp đôi DOCLING_TIMEOUT_SEC để buffer 2 lần retry HTTP.
}
```

Trong `Load()` — append SAU 3 dòng load env Plan 02 (`Extractor`, `DoclingServiceURL`, `DoclingTimeoutSec`):

```go
RAG: RAGConfig{
    // ... (Plan 02 đã thêm 3 dòng này phía trên) ...
    JobTimeoutDefaultSec: getEnvInt("JOB_TIMEOUT_DEFAULT_SEC", 120),
    JobTimeoutDoclingSec: getEnvInt("JOB_TIMEOUT_DOCLING_SEC", 240),
},
```

**2. `backend/.env.example`** — append SAU khối "M1 Phase 3 — Docling RAG Extractor" mà Plan 02 đã commit:

```
# Per-job timeout worker pool (Phase 3 WIRE-04).
# Default 120s đủ cho extract Go in-process (PDF/DOCX vài MB).
JOB_TIMEOUT_DEFAULT_SEC=120

# Job dùng Docling extractor (HTTP gọi sidecar) cần lâu hơn vì:
# DOCLING_TIMEOUT_SEC=180s × buffer ~1.3 cho retry exponential 1s+2s+4s = ~240s.
JOB_TIMEOUT_DOCLING_SEC=240
```
  </action>
  <verify>
    <automated>cd backend && go build ./internal/config/...</automated>
  </verify>
  <done>
- `RAGConfig` có 2 field mới (cộng dồn với 3 field Plan 02 → tổng 5 field RAG mới trong Phase 3).
- `Load()` đọc đúng 2 env mới với default 120/240.
- `.env.example` chứa cả 5 env (3 Plan 02 + 2 Plan 03).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkerManager nhận cfg + per-job context.WithTimeout</name>
  <files>backend/internal/worker/manager.go</files>
  <behavior>
- Constructor `NewWorkerManager` nhận thêm tham số `cfg config.RAGConfig` (hoặc đơn giản 3 string/int — xem Action). Sửa mọi caller (`cmd/server/main.go` hoặc tương đương — grep `NewWorkerManager(`).
- `processJob(parentCtx, job)`:
  - Xác định timeout: nếu `m.extractorMode == "docling"` → `m.doclingTimeout`, else `m.defaultTimeout`.
  - `jobCtx, cancel := context.WithTimeout(parentCtx, timeout)`; `defer cancel()`.
  - Truyền `jobCtx` vào `pipeline.ProcessWithChunks()` thay vì parentCtx.
  - Khi pipeline trả error, check `errors.Is(err, context.DeadlineExceeded)` → log riêng + update doc status với message `"job timeout after Xs"`.
- Worker goroutine vẫn dùng parentCtx (không timeout) — chỉ job ctx có timeout. Nếu job timeout, worker tiếp nhận job kế tiếp ngay.
  </behavior>
  <action>
Edit `backend/internal/worker/manager.go`:

1. **Sửa struct + constructor:**

```go
type WorkerManager struct {
    jobs     chan EmbedJob
    pipeline *rag.Pipeline
    docRepo  *repository.DocumentRepo
    workers  int
    wg       sync.WaitGroup
    cancel   context.CancelFunc

    // M1 Phase 3 — per-job timeout config (WIRE-04).
    extractorMode  string        // "docling" | "native" | "auto"
    defaultTimeout time.Duration // job native
    doclingTimeout time.Duration // job docling
}

// NewWorkerManager — signature mở rộng. Nếu workers <= 0 default 3.
func NewWorkerManager(
    workers int,
    pipeline *rag.Pipeline,
    docRepo *repository.DocumentRepo,
    ragCfg config.RAGConfig,
) *WorkerManager {
    if workers <= 0 {
        workers = 3
    }
    defTimeout := time.Duration(ragCfg.JobTimeoutDefaultSec) * time.Second
    if defTimeout <= 0 {
        defTimeout = 120 * time.Second
    }
    doclingTimeout := time.Duration(ragCfg.JobTimeoutDoclingSec) * time.Second
    if doclingTimeout <= 0 {
        doclingTimeout = 240 * time.Second
    }
    return &WorkerManager{
        jobs:           make(chan EmbedJob, workers*10),
        pipeline:       pipeline,
        docRepo:        docRepo,
        workers:        workers,
        extractorMode:  ragCfg.Extractor,
        defaultTimeout: defTimeout,
        doclingTimeout: doclingTimeout,
    }
}
```

Thêm import:
```go
"errors"
"time"
"github.com/medinet/hub-all-backend/internal/config"
```

2. **Sửa `processJob`:**

```go
func (m *WorkerManager) processJob(parentCtx context.Context, job EmbedJob) {
    docID, err := uuid.Parse(job.DocumentID)
    if err != nil { /* ... unchanged ... */ }

    slog.Info("processing job", "doc_id", job.DocumentID, "extractor", m.extractorMode)

    if err := m.docRepo.UpdateStatus(parentCtx, docID, "processing", nil); err != nil { /* ... */ }

    // ─── M1 Phase 3 — per-job timeout (WIRE-04) ───
    timeout := m.defaultTimeout
    if m.extractorMode == "docling" {
        timeout = m.doclingTimeout
    }
    jobCtx, cancel := context.WithTimeout(parentCtx, timeout)
    defer cancel()

    // SetProgressFunc — giữ nguyên logic cũ, dùng parentCtx cho UpdateProgress
    // (progress update không nên bị cắt cùng job timeout — Postgres write nhanh).
    m.pipeline.SetProgressFunc(func(docIDStr string, progress int, stage string) {
        id, perr := uuid.Parse(docIDStr)
        if perr != nil { return }
        if uerr := m.docRepo.UpdateProgress(parentCtx, id, progress); uerr != nil {
            slog.Warn("failed to update progress", "doc_id", docIDStr, "error", uerr)
        }
    })

    // Run pipeline với jobCtx (timeout-bounded).
    result, chunkResults, err := m.pipeline.ProcessWithChunks(
        jobCtx,
        job.DocumentID, job.DocName, job.FilePath, job.FileType, job.HubCode, job.Collection,
    )
    if err != nil {
        var errMsg string
        if errors.Is(err, context.DeadlineExceeded) {
            errMsg = fmt.Sprintf("job timeout after %s (extractor=%s)", timeout, m.extractorMode)
            slog.Error("job timeout", "doc_id", job.DocumentID, "timeout", timeout, "extractor", m.extractorMode)
        } else {
            errMsg = err.Error()
            slog.Error("pipeline failed", "doc_id", job.DocumentID, "error", err)
        }
        // Update status dùng parentCtx (jobCtx có thể đã expire).
        if updateErr := m.docRepo.UpdateStatus(parentCtx, docID, "error", &errMsg); updateErr != nil {
            slog.Error("failed to update doc error status", "doc_id", job.DocumentID, "error", updateErr)
        }
        return
    }

    // ... phần insert chunks + UpdateCompleted: dùng parentCtx (đảm bảo lưu xong dù job ctx đã cancel).
    // (Read existing code — chỉ thay m.docRepo.BatchInsertChunks(ctx, ...) → (parentCtx, ...))
}
```

Cần thêm import `"fmt"` (đã có sẵn? — verify, add nếu chưa).

3. **Update caller** — grep `NewWorkerManager(` trong repo:

```bash
grep -rn "NewWorkerManager(" backend/cmd/ backend/internal/
```

Sửa caller (thường ở `backend/cmd/server/main.go` hoặc `backend/cmd/api/main.go`) thêm tham số `cfg.RAG`:

```go
workerMgr := worker.NewWorkerManager(cfg.RAG.WorkerCount, pipeline, docRepo, cfg.RAG)
```
  </action>
  <verify>
    <automated>cd backend && go build ./... && go vet ./...</automated>
  </verify>
  <done>
- `cd backend && go build ./...` PASS — caller `NewWorkerManager` đã update.
- `processJob` có `context.WithTimeout` per-job với giá trị phụ thuộc `m.extractorMode`.
- Khi `errors.Is(err, context.DeadlineExceeded)`, doc status update với message format `"job timeout after Xs (extractor=...)"`.
- Worker không bị kill khi 1 job timeout — verify thủ công ở Plan 04 smoke (hoặc Plan 05 unit test nếu cover được).
  </done>
</task>

</tasks>

<verification>
- `cd backend && go build ./...` PASS.
- `grep -n "context.WithTimeout" backend/internal/worker/manager.go` ra ≥ 1 match.
- `grep -n "JobTimeoutDoclingSec\|JobTimeoutDefaultSec" backend/internal/config/config.go` ra 2 field + 2 line load env.
- `.env.example` document đủ 2 env mới.
- Caller `NewWorkerManager(...)` đã update signature mới — không có lỗi compile "wrong number of arguments".
</verification>

<success_criteria>
- WIRE-04 đáp ứng: job Docling có timeout 240s, job native có timeout 120s, không hard-code.
- Worker pool resilient với job timeout — không bị treo, không bị kill goroutine.
- Phase 4 (CFG) sẽ kế thừa pattern này khi thêm `auto` mode + circuit breaker.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/03-go-adapter-pipeline-wiring/03-03-SUMMARY.md` ghi:
- Signature mới `NewWorkerManager(workers, pipeline, docRepo, ragCfg)`.
- File caller đã update (đường dẫn cụ thể).
- 2 env mới + default values.
</output>
