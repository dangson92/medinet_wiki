---
phase: 03-go-adapter-pipeline-wiring
plan: 05
type: tdd
wave: 5
depends_on: ["03-02"]
files_modified:
  - backend/internal/rag/extractor/docling_test.go
  - backend/internal/rag/extractor/testdata/sample.pdf
autonomous: true
requirements: [WIRE-01, WIRE-06]
must_haves:
  truths:
    - "Test `TestDoclingExtractor_Success` verify: mock 200 OK với schema DSVC-02 hợp lệ → trả `[]chunker.Chunk` đúng số lượng + map metadata đầy đủ (`headers`, `page_start`, `is_table`, `source=docling`)"
    - "Test `TestDoclingExtractor_413_NoRetry` verify: mock 413 Payload Too Large → return error chứa 'client error', `httptest.Server` chỉ nhận 1 request (KHÔNG retry)"
    - "Test `TestDoclingExtractor_504_NoRetry` verify: mock 504 Gateway Timeout → return error chứa 'timeout', server chỉ nhận 1 request"
    - "Test `TestDoclingExtractor_NetworkError_Retry` verify: server đóng connection ngay → retry 2 lần (tổng 3 attempts) rồi fail, error chứa 'unreachable after 3 attempts'"
    - "Test `TestDoclingExtractor_500_Then_200` verify: lần 1 mock 500, lần 2 mock 200 → success, nhận đúng số chunks từ response lần 2"
    - "Test `TestDoclingExtractor_RequestIDPropagation` verify: ctx có request_id 'test-rid-xyz' → mock server đọc được header `X-Request-Id: test-rid-xyz` (WIRE-06)"
    - "Test `TestDoclingExtractor_CtxCancel` verify: ctx cancel giữa retry → return ctx error ngay, không hoàn tất retry còn lại"
  artifacts:
    - path: "backend/internal/rag/extractor/docling_test.go"
      provides: "Unit test mock httptest.Server cho DoclingExtractor"
      min_lines: 200
    - path: "backend/internal/rag/extractor/testdata/sample.pdf"
      provides: "Fixture file PDF nhỏ (1-2KB stub) — dummy binary upload, không cần parse được"
  key_links:
    - from: "docling_test.go"
      to: "DoclingExtractor (Plan 02)"
      via: "import same package extractor"
      pattern: "NewDoclingExtractor"
    - from: "docling_test.go"
      to: "requestid.With (Plan 01)"
      via: "context inject"
      pattern: "requestid\\.With"
---

<objective>
Tạo unit test cho `DoclingExtractor` (Plan 02) bằng `net/http/httptest.Server` mock — verify retry semantics, header propagation, schema parsing, error handling. Đây là **TDD plan** chuyên biệt vì test cần kiểm soát chính xác request count, status code sequence, và header inspection.

**Wave structure (revision round 1):** Plan 05 đã chuyển từ Wave 4 → Wave 5 do dồn từ Blocker 1 fix (Plan 03 → wave 3, Plan 04 → wave 4). depends_on giữ `["03-02"]` vì Plan 05 chỉ test target `DoclingExtractor` (Plan 02 sản phẩm) — KHÔNG cần Plan 04 wiring để chạy unit test. Đặt ở Wave 5 chỉ để tôn trọng thứ tự execute tổng (sau khi pipeline wiring xong, suite test full-stack cũng OK).

**Purpose:** Phase 3 không có integration test runtime (Docling sidecar deferred infra). Plan 05 đảm bảo logic adapter Go đúng spec WIRE-01 + WIRE-06 trước khi Phase 4-5 chạy smoke thật.

**Output:**
- `docling_test.go` mới với 6-7 test cases.
- `testdata/sample.pdf` stub (dummy binary, không cần valid PDF — DoclingExtractor chỉ upload, không parse).
</objective>

<execution_context>
@C:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-go-adapter-pipeline-wiring/03-CONTEXT.md
@.planning/phases/03-go-adapter-pipeline-wiring/03-02-SUMMARY.md
@.planning/phases/03-go-adapter-pipeline-wiring/03-04-SUMMARY.md
@backend/internal/rag/extractor/docling.go
@backend/internal/pkg/requestid/requestid.go

<interfaces>
<!-- Test target — Plan 02 đã tạo: -->
```go
func NewDoclingExtractor(baseURL string, timeoutSec int) *DoclingExtractor
func (d *DoclingExtractor) ExtractStructured(ctx, filePath) (string, []chunker.Chunk, error)
func (d *DoclingExtractor) Extract(ctx, filePath) (string, error)
const DoclingKey = "docling"
```

<!-- DTO nội bộ — test có thể access vì cùng package: -->
```go
type processChunkDTO struct { ... }
type processResponse struct { RequestID string; DocMeta map[string]any; Chunks []processChunkDTO }
```

<!-- Helper request_id (Plan 01): -->
```go
import "github.com/medinet/hub-all-backend/internal/pkg/requestid"
ctx := requestid.With(context.Background(), "test-rid-xyz")
```

<!-- Schema response mock cần khớp DSVC-02 (Phase 2): -->
{ "request_id": "...", "doc_meta": {...}, "chunks": [{ chunk_index, text, headers, caption, page_start, page_end, is_table, table_html, bbox, token_count }] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo fixture sample.pdf + skeleton test file</name>
  <files>backend/internal/rag/extractor/testdata/sample.pdf, backend/internal/rag/extractor/docling_test.go</files>
  <action>
**1. Tạo `backend/internal/rag/extractor/testdata/sample.pdf`** (binary stub):

Vì DoclingExtractor chỉ upload binary qua multipart và KHÔNG parse PDF (parse là việc của Docling Python), file chỉ cần tồn tại với vài byte. Tạo bằng:

```bash
mkdir -p backend/internal/rag/extractor/testdata
printf '%%PDF-1.4\n%%test stub for unit test\n%%%%EOF\n' > backend/internal/rag/extractor/testdata/sample.pdf
```

(File ~50 bytes, đủ để `os.Open` + `io.Copy` hoạt động.)

**2. Tạo `backend/internal/rag/extractor/docling_test.go`** — skeleton + helper:

```go
package extractor

import (
    "context"
    "encoding/json"
    "fmt"
    "io"
    "mime/multipart"
    "net/http"
    "net/http/httptest"
    "path/filepath"
    "strings"
    "sync/atomic"
    "testing"
    "time"

    "github.com/medinet/hub-all-backend/internal/pkg/requestid"
)

const fixturePath = "testdata/sample.pdf"

// mockHandler ghi nhận request count + cho phép tùy biến response per-attempt.
type mockHandler struct {
    requestCount  atomic.Int32
    capturedRID   atomic.Value // string — header X-Request-Id của request cuối
    capturedFile  atomic.Bool  // form có field "file"
    statusByAttempt []int      // status code lần lượt mỗi attempt; nil → default 200
    responseBody  string       // body trả khi 200
    closeConn     bool         // true → đóng connection ngay (simulate network error)
}

func (m *mockHandler) ServeHTTP(w http.ResponseWriter, r *http.Request) {
    n := m.requestCount.Add(1)
    m.capturedRID.Store(r.Header.Get(requestid.HeaderName))

    // Parse multipart để verify field "file" tồn tại.
    if err := r.ParseMultipartForm(10 << 20); err == nil {
        if _, _, err := r.FormFile("file"); err == nil {
            m.capturedFile.Store(true)
        }
    }

    if m.closeConn {
        // Hijack + close để simulate network error.
        hj, ok := w.(http.Hijacker)
        if ok {
            conn, _, _ := hj.Hijack()
            conn.Close()
        }
        return
    }

    status := http.StatusOK
    if int(n)-1 < len(m.statusByAttempt) {
        status = m.statusByAttempt[int(n)-1]
    }

    if status == http.StatusOK {
        w.Header().Set("Content-Type", "application/json")
        w.WriteHeader(status)
        body := m.responseBody
        if body == "" {
            body = defaultSuccessBody
        }
        _, _ = io.WriteString(w, body)
        return
    }

    w.WriteHeader(status)
    _, _ = io.WriteString(w, fmt.Sprintf(`{"error":"mock %d"}`, status))
}

const defaultSuccessBody = `{
  "request_id": "rid-from-server",
  "doc_meta": {"filename":"sample.pdf","page_count":2,"doc_type":"pdf","ocr_used":false},
  "chunks": [
    {"chunk_index":0,"text":"# Heading\n\nFirst chunk","headers":["Heading"],"caption":null,"page_start":1,"page_end":1,"is_table":false,"table_html":null,"bbox":null,"token_count":42},
    {"chunk_index":1,"text":"| col1 | col2 |\n|---|---|\n| a | b |","headers":["Heading","Sub"],"caption":null,"page_start":2,"page_end":2,"is_table":true,"table_html":"<table><tr><td>a</td><td>b</td></tr></table>","bbox":[10.0,20.0,300.0,400.0],"token_count":18}
  ]
}`

// newTestServer trả httptest.Server bind handler. Caller phải defer Close.
func newTestServer(h http.Handler) *httptest.Server {
    return httptest.NewServer(h)
}

// withFastBackoff tạo extractor với timeout ngắn để test chạy nhanh
// (KHÔNG override backoff vì hard-coded; dùng ctx timeout để cap test runtime).
func newTestExtractor(baseURL string) *DoclingExtractor {
    return NewDoclingExtractor(baseURL, 30) // 30s — vừa đủ buffer cho 1s+2s retry.
}

// Verify multipart field "file" được set trong build request.
// (helper nội bộ — tránh duplicate trong từng test.)
var _ = multipart.NewWriter // silence unused import nếu Go compiler than
```

LƯU Ý:
- `backoff` hard-coded 1s/2s — test sẽ chậm (~3s mỗi test có retry). Chấp nhận để KHÔNG sửa production code chỉ cho test (anti-pattern). Nếu thấy quá chậm sau khi run, refactor `DoclingExtractor` thêm field `backoffs []time.Duration` injectable, default `{1s, 2s}` — đó là task con của Plan 02 nếu cần.
- `httptest.NewServer` mặc định tạo HTTP/1.1 server, đủ cho mock.
  </action>
  <verify>
    <automated>cd backend && ls internal/rag/extractor/testdata/sample.pdf && go build ./internal/rag/extractor/...</automated>
  </verify>
  <done>
- File fixture tồn tại.
- Skeleton test file compile pass (chưa có test case cụ thể).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 6 test cases — RED→GREEN cho retry/header/parse</name>
  <files>backend/internal/rag/extractor/docling_test.go</files>
  <behavior>
6 test cases (chi tiết spec trong `must_haves.truths`):

1. `TestDoclingExtractor_Success` — happy path
2. `TestDoclingExtractor_413_NoRetry` — 413 không retry
3. `TestDoclingExtractor_504_NoRetry` — 504 không retry
4. `TestDoclingExtractor_NetworkError_Retry` — connection close → retry 2 lần fail
5. `TestDoclingExtractor_500_Then_200` — 500→200 recover
6. `TestDoclingExtractor_RequestIDPropagation` — header X-Request-Id propagate

Optional 7th: `TestDoclingExtractor_CtxCancel` — ctx cancel giữa retry.

Pattern assert mỗi test:
- Tạo mockHandler với scenario.
- `srv := newTestServer(h); defer srv.Close()`.
- `ext := newTestExtractor(srv.URL)`.
- `ctx := context.Background()` hoặc `requestid.With(...)`.
- Gọi `ext.ExtractStructured(ctx, fixturePath)`.
- Assert: error type, chunk count, metadata fields, request count, captured header.
  </behavior>
  <action>
Append 6 test functions vào `docling_test.go`. Mẫu chi tiết từng test:

```go
// ─── 1. Success happy path ───
func TestDoclingExtractor_Success(t *testing.T) {
    h := &mockHandler{}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    text, chunks, err := ext.ExtractStructured(context.Background(), fixturePath)
    if err != nil {
        t.Fatalf("expected nil error, got %v", err)
    }
    if got := h.requestCount.Load(); got != 1 {
        t.Errorf("expected 1 request, got %d", got)
    }
    if !h.capturedFile.Load() {
        t.Error("expected multipart field 'file' to be present")
    }
    if len(chunks) != 2 {
        t.Fatalf("expected 2 chunks, got %d", len(chunks))
    }
    // Chunk 0 — text
    if chunks[0].Index != 0 || chunks[0].TokenCount != 42 {
        t.Errorf("chunk 0: index/token_count wrong: %+v", chunks[0])
    }
    if chunks[0].ChunkType != "docling_text" {
        t.Errorf("chunk 0: ChunkType expected docling_text, got %q", chunks[0].ChunkType)
    }
    if v, _ := chunks[0].Metadata["source"].(string); v != "docling" {
        t.Errorf("chunk 0: metadata.source expected 'docling', got %v", chunks[0].Metadata["source"])
    }
    if h, _ := chunks[0].Metadata["headers"].([]string); len(h) == 0 || h[0] != "Heading" {
        t.Errorf("chunk 0: metadata.headers expected [Heading], got %v", chunks[0].Metadata["headers"])
    }
    if v, _ := chunks[0].Metadata["page_start"].(int); v != 1 {
        t.Errorf("chunk 0: page_start expected 1, got %v", chunks[0].Metadata["page_start"])
    }
    // Chunk 1 — table
    if chunks[1].ChunkType != "docling_table" {
        t.Errorf("chunk 1: ChunkType expected docling_table, got %q", chunks[1].ChunkType)
    }
    if v, _ := chunks[1].Metadata["is_table"].(bool); !v {
        t.Errorf("chunk 1: metadata.is_table expected true")
    }
    if v, _ := chunks[1].Metadata["table_html"].(string); !strings.Contains(v, "<table>") {
        t.Errorf("chunk 1: metadata.table_html missing or wrong: %v", chunks[1].Metadata["table_html"])
    }
    if !strings.Contains(text, "First chunk") || !strings.Contains(text, "col1") {
        t.Errorf("text concat wrong: %q", text)
    }
}

// ─── 2. 413 không retry ───
func TestDoclingExtractor_413_NoRetry(t *testing.T) {
    h := &mockHandler{statusByAttempt: []int{http.StatusRequestEntityTooLarge}}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    _, _, err := ext.ExtractStructured(context.Background(), fixturePath)
    if err == nil {
        t.Fatal("expected error, got nil")
    }
    if !strings.Contains(err.Error(), "client error") && !strings.Contains(err.Error(), "413") {
        t.Errorf("expected 413/client error, got %v", err)
    }
    if got := h.requestCount.Load(); got != 1 {
        t.Errorf("expected 1 request (no retry on 4xx), got %d", got)
    }
}

// ─── 3. 504 không retry ───
func TestDoclingExtractor_504_NoRetry(t *testing.T) {
    h := &mockHandler{statusByAttempt: []int{http.StatusGatewayTimeout}}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    _, _, err := ext.ExtractStructured(context.Background(), fixturePath)
    if err == nil {
        t.Fatal("expected error, got nil")
    }
    if !strings.Contains(err.Error(), "timeout") {
        t.Errorf("expected 'timeout' in error, got %v", err)
    }
    if got := h.requestCount.Load(); got != 1 {
        t.Errorf("expected 1 request (no retry on 504), got %d", got)
    }
}

// ─── 4. Network error → retry 2 lần fail ───
func TestDoclingExtractor_NetworkError_Retry(t *testing.T) {
    h := &mockHandler{closeConn: true}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    start := time.Now()
    _, _, err := ext.ExtractStructured(context.Background(), fixturePath)
    elapsed := time.Since(start)

    if err == nil {
        t.Fatal("expected error, got nil")
    }
    if !strings.Contains(err.Error(), "unreachable") && !strings.Contains(err.Error(), "after 3 attempts") {
        t.Errorf("expected 'unreachable after 3 attempts', got %v", err)
    }
    if got := h.requestCount.Load(); got != 3 {
        t.Errorf("expected 3 requests (1 + 2 retry), got %d", got)
    }
    // Tổng delay tối thiểu = 1s + 2s = 3s.
    if elapsed < 2900*time.Millisecond {
        t.Errorf("expected ≥ 2.9s elapsed (backoff 1s+2s), got %s", elapsed)
    }
}

// ─── 5. 500 → 200 recover ───
func TestDoclingExtractor_500_Then_200(t *testing.T) {
    h := &mockHandler{statusByAttempt: []int{http.StatusInternalServerError, http.StatusOK}}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    _, chunks, err := ext.ExtractStructured(context.Background(), fixturePath)
    if err != nil {
        t.Fatalf("expected success after retry, got %v", err)
    }
    if len(chunks) != 2 {
        t.Errorf("expected 2 chunks from default body, got %d", len(chunks))
    }
    if got := h.requestCount.Load(); got != 2 {
        t.Errorf("expected 2 requests (1 fail + 1 success), got %d", got)
    }
}

// ─── 6. Header X-Request-Id propagation (WIRE-06) ───
func TestDoclingExtractor_RequestIDPropagation(t *testing.T) {
    h := &mockHandler{}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    ctx := requestid.With(context.Background(), "test-rid-xyz-123")
    _, _, err := ext.ExtractStructured(ctx, fixturePath)
    if err != nil {
        t.Fatalf("unexpected error: %v", err)
    }
    captured, _ := h.capturedRID.Load().(string)
    if captured != "test-rid-xyz-123" {
        t.Errorf("expected X-Request-Id 'test-rid-xyz-123' propagated, got %q", captured)
    }
}

// (Optional) ─── 7. Ctx cancel giữa retry ───
func TestDoclingExtractor_CtxCancel(t *testing.T) {
    h := &mockHandler{statusByAttempt: []int{http.StatusInternalServerError, http.StatusInternalServerError, http.StatusOK}}
    srv := newTestServer(h); defer srv.Close()
    ext := newTestExtractor(srv.URL)

    ctx, cancel := context.WithTimeout(context.Background(), 500*time.Millisecond)
    defer cancel()

    _, _, err := ext.ExtractStructured(ctx, fixturePath)
    if err == nil {
        t.Fatal("expected ctx error, got nil")
    }
    if !strings.Contains(err.Error(), "context") && !strings.Contains(err.Error(), "deadline") {
        t.Errorf("expected ctx deadline error, got %v", err)
    }
    // Chỉ kịp 1 request trước khi backoff 1s vượt deadline 500ms.
    if got := h.requestCount.Load(); got > 2 {
        t.Errorf("expected ≤ 2 requests before ctx cancel, got %d", got)
    }
}
```

Cần xóa `var _ = multipart.NewWriter` và import `mime/multipart` ở Task 1 nếu không xài (hiện tại không xài trong test code).

Cleanup imports cuối cùng:
```go
import (
    "context"
    "fmt"
    "io"
    "net/http"
    "net/http/httptest"
    "strings"
    "sync/atomic"
    "testing"
    "time"

    "github.com/medinet/hub-all-backend/internal/pkg/requestid"
)
```

Bỏ `encoding/json`, `path/filepath`, `mime/multipart` — không dùng.
  </action>
  <verify>
    <automated>cd backend && go test ./internal/rag/extractor/ -run TestDoclingExtractor -v -count=1 -timeout 60s</automated>
  </verify>
  <done>
- 6 (hoặc 7) test cases PASS.
- `go test ./internal/rag/extractor/` exit code 0.
- Test runtime tổng < 30s (test #4 chiếm ~3s vì backoff 1+2s; test #7 ~0.5s; còn lại < 100ms).
- Coverage `DoclingExtractor.ExtractStructured` ≥ 80% (verify bằng `go test -cover`).
  </done>
</task>

</tasks>

<verification>
- `cd backend && go test ./internal/rag/extractor/ -v -count=1` PASS (cả test mới + test cũ nếu có).
- `cd backend && go test ./internal/rag/extractor/ -cover` báo coverage ≥ 50% cho package extractor (DoclingExtractor là code mới chính).
- 7 file extractor Go cũ (`pdf.go`, `docx.go`, ...) KHÔNG bị Plan 05 đụng (chỉ thêm `docling_test.go` + `testdata/sample.pdf`).
- Mock test verify đủ 5 case theo CONTEXT mục "Test strategy Phase 3" (success, 413, 504, network error, 5xx) + thêm test header propagation (WIRE-06).
</verification>

<success_criteria>
- WIRE-01 verified: HTTP client retry exponential 1s/2s, không retry 4xx/504.
- WIRE-06 verified: header `X-Request-Id` propagate đúng từ ctx sang HTTP request.
- 5/5 success criteria Phase 3 đạt được — riêng criterion 4 (request_id propagate cross-service log Go ↔ Docling thật) sẽ chính thức verify ở Phase 4-5 smoke. Plan 05 verify ở mức Go-side (header được gắn đúng).
- Sẵn sàng đánh dấu Phase 3 COMPLETED và chuyển sang Phase 4 CFG.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/03-go-adapter-pipeline-wiring/03-05-SUMMARY.md` ghi:
- 6 test PASS, runtime tổng, coverage %.
- Bảng kết luận Phase 3: 8 REQ-ID complete (WIRE-01..06, CHUNK-05, EXTRACT-05).
- Note cho Phase 4: pattern `cfg.RAG.Extractor` đã sẵn cho `auto` mode + circuit breaker; chỉ cần wrap thêm logic count failures + Redis state vào `extractAndChunk()` của pipeline.
- Note cho Phase 5: smoke runtime cần Docling sidecar chạy thật — Plan 05 (Phase 5) sẽ làm `make eval-smoke`.
</output>
