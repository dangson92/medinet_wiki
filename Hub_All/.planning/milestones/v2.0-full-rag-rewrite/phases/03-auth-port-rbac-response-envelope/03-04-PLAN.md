---
plan: 04
phase: 3
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - Hub_All/api/app/auth/schemas.py
  - Hub_All/api/app/auth/service.py
  - Hub_All/api/app/auth/dependencies.py
  - Hub_All/api/app/auth/router.py
  - Hub_All/api/app/auth/__init__.py
  - Hub_All/api/app/main.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
---

# Plan 04: Auth schemas + service (Redis blacklist + SETNX race) + router (login/refresh/logout/me)

## Objective

Wire toàn bộ auth flow: 4 endpoint `POST /api/auth/login`, `POST /api/auth/refresh`, `POST /api/auth/logout`, `GET /api/auth/me`. Service layer dùng `JWTManager` (Plan 03-02), `verify_password` (Plan 03-03), Redis blacklist với `SETNX` atomic chống concurrent refresh race (P16), response envelope (Plan 03-01). Mount router vào `app.main:create_app()`.

**Tại sao plan riêng:** AUTH-01..03 = 4 endpoint + service layer + dependency `get_current_user` — đây là task lớn nhất Phase 3. Plan 03-01/02/03 đã chuẩn bị primitives, Plan 03-04 chỉ compose. Tách Plan 03-05 (RBAC dependency `require_role` + integration test toàn phase) để Plan 03-04 không bloat.

## Must-Haves

**Truths (7 mục — observable behaviors):**
- `POST /api/auth/login` body `{"email":"admin@medinet.vn","password":"Admin@123"}` → 200, body envelope `{success:true, data:{access_token, refresh_token, user:{id, email, full_name, role, hub_assignments:[...]}}, error:null, meta:null}`.
- Login sai password → 401, body `{success:false, data:null, error:{code:"INVALID_CREDENTIALS", message:"Email hoặc mật khẩu không đúng"}, meta:null}`. Response time chống enumeration (luôn gọi `verify_password` kể cả khi user không tồn tại — dummy hash).
- `POST /api/auth/refresh` body `{"refresh_token":"<jwt>"}` → 200 token mới + old jti blacklist Redis `blacklist:<jti>` TTL=refresh_ttl. Concurrent 5 call cùng refresh_token → 1 PASS, 4 lần 401 (SETNX atomic — P16).
- `POST /api/auth/logout` Header `Authorization: Bearer <access>` → 200, access jti + refresh jti (nếu body có) blacklist Redis.
- `GET /api/auth/me` Header `Authorization: Bearer <access>` → 200, body `{success:true, data:{id, email, full_name, role, hub_assignments:[hub_uuid...]}}`.
- Truy cập `/api/auth/me` không header → 401 `MISSING_AUTHORIZATION`. Header sai format → 401 `INVALID_AUTHORIZATION_FORMAT`. Token expired/tampered → 401 `INVALID_TOKEN`. Token đã blacklist → 401 `TOKEN_REVOKED`.
- `app.routes` chứa 4 path mới: `/api/auth/login`, `/api/auth/refresh`, `/api/auth/logout`, `/api/auth/me`.

**Artifacts (6 file, 1 trong đó là EXTEND):**
- `Hub_All/api/app/auth/schemas.py` — Pydantic v2 `LoginRequest`, `LoginResponse`, `RefreshRequest`, `LogoutRequest`, `UserPublic`.
- `Hub_All/api/app/auth/service.py` — `AuthService` class với `login`, `refresh`, `logout`, `get_current_user_info` + Redis blacklist (`SETNX` cho refresh race P16).
- `Hub_All/api/app/auth/dependencies.py` — `get_jwt_manager`, `get_auth_service`, `get_current_user` (verify JWT + Redis blacklist check) — `require_role` để Plan 03-05 EXTEND.
- `Hub_All/api/app/auth/router.py` — `APIRouter(prefix="/api/auth", tags=["auth"])` + 4 endpoint.
- `Hub_All/api/app/auth/__init__.py` (EXTEND) — re-export `AuthService`, `get_current_user`, `auth_router`.
- `Hub_All/api/app/main.py` (EXTEND) — wire `app.state.jwt_manager = JWTManager(settings)` ở lifespan + `app.include_router(auth_router)` trong `create_app()`.

**Key links (5 connection):**
- `service.login` → `verify_password(req.password, user.password_hash)` (Plan 03-03) → `jwt_manager.issue_token_pair(...)` (Plan 03-02) → `INSERT INTO refresh_tokens (user_id, token_hash, expires_at)` (Phase 2 model `RefreshToken`).
- `service.refresh` → Redis `SETNX lock:refresh:<old_jti> 1 EX 30` để giành quyền rotate (P16); nếu lock fail → 401 "token đã được dùng"; nếu lock OK → verify + blacklist old jti + issue new pair.
- `dependencies.get_current_user` → `jwt_manager.verify_token(token, expected_type="access")` → check `redis.exists("blacklist:" + jti)` → load user từ DB qua `get_session()` (Phase 2 Plan 01).
- `router.login` → `service.login()` → `resp.ok(data=...)` (Plan 03-01 envelope code UPPER_SNAKE).
- `main.lifespan` → `app.state.jwt_manager = JWTManager(settings)` startup + `app.state.dummy_password_hash` (anti-timing — sinh 1 hash dummy `"x"` để service.login dùng khi user không tồn tại).

## Tasks

<task id="01">
<action>
Tạo `Hub_All/api/app/auth/schemas.py` — Pydantic v2 models cho request/response auth endpoint.

**5 schema bắt buộc:**

```python
"""Pydantic v2 schemas — request/response cho 4 auth endpoint.

Match shape Go cũ (D6 frontend compat):
- LoginRequest  / LoginResponse  → POST /api/auth/login
- RefreshRequest                 → POST /api/auth/refresh
- LogoutRequest                  → POST /api/auth/logout (optional refresh_token body)
- UserPublic                     → embedded trong LoginResponse + GET /api/auth/me

Pydantic v2 EmailStr requires `pydantic[email]` — đã có qua `pydantic>=2.7` dep.
Nếu missing, đổi `EmailStr` → `str` + validator manual (Phase 3 chấp nhận
nếu email-validator unbundle).
"""
from __future__ import annotations

from typing import Literal

from pydantic import BaseModel, EmailStr, Field


class LoginRequest(BaseModel):
    """POST /api/auth/login request body."""

    email: EmailStr
    password: str = Field(min_length=1, max_length=200)


class UserPublic(BaseModel):
    """Public user info — KHÔNG bao gồm password_hash hoặc bất kỳ sensitive field."""

    id: str  # UUID string
    email: str
    full_name: str | None
    role: Literal["admin", "editor", "viewer"]
    hub_assignments: list[str] = Field(
        default_factory=list,
        description="List UUID hub_id user có quyền truy cập (USER-03 multi-hub).",
    )


class LoginResponse(BaseModel):
    """Login success — data field trong envelope `{success, data, error, meta}`."""

    access_token: str
    refresh_token: str
    expires_at: int  # Unix timestamp access token expires (frontend dùng để show countdown)
    user: UserPublic


class RefreshRequest(BaseModel):
    """POST /api/auth/refresh request body."""

    refresh_token: str = Field(min_length=1)


class LogoutRequest(BaseModel):
    """POST /api/auth/logout — refresh_token optional (mục đích blacklist cả pair)."""

    refresh_token: str | None = None
```

**Lưu ý:** `EmailStr` cần `pydantic[email]` (dep `email-validator`). Kiểm tra `pyproject.toml` line 10 (`"pydantic>=2.7.0,<3"`) + `uv run python -c "from pydantic import EmailStr"` — nếu fail, **cập nhật pyproject.toml** thay `"pydantic>=2.7.0,<3"` → `"pydantic[email]>=2.7.0,<3"` rồi `uv sync --extra dev`. Document trong SUMMARY nếu phải bump dep.
</action>
<read_first>
- Hub_All/api/pyproject.toml
- Hub_All/api/app/models/auth.py
- Hub_All/backend/internal/model (đọc UserWithRoles + LoginRequest shape Go nếu có)
- Hub_All/backend/internal/handler/auth_handler.go
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/auth/schemas.py` tồn tại.
- `grep -c 'class LoginRequest' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `grep -c 'class LoginResponse' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `grep -c 'class UserPublic' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `grep -c 'class RefreshRequest' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `grep -c 'class LogoutRequest' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `grep -c 'hub_assignments' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `grep -c 'role: Literal\["admin", "editor", "viewer"\]' Hub_All/api/app/auth/schemas.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.auth.schemas import LoginRequest, LoginResponse, UserPublic, RefreshRequest, LogoutRequest; r = LoginRequest(email='admin@medinet.vn', password='x'); print(r.email, r.password)"` exits 0.
- Acceptance: Plan 03-05 test `tests/integration/test_auth_login.py::test_login_validation_error_on_bad_email_format` MUST verify `r.status_code == 422` khi POST `/api/auth/login` với `email='not-an-email'`. Plan 03-04 chỉ ship schema definition; runtime EmailStr validation reject email malformed sẽ verify qua pytest case nói trên ở Plan 03-05.
- `cd Hub_All/api && uv run ruff check app/auth/schemas.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth/schemas.py` exits 0.
</acceptance_criteria>
</task>

<task id="02">
<action>
Tạo `Hub_All/api/app/auth/service.py` — `AuthService` class chứa logic 4 use-case + Redis blacklist + anti-timing oracle.

**Dependencies injection (constructor):**
- `db: AsyncSession` (Phase 2 Plan 01 `get_session()`)
- `redis: redis.asyncio.Redis | None` (Phase 1 `app.state.redis`; nếu None → blacklist disabled, log warning)
- `jwt_manager: JWTManager` (Plan 03-02)
- `dummy_password_hash: str` (anti-timing — pre-computed hash của random plaintext, app.state)

**4 method:**

1. **`async def login(req: LoginRequest) -> LoginResponse`**
   - Query `User` qua `select(User).where(User.email == req.email, User.is_active.is_(True))`.
   - **ALWAYS call `verify_password`** (anti-timing): nếu user None → verify với `dummy_password_hash` (return False but same time); nếu có → verify với `user.password_hash`.
   - Nếu user None hoặc verify False → raise `AuthError("INVALID_CREDENTIALS", "Email hoặc mật khẩu không đúng")`.
   - Load hub_assignments: `select(UserHub.hub_id).where(UserHub.user_id == user.id)` → list str.
   - Issue token pair: `jwt_manager.issue_token_pair(user_id=str(user.id), email, full_name, role, hub_ids=hub_assignments)`.
   - Lưu refresh token hash vào DB: `INSERT INTO refresh_tokens (user_id, token_hash, expires_at) VALUES (...)`. `token_hash = sha256(refresh_token).hexdigest()` (T-02-03 mitigation — KHÔNG store plaintext).
   - Trả `LoginResponse(access_token, refresh_token, expires_at=int(access_expires_at.timestamp()), user=UserPublic(...))`.

2. **`async def refresh(req: RefreshRequest) -> LoginResponse`** — P16 mitigation (race condition).
   - Verify refresh token: `jwt_manager.verify_token(req.refresh_token, expected_type="refresh")` → `claims`. Raise `AuthError("INVALID_REFRESH_TOKEN", "Refresh token không hợp lệ")` nếu fail.
   - **P16 Redis SETNX atomic lock** trên jti cũ: `redis.set("lock:refresh:" + claims.jti, "1", nx=True, ex=30)`. Nếu return False (key đã tồn tại = ai đó đang refresh) → raise `AuthError("REFRESH_RACE", "Token đang được refresh đồng thời")` → 401. Nếu None (Redis chết) → fail-open (log warning, tiếp tục — Phase 3 không crash khi Redis down, defer hard fail Phase 10).
   - Check blacklist: `redis.exists("blacklist:" + claims.jti)` → nếu > 0 → raise `AuthError("TOKEN_REVOKED", "Refresh token đã bị thu hồi")`.
   - Load user qua `claims.sub` (UUID). Nếu user không tồn tại hoặc `is_active=False` → raise `AuthError("USER_DISABLED", "Tài khoản bị khoá")`.
   - Issue token pair MỚI.
   - **Blacklist jti CŨ ngay**: `redis.set("blacklist:" + claims.jti, "1", ex=jwt_manager.refresh_ttl_seconds)`. Cũng UPDATE `refresh_tokens SET revoked_at = NOW() WHERE token_hash = sha256(req.refresh_token)`.
   - INSERT refresh_tokens hash mới.
   - Trả `LoginResponse` mới.

3. **`async def logout(access_jti: str, access_exp: int, refresh_token: str | None) -> None`**
   - Blacklist access jti: `redis.set("blacklist:" + access_jti, "1", ex=max(1, access_exp - int(now.timestamp())))`.
   - Nếu body có refresh_token: verify → blacklist refresh jti + UPDATE `refresh_tokens.revoked_at = NOW()`.

4. **`async def get_current_user_info(user_id: str) -> UserPublic`**
   - Query user + hub_assignments → return `UserPublic`.

**Exception class** `AuthError(code: str, message: str)` — router catch và map qua envelope error helper.

Paste-ready (skeleton — executor implement đầy đủ method body):

```python
"""Auth service layer — Plan 03-04 (AUTH-01..03).

Logic 4 use-case + Redis blacklist + anti-timing oracle (Pitfall 6 + T-03-pw-timing).
P16 mitigation: SETNX atomic lock trên jti cũ trước khi rotate refresh token.
"""
from __future__ import annotations

import hashlib
import logging
from datetime import UTC, datetime, timedelta
from typing import Any

from redis.asyncio import Redis
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.jwt import JWTError, JWTManager
from app.auth.password import verify_password
from app.auth.schemas import (
    LoginRequest,
    LoginResponse,
    RefreshRequest,
    UserPublic,
)
from app.models.auth import RefreshToken, User, UserHub

logger = logging.getLogger(__name__)


class AuthError(Exception):
    """Auth-specific error → router map sang envelope error response."""

    def __init__(self, code: str, message: str) -> None:
        self.code = code
        self.message = message
        super().__init__(f"[{code}] {message}")


def _hash_refresh_token(token: str) -> str:
    """T-02-03 mitigation — store SHA-256 thay vì plaintext refresh token."""
    return hashlib.sha256(token.encode("utf-8")).hexdigest()


class AuthService:
    """4 use-case: login, refresh, logout, get_current_user_info."""

    def __init__(
        self,
        db: AsyncSession,
        redis: Redis | None,
        jwt_manager: JWTManager,
        dummy_password_hash: str,
    ) -> None:
        self.db = db
        self.redis = redis
        self.jwt_manager = jwt_manager
        self.dummy_password_hash = dummy_password_hash

    async def login(self, req: LoginRequest) -> LoginResponse:
        """AUTH-01 — verify credential, issue tokens, store refresh hash."""
        # Anti-timing — luôn verify_password kể cả user không tồn tại.
        stmt = select(User).where(
            User.email == str(req.email), User.is_active.is_(True)
        )
        user = (await self.db.execute(stmt)).scalar_one_or_none()
        hash_to_check = user.password_hash if user else self.dummy_password_hash
        ok = verify_password(req.password, hash_to_check)
        if user is None or not ok:
            logger.info(
                "auth_login_failed",
                extra={"email_present": user is not None, "reason": "credential"},
            )
            raise AuthError(
                "INVALID_CREDENTIALS", "Email hoặc mật khẩu không đúng"
            )

        # Hub assignments (USER-03 multi-hub).
        hub_stmt = select(UserHub.hub_id).where(UserHub.user_id == user.id)
        hub_ids = [str(h) for h in (await self.db.execute(hub_stmt)).scalars().all()]

        pair = self.jwt_manager.issue_token_pair(
            user_id=str(user.id),
            email=user.email,
            full_name=user.full_name,
            role=user.role,
            hub_ids=hub_ids,
        )

        # Lưu refresh hash (T-02-03 mitigation — KHÔNG plaintext).
        rt = RefreshToken(
            user_id=user.id,
            token_hash=_hash_refresh_token(pair.refresh_token),
            expires_at=pair.refresh_expires_at,
        )
        self.db.add(rt)
        await self.db.flush()

        logger.info("auth_login_success", extra={"user_id": str(user.id), "role": user.role})
        return LoginResponse(
            access_token=pair.access_token,
            refresh_token=pair.refresh_token,
            expires_at=int(pair.access_expires_at.timestamp()),
            user=UserPublic(
                id=str(user.id),
                email=user.email,
                full_name=user.full_name,
                role=user.role,
                hub_assignments=hub_ids,
            ),
        )

    async def refresh(self, req: RefreshRequest) -> LoginResponse:
        """AUTH-02 — rotate refresh token với SETNX atomic lock (P16)."""
        try:
            claims = self.jwt_manager.verify_token(
                req.refresh_token, expected_type="refresh"
            )
        except JWTError as e:
            raise AuthError("INVALID_REFRESH_TOKEN", str(e)) from e

        # P16 mitigation — SETNX atomic lock 30s trên jti cũ.
        if self.redis is not None:
            lock_key = f"lock:refresh:{claims.jti}"
            acquired = await self.redis.set(lock_key, "1", nx=True, ex=30)
            if not acquired:
                raise AuthError(
                    "REFRESH_RACE",
                    "Refresh token đang được xử lý — vui lòng thử lại",
                )
            # Check blacklist NGAY SAU lock acquired (case: refresh đã hoàn tất trước
            # khi lock TTL expire — old jti đã trong blacklist).
            already_revoked = await self.redis.exists(f"blacklist:{claims.jti}")
            if already_revoked:
                raise AuthError("TOKEN_REVOKED", "Refresh token đã bị thu hồi")
        else:
            logger.warning("auth_refresh_no_redis_lock — P16 race protection disabled")

        # Load user.
        from uuid import UUID

        stmt = select(User).where(User.id == UUID(claims.sub), User.is_active.is_(True))
        user = (await self.db.execute(stmt)).scalar_one_or_none()
        if user is None:
            raise AuthError("USER_DISABLED", "Tài khoản đã bị vô hiệu hoá")

        # Hub assignments fresh.
        hub_stmt = select(UserHub.hub_id).where(UserHub.user_id == user.id)
        hub_ids = [str(h) for h in (await self.db.execute(hub_stmt)).scalars().all()]

        pair = self.jwt_manager.issue_token_pair(
            user_id=str(user.id),
            email=user.email,
            full_name=user.full_name,
            role=user.role,
            hub_ids=hub_ids,
        )

        # Blacklist old jti + revoke DB row.
        if self.redis is not None:
            await self.redis.set(
                f"blacklist:{claims.jti}",
                "1",
                ex=self.jwt_manager.refresh_ttl_seconds,
            )
        old_hash = _hash_refresh_token(req.refresh_token)
        # UPDATE refresh_tokens SET revoked_at = NOW() WHERE token_hash = :h
        from sqlalchemy import update as sa_update

        await self.db.execute(
            sa_update(RefreshToken)
            .where(RefreshToken.token_hash == old_hash)
            .values(revoked_at=datetime.now(tz=UTC))
        )

        # INSERT new refresh hash.
        rt = RefreshToken(
            user_id=user.id,
            token_hash=_hash_refresh_token(pair.refresh_token),
            expires_at=pair.refresh_expires_at,
        )
        self.db.add(rt)
        await self.db.flush()

        return LoginResponse(
            access_token=pair.access_token,
            refresh_token=pair.refresh_token,
            expires_at=int(pair.access_expires_at.timestamp()),
            user=UserPublic(
                id=str(user.id),
                email=user.email,
                full_name=user.full_name,
                role=user.role,
                hub_assignments=hub_ids,
            ),
        )

    async def logout(
        self,
        *,
        access_jti: str,
        access_exp: int,
        refresh_token: str | None,
    ) -> None:
        """AUTH-03 — blacklist access (và refresh nếu body cung cấp)."""
        if self.redis is None:
            logger.warning("auth_logout_no_redis — blacklist disabled, defer Phase 10")
            return
        now_ts = int(datetime.now(tz=UTC).timestamp())
        access_ttl = max(1, access_exp - now_ts)
        await self.redis.set(f"blacklist:{access_jti}", "1", ex=access_ttl)

        if refresh_token:
            try:
                claims = self.jwt_manager.verify_token(
                    refresh_token, expected_type="refresh"
                )
                await self.redis.set(
                    f"blacklist:{claims.jti}",
                    "1",
                    ex=self.jwt_manager.refresh_ttl_seconds,
                )
                from sqlalchemy import update as sa_update

                old_hash = _hash_refresh_token(refresh_token)
                await self.db.execute(
                    sa_update(RefreshToken)
                    .where(RefreshToken.token_hash == old_hash)
                    .values(revoked_at=datetime.now(tz=UTC))
                )
            except JWTError:
                logger.info("auth_logout_invalid_refresh — bỏ qua")

    async def get_current_user_info(self, user_id: str) -> UserPublic:
        """AUTH-03 — GET /api/auth/me data."""
        from uuid import UUID

        stmt = select(User).where(User.id == UUID(user_id), User.is_active.is_(True))
        user = (await self.db.execute(stmt)).scalar_one_or_none()
        if user is None:
            raise AuthError("USER_NOT_FOUND", "Không tìm thấy tài khoản")
        hub_stmt = select(UserHub.hub_id).where(UserHub.user_id == user.id)
        hub_ids = [str(h) for h in (await self.db.execute(hub_stmt)).scalars().all()]
        return UserPublic(
            id=str(user.id),
            email=user.email,
            full_name=user.full_name,
            role=user.role,
            hub_assignments=hub_ids,
        )
```
</action>
<read_first>
- Hub_All/api/app/auth/jwt.py (Plan 03-02)
- Hub_All/api/app/auth/password.py (Plan 03-03)
- Hub_All/api/app/auth/schemas.py (Task 01)
- Hub_All/api/app/models/auth.py
- Hub_All/api/app/db/session.py
- Hub_All/backend/internal/service/auth_service.go
- Hub_All/.planning/research/PITFALLS.md (P16 — refresh race)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/auth/service.py` tồn tại.
- `grep -c 'class AuthError(Exception)' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'class AuthService' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'async def login' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'async def refresh' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'async def logout' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'async def get_current_user_info' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'self.dummy_password_hash' Hub_All/api/app/auth/service.py` ≥ 2 (anti-timing call site).
- `grep -c 'set(lock_key, "1", nx=True, ex=30)' Hub_All/api/app/auth/service.py` ≥ 1 (P16 SETNX).
- `grep -c 'blacklist:' Hub_All/api/app/auth/service.py` ≥ 3 (blacklist key bake login + refresh + logout).
- `grep -c '_hash_refresh_token' Hub_All/api/app/auth/service.py` ≥ 4 (login insert + refresh insert/update + logout update).
- `grep -c 'INVALID_CREDENTIALS' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'INVALID_REFRESH_TOKEN' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'TOKEN_REVOKED' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'REFRESH_RACE' Hub_All/api/app/auth/service.py` ≥ 1.
- `grep -c 'USER_DISABLED' Hub_All/api/app/auth/service.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.auth.service import AuthService, AuthError, _hash_refresh_token; h = _hash_refresh_token('abc'); assert len(h)==64; print('imports + hash OK')"` exits 0.
- `cd Hub_All/api && uv run ruff check app/auth/service.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth/service.py` exits 0.
</acceptance_criteria>
</task>

<task id="03">
<action>
Tạo `Hub_All/api/app/auth/dependencies.py` — FastAPI dependencies: `get_jwt_manager`, `get_auth_service`, `get_current_user`. `require_role` chỉ skeleton, Plan 03-05 EXTEND đầy đủ.

**3 dependency + 1 skeleton:**

1. `get_jwt_manager(request: Request) -> JWTManager` — lấy từ `request.app.state.jwt_manager` (lifespan Task 05 sẽ init).
2. `get_auth_service(db, redis, jwt_mgr, dummy_hash) -> AuthService` — compose từ `get_session` (Phase 2 Plan 01) + `request.app.state.redis` (Phase 1 lifespan) + `get_jwt_manager` + `request.app.state.dummy_password_hash`.
3. `get_current_user(request, token=Depends(oauth2_scheme), jwt_mgr, redis, db) -> User` — extract Bearer → verify_token (access) → check Redis blacklist `blacklist:<jti>` → load `User` từ DB qua `claims.sub`. Raise `HTTPException(401, code)` với `code` cụ thể (MISSING_AUTHORIZATION / INVALID_AUTHORIZATION_FORMAT / INVALID_TOKEN / TOKEN_REVOKED / USER_DISABLED).
4. `require_role(*roles)` — Plan 03-05 sẽ extend; ở Plan 03-04 chỉ tạo signature stub return `Callable` raise NotImplementedError nếu gọi (KHÔNG dùng trong router 04 vì 4 endpoint không cần role check — login/refresh/logout/me chỉ cần authenticate).

**oauth2_scheme:** dùng `fastapi.security.OAuth2PasswordBearer(tokenUrl="/api/auth/login", auto_error=False)` — `auto_error=False` để KHÔNG raise 403 tự động (envelope mình muốn 401 với code cụ thể).

Paste-ready:

```python
"""FastAPI dependencies — Plan 03-04 (AUTH-01..03) + skeleton AUTH-04 (Plan 03-05).

Dependencies:
- get_jwt_manager       — lấy JWTManager singleton từ app.state (init lifespan).
- get_auth_service      — compose AuthService với DB session + Redis + JWT.
- get_current_user      — extract Bearer + verify + blacklist check → User.
- require_role          — SKELETON (Plan 03-05 implement đầy đủ).
"""
from __future__ import annotations

import logging
from collections.abc import Callable
from typing import TYPE_CHECKING

from fastapi import Depends, HTTPException, Request, status
from fastapi.security import OAuth2PasswordBearer
from redis.asyncio import Redis
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.jwt import JWTError, JWTManager
from app.auth.service import AuthService
from app.db.session import get_session
from app.models.auth import User

if TYPE_CHECKING:
    pass

logger = logging.getLogger(__name__)

# auto_error=False → 401 với code cụ thể qua HTTPException, KHÔNG để FastAPI raise 403 mặc định.
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/login", auto_error=False)


def get_jwt_manager(request: Request) -> JWTManager:
    """Lấy JWTManager singleton (init ở lifespan Task 05)."""
    mgr = getattr(request.app.state, "jwt_manager", None)
    if mgr is None:
        raise HTTPException(
            status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
            detail={"code": "SERVICE_UNAVAILABLE", "message": "JWTManager chưa init"},
        )
    return mgr


def get_redis(request: Request) -> Redis | None:
    """Lấy Redis client (None nếu Redis down — Phase 3 fail-open)."""
    return getattr(request.app.state, "redis", None)


def get_dummy_password_hash(request: Request) -> str:
    """Lấy dummy hash pre-computed cho anti-timing — init ở lifespan."""
    h = getattr(request.app.state, "dummy_password_hash", None)
    if h is None:
        raise HTTPException(
            status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
            detail={"code": "SERVICE_UNAVAILABLE", "message": "dummy_password_hash chưa init"},
        )
    return h


def get_auth_service(
    db: AsyncSession = Depends(get_session),
    redis: Redis | None = Depends(get_redis),
    jwt_mgr: JWTManager = Depends(get_jwt_manager),
    dummy_hash: str = Depends(get_dummy_password_hash),
) -> AuthService:
    return AuthService(db=db, redis=redis, jwt_manager=jwt_mgr, dummy_password_hash=dummy_hash)


async def get_current_user(
    token: str | None = Depends(oauth2_scheme),
    jwt_mgr: JWTManager = Depends(get_jwt_manager),
    redis: Redis | None = Depends(get_redis),
    db: AsyncSession = Depends(get_session),
) -> User:
    """Verify Bearer access token → User entity.

    Trình tự reject:
    1. Token rỗng → 401 MISSING_AUTHORIZATION
    2. Sai signature/expired/sai issuer/sai alg → 401 INVALID_TOKEN
    3. Sai type (refresh thay vì access) → 401 INVALID_TOKEN_TYPE
    4. JTI trong Redis blacklist → 401 TOKEN_REVOKED
    5. User không tồn tại / disabled → 401 USER_DISABLED
    """
    if not token:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "MISSING_AUTHORIZATION", "message": "Yêu cầu Bearer token"},
            headers={"WWW-Authenticate": "Bearer"},
        )

    try:
        claims = jwt_mgr.verify_token(token, expected_type="access")
    except JWTError as e:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "INVALID_TOKEN", "message": str(e)},
            headers={"WWW-Authenticate": "Bearer"},
        ) from e

    # Blacklist check.
    if redis is not None:
        is_blacklisted = await redis.exists(f"blacklist:{claims.jti}")
        if is_blacklisted:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={"code": "TOKEN_REVOKED", "message": "Token đã bị thu hồi"},
                headers={"WWW-Authenticate": "Bearer"},
            )

    # Load user.
    from uuid import UUID

    stmt = select(User).where(User.id == UUID(claims.sub), User.is_active.is_(True))
    user = (await db.execute(stmt)).scalar_one_or_none()
    if user is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "USER_DISABLED", "message": "Tài khoản đã bị vô hiệu hoá"},
        )
    return user


def require_role(*roles: str) -> Callable:
    """Plan 03-05 sẽ implement đầy đủ.

    Hiện tại Plan 03-04 KHÔNG dùng (4 endpoint login/refresh/logout/me chỉ
    cần authenticate, không cần role-gate). Stub raise NotImplementedError
    nếu gọi để guard regression.
    """

    async def _dependency(user: User = Depends(get_current_user)) -> User:
        raise NotImplementedError(
            "require_role chưa implement — Plan 03-05 sẽ extend. "
            f"Requested roles: {roles}"
        )

    return _dependency
```
</action>
<read_first>
- Hub_All/api/app/auth/jwt.py
- Hub_All/api/app/auth/service.py (Task 02)
- Hub_All/api/app/db/session.py
- Hub_All/api/app/models/auth.py
- Hub_All/backend/internal/middleware/auth.go
- Hub_All/api/app/main.py (xem app.state.redis pattern)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/auth/dependencies.py` tồn tại.
- `grep -c 'def get_jwt_manager' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'def get_auth_service' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'async def get_current_user' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'def require_role' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'OAuth2PasswordBearer(tokenUrl="/api/auth/login", auto_error=False)' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'MISSING_AUTHORIZATION' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'INVALID_TOKEN' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'TOKEN_REVOKED' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'blacklist:' Hub_All/api/app/auth/dependencies.py` ≥ 1.
- `grep -c 'dummy_password_hash' Hub_All/api/app/auth/dependencies.py` ≥ 2.
- `cd Hub_All/api && uv run python -c "from app.auth.dependencies import get_jwt_manager, get_auth_service, get_current_user, require_role, oauth2_scheme; print('imports OK')"` exits 0.
- `cd Hub_All/api && uv run ruff check app/auth/dependencies.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth/dependencies.py` exits 0.
</acceptance_criteria>
</task>

<task id="04">
<action>
Tạo `Hub_All/api/app/auth/router.py` — FastAPI `APIRouter` 4 endpoint, response envelope qua `resp.ok/created/...` (Plan 03-01).

**4 endpoint:**

1. `POST /api/auth/login` — body `LoginRequest`, return envelope với `LoginResponse.model_dump()`.
2. `POST /api/auth/refresh` — body `RefreshRequest`.
3. `POST /api/auth/logout` — Header Bearer (Depends `get_current_user`), body `LogoutRequest` (optional). Cần trả 200 ngay cả nếu Redis down (fail-open).
4. `GET /api/auth/me` — Header Bearer (Depends `get_current_user`) → envelope với `UserPublic`.

**Lý do tách routing khỏi service:** Service return Pydantic model (dễ test pure Python); router gọi service + wrap qua envelope helper. Frontend D6 dùng envelope.

**Error handling:** Catch `AuthError` → map code → response code envelope. Cho login/refresh: `INVALID_CREDENTIALS, INVALID_REFRESH_TOKEN, USER_DISABLED, REFRESH_RACE, TOKEN_REVOKED` → 401. Pydantic ValidationError tự FastAPI handle (422 default — wrap qua handler? Phase 3 chấp nhận FastAPI default 422 chứa `detail` field; envelope-compat sẽ defer hardening Plan 03-05).

Paste-ready:

```python
"""Auth router — Plan 03-04 (AUTH-01..03).

4 endpoint:
    POST /api/auth/login    — body {email, password} → 200 LoginResponse / 401 INVALID_CREDENTIALS
    POST /api/auth/refresh  — body {refresh_token} → 200 LoginResponse / 401 INVALID_REFRESH_TOKEN
    POST /api/auth/logout   — Bearer + optional body {refresh_token} → 200
    GET  /api/auth/me       — Bearer → 200 UserPublic / 401 INVALID_TOKEN

Mọi response qua `app.pkg.response.ok/error_*` envelope (Plan 03-01) — KHÔNG
return Pydantic model raw để D6 frontend compat.
"""
from __future__ import annotations

import logging

from fastapi import APIRouter, Depends, Request
from fastapi.responses import JSONResponse

from app.auth.dependencies import (
    get_auth_service,
    get_current_user,
    get_jwt_manager,
)
from app.auth.jwt import JWTManager
from app.auth.schemas import (
    LoginRequest,
    LogoutRequest,
    RefreshRequest,
)
from app.auth.service import AuthError, AuthService
from app.models.auth import User
from app.pkg import response as resp

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/auth", tags=["auth"])


def _auth_error_to_response(e: AuthError) -> JSONResponse:
    """Map AuthError.code → 401 envelope với code Go-compat."""
    return resp.unauthorized(message=e.message, code=e.code)


@router.post("/login")
async def login(
    req: LoginRequest,
    service: AuthService = Depends(get_auth_service),
) -> JSONResponse:
    try:
        result = await service.login(req)
    except AuthError as e:
        return _auth_error_to_response(e)
    return resp.ok(data=result.model_dump())


@router.post("/refresh")
async def refresh(
    req: RefreshRequest,
    service: AuthService = Depends(get_auth_service),
) -> JSONResponse:
    try:
        result = await service.refresh(req)
    except AuthError as e:
        return _auth_error_to_response(e)
    return resp.ok(data=result.model_dump())


@router.post("/logout")
async def logout(
    request: Request,
    body: LogoutRequest | None = None,
    user: User = Depends(get_current_user),
    service: AuthService = Depends(get_auth_service),
    jwt_mgr: JWTManager = Depends(get_jwt_manager),
) -> JSONResponse:
    # Lấy access token + jti từ header (verify lại để có jti + exp).
    auth_header = request.headers.get("authorization", "")
    parts = auth_header.split(" ", 1)
    if len(parts) != 2 or parts[0].lower() != "bearer":
        return resp.unauthorized(
            message="Sai format Authorization header", code="INVALID_AUTHORIZATION_FORMAT"
        )
    token = parts[1]
    try:
        claims = jwt_mgr.verify_token(token, expected_type="access")
    except Exception:  # noqa: BLE001
        return resp.unauthorized(message="Token không hợp lệ", code="INVALID_TOKEN")

    refresh_token = body.refresh_token if body else None
    try:
        await service.logout(
            access_jti=claims.jti,
            access_exp=claims.exp,
            refresh_token=refresh_token,
        )
    except AuthError as e:
        return _auth_error_to_response(e)
    return resp.ok(data={"message": "Đăng xuất thành công"})


@router.get("/me")
async def me(
    user: User = Depends(get_current_user),
    service: AuthService = Depends(get_auth_service),
) -> JSONResponse:
    try:
        public = await service.get_current_user_info(str(user.id))
    except AuthError as e:
        return _auth_error_to_response(e)
    return resp.ok(data=public.model_dump())
```

**Cập nhật `Hub_All/api/app/auth/__init__.py`** thêm `router` + `AuthService` + `get_current_user` + `AuthError` re-export. Sau plan 03 đã có 11 export, plan 04 thêm 4 mới = 15 export. Đầy đủ:

```python
"""Auth package — Phase 3 (AUTH-01..06)."""
from __future__ import annotations

from app.auth.dependencies import (
    get_auth_service,
    get_current_user,
    get_jwt_manager,
    oauth2_scheme,
    require_role,
)
from app.auth.jwt import (
    JWT_ALGORITHM,
    JWT_ISSUER,
    JWTClaims,
    JWTError,
    JWTManager,
    TokenPair,
)
from app.auth.password import (
    ARGON2_HASH_LEN,
    ARGON2_MEMORY_COST,
    ARGON2_PARALLELISM,
    ARGON2_SALT_LEN,
    ARGON2_TIME_COST,
    hash_password,
    verify_password,
)
from app.auth.router import router as auth_router
from app.auth.service import AuthError, AuthService

__all__ = [
    "ARGON2_HASH_LEN",
    "ARGON2_MEMORY_COST",
    "ARGON2_PARALLELISM",
    "ARGON2_SALT_LEN",
    "ARGON2_TIME_COST",
    "AuthError",
    "AuthService",
    "JWT_ALGORITHM",
    "JWT_ISSUER",
    "JWTClaims",
    "JWTError",
    "JWTManager",
    "TokenPair",
    "auth_router",
    "get_auth_service",
    "get_current_user",
    "get_jwt_manager",
    "hash_password",
    "oauth2_scheme",
    "require_role",
    "verify_password",
]
```
</action>
<read_first>
- Hub_All/api/app/auth/schemas.py (Task 01)
- Hub_All/api/app/auth/service.py (Task 02)
- Hub_All/api/app/auth/dependencies.py (Task 03)
- Hub_All/api/app/pkg/response.py (Plan 03-01 — error code UPPER_SNAKE)
- Hub_All/backend/internal/handler/auth_handler.go (shape reference)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/auth/router.py` tồn tại.
- `grep -c 'router = APIRouter(prefix="/api/auth", tags=\["auth"\])' Hub_All/api/app/auth/router.py` ≥ 1.
- `grep -c '@router.post("/login")' Hub_All/api/app/auth/router.py` ≥ 1.
- `grep -c '@router.post("/refresh")' Hub_All/api/app/auth/router.py` ≥ 1.
- `grep -c '@router.post("/logout")' Hub_All/api/app/auth/router.py` ≥ 1.
- `grep -c '@router.get("/me")' Hub_All/api/app/auth/router.py` ≥ 1.
- `grep -c 'resp.ok(data=' Hub_All/api/app/auth/router.py` ≥ 4 (4 endpoint success path).
- `grep -c 'resp.unauthorized' Hub_All/api/app/auth/router.py` ≥ 2.
- `grep -c '_auth_error_to_response' Hub_All/api/app/auth/router.py` ≥ 4 (error mapping reuse).
- File `Hub_All/api/app/auth/__init__.py` re-exports `auth_router`: `grep -c 'auth_router' Hub_All/api/app/auth/__init__.py` ≥ 2.
- `cd Hub_All/api && uv run python -c "from app.auth import auth_router, AuthService, get_current_user, AuthError; print(auth_router.prefix)"` exits 0 với output `/api/auth`.
- `cd Hub_All/api && uv run ruff check app/auth/router.py app/auth/__init__.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth/router.py` exits 0.
</acceptance_criteria>
</task>

<task id="05">
<action>
Cập nhật `Hub_All/api/app/main.py` — wire JWTManager + dummy_password_hash vào lifespan + include `auth_router` trong `create_app`.

**Sửa 2 chỗ:**

1. **Lifespan startup** — sau khi init redis + cocoindex, thêm:

```python
    # 4) JWTManager — init khoá RS256 từ keys/private.pem + public.pem.
    try:
        from app.auth import JWTManager
        app.state.jwt_manager = JWTManager(settings)
        logger.info(
            "jwt_manager_ready: access_ttl=%ds refresh_ttl=%ds",
            settings.jwt_access_token_ttl,
            settings.jwt_refresh_token_ttl,
        )
    except Exception as e:  # noqa: BLE001
        app.state.jwt_manager = None
        logger.warning("jwt_manager_init_failed: %s", e)

    # 5) Anti-timing dummy hash — pre-compute 1 hash để service.login dùng
    #    khi user không tồn tại (response time KHÔNG leak email enumeration).
    try:
        from app.auth import hash_password
        app.state.dummy_password_hash = hash_password("dummy-not-a-real-password-Đ#")
        logger.info("dummy_password_hash_ready (anti-timing oracle)")
    except Exception as e:  # noqa: BLE001
        # Fallback dummy hash (định dạng hợp lệ nhưng KHÔNG verify được password thật).
        app.state.dummy_password_hash = (
            "$argon2id$v=19$m=65536,t=3,p=4"
            "$AAAAAAAAAAAAAAAAAAAAAAA"
            "$AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
        )
        logger.warning("dummy_password_hash_fallback: %s", e)
```

2. **Lifespan shutdown** — thêm reset `app.state.jwt_manager = None` (defensive, không required nhưng tốt cho test isolation).

3. **`create_app()`** — sau `app.add_middleware(ErrorHandlerMiddleware)` (Plan 03-01), THÊM:

```python
    # Mount auth router (Phase 3 AUTH-01..04).
    from app.auth import auth_router
    app.include_router(auth_router)
```

**KHÔNG đổi:** `healthz`, `readyz`, middleware order (Plan 03-01 đã set đúng).

**Update `readyz`** — thêm check `jwt_manager`:

```python
        # JWT manager check (Phase 3).
        if app.state.jwt_manager is not None:
            checks["jwt"] = "ok"
        else:
            checks["jwt"] = "not_ready"
            all_ok = False
```
</action>
<read_first>
- Hub_All/api/app/main.py
- Hub_All/api/app/auth/__init__.py (Task 04 đã có `auth_router`)
- Hub_All/api/app/auth/jwt.py
- Hub_All/api/app/auth/password.py
</read_first>
<acceptance_criteria>
- `grep -c 'from app.auth import JWTManager' Hub_All/api/app/main.py` ≥ 1 (trong lifespan).
- `grep -c 'app.state.jwt_manager = JWTManager(settings)' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'app.state.dummy_password_hash' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'app.include_router(auth_router)' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'jwt_manager_ready' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'checks\["jwt"\]' Hub_All/api/app/main.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.main import create_app; app = create_app(); paths = sorted({r.path for r in app.routes}); print(paths)" | grep -c '/api/auth/login'` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.main import create_app; app = create_app(); paths = sorted({r.path for r in app.routes}); assert '/api/auth/login' in paths and '/api/auth/refresh' in paths and '/api/auth/logout' in paths and '/api/auth/me' in paths; print('4 routes OK')"` exits 0 với output `4 routes OK`.
- `cd Hub_All/api && uv run ruff check app/main.py` exits 0.
- `cd Hub_All/api && uv run mypy app/main.py` exits 0.
</acceptance_criteria>
</task>

## Verification

- `cd Hub_All/api && uv run python -c "from app.main import create_app; app = create_app(); paths = {r.path for r in app.routes}; required = {'/api/auth/login','/api/auth/refresh','/api/auth/logout','/api/auth/me','/healthz','/readyz'}; missing = required - paths; assert not missing, missing; print('all routes OK')"` exits 0.
- `cd Hub_All/api && uv run ruff check app/auth app/main.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth app/main.py` exits 0.
- `cd Hub_All/api && uv run pytest tests/unit -v` PASS (existing unit test Phase 3 Plan 02/03 không broken).
- `cd Hub_All/api && uv run python -c "from app.auth import auth_router, AuthService, get_current_user, AuthError, JWTManager, JWTError, hash_password, verify_password, require_role; print('all exports OK')"` exits 0.

## Threat Model

**Trust boundaries:**

| Boundary | Description |
|---|---|
| Client `POST /api/auth/login` body → service `login` | Untrusted email + password. Pydantic validate email format + password length. Service luôn `verify_password` (anti-timing). |
| Client `POST /api/auth/refresh` body → Redis SETNX lock | P16 — multiple tab/curl race trên cùng refresh token. SETNX atomic = chỉ 1 winner. |
| Header `Authorization: Bearer ...` → `get_current_user` | Untrusted token. Verify alg=RS256 (Plan 03-02), issuer, exp, jti blacklist. |
| `refresh_tokens.token_hash` DB column | Lưu SHA-256, KHÔNG plaintext (T-02-03 Phase 2 mitigation đã áp dụng schema; Plan 03-04 service code dùng đúng). |

**STRIDE Threat Register:**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04-timing-oracle | I (Information disclosure) | `service.login` skip `verify_password` khi user không tồn tại → timing diff cho phép attacker enumerate email | mitigate | `service.login` LUÔN call `verify_password(req.password, hash_to_check)` — dùng `dummy_password_hash` pre-computed khi user None. Test Plan 03-05 đo response time delta (optional load test). |
| T-03-04-refresh-race (P16) | E (Elevation) + T | Concurrent 5 tab refresh cùng refresh_token → race condition: 2 tab cùng pass verify, cùng issue new pair → 2 active sessions từ 1 stolen token | mitigate | Redis SETNX `lock:refresh:<jti>` TTL=30s. Plan 03-05 integration test verify concurrent 5 call → 1 success, 4 lần 401 REFRESH_RACE. |
| T-03-04-token-fixation | E | Attacker pre-issue token rồi force user login với token cũ — refresh KHÔNG rotate jti | mitigate | Mỗi `issue_token_pair` sinh `jti = uuid4()` MỚI. Old jti immediately blacklisted sau rotate. Attacker pre-issue token không có path inject vào user session. |
| T-03-04-refresh-replay | E | Attacker steal refresh_token cũ + replay sau khi user đã refresh | mitigate | Sau rotate, old jti vào `blacklist:<jti>` TTL=refresh_ttl (7 ngày). Replay → check blacklist → 401 TOKEN_REVOKED. Test Plan 03-05. |
| T-03-04-logout-incomplete | E | Logout chỉ blacklist access, refresh vẫn dùng được → attacker giữ access đến exp + dùng refresh sinh access mới | mitigate | `logout` endpoint accept optional `refresh_token` body. Frontend D6 PHẢI gửi cả 2 khi logout (frontend code chưa sửa M2 — Phase 5 USER-* sẽ document). Plan 03-04 backend hỗ trợ đầy đủ; client compliance là responsibility riêng. |
| T-03-04-pii-log | I | Log refresh_token plaintext / password trong service hoặc dependencies | mitigate | Toàn bộ logger.info trong service.py chỉ log `user_id`, `role`, `email_present` (bool). KHÔNG bao giờ log `req.password`, `req.refresh_token`, `token_hash`. Convention bake CONVENTIONS.md section 5. Plan 03-05 grep regression check. |
| T-03-04-redis-down-fail-open | E | Redis down → blacklist skip → revoked token vẫn pass | accept | Phase 3 fail-open: Redis nil → log warning + tiếp tục. Rationale: M2 chấp nhận risk staleness vì Redis là cache, KHÔNG primary security. Phase 10 HARD-04 sẽ siết: Redis down → 503 toàn bộ auth endpoint. |

## Output

Sau khi complete tạo `Hub_All/.planning/phases/03-auth-port-rbac-response-envelope/03-04-SUMMARY.md`:
- 6 file modified/created.
- 4 endpoint mount thành công vào FastAPI app.
- JWTManager + dummy_password_hash init ở lifespan.
- Plan 03-05 sẵn sàng test toàn flow + extend `require_role` đầy đủ + integration test concurrent refresh race + JWT compat.
