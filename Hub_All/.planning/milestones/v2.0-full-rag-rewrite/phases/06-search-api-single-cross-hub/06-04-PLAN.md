---
phase: 06-search-api-single-cross-hub
plan: 04
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - Hub_All/api/tests/integration/conftest.py
  - Hub_All/api/tests/integration/test_search_hub_isolation.py
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04]
must_haves:
  truths:
    - "Viewer Hub A search với explicit hub_ids:[A,B] chỉ nhận chunk Hub A — KHÔNG thấy chunk Hub B (E4)"
    - "POST /api/search/cross-hub viewer Hub A+B chỉ trả chunk 2 hub đó, không hub C"
    - "Empty hub (chưa có chunk) trả results: [] status 200 KHÔNG lỗi"
    - "EXPLAIN ANALYZE vector query show Index Scan using ix_chunks_vector_hnsw KHÔNG Seq Scan"
    - "Search lần 1 cache_hit=false, lần 2 (cùng query) cache_hit=true"
    - "Sau document upload trong hub → cache invalidate, search lần kế tiếp cache_hit=false"
  artifacts:
    - path: "Hub_All/api/tests/integration/test_search_hub_isolation.py"
      provides: "E4 critical test suite cho search — hub isolation + cross-hub + cache + EXPLAIN + empty"
      contains: "@pytest.mark.critical"
    - path: "Hub_All/api/tests/integration/conftest.py"
      provides: "_insert_document + _insert_chunk helper seed chunks với vector"
      contains: "_insert_chunk"
  key_links:
    - from: "test_search_hub_isolation.py"
      to: "POST /api/search"
      via: "auth_client.post với viewer_token"
      pattern: "/api/search"
    - from: "test_search_hub_isolation.py"
      to: "ix_chunks_vector_hnsw"
      via: "EXPLAIN ANALYZE assert"
      pattern: "ix_chunks_vector_hnsw"
---

<objective>
Verify Phase 6 bằng integration test suite — critical-path-mandatory testing (CONVENTIONS §1): hub isolation E4 (điều kiện ship), cross-hub isolation, empty-result, EXPLAIN ANALYZE HNSW Index Scan, Redis cache hit + invalidation.

Purpose: Hub isolation cho search là EXIT-criteria-grade (E4) — viewer Hub A KHÔNG BAO GIỜ thấy chunk Hub B kể cả khi truyền explicit `hub_ids` chứa B. Test này chứng minh điều đó genuinely PASS against real DB. Cũng verify R2 mitigation (HNSW Index Scan, không Seq Scan — SC3) + SEARCH-04 cache.

KHÔNG phụ thuộc embedding API thật — `OPENAI_API_KEY` hiện là placeholder và `chunks` table rỗng. Test seed chunks với vector cố định trực tiếp qua SQL; query embedding được monkeypatch `embed_text` trả vector deterministic.

Output: `tests/integration/conftest.py` (thêm helper `_insert_document` + `_insert_chunk`), `tests/integration/test_search_hub_isolation.py` (mới — 6+ test, marker `@pytest.mark.critical`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/06-search-api-single-cross-hub/06-CONTEXT.md
@Hub_All/.planning/phases/06-search-api-single-cross-hub/06-PATTERNS.md
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/api/tests/integration/test_hub_isolation.py

<interfaces>
<!-- Contracts từ codebase. -->

conftest.py helpers ĐÃ CÓ (KHÔNG redeclare):
```python
async def _insert_user(*, email, role, full_name) -> str            # return user_id
async def _insert_hub(*, name, code, subdomain) -> str              # return hub_id
async def _assign_user_hub(*, user_id, hub_id) -> None
# fixtures: app_with_auth, auth_client, admin_user/editor_user/viewer_user,
#           admin_token/editor_token/viewer_token
# app_with_auth TRUNCATE mỗi test: users, refresh_tokens, user_hubs, hubs,
#   audit_logs, api_keys, documents, chunks RESTART IDENTITY CASCADE
```

Cột THẬT (migration 0001):
- documents(id UUID PK, hub_id UUID, uploaded_by UUID, filename TEXT, file_path TEXT,
  mime_type, file_size_bytes INT, status TEXT, chunk_count INT, attempts INT,
  created_at, updated_at, last_heartbeat) — status enum: pending|processing|completed|failed|failed_unsupported
- chunks(id UUID PK, document_id UUID FK, hub_id UUID, content TEXT, content_hash BYTEA,
  heading_path TEXT, page_start INT, page_end INT, vector Vector(1536), metadata JSONB, created_at)
- HNSW index tên THẬT: ix_chunks_vector_hnsw (vector_cosine_ops)

embed_text — app/services/search_service.py import `from app.services.embedder import ... embed_text`.
Monkeypatch target: patch `app.services.search_service.embed_text` (tên đã import vào module search_service).

Test seed pattern (test_hub_isolation.py): import `from tests.integration.conftest import _assign_user_hub, _insert_hub`,
INSERT trực tiếp qua `from app.db.session import get_engine; engine = get_engine()`.

Markers (pyproject.toml): `critical`, `integration`. addopts `--strict-markers`.
Response envelope: body["data"]["results"] là list SearchResultItem dict; body["data"]["cache_hit"] bool.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm _insert_document + _insert_chunk helper vào conftest.py</name>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py dòng 261-332 (_insert_user/_insert_hub/_assign_user_hub — pattern INSERT qua get_engine)
    - Hub_All/api/migrations/versions/0001_initial_schema.py §documents §chunks (cột thật)
  </read_first>
  <files>Hub_All/api/tests/integration/conftest.py</files>
  <action>
EXTEND `tests/integration/conftest.py` — thêm 2 helper async module-level, đặt SAU `_assign_user_hub` (dòng ~332). Pattern giống `_insert_hub`: `from app.db.session import get_engine`, `engine = get_engine()`, `async with engine.begin() as conn:`, INSERT qua `text(...)`.

`async def _insert_document(*, hub_id: str, filename: str, uploaded_by: str | None = None) -> str` — return document_id:
```python
async def _insert_document(
    *, hub_id: str, filename: str, uploaded_by: str | None = None
) -> str:
    """INSERT 1 document row, return document_id. Dùng cho search test."""
    from app.db.session import get_engine
    engine = get_engine()
    doc_id = str(uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO documents "
                "(id, hub_id, uploaded_by, filename, file_path, mime_type, "
                "file_size_bytes, status, chunk_count, attempts, "
                "created_at, updated_at, last_heartbeat) "
                "VALUES (:id, :hub, :uploader, :name, :path, "
                "'application/octet-stream', 100, 'completed', 1, 0, "
                "NOW(), NOW(), NOW())"
            ),
            {
                "id": doc_id,
                "hub": hub_id,
                "uploader": uploaded_by,
                "name": filename,
                "path": f"/tmp/{doc_id}",
            },
        )
    return doc_id
```

`async def _insert_chunk(*, document_id: str, hub_id: str, content: str, vector: list[float]) -> str` — return chunk_id. Vector truyền vào DB dạng pgvector literal string `'[...]'`:
```python
async def _insert_chunk(
    *, document_id: str, hub_id: str, content: str, vector: list[float]
) -> str:
    """INSERT 1 chunk row với vector. Dùng cho search test (chunks table rỗng M2)."""
    from app.db.session import get_engine
    engine = get_engine()
    chunk_id = str(uuid.uuid4())
    vec_literal = "[" + ",".join(repr(float(x)) for x in vector) + "]"
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO chunks "
                "(id, document_id, hub_id, content, content_hash, "
                "heading_path, page_start, page_end, vector, metadata, created_at) "
                "VALUES (:id, :doc, :hub, :content, :hash, "
                "'', 1, 1, CAST(:vec AS vector), '{}'::jsonb, NOW())"
            ),
            {
                "id": chunk_id,
                "doc": document_id,
                "hub": hub_id,
                "content": content,
                "hash": b"\\x00" * 4,
                "vec": vec_literal,
            },
        )
    return chunk_id
```
LƯU Ý: `content_hash` là BYTEA NOT NULL → truyền `b"\\x00"*4` (4 byte). `vector` Vector(1536) → literal phải đúng 1536 phần tử (test caller tạo list 1536-dim).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/conftest.py && uv run python -c "import tests.integration.conftest as c; assert hasattr(c,'_insert_document') and hasattr(c,'_insert_chunk')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async def _insert_document" Hub_All/api/tests/integration/conftest.py` match
    - `grep "async def _insert_chunk" Hub_All/api/tests/integration/conftest.py` match
    - `grep "CAST(:vec AS vector)" Hub_All/api/tests/integration/conftest.py` match
    - `uv run python -c "import tests.integration.conftest as c; assert hasattr(c,'_insert_chunk')"` exit 0
    - ruff exit 0
  </acceptance_criteria>
  <done>conftest có `_insert_document` + `_insert_chunk` seed helper; import OK; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo test_search_hub_isolation.py — E4 critical suite (hub isolation + cross-hub + cache + EXPLAIN + empty)</name>
  <read_first>
    - Hub_All/api/tests/integration/test_hub_isolation.py (template E4 — marker + fixtures + assertion pattern)
    - Hub_All/api/tests/integration/conftest.py (helpers _insert_hub/_assign_user_hub/_insert_document/_insert_chunk)
    - Hub_All/.planning/phases/06-search-api-single-cross-hub/06-PATTERNS.md §test_search_hub_isolation.py
    - Hub_All/api/app/services/search_service.py (embed_text import target để monkeypatch)
  </read_first>
  <files>Hub_All/api/tests/integration/test_search_hub_isolation.py</files>
  <action>
Tạo MỚI `Hub_All/api/tests/integration/test_search_hub_isolation.py`.

Imports: `from __future__ import annotations`, `import uuid`, `from typing import Any`, `import pytest`, `from tests.integration.conftest import (_assign_user_hub, _insert_chunk, _insert_document, _insert_hub)`.

Module helper `def _fixed_vector(seed: float = 0.1) -> list[float]` — trả `[seed] * 1536` (vector deterministic 1536-dim).

Module helper async `async def _patch_embed(monkeypatch: Any, vector: list[float]) -> None` — patch query embedding để KHÔNG gọi LiteLLM thật:
```python
async def _fake_embed(text: str, model: str | None = None) -> list[float]:
    return vector
monkeypatch.setattr("app.services.search_service.embed_text", _fake_embed)
```
(Đặt `_fake_embed` định nghĩa trong từng test hoặc helper — monkeypatch target `app.services.search_service.embed_text` vì search_service đã `from app.services.embedder import embed_text`.)

Mọi test marker `@pytest.mark.critical` + `@pytest.mark.integration` + `@pytest.mark.asyncio` (CONVENTIONS §1 — search critical path).

**Test 1 — `test_search_single_hub_returns_results`** (SEARCH-01): seed hub_a, assign viewer, `_insert_document` + `_insert_chunk` (content "khám bệnh đa khoa", vector `_fixed_vector(0.1)`). monkeypatch embed_text trả cùng vector. POST `/api/search` với `viewer_token`, body `{"query":"khám bệnh","hub_ids":[hub_a]}`. Assert `status_code == 200`, `body["data"]["results"]` có ≥1 item, item["hub_id"] == hub_a, `body["data"]["total_hubs_searched"] == 1`.

**Test 2 — `test_search_hub_isolation_explicit_hub_ids`** (E4 — critical nhất): seed hub_a + hub_b. assign viewer CHỈ hub_a. seed chunk vào CẢ hub_a và hub_b (content khác nhau, cùng vector). monkeypatch embed_text. POST `/api/search` body `{"query":"x","hub_ids":[hub_a, hub_b]}` với viewer_token. Assert `status_code == 200`; `all(r["hub_id"] == hub_a for r in body["data"]["results"])` — KHÔNG có chunk hub_b. Message assert: `"E4 VIOLATION — viewer Hub A thấy chunk Hub B"`.

**Test 3 — `test_cross_hub_search_isolation`** (SEARCH-03 E4): seed hub_a + hub_b + hub_c. assign viewer hub_a + hub_b (KHÔNG hub_c). seed chunk vào cả 3 hub. monkeypatch embed_text. POST `/api/search/cross-hub` body `{"query":"x","hub_ids":[hub_a, hub_b, hub_c]}` viewer_token. Assert 200; `set(r["hub_id"] for r in results) <= {hub_a, hub_b}` — hub_c bị loại; `body["data"]["total_hubs_searched"] == 2`.

**Test 4 — `test_search_empty_hub_returns_empty_list`** (Specifics): seed hub_a, assign viewer, KHÔNG seed chunk nào. monkeypatch embed_text. POST `/api/search` body `{"query":"x","hub_ids":[hub_a]}`. Assert `status_code == 200`, `body["data"]["results"] == []`, `body["success"] is True`.

**Test 5 — `test_search_cache_hit`** (SEARCH-04): seed hub_a + chunk, assign viewer, monkeypatch embed_text. POST `/api/search` body giống nhau 2 lần. Assert lần 1 `body1["data"]["cache_hit"] is False`; lần 2 `body2["data"]["cache_hit"] is True`. (Test này cần Redis testcontainer up — `app_with_auth` đã có redis fixture.)

**Test 6 — `test_explain_analyze_uses_hnsw_index`** (SEARCH-02 / SC3 / R2): seed hub_a + ≥5 chunk (đủ rows để planner chọn index — nếu 1K chunk không khả thi trong test thì seed ~50 chunk, vector khác nhau). Lấy asyncpg pool từ `app_with_auth.state.db_pool`. Chạy:
```python
pool = app_with_auth.state.db_pool
async with pool.acquire() as conn:
    async with conn.transaction():
        await conn.execute("SET LOCAL hnsw.ef_search = 200")
        plan_rows = await conn.fetch(
            "EXPLAIN (ANALYZE, FORMAT TEXT) "
            "SELECT id, 1 - (vector <=> $1::vector) AS score FROM chunks "
            "WHERE hub_id = ANY($2::uuid[]) AND vector IS NOT NULL "
            "ORDER BY vector <=> $1::vector LIMIT 10",
            vec_literal, [hub_a],
        )
plan_text = "\\n".join(r[0] for r in plan_rows)
assert "ix_chunks_vector_hnsw" in plan_text, f"HNSW index không dùng:\\n{plan_text}"
assert "Seq Scan" not in plan_text, f"Seq Scan xuất hiện (R2 vỡ):\\n{plan_text}"
```
`vec_literal` = `"[" + ",".join(repr(float(x)) for x in _fixed_vector()) + "]"`. LƯU Ý: với dataset nhỏ Postgres planner CÓ THỂ chọn Seq Scan — seed đủ chunk (~50+) để HNSW thắng; nếu test flaky với rows ít, tăng số chunk seed. Nếu sau khi seed 50 chunk vẫn Seq Scan, đặt `SET LOCAL enable_seqscan = off` TRƯỚC EXPLAIN để buộc index path (chấp nhận — verify index TỒN TẠI + dùng được, không phải verify planner cost model).

Tất cả test dùng fixture `auth_client`, `viewer_token`, `viewer_user`, `app_with_auth` từ conftest. Header `{"Authorization": f"Bearer {viewer_token}"}`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/test_search_hub_isolation.py && uv run python -m pytest tests/integration/test_search_hub_isolation.py -m critical -p no:cacheprovider -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@pytest.mark.critical" Hub_All/api/tests/integration/test_search_hub_isolation.py` trả ≥6
    - `grep "E4 VIOLATION" Hub_All/api/tests/integration/test_search_hub_isolation.py` match
    - `grep "ix_chunks_vector_hnsw" Hub_All/api/tests/integration/test_search_hub_isolation.py` match
    - `grep "Seq Scan" Hub_All/api/tests/integration/test_search_hub_isolation.py` match
    - `grep "cache_hit" Hub_All/api/tests/integration/test_search_hub_isolation.py` match
    - `pytest tests/integration/test_search_hub_isolation.py -m critical` — toàn bộ test PASS (đặc biệt test 2 + test 3 hub isolation)
    - ruff exit 0
  </acceptance_criteria>
  <done>6+ critical test PASS: single search, hub isolation E4 (explicit hub_ids reject), cross-hub isolation, empty result, cache hit, EXPLAIN HNSW Index Scan. ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → search API | Test mô phỏng viewer cố truy cập cross-hub — verify boundary enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | hub isolation test coverage | mitigate | Test 2 + Test 3 chứng minh viewer KHÔNG thấy chunk hub không assign kể cả explicit `hub_ids` — E4 EXIT criteria genuinely verified against real DB testcontainer |
| T-06-04-02 | Tampering | test seed bypass | accept | Test seed chunks/documents trực tiếp qua SQL (bypass upload flow) — chấp nhận: search isolation test cần data trong nhiều hub; ingest flow đã test riêng Phase 4 |
| T-06-04-03 | Spoofing | monkeypatch embed_text | accept | embed_text bị patch trả vector cố định — chấp nhận: test verify SQL filter + cache + isolation, KHÔNG verify chất lượng embedding (Phase 9 eval lo). Loại bỏ phụ thuộc OPENAI_API_KEY placeholder |
</threat_model>

<verification>
- `uv run ruff check tests/integration/conftest.py tests/integration/test_search_hub_isolation.py` exit 0
- `pytest tests/integration/test_search_hub_isolation.py -m critical` — tất cả test PASS
- Test 2 (`test_search_hub_isolation_explicit_hub_ids`) PASS = E4 EXIT criteria verified cho search
- Test 6 (`test_explain_analyze_uses_hnsw_index`) PASS = SC3 / R2 mitigation verified
- LƯU Ý: chạy per-file `pytest tests/integration/test_search_hub_isolation.py` (DEF-05-01 — cocoindex Environment singleton không boot nhiều file 1 process; `app_with_auth` set `COCOINDEX_SKIP_SETUP=1`)
</verification>

<success_criteria>
- 6+ test critical PASS
- Hub isolation E4: viewer Hub A search explicit `hub_ids:[A,B]` → CHỈ chunk Hub A (Test 2)
- Cross-hub isolation: viewer A+B request `[A,B,C]` → chỉ A,B (Test 3)
- Empty hub → `results: []` 200 không lỗi (Test 4)
- Cache: lần 1 cache_hit=false, lần 2 cache_hit=true (Test 5)
- EXPLAIN ANALYZE show `ix_chunks_vector_hnsw`, không `Seq Scan` (Test 6)
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-search-api-single-cross-hub/06-04-SUMMARY.md` theo template.
</output>
