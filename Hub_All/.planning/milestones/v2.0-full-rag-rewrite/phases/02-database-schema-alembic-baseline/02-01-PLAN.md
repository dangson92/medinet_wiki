---
plan: 01
phase: 2
wave: 1
depends_on: []
files_modified:
  - Hub_All/api/app/db/__init__.py
  - Hub_All/api/app/db/base.py
  - Hub_All/api/app/db/session.py
  - Hub_All/api/app/db/mixins.py
  - Hub_All/api/app/db/types.py
autonomous: true
requirements: [CORE-02]
---

# Plan 01: SQLAlchemy declarative base + async engine + session factory + mixins (`app/db/`)

## Objective
Dựng nền móng SQLAlchemy 2.0 async cho toàn bộ Phase 2-7: `DeclarativeBase` chung, async engine + session factory dùng `asyncpg`, FastAPI dependency `get_session()`, và 2 mixin tái dùng (`UUIDMixin`, `TimestampMixin`). Đây là "Wave 0 — Define contracts" cho Plan 02 (models) và Plan 03 (Alembic env.py) — mọi model class sau sẽ inherit `Base` từ đây, mọi migration sẽ import `Base.metadata` từ đây.

**Tại sao plan riêng:** Plan 02 cần 9 model class import `Base`; Plan 03 (Alembic env.py) cần `target_metadata = Base.metadata`. Tách layer hạ tầng `app/db/` thành 1 plan độc lập giúp Plan 02-05 KHÔNG phải scavenger-hunt tìm `Base` ở đâu — interfaces đã commit sẵn.

## Must-Haves

**Truths (3 mục, người dùng quan sát được):**
- Phase 3 (Auth) có thể `from app.db.session import get_session` để inject AsyncSession vào FastAPI router.
- Phase 2 Plan 02 có thể `from app.db.base import Base` + `from app.db.mixins import UUIDMixin, TimestampMixin` để định nghĩa 9 model class consistent.
- Phase 2 Plan 03 (Alembic) có thể `from app.db.base import Base` rồi `target_metadata = Base.metadata` để autogenerate detect mọi model.

**Artifacts (5 file):**
- `Hub_All/api/app/db/__init__.py` — re-export `Base`, `get_session`, `UUIDMixin`, `TimestampMixin` cho convenience.
- `Hub_All/api/app/db/base.py` — `class Base(DeclarativeBase)` với `MetaData` naming convention (cho Alembic deterministic constraint names).
- `Hub_All/api/app/db/session.py` — `create_async_engine()` factory + `async_sessionmaker` + FastAPI dependency `get_session()`.
- `Hub_All/api/app/db/mixins.py` — `UUIDMixin` (PK uuid4 default) + `TimestampMixin` (`created_at`, `updated_at` server defaults).
- `Hub_All/api/app/db/types.py` — re-export `pgvector.sqlalchemy.Vector` để mọi model dùng cùng path consistent (`from app.db.types import Vector`).

**Key links (3 connection):**
- `app/db/session.py` → `app.config.get_settings()` (lấy `database_url`) — pattern: `create_async_engine(settings.database_url, ...)`.
- `app/db/base.py` → `MetaData(naming_convention=...)` — pattern Alembic friendly cho FK/UQ/IX/CK/PK names.
- `app/db/types.py` → `pgvector.sqlalchemy.Vector` — pattern shim để Plan 02 import từ 1 nơi.

## Tasks

<task id="01">
<action>
Tạo `Hub_All/api/app/db/base.py` định nghĩa `Base = DeclarativeBase` với `MetaData` naming convention. **Tại sao cần naming convention:** Alembic autogenerate (Plan 03) so sánh model với DB hiện tại; nếu constraint không có deterministic name (`fk_documents_hub_id_hubs` thay vì `documents_hub_id_fkey1`), `alembic check` sẽ luôn báo drift sai (P20 mitigation). SQLAlchemy docs khuyến nghị naming convention cho mọi project dùng Alembic.

Nội dung paste-ready:

```python
"""SQLAlchemy 2.0 declarative base — chung cho toàn bộ ORM model M2.

Naming convention cho constraint deterministic — bắt buộc để Alembic autogenerate
KHÔNG báo drift sai (Pitfalls #P20). Mọi FK/UQ/CK/IX/PK sẽ có tên đoán được:
- ix_<table>_<column>      — index
- uq_<table>_<column>      — unique
- ck_<table>_<rule>        — check
- fk_<table>_<col>_<ref>   — foreign key
- pk_<table>               — primary key

Mọi model M2 PHẢI inherit `Base` từ file này.
"""
from __future__ import annotations

from sqlalchemy import MetaData
from sqlalchemy.orm import DeclarativeBase

NAMING_CONVENTION: dict[str, str] = {
    "ix": "ix_%(table_name)s_%(column_0_label)s",
    "uq": "uq_%(table_name)s_%(column_0_name)s",
    "ck": "ck_%(table_name)s_%(constraint_name)s",
    "fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s",
    "pk": "pk_%(table_name)s",
}


class Base(DeclarativeBase):
    """Declarative base cho mọi model — gắn MetaData với naming convention."""

    metadata = MetaData(naming_convention=NAMING_CONVENTION)
```
</action>
<read_first>
- Hub_All/api/app/config.py
- Hub_All/api/pyproject.toml
- Hub_All/.planning/research/STACK.md
- Hub_All/.planning/research/PITFALLS.md
- Hub_All/.planning/CONVENTIONS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/db/base.py` tồn tại.
- `grep -c 'class Base(DeclarativeBase)' Hub_All/api/app/db/base.py` ≥ 1.
- `grep -c 'NAMING_CONVENTION' Hub_All/api/app/db/base.py` ≥ 1.
- `grep -c '"fk": "fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s"' Hub_All/api/app/db/base.py` ≥ 1.
- `grep -c '"pk": "pk_%(table_name)s"' Hub_All/api/app/db/base.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.db.base import Base; print(Base.metadata.naming_convention['fk'])"` exits 0 và output có `fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s`.
</acceptance_criteria>
</task>

<task id="02">
<action>
Tạo `Hub_All/api/app/db/session.py` với async engine factory + `async_sessionmaker` + FastAPI dependency `get_session()`. Pattern:
- Lazy singleton `_engine` + `_session_factory` (init ở `lifespan` Phase 3; Phase 2 chỉ cần module import được, KHÔNG cần connect).
- `init_engine(settings)` — gọi từ FastAPI lifespan để bind engine với DSN.
- `dispose_engine()` — gọi khi shutdown.
- `get_session()` — FastAPI dependency `async def get_session() -> AsyncGenerator[AsyncSession, None]` (yield session, commit nếu không exception, rollback nếu có).

**Lưu ý DSN:** `settings.database_url` đã có prefix `postgresql+asyncpg://` (chuẩn SQLAlchemy async). KHÔNG strip prefix ở đây như Plan 03 Phase 1 đã làm cho asyncpg pool — `create_async_engine` HIỂU prefix này.

Nội dung paste-ready:

```python
"""SQLAlchemy 2.0 async engine + session factory + FastAPI dependency.

Vòng đời:
  - FastAPI lifespan startup → init_engine(settings)
  - Mỗi request → Depends(get_session) inject AsyncSession
  - FastAPI lifespan shutdown → dispose_engine()

Phase 2 (Plan 01) chỉ define API; Phase 3 (Auth) sẽ wire init_engine vào lifespan.
"""
from __future__ import annotations

from typing import AsyncGenerator

from sqlalchemy.ext.asyncio import (
    AsyncEngine,
    AsyncSession,
    async_sessionmaker,
    create_async_engine,
)

from app.config import Settings

_engine: AsyncEngine | None = None
_session_factory: async_sessionmaker[AsyncSession] | None = None


def init_engine(settings: Settings) -> AsyncEngine:
    """Khởi tạo async engine + session factory (gọi 1 lần ở lifespan startup).

    DSN `postgresql+asyncpg://...` (SQLAlchemy parse, async driver asyncpg).
    Pool: min 2, max 10 (đủ cho M2 < 100 concurrent). `pool_pre_ping=True` để
    tránh stale connection sau Postgres restart.
    """
    global _engine, _session_factory
    if _engine is not None:
        return _engine

    _engine = create_async_engine(
        settings.database_url,
        pool_size=10,
        max_overflow=5,
        pool_pre_ping=True,
        echo=False,
    )
    _session_factory = async_sessionmaker(
        bind=_engine,
        expire_on_commit=False,
        class_=AsyncSession,
    )
    return _engine


async def dispose_engine() -> None:
    """Đóng pool (gọi ở lifespan shutdown)."""
    global _engine, _session_factory
    if _engine is not None:
        await _engine.dispose()
        _engine = None
        _session_factory = None


def get_engine() -> AsyncEngine:
    """Truy cập engine sau khi đã init (raise nếu chưa init)."""
    if _engine is None:
        raise RuntimeError(
            "DB engine chưa init. Gọi init_engine(settings) trong lifespan startup."
        )
    return _engine


async def get_session() -> AsyncGenerator[AsyncSession, None]:
    """FastAPI dependency — yield AsyncSession scope per-request.

    Auto-commit nếu handler không raise, rollback nếu có exception.
    """
    if _session_factory is None:
        raise RuntimeError(
            "DB session factory chưa init. Gọi init_engine(settings) trong lifespan."
        )
    async with _session_factory() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```
</action>
<read_first>
- Hub_All/api/app/config.py
- Hub_All/api/app/main.py
- Hub_All/.planning/research/STACK.md
- Hub_All/.planning/research/ARCHITECTURE.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/db/session.py` tồn tại.
- `grep -c 'def init_engine' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'async def dispose_engine' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'def get_engine' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'async def get_session' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'create_async_engine' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'async_sessionmaker' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'pool_pre_ping=True' Hub_All/api/app/db/session.py` ≥ 1.
- `grep -c 'expire_on_commit=False' Hub_All/api/app/db/session.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.db.session import init_engine, dispose_engine, get_session, get_engine; print('imports OK')"` exits 0 với output `imports OK`.
</acceptance_criteria>
</task>

<task id="03">
<action>
Tạo `Hub_All/api/app/db/mixins.py` với 2 mixin tái dùng cho mọi model M2:

1. **`UUIDMixin`** — PK uuid4 mặc định (Postgres-side qua `gen_random_uuid()` của ext `pgcrypto`, fallback Python `uuid4()` nếu ext chưa enable). M2 sẽ dùng Postgres-side để concurrent INSERT không phụ thuộc Python.
2. **`TimestampMixin`** — `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` + `updated_at TIMESTAMPTZ NULL` (NULL khi chưa từng update; trigger Phase 5 nếu cần auto-touch, M2 chấp nhận set thủ công trong service layer).

**Lưu ý quan trọng:** Mọi UUID column dùng `sqlalchemy.dialects.postgresql.UUID(as_uuid=True)` để Python đọc ra `uuid.UUID` (không string). Server default `gen_random_uuid()` PHẢI có `CREATE EXTENSION IF NOT EXISTS pgcrypto` — Plan 04 (migration) sẽ add ext này lên đầu migration.

Nội dung paste-ready:

```python
"""SQLAlchemy mixin tái dùng cho mọi model M2.

Mixin pattern (Declarative + `mapped_column`):
- `UUIDMixin` — PK uuid4 với server default `gen_random_uuid()` (cần ext pgcrypto).
- `TimestampMixin` — `created_at` NOT NULL NOW(), `updated_at` nullable.

Sử dụng:
    class User(UUIDMixin, TimestampMixin, Base):
        __tablename__ = "users"
        email: Mapped[str] = mapped_column(Text, unique=True, nullable=False)
"""
from __future__ import annotations

import uuid
from datetime import datetime

from sqlalchemy import DateTime, text
from sqlalchemy.dialects.postgresql import UUID as PGUUID
from sqlalchemy.orm import Mapped, mapped_column


class UUIDMixin:
    """Primary key UUID4 — server-side default qua pgcrypto.gen_random_uuid()."""

    id: Mapped[uuid.UUID] = mapped_column(
        PGUUID(as_uuid=True),
        primary_key=True,
        server_default=text("gen_random_uuid()"),
    )


class TimestampMixin:
    """Audit timestamps — created_at NOT NULL NOW(), updated_at nullable."""

    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        nullable=False,
        server_default=text("NOW()"),
    )
    updated_at: Mapped[datetime | None] = mapped_column(
        DateTime(timezone=True),
        nullable=True,
    )
```
</action>
<read_first>
- Hub_All/api/app/db/base.py
- Hub_All/.planning/research/STACK.md
- Hub_All/.planning/CONVENTIONS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/db/mixins.py` tồn tại.
- `grep -c 'class UUIDMixin' Hub_All/api/app/db/mixins.py` ≥ 1.
- `grep -c 'class TimestampMixin' Hub_All/api/app/db/mixins.py` ≥ 1.
- `grep -c "server_default=text(\"gen_random_uuid()\")" Hub_All/api/app/db/mixins.py` ≥ 1.
- `grep -c "server_default=text(\"NOW()\")" Hub_All/api/app/db/mixins.py` ≥ 1.
- `grep -c 'PGUUID(as_uuid=True)' Hub_All/api/app/db/mixins.py` ≥ 1.
- `grep -c 'DateTime(timezone=True)' Hub_All/api/app/db/mixins.py` ≥ 2 (cả 2 cột timestamp).
- `cd Hub_All/api && uv run python -c "from app.db.mixins import UUIDMixin, TimestampMixin; print('imports OK')"` exits 0.
</acceptance_criteria>
</task>

<task id="04">
<action>
Tạo `Hub_All/api/app/db/types.py` — module shim re-export `pgvector.sqlalchemy.Vector` để Plan 02 (models) có 1 import path consistent `from app.db.types import Vector`. Lý do dùng shim: tách logic versioning lib (nếu sau này swap `pgvector` → `pgvecto-rs` hoặc bump version có breaking API, chỉ sửa 1 file).

Đồng thời tạo `Hub_All/api/app/db/__init__.py` re-export 4 ký hiệu chính cho convenience.

Nội dung `Hub_All/api/app/db/types.py`:

```python
"""Custom SQLAlchemy types — re-export pgvector cho consistent import path.

Plan 02 models dùng `from app.db.types import Vector` thay vì
`from pgvector.sqlalchemy import Vector` trực tiếp — tách logic versioning lib.
"""
from __future__ import annotations

from pgvector.sqlalchemy import Vector

__all__ = ["Vector"]
```

Nội dung `Hub_All/api/app/db/__init__.py`:

```python
"""Database layer — SQLAlchemy 2.0 async + pgvector.

Public API:
    from app.db import Base, UUIDMixin, TimestampMixin, Vector, get_session
"""
from __future__ import annotations

from app.db.base import Base
from app.db.mixins import TimestampMixin, UUIDMixin
from app.db.session import (
    dispose_engine,
    get_engine,
    get_session,
    init_engine,
)
from app.db.types import Vector

__all__ = [
    "Base",
    "TimestampMixin",
    "UUIDMixin",
    "Vector",
    "dispose_engine",
    "get_engine",
    "get_session",
    "init_engine",
]
```
</action>
<read_first>
- Hub_All/api/app/db/base.py
- Hub_All/api/app/db/session.py
- Hub_All/api/app/db/mixins.py
- Hub_All/api/pyproject.toml
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/db/types.py` tồn tại.
- File `Hub_All/api/app/db/__init__.py` tồn tại.
- `grep -c 'from pgvector.sqlalchemy import Vector' Hub_All/api/app/db/types.py` ≥ 1.
- `grep -c 'from app.db.base import Base' Hub_All/api/app/db/__init__.py` ≥ 1.
- `grep -c 'from app.db.mixins import TimestampMixin, UUIDMixin' Hub_All/api/app/db/__init__.py` ≥ 1.
- `grep -c 'from app.db.session import' Hub_All/api/app/db/__init__.py` ≥ 1.
- `grep -c 'from app.db.types import Vector' Hub_All/api/app/db/__init__.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.db import Base, UUIDMixin, TimestampMixin, Vector, get_session, init_engine, dispose_engine; print('imports OK')"` exits 0 với output `imports OK`.
- `cd Hub_All/api && uv run ruff check app/db` exits 0.
- `cd Hub_All/api && uv run mypy app/db` exits 0.
</acceptance_criteria>
</task>

## Verification

- `cd Hub_All/api && uv run python -c "from app.db import Base, UUIDMixin, TimestampMixin, Vector, get_session, init_engine; print(Base.__name__)"` exits 0 với output `Base`.
- `cd Hub_All/api && uv run ruff check app/db` exits 0.
- `cd Hub_All/api && uv run mypy app/db` exits 0.
- `cd Hub_All/api && uv run python -c "from app.db.base import Base; assert 'fk_%(table_name)s_%(column_0_name)s_%(referred_table_name)s' == Base.metadata.naming_convention['fk']"` exits 0 (naming convention bake đúng).
- `ls Hub_All/api/app/db/` liệt kê đủ 5 file: `__init__.py`, `base.py`, `mixins.py`, `session.py`, `types.py`.

## Threat Model

**Trust boundaries:**

| Boundary | Description |
|---|---|
| client → FastAPI route | Untrusted user input (JWT claims, query params) đi vào handler. Trust được thiết lập sau JWT verify (Phase 3). |
| FastAPI handler → DB session | Truy vấn tham số hóa (asyncpg + SQLAlchemy bind param) — KHÔNG bao giờ f-string SQL. |
| App schema (`public`) → CocoIndex schema (`cocoindex`) | Tách hoàn toàn — Alembic chỉ touch `public`, cocoindex tự quản schema riêng. |

**STRIDE Threat Register** (T1-T5 dispose toàn bộ ở Plan 01-04, code-level threats note cho phase sau):

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | T (Tampering) | `chunks.hub_id` NULL bypass | mitigate | `hub_id UUID NOT NULL REFERENCES hubs(id) ON DELETE CASCADE` ở Plan 02 model + Plan 04 migration. Test verify ở Plan 05 (`\d chunks` cho thấy NOT NULL + FK). KHÔNG cho phép NULL — attacker không thể insert row hub_id=NULL bypass `WHERE hub_id=$1` filter. |
| T-02-02 | I (Information disclosure) | Cross-hub leak qua `ON DELETE SET NULL` của `documents.hub_id` | mitigate | `documents.hub_id` dùng `ON DELETE CASCADE` (xóa hub = xóa toàn bộ document + chunks cascade qua FK chuỗi `chunks.document_id` CASCADE). Plan 02 model + Plan 04 migration. |
| T-02-03 | T (Tampering) | `refresh_tokens` lưu plaintext leak DB = leak toàn bộ session | mitigate | `refresh_tokens.token_hash TEXT NOT NULL UNIQUE` (Phase 3 sẽ hash SHA-256 trước khi INSERT). Plan 02 model column = `token_hash` (KHÔNG `token`). Verify Plan 05 schema introspect. |
| T-02-04 | E (Elevation of privilege) | `api_keys` plaintext key replay attack | mitigate | `api_keys.key_hash TEXT NOT NULL UNIQUE` (AES-GCM encrypted at rest Phase 5) + `key_prefix TEXT` (4-8 ký tự đầu cho UX hiển thị, KHÔNG đủ verify). Plan 02 model. |
| T-02-05 | R (Repudiation) | Audit log tampering (UPDATE/DELETE bởi attacker có DB access) | accept | M2 chấp nhận risk DB-level — code-level mitigation Phase 5: `audit_logs` chỉ expose endpoint GET (KHÔNG PUT/DELETE). Schema-level: KHÔNG bake DB-level RLS hoặc trigger anti-update trong M2 (defer v4.0 hardening). Documented trong PROJECT.md Out of Scope. |

**Lưu ý:** Threats T-02-01..04 mitigate ở schema level qua FK/NOT NULL/UNIQUE constraint trong Plan 02 (model) + Plan 04 (migration). Plan 05 verify SQL introspect `\d <table>` xác nhận constraint apply đúng. T-02-05 accept với rationale (low value, schema-level đã đủ vì attacker cần DB credentials).
