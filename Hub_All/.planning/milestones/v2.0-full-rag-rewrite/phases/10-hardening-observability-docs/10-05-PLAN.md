---
phase: 10-hardening-observability-docs
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - Hub_All/README.md
  - Hub_All/DEPLOY.md
  - Hub_All/api/.env.example
  - Hub_All/mcp_service/.env.example
  - Hub_All/CLAUDE.md
  - Hub_All/.planning/CONVENTIONS.md
autonomous: true
requirements: [HARD-04]
must_haves:
  truths:
    - "Hub_All/README.md mô tả stack Python (FastAPI + cocoindex + pgvector + MCP service) thay vì Go cũ"
    - "Hub_All/DEPLOY.md mới tồn tại với 5 section: prerequisites + quickstart + .env config + backup/restore + observability"
    - "Backup script documented: pg_dump --schema=public --schema=cocoindex medinet_central > backup.sql + pg_dump medinet_cocoindex > backup_cocoindex.sql"
    - ".env.example đủ 12+ key (DATABASE_URL, COCOINDEX_DATABASE_URL, REDIS_URL, OPENAI_API_KEY, GEMINI_API_KEY, JWT_PRIVATE_KEY_PATH, AES_KEY, APP_NAMESPACE, COCOINDEX_DB_SCHEMA, CORS_ALLOWED_ORIGINS, RATE_LIMIT_*_PER_MINUTE)"
    - "Hub_All/CLAUDE.md update reflect M2 done + section 'M2 closeout — pivot tới v3.0 Multi-hub Split'"
    - "Reference link tới SEED-v3.0 trong CLAUDE.md transition section"
  artifacts:
    - path: "Hub_All/README.md"
      provides: "Stack overview + quickstart Python (thay Go cũ)"
      contains: "FastAPI\\|cocoindex\\|pgvector"
    - path: "Hub_All/DEPLOY.md"
      provides: "Production deploy guide + backup + observability"
      contains: "pg_dump\\|prometheus\\|structlog"
    - path: "Hub_All/api/.env.example"
      provides: "12+ env var đầy đủ cho FastAPI api/"
    - path: "Hub_All/mcp_service/.env.example"
      provides: "Env var MCP service standalone + OAuth + CORS whitelist"
    - path: "Hub_All/CLAUDE.md"
      provides: "Section M2 closeout + v3.0 transition reference"
      contains: "v3.0\\|Multi-Hub Split"
  key_links:
    - from: "DEPLOY.md backup section"
      to: "pg_dump command medinet_central + medinet_cocoindex"
      via: "shell snippet copy-paste"
      pattern: "pg_dump.*medinet_central"
    - from: "CLAUDE.md M2 closeout"
      to: ".planning/seeds/v3.0-multi-hub-split.md"
      via: "markdown link relative"
      pattern: "seeds/v3.0-multi-hub-split.md"
    - from: "Hub_All/api/.env.example"
      to: "app/config.py Settings field list"
      via: "1-1 mapping tên biến env"
      pattern: "DATABASE_URL\\|JWT_PRIVATE_KEY_PATH"
---

<objective>
Hoàn tất HARD-04: cập nhật toàn bộ documentation cho stack Python M2 production-ready
+ ghi nhận M2 closeout + transition tới milestone v3.0 Multi-Hub Split (đã capture
ở `.planning/seeds/v3.0-multi-hub-split.md` 2026-05-21).

**Purpose:** Phase 10 là phase cuối M2 — sau khi 10-01..04 ship structlog +
Prometheus + integration test + CORS fix, document chính thức stack production
+ hướng dẫn ops deploy + backup + restore + .env config + observability
endpoint. CLAUDE.md (root Hub_All/) update reflect stack hiện tại + ghi nhận
M2 done, hướng tới v3.0.

**Output:**
- `Hub_All/README.md` — rewrite/cập nhật stack Python (file hiện chỉ có placeholder
  hoặc đề cập Go cũ, cần verify).
- `Hub_All/DEPLOY.md` mới — production deploy guide đầy đủ.
- `Hub_All/api/.env.example` — đủ 12+ key (xem must_haves.truths).
- `Hub_All/mcp_service/.env.example` — env var standalone MCP service.
- `Hub_All/CLAUDE.md` — update section "M2 closeout — pivot tới v3.0" + reference
  SEED-v3.0.
- `Hub_All/.planning/CONVENTIONS.md` — note section 5 logging đã ship (HARD-01 +
  Plan 10-01 tham chiếu).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/seeds/v3.0-multi-hub-split.md
@Hub_All/CLAUDE.md
@Hub_All/README.md
@Hub_All/api/pyproject.toml
@Hub_All/docker-compose.yml

<state_snapshot>
M2 phase status (đọc từ STATE.md):
- Phase 1-9 đã COMPLETE
- Phase 10 đang execute (plan này là 10-05 wave 3)
- M2 closeout pending sau khi 10-01..06 ship

v3.0 seed đã có sẵn:
- `.planning/seeds/v3.0-multi-hub-split.md` (2026-05-21)
- Trigger: M2 closeout = Phase 4 + 8.3 + 9 + 10 done (Phase 4/8.3/9 đã done; chờ Phase 10).
- Architectural decision LOCKED: multi-DB (D-V3-01), chunks+vector sync (D-V3-02),
  milestone-level scope (D-V3-03), M2 closeout bắt buộc trước (D-V3-04).
</state_snapshot>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub_All/README.md + DEPLOY.md + 2 .env.example</name>
  <files>
    Hub_All/README.md (rewrite/cập nhật)
    Hub_All/DEPLOY.md (mới)
    Hub_All/api/.env.example (rewrite full key list)
    Hub_All/mcp_service/.env.example (rewrite full key list)
  </files>
  <action>
    **A. `Hub_All/README.md` — rewrite outline (giữ tiếng Việt có dấu, ASCII-safe heading):**
    1. # Medinet Wiki — Hub_All (M2 Full RAG Rewrite)
    2. ## Stack
       - Backend: Python 3.12 · FastAPI 0.136 · CocoIndex 1.0.3 · pgvector pg16 · asyncpg · SQLAlchemy 2.0 async · Redis 7 · LiteLLM · structlog 25 · prometheus-client
       - Frontend: React 19 · Vite 6 · TypeScript 5.8 · Tailwind v4
       - MCP Service: standalone process (port 8190) — OAuth 2.0 + Streamable HTTP transport
       - Infra: Docker Compose 4-service (postgres pgvector pg16 + redis 7 + api + mcp-service)
    3. ## Quickstart (dev)
       ```bash
       cd Hub_All
       cp api/.env.example api/.env
       cp mcp_service/.env.example mcp_service/.env
       make keys
       docker compose up --build
       curl http://localhost:8180/healthz
       curl http://localhost:8180/readyz
       curl http://localhost:8180/metrics
       ```
    4. ## Test
       ```bash
       cd Hub_All/api
       uv run pytest -m critical
       uv run pytest
       ```
    5. ## Eval (Phase 9 EVAL framework)
       ```bash
       cd Hub_All
       make eval-smoke
       make eval-all
       ```
    6. ## Observability (Plan 10-01/02)
       - `GET /metrics` Prometheus scrape (5 metric)
       - structlog JSON log stdout (Docker capture)
       - Field schema: `level, msg, ts, request_id, user_id, hub_id, latency_ms, path, method, status`
    7. ## Documentation
       - `DEPLOY.md` production deploy + backup + restore
       - `CLAUDE.md` hướng dẫn AI assistant
       - `.planning/PROJECT.md` core value + risk register + EXIT criteria
       - `.planning/REQUIREMENTS.md` 38 REQ-ID M2
       - `.planning/ROADMAP.md` 10 phase + plan status
    8. ## Milestone status
       - M2 v2.0 — Full RAG Rewrite COMPLETE (Phase 1-10)
       - Next: v3.0 — Multi-Hub Split (seed `.planning/seeds/v3.0-multi-hub-split.md`)

    **B. `Hub_All/DEPLOY.md` mới — outline:**
    1. # Deploy guide — Medinet Wiki Hub_All M2
    2. ## 1. Prerequisites
       - Docker 24+ + Docker Compose v2
       - Postgres 16 (pgvector extension)
       - Redis 7
       - Python 3.12 (nếu chạy native không Docker)
       - 4GB RAM tối thiểu (cocoindex Rust core init + LMDB fingerprint)
       - Disk: 10GB cho `medinet_pgdata/` + `file_store/` + `.cocoindex/` LMDB
       - OpenAI API key tier paid (text-embedding-3-large@1536 + gpt-4o-mini)
    3. ## 2. Quickstart deploy — copy section quickstart từ README + ghi chú prod env (CORS prod, JWT key rotation, AES_KEY rotation defer v4.0)
    4. ## 3. .env config — liệt kê 12+ key với mô tả + default value (xem chi tiết section C bên dưới)
    5. ## 4. Backup / Restore (HARD-04 mandatory)
       ```bash
       BACKUP_DIR=/var/backups/medinet
       pg_dump --schema=public --schema=cocoindex medinet_central > $BACKUP_DIR/central_$(date +%F).sql
       pg_dump medinet_cocoindex > $BACKUP_DIR/cocoindex_$(date +%F).sql
       tar czf $BACKUP_DIR/file_store_$(date +%F).tar.gz Hub_All/file_store/
       tar czf $BACKUP_DIR/cocoindex_lmdb_$(date +%F).tar.gz Hub_All/.cocoindex/
       find $BACKUP_DIR -name "*.sql" -mtime +30 -delete
       ```
       Restore:
       ```bash
       psql -c "DROP DATABASE IF EXISTS medinet_central; CREATE DATABASE medinet_central;"
       psql medinet_central < $BACKUP_DIR/central_2026-XX-XX.sql
       tar xzf $BACKUP_DIR/file_store_2026-XX-XX.tar.gz -C Hub_All/
       tar xzf $BACKUP_DIR/cocoindex_lmdb_2026-XX-XX.tar.gz -C Hub_All/
       docker compose restart api
       ```
       Note quan trọng:
       - Backup CẢ 2 DB (central + cocoindex) — mất cocoindex DB = full re-embed (~$6.50/100K chunks)
       - Backup LMDB fingerprint `Hub_All/.cocoindex/` — mất LMDB = cocoindex coi mọi source mới
       - KHÔNG cần backup `file_store/` nếu original files lưu nguồn khác (GDrive/S3 defer v4.0)
    6. ## 5. Observability (Plan 10-01/10-02)
       - Prometheus scrape config snippet `prometheus.yml`:
         ```yaml
         scrape_configs:
           - job_name: 'medinet-api'
             scrape_interval: 15s
             static_configs:
               - targets: ['api:8180']
         ```
       - Grafana dashboard defer v4.0
       - Log aggregation: docker logs JSON → Loki/Datadog (field schema match Go log/slog xem CONVENTIONS section 5)
       - Trace correlation: header `X-Request-Id` (UUID4) propagate vào log entry + cocoindex flow log
    7. ## 6. Security checklist (production)
       - JWT keypair PKCS#8 sinh qua `make keys` (KHÔNG commit `.pem` vào git)
       - AES_KEY rotate annual (defer v4.0)
       - CORS_ALLOWED_ORIGINS chỉ origin production (KHÔNG localhost/192.168.* — P12)
       - MCP_OAUTH_SENSITIVE_ALLOWED_ORIGINS whitelist tối thiểu (claude.ai + inspector — Plan 10-04 CRIT-01)
       - rate_limit slowapi enabled (Phase 5 AUX-03)
       - audit log async batch (Phase 5 AUX-01)
       - `pg_dump` backup automated daily
    8. ## 7. M2 closeout summary — cite REQUIREMENTS.md 38/38 REQ done + critical path test ≥50% (HARD-03) + next milestone v3.0 seed reference

    **C. `Hub_All/api/.env.example` — full key list:**
    ```dotenv
    # Database — 2 logical DB trên cùng instance Postgres pgvector pg16
    DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/medinet_central
    COCOINDEX_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/medinet_cocoindex

    # Redis — cache + rate limit + audit queue
    REDIS_URL=redis://localhost:6379/0

    # LLM provider (default + fallback hot-swap)
    OPENAI_API_KEY=sk-replace-me
    GEMINI_API_KEY=AIza-replace-me

    # JWT keypair RS256 PKCS#8 — sinh qua `make keys`
    JWT_PRIVATE_KEY_PATH=keys/private.pem
    JWT_PUBLIC_KEY_PATH=keys/public.pem
    JWT_ACCESS_TOKEN_TTL=900
    JWT_REFRESH_TOKEN_TTL=604800

    # Encryption at rest — API key AES-GCM (32 byte = base64 44 char)
    AES_KEY=base64:CHANGEME_32_BYTES_BASE64_44_CHARS=

    # CocoIndex — pin namespace + schema riêng (R5 + P7)
    APP_NAMESPACE=medinet_prod
    COCOINDEX_DB_SCHEMA=cocoindex
    COCOINDEX_LMDB_PATH=.cocoindex

    # CORS — production chỉ origin thật, dev cho localhost
    CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8180
    APP_ENV=dev

    # Rate limit (Phase 5 AUX-03)
    RATE_LIMIT_SEARCH_PER_MINUTE=100
    RATE_LIMIT_UPLOAD_PER_MINUTE=30

    # Watchdog cocoindex (Phase 4 INGEST-06 + Plan 04-05)
    WATCHDOG_TIMEOUT_SECONDS=300
    ```

    **D. `Hub_All/mcp_service/.env.example`:**
    ```dotenv
    # API Service backend — MCP service gọi qua HTTP
    API_BASE_URL=http://api:8180
    HTTP_TIMEOUT=30

    # MCP service runtime
    MCP_SERVICE_HOST=0.0.0.0
    MCP_SERVICE_PORT=8190
    MCP_PATH_PREFIX=mcp

    # OAuth 2.0 (Phase 8.3 + Plan 10-04 CORS split)
    MCP_OAUTH_ISSUER_URL=https://mcp.medinet.vn/mcp
    MCP_OAUTH_ACCESS_TOKEN_TTL=3600
    MCP_OAUTH_REFRESH_TOKEN_TTL=2592000
    MCP_OAUTH_STATE_DB_PATH=.oauth/state.db

    # Plan 10-04 CRIT-01 fix — CORS sensitive whitelist
    MCP_OAUTH_SENSITIVE_ALLOWED_ORIGINS=https://claude.ai,https://inspector.modelcontextprotocol.io,http://localhost:6274,http://127.0.0.1:6274

    # Internal token cho API service gọi MCP internal endpoint (Phase 8.3 per-user bind)
    MCP_INTERNAL_TOKEN=CHANGEME_random_32_chars_min
    ```

    **E. Verify byte UTF-8:** Mọi file `.md` + `.env.example` ghi tiếng Việt có dấu. Test trên Windows console (cp1252 trap — đọc bằng `Get-Content -Encoding utf8`).

    **F. Verify no secret commit:** `.env.example` chỉ có placeholder `CHANGEME_*` + `sk-replace-me` — KHÔNG có key thật.
  </action>
  <verify>
    <automated>cd Hub_All && test -f README.md && test -f DEPLOY.md && test -f api/.env.example && test -f mcp_service/.env.example && python -c "c = open('DEPLOY.md', encoding='utf-8').read(); assert 'pg_dump' in c and 'medinet_central' in c and 'medinet_cocoindex' in c and '/metrics' in c" && python -c "c = open('api/.env.example', encoding='utf-8').read(); required = ['DATABASE_URL', 'COCOINDEX_DATABASE_URL', 'REDIS_URL', 'OPENAI_API_KEY', 'GEMINI_API_KEY', 'JWT_PRIVATE_KEY_PATH', 'AES_KEY', 'APP_NAMESPACE', 'COCOINDEX_DB_SCHEMA', 'CORS_ALLOWED_ORIGINS']; missing = [k for k in required if k not in c]; assert not missing, f'.env.example thieu key: {missing}'"</automated>
  </verify>
  <done>
    4 file tồn tại + có nội dung đầy đủ; verify script PASS; manual đọc lại README/DEPLOY thấy outline đúng spec; .env.example đủ 12+ key cho api + 8+ key cho mcp_service; KHÔNG có secret thật trong .env.example (placeholder hoặc CHANGEME_*).
  </done>
</task>

<task type="auto">
  <name>Task 2: Hub_All/CLAUDE.md M2 closeout section + .planning/CONVENTIONS.md note logging done</name>
  <files>
    Hub_All/CLAUDE.md (thêm section M2 done + v3.0 transition)
    Hub_All/.planning/CONVENTIONS.md (note section 5 đã ship HARD-01 Plan 10-01)
  </files>
  <action>
    **A. `Hub_All/CLAUDE.md` patch — thêm 2 thay đổi:**
    1. Section 2 (Milestone hiện tại) — đổi status thành:
       > **M2 — v2.0 Full RAG Rewrite ✅ COMPLETE** (10/10 phase done — xem `.planning/STATE.md`)

    2. **Thêm section MỚI sau section 5 (Cấu trúc commit), TRƯỚC dòng `---` cuối:**
       ```markdown
       ## 6. M2 closeout — Pivot tới v3.0 Multi-Hub Split

       M2 hoàn tất toàn bộ 38 REQ-ID v1 trên 10 phase (xem `.planning/STATE.md`).
       Critical path test ≥50% coverage (HARD-03), structlog JSON + Prometheus
       /metrics endpoint (HARD-01/02), README + DEPLOY + .env.example đầy đủ
       (HARD-04). MCP CRIT-01 (CORS sensitive split) đã đóng — Phase 10 Plan 10-04.

       **Next milestone:** **v3.0 — Multi-Hub Split** (subpath routing, multi-DB,
       cocoindex flow per-hub đẩy chunks+vector lên hub tổng). Architectural
       decision LOCKED 2026-05-21:
       - D-V3-01: DB topology = Postgres database riêng cùng instance
       - D-V3-02: Dataflow hub con → hub tổng = chunks + vector (denormalized read replica, sync 1 chiều)
       - D-V3-03: Scoping = milestone-level
       - D-V3-04: M2 closeout = bắt buộc trước v3.0

       **Trigger v3.0:** `/gsd-new-milestone v3.0` sau khi Phase 10 ship full + human UAT pass + retrospective ghi nhận.

       **Reference:** `.planning/seeds/v3.0-multi-hub-split.md` (7 phase đề xuất, ~35 plan, 4 risk register R-V3-1..4, 4 EXIT criteria E-V3-1..4).

       **Open question chốt ở /gsd-discuss-milestone v3.0:** Auth SSO design (JWKS từ hub tổng?), system settings sync (HTTP pull vs push), reverse proxy frontend prefix detect, migration data từ `medinet_central` cũ split sang DB hub con.
       ```

    3. Footer cập nhật: `*Cập nhật: 2026-05-21 (Phase 10 đang execute — hardening + observability + docs). Project: MEDWIKI. M2 closeout pending.*`

    **B. `.planning/CONVENTIONS.md` Section 5 (Logging Fields) — thêm note cuối section:**
    ```markdown
    ### Plan 10-01 status

    HARD-01 đã ship Phase 10 Plan 10-01:
    - `app/logging_config.py` — `configure_structlog()` + 3 ContextVar (request_id_var, user_id_var, hub_id_var)
    - `app/middleware/request_id.py` — set ContextVar + log "request_completed" với latency_ms
    - Wire vào `app/main.py` lifespan step 0 + cocoindex flow log qua `app/rag/setup.py`
    - 8 unit/integration test PASS

    Mọi log mới TRONG `app/` PHẢI dùng `structlog.get_logger(__name__)` thay vì
    `logging.getLogger(__name__)` — JSON output + ContextVar tự inject request_id.
    ```

    **C. Verify diff:** `git diff Hub_All/CLAUDE.md Hub_All/.planning/CONVENTIONS.md` — chỉ ADD (KHÔNG xoá decision/constraint quan trọng).
  </action>
  <verify>
    <automated>cd Hub_All && python -c "c = open('CLAUDE.md', encoding='utf-8').read(); assert 'M2 closeout' in c and 'v3.0 Multi-Hub Split' in c and 'seeds/v3.0-multi-hub-split.md' in c, 'CLAUDE.md missing required content'" && python -c "c = open('.planning/CONVENTIONS.md', encoding='utf-8').read(); assert 'Plan 10-01' in c and 'configure_structlog' in c, 'CONVENTIONS.md section 5 missing HARD-01 note'"</automated>
  </verify>
  <done>
    CLAUDE.md có section 6 "M2 closeout" + reference seed v3.0; CONVENTIONS.md section 5 có note Plan 10-01 done; verify script PASS; KHÔNG xoá nội dung quan trọng (chỉ ADD).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example → git repo public | KHÔNG được commit secret thật. Chỉ placeholder `CHANGEME_*` hoặc `sk-replace-me` |
| DEPLOY.md backup script → ops | Document chính xác — sai lệnh backup = mất data production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Information Disclosure | .env.example commit secret thật | mitigate | Verify script Task 1 grep `CHANGEME_*` + `sk-replace-me` — KHÔNG có giá trị thật. CI gate (Plan 10-06) thêm step `grep -E "sk-[a-zA-Z0-9]{20,}" .env.example` exit 1 nếu match |
| T-10-05-02 | Repudiation | DEPLOY.md backup script sai → ops mất data | mitigate | Backup section bao gồm `pg_dump` CẢ 2 DB (central + cocoindex) + LMDB tarball. Note "mất cocoindex DB = full re-embed $6.50/100K chunks" để ops hiểu hậu quả nếu skip backup cocoindex |
| T-10-05-03 | Tampering | CLAUDE.md M2 closeout section bị accidentally xoá decision/constraint | accept | Task 2 verify step C `git diff` chỉ ADD — manual review trước commit |
</threat_model>

<verification>
- 4 file tài liệu tồn tại: README.md, DEPLOY.md, api/.env.example, mcp_service/.env.example
- Verify script Task 1 PASS (assert pg_dump + medinet_central + medinet_cocoindex + /metrics trong DEPLOY.md; assert 10 key required trong api/.env.example)
- Verify script Task 2 PASS (CLAUDE.md có section "M2 closeout" + seed link; CONVENTIONS.md có note Plan 10-01)
- Manual: đọc lại DEPLOY.md backup section thấy đầy đủ pg_dump + restore + retention
- KHÔNG commit secret thật vào .env.example
</verification>

<success_criteria>
1. `Hub_All/README.md` rewrite/cập nhật stack Python (FastAPI + cocoindex + pgvector), KHÔNG còn reference Go cũ làm core stack.
2. `Hub_All/DEPLOY.md` mới với 7 section (prerequisites + quickstart + .env config + backup/restore + observability + security checklist + M2 closeout summary).
3. Backup script documented đúng spec HARD-04: `pg_dump --schema=public --schema=cocoindex medinet_central > backup.sql` + tarball LMDB + file_store.
4. `Hub_All/api/.env.example` có đủ 12+ key (DATABASE_URL, COCOINDEX_DATABASE_URL, REDIS_URL, OPENAI_API_KEY, GEMINI_API_KEY, JWT_PRIVATE_KEY_PATH, AES_KEY, APP_NAMESPACE, COCOINDEX_DB_SCHEMA, CORS_ALLOWED_ORIGINS, RATE_LIMIT_*).
5. `Hub_All/mcp_service/.env.example` có đủ env var MCP standalone + Plan 10-04 MCP_OAUTH_SENSITIVE_ALLOWED_ORIGINS.
6. `Hub_All/CLAUDE.md` có section 6 "M2 closeout — Pivot tới v3.0 Multi-Hub Split" + link `.planning/seeds/v3.0-multi-hub-split.md`.
7. `.planning/CONVENTIONS.md` section 5 có note ship status Plan 10-01.
8. KHÔNG có secret thật commit vào git.
</success_criteria>

<output>
Tạo `10-05-SUMMARY.md` ghi:
- Số file đã viết/sửa (6)
- Line count mỗi file (README, DEPLOY, 2 .env.example)
- Mapping HARD-04 acceptance line → file (4 mục)
- Mapping CLAUDE.md section 6 → SEED-v3.0 link (1 mục)
- Decision nếu có (vd cấu trúc DEPLOY 7 section thay vì 5 — lý do)
- Deviation nếu có
</output>
