---
phase: 09-eval-framework-quality-gate
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - eval/report.py
  - eval/tests/test_report.py
autonomous: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "`generate_eval_md(results)` sinh ra Markdown 7 section đầy đủ (Setup + Metrics + Per-Query Diff + Latency + Conclusion + Recommendations + Defer) — tabulate render Metrics + Per-Query bảng đẹp."
    - "`gate_verdict(top_3)` trả `(\"PASS\", 0)` nếu top_3 ≥ 0.75 absolute; `(\"FAIL\", 1)` nếu < 0.75. KHÔNG dùng +15pp delta (M1 abandoned)."
    - "EVAL.md được sinh ra DÙ verdict FAIL (chứa debug data per-query, latency, recommendations) — quan trọng cho iterate chunker/prompt."
    - "Verdict FAIL <60% top-3 → recommendations section explicit trigger E5 STOP M2b (RESEARCH constraint từ ROADMAP)."
    - "Unit test `test_report.py` cover 3 case (PASS ≥75%, FAIL borderline 70%, FAIL critical <60% trigger E5) — không cần backend."
  artifacts:
    - path: "Hub_All/eval/report.py"
      provides: "generate_eval_md + gate_verdict + main CLI (sinh EVAL.md từ results.json)"
      min_lines: 180
      exports: ["generate_eval_md", "gate_verdict", "main"]
    - path: "Hub_All/eval/tests/test_report.py"
      provides: "Unit test 3 verdict case + Markdown section integrity"
      contains: "gate_verdict"
  key_links:
    - from: "eval/report.py gate_verdict()"
      to: "ROADMAP Phase 9 SC + EXIT criteria E5 (Stop M2b nếu fail <60%)"
      via: "Threshold 0.75 absolute + cảnh báo <60%"
      pattern: "0.75"
    - from: "eval/report.py generate_eval_md()"
      to: "tabulate>=0.9 (PEP 621 dep eval/pyproject.toml)"
      via: "tabulate.tabulate(rows, headers, tablefmt='github')"
      pattern: "tabulate"
    - from: "eval/report.py main()"
      to: "eval/results.json (sinh từ Plan 09-04 run_eval.py)"
      via: "json.load + emit EVAL.md + sys.exit(0|1)"
      pattern: "sys.exit"
---

<objective>
Build `eval/report.py` — Markdown report generator + quality gate verdict. EVAL.md là deliverable cuối cùng Phase 9: 7 section, verdict PASS/FAIL, exit code CI-friendly.

Purpose: Tách `report.py` ra plan riêng (Wave 2 parallel với 09-02) vì:
- KHÔNG depend `lib.py` (chỉ depend `metrics.py` output schema).
- Template f-string + tabulate đủ đơn giản → tách giảm context cost cho Plan 09-04 orchestrator.
- Wave 4 gate verdict cần `report.py` CLI riêng (`python -m eval.report eval/results.json eval/EVAL.md`) để re-generate report mà KHÔNG re-run pipeline.

Output: `eval/report.py` import được standalone + unit test 3 verdict case (PASS / FAIL borderline / FAIL critical E5 trigger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-eval-framework-quality-gate/09-RESEARCH.md
@.planning/phases/09-eval-framework-quality-gate/09-01-SUMMARY.md
@.planning/PROJECT.md

<interfaces>
<!-- Schema results.json (Plan 09-04 emit, Plan 09-03 read) -->

```json
{
  "run_metadata": {
    "timestamp": "2026-05-21T10:30:00Z",
    "backend_url": "http://localhost:8180",
    "embedding_model": "openai/text-embedding-3-large@1536",
    "llm_model": "openai/gpt-4o-mini",
    "eval_hub_id": "...",
    "dataset_size": 10,
    "query_count": 12,
    "mock_embed": false
  },
  "upload_summary": {
    "completed": 8,
    "failed_unsupported": 2,
    "failed": 0,
    "timeout": 0,
    "documents": [
      {"filename":"...", "status":"completed", "chunk_count": 25, "id":"..."}
    ]
  },
  "retrieval_metrics": {
    "top_1_hit_rate": 0.667,
    "top_3_hit_rate": 0.833,
    "top_5_hit_rate": 0.917,
    "mrr": 0.75,
    "per_query": [
      {"id":"q-01","query":"...","expected_doc_id":"...","rank":1,"top_result_title":"...","result_count":10}
    ]
  },
  "latency": {
    "count": 12,
    "mean_ms": 245.5,
    "p50_ms": 230.0,
    "p95_ms": 480.0,
    "p99_ms": 510.0
  }
}
```

<!-- EXIT criteria E5 from PROJECT.md -->
- top_3 ≥ 0.75 → PASS exit 0
- 0.60 ≤ top_3 < 0.75 → FAIL exit 1 (iterate chunker/prompt 3 vòng)
- top_3 < 0.60 → FAIL exit 1 + trigger E5 STOP M2b (ship M2a standalone, discuss reranker/BM25 v3.0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build eval/report.py — generate_eval_md + gate_verdict + main CLI</name>
  <files>Hub_All/eval/report.py</files>
  <behavior>
- gate_verdict(top_3) → ("PASS", 0) nếu ≥ 0.75; ("FAIL", 1) nếu < 0.75; thêm metadata "trigger_e5: bool" để main() in cảnh báo.
- generate_eval_md(results) → Markdown string 7 section đầy đủ, tabulate render Metrics + Per-Query Diff.
- main(results_path, output_path) → đọc JSON, sinh MD, ghi file, in verdict + sys.exit(0|1).
- CLI: `python -m eval.report eval/results.json eval/EVAL.md` HOẶC `python -m eval.report` (default paths).
- EVAL.md PHẢI sinh dù FAIL (debug data quan trọng).
- Per-Query Diff bảng: id | query (truncated 50 char) | expected_doc_id | rank | top_result_title.
- Recommendations section nội dung conditional theo verdict:
  - PASS ≥ 0.75: ship M2 — log iterate tiếp cho VN medical quality v4.0.
  - FAIL 0.60–0.75: iterate chunker boundary VN, prompt expansion, hybrid BM25 candidate.
  - FAIL < 0.60: STOP M2b trigger E5 — ship M2a standalone, discuss reranker/BM25 v3.0.
  </behavior>
  <action>
**Tạo `Hub_All/eval/report.py`:**

```python
"""EVAL.md generator + quality gate verdict (Phase 9 EVAL-03).

7 section: Setup · Metrics · Per-Query Diff · Latency · Conclusion · Recommendations · Defer.
Gate: top_3 ≥ 0.75 absolute → PASS exit 0; < 0.75 → FAIL exit 1.
Trigger E5 STOP M2b nếu < 0.60 (per ROADMAP + PROJECT.md EXIT criteria).
"""
from __future__ import annotations

import json
import sys
from pathlib import Path
from typing import Any

from tabulate import tabulate


GATE_THRESHOLD_PASS = 0.75
GATE_THRESHOLD_E5_STOP = 0.60


def gate_verdict(top_3: float) -> dict:
    """Trả {verdict: 'PASS'|'FAIL', exit_code: 0|1, trigger_e5: bool}.

    Quy tắc (ROADMAP Phase 9 SC + PROJECT EXIT E5):
    - top_3 ≥ 0.75 → PASS exit 0
    - 0.60 ≤ top_3 < 0.75 → FAIL exit 1 (iterate chunker/prompt 3 vòng)
    - top_3 < 0.60 → FAIL exit 1 + trigger E5 STOP M2b
    """
    if top_3 >= GATE_THRESHOLD_PASS:
        return {"verdict": "PASS", "exit_code": 0, "trigger_e5": False}
    if top_3 < GATE_THRESHOLD_E5_STOP:
        return {"verdict": "FAIL", "exit_code": 1, "trigger_e5": True}
    return {"verdict": "FAIL", "exit_code": 1, "trigger_e5": False}


def _truncate(s: str, n: int = 50) -> str:
    if not s:
        return ""
    return s if len(s) <= n else s[: n - 1] + "…"


def _section_setup(results: dict) -> str:
    m = results.get("run_metadata", {})
    rows = [
        ["Timestamp", m.get("timestamp", "?")],
        ["Backend URL", m.get("backend_url", "?")],
        ["Embedding model", m.get("embedding_model", "?")],
        ["LLM model", m.get("llm_model", "?")],
        ["Eval hub ID", m.get("eval_hub_id", "?")],
        ["Dataset size", m.get("dataset_size", "?")],
        ["Query count", m.get("query_count", "?")],
        ["Mock embed", "YES (smoke regression — KHÔNG gate verdict)" if m.get("mock_embed") else "NO (real LLM gate)"],
    ]
    body = tabulate(rows, headers=["Key", "Value"], tablefmt="github")
    return f"## 1. Setup\n\n{body}\n"


def _section_metrics(results: dict) -> str:
    r = results.get("retrieval_metrics", {})
    u = results.get("upload_summary", {})
    rows = [
        ["Top-1 hit rate", f"{r.get('top_1_hit_rate', 0):.3f}", f"{r.get('top_1_hit_rate', 0)*100:.1f}%"],
        ["Top-3 hit rate (GATE)", f"{r.get('top_3_hit_rate', 0):.3f}", f"{r.get('top_3_hit_rate', 0)*100:.1f}%"],
        ["Top-5 hit rate", f"{r.get('top_5_hit_rate', 0):.3f}", f"{r.get('top_5_hit_rate', 0)*100:.1f}%"],
        ["MRR (Mean Reciprocal Rank)", f"{r.get('mrr', 0):.3f}", "—"],
    ]
    body = tabulate(rows, headers=["Metric", "Score", "Percent"], tablefmt="github")
    upload = (
        f"\n**Upload summary:** {u.get('completed', 0)} completed · "
        f"{u.get('failed_unsupported', 0)} failed_unsupported (R4 scanned PDF) · "
        f"{u.get('failed', 0)} failed · {u.get('timeout', 0)} timeout\n"
    )
    return f"## 2. Metrics\n\n{body}\n{upload}"


def _section_per_query(results: dict) -> str:
    pq = results.get("retrieval_metrics", {}).get("per_query", [])
    rows = []
    for q in pq:
        rank = q.get("rank")
        rank_display = str(rank) if rank is not None else "MISS"
        rows.append(
            [
                q.get("id", "?"),
                _truncate(q.get("query", ""), 50),
                _truncate(q.get("expected_doc_id", ""), 40),
                rank_display,
                _truncate(q.get("top_result_title", "") or "—", 40),
                q.get("result_count", 0),
            ]
        )
    body = tabulate(
        rows,
        headers=["ID", "Query", "Expected", "Rank", "Top result", "#Results"],
        tablefmt="github",
    )
    return f"## 3. Per-Query Diff\n\n{body}\n"


def _section_latency(results: dict) -> str:
    lat = results.get("latency", {})
    rows = [
        ["Count", lat.get("count", 0)],
        ["Mean (ms)", f"{lat.get('mean_ms', 0):.1f}"],
        ["p50 (ms)", f"{lat.get('p50_ms', 0):.1f}"],
        ["p95 (ms)", f"{lat.get('p95_ms', 0):.1f} (target <800ms single hub)"],
        ["p99 (ms)", f"{lat.get('p99_ms', 0):.1f}"],
    ]
    body = tabulate(rows, headers=["Metric", "Value"], tablefmt="github")
    p95 = lat.get("p95_ms", 0)
    budget = "✓ trong budget" if p95 < 800 else "✗ vượt budget 800ms"
    return f"## 4. Latency (search endpoint)\n\n{body}\n\n**p95 vs target 800ms:** {budget}\n"


def _section_conclusion(results: dict, verdict_info: dict) -> str:
    r = results.get("retrieval_metrics", {})
    top_3 = r.get("top_3_hit_rate", 0)
    verdict = verdict_info["verdict"]
    badge = "✅ **PASS**" if verdict == "PASS" else "❌ **FAIL**"
    rationale = (
        f"top-3 hit rate = `{top_3:.3f}` ({top_3*100:.1f}%) "
        f"{'≥' if verdict == 'PASS' else '<'} threshold `0.75` (75%)."
    )
    e5_warn = ""
    if verdict_info.get("trigger_e5"):
        e5_warn = (
            "\n\n> 🚨 **TRIGGER E5 — STOP M2b.** top-3 < 0.60 dù chạy gate.\n"
            "> Action: Ship M2a standalone (Phase 1-4 + EXIT GATE đã PASS 2026-05-21). "
            "Discuss reranker / hybrid BM25 cho v3.0 với user (PROJECT.md EXIT criteria E5)."
        )
    exit_code = verdict_info["exit_code"]
    return (
        f"## 5. Conclusion\n\n"
        f"{badge}\n\n{rationale}\n\n"
        f"**Exit code:** `{exit_code}` (CI-friendly: 0 PASS, 1 FAIL)."
        f"{e5_warn}\n"
    )


def _section_recommendations(verdict_info: dict, results: dict) -> str:
    verdict = verdict_info["verdict"]
    trigger_e5 = verdict_info.get("trigger_e5", False)
    lines: list[str] = ["## 6. Recommendations\n"]

    if verdict == "PASS":
        lines.append(
            "- ✅ Ship M2 — gate ≥ 75% top-3 đạt. Tiếp Phase 10 hardening.\n"
            "- Log iterate VN medical quality cải tiến cho v4.0:\n"
            "  - Hybrid BM25 + dense vector (RAG-V4-01 defer).\n"
            "  - Reranker Cohere rerank-3 / local cross-encoder (RAG-V4-02 defer).\n"
            "  - Eval dataset mở rộng (100+ query) khi có prod data thật (post-M2 deploy).\n"
            "- Document `EVAL.md` này như baseline; mỗi lần đổi chunker/prompt re-run `make eval-all`.\n"
        )
    elif trigger_e5:
        lines.append(
            "- 🚨 **TRIGGER E5 STOP M2b** (PROJECT.md EXIT criteria).\n"
            "- Stop M2b, ship M2a (Phase 1-4) standalone — đã PASS EXIT GATE 2026-05-21.\n"
            "- Discuss với user:\n"
            "  - Hybrid BM25 + dense (recall boost cho VN medical jargon).\n"
            "  - Reranker layer (Cohere rerank-3) — tăng precision sau retrieval.\n"
            "  - Embedding dim 3072 (text-embedding-3-large full) — cross-dim defer v4.0 NHƯNG có thể bump lên v3.0 nếu gate < 60%.\n"
            "  - Chunker strategy mới: semantic chunking + sentence-window retrieval.\n"
        )
    else:
        # FAIL borderline 0.60–0.75
        lines.append(
            "- ⚠️ FAIL borderline (60–75% top-3) — iterate trước khi trigger E5.\n"
            "- Iterate chunker boundary VN (3 vòng max — PROJECT.md):\n"
            "  - Vòng 1: tune `RecursiveSplitter` regex VN heading (Mục/Chương boundary).\n"
            "  - Vòng 2: prompt expansion (query rewrite cho VN medical jargon).\n"
            "  - Vòng 3: tăng `top_k_search=15` (post-filter sau retrieval).\n"
            "- Mỗi vòng `make eval-all` + ghi `EVAL-${date}.md` (gitignored).\n"
            "- Vòng 3 vẫn fail → trigger E5.\n"
        )

    # Latency advice
    p95 = results.get("latency", {}).get("p95_ms", 0)
    if p95 >= 800:
        lines.append(
            f"- ⚠️ Latency p95 = `{p95:.1f}ms` vượt budget 800ms single hub.\n"
            "  - Re-tune `SET hnsw.ef_search` xuống 100–150 (đánh đổi recall vs latency).\n"
            "  - Tăng connection pool size (asyncpg `min_size=10, max_size=20`).\n"
        )

    # MISS query analysis
    miss = [q for q in results.get("retrieval_metrics", {}).get("per_query", []) if q.get("rank") is None]
    if miss:
        lines.append(
            f"- {len(miss)} query MISS — review thủ công `Per-Query Diff` section 3 để hiểu loại miss:\n"
            "  - Heading nhiễu (chunker boundary sai).\n"
            "  - VN medical jargon (synonym expansion cần thiết).\n"
            "  - File scanned PDF (R4 expected `failed_unsupported`, KHÔNG count vào miss thật).\n"
        )

    return "\n".join(lines)


def _section_defer() -> str:
    return (
        "## 7. Defer (Out of Scope Phase 9)\n\n"
        "- **Answer quality (BLEU/ROUGE)** → defer v4.0.\n"
        "- **LLM-as-judge auto-grading** → defer v4.0 (12 query có expected_doc_id đủ ground truth).\n"
        "- **Statistical significance test** → defer v4.0 (12 query quá ít cho t-test).\n"
        "- **A/B multi embedding model (BGE-M3, sentence-transformers)** → defer v4.1.\n"
        "- **Visual dashboard Streamlit** → defer v4.0.\n"
        "- **Auto regression CI (GitHub Actions)** → defer Phase 10 HARD-03.\n"
        "- **Heading recall metric** → defer v4.1 (cocoindex chunker khác Go regex, không ground truth).\n"
        "- **Larger eval dataset 100+ queries** → defer khi có prod data thật post-M2 deploy.\n"
        "- **Cross-hub search eval** → defer v4.0 (REQUIREMENTS chỉ single-hub `/api/search`).\n"
        "- **Eval `/api/ask` end-to-end + citation correctness** → defer v4.0 (anti-injection LLM thật, response parse `[N]`).\n"
    )


def generate_eval_md(results: dict) -> str:
    """Sinh Markdown 7 section từ results dict. Idempotent — không side effect."""
    top_3 = results.get("retrieval_metrics", {}).get("top_3_hit_rate", 0)
    verdict_info = gate_verdict(top_3)

    header = (
        "# Eval Report — Phase 9 Quality Gate ≥75% top-3\n\n"
        f"> **Verdict:** {'✅ PASS' if verdict_info['verdict'] == 'PASS' else '❌ FAIL'} · "
        f"top-3 hit rate `{top_3:.3f}` ({top_3*100:.1f}%) · "
        f"Exit code `{verdict_info['exit_code']}`\n\n"
        "---\n\n"
    )

    sections = [
        _section_setup(results),
        _section_metrics(results),
        _section_per_query(results),
        _section_latency(results),
        _section_conclusion(results, verdict_info),
        _section_recommendations(verdict_info, results),
        _section_defer(),
    ]

    return header + "\n---\n\n".join(sections) + "\n"


def main(argv: list[str] | None = None) -> int:
    """CLI: python -m eval.report [results.json [EVAL.md]].

    Default paths: eval/results.json → eval/EVAL.md.
    """
    args = argv if argv is not None else sys.argv[1:]
    results_path = Path(args[0]) if len(args) >= 1 else Path("eval/results.json")
    output_path = Path(args[1]) if len(args) >= 2 else Path("eval/EVAL.md")

    if not results_path.exists():
        print(f"ERROR: results file không tồn tại: {results_path}", file=sys.stderr)
        return 2

    results: dict[str, Any] = json.loads(results_path.read_text(encoding="utf-8"))
    md = generate_eval_md(results)
    output_path.parent.mkdir(parents=True, exist_ok=True)
    output_path.write_text(md, encoding="utf-8")

    top_3 = results.get("retrieval_metrics", {}).get("top_3_hit_rate", 0)
    v = gate_verdict(top_3)
    print(
        f"Eval report written: {output_path}\n"
        f"Verdict: {v['verdict']} (top-3 = {top_3:.3f}, threshold 0.75)\n"
        f"Exit code: {v['exit_code']}"
        + ("\n🚨 TRIGGER E5 STOP M2b — discuss với user." if v.get("trigger_e5") else "")
    )
    return v["exit_code"]


if __name__ == "__main__":
    sys.exit(main())
```

**Verify import + ruff:**
```bash
cd Hub_All && python -c "from eval.report import generate_eval_md, gate_verdict, main; print(gate_verdict(0.80), gate_verdict(0.70), gate_verdict(0.50))"
# Expect 3 dict output: PASS/FAIL borderline/FAIL trigger_e5
ruff check eval/report.py
```
  </action>
  <verify>
    <automated>cd Hub_All && ruff check eval/report.py && python -c "from eval.report import generate_eval_md, gate_verdict, main; v1=gate_verdict(0.80); v2=gate_verdict(0.70); v3=gate_verdict(0.50); assert v1=={'verdict':'PASS','exit_code':0,'trigger_e5':False}; assert v2=={'verdict':'FAIL','exit_code':1,'trigger_e5':False}; assert v3=={'verdict':'FAIL','exit_code':1,'trigger_e5':True}; print('verdict logic OK')"</automated>
  </verify>
  <done>
- `eval/report.py` ≥ 180 dòng, ruff PASS.
- Export: generate_eval_md, gate_verdict, main.
- gate_verdict(0.80) = PASS exit 0; gate_verdict(0.70) = FAIL exit 1 không E5; gate_verdict(0.50) = FAIL exit 1 + trigger_e5.
- CLI `python -m eval.report results.json EVAL.md` chạy được.
- 7 section render qua tabulate `tablefmt='github'`.
- Recommendations conditional 3 case (PASS / FAIL borderline / FAIL E5).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit test test_report.py — 3 verdict case + Markdown integrity</name>
  <files>Hub_All/eval/tests/test_report.py</files>
  <behavior>
- test_gate_verdict_pass (top_3=0.80) → PASS exit 0 trigger_e5=False.
- test_gate_verdict_fail_borderline (top_3=0.70) → FAIL exit 1 trigger_e5=False.
- test_gate_verdict_fail_critical_triggers_e5 (top_3=0.50) → FAIL exit 1 trigger_e5=True.
- test_gate_verdict_boundary_pass (top_3=0.75 exact) → PASS exit 0.
- test_gate_verdict_boundary_e5 (top_3=0.60 exact) → FAIL không trigger_e5.
- test_generate_eval_md_pass_has_all_sections → Markdown chứa 7 section header (## 1. Setup ... ## 7. Defer).
- test_generate_eval_md_fail_e5_warning_appears → Markdown chứa "TRIGGER E5" khi top_3 < 0.60.
- test_main_writes_file_and_exits → main() ghi file + return exit_code đúng (dùng tmp_path).
  </behavior>
  <action>
**Tạo `Hub_All/eval/tests/test_report.py`:**

```python
"""Unit test eval/report.py — gate verdict logic + Markdown integrity (Phase 9 EVAL-03)."""
from __future__ import annotations

import json
from pathlib import Path

import pytest

from eval.report import gate_verdict, generate_eval_md, main


# ---- gate_verdict ----

@pytest.mark.critical
def test_gate_verdict_pass():
    v = gate_verdict(0.80)
    assert v == {"verdict": "PASS", "exit_code": 0, "trigger_e5": False}


@pytest.mark.critical
def test_gate_verdict_fail_borderline():
    v = gate_verdict(0.70)
    assert v == {"verdict": "FAIL", "exit_code": 1, "trigger_e5": False}


@pytest.mark.critical
def test_gate_verdict_fail_critical_triggers_e5():
    v = gate_verdict(0.50)
    assert v == {"verdict": "FAIL", "exit_code": 1, "trigger_e5": True}


def test_gate_verdict_boundary_pass_exact_075():
    v = gate_verdict(0.75)
    assert v["verdict"] == "PASS"
    assert v["exit_code"] == 0


def test_gate_verdict_boundary_e5_exact_060():
    v = gate_verdict(0.60)
    assert v["verdict"] == "FAIL"
    assert v["trigger_e5"] is False  # ≥ 0.60 không trigger


def test_gate_verdict_just_below_060():
    v = gate_verdict(0.599)
    assert v["trigger_e5"] is True


# ---- generate_eval_md ----

def _sample_results(top_3: float = 0.80) -> dict:
    return {
        "run_metadata": {
            "timestamp": "2026-05-21T10:00:00Z",
            "backend_url": "http://localhost:8180",
            "embedding_model": "openai/text-embedding-3-large@1536",
            "llm_model": "openai/gpt-4o-mini",
            "eval_hub_id": "test-uuid",
            "dataset_size": 10,
            "query_count": 12,
            "mock_embed": False,
        },
        "upload_summary": {
            "completed": 8,
            "failed_unsupported": 2,
            "failed": 0,
            "timeout": 0,
            "documents": [],
        },
        "retrieval_metrics": {
            "top_1_hit_rate": 0.50,
            "top_3_hit_rate": top_3,
            "top_5_hit_rate": 0.92,
            "mrr": 0.65,
            "per_query": [
                {"id": "q-01", "query": "Test query", "expected_doc_id": "doc-A.docx",
                 "rank": 1, "top_result_title": "doc-A.docx", "result_count": 10},
                {"id": "q-02", "query": "Miss query", "expected_doc_id": "doc-X.docx",
                 "rank": None, "top_result_title": "doc-Y.docx", "result_count": 5},
            ],
        },
        "latency": {
            "count": 12,
            "mean_ms": 245.5,
            "p50_ms": 230.0,
            "p95_ms": 480.0,
            "p99_ms": 510.0,
        },
    }


def test_generate_eval_md_pass_has_all_sections():
    md = generate_eval_md(_sample_results(top_3=0.80))
    # 7 section header
    for h in ["## 1. Setup", "## 2. Metrics", "## 3. Per-Query Diff",
              "## 4. Latency", "## 5. Conclusion", "## 6. Recommendations", "## 7. Defer"]:
        assert h in md, f"Section missing: {h}"
    assert "✅" in md or "PASS" in md
    # KHÔNG có cảnh báo E5
    assert "TRIGGER E5" not in md


def test_generate_eval_md_fail_e5_warning_appears():
    md = generate_eval_md(_sample_results(top_3=0.50))
    assert "TRIGGER E5" in md
    assert "STOP M2b" in md
    assert "M2a standalone" in md


def test_generate_eval_md_fail_borderline_iterate_advice():
    md = generate_eval_md(_sample_results(top_3=0.65))
    # KHÔNG E5 trigger (borderline)
    assert "TRIGGER E5" not in md or "STOP M2b" not in md.split("## 6. Recommendations")[0]
    # Recommendations có iterate advice
    assert "Iterate" in md or "iterate" in md


def test_generate_eval_md_includes_per_query_miss_marker():
    md = generate_eval_md(_sample_results(top_3=0.80))
    # Per-query có MISS marker
    assert "MISS" in md


def test_generate_eval_md_latency_budget_check():
    res = _sample_results(top_3=0.80)
    res["latency"]["p95_ms"] = 1500.0  # vượt budget
    md = generate_eval_md(res)
    assert "vượt budget" in md


# ---- main CLI ----

def test_main_writes_file_and_exits_zero_on_pass(tmp_path: Path):
    results_path = tmp_path / "results.json"
    output_path = tmp_path / "EVAL.md"
    results_path.write_text(json.dumps(_sample_results(top_3=0.80)), encoding="utf-8")

    exit_code = main([str(results_path), str(output_path)])

    assert exit_code == 0
    assert output_path.exists()
    md = output_path.read_text(encoding="utf-8")
    assert "PASS" in md
    assert "## 1. Setup" in md


def test_main_exits_one_on_fail(tmp_path: Path):
    results_path = tmp_path / "results.json"
    output_path = tmp_path / "EVAL.md"
    results_path.write_text(json.dumps(_sample_results(top_3=0.50)), encoding="utf-8")

    exit_code = main([str(results_path), str(output_path)])

    assert exit_code == 1
    md = output_path.read_text(encoding="utf-8")
    assert "FAIL" in md
    assert "TRIGGER E5" in md


def test_main_missing_results_returns_2(tmp_path: Path):
    missing = tmp_path / "nope.json"
    exit_code = main([str(missing), str(tmp_path / "out.md")])
    assert exit_code == 2
```

**Chạy test:**
```bash
cd Hub_All && pytest eval/tests/test_report.py -v
# Expect 12+ test PASS
```
  </action>
  <verify>
    <automated>cd Hub_All && pytest eval/tests/test_report.py -v --no-header && ruff check eval/tests/test_report.py</automated>
  </verify>
  <done>
- `eval/tests/test_report.py` PASS 12+ test (3 critical marker + 9 standard).
- 3 verdict case core (PASS / FAIL borderline / FAIL E5) verify đúng.
- Boundary case 0.75 và 0.60 exact verify đúng (PASS / không-E5).
- Markdown integrity: 7 section header tồn tại + "MISS" marker per-query + budget warning latency.
- main() ghi file + exit code đúng + handle missing file → return 2.
- ruff PASS.
  </done>
</task>

</tasks>

<verification>
1. `Hub_All/eval/report.py` ≥ 180 dòng, ruff PASS, importable standalone.
2. `pytest Hub_All/eval/tests/test_report.py` PASS 12+ test (3 critical).
3. `python -m eval.report` CLI chạy được (đọc results.json → ghi EVAL.md → exit 0/1).
4. gate_verdict(0.80)=PASS, gate_verdict(0.70)=FAIL no-E5, gate_verdict(0.50)=FAIL trigger_e5.
5. EVAL.md mẫu (test generate) có đủ 7 section + tabulate render Metrics + Per-Query Diff.
6. Recommendations conditional theo verdict (PASS / FAIL borderline / FAIL E5 STOP M2b).
</verification>

<success_criteria>
- `report.py` ready cho Plan 09-04 import + Wave 4 standalone CLI re-generate EVAL.md.
- Quality gate verdict logic chuẩn: ≥0.75 PASS, <0.60 trigger E5.
- EVAL.md sinh dù FAIL — đảm bảo debug data available cho iterate chunker/prompt.
- KHÔNG depend `lib.py` → Plan 09-03 + Plan 09-02 chạy parallel Wave 2.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-eval-framework-quality-gate/09-03-SUMMARY.md` ghi:
- Files created (eval/report.py + eval/tests/test_report.py).
- pytest output (12+ test PASS).
- Sample EVAL.md output cho mock data (paste 30 dòng đầu vào SUMMARY làm visual check).
- Confirm gate_verdict threshold (0.75 PASS / 0.60 E5) khớp ROADMAP + PROJECT.md.
</output>
</content>
</invoke>