---
phase: 09-eval-framework-quality-gate
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - eval/lib.py
  - eval/metrics.py
  - eval/tests/__init__.py
  - eval/tests/test_metrics.py
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "`APIClient` login admin được + upload file qua multipart + poll status đến completed/failed/failed_unsupported + search POST body (D-02 Phase 6) — JWT auto-refresh khi 401."
    - "`preflight_check()` fail-fast nếu backend KHÔNG healthz/readyz HOẶC `eval_hub` chưa seed — emit SystemExit kèm hint khắc phục cụ thể."
    - "`upload_and_wait()` tolerant scanned PDF 415 → trả `status='failed_unsupported'` (R4) + tolerant Phase 4 race retry (timeout ≥30s/file, poll 1s)."
    - "`compute_retrieval_metrics()` dùng match field `result.title.lower() == expected_doc_id.lower()` (M2 D-10 KHÔNG dùng `category`) → return `{top_1_hit_rate, top_3_hit_rate, top_5_hit_rate, mrr, per_query}`."
    - "`compute_latency_percentiles()` dùng `statistics.quantiles(latencies, n=100)` stdlib Python 3.12 → return `{p50_ms, p95_ms, p99_ms, mean_ms, count}` — KHÔNG dep numpy."
    - "Unit test `test_metrics.py` cover 3 case (top-1 hit, top-3 hit không top-1, không match → rank None) + latency percentile 12 sample — PASS pytest."
  artifacts:
    - path: "Hub_All/eval/lib.py"
      provides: "APIClient (login/refresh/upload/poll/search) + preflight_check() + get_eval_hub_id()"
      min_lines: 250
      exports: ["APIClient", "preflight_check", "get_eval_hub_id", "upload_and_wait"]
    - path: "Hub_All/eval/metrics.py"
      provides: "compute_retrieval_metrics + compute_latency_percentiles (pure Python stdlib)"
      min_lines: 80
      exports: ["compute_retrieval_metrics", "compute_latency_percentiles"]
    - path: "Hub_All/eval/tests/test_metrics.py"
      provides: "Unit test 6 case (3 metrics + 3 latency edge case)"
      contains: "@pytest.mark.critical"
  key_links:
    - from: "eval/lib.py APIClient.search()"
      to: "POST /api/search body {query, hub_ids, top_k} (Phase 6 D-02)"
      via: "httpx.AsyncClient POST + JSON body"
      pattern: "POST.*api/search"
    - from: "eval/lib.py upload_and_wait()"
      to: "POST /api/documents/upload multipart + GET /api/documents/:id poll"
      via: "httpx.AsyncClient multipart + polling 1s timeout 60s"
      pattern: "documents/upload"
    - from: "eval/metrics.py compute_retrieval_metrics()"
      to: "result.title.lower() match expected_doc_id (D-10 M2 search_service.py:_row_to_item line 142)"
      via: "filename match KHÔNG category"
      pattern: "title.*lower"
    - from: "eval/lib.py preflight_check()"
      to: "GET /healthz + GET /readyz + Postgres SELECT eval_hub"
      via: "httpx.get + psycopg.connect"
      pattern: "healthz|readyz"
---

<objective>
Build hai library core cho eval framework: `eval/lib.py` (HTTP client + preflight + upload+poll) và `eval/metrics.py` (top-K/MRR/latency percentile compute). Plan 09-04 sẽ import 2 module này. Plan 09-05 dùng `metrics.py` test smoke.

Purpose: Pattern M1 baseline.py (588 dòng) tái dùng 90% — chỉ adapt 4 điểm M2-specific (D-02 POST search body, D-10 title=filename match, R4 415 tolerant, Phase 4 race poll timeout ≥30s). Tách `lib.py` + `metrics.py` thành 2 file riêng để Plan 09-05 test smoke chỉ mock 1 nửa (mock LiteLLM embed trong `lib.py`, KHÔNG mock `metrics.py` compute pure).
Output: 2 library + 1 test file đủ để Plan 09-03 (report.py) + Plan 09-04 (run_eval.py) build orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-eval-framework-quality-gate/09-RESEARCH.md
@.planning/phases/09-eval-framework-quality-gate/09-01-SUMMARY.md
@.planning/CONVENTIONS.md

<interfaces>
<!-- Phase 6 D-02 search endpoint contract (api/app/schemas/search.py + api/app/routers/search.py) -->

POST /api/search
Request body:
```json
{
  "query": "string",
  "hub_ids": ["uuid"],
  "top_k": 10
}
```
Response envelope:
```json
{
  "success": true,
  "data": {
    "results": [
      {"id":"chunk-uuid", "title":"DMD_T1-01_...docx", "content":"...", "score":0.92, "document_id":"...", "hub_id":"..."}
    ]
  },
  "error": null,
  "meta": {"latency_ms": 145}
}
```
**KEY:** `result.title = filename` (D-10 M2, search_service.py:_row_to_item line 142). KHÔNG dùng `category` (Go cũ luôn None).

<!-- Phase 4 INGEST endpoints contract -->

POST /api/documents/upload (multipart)
- Field: `file` (binary), `hub_id` (str form)
- Response 202: `{success:true, data:{id, status:'pending', filename, ...}}`
- Response 415: `{success:false, error:{code:'unsupported_format', message:'...'}}` (R4 scanned PDF)

GET /api/documents/:id
- Response 200: `{success:true, data:{id, status: 'pending'|'processing'|'completed'|'failed'|'failed_unsupported', chunk_count: int, ...}}`

Phase 4 race fix (Plan 04-08): `trigger_cocoindex_update` initial delay 0.1s + retry 3 attempts (0.5s, 1.0s, 1.5s) = ~3.6s worst-case overhead. Poll timeout PHẢI ≥30s/file.

<!-- Phase 3 auth contract -->

POST /api/auth/login → `{data:{access_token, refresh_token, user}}` (RS256 TTL 15min + 7d)
POST /api/auth/refresh → rotate refresh, return new pair
- Auth header: `Authorization: Bearer <jwt>`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build eval/lib.py — APIClient + preflight_check + upload_and_wait</name>
  <files>Hub_All/eval/lib.py</files>
  <behavior>
- APIClient.__init__ ghi nhận base_url + admin email + admin password; tạo httpx.AsyncClient timeout=120s.
- APIClient.login() POST /api/auth/login → set self.access_token + self.refresh_token.
- APIClient.refresh() POST /api/auth/refresh → rotate token; raise nếu 401 (cần re-login).
- APIClient._request_with_retry(): add Bearer header, gặp 401 → gọi refresh() → retry 1 lần. Trả Response.
- APIClient.upload_document(file_path, hub_id) → POST multipart, trả {id, status, filename, ...} HOẶC raise nếu 415 (caller xử lý).
- APIClient.get_document(doc_id) → GET status.
- APIClient.search(query, hub_id, top_k=10) → POST body {query, hub_ids:[hub_id], top_k}, trả `data.results`.
- preflight_check(settings) → async; verify GET /healthz 200, GET /readyz 200, Postgres SELECT id FROM hubs WHERE code='eval' trả 1 row. Fail → SystemExit kèm hint cụ thể.
- get_eval_hub_id(settings) → SELECT id FROM hubs WHERE code='eval' → return UUID str.
- upload_and_wait(api, file_path, hub_id, timeout_sec=60, poll_sec=1.0) → upload, nếu 415 trả `{filename, status:'failed_unsupported', id:None}`; nếu OK poll status mỗi 1s đến completed/failed/failed_unsupported HOẶC timeout → trả dict {id, filename, status, chunk_count, error_message?}.
  </behavior>
  <action>
Build `Hub_All/eval/lib.py` theo pattern M1 baseline.py (RESEARCH.md Pattern 1-3) — adapt 4 điểm M2:

**1. Header + imports + Settings:**
```python
"""Eval framework lib — APIClient + preflight + upload+poll (Phase 9 EVAL-02).

Pattern source: M1 baseline.py (commit 0af44f0:Hub_All/eval/baseline.py).
M2 adapt:
- D-02 POST /api/search body {query, hub_ids, top_k} (KHÔNG GET ?q=)
- D-10 result.title = filename (KHÔNG category)
- R4 415 tolerant cho scanned PDF (failed_unsupported KHÔNG raise)
- Phase 4 race fix tolerant: poll timeout ≥30s/file (Plan 04-08 retry 0.5/1.0/1.5s)
"""
from __future__ import annotations

import asyncio
import os
import time
from dataclasses import dataclass, field
from pathlib import Path

import httpx
import psycopg
from dotenv import load_dotenv

load_dotenv()


@dataclass(slots=True)
class EvalSettings:
    backend_url: str = field(default_factory=lambda: os.getenv("BACKEND_URL", "http://localhost:8180"))
    admin_email: str = field(default_factory=lambda: os.getenv("ADMIN_EMAIL", "admin@medinet.vn"))
    admin_password: str = field(default_factory=lambda: os.getenv("ADMIN_PASSWORD", ""))
    db_host: str = field(default_factory=lambda: os.getenv("DB_HOST", "localhost"))
    db_port: int = field(default_factory=lambda: int(os.getenv("DB_PORT", "5432")))
    db_name: str = field(default_factory=lambda: os.getenv("DB_NAME", "medinet_central"))
    db_user: str = field(default_factory=lambda: os.getenv("DB_USER", "medinet"))
    db_password: str = field(default_factory=lambda: os.getenv("DB_PASSWORD", ""))
    eval_hub_code: str = field(default_factory=lambda: os.getenv("EVAL_HUB_CODE", "eval"))
    upload_timeout_sec: int = field(default_factory=lambda: int(os.getenv("EVAL_UPLOAD_TIMEOUT_SEC", "60")))
    poll_interval_sec: float = field(default_factory=lambda: float(os.getenv("EVAL_POLL_INTERVAL_SEC", "1.0")))


def _dsn(s: EvalSettings) -> str:
    return f"host={s.db_host} port={s.db_port} dbname={s.db_name} user={s.db_user} password={s.db_password}"
```

**2. APIClient class (RESEARCH Pattern 2):**
```python
class APIClient:
    """httpx async client với JWT auto-refresh on 401.

    M2 endpoint adaptations:
    - POST /api/auth/login (Phase 3)
    - POST /api/documents/upload (Phase 4) — multipart
    - GET /api/documents/:id (Phase 4) — poll status
    - POST /api/search (Phase 6 D-02) — body {query, hub_ids, top_k}
    """

    def __init__(self, base_url: str, email: str, password: str) -> None:
        self.base_url = base_url.rstrip("/")
        self.email = email
        self.password = password
        self.access_token: str | None = None
        self.refresh_token: str | None = None
        self._client = httpx.AsyncClient(timeout=httpx.Timeout(120.0))

    async def __aenter__(self) -> APIClient:
        return self

    async def __aexit__(self, *args: object) -> None:
        await self._client.aclose()

    async def login(self) -> None:
        r = await self._client.post(
            f"{self.base_url}/api/auth/login",
            json={"email": self.email, "password": self.password},
        )
        if r.status_code != 200:
            raise SystemExit(f"Login fail {r.status_code}: {r.text[:200]}")
        data = r.json()["data"]
        self.access_token = data["access_token"]
        self.refresh_token = data["refresh_token"]

    async def refresh(self) -> None:
        if not self.refresh_token:
            await self.login()
            return
        r = await self._client.post(
            f"{self.base_url}/api/auth/refresh",
            json={"refresh_token": self.refresh_token},
        )
        if r.status_code != 200:
            # Refresh token expired → fallback re-login
            await self.login()
            return
        data = r.json()["data"]
        self.access_token = data["access_token"]
        self.refresh_token = data["refresh_token"]

    async def _request_with_retry(self, method: str, url: str, **kw: object) -> httpx.Response:
        headers = dict(kw.pop("headers", {}) or {})
        headers["Authorization"] = f"Bearer {self.access_token}"
        r = await self._client.request(method, url, headers=headers, **kw)  # type: ignore[arg-type]
        if r.status_code == 401:
            await self.refresh()
            headers["Authorization"] = f"Bearer {self.access_token}"
            r = await self._client.request(method, url, headers=headers, **kw)  # type: ignore[arg-type]
        return r

    async def upload_document(self, file_path: Path, hub_id: str) -> tuple[int, dict]:
        """POST multipart. Trả (status_code, data_dict).
        Caller xử lý 202 (OK) vs 415 (failed_unsupported R4)."""
        with file_path.open("rb") as f:
            files = {"file": (file_path.name, f.read(), "application/octet-stream")}
            r = await self._request_with_retry(
                "POST",
                f"{self.base_url}/api/documents/upload",
                files=files,
                data={"hub_id": hub_id},
            )
        return r.status_code, r.json()

    async def get_document(self, doc_id: str) -> dict:
        r = await self._request_with_retry("GET", f"{self.base_url}/api/documents/{doc_id}")
        r.raise_for_status()
        return r.json()["data"]

    async def search(self, query: str, hub_id: str, top_k: int = 10) -> tuple[list[dict], float]:
        """POST /api/search body {query, hub_ids, top_k} (D-02). Trả (results, latency_ms)."""
        t0 = time.perf_counter()
        r = await self._request_with_retry(
            "POST",
            f"{self.base_url}/api/search",
            json={"query": query, "hub_ids": [hub_id], "top_k": top_k},
        )
        t1 = time.perf_counter()
        r.raise_for_status()
        results = r.json()["data"]["results"]
        return results, (t1 - t0) * 1000.0
```

**3. preflight_check + get_eval_hub_id (RESEARCH Pattern 1):**
```python
async def preflight_check(settings: EvalSettings) -> None:
    """Verify backend + Postgres + eval_hub seed — fail loud nếu thiếu."""
    async with httpx.AsyncClient(timeout=10) as client:
        try:
            r = await client.get(f"{settings.backend_url}/healthz")
            if r.status_code != 200:
                raise SystemExit(
                    f"Pre-flight FAIL: backend healthz {settings.backend_url} trả {r.status_code}.\n"
                    f"Khởi động: cd Hub_All && docker compose up -d hoặc (cd api && make dev)"
                )
            r = await client.get(f"{settings.backend_url}/readyz")
            if r.status_code != 200:
                raise SystemExit(
                    f"Pre-flight FAIL: backend KHÔNG ready (cocoindex flow + DB pool).\n"
                    f"Check uvicorn logs cho 'cocoindex_init_failed_fail_fast' ERROR."
                )
        except httpx.RequestError as e:
            raise SystemExit(f"Pre-flight FAIL: không kết nối backend: {e}") from e

    try:
        with psycopg.connect(_dsn(settings), connect_timeout=5) as conn, conn.cursor() as cur:
            cur.execute("SELECT id FROM hubs WHERE code = %s", (settings.eval_hub_code,))
            row = cur.fetchone()
            if not row:
                raise SystemExit(
                    f"Pre-flight FAIL: hub code='{settings.eval_hub_code}' chưa seed.\n"
                    f"Chạy: psql -h {settings.db_host} -U {settings.db_user} -d {settings.db_name} "
                    f"-f Hub_All/eval/scripts/seed_hub.sql"
                )
    except psycopg.OperationalError as e:
        raise SystemExit(f"Pre-flight FAIL: không kết nối Postgres: {e}") from e


async def get_eval_hub_id(settings: EvalSettings) -> str:
    with psycopg.connect(_dsn(settings)) as conn, conn.cursor() as cur:
        cur.execute("SELECT id FROM hubs WHERE code = %s", (settings.eval_hub_code,))
        row = cur.fetchone()
        if not row:
            raise SystemExit(f"eval_hub code='{settings.eval_hub_code}' không tồn tại")
        return str(row[0])
```

**4. upload_and_wait (RESEARCH Pattern 3):**
```python
async def upload_and_wait(
    api: APIClient,
    file_path: Path,
    hub_id: str,
    timeout_sec: int = 60,
    poll_sec: float = 1.0,
) -> dict:
    """Upload + poll. R4 415 tolerant cho scanned PDF.
    Phase 4 race: poll timeout ≥30s/file (Plan 04-08 retry overhead ~3.6s)."""
    status_code, body = await api.upload_document(file_path, hub_id)

    # R4 mitigation — scanned PDF expected 415
    if status_code == 415:
        err = body.get("error", {}) or {}
        return {
            "filename": file_path.name,
            "status": "failed_unsupported",
            "error_message": err.get("message", "unsupported format"),
            "id": None,
            "chunk_count": 0,
        }
    if status_code not in (200, 201, 202):
        return {
            "filename": file_path.name,
            "status": "failed",
            "error_message": f"Upload HTTP {status_code}: {body}",
            "id": None,
            "chunk_count": 0,
        }

    doc = body["data"]
    doc_id = doc["id"]
    last_status = doc.get("status", "pending")

    deadline = time.time() + timeout_sec
    while time.time() < deadline:
        await asyncio.sleep(poll_sec)
        try:
            s = await api.get_document(doc_id)
        except httpx.HTTPStatusError as e:
            return {
                "id": doc_id,
                "filename": file_path.name,
                "status": "failed",
                "error_message": f"Poll HTTP error: {e}",
                "chunk_count": 0,
            }
        last_status = s.get("status", "unknown")
        if last_status in ("completed", "failed", "failed_unsupported"):
            return {
                "id": doc_id,
                "filename": file_path.name,
                "status": last_status,
                "chunk_count": s.get("chunk_count", 0),
                "error_message": s.get("error_message"),
            }
    return {
        "id": doc_id,
        "filename": file_path.name,
        "status": "timeout",
        "error_message": f"Poll timeout {timeout_sec}s — last status: {last_status}",
        "chunk_count": 0,
    }
```

**Verify `ruff check eval/lib.py` + `python -c "from eval.lib import APIClient, preflight_check, upload_and_wait, get_eval_hub_id, EvalSettings"`.**
  </action>
  <verify>
    <automated>cd Hub_All && ruff check eval/lib.py && python -c "from eval.lib import APIClient, preflight_check, upload_and_wait, get_eval_hub_id, EvalSettings; print('imports OK')"</automated>
  </verify>
  <done>
- `eval/lib.py` ≥ 250 dòng, ruff PASS.
- Export: APIClient (login/refresh/_request_with_retry/upload_document/get_document/search), preflight_check, get_eval_hub_id, upload_and_wait, EvalSettings.
- POST /api/search body `{query, hub_ids:[hub_id], top_k}` đúng D-02 Phase 6.
- 415 scanned PDF KHÔNG raise — trả `status='failed_unsupported'` (R4).
- Poll timeout default 60s/file (tolerant Phase 4 race retry).
- JWT auto-refresh on 401 (1 retry sau refresh).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build eval/metrics.py + unit test test_metrics.py</name>
  <files>
    Hub_All/eval/metrics.py,
    Hub_All/eval/tests/__init__.py,
    Hub_All/eval/tests/test_metrics.py
  </files>
  <behavior>
- compute_retrieval_metrics(queries, per_query_results) → return {top_1_hit_rate, top_3_hit_rate, top_5_hit_rate, mrr, per_query} với `per_query=[{id, query, expected_doc_id, rank: int|None, top_result_title}]`.
  - Test 1: 1 query, top-1 hit → top_1_hit_rate=1.0, top_3=1.0, top_5=1.0, mrr=1.0, rank=1.
  - Test 2: 1 query, hit ở rank 2 → top_1=0.0, top_3=1.0, top_5=1.0, mrr=0.5, rank=2.
  - Test 3: 1 query, không match → top_1=0.0, top_3=0.0, top_5=0.0, mrr=0.0, rank=None.
  - Test 4: 3 query mix (1 top-1, 1 top-3 không top-1, 1 miss) → top_1=1/3, top_3=2/3, top_5=2/3, mrr=(1+1/2+0)/3.
- compute_latency_percentiles(latencies_ms) → return {count, mean_ms, p50_ms, p95_ms, p99_ms}.
  - Test 5: 12 sample [100, 110, 120,...,210] (step 10) → count=12, p50≈155, p95≈205, p99≈209.5.
  - Test 6: empty list → return {count:0, mean_ms:0, p50_ms:0, p95_ms:0, p99_ms:0} (NOT raise).
  </behavior>
  <action>
**Tạo `Hub_All/eval/metrics.py`:**
```python
"""Eval metrics — top-K hit rate + MRR + latency percentile (pure Python stdlib).

Phase 9 EVAL-02.
M2-specific match: result.title.lower() == expected_doc_id.lower() (D-10).
KHÔNG dùng category (Go cũ luôn None).
"""
from __future__ import annotations

import statistics
from typing import Any


def compute_retrieval_metrics(
    queries: list[dict],
    per_query_results: list[dict],
) -> dict:
    """Compute top-K hit rate + MRR.

    Args:
        queries: list[{id, query, expected_doc_id, expected_section, hub_id, notes}]
        per_query_results: list[{results: list[{title, score, ...}]}]
            (cùng độ dài, cùng thứ tự với queries)

    Returns:
        {top_1_hit_rate, top_3_hit_rate, top_5_hit_rate, mrr, per_query}
    """
    n = len(queries)
    if n == 0:
        return {
            "top_1_hit_rate": 0.0,
            "top_3_hit_rate": 0.0,
            "top_5_hit_rate": 0.0,
            "mrr": 0.0,
            "per_query": [],
        }
    if len(per_query_results) != n:
        raise ValueError(
            f"queries len ({n}) != per_query_results len ({len(per_query_results)})"
        )

    hits_at_1 = hits_at_3 = hits_at_5 = 0
    rr_sum = 0.0
    per_query: list[dict] = []

    for q, r in zip(queries, per_query_results, strict=True):
        expected = (q.get("expected_doc_id") or "").lower()
        results = r.get("results", []) or []
        rank: int | None = None
        for idx, res in enumerate(results, 1):
            # M2 D-10: title = filename (KHÔNG dùng category)
            title = (res.get("title") or "").lower()
            if title == expected:
                rank = idx
                break

        if rank == 1:
            hits_at_1 += 1
        if rank is not None and rank <= 3:
            hits_at_3 += 1
        if rank is not None and rank <= 5:
            hits_at_5 += 1
        if rank is not None:
            rr_sum += 1.0 / rank

        per_query.append(
            {
                "id": q.get("id"),
                "query": q.get("query"),
                "expected_doc_id": q.get("expected_doc_id"),
                "rank": rank,
                "top_result_title": (results[0].get("title") if results else None),
                "result_count": len(results),
            }
        )

    return {
        "top_1_hit_rate": hits_at_1 / n,
        "top_3_hit_rate": hits_at_3 / n,
        "top_5_hit_rate": hits_at_5 / n,
        "mrr": rr_sum / n,
        "per_query": per_query,
    }


def compute_latency_percentiles(latencies_ms: list[float]) -> dict[str, Any]:
    """Compute p50/p95/p99 latency từ list ms. Stdlib only (statistics.quantiles n=100)."""
    if not latencies_ms:
        return {
            "count": 0,
            "mean_ms": 0.0,
            "p50_ms": 0.0,
            "p95_ms": 0.0,
            "p99_ms": 0.0,
        }

    sorted_lat = sorted(latencies_ms)
    n = len(sorted_lat)
    if n == 1:
        v = sorted_lat[0]
        return {
            "count": 1,
            "mean_ms": v,
            "p50_ms": v,
            "p95_ms": v,
            "p99_ms": v,
        }

    # statistics.quantiles(n=100) → 99 cutpoint chia thành 100 phần bằng nhau
    # quantiles[i-1] = percentile i (i từ 1 đến 99)
    qs = statistics.quantiles(sorted_lat, n=100)
    return {
        "count": n,
        "mean_ms": statistics.fmean(sorted_lat),
        "p50_ms": qs[49],  # index 49 = percentile 50
        "p95_ms": qs[94],  # percentile 95
        "p99_ms": qs[98],  # percentile 99
    }
```

**Tạo `Hub_All/eval/tests/__init__.py` rỗng.**

**Tạo `Hub_All/eval/tests/test_metrics.py`:**
```python
"""Unit test eval/metrics.py — pure compute, không cần backend (Phase 9 EVAL-02)."""
from __future__ import annotations

import pytest

from eval.metrics import compute_latency_percentiles, compute_retrieval_metrics


@pytest.mark.critical
def test_top_1_hit_perfect():
    queries = [{"id": "q1", "query": "x", "expected_doc_id": "doc-A.docx"}]
    results = [{"results": [{"title": "doc-A.docx", "score": 0.9}]}]
    m = compute_retrieval_metrics(queries, results)
    assert m["top_1_hit_rate"] == 1.0
    assert m["top_3_hit_rate"] == 1.0
    assert m["top_5_hit_rate"] == 1.0
    assert m["mrr"] == 1.0
    assert m["per_query"][0]["rank"] == 1


@pytest.mark.critical
def test_top_3_hit_rank_2():
    queries = [{"id": "q1", "query": "x", "expected_doc_id": "doc-A.docx"}]
    results = [
        {
            "results": [
                {"title": "doc-B.docx", "score": 0.95},
                {"title": "doc-A.docx", "score": 0.80},
                {"title": "doc-C.docx", "score": 0.70},
            ]
        }
    ]
    m = compute_retrieval_metrics(queries, results)
    assert m["top_1_hit_rate"] == 0.0
    assert m["top_3_hit_rate"] == 1.0
    assert m["top_5_hit_rate"] == 1.0
    assert m["mrr"] == 0.5
    assert m["per_query"][0]["rank"] == 2


@pytest.mark.critical
def test_no_match():
    queries = [{"id": "q1", "query": "x", "expected_doc_id": "doc-A.docx"}]
    results = [{"results": [{"title": "other.docx"}, {"title": "another.docx"}]}]
    m = compute_retrieval_metrics(queries, results)
    assert m["top_1_hit_rate"] == 0.0
    assert m["top_3_hit_rate"] == 0.0
    assert m["mrr"] == 0.0
    assert m["per_query"][0]["rank"] is None


def test_three_query_mix():
    queries = [
        {"id": "q1", "query": "x", "expected_doc_id": "A.docx"},
        {"id": "q2", "query": "y", "expected_doc_id": "B.docx"},
        {"id": "q3", "query": "z", "expected_doc_id": "C.docx"},
    ]
    results = [
        {"results": [{"title": "A.docx"}]},                            # rank 1
        {"results": [{"title": "X.docx"}, {"title": "B.docx"}]},       # rank 2
        {"results": [{"title": "X.docx"}, {"title": "Y.docx"}]},       # miss
    ]
    m = compute_retrieval_metrics(queries, results)
    assert m["top_1_hit_rate"] == pytest.approx(1 / 3)
    assert m["top_3_hit_rate"] == pytest.approx(2 / 3)
    assert m["top_5_hit_rate"] == pytest.approx(2 / 3)
    assert m["mrr"] == pytest.approx((1 + 0.5 + 0) / 3)


def test_case_insensitive_match():
    queries = [{"id": "q1", "query": "x", "expected_doc_id": "DMD_T1-01.docx"}]
    results = [{"results": [{"title": "dmd_t1-01.DOCX"}]}]
    m = compute_retrieval_metrics(queries, results)
    assert m["top_1_hit_rate"] == 1.0  # case-insensitive


@pytest.mark.critical
def test_latency_percentiles_12_samples():
    lat = [100.0, 110.0, 120.0, 130.0, 140.0, 150.0, 160.0, 170.0, 180.0, 190.0, 200.0, 210.0]
    p = compute_latency_percentiles(lat)
    assert p["count"] == 12
    assert p["mean_ms"] == pytest.approx(155.0)
    # p50 từ statistics.quantiles(n=100)[49] với 12 sample
    assert 140.0 <= p["p50_ms"] <= 170.0
    assert 195.0 <= p["p95_ms"] <= 210.0
    assert p["p99_ms"] <= 210.0


def test_latency_empty():
    p = compute_latency_percentiles([])
    assert p == {"count": 0, "mean_ms": 0.0, "p50_ms": 0.0, "p95_ms": 0.0, "p99_ms": 0.0}


def test_latency_single_sample():
    p = compute_latency_percentiles([100.0])
    assert p["count"] == 1
    assert p["p50_ms"] == 100.0
    assert p["p99_ms"] == 100.0


def test_empty_queries():
    m = compute_retrieval_metrics([], [])
    assert m["top_1_hit_rate"] == 0.0
    assert m["per_query"] == []


def test_mismatch_length_raises():
    with pytest.raises(ValueError):
        compute_retrieval_metrics([{"id": "q1"}], [])
```

**Chạy test:**
```bash
cd Hub_All/eval && pytest tests/test_metrics.py -v
# Expect: 10 passed
```
  </action>
  <verify>
    <automated>cd Hub_All/eval && pytest tests/test_metrics.py -v --no-header && ruff check metrics.py tests/test_metrics.py</automated>
  </verify>
  <done>
- `eval/metrics.py` ≥ 80 dòng, ruff PASS, pure stdlib (KHÔNG dep numpy).
- `pytest tests/test_metrics.py` PASS 10/10 (4 critical marker + 6 standard).
- `compute_retrieval_metrics` dùng `title.lower() == expected.lower()` (case-insensitive D-10).
- `compute_latency_percentiles` empty list KHÔNG raise (trả zeros).
- Mix 3 query test verify công thức MRR đúng: `(1 + 1/2 + 0) / 3`.
  </done>
</task>

</tasks>

<verification>
1. `Hub_All/eval/lib.py` ≥ 250 dòng, exports đúng: APIClient, preflight_check, get_eval_hub_id, upload_and_wait, EvalSettings.
2. `Hub_All/eval/metrics.py` ≥ 80 dòng, exports: compute_retrieval_metrics, compute_latency_percentiles.
3. `pytest Hub_All/eval/tests/test_metrics.py` PASS 10/10.
4. `ruff check Hub_All/eval/` exit 0.
5. POST /api/search body khớp Phase 6 D-02 schema (verify trong code).
6. 415 scanned PDF KHÔNG raise (R4 mitigation).
</verification>

<success_criteria>
- 2 library core sẵn sàng cho Plan 09-03 (import metrics vào report.py) + Plan 09-04 (import cả 2 vào run_eval.py).
- Test smoke sẽ dùng `compute_retrieval_metrics` pure-Python KHÔNG cần stack thật → tách boundary rõ ràng.
- Phase 6 D-02 contract đúng + Phase 4 R4 415 tolerant + Phase 3 JWT auto-refresh — 3 lớp adapt M2 cover xong.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/09-eval-framework-quality-gate/09-02-SUMMARY.md` ghi:
- Files created (eval/lib.py + eval/metrics.py + eval/tests/test_metrics.py + eval/tests/__init__.py).
- pytest output (`tests/test_metrics.py` 10 PASS).
- Lines of code mỗi file.
- Confirm 4 M2 adapt: D-02 POST body, D-10 title match, R4 415 tolerant, Phase 4 race poll ≥30s.
</output>
</content>
</invoke>