---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 05
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - api/app/schemas/api_keys.py
  - api/app/schemas/audit.py
  - api/app/pkg/crypto.py
  - api/app/services/api_key_service.py
  - api/app/services/audit_query_service.py
  - api/app/routers/api_keys.py
  - api/app/routers/audit_logs.py
  - api/app/auth/dependencies.py
autonomous: true
requirements: [AUX-01, AUX-02, AUX-03]
user_setup:
  - service: aes-key
    why: "API key encryption-at-rest cần AES-256 key 32-byte"
    env_vars:
      - name: AES_KEY
        source: "Sinh local: python -c \"import os,base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())\" — KHÔNG commit vào git"

must_haves:
  truths:
    - "Admin tạo API key qua POST /api/api-keys → trả plaintext key 1 LẦN duy nhất lúc tạo"
    - "API key lưu at-rest mã hóa AES-GCM (không plaintext); GET KHÔNG bao giờ trả plaintext"
    - "POST /api/api-keys/:id/revoke soft-revoke (is_active=FALSE) — KHÔNG DELETE row"
    - "GET /api/audit-logs admin-only, filter date/action/hub_id, cap per_page ≤ 100"
    - "GET /api/audit-logs decorate @limiter.limit — vượt ngưỡng → 429 envelope (AUX-03)"
    - "Auth middleware accept header X-API-Key ngoài JWT"
  artifacts:
    - path: "api/app/services/api_key_service.py"
      provides: "ApiKeyService — create/get/list/update/revoke + AES-GCM crypto"
      exports: ["ApiKeyService"]
    - path: "api/app/pkg/crypto.py"
      provides: "AES-GCM encrypt/decrypt helper cho API key at-rest"
      exports: ["encrypt_secret", "decrypt_secret"]
    - path: "api/app/routers/api_keys.py"
      provides: "API key CRUD router admin-only — list/get/create/PUT/revoke"
      exports: ["router"]
    - path: "api/app/routers/audit_logs.py"
      provides: "GET /api/audit-logs read-only admin router, rate-limited (AUX-03)"
      exports: ["router"]
  key_links:
    - from: "api/app/services/api_key_service.py"
      to: "pkg.crypto.encrypt_secret"
      via: "encrypt at-rest trước khi INSERT key_hash"
      pattern: "encrypt_secret"
    - from: "api/app/auth/dependencies.py"
      to: "api_keys table"
      via: "get_api_key_or_jwt — X-API-Key header auth"
      pattern: "X-API-Key"
    - from: "api/app/routers/audit_logs.py"
      to: "app.middleware.limiter"
      via: "@limiter.limit decorator trên GET /api/audit-logs"
      pattern: "limiter.limit"
---

<objective>
Giao API Key management (AUX-02) + Audit Logs query endpoint (AUX-01 phần GET) + áp rate limit (AUX-03) lên một endpoint Phase 5 thật. Endpoint contract từ `frontend/src/services/api.ts` (D-07).

**Settings CRUD — discretion resolved (BLOCKER 2):** xem Plan 05-01 objective — `frontend/src/services/api.ts` có ZERO call `/api/settings` → Phase 5 KHÔNG implement settings endpoint; bảng `settings` để dành rag-config (D-03, Phase 7). Plan 05-05 KHÔNG tạo settings router — chỉ api-keys + audit-logs.

API Key endpoints (admin-only — AUX-02, D-07 — có PUT update + POST revoke, KHÔNG DELETE cứng):
- `GET /api/api-keys` — list, `page,per_page`
- `GET /api/api-keys/:id` — get single
- `POST /api/api-keys` — create `{name,permissions[],allowed_hub_ids?,rate_limit?}` → trả `APIKeyWithPlaintextAPI` (plaintext key CHỈ 1 lần)
- `PUT /api/api-keys/:id` — update metadata `{name?,permissions?,allowed_hub_ids?,rate_limit?}`
- `POST /api/api-keys/:id/revoke` — soft revoke (`is_active=FALSE`), KHÔNG DELETE row

Audit Logs endpoint (admin-only — AUX-01):
- `GET /api/audit-logs` — filter `date_from,date_to,actor_type,action,hub_id,page,per_page`, cap per_page ≤ 100. **Decorate `@limiter.limit` (AUX-03)** — endpoint Phase 5 cụ thể được rate-limit để 429 verify được trong Phase 5 (xem W4 dưới).

API key crypto (AUX-02): plaintext `mdk_<random>`; lưu `key_hash` = AES-GCM encrypt với `AES_KEY` env; `key_prefix` = 8 ký tự đầu (UX hiển thị). Auth middleware accept `X-API-Key` header ngoài JWT.

**W4 — Rate limit (AUX-03) áp endpoint thật:** Plan 05-02 tạo module `rate_limit.py` (slowapi `Limiter`) nhưng nếu KHÔNG endpoint nào decorate `@limiter.limit` thì AUX-03 / 429 behavior không verify được trong Phase 5. SC5 ROADMAP nhắm `/api/search` — endpoint đó thuộc Phase 6 (chưa tồn tại). Vì vậy Phase 5 decorate **`GET /api/audit-logs`** với `@limiter.limit` ở mức cấu hình được (`settings.rate_limit_audit_logs_per_minute`, mặc định 60/phút). `/api/search` + `/api/ask` decoration **defer Phase 6/7** khi các endpoint đó được tạo — ghi nhận rõ trong code comment.

**WARNING 1 — AUX-03 SC5 partially deferred (ghi nhận rõ cho downstream verifier):** ROADMAP AUX-03 SC5 nêu đích danh `/api/search` làm endpoint rate-limit. `/api/search` là endpoint Phase 6 (chưa tồn tại ở Phase 5). Phase 5 verify **cơ chế rate-limit + 429 envelope shape** trên `GET /api/audit-logs` (endpoint Phase 5 thật) — đủ chứng minh slowapi limiter + 429 envelope hoạt động. Phần decoration `@limiter.limit` cho `/api/search` + `/api/ask` **defer Phase 6/7** khi 2 endpoint đó được tạo. → Downstream verifier KHÔNG flag SC5 là hard gap: cơ chế đã verify Phase 5, chỉ còn việc dán decorator lên endpoint Phase 6/7. Ghi nhận này cũng có ở `success_criteria` dưới.

Purpose: AUX-01 (query) + AUX-02 + AUX-03 (áp lên endpoint thật). Phụ thuộc Plan 05-01 (cột api_keys permissions/allowed_hub_ids/rate_limit, audit_logs đã đủ; config knob `rate_limit_audit_logs_per_minute`) + Plan 05-02 (module `rate_limit.py` — `limiter`).
Output: `schemas/api_keys.py`, `schemas/audit.py`, `pkg/crypto.py`, `services/api_key_service.py`, `services/audit_query_service.py`, `routers/api_keys.py`, `routers/audit_logs.py` (rate-limited), `dependencies.py` mở rộng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- Router analog — documents.py (CRUD) -->
APIRouter(prefix="/api/api-keys" / "/api/audit-logs", tags=[...])
admin-only: Depends(require_role("admin"))  # noqa: B008
resp.created/ok/paginated/not_found/bad_request

<!-- D-07 contract — api.ts -->
createAPIKey body (api.ts:314): { name, permissions: string[], allowed_hub_ids?: string[], rate_limit?: number }
  → trả APIKeyWithPlaintextAPI = APIKeyAPI + { plain_key }
updateAPIKey body (api.ts:318): { name?, permissions?, allowed_hub_ids?, rate_limit? }  → PUT
revokeAPIKey (api.ts:322): POST /api/api-keys/:id/revoke → { message }
APIKeyAPI fields (api.ts:595): id, name, key_prefix, permissions[], allowed_hub_ids?, allowed_rag_configs?,
  rate_limit, expires_at?, status, requests_today, requests_7d, bandwidth_used, last_used_at?, created_by, created_at
  LƯU Ý: requests_today/requests_7d/bandwidth_used/allowed_rag_configs KHÔNG có cột → trả 0 / [] hằng số (M2 chưa track usage; defer v4.0)
  status: derive từ is_active (TRUE→"active", FALSE→"revoked")
getAuditLogs params (api.ts:295): date_from?, date_to?, actor_type?, action?, hub_id?, page?, per_page?
AuditLogAPI fields (api.ts:578): id, timestamp, user_id?, user_name?, is_ai, action, target?, hub_id?, hub_name?,
  ip_address?, user_agent?, request_id?, duration_ms?, payload?
  LƯU Ý: ip_address/user_agent/duration_ms/is_ai KHÔNG có cột audit_logs → ip_address/user_agent=null, duration_ms=null, is_ai=false hằng số

<!-- api_keys table sau Plan 05-01 0003: id, name, key_hash, key_prefix, permissions JSONB, allowed_hub_ids JSONB,
     rate_limit INT, created_by, hub_id, is_active, last_used_at, expires_at, created_at, updated_at -->
<!-- audit_logs table: id, user_id, action, target_type, target_id, hub_id, payload JSONB, request_id, created_at -->

<!-- Crypto — cryptography lib (cần verify trong pyproject.toml; nếu chưa: uv add cryptography) -->
from cryptography.hazmat.primitives.ciphers.aead import AESGCM
AESGCM(key_32_bytes) — nonce 12 bytes random; ciphertext = encrypt(nonce, plaintext, None)
AES_KEY env — config.py đã có Settings.aes_key (str). base64-decode → 32 bytes.

<!-- Plan 05-02 output — api/app/middleware/rate_limit.py:
  limiter (slowapi Limiter), rate_limit_exceeded_handler. Export qua app/middleware/__init__.py.
  @limiter.limit("<N>/minute") decorator — slowapi yêu cầu endpoint function có param `request: Request`. -->

<!-- Plan 05-01 output — config.py Settings:
  rate_limit_audit_logs_per_minute: int = 60 (W4 — limit cho GET /api/audit-logs) -->

<!-- Auth dependency — auth/dependencies.py: get_current_user, get_redis -->
<!-- audit — Plan 05-01: audit_logs đã đủ cột; query endpoint chỉ SELECT -->

<!-- DATA CONTRACT — tên method canonical (BLOCKER 1 resolved):
  ApiKeyService method verify X-API-Key = `verify_key` (KHÔNG `verify_plaintext`).
  Producer = Plan 05-05 (file này). Consumer = Plan 05-06 auth/api_key.py + Plan 05-05 get_api_key_or_jwt.
  Cả 2 consumer gọi ApiKeyService(db=db).verify_key(x_api_key). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pkg/crypto.py + schemas api_keys/audit — AES-GCM helper + Pydantic schemas</name>
  <files>api/app/pkg/crypto.py, api/app/schemas/api_keys.py, api/app/schemas/audit.py</files>
  <read_first>
    - api/app/config.py (Settings.aes_key — đã có; cần base64-decode → 32 bytes)
    - api/app/schemas/documents.py (analog Pydantic v2)
    - Hub_All/frontend/src/services/api.ts (APIKeyAPI ~595, APIKeyWithPlaintextAPI ~613, AuditLogAPI ~578)
  </read_first>
  <action>
**`api/app/pkg/crypto.py`** — AES-GCM helper (AUX-02):
- Kiểm tra `cryptography` trong `Hub_All/api/pyproject.toml`; nếu chưa → `cd Hub_All/api && uv add cryptography`.
- `from __future__ import annotations`. Import `base64`, `os`, `AESGCM` từ `cryptography.hazmat.primitives.ciphers.aead`.
- `def _load_key() -> bytes`: đọc `get_settings().aes_key`, `base64.urlsafe_b64decode`; nếu len ≠ 32 → `raise ValueError("AES_KEY phải là 32-byte base64 (sinh: base64.urlsafe_b64encode(os.urandom(32)))")`.
- `def encrypt_secret(plaintext: str) -> str`: `aes = AESGCM(_load_key())`; `nonce = os.urandom(12)`; `ct = aes.encrypt(nonce, plaintext.encode(), None)`; return `base64.urlsafe_b64encode(nonce + ct).decode()` (nonce prepend ciphertext).
- `def decrypt_secret(token: str) -> str`: `raw = base64.urlsafe_b64decode(token)`; `nonce, ct = raw[:12], raw[12:]`; `return AESGCM(_load_key()).decrypt(nonce, ct, None).decode()`.
- Docstring: ghi rõ "key_hash trong api_keys lưu output encrypt_secret — at-rest mã hóa AES-256-GCM (AUX-02). Verify X-API-Key: decrypt key_hash so plaintext input."

**`api/app/schemas/api_keys.py`** (`from __future__ import annotations`):
- `ApiKeyStatus = Literal["active", "revoked"]`.
- `class CreateApiKeyRequest(BaseModel)`: `name: str = Field(min_length=1, max_length=200)`, `permissions: list[str] = Field(default_factory=list)`, `allowed_hub_ids: list[str] | None = None`, `rate_limit: int = Field(default=100, ge=1, le=10000)`.
- `class UpdateApiKeyRequest(BaseModel)`: `name: str | None = Field(default=None, max_length=200)`, `permissions: list[str] | None = None`, `allowed_hub_ids: list[str] | None = None`, `rate_limit: int | None = Field(default=None, ge=1, le=10000)`.
- `class ApiKeyResponse(BaseModel)` (= APIKeyAPI shape): `id: str`, `name: str`, `key_prefix: str`, `permissions: list[str]`, `allowed_hub_ids: list[str] | None`, `allowed_rag_configs: list[str] = Field(default_factory=list)`, `rate_limit: int`, `expires_at: datetime | None`, `status: ApiKeyStatus`, `requests_today: int = 0`, `requests_7d: int = 0`, `bandwidth_used: int = 0`, `last_used_at: datetime | None`, `created_by: str | None`, `created_at: datetime`.
- `class ApiKeyWithPlaintext(ApiKeyResponse)`: `plain_key: str` (CHỈ POST create trả).

**`api/app/schemas/audit.py`** (`from __future__ import annotations`):
- `class AuditLogResponse(BaseModel)` (= AuditLogAPI shape): `id: str`, `timestamp: datetime`, `user_id: str | None`, `user_name: str | None`, `is_ai: bool = False`, `action: str`, `target: str | None`, `hub_id: str | None`, `hub_name: str | None`, `ip_address: str | None = None`, `user_agent: str | None = None`, `request_id: str | None`, `duration_ms: int | None = None`, `payload: dict | None`.
Import `datetime`, `Literal`, `BaseModel`, `Field`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/pkg/crypto.py app/schemas/api_keys.py app/schemas/audit.py && uv run mypy --strict app/pkg/crypto.py app/schemas/api_keys.py app/schemas/audit.py && uv run python -c "import base64,os; from app.config import get_settings; get_settings.cache_clear(); os.environ['AES_KEY']=base64.urlsafe_b64encode(os.urandom(32)).decode(); get_settings.cache_clear(); from app.pkg.crypto import encrypt_secret, decrypt_secret; t=encrypt_secret('mdk_secret123'); assert decrypt_secret(t)=='mdk_secret123' and t!='mdk_secret123'"</automated>
  </verify>
  <acceptance_criteria>
    - `cryptography` xuất hiện trong `Hub_All/api/pyproject.toml`
    - `api/app/pkg/crypto.py` export `encrypt_secret`, `decrypt_secret`; round-trip encrypt→decrypt khôi phục plaintext; ciphertext ≠ plaintext
    - `_load_key` raise ValueError nếu AES_KEY không phải 32-byte
    - `api/app/schemas/api_keys.py` có CreateApiKeyRequest, UpdateApiKeyRequest, ApiKeyResponse, ApiKeyWithPlaintext; `ApiKeyWithPlaintext` có field `plain_key`, `ApiKeyResponse` KHÔNG có
    - `api/app/schemas/audit.py` có AuditLogResponse khớp AuditLogAPI
    - `ruff check` + `mypy --strict` exit 0; crypto round-trip smoke test exit 0
  </acceptance_criteria>
  <done>crypto.py AES-GCM round-trip OK; schemas api_keys + audit khớp api.ts contract.</done>
</task>

<task type="auto">
  <name>Task 2: services api_key_service.py + audit_query_service.py</name>
  <files>api/app/services/api_key_service.py, api/app/services/audit_query_service.py</files>
  <read_first>
    - api/app/services/documents_service.py (analog CRUD raw SQL + WHERE-clause builder)
    - api/app/pkg/crypto.py (Task 1 output — encrypt_secret/decrypt_secret)
    - api/app/schemas/api_keys.py, api/app/schemas/audit.py (Task 1 output)
    - api/app/models/settings.py (ApiKey cột), api/app/models/audit.py (AuditLog cột)
  </read_first>
  <action>
**`api/app/services/api_key_service.py`**:
- `from __future__ import annotations`, import `logging`, `secrets`, `json`, `Any`, `UUID`, `uuid4`, `text`, `AsyncSession`, schemas api_keys, `encrypt_secret`/`decrypt_secret`.
- `class ApiKeyService`: `__init__(self, db: AsyncSession) -> None: self.db = db`.
- `def _map_row(row) -> ApiKeyResponse`: map DB row → ApiKeyResponse; `status` = `"active" if row.is_active else "revoked"`; `permissions`/`allowed_hub_ids` từ JSONB column (asyncpg trả list/dict trực tiếp, nếu str → json.loads); `requests_today/requests_7d/bandwidth_used=0`, `allowed_rag_configs=[]`.
- `async def create(self, *, req: CreateApiKeyRequest, created_by: UUID) -> ApiKeyWithPlaintext`:
  - `plain_key = f"mdk_{secrets.token_urlsafe(32)}"`. `key_prefix = plain_key[:8]`. `key_hash = encrypt_secret(plain_key)`.
  - `key_id = uuid4()`. INSERT api_keys: `text("INSERT INTO api_keys (id, name, key_hash, key_prefix, permissions, allowed_hub_ids, rate_limit, created_by, is_active, created_at, updated_at) VALUES (:id, :name, :hash, :prefix, CAST(:perms AS JSONB), CAST(:hubs AS JSONB), :rate, :by, TRUE, NOW(), NOW())")` — `perms`/`hubs` = `json.dumps(...)`.
  - Return `ApiKeyWithPlaintext` build từ giá trị + `plain_key=plain_key` (plaintext CHỈ trả 1 lần này).
- `async def get(self, key_id: UUID) -> ApiKeyResponse | None`: SELECT → `_map_row` hoặc None. KHÔNG trả plaintext (chỉ key_prefix).
- `async def list(self, *, page: int, per_page: int) -> tuple[list[ApiKeyResponse], int]`: COUNT + LIMIT/OFFSET ORDER BY created_at DESC.
- `async def update(self, *, key_id: UUID, req: UpdateApiKeyRequest) -> ApiKeyResponse | None`: SET clause động cho `name`, `permissions` (CAST JSONB), `allowed_hub_ids` (CAST JSONB), `rate_limit` không None; `updated_at=NOW()`; 0 row → None.
- `async def revoke(self, *, key_id: UUID) -> bool`: `UPDATE api_keys SET is_active=FALSE, updated_at=NOW() WHERE id=:id`; return rowcount>0. (Soft revoke — KHÔNG DELETE; D-07 + CONTEXT Claude's Discretion.)
- `async def verify_key(self, plain_key: str) -> dict[str, Any] | None`: cho X-API-Key auth — SELECT mọi api_keys WHERE `key_prefix = :prefix AND is_active=TRUE` (prefix = plain_key[:8]); với mỗi row `try: decrypt_secret(row.key_hash) == plain_key` → match → return `{"id":..., "permissions":..., "allowed_hub_ids":...}` + `UPDATE api_keys SET last_used_at=NOW() WHERE id=...`; không match → None. Bọc decrypt trong try/except. (**DATA CONTRACT — BLOCKER 1: tên method = `verify_key`** — canonical name. Plan 05-06 `auth/api_key.py` gọi `ApiKeyService(db=db).verify_key(...)`; Plan 05-05 Task 3 `get_api_key_or_jwt` cũng gọi `verify_key`. KHÔNG đặt tên `verify_plaintext`.)
- Timestamp `NOW()`; `logger = logging.getLogger(__name__)`.

**`api/app/services/audit_query_service.py`**:
- `from __future__ import annotations`, import `Any`, `text`, `AsyncSession`, `AuditLogResponse`.
- `class AuditQueryService`: `__init__(self, db: AsyncSession) -> None: self.db = db`.
- `async def list(self, *, date_from: str | None, date_to: str | None, action: str | None, hub_id: str | None, page: int, per_page: int) -> tuple[list[AuditLogResponse], int]`:
  - WHERE-clause builder: `created_at >= :date_from`, `created_at <= :date_to`, `action = :action`, `hub_id = :hub_id`.
  - `actor_type` param từ api.ts: M2 audit_logs KHÔNG có cột actor_type → bỏ qua filter này (chấp nhận; documented). LEFT JOIN `users` lấy `user_name` (full_name), LEFT JOIN `hubs` lấy `hub_name`.
  - COUNT + `SELECT al.id, al.created_at, al.user_id, u.full_name, al.action, al.target_id, al.hub_id, h.name, al.request_id, al.payload FROM audit_logs al LEFT JOIN users u ON al.user_id=u.id LEFT JOIN hubs h ON al.hub_id=h.id {where} ORDER BY al.created_at DESC LIMIT :limit OFFSET :offset`.
  - Map → `AuditLogResponse`: `timestamp` = created_at, `user_name` = full_name, `target` = target_id, `hub_name` = h.name, `is_ai=False`, `ip_address=None`, `user_agent=None`, `duration_ms=None`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/api_key_service.py app/services/audit_query_service.py && uv run mypy --strict app/services/api_key_service.py app/services/audit_query_service.py && uv run python -c "from app.services.api_key_service import ApiKeyService; from app.services.audit_query_service import AuditQueryService; assert hasattr(ApiKeyService, 'verify_key')"</automated>
  </verify>
  <acceptance_criteria>
    - `ApiKeyService` có method: create, get, list, update, revoke, verify_key, _map_row
    - `create` dùng `encrypt_secret` cho key_hash; trả `ApiKeyWithPlaintext` có plain_key; `get`/`list` trả `ApiKeyResponse` KHÔNG plaintext
    - `revoke` dùng `UPDATE ... is_active=FALSE` — `grep "DELETE FROM api_keys" api/app/services/api_key_service.py` KHÔNG có match
    - `verify_key` decrypt key_hash so sánh + UPDATE last_used_at; `grep "verify_plaintext" api/app/services/api_key_service.py` KHÔNG có match (BLOCKER 1 — tên canonical = verify_key)
    - `AuditQueryService.list` có WHERE-clause builder + LEFT JOIN users + hubs
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>api_key_service.py AES-GCM CRUD + soft revoke + verify_key (canonical name — BLOCKER 1); audit_query_service.py list filter + JOIN.</done>
</task>

<task type="auto">
  <name>Task 3: routers api_keys.py + audit_logs.py (rate-limited) + X-API-Key dependency</name>
  <files>api/app/routers/api_keys.py, api/app/routers/audit_logs.py, api/app/auth/dependencies.py</files>
  <read_first>
    - api/app/routers/documents.py (analog router CRUD)
    - api/app/auth/dependencies.py (get_current_user, require_role — thêm get_api_key_or_jwt)
    - api/app/services/api_key_service.py, api/app/services/audit_query_service.py (Task 2 output)
    - api/app/schemas/api_keys.py, api/app/schemas/audit.py (Task 1 output)
    - api/app/middleware/rate_limit.py + api/app/middleware/__init__.py (Plan 05-02 — limiter, cách @limiter.limit decorate)
    - api/app/config.py (Plan 05-01 — Settings.rate_limit_audit_logs_per_minute)
    - Hub_All/frontend/src/services/api.ts (D-07 — api-keys verb/path, audit-logs filter)
  </read_first>
  <action>
**`api/app/routers/api_keys.py`** — `router = APIRouter(prefix="/api/api-keys", tags=["api-keys"])`. `get_api_key_service`. Mọi endpoint `require_role("admin")` (AUX-02):
- `@router.get("")` `list_api_keys(page=1, per_page=20, ...)`: cap → `service.list` → `resp.paginated`.
- `@router.post("")` `create_api_key(req: CreateApiKeyRequest, user = Depends(require_role("admin")), service = ...)`: `result = await service.create(req=req, created_by=user.id)`; `resp.created(data=result.model_dump(mode="json"))` (data có `plain_key` — frontend hiển thị 1 lần).
- `@router.get("/{key_id}")` `get_api_key(...)`: UUID validate → INVALID_API_KEY_ID; `service.get`; None → not_found; `resp.ok`.
- `@router.put("/{key_id}")` `update_api_key(key_id, req: UpdateApiKeyRequest, ...)`: UUID validate; `service.update`; None → not_found; `resp.ok`.
- `@router.post("/{key_id}/revoke")` `revoke_api_key(key_id, ...)`: UUID validate; `ok = await service.revoke(key_id=key_uuid)`; not ok → not_found; `resp.ok(data={"message": "API key đã thu hồi"})`.
- `# noqa: B008` cho Depends.

**`api/app/routers/audit_logs.py`** — `router = APIRouter(prefix="/api/audit-logs", tags=["audit-logs"])`. `get_audit_query_service`:
- Import `from app.middleware import limiter` (Plan 05-02 output) + `from app.config import get_settings`.
- **W4 — AUX-03 rate limit áp endpoint thật:** decorate endpoint GET với `@limiter.limit`. slowapi yêu cầu endpoint function khai báo param `request: Request` (không Depends) để limiter đọc — đảm bảo signature có `request: Request`.
```python
@router.get("")
@limiter.limit(f"{get_settings().rate_limit_audit_logs_per_minute}/minute")
async def list_audit_logs(
    request: Request,
    date_from: str | None = None,
    date_to: str | None = None,
    actor_type: str | None = None,
    action: str | None = None,
    hub_id: str | None = None,
    page: int = 1,
    per_page: int = 20,
    user=Depends(require_role("admin")),  # noqa: B008
    service=Depends(get_audit_query_service),  # noqa: B008
) -> JSONResponse:
    ...
```
  - cap per_page/page. `items, total = await service.list(date_from=date_from, date_to=date_to, action=action, hub_id=hub_id, page=capped_page, per_page=capped_per_page)`. (`actor_type` nhận nhưng KHÔNG filter — documented Task 2.) `resp.paginated(...)`.
  - admin-only — `require_role("admin")`.
  - Comment ghi rõ: "@limiter.limit áp `GET /api/audit-logs` — endpoint Phase 5 cụ thể để AUX-03 / 429 verify được trong Phase 5 (W4 + WARNING 1). Limit cấu hình qua `settings.rate_limit_audit_logs_per_minute`. ROADMAP SC5 nêu `/api/search` — endpoint đó Phase 6: decoration `@limiter.limit` cho `/api/search` + `/api/ask` defer Phase 6/7 khi endpoint tồn tại. Cơ chế limiter + 429 envelope đã verify ở Phase 5 qua endpoint này."
  - LƯU Ý thứ tự decorator: `@router.get("")` ở TRÊN, `@limiter.limit(...)` ở DƯỚI (sát def function) — slowapi yêu cầu limiter decorator gần function nhất. Limiter dùng được cần `app.state.limiter` wired — Plan 05-06 Task 1 wire vào main.py; ở Phase 5 test (Plan 05-06 Task 3) verify cơ chế qua app đã wire.

**`api/app/auth/dependencies.py`** — thêm dependency `get_api_key_or_jwt` (AUX-02 — accept X-API-Key ngoài JWT):
```python
async def get_api_key_or_jwt(
    request: Request,
    x_api_key: str | None = Header(default=None, alias="X-API-Key"),  # noqa: B008
    token: str | None = Depends(oauth2_scheme),  # noqa: B008
    jwt_mgr: JWTManager = Depends(get_jwt_manager),  # noqa: B008
    redis: Redis | None = Depends(get_redis),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> User:
    """Auth qua X-API-Key HOẶC Bearer JWT (AUX-02).

    X-API-Key ưu tiên: verify qua ApiKeyService.verify_key → load
    api_keys.created_by user. Nếu không có X-API-Key → fallback get_current_user.
    """
```
  - Nếu `x_api_key`: `from app.services.api_key_service import ApiKeyService`; `result = await ApiKeyService(db=db).verify_key(x_api_key)`; None → raise HTTPException 401 `{"code":"INVALID_API_KEY","message":"API key không hợp lệ hoặc đã thu hồi"}`; else load `User` qua `created_by` → return User.
  - Nếu không có x_api_key → gọi logic `get_current_user` (re-dùng — có thể `return await get_current_user(token=token, jwt_mgr=jwt_mgr, redis=redis, db=db)`).
  - Import `Header` từ `fastapi`.
- Cập nhật `app/auth/__init__.py` re-export `get_api_key_or_jwt` nếu cần.

**W4 (warning advisory) — `get_api_key_or_jwt` là Phase 6/7 scaffolding:** dependency này KHÔNG được endpoint Phase 5 nào dùng (api-keys/audit-logs đều JWT admin-only). Nó là scaffolding cho endpoint external Phase 6/7 (search/ask nhận CẢ JWT lẫn X-API-Key). Ghi MỘT comment docstring trong `get_api_key_or_jwt`: "# Phase 6/7 scaffolding — chưa endpoint Phase 5 nào consume; smoke test qua test_x_api_key_invalid_rejected ở Plan 05-06 verify cùng cơ chế verify_key." KHÔNG over-engineer thêm test riêng — một dòng note đủ.

LƯU Ý phối hợp Plan 05-06: Plan 05-06 Task 1 TẠO file riêng `app/auth/api_key.py` với dependency `require_api_key` (X-API-Key cho external integration, trả `dict` principal). `get_api_key_or_jwt` ở plan này là biến thể trả `User` (cho endpoint nhận CẢ JWT lẫn key). Hai dependency cùng tồn tại, KHÔNG xung đột — `auth/dependencies.py` (plan 05-05) vs `auth/api_key.py` (plan 05-06) là file khác nhau. **Cả 2 gọi `ApiKeyService.verify_key`** (BLOCKER 1 — tên method canonical thống nhất).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/routers/api_keys.py app/routers/audit_logs.py app/auth/dependencies.py && uv run mypy --strict app/routers/api_keys.py app/routers/audit_logs.py app/auth/dependencies.py && uv run python -c "from app.routers.api_keys import router as k; from app.routers.audit_logs import router as a; from app.auth.dependencies import get_api_key_or_jwt; kp={(r.path, tuple(sorted(r.methods))) for r in k.routes}; assert ('/api/api-keys/{key_id}/revoke',('POST',)) in kp; assert ('/api/api-keys/{key_id}',('PUT',)) in kp; assert any(r.path=='/api/audit-logs' for r in a.routes)"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/routers/api_keys.py` export `router` prefix `/api/api-keys` với 5 endpoint (list/get/POST create/PUT update/POST revoke)
    - revoke dùng `@router.post(".../revoke")` KHÔNG `@router.delete` (D-07 soft revoke)
    - `api/app/routers/audit_logs.py` export `router` prefix `/api/audit-logs` với 1 GET endpoint, `require_role("admin")`
    - **W4:** `grep "limiter.limit" api/app/routers/audit_logs.py` có match — `GET /api/audit-logs` được decorate `@limiter.limit` với limit từ `settings.rate_limit_audit_logs_per_minute`; endpoint function có param `request: Request`
    - `grep -c "require_role(\"admin\")" api/app/routers/api_keys.py` ≥ 5
    - `api/app/auth/dependencies.py` có `get_api_key_or_jwt` chấp nhận `Header(alias="X-API-Key")`; gọi `ApiKeyService(db=db).verify_key(...)` (BLOCKER 1); có docstring note "Phase 6/7 scaffolding"
    - `ruff check` + `mypy --strict` exit 0; route assertion smoke test exit 0
  </acceptance_criteria>
  <done>api_keys.py 5 endpoint admin-only (POST revoke soft); audit_logs.py GET admin-only + @limiter.limit (W4 — AUX-03 verify được); X-API-Key dependency get_api_key_or_jwt gọi verify_key (BLOCKER 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/api-keys | response trả plaintext key 1 lần — sau đó không khôi phục được |
| X-API-Key header → API | external integration auth — verify decrypt key_hash |
| api_keys.key_hash at-rest | DB compromise — key mã hóa AES-GCM, attacker không đọc plaintext |
| client → GET /api/audit-logs | endpoint rate-limited — spam → 429 (AUX-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | API key plaintext leak qua GET / DB dump | mitigate | `key_hash` lưu AES-GCM encrypt (encrypt_secret); GET/list trả `ApiKeyResponse` KHÔNG có plaintext (chỉ key_prefix 8 ký tự); plaintext CHỈ POST create trả 1 lần |
| T-05-05-02 | Elevation of Privilege | viewer/editor tạo/thu hồi API key | mitigate | mọi endpoint api_keys.py `require_role("admin")` (AUX-02 admin-only) |
| T-05-05-03 | Spoofing | X-API-Key giả mạo / revoked key vẫn dùng được | mitigate | verify_key SELECT `WHERE key_prefix=:p AND is_active=TRUE` — revoked key (is_active=FALSE) bị loại; decrypt so khớp exact |
| T-05-05-04 | Information Disclosure | audit-logs lộ payload PII cho non-admin | mitigate | `GET /api/audit-logs` `require_role("admin")` → 403 cho viewer/editor |
| T-05-05-05 | Tampering | SQL injection qua audit filter date/action/hub_id | mitigate | raw SQL chỉ `text()` + named bind params |
| T-05-05-06 | Information Disclosure | AES_KEY hard-code / commit git → giải mã toàn bộ key | mitigate | AES_KEY qua env (user_setup); `.gitignore` `api/.env` (đã có Phase 1); `_load_key` validate 32-byte |
| T-05-05-07 | Denial of Service | Spam GET /api/audit-logs làm sập API | mitigate | `@limiter.limit` (W4 — AUX-03) trên endpoint; vượt ngưỡng `rate_limit_audit_logs_per_minute` → 429 envelope; test verify Plan 05-06 |
| T-05-05-08 | Repudiation | Không truy được key bị dùng khi nào | accept | `verify_key` UPDATE `last_used_at`; usage count chi tiết (requests_today/7d) defer v4.0 — M2 trả 0 |
</threat_model>

<verification>
- `uv run ruff check` + `uv run mypy --strict` cho 8 file thay đổi clean
- crypto round-trip smoke test pass
- Route smoke test: api-keys 5 endpoint, audit-logs 1 endpoint (rate-limited)
- `grep verify_plaintext api/app/services/api_key_service.py` KHÔNG có match (BLOCKER 1 — tên canonical verify_key)
</verification>

<success_criteria>
- API key CRUD 5 endpoint admin-only; plaintext trả 1 lần lúc tạo; revoke soft (is_active=FALSE)
- key_hash mã hóa AES-GCM at-rest; X-API-Key header auth qua get_api_key_or_jwt + verify_key (tên method canonical — BLOCKER 1)
- GET /api/audit-logs admin-only, filter + pagination cap 100; decorate @limiter.limit (W4 — AUX-03 429 verify được trong Phase 5)
- **AUX-03 SC5 partially deferred (WARNING 1):** cơ chế rate-limit + 429 envelope verify trên `/api/audit-logs` ở Phase 5; decoration `@limiter.limit` cho `/api/search` + `/api/ask` defer Phase 6/7 khi endpoint đó tồn tại — downstream verifier KHÔNG flag SC5 là hard gap
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-05-SUMMARY.md`
</output>
</content>
