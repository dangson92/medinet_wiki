---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/versions/0003_phase5_schema_reconcile.py
  - api/app/models/hub.py
  - api/app/models/auth.py
  - api/app/models/settings.py
  - api/app/config.py
  - api/app/services/audit_service.py
  - api/app/main.py
autonomous: true
requirements: [HUB-01, AUX-01]
user_setup: []

must_haves:
  truths:
    - "Bảng hubs có cột code, subdomain, status — khớp HubAPI type frontend api.ts"
    - "Bảng users có cột phone, department, avatar_url, status — khớp UserAPI type frontend api.ts"
    - "Bảng api_keys có cột permissions, allowed_hub_ids, rate_limit — khớp APIKeyAPI type"
    - "alembic upgrade head + downgrade base + upgrade head round-trip chạy clean trên DB trống"
    - "audit_service.enqueue_audit() non-blocking; flush task batch 2s/128 INSERT vào audit_logs"
    - "audit_flush_loop drain 100 entry đồng thời → audit_logs có đúng 100 row, request_id unique"
  artifacts:
    - path: "api/migrations/versions/0003_phase5_schema_reconcile.py"
      provides: "Migration thêm cột Phase 5 cho hubs/users/api_keys"
      contains: "revision: str = \"0003\""
    - path: "api/app/services/audit_service.py"
      provides: "Audit logger asyncio.Queue batch flush + enqueue_audit helper"
      exports: ["enqueue_audit", "audit_flush_loop", "AuditEntry"]
    - path: "api/app/config.py"
      provides: "Settings fields audit_batch_size, audit_flush_interval_seconds, rate_limit knobs"
      contains: "audit_batch_size"
  key_links:
    - from: "api/app/main.py"
      to: "audit_service.audit_flush_loop"
      via: "asyncio.create_task trong lifespan"
      pattern: "audit_flush_loop"
    - from: "api/app/services/audit_service.py"
      to: "audit_logs table"
      via: "batch INSERT qua get_engine()"
      pattern: "INSERT INTO audit_logs"
---

<objective>
Đặt nền móng schema + hạ tầng audit cho toàn Phase 5. Hai việc:

1. **Migration 0003 reconcile schema** — Bảng `hubs`, `users`, `api_keys` từ Phase 2 thiếu các cột mà frontend `api.ts` mong đợi (`HubAPI`, `UserAPI`, `APIKeyAPI`). Migration thêm cột; cập nhật SQLAlchemy models tương ứng. D-05: KHÔNG thêm `chroma_collection`/`db_host`/`db_port`/`db_name`/`db_user` vào bảng `hubs` (di sản Go multi-DB — drop hẳn khỏi M2).
2. **Audit logger asyncio.Queue (AUX-01)** — `audit_service.py` mới: `enqueue_audit()` non-blocking + `audit_flush_loop()` batch flush 2s HOẶC 128 entry → INSERT `audit_logs`. Wire vào `main.py` lifespan (analog watchdog task).

**Settings CRUD — discretion resolved (BLOCKER 2):** `frontend/src/services/api.ts` có ZERO call `/api/settings` / `getSettings` / `updateSettings` (orchestrator đã grep verify — 0 match) → KHÔNG implement settings endpoint trong Phase 5. Bảng `settings` (Phase 2 model — `api/app/models/settings.py`) để dành cho rag-config (D-03, Phase 7); trang `Settings.tsx` frontend dự kiến drive rag-config chứ không phải key-value CRUD. Phase title "Settings CRUD" → omission có chủ đích, auditable: KHÔNG có settings router, KHÔNG migration cho `settings` table trong Phase 5.

Purpose: Plan 05-03/04/05 (Hub/User/APIKey CRUD) cần cột schema đúng + `enqueue_audit` sẵn sàng để ghi audit non-blocking. Đây là Wave 1 — mọi plan khác phụ thuộc.
Output: Migration 0003, models cập nhật, `audit_service.py`, config knobs, lifespan wire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- Cột bảng HIỆN CÓ sau Phase 2 (executor cần biết để migration ADD đúng cột thiếu) -->

hubs (api/app/models/hub.py): id (UUID PK), slug (Text unique), name (Text), description (Text null), is_active (Boolean), created_at, updated_at
  → THIẾU cho HubAPI: code, subdomain, status

users (api/app/models/auth.py): id (UUID PK), email (Text unique), password_hash (Text), full_name (Text null), role (Text CHECK admin|editor|viewer), is_active (Boolean), created_at, updated_at
  → THIẾU cho UserAPI: phone, department, avatar_url, status

api_keys (api/app/models/settings.py): id (UUID PK), name (Text), key_hash (Text unique), key_prefix (Text), created_by (UUID null), hub_id (UUID null), is_active (Boolean), last_used_at, expires_at, created_at, updated_at
  → THIẾU cho APIKeyAPI: permissions, allowed_hub_ids, rate_limit

audit_logs (api/app/models/audit.py) — ĐÃ ĐỦ CỘT: id, user_id, action, target_type, target_id, hub_id, payload (JSONB), request_id, created_at

Migration analog — api/migrations/versions/0002_phase4_documents_indexes.py:
  revision: str = "0002"; down_revision = "0001"; upgrade()/downgrade() dùng op.create_index / op.add_column

config.py Settings ĐÃ CÓ: aes_key (str), watchdog_timeout_seconds (int=300). Pattern field declaration: pydantic-settings, default value inline.

Lifespan task analog — api/app/main.py:170-179 (watchdog):
  app.state.watchdog_task = asyncio.create_task(watchdog_loop())
Shutdown analog — main.py:187-195: task.cancel() + await + except CancelledError.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0003 + cập nhật SQLAlchemy models</name>
  <files>api/migrations/versions/0003_phase5_schema_reconcile.py, api/app/models/hub.py, api/app/models/auth.py, api/app/models/settings.py</files>
  <read_first>
    - api/migrations/versions/0002_phase4_documents_indexes.py (analog migration — revision/down_revision/upgrade/downgrade format)
    - api/migrations/versions/0001_initial_schema.py (xem cách định nghĩa cột + CHECK constraint cho hubs/users/api_keys)
    - api/app/models/hub.py, api/app/models/auth.py, api/app/models/settings.py (model hiện tại — cột đang có)
    - Hub_All/frontend/src/services/api.ts (D-07 contract — type HubAPI dòng ~392, UserAPI ~373, APIKeyAPI ~595)
    - **WARNING 5 — verify alembic head TRƯỚC:** chạy `ls Hub_All/api/migrations/versions/` (hoặc `cd Hub_All/api && uv run alembic heads`) để xác nhận head thực tế. Tại thời điểm plan: chỉ có `0001`, `0002` → head = `0002`. NẾU head không phải `0002` (vd. Phase 4 gap-closure Plan 04-07 đã thêm migration mới) → set `down_revision` = head thực tế, `revision` = id kế tiếp còn trống, và điều chỉnh acceptance-criteria grep tương ứng.
  </read_first>
  <action>
Tạo `api/migrations/versions/0003_phase5_schema_reconcile.py`. Header: `revision: str = "0003"`, `down_revision: Union[str, None] = "0002"`, `branch_labels`/`depends_on` = None (copy nguyên style 0002 incl. noqa comments).

**WARNING 5 — KHÔNG hard-code mù `down_revision`:** Chạy `cd Hub_All/api && uv run alembic heads` (hoặc `ls Hub_All/api/migrations/versions/`) TRƯỚC khi viết header. Head xác nhận tại thời điểm planning = `0002` (chỉ có `0001`, `0002` trong versions/). Nếu kết quả khác `0002` → `down_revision` = head thực tế trả về, `revision` = id kế tiếp còn trống (vd. `0004` nếu một migration khác đã chiếm `0003`). KHÔNG để 2 migration cùng `revision` id — alembic sẽ báo multiple heads và `upgrade head` fail.

**W1 — Quyết định slug-vs-code (GHI NHẬN RÕ, KHÔNG ngầm định):** Bảng `hubs` Phase 2 đã có cột `slug` NOT NULL unique; frontend `HubAPI` lại mong field `code`. Migration 0003 GIỮ `slug` làm **legacy NOT NULL mirror của `code`** và thêm `code` làm **field contract frontend chính thức**. Sau Phase 5, `hubs` có 2 cột giá trị-trùng (`slug` = `code.lower()`); `HubService.create` (Plan 05-03) set `slug = req.code.lower()`. `slug` KHÔNG drop ở M2 (tránh phá Phase 2 baseline + migration phức tạp) — defer dọn cột `slug` redundant về milestone schema-cleanup tương lai. Đây là quyết định có chủ đích — KHÔNG phải sơ suất.

`upgrade()` — dùng `op.add_column` + `op.create_check_constraint`:
- Bảng `hubs`: thêm 3 cột —
  - `code` `sa.Text()` nullable=False server_default `''` (existing rows cần giá trị; sau migration drop server_default qua `op.alter_column(..., server_default=None)`). `code` là field contract frontend; `slug` (Phase 2) giữ làm legacy mirror — xem W1 ở trên.
  - `subdomain` `sa.Text()` nullable=False server_default `''`, rồi drop server_default.
  - `status` `sa.Text()` nullable=False server_default `'active'`.
  - `op.create_check_constraint("hub_status_enum", "hubs", "status IN ('active','inactive')")`.
  - `op.create_unique_constraint("uq_hubs_code", "hubs", ["code"])`.
- Bảng `users`: thêm 4 cột —
  - `phone` `sa.Text()` nullable=True.
  - `department` `sa.Text()` nullable=True.
  - `avatar_url` `sa.Text()` nullable=True.
  - `status` `sa.Text()` nullable=False server_default `'active'` + `op.create_check_constraint("user_status_enum", "users", "status IN ('active','disabled')")`.
- Bảng `api_keys`: thêm 3 cột —
  - `permissions` `postgresql.JSONB` nullable=False server_default `text("'[]'::jsonb")`.
  - `allowed_hub_ids` `postgresql.JSONB` nullable=True.
  - `rate_limit` `sa.Integer()` nullable=False server_default `text("100")`.

`downgrade()` — đảo ngược: drop_constraint + drop_column theo thứ tự ngược (api_keys → users → hubs). PHẢI drop cả `uq_hubs_code`, `hub_status_enum`, `user_status_enum` constraint trước khi drop_column tương ứng — downgrade phải chạy CLEAN (verify gate Task 1 chạy `downgrade base`).

KHÔNG thêm `chroma_collection`, `db_host`, `db_port`, `db_name`, `db_user` vào `hubs` (D-05 — di sản Go drop hẳn). KHÔNG drop cột nào (Phase 2 baseline giữ nguyên — chỉ ADD). KHÔNG touch bảng `settings` (BLOCKER 2 — Phase 5 không implement settings CRUD; bảng `settings` để dành Phase 7 rag-config).

Cập nhật models (per D-05, D-07):
- `api/app/models/hub.py` class `Hub`: thêm `code: Mapped[str] = mapped_column(Text, nullable=False)`, `subdomain: Mapped[str] = mapped_column(Text, nullable=False)`, `status: Mapped[str] = mapped_column(Text, nullable=False, server_default=text("'active'"))`. Thêm `CheckConstraint("status IN ('active','inactive')", name="hub_status_enum")` vào `__table_args__`. (`slug` giữ nguyên — legacy mirror của `code`, W1.)
- `api/app/models/auth.py` class `User`: thêm `phone`, `department`, `avatar_url` (`Mapped[str | None]`, nullable=True), `status: Mapped[str]` (nullable=False server_default `'active'`). Thêm `CheckConstraint("status IN ('active','disabled')", name="user_status_enum")` vào `__table_args__` (cạnh `role_enum` đã có).
- `api/app/models/settings.py` class `ApiKey`: thêm `permissions: Mapped[dict] = mapped_column(JSONB, nullable=False, server_default=text("'[]'::jsonb"))`, `allowed_hub_ids: Mapped[dict | None] = mapped_column(JSONB, nullable=True)`, `rate_limit: Mapped[int] = mapped_column(Integer, nullable=False, server_default=text("100"))`. Import `Integer` từ sqlalchemy.

Lưu ý timestamp: KHÔNG dùng `datetime.utcnow()` — server-side `NOW()` (CONVENTIONS §"Raw SQL").
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/models migrations/versions/0003_phase5_schema_reconcile.py && uv run mypy --strict app/models && uv run alembic heads && uv run alembic upgrade head && uv run alembic downgrade base && uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - File `api/migrations/versions/0003_phase5_schema_reconcile.py` tồn tại; `revision` id = id kế tiếp còn trống sau head thực tế (head xác nhận = `0002` → `revision: str = "0003"`, `down_revision` = `"0002"`); NẾU head khác `0002` → revision/down_revision điều chỉnh theo head thực tế (WARNING 5)
    - `cd Hub_All/api && uv run alembic heads` trả về ĐÚNG 1 head sau khi thêm migration (KHÔNG multiple heads)
    - `grep -c "op.add_column" api/migrations/versions/0003_phase5_schema_reconcile.py` trả ≥ 10 (3 hub + 4 user + 3 api_keys)
    - `api/app/models/hub.py` chứa `code:` và `subdomain:` và `status:`; KHÔNG chứa `chroma_collection` hay `db_host`
    - `api/app/models/auth.py` class User chứa `phone:`, `department:`, `avatar_url:`, `status:`
    - `api/app/models/settings.py` class ApiKey chứa `permissions:`, `allowed_hub_ids:`, `rate_limit:`
    - `ruff check` exit 0, `mypy --strict app/models` exit 0
    - **Alembic round-trip runtime gate (BLOCKER 3):** `alembic upgrade head` → `downgrade base` → `upgrade head` chạy clean trên DB testcontainer/trống — migration KHÔNG chỉ pass lint mà phải CHẠY THẬT được. Cần Docker Postgres `pgvector/pgvector:pg16` sẵn (CONVENTIONS test stack); nếu môi trường chưa có DB → executor spin testcontainer Postgres (hoặc `docker compose up -d postgres`) trước khi chạy verify, set `DATABASE_URL`/alembic env trỏ vào DB sạch đó. Migration lỗi runtime (downgrade fail, constraint name sai, server_default drop sai thứ tự) → FAIL task gate này — KHÔNG để Wave 2-4 build trên schema vỡ.
  </acceptance_criteria>
  <done>Migration 0003 + 3 model cập nhật khớp HubAPI/UserAPI/APIKeyAPI; ruff + mypy clean; alembic upgrade→downgrade→upgrade round-trip PASS trên DB sạch; chỉ 1 alembic head.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit_service.py — asyncio.Queue batch logger + config knobs</name>
  <files>api/app/services/audit_service.py, api/app/config.py</files>
  <read_first>
    - api/app/services/watchdog.py (analog asyncio background task lifecycle)
    - api/app/services/documents_service.py (dòng 336-349 — SQL shape INSERT audit_logs)
    - api/app/db/session.py (get_engine — cross-session work `async with engine.begin()`)
    - api/app/config.py (pattern khai báo Settings field — aes_key, watchdog_timeout_seconds)
    - api/app/models/audit.py (cột audit_logs: user_id, action, target_type, target_id, hub_id, payload JSONB, request_id, created_at)
  </read_first>
  <behavior>
    - enqueue_audit(entry) gọi từ sync context → put_nowait, KHÔNG raise nếu queue đầy (drop + log warning)
    - audit_flush_loop drain queue: flush khi đạt audit_batch_size (128) HOẶC sau audit_flush_interval_seconds (2s)
    - Batch INSERT N entry trong 1 transaction qua get_engine() — KHÔNG N lần INSERT riêng
    - flush_pending() (gọi lúc shutdown) drain hết queue còn lại trước khi cancel task
    - payload dict serialize JSONB; action enum hợp lệ gồm security.hub_isolation_violation
  </behavior>
  <action>
Thêm vào `api/app/config.py` class Settings (sau `aes_key`):
```python
# Audit logger (Phase 5 AUX-01 — asyncio.Queue batch flush)
audit_batch_size: int = 128
audit_flush_interval_seconds: float = 2.0
audit_queue_max_size: int = 10000
# Rate limit (Phase 5 AUX-03 — slowapi; Plan 05-02 consume)
rate_limit_search_per_minute: int = 100
rate_limit_upload_per_minute: int = 30
rate_limit_audit_logs_per_minute: int = 60
rate_limit_enabled: bool = True
```

Tạo `api/app/services/audit_service.py`:
- `from __future__ import annotations` đầu file.
- Dataclass `AuditEntry` (dùng `@dataclass`): fields `action: str`, `user_id: str | None`, `target_type: str | None`, `target_id: str | None`, `hub_id: str | None`, `payload: dict[str, Any] | None`, `request_id: str | None`.
- Constant `AUDIT_ACTIONS: frozenset[str]` = {`auth.login`, `auth.refresh`, `document.upload`, `document.delete`, `rag-config.update`, `hub.create`, `hub.update`, `user.create`, `security.hub_isolation_violation`}.
- Module-level `_queue: asyncio.Queue[AuditEntry] | None = None` + `_get_queue()` lazy-init dùng `get_settings().audit_queue_max_size` cho maxsize.
- `def enqueue_audit(entry: AuditEntry) -> None`: `_get_queue().put_nowait(entry)`; bọc `try/except asyncio.QueueFull` → `logger.warning("audit_queue_full_dropped: action=%s", entry.action)` (non-blocking, KHÔNG raise — audit log mất 1 entry tốt hơn block main request thread).
- `async def _flush_batch(entries: list[AuditEntry]) -> None`: nếu rỗng return; `engine = get_engine()`; `async with engine.begin() as conn:` — batch INSERT bằng `conn.execute(text("INSERT INTO audit_logs (id, user_id, action, target_type, target_id, hub_id, payload, request_id, created_at) VALUES (gen_random_uuid(), :user_id, :action, :target_type, :target_id, :hub_id, CAST(:payload AS JSONB), :request_id, NOW())"), [list of param dicts])` — SQLAlchemy executemany khi truyền list. `payload` → `json.dumps(entry.payload) if entry.payload else None`.
- `async def audit_flush_loop() -> None`: vòng `while True`: drain queue tới `audit_batch_size` HOẶC timeout `audit_flush_interval_seconds` (dùng `asyncio.wait_for(queue.get(), timeout=...)` + collect, bắt `asyncio.TimeoutError` để flush sớm); gọi `_flush_batch`; bắt `asyncio.CancelledError` → flush phần còn lại rồi `raise`.
- `async def flush_pending() -> None`: drain toàn bộ queue hiện tại (non-blocking get_nowait loop) → `_flush_batch`.
- `logger = logging.getLogger(__name__)`.

KHÔNG dùng `datetime.utcnow()` — `NOW()` server-side trong SQL.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/audit_service.py app/config.py && uv run mypy --strict app/services/audit_service.py app/config.py && uv run python -c "from app.services.audit_service import enqueue_audit, audit_flush_loop, AuditEntry, AUDIT_ACTIONS; assert 'security.hub_isolation_violation' in AUDIT_ACTIONS"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/services/audit_service.py` tồn tại, export `enqueue_audit`, `audit_flush_loop`, `AuditEntry`, `flush_pending`, `AUDIT_ACTIONS`
    - `grep "security.hub_isolation_violation" api/app/services/audit_service.py` có match
    - `grep "INSERT INTO audit_logs" api/app/services/audit_service.py` có match
    - `api/app/config.py` chứa `audit_batch_size`, `audit_flush_interval_seconds`, `rate_limit_search_per_minute`, `rate_limit_audit_logs_per_minute`
    - `enqueue_audit` KHÔNG raise khi queue đầy (có `except` cho QueueFull)
    - `ruff check` + `mypy --strict` exit 0; import smoke test exit 0
  </acceptance_criteria>
  <done>audit_service.py có enqueue_audit non-blocking + batch flush loop; config có audit + rate-limit knobs (gồm rate_limit_audit_logs_per_minute cho W4).</done>
</task>

<task type="auto">
  <name>Task 3: Wire audit_flush_loop vào main.py lifespan</name>
  <files>api/app/main.py</files>
  <read_first>
    - api/app/main.py (dòng 164-179 watchdog task create; 184-195 shutdown cancel — analog cần copy)
    - api/app/services/audit_service.py (Task 2 output — audit_flush_loop, flush_pending)
  </read_first>
  <action>
Trong `api/app/main.py` `lifespan()`:
- Sau block watchdog task (sau dòng ~179), thêm step 8:
```python
# 8) Audit flush task (Phase 5 AUX-01 — asyncio.Queue batch flush 2s/128).
app.state.audit_task = None
try:
    from app.services.audit_service import audit_flush_loop
    app.state.audit_task = asyncio.create_task(audit_flush_loop())
    logger.info("audit_flush_task_started")
except Exception as e:  # noqa: BLE001
    logger.warning("audit_flush_task_start_failed: %s", e)
```
- Trong block `finally:` (shutdown), TRƯỚC dispose_engine (vì flush cần engine), thêm: cancel audit task + flush pending —
```python
if getattr(app.state, "audit_task", None) is not None:
    try:
        from app.services.audit_service import flush_pending
        await flush_pending()
    except Exception as e:  # noqa: BLE001
        logger.warning("audit_flush_pending_failed: %s", e)
    app.state.audit_task.cancel()
    try:
        await app.state.audit_task
    except asyncio.CancelledError:
        pass
    except Exception as e:  # noqa: BLE001
        logger.warning("audit_task_stop_failed: %s", e)
    app.state.audit_task = None
```
Đặt cancel audit_task TRƯỚC `dispose_engine()` — flush_pending dùng engine. `import asyncio` đã có ở module scope lifespan (dòng 108) — dùng lại.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/main.py && uv run mypy --strict app/main.py && uv run python -c "from app.main import create_app; create_app()"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "audit_flush_loop" api/app/main.py` có match (create_task trong lifespan)
    - `grep "flush_pending" api/app/main.py` có match (shutdown block)
    - `flush_pending` được gọi TRƯỚC `dispose_engine` trong block finally (audit flush cần engine còn sống)
    - `ruff check app/main.py` + `mypy --strict app/main.py` exit 0
    - `create_app()` import + chạy không raise
  </acceptance_criteria>
  <done>Lifespan start audit_flush_loop lúc startup, flush_pending + cancel lúc shutdown trước khi dispose engine.</done>
</task>

<task type="auto">
  <name>Task 4: test_audit_logger.py — concurrency test AUX-01 SC4 (WARNING 2 / HARD-03)</name>
  <files>api/tests/integration/test_audit_logger.py</files>
  <read_first>
    - api/app/services/audit_service.py (Task 2 output — enqueue_audit, audit_flush_loop, flush_pending, AuditEntry, AUDIT_ACTIONS)
    - api/tests/integration/conftest.py (fixture postgres_container, get_engine; pattern truncate per-test)
    - api/tests/integration/test_documents_list_delete.py (analog — async test + audit_logs SELECT COUNT pattern)
  </read_first>
  <action>
**WARNING 2 — ROADMAP AUX-03 SC4 chưa có test phủ:** SC4 yêu cầu "100 concurrent action → audit_logs có 100 row, request_id unique, non-blocking". Plan 05-06 chỉ test single-row flush qua hub-isolation. Task này thêm test concurrency chuyên biệt.

Tạo `api/tests/integration/test_audit_logger.py` — `@pytest.mark.integration` + `@pytest.mark.asyncio`. Test `test_audit_logger_100_concurrent` mark thêm `@pytest.mark.critical` (HARD-03 CI gate). KHÔNG `pytest.skip`, KHÔNG `assert True # TODO`.

Setup: TRUNCATE `audit_logs` trước test (qua `get_engine()` raw SQL — đảm bảo COUNT đếm đúng). audit_logs FK `user_id`/`hub_id` nullable → dùng `user_id=None`, `hub_id=None` để tránh phụ thuộc fixture user/hub.

Test cases:
1. `test_audit_logger_100_concurrent` (`@pytest.mark.critical`) — SC4:
   - Tạo 100 `AuditEntry` với `action="auth.login"` (action hợp lệ trong AUDIT_ACTIONS), mỗi entry `request_id = str(uuid4())` UNIQUE, `user_id=None`, `hub_id=None`, `payload={"i": i}`.
   - Enqueue đồng thời: gọi `enqueue_audit(entry)` cho cả 100 entry (enqueue là sync non-blocking — gọi tuần tự trong vòng for MÔ PHỎNG 100 action; hoặc bọc `asyncio.gather` các coroutine mỗi cái gọi enqueue để mô phỏng concurrency thật).
   - Flush: gọi `await flush_pending()` (drain toàn bộ queue → batch INSERT).
   - Assert: `SELECT COUNT(*) FROM audit_logs` == 100; `SELECT COUNT(DISTINCT request_id) FROM audit_logs` == 100 (mọi request_id unique — không mất, không trùng).
2. `test_enqueue_non_blocking_when_queue_full` — set queue maxsize nhỏ (hoặc enqueue vượt `audit_queue_max_size` mô phỏng) → `enqueue_audit` KHÔNG raise (drop + warning); assert gọi enqueue quá ngưỡng KHÔNG ném exception.
3. `test_audit_flush_loop_batches` — enqueue 130 entry (> batch_size 128), start `audit_flush_loop` task, poll-with-timeout (KHÔNG sleep cứng) tới khi `COUNT(*) >= 130`, cancel task; assert đủ 130 row. (Verify flush theo batch_size threshold.)

Envelope/assert: dùng raw SQL `text()` qua `async with get_engine().begin() as conn`. Mỗi test docstring ghi liên kết AUX-01 SC4.

LƯU Ý task count: Plan 05-01 giờ có 4 task (3 + 1 test-only). Vượt guideline 2-3 task nhưng được phép cho test-only task (theo revision instruction WARNING 2). Task 4 không sửa production code → context cost thấp (~10-15%).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/test_audit_logger.py && uv run pytest tests/integration/test_audit_logger.py -x -q && uv run pytest -m critical -q -k audit_logger</automated>
  </verify>
  <acceptance_criteria>
    - `api/tests/integration/test_audit_logger.py` tồn tại, có ≥3 test
    - `test_audit_logger_100_concurrent` mark `@pytest.mark.critical`; assert `COUNT(*) == 100` VÀ `COUNT(DISTINCT request_id) == 100`
    - KHÔNG có `pytest.skip` hay `assert True  # TODO` trong file
    - `pytest tests/integration/test_audit_logger.py` exit 0
    - `pytest -m critical -k audit_logger` exit 0 (HARD-03 — SC4 concurrency được phủ test critical)
    - `ruff check` exit 0
  </acceptance_criteria>
  <done>test_audit_logger.py phủ AUX-01 SC4 — 100 concurrent enqueue → 100 row audit_logs + request_id unique; enqueue non-blocking khi full; flush theo batch threshold. Test critical PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runtime | Migration 0003 chạy trên DB production có data — server_default đảm bảo existing rows không vi phạm NOT NULL |
| audit_logs payload | `payload JSONB` chứa dữ liệu do code app sinh — KHÔNG nhận raw từ client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Migration 0003 NOT NULL cột mới trên bảng có data | mitigate | server_default cho code/subdomain/status/permissions/rate_limit; existing rows nhận giá trị mặc định hợp lệ; downgrade() đảo ngược clean — alembic round-trip verify gate (Task 1) chứng minh runtime an toàn |
| T-05-01-02 | Information Disclosure | audit_logs.payload JSONB log PII (email, token) | mitigate | audit_service chỉ nhận `AuditEntry` từ code app; CONVENTIONS §5 cấm log PII raw — caller (Plan 05-03/04/05) chịu trách nhiệm hash email/redact token trước khi đưa vào payload; KHÔNG đưa password_hash/plaintext key vào payload |
| T-05-01-03 | Denial of Service | audit_flush_loop queue unbounded → OOM | mitigate | `audit_queue_max_size=10000` cap; `enqueue_audit` QueueFull → drop + warning, KHÔNG block main thread; test_enqueue_non_blocking_when_queue_full verify |
| T-05-01-04 | Repudiation | Mất audit entry khi shutdown đột ngột | accept | flush_pending() drain queue lúc graceful shutdown; hard-kill mất ≤ batch chưa flush — chấp nhận cho M2 (không phải hệ thống tài chính) |
</threat_model>

<verification>
- `cd Hub_All/api && uv run alembic heads` → ĐÚNG 1 head sau migration 0003
- `cd Hub_All/api && uv run alembic upgrade head` trên DB trống → tạo schema + 0003 cột mới không lỗi
- `uv run alembic downgrade base` → drop clean; `uv run alembic upgrade head` lại → idempotent (BLOCKER 3 — đã thành per-task gate Task 1)
- `uv run ruff check app && uv run mypy --strict app` clean
- `uv run python -c "from app.main import create_app; create_app()"` không raise
- `uv run pytest tests/integration/test_audit_logger.py -q` PASS — AUX-01 SC4 concurrency phủ test (WARNING 2)
</verification>

<success_criteria>
- Migration 0003 thêm đủ cột cho hubs (code/subdomain/status), users (phone/department/avatar_url/status), api_keys (permissions/allowed_hub_ids/rate_limit)
- Migration round-trip upgrade→downgrade→upgrade PASS runtime trên DB sạch (per-task gate); chỉ 1 alembic head
- Models SQLAlchemy khớp migration; KHÔNG có field di sản Go (D-05); slug giữ làm legacy mirror của code (W1 documented)
- Settings CRUD discretion resolved — KHÔNG implement (BLOCKER 2): frontend zero `/api/settings` call → bảng `settings` để dành Phase 7 rag-config
- audit_service.py: enqueue_audit non-blocking + audit_flush_loop batch 2s/128
- Lifespan wire audit task; ruff + mypy strict clean
- AUX-01 SC4 — test_audit_logger.py phủ 100 concurrent → 100 row + request_id unique (WARNING 2)
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-01-SUMMARY.md`
</output>
</content>
