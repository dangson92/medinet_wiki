---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - api/app/schemas/users.py
  - api/app/services/user_service.py
  - api/app/routers/users.py
  - api/app/routers/profile.py
autonomous: true
requirements: [USER-01, USER-02, USER-03]
user_setup: []

must_haves:
  truths:
    - "Admin tạo user với role + hub_assignments qua POST /api/users → 201"
    - "GET /api/users list filter hub_id/role/status/search, cap per_page ≤ 100"
    - "User update tách 3 endpoint: PUT /:id (profile), PATCH /:id/role, PATCH /:id/status (D-07)"
    - "POST /api/users/:id/reset-password sinh token TTL 1h log ra console (defer email v4.0)"
    - "Profile self-scoped: GET/PUT /api/profile + POST /api/profile/password — luôn dùng user.id từ JWT"
    - "Viewer không thể update profile user khác — profile router KHÔNG nhận :id param"
  artifacts:
    - path: "api/app/routers/users.py"
      provides: "User CRUD router admin-only — list/get/create/update/role/status/reset-password"
      exports: ["router"]
    - path: "api/app/routers/profile.py"
      provides: "Profile self-scoped router — get/update/change-password"
      exports: ["router"]
    - path: "api/app/services/user_service.py"
      provides: "UserService — CRUD + hub_assignments join + reset-password token"
      exports: ["UserService"]
    - path: "api/app/schemas/users.py"
      provides: "Pydantic v2 schemas user + profile"
  key_links:
    - from: "api/app/routers/users.py"
      to: "UserService"
      via: "Depends(get_user_service)"
      pattern: "UserService"
    - from: "api/app/services/user_service.py"
      to: "user_hubs join table"
      via: "bulk INSERT hub_assignments"
      pattern: "user_hubs"
    - from: "api/app/routers/profile.py"
      to: "get_current_user (KHÔNG :id param)"
      via: "self-scoped — user.id từ JWT"
      pattern: "get_current_user"
---

<objective>
Giao User Management CRUD (USER-01) + reset-password (USER-02) + Profile self-scoped (USER-03). Endpoint contract lấy từ `frontend/src/services/api.ts` (D-07).

User endpoints (admin-only — USER-01):
- `GET /api/users` — list, filter `hub_id,role,status,search,page,per_page`
- `GET /api/users/:id` — get single
- `POST /api/users` — create với `{email,name,password,phone?,department?,hub_id,role}` → 201
- `PUT /api/users/:id` — update profile fields `{name?,phone?,department?}` (D-07 — tách khỏi role/status)
- `PATCH /api/users/:id/role` — body `{hub_id,role}` (D-07 — endpoint riêng)
- `PATCH /api/users/:id/status` — body `{status}` (D-07 — endpoint riêng)
- `POST /api/users/:id/reset-password` — USER-02, sinh token 1h, **log console** (defer email v4.0)

Profile endpoints (self-scoped — USER-03, KHÔNG nhận `:id` — D-07 dùng `/api/profile`):
- `GET /api/profile` — self
- `PUT /api/profile` — self, `{name?,phone?,department?}`
- `POST /api/profile/password` — self, `{old_password,new_password}`

Purpose: USER-01/02/03. Phụ thuộc Plan 05-01 (cột users phone/department/avatar_url/status) + Plan 05-02 (enqueue_audit).
Output: `schemas/users.py`, `services/user_service.py`, `routers/users.py`, `routers/profile.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- Router analog — documents.py (CRUD) + auth/router.py (self-scoped /me) -->
APIRouter(prefix="/api/users", tags=["users"]) / APIRouter(prefix="/api/profile", tags=["profile"])
admin-only: Depends(require_role("admin"))  # noqa: B008
self-scoped: user: User = Depends(get_current_user)  # noqa: B008 — KHÔNG require_role

<!-- Password — api/app/auth/password.py + app/auth/__init__.py -->
from app.auth import hash_password           # đã export
from app.auth.password import verify_password

<!-- reset-password token (USER-02 M2 log only) — PATTERNS.md -->
import secrets
token = secrets.token_urlsafe(32)
# lưu Redis SET reset:{token} = user_id ex=3600 (analog auth/service.py:223 redis.set(key,"1",ex=ttl))
logger.info("user_reset_password_token: user_id=%s token=%s (M2 log-only)", uid, token)
# Redis lấy qua app.state.redis — router inject Depends(get_redis) từ auth/dependencies.py

<!-- hub_assignments join — auth/service.py:78-82 -->
select(UserHub.hub_id).where(UserHub.user_id == user.id)
POST /api/users với hub_id → INSERT users row + INSERT user_hubs (user_id, hub_id, assigned_at NOW())

<!-- D-07 contract — api.ts -->
createUser body (api.ts:265): { email, name, password, phone?, department?, hub_id, role }
updateUser body (api.ts:269): { name?, phone?, department? }  → PUT /api/users/:id
changeUserRole (api.ts:273): { hub_id, role }  → PATCH /api/users/:id/role
changeUserStatus (api.ts:277): { status }  → PATCH /api/users/:id/status
UserWithRolesAPI = { user: UserAPI, roles: RoleAPI[] }   — roles từ user_hubs join
UserAPI fields (api.ts:373): id, email, name, phone?, department?, avatar_url?, status, failed_login_count, created_at, updated_at
RoleAPI: { user_id, hub_id, role }
getProfile/updateProfile/changePassword → /api/profile (D-07)

<!-- users table sau Plan 05-01 migration 0003: id, email, password_hash, full_name, role, phone,
     department, avatar_url, status, is_active, created_at, updated_at.
     LƯU Ý: api.ts UserAPI dùng "name" — map từ cột full_name. failed_login_count KHÔNG có cột →
     trả 0 hằng số (M2 chưa track; defer). role/hub_id ở users.role là role mặc định; roles[] = user_hubs join. -->

<!-- audit — Plan 05-01 output -->
from app.services.audit_service import enqueue_audit, AuditEntry  # action "user.create"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/users.py — Pydantic v2 schemas user + profile</name>
  <files>api/app/schemas/users.py</files>
  <read_first>
    - api/app/schemas/documents.py (analog Pydantic v2)
    - api/app/auth/schemas.py (UserPublic — EmailStr, role Literal pattern)
    - Hub_All/frontend/src/services/api.ts (UserAPI ~373, RoleAPI ~386, UserWithRolesAPI ~573, createUser/updateUser/changeUserRole/changeUserStatus body)
  </read_first>
  <action>
Tạo `api/app/schemas/users.py` (`from __future__ import annotations` đầu file):
- `UserRole = Literal["admin", "editor", "viewer"]`, `UserStatus = Literal["active", "disabled"]`.
- `class CreateUserRequest(BaseModel)`: `email: EmailStr`, `name: str = Field(min_length=1, max_length=200)`, `password: str = Field(min_length=8, max_length=128)`, `phone: str | None = None`, `department: str | None = None`, `hub_id: str`, `role: UserRole`.
- `class UpdateUserRequest(BaseModel)`: `name: str | None = Field(default=None, max_length=200)`, `phone: str | None = None`, `department: str | None = None`.
- `class ChangeUserRoleRequest(BaseModel)`: `hub_id: str`, `role: UserRole`.
- `class ChangeUserStatusRequest(BaseModel)`: `status: UserStatus`.
- `class UpdateProfileRequest(BaseModel)`: `name: str | None = Field(default=None, max_length=200)`, `phone: str | None = None`, `department: str | None = None`.
- `class ChangePasswordRequest(BaseModel)`: `old_password: str = Field(min_length=1)`, `new_password: str = Field(min_length=8, max_length=128)`.
- `class UserResponse(BaseModel)` (= UserAPI shape): `id: str`, `email: str`, `name: str`, `phone: str | None`, `department: str | None`, `avatar_url: str | None`, `status: UserStatus`, `failed_login_count: int = 0`, `created_at: datetime`, `updated_at: datetime | None`.
- `class RoleAssignment(BaseModel)`: `user_id: str`, `hub_id: str`, `role: str`.
- `class UserWithRolesResponse(BaseModel)`: `user: UserResponse`, `roles: list[RoleAssignment]`.
Import `datetime`, `Literal`, `BaseModel`, `EmailStr`, `Field`. (`pydantic[email]` đã có dep — auth/schemas.py dùng EmailStr.)
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/schemas/users.py && uv run mypy --strict app/schemas/users.py && uv run python -c "from app.schemas.users import CreateUserRequest, UserWithRolesResponse, ChangeUserRoleRequest, UpdateProfileRequest"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/schemas/users.py` có 10 class: CreateUserRequest, UpdateUserRequest, ChangeUserRoleRequest, ChangeUserStatusRequest, UpdateProfileRequest, ChangePasswordRequest, UserResponse, RoleAssignment, UserWithRolesResponse
    - `CreateUserRequest` field `email` dùng `EmailStr`; `password` có `min_length=8`
    - `UserWithRolesResponse` có field `user` và `roles` (khớp UserWithRolesAPI)
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>schemas/users.py đủ schema cho user CRUD + profile, khớp api.ts contract.</done>
</task>

<task type="auto">
  <name>Task 2: services/user_service.py — UserService CRUD + hub_assignments + reset token</name>
  <files>api/app/services/user_service.py</files>
  <read_first>
    - api/app/services/documents_service.py (analog CRUD raw SQL + WHERE-clause builder dòng 244-260)
    - api/app/auth/service.py (dòng 78-82 — UserHub query pattern)
    - api/app/services/audit_service.py (Plan 05-01 — enqueue_audit, AuditEntry)
    - api/app/schemas/users.py (Task 1 output)
    - api/app/auth/password.py (verify_password) + app/auth/__init__.py (hash_password export)
    - api/app/models/auth.py (User, UserHub cột)
  </read_first>
  <action>
Tạo `api/app/services/user_service.py`:
- `from __future__ import annotations`, import `logging`, `secrets`, `Any`, `UUID`, `uuid4`, `text`, `AsyncSession`, `IntegrityError`, schemas users, `enqueue_audit`/`AuditEntry`, `from app.auth import hash_password`, `from app.auth.password import verify_password`.
- `class UserConflictError(Exception)` (email trùng).
- `class UserService`: `__init__(self, db: AsyncSession) -> None: self.db = db`.
- `async def create(self, *, req: CreateUserRequest, created_by: UUID, request_id: str | None = None) -> UserWithRolesResponse`:
  - `user_id = uuid4()`. `password_hash = hash_password(req.password)`.
  - INSERT users: `text("INSERT INTO users (id, email, password_hash, full_name, role, phone, department, status, is_active, created_at, updated_at) VALUES (:id, :email, :hash, :name, :role, :phone, :dept, 'active', TRUE, NOW(), NOW())")`. Bắt `IntegrityError` (email unique) → `raise UserConflictError(...)`.
  - INSERT user_hubs: `text("INSERT INTO user_hubs (user_id, hub_id, assigned_at) VALUES (:uid, :hid, NOW())")` với `req.hub_id`.
  - `enqueue_audit(AuditEntry(action="user.create", user_id=str(created_by), target_type="user", target_id=str(user_id), hub_id=req.hub_id, payload={"email": req.email, "role": req.role}, request_id=request_id))`.
  - Return `_build_user_with_roles(user_id)` (helper SELECT user + user_hubs).
- `async def _build_user_with_roles(self, user_id: UUID) -> UserWithRolesResponse | None`: SELECT user row + SELECT user_hubs (user_id, hub_id) join `users.role` → build `UserResponse` (map `full_name`→`name`, `failed_login_count=0`) + `roles: list[RoleAssignment]` (mỗi user_hub → RoleAssignment với role = users.role). None nếu user không tồn tại.
- `async def get(self, user_id: UUID) -> UserWithRolesResponse | None`: gọi `_build_user_with_roles`.
- `async def list(self, *, hub_id: str | None, role: str | None, status: str | None, search: str | None, page: int, per_page: int) -> tuple[list[UserWithRolesResponse], int]`:
  - WHERE-clause builder (analog documents_service.py:244-260): `role = :role`, `status = :status`, `email ILIKE :search OR full_name ILIKE :search` cho search.
  - `hub_id` filter: cần JOIN user_hubs — `WHERE id IN (SELECT user_id FROM user_hubs WHERE hub_id = :hub_id)`.
  - COUNT + LIMIT/OFFSET ORDER BY created_at DESC. Với mỗi user row → `_build_user_with_roles`.
- `async def update(self, *, user_id: UUID, req: UpdateUserRequest) -> UserWithRolesResponse | None`: SET clause động cho `full_name` (từ req.name), `phone`, `department` không None; `UPDATE users SET ... updated_at=NOW() WHERE id=:id`; 0 row → None; else `_build_user_with_roles`.
- `async def change_role(self, *, user_id: UUID, hub_id: str, role: str) -> bool`: `UPDATE users SET role=:role, updated_at=NOW() WHERE id=:id` + upsert user_hubs (`INSERT ... ON CONFLICT (user_id, hub_id) DO NOTHING`); return rowcount>0 cho user UPDATE.
- `async def change_status(self, *, user_id: UUID, status: str) -> bool`: `UPDATE users SET status=:status, is_active=:active, updated_at=NOW() WHERE id=:id` (active = status=='active'); return rowcount>0.
- `async def reset_password(self, *, user_id: UUID, redis: Any) -> str | None`:
  - Verify user tồn tại (`SELECT 1 FROM users WHERE id=:id`); None → return None.
  - `token = secrets.token_urlsafe(32)`. Nếu `redis is not None`: `await redis.set(f"reset:{token}", str(user_id), ex=3600)`.
  - `logger.info("user_reset_password_token: user_id=%s token=%s (M2 log-only, email defer v4.0)", user_id, token)`.
  - Return `token`.
- `async def change_password_self(self, *, user_id: UUID, old_password: str, new_password: str) -> bool | None`:
  - SELECT `password_hash FROM users WHERE id=:id`; None → return None.
  - `if not verify_password(old_password, row_hash): return False`.
  - `UPDATE users SET password_hash=:hash, updated_at=NOW() WHERE id=:id` với `hash_password(new_password)`; return True.
- `async def update_profile(self, *, user_id: UUID, req: UpdateProfileRequest) -> UserWithRolesResponse | None`: như `update` nhưng dùng UpdateProfileRequest.
- Timestamp luôn `NOW()`; `logger = logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/user_service.py && uv run mypy --strict app/services/user_service.py && uv run python -c "from app.services.user_service import UserService, UserConflictError"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/services/user_service.py` export `UserService`, `UserConflictError`
    - `UserService` có method: create, get, list, update, change_role, change_status, reset_password, change_password_self, update_profile
    - `grep "user_hubs" api/app/services/user_service.py` có match (hub_assignments join + INSERT)
    - `grep "hash_password" api/app/services/user_service.py` có match (POST create + change password)
    - `reset_password` dùng `secrets.token_urlsafe` + Redis `ex=3600` + `logger.info` (KHÔNG gửi email)
    - `change_password_self` gọi `verify_password` cho old_password
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>UserService có CRUD + hub_assignments join + reset token log-only + self password change.</done>
</task>

<task type="auto">
  <name>Task 3: routers/users.py + routers/profile.py — User CRUD + Profile self-scoped</name>
  <files>api/app/routers/users.py, api/app/routers/profile.py</files>
  <read_first>
    - api/app/routers/documents.py (analog router — get_X_service, require_role, UUID validate)
    - api/app/auth/router.py (analog self-scoped /me — get_current_user, KHÔNG :id param)
    - api/app/auth/dependencies.py (get_current_user, require_role, get_redis)
    - api/app/services/user_service.py (Task 2 output)
    - api/app/schemas/users.py (Task 1 output)
    - Hub_All/frontend/src/services/api.ts (D-07 — getUsers/createUser/updateUser PUT, changeUserRole PATCH, changeUserStatus PATCH, getProfile/updateProfile/changePassword /api/profile)
  </read_first>
  <action>
**`api/app/routers/users.py`** — `router = APIRouter(prefix="/api/users", tags=["users"])`. `get_user_service(db = Depends(get_session)) -> UserService`. Mọi endpoint `require_role("admin")`:
- `@router.get("")` `list_users(hub_id: str | None = None, role: str | None = None, status: str | None = None, search: str | None = None, page: int = 1, per_page: int = 20, user = Depends(require_role("admin")), service = ...)`:
  - cap per_page/page. `items, total = await service.list(hub_id=hub_id, role=role, status=status, search=search, page=capped_page, per_page=capped_per_page)`. `resp.paginated(...)`.
- `@router.post("")` `create_user(req: CreateUserRequest, request: Request, user = Depends(require_role("admin")), service = ...)`:
  - `request_id = getattr(request.state, "request_id", None)`. `try: result = await service.create(req=req, created_by=user.id, request_id=request_id)` `except UserConflictError as e: return resp.conflict(message=str(e), code="EMAIL_CONFLICT")`. `resp.created(data=result.model_dump(mode="json"))`.
- `@router.get("/{user_id}")` `get_user(...)`: UUID validate → INVALID_USER_ID; `service.get`; None → not_found. `resp.ok`.
- `@router.put("/{user_id}")` `update_user(user_id, req: UpdateUserRequest, ...)`: UUID validate; `service.update`; None → not_found. `resp.ok`.
- `@router.patch("/{user_id}/role")` `change_role(user_id, req: ChangeUserRoleRequest, ...)`: UUID validate; `ok = await service.change_role(user_id=..., hub_id=req.hub_id, role=req.role)`; not ok → not_found. `resp.ok(data={"message": "Cập nhật vai trò thành công"})`.
- `@router.patch("/{user_id}/status")` `change_status(user_id, req: ChangeUserStatusRequest, ...)`: tương tự → `resp.ok(data={"message": "Cập nhật trạng thái thành công"})`.
- `@router.post("/{user_id}/reset-password")` `reset_password(user_id, redis = Depends(get_redis), user = Depends(require_role("admin")), service = ...)`:
  - UUID validate. `token = await service.reset_password(user_id=user_uuid, redis=redis)`; None → not_found. `resp.ok(data={"message": "Token reset đã sinh (xem console log — email defer v4.0)"})`. (KHÔNG trả token trong response body — token chỉ log console; tránh leak qua API.)
- `# noqa: B008` cho Depends defaults. Import `get_redis` từ `app.auth.dependencies`.

**`api/app/routers/profile.py`** — `router = APIRouter(prefix="/api/profile", tags=["profile"])`. `get_user_service` (copy). KHÔNG nhận `:id` — luôn `user: User = Depends(get_current_user)` (viewer/editor/admin đều dùng cho CHÍNH MÌNH):
- `@router.get("")` `get_profile(user = Depends(get_current_user), service = ...)`: `result = await service.get(user.id)`; `resp.ok(data=result.model_dump(mode="json"))`.
- `@router.put("")` `update_profile(req: UpdateProfileRequest, user = Depends(get_current_user), service = ...)`: `result = await service.update_profile(user_id=user.id, req=req)`; `resp.ok(...)`.
- `@router.post("/password")` `change_password(req: ChangePasswordRequest, user = Depends(get_current_user), service = ...)`:
  - `result = await service.change_password_self(user_id=user.id, old_password=req.old_password, new_password=req.new_password)`.
  - `result is None` → `resp.not_found(...)`; `result is False` → `resp.bad_request(message="Mật khẩu cũ không đúng", code="INVALID_PASSWORD")`; True → `resp.ok(data={"message": "Đổi mật khẩu thành công"})`.
- SC3 CONTEXT: profile router KHÔNG nhận `:id` → viewer KHÔNG THỂ update user khác — luôn `user.id` từ JWT.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/routers/users.py app/routers/profile.py && uv run mypy --strict app/routers/users.py app/routers/profile.py && uv run python -c "from app.routers.users import router as u; from app.routers.profile import router as p; up={(r.path, tuple(sorted(r.methods))) for r in u.routes}; pp={(r.path, tuple(sorted(r.methods))) for r in p.routes}; assert ('/api/users/{user_id}/role',('PATCH',)) in up; assert ('/api/users/{user_id}',('PUT',)) in up; assert ('/api/profile',('GET',)) in pp; assert not any('{' in r.path for r in p.routes), 'profile must not have path params'"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/routers/users.py` export `router` prefix `/api/users` với 7 endpoint (list/get/create/PUT update/PATCH role/PATCH status/reset-password)
    - User update = `@router.put`, role + status = `@router.patch` riêng (D-07 — 3 endpoint tách)
    - `grep -c "require_role(\"admin\")" api/app/routers/users.py` ≥ 7
    - `api/app/routers/profile.py` export `router` prefix `/api/profile` với 3 endpoint; KHÔNG route nào có path param `{...}` (self-scoped)
    - profile router dùng `get_current_user` KHÔNG `require_role` (viewer/editor/admin self-access)
    - reset-password KHÔNG trả token trong response body (token chỉ log console)
    - `ruff check` + `mypy --strict` exit 0; route assertion smoke test exit 0
  </acceptance_criteria>
  <done>routers/users.py 7 endpoint admin-only (3 update tách D-07); routers/profile.py 3 endpoint self-scoped KHÔNG :id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/users | password plaintext trong body — hash ngay, KHÔNG lưu/log plaintext |
| client → /api/profile | self-scoped — user_id LUÔN từ JWT, KHÔNG từ payload/path |
| reset token → console | token nhạy cảm — log console M2, KHÔNG trả qua API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Elevation of Privilege | viewer/editor gọi user CRUD admin-only | mitigate | mọi endpoint users.py `require_role("admin")` → 403 (USER-01 admin-only) |
| T-05-04-02 | Elevation of Privilege | viewer update profile của user KHÁC qua :id | mitigate | profile router KHÔNG nhận `:id` — luôn `user.id` từ JWT (SC3 CONTEXT); route assertion test KHÔNG path param |
| T-05-04-03 | Information Disclosure | password_hash leak trong UserResponse / audit payload | mitigate | UserResponse Pydantic KHÔNG có field password_hash; audit payload chỉ email+role (KHÔNG password) |
| T-05-04-04 | Information Disclosure | reset token leak qua API response cho phép account takeover | mitigate | reset-password endpoint trả message generic — token CHỈ log console (USER-02 M2); attacker không đọc được console |
| T-05-04-05 | Spoofing | Đổi mật khẩu mà không biết mật khẩu cũ | mitigate | change_password_self gọi `verify_password(old_password, hash)` trước — sai → 400 INVALID_PASSWORD |
| T-05-04-06 | Tampering | SQL injection qua search/email/name | mitigate | raw SQL chỉ `text()` + named bind params; search dùng `ILIKE :search` parametrized |
| T-05-04-07 | Spoofing | Tạo 2 user trùng email | mitigate | email unique constraint (Phase 2) + IntegrityError → 409 EMAIL_CONFLICT |
</threat_model>

<verification>
- `uv run ruff check app/routers/users.py app/routers/profile.py app/services/user_service.py app/schemas/users.py` clean
- `uv run mypy --strict` 4 file clean
- Route smoke test: users 7 endpoint, profile 3 endpoint KHÔNG path param
</verification>

<success_criteria>
- User CRUD 7 endpoint admin-only; update tách 3 endpoint PUT/PATCH-role/PATCH-status (D-07)
- reset-password sinh token log console (defer email v4.0); KHÔNG trả token qua API
- Profile 3 endpoint self-scoped, KHÔNG :id — viewer không update được user khác
- audit user.create enqueue; password luôn hash; old password verify trước khi đổi
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-04-SUMMARY.md`
</output>
