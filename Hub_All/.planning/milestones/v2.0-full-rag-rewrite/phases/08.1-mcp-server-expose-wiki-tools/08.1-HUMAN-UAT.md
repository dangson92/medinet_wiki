---
status: partial
phase: 08.1-mcp-server-expose-wiki-tools
source: [08.1-VERIFICATION.md]
started: "2026-05-19T12:50:00Z"
updated: "2026-05-19T12:50:00Z"
---

## Current Test

[awaiting human testing]

## Tests

### 1. End-to-End AI Client Connection (SC1)
expected: AI client (Claude Desktop / Cursor / Claude Code) cấu hình MCP server `http://localhost:8180/mcp` với header `X-API-Key` kết nối thành công, gọi được `list_hubs`, `search_wiki`, `ask_wiki` — trả kết quả đúng định dạng (HubList / SearchResult / AskAnswer kèm citations), không 404/500. API key sai/thiếu → tool báo lỗi `MCP_UNAUTHORIZED`, không chạy tool.
result: [pending]

### 2. Usage Events Logging end-to-end (SC5)
expected: Sau một lần gọi `ask_wiki` thành công (Postgres + LLM provider thật đang chạy), `SELECT COUNT(*) FROM usage_events` tăng thêm 1; row mới có `user_id` khớp user của API key đã dùng, `hub_id` đúng (null khi cross-hub).
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
