# Phase 8.1: MCP Server — Expose Wiki Tools - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-19
**Phase:** 08.1-mcp-server-expose-wiki-tools
**Areas discussed:** Transport & triển khai, Xác thực MCP client, Bộ công cụ expose, Hub scoping qua MCP

---

## Gray area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Transport & triển khai | stdio vs Streamable HTTP; tiến trình riêng vs mount vào FastAPI | |
| Xác thực MCP client | X-API-Key (reuse Phase 5) vs OAuth 2.1 | |
| Bộ công cụ expose | search/ask/cross-hub/find_similar; resources/prompts; citation | |
| Hub scoping qua MCP | client truyền hub_id vs derive từ API key user | |

**User's choice:** "Bạn tự đưa ra quyết định tốt nhất dựa vào nền tảng hiện tại và mục tiêu của app" — user uỷ quyền toàn bộ 4 vùng xám cho Claude quyết định.

**Notes:** Không có vòng hỏi-đáp tương tác. Claude chốt toàn bộ quyết định dựa trên codebase scout + PROJECT.md goals + ràng buộc R3 anti-pivot. Chi tiết D-01..D-14 trong CONTEXT.md.

---

## Claude's Discretion

User uỷ quyền 100% — toàn bộ quyết định kiến trúc/auth/tool surface/hub scoping do Claude chốt. Các điểm để ngỏ cho researcher/planner: mount path `/mcp`, tool description text, format MCP error, audit logging tuỳ chọn, rate limiting MCP tool.

## Deferred Ideas

- MCP resources & prompts — phase tương lai.
- Write tools qua MCP — out of scope.
- OAuth 2.1 — defer, revisit nếu client không hỗ trợ custom header.
- `find_similar` qua MCP — defer.
