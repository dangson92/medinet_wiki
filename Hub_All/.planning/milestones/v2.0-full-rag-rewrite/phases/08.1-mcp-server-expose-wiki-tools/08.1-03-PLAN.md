---
phase: "08.1"
plan: "03"
type: execute
wave: 3
depends_on:
  - "08.1-01"
  - "08.1-02"
files_modified:
  - Hub_All/api/tests/unit/mcp/__init__.py
  - Hub_All/api/tests/unit/mcp/test_mcp_auth.py
  - Hub_All/api/tests/unit/mcp/test_mcp_tools.py
  - Hub_All/api/tests/unit/mcp/test_mcp_mount.py
autonomous: true
requirements:
  - MCP-01
  - MCP-02

must_haves:
  truths:
    - "test_mcp_auth: thiếu X-API-Key → ToolError MCP_UNAUTHORIZED (T-08.1-01-S)"
    - "test_mcp_auth: API key đã thu hồi → ToolError MCP_UNAUTHORIZED (T-08.1-01-S2)"
    - "test_mcp_tools: list_hubs trả HubList đúng cho user thường (chỉ hub được assign)"
    - "test_mcp_tools: list_hubs trả tất cả active hub cho admin"
    - "test_mcp_tools: search_wiki với hub ngoài phạm vi → ToolError HUB_ACCESS_DENIED (D-13 / T-08.1-02-E)"
    - "test_mcp_tools: ask_wiki trả AskAnswer với answer text + citations list"
    - "test_mcp_mount: GET /mcp hoặc POST /mcp trả JSON-RPC response hợp lệ (không 404)"
    - "pytest api/tests/unit/mcp/ -x -q PASS — 0 lỗi"
  artifacts:
    - path: "Hub_All/api/tests/unit/mcp/__init__.py"
      provides: "package marker"
      min_lines: 0
    - path: "Hub_All/api/tests/unit/mcp/test_mcp_auth.py"
      provides: "auth fail test cases"
      contains: "MCP_UNAUTHORIZED"
    - path: "Hub_All/api/tests/unit/mcp/test_mcp_tools.py"
      provides: "tool behavior + hub isolation test"
      contains: "HUB_ACCESS_DENIED"
    - path: "Hub_All/api/tests/unit/mcp/test_mcp_mount.py"
      provides: "MCP mount smoke test"
      contains: "/mcp"
  key_links:
    - from: "Hub_All/api/tests/unit/mcp/test_mcp_auth.py"
      to: "Hub_All/api/app/mcp/auth.authenticate_mcp_request"
      via: "monkeypatch mock get_http_headers + ApiKeyService"
      pattern: "monkeypatch.*get_http_headers"
    - from: "Hub_All/api/tests/unit/mcp/test_mcp_tools.py"
      to: "Hub_All/api/app/mcp/server"
      via: "monkeypatch inject mock auth + mock service"
      pattern: "monkeypatch.*authenticate_mcp_request"
---

<objective>
Tạo test suite cho MCP module: auth fail scenarios, tool behavior, hub isolation,
và smoke test mount /mcp. Test là pure-Python unit test không cần Postgres/Redis
container — dùng monkeypatch mock service layer.

Purpose: Bằng chứng tự động rằng auth gate hoạt động (không để tool chạy khi
thiếu key), hub isolation được enforce (HUB_ACCESS_DENIED khi hub ngoài phạm vi),
và 3 tool hoạt động đúng contract D-07/D-08/D-11.

Output: api/tests/unit/mcp/ với 4 file test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-CONTEXT.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-01-SUMMARY.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-02-SUMMARY.md

<interfaces>
<!-- Contracts từ Plan 01/02 + Codebase hiện hữu cho test setup -->

Từ Hub_All/api/app/mcp/server.py (Plan 02):
```python
# mcp.tool functions:
async def list_hubs() -> HubList: ...
async def search_wiki(query: str, hub_id: str | None = None, top_k: int = 10) -> SearchResult: ...
async def ask_wiki(query: str, hub_id: str | None = None) -> AskAnswer: ...
# Module pool singleton:
def set_pool(pool: asyncpg.Pool, redis: Any = None) -> None: ...
```

Từ Hub_All/api/app/mcp/auth.py (Plan 01):
```python
async def authenticate_mcp_request(pool: asyncpg.Pool) -> UserWithHubs: ...
# Monkeypatch target: "app.mcp.auth.get_http_headers" hoặc "app.mcp.server.authenticate_mcp_request"
```

Từ Hub_All/api/app/mcp/schemas.py (Plan 01):
```python
class AskAnswer(BaseModel): answer: str, citations: list[CitationItem]
class SearchResult(BaseModel): results: list[SearchResultItem], total: int
class HubList(BaseModel): hubs: list[HubItem]
```

Từ Hub_All/api/app/auth/dependencies.py:
```python
class UserWithHubs:
    user: User    # user.id, user.role
    hub_ids: list[str]
```

Từ Hub_All/api/app/repositories/hub_isolation.py:
```python
class HubIsolationError(Exception): ...
```

Analog test file: Hub_All/api/tests/unit/test_ai_chat.py (pure unit test + monkeypatch pattern)
Analog test file: Hub_All/api/tests/unit/test_hub_isolation.py (hub isolation scenario)

RESEARCH.md Validation Architecture — test pattern:
```python
# Gọi tool qua raw HTTP POST JSON-RPC (KHÔNG cần MCP client SDK):
async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
    resp = await client.post("/mcp", json={
        "jsonrpc": "2.0", "id": 1, "method": "initialize",
        "params": {"protocolVersion": "2024-11-05", "capabilities": {},
                   "clientInfo": {"name": "test", "version": "0"}}
    })
    resp = await client.post("/mcp", json={
        "jsonrpc": "2.0", "id": 2, "method": "tools/call",
        "params": {"name": "list_hubs", "arguments": {}}
    })
    data = resp.json()
    assert data["result"]["isError"] is True  # auth fail
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo test_mcp_auth.py — auth fail scenarios (thiếu key, key thu hồi)</name>
  <files>Hub_All/api/tests/unit/mcp/__init__.py, Hub_All/api/tests/unit/mcp/test_mcp_auth.py</files>
  <read_first>
    - Hub_All/api/tests/unit/test_ai_chat.py — đọc cấu trúc tổng thể: docstring format, import pattern, monkeypatch mock cách dùng, pytest.mark.asyncio
    - Hub_All/api/tests/unit/conftest.py — xem fixture có sẵn (nếu có app fixture)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md — section test_mcp_*.py (analog test_ai_chat.py + mock pool fixture)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md — Validation Architecture (test pattern JSON-RPC, wave 0 test map)
    - Hub_All/api/app/mcp/auth.py (Plan 01) — xem import path get_http_headers để biết monkeypatch target đúng
  </read_first>
  <behavior>
    - __init__.py: rỗng
    - test_mcp_auth.py test 1: monkeypatch "app.mcp.auth.get_http_headers" trả {} (không có x-api-key) → gọi tool (list_hubs hoặc search_wiki) → assert raise ToolError với message chứa "MCP_UNAUTHORIZED"
    - test_mcp_auth.py test 2: monkeypatch get_http_headers trả {"x-api-key": "invalid"} + monkeypatch ApiKeyService.verify_key trả None → assert raise ToolError "MCP_UNAUTHORIZED"
    - test_mcp_auth.py test 3 (optional): monkeypatch verify_key trả principal nhưng db query created_by = None → assert raise ToolError "MCP_UNAUTHORIZED"
    - Tất cả test dùng monkeypatch để mock pool singleton (set_pool với MagicMock) + mock auth path — KHÔNG cần Postgres
    - Nếu ToolError và ValueError cùng có thể raise từ tool call trung gian, dùng pytest.raises((ToolError, ValueError))
  </behavior>
  <action>
Tạo Hub_All/api/tests/unit/mcp/__init__.py (rỗng).

Tạo Hub_All/api/tests/unit/mcp/test_mcp_auth.py:

```python
"""Unit test MCP auth — Phase 8.1 Task 1.

Pure-Python logic test — KHÔNG cần Postgres/Redis. Phủ auth gate qua monkeypatch mock:
- Thiếu X-API-Key header → ToolError MCP_UNAUTHORIZED.
- API key không hợp lệ / đã thu hồi → ToolError MCP_UNAUTHORIZED.

Threat coverage:
- T-08.1-01-S  — Spoofing: thiếu header → gate reject.
- T-08.1-01-S2 — Spoofing: key đã thu hồi → gate reject.
- D-06 (CONTEXT.md) — key sai → KHÔNG để tool chạy.
"""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock

import pytest

# Xác nhận ToolError import path sau khi install mcp==1.9.4 (RESEARCH.md Open Question 1)
try:
    from fastmcp.exceptions import ToolError
except ImportError:
    from mcp.fastmcp.exceptions import ToolError  # type: ignore[no-redef]

from app.mcp import server as mcp_server_module


@pytest.fixture(autouse=True)
def _inject_mock_pool(monkeypatch: pytest.MonkeyPatch) -> None:
    """Inject mock pool vào singleton trước mỗi test — KHÔNG cần Postgres."""
    mock_pool = MagicMock()
    mock_pool.acquire = MagicMock(return_value=AsyncMock())
    mcp_server_module.set_pool(mock_pool)


@pytest.mark.asyncio
async def test_missing_api_key_raises_tool_error(monkeypatch: pytest.MonkeyPatch) -> None:
    """Thiếu X-API-Key header → authenticate_mcp_request raise ValueError → list_hubs raise ToolError.

    D-06: key sai/thiếu → MCP error ngay transport level, KHÔNG để tool chạy.
    """
    # Monkeypatch get_http_headers trong app.mcp.auth để trả header rỗng
    monkeypatch.setattr(
        "app.mcp.auth.get_http_headers",
        lambda include=None: {},  # không có x-api-key
    )
    with pytest.raises((ToolError, ValueError)) as exc_info:
        await mcp_server_module.list_hubs()
    assert "MCP_UNAUTHORIZED" in str(exc_info.value)


@pytest.mark.asyncio
async def test_invalid_api_key_raises_tool_error(monkeypatch: pytest.MonkeyPatch) -> None:
    """API key không hợp lệ / đã thu hồi → verify_key trả None → ToolError.

    T-08.1-01-S2: ApiKeyService.verify_key check is_active=TRUE trong DB.
    """
    # Header có key nhưng không hợp lệ
    monkeypatch.setattr(
        "app.mcp.auth.get_http_headers",
        lambda include=None: {"x-api-key": "invalid-key-revoked"},
    )
    # Mock ApiKeyService.verify_key trả None (key thu hồi hoặc không tồn tại)
    mock_svc = MagicMock()
    mock_svc.verify_key = AsyncMock(return_value=None)
    monkeypatch.setattr(
        "app.mcp.auth.ApiKeyService",
        lambda db: mock_svc,  # type: ignore[misc]
    )
    # Mock get_engine + AsyncSession để tránh DB call
    mock_engine = MagicMock()
    monkeypatch.setattr("app.mcp.auth.get_engine", lambda: mock_engine)

    mock_session = AsyncMock()
    mock_session.__aenter__ = AsyncMock(return_value=mock_session)
    mock_session.__aexit__ = AsyncMock(return_value=False)
    mock_session.execute = AsyncMock()
    mock_session.commit = AsyncMock()

    from unittest.mock import patch
    with patch("app.mcp.auth.AsyncSession", return_value=mock_session):
        with pytest.raises((ToolError, ValueError)) as exc_info:
            await mcp_server_module.list_hubs()
    assert "MCP_UNAUTHORIZED" in str(exc_info.value)


@pytest.mark.asyncio
async def test_search_wiki_auth_gate(monkeypatch: pytest.MonkeyPatch) -> None:
    """search_wiki thiếu key → ToolError MCP_UNAUTHORIZED (D-06).

    Xác nhận auth gate áp dụng cho TẤT CẢ tool, không chỉ list_hubs.
    """
    monkeypatch.setattr(
        "app.mcp.auth.get_http_headers",
        lambda include=None: {},
    )
    with pytest.raises((ToolError, ValueError)) as exc_info:
        await mcp_server_module.search_wiki(query="test query")
    assert "MCP_UNAUTHORIZED" in str(exc_info.value)
```
  </action>
  <acceptance_criteria>
    - pytest api/tests/unit/mcp/test_mcp_auth.py -v → 3 test PASS
    - grep "MCP_UNAUTHORIZED" Hub_All/api/tests/unit/mcp/test_mcp_auth.py → xuất hiện trong assert
    - grep "monkeypatch" Hub_All/api/tests/unit/mcp/test_mcp_auth.py → xuất hiện (không Postgres)
  </acceptance_criteria>
  <verify>
    <automated>cd Hub_All/api && COCOINDEX_SKIP_SETUP=1 pytest tests/unit/mcp/test_mcp_auth.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/unit/mcp/__init__.py tồn tại
    - test_mcp_auth.py: 3 test PASS (missing key, invalid key, search_wiki gate)
    - Tất cả test pure-Python — không import testcontainers, không cần Postgres
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo test_mcp_tools.py + test_mcp_mount.py — tool behavior + hub isolation + mount smoke</name>
  <files>Hub_All/api/tests/unit/mcp/test_mcp_tools.py, Hub_All/api/tests/unit/mcp/test_mcp_mount.py</files>
  <read_first>
    - Hub_All/api/tests/unit/test_hub_isolation.py — đọc cách mock HubIsolationError + assertion pattern hub isolation
    - Hub_All/api/tests/unit/test_ai_chat.py — đọc cách mock service async response
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md — Validation Architecture section (JSON-RPC test pattern cho test_mcp_mount)
    - Hub_All/api/app/mcp/server.py (Plan 02) — xem chính xác tên module path để monkeypatch authenticate_mcp_request
    - Hub_All/api/app/mcp/schemas.py (Plan 01) — xem field names SearchResultItem, AskAnswer, HubList để assert
    - Hub_All/api/tests/unit/conftest.py — xem fixture app nếu có; nếu không có dùng create_app()
  </read_first>
  <behavior>
    TEST_MCP_TOOLS (test_mcp_tools.py):
    - test_list_hubs_user: mock authenticate_mcp_request trả UserWithHubs(user có role="viewer", hub_ids=["hub-1"]); mock pool.acquire().fetch trả [{"id": "hub-1", "name": "Hub Y Tế", "description": None}]; assert HubList.hubs có 1 phần tử với id="hub-1"
    - test_list_hubs_admin: mock user role="admin"; mock pool.acquire().fetch trả 2 hub rows; assert HubList.hubs có 2 phần tử
    - test_search_wiki_hub_isolation: mock authenticate; mock SearchService.search để raise HubIsolationError("hub-b ngoài phạm vi"); assert ToolError với "HUB_ACCESS_DENIED" (D-13)
    - test_ask_wiki_returns_answer: mock authenticate; mock AskService.ask trả AskOutcome giả với answer="Câu trả lời [1]" + 1 citation; assert AskAnswer.answer == "Câu trả lời [1]" + len(citations) == 1
    - test_ask_wiki_usage_logged: mock authenticate + AskService.ask; mock asyncio.create_task; assert create_task được gọi (D-14 non-blocking log)

    TEST_MCP_MOUNT (test_mcp_mount.py):
    - test_mcp_endpoint_reachable: dùng ASGI transport, POST /mcp với JSON-RPC initialize → assert status 200 hoặc phản hồi JSON-RPC hợp lệ (không 404 không 500)
    - test_mcp_tools_list: POST /mcp với method tools/list → assert response có result.tools chứa 3 tool name

    QUAN TRỌNG cho test_mcp_mount: COCOINDEX_SKIP_SETUP=1 và mock pool trước khi create_app() — test không cần Postgres/Redis thật.
  </behavior>
  <action>
Tạo Hub_All/api/tests/unit/mcp/test_mcp_tools.py:

```python
"""Unit test MCP tool behavior + hub isolation — Phase 8.1 Task 2.

Pure-Python: mock authenticate_mcp_request + service layer, KHÔNG cần Postgres/Redis.

Threat coverage:
- T-08.1-02-E  — Elevation: hub ngoài phạm vi → HUB_ACCESS_DENIED.
- D-13 (CONTEXT.md) — hub_id filter, KHÔNG escalation.
- D-14 (CONTEXT.md) — ask_wiki log usage_events non-blocking.
"""
from __future__ import annotations

import asyncio
from dataclasses import dataclass
from unittest.mock import AsyncMock, MagicMock, patch

import pytest

try:
    from fastmcp.exceptions import ToolError
except ImportError:
    from mcp.fastmcp.exceptions import ToolError  # type: ignore[no-redef]

from app.auth.dependencies import UserWithHubs
from app.mcp import server as mcp_server_module
from app.mcp.schemas import AskAnswer, HubList, SearchResult
from app.repositories.hub_isolation import HubIsolationError


def _make_user(role: str = "viewer", hub_ids: list[str] | None = None) -> UserWithHubs:
    """Helper tạo UserWithHubs giả cho test."""
    user = MagicMock()
    user.id = "user-uuid-test"
    user.role = role
    return UserWithHubs(user=user, hub_ids=hub_ids or ["hub-1"])


@pytest.fixture(autouse=True)
def _inject_mock_pool(monkeypatch: pytest.MonkeyPatch) -> None:
    mock_pool = MagicMock()
    mock_conn = AsyncMock()
    mock_conn.__aenter__ = AsyncMock(return_value=mock_conn)
    mock_conn.__aexit__ = AsyncMock(return_value=False)
    mock_pool.acquire = MagicMock(return_value=mock_conn)
    mock_conn.fetch = AsyncMock(return_value=[])
    mcp_server_module.set_pool(mock_pool)


@pytest.mark.asyncio
async def test_list_hubs_viewer_returns_assigned_hubs(monkeypatch: pytest.MonkeyPatch) -> None:
    """list_hubs viewer: trả chỉ hub được assign (query user_hubs join)."""
    user = _make_user(role="viewer", hub_ids=["hub-1"])
    monkeypatch.setattr(
        "app.mcp.server.authenticate_mcp_request",
        AsyncMock(return_value=user),
    )
    # Mock pool.acquire().fetch cho non-admin query
    mock_rows = [{"id": "hub-1", "name": "Hub Y Tế", "description": "Tổng hợp"}]
    pool = mcp_server_module._get_pool()
    pool.acquire().__aenter__.return_value.fetch = AsyncMock(return_value=mock_rows)

    result = await mcp_server_module.list_hubs()

    assert isinstance(result, HubList)
    assert len(result.hubs) == 1
    assert result.hubs[0].id == "hub-1"
    assert result.hubs[0].name == "Hub Y Tế"


@pytest.mark.asyncio
async def test_list_hubs_admin_returns_all_active(monkeypatch: pytest.MonkeyPatch) -> None:
    """list_hubs admin: trả TẤT CẢ hub active (query hubs WHERE status='active')."""
    user = _make_user(role="admin", hub_ids=[])
    monkeypatch.setattr(
        "app.mcp.server.authenticate_mcp_request",
        AsyncMock(return_value=user),
    )
    mock_rows = [
        {"id": "hub-1", "name": "Hub A", "description": None},
        {"id": "hub-2", "name": "Hub B", "description": "Mô tả"},
    ]
    pool = mcp_server_module._get_pool()
    pool.acquire().__aenter__.return_value.fetch = AsyncMock(return_value=mock_rows)

    result = await mcp_server_module.list_hubs()

    assert isinstance(result, HubList)
    assert len(result.hubs) == 2


@pytest.mark.asyncio
async def test_search_wiki_hub_isolation_denied(monkeypatch: pytest.MonkeyPatch) -> None:
    """D-13: hub_id ngoài phạm vi user → HubIsolationError → ToolError HUB_ACCESS_DENIED.

    T-08.1-02-E: Elevation of Privilege qua param hub_id.
    """
    user = _make_user(role="viewer", hub_ids=["hub-a"])
    monkeypatch.setattr(
        "app.mcp.server.authenticate_mcp_request",
        AsyncMock(return_value=user),
    )
    mock_svc = MagicMock()
    mock_svc.search = AsyncMock(side_effect=HubIsolationError("hub-b ngoài phạm vi"))
    with patch("app.mcp.server.SearchService", return_value=mock_svc):
        with pytest.raises(ToolError) as exc_info:
            await mcp_server_module.search_wiki(query="test", hub_id="hub-b")
    assert "HUB_ACCESS_DENIED" in str(exc_info.value)


@pytest.mark.asyncio
async def test_ask_wiki_returns_answer_with_citations(monkeypatch: pytest.MonkeyPatch) -> None:
    """ask_wiki trả AskAnswer với answer text + citations list (D-11)."""
    user = _make_user(role="viewer", hub_ids=["hub-1"])
    monkeypatch.setattr(
        "app.mcp.server.authenticate_mcp_request",
        AsyncMock(return_value=user),
    )

    # Dựng mock AskOutcome
    mock_citation = MagicMock()
    mock_citation.chunk_id = "chunk-uuid-1"
    mock_citation.document_name = "Quy trình khám bệnh.docx"
    mock_citation.hub_name = "Hub Y Tế"
    mock_citation.content_snippet = "Bước 1: đăng ký..."
    mock_citation.score = 0.87

    mock_response = MagicMock()
    mock_response.answer = "Quy trình gồm 3 bước [1]."
    mock_response.citations = [mock_citation]

    mock_usage = MagicMock()
    mock_usage.model = "gpt-4o"
    mock_usage.prompt_tokens = 100
    mock_usage.completion_tokens = 50
    mock_usage.total_tokens = 150
    mock_usage.cost_usd = 0.001

    mock_outcome = MagicMock()
    mock_outcome.response = mock_response
    mock_outcome.usage = mock_usage

    mock_svc = MagicMock()
    mock_svc.ask = AsyncMock(return_value=mock_outcome)

    with patch("app.mcp.server.AskService", return_value=mock_svc):
        with patch("asyncio.create_task"):  # mock non-blocking usage log
            result = await mcp_server_module.ask_wiki(query="Quy trình khám bệnh?", hub_id="hub-1")

    assert isinstance(result, AskAnswer)
    assert result.answer == "Quy trình gồm 3 bước [1]."
    assert len(result.citations) == 1
    assert result.citations[0].chunk_id == "chunk-uuid-1"
    assert result.citations[0].score == pytest.approx(0.87)


@pytest.mark.asyncio
async def test_ask_wiki_logs_usage_nonblocking(monkeypatch: pytest.MonkeyPatch) -> None:
    """D-14: ask_wiki gọi asyncio.create_task(log_usage_event(...)) — non-blocking.

    Xác nhận create_task được gọi 1 lần sau khi AskService.ask() trả kết quả.
    """
    user = _make_user(role="viewer", hub_ids=["hub-1"])
    monkeypatch.setattr(
        "app.mcp.server.authenticate_mcp_request",
        AsyncMock(return_value=user),
    )

    mock_citation = MagicMock()
    mock_citation.chunk_id = "c1"; mock_citation.document_name = "doc"
    mock_citation.hub_name = "hub"; mock_citation.content_snippet = "..."
    mock_citation.score = 0.9

    mock_response = MagicMock()
    mock_response.answer = "Trả lời [1]."; mock_response.citations = [mock_citation]
    mock_usage = MagicMock()
    mock_usage.model = "gpt-4o"; mock_usage.prompt_tokens = 10
    mock_usage.completion_tokens = 5; mock_usage.total_tokens = 15; mock_usage.cost_usd = 0.0001

    mock_outcome = MagicMock()
    mock_outcome.response = mock_response; mock_outcome.usage = mock_usage

    mock_svc = MagicMock()
    mock_svc.ask = AsyncMock(return_value=mock_outcome)

    create_task_calls: list[object] = []
    original_create_task = asyncio.create_task

    def mock_create_task(coro: object, **kwargs: object) -> MagicMock:
        create_task_calls.append(coro)
        # Huỷ coroutine để tránh RuntimeWarning
        if hasattr(coro, "close"):
            coro.close()  # type: ignore[union-attr]
        return MagicMock()

    with patch("app.mcp.server.AskService", return_value=mock_svc):
        with patch("app.mcp.server.asyncio") as mock_asyncio:
            mock_asyncio.create_task = mock_create_task
            await mcp_server_module.ask_wiki(query="test", hub_id="hub-1")

    assert len(create_task_calls) == 1, "asyncio.create_task phải được gọi 1 lần cho D-14"
```

Tạo Hub_All/api/tests/unit/mcp/test_mcp_mount.py:

```python
"""Unit test MCP mount smoke — Phase 8.1 Task 2.

Xác nhận /mcp endpoint reachable + trả JSON-RPC hợp lệ cho tools/list.
Dùng ASGI transport — KHÔNG cần Postgres/Redis thật.

COCOINDEX_SKIP_SETUP=1 bắt buộc khi create_app() (cocoindex singleton).
"""
from __future__ import annotations

import os

import pytest
from httpx import ASGITransport, AsyncClient
from unittest.mock import AsyncMock, MagicMock

from app.mcp import server as mcp_server_module


@pytest.fixture
def app_with_mcp(monkeypatch: pytest.MonkeyPatch):  # type: ignore[return]
    """FastAPI app với MCP mount — mock pool, skip cocoindex."""
    os.environ.setdefault("COCOINDEX_SKIP_SETUP", "1")

    # Inject mock pool TRƯỚC create_app để lifespan không panic
    mock_pool = MagicMock()
    mock_pool.acquire = MagicMock(return_value=AsyncMock())
    mcp_server_module.set_pool(mock_pool)

    from app.main import create_app
    return create_app()


@pytest.mark.asyncio
async def test_mcp_endpoint_reachable(app_with_mcp) -> None:  # type: ignore[no-untyped-def]
    """GET /mcp hoặc POST /mcp initialize không trả 404 (MCP mount thành công).

    Smoke test MCP-01: server mount tại /mcp, transport Streamable HTTP active.
    """
    async with AsyncClient(
        transport=ASGITransport(app=app_with_mcp), base_url="http://test"
    ) as client:
        resp = await client.post(
            "/mcp",
            json={
                "jsonrpc": "2.0",
                "id": 1,
                "method": "initialize",
                "params": {
                    "protocolVersion": "2024-11-05",
                    "capabilities": {},
                    "clientInfo": {"name": "test-client", "version": "0.0.1"},
                },
            },
            headers={"Content-Type": "application/json"},
        )
    # MCP server phải phản hồi (200 OK, hoặc 4xx nếu initialize có yêu cầu khác)
    # Quan trọng: KHÔNG được 404 (mount thất bại) hoặc 500 (Task group not initialized)
    assert resp.status_code != 404, "/mcp KHÔNG mount — kiểm tra combine_lifespans"
    assert resp.status_code != 500, f"/mcp lỗi 500: {resp.text[:200]}"


@pytest.mark.asyncio
async def test_mcp_tools_list_has_three_tools(app_with_mcp) -> None:  # type: ignore[no-untyped-def]
    """tools/list trả đủ 3 tool: list_hubs, search_wiki, ask_wiki (MCP-02).

    Verify tool registration thành công sau mount.
    """
    async with AsyncClient(
        transport=ASGITransport(app=app_with_mcp), base_url="http://test"
    ) as client:
        # Initialize trước (stateless mode vẫn cần initialize cho tools/list)
        await client.post(
            "/mcp",
            json={
                "jsonrpc": "2.0", "id": 1, "method": "initialize",
                "params": {
                    "protocolVersion": "2024-11-05",
                    "capabilities": {},
                    "clientInfo": {"name": "test", "version": "0"},
                },
            },
            headers={"Content-Type": "application/json"},
        )
        resp = await client.post(
            "/mcp",
            json={
                "jsonrpc": "2.0", "id": 2, "method": "tools/list", "params": {},
            },
            headers={"Content-Type": "application/json"},
        )

    if resp.status_code == 200:
        data = resp.json()
        if "result" in data and "tools" in data["result"]:
            tool_names = {t["name"] for t in data["result"]["tools"]}
            assert "list_hubs" in tool_names, f"list_hubs missing: {tool_names}"
            assert "search_wiki" in tool_names, f"search_wiki missing: {tool_names}"
            assert "ask_wiki" in tool_names, f"ask_wiki missing: {tool_names}"
    else:
        pytest.skip(f"tools/list trả {resp.status_code} — kiểm tra MCP protocol version")
```
  </action>
  <acceptance_criteria>
    - pytest api/tests/unit/mcp/test_mcp_tools.py -v → 5 test PASS (list_hubs viewer/admin, hub isolation, ask_wiki answer, ask_wiki usage log)
    - pytest api/tests/unit/mcp/test_mcp_mount.py -v → 2 test PASS hoặc SKIP (không FAIL)
    - grep "HUB_ACCESS_DENIED" Hub_All/api/tests/unit/mcp/test_mcp_tools.py → xuất hiện trong assert
    - grep "create_task" Hub_All/api/tests/unit/mcp/test_mcp_tools.py → xuất hiện (D-14 verify)
    - Không có import testcontainers, không có @pytest.mark.integration (pure unit test)
  </acceptance_criteria>
  <verify>
    <automated>cd Hub_All/api && COCOINDEX_SKIP_SETUP=1 pytest tests/unit/mcp/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_mcp_tools.py: 5 test PASS
    - test_mcp_mount.py: 2 test PASS hoặc SKIP (không FAIL với "Task group not initialized")
    - pytest api/tests/unit/mcp/ -x -q → 0 failed (PASS hoặc SKIP accepted)
    - Tất cả test pure-Python — không cần DB container
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Ranh giới | Mô tả |
|-----------|-------|
| Test scope | Test không có trust boundary thật — dùng mock để isolate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-03-S | Spoofing | test_missing_api_key_raises_tool_error | mitigate | Test verify auth gate đầu tiên — thiếu header phải reject, không cho tool run |
| T-08.1-03-S2 | Spoofing | test_invalid_api_key_raises_tool_error | mitigate | Test verify revoked key → reject; mock verify_key=None simulate DB check |
| T-08.1-03-E | Elevation | test_search_wiki_hub_isolation_denied | mitigate | Test verify HUB_ACCESS_DENIED khi hub ngoài phạm vi → D-13 enforce |
| T-08.1-03-D | Denial of Service | test_ask_wiki_logs_usage_nonblocking | accept | Test verify create_task gọi 1 lần — đảm bảo non-blocking (KHÔNG block tool response) |
</threat_model>

<verification>
Sau khi hoàn thành Plan 03 (toàn bộ Phase 8.1):

```bash
cd Hub_All/api

# 1. Chạy toàn bộ test suite MCP
COCOINDEX_SKIP_SETUP=1 pytest tests/unit/mcp/ -v --tb=short

# 2. Regression — toàn bộ unit test không regress
COCOINDEX_SKIP_SETUP=1 pytest tests/unit/ -q --tb=short 2>&1 | tail -5

# 3. Ruff lint MCP module + test
uv run ruff check app/mcp/ tests/unit/mcp/

# 4. Xác nhận 3 tool đăng ký
python -c "
from app.mcp.server import mcp
import asyncio
async def check():
    tools = await mcp.get_tools()
    print('Tools:', list(tools.keys()))
asyncio.run(check())
"

# 5. Mount verify
COCOINDEX_SKIP_SETUP=1 python -c "
from app.main import create_app
app = create_app()
routes = [r.path for r in app.routes]
print('Routes:', [r for r in routes if 'mcp' in r.lower()])
"
```

Kết quả mong đợi:
- tests/unit/mcp/ — 0 FAILED (PASS hoặc SKIP)
- tests/unit/ — tất cả test cũ PASS (không regress)
- ruff — 0 lỗi
- tools: ['list_hubs', 'search_wiki', 'ask_wiki']
- routes: ['/mcp']
</verification>

<success_criteria>
1. 4 file test tồn tại: tests/unit/mcp/__init__.py + test_mcp_auth.py + test_mcp_tools.py + test_mcp_mount.py
2. COCOINDEX_SKIP_SETUP=1 pytest tests/unit/mcp/ -x -q → 0 FAILED
3. test_mcp_auth.py phủ: thiếu key → MCP_UNAUTHORIZED; key thu hồi → MCP_UNAUTHORIZED; search_wiki gate
4. test_mcp_tools.py phủ: list_hubs viewer/admin, hub isolation HUB_ACCESS_DENIED, ask_wiki answer + citations, ask_wiki asyncio.create_task D-14
5. test_mcp_mount.py phủ: /mcp reachable (không 404/500), tools/list có 3 tool
6. Regression: COCOINDEX_SKIP_SETUP=1 pytest tests/unit/ -q → tất cả test cũ PASS
</success_criteria>

<output>
Sau khi hoàn thành Plan 03, tạo file:
`.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-03-SUMMARY.md`

Ghi rõ:
- Số test PASS/SKIP/FAIL
- Bất kỳ test nào SKIP và lý do (thường là protocol version mismatch cho mount test)
- Regression count (số test unit/ tổng)
- Hướng dẫn kết nối Claude Code: .mcp.json config với url + X-API-Key header
</output>
