---
plan: 01
phase: 4
wave: 1
type: execute
depends_on: []
files_modified:
  - Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py
  - Hub_All/api/app/rag/__init__.py
  - Hub_All/api/app/config.py
  - Hub_All/api/.env.example
  - Hub_All/api/tests/integration/test_phase4_migration.py
autonomous: true
requirements: [INGEST-05, INGEST-06, INGEST-08]
must_haves:
  truths:
    - "alembic upgrade head trên DB trống chạy thành công chuỗi revision tới 0002 (exit 0); schema documents có composite index ix_documents_status_last_heartbeat phục vụ watchdog query Plan 04-05."
    - "Cột documents.last_heartbeat, documents.attempts, documents.error_message tồn tại đúng kiểu (Phase 2 migration 0001 đã baseline — Plan 04-01 chỉ thêm index còn thiếu, KHÔNG add_column lại)."
    - "Settings có field cocoindex_lmdb_path (Path, default ./.cocoindex/state.lmdb) + watchdog_timeout_seconds (int, default 300) — env COCOINDEX_DB và WATCHDOG_TIMEOUT_SECONDS configurable."
    - "Package app.rag tồn tại (app/rag/__init__.py) với docstring mô tả kiến trúc cocoindex 1.0.3 actual API — KHÔNG còn reference deprecated 0.x (flow_def decorator, cocoindex.init, FlowLiveUpdater)."
    - "pytest tests/integration/test_phase4_migration.py PASS — testcontainers Postgres pgvector pg16 chạy alembic upgrade head, assert index ix_documents_status_last_heartbeat + 3 cột watchdog tồn tại."
  artifacts:
    - path: Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py
      provides: "Alembic revision 0002 — composite index ix_documents_status_last_heartbeat (status, last_heartbeat) cho watchdog query nhanh."
      contains: "revision = \"0002\""
    - path: Hub_All/api/app/rag/__init__.py
      provides: "Package init app.rag — docstring mô tả cocoindex 1.0.3 App + lifespan + mount pattern; re-export setup_cocoindex sau khi Plan 04-03 ship setup.py."
      contains: "RAG package"
    - path: Hub_All/api/app/config.py
      provides: "Settings.cocoindex_lmdb_path + Settings.watchdog_timeout_seconds fields."
      contains: "cocoindex_lmdb_path"
    - path: Hub_All/api/.env.example
      provides: "Documented COCOINDEX_DB (LMDB path) + WATCHDOG_TIMEOUT_SECONDS env keys."
      contains: "COCOINDEX_DB"
    - path: Hub_All/api/tests/integration/test_phase4_migration.py
      provides: "Integration test alembic upgrade head qua testcontainers — verify index + cột documents."
      contains: "def test_phase4_migration"
  key_links:
    - from: Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py
      to: Hub_All/api/migrations/versions/0001_initial_schema.py
      via: "down_revision trỏ tới revision của migration 0001"
      pattern: "down_revision"
    - from: Hub_All/api/app/config.py
      to: Hub_All/api/.env.example
      via: "field cocoindex_lmdb_path đọc env COCOINDEX_DB; watchdog_timeout_seconds đọc WATCHDOG_TIMEOUT_SECONDS"
      pattern: "cocoindex_lmdb_path|watchdog_timeout_seconds"
---

# Plan 04-01: Migration index watchdog + cocoindex scaffolding hạ tầng

<objective>
Chuẩn bị nền hạ tầng cho toàn bộ Phase 4 trước khi viết flow cocoindex thực sự (Plan 04-03). Plan này [BLOCKING — Wave 1] tách riêng vì hai lý do:

1. **Schema delta cho watchdog (INGEST-06):** Phase 2 migration 0001 đã baseline cột `documents.last_heartbeat`, `documents.attempts`, `documents.error_message` NHƯNG chưa có composite index `(status, last_heartbeat)` mà watchdog Plan 04-05 cần để query nhanh `WHERE status='processing' AND last_heartbeat < NOW() - make_interval(...)`. Plan 04-01 thêm alembic revision `0002` đưa index này vào.

2. **Settings + package scaffolding:** Cocoindex 1.0.3 dùng LMDB local filesystem cho internal state (env `COCOINDEX_DB`, KHÔNG `COCOINDEX_DATABASE_URL` — xác nhận 04-COCOINDEX-API-RESEARCH.md Section 5). Settings cần field `cocoindex_lmdb_path`. Watchdog Plan 04-05 cần `watchdog_timeout_seconds` (300s — REVISION 2 headroom cho cocoindex update_blocking documents lớn). Package `app/rag/__init__.py` phải tồn tại để Plan 04-03 import được.

Purpose: Các Plan downstream (04-03 flow, 04-04 router, 04-05 watchdog) import được package + field config + index sẵn sàng.
Output: 1 migration mới, package init, 2 Settings fields, .env.example update, 1 integration test migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-COCOINDEX-API-RESEARCH.md

<interfaces>
Migration 0001 (Phase 2) đã có — xác nhận trước khi viết 0002:
- documents columns: id (UUID PK), hub_id (UUID FK), uploaded_by (UUID FK), filename, file_path, mime_type, file_size, status (CHECK enum 5 giá trị), chunk_count, attempts, last_heartbeat (TIMESTAMPTZ), error_message, created_at, updated_at.
- documents index đã có: ix_documents_hub_id_status.
- chunks table: id, document_id FK CASCADE, hub_id, content, content_hash BYTEA, heading_path, page_start, page_end, vector vector(1536), metadata JSONB, created_at.
- chunks index đã có: ix_chunks_vector_hnsw (HNSW vector_cosine_ops).

Settings hiện tại (app/config.py) — pydantic-settings BaseSettings. Thêm 2 field MỚI, KHÔNG đổi field hiện có.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic revision 0002 — composite index watchdog</name>
  <read_first>
    - Hub_All/api/migrations/versions/0001_initial_schema.py (xác nhận revision id của 0001 + tên các index/cột documents đã tồn tại)
    - Hub_All/api/migrations/env.py (xác nhận include_object filter + naming convention)
  </read_first>
  <action>
  Tạo file `Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py`.

  - `revision = "0002"`; `down_revision` = revision id thực tế của migration 0001 (đọc từ file 0001 — KHÔNG đoán; thường là `"0001"`).
  - `upgrade()`: `op.create_index("ix_documents_status_last_heartbeat", "documents", ["status", "last_heartbeat"], unique=False)`. KHÔNG `op.add_column` cho last_heartbeat/attempts/error_message — Phase 2 migration 0001 đã baseline (xác nhận qua read_first; nếu THỰC SỰ thiếu cột nào thì add, ghi rõ trong commit message).
  - `downgrade()`: `op.drop_index("ix_documents_status_last_heartbeat", table_name="documents")`.
  - Docstring tiếng Việt: "Phase 4 — composite index (status, last_heartbeat) phục vụ watchdog query Plan 04-05 flip stuck processing rows."

  Sau khi viết: chạy `cd Hub_All/api && uv run alembic upgrade head` trên DB dev rồi `uv run alembic check` — phải báo "No new upgrade operations detected" (KHÔNG drift).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run alembic upgrade head && uv run alembic check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'revision = "0002"' Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py` >= 1
    - `grep -c 'ix_documents_status_last_heartbeat' Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py` >= 2 (create + drop)
    - `grep -c 'down_revision' Hub_All/api/migrations/versions/0002_phase4_documents_indexes.py` >= 1
    - `uv run alembic check` xuất "No new upgrade operations detected"
  </acceptance_criteria>
  <done>Migration 0002 chain đúng sau 0001; alembic upgrade head exit 0; alembic check không drift.</done>
</task>

<task type="auto">
  <name>Task 2: Settings fields + .env.example + package app.rag init</name>
  <read_first>
    - Hub_All/api/app/config.py (xác nhận class Settings + pattern field hiện có)
    - Hub_All/api/.env.example (xác nhận format env keys hiện có)
    - Hub_All/api/app/rag/__init__.py (file đã tồn tại từ lần execute trước — REWRITE docstring, KHÔNG xoá)
  </read_first>
  <action>
  **app/config.py** — thêm 2 field vào class `Settings` (sau các field hiện có, KHÔNG đổi field cũ):
  - `cocoindex_lmdb_path: Path = Path(".cocoindex/state.lmdb")` — đọc env `COCOINDEX_DB`. Cocoindex 1.0.3 dùng LMDB local filesystem cho internal state (memo cache, fingerprint) — KHÔNG dùng Postgres (xác nhận 04-COCOINDEX-API-RESEARCH.md Section 5).
  - `watchdog_timeout_seconds: int = 300` — đọc env `WATCHDOG_TIMEOUT_SECONDS`. 5 phút (REVISION 2 — headroom cho cocoindex `update_blocking()` documents lớn; 2 phút quá ngắn gây false-flip).
  - Nếu Settings dùng `model_config` với `env` alias mapping, map field tới đúng env key. Nếu dùng auto-uppercase, đặt tên field khớp.

  **.env.example** — thêm 2 dòng có comment tiếng Việt:
  ```
  # Cocoindex 1.0.3 lưu internal state (memo cache, fingerprint, lineage) trong LMDB local FS — KHÔNG dùng Postgres.
  COCOINDEX_DB=.cocoindex/state.lmdb
  # Watchdog flip documents stuck 'processing' quá N giây → 'failed' (REVISION 2 — 5 phút headroom cocoindex update_blocking).
  WATCHDOG_TIMEOUT_SECONDS=300
  ```

  **app/rag/__init__.py** — REWRITE docstring (file đã tồn tại). Docstring tiếng Việt mô tả kiến trúc cocoindex 1.0.3 actual API: `coco.App(coco.AppConfig(name="medinet_wiki_ingest"), main_fn)` + `@coco.lifespan` provide asyncpg.Pool + `pg.mount_table_target` USER-managed + A4 BackgroundTasks trigger pattern (router add_task `trigger_cocoindex_update` chạy `cocoindex_app.update_blocking()` sau response 202). TUYỆT ĐỐI KHÔNG nhắc deprecated 0.x API bằng literal `@cocoindex.flow_def` / `cocoindex.init` / `FlowLiveUpdater` (cocoindex 1.0.3 KHÔNG có — và acceptance grep enforce =0). Nếu cần đề cập, dùng backtick literal `flow_def` (không có ký tự @ và dấu chấm liền kề). Giữ re-export `setup_cocoindex` nếu Plan 04-03 chưa chạy thì để comment "Plan 04-03 ship app.rag.setup" — KHÔNG import cứng gây ImportError.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run python -c "from app.config import get_settings; s=get_settings(); print(s.cocoindex_lmdb_path, s.watchdog_timeout_seconds)" && uv run ruff check app/config.py app/rag/__init__.py && uv run mypy --strict app/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cocoindex_lmdb_path' Hub_All/api/app/config.py` >= 1
    - `grep -c 'watchdog_timeout_seconds' Hub_All/api/app/config.py` >= 1
    - `grep -c 'COCOINDEX_DB' Hub_All/api/.env.example` >= 1
    - `grep -c 'WATCHDOG_TIMEOUT_SECONDS' Hub_All/api/.env.example` >= 1
    - `grep -c '@cocoindex.flow_def' Hub_All/api/app/rag/__init__.py` == 0
    - `grep -c 'cocoindex.init' Hub_All/api/app/rag/__init__.py` == 0
    - `grep -c 'FlowLiveUpdater' Hub_All/api/app/rag/__init__.py` == 0
    - lệnh python in ra giá trị 2 field (không raise); ruff + mypy --strict CLEAN
  </acceptance_criteria>
  <done>2 Settings field load được; .env.example documented; app/rag/__init__.py docstring đúng cocoindex 1.0.3, 0 reference deprecated.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test migration phase 4</name>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py (fixtures postgres_container, alembic_cfg — reuse)
    - Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py (pattern test alembic + testcontainers hiện có)
  </read_first>
  <action>
  Tạo `Hub_All/api/tests/integration/test_phase4_migration.py` — testcontainers Postgres `pgvector/pgvector:pg16`.

  - Fixture reuse `postgres_container` + `alembic_cfg` từ conftest.py.
  - `test_phase4_migration_upgrade_creates_watchdog_index` (`@pytest.mark.critical`): chạy `alembic upgrade head`; query `SELECT indexname FROM pg_indexes WHERE tablename='documents'` → assert `ix_documents_status_last_heartbeat` có trong kết quả.
  - `test_phase4_migration_documents_watchdog_columns` (`@pytest.mark.critical`): query `information_schema.columns WHERE table_name='documents'` → assert có `last_heartbeat`, `attempts`, `error_message`.
  - `test_phase4_migration_downgrade_upgrade_roundtrip` (`@pytest.mark.integration`): `alembic downgrade -1` rồi `upgrade head` → index lại tồn tại (xác nhận downgrade()/upgrade() đối xứng).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_phase4_migration.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_phase4_migration' Hub_All/api/tests/integration/test_phase4_migration.py` >= 2
    - `grep -c 'ix_documents_status_last_heartbeat' Hub_All/api/tests/integration/test_phase4_migration.py` >= 1
    - `pytest tests/integration/test_phase4_migration.py` — tất cả test PASS (exit 0)
  </acceptance_criteria>
  <done>3 test PASS trên testcontainer pgvector pg16; index + cột watchdog verified runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alembic migration → Postgres DDL | Migration chạy DDL với credentials DATABASE_URL (full RW) |
| .env.example → operator | File mẫu cấu hình — KHÔNG chứa secret thật |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T (Tampering) | Migration 0002 down_revision sai → alembic chain gãy, schema state lệch | mitigate | Task 1 đọc revision id thực tế từ file 0001 trước khi viết; `alembic check` xác nhận no-drift; integration test downgrade/upgrade roundtrip |
| T-04-01-02 | I (Info disclosure) | .env.example vô tình chứa secret thật (LMDB path / DB URL) | mitigate | .env.example chỉ chứa giá trị placeholder; COCOINDEX_DB là path local FS không nhạy cảm; .env thật gitignored |
| T-04-01-03 | D (DoS) | Index thiếu → watchdog query full table scan documents lớn → DB chậm | mitigate | Composite index (status, last_heartbeat) chính là mục tiêu Task 1 — watchdog query dùng index này |
</threat_model>

<verification>
- `alembic upgrade head` + `alembic check` exit 0, không drift.
- `from app.config import get_settings` load được 2 field mới.
- `pytest tests/integration/test_phase4_migration.py` PASS.
- `pytest tests/` full suite KHÔNG regression (Phase 1-3 + plan khác).
- ruff + mypy --strict CLEAN trên app/config.py + app/rag/__init__.py.
</verification>

<success_criteria>
- Migration 0002 ship, chain đúng, index ix_documents_status_last_heartbeat tồn tại.
- Settings.cocoindex_lmdb_path + watchdog_timeout_seconds load từ env.
- app/rag/__init__.py docstring cocoindex 1.0.3, 0 reference deprecated 0.x.
- 3 integration test migration PASS.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-01-SUMMARY.md` theo template summary.md.
</output>
