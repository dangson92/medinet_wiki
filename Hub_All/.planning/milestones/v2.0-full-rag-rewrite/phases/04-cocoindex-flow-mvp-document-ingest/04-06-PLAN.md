---
plan: 06
phase: 4
wave: 3
type: execute
depends_on: [01, 03, 04]
files_modified:
  - Hub_All/api/tests/integration/conftest.py
  - Hub_All/api/app/rag/setup.py
  - Hub_All/api/tests/integration/test_ingest_e2e.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]
must_haves:
  truths:
    - "setup_cocoindex idempotent — gọi nhiều lần trong cùng Python process KHÔNG raise 'environment already open' (cocoindex 1.0.3 LMDB Rust env chỉ open ONCE per process); detect env đã start → reuse, KHÔNG re-start."
    - "Integration test conftest dùng session-scoped fixture cho cocoindex setup — 1 lần setup/teardown cho toàn bộ integration suite, KHÔNG re-init mỗi test → fix regression 9/29 critical test ERROR (LMDB singleton)."
    - "test_ingest_e2e.py 3 E2E test chạy được (KHÔNG pytest.skip mask) trên testcontainers Postgres + Redis + cocoindex_app real (KHÔNG mock cocoindex) — mock CHỈ LiteLLM aembedding (dim 1536 deterministic, KHÔNG cần OPENAI_API_KEY)."
    - "E2E happy path: upload DOCX VN → 202 → poll GET /:id → status='completed' + chunk_count > 0; SELECT chunks WHERE document_id → rows có hub_id đúng, vector dim=1536, content_hash BYTEA set. Đây là verify trực tiếp SC2 — gap zero-chunks lần trước phải được đóng."
    - "E2E scanned PDF: upload scanned PDF → 415 + documents.status='failed_unsupported'."
    - "SC5 (content-hash incremental dedup) — KHÔNG dựa vào soft-assert tự động. test_e2e_content_hash_incremental_dedup CHỈ assert hard-invariant không phụ thuộc memo (chunks tồn tại + KHÔNG duplicate stable_chunk_id). Verification chính thức của SC5 (no-re-embed: upload cùng file 2 lần → OpenAI usage-log count KHÔNG tăng cho document đã embed) được ĐỊNH TUYẾN sang manual demo Plan 04-07 — KHÔNG có escape hatch soft-assert thay thế SC5."
    - "pytest -m critical chạy toàn integration suite trong 1 process KHÔNG còn ERROR 'environment already open' — regression LMDB singleton đóng."
  artifacts:
    - path: Hub_All/api/tests/integration/conftest.py
      provides: "Session-scoped cocoindex setup fixture — 1 lần setup/teardown cho integration suite."
      contains: "scope=\"session\""
    - path: Hub_All/api/app/rag/setup.py
      provides: "setup_cocoindex idempotent — detect LMDB env đã open → reuse."
      contains: "def setup_cocoindex"
    - path: Hub_All/api/tests/integration/test_ingest_e2e.py
      provides: "3 E2E test happy/scanned/dedup — cocoindex_app real, mock chỉ LiteLLM."
      contains: "def test_e2e"
  key_links:
    - from: Hub_All/api/tests/integration/conftest.py
      to: Hub_All/api/app/rag/setup.py
      via: "session-scoped fixture gọi setup_cocoindex 1 lần"
      pattern: "setup_cocoindex"
    - from: Hub_All/api/tests/integration/test_ingest_e2e.py
      to: Hub_All/api/app/routers/documents.py (POST /upload)
      via: "httpx upload DOCX → A4 BackgroundTask → cocoindex flow → chunks pgvector"
      pattern: "documents/upload"
---

# Plan 04-06: M2a EXIT GATE — fix LMDB singleton + E2E test suite + zero-chunks closure

<objective>
Đóng M2a EXIT GATE — verify pipeline upload DOCX VN → A4 BackgroundTasks → cocoindex flow → chunks pgvector chạy thật end-to-end. Plan này giải quyết 2 gap được 04-VERIFICATION.md ghi nhận từ lần execute trước:

1. **Regression LMDB singleton:** Cocoindex 1.0.3 Rust LMDB env CHỈ open ONCE per Python process. Khi nhiều test integration cùng pytest process re-init lifespan → `RuntimeError: environment already open`. 9/29 critical test ERROR ở fixture setup. → Fix: `setup_cocoindex` idempotent (detect env đã start → reuse) + integration conftest dùng **session-scoped** fixture cho cocoindex setup (1 lần cho toàn suite).

2. **Gap zero-chunks (SC2/SC5 FAILED):** Lần trước cocoindex flow generate 0 chunks cho DOCX VN hợp lệ. Root cause được xác định trong Plan 04-04 (COMMIT phải xảy ra TRƯỚC khi BackgroundTask chạy `update_blocking`, nếu không cocoindex `SELECT documents` không thấy row vừa INSERT). Plan 04-04 đã enforce COMMIT-after-INSERT. Plan này VERIFY trực tiếp: E2E test chạy `cocoindex_app` THẬT (KHÔNG mock cocoindex — chỉ mock LiteLLM) → upload DOCX VN → poll status='completed' + chunk_count > 0 + SELECT chunks pgvector. Nếu test vẫn 0 chunks → phải debug `index_document` invocation (log `index_document_chunks` Plan 04-03 Task 1 đã thêm) và fix forward trước khi đóng plan.

Đây là plan cuối Phase 4 cho M2a EXIT GATE — demo upload DOCX → chunks pgvector → SELECT verify (ROADMAP M2a EXIT GATE).

Purpose: đóng SC2/SC5 + regression LMDB singleton; INGEST-01/02/03 end-to-end verified.
Output: conftest session-scoped fixture, setup_cocoindex idempotent, test_ingest_e2e.py 3 test PASS thật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-COCOINDEX-API-RESEARCH.md
@Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-VERIFICATION.md

<interfaces>
Plan 04-03: setup_cocoindex(settings) sync; coco.start_blocking/stop_blocking; cocoindex_app.update_blocking().
Plan 04-04: POST /api/documents/upload + GET /api/documents/:id + trigger_cocoindex_update A4; documents_service COMMIT-after-INSERT.
cocoindex 1.0.3 constraint: LMDB Rust env open ONCE per process. coco.start_blocking lần 2 → RuntimeError "environment already open". Detect qua: bắt RuntimeError + kiểm tra message, hoặc check default env state trước khi start (đọc `cocoindex/_internal/environment.py` để biết cách query env đã start chưa).
ROADMAP SC2: <5s sau upload status pending → completed. E2E test timeout thực tế: dùng 120-300s (cocoindex Rust core init + initial backfill lần đầu chậm hơn SLA production — SLA <5s verify ở demo production thật).
LiteLLM mock: AsyncMock side_effect trả object có .data = [{"embedding": [floats]*1536, "index": 0}].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: setup_cocoindex idempotent — fix LMDB singleton</name>
  <read_first>
    - Hub_All/api/app/rag/setup.py (Plan 04-03 — setup_cocoindex hiện tại)
    - Hub_All/api/.venv/Lib/site-packages/cocoindex/_internal/environment.py (xác nhận cách detect default env đã start chưa — state attribute hoặc exception khi start_blocking lần 2)
  </read_first>
  <action>
  EXTEND `app/rag/setup.py` — làm `setup_cocoindex` idempotent:
  - Thêm module-level flag `_COCOINDEX_STARTED: bool = False` (hoặc dùng cách query env state nếu cocoindex expose).
  - Trong `setup_cocoindex`: nếu `_COCOINDEX_STARTED` đã True → log info "cocoindex_already_started_reuse" + chạy lại `cocoindex_app.update_blocking()` (re-process — vẫn cần cho test mới upload row) rồi `return`. KHÔNG gọi `coco.start_blocking()` lần 2.
  - Bao `coco.start_blocking()` trong try/except: nếu raise `RuntimeError` chứa "environment already open" → set `_COCOINDEX_STARTED=True`, log warning, tiếp tục (KHÔNG re-raise — env đã sẵn sàng). Sau start thành công cũng set `_COCOINDEX_STARTED=True`.
  - `@coco.lifespan` đăng ký lại lần 2 sẽ warn "Overriding default lifespan" — cosmetic, chấp nhận; hoặc guard chỉ đăng ký 1 lần qua cùng flag.
  - `stop_cocoindex` — KHÔNG reset `_COCOINDEX_STARTED` (env không thật sự đóng được trong cùng process — để tránh re-open). Comment giải thích cocoindex 1.0.3 LMDB singleton constraint.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run python -c "import inspect; from app.rag.setup import setup_cocoindex; src=inspect.getsource(setup_cocoindex); assert 'already' in src.lower() or '_COCOINDEX_STARTED' in src; print('idempotent OK')" && uv run ruff check app/rag/setup.py && uv run mypy --strict app/rag/setup.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '_COCOINDEX_STARTED' Hub_All/api/app/rag/setup.py` >= 2
    - `grep -ci 'already open\|already_started' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c 'RuntimeError' Hub_All/api/app/rag/setup.py` >= 1
    - lệnh python in "idempotent OK"; ruff + mypy --strict CLEAN
  </acceptance_criteria>
  <done>setup_cocoindex idempotent — gọi 2 lần KHÔNG raise environment-already-open.</done>
</task>

<task type="auto">
  <name>Task 2: conftest session-scoped cocoindex fixture</name>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py (fixtures hiện tại — app_with_auth, postgres_container; xác nhận scope)
    - Hub_All/api/app/rag/setup.py (Task 1 — setup_cocoindex idempotent)
  </read_first>
  <action>
  EXTEND `tests/integration/conftest.py`:
  - Thêm fixture `cocoindex_session` `scope="session"` (autouse=False) — gọi `setup_cocoindex(get_settings())` 1 lần đầu suite + `stop_cocoindex()` ở teardown. Vì cocoindex LMDB singleton, đây phải là session-scoped (KHÔNG function-scoped).
  - Fixture `app_with_auth` (hoặc fixture tạo FastAPI app cho integration test) — nếu hiện đang để lifespan tự chạy `setup_cocoindex` mỗi test → đổi để KHÔNG re-setup; depend trên `cocoindex_session` hoặc set `app.state.cocoindex_app` từ `get_cocoindex_app()` đã setup sẵn. Lifespan vẫn chạy nhưng `setup_cocoindex` idempotent (Task 1) → an toàn.
  - Mục tiêu: chạy `pytest -m critical` toàn bộ integration suite trong 1 process KHÔNG còn ERROR "environment already open".
  - Nếu fixture cocoindex cần Docker (cocoindex chỉ start được khi có Postgres) — gắn dependency `postgres_container` session-scoped.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_documents_upload.py tests/integration/test_auth_login.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'scope="session"' Hub_All/api/tests/integration/conftest.py` >= 1
    - `grep -c 'cocoindex' Hub_All/api/tests/integration/conftest.py` >= 1
    - `pytest tests/integration/test_documents_upload.py tests/integration/test_auth_login.py` — chạy chung 1 process, KHÔNG ERROR "environment already open", tất cả PASS
  </acceptance_criteria>
  <done>conftest session-scoped cocoindex fixture; integration suite chạy 1 process không LMDB ERROR.</done>
</task>

<task type="auto">
  <name>Task 3: test_ingest_e2e.py — 3 E2E test cocoindex_app real + zero-chunks closure</name>
  <read_first>
    - Hub_All/api/tests/integration/test_ingest_e2e.py (file đã tồn tại — REWRITE đúng spec)
    - Hub_All/api/tests/integration/conftest.py (Task 2 — cocoindex_session fixture)
    - Hub_All/api/app/services/documents_service.py (Plan 04-04 — trigger_cocoindex_update + COMMIT-after-INSERT)
    - Hub_All/api/app/rag/flow.py (Plan 04-03 — index_document log index_document_chunks)
  </read_first>
  <action>
  REWRITE `tests/integration/test_ingest_e2e.py` — 3 E2E test chạy THẬT (cocoindex_app real qua cocoindex_session fixture; KHÔNG mock cocoindex; CHỈ mock LiteLLM aembedding).

  - Fixture `mock_litellm_embedding(monkeypatch)` — monkeypatch `litellm.aembedding` global trả `SimpleNamespace(data=[{"embedding": [deterministic floats]*1536, "index": 0}])`. Seed cố định để stable.
  - Helper `_make_docx_vn(tmp_path)` — runtime-generate DOCX VN qua python-docx: "Mục 1. KHÁM TỔNG QUÁT" + 2-3 đoạn + "Mục 2. XÉT NGHIỆM" + 2-3 đoạn (đủ nội dung để chunk_vietnamese chắc chắn trả >= 1 chunk).
  - Helper `_create_hub(app)` INSERT hub SQL; `_wait_until(client, token, doc_id, target_statuses, timeout=120)` poll GET /:id mỗi 1s.

  **test_e2e_upload_docx_to_chunks_completed** (`@pytest.mark.critical`) — đây là verify SC2:
  - Upload DOCX VN → 202 + document_id.
  - `_wait_until` poll tới status in {completed, failed, failed_unsupported}, timeout 120s.
  - **assert status == 'completed'** + `chunk_count > 0`.
  - SELECT `chunks WHERE document_id=:id` → assert COUNT > 0, mọi row `hub_id` match, `vector_dims(vector) == 1536`, `content_hash IS NOT NULL`.
  - **Nếu status == 'failed' với error_message 'cocoindex flow generated 0 chunks'** → test FAIL với message rõ + executor PHẢI debug: kiểm tra log `index_document_chunks` (Plan 04-03) có fire không, kiểm tra `cocoindex_app.update_blocking()` có thấy row pending không (commit ordering Plan 04-04), kiểm tra `coco.mount_each` invoke `index_document`. Fix forward (Rule 1/Rule 4 escalate nếu là defect kiến trúc cocoindex) — KHÔNG để test skip/xfail.

  **test_e2e_pdf_scanned_failed_unsupported** (`@pytest.mark.critical`) — monkeypatch `detect_scanned_pdf=True` CẢ 2 module (file_extract + documents_service); upload PDF → assert 415 + envelope error.code UNSUPPORTED_FORMAT; SELECT documents → status='failed_unsupported'.

  **test_e2e_content_hash_incremental_dedup** (`@pytest.mark.integration`) — hard-invariant only, KHÔNG soft-assert SC5:
  - Upload cùng content 2 lần (cùng hub_id). Cocoindex memo + stable_chunk_id uuid5 deterministic per (doc_id, chunk_index).
  - Test này CHỈ assert hard-invariant không phụ thuộc memo behavior của cocoindex 1.0.3: (a) chunks của cả 2 document tồn tại, (b) KHÔNG có duplicate `stable_chunk_id` trong bảng chunks. ĐÂY KHÔNG PHẢI verification của SC5.
  - **KHÔNG dùng escape hatch "assert mức tối thiểu" để SC5 PASS qua test này.** SC5 (incremental no-re-embed: cocoindex memo content fingerprint → KHÔNG re-embed chunks của lần upload trước) KHÔNG thể assert tin cậy trong E2E test vì mock LiteLLM không phản ánh OpenAI usage thật. Vì vậy verification chính thức của SC5 được ĐỊNH TUYẾN sang manual demo Plan 04-07: upload cùng file 2 lần, quan sát OpenAI usage-log count KHÔNG tăng cho document đã embed.
  - Docstring/comment của `test_e2e_content_hash_incremental_dedup` PHẢI ghi rõ: "Test này CHỈ verify hard-invariant (no duplicate stable_chunk_id). SC5 incremental no-re-embed được verify ở Plan 04-07 manual demo (OpenAI usage-log count) — KHÔNG ở test này."
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_ingest_e2e.py -v && uv run ruff check tests/integration/test_ingest_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_e2e' Hub_All/api/tests/integration/test_ingest_e2e.py` >= 3
    - `grep -c 'pytest.skip' Hub_All/api/tests/integration/test_ingest_e2e.py` == 0 (KHÔNG mask — actual call)
    - `grep -c 'vector_dims' Hub_All/api/tests/integration/test_ingest_e2e.py` >= 1
    - `grep -c 'chunk_count' Hub_All/api/tests/integration/test_ingest_e2e.py` >= 1
    - `grep -ci 'Plan 04-07' Hub_All/api/tests/integration/test_ingest_e2e.py` >= 1 (SC5 routing ghi rõ trong docstring/comment)
    - `pytest tests/integration/test_ingest_e2e.py` — 3 test PASS (gồm happy path status='completed' chunk_count>0 — SC2 đóng)
    - ruff CLEAN
  </acceptance_criteria>
  <done>3 E2E test PASS; SC2 (chunks generated) verified end-to-end; SC5 verification định tuyến sang Plan 04-07 demo (KHÔNG soft-assert); regression LMDB đóng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → testcontainers Postgres/Redis | Test môi trường cô lập |
| mock LiteLLM → embedder | Mock thay API ngoài — KHÔNG dùng key thật trong CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | T (Tampering) | Mock LiteLLM vector nhiễm DB testcontainer giữa các test | accept | testcontainer per-session scope + TRUNCATE/cleanup per-test fixture; data test KHÔNG vào production |
| T-04-06-02 | D (DoS) | E2E test poll 120s × N test → CI bloat thời gian | mitigate | timeout 120s/test có giới hạn; cocoindex_session session-scoped tránh re-setup; mock LiteLLM nhanh (KHÔNG gọi OpenAI) |
| T-04-06-03 | I (Info disclosure) | E2E test log file_path / nội dung chunk y tế | mitigate | Test dùng DOCX runtime-generate nội dung giả; KHÔNG log nội dung thật |
| T-04-06-04 | R (Repudiation) | M2a EXIT GATE verify qua test tự động KHÔNG có demo người thật | accept | ROADMAP M2a EXIT GATE — demo production thật do operator chạy (docs/m2a-exit-gate-demo.md từ lần trước); plan này verify automated SC2 là tiền đề |
</threat_model>

<verification>
- `pytest tests/integration/test_ingest_e2e.py` PASS — 3 E2E test, gồm happy path status='completed' chunk_count>0.
- `pytest -m critical` — toàn bộ critical suite PASS trong 1 process, 0 ERROR "environment already open" (regression LMDB đóng). Đây là regression sweep cấp plan — KHÔNG phải per-task verify.
- `pytest tests/` full suite KHÔNG regression.
- ruff CLEAN.
- SC2 (cocoindex flow generate chunks) verified end-to-end — gap zero-chunks 04-VERIFICATION.md đóng. SC5 verification định tuyến sang Plan 04-07 manual demo (KHÔNG soft-assert ở test E2E).
</verification>

<success_criteria>
- setup_cocoindex idempotent — LMDB singleton regression đóng.
- conftest session-scoped cocoindex fixture — integration suite chạy 1 process clean.
- 3 E2E test PASS thật (cocoindex_app real).
- SC2: upload DOCX VN → chunks pgvector dim=1536 + content_hash + hub_id đúng.
- SC5: verification định tuyến sang Plan 04-07 manual demo (OpenAI usage-log count) — test E2E chỉ assert hard-invariant.
- M2a EXIT GATE — pipeline upload → chunks → SELECT verify đóng.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-06-SUMMARY.md` theo template summary.md.
</output>
</content>
</invoke>
