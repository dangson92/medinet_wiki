---
phase: 08-frontend-e2e-smoke
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - api/tests/integration/test_smoke_golden_path.py
  - api/tests/integration/test_vietnamese_filename.py
  - api/tests/integration/conftest.py
autonomous: true
requirements: [COMPAT-01]
must_haves:
  truths:
    - "Golden path API chạy tự động: login admin → upload DOCX hub_y_te → search cross-hub → ask có citation [N] → audit log thấy 4 entry"
    - "Upload file tên tiếng Việt 'Khám bệnh đa khoa.docx' trả filename UTF-8 đúng (không mojibake)"
    - "GET document detail sau upload trả name khớp tên file đã upload"
  artifacts:
    - path: "api/tests/integration/test_smoke_golden_path.py"
      provides: "Test suite golden path end-to-end qua FastAPI in-process"
      contains: "pytest.mark.critical"
      min_lines: 80
    - path: "api/tests/integration/test_vietnamese_filename.py"
      provides: "Test upload + đọc lại filename UTF-8 tiếng Việt"
      contains: "Khám bệnh"
  key_links:
    - from: "test_smoke_golden_path.py"
      to: "POST /api/documents/upload + POST /api/search/cross-hub + POST /api/ask + GET /api/audit-logs"
      via: "httpx.AsyncClient ASGITransport"
      pattern: "/api/(documents/upload|search/cross-hub|ask|audit-logs)"
---

<objective>
Viết test suite tự động chứng minh golden path API contract (ROADMAP Phase 8 SC2) và Vietnamese filename UTF-8 (SC4) — phần verify được bằng máy, không cần con người.

Purpose: SC2 + SC4 phần lớn kiểm chứng được tự động qua FastAPI in-process (httpx ASGITransport). Để Plan 08-04 (manual UAT) chỉ còn phần thật sự cần mắt người (render UI 11 trang), mọi thứ kiểm bằng API phải tự động hoá ở đây. LLM call MOCK (D-07-05-A — `OPENAI_API_KEY` M2 placeholder; pattern conftest đã có fixture `mock_llm`).
Output: 2 file test integration + conftest helper. Bao phủ ROADMAP Phase 8 SC2 (golden path) + SC4 (VN filename).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/CONVENTIONS.md

<interfaces>
<!-- Trạng thái thực tế đã verify lúc planning. Executor đọc kỹ conftest trước khi viết test. -->

api/tests/integration/conftest.py — đã có (mở rộng qua Phase 3/5/7):
- fixture `postgres_container` (scope module, pgvector/pgvector:pg16) + `redis_container`
- fixture `app_with_auth` (alembic upgrade head + lifespan) — LƯU Ý DEF-05-01: cocoindex 1.0.3 Environment KHÔNG re-open trong cùng process → mỗi file test dùng app fixture cho >1 test có thể FAIL từ test thứ 2. Phase 7 Plan 07-05 né bằng chạy per-file pytest. Test plan này CŨNG phải chạy per-file (DEF-05-01).
- fixture `admin_token` (POST /login lấy access token)
- fixture `mock_llm` (Phase 7 — monkeypatch litellm.acompletion trả completion giả deterministic) + helper `make_fake_completion`, `_make_vec`, `_wait_usage_count`

Endpoint dùng trong golden path (đã verify mount):
- POST /api/auth/login → {access_token, refresh_token, user}
- POST /api/documents/upload (multipart: file + hub_id Form) → 202 {document_id / id, name, status, ...}
- GET /api/documents/{id} → {id, name, file_type, status, chunk_count, ...}
- POST /api/search/cross-hub body {query, hub_ids:[...], top_k} → {results:[...], ...}
- POST /api/ask body {q, hub_id} → {answer, citations:[{chunk_id, ...}]}
- GET /api/audit-logs (admin) → list AuditLogAPI

documents_service.create() lưu filename gốc vào cột documents.filename; DocumentResponse field `name` = documents.filename (Plan 04-04 đưa shape về contract D6). Upload đa phần status='pending' → cocoindex BackgroundTasks. Trong test KHÔNG có cocoindex runtime → chunk_count có thể 0; test KHÔNG assert chunk_count>0 (đó là phần manual UAT Plan 08-04 với cocoindex thật).

AUDIT_ACTIONS (audit_service.py) gồm: auth.login, document.upload, document.delete, hub.create, user.create, ... — search/ask có thể KHÔNG enqueue audit (verify trong test, không giả định 4 entry cứng nếu code không log; xem Task 1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test suite golden path API end-to-end</name>
  <read_first>
    - api/tests/integration/conftest.py (toàn bộ — fixture app_with_auth, admin_token, mock_llm, helper)
    - api/tests/integration/test_ask_api.py (pattern Phase 7 — gọi /api/ask với mock_llm, assert citation)
    - api/app/routers/documents.py (response upload — field `id` vs `document_id`, status code 202)
    - api/app/services/audit_service.py (AUDIT_ACTIONS — action nào thực sự được enqueue)
    - api/app/routers/audit_logs.py (GET /api/audit-logs — filter param, shape response)
  </read_first>
  <behavior>
    - login admin → 200, response.json()["data"]["access_token"] là chuỗi không rỗng
    - upload DOCX nhỏ vào hub_y_te (hub seed) → 202, data có document id (key `id` hoặc `document_id` — kiểm theo router thực)
    - GET /api/documents/{id} → 200, data["name"] khớp tên file upload, data["status"] thuộc {pending, processing, completed}
    - POST /api/search/cross-hub với hub_ids chứa hub seed → 200, response có key `results` (list, có thể rỗng nếu chưa có chunk — KHÔNG assert độ dài)
    - POST /api/ask (mock_llm) → 200, data có `answer` (str) + `citations` (list)
    - GET /api/audit-logs (admin token) → 200, list chứa ÍT NHẤT entry action `auth.login` (login vừa thực hiện đã enqueue audit)
  </behavior>
  <action>
Tạo `api/tests/integration/test_smoke_golden_path.py` — chuỗi test mô phỏng golden path SC2 qua FastAPI in-process (`httpx.AsyncClient` + `ASGITransport`, fixture `app_with_auth`).

Vì DEF-05-01 (cocoindex Environment re-open) — viết golden path thành **MỘT test function `@pytest.mark.critical @pytest.mark.asyncio test_golden_path_end_to_end`** chạy tuần tự toàn chuỗi trong 1 test (tránh nhiều test cùng dùng app fixture). Các bước trong cùng function:

1. **Seed hub:** INSERT 1 hub `hub_y_te` qua engine (dùng pattern seed của conftest — INSERT hubs với `code`, `slug`, `name`, `status='active'`; lưu `hub_id`). Nếu conftest đã có fixture hub seed, tái dùng.
2. **Login:** POST /api/auth/login với admin credential (fixture `admin_token` đã làm việc này — có thể dùng trực tiếp `admin_token`; nếu cần token tươi, POST /login). Assert access_token tồn tại.
3. **Upload DOCX:** Tạo file DOCX tối giản hợp lệ in-memory. Dùng `python-docx` nếu có trong deps; nếu KHÔNG có, tạo file `.txt` (whitelist `{.docx,.txt,.md,.pdf}` chấp nhận `.txt`) với nội dung tiếng Việt ngắn — ghi rõ lựa chọn trong SUMMARY. POST multipart `/api/documents/upload` (file + `hub_id` Form) với Bearer admin. Assert 202. Lấy document id từ response (key đúng theo router).
4. **GET document detail:** GET /api/documents/{id} Bearer admin → 200, assert `name` khớp tên file, `status` hợp lệ.
5. **Cross-hub search:** POST /api/search/cross-hub body `{query:"khám bệnh", hub_ids:[hub_id], top_k:10}` Bearer admin → 200, assert key `results` tồn tại là list.
6. **Ask:** với fixture `mock_llm` active — POST /api/ask body `{q:"quy trình khám bệnh là gì", hub_id:hub_id}` Bearer admin → 200, assert `answer` là str, `citations` là list.
7. **Audit log:** GET /api/audit-logs Bearer admin → 200, parse list, assert có ít nhất 1 entry `action == "auth.login"`. Nếu audit flush async (asyncio.Queue 2s/128) — poll tối đa ~3s (pattern `_wait_usage_count` của conftest) cho tới khi entry xuất hiện hoặc timeout. KHÔNG hardcode "4 entry" — assert linh hoạt: login chắc chắn có; document.upload có thể có (verify từ AUDIT_ACTIONS code); nếu code thực sự enqueue 4 action thì assert ≥4, nếu không assert ≥1 và ghi chú trong SUMMARY action nào không được log (là input cho 08-04 manual checklist).

Mỗi bước assert status code rõ ràng kèm message in `resp.text` khi fail (để debug nhanh). KHÔNG assert `chunk_count>0` (không có cocoindex runtime in test).

Tuân `ruff` + conftest pattern hiện có.
  </action>
  <verify>
    <automated>cd api && python -m pytest tests/integration/test_smoke_golden_path.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - File `api/tests/integration/test_smoke_golden_path.py` tồn tại
    - Chứa 1 test `test_golden_path_end_to_end` với marker `@pytest.mark.critical`
    - `grep -E "/api/documents/upload|/api/search/cross-hub|/api/ask|/api/audit-logs" api/tests/integration/test_smoke_golden_path.py` khớp cả 4 endpoint
    - `pytest tests/integration/test_smoke_golden_path.py` PASS (chạy per-file — DEF-05-01)
    - `ruff check api/tests/integration/test_smoke_golden_path.py` exit 0
  </acceptance_criteria>
  <done>Golden path API SC2 chạy tự động PASS — login→upload→search→ask→audit verified end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test Vietnamese filename UTF-8 (SC4)</name>
  <read_first>
    - api/tests/integration/test_smoke_golden_path.py (pattern upload vừa viết Task 1 — tái dùng)
    - api/app/services/documents_service.py (create() — cách lưu filename, cột documents.filename)
    - api/app/routers/documents.py (upload — `file.filename`, `original_filename`; GET detail trả `name`)
    - api/app/services/file_store.py (save() — đặt tên file lưu đĩa; kiểm path traversal handling)
  </read_first>
  <behavior>
    - upload file tên "Khám bệnh đa khoa.docx" → 202, response data `name` == "Khám bệnh đa khoa.docx" (UTF-8 nguyên vẹn, không mojibake "KhÃ¡m")
    - GET /api/documents/{id} → data["name"] == "Khám bệnh đa khoa.docx"
    - tên file chứa dấu tiếng Việt KHÔNG gây path traversal: file lưu đĩa qua file_store.save() không escape khỏi file_store dir
  </behavior>
  <action>
Tạo `api/tests/integration/test_vietnamese_filename.py` — test UTF-8 filename SC4.

1 test `@pytest.mark.critical @pytest.mark.asyncio test_vietnamese_filename_roundtrip`:
- Seed hub + admin token (tái dùng pattern Task 1 / fixture conftest).
- Upload file đặt tên chính xác `"Khám bệnh đa khoa.docx"` (hoặc `.txt` nếu Task 1 dùng `.txt` — giữ nhất quán) — khi build multipart với httpx, truyền `files={"file": ("Khám bệnh đa khoa.docx", content, mime)}`. Đảm bảo tên truyền dạng `str` Unicode (không bytes mojibake).
- Assert response 202, `data["name"] == "Khám bệnh đa khoa.docx"` — so sánh bằng chuỗi Unicode literal. Nếu mojibake, assert sẽ fail rõ ràng.
- GET /api/documents/{id} → assert `data["name"]` vẫn `== "Khám bệnh đa khoa.docx"`.
- **Path traversal guard:** thêm assert phụ — sau upload, kiểm `data["file_path"]` (nếu response trả) hoặc query cột `documents.file_path` qua engine KHÔNG chứa `..` và nằm trong `file_store` dir. Đây là verify mục threat T-08-03 (VN filename không gây traversal). Nếu file_store.save() đã sinh tên UUID (không dùng tên gốc) thì assert file_path khớp pattern UUID — ghi rõ trong SUMMARY cơ chế nào đang bảo vệ.

Chạy per-file (DEF-05-01). Tuân `ruff`.
  </action>
  <verify>
    <automated>cd api && python -m pytest tests/integration/test_vietnamese_filename.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - File `api/tests/integration/test_vietnamese_filename.py` tồn tại
    - `grep "Khám bệnh đa khoa" api/tests/integration/test_vietnamese_filename.py` khớp (literal Unicode)
    - Test có marker `@pytest.mark.critical`
    - Test assert cả response upload VÀ GET detail trả `name` == chuỗi tiếng Việt nguyên vẹn
    - Test có assert path traversal (file_path không chứa `..`, nằm trong file_store)
    - `pytest tests/integration/test_vietnamese_filename.py` PASS
    - `ruff check api/tests/integration/test_vietnamese_filename.py` exit 0
  </acceptance_criteria>
  <done>VN filename SC4 verified tự động — upload + đọc lại tên đúng UTF-8, không traversal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multipart upload → file_store | filename do client cung cấp (có thể chứa `../` hoặc ký tự lạ) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | filename tiếng Việt / `../` gây path traversal khi file_store.save() | mitigate | Task 2 thêm assert file_path không chứa `..` + nằm trong file_store dir; verify cơ chế file_store hiện hành (UUID rename hoặc sanitize). Nếu test phát hiện traversal → BLOCKER báo về, không phải accept. |
| T-08-03-02 | Information Disclosure | test in nội dung response chứa token | accept | Test integration nội bộ, token sinh từ container ephemeral — không phải secret production. Không log token ra CI artifact dài hạn. |

Không có threat HIGH chưa giảm thiểu — đây là test suite; traversal được kiểm chủ động trong Task 2.
</threat_model>

<verification>
- `pytest tests/integration/test_smoke_golden_path.py` PASS (per-file)
- `pytest tests/integration/test_vietnamese_filename.py` PASS (per-file)
- `pytest -m critical tests/integration/test_smoke_golden_path.py tests/integration/test_vietnamese_filename.py` — 2 test critical PASS
- `ruff` clean trên 2 file test
</verification>

<success_criteria>
Golden path API (SC2) và Vietnamese filename UTF-8 (SC4) verified hoàn toàn tự động qua FastAPI in-process; LLM mock; chạy per-file né DEF-05-01. Manual UAT 08-04 chỉ còn phần render UI.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08-frontend-e2e-smoke/08-03-SUMMARY.md`
</output>
