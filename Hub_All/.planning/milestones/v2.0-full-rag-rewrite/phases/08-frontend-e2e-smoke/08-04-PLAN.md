---
phase: 08-frontend-e2e-smoke
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - api/scripts/smoke/boot_stack.sh
  - .planning/phases/08-frontend-e2e-smoke/08-SMOKE-CHECKLIST.md
  - .planning/phases/08-frontend-e2e-smoke/08-HUMAN-UAT.md
autonomous: false
requirements: [COMPAT-01]
must_haves:
  truths:
    - "docker compose up lên healthy 3-service postgres+redis+python-api, không reference Go"
    - "11 trang React render OK qua npm run dev không 500/CORS/401 sai (SyncQueue ngoại lệ EXCLUDED)"
    - "Golden path browser PASS: login admin → upload DOCX → cross-hub search → ask citation [1] clickable → audit log"
  artifacts:
    - path: "api/scripts/smoke/boot_stack.sh"
      provides: "Script boot stack + healthcheck verify tự động hoá phần boot được"
      min_lines: 30
    - path: ".planning/phases/08-frontend-e2e-smoke/08-SMOKE-CHECKLIST.md"
      provides: "Checklist 11 trang + golden path cho người verify"
      contains: "SyncQueue"
  key_links:
    - from: "api/scripts/smoke/boot_stack.sh"
      to: "docker compose + GET /healthz"
      via: "docker compose up + curl healthcheck"
      pattern: "healthz"
---

<objective>
Boot toàn bộ stack mới và verify 11 trang React render đúng + golden path chạy được trên trình duyệt thật — phần cuối cùng cần mắt người (ROADMAP Phase 8 SC1 + SC5, và SC2 mức browser).

Purpose: Plan 08-01/02/03 đã đối chiếu contract, fix gap api-side, và tự động hoá golden path API. Còn lại phần KHÔNG tự động được: render UI 11 trang React, citation `[1]` clickable trong CitationText.tsx, docker compose lên healthy thật. Plan này tự động hoá phần boot/healthcheck được, rồi checkpoint cho người click.
Output: Script boot + checklist + biên bản UAT. Bao phủ ROADMAP Phase 8 SC1 (11 trang render) + SC2 (golden path browser) + SC5 (compose healthy). Hoàn tất COMPAT-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-frontend-e2e-smoke/08-CONTRACT-DIFF.md
@.planning/phases/08-frontend-e2e-smoke/08-02-SUMMARY.md

<interfaces>
<!-- Trạng thái thực tế đã verify lúc planning. -->

docker-compose.yml — 3 service: postgres (pgvector/pgvector:pg16, healthcheck pg_isready), redis (redis:7-alpine, healthcheck redis-cli ping), python-api (build ./api, port host 8180→8080 sau Plan 08-02, healthcheck curl /healthz). KHÔNG service Go.

Healthcheck endpoint: GET /healthz → 200 {success:true, data:{status:"ok"}}; GET /readyz → ready khi DB pool + jwt + (cocoindex) sẵn sàng.

11 trang React cần verify (route trong frontend/src/App.tsx) — KÈM route path thực:
| # | Trang | Route path | Component |
|---|-------|-----------|-----------|
| 1 | Dashboard | `/` | Dashboard.tsx (render GeminiAssistant widget) |
| 2 | HubRegistry | `/registry` | HubRegistry.tsx |
| 3 | DocumentIngestion | `/documents` | DocumentIngestion.tsx |
| 4 | UserManagement | `/users` | UserManagement.tsx |
| 5 | AuditLog | `/logs` | AuditLog.tsx |
| 6 | APIKeyManagement | `/api-keys` | APIKeyManagement.tsx |
| 7 | CrossHubSearch | `/search` | CrossHubSearch.tsx (render CitationText) |
| 8 | Settings | `/settings` | Settings.tsx |
| 9 | GeminiAssistant | (component, hiện trên Dashboard/Settings) | components/GeminiAssistant.tsx |
| 10 | TokenUsage | `/usage` | TokenUsage.tsx |
| 11 | Profile | `/profile` | Profile.tsx |

NGOẠI LỆ — KHÔNG tính vào PASS: `/sync` (SyncQueue.tsx) + `/sync/review/:batchId` (SyncReview.tsx) — sync queue loại khỏi M2 (Phase 5 CONTEXT D-01), `/api/sync/*` không implement → trang dự kiến lỗi API. Ghi nhận "EXCLUDED" trong checklist.

Frontend dev server: `cd frontend && npm install && npm run dev` → Vite 6 mặc định http://localhost:5173. api.ts trỏ `http://<hostname>:8180` (sau Plan 08-02 backend ở 8180).

Lệnh local nhanh (MEMORY project_run_backend_local): uvicorn native + Postgres/Redis Docker — hoặc full `docker compose up`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script boot_stack.sh + checklist 11 trang</name>
  <read_first>
    - docker-compose.yml (3 service + healthcheck đã có)
    - api/scripts/init-db.sh (init 2 logical DB — chạy lúc postgres khởi tạo)
    - api/Makefile (target migrate / dev — cách apply alembic + chạy api)
    - Makefile (target up/down/logs root)
    - .planning/phases/08-frontend-e2e-smoke/08-CONTRACT-DIFF.md (gap phân loại — để checklist ghi đúng kỳ vọng mỗi trang)
  </read_first>
  <action>
Tạo `api/scripts/smoke/boot_stack.sh` — script bash tự động hoá phần boot kiểm được:

1. `docker compose up -d --build` từ thư mục `Hub_All/` (script `cd` về root repo qua `$(git rev-parse --show-toplevel)` hoặc path tương đối).
2. Chờ 3 service healthy: poll `docker compose ps --format json` (hoặc `docker inspect`) tối đa 90s cho tới khi postgres + redis + python-api đều `healthy`. In trạng thái mỗi 5s.
3. Verify HTTP: `curl -fsS http://localhost:8180/healthz` → assert chứa `"status"` và `"ok"`; `curl -fsS http://localhost:8180/readyz`. In kết quả.
4. Apply alembic migration nếu cần: `docker compose exec -T python-api alembic upgrade head` (hoặc lệnh migrate của project — kiểm api/Makefile). Báo lỗi rõ nếu fail.
5. Verify KHÔNG có service Go: assert `docker compose config --services` chỉ liệt kê `postgres redis python-api` — nếu có service nào tên chứa `go`/`backend` → fail in cảnh báo.
6. Exit 0 nếu mọi bước PASS, exit 1 + thông điệp rõ nếu bất kỳ bước fail. In hướng dẫn cuối: "Backend sẵn sàng tại http://localhost:8180 — tiếp theo: cd frontend && npm install && npm run dev".

Script idempotent (chạy lại được). Dùng `set -euo pipefail`. Comment tiếng Việt.

Tạo `.planning/phases/08-frontend-e2e-smoke/08-SMOKE-CHECKLIST.md` — checklist Markdown cho người verify, gồm:

**Section A — Boot stack (SC1 + SC5):** Các bước: chạy `bash api/scripts/smoke/boot_stack.sh` (mong đợi exit 0, 3 service healthy), rồi `cd frontend && npm install && npm run dev`. Checkbox cho từng mục.

**Section B — 11 trang render (SC1):** Bảng 11 dòng (lấy từ `<interfaces>`), mỗi dòng: số thứ tự, tên trang, route path đầy đủ (vd `http://localhost:5173/registry`), checkbox PASS/FAIL, cột "Lỗi mong đợi KHÔNG được có" = "500 / CORS error / 401 sai khi đã login". Thêm 2 dòng EXCLUDED cho `/sync` + `/sync/review` ghi rõ "dự kiến lỗi /api/sync/* — KHÔNG tính PASS". Với mỗi trang có gap FIX-API còn lại từ 08-CONTRACT-DIFF (vd version history trên DocumentIngestion) — ghi chú "feature X có thể lỗi cục bộ (EXCLUDED v4.1), trang vẫn phải render khung".

**Section C — Golden path browser (SC2):** Đánh số bước: (1) login admin tại `/login`, (2) vào `/documents` upload 1 file DOCX vào hub_y_te, (3) vào `/search` chạy cross-hub search "khám bệnh", (4) chạy ask "quy trình là gì" → quan sát citation render `[1]` clickable trong CitationText.tsx, (5) vào `/logs` xác nhận thấy entry tương ứng. Mỗi bước checkbox + ô ghi chú quan sát.

**Section D — Tiêu chí PASS tổng:** 11 trang render OK + golden path browser PASS + compose 3-service healthy + không có endpoint UNCLASSIFIED ngoài nhóm EXCLUDED đã biết.
  </action>
  <verify>
    <automated>bash -n api/scripts/smoke/boot_stack.sh && test -f .planning/phases/08-frontend-e2e-smoke/08-SMOKE-CHECKLIST.md && grep -q "SyncQueue" .planning/phases/08-frontend-e2e-smoke/08-SMOKE-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `api/scripts/smoke/boot_stack.sh` tồn tại, `bash -n` (syntax check) exit 0
    - Script chứa `set -euo pipefail`, lệnh `docker compose up`, `curl` tới `/healthz`, kiểm `docker compose config --services`
    - `08-SMOKE-CHECKLIST.md` tồn tại với 4 section A/B/C/D
    - Checklist Section B liệt kê đủ 11 trang + 2 dòng EXCLUDED (`SyncQueue`, `SyncReview`)
    - Checklist Section C có 5 bước golden path + mục citation `[1]` clickable
  </acceptance_criteria>
  <done>Script boot tự động hoá phần kiểm được; checklist đầy đủ cho người verify.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plan 08-01..03 đã: (1) đối chiếu contract frontend↔FastAPI sinh `08-CONTRACT-DIFF.md`; (2) fix gap api-side — map port 8180, thêm router `/api/ai/chat` cho GeminiAssistant, CORS dev origin; (3) tự động hoá golden path API + Vietnamese filename qua pytest in-process (đều PASS). Plan 08-04 Task 1 tạo `boot_stack.sh` + `08-SMOKE-CHECKLIST.md`.

Còn lại phần CHỈ con người verify được: render UI 11 trang React thật trên trình duyệt + citation `[1]` clickable + docker compose lên healthy thật.
  </what-built>
  <how-to-verify>
**Bước 1 — Boot stack (SC5):**
1. Chạy `bash api/scripts/smoke/boot_stack.sh` từ `Hub_All/`. Mong đợi: exit 0, in "3 service healthy", `/healthz` trả `{"success":true,"data":{"status":"ok"}}`.
2. Nếu chưa có keypair JWT: chạy `make api-keys` (hoặc `bash api/scripts/generate_keys.sh`) trước.
3. Mở terminal khác: `cd frontend && npm install && npm run dev`. Mong đợi Vite in `Local: http://localhost:5173/`.

**Bước 2 — 11 trang render (SC1):** Mở trình duyệt, login admin tại http://localhost:5173/login. Lần lượt mở 11 route trong `08-SMOKE-CHECKLIST.md` Section B. Mỗi trang: mở DevTools Console + Network — đánh dấu PASS nếu trang render khung nội dung, KHÔNG có lỗi 500, KHÔNG lỗi CORS, KHÔNG 401 sai (đã login mà vẫn 401). Trang `/sync` + `/sync/review` BỎ QUA (EXCLUDED — dự kiến lỗi `/api/sync/*`).

**Bước 3 — Golden path browser (SC2):** Theo `08-SMOKE-CHECKLIST.md` Section C — login → upload DOCX vào hub_y_te tại `/documents` → cross-hub search "khám bệnh" tại `/search` → ask "quy trình khám bệnh là gì" → xác nhận câu trả lời render citation `[1]` và **click được** (CitationText.tsx) → vào `/logs` thấy entry audit.

**Bước 4 — Ghi kết quả:** Điền `08-SMOKE-CHECKLIST.md` (tick PASS/FAIL từng mục). Executor sau đó tạo `08-HUMAN-UAT.md` tổng hợp: trang nào PASS, trang nào FAIL kèm lỗi, mục nào EXCLUDED. Nếu có trang FAIL do gap thật (không thuộc EXCLUDED) → ghi thành gap để `/gsd-plan-phase 8 --gaps` xử lý.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu 11 trang render OK + golden path PASS + compose healthy. Nếu có FAIL, mô tả trang nào + lỗi gì (Console/Network) để quyết định gap closure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Tổng hợp 08-HUMAN-UAT.md từ kết quả checklist</name>
  <read_first>
    - .planning/phases/08-frontend-e2e-smoke/08-SMOKE-CHECKLIST.md (kết quả người verify vừa điền)
    - .planning/phases/08-frontend-e2e-smoke/08-CONTRACT-DIFF.md (phân loại gap để đối chiếu)
  </read_first>
  <action>
Sau khi checkpoint resume, đọc `08-SMOKE-CHECKLIST.md` đã điền và tạo `.planning/phases/08-frontend-e2e-smoke/08-HUMAN-UAT.md` — biên bản UAT Phase 8:

- **Section "Kết quả 5 Success Criteria":** Bảng 5 dòng SC1..SC5, mỗi dòng trạng thái PASS / FAIL / PARTIAL + nguồn chứng minh (08-03 test cho SC2/SC4 phần API, checklist cho SC1/SC5 + SC2 browser, 08-CONTRACT-DIFF cho SC3).
- **Section "11 trang":** Bảng trạng thái mỗi trang + 2 dòng EXCLUDED.
- **Section "Gap còn lại":** Liệt kê mọi FAIL không thuộc EXCLUDED — mỗi gap mô tả triệu chứng + endpoint nghi ngờ → đánh dấu là input cho `/gsd-plan-phase 8 --gaps`. Nếu KHÔNG có gap, ghi "Không có gap — Phase 8 đủ điều kiện đóng".
- **Section "COMPAT-01":** Kết luận requirement COMPAT-01 đạt hay chưa, dẫn chứng.

Nếu checkpoint kết quả toàn PASS — UAT verdict "PASS, Phase 8 sẵn sàng đóng, tiếp Phase 9". Nếu có gap — verdict "PARTIAL, cần gap closure".
  </action>
  <verify>
    <automated>test -f .planning/phases/08-frontend-e2e-smoke/08-HUMAN-UAT.md && grep -qE "SC1|SC5|COMPAT-01" .planning/phases/08-frontend-e2e-smoke/08-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `08-HUMAN-UAT.md` tồn tại với 4 section (5 SC / 11 trang / Gap / COMPAT-01)
    - Bảng 5 SC có trạng thái rõ ràng từng dòng
    - Có verdict cuối (PASS hoặc PARTIAL) + bước tiếp theo
  </acceptance_criteria>
  <done>Biên bản UAT Phase 8 hoàn chỉnh, verdict + gap (nếu có) ghi rõ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Vite dev :5173 → FastAPI :8180 | luồng request thật end-to-end qua CORS |
| docker compose host → container | port 8180 expose ra host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | CORS leak khi smoke (P12) | mitigate | Smoke chạy `app_env=dev` — CORS cho localhost hợp lệ; checklist Bước 2 yêu cầu quan sát Network tab phát hiện CORS error. Production validator đã chặn ở Plan 08-02. |
| T-08-04-02 | Spoofing | 401 flow sai khi smoke | mitigate | Checklist Bước 2 kiểm rõ: đã login mà trang trả 401 = FAIL (auth flow lỗi) — phân biệt với 401 đúng (chưa login). |
| T-08-04-03 | Elevation of Privilege | hub isolation rò khi smoke cross-hub | accept | Hub isolation E4 đã verify ở Phase 5/6 (test_hub_isolation 6/6 PASS) — Phase 8 smoke không re-test isolation; chỉ verify render. Không bề mặt mới. |

Không có threat HIGH — phase verify-only; mọi mitigation bảo mật đã đóng ở phase trước (P12 Plan 08-02, hub isolation Phase 5/6). Checklist chủ động yêu cầu quan sát CORS/401.
</threat_model>

<verification>
- `bash -n api/scripts/smoke/boot_stack.sh` syntax OK
- `08-SMOKE-CHECKLIST.md` + `08-HUMAN-UAT.md` tồn tại đủ section
- Checkpoint human-verify: 11 trang render PASS + golden path browser PASS + compose 3-service healthy
- `08-HUMAN-UAT.md` có verdict + gap list
</verification>

<success_criteria>
docker compose 3-service healthy không Go (SC5); 11 trang React render OK (SC1, SyncQueue EXCLUDED); golden path browser PASS với citation `[1]` clickable (SC2); biên bản UAT ghi verdict. COMPAT-01 kết luận. Nếu có gap → input cho gap closure.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08-frontend-e2e-smoke/08-04-SUMMARY.md`
</output>
