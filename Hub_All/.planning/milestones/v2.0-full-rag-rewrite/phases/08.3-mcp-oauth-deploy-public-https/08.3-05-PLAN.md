---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - Hub_All/mcp_service/mcp_app/oauth/store.py
  - Hub_All/mcp_service/mcp_app/oauth/provider.py
  - Hub_All/mcp_service/mcp_app/oauth/login.py
  - Hub_All/mcp_service/mcp_app/server.py
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "Authorization code bị claim nguyên tử (DELETE ... RETURNING) TRƯỚC khi save_token — code đã dùng / save_token lỗi giữa chừng đều không thể replay; code hết hạn bị từ chối ngay tại exchange_authorization_code"
    - "exchange_refresh_token rotate định vị theo refresh_token caller cầm, kiểm cursor.rowcount == 1 — refresh token reuse bị phát hiện và raise lỗi"
    - "complete_authorization từ chối pending transaction quá hạn (created_at < now - 600)"
    - "revoke_token tra cứu record nhất quán theo cả access lẫn refresh token rồi xoá theo access_token của record"
    - "Payload downstream thiếu field bị validate sớm → OAuthStoreError có kiểm soát thay vì KeyError 500; login_callback render lỗi hệ thống chung"
    - "oauth_tokens.refresh_token có unique index — load_token_by_refresh không nhập nhằng kể cả DB cũ"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/oauth/store.py"
      provides: "claim_auth_code (DELETE RETURNING) + rotate_token (rowcount check) + unique index refresh_token"
      contains: "async def claim_auth_code"
    - path: "Hub_All/mcp_service/mcp_app/oauth/provider.py"
      provides: "exchange code/refresh đúng đắn single-use + kiểm hạn code tại exchange + complete_authorization kiểm hạn pending + revoke_token chuẩn hoá + validate shape payload"
      contains: "claim_auth_code"
    - path: "Hub_All/mcp_service/mcp_app/oauth/login.py"
      provides: "login_callback bắt thêm KeyError/TypeError"
      contains: "except (KeyError, TypeError)"
    - path: "Hub_All/mcp_service/mcp_app/server.py"
      provides: "_call_api validate shape new_pair trước khi dùng"
      contains: "refresh_token"
  key_links:
    - from: "provider.exchange_authorization_code"
      to: "store.claim_auth_code"
      via: "claim TRƯỚC save_token"
      pattern: "claim_auth_code"
    - from: "provider.exchange_refresh_token"
      to: "store.rotate_token"
      via: "rotate theo refresh_token + kiểm rowcount"
      pattern: "rotate_token"
---

<objective>
Vá toàn bộ lỗi đúng đắn vòng đời token OAuth phát hiện ở 08.3-REVIEW.md (CR-01,
CR-02, WR-02, WR-03, WR-04, WR-05). Đây là gap closure cho Phase 8.3 — 4 plan gốc
(08.3-01..04) đã code-complete; VERIFICATION.md kết luận 4/5 Success Criteria đạt,
chỉ SC3 (token lifecycle) FAILED partial do 2 lỗi Critical RFC 6749.

Purpose: Đưa SC3 từ FAILED → VERIFIED — authorization code và refresh token thực
sự single-use; "token đã dùng" bị từ chối chắc chắn (RFC 6749 §10.4/§10.5); code
hết hạn không exchange được kể cả khi gọi provider trực tiếp.

Output: store.py có thao tác claim/rotate nguyên tử + unique index; provider.py
exchange code/refresh đúng đắn (kèm guard hạn code tại exchange),
complete_authorization kiểm hạn pending, revoke_token chuẩn hoá, validate shape
payload downstream; login.py + server.py xử lý KeyError/TypeError có kiểm soát.

LƯU Ý: Plan này KHÔNG thêm/sửa test (test ở Plan 06 — wave 2). Regression test
hiện có (78/78 PASS) PHẢI vẫn xanh sau plan này — task cuối chạy regression gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-VERIFICATION.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Chữ ký method hiện có executor cần biết — trích từ codebase, KHÔNG cần khám phá thêm. -->

oauth/store.py — method hiện có (sẽ bổ sung/sửa):
```python
async def init_schema(self) -> None            # tạo 4 bảng từ _SCHEMA_STATEMENTS, idempotent
async def load_auth_code(self, code: str) -> dict | None
async def delete_auth_code(self, code: str) -> None
async def save_token(self, access_token, refresh_token, client_id, scopes,
                     downstream_jwt, downstream_refresh_token, user_payload, expires_at) -> None
async def load_token(self, access_token: str) -> dict | None
async def load_token_by_refresh(self, refresh_token: str) -> dict | None
async def update_oauth_token(self, old_access_token, new_access_token,
                             new_refresh_token, expires_at) -> None  # lỗi CR-02 — sẽ thay bằng rotate_token
async def delete_token(self, access_token: str) -> None
async def load_pending(self, txn: str) -> dict | None  # trả dict có khoá "created_at"
def _require_conn(self) -> aiosqlite.Connection
```
- `load_auth_code` trả dict khoá: client_id, code_payload, downstream_jwt, downstream_refresh_token, user_payload, expires_at.
- `_SCHEMA_STATEMENTS` là tuple câu CREATE TABLE; `init_schema` lặp execute từng câu.

oauth/provider.py — hằng số + helper:
```python
_AUTH_CODE_TTL = 600          # đã có sẵn ở đầu module
@staticmethod _now() -> int   # int(time.time())
```
- `exchange_authorization_code` (dòng 201-239): hiện gọi load_auth_code → save_token → delete_auth_code.
- `exchange_refresh_token` (dòng 257-290): hiện gọi load_token_by_refresh → update_oauth_token.
- `complete_authorization` (dòng 132-176): load_pending → save_auth_code → delete_pending.
- `revoke_token` (dòng 314-326).

GHI CHÚ SDK (xác minh bằng inspect lúc revise plan — `mcp.server.auth.handlers.token`):
- SDK mcp 1.27 route `/token` (`TokenHandler.handle`) tự verify PKCE: tính
  `base64url(sha256(code_verifier))` so với `auth_code.code_challenge` → trả HTTP
  400 `invalid_grant` "incorrect code_verifier" nếu sai. Provider glue KHÔNG cần
  tự verify PKCE.
- SDK route `/token` cũng kiểm `auth_code.expires_at < time.time()` → trả 400
  `invalid_grant` "authorization code has expired". Đây là lớp phòng vệ khi đi
  qua route SDK; guard trong provider (mục (2) Task 2) bảo đảm code hết hạn bị
  từ chối kể cả khi `exchange_authorization_code` được gọi trực tiếp.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: store.py — claim_auth_code (DELETE RETURNING), rotate_token (rowcount), unique index refresh_token</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/store.py (file đang sửa — xem _SCHEMA_STATEMENTS, init_schema, load_auth_code map dict, update_oauth_token cũ)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md (code mẫu CR-01, CR-02, WR-05 — copy verbatim)
  </read_first>
  <action>
Sửa `Hub_All/mcp_service/mcp_app/oauth/store.py` — 3 thay đổi:

(1) CR-01 — thêm method `claim_auth_code` (đọc + xoá code nguyên tử bằng
`DELETE ... RETURNING`). Đặt ngay SAU `load_auth_code`, TRƯỚC `delete_auth_code`.
`delete_auth_code` GIỮ NGUYÊN (vẫn dùng cho cleanup / test cũ). Map dict trả về
GIỐNG HỆT `load_auth_code` (cùng 6 khoá):

```python
async def claim_auth_code(self, code: str) -> dict | None:
    """Đọc + xoá authorization code trong MỘT bước nguyên tử (DELETE ... RETURNING).

    Khác load_auth_code: claim_auth_code XOÁ code ngay khi đọc — nếu thao tác
    save_token kế tiếp lỗi, code KHÔNG còn trong store để replay (CR-01,
    RFC 6749 §10.5). Trả None nếu code không tồn tại (đã bị claim trước đó).
    Lưu ý: claim_auth_code KHÔNG kiểm expires_at — caller (provider) tự kiểm
    hạn sau khi claim.
    """
    conn = self._require_conn()
    async with conn.execute(
        "DELETE FROM oauth_auth_codes WHERE code = ? RETURNING "
        "client_id, code_payload, downstream_jwt, "
        "downstream_refresh_token, user_payload, expires_at",
        (code,),
    ) as cursor:
        row = await cursor.fetchone()
    await conn.commit()
    if row is None:
        return None
    return {
        "client_id": row[0],
        "code_payload": json.loads(row[1]),
        "downstream_jwt": row[2],
        "downstream_refresh_token": row[3],
        "user_payload": json.loads(row[4]),
        "expires_at": row[5],
    }
```

(2) CR-02 — thêm method `rotate_token` (UPDATE theo refresh_token + trả
`cursor.rowcount == 1`). Đặt ngay SAU `update_oauth_token` (giữ
`update_oauth_token` cũ để không vỡ test cũ; provider sẽ chuyển sang
`rotate_token`):

```python
async def rotate_token(
    self,
    old_refresh_token: str,
    new_access_token: str,
    new_refresh_token: str,
    expires_at: int,
) -> bool:
    """Rotate token OAuth định vị theo refresh_token (giá trị caller cầm).

    Trả True nếu UPDATE khớp đúng 1 dòng; False nếu refresh token đã bị dùng/
    rotate (rowcount != 1) — caller PHẢI coi đây là refresh-token reuse và từ
    chối (CR-02, RFC 6749 §10.4). Khác update_oauth_token: định vị theo
    refresh_token + kiểm rowcount thay vì WHERE access_token đã hết hạn.
    """
    conn = self._require_conn()
    cursor = await conn.execute(
        "UPDATE oauth_tokens SET access_token = ?, refresh_token = ?, "
        "expires_at = ? WHERE refresh_token = ?",
        (new_access_token, new_refresh_token, expires_at, old_refresh_token),
    )
    await conn.commit()
    return cursor.rowcount == 1
```

(3) WR-05 — thêm unique index cho cột `refresh_token`. SQLite KHÔNG cho thêm
UNIQUE constraint qua `ALTER TABLE`, nên dùng `CREATE UNIQUE INDEX IF NOT EXISTS`
— áp được cho cả DB cũ lẫn DB mới. Thêm câu lệnh này vào CUỐI tuple
`_SCHEMA_STATEMENTS` (sau câu CREATE TABLE oauth_pending), để `init_schema()` tự
chạy ở lifespan startup:

```python
    """
    CREATE UNIQUE INDEX IF NOT EXISTS idx_oauth_tokens_refresh
        ON oauth_tokens(refresh_token)
    """,
```

(SQLite coi nhiều NULL là khác nhau nên unique index không cản refresh_token
NULL — an toàn cho mọi record hiện có.)

KHÔNG sửa logic nào khác trong store.py. Giữ mọi query parametrized (T-08.3-04).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run python -c "import ast,sys; t=ast.parse(open('mcp_app/oauth/store.py',encoding='utf-8').read()); ns={n.name for n in ast.walk(t) if isinstance(n,ast.AsyncFunctionDef)}; sys.exit(0 if {'claim_auth_code','rotate_token'} <= ns else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `store.py` chứa chuỗi `async def claim_auth_code`
    - `store.py` chứa chuỗi `DELETE FROM oauth_auth_codes WHERE code = ? RETURNING`
    - `store.py` chứa chuỗi `async def rotate_token`
    - `store.py` chứa chuỗi `WHERE refresh_token = ?` (trong rotate_token UPDATE)
    - `store.py` chứa chuỗi `return cursor.rowcount == 1`
    - `store.py` chứa chuỗi `CREATE UNIQUE INDEX IF NOT EXISTS idx_oauth_tokens_refresh`
    - `delete_auth_code` và `update_oauth_token` vẫn tồn tại (không xoá)
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/store.py` exits 0
  </acceptance_criteria>
  <done>store.py có claim_auth_code (DELETE RETURNING), rotate_token (rowcount check), unique index refresh_token trong _SCHEMA_STATEMENTS; method cũ giữ nguyên; ruff sạch.</done>
</task>

<task type="auto">
  <name>Task 2: provider.py — exchange code/refresh đúng đắn, kiểm hạn code + pending, revoke chuẩn hoá, validate payload</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (file đang sửa — exchange_authorization_code dòng 201-239, exchange_refresh_token 257-290, complete_authorization 132-176, revoke_token 314-326)
    - Hub_All/mcp_service/mcp_app/oauth/store.py (chữ ký claim_auth_code / rotate_token vừa thêm ở Task 1 — provider phải gọi đúng)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md (code mẫu CR-01, CR-02, WR-02, WR-03, WR-04)
  </read_first>
  <action>
Sửa `Hub_All/mcp_service/mcp_app/oauth/provider.py` — 5 thay đổi:

(1) Thêm hằng số module cạnh `_AUTH_CODE_TTL` (đầu file, sau dòng
`_AUTH_CODE_TTL = 600`):

```python
# TTL (giây) của pending-authorize transaction — khớp mốc 600s trong
# OAuthStore.cleanup_expired. WR-02: complete_authorization phải kiểm hạn.
_PENDING_TTL = 600
```

(2) CR-01 + kiểm hạn code (issue 3) — sửa `exchange_authorization_code`. Thay
`load_auth_code` bằng `claim_auth_code` (claim nguyên tử TRƯỚC `save_token`), VÀ
sau khi claim phải kiểm `expires_at` trước khi phát token. Thân method từ dòng
`record = await self._store.load_auth_code(...)` đổi thành:

```python
        record = await self._store.claim_auth_code(authorization_code.code)
        if record is None:
            raise OAuthStoreError(
                "Authorization code không tồn tại hoặc đã dùng"
            )
        # Code đã bị xoá nguyên tử — nếu save_token lỗi, code KHÔNG còn để replay.
        # Kiểm hạn TẠI exchange: load_authorization_code đã kiểm expires_at,
        # nhưng exchange_authorization_code có thể bị gọi trực tiếp (bỏ qua
        # load_authorization_code) — claim_auth_code KHÔNG kiểm hạn. Vì vậy
        # phải tự reject code hết hạn ở đây (RFC 6749 §10.5).
        if record["expires_at"] < self._now():
            raise OAuthStoreError("Authorization code đã hết hạn")
```

XOÁ dòng `await self._store.delete_auth_code(authorization_code.code)` (không
còn cần — claim_auth_code đã xoá). Phần `save_token(...)` + `return OAuthToken(...)`
GIỮ NGUYÊN.

(3) CR-02 — sửa `exchange_refresh_token`. Thay `update_oauth_token` bằng
`rotate_token` (định vị theo refresh_token caller cầm + kiểm rowcount). Đoạn từ
`new_access = self._new_opaque()` đổi thành:

```python
        new_access = self._new_opaque()
        new_refresh = self._new_opaque()
        ok = await self._store.rotate_token(
            old_refresh_token=refresh_token.token,
            new_access_token=new_access,
            new_refresh_token=new_refresh,
            expires_at=self._now() + self._access_token_ttl,
        )
        if not ok:
            # rowcount != 1 — refresh token đã bị dùng/rotate (reuse detected).
            logger.info(
                "MedinetOAuthProvider refresh token reuse — client_id=%s",
                record["client_id"],
            )
            raise OAuthStoreError(
                "Refresh token đã dùng — vui lòng kết nối lại"
            )
        result_scopes = scopes or record["scopes"]
```

Phần `return OAuthToken(...)` GIỮ NGUYÊN.

(4) WR-02 — sửa `complete_authorization` kiểm hạn pending. Đoạn:
`pending = await self._store.load_pending(txn)` + `if pending is None:` đổi thành:

```python
        pending = await self._store.load_pending(txn)
        if pending is None or pending["created_at"] < self._now() - _PENDING_TTL:
            raise OAuthStoreError("Phiên authorize hết hạn hoặc không hợp lệ")
```

Đồng thời WR-04 — validate shape `login_data` TRƯỚC khi truy cập field. Ngay sau
khối kiểm pending ở trên, TRƯỚC `code = self._new_opaque()`, thêm:

```python
        required = ("access_token", "refresh_token", "user")
        if not isinstance(login_data, dict) or not all(
            login_data.get(k) for k in required
        ):
            raise OAuthStoreError(
                "Phản hồi login từ API Service thiếu trường bắt buộc"
            )
```

(Khối `save_auth_code(... downstream_jwt=login_data["access_token"] ...)` GIỮ
NGUYÊN — sau validate, truy cập trực tiếp an toàn.)

(5) WR-03 — sửa `revoke_token` chuẩn hoá tra cứu. Thay toàn bộ thân method
(sau docstring) bằng:

```python
        if isinstance(token, RefreshToken):
            record = await self._store.load_token_by_refresh(token.token)
        else:
            record = await self._store.load_token(token.token)
        if record is not None:
            await self._store.delete_token(record["access_token"])
        logger.info(
            "MedinetOAuthProvider revoke token, client_id=%s", token.client_id
        )
```

KHÔNG sửa method nào khác. KHÔNG log giá trị token/code/JWT thô (T-08.3-08).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `provider.py` chứa chuỗi `_PENDING_TTL = 600`
    - `provider.py` chứa chuỗi `claim_auth_code(authorization_code.code)`
    - `provider.py` chứa chuỗi `record["expires_at"] < self._now()` và `Authorization code đã hết hạn` (guard hạn code tại exchange — issue 3)
    - `provider.py` KHÔNG còn chuỗi `delete_auth_code(authorization_code.code)`
    - `provider.py` chứa chuỗi `rotate_token(`
    - `provider.py` chứa chuỗi `old_refresh_token=refresh_token.token`
    - `provider.py` chứa chuỗi `if not ok:` và `Refresh token đã dùng`
    - `provider.py` KHÔNG còn chuỗi `update_oauth_token(` (đã thay bằng rotate_token)
    - `provider.py` chứa chuỗi `pending["created_at"] < self._now() - _PENDING_TTL`
    - `provider.py` chứa chuỗi `required = ("access_token", "refresh_token", "user")`
    - `provider.py` chứa chuỗi `load_token_by_refresh(token.token)` và `load_token(token.token)` trong revoke_token
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/provider.py` exits 0
    - `cd Hub_All/mcp_service && uv run python -c "import mcp_app.oauth.provider"` exits 0 (import sạch)
  </acceptance_criteria>
  <done>provider.py: exchange_authorization_code dùng claim_auth_code + reject code hết hạn; exchange_refresh_token dùng rotate_token + raise khi reuse; complete_authorization kiểm hạn pending + validate shape login_data; revoke_token tra cứu chuẩn hoá; ruff + import sạch.</done>
</task>

<task type="auto">
  <name>Task 3: login.py + server.py — xử lý KeyError/TypeError có kiểm soát, regression gate</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/login.py (file đang sửa — login_callback dòng 93-134, khối try/except quanh complete_authorization)
    - Hub_All/mcp_service/mcp_app/server.py (file đang sửa — _call_api dòng 215-234, truy cập new_pair["access_token"]/["refresh_token"])
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md (WR-04 — code mẫu validate shape)
  </read_first>
  <action>
WR-04 phần còn lại — hai file:

(1) `Hub_All/mcp_service/mcp_app/oauth/login.py` — `login_callback`: khối
`try: ... except OAuthStoreError:` quanh `provider.complete_authorization` hiện
chỉ bắt `OAuthStoreError`. Bổ sung nhánh bắt `KeyError`/`TypeError` (phòng vệ
trường hợp shape downstream lọt qua) — render lỗi hệ thống chung. Đổi khối
`except OAuthStoreError:` thành hai except:

```python
        except OAuthStoreError:
            logger.info("Login callback — txn hết hạn hoặc không hợp lệ")
            return render_login_form(
                txn, error="Phiên đăng nhập hết hạn, vui lòng kết nối lại"
            )
        except (KeyError, TypeError):
            logger.error("Login callback — payload downstream thiếu trường bắt buộc")
            return render_login_form(
                txn, error="Lỗi hệ thống, vui lòng thử lại sau"
            )
```

(KHÔNG log giá trị payload thô — chỉ log loại lỗi, T-08.3-08.)

(2) `Hub_All/mcp_service/mcp_app/server.py` — `_call_api`: sau
`new_pair = await client.refresh_jwt(...)` và khối `if new_pair is None: raise`,
TRƯỚC `await store.update_downstream_jwt(...)`, thêm validate shape:

```python
        if not all(new_pair.get(k) for k in ("access_token", "refresh_token")):
            logger.error("refresh_jwt trả payload thiếu access/refresh token")
            raise ApiUnauthorizedError(
                "Phản hồi refresh JWT thiếu trường bắt buộc"
            )
```

(Sau validate, `new_pair["access_token"]` / `new_pair["refresh_token"]` truy
cập trực tiếp an toàn — không đổi phần còn lại.)

XÁC NHẬN `ApiUnauthorizedError` đã được import ở đầu server.py (đã dùng trong
`except ApiUnauthorizedError` cùng hàm — chỉ tái dùng, không thêm import).

(3) Regression gate — sau khi sửa xong toàn bộ Plan 05 (Task 1+2+3), chạy:

```
cd Hub_All/mcp_service && uv run pytest -q
```

Test suite hiện 78/78 PASS. Plan 05 KHÔNG thêm test mới — nhưng các thay đổi
phải KHÔNG làm vỡ test cũ. Nếu test cũ vỡ vì hợp đồng đổi (ví dụ test gọi
`update_oauth_token` trực tiếp): KHÔNG nới lỏng fix — chỉnh test cho khớp hành vi
đúng đắn mới, hoặc nếu test cũ kiểm chính lỗi đang vá thì cập nhật kỳ vọng test
cho đúng RFC. Ghi rõ test nào đã chỉnh và vì sao trong SUMMARY. Mục tiêu: pytest
exit 0.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `login.py` chứa chuỗi `except (KeyError, TypeError):`
    - `login.py` nhánh except mới render `render_login_form(` với thông báo lỗi hệ thống
    - `server.py` chứa chuỗi `new_pair.get(k) for k in ("access_token", "refresh_token")`
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/login.py mcp_app/server.py` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q` exits 0 (regression không vỡ)
  </acceptance_criteria>
  <done>login_callback bắt thêm KeyError/TypeError render lỗi chung; server._call_api validate shape new_pair trước khi dùng; regression mcp_service pytest exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|----------|-------|
| Claude web (OAuth client) → MCP Service `/token` | Authorization code / refresh token client gửi — không tin cậy; có thể bị chặn/replay/dùng đồng thời |
| API Service → MCP Service (`api_client.login`/`refresh_jwt` response) | Payload downstream — shape có thể đổi hoặc thiếu field |
| Người dùng cuối → MCP Service `/login` (txn) | txn nhúng trong URL/HTML — có thể bị lưu lịch sử/log proxy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-21 | Spoofing/Elevation | `exchange_authorization_code` (provider.py) — authorization code replay / code hết hạn vẫn exchange | mitigate | Task 1+2: `claim_auth_code` dùng `DELETE ... RETURNING` claim code nguyên tử TRƯỚC `save_token`; code đã claim → None → raise OAuthStoreError. Sau claim kiểm `expires_at < now` → reject code hết hạn kể cả khi gọi exchange trực tiếp (RFC 6749 §10.5) |
| T-08.3-22 | Elevation/Tampering | `exchange_refresh_token` (provider.py) — refresh token double-spend / token mồ côi | mitigate | Task 1+2: `rotate_token` UPDATE theo `refresh_token` caller cầm + trả `cursor.rowcount == 1`; rowcount != 1 → provider raise lỗi reuse thay vì trả token không tồn tại trong store (RFC 6749 §10.4) |
| T-08.3-23 | Elevation | `complete_authorization` (provider.py) — pending txn replay window vô hạn | mitigate | Task 2: kiểm `pending["created_at"] < now - _PENDING_TTL` (600s) → raise nếu hết hạn; thu hẹp cửa sổ tấn công cho txn bị lộ (WR-02) |
| T-08.3-24 | Information Disclosure | `login_callback` / `_call_api` — KeyError lọt thành HTTP 500 + stack trace | mitigate | Task 2+3: validate shape payload downstream sớm → OAuthStoreError/ApiUnauthorizedError có kiểm soát; login_callback bắt `(KeyError, TypeError)` render thông báo chung — không lộ stack trace (T-08.3-08, WR-04) |
| T-08.3-25 | Tampering | `oauth_tokens.refresh_token` không UNIQUE — `load_token_by_refresh` nhập nhằng | mitigate | Task 1: `CREATE UNIQUE INDEX IF NOT EXISTS idx_oauth_tokens_refresh` áp cho cả DB cũ lẫn mới; loại nhập nhằng tra cứu refresh token (WR-05) |
| T-08.3-26 | Repudiation | Log thao tác token | accept | Provider chỉ log `client_id` + loại thao tác + trạng thái (`reuse`/`expired`); KHÔNG log giá trị token/JWT/code thô — phù hợp T-08.3-08, rủi ro thấp |
</threat_model>

<verification>
- `claim_auth_code` + `rotate_token` tồn tại trong store.py, dùng `DELETE ... RETURNING` / `WHERE refresh_token = ?` + `cursor.rowcount == 1`.
- `provider.py` exchange_authorization_code gọi `claim_auth_code` + reject code hết hạn (`expires_at < now`); exchange_refresh_token gọi `rotate_token` + raise khi `not ok`.
- `complete_authorization` kiểm `created_at < now - _PENDING_TTL` + validate shape `login_data`.
- `revoke_token` tra cứu theo cả access lẫn refresh.
- `login_callback` bắt `(KeyError, TypeError)`; `server._call_api` validate `new_pair`.
- `cd Hub_All/mcp_service && uv run ruff check mcp_app/` exits 0.
- `cd Hub_All/mcp_service && uv run pytest -q` exits 0 — regression 78/78 không vỡ (test mới ở Plan 06).
</verification>

<success_criteria>
- CR-01, CR-02, WR-02, WR-03, WR-04, WR-05 đều được vá theo code mẫu 08.3-REVIEW.md.
- Authorization code single-use kể cả khi save_token lỗi (claim nguyên tử); code hết hạn bị từ chối tại exchange_authorization_code.
- Refresh token reuse bị phát hiện qua rowcount → raise lỗi, không trả token mồ côi.
- Pending txn quá hạn bị từ chối; payload downstream thiếu field không gây 500.
- Regression mcp_service pytest exit 0.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-05-SUMMARY.md`.
Commit atomic: `fix(08.3-05): vá lỗi đúng đắn vòng đời token OAuth (CR-01/CR-02/WR-02..05)`.
</output>
</content>
</invoke>
