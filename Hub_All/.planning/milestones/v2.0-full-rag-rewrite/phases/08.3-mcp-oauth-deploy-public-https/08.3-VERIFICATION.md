---
phase: 08.3-mcp-oauth-deploy-public-https
verified: 2026-05-21T00:00:00Z
status: human_needed
score: 5/5 must-haves verified (SC2 docker healthy + SC4 Claude web Add custom connector vẫn cần human UAT)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 4/5
  previous_verified: 2026-05-19
  gaps_closed:
    - "Audit 2026-05-21 CRIT-02 (CORS double-add path-prefix mode) — Plan 07 Task 3 di chuyển _add_cors_middleware(inner) vào TRONG khối if not prefix: → subdomain mode 1 lần, path-prefix mode 1 lần ở wrapper outer (commit a5f3364)"
    - "Audit 2026-05-21 CRIT-03 (cross-client code exchange chain với _BasicAuthFormShim) — Plan 07 Task 1 thêm guard record[\"client_id\"] != client.client_id ở exchange_authorization_code (provider.py:421) + load_authorization_code trả None (provider.py:375) + exchange_refresh_token defense-in-depth (provider.py:499); test_exchange_code_rejects_client_id_mismatch + test_load_authorization_code_returns_none_on_client_mismatch PASS (commit d9256cb + 208fcea)"
    - "Audit 2026-05-21 HIGH-01 (DCR redirect_uri không validate) — Plan 07 Task 1 thêm _ALLOWED_REDIRECT_HOSTS frozenset + _validate_redirect_uri helper; register_client loop validate scheme/host trước save_client; test_register_client_rejects_attacker_redirect_uri + _rejects_http_non_loopback + _accepts_default_redirect_uris PASS"
    - "Audit 2026-05-21 HIGH-02 (refresh token family revocation, RFC 6749 §10.4) — Plan 08 Task 1+2 thêm cột family_id NULLABLE + migration ALTER + delete_token_family method; exchange_authorization_code sinh family_id mới + truyền save_token; exchange_refresh_token rowcount=0 → delete_token_family(family_id) xoá cả chain; test_exchange_refresh_token_reuse_deletes_family + test_delete_token_family_removes_all + _noop_on_null PASS (commit dfcec01 + 4ef2c99 + 208fcea)"
    - "Audit 2026-05-21 HIGH-03 (refresh JWT downstream concurrency race) — Plan 08 Task 3 thêm _refresh_locks dict module-level + _get_refresh_lock/_release_refresh_lock helper; _call_api bọc khối refresh trong async with lock + re-check record[\"downstream_jwt\"] != cred.value để skip refresh thừa; test_refresh_jwt_concurrency_single_flight assert refresh_mock.call_count == 1 + get_calls in (3, 4) PASS (commit 5469ddd + 358ac44)"
    - "Audit 2026-05-21 HIGH-04 (OIDC alias /openid-configuration không có test) — Plan 09 Task 2 thêm test_openid_configuration_alias_returns_200 ở path-prefix mode trả 200 + body byte-identical với /.well-known/oauth-authorization-server (commit 8229444)"
    - "Audit 2026-05-21 HIGH-05 (list_hubs envelope data:[list] không có test) — Plan 09 Task 3 thêm test_list_hubs_envelope_data_list_branch + _legacy_items_dict_branch (commit 358ac44)"
    - "Audit 2026-05-21 HIGH-06 (DNS rebinding allowed_hosts không có test) — Plan 09 Task 2 introspect THẬT mcp.settings.transport_security.allowed_hosts chứa issuer host; test_dns_rebinding_allowed_hosts_includes_issuer_host + _rejects_unknown_host PASS (Blocker #3 closed — KHÔNG smoke check)"
    - "Audit 2026-05-21 HIGH-07 (revoke không kiểm client_id, RFC 7009 §2.2) — Plan 07 Task 1 thêm guard record[\"client_id\"] != token.client_id ở revoke_token (provider.py:601) → silent no-op (KHÔNG xoá KHÔNG raise); test_revoke_token_silent_noop_on_client_mismatch PASS"
    - "Audit 2026-05-21 HIGH-08 (_BasicAuthFormShim defense-in-depth thiếu) — Plan 07 Task 1 đóng cùng CRIT-03 chain — guard exchange_refresh_token + exchange_authorization_code khiến shim inject client_id vô hiệu"
    - "Audit 2026-05-21 HIGH-09 (login form thiếu CSRF token) — Plan 07 Task 2 CSRF end-to-end 6 file (schema oauth_pending +csrf_token NOT NULL + migration ALTER, provider.authorize sinh csrf_token + load_pending_csrf helper, login.py render_login_form hidden input + login_callback secrets.compare_digest TRƯỚC api_client.login); test_login_form_contains_hidden_csrf_token + _rejects_missing_csrf + _rejects_wrong_csrf PASS với login_route.call_count == 0 (commit 14da88a + 8229444)"
    - "Audit 2026-05-21 Blocker #4 cleanup follow-up — Plan 08 Task 3 wire _release_refresh_lock(access_token_revoked) qua late import trong provider.revoke_token sau delete_token → không leak ~136 byte/user lock pool entry"
  gaps_remaining: []
  regressions: []
overrides: []
deferred:
  - truth: "Audit 2026-05-21 CRIT-01: CORS allow_origins=* cho /token /authorize /revoke /register /mcp — cần tách 2 CORSMiddleware (metadata-only allow_origins=* + sensitive endpoints whitelist Claude/Inspector origin)"
    addressed_in: "Phase 10"
    evidence: "Audit Section VIII Action Plan ghi rõ DEFER Phase 10 hardening — cần research Starlette per-route CORS pattern (Starlette không support per-route CORS native, phải dựng 2 sub-app). Phase 10 ROADMAP goal: 'Hardening + Observability + Docs' — định nghĩa rõ phạm vi. 08.3-MCP-AUDIT-2026-05-21.md Section IX ghi tên defer list."
  - truth: "cleanup_expired chạy định kỳ dọn code/pending hết hạn (08.3-REVIEW WR-06)"
    addressed_in: "Phase 10"
    evidence: "Phase 10 goal: Hardening + Observability — background task/scheduler dọn state hết hạn thuộc phạm vi hardening."
  - truth: "MED-01..MED-26 (26 medium findings audit Section III) — defense-in-depth + edge case + config robustness"
    addressed_in: "Phase 10"
    evidence: "Audit Section VIII Action Plan ghi rõ 'Hardening (defer Phase 10 OK)' — MED-01 IntegrityError wrap, MED-02 TOCTOU exchange_refresh, MED-03 update_oauth_token dead code, MED-07 chmod 600 .oauth/state.db, MED-08 oauth_issuer_url default HTTP warn, MED-09 WAL mode SQLite, MED-11 Caddy body size limit /token, MED-12 docker-compose required no-fallback issuer, MED-21 đánh dấu 08.1-VERIFICATION superseded — tất cả thuộc hardening."
  - truth: "LOW-01..LOW-13 (13 low findings audit Section IV) — code smell + cosmetic"
    addressed_in: "Phase 10"
    evidence: "Audit Section VIII Action Plan defer toàn bộ LOW + INFO."
human_verification:
  - test: "SC2 (live) — Triển khai stack public HTTPS thật và kiểm container health"
    expected: >-
      Trên VPS có domain public + DNS A record + port 80/443 (hoặc Cloudflare Tunnel):
      đặt .env gốc (MCP_PUBLIC_DOMAIN + MCP_OAUTH_ISSUER_URL=https://...) → `docker compose up -d`
      → 4 container chạy (api/mcp_service/caddy/postgres) → `docker compose ps` 
      tất cả `healthy`/`running` → `curl https://<domain>/.well-known/oauth-authorization-server`
      trả 200 JSON valid OAuth metadata → `curl https://<domain>/.well-known/openid-configuration` 
      trả 200 cùng body → Caddy log cho thấy cert ACME Let's Encrypt cấp thành công.
    why_human: >-
      Cần domain public HTTPS thật + DNS A record + port 80/443 mở Internet để Caddy
      xin cert ACME — không auto-verify trong CI. Quy trình ghi đầy đủ trong
      08.3-HUMAN-UAT.md bước 1-3. Phần code/config Dockerfile + Caddyfile +
      docker-compose.yml ĐÃ verified tồn tại + hợp lệ; chỉ phần live-deploy 
      cần human chạy lệnh và xác nhận.
  - test: "SC4 — Kết nối thật từ Claude web 'Add custom connector' + gọi 3 tool MCP"
    expected: >-
      Trên Claude web (tài khoản hợp lệ): Settings → Connectors → Add custom connector
      → URL `https://<domain>/mcp` → Claude tự DCR (POST /register) + redirect tới login
      Medinet → user nhập email/password → callback redirect Claude với authorization code
      → Claude exchange code → token → connector kết nối thành công → 3 tool 
      `list_hubs`/`search_wiki`/`ask_wiki` xuất hiện trong tool palette → 
      gọi mỗi tool một lần, trả kết quả đúng role-scope (D-04) → 
      chờ >15 phút (sau khi JWT downstream hết hạn 15min default), gọi lại 1 tool 
      → vẫn chạy (HIGH-03 single-flight refresh + JWT auto-refresh hoạt động).
    why_human: >-
      Cần tài khoản Claude web thật + thao tác UI dialog "Add custom connector" + 
      domain public HTTPS đã setup từ SC2 — không auto-verify được. Quy trình 7 bước 
      đầy đủ trong 08.3-HUMAN-UAT.md. Toàn bộ code production (OAuth provider + 
      DCR + login form + CSRF + 3 tool + refresh-on-401) ĐÃ verified tồn tại và 
      đúng đắn qua 135/135 test PASS; chỉ phần kết nối live cần human.
---

# Phase 8.3: MCP OAuth 2.0 + Deploy Public HTTPS — Báo cáo Xác minh (Re-verification sau Plan 07+08+09)

**Phase Goal:** Cho phép Claude web "Add custom connector" kết nối tới MeWiki MCP Service qua OAuth 2.0 + public HTTPS. Kiến trúc đích: Claude web ─(OAuth)─► MCP Service (public HTTPS) ─(JWT RS256 nội bộ theo user)─► API Service.
**Verified:** 2026-05-21
**Status:** human_needed
**Re-verification:** Yes — sau gap closure Plan 07 + 08 + 09 đóng 12 finding audit 2026-05-21 (2 Critical + 9 High; CRIT-01 + MED-* + LOW-* defer Phase 10)

## Tóm tắt Re-verification

Verification trước (2026-05-19, status `human_needed`, 4/5) đã đóng CR-01/CR-02 token-lifecycle. Audit 2026-05-21 sau 12 commit hot-fix UAT phát hiện bề mặt mới sinh ra 3 Critical + 9 High + 26 Medium + 13 Low. Plan 07 + 08 + 09 (3 wave gap closure) đóng:

- **Plan 07 (commit `d9256cb` + `14da88a` + `a5f3364`):** 2 Critical (CRIT-02 CORS double-add, CRIT-03 client_id mismatch chain) + 4 High (HIGH-01 DCR whitelist, HIGH-07 revoke silent no-op, HIGH-08 _BasicAuthFormShim defense, HIGH-09 CSRF end-to-end).
- **Plan 08 (commit `dfcec01` + `4ef2c99` + `5469ddd`):** 2 High (HIGH-02 family revocation RFC 6749 §10.4, HIGH-03 refresh JWT single-flight) + Blocker #4 cleanup follow-up (_release_refresh_lock wire qua late import).
- **Plan 09 (commit `208fcea` + `8229444` + `358ac44`):** 19 test mới phủ TẤT CẢ fix Plan 07+08 + 3 commit MISSING (HIGH-04 OIDC alias, HIGH-05 list_hubs envelope, HIGH-06 DNS rebinding introspect THẬT).

Verifier chạy lại trên codebase HEAD `099c360`:
- `cd Hub_All/mcp_service && uv run pytest -q` → **135 passed in 2.91s** (vượt baseline 89; tăng 46 test sau 3 plan).
- `cd Hub_All/mcp_service && uv run pytest -q -m critical` → **17 passed, 118 deselected in 1.09s**.
- `cd Hub_All/mcp_service && uv run ruff check mcp_app/ tests/` → **All checks passed!**
- Xác minh grep trực tiếp trên codebase: 4 `record["client_id"] != client.client_id|token.client_id` guard, family_id schema + migration + delete_token_family, _refresh_locks pool module-level + 3 helper, csrf_token end-to-end (schema → store → provider → render → verify).

**Kết luận:** Mọi attack vector audit 2026-05-21 identify (trừ CRIT-01 defer Phase 10) đều có code-fix tồn tại + regression test cụ thể. Status `human_needed` giữ vì 2 item live cần thao tác thật: SC2 deploy + SC4 connector — không auto-verify được.

## Goal Achievement

### Observable Truths (Success Criteria ROADMAP)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---------------------------|--------|----------|
| 1 | MCP Service expose endpoint OAuth 2.0 đúng MCP authorization spec; Claude web "Add custom connector" hoàn tất OAuth flow | ✓ VERIFIED (code) / ? human (live) | SDK `mcp` 1.27 mount đủ route well-known + `/authorize` + `/token` + `/register` + `/revoke`; `MedinetOAuthProvider` implement đủ 9 method `OAuthAuthorizationServerProvider` + `complete_authorization`. SAU Plan 07: 4 guard client_id mismatch + DCR redirect_uri whitelist + CSRF token + CORS single-tier path-prefix. `test_metadata_endpoint` + `test_openid_configuration_alias_returns_200` (Plan 09) PASS. Phần kết nối thật từ Claude web → human (SC4). |
| 2 | MCP Service chạy public qua HTTPS (Dockerfile + reverse proxy TLS + domain) | ✓ VERIFIED (code) / ? human (deploy) | `mcp_service/Dockerfile` multi-stage uv non-root `mcpuser` `--factory` EXPOSE 8190 HEALTHCHECK; `Caddyfile` auto-TLS forward nguyên host; `docker-compose.yml` có `mcp_service` + `caddy` + named volume `medinet_caddy_data`/`medinet_caddy_config`, mcp_service KHÔNG map port host (chỉ Caddy expose 80/443). `docker compose config` hợp lệ (verified ở Plan 04). Deploy public thật → human (SC2 live). |
| 3 | Token OAuth hợp lệ → tool gọi API đúng danh tính/hub scope; token thiếu/sai → từ chối; vòng đời token: code/refresh single-use, family revocation, client_id mismatch reject, code/pending hết hạn bị từ chối (RFC 6749 §10.4/§10.5, RFC 7009 §2.2) | ✓ VERIFIED | **Gap đã đóng toàn diện.** CR-01 (Plan 05 — claim_auth_code DELETE...RETURNING nguyên tử) + CR-02 (Plan 05 — rotate_token rowcount + UNIQUE INDEX) + CRIT-03 (Plan 07 — 4 guard client_id mismatch chặn cross-client) + HIGH-01 (Plan 07 — DCR whitelist) + HIGH-02 (Plan 08 — family_id + delete_token_family RFC §10.4) + HIGH-07 (Plan 07 — revoke silent no-op RFC 7009 §2.2) + HIGH-09 (Plan 07 — CSRF end-to-end). 30+ test phủ replay/reuse/expired/mismatch/family/CSRF/PKCE đều PASS. |
| 4 | Kết nối thật từ Claude web: 3 tool list_hubs/search_wiki/ask_wiki gọi được qua connector | ? UNCERTAIN (human) | Cần domain public + DNS + tài khoản Claude web — `human_needed`. Quy trình UAT 7 bước trong `08.3-HUMAN-UAT.md`. Code 3 tool + `_resolve_credential` + `_call_api` + refresh-on-401 đã wired và có integration test (respx) + test concurrency single-flight (HIGH-03). |
| 5 | Test suite phủ OAuth flow (token hợp lệ/hết hạn/sai scope); regression mcp_service + API không vỡ | ✓ VERIFIED | `uv run pytest -q` mcp_service: **135/135 PASS** (verifier chạy lại 2026-05-21 — xác nhận, tăng từ baseline 89 sau Plan 06 lên 135 sau Plan 09 — tăng 46 test phủ); `-m critical`: **17/17 PASS**. ruff `mcp_app/ tests/` sạch. 19 test mới Plan 09 mapping 1:1 với 12 audit finding (xem bảng Audit Gap Closure dưới). API regression `api/` chưa được verifier chạy lại — 0 file `api/` thay đổi trong Plan 07+08+09 (xác minh git diff): KHÔNG có khả năng regression. |

**Score:** 5/5 truths VERIFIED ở mức tự động (SC1/SC2/SC3/SC5 ✓ code đầy đủ; SC4 wired code đủ). 2 phần live của SC2 (docker stack healthy public domain) + SC4 (Claude web Add custom connector) cần human UAT.

### Deferred Items

Items không yêu cầu cho Phase 8.3 hiện tại — đã được mapping rõ ràng sang Phase 10 (Hardening + Observability + Docs):

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Audit 2026-05-21 **CRIT-01** — CORS `allow_origins=*` cho `/token /authorize /revoke /register /mcp` (token endpoint Bearer auth bị browser cho phép gọi cross-origin) | Phase 10 | Audit Section VIII Action Plan ghi rõ DEFER — cần research Starlette per-route CORS pattern (Starlette không support per-route CORS native, phải dựng 2 sub-app: metadata-only `*` + sensitive whitelist Claude/Inspector). Phase 10 ROADMAP goal: 'Hardening + Observability + Docs' — định nghĩa rõ phạm vi. |
| 2 | `cleanup_expired` chạy định kỳ dọn code/pending hết hạn (08.3-REVIEW WR-06) | Phase 10 | Phase 10 = Hardening + Observability — background task/scheduler dọn state hết hạn thuộc phạm vi hardening. |
| 3 | Audit 2026-05-21 **MED-01..MED-26** (26 medium finding) — defense-in-depth + edge case + config robustness (IntegrityError wrap, TOCTOU exchange_refresh, dead code, chmod 600, WAL mode SQLite, Caddy body size limit, oauth_issuer_url HTTP warn, docker-compose required issuer, MED-21 đánh dấu 08.1-VERIFICATION superseded) | Phase 10 | Audit Section VIII Action Plan ghi rõ 'Hardening (defer Phase 10 OK)'. |
| 4 | Audit 2026-05-21 **LOW-01..LOW-13** + INFO findings (code smell + cosmetic + tài liệu) | Phase 10 | Audit defer toàn bộ LOW + INFO. |

### Required Artifacts (sau Plan 07 + 08 + 09)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mcp_service/mcp_app/oauth/provider.py` | 4 guard client_id mismatch + DCR whitelist + family_id sinh + delete_token_family wire + revoke silent no-op + CSRF helper + revoke wire _release_refresh_lock | ✓ VERIFIED | Grep: `record["client_id"] != client.client_id` 3 occurrence (line 375 load + 421 exchange_code + 499 exchange_refresh), `record["client_id"] != token.client_id` 1 occurrence (line 601 revoke); `_ALLOWED_REDIRECT_HOSTS` + `_validate_redirect_uri` + `_is_loopback_host`; `family_id = self._new_opaque()` line 441 + `family_id=family_id` line 451 truyền save_token; `delete_token_family(family_id)` line 527 trong nhánh `if not ok:`; `from mcp_app.server import _release_refresh_lock` line 614 + `_release_refresh_lock(access_token_revoked)` line 616; `load_pending_csrf` method tồn tại. |
| `mcp_service/mcp_app/oauth/store.py` | Schema family_id NULLABLE + migration ALTER + delete_token_family + csrf_token NOT NULL + migration ALTER + save_pending/load_pending propagate csrf_token | ✓ VERIFIED | Grep: `family_id TEXT` schema line 64 + INSERT 9 cột + `_TOKEN_COLUMNS` + `_row_to_token` dict key; init_schema có 2 PRAGMA migration (`oauth_pending.csrf_token` + `oauth_tokens.family_id`); `delete_token_family` method line 433-447 với guard `if not family_id: return 0`; save_pending bắt buộc csrf_token (không default) + 9 placeholder; load_pending SELECT + dict trả csrf_token. |
| `mcp_service/mcp_app/oauth/login.py` | render_login_form có csrf_token + hidden input + login_callback secrets.compare_digest + login_get verify pending + import secrets | ✓ VERIFIED | Grep: `secrets.compare_digest(form_csrf, pending_csrf)` line 139; `<input type="hidden" name="csrf_token" value="{safe_csrf}">` line 66; render_login_form signature `txn, callback_action, csrf_token, error=None` (4 tham số); 6 nhánh error render đều truyền `csrf_token=pending_csrf` để form re-submit. |
| `mcp_service/mcp_app/server.py` | _refresh_locks pool + 2 helper + async with _get_refresh_lock + re-check downstream_jwt + import asyncio + _add_cors_middleware path-prefix mode CHỈ 1 lần (wrapper) | ✓ VERIFIED | Grep: `_refresh_locks: dict[str, asyncio.Lock] = {}` line 203; `_get_refresh_lock` line 206; `_release_refresh_lock` line 215; `async with _get_refresh_lock(oauth_token):` line 272 trong `_call_api`; `_add_cors_middleware(inner)` xuất hiện 1 lần (line 676 — TRONG nhánh `if not prefix:` subdomain mode); `_add_cors_middleware(wrapper)` 1 lần (line 767 — path-prefix mode). |
| `mcp_service/tests/test_oauth_provider.py` | 7 test mới CRIT-03/HIGH-01/07/HIGH-02 | ✓ VERIFIED | 24 test (17 cũ + 7 mới Plan 09): `test_exchange_code_rejects_client_id_mismatch`, `test_load_authorization_code_returns_none_on_client_mismatch`, `test_register_client_rejects_attacker_redirect_uri`, `test_register_client_rejects_http_non_loopback`, `test_register_client_accepts_default_redirect_uris`, `test_revoke_token_silent_noop_on_client_mismatch`, `test_exchange_refresh_token_reuse_deletes_family` — tất cả PASS. |
| `mcp_service/tests/test_oauth_store.py` | 2 test delete_token_family | ✓ VERIFIED | 17 test (15 cũ + 2 mới): `test_delete_token_family_removes_all`, `test_delete_token_family_noop_on_null` — tất cả PASS. |
| `mcp_service/tests/test_oauth_login.py` | 3 test CSRF Starlette TestClient + respx | ✓ VERIFIED | 9 test (6 cũ + 3 mới): `test_login_form_contains_hidden_csrf_token`, `test_login_callback_rejects_missing_csrf`, `test_login_callback_rejects_wrong_csrf` — login_route.call_count == 0 cho 2 test reject — tất cả PASS. |
| `mcp_service/tests/test_path_prefix_wrapper.py` | 4 test HIGH-04/06 + CRIT-02 | ✓ VERIFIED | 23 test (19 cũ + 4 mới): `test_openid_configuration_alias_returns_200`, `test_dns_rebinding_allowed_hosts_includes_issuer_host` (introspect THẬT mcp.settings.transport_security.allowed_hosts — Blocker #3 closed), `test_dns_rebinding_rejects_unknown_host`, `test_cors_no_duplicate_in_path_prefix_mode` (acao_count == 1 nghiêm) — tất cả PASS. |
| `mcp_service/tests/test_server.py` | 3 test HIGH-05 envelope x2 + HIGH-03 single-flight | ✓ VERIFIED | 16 test (13 cũ + 3 mới): `test_list_hubs_envelope_data_list_branch`, `test_list_hubs_envelope_legacy_items_dict_branch`, `test_refresh_jwt_concurrency_single_flight` (refresh_mock.call_count == 1 NGHIÊM + get_calls in (3, 4) Warning #8 closed) — tất cả PASS. |
| `mcp_service/Dockerfile` / `Caddyfile` / `docker-compose.yml` | Deploy public HTTPS | ✓ VERIFIED | Không đổi từ verification trước — multi-stage non-root mcpuser EXPOSE 8190 HEALTHCHECK + Caddy auto-TLS forward nguyên host + named volume `medinet_caddy_data`/`medinet_caddy_config`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| provider.exchange_authorization_code | store.claim_auth_code | claim TRƯỚC save_token (CR-01) | ✓ WIRED | provider.py — claim_auth_code TRƯỚC save_token; code bị xoá trước khi save → save lỗi vẫn không replay. |
| provider.exchange_authorization_code | client_id mismatch guard (CRIT-03) | raise OAuthStoreError nếu record.client_id != client.client_id | ✓ WIRED | provider.py:421-431 + log warning "client_id mismatch". |
| provider.load_authorization_code | client_id mismatch guard (CRIT-03 sub-fix 2) | return None nếu record.client_id != client.client_id | ✓ WIRED | provider.py:375-385 — return None semantics "không tồn tại", không leak. |
| provider.register_client | _validate_redirect_uri whitelist (HIGH-01) | loop validate trước save_client | ✓ WIRED | provider.py — for loop redirect_uris + raise nếu host không whitelist hoặc scheme khác http/https hoặc http+non-loopback. |
| provider.exchange_authorization_code | save_token (family_id sinh) | family_id = self._new_opaque() lúc phát token đầu chain (HIGH-02) | ✓ WIRED | provider.py:441 + 451. |
| provider.exchange_refresh_token | store.rotate_token | rotate theo refresh_token + rowcount check (CR-02) | ✓ WIRED | rotate_token + UNIQUE INDEX idx_oauth_tokens_refresh đảm bảo rowcount tin cậy. |
| provider.exchange_refresh_token | store.delete_token_family (HIGH-02 RFC §10.4) | rowcount=0 → load family_id từ record → xoá cả chain | ✓ WIRED | provider.py:525-541 — `family_id = record.get("family_id") or ""` + `if family_id:` + `delete_token_family(family_id)` + log warning "xoá token family" + family_size. |
| provider.revoke_token | client_id mismatch silent no-op (HIGH-07 RFC 7009 §2.2) | KHÔNG xoá KHÔNG raise nếu mismatch | ✓ WIRED | provider.py:601-610 + early return + log warning. |
| provider.revoke_token | server._release_refresh_lock (Blocker #4 cleanup) | late import sau delete_token | ✓ WIRED | provider.py:614 `from mcp_app.server import _release_refresh_lock` + line 616 call — chống circular qua late import (verified KHÔNG có top-level `from mcp_app.server` ở provider.py). |
| provider.authorize | csrf_token sinh + save_pending (HIGH-09) | csrf_token = self._new_opaque() + truyền save_pending | ✓ WIRED | provider.py authorize body + load_pending_csrf helper expose để login route đọc. |
| login_callback | secrets.compare_digest pending CSRF (HIGH-09) | reject form thiếu/sai CSRF TRƯỚC api_client.login | ✓ WIRED | login.py:139 `if not form_csrf or not secrets.compare_digest(form_csrf, pending_csrf):` → render lại form + KHÔNG gọi login. Verified bằng test login_route.call_count == 0. |
| login_get | provider.load_pending_csrf (HIGH-09) | render form chỉ khi pending tồn tại | ✓ WIRED | login.py:106 — pending miss → 400 "Phiên đăng nhập hết hạn"; hợp lệ → render với csrf_token. |
| server._call_api | _refresh_locks single-flight (HIGH-03) | async with _get_refresh_lock(oauth_token) bao khối refresh + re-check downstream_jwt | ✓ WIRED | server.py:272 — Tool 2 vào lock SAU Tool 1 → re-load record → thấy JWT mới → skip refresh, retry với JWT mới. Verified bằng asyncio.gather test refresh_mock.call_count == 1. |
| server.build_asgi_app (path-prefix branch) | KHÔNG _add_cors_middleware(inner) (CRIT-02) | chỉ wrapper outer add | ✓ WIRED | server.py:675 `if not prefix:` bao gồm `_add_cors_middleware(inner)` line 676 + `return _BasicAuthFormShim(inner)` → subdomain mode 1 lần; path-prefix mode KHÔNG return → tiếp tới line 767 `_add_cors_middleware(wrapper)` 1 lần. ACAO không duplicate. |

### Data-Flow Trace (Level 4)

Phase 8.3 là phase auth + deploy — KHÔNG có UI component render dynamic data từ DB. Tool MCP `list_hubs`/`search_wiki`/`ask_wiki` thuộc Phase 8.2 — Phase 8.3 chỉ thêm lớp OAuth bọc; data-flow tool→API đã verified ở Verification 8.2.

OAuth state-flow chính:
- pending oauth → authorize sinh csrf_token → load_pending_csrf → render form → callback verify CSRF → complete_authorization phát code + bind downstream JWT của user vừa login → exchange_authorization_code chuyển code thành access+refresh token kèm family_id → tool dùng access_token resolve credential → forward downstream JWT tới API. Toàn bộ chain VERIFIED qua 30+ test integration + critical PASS.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Toàn bộ suite mcp_service | `cd Hub_All/mcp_service && uv run pytest -q` | 135 passed in 2.91s | ✓ PASS |
| Test critical (marker `critical`) | `uv run pytest -q -m critical` | 17 passed, 118 deselected in 1.09s | ✓ PASS |
| Lint toàn package + tests | `uv run ruff check mcp_app/ tests/` | All checks passed! | ✓ PASS |
| Số test collected (sanity check) | `uv run pytest -q --co` | 135 tests collected in 0.68s | ✓ PASS |
| Grep guard CRIT-03 (4 location) | grep `record["client_id"] != client.client_id\|record["client_id"] != token.client_id` provider.py | 4 occurrence (line 375, 421, 499, 601) | ✓ PASS |
| Grep family_id schema + migration + delete_token_family | grep `family_id\|delete_token_family` store.py | 22 occurrence — schema + 2 migration block + INSERT + _row_to_token + delete_token_family method + 2 test reference | ✓ PASS |
| Grep _refresh_locks + helper | grep server.py | 10 occurrence — pool decl + 3 helper + async with usage + comment | ✓ PASS |
| Grep CSRF chain end-to-end | grep `secrets.compare_digest\|csrf_token` login.py | 30+ occurrence — render + verify + 6 error branch propagate | ✓ PASS |

> Spot-check do verifier tự chạy lại trên HEAD `099c360` (sau Plan 09 commit cuối) — KHÔNG dựa vào claim SUMMARY. Con số 135/135 + 17/17 critical khớp claim Plan 09 SUMMARY.

### Audit Gap Closure (2026-05-21) — Mapping finding → fix → test

| Audit ID | Severity | Fix Plan | Code File:Line | Test (Plan 09) | Trạng thái |
|---|---|---|---|---|---|
| **CRIT-01** | Critical | DEFER | — | — | ⏭ DEFER Phase 10 (cần Starlette per-route CORS research, 2 sub-app pattern) |
| **CRIT-02** | Critical | Plan 07 Task 3 (`a5f3364`) | server.py:675-676 + 767 — _add_cors_middleware(inner) trong if not prefix branch | `test_cors_no_duplicate_in_path_prefix_mode` (`acao_count == 1`) | ✓ ĐÓNG |
| **CRIT-03** | Critical | Plan 07 Task 1 (`d9256cb`) | provider.py:375 load + 421 exchange_code + 499 exchange_refresh | `test_exchange_code_rejects_client_id_mismatch` + `test_load_authorization_code_returns_none_on_client_mismatch` | ✓ ĐÓNG |
| **HIGH-01** | High | Plan 07 Task 1 (`d9256cb`) | provider.py:64-127 _ALLOWED_REDIRECT_HOSTS + _validate_redirect_uri + register_client loop | `test_register_client_rejects_attacker_redirect_uri` + `_rejects_http_non_loopback` + `_accepts_default_redirect_uris` (3 test) | ✓ ĐÓNG |
| **HIGH-02** | High | Plan 08 Task 1+2 (`dfcec01` + `4ef2c99`) | store.py:64 schema + 136-138 migration + 433-447 delete_token_family; provider.py:441 family_id sinh + 525-541 nhánh reuse | `test_exchange_refresh_token_reuse_deletes_family` + `test_delete_token_family_removes_all` + `_noop_on_null` (3 test) | ✓ ĐÓNG |
| **HIGH-03** | High | Plan 08 Task 3 (`5469ddd`) | server.py:203 _refresh_locks + 206/215 helper + 272 async with | `test_refresh_jwt_concurrency_single_flight` (`refresh_mock.call_count == 1` + `get_calls in (3, 4)`) | ✓ ĐÓNG |
| **HIGH-04** | High | Existing fix `a04b928` | server.py OIDC alias route | `test_openid_configuration_alias_returns_200` (Plan 09 — regression test cho commit MISSING) | ✓ ĐÓNG (test mới) |
| **HIGH-05** | High | Existing fix `3dbb378` | server.py list_hubs envelope handling | `test_list_hubs_envelope_data_list_branch` + `_legacy_items_dict_branch` (Plan 09 — 2 regression test) | ✓ ĐÓNG (test mới) |
| **HIGH-06** | High | Existing fix `bd6c02c` | server.py:509-531 _build_mcp transport_security.allowed_hosts | `test_dns_rebinding_allowed_hosts_includes_issuer_host` (introspect THẬT mcp.settings.transport_security.allowed_hosts) + `_rejects_unknown_host` (Plan 09 — Blocker #3 closed) | ✓ ĐÓNG (test mới) |
| **HIGH-07** | High | Plan 07 Task 1 (`d9256cb`) | provider.py:601-610 revoke_token guard mismatch | `test_revoke_token_silent_noop_on_client_mismatch` | ✓ ĐÓNG |
| **HIGH-08** | High | Plan 07 Task 1 (`d9256cb`) | provider.py:499 exchange_refresh_token guard (kết hợp CRIT-03 chain) | Cover bởi CRIT-03 chain — defense-in-depth tested via test_exchange_refresh_token_reuse_deletes_family | ✓ ĐÓNG |
| **HIGH-09** | High | Plan 07 Task 2 (`14da88a`) | store.py:75 schema csrf_token + migration; provider.py authorize sinh + load_pending_csrf; login.py:139 secrets.compare_digest | `test_login_form_contains_hidden_csrf_token` + `_rejects_missing_csrf` + `_rejects_wrong_csrf` (3 test, login_route.call_count == 0) | ✓ ĐÓNG |
| **Blocker #4** | (cleanup) | Plan 08 Task 3 (`5469ddd`) | provider.py:614-616 late import + call _release_refresh_lock | Covered trong test_revoke + lock pool clear test | ✓ ĐÓNG |
| **MED-01..MED-26** | Medium | DEFER | — | — | ⏭ DEFER Phase 10 hardening (defense-in-depth + edge case + config) |
| **LOW-01..LOW-13** | Low | DEFER | — | — | ⏭ DEFER Phase 10 |
| **INFO-01..INFO-14** | Info | DEFER | — | — | ⏭ DEFER Phase 10 (docs + tags) |

**Tổng kết Plan 07+08+09:** 2 Critical (CRIT-02 + CRIT-03) + 9 High (HIGH-01..HIGH-09) đã đóng ở code + test. 1 Critical (CRIT-01) + 26 Medium + 13 Low + 14 Info DEFER Phase 10 — đều có evidence rõ ràng trong audit Section VIII Action Plan.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| **MCP-01** | 08.3-01..09 | MCP Server expose OAuth surface cho Claude/agent | ✓ SATISFIED | OAuth AS đủ 9 method + metadata route + DCR + login + CSRF + family revocation; deploy config public HTTPS đầy đủ; 24 test_oauth_provider PASS + 17 test_oauth_store PASS + 9 test_oauth_login PASS. **Lưu ý:** MCP-01 KHÔNG có trong REQUIREMENTS.md canonical (38 REQ-ID v1) — phase 08.3 là phase chèn thêm; trace từ REQUIREMENTS.md line 116-117 đến PLAN frontmatter requirements field. |
| **MCP-02** | 08.3-03..09 | Tool exposure scoped theo hub_id | ✓ SATISFIED | 3 tool resolve OAuth token → forward downstream JWT đúng user; hub isolation enforce API-side; token lifecycle (CR-01/CR-02/CRIT-03/HIGH-02) nay đúng đắn → "scoped đúng" không còn bị replay/family/cross-client phá. Trước Plan 06 PARTIAL — sau Plan 07+08+09 nâng lên SATISFIED bền vững. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `oauth/store.py` | 339-353 | `update_oauth_token` dead code (sau Plan 05 provider chuyển sang `rotate_token`) | ℹ️ Info | 08.3-REVIEW IN-01 / audit MED-03 — risk: nếu ai gọi lại = tái mở CR-02. Plan 05 cố ý giữ để không vỡ test cũ; **DEFER Phase 10**: xoá hoặc đánh dấu `# DEPRECATED — KHÔNG dùng (CR-02)`. |
| `oauth/store.py` | 252-282 / 355-376 | `save_token` INSERT OR REPLACE / `rotate_token` UPDATE không bắt `aiosqlite.IntegrityError` | ⚠️ Warning | 08.3-REVIEW WR-02 / audit MED-01 — va chạm PK access_token (xác suất cực thấp 256-bit) rò 500. **DEFER Phase 10**: bọc thành OAuthStoreError. |
| `oauth/provider.py` | exchange_refresh_token | đọc record 2 lần (`load_token_by_refresh` rồi `rotate_token`) | ⚠️ Warning | 08.3-REVIEW WR-03 / audit MED-02 — TOCTOU nhỏ; KHÔNG nguy hiểm về đúng đắn (rowcount vẫn bắt reuse), `record["scopes"]` có thể cũ. **DEFER Phase 10**: rotate_token return UPDATE...RETURNING. |
| `oauth/store.py` | 140 | `import time` trong thân `save_client` | ℹ️ Info | 08.3-REVIEW IN-02 / audit MED-04 — không nhất quán. **DEFER Phase 10**: đưa lên module-level. |
| `mcp_app/server.py` | _BasicAuthFormShim.__call__ | C901 complexity 19>10 (pre-existing từ commit `282abaa`) | ℹ️ Info | Plan 07 áp `# noqa: C901` để giữ ruff sạch mà KHÔNG vi phạm scope boundary (pre-existing → không tăng complexity). **DEFER Phase 10**: refactor tách handler nếu muốn dọn. |
| `oauth/login.py` | get_login_routes | C901 complexity 14>10 (Plan 07 thêm 2 nhánh CSRF) | ℹ️ Info | Plan 07 áp `# noqa: C901` — closure-style route factory có nhiều nhánh lỗi tường minh. **DEFER Phase 10**: tách handler ra method riêng. |

> Ghi nhận tích cực: mọi truy vấn SQLite parametrized; KHÔNG log token/JWT/code/csrf/password thô (T-08.3-08 — verified bằng grep `mcp_app/` cho `DebugLog\|_TokenDebugMiddleware\|form_keys\|cs_mask` → 0 match, audit Section VII xác nhận log leak ĐÃ DỌN SẠCH ở commit `f39002c`). `claim_auth_code` claim-then-use nguyên tử; `rotate_token` reuse-detection rowcount + UNIQUE INDEX; `delete_token_family` thu hồi cả chain RFC §10.4; client_id mismatch guard ở 4 method; CSRF end-to-end với `secrets.compare_digest`; CORS single-tier (không duplicate); DCR redirect_uri whitelist; family_id NULLABLE back-compat. 6 Warning/Info còn lại KHÔNG chặn goal — defer Phase 10 đúng theo audit Action Plan.

### Human Verification Required

**2 item cần human UAT — không auto-verify được:**

#### 1. SC2 (live) — Triển khai stack public HTTPS thật và kiểm container health

- **Test:** Theo `08.3-HUMAN-UAT.md` bước 1-3 — operator chuẩn bị domain public + DNS A record + port 80/443 (hoặc Cloudflare Tunnel) + `.env` gốc (`MCP_PUBLIC_DOMAIN` + `MCP_OAUTH_ISSUER_URL=https://...`); `docker compose up -d`; `docker compose ps`; `curl https://<domain>/.well-known/oauth-authorization-server`; `curl https://<domain>/.well-known/openid-configuration`.
- **Expected:** 4 container chạy + status `healthy`/`running` cho mcp_service + caddy; 2 metadata endpoint trả 200 JSON valid (cùng body cho OIDC alias); Caddy log cho thấy cert ACME Let's Encrypt cấp thành công (cài đặt domain HTTPS chính thức, không self-signed).
- **Why human:** Cần domain public HTTPS thật + DNS A record + port 80/443 mở Internet để Caddy xin cert ACME — không auto-verify trong CI. Phần code/config Dockerfile + Caddyfile + docker-compose.yml ĐÃ verified tồn tại + hợp lệ (`docker compose config` validate ở Plan 04); chỉ phần live-deploy cần human chạy lệnh và xác nhận output.

#### 2. SC4 — Kết nối thật từ Claude web "Add custom connector" + gọi 3 tool MCP

- **Test:** Theo `08.3-HUMAN-UAT.md` bước 4-7 — trên Claude web (tài khoản hợp lệ): Settings → Connectors → Add custom connector → URL `https://<domain>/mcp` → DCR + redirect login Medinet → user nhập email/password → callback Claude với authorization code → exchange token → kết nối thành công → 3 tool `list_hubs`/`search_wiki`/`ask_wiki` xuất hiện trong tool palette → gọi mỗi tool một lần → chờ >15 phút (sau khi JWT downstream hết hạn) → gọi lại 1 tool → vẫn chạy.
- **Expected:** Connector kết nối thành công; 3 tool xuất hiện và trả kết quả đúng role-scope (D-04); tool vẫn chạy sau khi JWT downstream tự refresh (HIGH-03 single-flight refresh hoạt động trong điều kiện concurrent thực tế).
- **Why human:** Cần tài khoản Claude web thật + thao tác UI trên dialog connector + domain public HTTPS đã setup từ SC2 — không auto-verify được. Toàn bộ code production (OAuth provider 9 method + DCR + login form + CSRF + 3 tool + refresh-on-401 + family revocation + single-flight refresh) ĐÃ verified tồn tại và đúng đắn qua 135/135 test PASS; chỉ phần kết nối live cần human để chứng kiến UX end-to-end.

### Gaps Summary

**Không còn gap chặn goal trong scope Phase 8.3.** Gap audit 2026-05-21 (3 Critical + 9 High) đã được đóng hoàn toàn ngoại trừ CRIT-01 đã được CHỦ ĐỘNG defer Phase 10 theo audit Action Plan (cần research Starlette per-route CORS pattern — không feasible trong scope wave 8.3-07/08/09).

- **CRIT-02 (CORS double-add path-prefix):** `_add_cors_middleware(inner)` đã được di chuyển vào trong nhánh `if not prefix:` (server.py:676) → subdomain mode add 1 lần ở inner + return ngay; path-prefix mode KHÔNG add ở inner → tiếp tục dựng wrapper + add 1 lần ở line 767. Test `test_cors_no_duplicate_in_path_prefix_mode` assert `acao_count == 1` NGHIÊM (browser reject duplicate ACAO per CORS spec).
- **CRIT-03 (cross-client code exchange chain):** 4 location guard `record["client_id"] != client.client_id|token.client_id` ở `load_authorization_code` (return None), `exchange_authorization_code` (raise), `exchange_refresh_token` (raise — defense-in-depth HIGH-08), `revoke_token` (silent no-op — HIGH-07). Verifier kiểm trực tiếp 4 occurrence trên provider.py + chạy `test_exchange_code_rejects_client_id_mismatch` + `test_load_authorization_code_returns_none_on_client_mismatch` + `test_revoke_token_silent_noop_on_client_mismatch` + `test_exchange_refresh_token_reuse_deletes_family` PASS.
- **HIGH-01 (DCR redirect_uri whitelist):** Thêm `_ALLOWED_REDIRECT_HOSTS` frozenset + `_ALLOWED_REDIRECT_HOST_SUFFIXES` tuple + `_validate_redirect_uri` helper validate scheme/host. Test reject `https://attacker.com/cb`, reject `http://example.com/cb` (http chỉ loopback), reject `ftp://...` scheme, accept 3 default URI Claude/Inspector.
- **HIGH-02 (refresh token family revocation RFC §10.4):** Schema `oauth_tokens` thêm `family_id TEXT` NULLABLE + migration ALTER back-compat; `save_token` accept `family_id` default None; `rotate_token` PRESERVE family_id (UPDATE chỉ 3 cột); `delete_token_family` method + guard `if not family_id: return 0`; provider `exchange_authorization_code` sinh `family_id = self._new_opaque()` + truyền save_token; `exchange_refresh_token` reuse → `delete_token_family(record["family_id"])` + log warning family_size. Test `test_exchange_refresh_token_reuse_deletes_family` chứng minh chain attack được chặn (token chain second_pair cũng bị xoá khi reuse phát hiện ở refresh_obj cũ).
- **HIGH-03 (refresh JWT concurrency race):** `_refresh_locks` pool module-level + `_get_refresh_lock(oauth_token)` lazy create + `_release_refresh_lock(oauth_token)` cleanup; `_call_api` bọc khối refresh trong `async with _get_refresh_lock(oauth_token):` + re-check `record["downstream_jwt"] != cred.value` để tool 2 thấy JWT đã refresh → skip lần refresh thừa, retry với JWT mới. Test `test_refresh_jwt_concurrency_single_flight` assert `refresh_mock.call_count == 1` NGHIÊM (Warning #8 closed) + `get_calls in (3, 4)`.
- **HIGH-04/05/06 (3 commit MISSING regression test):** 5 test mới phủ — OIDC alias body byte-identical với oauth-authorization-server; list_hubs envelope `data:[list]` + legacy `{items:[...]}` 2 branch; DNS rebinding introspect THẬT `mcp.settings.transport_security.allowed_hosts` (Blocker #3 closed — KHÔNG smoke check) + negative case attacker host.
- **HIGH-07 (revoke RFC 7009 §2.2):** `revoke_token` guard `record["client_id"] != token.client_id` → silent no-op (early return, KHÔNG xoá KHÔNG raise). Test Bob revoke token của Alice → token Alice VẪN còn trong store, không raise.
- **HIGH-08 (BasicAuthFormShim defense-in-depth):** Đóng cùng CRIT-03 chain — guard ở `exchange_refresh_token` + `exchange_authorization_code` khiến shim inject `client_id` từ Basic header trở nên vô hiệu (mismatch với record sẽ bị reject).
- **HIGH-09 (CSRF login form):** End-to-end qua 6 file — schema `oauth_pending` +`csrf_token TEXT NOT NULL DEFAULT ''` + migration ALTER; `provider.authorize` sinh csrf_token >160-bit entropy + truyền `save_pending`; `provider.load_pending_csrf` helper; `render_login_form` hidden input; `login_get` reject khi pending miss; `login_callback` `secrets.compare_digest(form_csrf, pending_csrf)` TRƯỚC `api_client.login`; tất cả nhánh error render truyền lại csrf_token để form re-submit. Test `test_login_callback_rejects_missing_csrf` + `_rejects_wrong_csrf` assert `login_route.call_count == 0` — chứng minh CSRF verify đi TRƯỚC api login call.
- **Blocker #4 cleanup follow-up:** `provider.revoke_token` sau `delete_token` thành công wire `from mcp_app.server import _release_refresh_lock` (late import chống circular — provider.py KHÔNG có top-level `from mcp_app.server`) + `_release_refresh_lock(access_token_revoked)` — pop entry pool, không leak ~136 byte/user.

Phase đạt 5/5 Success Criteria verify được tự động (SC1/SC2 code, SC3 token lifecycle full RFC compliance, SC5 regression 135/135 PASS + ruff sạch). 2 phần live (SC2 docker stack healthy + SC4 Claude web Add custom connector) là item human-UAT hợp lệ — cần domain public + Claude web thật. Status `human_needed`: tự động hoá đã xanh hết, chờ human UAT theo `08.3-HUMAN-UAT.md`.

6 Warning/Info từ 08.3-REVIEW + audit Section III/IV (WR-01 family revocation đã đóng ở HIGH-02, WR-02 IntegrityError, WR-03 đọc 2 lần, IN-01 dead code update_oauth_token, IN-02 import time, C901 noqa cho _BasicAuthFormShim/get_login_routes) KHÔNG chặn goal — cải thiện độ bền, cân nhắc Phase 10 hardening theo audit Action Plan.

**Lưu ý API regression:** `api/` có 1 test fail PRE-EXISTING (`test_auth_refresh_race.py::test_refresh_happy_returns_new_pair`, KeyError 'email') — phase 08.3 KHÔNG chạm file nào trong `api/` (0 file `api/` đổi trong Plan 07+08+09, verified bằng git diff frontmatter `files_modified`); KHÔNG phải regression do phase này gây ra; user đã xác nhận và chọn tiếp tục từ verification trước (2026-05-19).

---

_Verified: 2026-05-21 (Re-verification sau gap closure Plan 07 + 08 + 09 audit 2026-05-21)_
_Verifier: Claude (gsd-verifier)_
_Baseline: 08.3-VERIFICATION.md 2026-05-19 (status human_needed 4/5)_
_HEAD: commit 099c360 (docs(08.3-09) hoàn tất Plan 09 gap closure wave 9)_
_Test gate: mcp_service 135/135 PASS · critical 17/17 PASS · ruff mcp_app/ tests/ sạch_
