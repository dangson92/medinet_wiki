---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 04
subsystem: mcp-service-deploy
tags: [docker, deploy, caddy, reverse-proxy, oauth, public-https, uat]
dependency_graph:
  requires:
    - "uv.lock cho mcp_service (Plan 01 — Dockerfile COPY uv.lock)"
    - "build_asgi_app() factory + route OAuth /.well-known mount sẵn (Plan 02)"
    - "Settings.oauth_issuer_url có default an toàn → HEALTHCHECK well-known tất định (Plan 01)"
    - "MCP_OAUTH_STATE_DB_PATH config (Plan 01) — đường dẫn SQLite OAuth state"
  provides:
    - "mcp_service/Dockerfile — image multi-stage uv non-root mcpuser, factory CMD port 8190"
    - "mcp_service/.dockerignore — loại trừ build context"
    - "Caddyfile — reverse proxy auto-TLS forward nguyên host tới mcp_service:8190"
    - "docker-compose.yml service mcp_service + caddy + 3 named volume"
    - "08.3-HUMAN-UAT.md — quy trình UAT thủ công SC4"
  affects:
    - "Phase 8.3 hoàn tất ở tầng triển khai — MCP-01/MCP-02 deploy-ready"
tech_stack:
  added:
    - "caddy:2-alpine — reverse proxy auto-TLS Let's Encrypt (image, không phải dependency Python)"
  patterns:
    - "multi-stage uv Dockerfile builder/runtime (mẫu api/Dockerfile)"
    - "HEALTHCHECK probe route tất định thay vì endpoint health riêng"
    - "uvicorn --factory cho ASGI factory function"
    - "env_file required:false — service chạy được khi chưa có .env (default ở environment:)"
    - "named volume cho state bền (OAuth SQLite + Caddy cert)"
key_files:
  created:
    - "Hub_All/mcp_service/Dockerfile"
    - "Hub_All/mcp_service/.dockerignore"
    - "Hub_All/Caddyfile"
    - "Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md"
  modified:
    - "Hub_All/docker-compose.yml"
    - "Hub_All/mcp_service/README.md"
    - "Hub_All/.gitignore"
decisions:
  - "env_file mcp_service/.env dùng required:false (Compose v2.24+) — operator chưa tạo .env vẫn `docker compose config`/`up` được vì mọi biến bắt buộc đã có default ở environment: (Rule 3 — gỡ blocking compose validate)"
  - "HEALTHCHECK probe /.well-known/oauth-authorization-server — route tất định (default issuer Plan 01 + SDK mount), không cần endpoint /healthz riêng"
  - "mcp_service KHÔNG map port host — chỉ Caddy expose 80/443; mọi truy cập public buộc qua TLS termination (T-08.3-20)"
metrics:
  duration: "~7 phút"
  completed_date: "2026-05-19"
  tasks_completed: 3
  files_changed: 7
---

# Phase 8.3 Plan 04: Deploy MCP Service public HTTPS Summary

Đưa MCP Service ra public qua HTTPS để Claude web "Add custom connector" kết nối được — hoàn tất SC2 ở tầng triển khai. Viết `mcp_service/Dockerfile` multi-stage `uv` (builder/runtime, non-root `mcpuser`, factory CMD `uvicorn --factory` port 8190, HEALTHCHECK probe route OAuth discovery tất định); thêm service `mcp_service` + reverse proxy `caddy` (auto-TLS Let's Encrypt) vào `docker-compose.yml` với 3 named volume; viết `Caddyfile` forward nguyên host (well-known route ở root KHÔNG bị strip — Pitfall 7). Thiết kế deploy để đổi reverse proxy (Caddy ↔ Cloudflare Tunnel) chỉ cần đổi `MCP_OAUTH_ISSUER_URL` + service compose — KHÔNG đụng `mcp_app`. Viết `08.3-HUMAN-UAT.md` — quy trình UAT thủ công SC4 (kết nối thật từ Claude web — `human_needed`).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Dockerfile multi-stage uv + .dockerignore | `3d12f95` (+ `3c6be72` sửa comment) | mcp_service/Dockerfile, mcp_service/.dockerignore |
| 2 | Caddyfile + service mcp_service/caddy trong compose | `a551d0c` | Caddyfile, docker-compose.yml, .gitignore |
| 3 | README OAuth/deploy + 08.3-HUMAN-UAT.md SC4 | `c3ec1e9` | mcp_service/README.md, 08.3-HUMAN-UAT.md |

## Implementation Notes

### Task 1 — Dockerfile multi-stage uv
- Copy cấu trúc `api/Dockerfile`, áp khác biệt: user `apiuser` → `mcpuser`; `COPY mcp_app ./mcp_app` (thay `app ./app`); BỎ dòng `COPY alembic.ini`/`COPY migrations` (MCP Service không DB chung — không migration); thay `mkdir /app/.cocoindex` bằng `mkdir -p /app/.oauth && chown mcpuser:mcpuser /app/.oauth` (thư mục SQLite OAuth state, tạo + chown TRƯỚC `USER mcpuser`).
- `EXPOSE 8190`; `HEALTHCHECK` probe `/.well-known/oauth-authorization-server` (`start-period=20s` cho app + lifespan `init_schema()` kịp khởi động) — route tất định vì `Settings.oauth_issuer_url` có default an toàn (Plan 01) nên `Settings()` không raise, SDK `mcp` mount well-known ngay khi `auth_server_provider` non-None.
- `CMD ["uvicorn", "mcp_app.server:build_asgi_app", "--factory", ...]` — `build_asgi_app` là FACTORY (Plan 02) → BẮT BUỘC cờ `--factory`.
- `.dockerignore`: loại `.venv`/cache/`tests`/`.env`/`*.db`/`.oauth`/`.git`/`README.md` khỏi build context.
- Fast pre-check `import mcp_app.server` chạy TRƯỚC `docker build` — PASS; `docker build` PASS; `docker run ... import` PASS.

### Task 2 — Caddyfile + compose
- `Caddyfile`: `reverse_proxy medinet-mcp:8190` forward nguyên host + path; domain qua biến `{$MCP_PUBLIC_DOMAIN}`. KHÔNG dùng `handle_path`/`uri strip_prefix` (Pitfall 7 — well-known discovery 404). Comment hướng dẫn dev local `MCP_PUBLIC_DOMAIN=localhost`/`:80`.
- `docker-compose.yml`: GIỮ nguyên 3 service cũ. Thêm `mcp_service` (build `./mcp_service`, `container_name: medinet-mcp`, `MCP_API_BASE_URL: http://python-api:8080` gọi qua docker network, `MCP_OAUTH_STATE_DB_PATH: /app/.oauth/state.db` trên volume, `depends_on: python-api healthy`, KHÔNG map port host) + `caddy` (`image: caddy:2-alpine`, expose `80`/`443`, mount `Caddyfile` + volume cert). Thêm 3 named volume: `medinet_mcp_oauth_state`, `medinet_caddy_data`, `medinet_caddy_config`.
- `.gitignore`: thêm `mcp_service/.env`.
- `docker compose config --quiet` — PASS.

### Task 3 — README + UAT
- `README.md`: thêm section "Phase 8.3 — OAuth 2.0 + Deploy public HTTPS" — kiến trúc đích, bảng 2 cách xác thực (OAuth + X-API-Key song song), cách chạy có OAuth (compose / native `--factory`), bảng 4 biến `MCP_OAUTH_*`, Caddy auto-TLS + fallback Cloudflare Tunnel, cảnh báo issuer prod phải `https`.
- `08.3-HUMAN-UAT.md`: format theo `08.2-HUMAN-UAT.md`. Tiền điều kiện (domain DNS + port 80/443 hoặc Cloudflare Tunnel + `.env` gốc compose + API Service chạy + tài khoản Medinet active). SC4 7 bước: probe well-known `curl`, add connector Claude web, login Medinet, connector kết nối + 3 tool xuất hiện, gọi `list_hubs` (role-scoped D-04), gọi `search_wiki`/`ask_wiki`, regression JWT refresh phiên dài >15 phút. Ô đánh dấu PASS/FAIL + domain thật đã dùng.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `mcp_service/.env` chưa tồn tại → `docker compose config` fail**
- **Found during:** Task 2
- **Issue:** Plan action 2 đề xuất `env_file: [./mcp_service/.env]` (copy mẫu `python-api` dùng `./api/.env`). Nhưng `mcp_service/` chỉ có `.env.example`, chưa có `.env` (khác `api/` — operator đã tạo `api/.env` local từ trước). `docker compose config --quiet` fail: `env file ...mcp_service\.env not found`. Acceptance criteria Task 2 yêu cầu `docker compose config` exit 0 — bị chặn.
- **Fix:** Đổi `env_file` của `mcp_service` sang cú pháp Compose v2.24+ `path: ./mcp_service/.env` + `required: false` (môi trường có Compose v5.1.3 — hỗ trợ). Service chạy được khi chưa có `.env` vì mọi biến bắt buộc (`MCP_API_BASE_URL`, `MCP_OAUTH_ISSUER_URL`, `MCP_OAUTH_STATE_DB_PATH`) đã có default tường minh ở block `environment:`. Thêm `mcp_service/.env` vào `.gitignore` (nhất quán `api/.env`).
- **Files modified:** docker-compose.yml, .gitignore
- **Commit:** `a551d0c`

**2. [Rule 3 - Blocking] Comment Dockerfile chứa chữ "alembic" → lệch tiêu chí `grep -c "alembic" == 0`**
- **Found during:** Overall verification
- **Issue:** Acceptance criteria Task 1 yêu cầu `grep -c "alembic" mcp_service/Dockerfile` trả 0. Comment header giải thích "KHÁC api: không alembic/migrations" vô tình chứa chữ `alembic` → `grep -c` trả 1. Dockerfile KHÔNG có directive migration nào — chỉ là chữ trong comment.
- **Fix:** Đổi comment "không alembic/migrations" → "không có bước migration". `grep -c` nay trả 0; ý nghĩa comment giữ nguyên.
- **Files modified:** mcp_service/Dockerfile
- **Commit:** `3c6be72`

## Verification

- `cd Hub_All/mcp_service && uv run python -c "import mcp_app.server"` — fast pre-check import. PASS
- `docker build -t medinet-mcp:test Hub_All/mcp_service` — build image multi-stage uv thành công. PASS
- `docker run --rm medinet-mcp:test python -c "import mcp_app.server"` — image import được package. PASS
- `cd Hub_All && docker compose config --quiet` — compose hợp lệ (5 service: postgres/redis/python-api/mcp_service/caddy). PASS
- `grep "mcp_app.server:build_asgi_app" / "--factory" / "EXPOSE 8190" / "/app/.oauth" / "well-known/oauth-authorization-server"` trong Dockerfile — đều tồn tại. PASS
- `grep -c "alembic" mcp_service/Dockerfile` — 0 (không migration). PASS
- `grep "reverse_proxy medinet-mcp:8190" Caddyfile` — tồn tại; KHÔNG có directive `handle_path`/`uri strip` (chỉ comment). PASS
- `grep "mcp_service:" / "caddy:" / "MCP_API_BASE_URL: http://python-api:8080"` trong compose — đều tồn tại. PASS
- `grep -c "medinet_mcp_oauth_state" docker-compose.yml` — 2 (mount service + block volumes). PASS
- mcp_service KHÔNG có `ports:` map host — chỉ caddy expose 80/443. PASS
- `08.3-HUMAN-UAT.md` tồn tại, có `SC4`/`human_needed`/`Add custom connector`/`well-known`/`Cloudflare Tunnel`. PASS
- README có section `Phase 8.3` + `MCP_OAUTH_ISSUER_URL`. PASS

## Threat Flags

Không phát hiện security surface mới ngoài `<threat_model>` của plan. Các mitigation T-08.3-16..20 (TLS bắt buộc, không strip well-known path, non-root container, OAuth state trên volume, không map port host) đều đã áp đúng trong Dockerfile/Caddyfile/compose.

## Self-Check: PASSED

- FOUND: Hub_All/mcp_service/Dockerfile
- FOUND: Hub_All/mcp_service/.dockerignore
- FOUND: Hub_All/Caddyfile
- FOUND: Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md
- FOUND: commit 3d12f95 (Task 1)
- FOUND: commit a551d0c (Task 2)
- FOUND: commit c3ec1e9 (Task 3)
- FOUND: commit 3c6be72 (sửa comment Dockerfile)

## TDD Gate Compliance

Plan type `execute`, không có task `tdd="true"` (Plan 04 là deploy/config — Dockerfile/Caddyfile/compose/docs, không có code logic test được bằng unit test). Verification dùng `docker build` + `docker compose config` + `import` smoke — phù hợp bản chất artifact deploy. Không có gate TDD áp dụng.
