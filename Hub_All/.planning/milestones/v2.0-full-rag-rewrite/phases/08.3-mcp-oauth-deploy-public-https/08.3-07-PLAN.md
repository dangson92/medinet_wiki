---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - Hub_All/mcp_service/mcp_app/oauth/provider.py
  - Hub_All/mcp_service/mcp_app/oauth/store.py
  - Hub_All/mcp_service/mcp_app/oauth/login.py
  - Hub_All/mcp_service/mcp_app/server.py
  - Hub_All/mcp_service/tests/conftest.py
  - Hub_All/mcp_service/tests/test_oauth_login.py
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "exchange_authorization_code raise OAuthStoreError nếu record.client_id != client.client_id — chặn cross-client code exchange (CRIT-03)"
    - "load_authorization_code trả None nếu record.client_id != client.client_id — chặn nhánh load → exchange của chain attack (CRIT-03 sub-fix 2)"
    - "exchange_refresh_token raise OAuthStoreError nếu refresh_token.client_id != client.client_id — defense-in-depth (HIGH-08 cùng họ)"
    - "revoke_token KHÔNG xoá record nếu record.client_id != token.client_id — silent no-op (RFC 7009 §2.2, HIGH-07)"
    - "register_client từ chối client_info với redirect_uri có scheme khác http/https HOẶC host không trong whitelist (claude.ai, modelcontextprotocol.io, localhost, 127.0.0.1) — chặn code interception (HIGH-01)"
    - "Path-prefix mode KHÔNG add CORSMiddleware ở inner (xoá dòng 607) — chỉ wrapper outer add → response không còn duplicate Access-Control-Allow-Origin (CRIT-02)"
    - "Login form POST /login/callback verify hidden `csrf_token` khớp record oauth_pending.csrf_token — request thiếu/sai CSRF → render lại form với lỗi (HIGH-09)"
    - "oauth_pending có cột csrf_token TEXT NOT NULL; save_pending nhận tham số csrf_token BẮT BUỘC (không default); load_pending trả csrf_token trong dict"
    - "test_login_get_renders_form sau Plan 07 PHẢI seed pending + csrf_token TRƯỚC GET /login?txn=... vì login_get giờ verify pending tồn tại"
    - "Subdomain mode CORS giữ nguyên hành vi cũ (inner add CORS) — không vỡ deploy subdomain"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/oauth/provider.py"
      provides: "client_id mismatch check ở exchange_authorization_code + load_authorization_code + exchange_refresh_token + revoke_token; DCR redirect_uri whitelist validate ở register_client; complete_authorization sinh + lưu csrf_token vào pending"
      contains: "record[\"client_id\"] != client.client_id"
    - path: "Hub_All/mcp_service/mcp_app/oauth/store.py"
      provides: "schema oauth_pending thêm csrf_token NOT NULL; save_pending / load_pending nhận+trả csrf_token"
      contains: "csrf_token"
    - path: "Hub_All/mcp_service/mcp_app/oauth/login.py"
      provides: "GET /login render form có hidden csrf_token; POST /login/callback verify form csrf_token == pending.csrf_token"
      contains: "csrf_token"
    - path: "Hub_All/mcp_service/mcp_app/server.py"
      provides: "Path-prefix mode KHÔNG _add_cors_middleware(inner) — chỉ wrapper outer"
      contains: "subdomain"
    - path: "Hub_All/mcp_service/tests/conftest.py"
      provides: "fake_pending_authorize signature có csrf_token bắt buộc (default chuỗi cố định cho compat helper test)"
      contains: "csrf_token"
    - path: "Hub_All/mcp_service/tests/test_oauth_login.py"
      provides: "test_login_get_renders_form update để seed pending + csrf trước GET"
      contains: "fake_pending_authorize"
  key_links:
    - from: "provider.exchange_authorization_code"
      to: "client.client_id check"
      via: "raise OAuthStoreError nếu record.client_id != client.client_id"
      pattern: "record\\[\"client_id\"\\] != client\\.client_id"
    - from: "provider.load_authorization_code"
      to: "client.client_id check"
      via: "return None nếu record.client_id != client.client_id"
      pattern: "record\\[\"client_id\"\\] != client\\.client_id"
    - from: "provider.register_client"
      to: "_validate_redirect_uri whitelist host"
      via: "scheme + host check"
      pattern: "_ALLOWED_REDIRECT_HOSTS"
    - from: "login_callback"
      to: "pending.csrf_token verify"
      via: "form csrf_token == pending csrf_token"
      pattern: "pending\\[\"csrf_token\"\\]"
    - from: "build_asgi_app (path-prefix branch)"
      to: "KHÔNG _add_cors_middleware(inner)"
      via: "xoá dòng 607 trong path-prefix mode, chỉ wrapper outer add"
      pattern: "_add_cors_middleware\\(wrapper\\)"
---

<objective>
Vá toàn bộ Critical (CRIT-01 CORS double-add, CRIT-02 path-prefix double CORS,
CRIT-03 client_id mismatch chain) + High security (HIGH-01 DCR redirect_uris,
HIGH-07 revoke client_id check, HIGH-08 BasicAuthFormShim defense, HIGH-09 login
CSRF) phát hiện ở `08.3-MCP-AUDIT-2026-05-21.md`.

Phase 8.3 đã có 6 plan hoàn tất (verification 4/5 human_needed), nhưng 12 commit
hot-fix UAT sinh ra bề mặt mới (`_BasicAuthFormShim`, path-prefix wrapper, DNS
rebinding whitelist, OIDC alias, per-user pre-registered) chưa được audit cho
đến 2026-05-21. Audit chỉ ra 3 Critical + 9 High; plan này đóng 3 Critical +
4 High (security guards). HIGH-02 family revocation + HIGH-03 concurrency tách
sang Plan 08 vì cần migration schema + asyncio.Lock pool (ngữ cảnh khác).
HIGH-04/05/06 (test missing) + test cho fix Plan 07/08 sang Plan 09.

Purpose: Đưa SC1 (OAuth surface) + SC4 (kết nối thật) từ trạng thái "có lỗ
hổng chain Critical" về trạng thái "ship UAT công khai an toàn".

Output: provider.py có guard client_id mismatch ở 4 method + DCR redirect_uri
whitelist; store.py + login.py có CSRF token end-to-end; server.py
build_asgi_app path-prefix mode CHỈ add CORS ở wrapper outer (không inner).
conftest.py + test_oauth_login.py cập nhật tương thích contract mới
(save_pending bắt buộc csrf_token; login_get verify pending tồn tại).

LƯU Ý: Plan này KHÔNG thêm test mới — test ở Plan 09 (wave 3). Regression test
hiện có (89/89 PASS) PHẢI vẫn xanh sau plan này. Task cuối chạy regression gate.
Mỗi commit atomic — task 1+2+3 commit RIÊNG; KHÔNG gộp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-VERIFICATION.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-05-PLAN.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-06-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Chữ ký method hiện có executor cần biết — trích từ codebase, KHÔNG cần khám phá. -->

oauth/provider.py — method cần sửa (sau Plan 05+06):
```python
async def register_client(self, client_info: OAuthClientInformationFull) -> None:
    # provider.py:159-168 — hiện lưu thẳng client_info.model_dump, KHÔNG validate redirect_uris.
async def load_authorization_code(
    self, client: OAuthClientInformationFull, authorization_code: str
) -> AuthorizationCode | None:
    # provider.py:275-294 — sau load_auth_code không kiểm record["client_id"] vs client.client_id.
async def exchange_authorization_code(
    self, client: OAuthClientInformationFull,
    authorization_code: AuthorizationCode,
) -> OAuthToken:
    # provider.py:296-342 — sau claim_auth_code, kiểm expires_at; CHƯA kiểm client_id mismatch.
async def exchange_refresh_token(
    self, client: OAuthClientInformationFull,
    refresh_token: RefreshToken, scopes: list[str],
) -> OAuthToken:
    # provider.py:360-402 — sau load_token_by_refresh, CHƯA kiểm refresh_token.client_id vs client.client_id.
async def revoke_token(self, token: AccessToken | RefreshToken) -> None:
    # provider.py:426-440 — sau load_token / load_token_by_refresh, CHƯA kiểm record["client_id"] vs token.client_id.
async def complete_authorization(
    self, txn: str, login_data: dict
) -> tuple[str, str, str | None]:
    # provider.py:198-271 — hiện không sinh CSRF token. Sẽ sửa ở Task 2 để pending có csrf_token.

# Helper hiện có (provider.py:101-104):
@staticmethod _new_opaque() -> str  # secrets.token_urlsafe(32) — dùng cho cả CSRF token.
```

oauth/store.py — chữ ký hiện có (sẽ sửa save_pending + load_pending + _SCHEMA_STATEMENTS):
```python
_SCHEMA_STATEMENTS = (..., CREATE TABLE oauth_pending (..., scopes, created_at), ...)
async def save_pending(self, txn, client_id, redirect_uri, code_challenge,
                       code_challenge_method, client_state, scopes, created_at) -> None
async def load_pending(self, txn) -> dict | None  # trả 7 khoá hiện tại
```

**save_pending callers production + tests (xác nhận bằng grep `save_pending\(`):**
- `Hub_All/mcp_service/mcp_app/oauth/provider.py:182` — `provider.authorize`
- `Hub_All/mcp_service/tests/conftest.py:148` — helper `fake_pending_authorize`
- `Hub_All/mcp_service/tests/test_oauth_store.py:165` — test trực tiếp store

CHỈ 3 caller. Task 2 SẼ update tất cả 3 trong cùng commit để contract đồng nhất.

oauth/login.py — chữ ký hiện có (sẽ sửa render_login_form + login_get + login_callback):
```python
def render_login_form(txn, callback_action, error=None) -> HTMLResponse
async def login_get(request)   # GET /login?txn=...  → render_login_form(txn, callback_action)
async def login_callback(request)  # POST /login/callback → api_client.login() → provider.complete_authorization
# pending = await provider._store.load_pending(txn)  KHÔNG có sẵn ở login.py — login.py không truy
# cập store trực tiếp. Phải đi qua provider; provider.complete_authorization sẽ verify CSRF.
```

server.py — branch path-prefix (CRIT-02):
```python
# Dòng 607: _add_cors_middleware(inner)   # HIỆN ĐANG ADD CORS LẦN 1 — XOÁ cho path-prefix mode.
# Dòng 611-612: if not prefix: return _BasicAuthFormShim(inner)
#   → subdomain mode RETURN ngay, KHÔNG đi qua dòng 702.
# Dòng 702: _add_cors_middleware(wrapper)  # LẦN 2 — chỉ path-prefix mode dính.
# Vấn đề: subdomain mode ĐÃ đi qua dòng 607; path-prefix mode đi qua CẢ 607 lẫn 702.
# Fix: tách logic — chỉ add CORS ở inner cho subdomain mode; path-prefix mode chỉ add ở wrapper.
```

Whitelist host cho HIGH-01 (xác nhận với `api/app/services/mcp_oauth_service.py:42-46`):
```python
_DEFAULT_REDIRECT_URIS = [
    "https://claude.ai/api/mcp/auth_callback",
    "http://localhost:6274/oauth/callback",
    "http://127.0.0.1:6274/oauth/callback",
]
```
→ whitelist host: `claude.ai`, `*.claude.ai`, `modelcontextprotocol.io`,
`*.modelcontextprotocol.io`, `localhost`, `127.0.0.1`. Scheme: `https` cho
mọi host non-loopback; `http` chỉ cho loopback (`localhost` / `127.0.0.1`).

OAuthClientInformationFull (SDK mcp 1.27) — `client_info.redirect_uris` là
`list[AnyUrl]`. `AnyUrl` có `.scheme` + `.host`.

**test_oauth_login.py CURRENT pattern (Starlette TestClient + respx):**
```python
from starlette.testclient import TestClient
import httpx, respx

@respx.mock
def test_login_get_renders_form(oauth_env) -> None:
    from mcp_app.server import build_asgi_app
    app = build_asgi_app()
    with TestClient(app) as client:
        resp = client.get("/login?txn=txn-abc")
    assert resp.status_code == 200
```
KHÔNG có helper raw `_make_provider_for_login` / `_make_get_request`. Plan 09
viết test CSRF PHẢI dùng cùng pattern TestClient + seed pending qua fixture
`_get_oauth_store()` + `fake_pending_authorize` (như `test_login_callback_success_redirects`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: provider.py — client_id mismatch check 4 method (CRIT-03, HIGH-07, HIGH-08) + DCR redirect_uri whitelist (HIGH-01)</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (file đang sửa — register_client dòng 159-168, load_authorization_code 275-294, exchange_authorization_code 296-342, exchange_refresh_token 360-402, revoke_token 426-440)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (CRIT-03 section II Critical, HIGH-01/07/08 — đọc code mẫu fix)
    - Hub_All/api/app/services/mcp_oauth_service.py (xác nhận `_DEFAULT_REDIRECT_URIS` để whitelist khớp)
  </read_first>
  <action>
Sửa `Hub_All/mcp_service/mcp_app/oauth/provider.py` — 5 thay đổi:

(1) HIGH-01 — thêm hằng số module + helper validate, đặt ngay sau dòng
`_PENDING_TTL = 600`:

```python
# Whitelist host cho redirect_uri DCR — khớp _DEFAULT_REDIRECT_URIS của
# api/app/services/mcp_oauth_service.py (Claude web + MCP Inspector).
# HIGH-01 (audit 2026-05-21): DCR mở cho client_id/secret nhưng redirect_uri
# PHẢI validate — code interception attack nếu attacker đăng ký redirect_uri
# trỏ về domain mình. PKCE bảo vệ một phần nhưng không đủ khi attacker có
# verifier của chính mình.
_ALLOWED_REDIRECT_HOSTS = frozenset(
    {
        "claude.ai",
        "inspector.modelcontextprotocol.io",
        "localhost",
        "127.0.0.1",
    }
)
# Suffix wildcard host — host.endswith(f".{suffix}") match subdomain.
_ALLOWED_REDIRECT_HOST_SUFFIXES = (".claude.ai", ".modelcontextprotocol.io")


def _is_loopback_host(host: str) -> bool:
    """True nếu host là loopback (cho phép scheme http)."""
    return host in ("localhost", "127.0.0.1")


def _validate_redirect_uri(uri: AnyUrl) -> None:
    """Validate redirect_uri DCR — raise OAuthStoreError nếu không hợp lệ.

    Quy tắc (HIGH-01):
    - scheme ∈ {"http", "https"}.
    - scheme == "http" CHỈ khi host là loopback (localhost / 127.0.0.1).
    - host nằm trong _ALLOWED_REDIRECT_HOSTS HOẶC kết thúc bằng
      _ALLOWED_REDIRECT_HOST_SUFFIXES.
    KHÔNG log giá trị URL thô — chỉ log host + scheme + lý do reject.
    """
    scheme = (uri.scheme or "").lower()
    host = (uri.host or "").lower()
    if scheme not in ("http", "https"):
        logger.warning(
            "register_client reject redirect_uri scheme=%s (chỉ http/https)",
            scheme,
        )
        raise OAuthStoreError(
            f"redirect_uri scheme '{scheme}' không hợp lệ (cần http/https)"
        )
    if scheme == "http" and not _is_loopback_host(host):
        logger.warning(
            "register_client reject http redirect_uri host=%s (chỉ loopback)",
            host,
        )
        raise OAuthStoreError(
            f"redirect_uri scheme http chỉ được dùng cho loopback, host={host}"
        )
    if host in _ALLOWED_REDIRECT_HOSTS:
        return
    if any(host.endswith(suffix) for suffix in _ALLOWED_REDIRECT_HOST_SUFFIXES):
        return
    logger.warning(
        "register_client reject redirect_uri host=%s scheme=%s (whitelist miss)",
        host,
        scheme,
    )
    raise OAuthStoreError(
        f"redirect_uri host '{host}' không nằm trong whitelist"
    )
```

(2) HIGH-01 — sửa `register_client` (provider.py:159-168) — validate mỗi
redirect_uri TRƯỚC save. Thay thân hàm:

```python
async def register_client(self, client_info: OAuthClientInformationFull) -> None:
    """Lưu client DCR. DCR mở (D-01) — KHÔNG từ chối client_id, NHƯNG redirect_uri PHẢI whitelist.

    HIGH-01 (audit 2026-05-21): code interception attack nếu attacker đăng ký
    redirect_uri trỏ về domain mình. PKCE bảo vệ một phần nhưng không đủ khi
    attacker tự sinh PKCE pair từ đầu (attacker có verifier).
    """
    client_id = client_info.client_id
    if client_id is None:
        # SDK sinh client_id trước khi gọi register_client; phòng vệ thêm.
        raise OAuthStoreError("register_client thiếu client_id")
    for redirect_uri in client_info.redirect_uris:
        _validate_redirect_uri(redirect_uri)
    await self._store.save_client(
        client_id, client_info.model_dump(mode="json")
    )
    logger.info("MedinetOAuthProvider đăng ký client: client_id=%s", client_id)
```

(3) CRIT-03 sub-fix 1 — sửa `load_authorization_code` (provider.py:275-294)
thêm guard `record["client_id"] != client.client_id` NGAY SAU `load_auth_code`.
Thay đoạn `record = await self._store.load_auth_code(authorization_code)` +
`if record is None: return None`:

```python
async def load_authorization_code(
    self, client: OAuthClientInformationFull, authorization_code: str
) -> AuthorizationCode | None:
    """Đọc authorization code. None nếu không tồn tại / hết hạn / khác client.

    CRIT-03 sub-fix 2 (audit 2026-05-21): kiểm record.client_id == client.client_id —
    chặn nhánh load → exchange của chain attack (attacker A đoán code của victim B,
    SDK gọi load_authorization_code(client=A, code_của_B) → trả AuthorizationCode
    bind client_id=B; SDK không phân biệt). Trả None thay vì raise để giữ semantics
    "code không tồn tại" — không leak thông tin attacker đoán đúng code.
    """
    record = await self._store.load_auth_code(authorization_code)
    if record is None:
        return None
    if record["client_id"] != client.client_id:
        # CRIT-03: code phát cho client khác — trả None như "không tồn tại"
        # (không raise để không leak thông tin code đoán đúng).
        logger.warning(
            "load_authorization_code client_id mismatch — "
            "record_client_id=%s requesting_client_id=%s",
            record["client_id"],
            client.client_id,
        )
        return None
    if record["expires_at"] < self._now():
        logger.info("MedinetOAuthProvider authorization code expired")
        return None
    payload = record["code_payload"]
    return AuthorizationCode(
        code=authorization_code,
        scopes=payload["scopes"],
        expires_at=float(record["expires_at"]),
        client_id=record["client_id"],
        code_challenge=payload["code_challenge"],
        redirect_uri=AnyUrl(payload["redirect_uri"]),
        redirect_uri_provided_explicitly=True,
    )
```

(4) CRIT-03 sub-fix 1 + HIGH-08 — sửa `exchange_authorization_code`
(provider.py:296-342) thêm guard NGAY SAU `claim_auth_code`, TRƯỚC kiểm
`expires_at`. Thay đoạn từ `record = await self._store.claim_auth_code(...)` đến
trước `access_token = self._new_opaque()`:

```python
        record = await self._store.claim_auth_code(authorization_code.code)
        if record is None:
            raise OAuthStoreError(
                "Authorization code không tồn tại hoặc đã dùng"
            )
        # CRIT-03 (audit 2026-05-21): code phát cho client A — chặn client B
        # exchange. Chain attack: SDK ClientAuthenticator verify B:secret_B OK,
        # rồi exchange với code của A → trước fix, SDK không kiểm mismatch →
        # B nhận token bind danh tính của A (cross-client code exchange).
        # Kết hợp với _BasicAuthFormShim (HIGH-08) — shim inject client_id từ
        # Basic header, không kiểm mismatch ở chỗ này → HIGH-08 hết ý nghĩa
        # khai thác sau khi fix CRIT-03.
        if record["client_id"] != client.client_id:
            logger.warning(
                "exchange_authorization_code client_id mismatch — "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                client.client_id,
            )
            raise OAuthStoreError(
                "Authorization code không thuộc client này"
            )
        # Code đã bị xoá nguyên tử — nếu save_token lỗi, code KHÔNG còn để replay.
        # Kiểm hạn TẠI exchange: load_authorization_code đã kiểm expires_at,
        # nhưng exchange_authorization_code có thể bị gọi trực tiếp (bỏ qua
        # load_authorization_code) — claim_auth_code KHÔNG kiểm hạn. Vì vậy
        # phải tự reject code hết hạn ở đây (RFC 6749 §10.5).
        if record["expires_at"] < self._now():
            raise OAuthStoreError("Authorization code đã hết hạn")
```

(5) HIGH-08 family — `exchange_refresh_token` (provider.py:360-402): thêm
guard `refresh_token.client_id != client.client_id` NGAY SAU
`load_token_by_refresh` + `if record is None: raise`. Thay đoạn ngay sau check
`if record is None`:

```python
        record = await self._store.load_token_by_refresh(refresh_token.token)
        if record is None:
            raise OAuthStoreError("Refresh token không tồn tại hoặc đã dùng")
        # HIGH-08 (audit 2026-05-21): defense-in-depth — refresh_token đối tượng
        # SDK truyền có client_id; record từ store có client_id. SDK thường
        # ràng buộc đúng ở route /token, nhưng phòng vệ thêm tại provider
        # (RFC 6749 §4.1.3 / §6).
        if record["client_id"] != client.client_id:
            logger.warning(
                "exchange_refresh_token client_id mismatch — "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                client.client_id,
            )
            raise OAuthStoreError(
                "Refresh token không thuộc client này"
            )
```

LƯU Ý ngữ cảnh Plan 08 Task 2 (HIGH-02 family revocation):
Plan 08 Task 2 sẽ THAY THẾ TIẾP block `exchange_refresh_token` đoạn `if not ok:`
(family revocation). Block guard `record["client_id"] != client.client_id` ở
trên TUYỆT ĐỐI KHÔNG bị Plan 08 đụng — Plan 08 chỉ thêm code TRONG nhánh
`if not ok:` SAU `rotate_token`. Plan 08 Task 2 sẽ ghi BEFORE/AFTER block
nguyên vẹn để executor copy-thay-thế chính xác (chống drift).

(6) HIGH-07 — sửa `revoke_token` (provider.py:426-440): KHÔNG xoá nếu mismatch
(silent no-op per RFC 7009 §2.2). Thay thân method (giữ docstring + signature):

```python
        if isinstance(token, RefreshToken):
            record = await self._store.load_token_by_refresh(token.token)
        else:
            record = await self._store.load_token(token.token)
        if record is None:
            logger.info(
                "MedinetOAuthProvider revoke token — không tìm thấy record, "
                "client_id=%s", token.client_id,
            )
            return
        # HIGH-07 (audit 2026-05-21, RFC 7009 §2.1+§2.2): "token was issued to
        # the client making the revocation request" — kiểm record.client_id ==
        # token.client_id; mismatch → silent no-op (KHÔNG xoá, KHÔNG raise) per
        # §2.2 ("invalid tokens do not cause an error response").
        if record["client_id"] != token.client_id:
            logger.warning(
                "revoke_token client_id mismatch — silent no-op "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                token.client_id,
            )
            return
        await self._store.delete_token(record["access_token"])
        logger.info(
            "MedinetOAuthProvider revoke token, client_id=%s", token.client_id
        )
```

KHÔNG sửa method khác. KHÔNG log giá trị token/code/JWT thô (T-08.3-08).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `provider.py` chứa chuỗi `_ALLOWED_REDIRECT_HOSTS = frozenset(`
    - `provider.py` chứa chuỗi `def _validate_redirect_uri(uri: AnyUrl) -> None:`
    - `provider.py` chứa chuỗi `for redirect_uri in client_info.redirect_uris:` và `_validate_redirect_uri(redirect_uri)` (HIGH-01)
    - `provider.py` chứa chuỗi `record["client_id"] != client.client_id` XUẤT HIỆN ≥3 LẦN (load_authorization_code + exchange_authorization_code + exchange_refresh_token)
    - `provider.py` chứa chuỗi `record["client_id"] != token.client_id` (revoke_token, HIGH-07)
    - `provider.py` chứa chuỗi `Authorization code không thuộc client này` (CRIT-03 message exchange)
    - `provider.py` chứa chuỗi `Refresh token không thuộc client này` (HIGH-08 message)
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/provider.py` exits 0
    - `cd Hub_All/mcp_service && uv run python -c "import mcp_app.oauth.provider"` exits 0
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py -q` exits 0 (regression — test cũ KHÔNG vỡ; test cũ dùng cùng client trong load và exchange nên mismatch không trigger)
  </acceptance_criteria>
  <done>provider.py có guard client_id mismatch ở load_authorization_code + exchange_authorization_code + exchange_refresh_token + revoke_token; register_client validate redirect_uri whitelist; ruff sạch; import sạch; regression test_oauth_provider 17/17 PASS.</done>
</task>

<task type="auto">
  <name>Task 2: store.py + provider.py + login.py + conftest.py + test_oauth_login.py — CSRF token end-to-end cho login form (HIGH-09)</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/store.py (file đang sửa — _SCHEMA_STATEMENTS oauth_pending dòng 67-78, save_pending 388-417, load_pending 419-438)
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (file đang sửa — complete_authorization 198-271 sinh csrf_token, save_pending có thêm tham số csrf_token; authorize 172-196 sửa để truyền csrf_token vào save_pending)
    - Hub_All/mcp_service/mcp_app/oauth/login.py (file đang sửa — render_login_form 32-78, get_login_routes 81-174)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (HIGH-09 section II)
    - Hub_All/mcp_service/tests/conftest.py (fake_pending_authorize hiện tại — signature CHƯA có csrf_token)
    - Hub_All/mcp_service/tests/test_oauth_login.py (file đang sửa — test_login_get_renders_form dòng 65-77 hiện chỉ GET không seed pending; SAU Plan 07 login_get verify pending tồn tại → test cũ vỡ nếu không seed)
    - Hub_All/mcp_service/tests/test_oauth_store.py (test trực tiếp save_pending — dòng 165 — sẽ update signature)
  </read_first>
  <action>
**Verify caller-grep trước khi đụng signature (Warning #7):**

Trước khi sửa `save_pending` signature, executor PHẢI chạy grep verify CHỈ có 3
caller (không có caller bí mật khác):

```bash
cd Hub_All && grep -rn "save_pending(" --include="*.py" .
```

Output kỳ vọng (3 caller, không hơn):
- `mcp_service/mcp_app/oauth/provider.py` — `provider.authorize` (production)
- `mcp_service/tests/conftest.py` — `fake_pending_authorize` (test helper)
- `mcp_service/tests/test_oauth_store.py` — test trực tiếp store

Nếu grep cho thêm caller → DỪNG, báo blocker user. Nếu đúng 3 → tiếp tục.

5 file thay đổi — đi đầu-cuối từ schema → save → render → verify → cập nhật test
cũ:

(1) `Hub_All/mcp_service/mcp_app/oauth/store.py` — schema + save_pending + load_pending:

(1a) `_SCHEMA_STATEMENTS` oauth_pending CREATE TABLE (dòng 67-78): thêm cột
`csrf_token TEXT NOT NULL DEFAULT ''` SAU cột `code_challenge_method`. Đổi thành:

```python
    """
    CREATE TABLE IF NOT EXISTS oauth_pending (
        txn TEXT PRIMARY KEY,
        client_id TEXT NOT NULL,
        redirect_uri TEXT NOT NULL,
        code_challenge TEXT NOT NULL,
        code_challenge_method TEXT NOT NULL,
        csrf_token TEXT NOT NULL DEFAULT '',
        client_state TEXT,
        scopes TEXT NOT NULL,
        created_at INTEGER NOT NULL
    )
    """,
```

`DEFAULT ''` để CREATE TABLE IF NOT EXISTS chạy được khi DB cũ chưa có cột; với
DB hoàn toàn mới (test :memory:) cũng OK vì save_pending TỰ truyền csrf_token
non-empty. SỬA `init_schema` (dòng 99-110) thêm migration ALTER cho DB cũ:

```python
    async def init_schema(self) -> None:
        """Mở connection + tạo 4 bảng (CREATE TABLE IF NOT EXISTS — idempotent).

        HIGH-09 migration: thêm cột csrf_token cho oauth_pending nếu DB cũ
        không có. SQLite không hỗ trợ ALTER TABLE ... ADD COLUMN IF NOT EXISTS
        — phải kiểm PRAGMA table_info trước.
        """
        if self._conn is None:
            dirname = os.path.dirname(self.db_path)
            if dirname:
                os.makedirs(dirname, exist_ok=True)
            self._conn = await aiosqlite.connect(self.db_path)
        for statement in _SCHEMA_STATEMENTS:
            await self._conn.execute(statement)
        # HIGH-09 migration — back-compat DB cũ.
        async with self._conn.execute(
            "PRAGMA table_info(oauth_pending)"
        ) as cursor:
            cols = {row[1] for row in await cursor.fetchall()}
        if "csrf_token" not in cols:
            await self._conn.execute(
                "ALTER TABLE oauth_pending ADD COLUMN csrf_token TEXT "
                "NOT NULL DEFAULT ''"
            )
        await self._conn.commit()
        logger.info("OAuthStore khởi tạo schema — 4 bảng sẵn sàng")
```

(1b) Sửa `save_pending` (store.py:388-417): thêm tham số `csrf_token: str`
BẮT BUỘC (KHÔNG default) TRƯỚC `client_state`. Tất cả 3 caller (provider.authorize,
fake_pending_authorize, test_oauth_store) SẼ được update trong (3) + (4) + (5).
Sửa signature + INSERT:

```python
    async def save_pending(
        self,
        txn: str,
        client_id: str,
        redirect_uri: str,
        code_challenge: str,
        code_challenge_method: str,
        csrf_token: str,
        client_state: str | None,
        scopes: list,
        created_at: int,
    ) -> None:
        """Lưu tham số authorize() — giữ giữa lúc /authorize và lúc user login.

        HIGH-09: csrf_token sinh ở provider.authorize, embed vào login form
        hidden, verify ở /login/callback. Tham số BẮT BUỘC (không default) —
        contract đồng nhất giữa production và test helper.
        """
        conn = self._require_conn()
        await conn.execute(
            "INSERT OR REPLACE INTO oauth_pending "
            "(txn, client_id, redirect_uri, code_challenge, code_challenge_method, "
            "csrf_token, client_state, scopes, created_at) "
            "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)",
            (
                txn,
                client_id,
                redirect_uri,
                code_challenge,
                code_challenge_method,
                csrf_token,
                client_state,
                json.dumps(scopes),
                created_at,
            ),
        )
        await conn.commit()
        logger.info("OAuthStore lưu pending authorize cho client_id=%s", client_id)
```

(1c) Sửa `load_pending` (store.py:419-438): SELECT thêm `csrf_token`, dict
return thêm khoá `csrf_token`. Sửa SELECT + dict:

```python
    async def load_pending(self, txn: str) -> dict | None:
        """Đọc pending authorize theo txn. None nếu không tồn tại (đã dùng/hết hạn)."""
        conn = self._require_conn()
        async with conn.execute(
            "SELECT client_id, redirect_uri, code_challenge, code_challenge_method, "
            "csrf_token, client_state, scopes, created_at "
            "FROM oauth_pending WHERE txn = ?",
            (txn,),
        ) as cursor:
            row = await cursor.fetchone()
        if row is None:
            return None
        return {
            "client_id": row[0],
            "redirect_uri": row[1],
            "code_challenge": row[2],
            "code_challenge_method": row[3],
            "csrf_token": row[4],
            "client_state": row[5],
            "scopes": json.loads(row[6]),
            "created_at": row[7],
        }
```

(2) `Hub_All/mcp_service/mcp_app/oauth/provider.py` — sửa `authorize` +
thêm helper `load_pending_csrf`:

(2a) Sửa `authorize` (provider.py:172-196) — sinh csrf_token + truyền vào
save_pending. Thay đoạn `txn = self._new_opaque()` + `await self._store.save_pending(...)`:

```python
        txn = self._new_opaque()
        csrf_token = self._new_opaque()  # HIGH-09 — embed vào login form hidden
        await self._store.save_pending(
            txn=txn,
            client_id=client.client_id or "",
            redirect_uri=str(params.redirect_uri),
            code_challenge=params.code_challenge,
            code_challenge_method="S256",
            csrf_token=csrf_token,
            client_state=params.state,
            scopes=list(params.scopes or []),
            created_at=self._now(),
        )
```

(2b) Thêm method `load_pending_csrf` TRƯỚC `# --- DCR (RFC 7591) ---` (vùng
helper nội bộ):

```python
    # --- CSRF helpers (HIGH-09) ---

    async def load_pending_csrf(self, txn: str) -> str | None:
        """Lấy csrf_token cho txn — login route dùng để render hidden + verify.

        Trả None nếu txn không tồn tại (đã dùng/hết hạn). Method này KHÔNG xoá
        pending — chỉ đọc.
        """
        pending = await self._store.load_pending(txn)
        if pending is None:
            return None
        return pending.get("csrf_token") or None
```

(3) `Hub_All/mcp_service/mcp_app/oauth/login.py` — sửa `render_login_form` +
`login_get` + `login_callback`:

(3a) Sửa `render_login_form` (login.py:32-78): thêm tham số
`csrf_token: str`. Thêm hidden input. Đổi signature + body:

```python
def render_login_form(
    txn: str, callback_action: str, csrf_token: str, error: str | None = None
) -> HTMLResponse:
    """Trả HTML form đăng nhập tối giản.

    Args:
        txn: transaction id — đặt vào hidden input để POST callback nối lại flow.
        callback_action: URL form action — TUYỆT ĐỐI (đã prepend issuer_url).
        csrf_token: CSRF token sinh ở provider.authorize, embed hidden, verify
            ở POST callback (HIGH-09).
        error: thông báo lỗi (nếu có) — hiển thị dòng đỏ phía trên form.
    """
    safe_txn = html.escape(txn, quote=True)
    safe_action = html.escape(callback_action, quote=True)
    safe_csrf = html.escape(csrf_token, quote=True)
    error_block = ""
    if error:
        error_block = (
            f'<p style="color:#c0392b;font-weight:bold">{html.escape(error)}</p>'
        )
    body = f"""<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Đăng nhập Medinet Wiki</title>
</head>
<body style="font-family:sans-serif;max-width:360px;margin:48px auto">
  <h2>Đăng nhập Medinet Wiki</h2>
  <p>Đăng nhập bằng tài khoản Medinet Wiki để cấp quyền truy cập.</p>
  {error_block}
  <form method="post" action="{safe_action}">
    <input type="hidden" name="txn" value="{safe_txn}">
    <input type="hidden" name="csrf_token" value="{safe_csrf}">
    <p>
      <label for="email">Email</label><br>
      <input type="email" id="email" name="email" required
             style="width:100%;padding:6px">
    </p>
    <p>
      <label for="password">Mật khẩu</label><br>
      <input type="password" id="password" name="password" required
             style="width:100%;padding:6px">
    </p>
    <button type="submit" style="padding:8px 16px">Đăng nhập</button>
  </form>
</body>
</html>"""
    return HTMLResponse(body)
```

(3b) Sửa `login_get` (login.py:95-103) — load csrf_token từ provider khi
render form:

```python
    async def login_get(request: Request) -> Response:
        """GET /login — trả HTML form. Thiếu `txn` hoặc pending miss -> 400."""
        txn = request.query_params.get("txn")
        if not txn:
            return HTMLResponse(
                "<p>Thiếu tham số txn — phiên đăng nhập không hợp lệ.</p>",
                status_code=400,
            )
        csrf = await provider.load_pending_csrf(txn)
        if csrf is None:
            return HTMLResponse(
                "<p>Phiên đăng nhập hết hạn hoặc không hợp lệ.</p>",
                status_code=400,
            )
        return render_login_form(txn, callback_action, csrf_token=csrf)
```

LƯU Ý hành vi mới: `login_get` giờ verify `pending` tồn tại — request không seed
pending sẽ nhận **400** thay vì 200. Test cũ `test_login_get_renders_form`
hiện gọi `client.get("/login?txn=txn-abc")` KHÔNG seed pending → SẼ VỠ (Blocker
#1). (5) bên dưới update test cũ để seed pending TRƯỚC GET.

(3c) Sửa `login_callback` (login.py:105-169) — verify csrf_token form khớp
csrf_token pending TRƯỚC khi gọi api_client.login. Đoạn `form = await request.form()`
+ kiểm txn không đổi; thêm khối verify CSRF ngay sau:

```python
    async def login_callback(request: Request) -> Response:
        """POST /login/callback — xác thực credential Medinet, phát code OAuth."""
        form = await request.form()
        txn = str(form.get("txn") or "")
        email = str(form.get("email") or "")
        password = str(form.get("password") or "")
        form_csrf = str(form.get("csrf_token") or "")

        if not txn:
            return HTMLResponse(
                "<p>Thiếu tham số txn — phiên đăng nhập không hợp lệ.</p>",
                status_code=400,
            )

        # HIGH-09: verify CSRF — form csrf_token PHẢI khớp pending csrf_token.
        # Thiếu / sai → render lại form với lỗi. KHÔNG log giá trị csrf_token
        # (T-08.3-08). load_pending_csrf trả None → pending không tồn tại
        # (đã dùng / hết hạn).
        pending_csrf = await provider.load_pending_csrf(txn)
        if pending_csrf is None:
            logger.info("Login callback — pending không tồn tại / hết hạn")
            return HTMLResponse(
                "<p>Phiên đăng nhập hết hạn hoặc không hợp lệ.</p>",
                status_code=400,
            )
        if not form_csrf or not secrets.compare_digest(form_csrf, pending_csrf):
            logger.warning("Login callback — CSRF token mismatch")
            return render_login_form(
                txn,
                callback_action,
                csrf_token=pending_csrf,
                error="Phiên đăng nhập không hợp lệ, vui lòng thử lại",
            )

        # (a) Xác thực credential Medinet qua API Service — KHÔNG verify tại MCP.
        try:
            login_data = await api_client.login(email, password)
        except ApiClientError:
            logger.error("Login callback — lỗi hạ tầng khi gọi API Service")
            return render_login_form(
                txn, callback_action, csrf_token=pending_csrf,
                error="Lỗi hệ thống, vui lòng thử lại sau",
            )

        # (b) Credential sai -> render lại form kèm lỗi.
        if login_data is None:
            return render_login_form(
                txn, callback_action, csrf_token=pending_csrf,
                error="Sai tài khoản hoặc mật khẩu",
            )

        # (c) Login OK -> phát authorization code, redirect về client.
        try:
            code, redirect_uri, client_state = await provider.complete_authorization(
                txn, login_data
            )
        except BindMismatchError:
            logger.info(
                "Login callback — bind mismatch (user khác chủ sở hữu connector)"
            )
            return render_login_form(
                txn,
                callback_action,
                csrf_token=pending_csrf,
                error=(
                    "Tài khoản này không phải chủ sở hữu connector. "
                    "Đăng nhập bằng đúng tài khoản đã sinh credentials, hoặc tạo "
                    "credentials mới ở Profile của tài khoản hiện tại."
                ),
            )
        except OAuthStoreError:
            logger.info("Login callback — txn hết hạn hoặc không hợp lệ")
            return render_login_form(
                txn, callback_action, csrf_token=pending_csrf,
                error="Phiên đăng nhập hết hạn, vui lòng kết nối lại",
            )
        except (KeyError, TypeError):
            logger.error("Login callback — payload downstream thiếu trường bắt buộc")
            return render_login_form(
                txn, callback_action, csrf_token=pending_csrf,
                error="Lỗi hệ thống, vui lòng thử lại sau",
            )

        location = f"{redirect_uri}?code={code}"
        if client_state is not None:
            location += f"&state={client_state}"
        logger.info("Login callback thành công — redirect về client")
        return RedirectResponse(location, status_code=302)
```

Thêm `import secrets` đầu file login.py (sau `import logging`).

(4) Sửa `Hub_All/mcp_service/tests/conftest.py` — `fake_pending_authorize`
helper. Vì `save_pending` giờ BẮT BUỘC `csrf_token`, helper PHẢI truyền. Cho
test back-compat, helper có default `csrf_token: str = "csrf-test-fixed"`. Sửa
signature thêm `csrf_token: str = "csrf-test-fixed"` SAU `code_challenge_method`.
Trong body, truyền `csrf_token=csrf_token` vào `await store.save_pending(...)`:

```python
async def fake_pending_authorize(
    store,
    *,
    txn: str = "txn-test",
    client_id: str = "client-test",
    redirect_uri: str = "https://claude.ai/api/mcp/auth_callback",
    code_challenge: str = "challenge-test",
    code_challenge_method: str = "S256",
    csrf_token: str = "csrf-test-fixed",  # HIGH-09 — helper test default
    client_state: str | None = "client-state-test",
    scopes: list[str] | None = None,
    created_at: int | None = None,
) -> str:
    """Seed 1 bản ghi oauth_pending để test login callback nối lại flow.

    Trả về `txn` đã seed. csrf_token mặc định `"csrf-test-fixed"` — test mới
    HIGH-09 truyền tường minh giá trị khác để test verify mismatch.
    """
    import time

    await store.save_pending(
        txn=txn,
        client_id=client_id,
        redirect_uri=redirect_uri,
        code_challenge=code_challenge,
        code_challenge_method=code_challenge_method,
        csrf_token=csrf_token,
        client_state=client_state,
        scopes=scopes or ["wiki"],
        created_at=created_at if created_at is not None else int(time.time()),
    )
    return txn
```

(5) **Update `tests/test_oauth_login.py::test_login_get_renders_form`** (Blocker #1)
— login_get giờ verify pending; GET không seed pending sẽ nhận 400. Sửa test
seed pending TRƯỚC GET, dùng pattern Starlette TestClient + `_get_oauth_store`
+ `fake_pending_authorize` giống `test_login_callback_success_redirects`:

```python
def test_login_get_renders_form(oauth_env) -> None:
    """GET /login?txn=x sau khi seed pending -> 200 + HTML chứa form + email + password + hidden csrf_token.

    Plan 07 (HIGH-09): login_get giờ load_pending_csrf — pending miss → 400.
    Test PHẢI seed pending TRƯỚC GET.
    """
    import asyncio

    from mcp_app.server import _get_oauth_store, build_asgi_app

    from .conftest import fake_pending_authorize

    app = build_asgi_app()
    with TestClient(app) as client:
        store = _get_oauth_store()
        txn = asyncio.new_event_loop().run_until_complete(
            fake_pending_authorize(store, txn="txn-abc", csrf_token="csrf-render-test")
        )
        resp = client.get(f"/login?txn={txn}")
    assert resp.status_code == 200
    body = resp.text
    assert "<form" in body
    assert 'name="email"' in body
    assert 'name="password"' in body
    assert "txn-abc" in body
    # HIGH-09: hidden csrf_token render đúng giá trị seed.
    assert 'name="csrf_token"' in body
    assert "csrf-render-test" in body
```

LƯU Ý các test khác trong test_oauth_login.py:
- `test_login_callback_wrong_credential`: hiện POST `/login/callback` với
  `txn=txn-x` KHÔNG seed pending → sau Plan 07 sẽ nhận 400 (pending miss) thay
  vì 200 render error. SỬA test seed pending + thêm `csrf_token` form khớp:
  ```python
  # Bên trong test:
  with TestClient(app) as client:
      store = _get_oauth_store()
      txn = asyncio.new_event_loop().run_until_complete(
          fake_pending_authorize(store, txn="txn-x", csrf_token="csrf-x")
      )
      resp = client.post(
          "/login/callback",
          data={"txn": txn, "csrf_token": "csrf-x",
                "email": "a@b.com", "password": "sai"},
      )
  ```
- `test_login_callback_success_redirects`: dùng `fake_pending_authorize` default
  `csrf_token="csrf-test-fixed"` → POST form PHẢI truyền `csrf_token="csrf-test-fixed"`.
  Sửa `data=` trong POST thêm `"csrf_token": "csrf-test-fixed"`.
- `test_lifespan_inits_schema` + `test_metadata_endpoint` + `test_protected_resource_metadata`:
  KHÔNG đụng login route → KHÔNG vỡ.

(6) Sửa `tests/test_oauth_store.py:165` test trực tiếp `save_pending` — thêm
kwarg `csrf_token="csrf-store-test"` vào call. Đọc test cũ trước khi sửa
(ĐỌC `test_oauth_store.py` quanh dòng 165) để biết tên test, thêm kwarg đúng vị
trí. Test trực tiếp store sẽ assert load_pending trả dict có `csrf_token` key
giá trị `"csrf-store-test"` (THÊM 1 assertion nhỏ — không bắt buộc, nhưng
recommended để verify migration end-to-end).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/store.py mcp_app/oauth/provider.py mcp_app/oauth/login.py tests/conftest.py tests/test_oauth_login.py tests/test_oauth_store.py</automated>
  </verify>
  <acceptance_criteria>
    - `store.py` `_SCHEMA_STATEMENTS` chứa chuỗi `csrf_token TEXT NOT NULL DEFAULT ''`
    - `store.py` `init_schema` chứa chuỗi `PRAGMA table_info(oauth_pending)` và `ALTER TABLE oauth_pending ADD COLUMN csrf_token`
    - `store.py` `save_pending` signature có tham số `csrf_token: str` (KHÔNG default) và INSERT 9 placeholder
    - `store.py` `load_pending` SELECT chứa `csrf_token` và dict return có key `csrf_token`
    - `provider.py` `authorize` chứa `csrf_token = self._new_opaque()` và truyền vào `save_pending(...csrf_token=csrf_token...)`
    - `provider.py` chứa `async def load_pending_csrf(self, txn: str) -> str | None`
    - `login.py` `render_login_form` signature có tham số `csrf_token: str`
    - `login.py` chứa chuỗi `secrets.compare_digest(form_csrf, pending_csrf)`
    - `login.py` chứa chuỗi `<input type="hidden" name="csrf_token"`
    - `login.py` chứa chuỗi `import secrets`
    - `conftest.py` `fake_pending_authorize` signature có `csrf_token: str = "csrf-test-fixed"` và truyền vào `save_pending(...csrf_token=csrf_token...)`
    - `test_oauth_login.py::test_login_get_renders_form` chứa `fake_pending_authorize` + assertion `'name="csrf_token"' in body` (Blocker #1 closed)
    - `test_oauth_login.py::test_login_callback_wrong_credential` chứa `fake_pending_authorize` + `csrf_token` trong data POST
    - `test_oauth_login.py::test_login_callback_success_redirects` POST data chứa `csrf_token`
    - `cd Hub_All && grep -rn "save_pending(" --include="*.py" . | wc -l` trả ĐÚNG số caller kỳ vọng (3 production+test + nội bộ `save_pending(` definition = 4 line; chứng minh không có caller bí mật)
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/ tests/` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q` exits 0 (regression — toàn bộ test cũ update tương thích contract mới)
  </acceptance_criteria>
  <done>schema oauth_pending có csrf_token (migration back-compat); save_pending/load_pending propagate csrf_token (BẮT BUỘC, không default); provider.authorize sinh + lưu; login_get verify pending tồn tại + render hidden; login_callback verify với secrets.compare_digest; conftest fake_pending_authorize default csrf_token cho compat helper; test_login_get_renders_form + test_login_callback_wrong_credential update seed pending; test_oauth_store update kwarg; ruff sạch; regression 89/89 PASS.</done>
</task>

<task type="auto">
  <name>Task 3: server.py — bỏ CORS double-add ở path-prefix mode (CRIT-02) + regression gate</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/server.py (file đang sửa — branch path-prefix dòng 600-708, `_add_cors_middleware(inner)` dòng 607, `_add_cors_middleware(wrapper)` dòng 702, `if not prefix: return _BasicAuthFormShim(inner)` dòng 611-612)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (CRIT-02 section I Critical)
    - Hub_All/mcp_service/tests/test_path_prefix_wrapper.py (test_cors_actual_get_metadata hiện PASS vì test ở root wrapper; KHÔNG vỡ sau fix vì wrapper outer giữ CORS)
  </read_first>
  <action>
Sửa `Hub_All/mcp_service/mcp_app/server.py` — DUY NHẤT 1 thay đổi logic, thật cẩn thận để KHÔNG vỡ subdomain mode:

Đọc kỹ flow `build_asgi_app`:
- Dòng 607: `_add_cors_middleware(inner)` — gắn lên inner CHO MỌI MODE.
- Dòng 611-612: `if not prefix: return _BasicAuthFormShim(inner)` — subdomain
  mode RETURN ngay (CORS đã add ở inner — DUY NHẤT 1 lần, không vỡ subdomain).
- Dòng 614+: path-prefix mode dựng wrapper Starlette, mount inner.
- Dòng 702: `_add_cors_middleware(wrapper)` — gắn lên wrapper outer.

Vấn đề CRIT-02: path-prefix mode CORS add CẢ inner (dòng 607) LẪN wrapper (dòng
702) → response duplicate `Access-Control-Allow-Origin`.

Fix: TÁCH branch — chỉ add CORS ở inner CHO subdomain mode; path-prefix mode
add CORS ở wrapper outer thôi. Cách clean nhất: di chuyển dòng 607
`_add_cors_middleware(inner)` vào TRONG khối `if not prefix:` — nghĩa là chỉ
subdomain mode mới add inner.

Cụ thể: tìm đoạn:

```python
    # CORS — RFC 8414 §3.1 / RFC 9728 §3.1 yêu cầu metadata endpoint OAuth
    # support CORS để browser-based client dùng được. MCP Inspector
    # (localhost:6274) + Claude web (claude.ai) fetch cross-origin → cần
    # `Access-Control-Allow-Origin`. Public AS — không dùng cookie credentials
    # (Authorization Bearer header), an toàn `allow_origins=*` +
    # `allow_credentials=False`. Áp dụng CẢ 2 mode (subdomain + path-prefix)
    # cùng config để hành vi đồng nhất.
    _add_cors_middleware(inner)

    # Subdomain/authority-root deploy → trả inner bọc _BasicAuthFormShim
    # (defense-in-depth Basic→form, xem class docstring).
    if not prefix:
        return _BasicAuthFormShim(inner)
```

THAY BẰNG:

```python
    # CORS — RFC 8414 §3.1 / RFC 9728 §3.1 yêu cầu metadata endpoint OAuth
    # support CORS để browser-based client dùng được. MCP Inspector
    # (localhost:6274) + Claude web (claude.ai) fetch cross-origin → cần
    # `Access-Control-Allow-Origin`.
    #
    # CRIT-02 (audit 2026-05-21): CHỈ add CORS Ở MỘT TẦNG. Trước fix:
    # subdomain mode add ở inner; path-prefix mode add ở CẢ inner (dòng cũ) +
    # wrapper (dòng dưới) → response duplicate `Access-Control-Allow-Origin`
    # → browser reject (CORS spec: header duplicate). Toàn bộ OAuth flow vỡ
    # với Claude web + MCP Inspector ở path-prefix deploy chính.
    #
    # Fix: subdomain mode add ở inner (return ngay sau); path-prefix mode add
    # ở wrapper outer THÔI (dòng `_add_cors_middleware(wrapper)` dưới).

    # Subdomain/authority-root deploy → trả inner bọc _BasicAuthFormShim
    # (defense-in-depth Basic→form, xem class docstring). CORS add ở inner —
    # request đi qua _BasicAuthFormShim → inner CORSMiddleware → SDK app.
    if not prefix:
        _add_cors_middleware(inner)
        return _BasicAuthFormShim(inner)
```

(Logic mới: subdomain mode CORS add ở inner — DUY NHẤT 1 lần; path-prefix mode
KHÔNG add ở inner — CORS chỉ ở wrapper outer, cũng DUY NHẤT 1 lần.)

KHÔNG đụng đến dòng 702 `_add_cors_middleware(wrapper)` — giữ nguyên.

Verify thủ công logic: subdomain (prefix="") → if branch chạy → `_add_cors_middleware(inner)` +
return. Path-prefix (prefix="mcp") → if KHÔNG chạy → tiếp dòng 614+ dựng
wrapper → cuối có `_add_cors_middleware(wrapper)`. ĐÚNG 1 lần mỗi mode.

(2) Regression gate cuối plan — chạy:

```
cd Hub_All/mcp_service && uv run pytest -q
```

Test mong đợi PASS:
- `test_cors_preflight_metadata_endpoint` (path-prefix mode, root metadata → wrapper outer xử lý OPTIONS, có CORS header) — vẫn PASS.
- `test_cors_actual_get_metadata` (path-prefix mode, GET root metadata) — vẫn PASS.
- `test_path_prefix_wraps_with_mount_and_metadata_routes` — không phụ thuộc CORS — PASS.
- Test subdomain mode (test_server.py + test_oauth_*) — CORS vẫn ở inner → PASS.

Test có thể vỡ:
- KHÔNG test nào hiện tại assert "ACAO xuất hiện ĐÚNG 1 lần ở path-prefix mode"
  → KHÔNG vỡ. (Test mới ở Plan 09 sẽ assert.)

Nếu pytest exit != 0: KHÔNG nới lỏng fix. Đọc trace, hiểu nguyên nhân:
- Test cũ có thể assert `_add_cors_middleware` gọi 2 lần ở path-prefix → sửa
  test cho đúng behavior mới (1 lần).
- Ghi rõ trong SUMMARY test nào chỉnh và vì sao.

Mục tiêu: pytest exit 0.

(3) Lint sạch:

```
cd Hub_All/mcp_service && uv run ruff check mcp_app/server.py
```

KHÔNG đụng module khác trong task này.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `server.py` đoạn `if not prefix:` block chứa CẢ HAI dòng `_add_cors_middleware(inner)` VÀ `return _BasicAuthFormShim(inner)`
    - `server.py` KHÔNG còn dòng `_add_cors_middleware(inner)` Ở NGOÀI `if not prefix:` branch (ở vị trí cũ dòng 607)
    - `server.py` vẫn còn dòng `_add_cors_middleware(wrapper)` ở branch path-prefix (gần dòng 702 cũ)
    - `cd Hub_All/mcp_service && uv run python -c "import ast; t=ast.parse(open('mcp_app/server.py',encoding='utf-8').read()); src=open('mcp_app/server.py',encoding='utf-8').read(); import sys; sys.exit(0 if src.count('_add_cors_middleware(inner)') == 1 and src.count('_add_cors_middleware(wrapper)') == 1 else 1)"` exits 0
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/server.py` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q -m critical` exits 0
  </acceptance_criteria>
  <done>server.py path-prefix mode KHÔNG add CORS ở inner; subdomain mode vẫn giữ CORS ở inner (sau if branch); _add_cors_middleware(inner) xuất hiện ĐÚNG 1 LẦN trong toàn file (trong if branch); _add_cors_middleware(wrapper) giữ nguyên; regression 89/89 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|----------|-------|
| Browser (Claude web / MCP Inspector) → MCP Service `/token` `/authorize` `/revoke` | CORS controlled — duplicate ACAO header crash flow (CRIT-02) |
| OAuth client A (DCR registered) → MCP Service `/token` | Khi A gửi code/refresh của client B kèm credential của A — chain cross-client (CRIT-03/HIGH-08) |
| OAuth client → MCP Service `/register` (DCR) | Client tự khai `redirect_uris` — attacker đăng ký redirect tới domain mình (HIGH-01) |
| OAuth client → MCP Service `/revoke` | Client A gọi revoke token của client B — RFC 7009 §2.1 (HIGH-07) |
| Browser user → MCP Service `/login` form | CSRF attack — attacker fishing user nhập credential cho txn của attacker (HIGH-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-32 | Elevation/Spoofing | `exchange_authorization_code` + `load_authorization_code` (provider.py) — cross-client code exchange | mitigate | Task 1: kiểm `record["client_id"] != client.client_id` ở CẢ 2 method — `exchange` raise OAuthStoreError, `load` trả None. Đóng CRIT-03 chain với `_BasicAuthFormShim` (HIGH-08). |
| T-08.3-33 | Elevation | `exchange_refresh_token` (provider.py) — cross-client refresh exchange | mitigate | Task 1: kiểm `record["client_id"] != client.client_id`. Defense-in-depth HIGH-08. |
| T-08.3-34 | Tampering | `revoke_token` (provider.py) — client A revoke token client B | mitigate | Task 1: kiểm `record["client_id"] != token.client_id` → silent no-op (RFC 7009 §2.2). HIGH-07. |
| T-08.3-35 | Spoofing/Information Disclosure | `register_client` (provider.py) — attacker đăng ký redirect_uri attacker-controlled, code interception | mitigate | Task 1: whitelist host (claude.ai, modelcontextprotocol.io, localhost, 127.0.0.1) + suffix subdomain; scheme http chỉ cho loopback. HIGH-01. |
| T-08.3-36 | Denial of Service | `build_asgi_app` path-prefix — duplicate CORS header → browser reject toàn bộ OAuth flow | mitigate | Task 3: bỏ `_add_cors_middleware(inner)` ở path-prefix mode (giữ wrapper outer); subdomain mode giữ inner. CRIT-02. |
| T-08.3-37 | Tampering/Spoofing | `login_callback` (login.py) — CSRF; attacker phishing user submit credential cho txn của attacker | mitigate | Task 2: hidden csrf_token sinh ở provider.authorize (lưu pending), embed login form, verify `secrets.compare_digest` ở callback. HIGH-09. |
| T-08.3-38 | Repudiation | Log mismatch client_id / CSRF mismatch | accept | Log chỉ ghi `client_id` + tên thao tác + `mismatch` — KHÔNG log token/code/csrf giá trị thô (T-08.3-08). Rủi ro thấp. |
</threat_model>

<verification>
- `provider.py` chứa `record["client_id"] != client.client_id` ≥ 3 lần (load_auth_code + exchange_auth_code + exchange_refresh_token).
- `provider.py` chứa `record["client_id"] != token.client_id` (revoke_token).
- `provider.py` chứa `_validate_redirect_uri` + `_ALLOWED_REDIRECT_HOSTS` + gọi `_validate_redirect_uri` trong `register_client`.
- `provider.py` chứa `load_pending_csrf` + `csrf_token = self._new_opaque()` trong `authorize`.
- `store.py` schema oauth_pending có cột `csrf_token`; `save_pending` / `load_pending` propagate `csrf_token`.
- `store.py` `init_schema` có ALTER migration cho DB cũ.
- `store.py` `save_pending` signature BẮT BUỘC `csrf_token` (KHÔNG default).
- `login.py` `render_login_form` có `csrf_token` parameter + hidden input; `login_callback` verify `secrets.compare_digest`.
- `login.py` `login_get` verify pending tồn tại (load_pending_csrf → 400 nếu None).
- `conftest.py` `fake_pending_authorize` có default `csrf_token: str = "csrf-test-fixed"` truyền vào save_pending.
- `test_oauth_login.py` 3 test (renders_form + wrong_credential + success_redirects) update seed pending + csrf_token form.
- `server.py` `_add_cors_middleware(inner)` xuất hiện ĐÚNG 1 LẦN trong `if not prefix:` branch; `_add_cors_middleware(wrapper)` giữ nguyên.
- `cd Hub_All/mcp_service && uv run ruff check mcp_app/ tests/` exits 0.
- `cd Hub_All/mcp_service && uv run pytest -q` exits 0 — regression 89/89 không vỡ.
</verification>

<success_criteria>
- CRIT-01 (CORS allow_origins=*): không trong scope plan này — defer Plan 10 (cần tách 2 sub-app Starlette). Đã ghi trong audit Action Plan; SUMMARY plan 07 ghi rõ.
- CRIT-02 (CORS double-add path-prefix): server.py path-prefix mode CHỈ add CORS ở wrapper outer.
- CRIT-03 (cross-client code exchange): provider.py guard client_id mismatch ở CẢ load_authorization_code LẪN exchange_authorization_code.
- HIGH-01 (DCR redirect_uris): register_client validate whitelist host + scheme; reject http non-loopback.
- HIGH-07 (revoke RFC 7009 §2.1): revoke_token silent no-op nếu client_id mismatch.
- HIGH-08 (BasicAuthFormShim defense): exchange_refresh_token guard client_id (Task 1 sub-fix); CRIT-03 fix đã chặn chain chính.
- HIGH-09 (CSRF login): csrf_token end-to-end pending → form hidden → POST verify.
- Regression mcp_service pytest 89/89 PASS (bao gồm test_login_get_renders_form + test_login_callback_wrong_credential + test_login_callback_success_redirects sau update); test critical PASS.

LƯU Ý CRIT-01 defer: theo audit Section VIII Action Plan, CRIT-01 cần research
Starlette per-route CORS pattern + dựng 2 sub-app. Tách khỏi plan 07 để giữ
plan này focused vào fix có code mẫu rõ ràng. CRIT-01 defer Phase 10
hardening — ghi rõ trong CONTEXT defer + audit Action Plan ghi nhận.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-07-SUMMARY.md`.
Commit atomic 3 task riêng:
- `fix(08.3-07): vá CRIT-03/HIGH-01/07/08 client_id mismatch + DCR redirect_uri whitelist`
- `fix(08.3-07): thêm CSRF token end-to-end cho login form (HIGH-09) + cập nhật test cũ contract mới`
- `fix(08.3-07): bỏ CORS double-add ở path-prefix mode (CRIT-02)`
</output>
</content>
</invoke>