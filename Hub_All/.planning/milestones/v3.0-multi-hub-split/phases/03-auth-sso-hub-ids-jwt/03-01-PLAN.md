---
phase: 03-auth-sso-hub-ids-jwt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/auth/jwks.py
  - api/app/main.py
  - api/app/config.py
  - api/tests/unit/test_jwks_publish.py
  - docker-compose.yml
autonomous: true
requirements:
  - SSO-01
tags:
  - jwks
  - rfc-7517
  - jwt-rs256
  - central-publish
  - public-key-export
  - d-v3-phase3-a
  - d-v3-phase3-d
  - r-v3-5
must_haves:
  truths:
    - "Central expose `GET /.well-known/jwks.json` public RS256 (D-V3-Phase3-A)"
    - "Response body khớp RFC 7517 JWK Set: `{\"keys\":[{\"kty\":\"RSA\",\"kid\":\"...\",\"use\":\"sig\",\"alg\":\"RS256\",\"n\":\"...\",\"e\":\"AQAB\"}]}`"
    - "Endpoint mount CHỈ khi `settings.hub_name == \"central\"` (D-V3-Phase3-D — hub con tự cache, KHÔNG re-publish)"
    - "Endpoint public (KHÔNG auth) + cache header `Cache-Control: public, max-age=3600` (RFC 7517 caching khuyến nghị)"
    - "Settings field `central_jwks_url: str | None = None` mới (Plan 03-02 hub con consume) — default None ở central, REQUIRED ở hub con (sẽ validate Plan 03-02)"
    - "JWK export thuần stdlib `cryptography` (M2 đã có qua PyJWT[crypto]) — KHÔNG thêm dep mới"
    - "Docker-compose 3 service hub con (yte/duoc/hcns) thêm env `CENTRAL_JWKS_URL=http://python-api-central:8080/.well-known/jwks.json` (intra-network HTTP)"
  artifacts:
    - path: "api/app/auth/jwks.py"
      provides: "publish_jwks() function + load_public_key_as_jwk() helper RFC 7517 export"
      contains: "def publish_jwks|def load_public_key_as_jwk"
    - path: "api/app/main.py"
      provides: "GET /.well-known/jwks.json route mount conditional central"
      contains: "/.well-known/jwks.json"
    - path: "api/app/config.py"
      provides: "Settings.central_jwks_url field mới (Plan 03-02 hub con consume)"
      contains: "central_jwks_url"
    - path: "api/tests/unit/test_jwks_publish.py"
      provides: "Unit test JWK serialize RFC 7517 + central mount / hub strip"
      contains: "def test_jwks_keys_shape_rfc_7517|def test_jwks_endpoint_mount_central_only"
    - path: "docker-compose.yml"
      provides: "CENTRAL_JWKS_URL env cho 3 hub con block"
      contains: "CENTRAL_JWKS_URL"
  key_links:
    - from: "api/app/main.py::create_app() (Phase 2 conditional mount block)"
      to: "api/app/auth/jwks.py::publish_jwks()"
      via: "@app.get('/.well-known/jwks.json') route handler"
      pattern: "/.well-known/jwks.json"
    - from: "api/app/auth/jwks.py::load_public_key_as_jwk()"
      to: "api/keys/public.pem (M2 keypair carry forward)"
      via: "settings.jwt_public_key_path → read_bytes → cryptography.hazmat.primitives.serialization.load_pem_public_key"
      pattern: "load_pem_public_key"
    - from: "docker-compose.yml (3 hub con service block)"
      to: "Settings.central_jwks_url (Phase 1 + Plan 03-02 consume)"
      via: "environment.CENTRAL_JWKS_URL: http://python-api-central:8080/.well-known/jwks.json"
      pattern: "CENTRAL_JWKS_URL"
---

<objective>
Mở rộng central FastAPI app expose JWKS endpoint `GET /.well-known/jwks.json` serve public key RS256 PKCS#8 dạng JWK Set (RFC 7517) — pattern chuẩn SSO mở rộng dễ tới hub con + frontend + MCP service (Phase 7). Endpoint CHỈ mount khi `settings.hub_name == "central"` (D-V3-Phase3-A LOCKED — KHÔNG shared keypair file mount / KHÔNG cookie domain `.medinet.vn`). JWK export thuần `cryptography` stdlib (M2 đã có qua PyJWT[crypto]) → KHÔNG thêm dep mới. Settings thêm field `central_jwks_url: str | None = None` cho Plan 03-02 hub con consume; docker-compose 3 hub con thêm env `CENTRAL_JWKS_URL=http://python-api-central:8080/.well-known/jwks.json`.

Plan này BLOCKING (Wave 1) — Plan 03-02 (hub con JWKSCache) phải có endpoint này lên trước.

Purpose: SSO-01 part 1 (publish layer). R-V3-5 mitigation (HA — central rotation key → hub con auto-detect ở next TTL refresh ở Plan 03-02). Pattern RFC 7517 — frontend Phase 5 + MCP Phase 7 reuse cùng JWKS verify path qua `jose.js` / Python lib.

Output: 4 file mới/sửa (`api/app/auth/jwks.py` mới + `api/app/main.py` thêm route + `api/app/config.py` thêm field + `tests/unit/test_jwks_publish.py` mới) + 1 file sửa minor (`docker-compose.yml` thêm env 3 hub con).

**Locked decisions consume (CONTEXT.md 03-CONTEXT.md):**
- **D-V3-Phase3-A:** JWKS endpoint pattern RFC 7517 (KHÔNG shared keypair / cookie domain) — endpoint format `{"keys":[{...}]}`.
- **D-V3-Phase3-D:** JWKS cache in-process LRU ở hub con (KHÔNG Redis) — Plan 03-01 chỉ ship publish layer; cache layer ở Plan 03-02.
- **Claude's Discretion (03-CONTEXT.md):** `jwcrypto` library proposal — REJECT (over-engineer). Dùng `cryptography` (M2 đã có qua PyJWT[crypto]) manual JWK export (RSAPublicNumbers → n/e base64url). Tránh thêm dep ngoài Python stdlib + M2 baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-01-PLAN.md
@Hub_All/api/app/auth/jwt.py
@Hub_All/api/app/main.py
@Hub_All/api/app/config.py
@Hub_All/docker-compose.yml

<interfaces>
<!-- M2 baseline + Phase 2 carry forward. Plan 03-01 consume nguyên xi, extend thêm. -->

Từ `api/app/auth/jwt.py` (M2 ship — Plan 03-01 KHÔNG sửa):

```python
JWT_ISSUER = "medinet-wiki"
JWT_ALGORITHM = "RS256"
JWT_ALGORITHMS_ALLOWED = [JWT_ALGORITHM]


class JWTManager:
    """Sign/verify JWT RS256. Load PEM 1 lần ở constructor."""
    def __init__(self, settings: Settings) -> None:
        self._private_pem = self._load_key(settings.jwt_private_key_path)
        self._public_pem = self._load_key(settings.jwt_public_key_path)
        # ...
```

Từ `api/app/config.py` (Phase 1 + Plan 02-05 ship — Plan 03-01 thêm 1 field mới):

```python
class Settings(BaseSettings):
    hub_name: str = "central"  # Plan 02-05 FACTOR-04 dynamic
    jwt_private_key_path: Path = Path("./keys/private.pem")
    jwt_public_key_path: Path = Path("./keys/public.pem")
    jwt_access_token_ttl: int = 900
    jwt_refresh_token_ttl: int = 604800
    # PLAN 03-01 THÊM:
    central_jwks_url: str | None = None  # central để None; hub con set qua env
```

Từ `api/app/main.py` (Phase 2 Plan 02-01 ship — Plan 03-01 thêm 1 route conditional):

```python
def create_app() -> FastAPI:
    settings = get_settings()
    # ... middleware setup ...

    # Tier 2 — Central-only routers (FACTOR-02 strip ở hub con)
    if settings.hub_name == "central":
        # ... 9 router include ...
        # PLAN 03-01 THÊM: JWKS endpoint mount cùng block central-only
        from app.auth.jwks import publish_jwks
        @app.get("/.well-known/jwks.json", ...)
        async def jwks_endpoint() -> JSONResponse: ...
```

Pattern PEM → JWK (RFC 7517) qua `cryptography` (M2 baseline — PyJWT[crypto] đã pull):

```python
from cryptography.hazmat.primitives import serialization
from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicKey, RSAPublicNumbers
import base64

def _int_to_base64url(n: int) -> str:
    """Big-int → base64url unpadded (RFC 7518 Section 6.3.1)."""
    byte_length = (n.bit_length() + 7) // 8
    data = n.to_bytes(byte_length, byteorder="big")
    return base64.urlsafe_b64encode(data).rstrip(b"=").decode("ascii")

def load_public_key_as_jwk(pem_path: Path, kid: str) -> dict[str, str]:
    pem_bytes = pem_path.read_bytes()
    pub_key = serialization.load_pem_public_key(pem_bytes)
    if not isinstance(pub_key, RSAPublicKey):
        raise ValueError("Public key không phải RSA")
    numbers: RSAPublicNumbers = pub_key.public_numbers()
    return {
        "kty": "RSA",
        "kid": kid,
        "use": "sig",
        "alg": "RS256",
        "n": _int_to_base64url(numbers.n),
        "e": _int_to_base64url(numbers.e),
    }
```

Pattern kid derivation: SHA-256 8 byte đầu của PEM (deterministic — central rotate key → kid đổi tự nhiên):

```python
import hashlib
def _derive_kid(pem_bytes: bytes) -> str:
    digest = hashlib.sha256(pem_bytes).digest()
    return base64.urlsafe_b64encode(digest[:8]).rstrip(b"=").decode("ascii")
```

M2 keypair path (carry forward Phase 1+2):
- `api/keys/private.pem` — PKCS#8 RS256 2048-bit (M2 generate qua `make keys`)
- `api/keys/public.pem` — public key SubjectPublicKeyInfo PEM (`-----BEGIN PUBLIC KEY-----`)
</interfaces>

<notes>
**Tách module mới `api/app/auth/jwks.py`:**

Lý do KHÔNG nhét vào `jwt.py` hiện hữu:
- `jwt.py` chỉ liên quan sign/verify JWT (RS256 encode/decode + claims model). JWK Set export là concern khác — RFC 7517 publish format.
- Plan 03-02 sẽ thêm `JWKSCache` class — cùng module `jwks.py` (publish ở central + cache ở hub con cùng concern "JWKS lifecycle").
- Phase 7 MCP service tái sử dụng module này (re-point central, cache JWKS) — tách module dễ import.

**KHÔNG sử dụng `jwcrypto`:**

`jwcrypto` là library phụ thuộc thêm (~500KB, license LGPL). `cryptography` (đã có qua `pyjwt[crypto]>=2.12`) export `RSAPublicNumbers` đủ build JWK manual ~10 LOC. Tránh dep mới — D-V3-Phase3-D Claude's Discretion ghi rõ "Python library: PyJWT (M2 đã có) + jwcrypto cho JWK export" nhưng analysis sau cho thấy 10 LOC manual rẻ hơn dep mới.

**KHÔNG đụng JWT_ISSUER + claims (defer Plan 03-03):**

Plan 03-01 chỉ ship layer PUBLISH JWKS endpoint. iss/aud/hub_ids claim refactor (`JWT_ISSUER` đổi từ `"medinet-wiki"` sang `"https://central/"` + `aud=["medinet-wiki"]` + `hub_ids` REQUIRED) defer Plan 03-03. Lý do tách: tránh quá nhiều file trong 1 plan + claim refactor làm M2 JWT cũ KHÔNG verify được (backward incompat — cần coordinate Plan 03-04 frontend rebuild login flow simulation).

**Settings field `central_jwks_url: str | None = None`:**

Plan 03-01 chỉ thêm field default None ở central (KHÔNG consume). Plan 03-02 thêm `@model_validator` enforce hub con phải set field này KHÔNG None (fail-loud boot nếu thiếu). Tách validator sang Plan 03-02 vì Plan 03-01 chưa có JWKSCache consume.

**docker-compose env `CENTRAL_JWKS_URL`:**

3 hub con (yte/duoc/hcns) thêm 1 dòng env trỏ `http://python-api-central:8080/.well-known/jwks.json` (intra-network Docker service name + port internal 8080 — KHÔNG host port 8180). Central service KHÔNG đụng. Plan 02-05 `docker-compose.override.yml.template` cũng cần thêm dòng này — Plan 03-01 update template để hub mới (`make hub-add`) tự inherit. KHÔNG cần TLS (intra-network).

**Cache header `Cache-Control: public, max-age=3600`:**

JWKS payload nhỏ + stable trong window 1h (matching hub con TTL Plan 03-02). Caddy + browser cache → giảm load central JWKS endpoint khi hub con + frontend cùng fetch. KHÔNG cần ETag (key rotation → kid đổi → cache miss tự nhiên qua kid mismatch khi verify JWT).
</notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo `api/app/auth/jwks.py` module với `publish_jwks()` + `load_public_key_as_jwk()` helpers RFC 7517 (TDD)</name>
  <files>api/app/auth/jwks.py, api/tests/unit/test_jwks_publish.py</files>
  <read_first>
    - Hub_All/api/app/auth/jwt.py (M2 JWTManager + JWT_ALGORITHM constant — Plan 03-01 reuse "RS256" constant + signing concern reference)
    - Hub_All/api/app/auth/__init__.py (re-export pattern)
    - Hub_All/api/app/config.py (Settings.jwt_public_key_path Path — Plan 03-01 reuse)
    - Hub_All/api/keys/public.pem (M2 keypair sample shape RSAPublicKey PEM — Plan 03-01 verify load_pem_public_key parse OK)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (D-V3-Phase3-A RFC 7517 spec + Claude's Discretion ghi rõ cryptography vs jwcrypto trade-off)
  </read_first>
  <behavior>
    - Test 1 (`test_load_public_key_as_jwk_shape_rsa_256`): Đọc `api/keys/public.pem` (M2 keypair) → `load_public_key_as_jwk(path, kid="test")` trả dict shape `{"kty":"RSA","kid":"test","use":"sig","alg":"RS256","n":"<base64url>","e":"<base64url>"}`. Verify 6 key có đủ + value `kty="RSA"`, `use="sig"`, `alg="RS256"`, `n` length ≥ 256 char (2048-bit RSA modulus encode base64url).
    - Test 2 (`test_jwks_keys_shape_rfc_7517`): `publish_jwks(public_key_path)` trả dict `{"keys": [<single JWK>]}` — list 1 phần tử (single-key rotation strategy v3.0-a; multi-key defer Phase 7).
    - Test 3 (`test_derive_kid_deterministic_sha256_8byte`): Cùng PEM bytes → cùng kid (deterministic). 2 PEM khác → kid khác. Kid length 11 char (base64url 8 byte unpadded = 11 char).
    - Test 4 (`test_int_to_base64url_no_padding`): `_int_to_base64url(65537)` → `"AQAB"` (RSA exponent e mặc định) — match JWK spec.
    - Test 5 (`test_load_public_key_rejects_non_rsa`): PEM EC key → `ValueError("Public key không phải RSA")` (raise sớm — defensive).
    - Test 6 (`test_load_public_key_rejects_missing_file`): Path không tồn tại → `OSError` (let bubble — caller log + return 503).
  </behavior>
  <action>
**Step 1: Tạo file `api/app/auth/jwks.py`** với 3 helper + 1 public function:

```python
"""JWKS publish layer — Plan 03-01 (SSO-01 part 1, D-V3-Phase3-A).

Module expose `publish_jwks()` cho central JWKS endpoint RFC 7517:
    GET /.well-known/jwks.json → {"keys": [{"kty":"RSA","kid":"...",...}]}

Stack: `cryptography` (M2 baseline qua PyJWT[crypto]) — KHÔNG thêm dep mới
(KHÔNG dùng `jwcrypto` — analysis cho thấy 10 LOC manual đủ; xem 03-CONTEXT.md
Claude's Discretion + Plan 03-01 notes).

Plan 03-02 sẽ thêm `JWKSCache` class cùng module này — publish + cache cùng
concern JWKS lifecycle. Phase 7 MCP service tái sử dụng cache layer.

Locked decisions:
    D-V3-Phase3-A — JWKS endpoint (KHÔNG shared keypair / cookie domain)
    D-V3-Phase3-D — In-process cache hub con (KHÔNG Redis)
"""
from __future__ import annotations

import base64
import hashlib
from pathlib import Path
from typing import TypedDict

from cryptography.hazmat.primitives import serialization
from cryptography.hazmat.primitives.asymmetric.rsa import (
    RSAPublicKey,
    RSAPublicNumbers,
)

# kid (Key ID) length sau base64url 8-byte SHA-256 prefix = 11 char unpadded.
# Cố định tránh nhầm kid với UUID/jti (32+ char).
_KID_BYTE_LENGTH = 8


class JWK(TypedDict):
    """RFC 7517 JWK shape cho RS256 sig key."""

    kty: str   # "RSA"
    kid: str   # base64url SHA-256 8-byte prefix
    use: str   # "sig"
    alg: str   # "RS256"
    n: str     # modulus base64url unpadded
    e: str     # exponent base64url unpadded (65537 = "AQAB")


class JWKSet(TypedDict):
    """RFC 7517 JWK Set shape."""

    keys: list[JWK]


def _int_to_base64url(n: int) -> str:
    """Big-int → base64url unpadded ASCII (RFC 7518 Section 6.3.1).

    JWK n/e fields dùng base64url-encoded big-endian unsigned int bytes.
    `n` (modulus 2048-bit) = 256 byte → 342 char base64url unpadded.
    `e` (exponent 65537) = 3 byte → "AQAB" (4 char unpadded).
    """
    byte_length = (n.bit_length() + 7) // 8
    data = n.to_bytes(byte_length, byteorder="big")
    return base64.urlsafe_b64encode(data).rstrip(b"=").decode("ascii")


def _derive_kid(pem_bytes: bytes) -> str:
    """kid = base64url của 8 byte đầu SHA-256 PEM (deterministic).

    Rotation: PEM mới → kid mới tự nhiên. Hub con cache phát hiện kid
    mismatch khi verify JWT → trigger re-fetch JWKS (Plan 03-02).
    """
    digest = hashlib.sha256(pem_bytes).digest()
    prefix = digest[:_KID_BYTE_LENGTH]
    return base64.urlsafe_b64encode(prefix).rstrip(b"=").decode("ascii")


def load_public_key_as_jwk(pem_path: Path, kid: str | None = None) -> JWK:
    """Đọc PEM public key → JWK dict RFC 7517.

    Args:
        pem_path: đường dẫn tới file PEM (`-----BEGIN PUBLIC KEY-----`).
        kid: nếu None, auto-derive từ SHA-256 PEM bytes (deterministic).

    Raises:
        OSError: file không tồn tại / không đọc được.
        ValueError: PEM parse fail HOẶC key không phải RSA.
    """
    pem_bytes = pem_path.read_bytes()
    pub_key = serialization.load_pem_public_key(pem_bytes)
    if not isinstance(pub_key, RSAPublicKey):
        raise ValueError(
            f"Public key tại {pem_path!s} không phải RSA "
            f"(type={type(pub_key).__name__}). JWKS endpoint yêu cầu RS256 — "
            f"chạy 'make keys' để sinh lại RSA keypair PKCS#8."
        )
    numbers: RSAPublicNumbers = pub_key.public_numbers()
    if kid is None:
        kid = _derive_kid(pem_bytes)
    return JWK(
        kty="RSA",
        kid=kid,
        use="sig",
        alg="RS256",
        n=_int_to_base64url(numbers.n),
        e=_int_to_base64url(numbers.e),
    )


def publish_jwks(public_key_path: Path) -> JWKSet:
    """Build JWK Set RFC 7517 từ central public key.

    Single-key strategy ở v3.0-a (1 active key + manual rotation — `make keys`
    overwrite → restart central). Multi-key rotation (overlap window) defer
    Phase 7 MCP service.

    Args:
        public_key_path: settings.jwt_public_key_path (Path).

    Returns:
        {"keys": [<single JWK>]} — JSON-serializable dict.

    Raises:
        OSError / ValueError: nếu PEM thiếu hoặc không phải RSA — central
        không thể boot mà thiếu keypair (M2 carry forward — `make keys` step
        bắt buộc Phase 1).
    """
    jwk = load_public_key_as_jwk(public_key_path)
    return JWKSet(keys=[jwk])


__all__ = ["JWK", "JWKSet", "load_public_key_as_jwk", "publish_jwks"]
```

**Step 2: Tạo file `api/tests/unit/test_jwks_publish.py`** với 6 test:

```python
"""Phase 3 SSO-01 — Unit test JWKS publish layer (Plan 03-01 Task 1).

Verify:
- load_public_key_as_jwk shape RFC 7517 với M2 keypair api/keys/public.pem.
- publish_jwks() trả {"keys": [<single JWK>]}.
- _derive_kid deterministic SHA-256 8-byte prefix base64url.
- _int_to_base64url match JWK spec (e=65537 → "AQAB").
- Defensive: non-RSA key reject + missing file raise OSError.
"""
from __future__ import annotations

import base64
import hashlib
from pathlib import Path

import pytest

from app.auth.jwks import (
    _derive_kid,
    _int_to_base64url,
    JWK,
    JWKSet,
    load_public_key_as_jwk,
    publish_jwks,
)

# Resolve api/keys/public.pem relative tới repo root (cwd = api/ khi pytest run).
_PUBLIC_KEY_PATH = Path("keys/public.pem")


@pytest.fixture
def public_key_path() -> Path:
    """Path tới M2 public.pem (api/keys/public.pem)."""
    if not _PUBLIC_KEY_PATH.exists():
        pytest.skip(
            "api/keys/public.pem không tồn tại — chạy 'make keys' trước"
        )
    return _PUBLIC_KEY_PATH


def test_load_public_key_as_jwk_shape_rsa_256(public_key_path: Path) -> None:
    """JWK trả về có đúng 6 key RFC 7517: kty/kid/use/alg/n/e + value cố định."""
    jwk = load_public_key_as_jwk(public_key_path, kid="test")
    assert set(jwk.keys()) == {"kty", "kid", "use", "alg", "n", "e"}
    assert jwk["kty"] == "RSA"
    assert jwk["kid"] == "test"
    assert jwk["use"] == "sig"
    assert jwk["alg"] == "RS256"
    # 2048-bit RSA modulus encode base64url ≥ 256 char (342 typical).
    assert len(jwk["n"]) >= 256
    # Exponent mặc định OpenSSL = 65537 → "AQAB" (3 byte base64url).
    assert jwk["e"] == "AQAB"


def test_load_public_key_as_jwk_auto_kid(public_key_path: Path) -> None:
    """Không truyền kid → auto-derive deterministic SHA-256 8-byte prefix."""
    jwk1 = load_public_key_as_jwk(public_key_path)
    jwk2 = load_public_key_as_jwk(public_key_path)
    assert jwk1["kid"] == jwk2["kid"]  # deterministic
    # base64url 8 byte unpadded = 11 char.
    assert len(jwk1["kid"]) == 11


def test_jwks_keys_shape_rfc_7517(public_key_path: Path) -> None:
    """publish_jwks() trả {\"keys\": [<single JWK>]} — RFC 7517 JWK Set."""
    jwks: JWKSet = publish_jwks(public_key_path)
    assert "keys" in jwks
    assert isinstance(jwks["keys"], list)
    assert len(jwks["keys"]) == 1  # single-key strategy v3.0-a
    jwk = jwks["keys"][0]
    assert jwk["kty"] == "RSA"
    assert jwk["alg"] == "RS256"


def test_derive_kid_deterministic_sha256_8byte() -> None:
    """Cùng PEM bytes → cùng kid; 2 PEM khác → kid khác."""
    pem_a = b"-----BEGIN PUBLIC KEY-----\nAAAA\n-----END PUBLIC KEY-----\n"
    pem_b = b"-----BEGIN PUBLIC KEY-----\nBBBB\n-----END PUBLIC KEY-----\n"
    kid_a1 = _derive_kid(pem_a)
    kid_a2 = _derive_kid(pem_a)
    kid_b = _derive_kid(pem_b)
    assert kid_a1 == kid_a2
    assert kid_a1 != kid_b
    # Verify SHA-256 first 8 byte = kid pre-base64url.
    expected = base64.urlsafe_b64encode(
        hashlib.sha256(pem_a).digest()[:8]
    ).rstrip(b"=").decode("ascii")
    assert kid_a1 == expected
    assert len(kid_a1) == 11


def test_int_to_base64url_no_padding() -> None:
    """RSA exponent 65537 → 'AQAB' (RFC 7518 Section 6.3.1 example)."""
    assert _int_to_base64url(65537) == "AQAB"
    # Verify 256 = 1 byte ([0x01,0x00]) → "AQ" (no padding).
    assert _int_to_base64url(256) == "AQA"


def test_load_public_key_rejects_non_rsa(tmp_path: Path) -> None:
    """EC public key PEM → ValueError 'không phải RSA'."""
    # Sinh EC key tạm (cryptography stdlib) để verify reject.
    from cryptography.hazmat.primitives import serialization as ser
    from cryptography.hazmat.primitives.asymmetric import ec

    ec_priv = ec.generate_private_key(ec.SECP256R1())
    ec_pub = ec_priv.public_key()
    ec_pem = ec_pub.public_bytes(
        encoding=ser.Encoding.PEM,
        format=ser.PublicFormat.SubjectPublicKeyInfo,
    )
    ec_path = tmp_path / "ec_pub.pem"
    ec_path.write_bytes(ec_pem)

    with pytest.raises(ValueError, match="không phải RSA"):
        load_public_key_as_jwk(ec_path)


def test_load_public_key_rejects_missing_file(tmp_path: Path) -> None:
    """File không tồn tại → OSError (caller log + return 503)."""
    missing = tmp_path / "nonexistent.pem"
    with pytest.raises(OSError):
        load_public_key_as_jwk(missing)
```

**Step 3: Update `api/app/auth/__init__.py`** thêm re-export 2 helper mới (optional convenience):

```python
# Sau dòng export hiện hữu:
from app.auth.jwks import JWK, JWKSet, publish_jwks  # noqa: E402

__all__ = [
    # ... (existing exports)
    "JWK",
    "JWKSet",
    "publish_jwks",
]
```

**KHÔNG đụng:**
- `api/app/auth/jwt.py` (JWTManager + JWT_ISSUER + JWT_ALGORITHM constants — Plan 03-03 sẽ sửa iss/aud claim).
- `api/app/auth/service.py` + `dependencies.py` (defer Plan 03-03 blacklist key rename + hub_ids claim build).
- `api/app/auth/router.py` (defer Plan 03-04 hub con 307 redirect refactor).
- `api/keys/public.pem` (M2 carry forward).

**Reject patterns:**
- KHÔNG dùng `jwcrypto` (over-engineer — 10 LOC manual rẻ hơn dep mới ~500KB LGPL).
- KHÔNG hardcode kid `"central-key-1"` (R-V3-5 mitigation: deterministic kid từ PEM bytes → key rotation phát hiện ngay qua kid mismatch).
- KHÔNG cache JWK trong-process ở publish layer (central rebuild mỗi request — 1ms acceptable + simpler + key rotation hot-reload tự nhiên qua container restart). Hub con cache là concern khác (Plan 03-02).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/app/auth/jwks.py` exists (mới tạo)
    - `grep -c "def publish_jwks" Hub_All/api/app/auth/jwks.py` ≥ 1
    - `grep -c "def load_public_key_as_jwk" Hub_All/api/app/auth/jwks.py` ≥ 1
    - `grep -c "def _derive_kid\|def _int_to_base64url" Hub_All/api/app/auth/jwks.py` ≥ 2
    - `grep -c "class JWK\|class JWKSet" Hub_All/api/app/auth/jwks.py` ≥ 2 (TypedDict)
    - `grep -c "RSAPublicKey\|RSAPublicNumbers" Hub_All/api/app/auth/jwks.py` ≥ 2
    - `grep -c "D-V3-Phase3-A\|SSO-01" Hub_All/api/app/auth/jwks.py` ≥ 2 (decision + REQ traceability)
    - `grep -c "jwcrypto" Hub_All/api/app/auth/jwks.py` = 0 (KHÔNG dùng)
    - File `Hub_All/api/tests/unit/test_jwks_publish.py` exists
    - `grep -c "def test_load_public_key_as_jwk_shape_rsa_256\|def test_jwks_keys_shape_rfc_7517\|def test_derive_kid_deterministic_sha256_8byte\|def test_int_to_base64url_no_padding\|def test_load_public_key_rejects_non_rsa\|def test_load_public_key_rejects_missing_file" Hub_All/api/tests/unit/test_jwks_publish.py` ≥ 6
    - `cd Hub_All/api && uv run ruff check app/auth/jwks.py tests/unit/test_jwks_publish.py` exit 0
    - `cd Hub_All/api && uv run mypy app/auth/jwks.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py -v` PASS ≥ 6 test (test_load_public_key_as_jwk_auto_kid bonus → 7)
  </acceptance_criteria>
  <done>
Module `api/app/auth/jwks.py` mới với `publish_jwks()` + `load_public_key_as_jwk()` + 2 helper `_derive_kid` / `_int_to_base64url`. 7 unit test PASS verify shape RFC 7517 + deterministic kid + defensive non-RSA/missing-file reject. KHÔNG thêm dep ngoài. M2 keypair `api/keys/public.pem` parse OK.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount `GET /.well-known/jwks.json` endpoint trong `create_app()` conditional central + Settings field `central_jwks_url`</name>
  <files>api/app/main.py, api/app/config.py, api/tests/unit/test_jwks_publish.py</files>
  <read_first>
    - Hub_All/api/app/main.py (Phase 2 Plan 02-01 conditional mount block line ~410-444 — Plan 03-01 thêm route mới cùng block)
    - Hub_All/api/app/config.py (Settings class — Plan 03-01 thêm field `central_jwks_url` sau jwt_refresh_token_ttl)
    - Hub_All/api/app/auth/jwks.py (Task 1 mới ship — Plan 03-01 import + dùng)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (D-V3-Phase3-A endpoint mount conditional central)
  </read_first>
  <action>
**Step 1: Thêm Settings field `central_jwks_url: str | None = None`** vào `api/app/config.py`:

Tìm block JWT (line ~97-101) hiện hữu:

```python
    # JWT
    jwt_private_key_path: Path = Path("./keys/private.pem")
    jwt_public_key_path: Path = Path("./keys/public.pem")
    jwt_access_token_ttl: int = 900
    jwt_refresh_token_ttl: int = 604800
```

Append 1 field MỚI (giữ block hiện hữu nguyên xi):

```python
    # JWT
    jwt_private_key_path: Path = Path("./keys/private.pem")
    jwt_public_key_path: Path = Path("./keys/public.pem")
    jwt_access_token_ttl: int = 900
    jwt_refresh_token_ttl: int = 604800
    # Phase 3 Plan 03-01 SSO-01 (D-V3-Phase3-A) — Hub con consume JWKS endpoint
    # từ central qua intra-network HTTP. Default None ở central (KHÔNG cần fetch
    # — central có local private.pem). Plan 03-02 sẽ thêm @model_validator
    # enforce hub con phải set field này KHÔNG None (fail-loud boot nếu thiếu).
    # docker-compose 3 hub con set env CENTRAL_JWKS_URL=http://python-api-central:8080/.well-known/jwks.json
    central_jwks_url: str | None = None
```

**KHÔNG thêm validator ở Plan 03-01** — Plan 03-02 sẽ add `@model_validator(mode="after")` enforce hub con required (vì validator cần coordinate với JWKSCache lifespan startup).

**Step 2: Thêm route handler `/.well-known/jwks.json`** trong `create_app()` block central-only (`api/app/main.py`):

Tìm block Tier 2 central-only mount (line ~410-444 — sau `app.include_router(mcp_oauth_internal_router)` + trước `else: logger.info(...)`). Thêm 1 route mới CÙNG block this:

```python
    if settings.hub_name == "central":
        from app.routers import (
            api_keys_router,
            audit_logs_router,
            hubs_router,
            mcp_oauth_internal_router,
            mcp_oauth_router,
            rag_config_router,
            sync_router,
            system_settings_router,
            users_router,
        )

        app.include_router(hubs_router)
        app.include_router(users_router)
        app.include_router(api_keys_router)
        app.include_router(audit_logs_router)
        app.include_router(rag_config_router)
        app.include_router(system_settings_router)
        app.include_router(sync_router)
        app.include_router(mcp_oauth_router)
        app.include_router(mcp_oauth_internal_router)

        # ──────────────────────────────────────────────────────────────────
        # Phase 3 Plan 03-01 (SSO-01, D-V3-Phase3-A) — JWKS endpoint RFC 7517
        # Central publish public RS256 key cho hub con + frontend Phase 5 + MCP
        # Phase 7 verify JWT. Endpoint PUBLIC (KHÔNG auth) + Cache-Control 1h
        # match Plan 03-02 hub con TTL. Single-key strategy v3.0-a; multi-key
        # rotation defer Phase 7.
        # ──────────────────────────────────────────────────────────────────
        from app.auth.jwks import publish_jwks

        @app.get(
            "/.well-known/jwks.json",
            tags=["jwks"],
            summary="JWK Set RFC 7517 — public key RS256 verify JWT",
        )
        async def jwks_endpoint() -> StarletteJSONResponse:
            """Trả JWK Set cho hub con/frontend/MCP verify JWT RS256.

            Plan 03-01 / SSO-01 / D-V3-Phase3-A. Public endpoint — KHÔNG auth.
            Cache 1h (matching hub con TTL Plan 03-02). Key rotation: `make keys`
            overwrite → kid đổi tự nhiên qua _derive_kid SHA-256 PEM.
            """
            try:
                jwks = publish_jwks(settings.jwt_public_key_path)
            except (OSError, ValueError) as e:
                logger.error(
                    "jwks_publish_failed: path=%s error=%s",
                    settings.jwt_public_key_path,
                    e,
                )
                return StarletteJSONResponse(
                    status_code=503,
                    content={
                        "success": False,
                        "data": None,
                        "error": {
                            "code": "JWKS_UNAVAILABLE",
                            "message": (
                                "Không đọc được JWT public key. "
                                "Chạy 'make keys' để sinh lại."
                            ),
                        },
                        "meta": None,
                    },
                )
            return StarletteJSONResponse(
                content=jwks,
                headers={"Cache-Control": "public, max-age=3600"},
            )
    else:
        logger.info(
            "central_only_routers_skipped: hub_name=%s — 9 routers stripped "
            "(FACTOR-02 enforce)",
            settings.hub_name,
        )
```

**Step 3: Bổ sung test mount endpoint** vào `api/tests/unit/test_jwks_publish.py` (append 2 test mới sau 7 test hiện hữu):

```python
# ────────────────────────────────────────────────────────────────────
# Plan 03-01 Task 2 — Mount endpoint conditional central
# ────────────────────────────────────────────────────────────────────


def _setup_env(monkeypatch, hub_name: str) -> None:
    """Boot env cho create_app() — pattern Phase 2 Plan 02-01."""
    if hub_name == "central":
        db = "medinet_central"
    else:
        db = f"medinet_hub_{hub_name}"
    monkeypatch.setenv("HUB_NAME", hub_name)
    monkeypatch.setenv(
        "DATABASE_URL", f"postgresql+asyncpg://u:p@localhost:5432/{db}"
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
    from app.config import get_settings

    get_settings.cache_clear()


def test_jwks_endpoint_mount_central_only(monkeypatch) -> None:
    """HUB_NAME=central → /.well-known/jwks.json mount; hub con → strip."""
    from fastapi.testclient import TestClient

    # Central — endpoint mount
    _setup_env(monkeypatch, "central")
    from app.main import create_app

    app_central = create_app()
    with TestClient(app_central) as client:
        resp = client.get("/.well-known/jwks.json")
        assert resp.status_code == 200, (
            f"Central phải mount JWKS endpoint, got {resp.status_code}"
        )
        body = resp.json()
        assert "keys" in body
        assert len(body["keys"]) == 1
        assert body["keys"][0]["kty"] == "RSA"
        assert body["keys"][0]["alg"] == "RS256"
        # Cache header verify
        assert "max-age=3600" in resp.headers.get("cache-control", "")

    # Hub yte — endpoint strip → 404 envelope (FACTOR-02 carry forward)
    _setup_env(monkeypatch, "yte")
    app_yte = create_app()
    with TestClient(app_yte) as client:
        resp = client.get("/.well-known/jwks.json")
        assert resp.status_code == 404, (
            f"Hub con phải strip JWKS endpoint, got {resp.status_code}"
        )
        body = resp.json()
        assert body["success"] is False
        assert body["error"]["code"] == "NOT_FOUND"


def test_settings_central_jwks_url_default_none(monkeypatch) -> None:
    """Settings.central_jwks_url default None ở central (Plan 03-01 chưa enforce hub con)."""
    _setup_env(monkeypatch, "central")
    from app.config import get_settings

    settings = get_settings()
    assert settings.central_jwks_url is None
    # Hub con vẫn None ở Plan 03-01 (Plan 03-02 sẽ add validator)
    _setup_env(monkeypatch, "yte")
    settings_yte = get_settings()
    assert settings_yte.central_jwks_url is None  # vẫn None default — Plan 03-02 enforce
```

**Step 4: Update docker-compose.yml** thêm env `CENTRAL_JWKS_URL` cho 3 hub con service (yte/duoc/hcns):

Trong block `python-api-yte.environment` (line ~113-126), thêm 1 dòng SAU `LOG_LEVEL`:

```yaml
  python-api-yte:
    <<: *api-template
    container_name: medinet-api-yte
    environment:
      HUB_NAME: yte
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_hub_yte
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Phase 3 Plan 03-01 SSO-01 — Hub con consume JWKS từ central (intra-network HTTP)
      CENTRAL_JWKS_URL: http://python-api-central:8080/.well-known/jwks.json
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
```

LẶP cho 2 service `python-api-duoc` + `python-api-hcns` (cùng dòng env). Central KHÔNG đụng (không cần CENTRAL_JWKS_URL).

**Step 5: Update `docker-compose.override.yml.template`** (FACTOR-04 Plan 02-05 carry forward) thêm dòng env tương tự để hub mới (`make hub-add`) tự inherit:

Tìm block `environment:` trong template, thêm 1 dòng tương tự ở vị trí cùng (sau LOG_LEVEL):

```yaml
      # Phase 3 Plan 03-01 SSO-01 — Hub con consume JWKS từ central
      CENTRAL_JWKS_URL: http://python-api-central:8080/.well-known/jwks.json
```

**KHÔNG đụng:**
- Central `python-api-central` service block (KHÔNG cần CENTRAL_JWKS_URL).
- `x-api-template` anchor (env per-service đã override toàn bộ — Plan 02-02 note line 9-11).
- Phase 1/2 lifespan + middleware + Tier 1 universal routers.

**Reject patterns:**
- KHÔNG mount JWKS endpoint ở TIER 1 universal (D-V3-Phase3-A: central publish, hub con consume — KHÔNG publish chéo).
- KHÔNG đặt JWKS endpoint dưới `/api/jwks.json` — RFC 7517 chuẩn `/.well-known/jwks.json`.
- KHÔNG dùng `@app.get(...)` không có envelope wrap cho 503 path — phải khớp shape D6.
- KHÔNG hardcode `CENTRAL_JWKS_URL` ở `Settings.central_jwks_url` default value — phải None default (Plan 03-02 hub con enforce).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py -v && cd .. && docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "central_jwks_url" Hub_All/api/app/config.py` ≥ 1
    - `grep -c "Phase 3 Plan 03-01\|SSO-01" Hub_All/api/app/config.py` ≥ 1 (REQ traceability)
    - `grep -c "/.well-known/jwks.json" Hub_All/api/app/main.py` ≥ 1
    - `grep -c "from app.auth.jwks import publish_jwks" Hub_All/api/app/main.py` ≥ 1
    - `grep -c "jwks_endpoint" Hub_All/api/app/main.py` ≥ 1
    - `grep -c "Cache-Control\|max-age=3600" Hub_All/api/app/main.py` ≥ 1 (cache header)
    - `grep -c "JWKS_UNAVAILABLE" Hub_All/api/app/main.py` ≥ 1 (503 fallback envelope)
    - `grep -c "CENTRAL_JWKS_URL" Hub_All/docker-compose.yml` ≥ 3 (yte + duoc + hcns)
    - `grep -c "CENTRAL_JWKS_URL" Hub_All/docker-compose.override.yml.template` ≥ 1 (FACTOR-04 inherit)
    - `grep -c "def test_jwks_endpoint_mount_central_only\|def test_settings_central_jwks_url_default_none" Hub_All/api/tests/unit/test_jwks_publish.py` ≥ 2 (Task 2 mới)
    - `cd Hub_All/api && uv run ruff check app/main.py app/config.py tests/unit/test_jwks_publish.py` exit 0
    - `cd Hub_All/api && uv run mypy app/main.py app/config.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py -v` PASS ≥ 9 test
    - `cd Hub_All && docker compose config --quiet` exit 0 (compose render OK)
    - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v` PASS toàn bộ (49 test Phase 2 KHÔNG regress)
  </acceptance_criteria>
  <done>
Central FastAPI mount `/.well-known/jwks.json` route trả JWK Set RFC 7517 với Cache-Control 1h; hub con strip endpoint → 404 envelope FACTOR-02 carry forward. Settings.central_jwks_url field mới (default None) — Plan 03-02 sẽ enforce hub con required. Docker compose 3 hub con + override template thêm env CENTRAL_JWKS_URL trỏ central intra-network. 9 unit test PASS (7 publish + 2 mount/strip). Phase 2 regression 49 test PASS.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet → Central JWKS endpoint | Endpoint public (KHÔNG auth). Anyone fetch JWK Set → verify JWT signed bởi central. Attacker chỉ enumerate kid + public modulus — KHÔNG private key |
| Intra-network → Central JWKS endpoint | 3 hub con (yte/duoc/hcns) fetch JWKS qua docker network `medinet_net` (HTTP intra-network — no TLS). Plan 03-02 enforce blocking fetch ở lifespan startup |
| Central process → PEM file `api/keys/public.pem` | M2 carry forward — read-only mount `/keys:ro` Docker; OS-level permission đảm bảo central process đọc được public.pem (KHÔNG ghi) |
| Central rotation → kid change | `make keys` overwrite → SHA-256 PEM đổi → kid mới → hub con cache phát hiện qua kid mismatch khi verify JWT (Plan 03-02 force re-fetch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| **T-03-01-01** | Information Disclosure | JWKS endpoint expose public RSA modulus + exponent | low | accept | Public key BY DESIGN (RFC 7517). Attacker chỉ verify JWT signed bởi central — KHÔNG forge JWT (cần private.pem). Threat = baseline RS256 public-key crypto, không phải Phase 3 specific. |
| **T-03-01-02** | Tampering | Man-in-the-middle thay JWKS response intra-network → hub con cache fake public key → JWT attacker-signed pass verify | medium | accept | v3.0-a deploy central + hub con cùng 1 Docker network `medinet_net` (Plan 02-02 LOCKED). Intra-network HTTP (no TLS) acceptable trong dev/staging. Production deploy có Caddy auto-TLS phía ngoài (Phase 5) + medinet_net network isolation. R-V3-5 mitigation: cache TTL 1h hard limit (Plan 03-02 24h hard limit) — fake key window hữu hạn. Defense-in-depth Phase 5 PROXY-01 sẽ add TLS-only nếu cross-host deploy. |
| **T-03-01-03** | DoS | Attacker spam `/.well-known/jwks.json` → central CPU spike (PEM parse + base64url encode mỗi request ~1ms) | low | mitigate | Cache-Control `public, max-age=3600` — Caddy + browser cache → giảm load central JWKS endpoint. Cocoindex/Redis/DB không liên quan endpoint này (pure CPU + filesystem read). Phase 5 Caddy rate limit WAF defer v4.0 nếu cần. |
| **T-03-01-04** | Information Disclosure | Logger error `jwks_publish_failed: path=...` leak file system path khi public.pem missing | low | accept | Path = `api/keys/public.pem` đã expose qua Settings field default + repo public. KHÔNG chứa secret. Log structlog JSON tới Loki/Datadog internal — KHÔNG public. |
| **T-03-01-05** | Tampering | Operator chạy `make keys` overwrite PEM khi central đang serve JWT → window mismatch giữa JWT signed bằng key cũ + JWKS publish key mới | medium | mitigate | Central process KHÔNG hot-reload PEM (M2 baseline: JWTManager load PEM 1 lần ở constructor). `make keys` overwrite → cần restart central container → JWT cũ signed bằng key cũ vẫn pass verify ở hub con (Plan 03-02 cache key cũ TTL 1h). Plan 03-03 sẽ blacklist old JWT khi user logout qua jti. Acceptable cho v3.0-a — proper key rotation strategy (overlap window) defer Phase 7 multi-key JWKS. |
| **T-03-01-06** | Elevation of Privilege | Hub con tự publish JWKS endpoint qua FACTOR-02 strip bypass | mitigate | mitigate | D-V3-Phase3-A LOCKED + Task 2 mount endpoint TRONG block `if settings.hub_name == "central":` — hub con KHÔNG mount → 404 envelope. Unit test `test_jwks_endpoint_mount_central_only` verify hub yte strip → 404. |
</threat_model>

<verification>
**Plan 03-01 verification chain:**

1. **File integrity (grep):**
   - `api/app/auth/jwks.py` (mới) — `publish_jwks` + `load_public_key_as_jwk` + 2 helper + TypedDict JWK/JWKSet
   - `api/app/main.py` — `/.well-known/jwks.json` route trong block central-only + import `publish_jwks` + Cache-Control header
   - `api/app/config.py` — field `central_jwks_url: str | None = None` mới sau JWT block
   - `api/tests/unit/test_jwks_publish.py` (mới) — 9 test (7 publish layer + 2 mount conditional)
   - `docker-compose.yml` — env `CENTRAL_JWKS_URL` ở 3 hub con block (yte/duoc/hcns)
   - `docker-compose.override.yml.template` — env `CENTRAL_JWKS_URL` ở template inline (FACTOR-04 inherit)

2. **Lint + type clean:**
   - `cd Hub_All/api && uv run ruff check app/auth/jwks.py app/main.py app/config.py tests/unit/test_jwks_publish.py` exit 0
   - `cd Hub_All/api && uv run mypy app/auth/jwks.py app/main.py app/config.py` exit 0

3. **Unit test PASS:**
   - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py -v` PASS ≥ 9 test

4. **Phase 2 regression:**
   - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v` PASS 49/49 (Phase 2 baseline)

5. **Compose render:**
   - `cd Hub_All && docker compose config --quiet` exit 0

6. **JWKS endpoint smoke (in-process — TestClient):**
   - Central: `GET /.well-known/jwks.json` → 200 + `{"keys":[{...}]}` shape + Cache-Control header.
   - Hub yte: `GET /.well-known/jwks.json` → 404 envelope `{success:false, error:{code:"NOT_FOUND",...}}`.

7. **JWK shape RFC 7517:**
   - Verify 6 key (kty/kid/use/alg/n/e) + value cố định (kty=RSA, alg=RS256, use=sig, e=AQAB).
</verification>

<success_criteria>
- Module `api/app/auth/jwks.py` mới ship với `publish_jwks()` + `load_public_key_as_jwk()` RFC 7517 compliant
- Central FastAPI mount `GET /.well-known/jwks.json` route trả JWK Set + Cache-Control 1h
- Hub con strip endpoint → 404 envelope (FACTOR-02 carry forward — D-V3-Phase3-A enforce)
- Settings.central_jwks_url field mới (default None — Plan 03-02 hub con enforce required)
- Docker-compose 3 hub con + override template thêm env CENTRAL_JWKS_URL trỏ central intra-network
- 9 unit test PASS (test_jwks_publish.py) + Phase 2 regression 49 test PASS
- Lint + mypy clean
- D-V3-Phase3-A honored (JWKS endpoint pattern RFC 7517, KHÔNG shared keypair / cookie domain)
- D-V3-Phase3-D honored (in-process layer — KHÔNG Redis cache JWK publish layer)
- KHÔNG thêm dep ngoài (cryptography qua PyJWT[crypto] M2 baseline)
</success_criteria>

<output>
Tạo `.planning/phases/03-auth-sso-hub-ids-jwt/03-01-SUMMARY.md`:
- Plan 03-01 interface ship: `publish_jwks()` + `load_public_key_as_jwk()` + JWK/JWKSet TypedDict + JWKS endpoint mount
- File modified/created: `api/app/auth/jwks.py` (mới), `api/app/main.py` (+1 route), `api/app/config.py` (+1 field), `tests/unit/test_jwks_publish.py` (mới, 9 test), `docker-compose.yml` (+3 env), `docker-compose.override.yml.template` (+1 env)
- D-V3-Phase3-A + D-V3-Phase3-D consumed
- Phase 2 regression KHÔNG break
- BLOCKING: Plan 03-02 tiêu thụ endpoint này — Wave 2
- Next: Plan 03-02 hub con `JWKSCache` class + lifespan startup blocking fetch + `get_current_user` dependency branch verify path
</output>
</content>
