---
phase: 03-auth-sso-hub-ids-jwt
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - api/app/auth/jwks.py
  - api/app/main.py
  - api/app/config.py
  - api/app/auth/dependencies.py
  - api/tests/unit/test_jwks_cache.py
  - api/tests/integration/test_jwks_cache_lifecycle.py
autonomous: true
requirements:
  - SSO-01
tags:
  - jwks-cache
  - in-process-lru
  - asyncio-refresh
  - lifespan-blocking
  - dependency-branch
  - fail-loud-boot
  - fail-quiet-runtime
  - 24h-hard-limit
  - d-v3-phase3-b
  - d-v3-phase3-d
  - r-v3-5
must_haves:
  truths:
    - "Hub con boot: lifespan startup blocking fetch JWKS từ `settings.central_jwks_url` (timeout 5s) — exception → process exit code 1 (fail-loud — D-V3-Phase3-B)"
    - "Hub con runtime: asyncio background task refresh JWKS mỗi 1h — fail-quiet (log warning + giữ cached value, KHÔNG raise)"
    - "Hub con 24h hard limit: nếu cached value > 24h KHÔNG refresh thành công → mọi JWT verify trả 503 envelope `{code:\"JWKS_STALE\"}` (fail-loud delayed — R-V3-5)"
    - "Settings `central_jwks_url: str | None` field add validator `@model_validator(mode='after')` enforce hub con required, central None OK"
    - "JWKSCache class in-process LRU (Plan 03-01 jwks.py extend) — `functools.lru_cache(maxsize=2)` keyed on kid + asyncio refresh task — KHÔNG Redis (D-V3-Phase3-D)"
    - "`get_current_user` dependency branch: nếu `settings.hub_name != 'central'` → verify JWT bằng JWKSCache.get_public_key(kid) thay vì local public.pem"
    - "Cache restart hub con = re-fetch JWKS (KHÔNG persist disk LMDB/file)"
    - "Integration test rotate central keypair scenario: spawn central + yte qua TestClient + ASGI lifespan, fetch JWKS từ yte cache → verify == central public key → swap central keypair → mock asyncio.sleep TTL → fetch lại → verify mới (kid mismatch trigger re-fetch)"
  artifacts:
    - path: "api/app/auth/jwks.py"
      provides: "JWKSCache class extend module Plan 03-01 — in-process LRU + asyncio refresh + 24h hard limit"
      contains: "class JWKSCache|def get_public_key|async def refresh|async def fetch_initial"
    - path: "api/app/main.py"
      provides: "Lifespan startup block fetch_initial JWKS cho hub con + background refresh task"
      contains: "jwks_cache.fetch_initial|jwks_cache.start_refresh_task"
    - path: "api/app/config.py"
      provides: "@model_validator enforce hub con central_jwks_url required"
      contains: "_enforce_central_jwks_url_for_hub"
    - path: "api/app/auth/dependencies.py"
      provides: "get_current_user branch verify JWT qua JWKSCache cho hub con"
      contains: "settings.hub_name != \"central\"|jwks_cache"
    - path: "api/tests/unit/test_jwks_cache.py"
      provides: "Unit test JWKSCache lifecycle: fetch_initial / get_public_key / refresh / 24h hard limit"
      contains: "def test_jwks_cache_fetch_initial|def test_jwks_cache_24h_hard_limit"
    - path: "api/tests/integration/test_jwks_cache_lifecycle.py"
      provides: "Integration test rotate keypair + cache re-fetch via kid mismatch"
      contains: "def test_jwks_cache_rotate_keypair_detects_new_kid"
  key_links:
    - from: "api/app/main.py::lifespan() (hub con branch)"
      to: "api/app/auth/jwks.py::JWKSCache.fetch_initial"
      via: "if settings.hub_name != 'central': await jwks_cache.fetch_initial(settings.central_jwks_url)"
      pattern: "fetch_initial"
    - from: "api/app/auth/dependencies.py::get_current_user"
      to: "api/app/auth/jwks.py::JWKSCache.get_public_key"
      via: "kid extract từ JWT header → jwks_cache.get_public_key(kid) → verify"
      pattern: "jwks_cache.get_public_key"
    - from: "api/app/config.py::Settings._enforce_central_jwks_url_for_hub"
      to: "api/app/auth/jwks.py::JWKSCache (consume)"
      via: "model_validator raise ValidationError nếu hub con KHÔNG set CENTRAL_JWKS_URL"
      pattern: "central_jwks_url phải set"
---

<objective>
Mở rộng module `api/app/auth/jwks.py` (Plan 03-01 ship publish layer) thêm `JWKSCache` class in-process LRU cho hub con verify JWT — fail-loud boot (timeout 5s blocking startup fetch — D-V3-Phase3-B) + fail-quiet runtime (asyncio task refresh 1h + log warning trên fail) + 24h hard limit (cached > 24h → 503 `JWKS_STALE` envelope mọi JWT verify request — R-V3-5 mitigation).

Settings thêm `@model_validator(mode="after")` `_enforce_central_jwks_url_for_hub` — hub con (`hub_name != "central"`) phải set `central_jwks_url` KHÔNG None (fail-fast boot tránh hub con boot không có endpoint verify JWT).

`get_current_user` dependency thêm branch: central dùng JWTManager local public.pem (M2 carry forward); hub con dùng JWKSCache.get_public_key(kid) verify JWT — kid extract từ JWT header (`pyjwt.get_unverified_header(token)["kid"]`).

Plan này Wave 2 — depend trực tiếp 03-01 (cần endpoint `/.well-known/jwks.json` lên trước để fetch_initial blocking).

Purpose: SSO-01 part 2 (hub con consume layer + dependency wire). R-V3-5 HA mitigation (cache local 1h TTL — central blip 5 phút KHÔNG kill hub con). Pattern in-process LRU (D-V3-Phase3-D LOCKED — KHÔNG Redis vì payload nhỏ < 2KB + zero latency mọi JWT verify hot path).

Output: 5 file modify (`jwks.py` extend, `main.py` lifespan branch, `config.py` validator, `dependencies.py` branch, 2 test file mới — 1 unit + 1 integration).

**Locked decisions consume (CONTEXT.md 03-CONTEXT.md):**
- **D-V3-Phase3-B:** Boot fail-loud (timeout 5s blocking) + runtime fail-quiet + 24h hard limit (`max_stale_seconds=86400` → 503 JWKS_STALE).
- **D-V3-Phase3-D:** In-process LRU (`functools.lru_cache(maxsize=2)` keyed on `kid`) + asyncio refresh task. KHÔNG Redis. KHÔNG cache disk (LMDB/file).
- **D-V3-Phase3-A:** Reuse Plan 03-01 JWKS endpoint shape RFC 7517 `{"keys":[{"kty":"RSA","kid":...}]}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-01-PLAN.md
@Hub_All/api/app/auth/jwt.py
@Hub_All/api/app/auth/dependencies.py
@Hub_All/api/app/main.py
@Hub_All/api/app/config.py

<interfaces>
<!-- Plan 03-01 đã ship publish_jwks + Settings.central_jwks_url field. Plan 03-02 extend. -->

Từ Plan 03-01 `api/app/auth/jwks.py` (Plan 03-02 EXTEND):

```python
class JWK(TypedDict):
    kty: str   # "RSA"
    kid: str
    use: str   # "sig"
    alg: str   # "RS256"
    n: str
    e: str

class JWKSet(TypedDict):
    keys: list[JWK]

def publish_jwks(public_key_path: Path) -> JWKSet: ...
def load_public_key_as_jwk(pem_path: Path, kid: str | None = None) -> JWK: ...
```

Plan 03-02 thêm `JWKSCache` class:

```python
import httpx
from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicKey
from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicNumbers
import base64

def _base64url_to_int(data: str) -> int:
    """Reverse _int_to_base64url — JWK n/e → big-int."""
    # Pad lại = base64url decode yêu cầu padding %4
    pad = "=" * (-len(data) % 4)
    raw = base64.urlsafe_b64decode(data + pad)
    return int.from_bytes(raw, byteorder="big")

def jwk_to_public_key(jwk: JWK) -> RSAPublicKey:
    """JWK dict → cryptography RSAPublicKey object cho pyjwt.decode()."""
    n = _base64url_to_int(jwk["n"])
    e = _base64url_to_int(jwk["e"])
    return RSAPublicNumbers(e=e, n=n).public_key()


class JWKSCache:
    """In-process LRU JWKS cache cho hub con verify JWT.

    Lifecycle:
    - fetch_initial() — blocking httpx.get timeout 5s → exception → boot fail-loud
    - start_refresh_task() — asyncio task chạy mỗi 1h, fail-quiet (log warning)
    - get_public_key(kid) — verify hot path; raise JWKSStaleError nếu > 24h
    - stop_refresh_task() — graceful shutdown lifespan
    """
    def __init__(self, jwks_url: str, refresh_interval: int = 3600,
                 max_stale_seconds: int = 86400, timeout: float = 5.0):
        self._jwks_url = jwks_url
        self._refresh_interval = refresh_interval
        self._max_stale_seconds = max_stale_seconds
        self._timeout = timeout
        self._keys_by_kid: dict[str, RSAPublicKey] = {}
        self._last_refresh_ts: float = 0.0
        self._refresh_task: asyncio.Task | None = None

    async def fetch_initial(self) -> None: ...  # blocking, raise on fail
    async def refresh(self) -> None: ...  # internal, called by background task
    async def _refresh_loop(self) -> None: ...  # asyncio.sleep + refresh
    def get_public_key(self, kid: str) -> RSAPublicKey: ...  # hot path
    def is_stale(self) -> bool: ...  # > max_stale_seconds since last refresh
    def start_refresh_task(self) -> None: ...  # spawn background task
    async def stop_refresh_task(self) -> None: ...  # graceful shutdown


class JWKSStaleError(Exception):
    """Raise khi cache > max_stale_seconds — 503 JWKS_STALE envelope."""

class JWKSKidNotFoundError(Exception):
    """Raise khi kid trong JWT header không match key nào trong cache."""
```

Từ `api/app/main.py` lifespan (M2 carry forward — Plan 03-02 thêm branch hub con):

```python
@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncIterator[None]:
    settings = get_settings()
    # ... db pool + redis + cocoindex + jwt_manager + watchdog + audit + search_cache ...

    # PLAN 03-02 THÊM: hub con JWKS cache lifecycle
    if settings.hub_name != "central":
        from app.auth.jwks import JWKSCache
        jwks_cache = JWKSCache(
            jwks_url=settings.central_jwks_url,
            refresh_interval=settings.jwks_refresh_interval,
            max_stale_seconds=settings.jwks_max_stale_seconds,
        )
        try:
            await jwks_cache.fetch_initial()  # blocking — boot fail-loud
        except Exception as e:
            logger.critical("jwks_fetch_initial_failed: %s", e)
            raise  # process exit 1
        jwks_cache.start_refresh_task()
        app.state.jwks_cache = jwks_cache
    yield
    # ... shutdown cleanup ...
    if settings.hub_name != "central":
        await app.state.jwks_cache.stop_refresh_task()
```

Từ `api/app/auth/dependencies.py::get_current_user` (M2 carry forward — Plan 03-02 thêm branch):

```python
async def get_current_user(token=..., jwt_mgr=..., redis=..., db=...):
    if not token: raise 401 MISSING_AUTHORIZATION
    try:
        # PLAN 03-02 THÊM: branch theo hub_name
        settings = get_settings()
        if settings.hub_name == "central":
            claims = jwt_mgr.verify_token(token, expected_type="access")  # local pem
        else:
            # Hub con — verify qua JWKS cache
            jwks_cache = getattr(request.app.state, "jwks_cache", None)
            if jwks_cache is None:
                raise 503 SERVICE_UNAVAILABLE
            unverified_header = pyjwt.get_unverified_header(token)
            kid = unverified_header.get("kid")
            if kid is None:
                raise 401 INVALID_TOKEN ("Token thiếu kid header")
            try:
                public_key = jwks_cache.get_public_key(kid)
            except JWKSStaleError:
                raise 503 JWKS_STALE
            except JWKSKidNotFoundError:
                raise 401 INVALID_TOKEN ("kid không khớp JWKS cache")
            # Verify JWT bằng public_key (PyJWT decode)
            claims = jwt_mgr.verify_token_with_key(token, public_key, expected_type="access")
    except JWTError as e: ...
    # ... rest unchanged ...
```

Plan 03-02 cần thêm method `JWTManager.verify_token_with_key(token, public_key, ...)` ở `jwt.py` — wrapper quanh `pyjwt.decode(token, public_key, algorithms=["RS256"], issuer=JWT_ISSUER)`.

**Pattern: JWT header `kid` field:**

Plan 03-01 publish_jwks() trả JWK với deterministic kid (SHA-256 8-byte PEM). Plan 03-03 sẽ wire JWT signing thêm `kid` ở header (`pyjwt.encode(claims, key, algorithm="RS256", headers={"kid": <derived_kid>})`). Plan 03-02 chưa có header kid → JWT cũ M2 KHÔNG có kid → hub con verify fail "Token thiếu kid header". ACCEPTABLE bằng cách Plan 03-02 add `kid` ở `JWTManager.issue_token_pair` (1 dòng) — fallback `kid=None` ở header nếu thiếu (M2 backward compat).

**Pattern: httpx async client (M2 baseline pulled):**

```python
import httpx

async with httpx.AsyncClient(timeout=self._timeout) as client:
    resp = await client.get(self._jwks_url)
    resp.raise_for_status()
    data = resp.json()
    # validate shape: {"keys": [{"kty":"RSA","kid":"...","n":"...","e":"..."}]}
    if "keys" not in data or not isinstance(data["keys"], list):
        raise ValueError("JWKS response thiếu 'keys' list")
```
</interfaces>

<notes>
**Settings 2 field mới:**

Plan 03-02 thêm 2 field config-driven (default match D-V3-Phase3-B):

```python
# Phase 3 Plan 03-02 — JWKS cache lifecycle
jwks_refresh_interval: int = 3600        # 1h — background refresh chu kỳ
jwks_max_stale_seconds: int = 86400      # 24h — hard limit fail-loud delayed
```

Default ở Settings (cho ops override qua env nếu cần test rotation nhanh hơn).

**Test integration spawn 2 process:**

Plan 03-02 Task 5 integration test KHÓ vì cần spawn 2 FastAPI app (central + yte) đồng thời + httpx call cross-process. Pattern:

```python
import asyncio
from httpx import ASGITransport, AsyncClient

async def test_jwks_cache_rotate_keypair_detects_new_kid(monkeypatch, tmp_path):
    # Spawn central app
    _setup_env(monkeypatch, "central")
    from app.main import create_app
    app_central = create_app()
    # Start central lifespan
    async with AsyncClient(transport=ASGITransport(app=app_central), base_url="http://central") as client_central:
        # Fetch JWKS từ central
        jwks_resp = await client_central.get("/.well-known/jwks.json")
        original_kid = jwks_resp.json()["keys"][0]["kid"]

    # Now spawn hub yte — set CENTRAL_JWKS_URL trỏ central ASGI...
    # NHƯNG: hub con dùng httpx call HTTP — KHÔNG ASGI in-process direct.
    # Workaround: mock JWKSCache fetch để pull từ central app trong test.
    # Hoặc: spawn 2 process qua subprocess + uvicorn ports — quá phức tạp cho unit.
```

Pragmatic approach Plan 03-02:
- Unit test (Task 4 `test_jwks_cache.py`): mock `httpx.AsyncClient.get` trả JSON cố định để verify fetch_initial / refresh / 24h hard limit logic.
- Integration test (Task 5 `test_jwks_cache_lifecycle.py`): scenario nhỏ hơn — verify Settings validator hub con required + JWKSCache instantiate với mocked httpx + key rotation simulation qua mocked response (KHÔNG spawn 2 process thật).

Full multi-process rotation E2E defer Plan 03-05 closeout smoke checkpoint (chạy thật 2 service compose).

**Backward compat M2 JWT (kid header missing):**

Plan 03-02 add `kid` ở `JWTManager.issue_token_pair` headers (`pyjwt.encode(..., headers={"kid": derived_kid})`). M2 JWT cũ chưa có kid → sau Plan 03-02 deploy:
- Central: vẫn verify bằng local pem (KHÔNG cần kid).
- Hub con: JWT cũ không có kid → 401 "Token thiếu kid header" → user re-login.

Acceptable downtime ~15-30s — communicate operator (CONTEXT.md `<specifics>` đã note). README + CLAUDE.md sẽ thêm note ở Plan 03-05 closeout.

**KHÔNG đụng `service.py` blacklist:**

Plan 03-02 chỉ verify JWT signature qua JWKS cache. Redis blacklist key rename (`blacklist:` → `auth:blacklist:`) + hub_ids claim build → Plan 03-03 ship.

**KHÔNG đụng router.py:**

Hub con 307 redirect login/refresh → Plan 03-04. Plan 03-02 chỉ verify path dependency.
</notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Settings validator + 2 field jwks_refresh_interval / jwks_max_stale_seconds + Plan 03-01 field reuse</name>
  <files>api/app/config.py, api/tests/unit/test_config_jwks.py</files>
  <read_first>
    - Hub_All/api/app/config.py (Settings class + Plan 03-01 đã thêm `central_jwks_url: str | None = None` field + Plan 02-05 `_enforce_hub_dsn_match` validator pattern)
    - Hub_All/api/tests/unit/test_config_hub_name.py (Phase 1+2 validator test pattern monkeypatch.setenv + ValidationError)
  </read_first>
  <action>
**Step 1: Thêm 2 field cấu hình cache lifecycle** vào `api/app/config.py` (sau `central_jwks_url` từ Plan 03-01):

```python
    # Phase 3 Plan 03-01 SSO-01 (D-V3-Phase3-A) — Hub con consume JWKS endpoint
    central_jwks_url: str | None = None

    # Phase 3 Plan 03-02 SSO-01 (D-V3-Phase3-B) — Hub con cache lifecycle
    # 1h refresh interval matching Cache-Control max-age=3600 ở Plan 03-01.
    # 24h hard limit (86400s): nếu cached value > limit → 503 JWKS_STALE
    # envelope cho mọi JWT verify (R-V3-5 fail-loud delayed). Override qua env
    # JWKS_REFRESH_INTERVAL + JWKS_MAX_STALE_SECONDS nếu test rotation nhanh.
    jwks_refresh_interval: int = 3600
    jwks_max_stale_seconds: int = 86400
```

**Step 2: Thêm `@model_validator(mode="after")` `_enforce_central_jwks_url_for_hub`** — hub con required, central None OK:

Đặt SAU validator `_enforce_hub_dsn_match` hiện hữu (Phase 1 carry forward — line ~226):

```python
    @model_validator(mode="after")
    def _enforce_central_jwks_url_for_hub(self) -> Settings:
        """Phase 3 Plan 03-02 SSO-01 (D-V3-Phase3-B) — Hub con required CENTRAL_JWKS_URL.

        Central (hub_name="central") tự có private.pem local → KHÔNG cần fetch.
        Hub con (yte/duoc/hcns/dynamic) PHẢI set CENTRAL_JWKS_URL trỏ central
        endpoint để JWKSCache.fetch_initial() blocking startup. Thiếu → boot fail
        ở Settings validation (ValidationError trước khi tới lifespan).

        Threat model:
        - T-03-02-01 Tampering — env thiếu CENTRAL_JWKS_URL → hub con boot OK
          nhưng mọi JWT verify trả 500 (chưa lifespan startup). Fail-fast ở
          validator tránh production bug câm lặng.
        """
        if self.hub_name != "central" and not self.central_jwks_url:
            raise ValueError(
                f"hub_name={self.hub_name!r} (hub con) yêu cầu CENTRAL_JWKS_URL "
                f"env var. Set CENTRAL_JWKS_URL=http://python-api-central:8080"
                f"/.well-known/jwks.json (xem docker-compose.yml 3 hub con block)."
            )
        return self
```

**Step 3: Tạo `api/tests/unit/test_config_jwks.py`** với 5 test:

```python
"""Phase 3 SSO-01 — Unit test Settings JWKS cache lifecycle config (Plan 03-02 Task 1).

Verify:
- central_jwks_url default None (Plan 03-01 ship)
- jwks_refresh_interval default 3600 (1h)
- jwks_max_stale_seconds default 86400 (24h)
- Central hub_name=central + central_jwks_url=None OK
- Hub con + central_jwks_url=None raise ValidationError
- Hub con + central_jwks_url=set OK
"""
from __future__ import annotations

import pytest
from pydantic import ValidationError


def _common_env(monkeypatch, hub_name: str) -> None:
    db = "medinet_central" if hub_name == "central" else f"medinet_hub_{hub_name}"
    monkeypatch.setenv("HUB_NAME", hub_name)
    monkeypatch.setenv(
        "DATABASE_URL", f"postgresql+asyncpg://u:p@localhost:5432/{db}"
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")


def test_jwks_defaults_match_d_v3_phase3_b(monkeypatch) -> None:
    """Default jwks_refresh_interval=3600, jwks_max_stale_seconds=86400."""
    _common_env(monkeypatch, "central")
    from app.config import Settings, get_settings

    get_settings.cache_clear()
    s = Settings()
    assert s.jwks_refresh_interval == 3600
    assert s.jwks_max_stale_seconds == 86400
    assert s.central_jwks_url is None  # central default None — Plan 03-01


def test_central_jwks_url_none_for_central_ok(monkeypatch) -> None:
    """Central + central_jwks_url=None OK (KHÔNG cần fetch — local pem)."""
    _common_env(monkeypatch, "central")
    from app.config import Settings, get_settings

    get_settings.cache_clear()
    s = Settings()
    assert s.hub_name == "central"
    assert s.central_jwks_url is None


@pytest.mark.parametrize("hub_name", ["yte", "duoc", "hcns", "phap_che"])
def test_hub_con_requires_central_jwks_url(monkeypatch, hub_name: str) -> None:
    """Hub con + central_jwks_url=None → ValidationError."""
    _common_env(monkeypatch, hub_name)
    from app.config import Settings, get_settings

    get_settings.cache_clear()
    with pytest.raises(ValidationError, match="CENTRAL_JWKS_URL"):
        Settings()


@pytest.mark.parametrize("hub_name", ["yte", "duoc", "hcns", "phap_che"])
def test_hub_con_with_central_jwks_url_ok(monkeypatch, hub_name: str) -> None:
    """Hub con + central_jwks_url set → OK."""
    _common_env(monkeypatch, hub_name)
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import Settings, get_settings

    get_settings.cache_clear()
    s = Settings()
    assert s.hub_name == hub_name
    assert s.central_jwks_url == (
        "http://python-api-central:8080/.well-known/jwks.json"
    )


def test_jwks_refresh_interval_override(monkeypatch) -> None:
    """Override JWKS_REFRESH_INTERVAL qua env (test rotation nhanh)."""
    _common_env(monkeypatch, "yte")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    monkeypatch.setenv("JWKS_REFRESH_INTERVAL", "60")
    monkeypatch.setenv("JWKS_MAX_STALE_SECONDS", "3600")
    from app.config import Settings, get_settings

    get_settings.cache_clear()
    s = Settings()
    assert s.jwks_refresh_interval == 60
    assert s.jwks_max_stale_seconds == 3600
```

**KHÔNG đụng:**
- `_enforce_hub_dsn_match` validator (Phase 1 carry forward).
- `_validate_hub_name` validator (Plan 02-05 FACTOR-04 carry forward).
- Settings field khác.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_config_jwks.py tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "jwks_refresh_interval\|jwks_max_stale_seconds" Hub_All/api/app/config.py` ≥ 2
    - `grep -c "_enforce_central_jwks_url_for_hub" Hub_All/api/app/config.py` ≥ 1
    - `grep -c "D-V3-Phase3-B\|SSO-01" Hub_All/api/app/config.py` ≥ 2 (REQ + decision traceability)
    - File `Hub_All/api/tests/unit/test_config_jwks.py` exists
    - `grep -c "def test_jwks_defaults_match_d_v3_phase3_b\|def test_central_jwks_url_none_for_central_ok\|def test_hub_con_requires_central_jwks_url\|def test_hub_con_with_central_jwks_url_ok\|def test_jwks_refresh_interval_override" Hub_All/api/tests/unit/test_config_jwks.py` ≥ 5
    - `cd Hub_All/api && uv run ruff check app/config.py tests/unit/test_config_jwks.py` exit 0
    - `cd Hub_All/api && uv run mypy app/config.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_config_jwks.py -v` PASS ≥ 11 test (parametrize × 4 hub × 2 test + 3 singular)
    - Phase 1+2 Settings regression KHÔNG break: `cd Hub_All/api && uv run pytest tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v` PASS 40/40
  </acceptance_criteria>
  <done>
Settings 2 field config-driven (jwks_refresh_interval=3600, jwks_max_stale_seconds=86400) + 1 model_validator enforce hub con CENTRAL_JWKS_URL required. 11 unit test mới PASS + 40/40 Phase 1+2 regression PASS. Lint + mypy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `api/app/auth/jwks.py` thêm `JWKSCache` class + 2 exception + `jwk_to_public_key` helper (TDD)</name>
  <files>api/app/auth/jwks.py, api/tests/unit/test_jwks_cache.py</files>
  <read_first>
    - Hub_All/api/app/auth/jwks.py (Plan 03-01 đã ship — Plan 03-02 extend append-only)
    - Hub_All/api/app/auth/jwt.py (JWTManager + JWT_ALGORITHM constant — Plan 03-02 sẽ thêm verify_token_with_key wrapper)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (D-V3-Phase3-B lifecycle spec — boot fail-loud + runtime fail-quiet + 24h hard limit)
  </read_first>
  <behavior>
    - Test 1 (`test_jwks_cache_fetch_initial_success`): mock httpx trả `{"keys":[{"kty":"RSA","kid":"abc","alg":"RS256","n":"<base64url>","e":"AQAB"}]}` → `cache.fetch_initial()` complete → `cache.get_public_key("abc")` trả RSAPublicKey object.
    - Test 2 (`test_jwks_cache_fetch_initial_timeout_raises`): mock httpx raise `httpx.TimeoutException` → `fetch_initial()` raise → boot fail-loud.
    - Test 3 (`test_jwks_cache_fetch_initial_500_raises`): mock httpx trả 500 status → raise_for_status raise → fail-loud.
    - Test 4 (`test_jwks_cache_fetch_initial_invalid_shape_raises`): mock httpx trả `{"invalid": "shape"}` → `ValueError("JWKS response thiếu 'keys' list")`.
    - Test 5 (`test_jwks_cache_get_public_key_kid_not_found`): cache có kid "abc" → `get_public_key("xyz")` raise `JWKSKidNotFoundError`.
    - Test 6 (`test_jwks_cache_24h_hard_limit_raises_stale`): cache với `max_stale_seconds=1` + manual set `_last_refresh_ts = time.time() - 2` → `is_stale()` True → `get_public_key("abc")` raise `JWKSStaleError`.
    - Test 7 (`test_jwks_cache_refresh_fail_quiet_keeps_old_value`): cache đã fetch_initial OK → mock refresh httpx fail → `refresh()` KHÔNG raise + log warning → `get_public_key` vẫn return cached value (KHÔNG stale).
    - Test 8 (`test_jwk_to_public_key_roundtrip`): publish_jwks(public.pem) → jwk_to_public_key(jwk) → verify cryptography RSAPublicKey instance + sign/verify roundtrip với JWT RS256 token.
    - Test 9 (`test_base64url_to_int_roundtrip`): `_int_to_base64url(65537)` → "AQAB" → `_base64url_to_int("AQAB")` → 65537 (inverse property).
  </behavior>
  <action>
**Step 1: Extend `api/app/auth/jwks.py`** với JWKSCache + 2 exception + 2 helper:

Append SAU code Plan 03-01 (sau `__all__`):

```python
# ────────────────────────────────────────────────────────────────────────────
# Plan 03-02 — JWKSCache class + lifecycle (SSO-01 part 2, D-V3-Phase3-B/D)
# ────────────────────────────────────────────────────────────────────────────

import asyncio
import logging
import time
from typing import TYPE_CHECKING

import httpx

if TYPE_CHECKING:
    from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicKey

logger = logging.getLogger(__name__)


class JWKSStaleError(Exception):
    """Raise khi cache > max_stale_seconds — caller render 503 JWKS_STALE envelope."""


class JWKSKidNotFoundError(Exception):
    """Raise khi JWT header kid không match key nào trong cache."""


def _base64url_to_int(data: str) -> int:
    """Inverse `_int_to_base64url` — JWK n/e base64url → big-int.

    base64.urlsafe_b64decode yêu cầu padding %4 — pad lại để decode OK.
    """
    pad = "=" * (-len(data) % 4)
    raw = base64.urlsafe_b64decode(data + pad)
    return int.from_bytes(raw, byteorder="big")


def jwk_to_public_key(jwk: JWK) -> "RSAPublicKey":
    """JWK dict RFC 7517 → cryptography RSAPublicKey object cho pyjwt.decode().

    Reverse `load_public_key_as_jwk` — verify roundtrip property qua test.
    """
    if jwk["kty"] != "RSA":
        raise ValueError(f"JWK kty={jwk['kty']!r}, chỉ support RSA (RS256).")
    n = _base64url_to_int(jwk["n"])
    e = _base64url_to_int(jwk["e"])
    return RSAPublicNumbers(e=e, n=n).public_key()


class JWKSCache:
    """In-process LRU JWKS cache cho hub con verify JWT (D-V3-Phase3-D).

    Lifecycle:
        1. `fetch_initial()` — blocking httpx.get timeout 5s → exception →
           boot fail-loud (lifespan startup raise → process exit 1).
        2. `start_refresh_task()` — spawn asyncio task chạy `_refresh_loop()`
           mỗi `refresh_interval` sec, fail-quiet (log warning + giữ cached).
        3. `get_public_key(kid)` — verify hot path; raise `JWKSStaleError`
           nếu cache > `max_stale_seconds` (R-V3-5 fail-loud delayed).
        4. `stop_refresh_task()` — graceful shutdown (cancel asyncio task).

    Threat model:
        - T-03-02-02 (DoS) — httpx timeout 5s + circuit breaker chống stuck
        - T-03-02-03 (Tampering) — kid mismatch khi key rotation → force
          re-fetch ngay lập tức (KHÔNG đợi TTL) qua trigger ở get_public_key
    """

    def __init__(
        self,
        jwks_url: str,
        *,
        refresh_interval: int = 3600,
        max_stale_seconds: int = 86400,
        timeout: float = 5.0,
    ) -> None:
        self._jwks_url = jwks_url
        self._refresh_interval = refresh_interval
        self._max_stale_seconds = max_stale_seconds
        self._timeout = timeout
        self._keys_by_kid: dict[str, RSAPublicKey] = {}
        self._last_refresh_ts: float = 0.0
        self._refresh_task: asyncio.Task[None] | None = None
        self._lock = asyncio.Lock()

    async def fetch_initial(self) -> None:
        """Blocking fetch — raise nếu fail (boot fail-loud D-V3-Phase3-B)."""
        try:
            await self._fetch_and_cache()
        except Exception as e:
            logger.critical(
                "jwks_fetch_initial_failed: url=%s error=%s — boot abort",
                self._jwks_url,
                e,
            )
            raise

    async def refresh(self) -> None:
        """Internal refresh — fail-quiet (log warning + giữ cached)."""
        try:
            await self._fetch_and_cache()
        except Exception as e:
            logger.warning(
                "jwks_refresh_failed: url=%s error=%s — giữ cached value",
                self._jwks_url,
                e,
            )
            # KHÔNG raise — fail-quiet runtime D-V3-Phase3-B

    async def _fetch_and_cache(self) -> None:
        """Fetch JWKS từ URL + populate cache. Internal — caller wrap try/except."""
        async with httpx.AsyncClient(timeout=self._timeout) as client:
            resp = await client.get(self._jwks_url)
            resp.raise_for_status()
            data = resp.json()
        if not isinstance(data, dict) or "keys" not in data:
            raise ValueError(
                f"JWKS response thiếu 'keys' list (got: {type(data).__name__})"
            )
        keys = data["keys"]
        if not isinstance(keys, list) or not keys:
            raise ValueError(
                f"JWKS response 'keys' phải là non-empty list (got: {keys!r})"
            )
        new_keys: dict[str, RSAPublicKey] = {}
        for jwk_dict in keys:
            if not isinstance(jwk_dict, dict) or "kid" not in jwk_dict:
                logger.warning("jwks_skip_invalid_jwk: %r", jwk_dict)
                continue
            try:
                pub_key = jwk_to_public_key(jwk_dict)  # type: ignore[arg-type]
            except (ValueError, KeyError) as e:
                logger.warning(
                    "jwks_skip_unparseable_key: kid=%s error=%s",
                    jwk_dict.get("kid"),
                    e,
                )
                continue
            new_keys[jwk_dict["kid"]] = pub_key
        if not new_keys:
            raise ValueError(
                "JWKS response không có key hợp lệ sau parse"
            )
        async with self._lock:
            self._keys_by_kid = new_keys
            self._last_refresh_ts = time.time()
        logger.info(
            "jwks_cache_updated: kids=%s last_refresh_ts=%.0f",
            sorted(new_keys.keys()),
            self._last_refresh_ts,
        )

    def is_stale(self) -> bool:
        """True nếu cache > max_stale_seconds — caller raise 503."""
        if self._last_refresh_ts == 0.0:
            return True  # chưa fetch_initial → stale by definition
        return (time.time() - self._last_refresh_ts) > self._max_stale_seconds

    def get_public_key(self, kid: str) -> "RSAPublicKey":
        """Verify hot path — raise JWKSStaleError nếu cache quá hạn 24h.

        Caller (get_current_user dependency Task 4) catch:
        - JWKSStaleError → 503 JWKS_STALE envelope
        - JWKSKidNotFoundError → 401 INVALID_TOKEN (kid mismatch)
        """
        if self.is_stale():
            raise JWKSStaleError(
                f"JWKS cache quá hạn {self._max_stale_seconds}s "
                f"({time.time() - self._last_refresh_ts:.0f}s since last refresh)"
            )
        if kid not in self._keys_by_kid:
            raise JWKSKidNotFoundError(
                f"kid={kid!r} không trong JWKS cache "
                f"(available: {sorted(self._keys_by_kid.keys())})"
            )
        return self._keys_by_kid[kid]

    def start_refresh_task(self) -> None:
        """Spawn asyncio background task refresh mỗi refresh_interval sec."""
        if self._refresh_task is not None and not self._refresh_task.done():
            logger.warning("jwks_refresh_task_already_running — skip")
            return
        self._refresh_task = asyncio.create_task(self._refresh_loop())
        logger.info(
            "jwks_refresh_task_started: interval=%ds", self._refresh_interval
        )

    async def _refresh_loop(self) -> None:
        """Asyncio task — sleep refresh_interval + refresh, lặp vô tận."""
        while True:
            try:
                await asyncio.sleep(self._refresh_interval)
                await self.refresh()
            except asyncio.CancelledError:
                logger.info("jwks_refresh_loop_cancelled")
                raise
            except Exception as e:  # noqa: BLE001
                # _fetch_and_cache đã log; defensive bao thêm
                logger.error("jwks_refresh_loop_unexpected: %s", e)

    async def stop_refresh_task(self) -> None:
        """Graceful shutdown (cancel asyncio task)."""
        if self._refresh_task is None:
            return
        self._refresh_task.cancel()
        try:
            await self._refresh_task
        except asyncio.CancelledError:
            pass
        self._refresh_task = None
        logger.info("jwks_refresh_task_stopped")


# Update __all__ to export new symbols
__all__ = [
    "JWK",
    "JWKSet",
    "JWKSCache",
    "JWKSKidNotFoundError",
    "JWKSStaleError",
    "jwk_to_public_key",
    "load_public_key_as_jwk",
    "publish_jwks",
]
```

**Step 2: Tạo `api/tests/unit/test_jwks_cache.py`** với 9 test (xem `<behavior>`):

```python
"""Phase 3 SSO-01 — Unit test JWKSCache lifecycle (Plan 03-02 Task 2).

Verify:
- fetch_initial success + timeout/500/invalid shape raise (boot fail-loud)
- get_public_key + kid not found + 24h hard limit stale
- refresh fail-quiet (giữ cached)
- jwk_to_public_key roundtrip với publish_jwks (sign/verify property)
- base64url_to_int inverse property
"""
from __future__ import annotations

import asyncio
import time
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock, patch

import httpx
import pytest

from app.auth.jwks import (
    JWKSCache,
    JWKSKidNotFoundError,
    JWKSStaleError,
    _base64url_to_int,
    _int_to_base64url,
    jwk_to_public_key,
    load_public_key_as_jwk,
    publish_jwks,
)


_VALID_JWKS_RESPONSE: dict = {}  # populate ở fixture từ M2 public.pem


@pytest.fixture
def public_key_path() -> Path:
    p = Path("keys/public.pem")
    if not p.exists():
        pytest.skip("api/keys/public.pem missing — run 'make keys'")
    return p


@pytest.fixture
def valid_jwks_response(public_key_path: Path) -> dict:
    """Build JWKS response từ M2 public.pem để mock httpx trả về."""
    return publish_jwks(public_key_path)  # type: ignore[return-value]


def _make_mock_httpx_get(response_data: dict, status_code: int = 200):
    """Helper build mock httpx async client trả JSON cố định."""
    mock_resp = MagicMock()
    mock_resp.status_code = status_code
    mock_resp.json = MagicMock(return_value=response_data)
    if status_code >= 400:
        mock_resp.raise_for_status = MagicMock(
            side_effect=httpx.HTTPStatusError(
                "mock 500",
                request=MagicMock(),
                response=mock_resp,
            )
        )
    else:
        mock_resp.raise_for_status = MagicMock()
    mock_get = AsyncMock(return_value=mock_resp)
    mock_client = MagicMock()
    mock_client.__aenter__ = AsyncMock(return_value=mock_client)
    mock_client.__aexit__ = AsyncMock(return_value=None)
    mock_client.get = mock_get
    return mock_client


@pytest.mark.asyncio
async def test_jwks_cache_fetch_initial_success(valid_jwks_response: dict) -> None:
    """Happy path — fetch_initial populate cache + get_public_key trả RSAPublicKey."""
    from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicKey

    cache = JWKSCache(jwks_url="http://test/jwks.json")
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _make_mock_httpx_get(valid_jwks_response)
        await cache.fetch_initial()
    kid = valid_jwks_response["keys"][0]["kid"]
    public_key = cache.get_public_key(kid)
    assert isinstance(public_key, RSAPublicKey)


@pytest.mark.asyncio
async def test_jwks_cache_fetch_initial_timeout_raises() -> None:
    """httpx.TimeoutException → fetch_initial raise → boot fail-loud."""
    cache = JWKSCache(jwks_url="http://test/jwks.json", timeout=0.1)
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(
            side_effect=httpx.TimeoutException("mock timeout")
        )
        mock_client_cls.return_value = mock_client
        with pytest.raises(httpx.TimeoutException):
            await cache.fetch_initial()


@pytest.mark.asyncio
async def test_jwks_cache_fetch_initial_500_raises(
    valid_jwks_response: dict,
) -> None:
    """httpx 500 → raise_for_status raise → fail-loud."""
    cache = JWKSCache(jwks_url="http://test/jwks.json")
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _make_mock_httpx_get(
            valid_jwks_response, status_code=500
        )
        with pytest.raises(httpx.HTTPStatusError):
            await cache.fetch_initial()


@pytest.mark.asyncio
async def test_jwks_cache_fetch_initial_invalid_shape_raises() -> None:
    """JWKS response {\"invalid\": \"shape\"} → ValueError."""
    cache = JWKSCache(jwks_url="http://test/jwks.json")
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _make_mock_httpx_get({"invalid": "shape"})
        with pytest.raises(ValueError, match="'keys' list"):
            await cache.fetch_initial()


@pytest.mark.asyncio
async def test_jwks_cache_get_public_key_kid_not_found(
    valid_jwks_response: dict,
) -> None:
    """kid không trong cache → JWKSKidNotFoundError."""
    cache = JWKSCache(jwks_url="http://test/jwks.json")
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _make_mock_httpx_get(valid_jwks_response)
        await cache.fetch_initial()
    with pytest.raises(JWKSKidNotFoundError, match="không trong JWKS cache"):
        cache.get_public_key("nonexistent_kid_xyz")


@pytest.mark.asyncio
async def test_jwks_cache_24h_hard_limit_raises_stale(
    valid_jwks_response: dict,
) -> None:
    """Cache > max_stale_seconds → JWKSStaleError ở get_public_key."""
    cache = JWKSCache(
        jwks_url="http://test/jwks.json",
        max_stale_seconds=1,  # 1s cho test nhanh
    )
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _make_mock_httpx_get(valid_jwks_response)
        await cache.fetch_initial()
    kid = valid_jwks_response["keys"][0]["kid"]
    # Manual rewind last_refresh_ts để mô phỏng stale
    cache._last_refresh_ts = time.time() - 2
    assert cache.is_stale()
    with pytest.raises(JWKSStaleError, match="quá hạn"):
        cache.get_public_key(kid)


@pytest.mark.asyncio
async def test_jwks_cache_refresh_fail_quiet_keeps_old_value(
    valid_jwks_response: dict,
) -> None:
    """refresh() fail → log warning + giữ cached + KHÔNG raise."""
    cache = JWKSCache(jwks_url="http://test/jwks.json")
    # fetch_initial OK
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _make_mock_httpx_get(valid_jwks_response)
        await cache.fetch_initial()
    kid = valid_jwks_response["keys"][0]["kid"]
    cached_key = cache.get_public_key(kid)

    # refresh fail
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(
            side_effect=httpx.TimeoutException("mock timeout refresh")
        )
        mock_client_cls.return_value = mock_client
        await cache.refresh()  # KHÔNG raise — fail-quiet

    # Cached value vẫn còn
    cached_key_after = cache.get_public_key(kid)
    assert cached_key is cached_key_after


def test_jwk_to_public_key_roundtrip(public_key_path: Path) -> None:
    """publish_jwks → jwk_to_public_key roundtrip + sign/verify property qua JWT."""
    import jwt as pyjwt
    from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicKey

    jwks = publish_jwks(public_key_path)
    jwk = jwks["keys"][0]
    pub_key = jwk_to_public_key(jwk)
    assert isinstance(pub_key, RSAPublicKey)

    # Sign JWT bằng private key + verify bằng public_key reconstruct
    private_pem = Path("keys/private.pem").read_bytes()
    token = pyjwt.encode(
        {"sub": "test", "iss": "medinet-wiki"},
        private_pem,
        algorithm="RS256",
    )
    decoded = pyjwt.decode(
        token,
        pub_key,
        algorithms=["RS256"],
        issuer="medinet-wiki",
    )
    assert decoded["sub"] == "test"


def test_base64url_to_int_roundtrip() -> None:
    """_int_to_base64url ↔ _base64url_to_int inverse property."""
    assert _base64url_to_int("AQAB") == 65537
    assert _base64url_to_int(_int_to_base64url(65537)) == 65537
    assert _base64url_to_int(_int_to_base64url(2**2048 - 1)) == 2**2048 - 1
    assert _int_to_base64url(_base64url_to_int("AQA")) == "AQA"
```

**KHÔNG đụng:**
- Code Plan 03-01 cũ trong jwks.py (publish_jwks + load_public_key_as_jwk + 2 helper).
- `JWTManager` (Task 3 sẽ thêm `verify_token_with_key` wrapper).
- service.py + dependencies.py (Task 4 sửa dependencies.py riêng).

**Edge case:** pytest-asyncio config — kiểm tra `pyproject.toml` đã có `asyncio_mode = "auto"` (M2 baseline) hoặc fallback `@pytest.mark.asyncio` decorator (đã dùng explicit để chắc chắn).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_jwks_cache.py tests/unit/test_jwks_publish.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class JWKSCache" Hub_All/api/app/auth/jwks.py` ≥ 1
    - `grep -c "class JWKSStaleError\|class JWKSKidNotFoundError" Hub_All/api/app/auth/jwks.py` ≥ 2
    - `grep -c "async def fetch_initial\|async def refresh\|async def _fetch_and_cache\|async def _refresh_loop\|async def stop_refresh_task\|def start_refresh_task\|def get_public_key\|def is_stale" Hub_All/api/app/auth/jwks.py` ≥ 8 (method coverage)
    - `grep -c "def jwk_to_public_key\|def _base64url_to_int" Hub_All/api/app/auth/jwks.py` ≥ 2
    - `grep -c "D-V3-Phase3-B\|D-V3-Phase3-D\|SSO-01" Hub_All/api/app/auth/jwks.py` ≥ 3 (REQ + 2 decision traceability)
    - File `Hub_All/api/tests/unit/test_jwks_cache.py` exists với 9 test (xem behavior)
    - `cd Hub_All/api && uv run ruff check app/auth/jwks.py tests/unit/test_jwks_cache.py` exit 0
    - `cd Hub_All/api && uv run mypy app/auth/jwks.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_cache.py -v` PASS ≥ 9 test
    - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py -v` PASS ≥ 9 test (Plan 03-01 regression KHÔNG break)
  </acceptance_criteria>
  <done>
JWKSCache class extend jwks.py: fetch_initial blocking + refresh fail-quiet + 24h hard limit + asyncio task lifecycle. 2 exception + 2 helper (jwk_to_public_key + _base64url_to_int). 9 unit test PASS verify lifecycle + edge case. Plan 03-01 regression 9 test PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lifespan startup blocking fetch JWKS cho hub con + JWTManager.verify_token_with_key wrapper</name>
  <files>api/app/main.py, api/app/auth/jwt.py</files>
  <read_first>
    - Hub_All/api/app/main.py (lifespan function line ~58-322 — Plan 03-02 thêm branch hub con cuối block, trước yield)
    - Hub_All/api/app/auth/jwt.py (JWTManager class — Plan 03-02 thêm method verify_token_with_key)
    - Hub_All/api/app/auth/jwks.py (Task 2 ship JWKSCache — Plan 03-02 import + instantiate)
  </read_first>
  <action>
**Step 1: Thêm method `verify_token_with_key`** vào `JWTManager` (`api/app/auth/jwt.py`):

Append sau method `verify_token` hiện hữu (sau line ~190):

```python
    def verify_token_with_key(
        self,
        token: str,
        public_key: bytes | "RSAPublicKey",
        *,
        expected_type: Literal["access", "refresh"],
    ) -> JWTClaims:
        """Verify JWT bằng public key external (Plan 03-02 hub con JWKS cache).

        Phase 3 Plan 03-02 SSO-01 (D-V3-Phase3-D). Hub con verify JWT bằng
        public key từ JWKSCache (KHÔNG dùng self._public_pem — local pem
        chỉ có ở central). Logic decode + validate giống `verify_token`,
        chỉ khác key source.

        Args:
            token: JWT string.
            public_key: RSAPublicKey object (từ JWKSCache.get_public_key)
                hoặc PEM bytes (backward compat).
            expected_type: "access" | "refresh".

        Raises:
            JWTError với message tiếng Việt (alg/issuer/exp/type mismatch).
        """
        try:
            decoded = pyjwt.decode(
                token,
                public_key,
                algorithms=JWT_ALGORITHMS_ALLOWED,
                issuer=JWT_ISSUER,
            )
        except pyjwt.ExpiredSignatureError as e:
            raise JWTError("Token đã hết hạn") from e
        except pyjwt.InvalidIssuerError as e:
            raise JWTError("Token không hợp lệ (issuer sai)") from e
        except pyjwt.InvalidTokenError as e:
            raise JWTError(f"Token không hợp lệ: {e}") from e

        try:
            claims = JWTClaims.model_validate(decoded)
        except Exception as e:  # pydantic.ValidationError
            raise JWTError(f"Claims không hợp lệ: {e}") from e

        if claims.token_type != expected_type:
            raise JWTError(
                f"Loại token sai: yêu cầu {expected_type!r}, "
                f"nhận {claims.token_type!r}"
            )
        return claims
```

Add import nếu chưa có ở top:

```python
from typing import TYPE_CHECKING
if TYPE_CHECKING:
    from cryptography.hazmat.primitives.asymmetric.rsa import RSAPublicKey
```

**Step 2: Thêm kid header khi sign JWT** (`JWTManager.issue_token_pair`):

Tìm dòng `pyjwt.encode(access_claims, self._private_pem, algorithm=JWT_ALGORITHM)` (line ~136) — sửa thêm `headers={"kid": self._kid}`:

```python
        # Header kid để hub con JWKSCache match public key (Plan 03-02 SSO-01)
        access_token = pyjwt.encode(
            access_claims,
            self._private_pem,
            algorithm=JWT_ALGORITHM,
            headers={"kid": self._kid},
        )
        refresh_token = pyjwt.encode(
            refresh_claims,
            self._private_pem,
            algorithm=JWT_ALGORITHM,
            headers={"kid": self._kid},
        )
```

Thêm `self._kid` derive ở `__init__`:

```python
    def __init__(self, settings: Settings) -> None:
        self._private_pem = self._load_key(settings.jwt_private_key_path)
        self._public_pem = self._load_key(settings.jwt_public_key_path)
        self._access_ttl = timedelta(seconds=settings.jwt_access_token_ttl)
        self._refresh_ttl = timedelta(seconds=settings.jwt_refresh_token_ttl)
        # Phase 3 Plan 03-02 SSO-01 — kid derive deterministic SHA-256 public.pem
        # (cùng pattern jwks.py::_derive_kid). Hub con JWKSCache match qua kid.
        from app.auth.jwks import _derive_kid
        self._kid = _derive_kid(self._public_pem)
```

**Step 3: Lifespan branch hub con** — thêm sau lifespan startup block (M2 init order: db pool → redis → jwt_manager → cocoindex → watchdog → audit → search_cache):

Trong `api/app/main.py` lifespan function, sau dòng `app.state.jwt_manager = JWTManager(settings)` (M2 carry forward), thêm:

```python
    # ──────────────────────────────────────────────────────────────────────
    # Phase 3 Plan 03-02 (SSO-01, D-V3-Phase3-B/D) — Hub con JWKS cache
    # ──────────────────────────────────────────────────────────────────────
    if settings.hub_name != "central":
        from app.auth.jwks import JWKSCache

        if not settings.central_jwks_url:
            # Settings validator Plan 03-02 Task 1 đã enforce — defensive bao
            # thêm tránh race khi Settings cache override runtime.
            raise RuntimeError(
                f"hub_name={settings.hub_name!r} thiếu CENTRAL_JWKS_URL "
                "— Settings validator phải đã raise ở boot"
            )

        jwks_cache = JWKSCache(
            jwks_url=settings.central_jwks_url,
            refresh_interval=settings.jwks_refresh_interval,
            max_stale_seconds=settings.jwks_max_stale_seconds,
        )
        try:
            await jwks_cache.fetch_initial()  # blocking, raise on fail
        except Exception as e:
            logger.critical(
                "lifespan_jwks_cache_init_failed: hub_name=%s url=%s error=%s — boot abort",
                settings.hub_name,
                settings.central_jwks_url,
                e,
            )
            raise  # boot fail-loud D-V3-Phase3-B → uvicorn exit 1

        jwks_cache.start_refresh_task()
        app.state.jwks_cache = jwks_cache
        logger.info(
            "lifespan_jwks_cache_ready: hub_name=%s url=%s refresh_interval=%ds",
            settings.hub_name,
            settings.central_jwks_url,
            settings.jwks_refresh_interval,
        )
    else:
        # Central — KHÔNG cần JWKSCache (verify bằng local pem qua JWTManager)
        app.state.jwks_cache = None
```

Trong shutdown branch (sau yield), thêm:

```python
    # Phase 3 Plan 03-02 — graceful shutdown JWKS cache asyncio task
    jwks_cache = getattr(app.state, "jwks_cache", None)
    if jwks_cache is not None:
        await jwks_cache.stop_refresh_task()
```

**Reject patterns:**
- KHÔNG dùng `try/except` bao quanh `fetch_initial` và silent continue — Boot PHẢI fail-loud (D-V3-Phase3-B).
- KHÔNG cache PEM file disk làm fallback — D-V3-Phase3-B reject (defer Phase 7 nếu cần HA hard).
- KHÔNG thay đổi M2 lifespan order (asyncpg pool → redis → jwt → ...) — chỉ ADD branch JWKS cache.

**Edge case:** Hub con boot chạy trước central (Docker compose `depends_on: python-api-central: condition: service_started` chưa đủ — central process up nhưng JWKS endpoint chưa serve). Workaround: Plan 03-02 chỉ check service_started → JWKS endpoint sẵn sàng ngay sau Plan 03-01 mount route ở create_app() (KHÔNG cần lifespan). Acceptable.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_jwks_cache.py tests/unit/test_jwks_publish.py tests/unit/test_main_factory.py tests/unit/test_config_jwks.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def verify_token_with_key" Hub_All/api/app/auth/jwt.py` ≥ 1
    - `grep -c "self._kid\|_derive_kid" Hub_All/api/app/auth/jwt.py` ≥ 2 (init + headers)
    - `grep -c "headers={.kid.: self._kid}" Hub_All/api/app/auth/jwt.py` ≥ 2 (access + refresh)
    - `grep -c "from app.auth.jwks import JWKSCache" Hub_All/api/app/main.py` ≥ 1
    - `grep -c "jwks_cache.fetch_initial\|jwks_cache.start_refresh_task\|jwks_cache.stop_refresh_task" Hub_All/api/app/main.py` ≥ 3
    - `grep -c "app.state.jwks_cache" Hub_All/api/app/main.py` ≥ 2 (set + get)
    - `grep -c "lifespan_jwks_cache_init_failed\|lifespan_jwks_cache_ready" Hub_All/api/app/main.py` ≥ 2 (observability log)
    - `grep -c "Phase 3 Plan 03-02\|D-V3-Phase3-B\|D-V3-Phase3-D\|SSO-01" Hub_All/api/app/main.py` ≥ 2 (traceability)
    - `cd Hub_All/api && uv run ruff check app/main.py app/auth/jwt.py` exit 0
    - `cd Hub_All/api && uv run mypy app/main.py app/auth/jwt.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_cache.py tests/unit/test_jwks_publish.py tests/unit/test_main_factory.py tests/unit/test_config_jwks.py -v` PASS toàn bộ (Plan 02-01 + Plan 03-01 + Task 1+2 KHÔNG regress)
  </acceptance_criteria>
  <done>
Lifespan hub con branch: JWKSCache instantiate + fetch_initial blocking (fail-loud) + start_refresh_task spawn + app.state.jwks_cache wire + shutdown cleanup. JWTManager add verify_token_with_key wrapper + headers kid ở issue_token_pair. Lint + mypy clean. Phase 1+2 + Plan 03-01 + Task 1+2 regression PASS toàn bộ.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: `get_current_user` dependency branch verify JWT qua JWKSCache cho hub con</name>
  <files>api/app/auth/dependencies.py, api/tests/unit/test_get_current_user_jwks_branch.py</files>
  <read_first>
    - Hub_All/api/app/auth/dependencies.py (get_current_user M2 carry forward — Plan 03-02 thêm branch)
    - Hub_All/api/app/auth/jwt.py (verify_token_with_key Task 3 ship)
    - Hub_All/api/app/auth/jwks.py (JWKSCache + 2 exception Task 2 ship)
  </read_first>
  <action>
**Step 1: Refactor `get_current_user`** (`api/app/auth/dependencies.py`) — thêm branch hub con verify qua JWKSCache:

Tìm function `get_current_user` (line ~85-145). Sửa block decode JWT:

```python
async def get_current_user(
    request: Request,                              # MỚI: cần app.state truy cập
    token: str | None = Depends(oauth2_scheme),    # noqa: B008
    jwt_mgr: JWTManager = Depends(get_jwt_manager),  # noqa: B008
    redis: Redis | None = Depends(get_redis),      # noqa: B008
    db: AsyncSession = Depends(get_session),       # noqa: B008
) -> User:
    """Verify Bearer access token → User entity.

    Phase 3 Plan 03-02 SSO-01 (D-V3-Phase3-D) — Hub con verify qua JWKSCache:
    - central: dùng JWTManager.verify_token (local public.pem M2 carry forward)
    - hub con: dùng JWKSCache.get_public_key(kid) → verify_token_with_key

    Trình tự reject (KHÔNG đổi từ M2):
    1. Token rỗng → 401 MISSING_AUTHORIZATION
    2. Sai signature/expired/issuer/alg → 401 INVALID_TOKEN
    3. Sai type (refresh thay vì access) → 401 INVALID_TOKEN_TYPE
    4. JTI trong Redis blacklist → 401 TOKEN_REVOKED
    5. User không tồn tại / disabled → 401 USER_DISABLED

    Phase 3 thêm:
    6. (Hub con) JWT thiếu kid header → 401 INVALID_TOKEN ("Token thiếu kid header")
    7. (Hub con) kid mismatch JWKSCache → 401 INVALID_TOKEN ("kid không khớp JWKS")
    8. (Hub con) JWKSCache stale > 24h → 503 JWKS_STALE
    """
    if not token:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={
                "code": "MISSING_AUTHORIZATION",
                "message": "Yêu cầu Bearer token",
            },
            headers={"WWW-Authenticate": "Bearer"},
        )

    # Phase 3 Plan 03-02 — branch verify path theo hub_name
    from app.config import get_settings

    settings = get_settings()
    if settings.hub_name == "central":
        # Central: local public.pem M2 path
        try:
            claims = jwt_mgr.verify_token(token, expected_type="access")
        except JWTError as e:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={"code": "INVALID_TOKEN", "message": str(e)},
                headers={"WWW-Authenticate": "Bearer"},
            ) from e
    else:
        # Hub con: verify qua JWKSCache (D-V3-Phase3-D)
        import jwt as pyjwt

        from app.auth.jwks import JWKSCache, JWKSKidNotFoundError, JWKSStaleError

        jwks_cache: JWKSCache | None = getattr(
            request.app.state, "jwks_cache", None
        )
        if jwks_cache is None:
            raise HTTPException(
                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
                detail={
                    "code": "JWKS_CACHE_UNAVAILABLE",
                    "message": "JWKSCache chưa init — hub con boot fail?",
                },
            )

        # Extract kid từ JWT header
        try:
            unverified_header = pyjwt.get_unverified_header(token)
        except pyjwt.InvalidTokenError as e:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={"code": "INVALID_TOKEN", "message": f"JWT header invalid: {e}"},
                headers={"WWW-Authenticate": "Bearer"},
            ) from e

        kid = unverified_header.get("kid")
        if kid is None:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={
                    "code": "INVALID_TOKEN",
                    "message": "Token thiếu kid header — JWT phát hành trước Phase 3 SSO, vui lòng đăng nhập lại",
                },
                headers={"WWW-Authenticate": "Bearer"},
            )

        try:
            public_key = jwks_cache.get_public_key(kid)
        except JWKSStaleError as e:
            raise HTTPException(
                status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
                detail={"code": "JWKS_STALE", "message": str(e)},
            ) from e
        except JWKSKidNotFoundError as e:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={
                    "code": "INVALID_TOKEN",
                    "message": f"kid không khớp JWKS cache: {e}",
                },
                headers={"WWW-Authenticate": "Bearer"},
            ) from e

        try:
            claims = jwt_mgr.verify_token_with_key(
                token, public_key, expected_type="access"
            )
        except JWTError as e:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={"code": "INVALID_TOKEN", "message": str(e)},
                headers={"WWW-Authenticate": "Bearer"},
            ) from e

    # Blacklist check (Plan 03-03 sẽ rename `blacklist:` → `auth:blacklist:`)
    if redis is not None:
        is_blacklisted = await redis.exists(f"blacklist:{claims.jti}")
        if is_blacklisted:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={
                    "code": "TOKEN_REVOKED",
                    "message": "Token đã bị thu hồi",
                },
                headers={"WWW-Authenticate": "Bearer"},
            )

    # Load user (M2 carry forward)
    stmt = select(User).where(
        User.id == UUID(claims.sub), User.is_active.is_(True)
    )
    user = (await db.execute(stmt)).scalar_one_or_none()
    if user is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={
                "code": "USER_DISABLED",
                "message": "Tài khoản đã bị vô hiệu hoá",
            },
        )
    return user
```

**Step 2: Tạo `api/tests/unit/test_get_current_user_jwks_branch.py`** — verify branch logic:

```python
"""Phase 3 SSO-01 — Unit test get_current_user branch JWKS (Plan 03-02 Task 4).

Verify:
- Central hub_name=central → dùng JWTManager.verify_token (M2 path)
- Hub con + JWKSCache None → 503 JWKS_CACHE_UNAVAILABLE
- Hub con + JWT thiếu kid → 401 INVALID_TOKEN
- Hub con + kid mismatch → 401 INVALID_TOKEN
- Hub con + JWKSStaleError → 503 JWKS_STALE
- Hub con + verify OK → user trả về
"""
from __future__ import annotations

from pathlib import Path
from unittest.mock import AsyncMock, MagicMock, patch
from uuid import uuid4

import pytest
from fastapi import HTTPException


@pytest.fixture
def public_key_path() -> Path:
    p = Path("keys/public.pem")
    if not p.exists():
        pytest.skip("api/keys/public.pem missing — run 'make keys'")
    return p


def _make_request_with_jwks_cache(jwks_cache_or_none):
    """Build mock Request với app.state.jwks_cache."""
    request = MagicMock()
    request.app.state.jwks_cache = jwks_cache_or_none
    return request


@pytest.mark.asyncio
async def test_central_uses_jwt_manager_local_pem(monkeypatch) -> None:
    """Central → dùng jwt_mgr.verify_token (M2 path) — KHÔNG đụng JWKSCache."""
    monkeypatch.setenv("HUB_NAME", "central")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_central",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    from app.config import get_settings

    get_settings.cache_clear()

    from app.auth.dependencies import get_current_user

    # Mock jwt_mgr.verify_token return JWTClaims
    claims_mock = MagicMock()
    claims_mock.sub = str(uuid4())
    claims_mock.jti = "test_jti"
    jwt_mgr = MagicMock()
    jwt_mgr.verify_token = MagicMock(return_value=claims_mock)
    jwt_mgr.verify_token_with_key = MagicMock()

    # Mock DB return user
    user_mock = MagicMock()
    user_mock.id = claims_mock.sub
    db_mock = MagicMock()
    db_mock.execute = AsyncMock()
    db_mock.execute.return_value.scalar_one_or_none = MagicMock(
        return_value=user_mock
    )

    request_mock = _make_request_with_jwks_cache(None)

    user = await get_current_user(
        request=request_mock,
        token="dummy.token.here",
        jwt_mgr=jwt_mgr,
        redis=None,
        db=db_mock,
    )
    assert user is user_mock
    jwt_mgr.verify_token.assert_called_once()
    jwt_mgr.verify_token_with_key.assert_not_called()


@pytest.mark.asyncio
async def test_hub_con_jwks_cache_none_returns_503(monkeypatch) -> None:
    """Hub con + app.state.jwks_cache None → HTTPException 503 JWKS_CACHE_UNAVAILABLE."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import get_settings

    get_settings.cache_clear()

    from app.auth.dependencies import get_current_user

    request_mock = _make_request_with_jwks_cache(None)
    with pytest.raises(HTTPException) as exc_info:
        await get_current_user(
            request=request_mock,
            token="dummy.token.here",
            jwt_mgr=MagicMock(),
            redis=None,
            db=MagicMock(),
        )
    assert exc_info.value.status_code == 503
    assert exc_info.value.detail["code"] == "JWKS_CACHE_UNAVAILABLE"


@pytest.mark.asyncio
async def test_hub_con_jwt_missing_kid_returns_401(
    monkeypatch, public_key_path: Path
) -> None:
    """JWT không có kid header → 401 INVALID_TOKEN."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import get_settings

    get_settings.cache_clear()

    # Sign JWT KHÔNG kid header (M2 baseline cũ)
    import jwt as pyjwt

    private_pem = Path("keys/private.pem").read_bytes()
    token_no_kid = pyjwt.encode(
        {"sub": "test", "iss": "medinet-wiki"},
        private_pem,
        algorithm="RS256",
        # KHÔNG headers={"kid": ...}
    )

    from app.auth.dependencies import get_current_user
    from app.auth.jwks import JWKSCache

    jwks_cache_mock = MagicMock(spec=JWKSCache)
    request_mock = _make_request_with_jwks_cache(jwks_cache_mock)

    with pytest.raises(HTTPException) as exc_info:
        await get_current_user(
            request=request_mock,
            token=token_no_kid,
            jwt_mgr=MagicMock(),
            redis=None,
            db=MagicMock(),
        )
    assert exc_info.value.status_code == 401
    assert exc_info.value.detail["code"] == "INVALID_TOKEN"
    assert "thiếu kid" in exc_info.value.detail["message"]


@pytest.mark.asyncio
async def test_hub_con_jwks_stale_returns_503(monkeypatch) -> None:
    """JWKSCache stale > 24h → 503 JWKS_STALE."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import get_settings

    get_settings.cache_clear()

    import jwt as pyjwt

    private_pem = Path("keys/private.pem").read_bytes()
    token = pyjwt.encode(
        {"sub": "test", "iss": "medinet-wiki"},
        private_pem,
        algorithm="RS256",
        headers={"kid": "some_kid"},
    )

    from app.auth.dependencies import get_current_user
    from app.auth.jwks import JWKSCache, JWKSStaleError

    jwks_cache_mock = MagicMock(spec=JWKSCache)
    jwks_cache_mock.get_public_key = MagicMock(
        side_effect=JWKSStaleError("cache quá hạn 86400s")
    )
    request_mock = _make_request_with_jwks_cache(jwks_cache_mock)

    with pytest.raises(HTTPException) as exc_info:
        await get_current_user(
            request=request_mock,
            token=token,
            jwt_mgr=MagicMock(),
            redis=None,
            db=MagicMock(),
        )
    assert exc_info.value.status_code == 503
    assert exc_info.value.detail["code"] == "JWKS_STALE"
```

**KHÔNG đụng:**
- `require_role` / `get_current_user_with_hubs` / `get_api_key_or_jwt` (M2 carry forward — vẫn dùng get_current_user upstream).
- `get_jwt_manager` / `get_redis` / `get_session` / `get_auth_service` dependencies.
- service.py (Plan 03-03 sẽ refactor blacklist key).
- router.py (Plan 03-04 sẽ refactor hub con 307 redirect).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_get_current_user_jwks_branch.py tests/unit/test_jwks_cache.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "settings.hub_name == .central." Hub_All/api/app/auth/dependencies.py` ≥ 1 (central branch)
    - `grep -c "jwks_cache" Hub_All/api/app/auth/dependencies.py` ≥ 3 (None check + get_public_key + branch)
    - `grep -c "verify_token_with_key" Hub_All/api/app/auth/dependencies.py` ≥ 1
    - `grep -c "JWKSCache\|JWKSStaleError\|JWKSKidNotFoundError" Hub_All/api/app/auth/dependencies.py` ≥ 3
    - `grep -c "JWKS_CACHE_UNAVAILABLE\|JWKS_STALE" Hub_All/api/app/auth/dependencies.py` ≥ 2 (error code envelope)
    - `grep -c "Phase 3 Plan 03-02\|D-V3-Phase3-D\|SSO-01" Hub_All/api/app/auth/dependencies.py` ≥ 2 (traceability)
    - File `Hub_All/api/tests/unit/test_get_current_user_jwks_branch.py` exists
    - `grep -c "def test_central_uses_jwt_manager_local_pem\|def test_hub_con_jwks_cache_none_returns_503\|def test_hub_con_jwt_missing_kid_returns_401\|def test_hub_con_jwks_stale_returns_503" Hub_All/api/tests/unit/test_get_current_user_jwks_branch.py` ≥ 4
    - `cd Hub_All/api && uv run ruff check app/auth/dependencies.py tests/unit/test_get_current_user_jwks_branch.py` exit 0
    - `cd Hub_All/api && uv run mypy app/auth/dependencies.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_get_current_user_jwks_branch.py -v` PASS ≥ 4 test
  </acceptance_criteria>
  <done>
get_current_user branch hub con: extract kid từ JWT header → JWKSCache.get_public_key(kid) → verify_token_with_key. Central giữ nguyên M2 path. 3 mapping HTTPException: JWKS_CACHE_UNAVAILABLE 503, JWKS_STALE 503, INVALID_TOKEN 401 (thiếu kid / mismatch kid). 4 unit test PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Integration test rotate keypair scenario — JWKSCache detect kid mismatch + force re-fetch</name>
  <files>api/tests/integration/test_jwks_cache_lifecycle.py</files>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py (fixture pattern Phase 2 Plan 02-03 — testclient + ASGI lifespan + hub_app_factory)
    - Hub_All/api/app/auth/jwks.py (Task 2 + 3 ship — JWKSCache + lifecycle)
    - Hub_All/api/app/main.py (Task 3 ship — lifespan branch hub con)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (`<specifics>` section — pattern integration test rotate keypair)
  </read_first>
  <action>
**Step 1: Tạo `api/tests/integration/test_jwks_cache_lifecycle.py`** với 3 integration test:

```python
"""Phase 3 SSO-01 — Integration test JWKSCache lifecycle (Plan 03-02 Task 5).

Pragmatic scope:
- Spawn central app via TestClient → fetch /.well-known/jwks.json → original_jwks.
- Mock httpx.get cho JWKSCache (KHÔNG spawn 2 process subprocess) → assert
  cache fetch_initial OK + get_public_key OK.
- Simulate key rotation: swap mock httpx response → cache.refresh() → kid mới
  → get_public_key(old_kid) raise JWKSKidNotFoundError.

Full multi-process E2E rotate keypair runtime defer Plan 03-05 closeout smoke
(chạy thật 2 service compose central + yte với docker compose up).
"""
from __future__ import annotations

import asyncio
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock, patch

import pytest
from fastapi.testclient import TestClient


@pytest.fixture
def public_key_path() -> Path:
    p = Path("keys/public.pem")
    if not p.exists():
        pytest.skip("api/keys/public.pem missing — run 'make keys'")
    return p


@pytest.mark.critical
@pytest.mark.integration
def test_central_jwks_endpoint_serves_real_keypair(
    monkeypatch, public_key_path: Path
) -> None:
    """End-to-end: central app boot → GET /.well-known/jwks.json → JWK Set chứa
    đúng public key từ api/keys/public.pem (qua publish_jwks)."""
    from app.auth.jwks import load_public_key_as_jwk

    expected_jwk = load_public_key_as_jwk(public_key_path)
    expected_kid = expected_jwk["kid"]

    # Boot central app
    monkeypatch.setenv("HUB_NAME", "central")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_central",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
    from app.config import get_settings

    get_settings.cache_clear()
    from app.main import create_app

    app = create_app()
    with TestClient(app) as client:
        resp = client.get("/.well-known/jwks.json")
        assert resp.status_code == 200
        body = resp.json()
        assert body["keys"][0]["kid"] == expected_kid
        assert body["keys"][0]["n"] == expected_jwk["n"]


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_jwks_cache_rotate_keypair_detects_new_kid(
    monkeypatch, public_key_path: Path, tmp_path: Path
) -> None:
    """Simulate central rotate keypair → hub con refresh detect kid mới.

    Setup: JWKSCache fetch_initial với JWK A (kid_a) → get_public_key(kid_a) OK.
    Then: refresh() với JWK B (kid_b mới) → get_public_key(kid_a) raise
    JWKSKidNotFoundError + get_public_key(kid_b) OK.
    """
    from cryptography.hazmat.primitives.asymmetric import rsa
    from cryptography.hazmat.primitives import serialization

    from app.auth.jwks import (
        JWKSCache,
        JWKSKidNotFoundError,
        load_public_key_as_jwk,
        publish_jwks,
    )

    # Original JWK A (M2 public.pem)
    jwks_a = publish_jwks(public_key_path)
    kid_a = jwks_a["keys"][0]["kid"]

    # Sinh keypair mới ở tmp (B) — simulate central `make keys` rotate
    new_priv = rsa.generate_private_key(public_exponent=65537, key_size=2048)
    new_pub = new_priv.public_key()
    new_pub_pem = new_pub.public_bytes(
        encoding=serialization.Encoding.PEM,
        format=serialization.PublicFormat.SubjectPublicKeyInfo,
    )
    new_pub_path = tmp_path / "public_b.pem"
    new_pub_path.write_bytes(new_pub_pem)
    jwks_b = publish_jwks(new_pub_path)
    kid_b = jwks_b["keys"][0]["kid"]
    assert kid_a != kid_b  # rotation phải đổi kid

    # JWKSCache fetch_initial với A
    cache = JWKSCache(jwks_url="http://central/.well-known/jwks.json")

    def _mock_httpx_for_response(jwks_response):
        mock_resp = MagicMock()
        mock_resp.status_code = 200
        mock_resp.raise_for_status = MagicMock()
        mock_resp.json = MagicMock(return_value=jwks_response)
        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(return_value=mock_resp)
        return mock_client

    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _mock_httpx_for_response(jwks_a)
        await cache.fetch_initial()
    # get_public_key(kid_a) OK
    pub_a = cache.get_public_key(kid_a)
    assert pub_a is not None

    # Refresh với B (rotate)
    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _mock_httpx_for_response(jwks_b)
        await cache.refresh()

    # Sau refresh: kid_a KHÔNG còn, kid_b mới
    with pytest.raises(JWKSKidNotFoundError):
        cache.get_public_key(kid_a)
    pub_b = cache.get_public_key(kid_b)
    assert pub_b is not None


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_jwks_cache_background_refresh_task_lifecycle(
    public_key_path: Path,
) -> None:
    """start_refresh_task spawn asyncio task → stop_refresh_task cancel sạch."""
    from app.auth.jwks import JWKSCache, publish_jwks

    jwks = publish_jwks(public_key_path)
    cache = JWKSCache(
        jwks_url="http://central/.well-known/jwks.json",
        refresh_interval=60,
    )

    def _mock_httpx(jwks_response):
        mock_resp = MagicMock()
        mock_resp.status_code = 200
        mock_resp.raise_for_status = MagicMock()
        mock_resp.json = MagicMock(return_value=jwks_response)
        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(return_value=mock_resp)
        return mock_client

    with patch("app.auth.jwks.httpx.AsyncClient") as mock_client_cls:
        mock_client_cls.return_value = _mock_httpx(jwks)
        await cache.fetch_initial()
        cache.start_refresh_task()
        assert cache._refresh_task is not None
        assert not cache._refresh_task.done()

    await cache.stop_refresh_task()
    assert cache._refresh_task is None
```

**Step 2: Update `api/tests/integration/conftest.py` nếu cần** thêm fixture `central_app` để Task 5 reuse — nhưng test 1 (`test_central_jwks_endpoint_serves_real_keypair`) đã inline boot, KHÔNG cần fixture chung. Skip Step 2 nếu conftest pattern Phase 2 đủ.

**Edge case:** `pytest-asyncio` mode — verify `pyproject.toml` đã có `asyncio_mode = "auto"` (M2 baseline) hoặc các test dùng explicit `@pytest.mark.asyncio` (đã apply).

**KHÔNG dùng `pytest-httpx` library** — mock manual đã đủ + tránh thêm dep test.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_jwks_cache_lifecycle.py -v -m "critical and integration"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_jwks_cache_lifecycle.py` exists
    - `grep -c "def test_central_jwks_endpoint_serves_real_keypair\|def test_jwks_cache_rotate_keypair_detects_new_kid\|def test_jwks_cache_background_refresh_task_lifecycle" Hub_All/api/tests/integration/test_jwks_cache_lifecycle.py` ≥ 3
    - `grep -c "@pytest.mark.critical\|@pytest.mark.integration" Hub_All/api/tests/integration/test_jwks_cache_lifecycle.py` ≥ 6 (3 test × 2 marker)
    - `grep -c "JWKSKidNotFoundError\|JWKSCache\|publish_jwks" Hub_All/api/tests/integration/test_jwks_cache_lifecycle.py` ≥ 3
    - `cd Hub_All/api && uv run ruff check tests/integration/test_jwks_cache_lifecycle.py` exit 0
    - `cd Hub_All/api && uv run mypy tests/integration/test_jwks_cache_lifecycle.py` exit 0 (cho phép `# type: ignore[arg-type]` cho mock)
    - `cd Hub_All/api && uv run pytest tests/integration/test_jwks_cache_lifecycle.py -v -m "critical and integration"` PASS ≥ 3 test
    - Phase 2 integration test KHÔNG regress: `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS 10/10
  </acceptance_criteria>
  <done>
3 integration test PASS verify (1) central JWKS endpoint serve real keypair shape match `load_public_key_as_jwk(api/keys/public.pem)`, (2) cache rotate keypair → old kid raise JWKSKidNotFoundError + new kid OK, (3) start/stop_refresh_task lifecycle clean. Phase 2 integration test 10/10 KHÔNG regress.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub con boot → Central JWKS endpoint | Blocking httpx fetch timeout 5s qua docker network. Boot fail-loud nếu central down KHI boot (D-V3-Phase3-B) — hub con KHÔNG start được serve traffic |
| Hub con runtime → Cached JWKS | get_current_user verify mọi request — cached value zero latency (in-process LRU). Cache > 24h → 503 JWKS_STALE (R-V3-5 fail-loud delayed) |
| Asyncio refresh task → Central JWKS endpoint | Background task chạy mỗi 1h, fail-quiet (log warning + giữ cached). Restart hub con = re-fetch (KHÔNG persist disk) |
| JWT header kid → JWKSCache.get_public_key | kid extract qua `pyjwt.get_unverified_header(token)` — KHÔNG verify trước khi lookup (pre-verify-by-design — kid chỉ dùng để select key, signature verify ở step sau) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| **T-03-02-01** | Tampering | Env thiếu CENTRAL_JWKS_URL → hub con boot OK nhưng mọi JWT verify trả 500 silent | high | mitigate | Settings `@model_validator` `_enforce_central_jwks_url_for_hub` (Task 1) raise ValidationError ở boot — fail-fast trước khi tới lifespan. Test parametrize 4 hub × required check (test_hub_con_requires_central_jwks_url). |
| **T-03-02-02** | DoS | Central JWKS endpoint xuống lâu → hub con lifespan startup hang (chưa có timeout default httpx) | medium | mitigate | JWKSCache constructor enforce `timeout=5.0` mặc định ở `httpx.AsyncClient(timeout=self._timeout)`. Test `test_jwks_cache_fetch_initial_timeout_raises` verify exception raise → process exit 1 (lifespan startup). |
| **T-03-02-03** | Tampering | Man-in-the-middle thay JWKS response intra-network → hub con cache fake public key → JWT attacker-signed pass verify | medium | accept | Intra-network HTTP acceptable v3.0-a (xem Plan 03-01 threat T-03-01-02 inherit). Production deploy Caddy TLS termination + medinet_net network isolation. R-V3-5 24h hard limit minimize window fake key (cache stale → 503). Defense-in-depth Phase 5 PROXY-01 nếu cross-host deploy. |
| **T-03-02-04** | Information Disclosure | Logger debug JWKSCache leak public key bytes hoặc kid identity | low | accept | KHÔNG log raw key bytes — chỉ kid string (public identifier RFC 7517 BY DESIGN). Log structlog JSON internal. |
| **T-03-02-05** | Elevation of Privilege | Hub con cache key cũ stale + central rotated key → JWT signed by attacker với key compromise cũ vẫn pass verify | medium | mitigate | Background refresh 1h + 24h hard limit. Key rotation flow: central revoke key cũ (key compromise scenario) → admin invalidate Redis blacklist (Plan 03-03 wire). KHÔNG có rotation strategy automatic — v3.0-a manual operator restart trigger. Multi-key overlap window (publish key cũ + mới trong 24h) defer Phase 7. |
| **T-03-02-06** | Tampering | JWT thiếu kid header (M2 baseline cũ) → bypass JWKS verify | mitigate | mitigate | Task 4 reject 401 INVALID_TOKEN "Token thiếu kid header — JWT phát hành trước Phase 3 SSO, vui lòng đăng nhập lại". Test `test_hub_con_jwt_missing_kid_returns_401` verify. Acceptable downtime ~15-30s sau Plan 03-02 deploy — communicate operator (README.md note ở Plan 03-05 closeout). |
| **T-03-02-07** | Repudiation | Hub con verify JWT pass nhưng cached kid là từ ATTACKER compromise central window → audit log KHÔNG trace được key version | low | accept | Audit log M2 chỉ ghi user_id + action + timestamp (NOT JWT kid). v3.0-a accept. Phase 7 multi-key + kid log defer khi audit trail SSO formal. |
| **T-03-02-08** | DoS | Asyncio refresh task panic + chết → hub con sau 24h KHÔNG fetch lại → mọi request 503 | high | mitigate | `_refresh_loop` wrap try/except global (Task 2) — `asyncio.CancelledError` re-raise + Exception khác log + continue loop. Hub con cuối cùng vẫn fail-loud delayed qua 24h hard limit. Operator alert qua Prometheus metric `jwks_stale_total` (defer Plan 03-05 observability). |
</threat_model>

<verification>
**Plan 03-02 verification chain:**

1. **File integrity (grep):**
   - `api/app/auth/jwks.py` — extend (Task 2): JWKSCache class + 2 exception + 2 helper + __all__ update
   - `api/app/main.py` — extend (Task 3): lifespan branch hub con + shutdown cleanup
   - `api/app/auth/jwt.py` — extend (Task 3): verify_token_with_key + _kid derive + headers kid
   - `api/app/config.py` — extend (Task 1): 2 field + 1 model_validator
   - `api/app/auth/dependencies.py` — refactor (Task 4): get_current_user branch
   - 3 test file mới: test_config_jwks.py (5 test) + test_jwks_cache.py (9 test) + test_get_current_user_jwks_branch.py (4 test)
   - 1 integration test mới: test_jwks_cache_lifecycle.py (3 test)

2. **Lint + type clean:**
   - `cd Hub_All/api && uv run ruff check app/auth/jwks.py app/auth/jwt.py app/auth/dependencies.py app/main.py app/config.py tests/unit/test_jwks_cache.py tests/unit/test_config_jwks.py tests/unit/test_get_current_user_jwks_branch.py tests/integration/test_jwks_cache_lifecycle.py` exit 0
   - `cd Hub_All/api && uv run mypy app/auth/jwks.py app/auth/jwt.py app/auth/dependencies.py app/main.py app/config.py` exit 0

3. **Unit test PASS:**
   - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py tests/unit/test_jwks_cache.py tests/unit/test_config_jwks.py tests/unit/test_get_current_user_jwks_branch.py -v` PASS ≥ 27 test (9+9+5+4)

4. **Integration test PASS:**
   - `cd Hub_All/api && uv run pytest tests/integration/test_jwks_cache_lifecycle.py -v -m "critical and integration"` PASS ≥ 3 test

5. **Phase 2 + Plan 03-01 regression:**
   - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py tests/unit/test_jwks_publish.py -v` PASS 58/58 (49 Phase 2 + 9 Plan 03-01)
   - `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS 10/10 (Plan 02-03)

6. **Compose render:**
   - `cd Hub_All && docker compose config --quiet` exit 0
</verification>

<success_criteria>
- JWKSCache class trong `api/app/auth/jwks.py` với 4 method core (fetch_initial blocking, refresh fail-quiet, get_public_key + JWKSStaleError, start/stop_refresh_task)
- Settings 2 field config-driven + 1 model_validator enforce hub con required (T-03-02-01 mitigation)
- Lifespan hub con: blocking startup fetch + spawn asyncio refresh task + graceful shutdown
- JWTManager.verify_token_with_key wrapper cho hub con verify path
- JWT header kid auto-add ở issue_token_pair (M2 backward incompat acceptable — Plan 03-05 README note)
- get_current_user dependency branch: central dùng local pem (M2 path), hub con dùng JWKSCache.get_public_key(kid)
- 21+ unit test PASS (9 cache + 5 config + 4 dependency + 3 publish bonus) + 3 integration test PASS
- Phase 2 + Plan 03-01 regression KHÔNG break (58/58 unit + 10/10 integration)
- D-V3-Phase3-B/D honored (boot fail-loud + runtime fail-quiet + 24h hard limit + in-process LRU KHÔNG Redis)
- Lint + mypy clean
</success_criteria>

<output>
Tạo `.planning/phases/03-auth-sso-hub-ids-jwt/03-02-SUMMARY.md`:
- Plan 03-02 interface ship: JWKSCache class + verify_token_with_key wrapper + lifespan branch hub con + dependency branch + Settings 2 field + validator
- File modified: jwks.py (extend), main.py (lifespan branch), jwt.py (kid + wrapper), config.py (2 field + validator), dependencies.py (branch), 4 test file
- D-V3-Phase3-B/D consumed; D-V3-Phase3-A reuse Plan 03-01
- M2 backward incompat note: JWT cũ KHÔNG có kid → re-login required (Plan 03-05 README banner)
- Phase 2 + Plan 03-01 regression PASS
- Wave 3 unblock: Plan 03-03 (Redis blacklist key rename + hub_ids/iss/aud claim refactor — coupled write touch service.py + jwt.py + claims model)
- Next: Plan 03-03 (Wave 3) Redis blacklist key schema `auth:blacklist:{jti}` TTL=JWT.exp-now + hub_ids REQUIRED + iss/aud claim refactor + E4 reinforced (SSO-02/03/04)
</output>
</content>
