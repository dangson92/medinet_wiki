---
phase: 03-auth-sso-hub-ids-jwt
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - api/app/auth/jwt.py
  - api/app/auth/service.py
  - api/app/auth/dependencies.py
  - api/tests/unit/test_jwt_iss_aud_hub_ids.py
  - api/tests/unit/test_redis_blacklist_key_schema.py
  - api/tests/integration/test_sso_blacklist_cross_process.py
autonomous: true
requirements:
  - SSO-02
  - SSO-03
  - SSO-04
tags:
  - jwt-iss-aud
  - hub-ids-required
  - redis-blacklist-key-rename
  - cross-process-blacklist
  - e4-reinforced
  - d-v3-phase3-e
  - d-v3-phase3-h
  - sso-02
  - sso-03
  - sso-04
must_haves:
  truths:
    - "JWT_ISSUER đổi từ `\"medinet-wiki\"` → giữ NGUYÊN `\"medinet-wiki\"` (chuỗi cố định cho v3.0-a) — KHÔNG đụng `https://central/` (defer Phase 7 multi-aud chia MCP) — RE-CONFIRM D-V3-Phase3-E"
    - "JWT thêm claim `aud: list[str] = [\"medinet-wiki\"]` (REQUIRED — D-V3-Phase3-E)"
    - "JWT claim `hub_ids: list[str]` REQUIRED (KHÔNG default empty list — M2 cũ JWT thiếu claim → 401 reject sau Plan 03-03 deploy)"
    - "JWTManager.verify_token + verify_token_with_key cả 2 path strict check audience=[\"medinet-wiki\"] (pyjwt audience parameter)"
    - "Redis blacklist key đổi từ `blacklist:{jti}` → `auth:blacklist:{jti}` (D-V3-Phase3-H — namespace clarity tránh collision Plan 03-03 + future feature key)"
    - "Redis blacklist TTL = JWT.exp - now() (auto-cleanup; KHÔNG cần cron) — D-V3-Phase3-H"
    - "Cross-process blacklist: central logout → revoke JWT → hub con `get_current_user` redis.exists() trả True trong < 1s (cùng Redis instance M2 baseline)"
    - "E4 reinforced: stale JWT chứa `hub_ids=[\"duoc\"]` post tới `medinet_hub_yte` `/api/profile` → 403 (KHÔNG 404 / data leak) — repo layer + dependency layer cùng enforce"
    - "Hub con `get_current_user_with_hubs`: claims.hub_ids load từ JWT (KHÔNG re-load DB hub con vì user.hubs table KHÔNG có ở hub con DB) — central path vẫn DB query"
    - "`HUB_NAME in claims.hub_ids` check ở hub con dependency (`get_current_user_for_hub_access`) — reject 403 FORBIDDEN nếu mismatch (SSO-04)"
  artifacts:
    - path: "api/app/auth/jwt.py"
      provides: "JWTClaims model bổ sung `aud: list[str]` + verify_token/verify_token_with_key audience strict check + hub_ids REQUIRED không default"
      contains: "aud: list\\[str\\]|audience=\\[JWT_AUDIENCE\\]"
    - path: "api/app/auth/service.py"
      provides: "AuthService.login/refresh build claims với aud + hub_ids REQUIRED + Redis key rename `auth:blacklist:`"
      contains: "auth:blacklist:|aud=\\[JWT_AUDIENCE\\]"
    - path: "api/app/auth/dependencies.py"
      provides: "Redis key rename + get_current_user_for_hub_access dependency mới (SSO-04 E4 reinforced)"
      contains: "auth:blacklist:|def get_current_user_for_hub_access|HUB_NAME in claims.hub_ids"
    - path: "api/tests/unit/test_jwt_iss_aud_hub_ids.py"
      provides: "Unit test JWT claim shape mới: aud REQUIRED + hub_ids REQUIRED + M2 baseline cũ reject"
      contains: "def test_jwt_with_aud_passes|def test_jwt_missing_aud_rejects|def test_jwt_missing_hub_ids_rejects"
    - path: "api/tests/unit/test_redis_blacklist_key_schema.py"
      provides: "Unit test key rename `auth:blacklist:` + TTL = exp - now"
      contains: "auth:blacklist:|def test_blacklist_key_prefix_auth|def test_blacklist_ttl_matches_exp"
    - path: "api/tests/integration/test_sso_blacklist_cross_process.py"
      provides: "Integration test E4 reinforced: stale JWT cross-hub reject 403 + blacklist propagate < 1s"
      contains: "def test_stale_jwt_cross_hub_returns_403|def test_blacklist_propagates_cross_process"
  key_links:
    - from: "api/app/auth/jwt.py::JWTManager.issue_token_pair"
      to: "JWT claim `aud: list[str]` + `hub_ids: list[str]` REQUIRED"
      via: "base dict + access/refresh_claims include aud + hub_ids"
      pattern: "\"aud\":|JWT_AUDIENCE"
    - from: "api/app/auth/service.py::AuthService.logout"
      to: "Redis SET `auth:blacklist:{access_jti}` TTL = access_exp - now"
      via: "await self.redis.set(f\"auth:blacklist:{access_jti}\", \"1\", ex=access_ttl)"
      pattern: "auth:blacklist:"
    - from: "api/app/auth/dependencies.py::get_current_user (Plan 03-02 wire)"
      to: "Redis EXISTS `auth:blacklist:{jti}`"
      via: "is_blacklisted = await redis.exists(f\"auth:blacklist:{claims.jti}\")"
      pattern: "auth:blacklist:"
    - from: "api/app/auth/dependencies.py::get_current_user_for_hub_access (MỚI)"
      to: "Hub con isolation check (SSO-04 E4)"
      via: "if settings.hub_name != \"central\" and settings.hub_name not in claims.hub_ids: raise 403"
      pattern: "HUB_NAME not in claims.hub_ids"
---

<objective>
Coordinated single plan refactor 3 việc tightly-coupled chạm cùng `jwt.py` + `service.py` + `dependencies.py` (KHÔNG thể parallel — tránh merge conflict):

1. **JWT claim refactor (D-V3-Phase3-E):** Thêm `aud: list[str] = ["medinet-wiki"]` REQUIRED (KHÔNG default empty list). `JWT_ISSUER="medinet-wiki"` GIỮ NGUYÊN — M2 baseline KHÔNG đụng (Plan 03-03 RE-CONFIRM CONTEXT.md D-V3-Phase3-E gốc đề xuất `https://central/` nhưng analysis: chuỗi cố định `medinet-wiki` đủ; URL-based iss defer Phase 7 khi MCP service formal split audience). `hub_ids: list[str]` đổi từ default `[]` → REQUIRED (M2 cũ JWT thiếu → 401 reject post-deploy). PyJWT decode strict check `audience=[JWT_AUDIENCE]` cả 2 path (verify_token central + verify_token_with_key hub con).

2. **Redis blacklist key schema (D-V3-Phase3-H):** Đổi key prefix từ `blacklist:{jti}` → `auth:blacklist:{jti}`. TTL = `JWT.exp - now()` (auto-expire — KHÔNG cần cron). Touch 5 vị trí: 4 trong `service.py` (login_lock_refresh + refresh blacklist + logout access + logout refresh) + 1 trong `dependencies.py` (get_current_user check). Plan 03-02 carry forward redis check pattern.

3. **E4 reinforced (SSO-04):** Thêm dependency mới `get_current_user_for_hub_access` — hub con check `HUB_NAME in claims.hub_ids` → reject 403 FORBIDDEN nếu mismatch (KHÔNG 404 hay data leak). DB-level isolation (Phase 1 `_enforce_hub_dsn_match`) + repository layer `WHERE hub_id = settings.hub_name` (M2 carry forward) đã có; Plan 03-03 add JWT claim layer thứ 3 (defense-in-depth).

Plan này Wave 3 — depend Plan 03-02 (JWKSCache + dependency branch). Plan 03-04 (auth router refactor 307 redirect) tiếp theo Wave 4 — depend service.py shape mới Plan 03-03 ship.

Purpose: SSO-02 + SSO-03 + SSO-04 (3 REQ-ID đóng cùng plan vì tightly-coupled file modify).

Output: 3 file modify (jwt.py, service.py, dependencies.py) + 3 test file mới (2 unit + 1 integration). M2 backward incompat: JWT cũ KHÔNG có `aud` claim → 401 reject (user re-login required; communicate Plan 03-05 README banner — kết hợp với kid missing reject Plan 03-02).

**Locked decisions consume (CONTEXT.md 03-CONTEXT.md):**
- **D-V3-Phase3-E:** iss/aud claim — iss giữ `"medinet-wiki"` (RE-CONFIRM — analysis cho thấy chuỗi cố định đủ cho v3.0-a; URL-based iss defer Phase 7 MCP split aud); aud `["medinet-wiki"]` REQUIRED; hub_ids REQUIRED (M2 cũ reject).
- **D-V3-Phase3-H:** Redis key `auth:blacklist:{jti}` + TTL=JWT.exp-now + marker value "1" + Redis instance M2 baseline chung (KHÔNG split per-hub DB).
- **SSO-04 spec (REQUIREMENTS.md line 50):** "Test integration: stale JWT chứa `hub_ids=[\"duoc\"]` post tới `medinet_hub_yte` API → 403 (không phải 404 hay data leak)" — Plan 03-03 wire dependency + integration test.
- **D-V3-Phase3-C carry forward:** Hub con KHÔNG sinh refresh — service.py login + refresh chỉ ở central. Plan 03-03 KHÔNG đụng login/refresh router (defer Plan 03-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-02-PLAN.md
@Hub_All/api/app/auth/jwt.py
@Hub_All/api/app/auth/service.py
@Hub_All/api/app/auth/dependencies.py

<interfaces>
<!-- Plan 03-02 đã ship JWKSCache + verify_token_with_key + dependency branch + kid header. Plan 03-03 thêm aud/hub_ids REQUIRED + Redis key rename + E4 dependency mới. -->

Từ Plan 03-02 `api/app/auth/jwt.py`:

```python
JWT_ISSUER = "medinet-wiki"      # Plan 03-03 GIỮ NGUYÊN (re-confirm D-V3-Phase3-E)
JWT_ALGORITHM = "RS256"
JWT_ALGORITHMS_ALLOWED = ["RS256"]

class JWTClaims(BaseModel):
    sub: str
    email: str
    name: str | None = None
    role: Literal["admin", "editor", "viewer"]
    hub_ids: list[str] = []           # PLAN 03-03 ĐỔI: REQUIRED (xoá default [])
    iss: str
    iat: int
    exp: int
    jti: str
    token_type: Literal["access", "refresh"]
    # PLAN 03-03 THÊM:
    aud: list[str]                    # REQUIRED


class JWTManager:
    def issue_token_pair(self, *, user_id, email, full_name, role, hub_ids) -> TokenPair: ...
    # PLAN 03-03 ĐỔI:
    # base dict thêm "aud": [JWT_AUDIENCE]
    # KHÔNG default hub_ids = [] (caller phải truyền)

    def verify_token(self, token, *, expected_type) -> JWTClaims:
        # PLAN 03-03 ĐỔI: pyjwt.decode(..., audience=[JWT_AUDIENCE]) strict check

    def verify_token_with_key(self, token, public_key, *, expected_type) -> JWTClaims:
        # PLAN 03-03 ĐỔI: tương tự audience check
```

Từ Plan 03-02 `api/app/auth/dependencies.py::get_current_user`:

```python
# Blacklist check (Plan 03-03 sẽ rename `blacklist:` → `auth:blacklist:`)
if redis is not None:
    is_blacklisted = await redis.exists(f"blacklist:{claims.jti}")  # ← PLAN 03-03 ĐỔI
```

Từ M2 `api/app/auth/service.py` (Plan 03-03 sửa 4 vị trí):

```python
# Line ~165 — refresh blacklist check
already_revoked = await self.redis.exists(f"blacklist:{claims.jti}")  # ← ĐỔI

# Line ~201 — refresh blacklist write
await self.redis.set(f"blacklist:{claims.jti}", "1", ex=...)  # ← ĐỔI

# Line ~247 — logout access blacklist
await self.redis.set(f"blacklist:{access_jti}", "1", ex=access_ttl)  # ← ĐỔI

# Line ~255 — logout refresh blacklist
await self.redis.set(f"blacklist:{claims.jti}", "1", ex=...)  # ← ĐỔI
```

Module constant mới:

```python
# api/app/auth/jwt.py — PLAN 03-03 THÊM:
JWT_AUDIENCE = "medinet-wiki"  # Single audience cho v3.0-a; split per-service defer Phase 7

# api/app/auth/service.py + dependencies.py — PLAN 03-03 THÊM:
REDIS_BLACKLIST_PREFIX = "auth:blacklist:"  # D-V3-Phase3-H
```

Dependency mới `get_current_user_for_hub_access`:

```python
# api/app/auth/dependencies.py — PLAN 03-03 THÊM:
async def get_current_user_for_hub_access(
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_session),
) -> User:
    """SSO-04 E4 reinforced — hub con check HUB_NAME in JWT.hub_ids.

    Central: bypass check (cross-hub by design).
    Hub con: claims.hub_ids load từ user_hubs DB (hub con KHÔNG có user_hubs
        full set vì DB hub-scoped) → fallback parse từ JWT claim trực tiếp.

    Note hub con không có user_hubs cross-hub data → claims.hub_ids đến từ JWT
    (issued bởi central với hub_ids đầy đủ user assignment). Hub con chỉ verify
    HUB_NAME in claims.hub_ids (claim đã verify signature qua JWKSCache Plan 03-02).
    """
    settings = get_settings()
    if settings.hub_name == "central":
        return user  # central cross-hub by design
    # Hub con: load claims fresh từ Authorization header (re-decode để parse hub_ids)
    # NHƯNG: get_current_user đã decode + verify. Workaround: dùng request.state cache
    # hoặc thêm claim object vào request.state ở Plan 03-02 (re-touch).
    # Pragmatic: tách dependency mới load + verify lần 2 — overhead ~1ms acceptable.
    # ALT: refactor get_current_user trả tuple (User, JWTClaims) — touch Plan 03-02 path.
    # Plan 03-03 chọn ALT: thay đổi get_current_user signature trả attached claims qua request.state.
    claims = getattr(request.state, "jwt_claims", None)
    if claims is None:
        # Defensive — get_current_user phải set claims; raise nếu missing (bug)
        raise HTTPException(status_code=500, detail={"code": "AUTH_STATE_MISSING", "message": "..."})
    if settings.hub_name not in claims.hub_ids:
        raise HTTPException(
            status_code=403,
            detail={
                "code": "CROSS_HUB_ACCESS_DENIED",
                "message": f"Token KHÔNG có quyền truy cập hub {settings.hub_name!r}",
            },
        )
    return user
```

**Wire `request.state.jwt_claims`:** Plan 03-03 update get_current_user (cả 2 branch central + hub con) set `request.state.jwt_claims = claims` sau khi verify thành công. get_current_user_for_hub_access dùng request.state để tránh re-decode (Plan 03-03 acceptable performance tradeoff).
</interfaces>

<notes>
**Quan trọng — RE-CONFIRM iss claim:**

CONTEXT.md D-V3-Phase3-E gốc đề xuất `iss: "https://central/"`. Plan 03-03 RE-CONFIRM giữ NGUYÊN `JWT_ISSUER = "medinet-wiki"` (M2 carry forward — KHÔNG đổi). Lý do:

1. **URL-based iss thêm complexity:** Cần Settings field `central_url` + dynamic build URL. Plan 03-01 đã thêm `central_jwks_url` (URL-based) — đủ cho discovery. iss claim chỉ identifier, KHÔNG cần URL format.
2. **M2 baseline `medinet-wiki` đã ship:** Đổi iss = M2 JWT cũ reject (đã có kid missing reject Plan 03-02 — double reject cùng deploy). Giữ iss giảm 1 backward incompat.
3. **aud claim `["medinet-wiki"]` đủ phân biệt:** Phase 7 MCP service split khi cần `["medinet-wiki-mcp"]` audience riêng — defer hợp lý.

**Tracking deviation:** Plan 03-03 RE-CONFIRM D-V3-Phase3-E chuẩn xác hơn — `iss="medinet-wiki"` cố định + `aud=["medinet-wiki"]` REQUIRED + `hub_ids` REQUIRED. Update CONTEXT.md note ở Plan 03-05 closeout. Decision NÀY trong scope Claude's Discretion (CONTEXT.md ghi rõ Plan 03-03 can refine).

**M2 backward incompat double:**

Plan 03-02 + 03-03 deploy cùng = JWT cũ M2 sẽ fail 2 reason:
1. Plan 03-02: thiếu `kid` header → 401 INVALID_TOKEN (hub con only).
2. Plan 03-03: thiếu `aud` claim → 401 INVALID_TOKEN (central + hub con).

User re-login = nhận JWT mới có cả kid + aud + hub_ids. Acceptable downtime ~15-30s — Plan 03-05 README sẽ document.

**Wire `request.state.jwt_claims`:**

get_current_user Plan 03-02 KHÔNG attach claims sang request.state. Plan 03-03 cần (cho get_current_user_for_hub_access reuse). Pattern: thêm 1 dòng `request.state.jwt_claims = claims` SAU khi verify pass (cả 2 branch). Plan 03-03 sửa get_current_user thêm `request: Request` parameter (đã có ở Plan 03-02 Task 4) + set state.

**KHÔNG đụng `audit_logs` shape:**

JWT claim mới (aud) KHÔNG yêu cầu lưu vào audit_logs. M2 audit logger ghi user_id + action + timestamp — không liên quan. Plan 03-03 KHÔNG đụng Plan 05-05 audit pipeline.

**KHÔNG migration DB:**

`refresh_tokens` table (M2 0001_initial_schema.py) KHÔNG đổi shape. Plan 03-03 chỉ refactor logic Python — KHÔNG cần Alembic migration mới.

**Defensive scaffolding `get_current_user_for_hub_access` chỉ dùng cho endpoint hub-scoped sensitive:**

Plan 03-03 add dependency mới NHƯNG KHÔNG wire vào endpoint hiện hữu. Reason: M2 router (`/api/profile`, `/api/documents`, `/api/search`, `/api/ask`, `/api/usage`) đã dùng `get_current_user_with_hubs` + `hub_filter_clause` (repo layer enforce hub_id WHERE clause). Defense-in-depth Plan 03-03 cung cấp dependency mới — Plan 03-04 hoặc Plan 03-05 sẽ wire cho 1-2 endpoint sensitive (vd `/api/documents` upload). Integration test Plan 03-03 dùng endpoint dummy hoặc decorate test endpoint với `get_current_user_for_hub_access` để verify behavior.
</notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JWT claim refactor `aud` + `hub_ids` REQUIRED + JWT_AUDIENCE constant + verify_token audience strict check</name>
  <files>api/app/auth/jwt.py, api/tests/unit/test_jwt_iss_aud_hub_ids.py</files>
  <read_first>
    - Hub_All/api/app/auth/jwt.py (M2 + Plan 03-02 — Plan 03-03 sửa JWTClaims model + issue_token_pair + verify_token + verify_token_with_key)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (D-V3-Phase3-E spec)
    - Hub_All/api/tests/unit/test_jwks_cache.py (Plan 03-02 pattern reference cho JWT sign/verify roundtrip)
  </read_first>
  <behavior>
    - Test 1 (`test_jwt_with_aud_and_hub_ids_passes`): issue_token_pair(hub_ids=["yte"]) → decode JWT bằng verify_token → claims.aud == ["medinet-wiki"] + claims.hub_ids == ["yte"].
    - Test 2 (`test_jwt_missing_aud_rejects`): Sign JWT raw KHÔNG aud claim (simulate M2 cũ) → verify_token raise JWTError "audience" message.
    - Test 3 (`test_jwt_wrong_aud_rejects`): Sign JWT raw với aud=["wrong-service"] → verify_token raise JWTError.
    - Test 4 (`test_jwt_missing_hub_ids_rejects`): Sign JWT raw KHÔNG hub_ids → JWTClaims.model_validate raise → verify_token raise JWTError ("Claims không hợp lệ").
    - Test 5 (`test_jwt_empty_hub_ids_allowed`): hub_ids=[] OK (user chưa assign hub — central admin trước onboard).
    - Test 6 (`test_verify_token_with_key_same_strict_audience`): verify_token_with_key (hub con path) cũng strict audience.
    - Test 7 (`test_jwt_audience_constant_value`): `from app.auth.jwt import JWT_AUDIENCE; assert JWT_AUDIENCE == "medinet-wiki"`.
    - Test 8 (`test_jwt_issuer_unchanged_medinet_wiki`): JWT_ISSUER giữ NGUYÊN — KHÔNG đổi URL.
  </behavior>
  <action>
**Step 1: Thêm `JWT_AUDIENCE` constant + refactor `JWTClaims`** trong `api/app/auth/jwt.py`:

Thêm constant SAU `JWT_ISSUER`:

```python
JWT_ISSUER = "medinet-wiki"
JWT_AUDIENCE = "medinet-wiki"     # Phase 3 Plan 03-03 (D-V3-Phase3-E) — single audience v3.0-a; split per-service defer Phase 7 MCP

JWT_ALGORITHM = "RS256"
JWT_ALGORITHMS_ALLOWED = [JWT_ALGORITHM]
```

Refactor `JWTClaims` model — thêm `aud` REQUIRED + đổi `hub_ids` từ default `[]` → REQUIRED:

```python
class JWTClaims(BaseModel):
    """Pydantic v2 model cho claims đã decode (validate sớm sau verify)."""

    sub: str
    email: str
    name: str | None = None
    role: Literal["admin", "editor", "viewer"]
    # Phase 3 Plan 03-03 (D-V3-Phase3-E) — hub_ids REQUIRED post-deploy.
    # M2 cũ JWT thiếu claim → 401 INVALID_TOKEN qua pydantic ValidationError.
    # User re-login để nhận JWT mới (acceptable downtime ~15-30s — README note Plan 03-05).
    hub_ids: list[str]
    iss: str
    aud: list[str]                # Phase 3 Plan 03-03 REQUIRED (RFC 7519 audience claim)
    iat: int
    exp: int
    jti: str
    token_type: Literal["access", "refresh"]
```

**Step 2: Refactor `issue_token_pair`** — base dict thêm `aud`:

```python
    def issue_token_pair(
        self,
        *,
        user_id: str,
        email: str,
        full_name: str | None,
        role: str,
        hub_ids: list[str],
    ) -> TokenPair:
        """Sinh cặp access + refresh JWT RS256 với aud + hub_ids REQUIRED."""
        now = datetime.now(tz=UTC)
        access_jti = str(uuid.uuid4())
        refresh_jti = str(uuid.uuid4())
        access_exp = now + self._access_ttl
        refresh_exp = now + self._refresh_ttl

        base: dict[str, Any] = {
            "sub": user_id,
            "email": email,
            "name": full_name,
            "role": role,
            "hub_ids": hub_ids,
            "iss": JWT_ISSUER,
            "aud": [JWT_AUDIENCE],   # Phase 3 Plan 03-03 D-V3-Phase3-E
            "iat": int(now.timestamp()),
        }
        # ... rest unchanged (access_token + refresh_token encode với headers={"kid": self._kid})
```

**Step 3: Refactor `verify_token`** — pyjwt audience strict check:

```python
    def verify_token(
        self,
        token: str,
        *,
        expected_type: Literal["access", "refresh"],
    ) -> JWTClaims:
        """Decode + validate. Phase 3 Plan 03-03 add audience strict check."""
        try:
            decoded = pyjwt.decode(
                token,
                self._public_pem,
                algorithms=JWT_ALGORITHMS_ALLOWED,
                issuer=JWT_ISSUER,
                audience=JWT_AUDIENCE,   # Phase 3 Plan 03-03 D-V3-Phase3-E
            )
        except pyjwt.ExpiredSignatureError as e:
            raise JWTError("Token đã hết hạn") from e
        except pyjwt.InvalidIssuerError as e:
            raise JWTError("Token không hợp lệ (issuer sai)") from e
        except pyjwt.InvalidAudienceError as e:
            raise JWTError(
                f"Token không hợp lệ (audience sai — yêu cầu {JWT_AUDIENCE!r})"
            ) from e
        except pyjwt.MissingRequiredClaimError as e:
            raise JWTError(
                f"Token thiếu claim bắt buộc: {e}. "
                "JWT phát hành trước Phase 3 SSO — vui lòng đăng nhập lại."
            ) from e
        except pyjwt.InvalidTokenError as e:
            raise JWTError(f"Token không hợp lệ: {e}") from e

        try:
            claims = JWTClaims.model_validate(decoded)
        except Exception as e:  # pydantic.ValidationError (hub_ids missing → ValidationError)
            raise JWTError(
                f"Claims không hợp lệ: {e}. "
                "JWT có thể phát hành trước Phase 3 SSO — vui lòng đăng nhập lại."
            ) from e

        if claims.token_type != expected_type:
            raise JWTError(
                f"Loại token sai: yêu cầu {expected_type!r}, "
                f"nhận {claims.token_type!r}"
            )
        return claims
```

**Step 4: Refactor `verify_token_with_key`** — same audience check:

```python
    def verify_token_with_key(
        self,
        token: str,
        public_key: bytes | "RSAPublicKey",
        *,
        expected_type: Literal["access", "refresh"],
    ) -> JWTClaims:
        """Phase 3 Plan 03-02 + 03-03 — hub con verify path với audience strict."""
        try:
            decoded = pyjwt.decode(
                token,
                public_key,
                algorithms=JWT_ALGORITHMS_ALLOWED,
                issuer=JWT_ISSUER,
                audience=JWT_AUDIENCE,   # Phase 3 Plan 03-03 strict
            )
        except pyjwt.ExpiredSignatureError as e:
            raise JWTError("Token đã hết hạn") from e
        except pyjwt.InvalidIssuerError as e:
            raise JWTError("Token không hợp lệ (issuer sai)") from e
        except pyjwt.InvalidAudienceError as e:
            raise JWTError(
                f"Token không hợp lệ (audience sai — yêu cầu {JWT_AUDIENCE!r})"
            ) from e
        except pyjwt.MissingRequiredClaimError as e:
            raise JWTError(
                f"Token thiếu claim bắt buộc: {e}. "
                "JWT phát hành trước Phase 3 SSO — vui lòng đăng nhập lại."
            ) from e
        except pyjwt.InvalidTokenError as e:
            raise JWTError(f"Token không hợp lệ: {e}") from e

        try:
            claims = JWTClaims.model_validate(decoded)
        except Exception as e:
            raise JWTError(
                f"Claims không hợp lệ: {e}. "
                "JWT có thể phát hành trước Phase 3 SSO — vui lòng đăng nhập lại."
            ) from e

        if claims.token_type != expected_type:
            raise JWTError(
                f"Loại token sai: yêu cầu {expected_type!r}, "
                f"nhận {claims.token_type!r}"
            )
        return claims
```

**Step 5: Update `api/app/auth/__init__.py`** thêm export `JWT_AUDIENCE`:

```python
# Tìm dòng "JWT_ISSUER" trong __all__ — thêm "JWT_AUDIENCE" cùng vị trí
# Tìm import "JWT_ISSUER" — thêm "JWT_AUDIENCE" cùng vị trí
```

**Step 6: Tạo `api/tests/unit/test_jwt_iss_aud_hub_ids.py`** với 8 test:

```python
"""Phase 3 SSO-02/03 — Unit test JWT iss/aud/hub_ids claim (Plan 03-03 Task 1).

Verify:
- JWT_AUDIENCE constant = "medinet-wiki"
- JWT_ISSUER unchanged = "medinet-wiki" (RE-CONFIRM D-V3-Phase3-E)
- issue_token_pair build claim với aud + hub_ids REQUIRED
- verify_token + verify_token_with_key strict check audience
- M2 cũ JWT thiếu aud/hub_ids → JWTError reject (backward incompat acceptable)
- hub_ids empty list OK (user chưa assign hub)
"""
from __future__ import annotations

from datetime import UTC, datetime, timedelta
from pathlib import Path

import jwt as pyjwt
import pytest


@pytest.fixture
def private_pem() -> bytes:
    p = Path("keys/private.pem")
    if not p.exists():
        pytest.skip("api/keys/private.pem missing")
    return p.read_bytes()


@pytest.fixture
def public_pem() -> bytes:
    p = Path("keys/public.pem")
    if not p.exists():
        pytest.skip("api/keys/public.pem missing")
    return p.read_bytes()


@pytest.fixture
def jwt_manager(monkeypatch):
    """Boot JWTManager với env JWT_PRIVATE_KEY_PATH + JWT_PUBLIC_KEY_PATH."""
    monkeypatch.setenv("HUB_NAME", "central")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_central",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("JWT_PRIVATE_KEY_PATH", "keys/private.pem")
    monkeypatch.setenv("JWT_PUBLIC_KEY_PATH", "keys/public.pem")
    from app.auth.jwt import JWTManager
    from app.config import get_settings

    get_settings.cache_clear()
    return JWTManager(get_settings())


def test_jwt_audience_constant_value() -> None:
    """JWT_AUDIENCE == 'medinet-wiki' (single v3.0-a)."""
    from app.auth.jwt import JWT_AUDIENCE

    assert JWT_AUDIENCE == "medinet-wiki"


def test_jwt_issuer_unchanged_medinet_wiki() -> None:
    """JWT_ISSUER GIỮ NGUYÊN — RE-CONFIRM D-V3-Phase3-E (KHÔNG URL-based)."""
    from app.auth.jwt import JWT_ISSUER

    assert JWT_ISSUER == "medinet-wiki"


def test_jwt_with_aud_and_hub_ids_passes(jwt_manager) -> None:
    """issue_token_pair build JWT với aud + hub_ids → verify_token PASS."""
    pair = jwt_manager.issue_token_pair(
        user_id="abc",
        email="user@medinet.vn",
        full_name="Test",
        role="viewer",
        hub_ids=["yte"],
    )
    claims = jwt_manager.verify_token(pair.access_token, expected_type="access")
    assert claims.aud == ["medinet-wiki"]
    assert claims.hub_ids == ["yte"]
    assert claims.iss == "medinet-wiki"


def test_jwt_missing_aud_rejects(jwt_manager, private_pem) -> None:
    """Sign JWT raw KHÔNG aud → verify_token raise JWTError."""
    from app.auth.jwt import JWT_ISSUER, JWT_ALGORITHM, JWTError

    now = datetime.now(tz=UTC)
    token_no_aud = pyjwt.encode(
        {
            "sub": "abc",
            "email": "u@m.vn",
            "name": "T",
            "role": "viewer",
            "hub_ids": ["yte"],
            "iss": JWT_ISSUER,
            # KHÔNG "aud"
            "iat": int(now.timestamp()),
            "exp": int((now + timedelta(minutes=15)).timestamp()),
            "jti": "test_jti",
            "token_type": "access",
        },
        private_pem,
        algorithm=JWT_ALGORITHM,
        headers={"kid": "test_kid"},
    )
    with pytest.raises(JWTError, match="audience"):
        jwt_manager.verify_token(token_no_aud, expected_type="access")


def test_jwt_wrong_aud_rejects(jwt_manager, private_pem) -> None:
    """JWT với aud=['wrong'] → verify_token raise JWTError."""
    from app.auth.jwt import JWT_ISSUER, JWT_ALGORITHM, JWTError

    now = datetime.now(tz=UTC)
    token_wrong_aud = pyjwt.encode(
        {
            "sub": "abc",
            "email": "u@m.vn",
            "name": "T",
            "role": "viewer",
            "hub_ids": ["yte"],
            "iss": JWT_ISSUER,
            "aud": ["wrong-service"],
            "iat": int(now.timestamp()),
            "exp": int((now + timedelta(minutes=15)).timestamp()),
            "jti": "test_jti",
            "token_type": "access",
        },
        private_pem,
        algorithm=JWT_ALGORITHM,
        headers={"kid": "test_kid"},
    )
    with pytest.raises(JWTError, match="audience"):
        jwt_manager.verify_token(token_wrong_aud, expected_type="access")


def test_jwt_missing_hub_ids_rejects(jwt_manager, private_pem) -> None:
    """JWT KHÔNG hub_ids claim → JWTClaims.model_validate raise → JWTError."""
    from app.auth.jwt import JWT_AUDIENCE, JWT_ISSUER, JWT_ALGORITHM, JWTError

    now = datetime.now(tz=UTC)
    token_no_hub_ids = pyjwt.encode(
        {
            "sub": "abc",
            "email": "u@m.vn",
            "name": "T",
            "role": "viewer",
            # KHÔNG "hub_ids"
            "iss": JWT_ISSUER,
            "aud": [JWT_AUDIENCE],
            "iat": int(now.timestamp()),
            "exp": int((now + timedelta(minutes=15)).timestamp()),
            "jti": "test_jti",
            "token_type": "access",
        },
        private_pem,
        algorithm=JWT_ALGORITHM,
        headers={"kid": "test_kid"},
    )
    with pytest.raises(JWTError, match="Claims không hợp lệ|hub_ids"):
        jwt_manager.verify_token(token_no_hub_ids, expected_type="access")


def test_jwt_empty_hub_ids_allowed(jwt_manager) -> None:
    """hub_ids=[] OK (user chưa assign hub — admin onboard scenario)."""
    pair = jwt_manager.issue_token_pair(
        user_id="abc",
        email="admin@medinet.vn",
        full_name="Admin",
        role="admin",
        hub_ids=[],   # empty allowed
    )
    claims = jwt_manager.verify_token(pair.access_token, expected_type="access")
    assert claims.hub_ids == []


def test_verify_token_with_key_same_strict_audience(jwt_manager, public_pem) -> None:
    """verify_token_with_key (hub con) cũng strict audience."""
    from cryptography.hazmat.primitives import serialization

    pub_key = serialization.load_pem_public_key(public_pem)
    pair = jwt_manager.issue_token_pair(
        user_id="abc",
        email="u@m.vn",
        full_name="T",
        role="viewer",
        hub_ids=["yte"],
    )
    claims = jwt_manager.verify_token_with_key(
        pair.access_token, pub_key, expected_type="access"
    )
    assert claims.aud == ["medinet-wiki"]
    assert claims.hub_ids == ["yte"]
```

**KHÔNG đụng:**
- TokenPair dataclass (M2 carry forward).
- JWT_ALGORITHM / JWT_ALGORITHMS_ALLOWED (M2 baseline).
- `_load_key` + `_kid` (Plan 03-02 + 03-01 carry forward).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_jwt_iss_aud_hub_ids.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "JWT_AUDIENCE\s*=\s*.medinet-wiki." Hub_All/api/app/auth/jwt.py` ≥ 1
    - `grep -c "audience=JWT_AUDIENCE" Hub_All/api/app/auth/jwt.py` ≥ 2 (verify_token + verify_token_with_key)
    - `grep -c "aud: list\[str\]" Hub_All/api/app/auth/jwt.py` ≥ 1 (JWTClaims field REQUIRED)
    - `grep -c "hub_ids: list\[str\]\b" Hub_All/api/app/auth/jwt.py` ≥ 1 (REQUIRED — không default = [])
    - `grep -c "InvalidAudienceError\|MissingRequiredClaimError" Hub_All/api/app/auth/jwt.py` ≥ 4 (2 path × 2 exception)
    - `grep -c "D-V3-Phase3-E\|SSO-02\|SSO-03" Hub_All/api/app/auth/jwt.py` ≥ 2 (traceability)
    - `grep -c "JWT_AUDIENCE" Hub_All/api/app/auth/__init__.py` ≥ 1 (re-export)
    - File `Hub_All/api/tests/unit/test_jwt_iss_aud_hub_ids.py` exists với 8 test
    - `cd Hub_All/api && uv run ruff check app/auth/jwt.py app/auth/__init__.py tests/unit/test_jwt_iss_aud_hub_ids.py` exit 0
    - `cd Hub_All/api && uv run mypy app/auth/jwt.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_jwt_iss_aud_hub_ids.py -v` PASS ≥ 8 test
  </acceptance_criteria>
  <done>
JWTClaims model: aud REQUIRED + hub_ids REQUIRED. issue_token_pair build aud=[JWT_AUDIENCE]. verify_token + verify_token_with_key strict audience check. JWT_AUDIENCE constant + re-export. 8 unit test PASS verify backward incompat reject (aud/hub_ids missing) + happy path.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redis blacklist key rename `auth:blacklist:` + REDIS_BLACKLIST_PREFIX constant + TTL = exp - now (5 vị trí touch)</name>
  <files>api/app/auth/service.py, api/app/auth/dependencies.py, api/tests/unit/test_redis_blacklist_key_schema.py</files>
  <read_first>
    - Hub_All/api/app/auth/service.py (M2 — 4 vị trí dùng `f"blacklist:{...}"`)
    - Hub_All/api/app/auth/dependencies.py (M2 + Plan 03-02 — 1 vị trí dùng `f"blacklist:{...}"`)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (D-V3-Phase3-H key schema spec)
  </read_first>
  <behavior>
    - Test 1 (`test_blacklist_key_prefix_auth`): mock Redis client → AuthService.logout(access_jti="abc", access_exp=..., refresh_token=None) → assert `redis.set` called with key starting `"auth:blacklist:abc"`.
    - Test 2 (`test_blacklist_ttl_matches_exp_minus_now`): logout với access_exp = now + 100s → assert TTL kwarg `ex` ≈ 100s (allow ±5s tolerance).
    - Test 3 (`test_blacklist_ttl_minimum_1_second`): logout với access_exp ≤ now (token đã expired) → TTL = 1s (max(1, ...) guard M2 carry forward).
    - Test 4 (`test_blacklist_value_marker_only`): assert `redis.set(..., "1", ex=...)` — value = "1" (marker, KHÔNG metadata).
    - Test 5 (`test_blacklist_key_prefix_constant_exposed`): `from app.auth.dependencies import REDIS_BLACKLIST_PREFIX; assert REDIS_BLACKLIST_PREFIX == "auth:blacklist:"`.
    - Test 6 (`test_get_current_user_uses_renamed_key`): mock redis.exists(`auth:blacklist:jti_xyz`) → trả 1 → get_current_user raise 401 TOKEN_REVOKED. Verify key prefix dùng `auth:blacklist:` (KHÔNG `blacklist:`).
  </behavior>
  <action>
**Step 1: Thêm `REDIS_BLACKLIST_PREFIX` constant** — đặt ở `api/app/auth/dependencies.py` module-level (vì dependencies.py là entrypoint dùng):

```python
# Thêm sau import block, trước function definitions
# Phase 3 Plan 03-03 (D-V3-Phase3-H) — Redis blacklist key schema.
# Đổi từ M2 `blacklist:{jti}` → `auth:blacklist:{jti}` để namespace clarity
# (tránh collision với future blacklist feature, vd `apikey:blacklist:`).
# TTL = JWT.exp - now() (auto-expire khi token expired anyway — KHÔNG cron).
# Value "1" marker; metadata audit qua audit_logs table M2 carry forward.
REDIS_BLACKLIST_PREFIX = "auth:blacklist:"
```

Re-export ở `api/app/auth/__init__.py`:

```python
from app.auth.dependencies import REDIS_BLACKLIST_PREFIX
```

**Step 2: Refactor `api/app/auth/service.py`** — đổi 4 vị trí key:

```python
# Import constant đầu file
from app.auth.dependencies import REDIS_BLACKLIST_PREFIX
```

(Hoặc inline f-string mới — vì service.py + dependencies.py cùng concern, có thể đặt constant ở `service.py` thay vì dependencies.py để tránh circular import. Plan 03-03 chọn đặt ở `api/app/auth/_blacklist.py` mini-module mới — tách concern + tránh circular.)

**ALT Step 1 (preferred):** Tạo `api/app/auth/_blacklist.py` mini-module:

```python
"""Phase 3 Plan 03-03 (D-V3-Phase3-H) — Redis blacklist key schema.

Module nhỏ chứa constant + helper để tránh circular import giữa
service.py + dependencies.py (cả 2 cần access prefix).
"""
from __future__ import annotations

REDIS_BLACKLIST_PREFIX = "auth:blacklist:"


def make_blacklist_key(jti: str) -> str:
    """Build Redis key `auth:blacklist:{jti}` — D-V3-Phase3-H."""
    return f"{REDIS_BLACKLIST_PREFIX}{jti}"
```

Sửa `service.py`:

```python
# Top imports
from app.auth._blacklist import make_blacklist_key

# Refresh logic line ~165
already_revoked = await self.redis.exists(make_blacklist_key(claims.jti))

# Refresh blacklist write line ~201
await self.redis.set(
    make_blacklist_key(claims.jti),
    "1",
    ex=self.jwt_manager.refresh_ttl_seconds,
)

# Logout access line ~247
await self.redis.set(
    make_blacklist_key(access_jti), "1", ex=access_ttl
)

# Logout refresh line ~255
await self.redis.set(
    make_blacklist_key(claims.jti),
    "1",
    ex=self.jwt_manager.refresh_ttl_seconds,
)
```

Sửa `dependencies.py::get_current_user` line ~121 (Plan 03-02 path):

```python
from app.auth._blacklist import make_blacklist_key

# ... existing function body ...
if redis is not None:
    is_blacklisted = await redis.exists(make_blacklist_key(claims.jti))
    if is_blacklisted:
        raise HTTPException(...)
```

**Step 3: Update `api/app/auth/__init__.py`** export `make_blacklist_key + REDIS_BLACKLIST_PREFIX`:

```python
from app.auth._blacklist import REDIS_BLACKLIST_PREFIX, make_blacklist_key

__all__ = [
    # ... existing
    "REDIS_BLACKLIST_PREFIX",
    "make_blacklist_key",
]
```

**Step 4: Tạo `api/tests/unit/test_redis_blacklist_key_schema.py`** với 6 test:

```python
"""Phase 3 SSO-02 — Unit test Redis blacklist key schema (Plan 03-03 Task 2).

Verify:
- REDIS_BLACKLIST_PREFIX constant = "auth:blacklist:" (D-V3-Phase3-H)
- make_blacklist_key("jti_xyz") → "auth:blacklist:jti_xyz"
- AuthService.logout dùng renamed key
- TTL = exp - now() với min 1s guard (M2 carry forward)
- Value "1" marker only
- get_current_user dependency dùng renamed key
"""
from __future__ import annotations

from datetime import UTC, datetime
from unittest.mock import AsyncMock, MagicMock


def test_blacklist_key_prefix_constant_exposed() -> None:
    """REDIS_BLACKLIST_PREFIX = 'auth:blacklist:' (D-V3-Phase3-H)."""
    from app.auth import REDIS_BLACKLIST_PREFIX

    assert REDIS_BLACKLIST_PREFIX == "auth:blacklist:"


def test_make_blacklist_key_format() -> None:
    """make_blacklist_key('jti_xyz') → 'auth:blacklist:jti_xyz'."""
    from app.auth import make_blacklist_key

    assert make_blacklist_key("jti_xyz") == "auth:blacklist:jti_xyz"
    assert make_blacklist_key("abc-123") == "auth:blacklist:abc-123"


import pytest


@pytest.mark.asyncio
async def test_logout_blacklist_key_prefix_auth() -> None:
    """AuthService.logout call redis.set với key 'auth:blacklist:{jti}'."""
    from app.auth.service import AuthService

    redis_mock = AsyncMock()
    db_mock = AsyncMock()
    jwt_mock = MagicMock()
    jwt_mock.refresh_ttl_seconds = 604800
    service = AuthService(
        db=db_mock,
        redis=redis_mock,
        jwt_manager=jwt_mock,
        dummy_password_hash="dummy",
    )
    now_ts = int(datetime.now(tz=UTC).timestamp())
    await service.logout(
        access_jti="jti_abc",
        access_exp=now_ts + 100,
        refresh_token=None,
    )
    # Assert set called với key prefix đúng
    redis_mock.set.assert_called_once()
    call_args = redis_mock.set.call_args
    key = call_args.args[0] if call_args.args else call_args.kwargs.get("name")
    assert key == "auth:blacklist:jti_abc"
    # Value "1" marker
    value = call_args.args[1] if len(call_args.args) > 1 else call_args.kwargs.get("value")
    assert value == "1"


@pytest.mark.asyncio
async def test_logout_blacklist_ttl_matches_exp_minus_now() -> None:
    """TTL ≈ access_exp - now() (±5s tolerance)."""
    from app.auth.service import AuthService

    redis_mock = AsyncMock()
    db_mock = AsyncMock()
    jwt_mock = MagicMock()
    service = AuthService(
        db=db_mock,
        redis=redis_mock,
        jwt_manager=jwt_mock,
        dummy_password_hash="dummy",
    )
    now_ts = int(datetime.now(tz=UTC).timestamp())
    await service.logout(
        access_jti="abc",
        access_exp=now_ts + 100,
        refresh_token=None,
    )
    call_args = redis_mock.set.call_args
    ttl = call_args.kwargs.get("ex")
    assert 95 <= ttl <= 100  # tolerance ±5s for test wall-clock


@pytest.mark.asyncio
async def test_logout_blacklist_ttl_min_1_second_for_expired() -> None:
    """Token đã expired → TTL = max(1, exp - now) = 1s (M2 guard carry forward)."""
    from app.auth.service import AuthService

    redis_mock = AsyncMock()
    db_mock = AsyncMock()
    jwt_mock = MagicMock()
    service = AuthService(
        db=db_mock,
        redis=redis_mock,
        jwt_manager=jwt_mock,
        dummy_password_hash="dummy",
    )
    now_ts = int(datetime.now(tz=UTC).timestamp())
    await service.logout(
        access_jti="abc",
        access_exp=now_ts - 100,  # đã expired
        refresh_token=None,
    )
    call_args = redis_mock.set.call_args
    assert call_args.kwargs.get("ex") == 1


@pytest.mark.asyncio
async def test_get_current_user_uses_renamed_key(monkeypatch) -> None:
    """get_current_user query redis.exists(auth:blacklist:jti) → 1 → 401 TOKEN_REVOKED."""
    from fastapi import HTTPException
    from app.auth.dependencies import get_current_user

    monkeypatch.setenv("HUB_NAME", "central")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_central",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    from app.config import get_settings

    get_settings.cache_clear()

    claims_mock = MagicMock()
    claims_mock.sub = "user_abc"
    claims_mock.jti = "jti_xyz"
    jwt_mgr = MagicMock()
    jwt_mgr.verify_token = MagicMock(return_value=claims_mock)
    redis_mock = AsyncMock()
    redis_mock.exists = AsyncMock(return_value=1)  # blacklisted
    db_mock = AsyncMock()
    request_mock = MagicMock()
    request_mock.app.state.jwks_cache = None

    with pytest.raises(HTTPException) as exc_info:
        await get_current_user(
            request=request_mock,
            token="dummy.token.here",
            jwt_mgr=jwt_mgr,
            redis=redis_mock,
            db=db_mock,
        )
    assert exc_info.value.status_code == 401
    assert exc_info.value.detail["code"] == "TOKEN_REVOKED"
    # Verify key prefix renamed
    redis_mock.exists.assert_called_once_with("auth:blacklist:jti_xyz")
```

**KHÔNG đụng:**
- `lock:refresh:` key (P16 mitigation M2 — KHÔNG đổi, không related blacklist).
- AuthService.login (KHÔNG ghi blacklist — chỉ refresh + logout ghi).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_redis_blacklist_key_schema.py tests/unit/test_jwt_iss_aud_hub_ids.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/app/auth/_blacklist.py` exists (mới)
    - `grep -c "REDIS_BLACKLIST_PREFIX\s*=\s*.auth:blacklist:." Hub_All/api/app/auth/_blacklist.py` ≥ 1
    - `grep -c "def make_blacklist_key" Hub_All/api/app/auth/_blacklist.py` ≥ 1
    - `grep -c "make_blacklist_key\|REDIS_BLACKLIST_PREFIX" Hub_All/api/app/auth/__init__.py` ≥ 2
    - `grep -c "make_blacklist_key" Hub_All/api/app/auth/service.py` ≥ 4 (4 vị trí touch)
    - `grep -c "make_blacklist_key" Hub_All/api/app/auth/dependencies.py` ≥ 1 (get_current_user)
    - `grep -cE "f.blacklist:" Hub_All/api/app/auth/service.py` = 0 (KHÔNG còn old key prefix)
    - `grep -cE "f.blacklist:" Hub_All/api/app/auth/dependencies.py` = 0 (KHÔNG còn old key prefix)
    - `grep -c "D-V3-Phase3-H\|SSO-02" Hub_All/api/app/auth/_blacklist.py` ≥ 1
    - File `Hub_All/api/tests/unit/test_redis_blacklist_key_schema.py` exists với 6 test
    - `cd Hub_All/api && uv run ruff check app/auth/_blacklist.py app/auth/service.py app/auth/dependencies.py app/auth/__init__.py tests/unit/test_redis_blacklist_key_schema.py` exit 0
    - `cd Hub_All/api && uv run mypy app/auth/_blacklist.py app/auth/service.py app/auth/dependencies.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_redis_blacklist_key_schema.py -v` PASS ≥ 6 test
  </acceptance_criteria>
  <done>
Module `_blacklist.py` mới với `make_blacklist_key()` + `REDIS_BLACKLIST_PREFIX="auth:blacklist:"`. 4 vị trí service.py + 1 dependencies.py refactor dùng helper. 6 unit test PASS verify key prefix + TTL semantic + value marker.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SSO-04 E4 reinforced — dependency `get_current_user_for_hub_access` + wire request.state.jwt_claims</name>
  <files>api/app/auth/dependencies.py, api/tests/unit/test_get_current_user_jwks_branch.py</files>
  <read_first>
    - Hub_All/api/app/auth/dependencies.py (Plan 03-02 Task 4 get_current_user branch hub con + Plan 03-03 Task 2 Redis key rename)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md (SSO-04 E4 reinforced spec)
    - Hub_All/api/tests/unit/test_get_current_user_jwks_branch.py (Plan 03-02 Task 4 — Plan 03-03 append 2 test mới)
  </read_first>
  <action>
**Step 1: Update `get_current_user`** attach `request.state.jwt_claims = claims` sau khi verify pass (cả 2 branch central + hub con):

```python
async def get_current_user(
    request: Request,
    token: str | None = Depends(oauth2_scheme),
    jwt_mgr: JWTManager = Depends(get_jwt_manager),
    redis: Redis | None = Depends(get_redis),
    db: AsyncSession = Depends(get_session),
) -> User:
    # ... existing logic Plan 03-02 + 03-03 Task 2 ...

    # SAU khi claims verify pass (cả central + hub con branch):
    request.state.jwt_claims = claims  # PLAN 03-03 Task 3 — wire cho get_current_user_for_hub_access reuse

    # Blacklist check ...
    # Load user ...
    return user
```

Đặt 2 dòng `request.state.jwt_claims = claims` sau mỗi branch verify pass (central path + hub con path).

**Step 2: Thêm dependency mới `get_current_user_for_hub_access`** — SSO-04 E4 reinforced:

Append SAU `get_current_user_with_hubs` (line ~221):

```python
async def get_current_user_for_hub_access(
    request: Request,
    user: User = Depends(get_current_user),  # noqa: B008
) -> User:
    """SSO-04 E4 reinforced — hub con verify HUB_NAME in JWT.hub_ids.

    Phase 3 Plan 03-03 D-V3-Phase3-E + SSO-04. Defense-in-depth bên cạnh:
    - Layer 1: Phase 1 `_enforce_hub_dsn_match` DB-level isolation
    - Layer 2: repository `WHERE hub_id = settings.hub_name` (M2 carry forward)
    - Layer 3 (MỚI): JWT claim hub_ids enforcement ở dependency level

    Central (hub_name="central"): bypass check (cross-hub by design — cross-hub
        search + admin endpoints expose ở central).
    Hub con (yte/duoc/hcns/dynamic): claims.hub_ids load từ JWT (verified Plan
        03-02 JWKSCache signature). Check HUB_NAME in claims.hub_ids → reject
        403 CROSS_HUB_ACCESS_DENIED nếu mismatch.

    Usage:
        @router.post("/api/documents", dependencies=[Depends(get_current_user_for_hub_access)])
        async def upload(user=Depends(get_current_user_for_hub_access)): ...

    Behavior:
        - get_current_user fail (401) → exception raise trước, dep này KHÔNG run.
        - Central: return user (bypass).
        - Hub con + HUB_NAME in claims.hub_ids: return user.
        - Hub con + HUB_NAME not in claims.hub_ids: raise 403 CROSS_HUB_ACCESS_DENIED.

    Threat model (SSO-04):
        - T-03-03-01 stale JWT cross-hub access — JWT issued cho user yte +
          hub_ids=["yte"] post tới hub duoc → settings.hub_name="duoc" not in
          ["yte"] → 403. KHÔNG 404 (leak hub existence) / 500 (server error) /
          200 (data leak).
    """
    from app.config import get_settings

    settings = get_settings()
    if settings.hub_name == "central":
        return user  # cross-hub by design

    claims = getattr(request.state, "jwt_claims", None)
    if claims is None:
        # Defensive — get_current_user phải set claims; bug nếu missing
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail={
                "code": "AUTH_STATE_MISSING",
                "message": (
                    "request.state.jwt_claims không set — internal bug "
                    "(get_current_user dependency chain broken)"
                ),
            },
        )

    if settings.hub_name not in claims.hub_ids:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={
                "code": "CROSS_HUB_ACCESS_DENIED",
                "message": (
                    f"Token KHÔNG có quyền truy cập hub "
                    f"{settings.hub_name!r} (hub_ids JWT = {claims.hub_ids!r})"
                ),
            },
        )
    return user
```

**Step 3: Update `__all__` ở `dependencies.py`** export dependency mới — nếu module chưa có `__all__`, thêm:

Nếu `api/app/auth/__init__.py` đã re-export, thêm `get_current_user_for_hub_access` vào danh sách.

**Step 4: Append 2 test mới vào `api/tests/unit/test_get_current_user_jwks_branch.py`** (Plan 03-02 Task 4 file — Plan 03-03 extend):

```python
# ────────────────────────────────────────────────────────────────────
# Plan 03-03 Task 3 — get_current_user_for_hub_access SSO-04 E4 reinforced
# ────────────────────────────────────────────────────────────────────


@pytest.mark.asyncio
async def test_central_bypass_hub_access_check(monkeypatch) -> None:
    """Central → get_current_user_for_hub_access return user (bypass)."""
    monkeypatch.setenv("HUB_NAME", "central")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_central",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    from app.config import get_settings

    get_settings.cache_clear()

    from app.auth.dependencies import get_current_user_for_hub_access

    user_mock = MagicMock()
    request_mock = MagicMock()
    # KHÔNG cần state.jwt_claims cho central (bypass)
    result = await get_current_user_for_hub_access(
        request=request_mock, user=user_mock
    )
    assert result is user_mock


@pytest.mark.asyncio
async def test_hub_con_reject_cross_hub_jwt(monkeypatch) -> None:
    """Hub yte + claims.hub_ids=['duoc'] → 403 CROSS_HUB_ACCESS_DENIED."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import get_settings

    get_settings.cache_clear()

    from fastapi import HTTPException
    from app.auth.dependencies import get_current_user_for_hub_access

    user_mock = MagicMock()
    claims_mock = MagicMock()
    claims_mock.hub_ids = ["duoc"]  # stale JWT cross-hub
    request_mock = MagicMock()
    request_mock.state.jwt_claims = claims_mock

    with pytest.raises(HTTPException) as exc_info:
        await get_current_user_for_hub_access(
            request=request_mock, user=user_mock
        )
    assert exc_info.value.status_code == 403
    assert exc_info.value.detail["code"] == "CROSS_HUB_ACCESS_DENIED"


@pytest.mark.asyncio
async def test_hub_con_accept_matching_hub_jwt(monkeypatch) -> None:
    """Hub yte + claims.hub_ids=['yte', 'duoc'] → return user (yte in hub_ids)."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import get_settings

    get_settings.cache_clear()

    from app.auth.dependencies import get_current_user_for_hub_access

    user_mock = MagicMock()
    claims_mock = MagicMock()
    claims_mock.hub_ids = ["yte", "duoc"]  # match
    request_mock = MagicMock()
    request_mock.state.jwt_claims = claims_mock

    result = await get_current_user_for_hub_access(
        request=request_mock, user=user_mock
    )
    assert result is user_mock


@pytest.mark.asyncio
async def test_hub_con_state_missing_raises_500(monkeypatch) -> None:
    """Defensive — request.state.jwt_claims missing → 500 AUTH_STATE_MISSING (bug)."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv(
        "CENTRAL_JWKS_URL",
        "http://python-api-central:8080/.well-known/jwks.json",
    )
    from app.config import get_settings

    get_settings.cache_clear()

    from fastapi import HTTPException
    from app.auth.dependencies import get_current_user_for_hub_access

    user_mock = MagicMock()
    request_mock = MagicMock()
    # Spec hardening: state.jwt_claims is None
    request_mock.state.jwt_claims = None

    with pytest.raises(HTTPException) as exc_info:
        await get_current_user_for_hub_access(
            request=request_mock, user=user_mock
        )
    assert exc_info.value.status_code == 500
    assert exc_info.value.detail["code"] == "AUTH_STATE_MISSING"
```

**KHÔNG đụng:**
- `require_role` (M2 RBAC carry forward).
- `get_current_user_with_hubs` (M2 carry forward — vẫn dùng cho central admin endpoint).
- `get_api_key_or_jwt` (M2 carry forward).
- service.py login/refresh logic (Plan 03-04 sẽ refactor router redirect — service.py KHÔNG đụng).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_get_current_user_jwks_branch.py tests/unit/test_jwt_iss_aud_hub_ids.py tests/unit/test_redis_blacklist_key_schema.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def get_current_user_for_hub_access" Hub_All/api/app/auth/dependencies.py` ≥ 1
    - `grep -c "CROSS_HUB_ACCESS_DENIED\|AUTH_STATE_MISSING" Hub_All/api/app/auth/dependencies.py` ≥ 2
    - `grep -c "request.state.jwt_claims" Hub_All/api/app/auth/dependencies.py` ≥ 3 (2 set ở get_current_user branch + 1 read ở get_current_user_for_hub_access)
    - `grep -c "SSO-04\|D-V3-Phase3-E\|E4 reinforced" Hub_All/api/app/auth/dependencies.py` ≥ 2 (REQ + decision)
    - `grep -c "settings.hub_name not in claims.hub_ids\|settings.hub_name == .central." Hub_All/api/app/auth/dependencies.py` ≥ 2 (central bypass + hub con check)
    - `grep -c "def test_central_bypass_hub_access_check\|def test_hub_con_reject_cross_hub_jwt\|def test_hub_con_accept_matching_hub_jwt\|def test_hub_con_state_missing_raises_500" Hub_All/api/tests/unit/test_get_current_user_jwks_branch.py` ≥ 4 (Plan 03-03 Task 3 mới)
    - `cd Hub_All/api && uv run ruff check app/auth/dependencies.py tests/unit/test_get_current_user_jwks_branch.py` exit 0
    - `cd Hub_All/api && uv run mypy app/auth/dependencies.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_get_current_user_jwks_branch.py -v` PASS ≥ 8 test (4 Plan 03-02 + 4 Plan 03-03 Task 3)
  </acceptance_criteria>
  <done>
Dependency mới `get_current_user_for_hub_access` SSO-04 E4 reinforced: central bypass + hub con check `HUB_NAME in claims.hub_ids` → 403 CROSS_HUB_ACCESS_DENIED. `request.state.jwt_claims` wire ở get_current_user cả 2 branch. 4 unit test PASS (3 happy/reject + 1 defensive AUTH_STATE_MISSING).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Integration test SSO-02/03/04 cross-process — stale JWT cross-hub 403 + blacklist propagate < 1s</name>
  <files>api/tests/integration/test_sso_blacklist_cross_process.py</files>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py (fixture hub_app_factory Phase 2 Plan 02-03)
    - Hub_All/api/tests/integration/test_jwks_cache_lifecycle.py (Plan 03-02 pattern)
    - Hub_All/api/tests/integration/test_factor_hub_scoped.py (Phase 2 endpoint matrix integration test pattern)
  </read_first>
  <action>
**Step 1: Tạo `api/tests/integration/test_sso_blacklist_cross_process.py`** với 3 integration test:

```python
"""Phase 3 SSO-02/03/04 — Integration test cross-process blacklist + E4 reinforced
(Plan 03-03 Task 4).

Pragmatic scope (KHÔNG spawn 2 process subprocess):
- Stale JWT hub_ids=['duoc'] post tới endpoint hub_yte → 403 CROSS_HUB_ACCESS_DENIED
  (verify dependency get_current_user_for_hub_access wire vào test endpoint).
- Blacklist cross-process: 1 Redis instance + 2 app (central + yte) qua hub_app_factory.
  Central logout (set blacklist key) → hub yte get_current_user check (exists) → 401 TOKEN_REVOKED.
  Pattern: shared Redis mock object hoặc real Redis nếu fixture có (cần testcontainers).

Full multi-process E2E `docker compose up python-api-central python-api-yte` +
curl matrix defer Plan 03-05 closeout smoke checkpoint.
"""
from __future__ import annotations

from datetime import UTC, datetime, timedelta
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock

import jwt as pyjwt
import pytest
from fastapi import APIRouter, Depends, FastAPI
from fastapi.testclient import TestClient


@pytest.fixture
def public_pem() -> bytes:
    p = Path("keys/public.pem")
    if not p.exists():
        pytest.skip("api/keys/public.pem missing")
    return p.read_bytes()


@pytest.fixture
def private_pem() -> bytes:
    p = Path("keys/private.pem")
    if not p.exists():
        pytest.skip("api/keys/private.pem missing")
    return p.read_bytes()


@pytest.mark.critical
@pytest.mark.integration
def test_stale_jwt_cross_hub_returns_403_envelope(
    monkeypatch, hub_app_factory, private_pem, public_pem
) -> None:
    """SSO-04 E4 reinforced — stale JWT hub_ids=['duoc'] post tới hub yte → 403."""
    from app.auth.jwt import JWT_AUDIENCE, JWT_ISSUER, JWT_ALGORITHM
    from app.auth.dependencies import get_current_user_for_hub_access
    from app.auth.jwks import _derive_kid

    # Build stale JWT claim hub_ids=["duoc"] (user thuộc duoc — KHÔNG yte)
    kid = _derive_kid(public_pem)
    now = datetime.now(tz=UTC)
    stale_token = pyjwt.encode(
        {
            "sub": "user_stale_abc",
            "email": "user@duoc.medinet.vn",
            "name": "User Duoc",
            "role": "viewer",
            "hub_ids": ["duoc"],
            "iss": JWT_ISSUER,
            "aud": [JWT_AUDIENCE],
            "iat": int(now.timestamp()),
            "exp": int((now + timedelta(minutes=15)).timestamp()),
            "jti": "jti_stale_test",
            "token_type": "access",
        },
        private_pem,
        algorithm=JWT_ALGORITHM,
        headers={"kid": kid},
    )

    # Boot hub yte app + add test endpoint dùng get_current_user_for_hub_access
    yte_app = hub_app_factory("yte")
    test_router = APIRouter()

    @test_router.get("/api/test/hub-access")
    async def test_endpoint(
        user=Depends(get_current_user_for_hub_access),  # noqa: B008
    ):
        return {"user_id": str(user.id)}

    yte_app.include_router(test_router)

    with TestClient(yte_app) as client:
        resp = client.get(
            "/api/test/hub-access",
            headers={"Authorization": f"Bearer {stale_token}"},
        )
        assert resp.status_code == 403, (
            f"Stale JWT hub_ids=['duoc'] post tới hub yte → expect 403, got {resp.status_code}"
        )
        body = resp.json()
        assert body["success"] is False
        assert body["error"]["code"] == "CROSS_HUB_ACCESS_DENIED"
        # Message phải reveal hub mismatch (helpful — KHÔNG leak data)
        assert "yte" in body["error"]["message"]


@pytest.mark.critical
@pytest.mark.integration
def test_matching_jwt_hub_accepts(
    monkeypatch, hub_app_factory, private_pem, public_pem
) -> None:
    """SSO-03 — JWT hub_ids chứa hub_name → endpoint accept."""
    from app.auth.jwt import JWT_AUDIENCE, JWT_ISSUER, JWT_ALGORITHM
    from app.auth.dependencies import get_current_user_for_hub_access
    from app.auth.jwks import _derive_kid

    kid = _derive_kid(public_pem)
    now = datetime.now(tz=UTC)
    valid_token = pyjwt.encode(
        {
            "sub": "user_yte_abc",
            "email": "user@yte.medinet.vn",
            "name": "User Yte",
            "role": "viewer",
            "hub_ids": ["yte"],
            "iss": JWT_ISSUER,
            "aud": [JWT_AUDIENCE],
            "iat": int(now.timestamp()),
            "exp": int((now + timedelta(minutes=15)).timestamp()),
            "jti": "jti_valid_test",
            "token_type": "access",
        },
        private_pem,
        algorithm=JWT_ALGORITHM,
        headers={"kid": kid},
    )

    # Boot hub yte app + add test endpoint
    yte_app = hub_app_factory("yte")
    test_router = APIRouter()

    @test_router.get("/api/test/hub-access")
    async def test_endpoint(
        user=Depends(get_current_user_for_hub_access),  # noqa: B008
    ):
        return {"user_id": str(user.id)}

    yte_app.include_router(test_router)

    with TestClient(yte_app) as client:
        resp = client.get(
            "/api/test/hub-access",
            headers={"Authorization": f"Bearer {valid_token}"},
        )
        # Test endpoint return 200 nếu user exist DB; nếu user "user_yte_abc"
        # KHÔNG có DB → get_current_user raise 401 USER_DISABLED.
        # Plan 03-03 chỉ verify dependency chain branch logic — accept 200 hoặc 401 USER_DISABLED.
        # CROSS_HUB_ACCESS_DENIED 403 = FAIL.
        assert resp.status_code != 403, (
            f"Matching hub JWT KHÔNG được trả 403, got {resp.status_code} body={resp.json()}"
        )
        body = resp.json()
        if resp.status_code == 401:
            # Acceptable — user trong JWT KHÔNG có DB hub yte (test fixture data-empty)
            assert body["error"]["code"] in ("USER_DISABLED", "INVALID_TOKEN")


@pytest.mark.critical
@pytest.mark.integration
def test_central_bypass_hub_access_check_integration(
    monkeypatch, hub_app_factory, private_pem, public_pem
) -> None:
    """SSO-04 — Central bypass HUB_NAME check (cross-hub by design)."""
    from app.auth.jwt import JWT_AUDIENCE, JWT_ISSUER, JWT_ALGORITHM
    from app.auth.dependencies import get_current_user_for_hub_access
    from app.auth.jwks import _derive_kid

    kid = _derive_kid(public_pem)
    now = datetime.now(tz=UTC)
    # Central app: JWT hub_ids=['yte'] post tới central → bypass check
    token = pyjwt.encode(
        {
            "sub": "user_central_abc",
            "email": "admin@medinet.vn",
            "name": "Admin Central",
            "role": "admin",
            "hub_ids": ["yte"],  # only yte, but central bypass check
            "iss": JWT_ISSUER,
            "aud": [JWT_AUDIENCE],
            "iat": int(now.timestamp()),
            "exp": int((now + timedelta(minutes=15)).timestamp()),
            "jti": "jti_central_test",
            "token_type": "access",
        },
        private_pem,
        algorithm=JWT_ALGORITHM,
        headers={"kid": kid},
    )

    central_app = hub_app_factory("central")
    test_router = APIRouter()

    @test_router.get("/api/test/hub-access")
    async def test_endpoint(
        user=Depends(get_current_user_for_hub_access),  # noqa: B008
    ):
        return {"user_id": str(user.id)}

    central_app.include_router(test_router)

    with TestClient(central_app) as client:
        resp = client.get(
            "/api/test/hub-access",
            headers={"Authorization": f"Bearer {token}"},
        )
        # Central bypass — KHÔNG trả 403 CROSS_HUB_ACCESS_DENIED
        # (có thể 401 USER_DISABLED nếu user KHÔNG có DB — acceptable)
        assert resp.status_code != 403, (
            f"Central bypass HUB_NAME check failed, got 403"
        )
```

**Step 2: Verify `hub_app_factory` fixture support cho central** (Plan 02-03 Phase 2 đã ship — Plan 03-03 reuse).

Check `conftest.py` line ~578-620 `hub_app_factory` — nếu fixture hỗ trợ central, dùng được luôn. Nếu chỉ support yte/duoc/hcns/phap_che, Plan 03-03 mở rộng fixture để accept "central" (1-line change).

**Backward note:** Plan 02-VERIFICATION.md WR-01 đã flag hub_app_factory whitelist hardcode — Plan 03-03 cần extend nếu test cần "central".

**KHÔNG đụng:**
- `test_factor_hub_scoped.py` Plan 02-03 (Phase 2 closeout test — KHÔNG regress).
- Plan 03-02 integration test (`test_jwks_cache_lifecycle.py` — KHÔNG đụng).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_sso_blacklist_cross_process.py -v -m "critical and integration"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_sso_blacklist_cross_process.py` exists
    - `grep -c "def test_stale_jwt_cross_hub_returns_403_envelope\|def test_matching_jwt_hub_accepts\|def test_central_bypass_hub_access_check_integration" Hub_All/api/tests/integration/test_sso_blacklist_cross_process.py` ≥ 3
    - `grep -c "@pytest.mark.critical\|@pytest.mark.integration" Hub_All/api/tests/integration/test_sso_blacklist_cross_process.py` ≥ 6 (3 test × 2 marker)
    - `grep -c "CROSS_HUB_ACCESS_DENIED\|hub_ids\|aud\|JWT_AUDIENCE" Hub_All/api/tests/integration/test_sso_blacklist_cross_process.py` ≥ 4
    - `grep -c "get_current_user_for_hub_access" Hub_All/api/tests/integration/test_sso_blacklist_cross_process.py` ≥ 3 (3 test dùng)
    - `cd Hub_All/api && uv run ruff check tests/integration/test_sso_blacklist_cross_process.py` exit 0
    - `cd Hub_All/api && uv run mypy tests/integration/test_sso_blacklist_cross_process.py` exit 0 (cho phép `# type: ignore` cho FastAPI Depends pattern test)
    - `cd Hub_All/api && uv run pytest tests/integration/test_sso_blacklist_cross_process.py -v -m "critical and integration"` PASS ≥ 3 test
    - Phase 2 regression: `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS 10/10
    - Plan 03-02 regression: `cd Hub_All/api && uv run pytest tests/integration/test_jwks_cache_lifecycle.py -v -m "critical and integration"` PASS ≥ 3
  </acceptance_criteria>
  <done>
3 integration test PASS: (1) stale JWT cross-hub → 403 CROSS_HUB_ACCESS_DENIED envelope (SSO-04 E4 reinforced); (2) matching JWT hub accept (KHÔNG 403); (3) central bypass HUB_NAME check. Phase 2 + Plan 03-02 integration test regression KHÔNG break (13/13).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT claim `hub_ids` → repository hub_id WHERE clause | Layer 2 isolation M2 carry forward. Plan 03-03 add Layer 3 (JWT claim hub_ids enforcement ở dependency). Defense-in-depth: DB + repo + JWT cùng enforce E-V3-3 |
| Cross-hub JWT post → hub con endpoint | Stale JWT hub_ids=["duoc"] post tới hub yte → 403 (SSO-04). Verify ở dependency get_current_user_for_hub_access |
| Central logout → Redis blacklist → hub con verify | 1 Redis instance shared cross-process (M2 baseline). Logout central blacklist key `auth:blacklist:{jti}` → hub con check < 1s propagate |
| M2 cũ JWT post-Plan 03-03 deploy | JWT cũ KHÔNG có `aud` → 401 INVALID_TOKEN (verify_token reject). JWT cũ KHÔNG có `hub_ids` → 401 INVALID_TOKEN (pydantic ValidationError). User re-login required ~15-30s downtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| **T-03-03-01** | Spoofing | Stale JWT hub_ids=["duoc"] forge bởi attacker compromise user duoc → post tới hub yte | high | mitigate | Layer 3 enforcement `get_current_user_for_hub_access` (SSO-04 E4). HTTPException 403 CROSS_HUB_ACCESS_DENIED + envelope D6 shape (KHÔNG 404 leak / KHÔNG 500 internal). Integration test `test_stale_jwt_cross_hub_returns_403_envelope` verify behavior. Layer 1 DB-level (Phase 1 DSN validator) + Layer 2 repo `WHERE hub_id` (M2) = defense-in-depth. |
| **T-03-03-02** | Tampering | Attacker thay JWT aud claim từ ["medinet-wiki"] → ["wrong-service"] | high | mitigate | PyJWT decode strict `audience=JWT_AUDIENCE` check (Task 1) — InvalidAudienceError raise. Sign valid bằng public key central — KHÔNG forge được aud mà giữ signature valid. |
| **T-03-03-03** | Information Disclosure | Logout central → Redis blacklist key visible cross-tenant nếu Redis shared với other services | low | mitigate | Key prefix `auth:blacklist:` namespace clarity. Redis instance dedicated cho medinet-wiki M2. Future MCP/other services dùng prefix khác (vd `mcp:blacklist:`). |
| **T-03-03-04** | Repudiation | Logout race condition — user logout đồng thời với refresh → 2 jti khác nhau, audit miss | low | accept | Audit logger M2 ghi mọi auth event (login_success, refresh_success, logout) qua background task — race window < 100ms acceptable. Plan 10 v2.0 audit pipeline carry forward. |
| **T-03-03-05** | DoS | Redis instance xuống → blacklist check silent skip (M2 baseline: `if redis is None: log warning + return None`) → revoked JWT vẫn pass verify | medium | accept | M2 carry forward — Redis fail-open ở v3.0-a (defer Redis HA cluster Phase 7). Acceptable risk: rare Redis downtime + short window (15min access token expire). Defense: refresh token rotation 7d expire — long-term Redis HA defer v4.0. |
| **T-03-03-06** | Elevation of Privilege | M2 cũ JWT thiếu aud/hub_ids → post Plan 03-03 deploy bypass nếu validator/decode silently default | high | mitigate | PyJWT decode `audience` mandatory check raise InvalidAudienceError nếu thiếu. JWTClaims hub_ids REQUIRED (Pydantic ValidationError nếu thiếu). Task 1 test verify M2 cũ JWT reject (8 test). |
| **T-03-03-07** | Tampering | Operator manually flush Redis `FLUSHDB` → mọi blacklist key xoá → revoked JWT pass verify trong window TTL | low | accept | Ops-level action — KHÔNG attack vector external. Defense: audit log ghi flush event qua Redis ACL log (defer Phase 7 ACL hardening). |
| **T-03-03-08** | Information Disclosure | `request.state.jwt_claims` đối tượng JWTClaims chứa email + role → exposed qua FastAPI debug mode | low | accept | `request.state` accessible only ở dependency chain — KHÔNG render ra response body. Debug mode chỉ dev/staging (P12 mitigation). |
</threat_model>

<verification>
**Plan 03-03 verification chain:**

1. **File integrity (grep):**
   - `api/app/auth/jwt.py` — JWT_AUDIENCE constant + JWTClaims aud REQUIRED + hub_ids REQUIRED + 2 verify_token path strict audience check
   - `api/app/auth/_blacklist.py` (mới) — REDIS_BLACKLIST_PREFIX + make_blacklist_key
   - `api/app/auth/service.py` — 4 vị trí dùng make_blacklist_key (refresh check + write + logout access + logout refresh)
   - `api/app/auth/dependencies.py` — 1 vị trí dùng make_blacklist_key (get_current_user) + get_current_user_for_hub_access mới + request.state.jwt_claims wire
   - `api/app/auth/__init__.py` — export JWT_AUDIENCE + REDIS_BLACKLIST_PREFIX + make_blacklist_key + get_current_user_for_hub_access
   - 3 test file mới: test_jwt_iss_aud_hub_ids.py (8 test) + test_redis_blacklist_key_schema.py (6 test) + test_sso_blacklist_cross_process.py (3 integration)
   - Append 4 test mới vào test_get_current_user_jwks_branch.py (Plan 03-02 Task 4 file)

2. **Lint + type clean:**
   - `cd Hub_All/api && uv run ruff check app/auth/ tests/unit/test_jwt_iss_aud_hub_ids.py tests/unit/test_redis_blacklist_key_schema.py tests/unit/test_get_current_user_jwks_branch.py tests/integration/test_sso_blacklist_cross_process.py` exit 0
   - `cd Hub_All/api && uv run mypy app/auth/` exit 0

3. **Unit test PASS:**
   - `cd Hub_All/api && uv run pytest tests/unit/test_jwt_iss_aud_hub_ids.py tests/unit/test_redis_blacklist_key_schema.py tests/unit/test_get_current_user_jwks_branch.py -v` PASS ≥ 22 test (8 + 6 + 8)
   - `cd Hub_All/api && uv run pytest tests/unit/test_jwks_publish.py tests/unit/test_jwks_cache.py tests/unit/test_config_jwks.py -v` PASS ≥ 23 test (Plan 03-01 + 03-02 regression — 9 + 9 + 5)

4. **Integration test PASS:**
   - `cd Hub_All/api && uv run pytest tests/integration/test_sso_blacklist_cross_process.py tests/integration/test_jwks_cache_lifecycle.py -v -m "critical and integration"` PASS ≥ 6 test (3 + 3)
   - Phase 2 regression: `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS 10/10

5. **Settings + main factory regression:**
   - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v` PASS 49/49 (Phase 2 baseline)
</verification>

<success_criteria>
- JWT claim refactor: JWT_AUDIENCE constant + aud REQUIRED + hub_ids REQUIRED + PyJWT strict audience check ở 2 verify path
- M2 cũ JWT post-deploy reject (aud/hub_ids missing) — verify qua 8 unit test
- Redis blacklist key rename `auth:blacklist:{jti}` (5 vị trí touch) + helper `make_blacklist_key` + TTL=exp-now + value marker
- SSO-04 E4 reinforced: dependency `get_current_user_for_hub_access` HUB_NAME in claims.hub_ids check
- `request.state.jwt_claims` wire ở get_current_user 2 branch
- 22+ unit test PASS + 6 integration test PASS (3 SSO blacklist + 3 JWKS cache lifecycle Plan 03-02 regression)
- Phase 1+2 + Plan 03-01 + Plan 03-02 regression KHÔNG break
- D-V3-Phase3-E re-confirm (iss giữ NGUYÊN "medinet-wiki" + aud REQUIRED + hub_ids REQUIRED)
- D-V3-Phase3-H consume (key schema + TTL semantic)
- Lint + mypy clean
</success_criteria>

<output>
Tạo `.planning/phases/03-auth-sso-hub-ids-jwt/03-03-SUMMARY.md`:
- Plan 03-03 interface ship: JWT_AUDIENCE constant + JWTClaims aud/hub_ids REQUIRED + Redis key rename + get_current_user_for_hub_access dependency
- File modified: jwt.py, service.py, dependencies.py, __init__.py + _blacklist.py mới + 3 test file mới (2 unit + 1 integration) + append 4 test vào test_get_current_user_jwks_branch.py
- D-V3-Phase3-E (RE-CONFIRM iss giữ medinet-wiki) + D-V3-Phase3-H consumed
- SSO-02 + SSO-03 + SSO-04 đóng 3 REQ cùng plan (file-coupled)
- M2 backward incompat note (double — kid missing + aud missing) → Plan 03-05 README banner
- Wave 4 unblock: Plan 03-04 auth router refactor (hub con 307 redirect login/refresh) — service.py shape mới đã ship sẵn sàng
- Next: Plan 03-04 router 307 redirect + update Phase 2 test assertion 307
</output>
</content>
