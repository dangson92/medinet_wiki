---
phase: 03-auth-sso-hub-ids-jwt
plan: 05
type: execute
wave: 5
depends_on:
  - "03-04"
files_modified:
  - Hub_All/CLAUDE.md
  - Hub_All/.planning/STATE.md
  - Hub_All/.planning/REQUIREMENTS.md
  - Hub_All/README.md
autonomous: false
requirements:
  - SSO-01
  - SSO-02
  - SSO-03
  - SSO-04
tags:
  - closeout
  - claude-md-update
  - state-md-update
  - requirements-md-update
  - smoke-compose-checkpoint
  - v3.0-a-exit-gate-preview
  - backward-incompat-banner
  - readme-update
must_haves:
  truths:
    - "CLAUDE.md section 6 update — v3.0 progress table Phase 3 DONE 5 plan SSO-01..04; Phase 3 pattern subsection mô tả JWKS endpoint + cache + hub_ids/aud claim + 307 redirect + E4 reinforced"
    - "STATE.md frontmatter status + completed_phases 2→3 + completed_plans + percent update; Current Position Phase 3 DONE; Phase 3 Results Summary table; Next Action v3.0-a EXIT GATE preview"
    - "REQUIREMENTS.md SSO-01..04 mark `[x]` + note SSO-04 E4 reinforced inline (Plan 03-03 dependency get_current_user_for_hub_access)"
    - "README.md thêm section ## SSO Backward Incompat Note — M2 cũ JWT thiếu kid + aud + hub_ids sẽ 401 reject, user re-login ~15-30s downtime (Plan 03-02 + 03-03 ship)"
    - "v3.0-a EXIT GATE preview demo: Phase 1+2+3 DONE → 1 hub con (yte) + central + Redis + Postgres deploy được; JWT SSO login → JWKS verify → cross-hub isolation 403 enforce"
    - "Smoke compose checkpoint Task: docker compose up postgres + redis + python-api-central + python-api-yte → curl matrix verify (login central → JWT → curl /api/profile ở hub yte qua port 8181 với Bearer JWT → 200 hoặc 401 USER_DISABLED nếu user không trong DB hub yte → logout central → curl lại → 401 TOKEN_REVOKED < 1s)"
    - "Auto-chain pause expected ở Task 4 (checkpoint:human-action) — user resume signal `approved` / `failed` / `skip smoke`"
  artifacts:
    - path: "Hub_All/CLAUDE.md"
      provides: "Section 6 v3.0 progress table update + Phase 3 pattern subsection + footer changelog"
      contains: "Phase 3 .* DONE\\|SSO-01..04\\|JWKS endpoint\\|307 redirect"
    - path: "Hub_All/.planning/STATE.md"
      provides: "Frontmatter + Current Position + Phase 3 Results Summary table + Next Action /gsd-discuss-phase 4 hoặc v3.0-a EXIT GATE accept"
      contains: "Phase 3 DONE\\|completed_phases: 3\\|v3.0-a EXIT GATE"
    - path: "Hub_All/.planning/REQUIREMENTS.md"
      provides: "SSO-01..04 mark [x] + E4 reinforced note Plan 03-03"
      contains: "\\[x\\] \\*\\*SSO-01\\*\\*\\|E4 reinforced.*Plan 03-03\\|get_current_user_for_hub_access"
    - path: "Hub_All/README.md"
      provides: "SSO Backward Incompat note + JWKS endpoint deploy step + JWT re-login warning"
      contains: "SSO Backward Incompat\\|JWT.*re-login\\|/.well-known/jwks.json"
  key_links:
    - from: "Plan 03-01..04 deliverable"
      to: "CLAUDE.md section 6 v3.0 progress table"
      via: "Append subsection Phase 3 pattern + table row update"
      pattern: "Phase 3 .* DONE"
    - from: "Plan 03-01..04 deliverable"
      to: "STATE.md Phase 3 Results Summary table"
      via: "5 plan row + commit count + test count + smoke checkpoint result"
      pattern: "Phase 3 Results Summary"
    - from: "Plan 03-05 Task 4 smoke checkpoint"
      to: "v3.0-a EXIT GATE preview demo"
      via: "docker compose 2 service runtime + curl golden path"
      pattern: "v3.0-a EXIT GATE preview"
---

<objective>
Phase 3 v3.0 Multi-Hub Split SSO closeout — 4 deliverable docs + 1 smoke checkpoint runtime:

1. **CLAUDE.md section 6 update** — v3.0 progress table row Phase 3 DONE 5 plan + Phase 3 pattern subsection (JWKS endpoint + cache + hub_ids/aud claim + 307 redirect + E4 reinforced + Redis blacklist key schema mới) + footer changelog.
2. **STATE.md update** — frontmatter (completed_phases 2→3, completed_plans, percent, status), Current Position Phase 3 DONE, Phase 3 Results Summary table, Next Action v3.0-a EXIT GATE preview (giữa Phase 3-4).
3. **REQUIREMENTS.md update** — SSO-01..04 mark `[x]` + note SSO-04 E4 reinforced inline (Plan 03-03 dependency `get_current_user_for_hub_access`) + cross-reference 5 plan ship.
4. **README.md update** — section mới `## SSO Backward Incompat (Phase 3 v3.0)` thông báo M2 cũ JWT 401 reject sau Plan 03-02 + 03-03 deploy (kid + aud + hub_ids missing); user re-login ~15-30s downtime; deploy step `make keys` + restart container.
5. **Smoke compose checkpoint Task 5** (`checkpoint:human-action`) — docker compose 2 service runtime (central + yte) + golden path curl matrix verify:
   - `curl POST localhost:8180/api/auth/login` → 401 (user dummy chưa seed) HOẶC 200 nếu user M2 còn
   - `curl POST localhost:8181/api/auth/login` (hub yte) → 307 Location: central
   - `curl GET localhost:8180/.well-known/jwks.json` → 200 JWK Set
   - `curl GET localhost:8181/.well-known/jwks.json` → 404 envelope (FACTOR-02 strip)
   - Bonus (nếu user M2 còn): central login → JWT → curl hub yte với Bearer → verify path JWKSCache + E4 isolation 403 nếu hub_ids không match

Plan này Wave 5 — depend Plan 03-04 (auth router refactor ship xong). v3.0-a EXIT GATE TRIGGER sau Plan 03-05 close.

Output: 4 file docs modify + 1 checkpoint pause. Plan 03-05 KHÔNG ship code mới — pure closeout.

**Locked decisions consume (CONTEXT.md 03-CONTEXT.md):**
- **D-V3-Phase3-A..H** (8 decision) — Plan 03-01..04 đã ship; Plan 03-05 documentation closeout.
- **D-V3-Phase3-F:** Frontend redirect wire defer Phase 5 — README note explicit.
- **`<specifics>` CONTEXT.md:** Backward incompat warning operator — Plan 03-05 README + CLAUDE.md note.
- **ROADMAP.md v3.0-a EXIT GATE preview (giữa Phase 3-4):** Plan 03-05 trigger sau ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/README.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-01-PLAN.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-02-PLAN.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-03-PLAN.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-04-PLAN.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-04-SUMMARY.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-05-SUMMARY.md

<interfaces>
<!-- Plan 03-01..04 đã ship. Plan 03-05 closeout docs reflect implementation. -->

Pattern Phase 2 Plan 02-04 closeout (mirror — chỉ thay nội dung Phase 3):

```markdown
# Hub_All/CLAUDE.md section 6 — append subsection
### v3.0 progress (cập nhật khi mỗi Phase ship)

| Phase | Status | Plans | Date | REQ-ID |
|-------|--------|-------|------|--------|
| 1 — Multi-DB topology | ✅ DONE | 5 plan | 2026-05-21 | TOPO-01..04 (4) |
| 2 — Hub-con codebase factor | ✅ DONE | 5 plan | 2026-05-22 | FACTOR-01..04 (4) |
| 3 — Auth SSO + hub_ids JWT | ✅ **DONE** | **5 plan** | **2026-05-XX** | **SSO-01..04 (4)** |
| 4 — Cross-hub data sync | 📋 Next | — | — | SYNC-01..05 |
...

🚦 **v3.0-a EXIT GATE** triggered after Phase 3 — demo 1 hub con + central + JWT SSO + golden path → user accept tiếp tục v3.0-b (Phase 4-7).

### Phase 3 pattern (SSO-01..04 — 2026-05-XX)

- **JWKS endpoint (Plan 03-01 SSO-01):** Central `GET /.well-known/jwks.json` serve public RS256 key RFC 7517 format `{"keys":[{"kty":"RSA","kid":"...","alg":"RS256","n":"...","e":"AQAB"}]}`. Cache-Control 1h. Hub con strip endpoint → 404 envelope (FACTOR-02 carry forward).
- **JWKSCache + lifespan (Plan 03-02 SSO-01):** Hub con `api/app/auth/jwks.py::JWKSCache` in-process LRU + asyncio refresh task. Lifespan startup blocking fetch (timeout 5s — boot fail-loud) + background refresh 1h (fail-quiet) + 24h hard limit (`JWKSStaleError` → 503 envelope).
- **JWT claim refactor (Plan 03-03 SSO-02/03):** `aud=["medinet-wiki"]` REQUIRED + `hub_ids: list[str]` REQUIRED (KHÔNG default empty). PyJWT strict audience check ở 2 verify path (central local pem + hub con JWKSCache).
- **Redis blacklist key rename (Plan 03-03 SSO-02 D-V3-Phase3-H):** `blacklist:{jti}` → `auth:blacklist:{jti}` (5 vị trí touch: 4 service.py + 1 dependencies.py). TTL = JWT.exp - now (auto-expire).
- **SSO-04 E4 reinforced (Plan 03-03):** Dependency mới `get_current_user_for_hub_access` Layer 3 enforcement — hub con check `HUB_NAME in claims.hub_ids` → 403 CROSS_HUB_ACCESS_DENIED. Defense-in-depth (Layer 1 DB DSN + Layer 2 repo + Layer 3 JWT claim).
- **307 redirect (Plan 03-04 SSO-02 D-V3-Phase3-G):** Hub con `POST /api/auth/login` + `POST /api/auth/refresh` trả 307 Location: central (browser auto-follow preserve POST + body). Hub con `logout` + `me` giữ local handle (verify JWT qua JWKSCache + Redis blacklist Plan 03-03).
- **Backward incompat:** M2 cũ JWT thiếu kid + aud + hub_ids → 401 reject. User re-login ~15-30s downtime sau Plan 03-02 + 03-03 deploy. Operator broadcast.
- **Frontend wire:** Defer Phase 5 PROXY-02 D-V3-Phase3-F (D-V3-06 D6 expire formally Phase 5).
```

```markdown
# Hub_All/.planning/STATE.md — frontmatter + Current Position update

---
gsd_state_version: 1.0
milestone: v3.0
milestone_name: Multi-Hub Split
status: Phase 3 DONE 2026-05-XX ✅ ... 5 plans / X+ commits / Y+ tests PASS. JWKS endpoint + cache + JWT iss/aud/hub_ids REQUIRED + Redis blacklist key rename + 307 redirect hub con login/refresh + SSO-04 E4 reinforced ... v3.0-a EXIT GATE triggered ...
last_updated: "2026-05-XXTHH:MM:SS.000Z"
progress:
  total_phases: 7
  completed_phases: 3
  total_plans: 15
  completed_plans: 15
  percent: 47
---
```

```markdown
# Hub_All/.planning/REQUIREMENTS.md SSO-01..04 mark [x] + note E4 reinforced

### SSO — Auth SSO + hub_ids trong JWT (4 REQ — GA-V3-A chốt ở `/gsd-discuss-phase 3`)

- [x] **SSO-01** ✅ Phase 3: Central expose JWKS endpoint ... (DONE 2026-05-XX — Plan 03-01 publish layer + Plan 03-02 hub con cache + integration test rotate keypair PASS)
- [x] **SSO-02** ✅ Phase 3: Refresh token blacklist Redis chung ... (DONE 2026-05-XX — Plan 03-03 key rename `auth:blacklist:` + Plan 03-04 307 redirect hub con + integration test cross-process PASS)
- [x] **SSO-03** ✅ Phase 3: JWT claim `hub_ids: list[str]` ... (DONE 2026-05-XX — Plan 03-03 hub_ids REQUIRED + aud REQUIRED + PyJWT strict audience)
- [x] **SSO-04** ✅ Phase 3: E4 reinforced ... (DONE 2026-05-XX — Plan 03-03 dependency `get_current_user_for_hub_access` Layer 3 enforce + integration test stale JWT cross-hub 403 PASS)

  > **NOTE Phase 3 closeout (Plan 03-05 SSO-01..04 — 2026-05-XX):** 5 plan ship đầy đủ 4 REQ-ID. Backward incompat: M2 cũ JWT thiếu kid + aud + hub_ids → 401 reject sau Plan 03-02 + 03-03 deploy; user re-login ~15-30s downtime (operator broadcast Slack). Frontend wire (form action central) defer Phase 5 PROXY-02 (D-V3-06 D6 expire). v3.0-a progress: Phase 1+2+3 DONE (3/3 phase v3.0-a) — EXIT GATE triggered.
```

```markdown
# Hub_All/README.md — section mới ## SSO Backward Incompat (Phase 3 v3.0)

## SSO Backward Incompat (Phase 3 v3.0 — 2026-05-XX)

Phase 3 ship SSO infrastructure mới (JWKS endpoint + cache + JWT claim refactor + Redis blacklist key rename + 307 redirect hub con login/refresh). **User cần re-login sau khi deploy.**

### Tại sao re-login?

Plan 03-02 + 03-03 add 3 yêu cầu mới cho JWT:

1. **Header `kid`** (Plan 03-02): JWT phát hành sau Plan 03-02 có header `kid` (SHA-256 8-byte PEM). Hub con dùng kid để match public key trong JWKSCache. M2 cũ JWT KHÔNG có kid → hub con verify fail 401 INVALID_TOKEN.
2. **Claim `aud=["medinet-wiki"]`** (Plan 03-03): PyJWT strict audience check ở verify_token + verify_token_with_key. M2 cũ JWT KHÔNG có aud → 401 InvalidAudienceError.
3. **Claim `hub_ids: list[str]`** REQUIRED (Plan 03-03): JWTClaims pydantic validate REQUIRED. M2 cũ JWT KHÔNG có hub_ids → 401 ValidationError.

### Deploy steps

1. **Backup database** (M2 baseline carry forward):
   ```bash
   docker exec medinet-postgres pg_dumpall -U medinet > backup-pre-phase3.sql
   ```

2. **Verify M2 JWT keypair còn** (Plan 03-01 reuse):
   ```bash
   ls -la api/keys/private.pem api/keys/public.pem
   # Nếu mất, sinh lại: cd api && make keys
   ```

3. **Deploy services**:
   ```bash
   docker compose up -d postgres redis
   docker compose build python-api-central python-api-yte python-api-duoc python-api-hcns
   docker compose up -d python-api-central python-api-yte python-api-duoc python-api-hcns
   ```

4. **Verify JWKS endpoint serve correctly**:
   ```bash
   curl http://localhost:8180/.well-known/jwks.json
   # Expect: {"keys":[{"kty":"RSA","kid":"...","alg":"RS256",...}]}
   ```

5. **Broadcast user re-login** (Slack/Email):
   > "Hệ thống Medinet Wiki vừa nâng cấp SSO. Vui lòng đăng xuất + đăng nhập lại để nhận token mới. Phiên hiện tại sẽ tự động hết hạn trong vài phút."

### Endpoint mapping mới (Phase 3 Plan 03-04 SSO-02)

| Endpoint | Central (8180) | Hub yte (8181) / duoc (8182) / hcns (8183) |
|---|---|---|
| `POST /api/auth/login` | Handle local (M2) | **307 Location: central** |
| `POST /api/auth/refresh` | Handle local (M2) | **307 Location: central** |
| `POST /api/auth/logout` | Handle local (blacklist Redis chung) | Handle local (blacklist Redis chung) |
| `GET /api/auth/me` | Handle local | Handle local (verify JWT qua JWKSCache) |
| `GET /.well-known/jwks.json` | **200 JWK Set** | 404 envelope (FACTOR-02 strip) |

### Cross-hub isolation enforcement (SSO-04 E4 reinforced)

Plan 03-03 add dependency `get_current_user_for_hub_access` — hub con check `HUB_NAME in JWT.hub_ids` → reject 403 CROSS_HUB_ACCESS_DENIED nếu mismatch. Defense-in-depth bên cạnh Phase 1 DB-level DSN validator + M2 repo layer.
```

Pattern smoke checkpoint Task 5 (mirror Phase 2 Plan 02-04 Task 1 SKIP rationale — Plan 03-05 quyết định human-action):

```bash
# Task 5 smoke commands (operator chạy nếu Docker available)
cd Hub_All
docker compose up -d postgres redis
sleep 5
docker compose build python-api-central python-api-yte
docker compose up -d python-api-central python-api-yte
sleep 10
# Verify central JWKS endpoint
curl -s http://localhost:8180/.well-known/jwks.json | jq .
# Verify hub yte strip
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8181/.well-known/jwks.json   # 404
# Verify hub yte login 307 redirect
curl -s -o /dev/null -w "%{http_code}\n%{redirect_url}\n" \
  -X POST -H "Content-Type: application/json" \
  -d '{"email":"u@m.vn","password":"x"}' http://localhost:8181/api/auth/login
# Expect: 307 + Location: http://python-api-central:8080/api/auth/login
# Cleanup
docker compose down
```
</interfaces>

<notes>
**Task 5 smoke checkpoint pre-resolution flexibility:**

Theo pattern Phase 2 Plan 02-04 Task 1 + Plan 02-05 Task 3, smoke compose runtime checkpoint thường được pre-resolved SKIP nếu user không có Docker Desktop local. Plan 03-05 KHÔNG bắt buộc — operator có thể:
- **Approve:** Chạy 6 curl command verify (xem `<interfaces>` script trên).
- **Skip với rationale:** Defer Phase 7 MIGRATE-05 full E2E (in-process integration test Plan 03-02/03/04 đã cover semantic — 6 integration test + 22+ unit test PASS).
- **Failed:** Re-execute hoặc rollback (xem deploy step rollback).

Plan 03-05 dùng `checkpoint:human-action` để cho user quyền quyết định runtime.

**v3.0-a EXIT GATE preview demo (giữa Phase 3 và Phase 4):**

Sau Plan 03-05 close, ROADMAP.md trigger 🚦 v3.0-a EXIT GATE. Demo deliverable:
- 1 hub con (yte) + central + Redis + Postgres deploy được trên Docker compose.
- User login `https://central/api/auth/login` → JWT valid.
- User truy cập `https://central/yte/api/...` (direct port test trước Caddy lên Phase 5) → hub con verify JWT qua JWKS → 200.
- Hub con CHỈ truy cập data hub yte (test cross-hub access → 403 SSO-04).
- Golden path login → upload (local hub yte chỉ) → search local → PASS.

**Nếu user accept v3.0-a:** Tiếp tục v3.0-b (Phase 4-7 Sync + Proxy + Settings + Migration). Never pivot multi-DB topology (R-V3-1 mitigation).

**Nếu user reject:** Mở `/gsd-discuss-milestone v3.0` re-discuss D-V3-01 topology choice.

**KHÔNG đụng:**
- ROADMAP.md Phase 3 row (giữ traceability commit seed) — chỉ update Progress table.
- Phase 1+2 SUMMARY archive.
- Source code (Plan 03-01..04 đã ship).
- Database / Alembic.

**STATE.md count cập nhật:**

Phase 1 = 5 plan, Phase 2 = 5 plan, Phase 3 = 5 plan = total 15 plan / ~32 estimate ≈ 47%.

Phase 2 STATE.md (đã cập nhật ở Plan 02-05) ghi total_plans=10, percent=31. Plan 03-05 cập nhật:
- `total_plans: 10 → 15` (5 Phase 3 ship)
- `completed_plans: 10 → 15`
- `percent: 31 → 47`
- `completed_phases: 2 → 3`

Per-plan metric: Phase 3 sẽ có Performance Metrics block tương tự Phase 1/2 (5 plan row × ~30 phút avg ≈ 2.5h total Phase 3 estimate).
</notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CLAUDE.md section 6 — v3.0 progress table Phase 3 DONE + Phase 3 pattern subsection + footer</name>
  <files>Hub_All/CLAUDE.md</files>
  <read_first>
    - Hub_All/CLAUDE.md (Phase 2 Plan 02-04 + Plan 02-05 đã update section 6 — Plan 03-05 mirror pattern thêm Phase 3 row + subsection)
  </read_first>
  <action>
**Step 1: Tìm section 6 v3.0 progress table** trong `Hub_All/CLAUDE.md`. Phase 2 Plan 02-04 đã ship bảng 7 phase status. Plan 03-05 update row Phase 3:

```markdown
| Phase | Status | Plans | Date | REQ-ID |
|-------|--------|-------|------|--------|
| 1 — Multi-DB topology + per-hub Alembic | ✅ DONE | 5 plan | 2026-05-21 | TOPO-01..04 (4) |
| 2 — Hub-con codebase factor | ✅ DONE | 5 plan | 2026-05-22 | FACTOR-01..04 (4 — FACTOR-04 added 2026-05-22 Plan 02-05) |
| 3 — Auth SSO + hub_ids JWT | ✅ **DONE** | **5 plan** | **2026-05-XX** | **SSO-01..04 (4)** |
| 🚦 v3.0-a EXIT GATE | ✅ **TRIGGERED** | — | 2026-05-XX | Demo 1 hub con yte + central + JWT SSO + golden path → user accept tiếp tục v3.0-b |
| 4 — Cross-hub data sync | 📋 Next | — | — | SYNC-01..05 |
| 5 — Reverse proxy + frontend subpath | 📋 Backlog | — | — | PROXY-01..04 |
| 6 — System settings sync | 📋 Backlog | — | — | SETTINGS-01..04 |
| 7 — Migration + smoke E2E | 📋 Backlog | — | — | MIGRATE-01..05 |
```

**Step 2: Append subsection "Phase 3 pattern (SSO-01..04 — 2026-05-XX)"** SAU "Phase 2 FACTOR-04 dynamic hub registration":

```markdown
### Phase 3 pattern (SSO-01..04 — 2026-05-XX)

5 plan đóng 4 REQ-ID Auth SSO + hub_ids JWT:

- **Plan 03-01 SSO-01 — JWKS endpoint publish (D-V3-Phase3-A):** `api/app/auth/jwks.py` module mới với `publish_jwks()` + `load_public_key_as_jwk()` RFC 7517 (`kty:RSA, kid:SHA-256[:8], use:sig, alg:RS256, n, e`). Central mount `GET /.well-known/jwks.json` conditional (`if settings.hub_name == "central"` block) + Cache-Control 1h. Hub con strip → 404 envelope (FACTOR-02 carry forward).
- **Plan 03-02 SSO-01 — JWKSCache hub con (D-V3-Phase3-B/D):** `JWKSCache` class in-process LRU + asyncio refresh task. Lifespan startup blocking fetch (timeout 5s — boot fail-loud) + background refresh 1h (fail-quiet) + 24h hard limit. `get_current_user` branch hub con verify qua `JWKSCache.get_public_key(kid)` thay vì local pem. Settings 3 field mới: `central_jwks_url` + `jwks_refresh_interval` + `jwks_max_stale_seconds` + 1 model_validator enforce hub con required.
- **Plan 03-03 SSO-02/03/04 — JWT claim + Redis key + E4 reinforced (D-V3-Phase3-E/H):** `aud=["medinet-wiki"]` REQUIRED + `hub_ids: list[str]` REQUIRED (KHÔNG default empty). PyJWT strict audience check ở 2 verify path. Redis blacklist key rename `blacklist:{jti}` → `auth:blacklist:{jti}` (5 vị trí touch). Dependency mới `get_current_user_for_hub_access` Layer 3 enforce — hub con check `HUB_NAME in claims.hub_ids` → 403 CROSS_HUB_ACCESS_DENIED. Defense-in-depth (Layer 1 DB DSN + Layer 2 repo + Layer 3 JWT claim).
- **Plan 03-04 SSO-02 — 307 redirect hub con (D-V3-Phase3-G):** Hub con `POST /api/auth/login` + `POST /api/auth/refresh` trả 307 Location: central (preserve POST + body — RFC 7231). Hub con `logout` + `me` giữ local handle (verify JWT qua JWKSCache + blacklist Redis chung Plan 03-03). Settings.central_url field mới + docker-compose env CENTRAL_URL 3 hub con + override.yml.template. Phase 2 integration test `test_factor_hub_scoped.py` assertion update split (10 LOCAL + 2 SSO_REDIRECT).
- **Plan 03-05 closeout — docs + smoke checkpoint:** CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md update; smoke compose 2 service checkpoint (central + yte) curl matrix; v3.0-a EXIT GATE preview trigger.

**Backward incompat (operator broadcast):** M2 cũ JWT thiếu kid + aud + hub_ids → 401 reject sau Plan 03-02 + 03-03 deploy. User re-login ~15-30s downtime. Communicate Slack/Email trước deploy. Xem README.md "SSO Backward Incompat (Phase 3 v3.0)" section.

**Frontend wire:** Defer Phase 5 PROXY-02 (D-V3-Phase3-F + D-V3-06 D6 expire formally Phase 5).
```

**Step 3: Update footer changelog:**

```markdown
*Cập nhật: 2026-05-XX (Phase 3 DONE — SSO-01..04 ship 5 plan). Project: MEDWIKI. M2 v2.0 done; v3.0 Multi-Hub Split — Phase 1+2+3 DONE (15/~32 plan ≈ 47%), v3.0-a EXIT GATE triggered. Next: /gsd-discuss-phase 4 Cross-hub data sync (GA-V3-D chốt) hoặc accept v3.0-a tiếp tục v3.0-b.*
```

**KHÔNG đụng:** Section 1-5 + M2 closeout description + 4 D-V3-01..04 LOCKED + GA-V3-B/C/D open question. GA-V3-A status update từ "open" → "✅ DONE Plan 03-01..05" có thể được thêm ở Plan 03-05 nếu cần — Plan 03-05 update để CLAUDE.md reflect chính xác.
  </action>
  <verify>
    <automated>grep -c "Phase 3 .* DONE\|SSO-01..04\|JWKS endpoint\|307 redirect\|JWKSCache" Hub_All/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 3.*DONE\|SSO-01..04" Hub_All/CLAUDE.md` ≥ 2
    - `grep -c "v3.0-a EXIT GATE.*TRIGGERED" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "Plan 03-01\|Plan 03-02\|Plan 03-03\|Plan 03-04\|Plan 03-05" Hub_All/CLAUDE.md` ≥ 5
    - `grep -c "JWKSCache\|publish_jwks\|jwks.json" Hub_All/CLAUDE.md` ≥ 2
    - `grep -c "307 redirect\|307 Location" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "auth:blacklist:\|D-V3-Phase3-H" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "get_current_user_for_hub_access\|E4 reinforced" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "Backward incompat\|kid + aud + hub_ids" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "Frontend wire.*defer Phase 5\|D-V3-Phase3-F" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "Cập nhật: 2026-05" Hub_All/CLAUDE.md` ≥ 1 (footer)
    - 6 section heading vẫn nguyên (## 1 ... ## 6)
  </acceptance_criteria>
  <done>
CLAUDE.md section 6 v3.0 progress table Phase 3 DONE 5 plan + v3.0-a EXIT GATE TRIGGERED row + Phase 3 pattern subsection 5 plan detail + backward incompat note + footer changelog. KHÔNG đụng section 1-5 + M2 closeout.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update STATE.md — frontmatter + Current Position + Phase 3 Results Summary + Next Action</name>
  <files>Hub_All/.planning/STATE.md</files>
  <read_first>
    - Hub_All/.planning/STATE.md (Phase 2 Plan 02-05 closeout — Plan 03-05 mirror pattern)
  </read_first>
  <action>
**Step 1: Update frontmatter** trên đầu STATE.md:

```yaml
---
gsd_state_version: 1.0
milestone: v3.0
milestone_name: Multi-Hub Split
status: Phase 3 DONE 2026-05-XX ✅ — Auth SSO + hub_ids JWT 5 plan ship SSO-01..04. JWKS endpoint central + cache hub con (in-process LRU + 24h hard limit) + JWT aud/hub_ids REQUIRED + Redis blacklist key rename `auth:blacklist:` + 307 redirect hub con login/refresh + SSO-04 E4 reinforced dependency Layer 3 + frontend wire defer Phase 5 PROXY-02. M2 cũ JWT thiếu kid/aud/hub_ids → 401 reject sau deploy; user re-login ~15-30s downtime. v3.0-a EXIT GATE triggered — Phase 1+2+3 DONE (3/3 phase v3.0-a — 15/~32 plan ≈ 47%). Next: /gsd-discuss-phase 4 Cross-hub data sync (GA-V3-D chốt) hoặc user accept v3.0-a tiếp tục v3.0-b.
last_updated: "2026-05-XXTHH:MM:SS.000Z"
progress:
  total_phases: 7
  completed_phases: 3
  total_plans: 15
  completed_plans: 15
  percent: 47
---
```

**Step 2: Update Current Position** section:

```markdown
## Current Position

- **Phase:** 3 — Auth SSO + hub_ids trong JWT ✅ **DONE 2026-05-XX — SSO-01..04 fully shipped (Wave 1-5 ✅)**
- **Plan:** 5/5 plans complete (03-01..03-05) cho SSO-01..04 ở `.planning/phases/03-auth-sso-hub-ids-jwt/`.
- **Status:** Phase 3 closed SSO-01..04 — JWKS endpoint central (`/.well-known/jwks.json` RFC 7517 single-key RS256); hub con `JWKSCache` in-process LRU + asyncio refresh + 24h hard limit (D-V3-Phase3-B/D); JWT claim refactor `aud=["medinet-wiki"]` REQUIRED + `hub_ids: list[str]` REQUIRED + PyJWT strict audience check (D-V3-Phase3-E); Redis blacklist key rename `auth:blacklist:{jti}` (5 vị trí touch — D-V3-Phase3-H); 307 redirect hub con `POST /api/auth/login` + `POST /api/auth/refresh` (D-V3-Phase3-G); SSO-04 E4 reinforced dependency `get_current_user_for_hub_access` Layer 3 enforcement. Settings field mới: `central_jwks_url` + `central_url` + 2 model_validator hub con required. Docker-compose 3 hub con + override.yml.template thêm 2 env CENTRAL_JWKS_URL + CENTRAL_URL. M2 backward incompat (kid + aud + hub_ids missing) — user re-login required.
- **Last activity:** 2026-05-XX — `/gsd-execute-phase 3` wave-based execution complete:
  - **03-01 DONE ✅** (Wave 1 BLOCKING): JWKS endpoint publish layer + module `api/app/auth/jwks.py` mới (publish_jwks + load_public_key_as_jwk + 2 helper + JWK/JWKSet TypedDict). 9 unit test PASS test_jwks_publish.py. SUMMARY: `.planning/phases/03-auth-sso-hub-ids-jwt/03-01-SUMMARY.md`.
  - **03-02 DONE ✅** (Wave 2): JWKSCache class + lifespan branch hub con + dependency branch + Settings 2 field + model_validator. 21+ unit test PASS (9 cache + 5 config + 4 dependency + 3 publish) + 3 integration test PASS (test_jwks_cache_lifecycle.py).
  - **03-03 DONE ✅** (Wave 3): JWT claim aud/hub_ids REQUIRED + JWT_AUDIENCE constant + PyJWT strict audience check (2 path) + Redis blacklist key rename `auth:blacklist:` + dependency `get_current_user_for_hub_access` SSO-04 E4 reinforced + request.state.jwt_claims wire. 22+ unit test PASS + 3 integration test PASS (test_sso_blacklist_cross_process.py).
  - **03-04 DONE ✅** (Wave 4): auth router refactor login + refresh 307 redirect hub con; logout + me giữ local. Settings.central_url field + model_validator. Docker-compose CENTRAL_URL env 3 hub con + override.yml.template. Phase 2 integration test split 2 list (10 LOCAL + 2 SSO_REDIRECT) — KHÔNG regress 10/10. 13+ unit test PASS.
  - **03-05 DONE ✅** (Wave 5 closeout): CLAUDE.md section 6 + STATE.md frontmatter + Current Position + Phase 3 Results Summary + REQUIREMENTS.md SSO-01..04 mark [x] + note E4 reinforced + README.md SSO Backward Incompat section + smoke checkpoint Task 5 (chạy/skip tuỳ operator). v3.0-a EXIT GATE TRIGGERED. SUMMARY: `.planning/phases/03-auth-sso-hub-ids-jwt/03-05-SUMMARY.md`.
```

**Step 3: Append "Phase 3 Planning Summary" table** sau Phase 2 Planning Summary table:

```markdown
## Phase 3 Planning Summary

| Plan | Wave | Objective | Tasks | Files modified | REQ | Status |
|------|------|-----------|-------|----------------|-----|--------|
| 03-01 | 1 | JWKS endpoint publish layer (publish_jwks + RFC 7517 helper + central mount conditional) | 2 (tdd + auto) | `api/app/auth/jwks.py` (new), `api/app/main.py`, `api/app/config.py`, `tests/unit/test_jwks_publish.py` (new), `docker-compose.yml` | SSO-01 | ✅ **DONE 2026-05-XX** (9/9 unit test PASS) |
| 03-02 | 2 | JWKSCache class hub con + lifespan branch + dependency branch + Settings 2 field + validator | 5 (1 settings + 1 tdd cache + 2 wire + 1 integration) | `api/app/auth/jwks.py` (extend), `api/app/main.py`, `api/app/auth/jwt.py`, `api/app/auth/dependencies.py`, `api/app/config.py`, 4 test file | SSO-01 | ✅ **DONE 2026-05-XX** (21+ unit + 3 integration PASS) |
| 03-03 | 3 | JWT claim refactor (aud + hub_ids REQUIRED) + Redis blacklist key rename + dependency SSO-04 E4 reinforced | 4 (1 tdd jwt + 1 tdd blacklist + 1 dependency + 1 integration) | `api/app/auth/jwt.py`, `api/app/auth/_blacklist.py` (new), `api/app/auth/service.py`, `api/app/auth/dependencies.py`, 3 test file | SSO-02, SSO-03, SSO-04 | ✅ **DONE 2026-05-XX** (22+ unit + 3 integration PASS) |
| 03-04 | 4 | auth router 307 redirect hub con login/refresh + Settings central_url + Phase 2 integration test update | 4 (1 settings + 1 router + 1 integration test update + 1 docs) | `api/app/auth/router.py`, `api/app/config.py`, `docker-compose.yml`, `docker-compose.override.yml.template`, `tests/unit/test_auth_router_hub_redirect.py` (new), `tests/integration/test_factor_hub_scoped.py`, `.planning/REQUIREMENTS.md` | SSO-02 | ✅ **DONE 2026-05-XX** (13+ unit + 10/10 Phase 2 integration regression) |
| 03-05 | 5 | Closeout — CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md + smoke checkpoint runtime | 5 (4 docs + 1 checkpoint) | `Hub_All/CLAUDE.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `Hub_All/README.md` | SSO-01..04 closeout | ✅ **DONE 2026-05-XX** (Task 5 smoke approve/skip tuỳ operator) |

**Coverage:** 4/4 REQ (SSO-01..04) covered ≥ 1 plan/REQ.

**Critical path:** 03-01 ✅ (Wave 1 BLOCKING) → 03-02 (Wave 2 depend 03-01) → 03-03 (Wave 3 depend 03-02) → 03-04 (Wave 4 depend 03-03) → 03-05 (Wave 5 closeout).

**Auto-chain pause expected:** Plan 03-05 Task 5 (`checkpoint:human-action gate=blocking`) — smoke compose runtime central + yte. User resume signal `approved` / `failed` / `skip smoke`.
```

**Step 4: Append "Phase 3 Results Summary" table** sau Phase 3 Planning Summary:

```markdown
## Phase 3 Results Summary

| Plan | Wave | Objective | Commits | Tests |
|------|------|-----------|---------|-------|
| 03-01 | 1 | JWKS endpoint publish layer | X commit | unit 9/9 PASS test_jwks_publish.py |
| 03-02 | 2 | JWKSCache hub con + lifespan + dependency branch | X commit | unit 21+ PASS (cache 9 + config 5 + dependency 4 + publish 3) + integration 3 PASS test_jwks_cache_lifecycle.py |
| 03-03 | 3 | JWT aud/hub_ids REQUIRED + Redis key rename + SSO-04 E4 reinforced | X commit | unit 22+ PASS (jwt 8 + blacklist 6 + dependency append 4 + jwks regression) + integration 3 PASS test_sso_blacklist_cross_process.py |
| 03-04 | 4 | auth router 307 redirect + Settings central_url + Phase 2 test update | X commit | unit 13+ PASS (router 5 + config 8) + Phase 2 integration 10/10 regression maintain |
| 03-05 | 5 | Closeout docs + smoke checkpoint runtime | X commit | Docs update grep verify PASS; Task 5 smoke approve/skip tuỳ operator |

**Phase 3 deliverable summary:**
- JWKS endpoint central (`/.well-known/jwks.json` RFC 7517 single-key RS256 + Cache-Control 1h) + hub con strip 404 envelope (FACTOR-02 carry forward).
- JWKSCache hub con in-process LRU + asyncio refresh 1h (fail-quiet) + 24h hard limit (fail-loud delayed via 503 JWKS_STALE).
- JWT claim refactor: `aud=["medinet-wiki"]` REQUIRED + `hub_ids: list[str]` REQUIRED + PyJWT strict audience check.
- Redis blacklist key rename `auth:blacklist:{jti}` (5 vị trí service.py + dependencies.py) + TTL=exp-now auto-cleanup.
- 307 redirect hub con login/refresh tới central (D-V3-Phase3-G) — preserve POST + body RFC 7231.
- SSO-04 E4 reinforced: dependency `get_current_user_for_hub_access` Layer 3 enforcement defense-in-depth.
- Settings 2 field mới: `central_jwks_url` + `central_url` + 2 model_validator hub con required.
- Docker-compose 3 hub con + override.yml.template thêm CENTRAL_JWKS_URL + CENTRAL_URL env.
- Backward incompat: M2 cũ JWT 401 reject; user re-login required ~15-30s downtime.
- Frontend wire defer Phase 5 PROXY-02 (D-V3-Phase3-F + D-V3-06 D6 expire).

**v3.0-a progress: Phase 1+2+3 DONE (3/3 phase v3.0-a — 15/~32 plan ≈ 47%). 🚦 v3.0-a EXIT GATE TRIGGERED giữa Phase 3-4 — demo 1 hub con yte + central + JWT SSO + golden path PASS → user accept tiếp tục v3.0-b (Phase 4-7 sync + proxy + settings + migration).**
```

**Step 5: Update Next Action** section:

```markdown
## Next Action

1. **(BLOCKING — v3.0-a EXIT GATE)** Demo 1 hub con (yte) + central + Redis + Postgres deploy được trên Docker compose; user login `https://central/api/auth/login` → JWT valid; user truy cập `https://central/yte/api/...` → hub con verify JWT qua JWKS → 200; hub con CHỈ truy cập data hub yte (test cross-hub → 403); golden path login → upload local hub yte → search local → PASS. **User accept → tiếp tục v3.0-b. User reject → re-discuss D-V3-01 topology choice qua `/gsd-discuss-milestone v3.0`.**
2. **(Recommended sau accept v3.0-a)** `/gsd-discuss-phase 4` — Cross-hub data sync (GA-V3-D chốt: cocoindex target thứ 2 vs Postgres logical replication vs outbox + worker).
3. (Optional) `/gsd-code-review 3` — advisory code review trên 5 plan Phase 3 (workflow.code_review gate).
4. (Optional) `/gsd-verify-work 3` — manual UAT 4 SC nếu user muốn extra verify ngoài automated test.
```

**KHÔNG đụng:** Accumulated Context v2.0 + v3.0 LOCKED decisions + Carry-forward risks + Phase 1+2 Results Summary (giữ).
  </action>
  <verify>
    <automated>grep -c "Phase 3 DONE\|completed_phases: 3\|completed_plans: 15\|percent: 47\|Phase 3 Results Summary\|v3.0-a EXIT GATE" Hub_All/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "completed_phases: 3" Hub_All/.planning/STATE.md` ≥ 1
    - `grep -c "completed_plans: 15" Hub_All/.planning/STATE.md` ≥ 1
    - `grep -c "total_plans: 15" Hub_All/.planning/STATE.md` ≥ 1
    - `grep -c "percent: 47" Hub_All/.planning/STATE.md` ≥ 1
    - `grep -c "Phase 3 DONE" Hub_All/.planning/STATE.md` ≥ 2
    - `grep -c "Phase 3 Planning Summary\|Phase 3 Results Summary" Hub_All/.planning/STATE.md` ≥ 2
    - `grep -cE "03-0[1-5] \| [1-5]" Hub_All/.planning/STATE.md` ≥ 5 (5 plan row × 2 table ≥ 10 actually)
    - `grep -c "v3.0-a EXIT GATE.*TRIGGERED\|v3.0-a EXIT GATE" Hub_All/.planning/STATE.md` ≥ 2
    - `grep -c "JWKSCache\|JWKS endpoint\|307 redirect\|aud.*REQUIRED\|hub_ids.*REQUIRED" Hub_All/.planning/STATE.md` ≥ 3
    - `grep -c "/gsd-discuss-phase 4" Hub_All/.planning/STATE.md` ≥ 1
    - Phase 1+2 Results Summary KHÔNG xoá: `grep -c "Phase 1 Results Summary\|Phase 2 Results Summary" Hub_All/.planning/STATE.md` ≥ 2
  </acceptance_criteria>
  <done>
STATE.md frontmatter Phase 3 DONE + completed_phases=3 + 15 plan + 47% + Current Position + Phase 3 Planning + Results Summary + Next Action v3.0-a EXIT GATE preview. Phase 1+2 Results Summary KHÔNG xoá (carry forward).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update REQUIREMENTS.md SSO-01..04 mark [x] + note E4 reinforced Plan 03-03</name>
  <files>Hub_All/.planning/REQUIREMENTS.md</files>
  <read_first>
    - Hub_All/.planning/REQUIREMENTS.md (FACTOR-03 đã có note Plan 02-04 + Plan 03-04 — Plan 03-05 update SSO-01..04 rows mark done)
  </read_first>
  <action>
**Step 1: Tìm SSO-01..04 rows** trong `Hub_All/.planning/REQUIREMENTS.md` line ~45-50. Phase 3 chưa update — Plan 03-05 mark `[x]` cho 4 row.

```markdown
### SSO — Auth SSO + hub_ids trong JWT (4 REQ — GA-V3-A chốt ở `/gsd-discuss-phase 3`)

- [x] **SSO-01** ✅ Phase 3: Central expose JWKS endpoint `GET /.well-known/jwks.json` public key RS256 (PKCS#8). Hub con cache JWKS local TTL 1h ở startup + refresh background (R-V3-5 mitigation HA). Hub con verify JWT bằng JWKS đã cache thay vì shared keypair file. Test integration: rotate central keypair → hub con detect mới trong TTL window. — **DONE 2026-05-XX** (Plan 03-01 publish layer + Plan 03-02 hub con cache; integration test rotate keypair `test_jwks_cache_rotate_keypair_detects_new_kid` PASS — `JWKSCache` in-process LRU + asyncio refresh 1h fail-quiet + 24h hard limit fail-loud delayed; D-V3-Phase3-A/B/D LOCKED)
- [x] **SSO-02** ✅ Phase 3: Refresh token blacklist Redis chung — central + hub con cùng kết nối 1 Redis instance, blacklist key `auth:blacklist:<jti>` valid cross-process. Hub con KHÔNG sinh refresh token (login + refresh chỉ ở central qua `POST /api/auth/login` central URL). User login 1 lần ở central → JWT valid trên tất cả hub con qua subpath redirect. — **DONE 2026-05-XX** (Plan 03-03 key rename `blacklist:` → `auth:blacklist:` 5 vị trí touch + Plan 03-04 307 redirect hub con login/refresh; integration test cross-process `test_sso_blacklist_cross_process` PASS; D-V3-Phase3-C/G/H LOCKED)
- [x] **SSO-03** ✅ Phase 3: JWT claim `hub_ids: list[str]` reflect user's hub assignments — central verify access cross-hub search dùng `hub_ids` (intersection với requested `hub_ids` body). Hub con verify access local hub bằng so sánh `current_user.hub_ids` chứa `HUB_NAME` của process. JWT issuer URL cố định central (KHÔNG đổi per-hub). — **DONE 2026-05-XX** (Plan 03-03 hub_ids REQUIRED + aud=`["medinet-wiki"]` REQUIRED + PyJWT strict audience check 2 verify path; iss giữ `"medinet-wiki"` RE-CONFIRM D-V3-Phase3-E URL-based defer Phase 7 MCP split)
- [x] **SSO-04** ✅ Phase 3: E4 reinforced — hub con KHÔNG truy cập được data hub khác kể cả khi JWT compromised (DB-level isolation từ TOPO-04 + repository layer vẫn enforce `WHERE hub_id = settings.hub_name`). Test integration: stale JWT chứa `hub_ids=["duoc"]` post tới `medinet_hub_yte` API → 403 (không phải 404 hay data leak). — **DONE 2026-05-XX** (Plan 03-03 dependency mới `get_current_user_for_hub_access` Layer 3 enforce defense-in-depth bên cạnh Layer 1 DB DSN validator Phase 1 + Layer 2 repo M2; integration test `test_stale_jwt_cross_hub_returns_403_envelope` PASS verify 403 CROSS_HUB_ACCESS_DENIED envelope D6)

  > **NOTE Phase 3 closeout (Plan 03-05 SSO-01..04 — 2026-05-XX):** 5 plan ship đầy đủ 4 REQ-ID:
  >
  > 1. Plan 03-01 — JWKS endpoint publish (D-V3-Phase3-A)
  > 2. Plan 03-02 — JWKSCache hub con + lifespan + dependency (D-V3-Phase3-B/D)
  > 3. Plan 03-03 — JWT aud/hub_ids REQUIRED + Redis key rename + E4 reinforced (D-V3-Phase3-E/H + SSO-04)
  > 4. Plan 03-04 — 307 redirect hub con login/refresh + Settings central_url (D-V3-Phase3-G)
  > 5. Plan 03-05 — Closeout docs + smoke checkpoint
  >
  > **Backward incompat:** M2 cũ JWT thiếu kid (Plan 03-02 add header) + aud (Plan 03-03 add claim) + hub_ids (Plan 03-03 REQUIRED) → 401 reject sau deploy. User re-login ~15-30s downtime (Slack/Email broadcast). Xem README.md "SSO Backward Incompat (Phase 3 v3.0)" section deploy steps.
  >
  > **Frontend wire:** Defer Phase 5 PROXY-02 (D-V3-06 D6 expire formally Phase 5 — frontend rewrite `<form action="https://central/api/auth/login">` redirect flow).
  >
  > **v3.0-a progress:** Phase 1+2+3 DONE (3/3 phase v3.0-a). 🚦 v3.0-a EXIT GATE TRIGGERED — user accept demo (1 hub con yte + central + JWT SSO + golden path) → tiếp tục v3.0-b (Phase 4-7).
```

**Step 2: KHÔNG đụng:**
- TOPO-01..04 (Phase 1 done — Plan 01-XX update).
- FACTOR-01..04 (Phase 2 done — Plan 02-XX update + Plan 03-04 note FACTOR-03 extend).
- SYNC/PROXY/SETTINGS/MIGRATE rows (Phase 4-7 chưa start).
- Traceability table cuối file (SSO-01..04 → Phase 3 row đã có sẵn).
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*SSO-0[1-4]\*\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "Plan 03-0[1-5]\|E4 reinforced\|get_current_user_for_hub_access" Hub_All/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\[x\] \*\*SSO-0[1-4]\*\*" Hub_All/.planning/REQUIREMENTS.md` ≥ 4 (4 REQ mark done)
    - `grep -c "Plan 03-01\|Plan 03-02\|Plan 03-03\|Plan 03-04\|Plan 03-05" Hub_All/.planning/REQUIREMENTS.md` ≥ 5
    - `grep -c "auth:blacklist:\|D-V3-Phase3-H" Hub_All/.planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "get_current_user_for_hub_access\|E4 reinforced" Hub_All/.planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "Backward incompat\|kid.*aud.*hub_ids" Hub_All/.planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "v3.0-a EXIT GATE.*TRIGGERED" Hub_All/.planning/REQUIREMENTS.md` ≥ 1
    - TOPO-01..04 + FACTOR-01..04 marks giữ NGUYÊN (Phase 1+2 carry forward): `grep -cE "\[x\] \*\*TOPO-0[1-4]\*\*|\[x\] \*\*FACTOR-0[1-4]\*\*" Hub_All/.planning/REQUIREMENTS.md` ≥ 8
    - Plan 03-04 FACTOR-03 note KHÔNG đụng: `grep -c "10 endpoint handle LOCAL\|2 endpoint SSO REDIRECT" Hub_All/.planning/REQUIREMENTS.md` ≥ 2
  </acceptance_criteria>
  <done>
REQUIREMENTS.md SSO-01..04 mark [x] + note Phase 3 closeout 5 plan + backward incompat + frontend wire defer + v3.0-a EXIT GATE TRIGGERED. KHÔNG đụng TOPO/FACTOR/SYNC/PROXY/SETTINGS/MIGRATE rows.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: README.md — section mới ## SSO Backward Incompat (Phase 3 v3.0) + deploy steps + endpoint mapping</name>
  <files>Hub_All/README.md</files>
  <read_first>
    - Hub_All/README.md (Phase 2 Plan 02-05 đã thêm section "Add a new hub" — Plan 03-05 append section mới sau)
  </read_first>
  <action>
**Step 1: Tìm vị trí phù hợp** trong `Hub_All/README.md` — sau section "Add a new hub" (Plan 02-05) + trước "Milestone status" section.

**Step 2: Append section mới `## SSO Backward Incompat (Phase 3 v3.0)`** với 4 sub-section:

```markdown
## SSO Backward Incompat (Phase 3 v3.0 — 2026-05-XX)

Phase 3 ship SSO infrastructure mới (JWKS endpoint + cache + JWT claim refactor + Redis blacklist key rename + 307 redirect hub con login/refresh). **User cần re-login sau khi deploy.**

### Tại sao re-login?

Plan 03-02 + 03-03 add 3 yêu cầu mới cho JWT:

1. **Header `kid`** (Plan 03-02 D-V3-Phase3-B): JWT phát hành sau Plan 03-02 có header `kid` = SHA-256 8-byte PEM (base64url unpadded, 11 char). Hub con dùng kid để match public key trong JWKSCache. M2 cũ JWT KHÔNG có kid → hub con verify fail `401 INVALID_TOKEN` (`"Token thiếu kid header — JWT phát hành trước Phase 3 SSO, vui lòng đăng nhập lại"`).
2. **Claim `aud=["medinet-wiki"]`** (Plan 03-03 D-V3-Phase3-E): PyJWT strict audience check ở `verify_token` + `verify_token_with_key`. M2 cũ JWT KHÔNG có aud → `401 InvalidAudienceError`.
3. **Claim `hub_ids: list[str]`** REQUIRED (Plan 03-03 D-V3-Phase3-E): JWTClaims pydantic validate REQUIRED (KHÔNG default empty). M2 cũ JWT KHÔNG có hub_ids → `401 Claims không hợp lệ`.

### Deploy steps

1. **Backup database** (M2 baseline carry forward):
   ```bash
   docker exec medinet-postgres pg_dumpall -U medinet > backup-pre-phase3-$(date +%Y%m%d).sql
   ```

2. **Verify M2 JWT keypair còn** (Plan 03-01 reuse — KHÔNG sinh lại):
   ```bash
   ls -la api/keys/private.pem api/keys/public.pem
   # Nếu mất: cd api && make keys  (CẢNH BÁO: rotation = mọi JWT cũ revoke ngay lập tức)
   ```

3. **Build + deploy 4 service**:
   ```bash
   docker compose up -d postgres redis
   docker compose build python-api-central python-api-yte python-api-duoc python-api-hcns
   docker compose up -d python-api-central python-api-yte python-api-duoc python-api-hcns
   ```

4. **Verify JWKS endpoint** (central serve OK):
   ```bash
   curl http://localhost:8180/.well-known/jwks.json | jq .
   # Expect: {"keys":[{"kty":"RSA","kid":"<11-char>","use":"sig","alg":"RS256","n":"...","e":"AQAB"}]}
   ```

5. **Verify hub con strip JWKS** (FACTOR-02 enforce):
   ```bash
   curl -o /dev/null -w "%{http_code}\n" http://localhost:8181/.well-known/jwks.json
   # Expect: 404
   ```

6. **Verify hub con 307 redirect login** (D-V3-Phase3-G):
   ```bash
   curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" \
     -X POST -H "Content-Type: application/json" \
     -d '{"email":"u@m.vn","password":"x"}' \
     http://localhost:8181/api/auth/login
   # Expect: 307 http://python-api-central:8080/api/auth/login
   ```

7. **Broadcast user re-login** (Slack/Email):
   > "Hệ thống Medinet Wiki vừa nâng cấp SSO (Phase 3 v3.0 Multi-Hub Split). Vui lòng đăng xuất + đăng nhập lại để nhận token mới. Phiên hiện tại sẽ tự động hết hạn trong vài phút. Mọi tài liệu đã upload + lịch sử search KHÔNG ảnh hưởng."

### Endpoint mapping mới (Phase 3 Plan 03-04 SSO-02)

| Endpoint | Central (port 8180) | Hub yte/duoc/hcns (port 8181-8183) |
|---|---|---|
| `POST /api/auth/login` | Handle local (M2 path) | **307 Location: `{CENTRAL_URL}/api/auth/login`** |
| `POST /api/auth/refresh` | Handle local (M2 path) | **307 Location: `{CENTRAL_URL}/api/auth/refresh`** |
| `POST /api/auth/logout` | Handle local (blacklist Redis chung) | Handle local (blacklist Redis chung — central thấy ngay) |
| `GET /api/auth/me` | Handle local | Handle local (verify JWT qua JWKSCache + Redis blacklist) |
| `GET /.well-known/jwks.json` | **200 JWK Set RFC 7517** | 404 envelope (FACTOR-02 strip) |

### Cross-hub isolation enforcement (SSO-04 E4 reinforced)

Plan 03-03 thêm dependency `get_current_user_for_hub_access` — hub con check `HUB_NAME in JWT.hub_ids` → reject `403 CROSS_HUB_ACCESS_DENIED` nếu mismatch. Defense-in-depth bên cạnh Phase 1 DB-level DSN validator + M2 repo layer `WHERE hub_id`.

**Example:** Stale JWT compromised từ user duoc (`hub_ids=["duoc"]`) post tới hub yte API:

```bash
# Stale JWT hub_ids=["duoc"] (compromised)
TOKEN="eyJ..."

# Post tới hub yte (port 8181)
curl -H "Authorization: Bearer $TOKEN" http://localhost:8181/api/profile
# Expect: 403 {"success":false,"error":{"code":"CROSS_HUB_ACCESS_DENIED","message":"..."}}
```

### Rollback (nếu cần)

Nếu deploy fail (vd JWKS endpoint 503, hub con boot fail-loud, mass 401 user re-login):

```bash
# 1. Stop services
docker compose down

# 2. Checkout commit trước Phase 3 (xem git log)
git checkout <commit-pre-phase-3-sha>

# 3. Re-build + deploy
docker compose build python-api-central python-api-yte python-api-duoc python-api-hcns
docker compose up -d

# 4. Restore database backup nếu cần (KHÔNG bắt buộc — Phase 3 KHÔNG migration schema)
psql -U medinet -d medinet_central < backup-pre-phase3-<date>.sql
```

Phase 3 KHÔNG Alembic migration schema — rollback chỉ revert source code + restart container. Database `refresh_tokens` table không đổi shape.

### Reference

- `Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-CONTEXT.md` — 8 decision D-V3-Phase3-A..H
- `Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-01..03-05-PLAN.md` — Implementation detail per plan
- `Hub_All/.planning/REQUIREMENTS.md` § SSO-01..04 — REQ-ID spec
- `Hub_All/CLAUDE.md` section 6 — v3.0 progress + Phase 3 pattern subsection
```

**KHÔNG đụng:**
- Section "Add a new hub" (Plan 02-05 FACTOR-04).
- Milestone status section.
- Project overview / Quick start.
- Stack / Architecture section.
  </action>
  <verify>
    <automated>grep -c "SSO Backward Incompat\|JWT.*re-login\|/.well-known/jwks.json\|307 Location\|CROSS_HUB_ACCESS_DENIED" Hub_All/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## SSO Backward Incompat" Hub_All/README.md` ≥ 1
    - `grep -c "JWT.*re-login\|đăng nhập lại" Hub_All/README.md` ≥ 1
    - `grep -c "/.well-known/jwks.json" Hub_All/README.md` ≥ 2 (verify endpoint + endpoint mapping table)
    - `grep -c "307 Location\|307 .*central" Hub_All/README.md` ≥ 2
    - `grep -c "CROSS_HUB_ACCESS_DENIED\|E4 reinforced\|get_current_user_for_hub_access" Hub_All/README.md` ≥ 2
    - `grep -c "Backup database\|backup-pre-phase3\|Rollback" Hub_All/README.md` ≥ 2 (deploy + rollback procedure)
    - `grep -c "kid.*Plan 03-02\|aud.*Plan 03-03\|hub_ids.*Plan 03-03" Hub_All/README.md` ≥ 2 (3 yêu cầu mới explain)
    - `grep -c "v3.0\|Phase 3" Hub_All/README.md` ≥ 3
    - Section "Add a new hub" KHÔNG xoá: `grep -c "## Add a new hub" Hub_All/README.md` ≥ 1
  </acceptance_criteria>
  <done>
README.md section mới `## SSO Backward Incompat (Phase 3 v3.0)` đầy đủ: tại sao re-login (3 yêu cầu mới) + deploy steps (7 step) + endpoint mapping table 5 entry + cross-hub isolation example + rollback procedure + reference. KHÔNG đụng section "Add a new hub" Plan 02-05.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: SMOKE COMPOSE CHECKPOINT — verify 2 service (central + yte) runtime + JWKS endpoint + 307 redirect + golden path</name>
  <what-built>
Plan 03-01..04 ship JWKS endpoint central + hub con cache + JWT claim refactor + Redis blacklist key rename + 307 redirect hub con login/refresh + SSO-04 E4 reinforced dependency. Task 5 verify runtime compose 2 service (central + yte) hoạt động đúng SSO contract trên Docker compose thật.

Theo pattern Phase 2 Plan 02-04 Task 1 + Plan 02-05 Task 3, checkpoint này CÓ THỂ pre-resolved SKIP nếu user không có Docker Desktop local — in-process integration test Plan 03-02/03/04 đã cover semantic (6 integration test + 30+ unit test PASS) + smoke runtime defer Phase 7 MIGRATE-05 full E2E golden path.
  </what-built>
  <how-to-verify>
**Option A: APPROVE (chạy smoke compose)**

Operator chạy 7 step sau (yêu cầu Docker Desktop running):

```bash
cd Hub_All

# 1. Start postgres + redis
docker compose up -d postgres redis
sleep 5

# 2. Build 2 service
docker compose build python-api-central python-api-yte

# 3. Start 2 service
docker compose up -d python-api-central python-api-yte
sleep 15  # đợi lifespan startup (asyncpg pool + redis + cocoindex + JWKS fetch_initial cho yte)

# 4. Verify central JWKS endpoint
echo "=== Central JWKS endpoint ==="
curl -s http://localhost:8180/.well-known/jwks.json | jq .
# Expect: {"keys":[{"kty":"RSA","kid":"<11-char>","use":"sig","alg":"RS256","n":"...","e":"AQAB"}]}

# 5. Verify hub yte strip JWKS (FACTOR-02 carry forward)
echo "=== Hub yte JWKS endpoint (expect 404) ==="
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8181/.well-known/jwks.json
# Expect: 404

# 6. Verify hub yte 307 redirect login (D-V3-Phase3-G)
echo "=== Hub yte login 307 redirect ==="
curl -s -o /dev/null -w "%{http_code} %{redirect_url}\n" \
  -X POST -H "Content-Type: application/json" \
  -d '{"email":"u@m.vn","password":"x"}' \
  http://localhost:8181/api/auth/login
# Expect: 307 http://python-api-central:8080/api/auth/login

# 7. Verify hub yte logs JWKSCache ready + central_only_routers_skipped (observability)
echo "=== Hub yte lifespan logs ==="
docker logs medinet-api-yte 2>&1 | grep -E "central_only_routers_skipped|lifespan_jwks_cache_ready|jwks_cache_updated" | head -5
# Expect:
#   central_only_routers_skipped: hub_name=yte — 9 routers stripped (FACTOR-02 enforce)
#   jwks_cache_updated: kids=['<kid>'] last_refresh_ts=<ts>
#   lifespan_jwks_cache_ready: hub_name=yte url=http://python-api-central:8080/.well-known/jwks.json refresh_interval=3600s

# Cleanup
docker compose down
```

**Bonus (nếu user M2 còn — Plan 03-03 + 03-04 backward incompat):**

```bash
# Login central với user M2 (acceptable nếu user đã re-login post-deploy)
TOKEN=$(curl -s -X POST -H "Content-Type: application/json" \
  -d '{"email":"admin@medinet.vn","password":"<password>"}' \
  http://localhost:8180/api/auth/login | jq -r '.data.access_token')
echo "Token: $TOKEN"

# Verify central /api/auth/me
curl -H "Authorization: Bearer $TOKEN" http://localhost:8180/api/auth/me | jq .
# Expect: 200 {"success":true,"data":{"user":{...},"roles":[...]}}

# Verify hub yte /api/auth/me (JWT pass JWKSCache verify path)
curl -H "Authorization: Bearer $TOKEN" http://localhost:8181/api/auth/me | jq .
# Expect: 200 nếu user có hub_ids chứa "yte" (qua get_current_user JWKSCache verify path)
#         403 CROSS_HUB_ACCESS_DENIED nếu user chỉ có hub khác (E4 reinforced)

# Logout central → blacklist
curl -X POST -H "Authorization: Bearer $TOKEN" http://localhost:8180/api/auth/logout

# Sleep < 1s
sleep 1

# Verify hub yte reject (Redis blacklist propagate)
curl -H "Authorization: Bearer $TOKEN" http://localhost:8181/api/auth/me | jq .
# Expect: 401 TOKEN_REVOKED
```

**Option B: SKIP với rationale (pre-resolved acceptable)**

Rationale 3 lớp evidence cover semantic SSO-01..04:

1. **In-process integration test Plan 03-02/03/04 PASS** — 6 integration test (3 JWKS cache lifecycle + 3 SSO blacklist cross-process) qua TestClient + ASGI lifespan đã verify đầy đủ:
   - JWKS endpoint serve real keypair shape RFC 7517
   - JWKSCache rotate keypair detect kid mismatch
   - JWKSCache background refresh task lifecycle
   - Stale JWT cross-hub 403 CROSS_HUB_ACCESS_DENIED envelope
   - Matching JWT hub accept (SSO-03)
   - Central bypass HUB_NAME check (SSO-04)

2. **Unit test 30+ PASS** — `test_jwks_publish` (9) + `test_jwks_cache` (9) + `test_config_jwks` (8) + `test_jwt_iss_aud_hub_ids` (8) + `test_redis_blacklist_key_schema` (6) + `test_get_current_user_jwks_branch` (8) + `test_auth_router_hub_redirect` (5) — cover JWKS lifecycle + JWT claim + Redis key + dependency branch + 307 redirect.

3. **Static verify chain** — ruff + mypy clean (Plan 03-01..04) + Phase 2 integration regression 10/10 PASS + Phase 1+2 + Plan 03-01..03 unit regression PASS.

Smoke compose runtime defer Phase 7 MIGRATE-05 full E2E golden path (4 service compose + frontend rendering + 3 hub con cross-hub search + ask + citation).

**Option C: FAILED**

Document failure scenario + re-execute hoặc rollback (xem README.md "Rollback" section).
  </how-to-verify>
  <resume-signal>Type `approved` (smoke compose PASS) / `skip smoke` (defer Phase 7) / `failed: <description>` (rollback)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs update | KHÔNG impact runtime — chỉ thông tin |
| Smoke checkpoint runtime | Operator execute 7 step + verify expected output — KHÔNG modify state ngoài Docker volume |
| Backward incompat broadcast | User re-login communicate qua Slack/Email — operator responsibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| **T-03-05-01** | Information Disclosure | README.md "SSO Backward Incompat" section leak `kid` format + JWT claim shape | low | accept | Public information by design (RFC 7517 + RFC 7519). Reveal pattern KHÔNG giúp attacker forge JWT (cần private key). |
| **T-03-05-02** | Repudiation | STATE.md / REQUIREMENTS.md mark `[x]` SSO-01..04 mà KHÔNG có evidence runtime | low | mitigate | Plan 03-05 Task 5 smoke checkpoint REQUIRED (approve / skip với rationale rõ). Evidence chain: 30+ unit test + 6 integration test + static verify chain Plan 03-01..04 + (optional) runtime smoke. |
| **T-03-05-03** | Tampering | Operator deploy Phase 3 nhưng KHÔNG broadcast user re-login → mass 401 confused users | medium | mitigate | README.md "SSO Backward Incompat" deploy step 7 explicit broadcast template. CLAUDE.md section 6 Phase 3 pattern subsection note backward incompat. Operator responsibility (KHÔNG technical mitigation). |
| **T-03-05-04** | Denial of Service | Smoke checkpoint runtime đụng cocoindex Environment singleton race (Phase 2 Plan 02-02 documented) → 2 hub con boot conflict | medium | accept | Layer 2 isolation đã có (LMDB volume per-hub `medinet_cocoindex_{central,yte}` Plan 02-02). Smoke chỉ 2 service runtime — acceptable risk. Operator rollback nếu fail. |
| **T-03-05-05** | Elevation of Privilege | v3.0-a EXIT GATE accept mà KHÔNG verify đầy đủ → user accept v3.0-b nhưng SSO chưa work cross-hub thật | high | mitigate | Plan 03-05 Task 5 smoke checkpoint cover golden path 7 step + Bonus user M2 còn. Integration test Plan 03-02/03/04 cover semantic. User accept dựa trên evidence chain rõ ràng — KHÔNG accept blind. |
</threat_model>

<verification>
**Plan 03-05 verification chain:**

1. **File integrity (grep) — 4 file docs:**
   - `Hub_All/CLAUDE.md` — section 6 v3.0 progress table Phase 3 DONE + Phase 3 pattern subsection + footer
   - `Hub_All/.planning/STATE.md` — frontmatter + Current Position + Phase 3 Planning + Phase 3 Results Summary + Next Action
   - `Hub_All/.planning/REQUIREMENTS.md` — SSO-01..04 mark [x] + note Phase 3 closeout
   - `Hub_All/README.md` — section mới "SSO Backward Incompat (Phase 3 v3.0)"

2. **Acceptance criteria grep verify:**
   - Task 1 CLAUDE.md: ≥ 10 grep PASS
   - Task 2 STATE.md: ≥ 11 grep PASS
   - Task 3 REQUIREMENTS.md: ≥ 7 grep PASS
   - Task 4 README.md: ≥ 8 grep PASS

3. **Markdown structure preserve:**
   - 6 section heading CLAUDE.md unchanged
   - STATE.md YAML frontmatter parse OK
   - REQUIREMENTS.md structure (REQ groups + Traceability table) preserve
   - README.md sections Add new hub + Milestone status preserve

4. **Phase 1+2 docs carry forward:**
   - STATE.md Phase 1+2 Results Summary KHÔNG xoá
   - REQUIREMENTS.md TOPO/FACTOR rows + Plan 02-04/03-04 note WRN-05 + Plan 03-04 FACTOR-03 note giữ
   - CLAUDE.md section 1-5 unchanged

5. **Task 5 smoke checkpoint:**
   - User resume signal `approved` / `skip smoke` / `failed`
   - Evidence chain in-process integration test 6 PASS + unit test 30+ PASS
   - Rollback procedure documented README.md
</verification>

<success_criteria>
- 4 file docs update đầy đủ (CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md)
- SSO-01..04 mark [x] với note Phase 3 closeout 5 plan ship
- Phase 3 Planning Summary + Phase 3 Results Summary table mirror Phase 1+2 pattern
- v3.0-a EXIT GATE TRIGGERED noted ở STATE.md + CLAUDE.md + REQUIREMENTS.md
- README.md SSO Backward Incompat section explicit (3 yêu cầu mới + 7 deploy step + endpoint mapping + cross-hub example + rollback)
- Smoke compose checkpoint Task 5 — user choose approve/skip/failed với evidence chain rõ ràng
- Phase 1+2 docs KHÔNG regress (Results Summary + REQ mark + section heading preserve)
- KHÔNG đụng source code (Plan 03-01..04 đã ship)
- Phase 3 v3.0 Multi-Hub Split SSO closed — ready v3.0-a EXIT GATE user decision
</success_criteria>

<output>
Tạo `.planning/phases/03-auth-sso-hub-ids-jwt/03-05-SUMMARY.md`:
- Plan 03-05 deliverable: 4 file docs update + Task 5 smoke checkpoint result (approve/skip/failed)
- Phase 3 v3.0 closed — 5 plan ship SSO-01..04 đầy đủ
- v3.0-a EXIT GATE TRIGGERED — user decision pending (accept tiếp tục v3.0-b hoặc reject re-discuss D-V3-01)
- Backward incompat user re-login communicated qua README.md
- Frontend wire defer Phase 5 PROXY-02
- Next: User decision v3.0-a EXIT GATE → `/gsd-discuss-phase 4` (accept) hoặc `/gsd-discuss-milestone v3.0` (reject)
</output>
</content>
