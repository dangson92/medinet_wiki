---
phase: 06
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - api/app/auth/api_key.py
  - api/app/auth/dependencies.py
  - api/app/routers/rag_config.py
  - api/app/routers/api_keys.py
  - api/app/schemas/api_keys.py
  - api/tests/unit/test_require_api_key_hub_branch.py
  - api/tests/unit/test_require_internal_auth.py
  - api/tests/unit/test_rag_config_publish_invalidate.py
  - api/tests/unit/test_api_keys_verify_endpoint.py
autonomous: true
requirements: [SETTINGS-02, SETTINGS-03]
tags: [settings-sync, refactor, router-extend, dependency, internal-auth]

must_haves:
  truths:
    - "`require_api_key` dependency branch verify path theo `settings.hub_name`: central=local ApiKeyService / hub con=app.state.api_key_verify_client.verify (HTTP proxy)"
    - "`require_internal_auth` dependency mới ở `api/app/auth/dependencies.py` verify header `X-Internal-Auth` constant-time compare hmac.compare_digest → 401 INTERNAL_AUTH_FAIL"
    - "`update_rag_config` PUT extend publish best-effort `settings:invalidate` channel với payload JSON `{config_key:'rag_config', hub:'*', timestamp:<unix>}`"
    - "`POST /api/api-keys/verify` endpoint MỚI ở `api_keys_router` (central-only mount FACTOR-02) với `require_internal_auth` dep + `VerifyApiKeyRequest` schema body `{api_key: str}` → return `{valid: bool, principal: dict | null}`"
    - "Unit test ≥ 22 PASS (6 require_api_key branch + 6 require_internal_auth + 4 rag_config publish + 6 api_keys/verify endpoint)"
  artifacts:
    - path: "api/app/auth/api_key.py"
      provides: "require_api_key dependency với branch hub_name"
      contains: "settings.hub_name"
    - path: "api/app/auth/dependencies.py"
      provides: "require_internal_auth dependency mới"
      contains: "async def require_internal_auth"
    - path: "api/app/routers/rag_config.py"
      provides: "update_rag_config extend publish invalidate"
      contains: "settings:invalidate"
    - path: "api/app/routers/api_keys.py"
      provides: "POST /api/api-keys/verify endpoint mới"
      contains: "verify_api_key"
    - path: "api/app/schemas/api_keys.py"
      provides: "VerifyApiKeyRequest Pydantic schema"
      contains: "class VerifyApiKeyRequest"
  key_links:
    - from: "api/app/auth/api_key.py"
      to: "Wave 4 lifespan app.state.api_key_verify_client"
      via: "request.app.state attribute"
      pattern: "request.app.state.api_key_verify_client"
    - from: "api/app/auth/dependencies.py::require_internal_auth"
      to: "api/app/routers/api_keys.py::verify_api_key endpoint"
      via: "FastAPI Depends() dependency injection"
      pattern: "Depends(require_internal_auth)"
    - from: "api/app/routers/rag_config.py::update_rag_config"
      to: "Wave 2 settings_subscriber_loop receive event"
      via: "Redis pub/sub channel settings:invalidate"
      pattern: "settings:invalidate"
    - from: "api/app/routers/api_keys.py POST /verify"
      to: "Wave 2 ApiKeyVerifyClient consume from hub con"
      via: "HTTP POST + X-Internal-Auth header"
      pattern: "X-Internal-Auth"
---

<objective>
Wave 3 ship request-response integration của Phase 6 — REFACTOR `require_api_key` dependency branch (central local / hub con proxy via app.state.api_key_verify_client), tạo `require_internal_auth` dependency mới (constant-time secret compare), extend `update_rag_config` publish pub/sub invalidate best-effort fail-open, và add endpoint MỚI `POST /api/api-keys/verify` central-only mount.

**Purpose:** Wave 3 là phần "wiring" giữa M2 endpoint hiện hữu + Phase 6 client. Mỗi REQ-ID end-to-end:
- SETTINGS-02 (pub/sub invalidate): rag_config PUT publish → Wave 2 subscriber consume.
- SETTINGS-03 (apikey verify proxy): require_api_key hub con branch dùng client → POST /api/api-keys/verify central → ApiKeyService.verify_key M2.

File-disjoint với Wave 2 (Wave 2 chỉ tạo settings_sync/ module, Wave 3 sửa auth + routers M2 hiện hữu). Cần Wave 1 (Settings) + Wave 2 (client class exists) ready trước.

**Output:** 4 file source touch + 1 schema mới + 4 test file unit ≥ 22 test PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/06-system-settings-sync/06-CONTEXT.md
@Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md
@Hub_All/.planning/phases/06-system-settings-sync/06-01-PLAN.md
@Hub_All/.planning/phases/06-system-settings-sync/06-02-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- M2 carry forward + Wave 2 client ready -->

From api/app/auth/api_key.py (M2 baseline — current require_api_key):
```python
from fastapi import Depends, Header, HTTPException, status
from app.services.api_key_service import ApiKeyService

async def require_api_key(
    x_api_key: str | None = Header(default=None, alias="X-API-Key"),
    db: AsyncSession = Depends(get_session),
) -> dict[str, Any]:
    if not x_api_key:
        raise HTTPException(401, detail={"code":"API_KEY_MISSING","message":"..."})
    principal = await ApiKeyService(db=db).verify_key(x_api_key)
    if principal is None:
        raise HTTPException(401, detail={"code":"API_KEY_INVALID","message":"..."})
    return principal
```

From api/app/auth/dependencies.py (Phase 3 require_role factory — analog factory pattern KHÔNG dùng cho require_internal_auth direct dependency, nhưng dùng Header() + HTTPException same):
```python
def require_role(*roles: str) -> Callable[[User], Awaitable[User]]:
    async def _dependency(user: User = Depends(get_current_user)) -> User:
        if user.role not in allowed:
            raise HTTPException(403, detail={"code":"FORBIDDEN", ...})
        return user
    return _dependency
```

From api/app/routers/rag_config.py (M2 current — update_rag_config endpoint):
```python
from fastapi import APIRouter, Depends, Request
from fastapi.responses import JSONResponse
from app.services.rag_config_service import RagConfigService

router = APIRouter(prefix="/api/rag-config", tags=["rag-config"])

@router.put("", response_model=None)
async def update_rag_config(
    req: UpdateRagConfigRequest,
    user: User = Depends(require_role("admin")),
    db: AsyncSession = Depends(get_session),
) -> JSONResponse | dict[str, Any]:
    result = await RagConfigService(db=db).update_config(req=req, updated_by=user.id)
    if isinstance(result, str):
        return JSONResponse(status_code=400, content={"error": result})
    return result
```

From api/app/routers/api_keys.py (M2 current — POST revoke pattern analog):
```python
@router.post("/{key_id}/revoke")
async def revoke_api_key(
    key_id: str,
    user: User = Depends(require_role("admin")),
    service: ApiKeyService = Depends(get_api_key_service),
) -> JSONResponse:
    ...
    return resp.ok(data={"message": "API key đã thu hồi"})
```

From api/app/services/api_key_service.py (M2 — ApiKeyService.verify_key shape):
```python
async def verify_key(self, key: str) -> dict[str, Any] | None:
    # Returns principal dict {id, permissions, allowed_hub_ids} or None
```

From api/app/settings_sync/client.py (Plan 06-02 — class ready):
```python
class ApiKeyVerifyClient:
    async def verify(self, api_key: str) -> dict[str, Any] | None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor `require_api_key` branch + new `require_internal_auth` dependency + unit test</name>
  <files>api/app/auth/api_key.py, api/app/auth/dependencies.py, api/tests/unit/test_require_api_key_hub_branch.py, api/tests/unit/test_require_internal_auth.py</files>
  <read_first>
    - api/app/auth/api_key.py (M2 current require_api_key — đọc full file để giữ signature compatible + thêm `request: Request` param)
    - api/app/auth/dependencies.py (Phase 3 require_role factory + get_current_user branch pattern lines 122-212 carry forward)
    - api/app/settings_sync/client.py (Plan 06-02 — ApiKeyVerifyClient.verify signature)
    - api/app/settings_sync/__init__.py (Plan 06-02 — re-export check)
    - api/tests/unit/test_auth_api_key.py (M2 — đọc current test cases để giữ regression)
    - Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md (Wave 3 require_api_key + require_internal_auth section)
  </read_first>
  <behavior>
    **test_require_api_key_hub_branch.py (~6 test):**
    - Test 1: `settings.hub_name="central"` + valid x_api_key → call `ApiKeyService(db).verify_key()` (M2 local) → return principal dict. Mock ApiKeyService.
    - Test 2: `settings.hub_name="yte"` + valid x_api_key → call `request.app.state.api_key_verify_client.verify(x_api_key)` → return principal. Mock client.
    - Test 3: `settings.hub_name="yte"` + `request.app.state.api_key_verify_client = None` → raise HTTPException 503 code `APIKEY_VERIFY_CLIENT_UNAVAILABLE`.
    - Test 4: `settings.hub_name="yte"` + client return None → raise HTTPException 401 code `API_KEY_INVALID` (M2 carry forward).
    - Test 5: Missing X-API-Key header → 401 code `API_KEY_MISSING` (M2 regression — KHÔNG branch).
    - Test 6: `settings.hub_name="central"` + None from ApiKeyService.verify_key → 401 code `API_KEY_INVALID`.

    **test_require_internal_auth.py (~6 test):**
    - Test 1: Header `X-Internal-Auth: <correct_secret>` match → no raise (return None).
    - Test 2: Header `X-Internal-Auth: <wrong_secret>` → raise HTTPException 401 code `INTERNAL_AUTH_FAIL`.
    - Test 3: Missing header → raise HTTPException 401 code `INTERNAL_AUTH_FAIL`.
    - Test 4: Empty header value `""` → raise 401.
    - Test 5: `hmac.compare_digest` called (constant-time mitigation T-06-04-01) — assert via monkeypatch spy.
    - Test 6: Settings env loaded — verify dependency reads `settings.settings_proxy_secret` (not hardcoded).
  </behavior>
  <action>
    **Refactor `api/app/auth/api_key.py::require_api_key`** — Thêm param `request: Request` + branch verify path theo `settings.hub_name`:

    ```python
    """Phase 6 Plan 06-03 SETTINGS-03 (D-V3-Phase6-A) — require_api_key branch verify path.

    Central (hub_name="central"): M2 carry forward local ApiKeyService.verify_key
      (AES-GCM decrypt + key_prefix match — M2 AUX-02 unchanged).
    Hub con (yte/duoc/hcns/dynamic): HTTP proxy qua ApiKeyVerifyClient ở
      app.state.api_key_verify_client (Plan 06-02 client + Plan 06-04 lifespan).
    """
    from __future__ import annotations

    from typing import Any

    from fastapi import Depends, Header, HTTPException, Request, status
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.config import get_settings
    from app.db.session import get_session
    from app.services.api_key_service import ApiKeyService


    async def require_api_key(
        request: Request,
        x_api_key: str | None = Header(default=None, alias="X-API-Key"),
        db: AsyncSession = Depends(get_session),
    ) -> dict[str, Any]:
        """Phase 6 Plan 06-03 — branch verify path theo hub_name."""
        if not x_api_key:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={
                    "code": "API_KEY_MISSING",
                    "message": "Thiếu header X-API-Key",
                },
            )

        settings = get_settings()
        if settings.hub_name == "central":
            # Central: M2 local verify carry forward (AES-GCM decrypt + match).
            principal = await ApiKeyService(db=db).verify_key(x_api_key)
        else:
            # Hub con: proxy qua ApiKeyVerifyClient (D-V3-Phase6-A LOCKED).
            client = getattr(request.app.state, "api_key_verify_client", None)
            if client is None:
                raise HTTPException(
                    status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
                    detail={
                        "code": "APIKEY_VERIFY_CLIENT_UNAVAILABLE",
                        "message": (
                            "ApiKeyVerifyClient chưa init — hub con boot fail? "
                            "Plan 06-04 lifespan SETTINGS_SKIP_FETCH=1 test mode."
                        ),
                    },
                )
            principal = await client.verify(x_api_key)

        if principal is None:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={
                    "code": "API_KEY_INVALID",
                    "message": "API key không hợp lệ hoặc đã thu hồi",
                },
            )
        return principal
    ```

    **Thêm `require_internal_auth` vào `api/app/auth/dependencies.py`** — Đặt SAU cluster `require_role` factory:

    ```python
    async def require_internal_auth(
        x_internal_auth: str | None = Header(default=None, alias="X-Internal-Auth"),
    ) -> None:
        """Phase 6 Plan 06-03 SETTINGS-03 (D-V3-Phase6-D) — Internal-only endpoint gate.

        Verify header `X-Internal-Auth: <settings_proxy_secret>` constant-time compare
        hmac.compare_digest. 401 INTERNAL_AUTH_FAIL nếu mismatch hoặc thiếu.

        Dùng cho POST /api/api-keys/verify (Plan 06-03 Task 3 — hub con call central
        proxy). KHÔNG expose ra public internet (Caddy block /api/api-keys/verify
        nếu cần — review Plan 05-01 caddy config nếu deploy public-facing).

        Threat model:
        - T-06-03-01 Tampering — secret leak → attacker bypass.
          hmac.compare_digest tránh timing attack (secret entropy ≥ 128-bit Plan 06-01).
        """
        import hmac

        from app.config import get_settings

        settings = get_settings()
        expected = settings.settings_proxy_secret
        if not x_internal_auth or not hmac.compare_digest(
            x_internal_auth, expected
        ):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail={
                    "code": "INTERNAL_AUTH_FAIL",
                    "message": "Internal auth header missing or invalid",
                },
            )
    ```

    **Test files:** 2 file riêng (6 + 6 = 12 test). Mock `get_settings()` qua `monkeypatch.setattr("app.auth.api_key.get_settings", lambda: mock_settings)`. Mock `Request` qua `MagicMock(app=MagicMock(state=MagicMock(api_key_verify_client=...)))`.

    **Regression note (Rule 3):** Test M2 hiện hữu `test_auth_api_key.py` (nếu exists) call `require_api_key(x_api_key=..., db=...)` thiếu `request` param sẽ FAIL. Update fixture truyền `request=MagicMock(app=MagicMock(state=MagicMock()))` HOẶC dùng FastAPI `TestClient` end-to-end. Pattern carry forward Plan 03-02 Rule 3 fixture update.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_require_api_key_hub_branch.py tests/unit/test_require_internal_auth.py -v --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "settings.hub_name" api/app/auth/api_key.py >= 1`.
    - `grep -c "api_key_verify_client" api/app/auth/api_key.py >= 1`.
    - `grep -c "APIKEY_VERIFY_CLIENT_UNAVAILABLE" api/app/auth/api_key.py >= 1`.
    - `grep -c "request: Request" api/app/auth/api_key.py >= 1` (signature đã extend).
    - `grep -c "async def require_internal_auth" api/app/auth/dependencies.py >= 1`.
    - `grep -c "hmac.compare_digest" api/app/auth/dependencies.py >= 1`.
    - `grep -c "INTERNAL_AUTH_FAIL" api/app/auth/dependencies.py >= 1`.
    - `grep -c "settings_proxy_secret" api/app/auth/dependencies.py >= 1`.
    - Test files exist với `grep -c "def test_" >= 6` mỗi file.
    - pytest exit 0 với "12 passed".
    - Regression `uv run pytest tests/unit/ -k "auth" --no-header` PASS (KHÔNG break M2 auth test + Phase 3 SSO test).
    - `uv run ruff check api/app/auth/api_key.py api/app/auth/dependencies.py` exit 0.
    - `uv run mypy --strict api/app/auth/api_key.py api/app/auth/dependencies.py` exit 0.
  </acceptance_criteria>
  <done>require_api_key branch verify path + require_internal_auth dep với constant-time compare. 12 unit test PASS + regression auth test cluster KHÔNG break.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `update_rag_config` publish + add `POST /api/api-keys/verify` + unit test</name>
  <files>api/app/routers/rag_config.py, api/app/routers/api_keys.py, api/app/schemas/api_keys.py, api/tests/unit/test_rag_config_publish_invalidate.py, api/tests/unit/test_api_keys_verify_endpoint.py</files>
  <read_first>
    - api/app/routers/rag_config.py (M2 current — đọc `update_rag_config` PUT endpoint full để biết vị trí insert publish call sau service.update_config success)
    - api/app/routers/api_keys.py (M2 current — đọc full router để biết APIRouter prefix + tags + chỗ insert POST /verify; check tags để dùng đúng)
    - api/app/schemas/api_keys.py (M2 — đọc current schema để add VerifyApiKeyRequest đúng style)
    - api/app/services/api_key_service.py (M2 — đọc verify_key return shape để verify endpoint trả đúng `{valid, principal}` structure)
    - api/app/auth/dependencies.py (Plan 06-03 Task 1 — require_internal_auth ready)
    - api/app/settings_sync/keys.py (Plan 06-01 — SETTINGS_INVALIDATE_CHANNEL constant)
    - Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md (Wave 3 rag_config publish + api_keys/verify section)
  </read_first>
  <behavior>
    **test_rag_config_publish_invalidate.py (~4 test):**
    - Test 1: PUT /api/rag-config success + `request.app.state.redis` is mock async → assert `redis.publish("settings:invalidate", json_payload)` called với payload contains `"config_key":"rag_config"` + `"hub":"*"` + `"timestamp":<int>`.
    - Test 2: PUT success + `request.app.state.redis = None` → KHÔNG raise + log warning (best-effort fail-open).
    - Test 3: PUT success + redis.publish raise ConnectionError → KHÔNG block PUT response 200 (fail-open).
    - Test 4: PUT fail (service returns error string) → KHÔNG publish (early return 400).

    **test_api_keys_verify_endpoint.py (~6 test):**
    - Test 1: POST /api/api-keys/verify với header `X-Internal-Auth: <correct>` + body `{api_key:"mdk_valid"}` → 200 + `{valid: true, principal: {...}}` (mock ApiKeyService.verify_key return dict).
    - Test 2: Header `X-Internal-Auth: <wrong>` → 401 INTERNAL_AUTH_FAIL (require_internal_auth dep enforce).
    - Test 3: Missing `X-Internal-Auth` header → 401.
    - Test 4: Correct header + body `{api_key:"invalid"}` → 200 + `{valid: false, principal: null}` (verify_key return None).
    - Test 5: Missing `api_key` field trong body → 422 (Pydantic validation).
    - Test 6: Endpoint chỉ available khi `settings.hub_name="central"` — strip ở hub con (FACTOR-02 carry forward; integration test wave 4 verify, unit test verify router include conditional logic ở main.py hoặc skip).
  </behavior>
  <action>
    **Extend `api/app/routers/rag_config.py::update_rag_config`** — Thêm publish best-effort sau success:

    ```python
    import json
    import logging
    import time
    from fastapi import APIRouter, Depends, Request
    # ... existing imports ...

    logger = logging.getLogger(__name__)

    @router.put("", response_model=None)
    async def update_rag_config(
        request: Request,  # MỚI — access app.state.redis (Plan 06-03 SETTINGS-02)
        req: UpdateRagConfigRequest,
        user: User = Depends(require_role("admin")),
        db: AsyncSession = Depends(get_session),
    ) -> JSONResponse | dict[str, Any]:
        """PUT /api/rag-config — update + hot-swap + Phase 6 publish pub/sub invalidate.

        Phase 6 Plan 06-03 SETTINGS-02 (D-V3-Phase6-C): publish best-effort fail-open
        sau service.update_config() success. Redis None / down → log warning KHÔNG
        block PUT (TTL natural fallback E-V3-4 < 30s threshold).
        """
        result = await RagConfigService(db=db).update_config(
            req=req, updated_by=user.id
        )
        if isinstance(result, str):
            return JSONResponse(status_code=400, content={"error": result})

        # Phase 6 Plan 06-03 SETTINGS-02 — publish best-effort fail-open
        redis = getattr(request.app.state, "redis", None)
        if redis is not None:
            try:
                payload = json.dumps({
                    "config_key": "rag_config",
                    "hub": "*",  # broadcast all hub con
                    "timestamp": int(time.time()),
                })
                await redis.publish("settings:invalidate", payload)
                logger.info(
                    "settings_invalidate_published: config_key=rag_config hub=*"
                )
            except Exception as e:  # noqa: BLE001 — best-effort fail-open
                logger.warning("settings_invalidate_publish_failed: %s", e)

        return result
    ```

    **Add Pydantic schema `api/app/schemas/api_keys.py`**:

    ```python
    # Append (giữ existing class CreateApiKeyRequest, etc.)

    class VerifyApiKeyRequest(BaseModel):
        """Phase 6 Plan 06-03 SETTINGS-03 (D-V3-Phase6-D) — body POST /api/api-keys/verify."""

        api_key: str = Field(..., min_length=1, max_length=256)
    ```

    **Add endpoint `api/app/routers/api_keys.py POST /verify`** — Insert SAU cluster POST `/{key_id}/revoke`:

    ```python
    from app.auth.dependencies import require_internal_auth
    from app.schemas.api_keys import VerifyApiKeyRequest

    @router.post("/verify", response_model=None)
    async def verify_api_key(
        req: VerifyApiKeyRequest,
        _internal: None = Depends(require_internal_auth),  # MỚI gate internal
        service: ApiKeyService = Depends(get_api_key_service),
    ) -> dict[str, Any]:
        """POST /api/api-keys/verify — Internal proxy cho hub con verify X-API-Key.

        Phase 6 Plan 06-03 SETTINGS-03 (D-V3-Phase6-D). Endpoint MỚI central-only
        mount (FACTOR-02 carry forward — api_keys_router central-only ở main.py).

        Body: `{"api_key": "mdk_..."}` (Pydantic VerifyApiKeyRequest).
        Header: `X-Internal-Auth: <settings_proxy_secret>` (require_internal_auth dep).
        Response: `{valid: bool, principal: dict | null}` raw dict (KHÔNG envelope —
        hub con ApiKeyVerifyClient parse raw, pattern song song /api/rag-config M2).

        Central giữ AES-GCM at-rest M2 AUX-02 (KHÔNG đụng verify_key private logic).
        """
        principal = await service.verify_key(req.api_key)
        if principal is None:
            return {"valid": False, "principal": None}
        return {"valid": True, "principal": principal}
    ```

    **Conditional mount note:** Endpoint mount qua existing `api_keys_router` central-only (FACTOR-02 Phase 2 — `api/app/main.py::create_app` check `if settings.hub_name == "central":` block include `api_keys_router`). KHÔNG cần thay đổi main.py.

    **Test files:** 2 file. Test 1: Mock app.state.redis qua FastAPI override + `AsyncMock.publish`. Test 2: Mock `require_internal_auth` qua FastAPI dependency override + mock ApiKeyService.verify_key qua override `get_api_key_service`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_rag_config_publish_invalidate.py tests/unit/test_api_keys_verify_endpoint.py -v --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "settings:invalidate" api/app/routers/rag_config.py >= 1`.
    - `grep -c "config_key.*rag_config" api/app/routers/rag_config.py >= 1`.
    - `grep -c "request: Request" api/app/routers/rag_config.py >= 1` (param mới).
    - `grep -c "best-effort fail-open\|noqa: BLE001" api/app/routers/rag_config.py >= 1`.
    - `grep -c "class VerifyApiKeyRequest" api/app/schemas/api_keys.py >= 1`.
    - `grep -c "async def verify_api_key" api/app/routers/api_keys.py >= 1`.
    - `grep -c "/verify" api/app/routers/api_keys.py >= 1` (path).
    - `grep -c "require_internal_auth" api/app/routers/api_keys.py >= 1`.
    - `grep -c "VerifyApiKeyRequest" api/app/routers/api_keys.py >= 1`.
    - Test files exist với `grep -c "def test_" >= 4` (rag_config) và `>= 6` (api_keys).
    - pytest exit 0 với "10 passed" tổng.
    - Regression `uv run pytest tests/unit/ -k "rag_config or api_keys" --no-header` PASS (KHÔNG break M2 test).
    - `uv run ruff check api/app/routers/rag_config.py api/app/routers/api_keys.py api/app/schemas/api_keys.py` exit 0.
    - `uv run mypy --strict api/app/routers/rag_config.py api/app/routers/api_keys.py api/app/schemas/api_keys.py` exit 0.
  </acceptance_criteria>
  <done>update_rag_config publish settings:invalidate best-effort + POST /api/api-keys/verify endpoint mới với require_internal_auth gate. 10 unit test PASS + regression M2 KHÔNG break.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hub con request → require_api_key | x_api_key plaintext qua Header — Phase 6 KHÔNG đụng (M2 AUX-02 protocol baseline); hub con verify proxy giảm plaintext storage cục bộ. |
| require_api_key → app.state.api_key_verify_client | Dependency-injected via request.app.state (initialized Wave 4 lifespan); null check 503 fail-loud thay vì silent fall through. |
| hub con HTTP → POST /api/api-keys/verify | Body plaintext api_key + header X-Internal-Auth (32-char secret); intra-network medinet_net Docker only. |
| central PUT /api/rag-config → Redis publish | Best-effort fail-open: Redis down KHÔNG block admin write (Pitfall pattern carry forward search_cache.py M2 publish_invalidate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | `X-Internal-Auth` secret leak / brute force | mitigate | hmac.compare_digest constant-time + secret length ≥ 32 char (128-bit entropy Plan 06-01 validator); rate limit 100 req/s per IP defer follow-up plan nếu cần (Plan 06-XX rate_limit_internal Settings field — defer Phase 7). |
| T-06-03-02 | Elevation of Privilege | hub con bypass require_api_key | mitigate | Wave 4 lifespan fail-loud nếu ApiKeyVerifyClient init fail → uvicorn exit 1; runtime `app.state.api_key_verify_client = None` → 503 (KHÔNG silent fallback local DB). |
| T-06-03-03 | Information Disclosure | api_key plaintext qua POST body intra-network | accept | Docker network medinet_net isolated; mTLS / encrypted body defer v4.0 HARD-V4-05 production hardening. |
| T-06-03-04 | DoS | publish_invalidate Redis hang block admin PUT | mitigate | try/except wrap publish + log warning (KHÔNG raise) — best-effort fail-open pattern carry forward search_cache.py M2; Redis.publish non-blocking. |
| T-06-03-05 | Spoofing | api_keys/verify endpoint expose public internet | mitigate | FACTOR-02 carry forward — `api_keys_router` central-only mount; require_internal_auth dep enforce secret check; review Plan 05-01 Caddy /api/api-keys/* route gate nếu public-facing deploy. |
| T-06-03-06 | Tampering | rag_config publish payload manipulation | mitigate | publish payload JSON encode server-side (KHÔNG user input pass-through); Wave 2 subscriber Pydantic `InvalidateMessage` validate Literal enum reject (defense in depth). |
| T-06-03-07 | Repudiation | api_keys verify audit log missing | accept | M2 AUX-02 audit log key creation/revoke; verify call high-frequency log volume defer Phase 7 audit log granular OR Prometheus counter `APIKEY_VERIFY_TOTAL` đủ debug (Plan 06-01 W7). |
</threat_model>

<verification>
**Wave 3 verify cả 2 task PASS:**

```bash
cd Hub_All/api
uv run pytest tests/unit/test_require_api_key_hub_branch.py tests/unit/test_require_internal_auth.py tests/unit/test_rag_config_publish_invalidate.py tests/unit/test_api_keys_verify_endpoint.py -v --no-header
# Expected: 22 passed (6 + 6 + 4 + 6)

# Regression M2 + Phase 3+4 auth + routers
uv run pytest tests/unit/ -k "auth or rag_config or api_keys or sso" --no-header 2>&1 | tail -10
# Expected: tất cả PASS — KHÔNG regression
```
</verification>

<success_criteria>
- `require_api_key` dependency branch verify path central/hub con với 503 fail-loud nếu client missing.
- `require_internal_auth` dependency mới với constant-time hmac.compare_digest secret check.
- `update_rag_config` PUT extend publish `settings:invalidate` best-effort fail-open sau success.
- `POST /api/api-keys/verify` endpoint mới central-only mount (FACTOR-02 carry forward) với require_internal_auth + VerifyApiKeyRequest schema.
- ≥ 22 unit test PASS (6 + 6 + 4 + 6).
- Regression M2 auth + Phase 3 SSO + Phase 4 sync + Phase 5 PROXY auth test KHÔNG break.
- ruff + mypy --strict clean trên 5 file source modified.
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-settings-sync/06-03-SUMMARY.md` ghi:
- Files modified với line count + before/after.
- 22 unit test PASS detail.
- Regression status (M2 auth + Phase 3+4+5 unit test cluster).
- Deviation nếu có.
- Next: Wave 4 (Plan 06-04) lifespan integration BLOCKING depend Wave 1+2+3.
</output>
