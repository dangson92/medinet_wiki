---
phase: 06
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - api/app/settings_sync/client.py
  - api/app/settings_sync/subscriber.py
  - api/app/settings_sync/__init__.py
  - api/tests/unit/test_settings_sync_client.py
  - api/tests/unit/test_settings_sync_subscriber.py
autonomous: true
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-04]
tags: [settings-sync, httpx-client, redis-pubsub, asyncio-task]

must_haves:
  truths:
    - "Module `api/app/settings_sync/client.py` có 3 class `RagConfigClient` + `HubRegistryClient` + `ApiKeyVerifyClient` với `fetch_initial()` blocking + `get/list_hubs/verify` hot path"
    - "Module `api/app/settings_sync/subscriber.py` có `settings_subscriber_loop(redis, hub_name, reconnect_seconds)` async loop subscribe `settings:invalidate` channel"
    - "Pydantic `InvalidateMessage` schema validate payload qua `config_key: Literal['rag_config','hub_registry','apikey']` + `hub: str` + `key_id: str | None` + `timestamp: int`"
    - "Subscriber phân nhánh 3 config_key flush đúng cache key (rag_config per-hub / hub_registry singleton / apikey hash hoặc full SCAN)"
    - "Subscriber reconnect logic on ConnectionError → asyncio.sleep + retry (KHÔNG fail-loud)"
    - "SettingsUnavailableError exception class fail-loud khi Redis miss + central down"
    - "Unit test ≥ 20 PASS (8 client + 8 subscriber + 4 keys/metrics already from 06-01)"
  artifacts:
    - path: "api/app/settings_sync/client.py"
      provides: "3 HTTP client class + SettingsUnavailableError"
      contains: "class RagConfigClient"
    - path: "api/app/settings_sync/subscriber.py"
      provides: "settings_subscriber_loop asyncio task"
      contains: "async def settings_subscriber_loop"
    - path: "api/app/settings_sync/__init__.py"
      provides: "Re-export Wave 2 classes + InvalidateMessage + SettingsUnavailableError"
      contains: "RagConfigClient"
    - path: "api/tests/unit/test_settings_sync_client.py"
      provides: "≥8 unit test mock httpx + mock redis"
      contains: "test_rag_config_client_fetch_initial"
    - path: "api/tests/unit/test_settings_sync_subscriber.py"
      provides: "≥8 unit test mock redis pubsub + payload parse"
      contains: "test_subscriber_parses_valid_payload"
  key_links:
    - from: "api/app/settings_sync/client.py"
      to: "api/app/settings_sync/keys.py + metrics.py"
      via: "import constants + collectors từ Wave 1 scaffold"
      pattern: "from app.settings_sync.keys import"
    - from: "api/app/settings_sync/subscriber.py"
      to: "Redis pub/sub channel SETTINGS_INVALIDATE_CHANNEL"
      via: "redis.pubsub().subscribe()"
      pattern: "SETTINGS_INVALIDATE_CHANNEL"
    - from: "api/app/settings_sync/client.py"
      to: "Wave 3 require_api_key refactor + Wave 4 lifespan integration"
      via: "module-level class import + app.state.<X>_client storage"
      pattern: "from app.settings_sync.client import"
---

<objective>
Wave 2 ship business logic core của Phase 6 — 3 HTTP client class (`RagConfigClient` + `HubRegistryClient` + `ApiKeyVerifyClient`) với httpx async + Redis cache + Prometheus metric emit + fail-loud lifecycle, và `settings_subscriber_loop` asyncio task subscribe `settings:invalidate` channel với Pydantic payload validate + 3 config_key flush branch + reconnect retry.

**Purpose:** 3 client + 1 subscriber là core machinery Phase 6. Wave 1 scaffold (keys + metrics + Settings) đã ready; Wave 2 implement consumer logic. File-disjoint với Wave 3 (Wave 3 sửa router/dependencies/auth, Wave 2 chỉ touch settings_sync/ module + test mới) — có thể chạy parallel-able với Wave 3 nếu cần.

**Output:** `client.py` + `subscriber.py` ready để Wave 4 lifespan integrate. ≥16 unit test mới PASS với mock httpx + mock Redis fixture pattern carry forward từ `test_jwks_cache.py` Phase 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/phases/06-system-settings-sync/06-CONTEXT.md
@Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md
@Hub_All/.planning/phases/06-system-settings-sync/06-01-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Wave 1 scaffold ready từ Plan 06-01 -->

From api/app/settings_sync/keys.py (Plan 06-01):
```python
SETTINGS_INVALIDATE_CHANNEL: str = "settings:invalidate"
RAG_CONFIG_KEY_PREFIX: str = "settings:rag_config:"
HUB_REGISTRY_KEY: str = "settings:hub_registry"
APIKEY_VERIFY_KEY_PREFIX: str = "apikey:verify:"

def make_apikey_cache_key(api_key: str) -> str: ...
```

From api/app/settings_sync/metrics.py (Plan 06-01):
```python
SETTINGS_CACHE_HIT_TOTAL: Counter   # labels: (hub_name, key_type)
SETTINGS_CACHE_MISS_TOTAL: Counter
SETTINGS_PULL_LATENCY_SECONDS: Histogram  # labels: (hub_name, endpoint)
SETTINGS_INVALIDATE_RECEIVED_TOTAL: Counter
SETTINGS_STALE_SECONDS: Gauge
APIKEY_VERIFY_TOTAL: Counter  # labels: (hub_name, result)
```

From api/app/auth/jwks.py::JWKSCache (Plan 03-02 analog — httpx async + fetch_initial blocking + background refresh + in-process cache + lifecycle):
```python
class JWKSCache:
    def __init__(self, jwks_url, *, refresh_interval=3600, max_stale_seconds=86400, timeout=5.0): ...
    async def fetch_initial(self) -> None:
        try:
            await self._fetch_and_cache()
        except Exception as e:
            logger.critical("jwks_fetch_initial_failed: ...")
            raise

    async def _fetch_and_cache(self) -> None:
        async with httpx.AsyncClient(timeout=self._timeout) as client:
            resp = await client.get(self._jwks_url)
            resp.raise_for_status()
            data = resp.json()
```

From api/app/services/search_cache.py::search_cache_subscriber (M2 analog — pub/sub subscriber loop):
```python
async def search_cache_subscriber(redis: Any) -> None:
    pubsub = redis.pubsub()
    try:
        await pubsub.psubscribe(INVALIDATE_CHANNEL_PATTERN)
        async for message in pubsub.listen():
            if message is None or message.get("type") != "pmessage":
                continue
            channel = message.get("channel", "")
            parts = channel.split(":")
            if len(parts) == 3 and parts[0] == "hub" and parts[2] == "invalidate":
                await invalidate_hub(redis, parts[1])
    except asyncio.CancelledError:
        logger.info("search_cache_subscriber_cancelled")
        raise
    except Exception as e:
        logger.warning("search_cache_subscriber_error: %s", e)
    finally:
        try:
            await pubsub.aclose()
        except Exception:
            pass
```

From api/tests/unit/test_jwks_cache.py (Plan 03-02 mock httpx fixture):
```python
def _make_mock_httpx_get(response_data: dict, status_code: int = 200) -> MagicMock:
    mock_resp = MagicMock()
    mock_resp.status_code = status_code
    mock_resp.json = MagicMock(return_value=response_data)
    if status_code >= 400:
        mock_resp.raise_for_status = MagicMock(
            side_effect=httpx.HTTPStatusError("mock", request=MagicMock(), response=mock_resp)
        )
    else:
        mock_resp.raise_for_status = MagicMock()
    mock_get = AsyncMock(return_value=mock_resp)
    mock_client = MagicMock()
    mock_client.__aenter__ = AsyncMock(return_value=mock_client)
    mock_client.__aexit__ = AsyncMock(return_value=None)
    mock_client.get = mock_get
    return mock_client
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `api/app/settings_sync/client.py` 3 HTTP client class + unit test</name>
  <files>api/app/settings_sync/client.py, api/app/settings_sync/__init__.py, api/tests/unit/test_settings_sync_client.py</files>
  <read_first>
    - api/app/auth/jwks.py (lines bao quanh class JWKSCache — Plan 03-02 fetch_initial + _fetch_and_cache + refresh task pattern)
    - api/app/settings_sync/keys.py (Plan 06-01 — RAG_CONFIG_KEY_PREFIX, HUB_REGISTRY_KEY, APIKEY_VERIFY_KEY_PREFIX, make_apikey_cache_key)
    - api/app/settings_sync/metrics.py (Plan 06-01 — 6 collector)
    - api/app/services/rag_config_service.py (M2 — đọc shape `get_config()` return dict để client parse đúng)
    - api/app/services/api_key_service.py (M2 — đọc shape `verify_key()` return dict để client emulate)
    - api/tests/unit/test_jwks_cache.py (Plan 03-02 mock httpx fixture `_make_mock_httpx_get` reuse)
    - Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md (Wave 2 client.py adapt 3 class section)
  </read_first>
  <behavior>
    **test_settings_sync_client.py (~10 test):**

    *RagConfigClient (4 test):*
    - Test 1: `fetch_initial()` happy path — mock httpx GET `{central_url}/api/rag-config` → 200 + JSON `{provider:"openai", model:"gpt-4", dimensions:1536}` → assert Redis `.setex(key="settings:rag_config:yte", time=60, value=json.dumps(...))` called → assert `SETTINGS_CACHE_MISS_TOTAL.labels("yte","rag_config")` inc.
    - Test 2: `fetch_initial()` httpx timeout → raise `SettingsUnavailableError` (boot fail-loud).
    - Test 3: `get()` cache hit — mock Redis `.get(key)` return bytes JSON → assert KHÔNG httpx call → assert `SETTINGS_CACHE_HIT_TOTAL.labels(...,"rag_config")` inc + return dict.
    - Test 4: `get()` cache miss + central 500 + Redis empty → raise `SettingsUnavailableError` envelope.

    *HubRegistryClient (3 test):*
    - Test 5: `fetch_initial()` happy — mock httpx GET `{central_url}/api/hubs` → 200 + JSON list 4 hub → assert Redis `.setex(key="settings:hub_registry", time=300, ...)` called (singleton, KHÔNG per-hub).
    - Test 6: `list_hubs()` cache hit returns parsed list.
    - Test 7: `fetch_initial()` 401 unauthorized (central reject) → raise `SettingsUnavailableError`.

    *ApiKeyVerifyClient (3 test):*
    - Test 8: `verify("mdk_abc")` cache miss → mock httpx POST `{central_url}/api/api-keys/verify` body `{"api_key":"mdk_abc"}` header `X-Internal-Auth: <secret>` → 200 + `{valid:true, principal:{...}}` → assert Redis `.setex(key="apikey:verify:<sha256[:16]>", time=60, value=json.dumps(principal))` + `APIKEY_VERIFY_TOTAL.labels("yte","valid")` inc + return principal.
    - Test 9: `verify("bad_key")` cache miss → 401 from central → return None + `APIKEY_VERIFY_TOTAL.labels("yte","invalid")` inc + KHÔNG cache negative (assert Redis setex NOT called).
    - Test 10: `verify("mdk_abc")` cache hit (Redis returns bytes JSON) → assert KHÔNG httpx call + `APIKEY_VERIFY_TOTAL.labels("yte","cached")` inc + return cached principal.
  </behavior>
  <action>
    **File `api/app/settings_sync/client.py`** (~280-350 LOC):

    ```python
    """Phase 6 settings_sync — 3 HTTP client class cho hub con (D-V3-Phase6-A LOCKED).

    - RagConfigClient: GET {central_url}/api/rag-config + Redis cache TTL 60s.
    - HubRegistryClient: GET {central_url}/api/hubs + Redis cache TTL 300s.
    - ApiKeyVerifyClient: POST {central_url}/api/api-keys/verify + Redis cache TTL 60s
      + header X-Internal-Auth=<settings_proxy_secret> (D-V3-Phase6-D).

    Lifecycle song song Phase 3 JWKSCache:
    - fetch_initial(): blocking 5s — raise SettingsUnavailableError on fail (boot fail-loud).
    - get/list_hubs/verify(): hot path — Redis cache hit > miss > HTTP fetch > Redis setex.

    Threat model:
    - T-06-02-01 Information Disclosure: apikey cache key hash (KHONG plaintext) qua
      make_apikey_cache_key (T-06-04-02 carry forward).
    - T-06-02-02 DoS: fetch_initial timeout 5s; refresh task timeout 10s read relax.
    - T-06-02-03 Tampering: ApiKeyVerifyClient send X-Internal-Auth header secret
      (constant-time compare ở central — Plan 06-03 require_internal_auth dep).
    """
    from __future__ import annotations

    import json
    import logging
    import time
    from typing import Any

    import httpx

    from app.settings_sync.keys import (
        APIKEY_VERIFY_KEY_PREFIX,
        HUB_REGISTRY_KEY,
        RAG_CONFIG_KEY_PREFIX,
        make_apikey_cache_key,
    )
    from app.settings_sync.metrics import (
        APIKEY_VERIFY_TOTAL,
        SETTINGS_CACHE_HIT_TOTAL,
        SETTINGS_CACHE_MISS_TOTAL,
        SETTINGS_PULL_LATENCY_SECONDS,
        SETTINGS_STALE_SECONDS,
    )

    logger = logging.getLogger(__name__)

    _DEFAULT_TIMEOUT_INITIAL = httpx.Timeout(connect=2.0, read=5.0)
    _DEFAULT_TIMEOUT_REFRESH = httpx.Timeout(connect=5.0, read=10.0)


    class SettingsUnavailableError(RuntimeError):
        """Phase 6 SETTINGS-01 — fail-loud khi central down + Redis cache empty/expired.

        Raised qua RagConfigClient.get() + HubRegistryClient.list_hubs() khi:
        1. Redis cache miss (TTL expire HOAC chua fetch_initial).
        2. httpx fetch central fail (timeout / 5xx / connection).
        ErrorHandlerMiddleware wrap thanh 503 envelope code=SETTINGS_UNAVAILABLE.
        """


    class RagConfigClient:
        """SETTINGS-01 — fetch rag_config tu central + cache Redis TTL 60s."""

        def __init__(
            self,
            *,
            central_url: str,
            redis: Any,
            hub_name: str,
            ttl: int = 60,
        ) -> None:
            self._central_url = central_url.rstrip("/")
            self._redis = redis
            self._hub_name = hub_name
            self._ttl = ttl
            self._cache_key = f"{RAG_CONFIG_KEY_PREFIX}{hub_name}"
            self._endpoint = "/api/rag-config"
            self._last_pull_ts: float = 0.0

        async def fetch_initial(self) -> None:
            """Blocking fetch — raise SettingsUnavailableError on fail (boot fail-loud)."""
            try:
                await self._fetch_and_cache(timeout=_DEFAULT_TIMEOUT_INITIAL)
            except Exception as e:
                logger.critical(
                    "rag_config_client_fetch_initial_failed: hub=%s url=%s%s err=%s",
                    self._hub_name, self._central_url, self._endpoint, e,
                )
                raise SettingsUnavailableError(
                    f"RagConfigClient fetch_initial failed: {e}"
                ) from e

        async def get(self) -> dict[str, Any]:
            """Hot path — Redis cache hit > miss > HTTP fetch > Redis setex."""
            if self._redis is not None:
                cached = await self._redis.get(self._cache_key)
                if cached is not None:
                    SETTINGS_CACHE_HIT_TOTAL.labels(
                        hub_name=self._hub_name, key_type="rag_config"
                    ).inc()
                    return json.loads(cached if isinstance(cached, str) else cached.decode())
            # Cache miss — fetch + cache
            SETTINGS_CACHE_MISS_TOTAL.labels(
                hub_name=self._hub_name, key_type="rag_config"
            ).inc()
            try:
                data = await self._fetch_and_cache(timeout=_DEFAULT_TIMEOUT_REFRESH)
            except Exception as e:
                raise SettingsUnavailableError(
                    f"RagConfigClient.get failed: {e}"
                ) from e
            return data

        async def _fetch_and_cache(self, *, timeout: httpx.Timeout) -> dict[str, Any]:
            start = time.monotonic()
            async with httpx.AsyncClient(timeout=timeout) as client:
                resp = await client.get(f"{self._central_url}{self._endpoint}")
                resp.raise_for_status()
                data = resp.json()
            latency = time.monotonic() - start
            SETTINGS_PULL_LATENCY_SECONDS.labels(
                hub_name=self._hub_name, endpoint=self._endpoint
            ).observe(latency)
            if self._redis is not None:
                await self._redis.setex(
                    self._cache_key, self._ttl, json.dumps(data)
                )
            self._last_pull_ts = time.time()
            SETTINGS_STALE_SECONDS.labels(
                hub_name=self._hub_name, key_type="rag_config"
            ).set(0)
            return data


    class HubRegistryClient:
        """SETTINGS-04 — fetch hub_registry tu central + cache Redis TTL 300s."""

        def __init__(
            self,
            *,
            central_url: str,
            redis: Any,
            hub_name: str,
            ttl: int = 300,
        ) -> None:
            self._central_url = central_url.rstrip("/")
            self._redis = redis
            self._hub_name = hub_name
            self._ttl = ttl
            self._cache_key = HUB_REGISTRY_KEY  # singleton, KHONG per-hub
            self._endpoint = "/api/hubs"

        async def fetch_initial(self) -> None:
            try:
                await self._fetch_and_cache(timeout=_DEFAULT_TIMEOUT_INITIAL)
            except Exception as e:
                logger.critical(
                    "hub_registry_client_fetch_initial_failed: hub=%s err=%s",
                    self._hub_name, e,
                )
                raise SettingsUnavailableError(
                    f"HubRegistryClient fetch_initial failed: {e}"
                ) from e

        async def list_hubs(self) -> list[dict[str, Any]]:
            if self._redis is not None:
                cached = await self._redis.get(self._cache_key)
                if cached is not None:
                    SETTINGS_CACHE_HIT_TOTAL.labels(
                        hub_name=self._hub_name, key_type="hub_registry"
                    ).inc()
                    return json.loads(
                        cached if isinstance(cached, str) else cached.decode()
                    )
            SETTINGS_CACHE_MISS_TOTAL.labels(
                hub_name=self._hub_name, key_type="hub_registry"
            ).inc()
            try:
                data = await self._fetch_and_cache(timeout=_DEFAULT_TIMEOUT_REFRESH)
            except Exception as e:
                raise SettingsUnavailableError(
                    f"HubRegistryClient.list_hubs failed: {e}"
                ) from e
            return data

        async def _fetch_and_cache(
            self, *, timeout: httpx.Timeout
        ) -> list[dict[str, Any]]:
            start = time.monotonic()
            async with httpx.AsyncClient(timeout=timeout) as client:
                resp = await client.get(f"{self._central_url}{self._endpoint}")
                resp.raise_for_status()
                data = resp.json()
            latency = time.monotonic() - start
            SETTINGS_PULL_LATENCY_SECONDS.labels(
                hub_name=self._hub_name, endpoint=self._endpoint
            ).observe(latency)
            if self._redis is not None:
                await self._redis.setex(
                    self._cache_key, self._ttl, json.dumps(data)
                )
            return data


    class ApiKeyVerifyClient:
        """SETTINGS-03 — proxy verify X-API-Key qua central + cache Redis TTL 60s.

        Cache key hash SHA-256 prefix 16 char (T-06-04-02 — KHONG plaintext).
        KHONG cache negative response (401) per CONTEXT Claude's Discretion.
        """

        def __init__(
            self,
            *,
            central_url: str,
            redis: Any,
            hub_name: str,
            settings_proxy_secret: str,
            ttl: int = 60,
        ) -> None:
            self._central_url = central_url.rstrip("/")
            self._redis = redis
            self._hub_name = hub_name
            self._secret = settings_proxy_secret
            self._ttl = ttl
            self._endpoint = "/api/api-keys/verify"

        async def verify(self, api_key: str) -> dict[str, Any] | None:
            cache_key = make_apikey_cache_key(api_key)
            # Cache hit check
            if self._redis is not None:
                cached = await self._redis.get(cache_key)
                if cached is not None:
                    APIKEY_VERIFY_TOTAL.labels(
                        hub_name=self._hub_name, result="cached"
                    ).inc()
                    return json.loads(
                        cached if isinstance(cached, str) else cached.decode()
                    )
            # HTTP fetch
            start = time.monotonic()
            try:
                async with httpx.AsyncClient(
                    timeout=_DEFAULT_TIMEOUT_REFRESH
                ) as client:
                    resp = await client.post(
                        f"{self._central_url}{self._endpoint}",
                        json={"api_key": api_key},
                        headers={"X-Internal-Auth": self._secret},
                    )
            except Exception as e:
                logger.warning(
                    "apikey_verify_client_http_error: hub=%s err=%s",
                    self._hub_name, e,
                )
                APIKEY_VERIFY_TOTAL.labels(
                    hub_name=self._hub_name, result="invalid"
                ).inc()
                return None
            latency = time.monotonic() - start
            SETTINGS_PULL_LATENCY_SECONDS.labels(
                hub_name=self._hub_name, endpoint=self._endpoint
            ).observe(latency)

            if resp.status_code == 401:
                APIKEY_VERIFY_TOTAL.labels(
                    hub_name=self._hub_name, result="invalid"
                ).inc()
                return None  # KHONG cache negative
            if resp.status_code != 200:
                logger.warning(
                    "apikey_verify_client_unexpected_status: hub=%s status=%d",
                    self._hub_name, resp.status_code,
                )
                APIKEY_VERIFY_TOTAL.labels(
                    hub_name=self._hub_name, result="invalid"
                ).inc()
                return None

            data = resp.json()
            principal = data.get("principal") if data.get("valid") else None
            if principal is None:
                APIKEY_VERIFY_TOTAL.labels(
                    hub_name=self._hub_name, result="invalid"
                ).inc()
                return None
            # Cache positive only (T-06-02-04 — KHONG cache negative)
            if self._redis is not None:
                await self._redis.setex(
                    cache_key, self._ttl, json.dumps(principal)
                )
            APIKEY_VERIFY_TOTAL.labels(
                hub_name=self._hub_name, result="valid"
            ).inc()
            return principal


    __all__ = [
        "ApiKeyVerifyClient",
        "HubRegistryClient",
        "RagConfigClient",
        "SettingsUnavailableError",
    ]
    ```

    **Update `api/app/settings_sync/__init__.py`** — append 4 re-export (RagConfigClient + HubRegistryClient + ApiKeyVerifyClient + SettingsUnavailableError) vào import block + `__all__`.

    **Test file `api/tests/unit/test_settings_sync_client.py`** — 10 test case per `<behavior>`. Reuse `_make_mock_httpx_get` helper từ test_jwks_cache.py (copy hoặc import). Mock Redis qua `AsyncMock` với `.get()` + `.setex()` async methods. Fixture `settings_proxy_secret = "x" * 32`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_settings_sync_client.py -v --no-header 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `api/app/settings_sync/client.py` exists.
    - `grep -c "class RagConfigClient" api/app/settings_sync/client.py >= 1`.
    - `grep -c "class HubRegistryClient" api/app/settings_sync/client.py >= 1`.
    - `grep -c "class ApiKeyVerifyClient" api/app/settings_sync/client.py >= 1`.
    - `grep -c "class SettingsUnavailableError" api/app/settings_sync/client.py >= 1`.
    - `grep -c "async def fetch_initial" api/app/settings_sync/client.py >= 2` (RagConfig + HubRegistry).
    - `grep -c "X-Internal-Auth" api/app/settings_sync/client.py >= 1` (ApiKeyVerifyClient).
    - `grep -c "make_apikey_cache_key" api/app/settings_sync/client.py >= 1`.
    - `grep -c "RagConfigClient\|HubRegistryClient\|ApiKeyVerifyClient\|SettingsUnavailableError" api/app/settings_sync/__init__.py >= 4`.
    - Test file `api/tests/unit/test_settings_sync_client.py` exists với `grep -c "def test_" >= 10`.
    - pytest exit 0 với "10 passed" output.
    - `uv run ruff check api/app/settings_sync/client.py api/tests/unit/test_settings_sync_client.py` exit 0.
    - `uv run mypy --strict api/app/settings_sync/client.py` exit 0.
  </acceptance_criteria>
  <done>3 client class implemented với httpx + Redis cache + Prometheus emit + fail-loud lifecycle. 10 unit test PASS + ruff/mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `api/app/settings_sync/subscriber.py` asyncio loop + Pydantic payload + reconnect + unit test</name>
  <files>api/app/settings_sync/subscriber.py, api/app/settings_sync/__init__.py, api/tests/unit/test_settings_sync_subscriber.py</files>
  <read_first>
    - api/app/services/search_cache.py (M2 — đọc full `search_cache_subscriber` function lines 98-138 reuse pattern psubscribe + listen + CancelledError)
    - api/app/settings_sync/keys.py (Plan 06-01 — SETTINGS_INVALIDATE_CHANNEL + 3 cache key prefix dùng để flush)
    - api/app/settings_sync/metrics.py (Plan 06-01 — SETTINGS_INVALIDATE_RECEIVED_TOTAL collector)
    - api/tests/unit/test_search_cache_subscriber.py (nếu exists) HOẶC pattern test asyncio.CancelledError trong test_jwks_cache.py
    - Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md (Wave 2 subscriber.py adapt section)
  </read_first>
  <behavior>
    **test_settings_sync_subscriber.py (~8 test):**

    *Pydantic payload validate (3 test):*
    - Test 1: `InvalidateMessage.model_validate({"config_key":"rag_config","hub":"*","timestamp":1234})` accept.
    - Test 2: `InvalidateMessage.model_validate({"config_key":"INVALID","hub":"*","timestamp":1234})` raise ValidationError (Literal enum reject).
    - Test 3: `InvalidateMessage.model_validate({"config_key":"apikey","hub":"yte","key_id":"<uuid>","timestamp":1234})` accept với optional key_id.

    *Subscriber loop (5 test):*
    - Test 4: subscriber receive valid `config_key=rag_config, hub="*"` → assert mock `redis.delete("settings:rag_config:yte")` called + `SETTINGS_INVALIDATE_RECEIVED_TOTAL.labels("yte","rag_config")` inc.
    - Test 5: subscriber receive `config_key=rag_config, hub="duoc"` ở hub `yte` → assert KHÔNG delete (hub mismatch skip).
    - Test 6: subscriber receive `config_key=hub_registry` → assert `redis.delete("settings:hub_registry")` called.
    - Test 7: subscriber receive invalid JSON → log warning skip (KHÔNG crash loop). Assert KHÔNG `redis.delete` call.
    - Test 8: subscriber raise `asyncio.CancelledError` → loop log "cancelled" + re-raise (graceful shutdown).
  </behavior>
  <action>
    **File `api/app/settings_sync/subscriber.py`** (~150-200 LOC):

    ```python
    """Phase 6 settings_sync — pub/sub subscriber loop (D-V3-Phase6-C LOCKED).

    Lang nghe channel `settings:invalidate` (1 channel duy nhat — KHONG split 3 channel).
    Pydantic InvalidateMessage validate payload truoc khi flush cache.
    3 config_key flush branch:
    - rag_config: hub="*" HOAC hub==settings.hub_name → del settings:rag_config:{hub_name}.
    - hub_registry: any hub → del settings:hub_registry (singleton).
    - apikey: key_id set → del apikey:verify:{hash}; null → SCAN+DEL all apikey:verify:*.

    Reconnect logic: ConnectionError → asyncio.sleep(reconnect_seconds) + retry (KHONG fail-loud).
    CancelledError → re-raise (graceful shutdown).

    Threat model:
    - T-06-02-05 Tampering: Pydantic Literal enum reject invalid config_key (T-06-03-03).
    - T-06-02-06 DoS: subscriber crash KHONG block lifespan (best-effort fail-open).
    """
    from __future__ import annotations

    import asyncio
    import json
    import logging
    from typing import Any, Literal

    from pydantic import BaseModel, Field, ValidationError

    from app.settings_sync.keys import (
        APIKEY_VERIFY_KEY_PREFIX,
        HUB_REGISTRY_KEY,
        RAG_CONFIG_KEY_PREFIX,
        SETTINGS_INVALIDATE_CHANNEL,
    )
    from app.settings_sync.metrics import SETTINGS_INVALIDATE_RECEIVED_TOTAL

    logger = logging.getLogger(__name__)


    class InvalidateMessage(BaseModel):
        """Phase 6 SETTINGS-02 (D-V3-Phase6-C) — pub/sub payload schema."""

        config_key: Literal["rag_config", "hub_registry", "apikey"]
        hub: str = Field(..., min_length=1, max_length=32)
        key_id: str | None = None
        timestamp: int


    async def _handle_invalidate(
        redis: Any, hub_name: str, msg: InvalidateMessage
    ) -> None:
        """Phan nhanh 3 config_key flush cache key dung."""
        if msg.config_key == "rag_config":
            # rag_config: hub="*" broadcast HOAC hub specific match
            if msg.hub != "*" and msg.hub != hub_name:
                return  # mismatch skip
            await redis.delete(f"{RAG_CONFIG_KEY_PREFIX}{hub_name}")
            logger.info(
                "settings_invalidate_rag_config: hub=%s flushed",
                hub_name,
            )
        elif msg.config_key == "hub_registry":
            await redis.delete(HUB_REGISTRY_KEY)
            logger.info("settings_invalidate_hub_registry: flushed singleton")
        elif msg.config_key == "apikey":
            if msg.key_id:
                # Specific key revoke: cannot compute hash without plaintext;
                # full flush all apikey:verify:* (60s TTL — acceptable burn).
                # Alternative: payload carry hash_prefix instead of key_id.
                # Phase 6 simpler: SCAN + DEL pattern.
                cursor: int = 0
                while True:
                    cursor, keys = await redis.scan(
                        cursor=cursor,
                        match=f"{APIKEY_VERIFY_KEY_PREFIX}*",
                        count=100,
                    )
                    if keys:
                        await redis.delete(*keys)
                    if cursor == 0:
                        break
                logger.info(
                    "settings_invalidate_apikey: full flush all (key_id=%s)",
                    msg.key_id,
                )
            else:
                # Null key_id → full flush apikey:verify:*
                cursor = 0
                while True:
                    cursor, keys = await redis.scan(
                        cursor=cursor,
                        match=f"{APIKEY_VERIFY_KEY_PREFIX}*",
                        count=100,
                    )
                    if keys:
                        await redis.delete(*keys)
                    if cursor == 0:
                        break
                logger.info("settings_invalidate_apikey: full flush all")
        SETTINGS_INVALIDATE_RECEIVED_TOTAL.labels(
            hub_name=hub_name, key_type=msg.config_key
        ).inc()


    async def settings_subscriber_loop(
        redis: Any,
        *,
        hub_name: str,
        reconnect_seconds: int = 5,
    ) -> None:
        """Settings invalidate subscriber loop — outer while True wrap reconnect.

        Mỗi inner iteration: subscribe channel + listen messages + handle. Khi
        ConnectionError / pubsub exception → log warning + asyncio.sleep + retry.
        CancelledError → re-raise (graceful shutdown).
        """
        if redis is None:
            logger.warning(
                "settings_subscriber: redis None — subscriber khong khoi dong"
            )
            return

        while True:
            pubsub = None
            try:
                pubsub = redis.pubsub()
                await pubsub.subscribe(SETTINGS_INVALIDATE_CHANNEL)
                logger.info(
                    "settings_subscriber_started: channel=%s hub=%s",
                    SETTINGS_INVALIDATE_CHANNEL, hub_name,
                )
                async for message in pubsub.listen():
                    if message is None or message.get("type") != "message":
                        continue
                    raw = message.get("data")
                    if isinstance(raw, bytes):
                        raw = raw.decode("utf-8")
                    try:
                        payload = json.loads(raw)
                        msg = InvalidateMessage.model_validate(payload)
                    except (json.JSONDecodeError, ValidationError) as e:
                        logger.warning(
                            "settings_subscriber_invalid_payload: err=%s raw=%r",
                            e, raw,
                        )
                        continue
                    try:
                        await _handle_invalidate(redis, hub_name, msg)
                    except Exception as e:  # noqa: BLE001
                        logger.warning(
                            "settings_subscriber_handle_error: msg=%s err=%s",
                            msg, e,
                        )
            except asyncio.CancelledError:
                logger.info("settings_subscriber_cancelled: hub=%s", hub_name)
                raise
            except Exception as e:  # noqa: BLE001
                logger.warning(
                    "settings_subscriber_error_reconnect: hub=%s err=%s sleep=%ds",
                    hub_name, e, reconnect_seconds,
                )
            finally:
                if pubsub is not None:
                    try:
                        await pubsub.aclose()
                    except Exception:  # noqa: BLE001
                        pass
            # Reconnect delay (only reached on exception, not CancelledError)
            await asyncio.sleep(reconnect_seconds)


    __all__ = [
        "InvalidateMessage",
        "settings_subscriber_loop",
    ]
    ```

    **Update `api/app/settings_sync/__init__.py`** — append re-export `InvalidateMessage` + `settings_subscriber_loop`.

    **Test file** — 8 test case. Mock Redis `pubsub()` return `AsyncMock` với:
    - `.subscribe()` async no-op.
    - `.listen()` → async iterator yield list message dict (test fixture pre-build payload).
    - `.aclose()` async no-op.
    - `.delete()` + `.scan()` mocked AsyncMock count call count.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_settings_sync_subscriber.py -v --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `api/app/settings_sync/subscriber.py` exists.
    - `grep -c "class InvalidateMessage" api/app/settings_sync/subscriber.py >= 1`.
    - `grep -c "config_key: Literal" api/app/settings_sync/subscriber.py >= 1`.
    - `grep -c "async def settings_subscriber_loop" api/app/settings_sync/subscriber.py >= 1`.
    - `grep -c "SETTINGS_INVALIDATE_CHANNEL" api/app/settings_sync/subscriber.py >= 1`.
    - `grep -c "asyncio.CancelledError" api/app/settings_sync/subscriber.py >= 1` (graceful shutdown).
    - `grep -c "reconnect_seconds" api/app/settings_sync/subscriber.py >= 2` (param + usage).
    - `grep -c "InvalidateMessage\|settings_subscriber_loop" api/app/settings_sync/__init__.py >= 2`.
    - Test file `api/tests/unit/test_settings_sync_subscriber.py` exists với `grep -c "def test_" >= 8`.
    - pytest exit 0 với "8 passed".
    - `uv run ruff check api/app/settings_sync/subscriber.py api/tests/unit/test_settings_sync_subscriber.py` exit 0.
    - `uv run mypy --strict api/app/settings_sync/subscriber.py` exit 0.
  </acceptance_criteria>
  <done>Subscriber loop với Pydantic payload validate + 3 config_key branch + reconnect logic + CancelledError graceful. 8 unit test PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hub con httpx → central HTTP | httpx async call qua intra-network Docker DNS (central:8080) — KHÔNG TLS (medinet_net isolated); attacker MITM cần break Docker network. |
| hub con httpx body → central endpoint | api_key plaintext gửi trong POST body (KHÔNG header — body encryption defer mTLS v4.0); intra-network only. |
| pub/sub channel → subscriber payload parse | Pydantic Literal enum reject invalid config_key — attacker compromise Redis publish KHÔNG inject arbitrary code (T-06-03-03 carry forward). |
| Redis cache key namespace | Hash apikey:verify:* key — KHÔNG plaintext (T-06-04-02). rag_config + hub_registry namespace separate KHÔNG conflict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | apikey plaintext trong Redis cache key | mitigate | `make_apikey_cache_key()` SHA-256 hex prefix 16 char — Plan 06-01 carry forward; subscriber `apikey` branch dùng SCAN+DEL pattern (KHÔNG cần plaintext compute key). |
| T-06-02-02 | DoS | httpx fetch_initial blocking lifespan boot | mitigate | Timeout connect=2s, read=5s `_DEFAULT_TIMEOUT_INITIAL`; fail-loud raise → uvicorn exit 1 (operator catch boot error). Refresh task timeout connect=5s, read=10s relax. |
| T-06-02-03 | Tampering | pub/sub payload injection | mitigate | `InvalidateMessage` Pydantic Literal `config_key` enum 3 value + `hub` str max_length=32 + invalid JSON / ValidationError → log warning skip (KHÔNG crash subscriber). |
| T-06-02-04 | Information Disclosure | cache positive 401 response | mitigate | `ApiKeyVerifyClient.verify()` 401 → return None + KHÔNG `redis.setex` (no negative cache per Claude's Discretion). |
| T-06-02-05 | DoS | subscriber crash block lifespan | mitigate | Best-effort fail-open: outer `while True:` wrap try/except → ConnectionError log warning + asyncio.sleep(reconnect_seconds) + retry; CancelledError re-raise graceful shutdown. |
| T-06-02-06 | Spoofing | ApiKeyVerifyClient header forge | mitigate | Header `X-Internal-Auth: {settings_proxy_secret}` 32-char min (Plan 06-01 validator); central side hmac.compare_digest constant-time check (Plan 06-03 require_internal_auth dep). |
| T-06-02-07 | Tampering | rag_config response shape break | accept | M2 schema RagConfigService.get_config() shape stable (D5 hot-swap pattern); future schema migration sẽ bump version field — defer v4.0. |
| T-06-02-08 | Repudiation | apikey verify cache hit log absent | accept | Per CONTEXT Claude's Discretion: log only on miss + fetch latency. Cache hit chỉ emit Prometheus counter (KHÔNG structlog log volume). |
</threat_model>

<verification>
**Wave 2 verify cả 2 task PASS:**

```bash
cd Hub_All/api
uv run pytest tests/unit/test_settings_sync_client.py tests/unit/test_settings_sync_subscriber.py -v --no-header
# Expected: 18 passed (10 client + 8 subscriber)

# Regression — full unit suite
uv run pytest tests/unit/ --no-header 2>&1 | tail -10
# Expected: tất cả Phase 1..5 unit test PASS — KHÔNG regression
```
</verification>

<success_criteria>
- 3 client class (`RagConfigClient` + `HubRegistryClient` + `ApiKeyVerifyClient`) với httpx async + Redis cache + Prometheus emit + fail-loud lifecycle.
- `SettingsUnavailableError` exception class cho 503 envelope (Wave 3 router consume).
- `settings_subscriber_loop` async + Pydantic `InvalidateMessage` schema + 3 config_key flush branch + reconnect retry + CancelledError graceful.
- 18 unit test PASS (10 client + 8 subscriber) + module-level type check + ruff clean.
- `api/app/settings_sync/__init__.py` re-export đầy đủ public API (10+ symbol).
- Regression Phase 1..5 unit test KHÔNG break.
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-settings-sync/06-02-SUMMARY.md` ghi:
- Files modified với line count.
- 18 unit test PASS detail.
- Regression status.
- Deviation nếu có (Rule 1/2/3 from execute-plan workflow).
- Next: Wave 3 (Plan 06-03) extend router + dependencies.
</output>
