---
phase: 06
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - api/app/main.py
  - api/tests/integration/test_settings_sync_lifespan_integration.py
  - api/tests/integration/test_settings_sync_pubsub_e2e.py
autonomous: true
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04]
tags: [settings-sync, lifespan, integration-test, asgi]

must_haves:
  truths:
    - "`api/app/main.py::lifespan` thêm block hub con (`if settings.hub_name != \"central\":`) init 3 client (`RagConfigClient` + `HubRegistryClient` + `ApiKeyVerifyClient`) + spawn `settings_subscriber_task`"
    - "Boot fail-loud nếu `rag_client.fetch_initial()` HOẶC `hub_client.fetch_initial()` raise → uvicorn exit 1 (R-V3-6 LOW + R-V3-5 pattern carry forward)"
    - "Test-mode escape hatch `SETTINGS_SKIP_FETCH=1` env bypass blocking fetch_initial (pattern song song JWKS_SKIP_FETCH + SYNC_SKIP_CENTRAL_POOL)"
    - "Shutdown graceful: `settings_subscriber_task.cancel()` + `asyncio.wait_for(timeout=10s)` (carry forward Plan 03-02 + 04-04)"
    - "`app.state.rag_config_client` + `app.state.hub_registry_client` + `app.state.api_key_verify_client` + `app.state.settings_subscriber_task` populate ở runtime hub con"
    - "Integration test ASGI lifespan ≥ 8 PASS (6 lifespan integration + 2 pubsub e2e — defer pubsub e2e nếu khó setup real Redis fixture)"
  artifacts:
    - path: "api/app/main.py"
      provides: "lifespan block hub con settings_sync init + shutdown"
      contains: "settings_subscriber_task"
    - path: "api/tests/integration/test_settings_sync_lifespan_integration.py"
      provides: "ASGI lifespan mock httpx + state populate + shutdown verify"
      contains: "test_lifespan_populates_settings_clients"
    - path: "api/tests/integration/test_settings_sync_pubsub_e2e.py"
      provides: "(optional) Real Redis fixture or fakeredis e2e pub/sub test"
      contains: "test_central_publish_invalidates_yte_cache"
  key_links:
    - from: "api/app/main.py::lifespan"
      to: "Wave 2 client + subscriber module imports"
      via: "from app.settings_sync.client + subscriber import"
      pattern: "from app.settings_sync"
    - from: "app.state.api_key_verify_client populated by lifespan"
      to: "Wave 3 require_api_key dependency consume"
      via: "request.app.state.api_key_verify_client attribute"
      pattern: "app.state.api_key_verify_client"
    - from: "Lifespan settings_subscriber_task spawn"
      to: "Wave 2 settings_subscriber_loop async iter"
      via: "asyncio.create_task wrap loop"
      pattern: "asyncio.create_task(settings_subscriber_loop"
---

<objective>
Wave 4 BLOCKING — wire Phase 6 vào `api/app/main.py::lifespan` để hub con startup init 3 client (`RagConfigClient` + `HubRegistryClient` + `ApiKeyVerifyClient`) blocking fetch_initial + spawn `settings_subscriber_task` (`asyncio.create_task(settings_subscriber_loop(...))`), shutdown graceful cancel + wait. Add 1-2 integration test ASGI lifespan verify state populate + escape hatch + shutdown.

**Purpose:** Wave 4 = "glue" code biến Wave 2 + Wave 3 thành runtime working. Tất cả test integration ASGI tới giờ chỉ mock state.client = MagicMock; Wave 4 verify lifespan thực sự populate state đúng object class + cancel task graceful không leak coroutine.

**Critical depends:** Wave 1 (Settings + scaffold) + Wave 2 (client + subscriber) + Wave 3 (require_api_key consume app.state.api_key_verify_client) phải DONE. Lifespan trigger fetch_initial blocking 5s → cần CENTRAL_URL set + Plan 03-04 Settings.central_url ready (đã có Phase 3 carry forward).

**Output:** main.py lifespan block ~50-80 LOC + 2 integration test file ASGI lifespan verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/06-system-settings-sync/06-CONTEXT.md
@Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md
@Hub_All/.planning/phases/06-system-settings-sync/06-01-PLAN.md
@Hub_All/.planning/phases/06-system-settings-sync/06-02-PLAN.md
@Hub_All/.planning/phases/06-system-settings-sync/06-03-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Lifespan integration target — main.py lines bao quanh JWKSCache block (Phase 3) + central_sync_pool (Phase 4) -->

From api/app/main.py lines 207-248 (Phase 3 JWKSCache block — analog gần nhất):
```python
app.state.jwks_cache = None
if settings.hub_name != "central":
    if os.environ.get("JWKS_SKIP_FETCH") == "1":
        logger.warning("jwks_cache_skipped: JWKS_SKIP_FETCH=1 (test mode)")
    else:
        from app.auth.jwks import JWKSCache
        if not settings.central_jwks_url:
            raise RuntimeError(...)
        jwks_cache = JWKSCache(
            jwks_url=settings.central_jwks_url,
            refresh_interval=settings.jwks_refresh_interval,
            max_stale_seconds=settings.jwks_max_stale_seconds,
        )
        try:
            await jwks_cache.fetch_initial()  # blocking, raise on fail
        except Exception as e:
            logger.critical("lifespan_jwks_cache_init_failed: ...")
            raise  # boot fail-loud → uvicorn exit 1
        jwks_cache.start_refresh_task()
        app.state.jwks_cache = jwks_cache
        logger.info("lifespan_jwks_cache_ready: ...")
```

From api/app/main.py lines 380-392 (search_cache subscriber spawn pattern — universal mount):
```python
app.state.search_cache_task = None
if app.state.redis is not None:
    try:
        from app.services.search_cache import search_cache_subscriber
        app.state.search_cache_task = asyncio.create_task(
            search_cache_subscriber(app.state.redis)
        )
        logger.info("search_cache_subscriber_task_started")
    except Exception as e:
        logger.warning("search_cache_subscriber_start_failed: %s", e)
```

From api/app/main.py lines 441-449 (sync_worker_task spawn hub con-only — Phase 4 Plan 04-04):
```python
if settings.hub_name != "central":
    app.state.sync_worker_task = asyncio.create_task(sync_worker_loop(app))
    logger.info("lifespan_sync_worker_started: hub=%s", settings.hub_name)
```

From api/app/main.py shutdown section (graceful cancel pattern):
```python
if app.state.search_cache_task is not None:
    app.state.search_cache_task.cancel()
    try:
        await asyncio.wait_for(app.state.search_cache_task, timeout=10.0)
    except (asyncio.CancelledError, asyncio.TimeoutError):
        pass
    logger.info("search_cache_subscriber_task_stopped")
```

From api/app/settings_sync/client.py (Plan 06-02 — class signatures):
```python
class RagConfigClient:
    def __init__(self, *, central_url: str, redis: Any, hub_name: str, ttl: int = 60): ...
    async def fetch_initial(self) -> None: ...

class HubRegistryClient:
    def __init__(self, *, central_url: str, redis: Any, hub_name: str, ttl: int = 300): ...
    async def fetch_initial(self) -> None: ...

class ApiKeyVerifyClient:
    def __init__(self, *, central_url, redis, hub_name, settings_proxy_secret, ttl: int = 60): ...
    async def verify(self, api_key: str) -> dict | None: ...  # NO fetch_initial — lazy verify
```

From api/app/settings_sync/subscriber.py (Plan 06-02):
```python
async def settings_subscriber_loop(
    redis: Any,
    *,
    hub_name: str,
    reconnect_seconds: int = 5,
) -> None: ...
```

From api/tests/integration/test_jwks_cache_lifecycle.py (Plan 03-02 — ASGI lifespan pattern):
```python
import asgi_lifespan
async def test_lifespan_populates_jwks_cache(...):
    app = create_app()
    async with LifespanManager(app):
        assert app.state.jwks_cache is not None
        ...
```

From api/tests/integration/test_sync_lifespan_integration.py (Plan 04-04 — mock asyncpg + state assert):
```python
async def test_lifespan_central_sync_pool_populated(monkeypatch):
    monkeypatch.setenv("SYNC_SKIP_CENTRAL_POOL", "0")
    # ... mock create_pool ...
    async with LifespanManager(app):
        assert app.state.central_sync_pool is not None
        assert app.state.sync_worker_task is not None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lifespan block hub con settings_sync init + shutdown graceful</name>
  <files>api/app/main.py</files>
  <read_first>
    - api/app/main.py (đọc full lifespan function — locate JWKSCache block ~lines 207-248 + central_sync_pool block ~275-323 + search_cache subscriber spawn ~380-392 + sync_worker spawn ~441-449 + shutdown section. Phase 6 block insert SAU sync_worker (Wave 4 = block cuối cùng trong hub con init chain) + shutdown SAU sync_worker shutdown.)
    - api/app/settings_sync/__init__.py (Plan 06-01 + 06-02 — verify re-export đầy đủ)
    - api/app/settings_sync/client.py (Plan 06-02 — verify signature __init__ params chính xác)
    - api/app/settings_sync/subscriber.py (Plan 06-02 — verify settings_subscriber_loop signature)
    - Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md (Wave 4 lifespan section — full block excerpt)
  </read_first>
  <action>
    **Insert vào `api/app/main.py` lifespan function** — sau Phase 4 sync_worker spawn block (~line 441-449), TRƯỚC `yield`:

    ```python
    # ──────────────────────────────────────────────────────────────────────
    # Phase 6 Plan 06-04 (SETTINGS-01..04, D-V3-Phase6-A/B/C/D) — Hub con settings_sync
    # ──────────────────────────────────────────────────────────────────────
    # Hub con (settings.hub_name != "central") spawn 3 client + 1 subscriber task.
    # - RagConfigClient.fetch_initial() blocking 5s (R-V3-6 LOW + D-V3-Phase6-A
    #   fail-loud boot pattern song song JWKSCache Plan 03-02).
    # - HubRegistryClient.fetch_initial() blocking 5s.
    # - ApiKeyVerifyClient KHÔNG fetch_initial (lazy — verify on demand qua
    #   require_api_key dependency Plan 06-03).
    # - settings_subscriber_task asyncio.create_task spawn subscribe settings:invalidate.
    #
    # Test-mode escape hatch: env `SETTINGS_SKIP_FETCH=1` bypass blocking fetch_initial
    # (pattern song song COCOINDEX_SKIP_SETUP + JWKS_SKIP_FETCH + SYNC_SKIP_CENTRAL_POOL).
    app.state.rag_config_client = None
    app.state.hub_registry_client = None
    app.state.api_key_verify_client = None
    app.state.settings_subscriber_task = None

    if settings.hub_name != "central":
        if os.environ.get("SETTINGS_SKIP_FETCH") == "1":
            logger.warning(
                "settings_sync_skipped: SETTINGS_SKIP_FETCH=1 (test mode — "
                "lifespan bypass blocking fetch_initial)"
            )
        else:
            from app.settings_sync.client import (
                ApiKeyVerifyClient,
                HubRegistryClient,
                RagConfigClient,
            )
            from app.settings_sync.subscriber import settings_subscriber_loop

            if not settings.central_url:
                raise RuntimeError(
                    f"hub_name={settings.hub_name!r} thiếu CENTRAL_URL — "
                    "Settings validator phải đã raise ở boot (Phase 3 Plan 03-04 "
                    "_enforce_central_url_for_hub)."
                )

            rag_client = RagConfigClient(
                central_url=settings.central_url,
                redis=app.state.redis,
                hub_name=settings.hub_name,
                ttl=settings.settings_cache_ttl_rag_config,
            )
            hub_client = HubRegistryClient(
                central_url=settings.central_url,
                redis=app.state.redis,
                hub_name=settings.hub_name,
                ttl=settings.settings_cache_ttl_hub_registry,
            )
            apikey_client = ApiKeyVerifyClient(
                central_url=settings.central_url,
                redis=app.state.redis,
                hub_name=settings.hub_name,
                settings_proxy_secret=settings.settings_proxy_secret,
                ttl=settings.settings_cache_ttl_apikey,
            )
            try:
                await rag_client.fetch_initial()  # blocking 5s, raise on fail
                await hub_client.fetch_initial()
            except Exception as e:
                logger.critical(
                    "lifespan_settings_sync_init_failed: hub_name=%s "
                    "central_url=%s err=%s — boot abort",
                    settings.hub_name, settings.central_url, e,
                )
                raise  # boot fail-loud D-V3-Phase6-A → uvicorn exit 1

            app.state.rag_config_client = rag_client
            app.state.hub_registry_client = hub_client
            app.state.api_key_verify_client = apikey_client

            # Spawn subscriber task (best-effort — Redis None KHÔNG fail-loud
            # per CONTEXT Claude's Discretion fail-quiet subscriber).
            if app.state.redis is not None:
                app.state.settings_subscriber_task = asyncio.create_task(
                    settings_subscriber_loop(
                        app.state.redis,
                        hub_name=settings.hub_name,
                        reconnect_seconds=settings.settings_subscriber_reconnect_seconds,
                    )
                )
                logger.info(
                    "settings_subscriber_task_started: hub=%s",
                    settings.hub_name,
                )
            else:
                logger.warning(
                    "settings_subscriber_skipped: redis None — TTL natural fallback"
                )
            logger.info(
                "lifespan_settings_sync_ready: hub=%s central_url=%s",
                settings.hub_name, settings.central_url,
            )
    ```

    **Insert vào shutdown section SAU yield** — trước `sync_worker_task.cancel()` shutdown OR sau (any order in shutdown safe — KHÔNG depend nhau):

    ```python
    # Phase 6 Plan 06-04 — Settings subscriber task graceful shutdown.
    if app.state.settings_subscriber_task is not None:
        app.state.settings_subscriber_task.cancel()
        try:
            await asyncio.wait_for(
                app.state.settings_subscriber_task, timeout=10.0
            )
        except (asyncio.CancelledError, asyncio.TimeoutError):
            pass
        logger.info("settings_subscriber_task_stopped")
    ```
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run mypy --strict app/main.py 2>&1 | tail -15 && grep -c "settings_sync" app/main.py && grep -c "SETTINGS_SKIP_FETCH" app/main.py && grep -c "settings_subscriber_task" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from app.settings_sync.client import" api/app/main.py >= 1`.
    - `grep -c "from app.settings_sync.subscriber import settings_subscriber_loop" api/app/main.py >= 1`.
    - `grep -c "RagConfigClient(" api/app/main.py >= 1` (instantiate).
    - `grep -c "HubRegistryClient(" api/app/main.py >= 1`.
    - `grep -c "ApiKeyVerifyClient(" api/app/main.py >= 1`.
    - `grep -c "rag_client.fetch_initial" api/app/main.py >= 1`.
    - `grep -c "hub_client.fetch_initial" api/app/main.py >= 1`.
    - `grep -c "SETTINGS_SKIP_FETCH" api/app/main.py >= 1` (escape hatch).
    - `grep -c "settings_subscriber_task" api/app/main.py >= 4` (init None + create_task + cancel + wait_for).
    - `grep -c "app.state.rag_config_client" api/app/main.py >= 2` (default None + assign).
    - `grep -c "app.state.hub_registry_client" api/app/main.py >= 2`.
    - `grep -c "app.state.api_key_verify_client" api/app/main.py >= 2`.
    - `grep -c "lifespan_settings_sync_init_failed" api/app/main.py >= 1` (fail-loud log).
    - `grep -c "asyncio.wait_for(.*settings_subscriber_task" api/app/main.py >= 1` (shutdown graceful).
    - `uv run ruff check api/app/main.py` exit 0.
    - `uv run mypy --strict api/app/main.py` exit 0.
    - `python -c "import sys; sys.path.insert(0, 'api'); from app.main import create_app; app = create_app(); print('OK')"` exit 0 (assuming env mode central — verify import resolve KHÔNG syntax error).
  </acceptance_criteria>
  <done>main.py lifespan block hub con settings_sync init + shutdown graceful. ruff + mypy clean. Module import resolve OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test ASGI lifespan + (optional) pubsub e2e</name>
  <files>api/tests/integration/test_settings_sync_lifespan_integration.py, api/tests/integration/test_settings_sync_pubsub_e2e.py</files>
  <read_first>
    - api/tests/integration/test_jwks_cache_lifecycle.py (Plan 03-02 — ASGI LifespanManager pattern + mock httpx fetch)
    - api/tests/integration/test_sync_lifespan_integration.py (Plan 04-04 — mock asyncpg + state assert + escape hatch usage)
    - api/tests/integration/conftest.py (Phase 2 Plan 02-03 — hub_app_factory fixture nếu có thể reuse)
    - api/app/main.py (Plan 06-04 Task 1 — verify state attribute name khớp test assertion)
    - Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md (Wave 4 integration test section)
  </read_first>
  <behavior>
    **test_settings_sync_lifespan_integration.py (~6 test):**

    *Escape hatch (1 test):*
    - Test 1: `SETTINGS_SKIP_FETCH=1` set → LifespanManager(app_with_yte) startup OK → assert `app.state.rag_config_client is None` + `app.state.hub_registry_client is None` + `app.state.api_key_verify_client is None` + `app.state.settings_subscriber_task is None` (skip path).

    *Happy path with mock httpx (4 test):*
    - Test 2: hub yte boot + mock httpx GET /api/rag-config → 200 dict + GET /api/hubs → 200 list → assert `app.state.rag_config_client` is instance `RagConfigClient` + `app.state.hub_registry_client` is `HubRegistryClient` + `app.state.api_key_verify_client` is `ApiKeyVerifyClient`.
    - Test 3: Mock httpx + Redis fakeredis → assert `app.state.settings_subscriber_task` is asyncio.Task + `.done() == False` (running).
    - Test 4: Shutdown → assert `settings_subscriber_task.cancelled() == True` HOẶC `.done() == True` sau LifespanManager exit.
    - Test 5: Boot fail-loud — mock httpx raise ConnectionError → LifespanManager startup raise → uvicorn exit 1 simulate (assert raises).

    *Central mode (1 test):*
    - Test 6: Central app boot (hub_name="central") → assert `app.state.rag_config_client is None` (block skip — KHÔNG hub con).

    **test_settings_sync_pubsub_e2e.py (~2 test, optional):**

    *Real Redis e2e (fakeredis fallback nếu testcontainer phức tạp):*
    - Test 7 (optional): Spawn central app + yte app share fakeredis instance → central call PUT /api/rag-config → assert yte app.state.redis cache key `settings:rag_config:yte` được DEL trong < 2s (subscriber receive + flush).
    - Test 8 (optional): Spawn central → publish manual `redis.publish("settings:invalidate", json)` → assert yte cache `settings:hub_registry` deleted.

    **Defer if too complex:** Test 7+8 nếu fakeredis-py KHÔNG support `pubsub().subscribe()` async iter đúng → defer Phase 7 MIGRATE-05 full E2E.
  </behavior>
  <action>
    **File `api/tests/integration/test_settings_sync_lifespan_integration.py`** — Pattern carry forward `test_jwks_cache_lifecycle.py`:

    ```python
    """Phase 6 Plan 06-04 — Integration test ASGI lifespan settings_sync state populate + shutdown."""
    from __future__ import annotations

    from unittest.mock import AsyncMock, MagicMock, patch

    import httpx
    import pytest
    from asgi_lifespan import LifespanManager

    from app.main import create_app
    from app.settings_sync.client import (
        ApiKeyVerifyClient,
        HubRegistryClient,
        RagConfigClient,
    )


    @pytest.fixture
    def yte_env(monkeypatch):
        """Hub yte env fixture."""
        monkeypatch.setenv("HUB_NAME", "yte")
        monkeypatch.setenv("CENTRAL_URL", "http://python-api-central:8080")
        monkeypatch.setenv(
            "CENTRAL_JWKS_URL",
            "http://python-api-central:8080/.well-known/jwks.json",
        )
        monkeypatch.setenv(
            "HUB_ID", "00000000-0000-4000-a000-000000000001"
        )
        monkeypatch.setenv(
            "CENTRAL_SYNC_DSN",
            "postgresql+asyncpg://medinet:test@localhost:5432/medinet_central",
        )
        monkeypatch.setenv("SETTINGS_PROXY_SECRET", "x" * 32)
        monkeypatch.setenv("JWKS_SKIP_FETCH", "1")
        monkeypatch.setenv("SYNC_SKIP_CENTRAL_POOL", "1")
        monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
        # Default SETTINGS_SKIP_FETCH=0 — test override per case


    @pytest.mark.integration
    @pytest.mark.asyncio
    async def test_skip_fetch_flag_bypasses_settings_clients(yte_env, monkeypatch):
        """SETTINGS_SKIP_FETCH=1 → app.state.rag_config_client is None."""
        monkeypatch.setenv("SETTINGS_SKIP_FETCH", "1")
        app = create_app()
        async with LifespanManager(app):
            assert app.state.rag_config_client is None
            assert app.state.hub_registry_client is None
            assert app.state.api_key_verify_client is None
            assert app.state.settings_subscriber_task is None


    def _mock_httpx_factory(rag_config_data, hubs_data):
        """Build mock httpx.AsyncClient that returns canned responses based on URL path."""
        async def _mock_get(url, *args, **kwargs):
            resp = MagicMock()
            resp.raise_for_status = MagicMock()
            if "/rag-config" in url:
                resp.json = MagicMock(return_value=rag_config_data)
            elif "/hubs" in url:
                resp.json = MagicMock(return_value=hubs_data)
            else:
                resp.json = MagicMock(return_value={})
            resp.status_code = 200
            return resp

        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(side_effect=_mock_get)
        return mock_client


    @pytest.mark.integration
    @pytest.mark.asyncio
    async def test_lifespan_populates_settings_clients(yte_env, monkeypatch):
        """Mock httpx fetch_initial happy → 3 client instance populated."""
        monkeypatch.setenv("SETTINGS_SKIP_FETCH", "0")
        rag_config_data = {"provider": "openai", "model": "gpt-4", "dimensions": 1536}
        hubs_data = [{"id": "yte", "subpath": "/yte", "active": True}]
        mock_client = _mock_httpx_factory(rag_config_data, hubs_data)

        # Patch httpx.AsyncClient global to return mock
        with patch("httpx.AsyncClient", return_value=mock_client):
            app = create_app()
            async with LifespanManager(app):
                assert isinstance(app.state.rag_config_client, RagConfigClient)
                assert isinstance(app.state.hub_registry_client, HubRegistryClient)
                assert isinstance(app.state.api_key_verify_client, ApiKeyVerifyClient)


    @pytest.mark.integration
    @pytest.mark.asyncio
    async def test_lifespan_spawns_subscriber_task(yte_env, monkeypatch):
        """app.state.settings_subscriber_task is running asyncio.Task."""
        monkeypatch.setenv("SETTINGS_SKIP_FETCH", "0")
        # ... similar mock httpx setup ...
        # ... mock app.state.redis qua mock redis pubsub() returning AsyncMock iterator ...
        # assert app.state.settings_subscriber_task is not None
        # assert isinstance(app.state.settings_subscriber_task, asyncio.Task)
        # assert app.state.settings_subscriber_task.done() is False
        pass  # implement per pattern


    @pytest.mark.integration
    @pytest.mark.asyncio
    async def test_lifespan_shutdown_cancels_subscriber_task(yte_env, monkeypatch):
        """Shutdown lifespan → subscriber_task cancelled or done."""
        # ... setup ...
        # async with LifespanManager(app):
        #     task = app.state.settings_subscriber_task
        # # After exit, task should be done or cancelled
        # assert task.done()
        pass


    @pytest.mark.integration
    @pytest.mark.asyncio
    async def test_lifespan_fetch_initial_fail_raises(yte_env, monkeypatch):
        """Mock httpx raise ConnectionError → LifespanManager startup raise (boot fail-loud)."""
        monkeypatch.setenv("SETTINGS_SKIP_FETCH", "0")
        async def _fail_get(*args, **kwargs):
            raise httpx.ConnectError("mock central down")

        mock_client = MagicMock()
        mock_client.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client.__aexit__ = AsyncMock(return_value=None)
        mock_client.get = AsyncMock(side_effect=_fail_get)

        with patch("httpx.AsyncClient", return_value=mock_client):
            app = create_app()
            with pytest.raises(Exception):  # SettingsUnavailableError wrapped
                async with LifespanManager(app):
                    pass  # should not reach here


    @pytest.mark.integration
    @pytest.mark.asyncio
    async def test_central_mode_skips_settings_clients(monkeypatch):
        """Central app (hub_name=central) → settings_sync block skipped."""
        monkeypatch.setenv("HUB_NAME", "central")
        monkeypatch.setenv(
            "POSTGRES_DSN",
            "postgresql+asyncpg://medinet:test@localhost:5432/medinet_central",
        )
        monkeypatch.setenv("SETTINGS_PROXY_SECRET", "x" * 32)
        monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
        app = create_app()
        async with LifespanManager(app):
            assert app.state.rag_config_client is None
            assert app.state.hub_registry_client is None
            assert app.state.api_key_verify_client is None
    ```

    **File `api/tests/integration/test_settings_sync_pubsub_e2e.py`** — Optional, dùng `fakeredis-py` nếu compatible với async pubsub; nếu KHÔNG → defer Phase 7 với pytest.mark.skip + rationale:

    ```python
    """Phase 6 Plan 06-04 — (Optional) Real Redis pub/sub e2e test.

    Defer Phase 7 MIGRATE-05 nếu fakeredis-py KHÔNG support async pubsub.listen() đúng.
    """
    import pytest

    pytest.skip(
        "Phase 6 e2e pub/sub test defer Phase 7 MIGRATE-05 full E2E real Redis fixture. "
        "Plan 06-04 Task 1 + Plan 06-02 subscriber unit test cover semantic. "
        "fakeredis-py async pubsub.listen() KHÔNG yield message reliable.",
        allow_module_level=True,
    )

    # Placeholder structure khi enable Phase 7:
    # @pytest.mark.integration
    # async def test_central_publish_invalidates_yte_cache(real_redis):
    #     # spawn 2 LifespanManager share real_redis fixture
    #     # central PUT /api/rag-config → assert yte app.state.redis.get(...) is None < 2s
    #     pass
    ```
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_settings_sync_lifespan_integration.py -v --no-header -m integration 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `api/tests/integration/test_settings_sync_lifespan_integration.py` exists với `grep -c "def test_\|async def test_" >= 6`.
    - File `api/tests/integration/test_settings_sync_pubsub_e2e.py` exists (có thể skip module-level — counted = 0 chạy, OK).
    - `grep -c "LifespanManager" api/tests/integration/test_settings_sync_lifespan_integration.py >= 5` (use trong 5+ test).
    - `grep -c "SETTINGS_SKIP_FETCH" api/tests/integration/test_settings_sync_lifespan_integration.py >= 1`.
    - `grep -c "RagConfigClient\|HubRegistryClient\|ApiKeyVerifyClient" api/tests/integration/test_settings_sync_lifespan_integration.py >= 3` (assert isinstance).
    - `grep -c "app.state.settings_subscriber_task" api/tests/integration/test_settings_sync_lifespan_integration.py >= 1`.
    - pytest exit 0 với "6 passed" (lifespan integration; pubsub e2e skipped OK).
    - Regression `uv run pytest tests/integration/ -m integration --no-header` PASS toàn bộ Phase 2..4 integration test KHÔNG break.
    - `uv run ruff check api/tests/integration/test_settings_sync_lifespan_integration.py api/tests/integration/test_settings_sync_pubsub_e2e.py` exit 0.
  </acceptance_criteria>
  <done>≥6 integration test lifespan ASGI PASS verify state populate + escape hatch + shutdown graceful + fail-loud + central skip. pubsub e2e defer Phase 7 nếu phức tạp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifespan startup → external httpx central call | Blocking fetch_initial 5s → fail-loud raise → uvicorn exit 1; operator catch boot error (R-V3-6 LOW). |
| lifespan asyncio task → shared `app.state` | Subscriber task background runs against `app.state.redis` — race condition risk nếu lifespan đang init state khác. Mitigate: spawn task SAU 3 client populate (sequential). |
| Subscriber task → `app.state.<X>_client` mutation | Subscriber chỉ flush Redis cache, KHÔNG mutate client object → safe concurrent. |
| Shutdown task.cancel() | `asyncio.wait_for(timeout=10s)` graceful — pubsub.aclose() trong subscriber finally block đảm bảo Redis connection close. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | DoS | Lifespan blocking fetch_initial hang | mitigate | `_DEFAULT_TIMEOUT_INITIAL = Timeout(connect=2s, read=5s)` từ Wave 2 client; fail-loud raise → uvicorn exit 1 (operator catch). Total worst case ~10s (2 client × 5s). |
| T-06-04-02 | Elevation of Privilege | Hub con boot pass settings_proxy_secret env empty | mitigate | Plan 06-01 `_enforce_settings_proxy_secret` validator length ≥ 32 char raise ValidationError TRƯỚC lifespan trigger; ApiKeyVerifyClient init crash early nếu secret missing. |
| T-06-04-03 | Resource exhaustion | Subscriber task leak coroutine sau shutdown | mitigate | Shutdown graceful `task.cancel() + asyncio.wait_for(timeout=10s)` + subscriber finally `pubsub.aclose()` (Plan 06-02 carry forward search_cache.py M2 pattern). |
| T-06-04-04 | Tampering | Lifespan order race app.state.redis | mitigate | `app.state.redis` init từ M2 baseline lifespan ~line 108 (TRƯỚC Phase 6 block ~line 441+); Wave 4 block check `if app.state.redis is not None:` defensive guard. |
| T-06-04-05 | Spoofing | Test mode escape hatch SETTINGS_SKIP_FETCH=1 production leak | mitigate | Env flag chỉ check trong lifespan code — KHÔNG ảnh hưởng prod (operator KHÔNG set var); log.warning("test mode") rõ ràng + README.md Phase 6 deploy KHÔNG mention var (defer Plan 06-05 README section). |
| T-06-04-06 | Repudiation | Lifespan boot failure KHÔNG log evidence | mitigate | `logger.critical("lifespan_settings_sync_init_failed: ...")` structlog JSON output với hub_name + central_url + err — operator inspect docker logs sau crash. |
</threat_model>

<verification>
**Wave 4 verify khi cả 2 task PASS:**

```bash
cd Hub_All/api
uv run mypy --strict app/main.py
uv run pytest tests/integration/test_settings_sync_lifespan_integration.py -v --no-header -m integration
# Expected: 6 passed (pubsub e2e skipped OK)

# Regression — full integration suite (Phase 2 + 3 + 4 + 6 lifespan integration test)
uv run pytest tests/integration/ -m integration --no-header 2>&1 | tail -15
# Expected: tất cả PASS — KHÔNG regression
```
</verification>

<success_criteria>
- `api/app/main.py::lifespan` thêm block hub con settings_sync init (4 app.state mới + 3 client + 1 task) + shutdown graceful.
- Boot fail-loud nếu fetch_initial raise → uvicorn exit 1 (R-V3-6 LOW pattern).
- Test mode escape hatch `SETTINGS_SKIP_FETCH=1` env hoạt động.
- ≥6 integration test ASGI lifespan PASS (skip path + populate + spawn + shutdown + fail-loud + central skip).
- pubsub e2e test optional skip với rationale defer Phase 7 nếu fakeredis async limit.
- Regression Phase 2..4 integration test KHÔNG break.
- ruff + mypy --strict clean trên main.py.
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-settings-sync/06-04-SUMMARY.md` ghi:
- main.py lines added (Phase 6 block init + shutdown).
- Integration test count PASS.
- Regression status full integration suite.
- Deviation nếu có.
- Next: Wave 5 (Plan 06-05) closeout docs.
</output>
