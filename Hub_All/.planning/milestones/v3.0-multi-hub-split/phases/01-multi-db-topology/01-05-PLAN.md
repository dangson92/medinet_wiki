---
phase: 01-multi-db-topology
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - api/scripts/hub-init.sh
  - Makefile
  - api/Makefile
  - api/tests/integration/test_hub_isolation_db_level.py
  - ../.github/workflows/test.yml  # OUTSIDE Hub_All/ workspace — relative từ project root (Hub_All/) lên parent
autonomous: true
requirements:
  - TOPO-01
  - TOPO-02
  - TOPO-04
tags:
  - dynamic-hub
  - isolation-test
  - ci-workflow
  - schema-push
  - r-v3-3
  - e-v3-3

must_haves:
  truths:
    - "Script hub-init.sh thêm hub mới động: CREATE DATABASE medinet_hub_<name> + CREATE EXTENSION vector + verify HNSW + alembic upgrade head — KHÔNG cần docker compose down"
    - "Root Makefile target hub-init proxy sang api/scripts/hub-init.sh với HUB env arg"
    - "Integration test test_hub_isolation_db_level.py PASS: HUB_NAME=yte process KHÔNG access được medinet_hub_duoc qua DB connection (E-V3-3 enforce)"
    - "CI workflow ../.github/workflows/test.yml (OUTSIDE Hub_All/) step mới chạy make migrate-all + alembic-head-check.sh sau pytest (R-V3-3 mitigation) — preserve working-directory: Hub_All/api M2 pattern"
    - "[BLOCKING] make migrate-all apply 4 DB tuần tự + alembic-head-check.sh PASS — chạy SAU tất cả file modify Plan 01-04 TRƯỚC verification cuối — SAFE default Option B (preserve M2 central data, dùng make hub-init thêm 3 hub mới)"
  artifacts:
    - path: "api/scripts/hub-init.sh"
      provides: "Bash script add hub dynamic — CREATE DB + ext vector + HNSW verify + alembic upgrade head"
      contains: "medinet_hub_\\|alembic.*-x hub="
    - path: "Makefile"
      provides: "Root proxy target hub-init"
      contains: "hub-init:"
    - path: "api/Makefile"
      provides: "API-side hub-init delegate"
      contains: "hub-init:"
    - path: "api/tests/integration/test_hub_isolation_db_level.py"
      provides: "Integration test 4 DB testcontainers + connection isolation verify"
      contains: "test_hub_yte_cannot_reach_duoc"
    - path: "../.github/workflows/test.yml"
      provides: "CI step migrate-all + alembic-head-check.sh (file ngoài Hub_All workspace — relative parent dir)"
      contains: "migrate-all\\|alembic-head-check"
  key_links:
    - from: "Makefile root (hub-init)"
      to: "api/scripts/hub-init.sh"
      via: "bash api/scripts/hub-init.sh $(HUB)"
      pattern: "hub-init.sh.*HUB"
    - from: "hub-init.sh"
      to: "init-db.sh pattern (Plan 01)"
      via: "psql conditional CREATE DATABASE + CREATE EXTENSION + HNSW verify"
      pattern: "vector(1536)\\|hnsw"
    - from: "hub-init.sh"
      to: "alembic env.py per-hub (Plan 03)"
      via: "uv run alembic -x hub=<name> upgrade head"
      pattern: "alembic.*-x hub="
    - from: "Integration test"
      to: "Settings.hub_name validator (Plan 02)"
      via: "monkeypatch HUB_NAME=yte + DATABASE_URL trỏ medinet_hub_duoc → Settings raise"
      pattern: "DSN mismatch"
    - from: "../.github/workflows/test.yml (parent dir của Hub_All)"
      to: "Makefile migrate-all + alembic-head-check.sh"
      via: "step CI sau pytest, preserve working-directory: Hub_All/api"
      pattern: "make migrate-all"
---

<objective>
Đóng Phase 1 với 4 deliverable:

1. **Script dynamic hub-init** (TOPO-01 part 2) — `api/scripts/hub-init.sh HUB=<name>` tạo DB mới + extension + HNSW verify + alembic upgrade head, KHÔNG cần `docker compose down`.
2. **Integration test DB-level isolation** (TOPO-04 part 2, E-V3-3) — test `HUB_NAME=yte` process KHÔNG access được `medinet_hub_duoc` qua DB connection (Settings validator + db/session enforce).
3. **CI workflow head check** (TOPO-02 part 2, R-V3-3 mitigation) — `../.github/workflows/test.yml` (file ở parent dir của `Hub_All/`, OUTSIDE workspace) thêm step chạy `make migrate-all` + `alembic-head-check.sh` để CI gate block PR nếu Alembic drift. Preserve M2 `working-directory: Hub_All/api` pattern.
4. **[BLOCKING] Schema push (SAFE default)** — chạy `make hub-init HUB={yte,duoc,hcns}` thêm 3 hub mới mà KHÔNG xoá M2 central volume + `make migrate-all` apply Alembic upgrade head trên 4 DB + `alembic-head-check.sh` PASS, TRƯỚC verification cuối.

Purpose: TOPO-01 part 2 + TOPO-02 part 2 + TOPO-04 part 2 — đóng các REQ phần dynamic + isolation enforce + CI gate.

Output: 5 file modify/tạo (4 trong `Hub_All/`, 1 ngoài) + 1 schema push checkpoint blocking.

**Project structure note:** Project root verification context = `Hub_All/`. File `.github/workflows/test.yml` nằm ở `c:\...\medinet_wiki\.github\workflows\test.yml` — **OUTSIDE** `Hub_All/`. Mọi reference từ Plan này dùng đường dẫn relative `../.github/workflows/test.yml` (relative từ `Hub_All/` cwd) HOẶC absolute path khi command chạy ở context khác. CI workflow M2 đã có `working-directory: Hub_All/api` ở các step — Plan 05 Task 3 phải preserve pattern này.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/01-multi-db-topology/01-01-PLAN.md
@Hub_All/.planning/phases/01-multi-db-topology/01-02-PLAN.md
@Hub_All/.planning/phases/01-multi-db-topology/01-03-PLAN.md
@Hub_All/.planning/phases/01-multi-db-topology/01-04-PLAN.md
@Hub_All/api/scripts/init-db.sh
@Hub_All/api/Makefile
@Hub_All/Makefile
@Hub_All/api/tests/integration/conftest.py

<interfaces>
<!-- Snapshot CI workflow M2 (analog) — extend với migrate-all step -->

`../.github/workflows/test.yml` (PATH: parent dir của `Hub_All/`, OUTSIDE workspace) hiện tại có job test-api (line 14-87):
- `defaults.run.working-directory: Hub_All/api` (line 18-20) — **MUST PRESERVE** ở các step migrate-all + bootstrap. Step Plan 05 Task 3 thêm dùng cùng working-directory để uv/make/alembic command resolve đúng.
- Step Setup Python 3.12 + uv (no working-directory override — dùng default)
- Step Sync deps `uv sync --extra dev --frozen`
- Step Ruff check `uv run ruff check app tests`
- Step Mypy strict (chỉ critical modules)
- Step Pytest critical path + coverage gate (`--cov-fail-under=50`)
- Step Upload coverage report

→ Thêm step NEW sau "Pytest critical path" (line 79):
- Service container Postgres `pgvector/pgvector:pg16` (cùng image M2) — declare ở job-level `services:` (port 5432 expose)
- Step "Bootstrap 4 DB" — psql tạo 3 DB hub con + EXTENSION vector (service container KHÔNG mount init-db.sh)
- Step "Apply migrations 4 DB" chạy `make migrate-all` (working-directory: Hub_All/api preserve)
- Step "Verify head SHA match" — implicit qua migrate-all (Plan 03 đã wire alembic-head-check.sh vào migrate-all).

`api/tests/integration/conftest.py` hiện tại có:
- `postgres_container` fixture (PostgresContainer pgvector/pgvector:pg16, scope module)
- `redis_container` fixture
- `auth_env` fixture set env vars trỏ container

→ Test mới `test_hub_isolation_db_level.py` cần fixture mới: 4 DB cùng instance. Plan 05 chọn pattern context manager `with psycopg2.connect(...) as conn:` cho lifecycle gọn (W6 fix).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo script api/scripts/hub-init.sh + Makefile root target hub-init</name>
  <files>api/scripts/hub-init.sh, Makefile, api/Makefile</files>
  <read_first>
    - api/scripts/init-db.sh (analog pattern bash imperative)
    - Makefile (root — pattern proxy target sang api/)
    - api/Makefile (analog target style + UV variable + .PHONY)
  </read_first>
  <action>
**Step 1: Tạo `api/scripts/hub-init.sh`** — bash script imperative thêm hub động:

```bash
#!/usr/bin/env bash
# Medinet Wiki API — Dynamic hub initialization (Phase 1 v3.0 TOPO-01 part 2).
#
# Tạo 1 hub mới khi cluster Postgres đang chạy (không cần docker compose down):
#   1. CREATE DATABASE medinet_hub_<HUB> (idempotent)
#   2. CREATE EXTENSION vector
#   3. VERIFY HNSW vector(1536) build OK
#   4. uv run alembic -x hub=<HUB> upgrade head
#
# Usage:
#   bash api/scripts/hub-init.sh <hub_name>
#   HUB=<name> bash api/scripts/hub-init.sh
#
# Yêu cầu prereq:
#   - Postgres container đang chạy (Phase 1 Plan 01 init-db.sh đã chạy lần đầu HOẶC M2 cluster có sẵn)
#   - PSQL có thể connect superuser: PGHOST/PGPORT/PGUSER/PGPASSWORD env hoặc .pgpass
#   - api/.env DATABASE_URL trỏ medinet_central (để alembic -x hub=<HUB> resolve)
#
# Validation regex: pattern hub_name = `^[a-z][a-z0-9_]{1,30}$`
# (lowercase a-z bắt đầu + a-z 0-9 underscore 1-30 char) — Postgres identifier safe.
#
# Note: KHÔNG validate hub_name in _VALID_HUBS hardcoded — script này cho phép
# thêm hub MỚI (vd "phap_che", "marketing"). Validation xảy ra ở Settings layer
# (Plan 02) — nếu hub mới chưa có trong Literal, Settings sẽ raise ValueError
# ở deploy time. Để chuyển hub mới vào production, dev phải:
#   1. Chạy script này tạo DB
#   2. Update Literal trong app/config.py + Plan 03 env.py + Plan 04 flow.py

set -euo pipefail

HUB=${1:-${HUB:-}}
if [ -z "$HUB" ]; then
    echo "[hub-init] ERROR: thieu hub name. Usage: bash hub-init.sh <hub_name>"
    exit 2
fi

# Sanitize: chỉ accept lowercase a-z 0-9 underscore (Postgres identifier safe)
# Regex pattern xuất hiện literal trong validation block — Plan 05 Task 1 grep
# `grep -cF '^[a-z][a-z0-9_]' Hub_All/api/scripts/hub-init.sh` ≥ 1 cover.
if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{1,30}$ ]]; then
    echo "[hub-init] ERROR: hub name '$HUB' invalid. Pattern: ^[a-z][a-z0-9_]{1,30}$"
    exit 2
fi

DB_NAME="medinet_hub_$HUB"
PGUSER_EFFECTIVE=${PGUSER:-medinet}

echo "[hub-init] === Tao hub '$HUB' (DB '$DB_NAME') ==="

# (1/4) CREATE DATABASE idempotent
echo "[hub-init] (1/4) CREATE DATABASE $DB_NAME (idempotent)..."
exists=$(psql -tA -v ON_ERROR_STOP=1 -U "$PGUSER_EFFECTIVE" -d postgres \
    -c "SELECT 1 FROM pg_database WHERE datname='$DB_NAME'")
if [ "$exists" != "1" ]; then
    psql -v ON_ERROR_STOP=1 -U "$PGUSER_EFFECTIVE" -d postgres \
        -c "CREATE DATABASE $DB_NAME OWNER $PGUSER_EFFECTIVE;"
else
    echo "[hub-init] (1/4) $DB_NAME da ton tai, skip."
fi

# (2/4) CREATE EXTENSION vector
echo "[hub-init] (2/4) CREATE EXTENSION IF NOT EXISTS vector tren $DB_NAME..."
psql -v ON_ERROR_STOP=1 -U "$PGUSER_EFFECTIVE" -d "$DB_NAME" \
    -c "CREATE EXTENSION IF NOT EXISTS vector;"

# (3/4) VERIFY HNSW vector(1536) build OK (R1)
echo "[hub-init] (3/4) VERIFY HNSW vector(1536) build OK..."
psql -v ON_ERROR_STOP=1 -U "$PGUSER_EFFECTIVE" -d "$DB_NAME" <<-EOSQL
    DROP TABLE IF EXISTS _hnsw_dim_check;
    CREATE TABLE _hnsw_dim_check (id serial primary key, v vector(1536));
    CREATE INDEX _hnsw_dim_check_idx ON _hnsw_dim_check USING hnsw (v vector_cosine_ops);
    DROP TABLE _hnsw_dim_check;
EOSQL

# (4/4) Apply Alembic migrations
echo "[hub-init] (4/4) Apply Alembic upgrade head tren $DB_NAME..."
# Caller phải set DATABASE_URL=...medinet_central trong api/.env để env.py base DSN OK.
# alembic -x hub=$HUB sẽ resolve sang medinet_hub_$HUB (Plan 03).
(cd "$(dirname "$0")/.." && uv run alembic -x hub="$HUB" upgrade head)

echo "[hub-init] DONE — hub '$HUB' ready (DB $DB_NAME, alembic upgrade head applied)."
```

Set executable bit: chmod +x (LF line ending, UTF-8 no BOM, ASCII echo strings — locale container có thể không hỗ trợ tiếng Việt có dấu).

**Step 2: Update `api/Makefile`** — thêm target hub-init:

```makefile
# Append targets to .PHONY:
.PHONY: install dev keys keys-verify keys-verify-jwt lint test test-cov clean \
        migrate-up migrate-down migrate-check migrate-history cocoindex-setup \
        migrate-all migrate-status hub-init

# Append target sau migrate-status:
hub-init:
	@if [ -z "$(HUB)" ]; then echo "Usage: make hub-init HUB=<name>"; exit 2; fi
	@bash scripts/hub-init.sh $(HUB)
```

**Step 3: Update `Makefile` root** — thêm proxy target:

```makefile
# Update .PHONY:
.PHONY: help up down logs api-dev api-test api-lint api-keys api-install \
        eval-install eval-seed eval-clean eval-smoke eval-all eval-report \
        eval-readme eval-restore hub-init

# Append target sau api-lint section:

# ═══════════════════════════════════════════════════════════════════════════
# Phase 1 v3.0 — Dynamic hub initialization (TOPO-01 part 2)
# ═══════════════════════════════════════════════════════════════════════════
# Thêm hub mới mà KHÔNG cần docker compose down. Usage:
#   make hub-init HUB=phap_che   # tạo DB medinet_hub_phap_che + ext vector + alembic upgrade head
# Sau khi script DONE, cần update app/config.py Literal hub_name để Settings accept hub mới.

hub-init:
	@if [ -z "$(HUB)" ]; then echo "Usage: make hub-init HUB=<name>"; exit 2; fi
	$(MAKE) -C api hub-init HUB=$(HUB)
```

Update help string root Makefile thêm dòng:
```makefile
@echo "Phase 1 v3.0 Dynamic hub-init:"
@echo "  hub-init HUB=<name>     - them hub moi (CREATE DB + ext + alembic upgrade head)"
```
  </action>
  <verify>
    <automated>cd Hub_All && bash -n api/scripts/hub-init.sh && make -n hub-init HUB=phap_che 2>&1 | grep -v "Usage:" || true</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/scripts/hub-init.sh` exists, `bash -n Hub_All/api/scripts/hub-init.sh` exit 0
    - `grep -c "medinet_hub_" Hub_All/api/scripts/hub-init.sh` ≥ 1 (DB name pattern)
    - `grep -c "alembic.*-x hub=" Hub_All/api/scripts/hub-init.sh` ≥ 1 (chain với Plan 03)
    - `grep -c "vector(1536)\|hnsw" Hub_All/api/scripts/hub-init.sh` ≥ 2 (R1 carry forward)
    - `grep -c "SELECT 1 FROM pg_database" Hub_All/api/scripts/hub-init.sh` ≥ 1 (idempotent)
    - `grep -cF '^[a-z][a-z0-9_]' Hub_All/api/scripts/hub-init.sh` ≥ 1 (regex validate hub name pattern xuất hiện literal trong validation block — fixed-string grep `-F` để escape `[]` đúng)
    - `grep -c "^hub-init:" Hub_All/api/Makefile` ≥ 1
    - `grep -c "^hub-init:" Hub_All/Makefile` ≥ 1 (root proxy)
    - `grep -c "MAKE.*-C api hub-init" Hub_All/Makefile` ≥ 1 (root proxy delegate đúng)
    - `cd Hub_All && make -n hub-init HUB=foo` exit 0 (dry-run parse OK — chạy từ `Hub_All/` cwd)
    - `cd Hub_All/api && make -n hub-init` exit 2 (missing HUB enforce — chạy từ `Hub_All/api` cwd)
  </acceptance_criteria>
  <done>
hub-init.sh script bash valid + idempotent + alembic chain; root + api Makefile target proxy đúng; dry-run parse OK; missing HUB enforce exit 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo integration test test_hub_isolation_db_level.py (E-V3-3 enforce)</name>
  <files>api/tests/integration/test_hub_isolation_db_level.py</files>
  <read_first>
    - api/tests/integration/conftest.py (fixture patterns: postgres_container, redis_container, env vars)
    - api/tests/integration/test_hub_isolation.py (analog test E4 v2.0 logical isolation)
    - api/app/config.py (Plan 02 Settings.hub_name + validator)
  </read_first>
  <behavior>
    - Test 1 (`test_settings_yte_with_central_dsn_raises`): HUB_NAME=yte + DATABASE_URL trỏ medinet_central → Settings() raise ValidationError chứa "DSN mismatch hub_name"
    - Test 2 (`test_settings_yte_with_duoc_dsn_raises`): HUB_NAME=yte + DATABASE_URL trỏ medinet_hub_duoc → raise ValidationError (cross-hub typo)
    - Test 3 (`test_settings_yte_with_yte_dsn_succeeds`): HUB_NAME=yte + DATABASE_URL trỏ medinet_hub_yte → Settings instantiate OK
    - Test 4 (`test_db_connection_yte_cannot_reach_duoc`): asyncpg connect với DSN medinet_hub_yte → query SELECT current_database() trả "medinet_hub_yte"; KHÔNG có cách thay current DB qua cùng connection (asyncpg connection bound 1 DB)
    - Test 5 (`test_4_dbs_independent_data`): seed bảng test ở mỗi DB hub khác nhau → verify cross-hub query SELECT từ medinet_hub_yte KHÔNG thấy data của medinet_hub_duoc (do connection bound)
  </behavior>
  <action>
Tạo file `api/tests/integration/test_hub_isolation_db_level.py`:

```python
"""E-V3-3 enforce — DB-level hub isolation critical test (Phase 1 v3.0 TOPO-04).

EXIT criteria E-V3-3 (PROJECT.md): Hub isolation bug DB-level KHÔNG fixable trong
7 ngày = ship-blocker. v3.0 reinforce E4 từ logical (WHERE hub_id) sang physical
(DB riêng cùng instance). Process HUB_NAME=yte KHÔNG được kết nối medinet_hub_duoc.

Test này KHÔNG thay test_hub_isolation.py (E4 v2.0 logical) — chạy song song.
v2.0 test = repository layer WHERE hub_id; v3.0 test = process-config + DB conn.

MỌI test marker @pytest.mark.critical (HARD-03 CI gate) + @pytest.mark.integration.

Reuse fixture pattern conftest.py: PostgresContainer pgvector/pgvector:pg16 module-scope.
W6 fix: fixture pg_container_4dbs dùng `with psycopg2.connect(...) as conn:` context manager
cho lifecycle gọn — loại bỏ pattern manual close + no-op .replace() M2 cũ.
"""
from __future__ import annotations

from collections.abc import Iterator

import asyncpg
import psycopg2  # testcontainers-postgres dep
import pytest
from pydantic import ValidationError
from testcontainers.postgres import PostgresContainer


@pytest.fixture(scope="module")
def pg_container_4dbs() -> Iterator[PostgresContainer]:
    """Postgres container với 4 DB medinet (central + yte + duoc + hcns) pre-created.

    W6 refactor: dùng context manager `with psycopg2.connect(...) as conn:` cho
    lifecycle gọn — autocommit set qua `conn.autocommit = True` ngay sau enter
    (psycopg2 connection context manager commit/rollback transaction, KHÔNG close
    connection theo PEP-249 spec → phải close thủ công ngoài with block hoặc
    dùng `contextlib.closing`).
    """
    from contextlib import closing

    with PostgresContainer("pgvector/pgvector:pg16", driver=None) as pg:
        host = pg.get_container_host_ip()
        port = pg.get_exposed_port(5432)

        # Step 1: connect superuser default DB (postgres) → CREATE 4 DB nghiệp vụ
        with closing(psycopg2.connect(
            host=host, port=port, user=pg.username, password=pg.password, dbname="postgres",
        )) as conn:
            conn.autocommit = True
            with conn.cursor() as cur:
                for db in [
                    "medinet_central",
                    "medinet_hub_yte",
                    "medinet_hub_duoc",
                    "medinet_hub_hcns",
                ]:
                    # Idempotent: check pg_database trước CREATE (testcontainer fresh,
                    # nhưng safety cho re-run module-scope)
                    cur.execute("SELECT 1 FROM pg_database WHERE datname=%s", (db,))
                    if cur.fetchone() is None:
                        cur.execute(f"CREATE DATABASE {db}")

        # Step 2: CREATE EXTENSION vector trên mỗi DB
        for db in [
            "medinet_central",
            "medinet_hub_yte",
            "medinet_hub_duoc",
            "medinet_hub_hcns",
        ]:
            with closing(psycopg2.connect(
                host=host, port=port, user=pg.username, password=pg.password, dbname=db,
            )) as db_conn:
                db_conn.autocommit = True
                with db_conn.cursor() as db_cur:
                    db_cur.execute("CREATE EXTENSION IF NOT EXISTS vector")

        yield pg


def _build_dsn(pg: PostgresContainer, db_name: str) -> str:
    """Build SQLAlchemy async DSN cho DB hub cụ thể."""
    host = pg.get_container_host_ip()
    port = pg.get_exposed_port(5432)
    return f"postgresql+asyncpg://{pg.username}:{pg.password}@{host}:{port}/{db_name}"


def _build_asyncpg_dsn(pg: PostgresContainer, db_name: str) -> str:
    """Build asyncpg raw DSN cho DB hub cụ thể."""
    host = pg.get_container_host_ip()
    port = pg.get_exposed_port(5432)
    return f"postgresql://{pg.username}:{pg.password}@{host}:{port}/{db_name}"


@pytest.fixture(autouse=True)
def _reset_settings_cache():
    """Reset lru_cache Settings sau mỗi test để re-load env mới."""
    from app.config import get_settings
    get_settings.cache_clear()
    yield
    get_settings.cache_clear()


@pytest.mark.critical
@pytest.mark.integration
def test_settings_yte_with_central_dsn_raises(pg_container_4dbs, monkeypatch):
    """HUB_NAME=yte + DATABASE_URL trỏ medinet_central → ValidationError fail-fast."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv("DATABASE_URL", _build_dsn(pg_container_4dbs, "medinet_central"))
    monkeypatch.setenv("COCOINDEX_DATABASE_URL", _build_asyncpg_dsn(pg_container_4dbs, "medinet_central"))
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")

    from app.config import Settings
    with pytest.raises(ValidationError, match="DSN mismatch hub_name"):
        Settings()


@pytest.mark.critical
@pytest.mark.integration
def test_settings_yte_with_duoc_dsn_raises(pg_container_4dbs, monkeypatch):
    """HUB_NAME=yte + DATABASE_URL trỏ medinet_hub_duoc (cross-hub typo) → ValidationError."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv("DATABASE_URL", _build_dsn(pg_container_4dbs, "medinet_hub_duoc"))
    monkeypatch.setenv("COCOINDEX_DATABASE_URL", _build_asyncpg_dsn(pg_container_4dbs, "medinet_central"))
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")

    from app.config import Settings
    with pytest.raises(ValidationError, match="DSN mismatch hub_name"):
        Settings()


@pytest.mark.critical
@pytest.mark.integration
def test_settings_yte_with_yte_dsn_succeeds(pg_container_4dbs, monkeypatch):
    """HUB_NAME=yte + DATABASE_URL trỏ medinet_hub_yte → instantiate OK."""
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv("DATABASE_URL", _build_dsn(pg_container_4dbs, "medinet_hub_yte"))
    monkeypatch.setenv("COCOINDEX_DATABASE_URL", _build_asyncpg_dsn(pg_container_4dbs, "medinet_central"))
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")

    from app.config import Settings
    s = Settings()
    assert s.hub_name == "yte"
    assert s.database_url.endswith("/medinet_hub_yte")


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_db_connection_yte_cannot_reach_duoc(pg_container_4dbs):
    """Asyncpg connection bound 1 DB — process kết nối medinet_hub_yte
    KHÔNG có cách query data medinet_hub_duoc qua cùng connection.

    Postgres asyncpg connection = 1 connection = 1 database. Đổi DB phải reconnect
    với DSN khác. Process layer (Phase 2 FACTOR-01) sẽ enforce qua Settings validator
    + init_engine 1 lần lifespan startup.
    """
    yte_dsn = _build_asyncpg_dsn(pg_container_4dbs, "medinet_hub_yte")
    conn = await asyncpg.connect(yte_dsn)
    try:
        current = await conn.fetchval("SELECT current_database()")
        assert current == "medinet_hub_yte"

        # Tạo 1 bảng test trong yte
        await conn.execute("DROP TABLE IF EXISTS _iso_test")
        await conn.execute("CREATE TABLE _iso_test (id int, hub text)")
        await conn.execute("INSERT INTO _iso_test VALUES (1, 'yte_marker')")

        # Verify yte data
        rows = await conn.fetch("SELECT * FROM _iso_test")
        assert len(rows) == 1 and rows[0]["hub"] == "yte_marker"

        await conn.execute("DROP TABLE _iso_test")
    finally:
        await conn.close()


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_4_dbs_independent_data(pg_container_4dbs):
    """Seed bảng test ở mỗi DB hub khác nhau → verify cross-DB không thấy data.

    Verify physical isolation: DB yte INSERT data → DB duoc query KHÔNG thấy.
    """
    # Seed mỗi DB
    for hub, marker in [("yte", "alpha"), ("duoc", "beta"), ("hcns", "gamma")]:
        dsn = _build_asyncpg_dsn(pg_container_4dbs, f"medinet_hub_{hub}")
        conn = await asyncpg.connect(dsn)
        try:
            await conn.execute("DROP TABLE IF EXISTS _iso_marker")
            await conn.execute("CREATE TABLE _iso_marker (m text)")
            await conn.execute("INSERT INTO _iso_marker VALUES ($1)", marker)
        finally:
            await conn.close()

    # Verify mỗi DB chỉ thấy marker của chính nó
    for hub, expected in [("yte", "alpha"), ("duoc", "beta"), ("hcns", "gamma")]:
        dsn = _build_asyncpg_dsn(pg_container_4dbs, f"medinet_hub_{hub}")
        conn = await asyncpg.connect(dsn)
        try:
            result = await conn.fetchval("SELECT m FROM _iso_marker")
            assert result == expected, (
                f"Cross-DB leak detected: medinet_hub_{hub} thấy marker {result!r} "
                f"thay vì {expected!r} — E-V3-3 fail."
            )
        finally:
            await conn.close()

    # Cleanup
    for hub in ["yte", "duoc", "hcns"]:
        dsn = _build_asyncpg_dsn(pg_container_4dbs, f"medinet_hub_{hub}")
        conn = await asyncpg.connect(dsn)
        try:
            await conn.execute("DROP TABLE IF EXISTS _iso_marker")
        finally:
            await conn.close()
```

Note: `pg_container_4dbs` fixture (W6 refactor) dùng `contextlib.closing(psycopg2.connect(...))` cho lifecycle gọn — connection autocommit set ngay sau enter; nested `with conn.cursor()` đảm bảo cursor close. Loại bỏ pattern `.replace("postgresql://", "postgresql://", 1)` no-op M2 cũ (vô nghĩa — string không đổi). Nếu `psycopg2` không có trong test deps → testcontainers-postgres tự pull (verify trong pyproject.toml).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_hub_isolation_db_level.py -v -m "critical and integration"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_hub_isolation_db_level.py` exists
    - `grep -c "def test_settings_yte_with_central_dsn_raises\|def test_settings_yte_with_duoc_dsn_raises\|def test_settings_yte_with_yte_dsn_succeeds\|def test_db_connection_yte_cannot_reach_duoc\|def test_4_dbs_independent_data" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 5 (5 test theo behavior)
    - `grep -c "@pytest.mark.critical" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 5 (mọi test critical)
    - `grep -c "@pytest.mark.integration" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 5
    - `grep -c "PostgresContainer.*pgvector" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 1 (image pin)
    - `grep -c "medinet_hub_yte\|medinet_hub_duoc\|medinet_hub_hcns" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 3
    - `grep -c "E-V3-3" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 1
    - `grep -c "from contextlib import closing" Hub_All/api/tests/integration/test_hub_isolation_db_level.py` ≥ 1 (W6 — context manager pattern)
    - `grep -c 'replace("postgresql' Hub_All/api/tests/integration/test_hub_isolation_db_level.py` == 0 (W6 — no-op .replace() đã xoá)
    - `cd Hub_All/api && uv run pytest tests/integration/test_hub_isolation_db_level.py -v -m "critical and integration"` PASS 5/5 (yêu cầu Docker Desktop chạy testcontainers)
    - `cd Hub_All/api && uv run mypy tests/integration/test_hub_isolation_db_level.py` exit 0
    - `cd Hub_All/api && uv run ruff check tests/integration/test_hub_isolation_db_level.py` exit 0
  </acceptance_criteria>
  <done>
Integration test PASS 5/5; E-V3-3 enforce verify Settings validator + asyncpg connection-bound isolation; cross-DB query KHÔNG leak; fixture dùng context manager gọn (W6 fix).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update ../.github/workflows/test.yml — thêm CI step migrate-all + alembic-head-check.sh (R-V3-3 mitigation)</name>
  <files>../.github/workflows/test.yml  # OUTSIDE Hub_All/ — file ở parent dir (c:\...\medinet_wiki\.github\workflows\test.yml). Plan 05 reference qua relative `../.github/workflows/test.yml` từ `Hub_All/` cwd HOẶC absolute path. CI workflow M2 hiện tại đã có `defaults.run.working-directory: Hub_All/api` ở job test-api (line 18-20) — Plan 05 Task 3 PRESERVE pattern này cho steps mới.</files>
  <read_first>
    - ../.github/workflows/test.yml (file đang sửa — job test-api existing steps + `defaults.run.working-directory: Hub_All/api` ở line 18-20 PHẢI preserve)
    - api/scripts/alembic-head-check.sh (Plan 03 — script CI gọi)
    - api/Makefile (target migrate-all đã có Plan 03)
  </read_first>
  <action>
**Project structure context:** File `test.yml` nằm OUTSIDE `Hub_All/` ở path `c:\...\medinet_wiki\.github\workflows\test.yml`. Khi execute task này, command relative reference qua `../.github/workflows/test.yml` (relative từ `Hub_All/` cwd) HOẶC absolute path. CI workflow đã có `defaults.run.working-directory: Hub_All/api` (line 18-20) — Plan 05 phải preserve pattern này, các step mới của Plan 05 KHÔNG override working-directory (trừ "Bootstrap 4 DB" có thể dùng `working-directory: Hub_All` để psql command access từ project root).

Mở `../.github/workflows/test.yml`. Thêm 1 service container + 2 step mới vào job `test-api`:

**Step 1: Thêm `services` block** (chèn ngay sau `defaults` block line 21, trước `steps:`):

```yaml
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: medinet
          POSTGRES_PASSWORD: medinet_ci_pwd
          POSTGRES_DB: medinet_central
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U medinet -d medinet_central"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
```

**Step 2: Thêm step "Bootstrap 4 DB" sau "Sync deps" (line 41)** — note: step này dùng `working-directory: Hub_All` (parent dir của `Hub_All/api`) để run psql command từ project root, KHÔNG dùng default `Hub_All/api`:

```yaml
      - name: Bootstrap 4 DB cho per-hub Alembic (Plan 01 init-db.sh equivalent)
        env:
          PGHOST: localhost
          PGPORT: 5432
          PGUSER: medinet
          PGPASSWORD: medinet_ci_pwd
        working-directory: Hub_All
        run: |
          # CI service container postgres KHÔNG mount init-db.sh — bootstrap thủ công
          # 3 DB hub con + CREATE EXTENSION vector (central đã có qua POSTGRES_DB env)
          for db in medinet_central medinet_hub_yte medinet_hub_duoc medinet_hub_hcns; do
            psql -d postgres -c "SELECT 1 FROM pg_database WHERE datname='$db'" -tA | grep -q 1 || \
              psql -d postgres -c "CREATE DATABASE $db OWNER medinet"
            psql -d "$db" -c "CREATE EXTENSION IF NOT EXISTS vector"
          done
          echo "[ci-bootstrap] 4 DB ready."
```

**Step 3: Thêm step "Apply migrate-all + verify head SHA" sau "Pytest critical path"** (line 79) — step này dùng default `working-directory: Hub_All/api` (preserve M2 pattern, KHÔNG override):

```yaml
      - name: Apply migrate-all + verify head SHA match (TOPO-02, R-V3-3 mitigation)
        env:
          DATABASE_URL: postgresql+asyncpg://medinet:medinet_ci_pwd@localhost:5432/medinet_central
          COCOINDEX_DATABASE_URL: postgresql://medinet:medinet_ci_pwd@localhost:5432/medinet_central
          REDIS_URL: redis://localhost:6379/0
          HUB_NAME: central
          # JWT keys placeholder (test mode — Plan 03 v2.0 generate keys runtime)
          JWT_PRIVATE_KEY_PATH: ./keys/private.pem
          JWT_PUBLIC_KEY_PATH: ./keys/public.pem
        # working-directory default Hub_All/api (preserve M2) — make migrate-all chạy từ api/
        run: |
          # Generate keys nếu missing (CI fresh runner)
          bash scripts/generate_keys.sh 2>/dev/null || true
          # Apply 4 DB tuần tự + verify head SHA match
          make migrate-all
```

**Step 4: Verify job dependency** — existing pipeline dùng testcontainers spin Postgres riêng trong pytest fixture (`postgres_container` test_critical_path_coverage.py). Service container CI ngoài + testcontainers KHÔNG conflict (port 5432 service ngoài fixed, testcontainers random ephemeral port). OK.

Lưu ý:
- Migrate-all step yêu cầu `bash scripts/alembic-head-check.sh` chạy được (Plan 03 đã wire vào `make migrate-all`). CI runner ubuntu-22.04 có sẵn bash + psql client.
- Path reference toàn bộ Plan 05 dùng relative từ `Hub_All/`: `../.github/workflows/test.yml`. Khi `gsd-execute-phase` chạy, cwd = `Hub_All/` → path resolve đúng.
- W7 fix consistency: mọi command verification chạy từ `Hub_All/` cwd, paths relative.
  </action>
  <verify>
    <automated>cd Hub_All && python -c "import yaml; yaml.safe_load(open('../.github/workflows/test.yml'))" && grep -c "migrate-all\|alembic-head-check\|medinet_hub_" ../.github/workflows/test.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "make migrate-all" Hub_All/../.github/workflows/test.yml` ≥ 1 (path từ Hub_All/ relative — verifier resolve qua parent dir)
    - `grep -c "medinet_hub_yte\|medinet_hub_duoc\|medinet_hub_hcns" Hub_All/../.github/workflows/test.yml` ≥ 3 (CI bootstrap 3 hub con)
    - `grep -c "services:" Hub_All/../.github/workflows/test.yml` ≥ 1 (postgres service container)
    - `grep -c "pgvector/pgvector:pg16" Hub_All/../.github/workflows/test.yml` ≥ 1 (image pin)
    - `grep -c "R-V3-3\|TOPO-02" Hub_All/../.github/workflows/test.yml` ≥ 1 (REQ ID traceability)
    - `grep -c "HUB_NAME: central" Hub_All/../.github/workflows/test.yml` ≥ 1
    - `grep -c "working-directory: Hub_All/api" Hub_All/../.github/workflows/test.yml` ≥ 1 (M2 pattern preserved — `defaults.run.working-directory` ở line 18-20)
    - `grep -c "working-directory: Hub_All$\|working-directory: Hub_All " Hub_All/../.github/workflows/test.yml` ≥ 1 (Plan 05 bootstrap step dùng Hub_All/ root cho psql command)
    - `cd Hub_All && python -c "import yaml; yaml.safe_load(open('../.github/workflows/test.yml'))"` exit 0 (YAML valid, command chạy từ Hub_All/ cwd)
    - `grep -c "Bootstrap 4 DB\|Apply migrate-all" Hub_All/../.github/workflows/test.yml` ≥ 2 (cả 2 step name)
  </acceptance_criteria>
  <done>
../.github/workflows/test.yml CI workflow extend với postgres service + bootstrap 4 DB + migrate-all + auto head check; YAML valid; R-V3-3 mitigation chain wire CI gate; M2 working-directory: Hub_All/api pattern preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: [BLOCKING] Schema push — SAFE default Option B (preserve M2 central) — chạy make hub-init x3 + make migrate-all + alembic-head-check.sh PASS</name>
  <files type="runtime-only">none — schema push execution against live Postgres (no file modify; verification chuỗi lệnh runtime against running cluster)</files>
  <read_first>
    - api/Makefile (migrate-all target + hub-init target Plan 03 + Plan 05 Task 1 đã có)
    - api/scripts/alembic-head-check.sh (Plan 03 đã có)
    - api/scripts/hub-init.sh (Plan 05 Task 1 đã có)
    - api/scripts/init-db.sh (Plan 01 đã có — chỉ fallback Option A)
    - api/.env (deployer phải có DATABASE_URL=...medinet_central + HUB_NAME=central)
  </read_first>
  <action>
**[BLOCKING] Schema push task — chạy SAU khi tất cả file Plan 01-04 + Plan 05 Task 1-3 ship TRƯỚC verification cuối. Task này runtime-only — KHÔNG modify file nào.**

**SAFE default: Option B (preserve M2 central data) — KHÔNG xoá volume M2 hiện tại.**

Default action `docker compose down -v` Option A XOÁ TOÀN BỘ M2 corpus volume (medinet_central + medinet_cocoindex data) — KHÔNG safe nếu user có M2 corpus đã ingest. Plan 05 Task 4 default sang Option B: chỉ thêm 3 hub mới (yte/duoc/hcns) qua `make hub-init` mà KHÔNG động volume hiện tại. M2 central data preserve nguyên.

Option A (`docker compose down -v`) CHỈ dùng nếu user CHỦ Ý xoá M2 data — yêu cầu manual confirm (`MEDINET_CONFIRM_RESET=YES_DELETE_M2` env trước khi chạy).

**Sequence Option B (SAFE default):**

```bash
# Step 0: Verify cluster đang chạy + medinet_central preserve
cd Hub_All
docker compose ps postgres
# Expected: postgres service "Up (healthy)" status

# Verify M2 central DB tồn tại (preserve check)
docker exec medinet-postgres psql -U medinet -d postgres -c \
  "SELECT datname FROM pg_database WHERE datname='medinet_central'" | grep -q medinet_central
# Exit 0 nếu central tồn tại — OK preserve.
# Exit 1 nếu central missing — fresh cluster, fallback init-db.sh chạy lần đầu (docker compose up tự trigger).

# Step 1: ensure postgres + redis up (M2 medinet_central preserved)
docker compose up -d
sleep 10  # wait healthcheck

# Step 2: Dynamic add 3 hub mới (KHÔNG động M2 volume hiện tại)
cd Hub_All/api
make hub-init HUB=yte
make hub-init HUB=duoc
make hub-init HUB=hcns

# Mỗi hub-init script execute Plan 05 Task 1 sequence:
# (1/4) CREATE DATABASE medinet_hub_<hub> idempotent
# (2/4) CREATE EXTENSION vector
# (3/4) VERIFY HNSW vector(1536) build OK
# (4/4) uv run alembic -x hub=<hub> upgrade head

# Step 3: Re-apply migrations toàn bộ 4 DB (central re-check + 3 hub mới)
# Lý do: alembic head có thể đã advance sau hub-init (Plan 04 cocoindex schema migration)
# → migrate-all đảm bảo central cùng head SHA với 3 hub mới.
make migrate-all

# Expected output:
# [migrate-all] === hub=central ===  (no-op nếu đã ở head)
# [migrate-all] === hub=yte ===      (no-op nếu hub-init đã apply)
# [migrate-all] === hub=duoc ===
# [migrate-all] === hub=hcns ===
# [alembic-head-check] PASS — tất cả 4 DB cùng head SHA: <rev_sha>

# Step 4: Verify head SHA match standalone
bash scripts/alembic-head-check.sh
# Expected: "PASS — tất cả 4 DB cùng head SHA: <rev_sha>" + exit 0
```

**Sequence Option A (DESTRUCTIVE — chỉ khi user CHỦ Ý xoá M2 data):**

```bash
# Yêu cầu env confirm tường minh:
if [ "$MEDINET_CONFIRM_RESET" != "YES_DELETE_M2" ]; then
    echo "Option A xoá M2 data — set MEDINET_CONFIRM_RESET=YES_DELETE_M2 để confirm"
    exit 1
fi

cd Hub_All
docker compose down -v  # XOÁ medinet_pgdata volume — M2 central + cocoindex data MẤT
docker compose up -d postgres
sleep 30  # wait init-db.sh Plan 01 chạy tạo 4 DB fresh
cd api
make migrate-all  # apply Alembic head trên 4 DB fresh
bash scripts/alembic-head-check.sh
```

**Spot-check verify HNSW index tồn tại trên 4 DB (sau khi migrate-all):**

```bash
for db in medinet_central medinet_hub_yte medinet_hub_duoc medinet_hub_hcns; do
    echo "=== Verify $db ==="
    docker exec medinet-postgres psql -U medinet -d $db -c \
        "SELECT indexname FROM pg_indexes WHERE tablename='chunks' AND indexname='ix_chunks_vector_hnsw'"
    # \d chunks per hub — verify schema có chunks table + HNSW index
    docker exec medinet-postgres psql -U medinet -d $db -c "\d chunks" | grep "ix_chunks_vector_hnsw"
done
```

Expected: mỗi DB trả 1 row chứa `ix_chunks_vector_hnsw` ở pg_indexes + `\d chunks` show index.

**Spot-check `alembic current` từng DB qua migrate-status:**

```bash
cd Hub_All/api
make migrate-status
```

Verify 4 DB cùng head SHA.

**Fail conditions (rollback / re-investigate):**
- `make hub-init HUB=<name>` raise lỗi ở step 1-3 (DB create / ext / HNSW verify) → init-db pattern Plan 01 wrong → re-investigate Plan 05 Task 1 script.
- `make hub-init HUB=<name>` raise lỗi ở step 4 (alembic upgrade) → Plan 03 env.py resolve_env_database_url bug → re-investigate Plan 03 Task 1.
- `make migrate-all` raise lỗi ở bất kỳ hub → re-investigate Alembic env.py Plan 03 + Settings.database_url Plan 02 chain.
- `alembic-head-check.sh` exit 1 (drift) → R-V3-3 trigger; investigate version mismatch + re-run migrate-all.
- HNSW index missing ở 1 DB → init-db.sh Plan 01 hoặc hub-init.sh Plan 05 Task 1 không apply ext vector đúng → re-investigate.

**KHÔNG có file modification ở task này** — chỉ runtime execution + verification. Files modified declared ở 3 task trước (1, 2, 3) — frontmatter `files_modified` Plan 05 phản ánh.

Task này blocking cho verification cuối (Phase 1 close). Nếu bất kỳ step fail → STOP execute-phase + report root cause cho user trước khi tiếp tục.
  </action>
  <verify>
    <automated>cd Hub_All/api && make migrate-status 2>&1 | grep -E "head=|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/api && make hub-init HUB=yte` exit 0 (Plan 05 Task 1 script chain Plan 03 alembic OK)
    - `cd Hub_All/api && make hub-init HUB=duoc` exit 0
    - `cd Hub_All/api && make hub-init HUB=hcns` exit 0
    - `cd Hub_All/api && make migrate-all` exit 0 (4 DB apply tuần tự + head check PASS)
    - `cd Hub_All/api && bash scripts/alembic-head-check.sh` tail line chứa "PASS"
    - `cd Hub_All/api && bash scripts/alembic-head-check.sh` exit 0
    - 4 DB cùng head SHA (verify qua `make migrate-status` output)
    - `docker exec medinet-postgres psql -U medinet -d medinet_hub_yte -c "SELECT 1 FROM pg_indexes WHERE tablename='chunks' AND indexname='ix_chunks_vector_hnsw'"` trả ≥ 1 row
    - `docker exec medinet-postgres psql -U medinet -d medinet_hub_yte -c "\d chunks"` output chứa "ix_chunks_vector_hnsw" (index attached to chunks table)
    - Tương tự cho `medinet_hub_duoc`, `medinet_hub_hcns`, `medinet_central`
    - `docker exec medinet-postgres psql -U medinet -d medinet_hub_yte -c "SELECT COUNT(*) FROM alembic_version"` trả `1` (đúng 1 head rev)
    - **M2 central preserve verify**: `docker exec medinet-postgres psql -U medinet -d medinet_central -c "SELECT COUNT(*) FROM documents"` exit 0 (table tồn tại; nếu M2 đã ingest > 0 rows preserve — verify count >= count trước Plan 05)
  </acceptance_criteria>
  <done>
Schema push 4 DB DONE qua Option B SAFE default (preserve M2 central). alembic-head-check.sh PASS. HNSW vector(1536) index tồn tại trên 4 DB. M2 central data preserve (documents table intact). Phase 1 ready cho verification cuối.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev → hub-init.sh | HUB arg shell sanitize regex `^[a-z][a-z0-9_]{1,30}$` chống injection |
| hub-init.sh → Postgres superuser | psql -U medinet (POSTGRES_USER) — superuser cần để CREATE DATABASE |
| Integration test → testcontainers postgres | PostgresContainer isolated per test module — không leak vào local dev cluster |
| CI workflow (../github/workflows/test.yml) → service container postgres | GitHub Actions service container ephemeral; credentials hard-coded test env (medinet_ci_pwd) — chấp nhận |
| [BLOCKING] Schema push → existing M2 volume | Option B default preserve M2 central volume (KHÔNG `docker compose down -v`); Option A destructive yêu cầu MEDINET_CONFIRM_RESET=YES_DELETE_M2 env confirm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | hub-init.sh accept HUB arg với SQL injection (vd `; DROP DATABASE medinet_central;`) | mitigate | Regex `^[a-z][a-z0-9_]{1,30}$` validate HUB pattern Postgres-identifier-safe (lowercase ASCII + digits + underscore, ≤ 30 char). Reject sớm exit 2 trước psql call. |
| T-01-05-02 | Elevation of Privilege | Dev chạy hub-init.sh trên prod cluster vô tình | accept | Script không có guard env detection; dev responsibility. Production deploy sẽ qua ops automation (Phase 7 migration) — hub-init.sh chỉ cho dev/staging dynamic add. |
| T-01-05-03 | Information Disclosure | Integration test seed `_iso_marker` table trong container test → có thể leak nếu test fail giữa chừng | mitigate | Test cleanup `DROP TABLE IF EXISTS _iso_marker` trong finally block + container module-scope (sau test module xong là drop hết). |
| T-01-05-04 | Denial of Service | CI service container postgres không đủ resource → migrate-all timeout | accept | Job timeout-minutes: 20 (M2 baseline). 4 DB × ~4 migration × ~5s = ~80s — đủ headroom. Nếu fail timeout → ops bump timeout-minutes or split CI job. |
| T-01-05-05 | Repudiation | Schema push step 4 ([BLOCKING]) chạy thủ công không log evidence | mitigate | make migrate-all output có timestamp + hub identity + rev SHA per hub; capture qua execute-phase summary.md (template HARD-04 retrospective). |
| T-01-05-06 | Information Disclosure | hub-init.sh log PGPASSWORD ra stdout vô tình | mitigate | Script KHÔNG echo $PGPASSWORD; psql đọc PGPASSWORD từ env (libpq pattern) — không log credential. Verify qua grep `grep -c "PGPASSWORD" Hub_All/api/scripts/hub-init.sh` chỉ thấy ở comment hoặc env reference (không echo). |
| T-01-05-07 | Tampering / Data Loss | [BLOCKING] Schema push Option A (`docker compose down -v`) vô tình xoá M2 central volume khi user KHÔNG chủ ý | mitigate | Plan 05 Task 4 default Option B (preserve M2 central — chỉ `make hub-init` add 3 hub mới, KHÔNG động volume hiện tại). Option A guard tường minh `MEDINET_CONFIRM_RESET=YES_DELETE_M2` env. Acceptance criteria verify documents table preserve (`SELECT COUNT(*) FROM documents` exit 0). |
</threat_model>

<verification>
**Phase 1 close verification chain (sau khi mọi Plan 01-05 ship + schema push DONE):**

1. **File integrity (grep)**:
   - Files modified verified across Plan 01-05: docker-compose.yml, api/scripts/init-db.sh, api/app/config.py, api/app/db/session.py, api/.env.example, api/migrations/env.py, api/Makefile, api/scripts/alembic-head-check.sh, api/app/rag/flow.py, api/app/rag/setup.py, api/scripts/hub-init.sh, Makefile, api/tests/integration/test_hub_isolation_db_level.py, ../.github/workflows/test.yml
   - Total ≥ 14 file modify (đủ scope Phase 1)

2. **Unit + integration test PASS**:
   - `cd Hub_All/api && uv run pytest tests/unit/test_config_hub_name.py tests/unit/test_alembic_env_hub_arg.py tests/unit/test_flow_per_hub_naming.py -v` PASS toàn bộ
   - `cd Hub_All/api && uv run pytest tests/integration/test_hub_isolation_db_level.py -v -m "critical and integration"` PASS 5/5

3. **Lint + type clean**:
   - `cd Hub_All/api && uv run ruff check app tests migrations scripts` exit 0
   - `cd Hub_All/api && uv run mypy app/config.py app/db/session.py app/rag/flow.py app/rag/setup.py migrations/env.py` exit 0

4. **Schema push PASS (Task 4 acceptance, Option B SAFE default)**:
   - 4 DB cùng head SHA
   - HNSW vector(1536) index tồn tại ở mỗi DB
   - M2 central data preserve (documents table COUNT >= pre-Plan 05 count)

5. **CI workflow YAML valid**:
   - `cd Hub_All && python -c "import yaml; yaml.safe_load(open('../.github/workflows/test.yml'))"` exit 0

6. **Goal verification (Phase 1 success criteria)**:
   - SC1: `make hub-init HUB=yte/duoc/hcns` + verify `docker exec medinet-postgres psql -U medinet -d postgres -c "SELECT count(*) FROM pg_database WHERE datname LIKE 'medinet_hub_%'"` trả `3`
   - SC2: `cd Hub_All/api && make migrate-all && bash scripts/alembic-head-check.sh` tail line "PASS"
   - SC3: cocoindex flow per-hub name → smoke ở Phase 2 (FACTOR-01 wire lifespan); Plan 01-04 unit test cover pattern; **note**: Sau Plan 04 deploy default HUB_NAME=central, cocoindex App name `medinet_wiki_ingest` (M2) → `medinet_central_ingest` (Phase 1) gây orphan M2 cocoindex internal state — acceptable state reset (Plan 04 threat T-01-04-XX + post-deploy re-ingest documents.status=pending mitigation; xem chi tiết Plan 04 threat model)
   - SC4: Integration test test_hub_isolation_db_level.py PASS 5/5
</verification>

<success_criteria>
- hub-init.sh script bash valid + idempotent + alembic chain (Task 1)
- Root + api Makefile target hub-init proxy đúng (Task 1)
- Integration test test_hub_isolation_db_level.py PASS 5/5 với testcontainers (Task 2)
- CI workflow ../.github/workflows/test.yml extend postgres service + migrate-all + head check (Task 3)
- [BLOCKING] make hub-init x3 + make migrate-all apply 4 DB + head check PASS + HNSW index verified + M2 central preserve (Task 4 SAFE default Option B)
- Phase 1 success criteria 1-4 (PROJECT.md ROADMAP) verified
</success_criteria>

<output>
Tạo `.planning/phases/01-multi-db-topology/01-05-SUMMARY.md`:
- Dynamic hub-init.sh interface (HUB arg + sanitize regex + alembic chain)
- E-V3-3 enforce: 5 integration test cover Settings validator + asyncpg connection-bound
- R-V3-3 mitigation CI gate: postgres service + migrate-all + alembic-head-check.sh ở `../.github/workflows/test.yml` (OUTSIDE Hub_All/, preserve working-directory: Hub_All/api M2 pattern)
- Schema push DONE qua Option B SAFE default — preserve M2 central data, dynamic add 3 hub mới; timestamp + 4 DB head SHA + HNSW index verified
- Phase 1 close confirmation: 4 success criteria PASS

Cập nhật `.planning/STATE.md`:
- Position: Phase 1 — Plan 05 done — Phase 1 close
- progress.completed_plans = 5, total_plans = 5, completed_phases = 0 → 1, percent ≈ 14%
- Next action: `/gsd-discuss-phase 2` Hub-con codebase factor
</output>
