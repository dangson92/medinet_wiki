---
phase: 5
plan: 05
type: execute
wave: 4
depends_on: [05-01]
files_modified:
  - Hub_All/api/scripts/hub-add.sh
autonomous: true
requirements: [PROXY-01]
requirements_extended: [FACTOR-04]
tags: [scripts, hub-add, caddy-reload, factor-04-extend, v3.0, phase5]

must_haves:
  truths:
    - "hub-add.sh thêm step 8 — sed-edit .env update HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX idempotent atomic (preserve other env vars)"
    - "hub-add.sh thêm step 9 — `caddy validate` PRE-check + `caddy reload` zero-downtime + smoke `curl /<new>/api/health`"
    - "Step numbering updated (7/7) → (9/9) trong comment header"
    - "bash -n syntax check exit 0"
    - "Reserved hub blacklist + regex validate Plan 02-05 carry forward (Step 1-3 unchanged) — T-5-05 mitigation"
  artifacts:
    - path: "Hub_All/api/scripts/hub-add.sh"
      provides: "9-step pipeline (Plan 02-05 7-step + Phase 5 step 8 HUBS_ALLOWLIST update + step 9 caddy validate + reload + smoke)"
      contains: "caddy reload"
  key_links:
    - from: "hub-add.sh step 8"
      to: ".env HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX"
      via: "sed atomic edit qua tmp file + mv"
      pattern: "HUBS_ALLOWLIST="
    - from: "hub-add.sh step 9"
      to: "Caddy admin API zero-downtime reload"
      via: "docker compose exec -T caddy caddy reload --config /etc/caddy/Caddyfile"
      pattern: "caddy reload"
---

<objective>
Wave 4 — Mở rộng `Hub_All/api/scripts/hub-add.sh` thêm step 8 + 9 sau Plan 02-05 7-step pipeline (FACTOR-04 carry forward). Step 8: idempotent atomic update `.env` HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX (preserve các env khác). Step 9: PRE-validate Caddyfile (avoid silent rollback per Pitfall 7) + zero-downtime `caddy reload` + smoke `curl /<new>/api/health` post-reload.

Purpose: PROXY-01 + FACTOR-04 extend — operator chạy `make hub-add HUB=phap_che` → tự động register hub mới với Caddy KHÔNG cần manual edit Caddyfile / restart container. T-5-05 mitigation carry forward Plan 02-05 (regex validate + RESERVED_HUB_NAMES blacklist) — Phase 5 KHÔNG relax guard.

Output: 1 file edit (append ~85 dòng); bash -n syntax check PASS; dry-run với mock HUB không side-effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md
@.planning/phases/02-hub-con-codebase-factor/02-05-PLAN.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-01-PLAN.md

<interfaces>
<!-- Plan 02-05 hub-add.sh existing structure (7-step pipeline) -->
From Hub_All/api/scripts/hub-add.sh (Plan 02-05):
Steps 1-7:
1. Regex validate `^[a-z][a-z0-9_]{0,15}$` HUB= input (T-5-05 mitigation)
2. RESERVED_HUB_NAMES blacklist check (postgres/cocoindex/template0/template1/public/medinet)
3. Duplicate detect (existing in docker-compose.override.yml)
4. Auto-detect port (max+1 or operator PORT=<port>)
5. Call hub-init.sh (DB layer Phase 1)
6. Append docker-compose.override.yml.template sed substitute
7. (7a) Append service block + (7b) Append volume + (7c) docker compose config --quiet verify

Existing end (line ~218):
echo "[hub-add] DONE — hub '$HUB' ready."

<!-- Plan 05-01 .env structure to consume -->
From Plan 05-01 .env.example (operator copy to .env):
HUBS_ALLOWLIST=yte,duoc,hcns
HUBS_ALLOWLIST_REGEX=yte|duoc|hcns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append step 8 + step 9 vào hub-add.sh (PROXY-01 + FACTOR-04 + T-5-05 carry forward)</name>
  <files>Hub_All/api/scripts/hub-add.sh</files>
  <read_first>
    - Hub_All/api/scripts/hub-add.sh (full file — đọc end of file để hiểu vị trí "DONE" message + understand variable names $HUB, $PORT, $COMPOSE_ROOT)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"hub-add.sh extension Phase 5 (step 8 + 9)" lines 689-775 (verbatim bash snippet)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md §"Implementation Decisions A3" (FACTOR-04 carry forward)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"Pitfall 7" lines 558-562 (Caddy reload silent rollback — PRE-validate required)
  </read_first>
  <action>
INSERT block sau khi Step 7c success exit (sau line `echo "[hub-add] DONE — hub '$HUB' ready."` chuyển xuống cuối — tức là block 8+9 PHẢI nằm GIỮA Step 7c và DONE message):

**Locate insertion point:** Tìm dòng `echo "[hub-add] DONE — hub '$HUB' ready."` trong hub-add.sh. INSERT block sau dưới TRƯỚC dòng đó (để DONE message in cuối cùng SAU khi 9 step complete):

```bash

# ──────────────────────────────────────────────────────────────────────────
# (8/9) Cap nhat .env HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX (Phase 5 PROXY-01)
# ──────────────────────────────────────────────────────────────────────────
# Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"hub-add.sh extension"
# Atomic edit qua tmp file + mv — preserve other env vars in .env

ENV_FILE="$COMPOSE_ROOT/.env"

# Idempotent: tao .env tu .env.example neu chua co
if [ ! -f "$ENV_FILE" ]; then
    if [ -f "$COMPOSE_ROOT/.env.example" ]; then
        cp "$COMPOSE_ROOT/.env.example" "$ENV_FILE"
        echo "[hub-add] (8) Created .env tu .env.example"
    else
        # Minimum .env fallback - Phase 5 minimum keys
        echo "HUBS_ALLOWLIST=yte,duoc,hcns" > "$ENV_FILE"
        echo "HUBS_ALLOWLIST_REGEX=yte|duoc|hcns" >> "$ENV_FILE"
    fi
fi

# Extract current allowlist (default empty if missing key)
CURRENT_ALLOWLIST=$(grep -E "^HUBS_ALLOWLIST=" "$ENV_FILE" | head -1 | cut -d= -f2- || echo "")

# Check duplicate (hub da co)
if [[ ",$CURRENT_ALLOWLIST," == *",$HUB,"* ]]; then
    echo "[hub-add] (8) HUB '$HUB' da co trong HUBS_ALLOWLIST — skip env update"
else
    # Append hub vao allowlist
    NEW_ALLOWLIST="${CURRENT_ALLOWLIST:+$CURRENT_ALLOWLIST,}$HUB"
    NEW_REGEX=$(echo "$NEW_ALLOWLIST" | tr ',' '|')

    # Atomic edit qua tmp file + mv (preserve other env vars in .env)
    TMP_ENV=$(mktemp)
    trap 'rm -f "$TMP_ENV"' EXIT

    # Update HUBS_ALLOWLIST
    if grep -qE "^HUBS_ALLOWLIST=" "$ENV_FILE"; then
        sed "s|^HUBS_ALLOWLIST=.*|HUBS_ALLOWLIST=$NEW_ALLOWLIST|" "$ENV_FILE" > "$TMP_ENV"
    else
        cp "$ENV_FILE" "$TMP_ENV"
        echo "HUBS_ALLOWLIST=$NEW_ALLOWLIST" >> "$TMP_ENV"
    fi

    # Update HUBS_ALLOWLIST_REGEX (in tmp file, not original)
    if grep -qE "^HUBS_ALLOWLIST_REGEX=" "$TMP_ENV"; then
        TMP_ENV2=$(mktemp)
        sed "s|^HUBS_ALLOWLIST_REGEX=.*|HUBS_ALLOWLIST_REGEX=$NEW_REGEX|" "$TMP_ENV" > "$TMP_ENV2"
        mv "$TMP_ENV2" "$TMP_ENV"
    else
        echo "HUBS_ALLOWLIST_REGEX=$NEW_REGEX" >> "$TMP_ENV"
    fi

    mv "$TMP_ENV" "$ENV_FILE"
    trap - EXIT
    echo "[hub-add] (8) Updated .env HUBS_ALLOWLIST=$NEW_ALLOWLIST"
    echo "[hub-add] (8) Updated .env HUBS_ALLOWLIST_REGEX=$NEW_REGEX"
fi

# ──────────────────────────────────────────────────────────────────────────
# (9/9) Caddy validate + reload zero-downtime (Phase 5 PROXY-01)
# ──────────────────────────────────────────────────────────────────────────
# Pitfall 7 mitigation: PRE-validate config TRUOC reload — Caddy silent rollback
# old config khi new config invalid; operator KHONG biet failure.

# Check neu caddy container dang chay
if docker compose ps caddy --format json 2>/dev/null | grep -q '"State":"running"'; then
    echo "[hub-add] (9) Validate Caddy config TRUOC reload..."

    # PRE-validate — fail-fast neu config invalid (avoid silent rollback)
    if ! docker compose exec -T caddy caddy validate --config /etc/caddy/Caddyfile 2>&1; then
        echo "[hub-add] ERROR: Caddy config validate FAIL — rollback .env edit can"
        echo "  Manual rollback: sed -i 's|^HUBS_ALLOWLIST=.*|HUBS_ALLOWLIST=$CURRENT_ALLOWLIST|' $ENV_FILE"
        echo "                  sed -i 's|^HUBS_ALLOWLIST_REGEX=.*|HUBS_ALLOWLIST_REGEX=$(echo $CURRENT_ALLOWLIST | tr ',' '|')|' $ENV_FILE"
        exit 4
    fi

    echo "[hub-add] (9) Validate PASS — reloading Caddy (zero-downtime)..."

    # Atomic reload qua Caddy admin API
    if ! docker compose exec -T caddy caddy reload --config /etc/caddy/Caddyfile 2>&1; then
        echo "[hub-add] ERROR: Caddy reload FAIL"
        exit 4
    fi

    # Smoke checkpoint: verify hub moi route work post-reload
    sleep 1  # cho Caddy settle

    if command -v curl >/dev/null 2>&1 && [ -n "${WIKI_PUBLIC_DOMAIN:-localhost}" ]; then
        DOMAIN="${WIKI_PUBLIC_DOMAIN:-localhost}"
        SMOKE_URL="https://${DOMAIN}/${HUB}/api/health"
        echo "[hub-add] (9) Smoke check: curl -k $SMOKE_URL"
        if curl -k -sf -o /dev/null --max-time 5 "$SMOKE_URL" 2>/dev/null; then
            echo "[hub-add] (9) Smoke PASS — hub '$HUB' route work qua Caddy"
        else
            echo "[hub-add] WARN: Smoke check returned non-200 hoac timeout."
            echo "         Backend container python-api-$HUB co the chua up."
            echo "         Run: docker compose up -d python-api-$HUB && curl -k $SMOKE_URL"
        fi
    else
        echo "[hub-add] (9) Skip smoke check (curl khong co hoac WIKI_PUBLIC_DOMAIN khong set)"
    fi
else
    echo "[hub-add] (9) Caddy container chua running — skip reload (se apply khi 'docker compose up')"
    echo "         Sau khi up: docker compose exec caddy caddy reload --config /etc/caddy/Caddyfile"
fi
```

**Sau block 8+9, GIỮ NGUYÊN existing DONE message + next steps echo lines.** Cập nhật comment header trong DONE message section nếu có reference "(7/7)" → "(9/9)".

**Step 2 — Update comment header trong file đầu (hub-add.sh header docstring nếu có) — find existing `# 7-step pipeline` hoặc tương đương → REPLACE → `# 9-step pipeline (Phase 5 v3.0-05 PROXY-01 extend)`:**

Locate comment block top of file (line 1-30) — nếu có `7-step` text → replace với `9-step`. Nếu không có explicit count, skip.

**T-5-05 mitigation note:** Step 1-3 Plan 02-05 (regex `^[a-z][a-z0-9_]{0,15}$` + RESERVED_HUB_NAMES blacklist + duplicate detect) carry forward — Phase 5 step 8 + 9 KHÔNG relax guard. `$HUB` variable đã pass validate ở Step 1-2 — quote `"$HUB"` trong sed expressions + double-quote `--config /etc/caddy/Caddyfile` argv (KHÔNG eval interpolate user input).
  </action>
  <verify>
    <automated>bash -n Hub_All/api/scripts/hub-add.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n Hub_All/api/scripts/hub-add.sh` exit 0 (syntax valid)
    - `grep -q "(8/9) Cap nhat .env HUBS_ALLOWLIST" Hub_All/api/scripts/hub-add.sh` exit 0
    - `grep -q "(9/9) Caddy validate + reload" Hub_All/api/scripts/hub-add.sh` exit 0
    - `grep -q "HUBS_ALLOWLIST_REGEX" Hub_All/api/scripts/hub-add.sh` exit 0
    - `grep -q "caddy validate --config /etc/caddy/Caddyfile" Hub_All/api/scripts/hub-add.sh` exit 0 (PRE-validate per Pitfall 7)
    - `grep -q "caddy reload --config /etc/caddy/Caddyfile" Hub_All/api/scripts/hub-add.sh` exit 0 (zero-downtime reload)
    - `grep -q "curl.*api/health" Hub_All/api/scripts/hub-add.sh` exit 0 (smoke check post-reload)
    - `grep -q "WIKI_PUBLIC_DOMAIN" Hub_All/api/scripts/hub-add.sh` exit 0 (env consume)
    - `grep -q "tr ',' '|'" Hub_All/api/scripts/hub-add.sh` exit 0 (HUBS_ALLOWLIST → REGEX derive)
    - `grep -q "trap.*rm -f" Hub_All/api/scripts/hub-add.sh` exit 0 (tmp file cleanup on exit)
    - Step 1 regex validate carry forward Plan 02-05: `grep -q "\[a-z\]\[a-z0-9_\]{0,15}" Hub_All/api/scripts/hub-add.sh` exit 0
    - RESERVED blacklist carry forward: `grep -q "RESERVED_HUB_NAMES\|postgres" Hub_All/api/scripts/hub-add.sh` exit 0
    - DONE message vẫn cuối: `tail -20 Hub_All/api/scripts/hub-add.sh | grep -q "DONE"` exit 0
  </acceptance_criteria>
  <done>
    hub-add.sh mở rộng từ 7-step → 9-step pipeline. Step 8 idempotent sed-edit .env atomic; Step 9 PRE-validate + reload + smoke. bash -n PASS. T-5-05 carry forward strict guard từ Plan 02-05.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dry-run test hub-add.sh với mock HUB không side-effect (PROXY-01 + FACTOR-04 verify)</name>
  <files>Hub_All/api/scripts/hub-add.sh (read-only verification — KHÔNG edit)</files>
  <read_first>
    - Hub_All/api/scripts/hub-add.sh (sau Task 1 edit — đọc full để hiểu flow + state side-effect)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-05-02 (dry-run với mock hub cleanup)
  </read_first>
  <action>
**Manual dry-run pipeline test (single-shot — non-destructive):**

Mục đích: Verify script syntax + logic flow KHÔNG side-effect production .env hoặc Caddy container.

**Step 1 — Backup .env (nếu tồn tại):**
```bash
cd Hub_All
[ -f .env ] && cp .env .env.bak_phase5_test
```

**Step 2 — Test bash syntax explicit:**
```bash
bash -n api/scripts/hub-add.sh
echo "Syntax check exit: $?"  # Expect 0
```

**Step 3 — Inspect script flow (read-only, no exec):**
```bash
# Verify step ordering: 1→2→3→4→5→6→7→8→9
grep -nE "^\s*#\s*\([0-9]+/[0-9]+\)|^# \(Step [0-9]+\)" api/scripts/hub-add.sh
# Expect output mentioning steps 1 through 9 in numeric order
```

**Step 4 — Verify idempotent behavior (script structure self-check):**
```bash
# Check that step 8 handles duplicate detection
grep -A 2 "(8/9) Cap nhat" api/scripts/hub-add.sh | grep -q "duplicate\|da co\|skip"
echo "Idempotent check: $?"  # Expect 0
```

**Step 5 — Restore .env (nếu backed up):**
```bash
[ -f .env.bak_phase5_test ] && mv .env.bak_phase5_test .env || true
```

**Note (defer Phase 7 MIGRATE-05 cho LIVE end-to-end):** Full live dry-run `HUB=tmp_test bash api/scripts/hub-add.sh` (with actual DB create + override append + Caddy reload + curl smoke) defer Phase 7 MIGRATE-05 (carry forward v3.0-b precedent — Plan 02-05 + 03-05 + 04-07 đều skip live smoke). Plan 05-05 chỉ verify script SYNTAX + STRUCTURE; live test runtime Phase 7.

**Threat mitigation:**
- T-5-05 mitigated qua Step 1-2 Plan 02-05 carry forward (đã verified ở Plan 02-05 unit test 29/29 PASS) — Phase 5 Task 2 KHÔNG re-test guard (out of scope).
  </action>
  <verify>
    <automated>cd Hub_All &amp;&amp; bash -n api/scripts/hub-add.sh &amp;&amp; grep -nE "^\s*#\s*\([0-9]+/9\)" api/scripts/hub-add.sh | wc -l | grep -q "^[2-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n Hub_All/api/scripts/hub-add.sh` exit 0
    - Step ordering verified — grep for step headers count: `grep -cE "^# .*\([0-9]+/9\)" Hub_All/api/scripts/hub-add.sh` returns ≥ 2 (Step 8 + Step 9 — Plan 02-05 step headers có thể dùng format khác (7/7) → KHÔNG mandate hết 9 step numbered (9/9), chỉ verify Step 8 + Step 9 mới có format (X/9))
    - Idempotent duplicate handling: `grep -A 3 "8/9" Hub_All/api/scripts/hub-add.sh | grep -qE "da co|skip|duplicate"` exit 0
    - PRE-validate before reload: `grep -B 2 "caddy reload" Hub_All/api/scripts/hub-add.sh | grep -q "caddy validate"` exit 0
    - .env atomic edit pattern: `grep -q "mktemp\|TMP_ENV" Hub_All/api/scripts/hub-add.sh` exit 0
    - `cd Hub_All && bash -x api/scripts/hub-add.sh --help 2>&1 | head -5` không panic (chấp nhận exit code khác 0 nếu script không support --help, chỉ verify KHÔNG crash unbound variable)
  </acceptance_criteria>
  <done>
    Script structure + ordering verify; bash syntax PASS; idempotent duplicate handling present; PRE-validate before reload pattern present; atomic .env edit pattern present. Live end-to-end test defer Phase 7 MIGRATE-05 per v3.0-b precedent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator $HUB= shell input → hub-add.sh | Trusted operator (sudo CLI); validated by Step 1-2 Plan 02-05 (regex + RESERVED blacklist) |
| hub-add.sh → docker compose exec caddy | Trusted internal API; argv quoted |
| hub-add.sh → .env file edit | File system write; atomic via tmp+mv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-05 | Tampering / Shell injection | hub-add.sh `HUB=` argv | mitigate | Plan 02-05 carry forward Step 1-2: regex `^[a-z][a-z0-9_]{0,15}$` validate + RESERVED_HUB_NAMES blacklist (6 names: postgres/cocoindex/template0/template1/public/medinet). Phase 5 step 8 + 9 KHÔNG relax — `$HUB` quoted trong sed expressions + caddy reload argv. |
| T-5-14 | Tampering | sed atomic edit .env | mitigate | tmp file + mv pattern (POSIX atomic rename); trap cleanup on EXIT. Preserve other env vars (KHÔNG overwrite POSTGRES_PASSWORD etc). |
| T-5-15 | DoS | Caddy reload silent rollback (Pitfall 7) | mitigate | PRE-validate `caddy validate --config` TRƯỚC reload — fail-fast với explicit error message + rollback instruction. Smoke `curl /<new>/api/health` post-reload verify route work. |
| T-5-16 | Spoofing | curl smoke check `-k` accept self-signed | accept | Dev local WIKI_PUBLIC_DOMAIN=localhost dùng self-signed cert; `-k` flag chấp nhận. Prod ACME Let's Encrypt valid cert KHÔNG cần `-k`. Document trong comment. |
</threat_model>

<verification>
- bash -n syntax check PASS
- Step 8 + 9 structure present + idempotent + atomic
- PRE-validate before reload pattern (Pitfall 7 mitigation)
- T-5-05 strict guard carry forward Plan 02-05 (no re-test ở plan này)
- Live dry-run defer Phase 7 MIGRATE-05 per v3.0-b precedent
</verification>

<success_criteria>
Wave 4 ship — hub-add.sh 9-step pipeline ready. Operator `make hub-add HUB=<new>` tự register full chain (DB → override.yml → .env → caddy reload → smoke). PROXY-01 + FACTOR-04 extend satisfied. Plan 05-06 closeout có thể document FACTOR-04 deploy workflow trong README.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reverse-proxy-frontend-subpath/05-05-SUMMARY.md` với:
- 1 task commit (atomic): feat(05-05) hub-add.sh 9-step pipeline extend FACTOR-04 + 1 verify commit
- Acceptance criteria PASS table (12+ grep + bash -n)
- Deviations log (nếu script line count quá lớn — split helper function out)
- Carry-forward note: T-5-05 strict guard Plan 02-05 KHÔNG relax; live dry-run defer Phase 7 MIGRATE-05
- Next plan handoff: Plan 05-06 (closeout docs + smoke) Wave 5 depend Plan 05-01..05 cumulative
</output>
