---
phase: 5
plan: 06
type: execute
wave: 5
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - Hub_All/CLAUDE.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - Hub_All/README.md
autonomous: false
requirements: [PROXY-01, PROXY-02, PROXY-03, PROXY-04]
tags: [closeout, docs, d6-expire, smoke-checkpoint, v3.0, phase5]

must_haves:
  truths:
    - "`Hub_All/CLAUDE.md` §3 (Quy tắc làm việc) ghi rõ D6 EXPIRED ở Phase 5 v3.0-05 (PROXY-03 satisfied)"
    - "`Hub_All/CLAUDE.md` §6 v3.0 progress table — Phase 5 row marked DONE + Phase 5 pattern subsection (match style Phase 2/3/4)"
    - "`.planning/STATE.md` frontmatter completed_phases 4→5 + percent 69→~85 + Current Position + Phase 5 Results Summary"
    - "`.planning/REQUIREMENTS.md` PROXY-01..04 mark `[x]` + NOTE Phase 5 closeout 6-plan list"
    - "`.planning/ROADMAP.md` Phase 5 status DONE + Progress table v3.0 update"
    - "`Hub_All/README.md` section mới 'Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)' với env list + Caddy reload + smoke commands"
    - "Task 5b checkpoint:human-action smoke 4 hub × 11 trang — resume signal `approved` / `partial: branding only` / `skip smoke`"
  artifacts:
    - path: "Hub_All/CLAUDE.md"
      provides: "§3 D6 EXPIRED + §6 Phase 5 progress row + Phase 5 pattern subsection"
      contains: "D6 EXPIRED"
    - path: ".planning/STATE.md"
      provides: "Phase 5 DONE frontmatter + Current Position + Results Summary + Next Action Phase 6"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PROXY-01..04 mark [x] + closeout note"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 row status DONE + Progress table update"
    - path: "Hub_All/README.md"
      provides: "Section mới Reverse Proxy Subpath Deploy Notes (env + caddy reload + smoke commands)"
  key_links:
    - from: "CLAUDE.md §3"
      to: "D-V3-06 D6 expire formally"
      via: "explicit note 'D6 EXPIRED ở Phase 5 v3.0-05'"
      pattern: "D6 EXPIRED"
    - from: "STATE.md frontmatter"
      to: "progress.completed_phases = 5"
      via: "frontmatter YAML update"
      pattern: "completed_phases: 5"
---

<objective>
Wave 5 closeout — Documentation update + manual smoke checkpoint cho 4 hub × 11 trang React COMPAT-01. 4 docs file edit (CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md) reflect Phase 5 DONE. Task 5b `checkpoint:human-action gate=blocking` cho manual smoke regression (W-01 split: Task 5a auto README write + Task 5b checkpoint pause) — user resume signal pattern carry forward Plan 03-05 + 04-07 (`approved` / `partial: branding only` / `skip smoke`).

Purpose: PROXY-03 satisfied (D6 expire formally documented) + PROXY-04 verify (manual smoke branding 4 hub) + R-V3-2 mitigation (11 trang regression human verify) + Phase 5 deliverable closeout. v3.0 progress 4/7 phase v3.0-b → 5/7 (≈ 85%).

Output: 5 file docs edit + 1 checkpoint pause; v3.0-b progress 22 → 27 plan ≈ 85%.

**autonomous: false** — Task 5 checkpoint pause cần user input.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@Hub_All/README.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md

<interfaces>
<!-- Patterns to follow từ Phase 3 + 4 closeout (03-05-PLAN.md + 04-07-PLAN.md) -->
- CLAUDE.md §6 v3.0 progress table row update (Phase 5 status ✅ DONE + Plan count 6 + Date)
- CLAUDE.md §6 Phase 5 pattern subsection theo style Phase 4 (deliverable summary + key links + carry forward semantics)
- STATE.md frontmatter `status:` long-form summary + frontmatter progress.completed_phases + percent update
- STATE.md Current Position section update
- STATE.md "Phase N Planning Summary" table + "Phase N Results Summary" table
- REQUIREMENTS.md inline checkbox PROXY-01..04 → [x] + footer NOTE Phase 5 closeout plan list
- ROADMAP.md Phase 5 row update status DONE + v3.0 progress table
- README.md NEW section "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)"
- Task 5a auto README write + Task 5b checkpoint:human-action with resume signal pattern carry forward (W-01 split clean atomicity)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hub_All/CLAUDE.md — §3 D6 EXPIRED note + §6 Phase 5 progress row + Phase 5 pattern subsection (PROXY-03)</name>
  <files>Hub_All/CLAUDE.md</files>
  <read_first>
    - Hub_All/CLAUDE.md (full file — read §3 "Quy tắc làm việc" Constraint M2 section to find D6 reference; read §6 v3.0 progress table + Phase 4 pattern subsection để clone style cho Phase 5)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md §"Phase Boundary" item 3 (D-V3-06 D6 expire formally)
  </read_first>
  <action>
**Step 1 — §3 "Constraint M2" subsection — find "Frontend D6" line (existing `- **Frontend D6:** KHÔNG sửa React 19. URL `/api/*` giữ qua FastAPI mount port 8080.`) → ADD strikethrough + EXPIRED note:**

REPLACE existing line with:
```markdown
- **Frontend D6 (EXPIRED ở Phase 5 v3.0-05 — PROXY-03):** ~~KHÔNG sửa React 19. URL `/api/*` giữ qua FastAPI mount port 8080.~~ → **D6 EXPIRED 2026-05-22** (PROXY-03 satisfied). Phase 5 cho phép FE rewrite cho prefix detect (PROXY-02 + D-V3-Phase5-B1/B3) + per-hub login branding (PROXY-04 + D-V3-Phase5-D1/D2). Smoke regression M2 COMPAT-01 11 trang carry forward (R-V3-2 mitigation — Plan 05-06 manual checklist 4 hub × 11 trang). M2 contract response envelope `{success, data, error, meta}` shape KHÔNG đổi (LOCKED carry forward).
```

**Step 2 — §6 v3.0 progress table — find Phase 5 row hiện tại `| 5 — Reverse Proxy + Frontend Subpath | 📋 Next | — | — | PROXY-01..04 |` → UPDATE:**

REPLACE với:
```markdown
| 5 — Reverse Proxy + Frontend Subpath | ✅ DONE | 6 plan | 2026-05-22 | PROXY-01..04 (4) |
```

**Step 3 — §6 ADD Phase 5 pattern subsection (clone style Phase 4 subsection) — INSERT sau "### Phase 4 Cross-hub Data Sync pattern" subsection, TRƯỚC closing `---` separator:**

```markdown

### Phase 5 Reverse Proxy + Frontend Subpath pattern (PROXY-01..04 — 2026-05-22)

6 plan đóng 4 REQ-ID PROXY (Caddy reverse proxy + frontend 1-build prefix detect + D6 expire formally + per-hub login branding) + 16 D-V3-Phase5-A1..D4 LOCKED + FACTOR-04 extend (hub-add.sh step 8+9):

- **Plan 05-01 PROXY-01 — Caddy + docker-compose scaffolding (D-V3-Phase5-A1/A2/A4):** `Hub_All/Caddyfile` ADD `{$WIKI_PUBLIC_DOMAIN}` server block parallel MCP block (Phase 8.3 carry forward) — `path_regexp hub_api ^/({$HUBS_ALLOWLIST_REGEX:yte|duoc|hcns})/api/(.*)$` + `route { uri strip_prefix /{re.hub_api.1}; reverse_proxy http://python-api-{re.hub_api.1}:8080 }` (T-5-01 anchor mitigation) + central `/api/*` handle no-strip + `/.well-known/*` JWKS pass-through (Phase 3 Plan 03-01 carry forward) + static SPA `file_server` `try_files {path} /index.html`. `docker-compose.yml` caddy service ADD `WIKI_PUBLIC_DOMAIN` + `HUBS_ALLOWLIST_REGEX` env + `./frontend/dist:/srv/wiki/dist:ro` mount + `depends_on: python-api-central`. `.env.example` document 3 env vars dev vs prod.

- **Plan 05-02 PROXY-02 — Frontend prefix detect runtime + vitest Wave 0 (D-V3-Phase5-B1/B3 + GA-V3-C):** Wave 0 install `vitest@^2 + @testing-library/react@^16 + @testing-library/jest-dom@^6 + jsdom@^25` + `frontend/vitest.config.ts` jsdom env + `frontend/src/test-setup.ts` jest-dom matchers + `"test": "vitest run"` npm script. Refactor `frontend/src/services/api.ts` module-level compute `PREFIX/API_BASE/APP_BASE/CURRENT_HUB` từ `window.location.pathname.split('/').filter(Boolean)[0]` + `KNOWN_HUBS` từ `window.__HUB_CONFIG__.allowlist` runtime fallback hardcode `['yte','duoc','hcns']` (T-5-02 backend Caddy regex authoritative — FE allowlist UX-only). `App.tsx` wrap `<BrowserRouter basename={APP_BASE}>` auto-prepend basename cho 13 route hiện tại — KHÔNG đụng path absolute.

- **Plan 05-03 PROXY-04 — Per-hub branding registry + 4 SVG asset (D-V3-Phase5-D1/D3):** `frontend/src/branding/index.ts` Vite `import.meta.glob('./*/index.ts', { eager: true })` + `BrandingConfig` type + `getBranding(hub)` fallback central + `getContrastTextColor(themeColor)` WCAG 1.4.11 mitigation (hcns amber `#f59e0b` luminance > 0.55 → 'slate-900', 3 hub khác → 'white'). 4 hub config `branding/<hub>/index.ts`: central indigo `#6366f1` (Medinet brand) + yte emerald `#10b981` (Hub Y tế) + duoc sky `#0ea5e9` (Hub Dược) + hcns amber `#f59e0b` (Hub HCNS). 4 SVG placeholder text-only initial letter (M/Y/D/H) `frontend/public/branding/<hub>/logo.svg` (Vite copy public → dist → Caddy file_server). T-5-03 path traversal mitigation qua regex constrain `^\.\/([a-z][a-z0-9_]{0,15})\/index\.ts$`.

- **Plan 05-04 PROXY-02 + PROXY-04 — Login + Layout + crossHubSearch wire (D-V3-Phase5-B4/C1/D2 + D-V3-Phase4-D3 carry forward):** `Login.tsx` 4 state machine (A central no-return, B central with valid return + chip, C hub direct visit + skeleton redirect, D invalid return silent fallback) + branding logo + title + tagline + themeColor inline CSS var `--hub-theme` + submit button gradient `color-mix(in srgb, ...)` + T-5-04 strict return validation 4-layer (strip `/` + reject `//` + reject `://` + regex hub format + KNOWN_HUBS allowlist). `Layout.tsx` sidebar header logo bg + title swap qua module-level `getBranding(CURRENT_HUB)` + hover ring themeColor (UI-SPEC §3 LOCKED scope — chỉ sidebar header touch, KHÔNG cascade tất cả component R-V3-2 mitigation). `api.ts::crossHubSearch` REPLACE → ABSOLUTE path `/api/search/cross-hub` qua `requestAbsolute<T>` helper (bypass `${API_BASE}` prefix — D-V3-Phase4-D3 LOCKED hub con strip endpoint, Caddy `/api/*` route central).

- **Plan 05-05 PROXY-01 + FACTOR-04 extend — hub-add.sh step 8+9 (D-V3-Phase5-A3):** Append `api/scripts/hub-add.sh` Plan 02-05 7-step pipeline → 9-step. Step 8: atomic sed-edit `.env` HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX (tmp file + mv preserve other env, duplicate skip idempotent). Step 9: PRE-validate `caddy validate --config /etc/caddy/Caddyfile` TRƯỚC reload (Pitfall 7 silent rollback mitigation) + `caddy reload` zero-downtime + smoke `curl -k https://${WIKI_PUBLIC_DOMAIN}/${HUB}/api/health` post-reload. T-5-05 strict guard Plan 02-05 carry forward (regex + RESERVED blacklist KHÔNG relax).

- **Plan 05-06 closeout — docs + smoke checkpoint 4 hub × 11 trang (file này, 2026-05-22):** CLAUDE.md §3 D6 EXPIRED note + §6 Phase 5 progress row + pattern subsection (file này) + STATE.md frontmatter + REQUIREMENTS.md PROXY-01..04 mark [x] + ROADMAP.md Phase 5 DONE + README.md section mới "Reverse Proxy Subpath Deploy Notes". Task 5 `checkpoint:human-action gate=blocking` smoke 4 hub × 11 trang React M2 COMPAT-01 (UI-SPEC §7 — 44 checkpoint). User resume signal carry forward Plan 03-05 + 04-07 pattern: `approved` / `partial: branding only` / `skip smoke`.

**Architecture insights (Phase 5):**

1. **Single source-of-truth env-driven multi-hub:** `.env HUBS_ALLOWLIST=yte,duoc,hcns` → Caddy `HUBS_ALLOWLIST_REGEX=yte|duoc|hcns` qua docker-compose env wire + frontend `window.__HUB_CONFIG__.allowlist` (Phase 6 SETTINGS-04 sẽ sync DB-driven). `make hub-add` cập nhật cả Caddy regex + hub-init DB + override.yml + .env atomic chain.
2. **1 build dùng chung 4 deploy:** Vite `base='/'` + asset absolute `/assets/*` + react-router `BrowserRouter basename={APP_BASE}` auto-prepend → KHÔNG cần build matrix `VITE_HUB_NAME=yte` × 4 (GA-V3-C khuyến nghị seed CONFIRMED).
3. **Per-hub branding scope minimal:** Chỉ Login.tsx + Layout.tsx sidebar header touch — Dashboard/Documents/Search/11 trang nội dung giữ NGUYÊN M2 (R-V3-2 mitigation). Full Tailwind cascade defer v4.0.
4. **Theme color delivery inline CSS variable `--hub-theme`:** Runtime swap per-hub qua React `style={{ '--hub-theme': themeColor }}` outermost wrapper + child `color-mix(in srgb, var(--hub-theme) ...)` — KHÔNG cần Tailwind plugin (build overhead). Browser support Chrome 111+/Safari 16.4+/Firefox 113+ ≥ 95% user 2026.
5. **localStorage same-origin SSO:** `wiki.medinet.vn` root domain → token share xuyên `/yte/`, `/duoc/`, `/` subpath cùng origin (Web Storage API scope by origin). XSS concern M2 carry forward — defer v4.0 HARD-V4-05 httpOnly cookie.
6. **307 redirect failsafe (Plan 03-04 carry forward):** Backend hub con `/api/auth/login` + `/api/auth/refresh` 307 → central — Phase 5 FE Login.tsx mount redirect là PRIMARY UX (D-V3-Phase5-B4); 307 backend là LAYER 2 safety cho `api.ts::tryRefresh()` POST same-origin redirect: 'follow' (RFC 7231 preserve POST body).
7. **Cross-hub endpoint absolute path override:** `api.crossHubSearch()` dùng `/api/search/cross-hub` absolute (KHÔNG `${API_BASE}` prefix) — D-V3-Phase4-D3 LOCKED hub con strip endpoint, Caddy `/api/*` handle central route. Pattern reusable cho future central-only endpoint (vd Phase 6 admin).

**T-5-01..07 STRIDE coverage:**
- T-5-01 Caddy open redirect: Plan 05-01 path_regexp anchor `^/(...)/api/(.*)$` — unknown hub fall through file_server (no arbitrary upstream).
- T-5-02 KNOWN_HUBS tampering: Plan 05-02 + 05-04 — backend Caddy regex authoritative, FE UX-only.
- T-5-03 Logo asset path traversal: Plan 05-03 — regex constrain `^\.\/([a-z][a-z0-9_]{0,15})\/index\.ts$` + path schema locked.
- T-5-04 Login ?return open redirect: Plan 05-04 — 4-layer validation (strip + reject absolute + regex + allowlist).
- T-5-05 hub-add.sh shell injection: Plan 05-05 — Plan 02-05 strict guard carry forward (regex + RESERVED blacklist), `$HUB` quoted.
- T-5-06 XSS via theme color: Plan 05-03 + 05-04 — themeColor hardcoded TS const, React style escape.
- T-5-07 localStorage XSS exfil: ACCEPTED M2 carry forward — defer v4.0 HARD-V4-05.

**Backward compat (Phase 5 KHÔNG break M2):**
- API endpoint URL/envelope shape `{success, data, error, meta}` LOCKED — Phase 5 chỉ đổi FE base URL compute (relative path qua Caddy).
- M2 11 trang React component shell preserve (R-V3-2) — chỉ touch Login.tsx + Layout.tsx sidebar header.
- M2 localStorage same-origin pattern preserve (D-V3-Phase5-C2).
- M2 authentication flow preserve — Login form same-origin POST + JWT Bearer token header.

**v3.0-b progress:** Phase 5 hoàn tất 4/4 REQ-ID PROXY. v3.0-b 2/4 phase DONE (Phase 4 + 5). Next: Phase 6 SETTINGS sync (SETTINGS-01..04 — rag_config HTTP pull + Redis pub/sub invalidate + api_keys proxy + hub_registry read-only).

**Reference:**
- `.planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md` — 16 D-V3-Phase5-A1..D4 LOCKED 2026-05-22.
- `.planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md` — Visual design contract (4 hub branding + Login state machine + Layout sidebar + theme delivery).
- `.planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md` — Per-task verification map (Wave 0 vitest infra + 5 test file).
- `.planning/phases/05-reverse-proxy-frontend-subpath/05-{01..06}-PLAN.md` — implementation chi tiết 6 plan.
- `.planning/phases/05-reverse-proxy-frontend-subpath/05-{01..06}-SUMMARY.md` — deliverable + commit + test count per plan.

```

**Step 4 — Update footer changelog (cuối file CLAUDE.md):**

Find existing `*Cập nhật:` line cuối file → REPLACE với:
```markdown
*Cập nhật: 2026-05-22 (Phase 5 DONE — PROXY-01..04 ship 6 plan; Caddy reverse proxy `wiki.domain.com/<hub>/api/*` strip prefix + central no-strip + .well-known JWKS pass + static SPA fallback + frontend 1-build prefix detect runtime via `window.location.pathname.split('/')[1]` + KNOWN_HUBS runtime config + react-router BrowserRouter basename auto-prepend + per-hub branding Vite glob registry 4 hub initial + theme color inline CSS var `--hub-theme` + Login 4 state machine + T-5-04 strict return validation + crossHubSearch absolute path override + hub-add.sh 9-step pipeline FACTOR-04 extend + D6 EXPIRED formally PROXY-03). Project: MEDWIKI. M2 v2.0 done; v3.0 Multi-Hub Split — Phase 1+2+3+4+5 DONE (28/~32 plan ≈ 87%), v3.0-a EXIT GATE TRIGGERED + v3.0-b mid-flight. Next: `/gsd-discuss-phase 6` System Settings Sync (SETTINGS-01..04 — rag_config HTTP pull + Redis pub/sub invalidate + api_keys proxy + hub_registry read-only).*
```
  </action>
  <verify>
    <automated>grep -q "D6 EXPIRED" Hub_All/CLAUDE.md &amp;&amp; grep -q "Phase 5 Reverse Proxy + Frontend Subpath pattern" Hub_All/CLAUDE.md &amp;&amp; grep -E "^\| 5 — Reverse Proxy.*\| ✅ DONE" Hub_All/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "D6 EXPIRED" Hub_All/CLAUDE.md` exit 0 (§3 expire note)
    - `grep -q "Phase 5 Reverse Proxy + Frontend Subpath pattern" Hub_All/CLAUDE.md` exit 0 (§6 pattern subsection)
    - `grep -qE "^\| 5 — Reverse Proxy.*\| ✅ DONE.*\| 6 plan.*\| 2026-05-22.*\| PROXY-01\.\.04" Hub_All/CLAUDE.md` exit 0 (progress table row)
    - `grep -q "PROXY-03 satisfied" Hub_All/CLAUDE.md` exit 0
    - `grep -q "Plan 05-01 PROXY-01" Hub_All/CLAUDE.md` exit 0 (subsection lists 6 plan)
    - `grep -q "Plan 05-06 closeout" Hub_All/CLAUDE.md` exit 0
    - `grep -q "T-5-01..07" Hub_All/CLAUDE.md` exit 0 (STRIDE coverage section)
    - `grep -q "v3.0-b progress" Hub_All/CLAUDE.md` exit 0
    - Existing §1-5 sections preserve: `grep -c "^## " Hub_All/CLAUDE.md` >= 6 (Tổng quan + Milestone + Quy tắc + Lệnh + Commit + M2 closeout)
    - Footer date updated: `tail -3 Hub_All/CLAUDE.md | grep -q "2026-05-22.*Phase 5 DONE"` exit 0
  </acceptance_criteria>
  <done>
    CLAUDE.md §3 D6 EXPIRED + §6 progress table row + Phase 5 pattern subsection + footer changelog updated. 6 plan list + 7 architecture insights + STRIDE coverage + v3.0-b progress documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/STATE.md — frontmatter + Current Position + Phase 5 Results Summary</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (đọc frontmatter line 1-13 + Current Position + Phase 4 Results Summary line ~340-end để clone format cho Phase 5)
  </read_first>
  <action>
**Step 1 — Update frontmatter (line 5-12):**

REPLACE existing `status:` long-form summary với 1 dòng tóm tắt Phase 5 DONE (preserve YAML format).

REPLACE existing `progress:` block:
```yaml
progress:
  total_phases: 7
  completed_phases: 5
  total_plans: 28
  completed_plans: 28
  percent: 87
```

REPLACE `last_updated: "2026-05-22T..."` (giữ ISO format, update timestamp).

**Step 2 — Update "## Current Position" section:**

REPLACE existing với:
```markdown
## Current Position

- **Phase:** 5 — Reverse Proxy + Frontend Subpath ✅ **DONE 2026-05-22 — PROXY-01..04 fully shipped (Wave 1 BLOCKING + Wave 2 parallel × 2 + Wave 3 + Wave 4 + Wave 5 closeout ✅)**
- **Plan:** 6/6 plans complete (05-01..05-06) cho PROXY-01..04 + FACTOR-04 extend ở `.planning/phases/05-reverse-proxy-frontend-subpath/`.
- **Status:** Phase 5 closed PROXY-01..04 — Caddy reverse proxy `wiki.domain.com/<hub>/api/*` qua `path_regexp hub_api` + `uri strip_prefix /{re.hub_api.1}` + `reverse_proxy http://python-api-{re.hub_api.1}:8080` (port STATIC :8080 per Pitfall 1) + central `/api/*` no-strip + `/.well-known/*` JWKS pass + static SPA `try_files {path} /index.html` (D-V3-Phase5-A1/A2 LOCKED). Frontend 1-build runtime detect `window.location.pathname.split('/')[1]` + KNOWN_HUBS `window.__HUB_CONFIG__.allowlist` fallback hardcode `['yte','duoc','hcns']` + react-router `BrowserRouter basename={APP_BASE}` auto-prepend (D-V3-Phase5-B1/B3). Per-hub branding `frontend/src/branding/` Vite glob registry 4 hub initial (central indigo / yte emerald / duoc sky / hcns amber) + theme color inline CSS variable `--hub-theme` + WCAG 1.4.11 mitigation `getContrastTextColor()` hcns amber slate-900 (D-V3-Phase5-D1/D2/D3). Login.tsx 4 state machine (A/B/C/D per UI-SPEC §2) + T-5-04 strict ?return validation 4-layer + window.location.replace cross-prefix redirect (D-V3-Phase5-B4/C1). Layout.tsx sidebar header logo bg + title swap module-level `getBranding(CURRENT_HUB)` + hover ring themeColor (UI-SPEC §3 LOCKED scope). api.crossHubSearch ABSOLUTE path `/api/search/cross-hub` qua `requestAbsolute<T>` helper bypass `${API_BASE}` prefix (D-V3-Phase4-D3 carry forward). hub-add.sh 9-step pipeline extend FACTOR-04 (Step 8 sed-edit `.env` atomic + Step 9 PRE-validate + caddy reload zero-downtime + smoke curl) (D-V3-Phase5-A3 + T-5-05 Plan 02-05 carry forward). D6 EXPIRED formally CLAUDE.md §3 (PROXY-03). vitest Wave 0 infra (vitest@^2 + @testing-library/react + jsdom) + 5 new test file 40+ test PASS. R-V3-2 mitigation manual smoke 4 hub × 11 trang checkpoint Plan 05-06 Task 5 (carry forward Plan 03-05/04-07 resume signal pattern). Backward compat M2 KHÔNG break — endpoint URL/envelope shape UNCHANGED, 11 trang nội dung React preserve (chỉ Login + Layout sidebar header touch). 7 STRIDE threat coverage T-5-01..07 (6 mitigate + 1 accept localStorage XSS defer v4.0).
- **Last activity:** 2026-05-22 — `/gsd-execute-phase 5` wave-based execution complete cho PROXY-01..04:
  - **05-01 DONE ✅** (Wave 1 BLOCKING): Caddy wiki block + docker-compose env + .env.example.
  - **05-02 DONE ✅** (Wave 2 parallel): vitest install + api.ts prefix detect + App.tsx basename.
  - **05-03 DONE ✅** (Wave 2 parallel): Branding registry + 4 hub config + 4 SVG placeholder + WCAG helper.
  - **05-04 DONE ✅** (Wave 3): Login.tsx 4 state + Layout.tsx sidebar swap + api.crossHubSearch absolute override.
  - **05-05 DONE ✅** (Wave 4): hub-add.sh step 8 + 9 (HUBS_ALLOWLIST sed + caddy validate + reload + smoke).
  - **05-06 DONE ✅** (Wave 5 closeout 2026-05-22): CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md update; smoke checkpoint resume signal.
```

**Step 3 — ADD "## Phase 5 Planning Summary" section sau Phase 4 Planning Summary table:**

```markdown
## Phase 5 Planning Summary

| Plan | Wave | Objective | Tasks | Files modified | REQ | Status |
|------|------|-----------|-------|----------------|-----|--------|
| 05-01 | 1 BLOCKING | Caddyfile wiki block + docker-compose caddy service env + frontend/dist mount + .env.example 3 env | 3 (auto) | `Hub_All/Caddyfile`, `Hub_All/docker-compose.yml`, `Hub_All/.env.example` | PROXY-01 | ✅ DONE 2026-05-22 |
| 05-02 | 2 parallel | Wave 0 vitest infra + api.ts module-level prefix detect + App.tsx BrowserRouter basename + 10 vitest test PASS | 3 (1 wave 0 + 2 tdd) | `frontend/package.json`, `frontend/vitest.config.ts`, `frontend/src/test-setup.ts`, `frontend/src/services/api.ts`, `frontend/src/App.tsx`, 2 test file mới | PROXY-02 | ✅ DONE 2026-05-22 |
| 05-03 | 2 parallel | Branding registry Vite glob + 4 hub config + 4 SVG placeholder + WCAG getContrastTextColor + 17 vitest test PASS | 3 (auto) | `frontend/src/branding/{index,central,yte,duoc,hcns}/index.ts` (5 file mới) + `frontend/public/branding/{central,yte,duoc,hcns}/logo.svg` (4 file mới) + `frontend/src/branding/__tests__/registry.spec.ts` (1 file mới) | PROXY-04 | ✅ DONE 2026-05-22 |
| 05-04 | 3 | Login.tsx 4 state machine + branding inline CSS var + T-5-04 strict return validation + Layout.tsx sidebar swap + api.crossHubSearch absolute override + 12 vitest test PASS | 3 (tdd) | `frontend/src/pages/Login.tsx`, `frontend/src/Layout.tsx`, `frontend/src/services/api.ts`, `frontend/src/pages/__tests__/Login.spec.tsx`, `frontend/src/__tests__/Layout.spec.tsx` | PROXY-02 + PROXY-04 | ✅ DONE 2026-05-22 |
| 05-05 | 4 | hub-add.sh 9-step pipeline extend — Step 8 sed-edit .env HUBS_ALLOWLIST atomic + Step 9 PRE-validate + caddy reload zero-downtime + smoke curl | 2 (1 auto + 1 verify) | `Hub_All/api/scripts/hub-add.sh` | PROXY-01 + FACTOR-04 extend | ✅ DONE 2026-05-22 |
| 05-06 | 5 closeout | Docs update CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md + manual smoke checkpoint 4 hub × 11 trang | 6 (5 auto: 4 docs + 1 README + 1 checkpoint:human-action) [W-01 split] | `Hub_All/CLAUDE.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `Hub_All/README.md` | PROXY-03 + PROXY-04 verify | ✅ DONE 2026-05-22 (Task 5b smoke: ${resume_signal_placeholder}) |

**Coverage:** 4/4 REQ (PROXY-01..04) covered ≥ 1 plan/REQ.

**Critical path:** 05-01 ✅ (Wave 1 BLOCKING) → 05-02 ⊥ 05-03 (parallel Wave 2 file-disjoint) → 05-04 (Wave 3 depend 05-02 + 05-03) → 05-05 (Wave 4 depend 05-01 — parallel-able với Wave 3) → 05-06 (Wave 5 closeout depend all).

**Auto-chain pause expected:** Plan 05-06 Task 5b (`checkpoint:human-action gate=blocking`) — manual smoke 4 hub × 11 trang React M2 COMPAT-01. User resume signal: `approved` / `partial: branding only` / `skip smoke` (carry forward pattern Plan 03-05 + 04-07 v3.0-b precedent).

## Phase 5 Results Summary

| Plan | Wave | Objective | Commits | Tests |
|------|------|-----------|---------|-------|
| 05-01 | 1 | Caddyfile wiki block + docker-compose + .env.example | 3 | caddy validate exit 0 + docker compose config exit 0 + grep checks |
| 05-02 | 2 | vitest install + api.ts prefix detect + App.tsx basename | 3 | 10/10 vitest PASS (7 api + 3 App) + tsc lint PASS |
| 05-03 | 2 | Branding registry + 4 hub config + 4 SVG + WCAG helper | 3 | 17/17 vitest PASS + tsc lint PASS |
| 05-04 | 3 | Login.tsx 4 state + Layout sidebar + crossHubSearch absolute | 3 | 12/12 vitest PASS (6 Login + 4 Layout + 2 crossHub) + tsc lint PASS |
| 05-05 | 4 | hub-add.sh 9-step extend | 1+1 | bash -n syntax + structure verify |
| 05-06 | 5 | Closeout docs + smoke checkpoint | 5 | grep acceptance 5 file PASS + Task 5 smoke ${signal} |

**Phase 5 deliverable summary:**
- Caddy `wiki.domain.com/<hub>/api/*` route đúng container hub + strip prefix (PROXY-01 — Caddyfile + docker-compose 2 env + caddy validate PASS).
- 1 build dùng chung 4 deploy — runtime prefix detect + react-router basename auto-prepend (PROXY-02 — api.ts + App.tsx, 10 vitest test PASS).
- D6 EXPIRED formally Phase 5 v3.0-05 — CLAUDE.md §3 explicit note + 11 trang React M2 COMPAT-01 invariant preserve (PROXY-03 — manual smoke checkpoint 4 hub × 11 trang).
- Per-hub login branding — 4 hub initial logo + title VN + tagline VN + themeColor inline CSS var `--hub-theme` (PROXY-04 — branding registry + Login state machine + Layout sidebar swap, 17 vitest registry test PASS).
- FACTOR-04 extend — `make hub-add` chain DB → override.yml → .env → caddy reload → smoke đầy đủ (hub-add.sh 9-step pipeline).

**v3.0-b progress: Phase 4+5 DONE (2/4 phase v3.0-b — 28/~32 plan ≈ 87%). Next: Phase 6 SETTINGS sync hoặc Phase 7 MIGRATE (depend chain).**
```

**Step 4 — Update "## Next Action" section (cuối file STATE.md):**

REPLACE với:
```markdown
## Next Action

`/gsd-discuss-phase 6` System Settings Sync (SETTINGS-01..04 — `rag_config` HTTP pull + Redis pub/sub invalidate `settings:invalidate` channel + `api_keys` verify proxy + `hub_registry` read-only hub con). GA-V3-B chốt ở discuss-phase (HTTP pull khuyến nghị seed vs push webhook vs env var local).
```

Note: `${signal}` placeholder ở Phase 5 Results Summary sẽ được Task 5 checkpoint replace post-resume (`approved` / `partial` / `skip`).
  </action>
  <verify>
    <automated>grep -qE "^completed_phases: 5$" .planning/STATE.md &amp;&amp; grep -qE "^percent: 8[5-9]$" .planning/STATE.md &amp;&amp; grep -q "Phase 5 Planning Summary" .planning/STATE.md &amp;&amp; grep -q "Phase 5 Results Summary" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE "^completed_phases: 5$" .planning/STATE.md` exit 0
    - `grep -qE "^percent: 8[5-9]$" .planning/STATE.md` exit 0 (~85-87%)
    - `grep -qE "^total_plans: 2[8-9]$" .planning/STATE.md` exit 0 (28 plan)
    - `grep -q "Phase 5 Planning Summary" .planning/STATE.md` exit 0
    - `grep -q "Phase 5 Results Summary" .planning/STATE.md` exit 0
    - `grep -q "Phase 5 — Reverse Proxy + Frontend Subpath ✅ \*\*DONE 2026-05-22" .planning/STATE.md` exit 0
    - `grep -q "PROXY-01..04" .planning/STATE.md` exit 0
    - `grep -q "/gsd-discuss-phase 6" .planning/STATE.md` exit 0 (Next Action)
    - Phase 1-4 Summary tables preserve: `grep -c "^## Phase [0-9]+ Planning Summary" .planning/STATE.md` >= 5 (Phase 2 + 3 + 4 + 5; Phase 1 dùng tên khác)
    - YAML frontmatter valid: `head -13 .planning/STATE.md | grep -qE "^---$"` (front + back delimiter)
  </acceptance_criteria>
  <done>
    STATE.md frontmatter + Current Position + Phase 5 Planning Summary + Results Summary + Next Action all updated. v3.0-b progress 87%. Previous phases preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/REQUIREMENTS.md — PROXY-01..04 mark [x] + closeout NOTE</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 100-115 (PROXY section line 108-113 + section header)
  </read_first>
  <action>
**Step 1 — Mark PROXY-01..04 as `[x]`:** Find lines 110-113 và replace `- [ ] **PROXY-XX**:` → `- [x] **PROXY-XX**:` cho 4 REQ.

**Step 2 — ADD NOTE block sau line 113 (cuối PROXY section, TRƯỚC `### SETTINGS` section):**

```markdown

> **NOTE Phase 5 closeout 2026-05-22 (6-plan list):**
> - **Plan 05-01** Wave 1 BLOCKING: `Hub_All/Caddyfile` ADD `{$WIKI_PUBLIC_DOMAIN}` server block parallel MCP block + `path_regexp hub_api ^/({$HUBS_ALLOWLIST_REGEX:yte|duoc|hcns})/api/(.*)$` + `uri strip_prefix /{re.hub_api.1}` + `reverse_proxy http://python-api-{re.hub_api.1}:8080` (port STATIC :8080) + central `/api/*` no-strip + `/.well-known/*` JWKS pass + static SPA `try_files {path} /index.html`. `docker-compose.yml` caddy service env wire + `./frontend/dist:/srv/wiki/dist:ro` mount + depends_on python-api-central. `.env.example` 3 env document. PROXY-01 partial.
> - **Plan 05-02** Wave 2 parallel: Wave 0 vitest@^2 + @testing-library/react@^16 + @testing-library/jest-dom@^6 + jsdom@^25 install + `frontend/vitest.config.ts` jsdom env + `frontend/src/test-setup.ts` jest-dom matchers + `"test": "vitest run"` npm script. `frontend/src/services/api.ts` module-level export `PREFIX/API_BASE/APP_BASE/CURRENT_HUB` từ `window.location.pathname` + `KNOWN_HUBS` runtime config fallback hardcode `['yte','duoc','hcns']`. `frontend/src/App.tsx` wrap `<BrowserRouter basename={APP_BASE}>`. 10 vitest test PASS (7 api + 3 App). PROXY-02 partial.
> - **Plan 05-03** Wave 2 parallel: `frontend/src/branding/index.ts` Vite `import.meta.glob('./*/index.ts', { eager: true })` + `BrandingConfig` type + `getBranding(hub)` fallback central + `getContrastTextColor(themeColor)` WCAG 1.4.11 mitigation hcns amber. 4 hub config `branding/<hub>/index.ts` + 4 SVG placeholder `public/branding/<hub>/logo.svg` (M/Y/D/H text-only). 17 vitest test PASS. PROXY-04 partial.
> - **Plan 05-04** Wave 3: `Login.tsx` 4 state machine (A/B/C/D per UI-SPEC §2) + branding inline CSS var `--hub-theme` + submit button gradient `color-mix()` + T-5-04 strict ?return 4-layer validation (strip `/` + reject `//` + reject `://` + regex hub format + KNOWN_HUBS allowlist) + window.location.replace cross-prefix. `Layout.tsx` sidebar header logo bg + title swap module-level `getBranding(CURRENT_HUB)` + hover ring themeColor. `api.ts::crossHubSearch` REPLACE → ABSOLUTE `/api/search/cross-hub` qua `requestAbsolute<T>` helper (D-V3-Phase4-D3 carry forward). 12 vitest test PASS. PROXY-02 + PROXY-04 satisfied.
> - **Plan 05-05** Wave 4: `api/scripts/hub-add.sh` 7-step → 9-step. Step 8 atomic sed-edit `.env` HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX (tmp file + mv preserve, duplicate skip idempotent). Step 9 PRE-validate `caddy validate --config` (Pitfall 7 silent rollback mitigation) + `caddy reload` zero-downtime + smoke `curl -k https://${WIKI_PUBLIC_DOMAIN}/${HUB}/api/health`. T-5-05 strict guard Plan 02-05 carry forward (regex + RESERVED blacklist + `$HUB` quoted). PROXY-01 + FACTOR-04 extend.
> - **Plan 05-06** Wave 5 closeout: CLAUDE.md §3 D6 EXPIRED + §6 progress row + Phase 5 pattern subsection (file CLAUDE.md). STATE.md frontmatter + Current Position + Phase 5 Planning + Results Summary + Next Action. REQUIREMENTS.md PROXY-01..04 mark [x] + NOTE này. ROADMAP.md Phase 5 row DONE + Progress table. README.md NEW section "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)". Task 5 `checkpoint:human-action gate=blocking` smoke 4 hub × 11 trang React M2 COMPAT-01 (UI-SPEC §7 — 44 checkpoint). Resume signal: `approved` / `partial: branding only` / `skip smoke` (carry forward Plan 03-05 + 04-07 v3.0-b precedent).
>
> **Backward compat:** Phase 5 KHÔNG break M2 — endpoint URL/envelope shape `{success, data, error, meta}` LOCKED unchanged + 11 trang nội dung React preserve (chỉ Login.tsx + Layout.tsx sidebar header touch — R-V3-2 mitigation D2 scope minimal). M2 frontend hardcode `${window.location.hostname}:8180` REMOVED → relative path qua Caddy gateway. localStorage same-origin SSO carry forward (D-V3-Phase5-C2). 307 backend redirect (Plan 03-04 SSO-02) là failsafe layer 2 cho `api.ts::tryRefresh()`.
>
> **STRIDE T-5-01..07 coverage:** T-5-01 Caddy open redirect (mitigate path_regexp anchor) + T-5-02 KNOWN_HUBS tampering (mitigate backend Caddy authoritative) + T-5-03 logo asset path traversal (mitigate regex constrain) + T-5-04 Login ?return open redirect (mitigate 4-layer validation) + T-5-05 hub-add.sh shell injection (mitigate Plan 02-05 carry forward) + T-5-06 XSS theme color (mitigate compile-time const) + T-5-07 localStorage XSS exfil (accept — defer v4.0 HARD-V4-05).
>
> **v3.0-b progress:** Phase 5 DONE (2/4 phase v3.0-b — 28/~32 plan ≈ 87%). Next: `/gsd-discuss-phase 6` System Settings Sync (SETTINGS-01..04 — rag_config HTTP pull TTL + Redis pub/sub invalidate + api_keys proxy + hub_registry read-only).

```

GIỮ NGUYÊN section `### SETTINGS` + downstream MIGRATE section.
  </action>
  <verify>
    <automated>grep -qE "^- \[x\] \*\*PROXY-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^- \[x\] \*\*PROXY-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^- \[x\] \*\*PROXY-03\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^- \[x\] \*\*PROXY-04\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "NOTE Phase 5 closeout" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - 4 REQ checked: `grep -cE "^- \[x\] \*\*PROXY-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns 4
    - `grep -q "NOTE Phase 5 closeout 2026-05-22 (6-plan list)" .planning/REQUIREMENTS.md` exit 0
    - All 6 plan referenced: `grep -cE "Plan 05-0[1-6]" .planning/REQUIREMENTS.md` returns >= 6
    - STRIDE coverage documented: `grep -q "T-5-01..07 coverage" .planning/REQUIREMENTS.md` exit 0
    - Section SETTINGS preserved: `grep -q "^### SETTINGS" .planning/REQUIREMENTS.md` exit 0
    - Section MIGRATE preserved: `grep -q "^### MIGRATE" .planning/REQUIREMENTS.md` exit 0
    - Phase 4 SYNC note preserved (no regression): `grep -q "NOTE Phase 4 closeout" .planning/REQUIREMENTS.md` exit 0
  </acceptance_criteria>
  <done>
    PROXY-01..04 marked [x] + 6-plan closeout NOTE with 6 plan detail + backward compat + STRIDE + v3.0-b progress. SETTINGS + MIGRATE sections preserved.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update .planning/ROADMAP.md — Phase 5 row DONE + Progress table</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read Phase 5 entry line 206-222 + v3.0 progress table nếu có)
  </read_first>
  <action>
**Step 1 — Find Phase 5 entry line 206 (`### Phase 5 — Reverse Proxy + Frontend Subpath (GA-V3-C, D-V3-06)`) — ADD status badge after heading:**

```markdown
### Phase 5 — Reverse Proxy + Frontend Subpath (GA-V3-C, D-V3-06) ✅ DONE 2026-05-22
```

**Step 2 — Sau Goal + Requirements + Success criteria 4 SC (line 207-217) — ADD `**Plans:**` block (nếu chưa có) hoặc UPDATE existing với 6 plan list (parallel với pattern Phase 4):**

```markdown

**Plans:** 6 plans

Plans:
- [x] 05-01-PLAN.md — Caddyfile wiki block (path_regexp + handle + strip + central + .well-known + static SPA) + docker-compose caddy env (WIKI_PUBLIC_DOMAIN + HUBS_ALLOWLIST_REGEX + frontend/dist mount + depends_on python-api-central) + .env.example 3 env document (PROXY-01 — D-V3-Phase5-A1/A2/A4) — **DONE 2026-05-22**
- [x] 05-02-PLAN.md — Wave 0 vitest infra install (vitest@^2 + @testing-library/react@^16 + jest-dom + jsdom + vitest.config.ts + test-setup.ts + npm script) + api.ts module-level prefix detect (PREFIX/API_BASE/APP_BASE/CURRENT_HUB từ window.location + KNOWN_HUBS runtime/fallback hardcode) + App.tsx BrowserRouter basename={APP_BASE} + 10 vitest test PASS (PROXY-02 — D-V3-Phase5-B1/B3 + GA-V3-C confirmed)  — **DONE 2026-05-22**
- [x] 05-03-PLAN.md — Branding registry Vite glob (`import.meta.glob('./*/index.ts', { eager: true })`) + BrandingConfig type + getBranding fallback central + getContrastTextColor WCAG helper + 4 hub config (central indigo #6366f1 / yte emerald #10b981 / duoc sky #0ea5e9 / hcns amber #f59e0b) + 4 SVG placeholder text-only initial (M/Y/D/H) + 17 vitest test PASS (PROXY-04 — D-V3-Phase5-D1/D3) — **DONE 2026-05-22**
- [x] 05-04-PLAN.md — Login.tsx 4 state machine (A central no-return / B central + valid return chip / C hub direct visit skeleton redirect / D invalid silent fallback) + branding inline CSS var --hub-theme + T-5-04 strict ?return 4-layer validation (strip + reject absolute URL + regex + KNOWN_HUBS) + Layout.tsx sidebar header logo bg + title swap module-level getBranding(CURRENT_HUB) + hover ring themeColor + api.ts crossHubSearch ABSOLUTE path '/api/search/cross-hub' qua requestAbsolute<T> helper (D-V3-Phase4-D3 carry forward) + 12 vitest test PASS (PROXY-02 + PROXY-04 — D-V3-Phase5-B4/C1/D2) — **DONE 2026-05-22**
- [x] 05-05-PLAN.md — hub-add.sh 7-step → 9-step pipeline extend FACTOR-04 (Step 8 atomic sed-edit .env HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX preserve other env + duplicate skip idempotent + Step 9 PRE-validate caddy validate trước reload [Pitfall 7] + caddy reload zero-downtime + smoke curl) + T-5-05 strict guard Plan 02-05 carry forward (PROXY-01 + FACTOR-04 extend — D-V3-Phase5-A3) — **DONE 2026-05-22**
- [x] 05-06-PLAN.md — Closeout CLAUDE.md §3 D6 EXPIRED + §6 progress row + Phase 5 pattern subsection + STATE.md frontmatter + Phase 5 Planning + Results Summary + REQUIREMENTS.md PROXY-01..04 mark [x] + NOTE Phase 5 closeout + ROADMAP.md Phase 5 row DONE (file này) + README.md NEW section Reverse Proxy Subpath Deploy Notes + manual smoke checkpoint 4 hub × 11 trang React M2 COMPAT-01 (UI-SPEC §7 — 44 checkpoint, R-V3-2 mitigation) (PROXY-03 + PROXY-04 verify) — **DONE 2026-05-22**

```

GIỮ NGUYÊN existing Goal + Requirements + Success criteria + discuss-phase gray areas + downstream Phase 6/7 sections.
  </action>
  <verify>
    <automated>grep -q "### Phase 5 — Reverse Proxy + Frontend Subpath (GA-V3-C, D-V3-06) ✅ DONE 2026-05-22" .planning/ROADMAP.md &amp;&amp; grep -cE "^- \[x\] 05-0[1-6]-PLAN.md" .planning/ROADMAP.md | grep -q "^6$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "### Phase 5 — Reverse Proxy + Frontend Subpath (GA-V3-C, D-V3-06) ✅ DONE 2026-05-22" .planning/ROADMAP.md` exit 0
    - 6 plan checked: `grep -cE "^- \[x\] 05-0[1-6]-PLAN.md" .planning/ROADMAP.md` returns 6
    - `grep -q "PROXY-01..04" .planning/ROADMAP.md` exit 0 (Requirements line preserved)
    - Phase 6 SETTINGS section preserve: `grep -q "^### Phase 6 — System Settings Sync" .planning/ROADMAP.md` exit 0
    - Phase 7 MIGRATE section preserve: `grep -q "^### Phase 7 — Migration" .planning/ROADMAP.md` exit 0
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 5 row DONE + 6 plan checkbox marked [x] với 1-line objective summary. Phase 6/7 backlog preserved.
  </done>
</task>

<task type="auto">
  <name>Task 5a: Hub_All/README.md add "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)" section (W-01 split — README write only, atomic auto)</name>
  <files>Hub_All/README.md</files>
  <read_first>
    - Hub_All/README.md (full file — locate section "Cross-hub Sync Deploy Notes (Phase 4 v3.0)" để insert sau; section "Milestone status" để insert trước)
  </read_first>
  <action>
INSERT section mới SAU section "Cross-hub Sync Deploy Notes (Phase 4 v3.0)" (cuối Phase 4 doc), TRƯỚC section "Milestone status":

```markdown

## Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)

Phase 5 v3.0 ship Caddy reverse proxy subpath routing đa hub + frontend 1-build prefix detect runtime + per-hub login branding + D-V3-06 D6 expire formally.

### Env vars (root `.env`)

```bash
# Domain serve qua HTTPS Caddy auto-TLS:
# - Dev local: localhost → Caddy self-signed cert (browser cảnh báo, chấp nhận với -k)
# - Prod: wiki.medinet.vn → Caddy auto ACME Let's Encrypt (cần port 80+443 open Internet)
WIKI_PUBLIC_DOMAIN=localhost

# Hub allowlist — comma-separated; operator dùng `make hub-add HUB=<name>` tự update + caddy reload
HUBS_ALLOWLIST=yte,duoc,hcns

# Regex pipe-separated cho Caddy path_regexp (auto-derived từ HUBS_ALLOWLIST — sync atomic qua hub-add.sh step 8)
HUBS_ALLOWLIST_REGEX=yte|duoc|hcns
```

### Routing semantics

| URL pattern | Caddy handle | Upstream | Notes |
|-------------|--------------|----------|-------|
| `wiki.domain.com/<hub>/api/*` | `@hub_api path_regexp` + `uri strip_prefix /<hub>` | `http://python-api-<hub>:8080` | Hub con backend nhận `/api/*` (KHÔNG `/<hub>/api/*`) — M2 router code unchanged |
| `wiki.domain.com/api/*` | `handle /api/*` no-strip | `http://python-api-central:8080` | Central API route + cross-hub `/api/search/cross-hub` |
| `wiki.domain.com/.well-known/*` | `handle /.well-known/*` | `http://python-api-central:8080` | JWKS endpoint Phase 3 SSO-01 |
| `wiki.domain.com/<any>` (catch-all) | `file_server` + `try_files {path} /index.html` | `dist/index.html` SPA | React bootstrap → prefix detect → render đúng route |
| `wiki.domain.com/branding/<hub>/logo.svg` | `file_server` | `dist/branding/<hub>/logo.svg` | Vite copy `public/branding/` → `dist/` |

### Deploy steps

```bash
# 1. Build frontend (host machine — Vite output dist/)
cd Hub_All/frontend && npm run build && cd ..

# 2. Up compose (Postgres + Redis + N python-api + Caddy)
cd Hub_All
cp .env.example .env  # nếu chưa có
docker compose up -d

# 3. Verify Caddy validate + smoke
docker compose exec caddy caddy validate --config /etc/caddy/Caddyfile
curl -k -i https://localhost/api/health       # central
curl -k -i https://localhost/yte/api/health   # python-api-yte (prefix stripped)
curl -k -i https://localhost/                 # index.html SPA
curl -k -i https://localhost/yte/dashboard    # index.html (SPA fallback) → React bootstrap → render Dashboard với basename=/yte
```

### Add a new hub (FACTOR-04 extend Phase 5 + Plan 02-05 carry forward)

`make hub-add HUB=<name>` chain 9 step:
1-7: DB create + override.yml append + compose config verify (Plan 02-05)
8: sed-edit `.env` HUBS_ALLOWLIST + HUBS_ALLOWLIST_REGEX atomic
9: PRE-validate Caddy + reload zero-downtime + smoke curl `/<new>/api/health`

```bash
cd Hub_All
make hub-add HUB=phap_che PORT=8184
# Output: hub registered, Caddy reloaded, smoke check ...
```

### Backward incompat (operator broadcast)

- **Frontend hardcode `${window.location.hostname}:8180`** đã REMOVE (api.ts Plan 05-02). Direct browser bookmark `localhost:8180/...` không còn work — phải qua `wiki.medinet.vn` (Caddy gateway) hoặc `localhost` HTTPS.
- **localStorage same-origin scope** carry forward M2 — token share xuyên subpath cùng origin. Logout `/yte/` → cleared cross-hub (TRUE SSO behavior). XSS concern accept — defer v4.0 HARD-V4-05 httpOnly cookie.
- **11 trang React M2 COMPAT-01** — chỉ Login.tsx + Layout.tsx sidebar header touch (R-V3-2 mitigation D2 scope minimal). Dashboard + Documents + Search + 8 trang khác giữ NGUYÊN styling.

### Rollback procedure

```bash
# Revert Caddyfile + docker-compose
cd Hub_All
git checkout HEAD~1 -- Caddyfile docker-compose.yml
docker compose exec caddy caddy reload --config /etc/caddy/Caddyfile

# Revert frontend (api.ts + App.tsx + Login.tsx + Layout.tsx)
git checkout HEAD~1 -- frontend/src/services/api.ts frontend/src/App.tsx frontend/src/pages/Login.tsx frontend/src/Layout.tsx
cd frontend && npm run build && cd ..
docker compose restart caddy  # reload dist mount

# Hub registry rollback (manual sed .env)
sed -i 's|^HUBS_ALLOWLIST=.*|HUBS_ALLOWLIST=yte,duoc,hcns|' .env
sed -i 's|^HUBS_ALLOWLIST_REGEX=.*|HUBS_ALLOWLIST_REGEX=yte|duoc|hcns|' .env
docker compose exec caddy caddy reload --config /etc/caddy/Caddyfile
```

```

GIỮ NGUYÊN existing sections (Add a new hub Plan 02-05 + SSO Backward Incompat Phase 3 + Cross-hub Sync Phase 4 + Milestone status).
  </action>
  <verify>
    <automated>grep -q "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)" Hub_All/README.md &amp;&amp; grep -q "WIKI_PUBLIC_DOMAIN=localhost" Hub_All/README.md &amp;&amp; grep -q "make hub-add HUB=phap_che" Hub_All/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "## Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)" Hub_All/README.md` exit 0
    - `grep -q "WIKI_PUBLIC_DOMAIN=localhost" Hub_All/README.md` exit 0 (env section)
    - `grep -q "HUBS_ALLOWLIST=yte,duoc,hcns" Hub_All/README.md` exit 0
    - `grep -q "HUBS_ALLOWLIST_REGEX=yte|duoc|hcns" Hub_All/README.md` exit 0
    - `grep -q "wiki.domain.com/<hub>/api/\*" Hub_All/README.md` exit 0 (routing semantics)
    - `grep -q "make hub-add HUB=phap_che" Hub_All/README.md` exit 0 (FACTOR-04 extend)
    - `grep -q "Rollback procedure" Hub_All/README.md` exit 0
    - `grep -q "caddy validate --config" Hub_All/README.md` exit 0 (Caddy validate command)
    - `grep -q "caddy reload --config" Hub_All/README.md` exit 0 (Caddy reload command)
    - `grep -q "curl -k -i https://localhost/yte/api/health" Hub_All/README.md` exit 0 (smoke curl example)
    - Existing sections preserved: `grep -q "Cross-hub Sync Deploy Notes (Phase 4 v3.0)" Hub_All/README.md` exit 0 + `grep -q "SSO Backward Incompat (Phase 3 v3.0)" Hub_All/README.md` exit 0 + `grep -q "Milestone status" Hub_All/README.md` exit 0
    - Markdown structure preserve: `head -1 Hub_All/README.md | grep -q "^# "` exit 0 (top H1 intact)
  </acceptance_criteria>
  <done>
    README.md section "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)" ship — env list (WIKI_PUBLIC_DOMAIN, HUBS_ALLOWLIST, HUBS_ALLOWLIST_REGEX) + caddy validate/reload commands + smoke curl example + rollback procedure. Markdown structure preserved. Atomic auto task — no checkpoint mixed in.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5b (checkpoint:human-action gate=blocking): Manual smoke 4 hub × 11 trang React M2 COMPAT-01 (W-01 split — checkpoint only, PROXY-03 + R-V3-2 verify)</name>
  <read_first>
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md §7 (smoke regression checklist 4 hub × 11 page + 44 checkpoint)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"Smoke Regression Strategy" lines 896-983 (full manual checklist + bash commands)
  </read_first>
  <what-built>
    Plan 05-01 → 05-06 Task 5a delivered: Caddy reverse proxy + frontend 1-build prefix detect + per-hub branding (4 hub initial central/yte/duoc/hcns) + Login 4 state machine + Layout sidebar swap + crossHubSearch absolute path + hub-add.sh 9-step + tryRefresh redirect: 'follow' explicit (B-02) + README "Reverse Proxy Subpath Deploy Notes" section. 40+ vitest test PASS + caddy validate PASS + docker compose config PASS.
  </what-built>
  <how-to-verify>
**Setup commands (run trước khi smoke):**
```bash
cd Hub_All

# .env setup nếu chưa có
[ ! -f .env ] && cp .env.example .env
grep -qE "^WIKI_PUBLIC_DOMAIN=" .env || echo "WIKI_PUBLIC_DOMAIN=localhost" >> .env
grep -qE "^HUBS_ALLOWLIST=" .env || echo "HUBS_ALLOWLIST=yte,duoc,hcns" >> .env
grep -qE "^HUBS_ALLOWLIST_REGEX=" .env || echo "HUBS_ALLOWLIST_REGEX=yte|duoc|hcns" >> .env

# Build frontend
cd frontend && npm run build && cd ..

# Up compose
docker compose up -d
sleep 10
docker compose ps  # verify 8 service healthy (postgres + redis + 4 python-api + caddy + mcp_service)
```

**Verify checklist Caddy routing:**
```bash
curl -k -i https://localhost/api/health           # → 200 (central)
curl -k -i https://localhost/yte/api/health       # → 200 (python-api-yte, prefix stripped)
curl -k -i https://localhost/duoc/api/health      # → 200
curl -k -i https://localhost/hcns/api/health      # → 200
curl -k -i https://localhost/                     # → index.html
curl -k -i https://localhost/yte/dashboard        # → index.html (SPA fallback)
curl -k -i https://localhost/assets/index-*.js    # → 200 actual JS asset
curl -k -i https://localhost/branding/yte/logo.svg # → 200 SVG content
```

**Browser smoke 4 hub × 11 trang (UI-SPEC §7.2 — 44 checkpoint):**

1. **Central baseline** `https://localhost/login` — verify Medinet indigo branding + form render + login → Dashboard
2. **Hub redirect** `https://localhost/yte/login` — verify auto redirect tới `/login?return=/yte` + emerald branding + chip "Sau đăng nhập sẽ vào Hub Y tế Medinet"
3. **Per-hub Layout** `/yte/dashboard`, `/duoc/dashboard`, `/hcns/dashboard` — verify sidebar logo + title đúng hub
4. **11 trang per hub** (Dashboard / Documents / DocumentIngestion / Search/CrossHubSearch / HubRegistry / UserManagement / APIKeyManagement / AuditLog / Profile / Settings / Sync) — verify (a) layout không break, (b) Network DevTools call đúng `/<hub>/api/*`, (c) central-only pages render error gracefully ở hub con, (d) CrossHubSearch absolute path `/api/search/cross-hub` PASS qua Caddy
  </how-to-verify>
  <resume-signal>
**Resume signal options:**
- **`approved`** — Full smoke 44 checkpoint PASS → Phase 5 ship complete
- **`partial: <list>`** — Subset PASS (vd "partial: branding only" = 4 hub × Login + Dashboard branding verify PASS, 9 trang khác defer Phase 7 MIGRATE-05)
- **`skip smoke`** — Defer full smoke Phase 7 MIGRATE-05 (carry forward Plan 03-05 + 04-07 v3.0-b precedent — evidence chain 40+ vitest test in-process + caddy validate + docker compose config cover semantic)
- **`regress in <component>`** — User thấy break ở component cụ thể → reopen `/gsd-debug` plan

**Auto-fallback rule (auto-chain context active):** Nếu user KHÔNG respond trong reasonable window, orchestrator default tới `skip smoke` per v3.0-b precedent (Plan 03-05 + 04-07 pre-resolved skip pattern). Document explicit choice trong SUMMARY.md.

After resume signal received:
- Update STATE.md `Phase 5 Results Summary` row 05-06 Task 5b placeholder `${resume_signal_placeholder}` → actual signal
- Create `.planning/phases/05-reverse-proxy-frontend-subpath/05-06-SUMMARY.md`
  </resume-signal>
  <acceptance_criteria>
    - Smoke checkpoint presented với 3+1 resume signal options (`approved` / `partial: <list>` / `skip smoke` / `regress in <component>`) + auto-fallback rule
    - User signal received OR auto-fallback `skip smoke` applied per v3.0-b precedent
    - STATE.md placeholder `${resume_signal_placeholder}` replaced với actual signal post-resume
    - SUMMARY.md documents explicit choice + rationale
  </acceptance_criteria>
  <done>
    Manual smoke 4 hub × 11 trang React M2 COMPAT-01 checkpoint resume signal recorded. Phase 5 closeout complete. Atomic checkpoint task — no auto write mixed in (W-01 split clean).
  </done>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User operator → smoke checkpoint resume signal | Trusted developer; signal choice documented in SUMMARY |
| Docs file → repository state of truth | Trusted committer; grep-verifiable acceptance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-17 | Repudiation | Smoke checkpoint resume signal lost | mitigate | Resume signal recorded trong STATE.md `Phase 5 Results Summary` + SUMMARY.md commit (git history evidence). Auto-fallback `skip smoke` documented explicitly. |
| T-5-18 | Tampering | Docs edit conflict (parallel writes) | mitigate | 5 docs file edit sequential per task — KHÔNG parallel write. Each task atomic commit. |
</threat_model>

<verification>
- 5 docs file edit grep-verifiable
- Smoke checkpoint presented + resume signal recorded
- Phase 4-3-2-1 sections + downstream Phase 6/7 sections preserved (no regression)
- v3.0-b progress 87% reflect across CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md
</verification>

<success_criteria>
Wave 5 closeout ship — Phase 5 v3.0-05 DONE. 6 plan + 4 REQ-ID PROXY-01..04 + FACTOR-04 extend + D6 EXPIRED formally + STRIDE T-5-01..07 covered. Manual smoke resume signal recorded. v3.0-b 2/4 phase DONE (Phase 4+5 — 28/~32 plan ≈ 87%). Next: `/gsd-discuss-phase 6`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reverse-proxy-frontend-subpath/05-06-SUMMARY.md` với:
- 6 task commits (5 auto: 4 docs + 1 README write + 1 checkpoint) [W-01 split] + 1 SUMMARY commit = 6 commits total
- Acceptance criteria PASS table (5 docs file grep + smoke checkpoint signal)
- Manual smoke result table (4 hub × 11 trang status, hoặc explicit `skip smoke` rationale)
- v3.0 progress final table update (Phase 5 DONE → next Phase 6)
- Phase 5 deliverable consolidated summary (6 plan + 4 REQ + 16 LOCKED decisions + 7 STRIDE threats + FACTOR-04 extend)
- Backward compat verification table (M2 endpoint shape + 11 trang preserve + localStorage carry forward)
- Resume signal documented + auto-fallback rationale (nếu skip per v3.0-b precedent)
- Next action: `/gsd-discuss-phase 6` System Settings Sync
</output>
