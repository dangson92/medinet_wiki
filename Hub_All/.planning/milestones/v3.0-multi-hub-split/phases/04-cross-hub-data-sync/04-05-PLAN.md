---
phase: 04-cross-hub-data-sync
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - api/app/services/search_service.py
  - api/app/main.py
  - api/app/routers/search.py
  - api/tests/unit/test_search_cross_hub_refactor.py
  - api/tests/integration/test_factor_hub_scoped.py
autonomous: true
requirements:
  - SYNC-03
tags:
  - search
  - cross-hub
  - refactor
  - hnsw
user_setup: []

must_haves:
  truths:
    - "`SearchService._search_cross_hub_impl` thay vì fan-out parallel `_search_one(hub_id)` per hub bằng 1 SQL aggregated `SELECT ... FROM chunks WHERE hub_id = ANY($1::uuid[]) ORDER BY vector <=> $embedding LIMIT $k`"
    - "Public API `SearchService.search_cross_hub()` signature KHÔNG đổi (backward compat M2 contract — ask_service consumer giữ nguyên)"
    - "Hub con KHÔNG mount `/api/search/cross-hub` (FACTOR-02 extend — central-only endpoint mới)"
    - "Hub con yte/duoc/hcns GET/POST `/api/search/cross-hub` → 404 envelope D6 NOT_FOUND (KHÔNG 403 — endpoint không exist)"
    - "Central giữ `/api/search/cross-hub` đầy đủ với HNSW iterative_scan=relaxed_order + ef_search=200 (carry forward M2 Phase 6)"
    - "Cross-hub search query 1 connection (KHÔNG fan-out N task) — Prometheus SEARCH_LATENCY{hub_scope='cross'} reflect 1 latency thay vì max(N)"
  artifacts:
    - path: "api/app/services/search_service.py"
      provides: "Refactored _search_cross_hub_impl — 1 SQL aggregated, KHÔNG asyncio.gather fan-out"
      contains: "WHERE hub_id = ANY"
      contains_alt: "ORDER BY vector"
    - path: "api/app/main.py"
      provides: "Conditional mount /api/search/cross-hub endpoint ở central; strip ở hub con"
      contains: "search_router_cross_hub"
      contains_alt: "if settings.hub_name == \"central\""
    - path: "api/app/routers/search.py"
      provides: "Tách 2 endpoint: /api/search (universal local) + /api/search/cross-hub (central-only)"
      contains: "search_cross_hub_router"
    - path: "api/tests/unit/test_search_cross_hub_refactor.py"
      provides: "Unit test _search_cross_hub_impl SQL shape + backward compat signature"
      min_lines: 120
    - path: "api/tests/integration/test_factor_hub_scoped.py"
      provides: "Update endpoint matrix list — /api/search/cross-hub central-only (FACTOR-02 extend)"
      contains: "/api/search/cross-hub"
  key_links:
    - from: "api/app/services/search_service.py::_search_cross_hub_impl"
      to: "_run_vector_query (M2 helper carry forward)"
      via: "Single SQL với hub_ids=list[str] (KHÔNG fan-out single hub_id)"
      pattern: "all_hubs=False.*hub_ids=hub_ids"
    - from: "api/app/main.py create_app"
      to: "search_cross_hub_router conditional mount"
      via: "if settings.hub_name == 'central': app.include_router(search_cross_hub_router)"
      pattern: "search_cross_hub_router"
---

<objective>
Refactor cross-hub search SYNC-03 (D-V3-Phase4-D1): thay fan-out parallel `_search_one(hub_id)` qua N asyncio task bằng 1 SQL aggregated dùng `WHERE hub_id = ANY($1::uuid[])`. Strip endpoint `/api/search/cross-hub` ở hub con (D-V3-Phase4-D3 — central-only).

Purpose:
- **E-V3-2 mitigation:** Cross-hub search p95 < 1.5s carry forward — refactor giảm N connections + N re-rank operations về 1 SQL → faster + HNSW iterative_scan KHÔNG bị split N hub query.
- **D-V3-02 honored:** Aggregated chunks ở `medinet_central.chunks` đã có (sync 1 chiều từ hub con) — KHÔNG cần fan-out HTTP.
- **FACTOR-02 extend:** Hub con KHÔNG own cross-hub aggregated data → strip endpoint là correct semantic.

Output:
- `services/search_service.py` `_search_cross_hub_impl` refactor (single SQL)
- `routers/search.py` tách 2 router: `search_router` (universal /api/search local) + `search_cross_hub_router` (central-only /api/search/cross-hub)
- `main.py` mount conditional `search_cross_hub_router` ở central block
- Unit test verify SQL shape + signature giữ nguyên
- Integration test `test_factor_hub_scoped.py` update endpoint matrix
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-04-PLAN.md

# Reference search M2 + Phase 2 factor pattern
@Hub_All/api/app/services/search_service.py
@Hub_All/api/app/routers/search.py
@Hub_All/api/app/main.py
@Hub_All/api/tests/integration/test_factor_hub_scoped.py

<interfaces>
<!-- M2 _search_cross_hub_impl current state (services/search_service.py:338+) -->

```python
async def _search_cross_hub_impl(
    self,
    *,
    body: SearchRequest,
    user: UserWithHubs,
) -> dict[str, Any]:
    """M2 fan-out implementation — refactor target SYNC-03."""
    t0 = time.perf_counter()
    top_k = _resolve_top_k(body.top_k)
    hub_ids = intersect_hubs(body.hub_ids, user.hub_ids, user.user.role)

    # admin no filter → query all hubs
    if user.user.role == "admin" and not body.hub_ids:
        async with self.pool.acquire() as conn:
            rows = await conn.fetch("SELECT id FROM hubs WHERE is_active = TRUE")
        hub_ids = [str(r["id"]) for r in rows]

    if not hub_ids:
        return SearchResponse(results=[], total_hubs_searched=0, ...).model_dump(...)

    # ... Redis cache get
    query_vector = await embed_text(body.query)

    # FAN-OUT pattern (refactor target)
    async def _search_one(hub_id: str) -> list[Any]:
        rows, _ = await self._run_vector_query(
            query_vector=query_vector,
            hub_ids=[hub_id],  # single hub per task
            top_k=top_k,
            all_hubs=False,
        )
        return rows

    # ... asyncio.gather(*[_search_one(h) for h in hub_ids])
    # ... re-rank merged results → top_k
```

D-V3-Phase4-D1 target:
```python
# Replace fan-out với 1 SQL:
async def _search_one_aggregated(hub_ids: list[str]) -> list[Any]:
    rows, _ = await self._run_vector_query(
        query_vector=query_vector,
        hub_ids=hub_ids,  # ALL hubs in 1 query
        top_k=top_k,
        all_hubs=False,
    )
    return rows
```

`_run_vector_query` đã handle `hub_ids: list[str]` qua `WHERE hub_id = ANY($1::uuid[])` (M2 Phase 6 existing — verify nó accept multi-hub list, KHÔNG chỉ single).

Phase 2 endpoint matrix from REQUIREMENTS.md FACTOR-03:
```
10 endpoint handle LOCAL hub con + 2 SSO_REDIRECT
```
Plan 04-05 thêm: 1 endpoint central-only (cross-hub) → tổng matrix update.

From api/app/main.py current state Phase 2 (Plan 02-01):
```python
# Tier 1 universal mount (mọi hub):
from app.routers import search_router  # ← contain BOTH /api/search + /api/search/cross-hub M2

app.include_router(search_router)
```

Plan 04-05 thay đổi: tách `search_router` ra `search_router` (universal local) + `search_cross_hub_router` (central-only). Backward compat: import path `from app.routers import search_router` vẫn export — Plan 04-05 add `search_cross_hub_router` thêm.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor _search_cross_hub_impl — 1 SQL aggregated thay fan-out</name>
  <read_first>
    - api/app/services/search_service.py TOÀN BỘ (đọc `_run_vector_query` helper + `_search_cross_hub_impl` current state lines 338-450)
    - api/app/services/search_service.py grep "_run_vector_query" tìm signature + xác nhận accept `hub_ids: list[str]` multi-hub array
    - api/app/repositories/search_repository.py hoặc tương tự (M2 vector query SQL builder)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md D-V3-Phase4-D1
  </read_first>
  <pre_task_verify>
    # W4 fix (CR Iteration 1) — confirm M2 inner function name `_search_one` còn tồn tại
    # trước khi rely on acceptance grep `async def _search_one` returns 0.
    cd api && grep -n "async def _search" app/services/search_service.py
    # Expected output (current M2 — verified 2026-05-22):
    #   239:    async def _search_single_impl(
    #   338:    async def _search_cross_hub_impl(  # noqa: C901
    #   391:        async def _search_one(hub_id: str) -> list[Any]:
    # Nếu inner function name khác `_search_one`, update Task 1 acceptance grep pattern.
  </pre_task_verify>
  <behavior>
    - Test 1 (test_search_cross_hub_signature_unchanged): `SearchService.search_cross_hub(body, user)` signature giữ nguyên (kwargs `body: SearchRequest, user: UserWithHubs`); return type `dict[str, Any]`.
    - Test 2 (test_search_cross_hub_empty_hub_ids_returns_empty): user non-admin với user.hub_ids=[] → return `{"results": [], "total_hubs_searched": 0}` envelope.
    - Test 3 (test_search_cross_hub_admin_no_filter_queries_all_active): admin user + body.hub_ids=[] → query "SELECT id FROM hubs WHERE is_active = TRUE" → hub_ids = active list.
    - Test 4 (test_search_cross_hub_intersects_jwt_and_body): user.hub_ids=["yte","duoc"] + body.hub_ids=["yte","hcns"] → intersect = ["yte"] (SSO-03 carry forward).
    - Test 5 (test_search_cross_hub_uses_single_sql_no_fanout): mock `_run_vector_query` AsyncMock — verify called EXACTLY 1 lần với `hub_ids=[<all hubs list>]`, KHÔNG nhiều lần với single hub_id.
    - Test 6 (test_search_cross_hub_redis_cache_get_set_unchanged): cache_key generated từ (query, hub_ids sorted, top_k, min_score) — set + get behavior unchanged.
    - Test 7 (test_search_cross_hub_response_shape_results_list): result dict chứa `results` key (list of items), `total_hubs_searched` (= len(hub_ids)), `query_time_ms`, `cache_hit`.
    - Test 8 (test_search_cross_hub_no_asyncio_gather): inspect.getsource(_search_cross_hub_impl) KHÔNG chứa `asyncio.gather(` — guarantee refactor xoá fan-out pattern.
  </behavior>
  <action>
    Sửa `api/app/services/search_service.py::_search_cross_hub_impl`:

    Giữ NGUYÊN:
    - Early return empty (hub_ids = [])
    - admin no-filter branch (query active hubs)
    - intersect_hubs logic (SSO-03)
    - Redis cache get/set (D-11 carry forward)
    - embed_text query (D-09)
    - return SearchResponse envelope shape

    THAY THẾ block fan-out (asyncio.gather + _search_one + re-rank):

    ```python
    # Phase 4 Plan 04-05 (SYNC-03 / D-V3-Phase4-D1) — Refactor 1 SQL aggregated.
    # M2 fan-out per-hub đã thay bằng single SQL `WHERE hub_id = ANY($1::uuid[])`.
    # HNSW iterative_scan=relaxed_order + ef_search=200 carry forward (M2 Phase 6
    # _run_vector_query handle ở repo layer).
    # E-V3-2: cross-hub p95 < 1.5s — single SQL avoid asyncio.gather overhead +
    # avoid N-times re-rank merge. Re-rank tự nhiên qua ORDER BY vector <=>
    # embedding LIMIT $k của SQL.
    rows, hubs_queried_count = await self._run_vector_query(
        query_vector=query_vector,
        hub_ids=hub_ids,  # ALL hubs in 1 list — _run_vector_query dùng ANY($1::uuid[])
        top_k=top_k,
        all_hubs=False,
    )

    # Build items từ rows
    items = [self._row_to_search_item(row) for row in rows]
    query_time_ms = int((time.perf_counter() - t0) * 1000)

    result = SearchResponse(
        results=items,
        total_hubs_searched=len(hub_ids),
        query_time_ms=query_time_ms,
        cache_hit=False,
    ).model_dump(mode="json")

    # Redis cache set (D-11)
    if self.redis is not None:
        try:
            await self.redis.set(cache_key, json.dumps(result), ex=CACHE_TTL_SECONDS)
            await tag_cache_key(self.redis, cache_key, hub_ids)
        except Exception as exc:  # noqa: BLE001
            logger.warning("cross-hub cache set thất bại: %s", exc)

    logger.info(
        "cross_hub_search_completed: hub_count=%d result_count=%d query_time_ms=%d",
        len(hub_ids), len(items), query_time_ms,
    )
    return result
    ```

    Lưu ý XOÁ:
    - `async def _search_one(hub_id: str)` inner function
    - `asyncio.gather(*[_search_one(h) for h in hub_ids])`
    - Manual re-rank merge logic (SQL ORDER BY tự nhiên handle global top_k)

    Helper `_row_to_search_item` có thể đã có ở M2 repo/service — nếu chưa, dùng pattern existing _search_single_impl row-to-dict transform.

    Verify `_run_vector_query` signature accept `hub_ids: list[str]` multi-element — nếu chỉ accept single, cần extend (M2 Phase 6 đã handle ANY array — verify trong source).
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_search_cross_hub_refactor.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_run_vector_query" api/app/services/search_service.py` returns ≥ 2 lines (_search_single_impl + _search_cross_hub_impl REFACTORED both call).
    - `grep -c "asyncio.gather" api/app/services/search_service.py` returns 0 trong `_search_cross_hub_impl` (có thể vẫn còn ở method khác — verify pattern context).
    - `grep -n "async def _search_one" api/app/services/search_service.py` returns 0 (inner function XOÁ).
    - `grep -n "Phase 4 Plan 04-05" api/app/services/search_service.py` returns ≥ 1 line (refactor marker).
    - `grep -n "hub_ids=hub_ids" api/app/services/search_service.py` returns ≥ 1 line (multi-hub pass).
    - Unit test: `cd api && uv run pytest tests/unit/test_search_cross_hub_refactor.py -v` PASS ≥ 8 tests.
    - M2 search service regression: `cd api && uv run pytest tests/unit/test_search_service.py -v 2>&1 | tail -5` PASS (KHÔNG break single-hub search).
    - `cd api && uv run ruff check app/services/search_service.py` exit 0.
    - `cd api && uv run mypy --strict app/services/search_service.py` exit 0.
  </acceptance_criteria>
  <done>_search_cross_hub_impl refactor 1 SQL aggregated + XOÁ fan-out + 8 unit test PASS + M2 search regression PASS; ruff + mypy PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Tách search_router → 2 router (universal local + central-only cross-hub) + mount conditional</name>
  <read_first>
    - api/app/routers/search.py TOÀN BỘ (đọc current router structure — confirm có endpoint /api/search local + /api/search/cross-hub trong cùng router)
    - api/app/main.py lines 459-505 (current Tier 1 universal mount + Tier 2 central-only block)
    - api/app/routers/__init__.py (xác nhận export search_router)
    - api/tests/integration/test_factor_hub_scoped.py current matrix lines reference
  </read_first>
  <action>
    **A. routers/search.py refactor:**

    Hiện tại `search.py` likely có pattern:
    ```python
    router = APIRouter(prefix="/api/search", tags=["search"])

    @router.post("")  # /api/search local
    async def search_single(...): ...

    @router.post("/cross-hub")  # /api/search/cross-hub
    async def search_cross_hub(...): ...
    ```

    Refactor thành 2 router:
    ```python
    """Phase 4 Plan 04-05 (SYNC-03 D-V3-Phase4-D3) — Tách 2 router cho FACTOR-02 extend.

    - search_router (universal): POST /api/search local single-hub — mount mọi hub
    - search_cross_hub_router (central-only): POST /api/search/cross-hub — chỉ central
    """
    from __future__ import annotations
    # ... existing imports

    # Universal router — single-hub local search
    search_router = APIRouter(prefix="/api/search", tags=["search"])

    @search_router.post("")
    async def search_single(
        body: SearchRequest,
        ...
    ) -> JSONResponse:
        """POST /api/search — local hub search (universal mount)."""
        # ... existing logic — KHÔNG đổi

    # Phase 4 Plan 04-05 — Central-only router cho cross-hub aggregated search.
    # Tách khỏi search_router để main.py mount conditional theo settings.hub_name.
    # FACTOR-02 extend: hub con KHÔNG own cross-hub aggregated data
    # (medinet_central.chunks denormalized read replica per D-V3-02).
    search_cross_hub_router = APIRouter(prefix="/api/search", tags=["search"])

    @search_cross_hub_router.post("/cross-hub")
    async def search_cross_hub(
        body: SearchRequest,
        # ... existing dependencies
    ) -> JSONResponse:
        """POST /api/search/cross-hub — central-only aggregated search.

        Plan 04-05 SYNC-03 / D-V3-Phase4-D1 — 1 SQL aggregated (KHÔNG fan-out).
        Hub con strip → 404 NOT_FOUND envelope D6 (FACTOR-02 extend).
        """
        # ... call SearchService.search_cross_hub (public API signature unchanged)
    ```

    **B. routers/__init__.py update:**
    ```python
    from app.routers.search import search_cross_hub_router, search_router
    # ... existing exports

    __all__ = [
        # ... existing
        "search_cross_hub_router",
    ]
    ```

    **C. main.py create_app() conditional mount:**

    Trong Tier 1 universal mount (giữ nguyên search_router):
    ```python
    app.include_router(search_router)  # universal /api/search local
    ```

    Trong Tier 2 central-only mount block (`if settings.hub_name == "central":`):
    Thêm:
    ```python
    from app.routers import (
        # ... existing central-only imports
        search_cross_hub_router,
    )

    # ... existing app.include_router calls

    # Phase 4 Plan 04-05 (SYNC-03 D-V3-Phase4-D3) — Cross-hub aggregated search
    # central-only. Hub con strip → 404 envelope D6 (FACTOR-02 extend).
    app.include_router(search_cross_hub_router)
    ```

    **D. Verify with curl semantic:**
    - Central: `curl -X POST http://localhost:8180/api/search/cross-hub -d '{...}'` → 200 (or 401 if no JWT, 422 if body invalid — NOT 404).
    - Hub con yte: `curl -X POST http://localhost:8181/api/search/cross-hub -d '{...}'` → 404 envelope `{"success":false,"error":{"code":"NOT_FOUND",...}}`.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_main_factory.py tests/integration/test_factor_hub_scoped.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "search_cross_hub_router" api/app/routers/search.py` returns ≥ 2 lines (declaration + decorator).
    - `grep -n "search_cross_hub_router" api/app/routers/__init__.py` returns ≥ 1 line (export).
    - `grep -n "search_cross_hub_router" api/app/main.py` returns ≥ 2 lines (import + include_router).
    - `grep -n "@search_cross_hub_router.post" api/app/routers/search.py` returns ≥ 1 line.
    - `grep -n "@search_router.post" api/app/routers/search.py` returns ≥ 1 line (local search KHÔNG move).
    - Unit test: `cd api && uv run pytest tests/unit/test_main_factory.py -v` PASS 9/9 (Phase 2 Plan 02-01 regression — KHÔNG break universal/central count).
    - `cd api && uv run ruff check app/routers/search.py app/main.py` exit 0.
    - `cd api && uv run mypy --strict app/routers/search.py app/main.py` exit 0.
  </acceptance_criteria>
  <done>2 router tách + conditional mount + Phase 2 Plan 02-01 unit regression 9/9 PASS; ruff + mypy PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update integration test test_factor_hub_scoped.py — endpoint matrix thêm cross-hub central-only</name>
  <read_first>
    - api/tests/integration/test_factor_hub_scoped.py TOÀN BỘ (Phase 2 Plan 02-03 + Plan 03-04 split 2 list 10 LOCAL + 2 SSO_REDIRECT)
    - api/tests/integration/conftest.py (hub_app_factory fixture)
    - api/app/main.py (Tier 1 + Tier 2 list updated từ Task 2)
  </read_first>
  <behavior>
    - Test 1 (test_central_mounts_search_cross_hub): central app → POST /api/search/cross-hub → != 404 (200/401/422 acceptable — endpoint exists).
    - Test 2 (test_hub_con_strips_search_cross_hub_yte): yte app → POST /api/search/cross-hub → 404 envelope code="NOT_FOUND".
    - Test 3 (test_hub_con_strips_search_cross_hub_duoc): duoc tương tự yte.
    - Test 4 (test_hub_con_strips_search_cross_hub_hcns): hcns tương tự.
    - Test 5 (test_hub_con_mounts_search_local): yte app → POST /api/search → != 404 (local search vẫn mount).
  </behavior>
  <action>
    Sửa `api/tests/integration/test_factor_hub_scoped.py`:

    Hiện tại có:
    ```python
    HUB_SCOPED_LOCAL_ENDPOINTS = [...]  # 10 endpoint handle local hub con
    HUB_SCOPED_SSO_REDIRECT_ENDPOINTS = [...]  # 2 endpoint redirect 307
    CENTRAL_ONLY_ENDPOINTS = [...]  # 8 endpoint strip ở hub con
    ```

    Thêm/cập nhật `CENTRAL_ONLY_ENDPOINTS` để bao gồm `/api/search/cross-hub`:
    ```python
    CENTRAL_ONLY_ENDPOINTS = [
        # Existing 8 endpoint M2/Phase 2:
        ("POST", "/api/hubs"),
        ("PUT", "/api/hubs/{id}"),
        # ... existing 7 more
        # Phase 4 Plan 04-05 (SYNC-03) — Cross-hub aggregated search central-only:
        ("POST", "/api/search/cross-hub"),
    ]
    ```

    Update existing parametrize test (Phase 2 Plan 02-03 pattern) — assertion list grow từ 8 → 9 central-only endpoint. Verify test parametrize loop cover all entries.

    Lưu ý format endpoint matrix có thể là list[tuple[str, str]] hoặc dict — adapt theo existing format.

    Có thể cần thêm dedicated test:
    ```python
    @pytest.mark.parametrize("hub_name", ["yte", "duoc", "hcns"])
    @pytest.mark.asyncio
    async def test_hub_con_strips_search_cross_hub(hub_app_factory, hub_name):
        """Plan 04-05 SYNC-03 / D-V3-Phase4-D3 — Hub con strip cross-hub endpoint."""
        app = hub_app_factory(hub_name, extra_env={
            "JWKS_SKIP_FETCH": "1",
            "COCOINDEX_SKIP_SETUP": "1",
        })
        from fastapi.testclient import TestClient
        with TestClient(app) as client:
            resp = client.post("/api/search/cross-hub", json={"query": "test", "top_k": 5})
            assert resp.status_code == 404
            body = resp.json()
            assert body["success"] is False
            assert body["error"]["code"] == "NOT_FOUND"


    @pytest.mark.asyncio
    async def test_central_mounts_search_cross_hub(hub_app_factory):
        """Plan 04-05 SYNC-03 — Central giữ cross-hub endpoint."""
        app = hub_app_factory("central", extra_env={
            "JWKS_SKIP_FETCH": "1",
            "COCOINDEX_SKIP_SETUP": "1",
        })
        from fastapi.testclient import TestClient
        with TestClient(app) as client:
            resp = client.post("/api/search/cross-hub", json={"query": "test", "top_k": 5})
            # 401 (no JWT) or 422 (validation) — KHÔNG 404
            assert resp.status_code != 404
    ```
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -x -m "critical and integration"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "/api/search/cross-hub" api/tests/integration/test_factor_hub_scoped.py` returns ≥ 2 lines (CENTRAL_ONLY list + test function).
    - `grep -n "test_hub_con_strips_search_cross_hub" api/tests/integration/test_factor_hub_scoped.py` returns ≥ 1 line.
    - `grep -n "test_central_mounts_search_cross_hub" api/tests/integration/test_factor_hub_scoped.py` returns ≥ 1 line.
    - Integration test: `cd api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS (10 existing + 4 new = 14 PASS).
    - Phase 2+3 unit regression: `cd api && uv run pytest tests/unit/test_main_factory.py tests/unit/test_auth_router_hub_redirect.py -v 2>&1 | tail -5` PASS 19/19.
    - `cd api && uv run ruff check tests/integration/test_factor_hub_scoped.py` exit 0.
  </acceptance_criteria>
  <done>Endpoint matrix update + 4 new test PASS + 10 existing test PASS (total 14) + Phase 2/3 unit regression PASS; ruff PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client POST /api/search/cross-hub | JWT verify (SSO-04 carry forward) + hub_ids intersect with body.hub_ids (SSO-03) |
| Central SearchService → medinet_central.chunks | Single SQL `WHERE hub_id = ANY` — data đã sync 1 chiều từ hub con (D-V3-02 LOCKED) |
| Hub con stripped endpoint | 404 envelope D6 NOT_FOUND (FACTOR-02 extend — KHÔNG 403 leak endpoint existence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Spoofing | Stale JWT cross-hub access (user hub_ids legacy) | mitigate | SSO-03 carry forward — intersect_hubs(body.hub_ids, user.hub_ids) ở service layer; SSO-04 Layer 3 dependency get_current_user_for_hub_access KHÔNG apply cross-hub (central bypass — cross-hub by design) |
| T-04-05-02 | Tampering | Body.hub_ids include hub user KHÔNG có quyền | mitigate | intersect_hubs(body.hub_ids, user.hub_ids, role) — admin bypass intersect (cross-hub by design), non-admin filter chỉ hub trong user.hub_ids |
| T-04-05-03 | Info Disclosure | Hub con call /api/search/cross-hub leak central data | mitigate | FACTOR-02 extend strip endpoint hub con → 404 NOT_FOUND envelope. Endpoint chỉ tồn tại ở central process. Hub con KHÔNG có pool tới central read (chỉ central_sync_pool tới write — Plan 04-04). |
| T-04-05-04 | Repudiation | Cross-hub search query KHÔNG audit | accept | logger.info `cross_hub_search_completed: hub_count + result_count + query_time_ms` — đủ audit cho v3.0-b; full audit log defer Phase 6 SETTINGS-04 |
| T-04-05-05 | DoS | Aggregated SQL 4 hub × 1M chunks = 4M ANY array query timeout | mitigate | HNSW iterative_scan=relaxed_order + ef_search=200 carry forward (M2 Phase 6) — index seek per hub_id partition; LIMIT $top_k ORDER BY trim ngay; Postgres ANY() qua btree hash hub_id |
| T-04-05-06 | Elevation | Hub con compromise → bypass strip mount /api/search/cross-hub | accept | In-process mount conditional — KHÔNG cross-process attack. Plan 04-04 lifespan đã enforce skip central_sync_pool ở central, vice versa. |
</threat_model>

<verification>
- `_search_cross_hub_impl` refactor 1 SQL aggregated + XOÁ asyncio.gather fan-out.
- Public API signature `search_cross_hub(body, user)` giữ nguyên (M2 ask_service consumer compat).
- routers/search.py tách 2 router + main.py mount conditional.
- Integration test endpoint matrix update — 9 central-only (was 8) + 14 PASS.
- Unit test ≥ 8 verify SQL shape + signature unchanged.
- Phase 2+3 unit + integration regression PASS.
- ruff + mypy --strict PASS.
- E-V3-2 cross-hub p95 < 1.5s live measure defer Phase 7 MIGRATE-05 runtime smoke (in-process unit test mock asyncpg KHÔNG đo latency thực).
</verification>

<success_criteria>
- `_search_cross_hub_impl` 1 SQL aggregated; SearchService.search_cross_hub() signature unchanged.
- routers/search.py tách 2 router (search_router universal + search_cross_hub_router central-only).
- main.py conditional mount search_cross_hub_router ở central block.
- 8+4 = 12+ unit + 4 integration test PASS.
- Phase 2+3 regression PASS.
- Git commits: `refactor(04-05): _search_cross_hub_impl 1 SQL aggregated thay fan-out (SYNC-03 D-V3-Phase4-D1)` + `feat(04-05): tách search_cross_hub_router central-only (D-V3-Phase4-D3)` + test commit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-hub-data-sync/04-05-SUMMARY.md`.
</output>
