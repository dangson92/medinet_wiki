---
phase: 04-cross-hub-data-sync
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - api/app/db/dsn.py
  - api/app/main.py
  - api/app/rag/flow.py
  - api/tests/integration/conftest.py
  - api/tests/integration/test_sync_lifespan_integration.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-05
tags:
  - lifespan
  - integration
  - asyncpg-pool
  - pgvector
  - cocoindex
user_setup: []

must_haves:
  truths:
    - "Helper `_to_asyncpg_dsn` extract sang module mới `app/db/dsn.py` (W3 fix — Plan 04-06 + 04-04 import từ shared helper, KHÔNG circular import qua app.main)"
    - "create_app() lifespan hub con (settings.hub_name != 'central') init `app.state.central_sync_pool = await asyncpg.create_pool(settings.central_sync_dsn, init=_init_central_sync_conn)` với pgvector codec register PER connection (BLOCKER 2 fix end-to-end serialization)"
    - "create_app() lifespan hub con spawn `app.state.sync_worker_task = asyncio.create_task(sync_worker_loop(app))` SAU pool ready"
    - "Central lifespan KHÔNG spawn worker (skip per D-V3-Phase4-A3) — log evidence `sync_worker_skip_central`"
    - "Graceful shutdown: cancel worker task + await gather timeout 10s + close central_sync_pool TRƯỚC db_pool close"
    - "rag/flow.py ChunkRow.hub_id wire từ `settings.hub_id` (Plan 04-02 Settings field UUID) defensive guard skip nếu mismatch"
    - "Integration test conftest.py thêm fixture `integration_db_pool` (W9 fix) — skipif `INTEGRATION_DB_URL` env không set"
    - "Integration test verify: hub con boot lifespan → mock asyncpg pool → app.state.central_sync_pool init OK + sync_worker_task spawn OK; central skip"
    - "Integration test mới: parse real trigger payload shape Plan 04-01 (hex content_hash + vector float4 list) qua ChunkPayload Pydantic + verify central PUSH_INSERT_CHUNK_SQL execute mocked không raise (BLOCKER 2 end-to-end verify)"
  artifacts:
    - path: "api/app/db/dsn.py"
      provides: "Shared `_to_asyncpg_dsn` helper extract khỏi app/main.py — W3 fix circular import"
      contains: "def _to_asyncpg_dsn"
      min_lines: 30
    - path: "api/app/main.py"
      provides: "Lifespan extend central_sync_pool init (pgvector codec register init param) + sync_worker_task spawn + graceful shutdown; import _to_asyncpg_dsn từ app.db.dsn"
      contains: "central_sync_pool"
      contains_alt: "sync_worker_task"
      contains_extra: "register_vector"
    - path: "api/app/rag/flow.py"
      provides: "ChunkRow.hub_id wire defensive guard match Settings.hub_id"
      contains: "settings.hub_id"
    - path: "api/tests/integration/conftest.py"
      provides: "Fixture `integration_db_pool` mới (W9 fix) — skipif INTEGRATION_DB_URL không set"
      contains: "integration_db_pool"
    - path: "api/tests/integration/test_sync_lifespan_integration.py"
      provides: "Integration test hub con boot + outbox flow E2E + trigger payload parse via ChunkPayload (BLOCKER 2 verify)"
      min_lines: 250
  key_links:
    - from: "api/app/main.py::lifespan"
      to: "app.sync.worker.sync_worker_loop"
      via: "asyncio.create_task spawn ở hub con conditional SAU central_sync_pool ready"
      pattern: "sync_worker_task = asyncio.create_task"
    - from: "api/app/main.py::lifespan"
      to: "asyncpg.Pool(settings.central_sync_dsn) với register_vector init param"
      via: "create_pool init=_init_central_sync_conn → register_vector per connection"
      pattern: "central_sync_pool = await asyncpg.create_pool"
    - from: "api/app/db/dsn.py::_to_asyncpg_dsn"
      to: "api/app/main.py + api/app/observability/checksum_scheduler.py + api/app/routers/sync.py"
      via: "shared module-level helper KHÔNG circular import"
      pattern: "from app.db.dsn import _to_asyncpg_dsn"
---

<objective>
Wave 3 INTEGRATION — wire Plan 04-01 (migration + trigger) + Plan 04-02 (Settings field) + Plan 04-03 (sync module) vào `create_app()` lifespan. Hub con lifespan thêm 2 step mới: (a) init `central_sync_pool` asyncpg pool dedicated tới central VỚI pgvector codec register per connection (BLOCKER 2 fix); (b) spawn `sync_worker_task`. Graceful shutdown ngược thứ tự.

Bổ sung:
- **W3 fix:** extract `_to_asyncpg_dsn` từ app/main.py sang app/db/dsn.py — Plan 04-06 + 04-04 + sync.py router import từ shared module (KHÔNG circular import).
- **W9 fix:** `integration_db_pool` fixture mới ở conftest.py với skipif `INTEGRATION_DB_URL` env không set.
- **BLOCKER 2 end-to-end verify:** integration test parse real trigger payload (hex content_hash + vector float4 list) qua ChunkPayload + verify PUSH_INSERT_CHUNK_SQL execute mocked không raise.
- Wire `Settings.hub_id` (Plan 04-02) vào `rag/flow.py::index_document` defensive guard.

Purpose: Wave 3 BLOCKING — Plan 04-05 + 04-06 deploy được sau khi lifespan integrate worker.

Output:
- `api/app/db/dsn.py` (~30 LOC — shared _to_asyncpg_dsn helper) **NEW**
- `api/app/main.py` lifespan extend: hub con init central_sync_pool (register_vector init) + spawn sync_worker_task; central log skip
- `api/app/rag/flow.py` ChunkRow.hub_id defensive guard
- `api/tests/integration/conftest.py` integration_db_pool fixture mới
- `api/tests/integration/test_sync_lifespan_integration.py` 5+ integration test bao gồm payload parse roundtrip
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-01-PLAN.md
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-02-PLAN.md
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-03-PLAN.md

# Reference lifespan pattern
@Hub_All/api/app/main.py
@Hub_All/api/app/rag/flow.py
@Hub_All/api/tests/integration/conftest.py
@Hub_All/api/tests/integration/test_jwks_cache_lifecycle.py

<interfaces>
<!-- Existing _to_asyncpg_dsn in app/main.py line 50 (Plan 1 carry forward) -->

From api/app/main.py (current state):
```python
# Line 50 — _to_asyncpg_dsn module-level helper
def _to_asyncpg_dsn(sqlalchemy_dsn: str) -> str:
    """Convert SQLAlchemy DSN (postgresql+asyncpg://...) → asyncpg DSN (postgresql://...)."""
    if sqlalchemy_dsn.startswith("postgresql+asyncpg://"):
        return sqlalchemy_dsn.replace("postgresql+asyncpg://", "postgresql://", 1)
    return sqlalchemy_dsn
```

**W3 fix:** Extract sang `api/app/db/dsn.py` (mới) — Plan 04-06 + 04-04 + sync.py router import từ shared module. app/main.py giữ re-export hoặc xoá inline.

**BLOCKER 2 end-to-end pgvector serialization:**
1. Plan 04-01 trigger emit vector qua `to_jsonb(NEW.vector::float4[])` → list[float] trong JSONB.
2. Plan 04-03 ChunkPayload.vector field accept `list[float] | None`.
3. Plan 04-04 PUSH_INSERT_CHUNK_SQL bind `$9::vector` — asyncpg cần biết encode list[float] → pgvector binary format.
4. **PHẢI register pgvector codec ở central_sync_pool** qua `pgvector.asyncpg.register_vector(conn)` ở `init` param của `create_pool`.

```python
from pgvector.asyncpg import register_vector

async def _init_central_sync_conn(conn: asyncpg.Connection) -> None:
    await register_vector(conn)

pool = await asyncpg.create_pool(dsn, init=_init_central_sync_conn, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract `_to_asyncpg_dsn` sang app/db/dsn.py (W3 fix circular import)</name>
  <read_first>
    - api/app/main.py lines 45-60 (current _to_asyncpg_dsn helper)
    - api/app/db/session.py grep "register_vector" — confirm hub con db_pool đã register pgvector codec chưa
    - api/app/db/__init__.py (verify package structure)
  </read_first>
  <action>
    **A. Tạo `api/app/db/dsn.py` mới:**

    ```python
    """Phase 4 Plan 04-04 (W3 fix) — Shared DSN conversion helpers.

    Extract khỏi app/main.py để Plan 04-06 (checksum_scheduler.py + sync.py router)
    + Plan 04-04 (lifespan) import từ module shared — tránh circular import qua app.main.
    """
    from __future__ import annotations


    def _to_asyncpg_dsn(sqlalchemy_dsn: str) -> str:
        """Convert SQLAlchemy DSN (postgresql+asyncpg://...) → asyncpg DSN (postgresql://...).

        SQLAlchemy + Alembic dùng prefix `postgresql+asyncpg://` để chỉ driver;
        asyncpg.create_pool() cần raw `postgresql://`. Strip prefix idempotent.
        """
        if sqlalchemy_dsn.startswith("postgresql+asyncpg://"):
            return sqlalchemy_dsn.replace("postgresql+asyncpg://", "postgresql://", 1)
        return sqlalchemy_dsn


    __all__ = ["_to_asyncpg_dsn"]
    ```

    **B. Sửa `api/app/main.py`** — xoá inline `_to_asyncpg_dsn` + import từ `app.db.dsn`:

    ```python
    from app.db.dsn import _to_asyncpg_dsn  # Phase 4 Plan 04-04 — extracted for cross-module reuse
    ```

    Giữ tất cả call sites trong main.py (line 82+) — chúng vẫn dùng `_to_asyncpg_dsn(...)` nhưng import từ module mới.

    **C. Verify hub con db_pool đã register pgvector codec chưa** (read api/app/db/session.py + grep `register_vector`). Nếu CHƯA register ở local db_pool, thêm `init=_init_pgvector_conn` cho local db_pool ở main.py lifespan step 1 (M2 db_pool init). Document deviation trong action note.

    **D. Verify Plan 04-06 checksum_scheduler.py + Plan 04-06 sync.py router import path đã đổi sang `from app.db.dsn import _to_asyncpg_dsn` (Plan 04-06 thay vì `from app.main import _to_asyncpg_dsn` — W3 fix circular import).** Document trong Plan 04-06 PLAN (update separately) — Task 1 này chỉ owns app/db/dsn.py + app/main.py.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_main_factory.py -v -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `api/app/db/dsn.py` exists ≥ 30 LOC.
    - `grep -n "def _to_asyncpg_dsn" api/app/db/dsn.py` returns ≥ 1 line.
    - `grep -n "from app.db.dsn import _to_asyncpg_dsn" api/app/main.py` returns ≥ 1 line.
    - `grep -c "def _to_asyncpg_dsn" api/app/main.py` returns 0 (inline xoá).
    - `cd api && uv run pytest tests/unit/test_main_factory.py -v` PASS 9/9 (Phase 2 regression KHÔNG break).
    - `cd api && uv run ruff check app/db/dsn.py app/main.py` exit 0.
    - `cd api && uv run mypy --strict app/db/dsn.py app/main.py` exit 0.
  </acceptance_criteria>
  <done>app/db/dsn.py mới + app/main.py import từ shared module (W3 fix) + Phase 2 unit regression 9/9 PASS; ruff + mypy PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Lifespan extend — central_sync_pool init với pgvector codec register + sync_worker_task spawn + graceful shutdown</name>
  <read_first>
    - api/app/main.py lines 58-389 (đọc TOÀN BỘ lifespan + create_app — pattern Phase 3 JWKS cache spawn/shutdown reference)
    - api/app/sync/worker.py (Plan 04-03 — sync_worker_loop signature)
    - api/app/sync/__init__.py (Plan 04-03 — public re-export)
    - api/app/config.py Settings.central_sync_dsn (Plan 04-02 vừa add)
    - api/app/db/dsn.py (Task 1 vừa tạo — _to_asyncpg_dsn import)
  </read_first>
  <action>
    Sửa `api/app/main.py` lifespan:

    **A. Add imports + helper SAU `from app.db.dsn import _to_asyncpg_dsn` (Task 1):**

    ```python
    # Phase 4 Plan 04-04 — pgvector codec register cho central_sync_pool
    # BLOCKER 2 fix end-to-end serialization — Plan 04-03 PUSH_INSERT_CHUNK_SQL bind $9::vector
    # cần asyncpg connection biết encode list[float] → pgvector binary format.
    try:
        from pgvector.asyncpg import register_vector
    except ImportError:
        register_vector = None  # type: ignore


    async def _init_central_sync_conn(conn) -> None:
        """Register pgvector codec cho central_sync_pool connections (BLOCKER 2 fix)."""
        if register_vector is not None:
            await register_vector(conn)
    ```

    **B. Insert step 6.5 SAU `sqlalchemy_engine_ready` log + TRƯỚC watchdog (step 7)**:

    ```python
    # Phase 4 Plan 04-04 (SYNC-01) — Hub con central_sync_pool init với pgvector codec register
    # T-04-03-01 Spoofing mitigation: verify pool kết nối ĐÚNG medinet_central.
    # BLOCKER 2 fix: init=_init_central_sync_conn register pgvector per conn cho $9::vector bind.
    app.state.central_sync_pool = None
    if settings.hub_name != "central":
        try:
            if not settings.central_sync_dsn:
                raise RuntimeError(
                    f"hub_name={settings.hub_name!r} thiếu CENTRAL_SYNC_DSN"
                )
            app.state.central_sync_pool = await asyncpg.create_pool(
                dsn=_to_asyncpg_dsn(settings.central_sync_dsn),
                init=_init_central_sync_conn,  # BLOCKER 2 fix — pgvector codec per conn
                min_size=1,
                max_size=5,
            )
            # Verify pool trỏ ĐÚNG medinet_central (T-04-03-01 mitigation)
            async with app.state.central_sync_pool.acquire() as conn:
                actual_db = await conn.fetchval("SELECT current_database()")
            if actual_db != "medinet_central":
                raise RuntimeError(
                    f"CENTRAL_SYNC_DSN trỏ sai DB: expected 'medinet_central', got {actual_db!r}"
                )
            logger.info(
                "central_sync_pool_ready: hub=%s target_db=%s pgvector_codec=%s",
                settings.hub_name, actual_db, register_vector is not None,
            )
        except Exception as e:
            logger.critical(
                "central_sync_pool_init_failed_fail_fast: hub=%s err=%s",
                settings.hub_name, e,
            )
            raise
    ```

    **C. Insert step 9.5 SAU `search_cache_subscriber_task_started`** — sync_worker_task spawn:

    ```python
    # Phase 4 Plan 04-04 (SYNC-01/02/05) — Hub con sync_worker_task spawn
    app.state.sync_worker_task = None
    if settings.hub_name != "central":
        try:
            from app.sync import sync_worker_loop
            app.state.sync_worker_task = asyncio.create_task(
                sync_worker_loop(app)
            )
            logger.info(
                "sync_worker_task_started: hub=%s batch_size=%d poll_interval=%.1fs",
                settings.hub_name, settings.sync_batch_size, settings.sync_poll_interval,
            )
        except Exception as e:  # noqa: BLE001
            logger.warning("sync_worker_task_start_failed: %s", e)
    else:
        logger.info("sync_worker_task_skipped: hub_name=central (D-V3-Phase4-A3)")
    ```

    **D. Shutdown ngược thứ tự** — TRƯỚC khi shutdown JWKS cache (đầu shutdown sequence):

    ```python
    # Phase 4 Plan 04-04 — Cancel sync_worker_task TRƯỚC mọi shutdown khác
    if getattr(app.state, "sync_worker_task", None) is not None:
        app.state.sync_worker_task.cancel()
        try:
            await asyncio.wait_for(app.state.sync_worker_task, timeout=10.0)
        except asyncio.CancelledError:
            pass
        except asyncio.TimeoutError:
            logger.warning("sync_worker_task_shutdown_timeout: hub=%s — forcing exit", settings.hub_name)
        except Exception as e:  # noqa: BLE001
            logger.warning("sync_worker_task_stop_failed: %s", e)
        app.state.sync_worker_task = None

    # Close central_sync_pool SAU worker cancel
    if getattr(app.state, "central_sync_pool", None) is not None:
        try:
            await app.state.central_sync_pool.close()
        except Exception as e:  # noqa: BLE001
            logger.warning("central_sync_pool_close_failed: %s", e)
        app.state.central_sync_pool = None
    ```

    **E. Optional W3 verify hub con db_pool pgvector register** — read `api/app/db/session.py` grep "register_vector". Nếu chưa có:
    - Document deviation: hub con local db_pool worker dùng SQLAlchemy (KHÔNG asyncpg.execute với vector binding); chỉ central_sync_pool cần pgvector codec vì worker push qua raw asyncpg.execute.
    - Nếu cần register cho local db_pool (vd cocoindex direct execute), thêm `init=_init_central_sync_conn` cho local db_pool ở main.py lifespan step 1.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_main_factory.py -v -x 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "central_sync_pool" api/app/main.py` returns ≥ 4 lines.
    - `grep -n "sync_worker_task" api/app/main.py` returns ≥ 4 lines.
    - `grep -n "register_vector" api/app/main.py` returns ≥ 1 line (BLOCKER 2 fix).
    - `grep -n "_init_central_sync_conn" api/app/main.py` returns ≥ 2 lines (definition + create_pool init param).
    - `grep -n "init=_init_central_sync_conn" api/app/main.py` returns ≥ 1 line.
    - `grep -n "await asyncpg.create_pool" api/app/main.py` returns ≥ 2 lines (M2 db_pool + Phase 4 central_sync_pool).
    - `grep -n 'actual_db != "medinet_central"' api/app/main.py` returns ≥ 1 line.
    - Phase 1+2+3 unit regression: `cd api && uv run pytest tests/unit/test_main_factory.py -v` exit 0 (9/9 PASS).
    - `cd api && uv run ruff check app/main.py` exit 0.
    - `cd api && uv run mypy --strict app/main.py` exit 0.
  </acceptance_criteria>
  <done>Lifespan extend với central_sync_pool init (pgvector codec register init param — BLOCKER 2 fix) + sync_worker spawn + graceful shutdown; 9/9 Phase 2 unit regression PASS.</done>
</task>

<task type="auto">
  <name>Task 3: ChunkRow.hub_id wire Settings.hub_id (Plan 04-02 UUID env) — defensive guard rag/flow.py</name>
  <read_first>
    - api/app/rag/flow.py lines 160-220 (đọc index_document function — current ChunkRow construction)
    - api/app/services/documents_service.py grep "hub_id"
    - api/app/config.py Settings.hub_id (Plan 04-02 vừa add)
  </read_first>
  <action>
    Sửa `api/app/rag/flow.py` function `index_document`:

    Thay block đọc doc_row["hub_id"]:
    ```python
    from app.config import get_settings as _get_settings
    _phase4_settings = _get_settings()

    doc_id = uuid.UUID(str(doc_row["id"]))
    doc_hub_id = uuid.UUID(str(doc_row["hub_id"]))

    # Phase 4 Plan 04-04 (D-V3-Phase4-D2) — defensive guard skip mismatch
    if _phase4_settings.hub_id is not None:
        expected_hub_id = uuid.UUID(_phase4_settings.hub_id)
        if doc_hub_id != expected_hub_id:
            import logging
            logging.getLogger(__name__).warning(
                "ingest_skip_hub_id_mismatch: doc_id=%s doc_hub_id=%s settings_hub_id=%s",
                doc_id, doc_hub_id, expected_hub_id,
            )
            return

    hub_id = doc_hub_id
    ```
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit -k "flow or rag or cocoindex" -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_phase4_settings = _get_settings" api/app/rag/flow.py` returns ≥ 1 line.
    - `grep -n "ingest_skip_hub_id_mismatch" api/app/rag/flow.py` returns ≥ 1 line.
    - Existing rag flow tests vẫn PASS.
    - `cd api && uv run ruff check app/rag/flow.py` exit 0.
    - `cd api && uv run mypy --strict app/rag/flow.py` exit 0.
  </acceptance_criteria>
  <done>flow.py defensive guard hub_id match settings.hub_id + warning log; existing tests PASS; ruff + mypy PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Integration test conftest.py fixture `integration_db_pool` (W9) + test_sync_lifespan_integration.py 5+ test bao gồm BLOCKER 2 trigger payload roundtrip</name>
  <read_first>
    - api/tests/integration/conftest.py (đọc hub_app_factory pattern Phase 2 + 3)
    - api/tests/integration/test_jwks_cache_lifecycle.py (pattern integration TestClient + asgi-lifespan)
    - api/tests/integration/test_factor_hub_scoped.py
    - api/app/sync/worker.py + models.py (Plan 04-03)
  </read_first>
  <behavior>
    - Test 1 (test_lifespan_hub_con_init_central_sync_pool): boot hub_app_factory("yte") với mock asyncpg pool → app.state.central_sync_pool != None + sync_worker_task running + log evidence.
    - Test 2 (test_lifespan_central_skips_sync_worker): boot "central" → central_sync_pool == None + sync_worker_task == None.
    - Test 3 (test_lifespan_hub_con_fail_fast_dsn_mismatch): mock fetchval trả "medinet_hub_yte" (sai) → lifespan raise RuntimeError "CENTRAL_SYNC_DSN trỏ sai DB".
    - Test 4 (test_graceful_shutdown_cancels_worker_within_10s): boot lifespan → check task running → shutdown → task cancelled.
    - Test 5 (test_central_sync_pool_uses_pgvector_init) **BLOCKER 2 verify**: mock asyncpg.create_pool capture init param → assert == `_init_central_sync_conn`.
    - Test 6 (test_chunk_payload_roundtrip_trigger_shape) **BLOCKER 2 end-to-end**: Tạo dict mimic trigger Plan 04-01 output — `{"id": uuid, "document_id": uuid, "hub_id": uuid, "content": "...", "content_hash": "ab" * 16, "vector": [0.1] * 1536, "metadata": {}, "created_at": "..."}` → ChunkPayload.model_validate(...) → assert content_hash == bytes (16 bytes) + vector == list[float] 1536 → pass thẳng vào mock conn.execute với args (id, document_id, hub_id, content, content_hash, ..., vector, ...) → KHÔNG raise.
    - Test 7 (test_outbox_trigger_fires_on_chunks_insert) — skipif INTEGRATION_DB_URL: real DB INSERT chunk → trigger fires → sync_outbox row exists với hex content_hash + list float vector trong payload JSONB.
  </behavior>
  <action>
    **A. Sửa `api/tests/integration/conftest.py`** — Append fixture `integration_db_pool` (W9 fix):

    ```python
    # Phase 4 Plan 04-04 Task 4 (W9 fix) — Live integration DB pool fixture
    # Skipif INTEGRATION_DB_URL env không set — defer Phase 7 MIGRATE-05 runtime smoke.
    import os
    from typing import AsyncIterator
    import asyncpg
    import pytest

    try:
        from pgvector.asyncpg import register_vector
    except ImportError:
        register_vector = None


    @pytest.fixture(scope="session")
    async def integration_db_pool() -> AsyncIterator[asyncpg.Pool | None]:
        """Live asyncpg pool tới test DB cho integration test thực thi trigger.

        Skip nếu env INTEGRATION_DB_URL không set — local dev KHÔNG cần testdb up.
        Phase 7 MIGRATE-05 sẽ wire pytest-docker + alembic apply 0005 trước test.
        """
        url = os.environ.get("INTEGRATION_DB_URL")
        if not url:
            yield None
            return

        async def _init(conn) -> None:
            if register_vector is not None:
                await register_vector(conn)

        pool = await asyncpg.create_pool(
            url, init=_init, min_size=1, max_size=2,
        )
        try:
            yield pool
        finally:
            await pool.close()
    ```

    **B. Tạo `api/tests/integration/test_sync_lifespan_integration.py`** với 7 test cover behavior 1-7:

    ```python
    """Plan 04-04 (SYNC-01/02/05) — Integration test lifespan + outbox flow E2E + BLOCKER 2 payload roundtrip."""
    from __future__ import annotations

    import os
    import uuid
    from unittest.mock import AsyncMock, MagicMock, patch

    import pytest
    from asgi_lifespan import LifespanManager

    from app.sync.models import ChunkPayload

    pytestmark = [pytest.mark.integration]


    class _ContextManagerMock:
        def __init__(self, conn):
            self.conn = conn
        async def __aenter__(self):
            return self.conn
        async def __aexit__(self, *args):
            return None


    @pytest.mark.asyncio
    async def test_lifespan_hub_con_init_central_sync_pool(hub_app_factory):
        mock_conn = AsyncMock()
        mock_conn.fetchval = AsyncMock(return_value="medinet_central")
        mock_pool = AsyncMock()
        mock_pool.acquire = lambda: _ContextManagerMock(mock_conn)
        mock_pool.close = AsyncMock()

        with patch("asyncpg.create_pool", AsyncMock(return_value=mock_pool)):
            app = hub_app_factory("yte", extra_env={
                "HUB_ID": "00000000-0000-0000-0000-000000000001",
                "CENTRAL_SYNC_DSN": "postgresql+asyncpg://u:p@h:5432/medinet_central",
                "JWKS_SKIP_FETCH": "1",
                "COCOINDEX_SKIP_SETUP": "1",
            })
            async with LifespanManager(app):
                assert app.state.central_sync_pool is not None
                assert app.state.sync_worker_task is not None
                assert app.state.sync_worker_task.done() is False
            assert app.state.central_sync_pool is None
            assert app.state.sync_worker_task is None


    @pytest.mark.asyncio
    async def test_lifespan_central_skips_sync_worker(hub_app_factory):
        app = hub_app_factory("central", extra_env={
            "JWKS_SKIP_FETCH": "1", "COCOINDEX_SKIP_SETUP": "1",
        })
        async with LifespanManager(app):
            assert getattr(app.state, "central_sync_pool", None) is None
            assert getattr(app.state, "sync_worker_task", None) is None


    @pytest.mark.asyncio
    async def test_lifespan_hub_con_fail_fast_dsn_mismatch(hub_app_factory):
        mock_conn = AsyncMock()
        mock_conn.fetchval = AsyncMock(return_value="medinet_hub_yte")  # WRONG
        mock_pool = AsyncMock()
        mock_pool.acquire = lambda: _ContextManagerMock(mock_conn)
        mock_pool.close = AsyncMock()

        with patch("asyncpg.create_pool", AsyncMock(return_value=mock_pool)):
            app = hub_app_factory("yte", extra_env={
                "HUB_ID": "00000000-0000-0000-0000-000000000001",
                "CENTRAL_SYNC_DSN": "postgresql+asyncpg://u:p@h:5432/medinet_hub_yte",
                "JWKS_SKIP_FETCH": "1", "COCOINDEX_SKIP_SETUP": "1",
            })
            with pytest.raises((RuntimeError, Exception)) as exc_info:
                async with LifespanManager(app):
                    pass
            err = str(exc_info.value)
            assert "CENTRAL_SYNC_DSN trỏ sai DB" in err or "medinet_central" in err


    @pytest.mark.asyncio
    async def test_central_sync_pool_uses_pgvector_init(hub_app_factory):
        """BLOCKER 2 verify — create_pool init param == _init_central_sync_conn."""
        mock_conn = AsyncMock()
        mock_conn.fetchval = AsyncMock(return_value="medinet_central")
        mock_pool = AsyncMock()
        mock_pool.acquire = lambda: _ContextManagerMock(mock_conn)
        mock_pool.close = AsyncMock()

        create_pool_mock = AsyncMock(return_value=mock_pool)
        with patch("asyncpg.create_pool", create_pool_mock):
            app = hub_app_factory("yte", extra_env={
                "HUB_ID": "00000000-0000-0000-0000-000000000001",
                "CENTRAL_SYNC_DSN": "postgresql+asyncpg://u:p@h:5432/medinet_central",
                "JWKS_SKIP_FETCH": "1", "COCOINDEX_SKIP_SETUP": "1",
            })
            async with LifespanManager(app):
                pass

        # Inspect call args — confirm `init` kwarg passed
        calls_with_init = [
            c for c in create_pool_mock.call_args_list
            if "init" in c.kwargs and c.kwargs["init"] is not None
        ]
        assert len(calls_with_init) >= 1, "central_sync_pool must use init=_init_central_sync_conn"


    def test_chunk_payload_roundtrip_trigger_shape():
        """BLOCKER 2 end-to-end — parse real trigger Plan 04-01 payload shape."""
        trigger_payload = {
            "id": str(uuid.uuid4()),
            "document_id": str(uuid.uuid4()),
            "hub_id": str(uuid.uuid4()),
            "content": "test content Vietnamese có dấu",
            "content_hash": "ab" * 16,  # hex string từ encode(.., 'hex') — 32 hex char = 16 bytes
            "vector": [0.1] * 1536,  # list[float] từ to_jsonb(NEW.vector::float4[])
            "heading_path": "Section 1.2",
            "page_start": 1,
            "page_end": 3,
            "metadata": {"source": "test"},
            "created_at": "2026-05-22T00:00:00+00:00",
        }
        payload = ChunkPayload.model_validate(trigger_payload)
        assert isinstance(payload.content_hash, bytes)
        assert len(payload.content_hash) == 16
        assert isinstance(payload.vector, list)
        assert len(payload.vector) == 1536
        assert all(isinstance(v, float) for v in payload.vector[:10])


    @pytest.mark.skipif(
        not os.environ.get("INTEGRATION_DB_URL"),
        reason="Trigger test requires live Postgres testdb — set INTEGRATION_DB_URL",
    )
    @pytest.mark.asyncio
    async def test_outbox_trigger_fires_on_chunks_insert(integration_db_pool):
        if integration_db_pool is None:
            pytest.skip("INTEGRATION_DB_URL not set")
        async with integration_db_pool.acquire() as conn:
            await conn.execute("TRUNCATE sync_outbox")
            chunk_id = uuid.uuid4()
            doc_id = uuid.uuid4()
            hub_id = uuid.uuid4()
            # INSERT chunks (assume migration 0005 applied + documents + hubs row exist)
            await conn.execute(
                """INSERT INTO chunks (id, document_id, hub_id, content, content_hash, vector, metadata)
                   VALUES ($1, $2, $3, 'test', E'\\\\x' || repeat('ab', 16), NULL, '{}')""",
                chunk_id, doc_id, hub_id,
            )
            rows = await conn.fetch("SELECT * FROM sync_outbox WHERE chunk_id = $1", chunk_id)
            assert len(rows) == 1
            assert rows[0]["op_type"] == "insert"
            assert rows[0]["status"] == "pending"
            # BLOCKER 2 verify — payload có hex content_hash + null vector (this row)
            payload = rows[0]["payload"]
            assert payload["id"] == str(chunk_id)
            assert isinstance(payload["content_hash"], str)
            assert len(payload["content_hash"]) == 32  # 16 bytes = 32 hex char


    @pytest.mark.asyncio
    async def test_graceful_shutdown_cancels_worker_within_10s(hub_app_factory):
        mock_conn = AsyncMock()
        mock_conn.fetchval = AsyncMock(return_value="medinet_central")
        mock_pool = AsyncMock()
        mock_pool.acquire = lambda: _ContextManagerMock(mock_conn)
        mock_pool.close = AsyncMock()

        with patch("asyncpg.create_pool", AsyncMock(return_value=mock_pool)):
            app = hub_app_factory("yte", extra_env={
                "HUB_ID": "00000000-0000-0000-0000-000000000001",
                "CENTRAL_SYNC_DSN": "postgresql+asyncpg://u:p@h:5432/medinet_central",
                "JWKS_SKIP_FETCH": "1", "COCOINDEX_SKIP_SETUP": "1",
            })
            worker_task_ref = None
            async with LifespanManager(app):
                worker_task_ref = app.state.sync_worker_task
                assert worker_task_ref is not None
            assert worker_task_ref.done() is True
            mock_pool.close.assert_awaited()
    ```
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/integration/test_sync_lifespan_integration.py -v -x -m "not skipif"</automated>
  </verify>
  <acceptance_criteria>
    - File `api/tests/integration/test_sync_lifespan_integration.py` exists ≥ 250 LOC.
    - `grep -n "test_lifespan_hub_con_init_central_sync_pool" api/tests/integration/test_sync_lifespan_integration.py` returns ≥ 1.
    - `grep -n "test_lifespan_central_skips_sync_worker" api/tests/integration/test_sync_lifespan_integration.py` returns ≥ 1.
    - `grep -n "test_lifespan_hub_con_fail_fast_dsn_mismatch" api/tests/integration/test_sync_lifespan_integration.py` returns ≥ 1.
    - `grep -n "test_central_sync_pool_uses_pgvector_init" api/tests/integration/test_sync_lifespan_integration.py` returns ≥ 1 (BLOCKER 2 init param verify).
    - `grep -n "test_chunk_payload_roundtrip_trigger_shape" api/tests/integration/test_sync_lifespan_integration.py` returns ≥ 1 (BLOCKER 2 end-to-end).
    - `grep -n "test_graceful_shutdown_cancels_worker_within_10s" api/tests/integration/test_sync_lifespan_integration.py` returns ≥ 1.
    - `grep -n "integration_db_pool" api/tests/integration/conftest.py` returns ≥ 1 line (W9 fixture).
    - `cd api && uv run pytest tests/integration/test_sync_lifespan_integration.py -v` PASS ≥ 6 tests (5 mock + 1 payload roundtrip; skipif 1 live DB).
    - Phase 2+3 integration regression: `cd api && uv run pytest tests/integration -m "critical and integration" -v 2>&1 | tail -10` exit 0.
    - `cd api && uv run ruff check tests/integration/test_sync_lifespan_integration.py tests/integration/conftest.py` exit 0.
  </acceptance_criteria>
  <done>integration_db_pool fixture (W9) + 6 mock-based + 1 live-DB skipif test + BLOCKER 2 init param verify + payload roundtrip ChunkPayload + Phase 2+3 integration regression PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env CENTRAL_SYNC_DSN → lifespan create_pool | Verify SELECT current_database() == 'medinet_central' fail-fast (T-04-03-01) |
| FastAPI lifespan startup → asyncio.create_task sync_worker_loop | Task lifecycle tied to lifespan — graceful cancel + await within 10s timeout |
| ChunkRow.hub_id source = doc_row["hub_id"] | Defensive assert match settings.hub_id (operator deploy responsibility) |
| asyncpg create_pool init → pgvector codec | BLOCKER 2 fix — register_vector per conn cho $9::vector binding (PUSH_INSERT_CHUNK_SQL Plan 04-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Spoofing | Operator paste CENTRAL_SYNC_DSN trỏ medinet_hub_yte (sai DB) | mitigate | Task 2 verify pool fetchval `SELECT current_database()` == 'medinet_central' fail-fast |
| T-04-04-02 | Tampering | Documents row hub_id legacy != settings.hub_id sau migration v3.0 | mitigate | Task 3 rag/flow.py defensive guard skip mismatch + warning log |
| T-04-04-03 | Repudiation | Lifespan startup/shutdown KHÔNG audit | mitigate | structlog evidence: central_sync_pool_ready + sync_worker_task_started + ... |
| T-04-04-04 | Info Disclosure | DSN credentials log to stdout | mitigate | logger chỉ log target_db + hub_name (KHÔNG full DSN) |
| T-04-04-05 | DoS | Worker task hang shutdown → uvicorn force kill | mitigate | asyncio.wait_for(task, timeout=10.0) + log warning |
| T-04-04-06 | Integrity | pgvector codec NOT register → $9::vector binding fail runtime | mitigate | BLOCKER 2 FIX — init=_init_central_sync_conn register_vector per conn; Task 4 test_central_sync_pool_uses_pgvector_init verify init param passed |
| T-04-04-07 | Integrity | Circular import từ Plan 04-06 `from app.main import _to_asyncpg_dsn` | mitigate | W3 FIX — Task 1 extract sang `app/db/dsn.py` shared module |
</threat_model>

<verification>
- W3 fix: app/db/dsn.py mới + main.py + Plan 04-06 modules import từ shared.
- Lifespan extend đủ 2 step (central_sync_pool + sync_worker_task) hub con only + pgvector codec register (BLOCKER 2 fix).
- W9 fix: integration_db_pool fixture với skipif.
- BLOCKER 2 end-to-end: init param verify + ChunkPayload roundtrip parse trigger Plan 04-01 shape.
- 6 mock integration test PASS + 1 live-DB skipif.
- Phase 1+2+3 unit + integration regression PASS.
- ruff + mypy --strict PASS.
</verification>

<success_criteria>
- `api/app/db/dsn.py` mới (W3 fix).
- `api/app/main.py` lifespan thêm central_sync_pool (pgvector codec register) + sync_worker_task spawn + shutdown reverse.
- `api/app/rag/flow.py` defensive guard hub_id mismatch.
- `api/tests/integration/conftest.py` integration_db_pool fixture (W9 fix).
- `api/tests/integration/test_sync_lifespan_integration.py` 6+ mock test PASS + BLOCKER 2 roundtrip verify.
- Phase 2+3 integration regression PASS.
- Git commits: `feat(04-04): extract _to_asyncpg_dsn helper (W3)` + `feat(04-04): lifespan central_sync_pool register_vector + sync_worker_task spawn (SYNC-01/02/05 BLOCKER 2 fix)` + `feat(04-04): rag/flow.py hub_id guard + integration_db_pool fixture` + test commit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-hub-data-sync/04-04-SUMMARY.md`.
</output>
</content>
</invoke>