---
phase: 02-hub-con-codebase-factor
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - FACTOR-01
tags:
  - docker-compose
  - 4-service
  - yaml-anchor
  - cocoindex-lmdb-per-hub
  - port-mapping
  - mcp-re-point
  - d-v3-phase2-c

must_haves:
  truths:
    - "docker-compose.yml có 4 service FastAPI dedicated (python-api-central + python-api-yte + python-api-duoc + python-api-hcns) — KHÔNG dùng deploy.replicas (replicas KHÔNG cho phép env per-replica khác nhau)"
    - "YAML anchor `x-api-template: &api-template` dedupe shared config (build / env_file / volumes keys ro / depends_on / network)"
    - "Mỗi service dùng `<<: *api-template` + override HUB_NAME + DATABASE_URL + COCOINDEX volume per-hub + container_name + ports (D-V3-Phase2-C)"
    - "Port mapping host: central 8180 (giữ M2 backward-compat — frontend hardcode), yte 8181, duoc 8182, hcns 8183 — container internal vẫn 8080"
    - "Cocoindex LMDB volume per-hub (medinet_cocoindex_central, medinet_cocoindex_yte, medinet_cocoindex_duoc, medinet_cocoindex_hcns) — cocoindex 1.0.3 Environment singleton KHÔNG share state cross HUB_NAME"
    - "mcp_service.MCP_API_BASE_URL re-point từ `http://python-api:8080` (M2) sang `http://python-api-central:8080` (D-V3-Phase2-C — service rename)"
    - "Caddy + postgres + redis service KHÔNG đổi (Phase 5 PROXY-01 sẽ extend Caddyfile)"
    - "docker compose config validate YAML đúng + 4 service python-api-* render đầy đủ (yamllint + docker compose config exit 0)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "4 service FastAPI dedicated với YAML anchor + per-hub cocoindex volume + port 8180-8183"
      contains: "x-api-template: &api-template\\|python-api-central:\\|python-api-yte:\\|python-api-duoc:\\|python-api-hcns:\\|medinet_cocoindex_yte:"
  key_links:
    - from: "docker-compose.yml services python-api-*"
      to: "x-api-template anchor"
      via: "<<: *api-template inherit shared config"
      pattern: "<<: \\*api-template"
    - from: "docker-compose.yml services python-api-yte"
      to: "postgres.medinet_hub_yte DB (Phase 1 init-db.sh)"
      via: "DATABASE_URL: postgresql+asyncpg://medinet:...@postgres:5432/medinet_hub_yte"
      pattern: "medinet_hub_yte"
    - from: "docker-compose.yml mcp_service"
      to: "python-api-central:8080 (re-point từ python-api)"
      via: "MCP_API_BASE_URL: http://python-api-central:8080"
      pattern: "python-api-central:8080"
    - from: "docker-compose.yml services python-api-yte"
      to: "Settings._enforce_hub_dsn_match (Phase 1)"
      via: "HUB_NAME=yte + DATABASE_URL/medinet_hub_yte match validator"
      pattern: "HUB_NAME: yte"
---

<objective>
Refactor `Hub_All/docker-compose.yml` từ 1 service `python-api` (M2) sang 4 service FastAPI dedicated (`python-api-central` + `python-api-yte` + `python-api-duoc` + `python-api-hcns`) với YAML anchor `x-api-template: &api-template` dedupe shared config. Mỗi service override `HUB_NAME` + `DATABASE_URL` + cocoindex LMDB volume + container_name + ports. Port mapping: central 8180 (M2 backward-compat), yte 8181, duoc 8182, hcns 8183. Re-point `mcp_service.MCP_API_BASE_URL` từ `http://python-api:8080` sang `http://python-api-central:8080`.

Plan 02-02 chạy Wave 2 parallel với Plan 02-03 — file-disjoint (chỉ sửa `docker-compose.yml`, KHÔNG đụng `tests/`).

Purpose: FACTOR-01 (1 codebase deploy nhiều lần — Docker layer wiring) — đóng phần infrastructure Phase 2.

Output: 1 file modify (`docker-compose.yml`) với 4 service template.

**Locked decisions consume (CONTEXT.md):**
- D-V3-Phase2-C: 4 service dedicated + YAML anchor (KHÔNG `deploy.replicas` — replicas không cho phép env per-replica khác).
- Port mapping cố định: central 8180 / yte 8181 / duoc 8182 / hcns 8183.
- Cocoindex LMDB volume per-hub (Environment singleton KHÔNG share).
- MCP service re-point sang central (KHÔNG fan-out per-hub — LOCKED D-V3-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md
@Hub_All/.planning/phases/01-multi-db-topology/01-01-PLAN.md
@Hub_All/.planning/phases/01-multi-db-topology/01-04-PLAN.md
@Hub_All/.planning/phases/01-multi-db-topology/01-05-PLAN.md
@Hub_All/docker-compose.yml
@Hub_All/api/app/config.py

<interfaces>
<!-- Docker compose M2 hiện tại — 5 service: postgres + redis + python-api + mcp_service + caddy. Plan 02-02 thay python-api bằng 4 service dedicated; giữ nguyên 4 service còn lại. -->

`Hub_All/docker-compose.yml` M2 hiện tại (line 38-74):

```yaml
  python-api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: medinet-api
    env_file:
      - ./api/.env
    environment:
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_central
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
    volumes:
      - ./api/keys:/keys:ro
      - ./file_store:/file_store
      - medinet_cocoindex_state:/app/.cocoindex
    ports:
      - "8180:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks: [medinet_net]
```

`mcp_service` block (line 76-104) hiện có:
```yaml
    environment:
      MCP_API_BASE_URL: http://python-api:8080
    depends_on:
      python-api:
        condition: service_healthy
```

→ Plan 02-02 đổi `http://python-api:8080` → `http://python-api-central:8080` + `depends_on: python-api-central`.

Phase 1 ship 4 DB trên cùng instance Postgres (init-db.sh tạo medinet_central + medinet_hub_yte + medinet_hub_duoc + medinet_hub_hcns). Plan 02-02 wire 4 service FastAPI mỗi service trỏ 1 DB qua DATABASE_URL.

Settings `_enforce_hub_dsn_match` validator (Phase 1) sẽ verify HUB_NAME ↔ DSN match ở startup mỗi container — yte container trỏ medinet_hub_duoc → boot fail-fast.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Thay python-api service M2 bằng 4 service FastAPI dedicated với YAML anchor + cocoindex LMDB volume per-hub + port 8180-8183 + mcp_service re-point</name>
  <files>docker-compose.yml</files>
  <read_first>
    - Hub_All/docker-compose.yml (file đang sửa — toàn bộ 140 dòng)
    - Hub_All/api/app/config.py (Settings.hub_name Literal + DSN validator pattern medinet_central / medinet_hub_<name>)
    - Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md (D-V3-Phase2-C YAML anchor layout đã đặc tả chi tiết)
  </read_first>
  <action>
**Step 1: Backup mentale layout hiện tại** — docker-compose.yml có 5 service: `postgres` (line 4-21) + `redis` (line 23-36) + `python-api` (line 38-74) + `mcp_service` (line 76-104) + `caddy` (line 106-125) + volumes block (line 127-135) + networks (line 137-139). Plan 02-02 CHỈ thay `python-api` thành 4 service + sửa 2 chỗ ở `mcp_service` (env + depends_on) + thêm 3 volume cocoindex per-hub vào volumes block.

**Step 2: Thay TOÀN BỘ block `python-api` (line 38-74) bằng template + 4 service dedicated.**

Chèn anchor `x-api-template:` ở TRƯỚC `services:` block (line 3 — sau `name: medinet-wiki`):

```yaml
# ═════════════════════════════════════════════════════════════════════════
# Phase 2 v3.0 FACTOR-01 (D-V3-Phase2-C) — Shared template cho 4 service
# FastAPI dedicated (central + yte + duoc + hcns). Mỗi service inherit qua
# `<<: *api-template` rồi override HUB_NAME + DATABASE_URL + COCOINDEX volume
# + container_name + ports. KHÔNG dùng deploy.replicas — replicas không cho
# phép env per-replica khác nhau (docker compose limitation; Swarm mode khác).
# ═════════════════════════════════════════════════════════════════════════
x-api-template: &api-template
  build:
    context: ./api
    dockerfile: Dockerfile
  env_file:
    - ./api/.env
  environment:
    REDIS_URL: redis://redis:6379/0
    APP_NAMESPACE: medinet_prod
    COCOINDEX_DB_SCHEMA: cocoindex
    JWT_PRIVATE_KEY_PATH: /keys/private.pem
    JWT_PUBLIC_KEY_PATH: /keys/public.pem
    FILE_STORE_DIR: /file_store
    APP_ENV: ${APP_ENV:-dev}
    LOG_LEVEL: ${LOG_LEVEL:-info}
    # Gap SC5 (Phase 1 Plan 01-04): ép cocoindex LMDB state vào thư mục tuyệt
    # đối writable trong container. environment: thắng env_file.
    COCOINDEX_DB: /app/.cocoindex/state.lmdb
    COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
  volumes:
    - ./api/keys:/keys:ro
    - ./file_store:/file_store
    # cocoindex LMDB volume override per-service (medinet_cocoindex_<hub>)
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  networks: [medinet_net]

services:
```

**LƯU Ý:** Nếu `services:` key đã có (line 3 docker-compose.yml hiện tại — sau name: medinet-wiki), thì chèn `x-api-template:` block VÀO TRƯỚC `services:` (top-level key cùng cấp). YAML cho phép multiple top-level key — `name` + `x-api-template` + `services` + `volumes` + `networks` đều top-level.

**Step 3: Thay block `python-api:` (line 38-74) bằng 4 service dedicated.**

```yaml
  # ─────────────────────────────────────────────────────────────────────
  # Phase 2 v3.0 — Central process (HUB_NAME=central, DB medinet_central)
  # Port host 8180 = M2 backward-compat (frontend api.ts hardcode + Hyper-V
  # excluded range 8038-8137 Windows). Cocoindex LMDB volume riêng.
  # ─────────────────────────────────────────────────────────────────────
  python-api-central:
    <<: *api-template
    container_name: medinet-api-central
    environment:
      HUB_NAME: central
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_central
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
    volumes:
      - ./api/keys:/keys:ro
      - ./file_store:/file_store
      - medinet_cocoindex_central:/app/.cocoindex
    ports:
      - "8180:8080"

  # ─────────────────────────────────────────────────────────────────────
  # Phase 2 v3.0 — Hub y_te (HUB_NAME=yte, DB medinet_hub_yte). Port 8181.
  # ─────────────────────────────────────────────────────────────────────
  python-api-yte:
    <<: *api-template
    container_name: medinet-api-yte
    environment:
      HUB_NAME: yte
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_hub_yte
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
    volumes:
      - ./api/keys:/keys:ro
      - ./file_store:/file_store
      - medinet_cocoindex_yte:/app/.cocoindex
    ports:
      - "8181:8080"

  # ─────────────────────────────────────────────────────────────────────
  # Phase 2 v3.0 — Hub duoc (HUB_NAME=duoc, DB medinet_hub_duoc). Port 8182.
  # ─────────────────────────────────────────────────────────────────────
  python-api-duoc:
    <<: *api-template
    container_name: medinet-api-duoc
    environment:
      HUB_NAME: duoc
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_hub_duoc
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
    volumes:
      - ./api/keys:/keys:ro
      - ./file_store:/file_store
      - medinet_cocoindex_duoc:/app/.cocoindex
    ports:
      - "8182:8080"

  # ─────────────────────────────────────────────────────────────────────
  # Phase 2 v3.0 — Hub hcns (HUB_NAME=hcns, DB medinet_hub_hcns). Port 8183.
  # ─────────────────────────────────────────────────────────────────────
  python-api-hcns:
    <<: *api-template
    container_name: medinet-api-hcns
    environment:
      HUB_NAME: hcns
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_hub_hcns
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
    volumes:
      - ./api/keys:/keys:ro
      - ./file_store:/file_store
      - medinet_cocoindex_hcns:/app/.cocoindex
    ports:
      - "8183:8080"
```

**Lý do mỗi service VIẾT LẠI environment + volumes thay vì merge từ anchor:**
- Docker Compose YAML merge key `<<:` không merge dict lồng cấp 2 (environment / volumes là list/dict). Override 1 environment key sẽ XOÁ TOÀN BỘ environment template — gây thiếu APP_NAMESPACE/JWT_*. SAFE pattern: override toàn bộ environment + volumes ở mỗi service.
- Build / env_file / depends_on / networks merge OK qua `<<:` (top-level key đơn giản).
- Acceptance criteria verify mỗi service có đủ 13 env var (HUB_NAME + DATABASE_URL + COCOINDEX_DATABASE_URL + REDIS_URL + APP_NAMESPACE + COCOINDEX_DB_SCHEMA + JWT_PRIVATE_KEY_PATH + JWT_PUBLIC_KEY_PATH + FILE_STORE_DIR + APP_ENV + LOG_LEVEL + COCOINDEX_DB + COCOINDEX_LMDB_PATH).

**Step 4: Re-point mcp_service.MCP_API_BASE_URL + depends_on.**

Block `mcp_service:` (line 76-104) — sửa 2 chỗ:

```yaml
    environment:
      # Phase 2 v3.0 (D-V3-Phase2-C): re-point từ python-api (M2) → python-api-central
      # MCP service gọi central cho cross-hub aggregate (LOCKED D-V3-02 — KHÔNG fan-out).
      MCP_API_BASE_URL: http://python-api-central:8080
      ...
    depends_on:
      python-api-central:
        condition: service_healthy
```

Giữ nguyên các block khác trong mcp_service (MCP_OAUTH_ISSUER_URL, MCP_OAUTH_STATE_DB_PATH, volumes, container_name, restart).

**Step 5: Thêm 4 volume cocoindex per-hub vào block `volumes:` (line 127-135).**

```yaml
volumes:
  medinet_pgdata:
  medinet_redis_data:
  # Phase 2 v3.0 (D-V3-Phase2-C): cocoindex LMDB volume per-hub.
  # cocoindex 1.0.3 Environment singleton KHÔNG share state cross HUB_NAME.
  # Mỗi container mount 1 volume riêng → state isolated.
  medinet_cocoindex_central:
  medinet_cocoindex_yte:
  medinet_cocoindex_duoc:
  medinet_cocoindex_hcns:
  # Phase 8.3 — OAuth state SQLite (clients/codes/tokens/pending) sống sót restart.
  medinet_mcp_oauth_state:
  # Phase 8.3 — Caddy cert ACME + config (tránh xin lại cert mỗi lần restart).
  medinet_caddy_data:
  medinet_caddy_config:
```

XOÁ `medinet_cocoindex_state:` (M2 single volume — Plan 02-02 thay bằng 4 volume per-hub). Tuy nhiên, NẾU user có M2 data trên `medinet_cocoindex_state` volume hiện tại + chưa muốn lose (Phase 7 migrate sẽ làm formally), giữ alias `medinet_cocoindex_state:` để compatibility (chỉ là named volume — không reference từ service nào sau Plan 02-02). SAFE default: XOÁ. Nếu user feedback regress, restore qua git.

**Step 6: KHÔNG đổi.**
- `postgres:` service (line 4-21) — Phase 1 init-db.sh tạo 4 DB.
- `redis:` service (line 23-36).
- `caddy:` service (line 106-125) — Phase 5 PROXY-01 sẽ extend Caddyfile.
- `networks:` block (line 137-139).
- File `Caddyfile` ở root project.

**Reject patterns:**
- KHÔNG dùng `deploy: replicas: 4` — D-V3-Phase2-C reject. Docker Compose replicas KHÔNG support env per-replica khác nhau ngoài Swarm mode + service template phức tạp.
- KHÔNG dùng 1 cocoindex LMDB volume shared cho 4 service — cocoindex 1.0.3 Environment singleton conflict (TOPO-03 Phase 1 đã isolate qua APP_NAMESPACE + flow name; volume isolation là layer thứ 2 defensive).
- KHÔNG đổi port mapping internal (vẫn 8080 — Dockerfile EXPOSE/uvicorn 8080) — chỉ thay host port mapping.
- KHÔNG fan-out MCP service sang 4 hub — LOCKED D-V3-02 (MCP gọi central aggregate).
  </action>
  <verify>
    <automated>cd Hub_All && docker compose config --quiet && docker compose config --services | sort</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^x-api-template: &api-template$" Hub_All/docker-compose.yml` ≥ 1 (anchor declared)
    - `grep -cE "^  python-api-(central|yte|duoc|hcns):$" Hub_All/docker-compose.yml` == 4 (đúng 4 service dedicated)
    - `grep -c "<<: \*api-template" Hub_All/docker-compose.yml` == 4 (mỗi service inherit anchor)
    - `grep -c "HUB_NAME: central" Hub_All/docker-compose.yml` ≥ 1
    - `grep -c "HUB_NAME: yte" Hub_All/docker-compose.yml` ≥ 1
    - `grep -c "HUB_NAME: duoc" Hub_All/docker-compose.yml` ≥ 1
    - `grep -c "HUB_NAME: hcns" Hub_All/docker-compose.yml` ≥ 1
    - `grep -c "/medinet_central$\|/medinet_hub_yte$\|/medinet_hub_duoc$\|/medinet_hub_hcns$" Hub_All/docker-compose.yml` ≥ 4 (DSN suffix mỗi hub)
    - `grep -cE "^      - \"818[0-3]:8080\"" Hub_All/docker-compose.yml` == 4 (port 8180-8183 host : 8080 container)
    - `grep -c "medinet_cocoindex_central:\|medinet_cocoindex_yte:\|medinet_cocoindex_duoc:\|medinet_cocoindex_hcns:" Hub_All/docker-compose.yml` ≥ 8 (4 volume definition trong volumes block + 4 reference trong service volumes)
    - `grep -c "MCP_API_BASE_URL: http://python-api-central:8080" Hub_All/docker-compose.yml` ≥ 1 (re-point done)
    - `grep -c "MCP_API_BASE_URL: http://python-api:8080" Hub_All/docker-compose.yml` == 0 (M2 reference đã xoá hết)
    - `grep -cE "^    python-api-central:" Hub_All/docker-compose.yml` ≥ 1 (mcp_service.depends_on update sang central)
    - `grep -c "container_name: medinet-api-central\|container_name: medinet-api-yte\|container_name: medinet-api-duoc\|container_name: medinet-api-hcns" Hub_All/docker-compose.yml` == 4
    - `cd Hub_All && docker compose config --quiet` exit 0 (YAML + anchor merge valid)
    - `cd Hub_All && docker compose config --services | sort | tr '\n' ',' | grep -c "caddy,mcp_service,postgres,python-api-central,python-api-duoc,python-api-hcns,python-api-yte,redis,"` == 1 (đúng 8 service tổng)
    - `cd Hub_All && docker compose config | grep -c "python-api-"` ≥ 4 (config render đủ 4 service)
  </acceptance_criteria>
  <done>
docker-compose.yml refactor xong: 1 anchor `x-api-template` + 4 service `python-api-{central,yte,duoc,hcns}` mỗi service inherit anchor + override 13 env var + cocoindex LMDB volume per-hub + port 8180-8183; mcp_service re-point sang python-api-central; 4 volume cocoindex declared; `docker compose config --quiet` exit 0; 8 service tổng render đúng.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker host port → container | Host port 8180-8183 expose tới network host — dev/test direct curl trước khi Caddy Phase 5 strip prefix subpath; production deploy Phase 7 sẽ siết Caddy whitelist (PROXY-01) |
| Container env (HUB_NAME + DATABASE_URL) → Settings._enforce_hub_dsn_match | Phase 1 validator boot-time enforce HUB_NAME ↔ DSN match — container start fail-fast nếu env mismatch (vd HUB_NAME=yte + DSN medinet_hub_duoc) |
| Container python-api-* → postgres service | 4 container cùng kết nối postgres service (4 DB logical riêng); credentials shared (POSTGRES_PASSWORD env) — chấp nhận trong single Postgres instance topology (D-V3-01 LOCKED) |
| Container python-api-yte → medinet_cocoindex_yte volume | Cocoindex LMDB state isolated per-hub volume — KHÔNG share state cross HUB_NAME (1.0.3 Environment singleton conflict) |
| mcp_service → python-api-central | MCP gọi central qua HTTP internal docker network (KHÔNG fan-out 4 hub — LOCKED D-V3-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | Host port 8181-8183 expose hub con direct → bypass Caddy Phase 5 subpath strip → attacker external truy cập trực tiếp hub con (vd `curl http://host:8181/api/auth/login`) | accept | Dev/staging acceptable cho debug + smoke test (Plan 02-03 integration test cần direct port). Production Phase 7 sẽ siết qua firewall rule + Caddy expose CHỈ port 80/443; host port internal CHỈ accessible từ internal network. Phase 2 KHÔNG cover production hardening — ROADMAP scope. |
| T-02-02-02 | Tampering | Container python-api-yte có env DATABASE_URL trỏ sai DB (vd typo / build artifact stale) → boot connect medinet_hub_duoc → cross-hub access | mitigate | Settings._enforce_hub_dsn_match validator Phase 1 raise ValidationError boot-time + container exit 1 → docker compose restart loop visible qua `docker compose ps`. Acceptance criteria verify HUB_NAME ↔ DSN suffix match đúng 4 service (grep DSN endswith /medinet_hub_<name>). |
| T-02-02-03 | Information Disclosure | medinet_cocoindex_yte volume readable từ container khác qua docker volume mount manual (vd dev mount volume khác lên container hcns) | accept | Volume isolation depend on docker daemon trust boundary. Dev tools (`docker run -v medinet_cocoindex_yte:/x ...`) bypass được — out of scope Phase 2. Production access control qua docker socket permission (ops responsibility). |
| T-02-02-04 | Denial of Service | 4 service FastAPI cùng postgres instance → connection pool exhaustion nếu mỗi service pool max_size=10 và Postgres max_connections default 100 | accept | M2 baseline asyncpg pool max_size=10 × 4 service = 40 connection + admin + cocoindex internal = ~60 connection. Postgres default 100 max_connections đủ headroom. Nếu fail trong Phase 4/7 sync workload → tune Postgres `max_connections=200` + pool tune (HARD-V4-XX defer). |
| T-02-02-05 | Spoofing | mcp_service đổi MCP_API_BASE_URL sang python-api-yte (hub con) → MCP forward request cross-hub aggregate qua hub con → leak data | mitigate | docker-compose.yml hardcode MCP_API_BASE_URL=python-api-central. Container env override ở runtime KHÔNG được vì image build-time. Plan 02-02 acceptance criteria verify `MCP_API_BASE_URL: http://python-api-central:8080` literal trong compose YAML. |
| T-02-02-06 | Repudiation | docker compose up 4 service không log evidence để forensic — operator không biết hub nào up khi nào | mitigate | Container logs viewable qua `docker logs medinet-api-{central,yte,duoc,hcns}` riêng biệt (container_name unique per service). Structlog JSON ship + Prometheus `/metrics` per port — operator dashboard parse hub_id từ log + metric label. |
</threat_model>

<verification>
**Plan 02-02 verification chain:**

1. **File integrity (grep)**:
   - `docker-compose.yml`: 1 anchor x-api-template + 4 service python-api-* + 4 volume cocoindex per-hub + MCP re-point central
   - M2 single `python-api:` service block XOÁ hết
   - M2 single `medinet_cocoindex_state` volume reference XOÁ hết

2. **YAML + Docker Compose valid**:
   - `cd Hub_All && docker compose config --quiet` exit 0
   - `cd Hub_All && docker compose config --services | wc -l` == 8 (postgres + redis + 4 python-api + mcp_service + caddy)
   - `cd Hub_All && docker compose config | yq '.services | keys'` chứa 4 service python-api-*

3. **Boot smoke (manual / Plan 02-03 cover thêm integration)**:
   - `cd Hub_All && docker compose up -d postgres redis` lên healthy → `docker compose up -d python-api-central python-api-yte`
   - `curl http://localhost:8180/api/health` (central) → 200
   - `curl http://localhost:8181/api/health` (yte) → 200
   - `docker logs medinet-api-yte | grep central_only_routers_skipped` → match (Plan 02-01 Task 1 evidence)
   - Detailed integration smoke ở Plan 02-03 + Plan 02-04 closeout.

4. **Phase 1 regression KHÔNG break**:
   - `cd Hub_All/api && make migrate-all && bash scripts/alembic-head-check.sh` PASS (4 DB cùng head SHA)
</verification>

<success_criteria>
- docker-compose.yml refactor xong: anchor + 4 service dedicated + cocoindex LMDB volume per-hub + port 8180-8183 + mcp_service re-point (Task 1)
- `docker compose config --quiet` exit 0
- 8 service tổng (postgres + redis + 4 python-api + mcp_service + caddy)
- 4 service python-api-* mỗi service đầy đủ HUB_NAME + DATABASE_URL + COCOINDEX_DATABASE_URL + 10 env var shared
- mcp_service.MCP_API_BASE_URL = http://python-api-central:8080 (re-point done, KHÔNG còn `python-api:8080`)
- 4 volume cocoindex per-hub declared trong volumes block
- FACTOR-01 satisfied: 4 service dedicated deploy 1 codebase 4 mode (verify qua docker compose config + acceptance criteria)
- D-V3-Phase2-C honored (4 service dedicated với YAML anchor, KHÔNG deploy.replicas)
</success_criteria>

<output>
Tạo `.planning/phases/02-hub-con-codebase-factor/02-02-SUMMARY.md`:
- docker-compose.yml diff: x-api-template anchor + 4 service template + 4 cocoindex volume + mcp_service re-point
- Service list (8 service): postgres + redis + python-api-{central,yte,duoc,hcns} + mcp_service + caddy
- Port mapping table (host 8180/8181/8182/8183 → container 8080) + container_name (medinet-api-{central,yte,duoc,hcns})
- MCP service re-point traceability (M2 → python-api-central)
- Cocoindex LMDB volume isolation per-hub (R5 + APP_NAMESPACE Phase 1 defensive layer 2)
- FACTOR-01 Docker layer wiring done; Plan 02-03 integration test sẽ smoke 2 service đồng thời
</output>
</content>
