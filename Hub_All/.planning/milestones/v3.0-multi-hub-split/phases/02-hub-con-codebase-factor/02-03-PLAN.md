---
phase: 02-hub-con-codebase-factor
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - api/tests/integration/test_factor_hub_scoped.py
  - api/tests/integration/conftest.py
autonomous: true
requirements:
  - FACTOR-02
  - FACTOR-03
tags:
  - integration-test
  - endpoint-matrix
  - envelope-shape
  - hub-scoped
  - central-only
  - factor-02
  - factor-03
  - d-v3-phase2-d
  - d-v3-phase2-e
  - d-v3-phase2-h

must_haves:
  truths:
    - "Integration test test_factor_hub_scoped.py PASS: 12 hub-scoped endpoint mount ở hub yte (FACTOR-03 — KHÔNG 404) + 8 central-only endpoint strip ở hub yte (404 envelope) + central giữ NGUYÊN 20 endpoint"
    - "Pytest fixture hub_app_factory parameterized HUB_NAME + DATABASE_URL trỏ /medinet_hub_<name> (Phase 1 _enforce_hub_dsn_match satisfy) + COCOINDEX_SKIP_SETUP=1 escape hatch"
    - "404 envelope shape verify: response.json() == {success:false, data:null, error:{code, message}, meta:null} (D-V3-Phase2-E)"
    - "Hub-scoped endpoint mount verify: response.status_code != 404 (mount đúng, dependency có thể reject 401/422 — KHÔNG quan trọng, miễn không 404)"
    - "Test parametrize 3 hub con (yte/duoc/hcns) — FACTOR-02 hold ở mọi hub"
    - "Phase 1 _enforce_hub_dsn_match KHÔNG regress: HUB_NAME=yte + DSN central → ValidationError raise (E-V3-3 carry forward)"
    - "Marker @pytest.mark.critical + @pytest.mark.integration cho mọi test (HARD-03 CI gate)"
  artifacts:
    - path: "api/tests/integration/test_factor_hub_scoped.py"
      provides: "Integration test endpoint matrix 12 hub-scoped + 8 central-only + envelope shape verify"
      contains: "def test_hub_strips_central_only_routers\\|def test_hub_mounts_hub_scoped_routers\\|def test_central_mounts_all_routers\\|HUB_SCOPED_ENDPOINTS\\|CENTRAL_ONLY_ENDPOINTS"
    - path: "api/tests/integration/conftest.py"
      provides: "Fixture hub_app_factory parameterized HUB_NAME + DSN per-hub"
      contains: "def hub_app_factory\\|HUB_NAME"
  key_links:
    - from: "api/tests/integration/test_factor_hub_scoped.py"
      to: "api/app/main.py::create_app() Plan 02-01"
      via: "hub_app_factory fixture call create_app() sau monkeypatch HUB_NAME"
      pattern: "create_app\\(\\)"
    - from: "api/tests/integration/test_factor_hub_scoped.py"
      to: "api/app/middleware/error_handler.py (M2)"
      via: "TestClient request 404 path → ErrorHandlerMiddleware wrap envelope"
      pattern: "error.*NOT_FOUND\\|error.*code"
    - from: "api/tests/integration/conftest.py::hub_app_factory"
      to: "Settings._enforce_hub_dsn_match (Phase 1)"
      via: "monkeypatch DATABASE_URL endswith /medinet_hub_<name> + get_settings.cache_clear()"
      pattern: "medinet_hub_"
---

<objective>
Tạo integration test `api/tests/integration/test_factor_hub_scoped.py` verify endpoint matrix FACTOR-02 + FACTOR-03:

- **12 endpoint hub-scoped** (4 auth + 2 profile + 3 documents + search + ask + usage — theo D-V3-Phase2-D matrix) MOUNT ở hub yte/duoc/hcns (status code != 404 — mount đúng, dependency có thể reject 401/422 OK).
- **8 endpoint central-only** (rag-config + api-keys + hubs + users + audit-logs + system-settings + sync + mcp) STRIP ở hub yte/duoc/hcns → 404 envelope shape (D-V3-Phase2-E).
- **Central giữ NGUYÊN** 20 endpoint (12 hub-scoped + 8 central-only) — M2 backward-compat.

Extend `api/tests/integration/conftest.py` với fixture `hub_app_factory` parameterized HUB_NAME + DATABASE_URL trỏ /medinet_hub_<name>. Test PASS ở mức TestClient in-process (KHÔNG cần docker compose up — Plan 02-04 sẽ smoke compose-level).

Plan 02-03 chạy Wave 2 parallel với Plan 02-02 — file-disjoint (chỉ sửa `tests/`, KHÔNG đụng `docker-compose.yml`).

Purpose: FACTOR-02 + FACTOR-03 satisfied at integration level — verify endpoint contract chính xác.

Output: 1 file mới (`tests/integration/test_factor_hub_scoped.py`) + 1 file extend (`tests/integration/conftest.py` thêm fixture mới — KHÔNG đụng fixture cũ).

**Locked decisions consume (CONTEXT.md):**
- D-V3-Phase2-D: 12 endpoint hub-scoped matrix (4 auth + 2 profile + 3 documents + search + ask + usage).
- D-V3-Phase2-E: 404 envelope shape `{success:false, data:null, error:{code, message}, meta:null}`.
- D-V3-Phase2-H: Integration test scenario verify 4 service mode (central + 3 hub con).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-01-PLAN.md
@Hub_All/.planning/phases/01-multi-db-topology/01-05-PLAN.md
@Hub_All/api/tests/integration/conftest.py
@Hub_All/api/app/main.py
@Hub_All/api/app/config.py
@Hub_All/api/app/middleware/error_handler.py
@Hub_All/api/app/routers/sync.py

<interfaces>
<!-- Plan 02-01 ship create_app() conditional. Plan 02-03 consume qua TestClient. -->

Từ `api/app/main.py` Plan 02-01 (Wave 1 dependency):
```python
def create_app() -> FastAPI:
    settings = get_settings()
    # Tier 1 universal: 7 router
    # Tier 2 central-only: if settings.hub_name == "central": 9 router
    return app
```

Test pattern (Plan 02-01 Task 2 analog — unit level):
```python
monkeypatch.setenv("HUB_NAME", "yte")
monkeypatch.setenv("DATABASE_URL", "postgresql+asyncpg://u:p@h:5432/medinet_hub_yte")
monkeypatch.setenv("COCOINDEX_DATABASE_URL", "postgresql://u:p@h:5432/medinet_cocoindex")
monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
get_settings.cache_clear()
from app.main import create_app
app = create_app()
```

Plan 02-03 nâng test lên integration level qua `fastapi.testclient.TestClient` + lifespan boot (asgi-lifespan LifespanManager). Lifespan có asyncpg.create_pool nên KHÔNG cần Postgres thật (Phase 1 lifespan pattern fail-soft — KHÔNG raise nếu pool init fail). Cocoindex setup skip qua COCOINDEX_SKIP_SETUP=1. Test in-process — KHÔNG cần testcontainers ở plan này.

Từ `api/app/middleware/error_handler.py` (M2 ship — Plan 02-03 verify):
- 404 không-mount → ErrorHandlerMiddleware wrap envelope `{success:false, data:null, error:{code:"NOT_FOUND", message}, meta:null}`.
- HTTPException handler bổ sung (main.py line 462-486) cover 401/403 dependency reject (mount route OK nhưng auth fail).

Từ `api/app/routers/sync.py` (M2 compat stub — verify trước khi viết matrix):
- 6 endpoint thực tế: `GET /api/sync/stats`, `GET /api/sync/batches`, `GET /api/sync/batches/{id}`, `POST /api/sync/batches`, `POST /api/sync/batches/{id}/pages/{page_id}/approve`, `POST /api/sync/batches/{id}/pages/{page_id}/reject`.
- KHÔNG có `/api/sync/run` (legacy Go-era endpoint đã KHÔNG được port — Plan 02-03 dùng `GET /api/sync/stats` làm proxy verify FACTOR-02 strip ở hub con).

Endpoint matrix D-V3-Phase2-D:

```python
# 12 endpoint hub-scoped (mount ở hub con) — FACTOR-03
HUB_SCOPED_ENDPOINTS = [
    ("POST", "/api/auth/login"),
    ("POST", "/api/auth/refresh"),
    ("POST", "/api/auth/logout"),
    ("GET",  "/api/auth/me"),
    ("GET",  "/api/profile"),
    ("PATCH", "/api/profile"),
    ("POST", "/api/documents"),
    ("GET",  "/api/documents"),
    ("DELETE", "/api/documents/{id}"),  # template path
    ("POST", "/api/search"),
    ("POST", "/api/ask"),
    ("GET",  "/api/usage"),
]

# 8 endpoint central-only (strip ở hub con) — FACTOR-02
# NOTE: sync_router thực tế chỉ có /stats, /batches, /batches/{id}, approve, reject
# (KHÔNG có /run). Plan 02-03 dùng GET /api/sync/stats làm proxy verify strip.
CENTRAL_ONLY_ENDPOINTS = [
    ("GET",  "/api/rag-config"),
    ("GET",  "/api/api-keys"),
    ("GET",  "/api/hubs"),
    ("GET",  "/api/users"),
    ("GET",  "/api/audit-logs"),
    ("GET",  "/api/system-settings"),
    ("GET",  "/api/sync/stats"),   # sync_router (M2 compat stub — verify mount/strip semantic)
    ("GET",  "/api/mcp/my-oauth-client"),
]
```

Hub-scoped test: assert status_code != 404 (mount mới quan trọng — auth reject 401 OK).
Central-only test: assert status_code == 404 AND response.json()["error"]["code"] in ("NOT_FOUND", "ERROR") AND response.json()["success"] is False.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend conftest.py với fixture hub_app_factory parameterized HUB_NAME + DSN per-hub</name>
  <files>api/tests/integration/conftest.py</files>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py (file đang sửa — fixture pattern hiện hữu postgres_container, alembic_cfg, app_with_auth, redis_container)
    - Hub_All/.planning/phases/01-multi-db-topology/01-05-PLAN.md (fixture pg_container_4dbs pattern — Plan 02-03 reuse phần boot 4 DB)
    - Hub_All/api/app/config.py (Settings + _enforce_hub_dsn_match)
  </read_first>
  <behavior>
    - fixture `hub_app_factory` returns callable(hub_name: str) -> FastAPI
    - Mỗi lần call: monkeypatch HUB_NAME + DATABASE_URL + COCOINDEX_DATABASE_URL + REDIS_URL + COCOINDEX_SKIP_SETUP + get_settings.cache_clear() + import & call create_app()
    - DSN format: HUB_NAME=central → /medinet_central; HUB_NAME=<hub> → /medinet_hub_<hub>
    - KHÔNG cần docker / testcontainers — DSN giả lập (Settings validator chỉ check suffix, lifespan asyncpg pool fail-soft)
    - Cleanup get_settings.cache_clear() sau test (autouse fixture trong test_factor_hub_scoped.py — KHÔNG ở conftest.py để tránh affect Phase 1/M2 fixture)
  </behavior>
  <action>
**APPEND-ONLY extend** `api/tests/integration/conftest.py` ở CUỐI FILE (sau các fixture hiện hữu) — KHÔNG đụng `postgres_container`, `alembic_cfg`, `app_with_auth`, fixture v2.0 hiện có.

Thêm 1 fixture mới + 1 helper:

```python
# ═════════════════════════════════════════════════════════════════════════
# Phase 2 v3.0 FACTOR-01..03 (D-V3-Phase2-A/B/D/H) — hub_app_factory fixture
# Boot create_app() với HUB_NAME + DSN per-hub override. KHÔNG cần
# testcontainers Postgres thật — Settings validator chỉ check DSN suffix;
# lifespan asyncpg.create_pool fail-soft (Phase 1 skeleton pattern); cocoindex
# bypass qua COCOINDEX_SKIP_SETUP=1.
#
# Test consume qua TestClient(app) hoặc httpx.AsyncClient + ASGITransport.
#
# CLEANUP NOTE: Plan 02-03 KHÔNG đặt autouse cleanup fixture ở conftest.py.
# Lý do — autouse module-level conftest sẽ chạy cho MỌI test trong thư mục
# integration/ (bao gồm Phase 1 hub_isolation + Phase 3 v2.0 auth_client fixture
# chain) → có thể clear lru_cache singleton GIỮA assert của test khác → flaky.
# Cleanup autouse được khai báo CỤC BỘ trong test_factor_hub_scoped.py
# (Plan 02-03 Task 2) — chỉ áp dụng test file Phase 2.
# ═════════════════════════════════════════════════════════════════════════
from typing import Callable

import pytest as _pytest_phase2  # alias để tránh re-import top-level

_PHASE2_FAKE_PG_HOST = "localhost"
_PHASE2_FAKE_PG_PORT = "5432"


def _phase2_build_dsn(hub_name: str, async_driver: bool = True) -> str:
    """Build DSN giả lập trỏ DB theo hub_name (KHÔNG cần Postgres thật).

    Settings._enforce_hub_dsn_match validator chỉ check segment cuối path
    endswith /medinet_central hay /medinet_hub_<hub>. Host/port/credential
    KHÔNG bị validate ở Settings layer — lifespan asyncpg pool sẽ fail-soft
    nếu connect ko được (Phase 1 skeleton accept).
    """
    if hub_name == "central":
        db = "medinet_central"
    else:
        db = f"medinet_hub_{hub_name}"
    prefix = "postgresql+asyncpg" if async_driver else "postgresql"
    return f"{prefix}://u:p@{_PHASE2_FAKE_PG_HOST}:{_PHASE2_FAKE_PG_PORT}/{db}"


@_pytest_phase2.fixture
def hub_app_factory(monkeypatch):
    """Factory tạo FastAPI app cho từng hub_name.

    Usage trong test:
        def test_xxx(hub_app_factory):
            app = hub_app_factory("yte")
            with TestClient(app) as client:
                resp = client.get("/api/rag-config")
                assert resp.status_code == 404

    Phase 2 FACTOR-01 (D-V3-Phase2-A): create_app() factory no-arg, đọc
    settings.hub_name → conditional mount router.

    KHÔNG cleanup explicit ở yield — monkeypatch tự undo env sau test;
    get_settings.cache_clear() trong factory; test_factor_hub_scoped.py
    khai báo autouse cleanup CỤC BỘ (KHÔNG ở conftest.py — tránh affect
    Phase 1/M2 fixture chain).
    """
    from app.config import get_settings

    def _factory(hub_name: str):
        if hub_name not in ("central", "yte", "duoc", "hcns"):
            raise ValueError(
                f"hub_name {hub_name!r} không thuộc Literal Settings.hub_name; "
                f"valid: central|yte|duoc|hcns"
            )
        monkeypatch.setenv("HUB_NAME", hub_name)
        monkeypatch.setenv("DATABASE_URL", _phase2_build_dsn(hub_name, async_driver=True))
        monkeypatch.setenv(
            "COCOINDEX_DATABASE_URL",
            f"postgresql://u:p@{_PHASE2_FAKE_PG_HOST}:{_PHASE2_FAKE_PG_PORT}/medinet_cocoindex",
        )
        monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
        # COCOINDEX_SKIP_SETUP=1 — bypass cocoindex Environment singleton
        # (DEF-05-01 pattern M2; create_app() lifespan skip step 3 setup_cocoindex).
        monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
        # APP_ENV=dev — bypass CORS LAN-in-prod validator (Phase 1 carry forward).
        monkeypatch.setenv("APP_ENV", "dev")

        # Force re-parse env (lru_cache singleton).
        get_settings.cache_clear()

        # Import sau khi env set — create_app() đọc get_settings() top-level
        # module-import time = OK vì cache_clear() force refresh.
        from app.main import create_app

        return create_app()

    return _factory
```

**KHÔNG đụng / sửa:**
- `postgres_container` fixture
- `alembic_cfg` fixture
- `redis_container` fixture
- `auth_env` / `app_with_auth` / `auth_client` fixture (Phase 3 v2.0 chain)
- `admin_user` / `editor_user` / `viewer_user` fixture
- `admin_token` / `editor_token` / `viewer_token` fixture

**KHÔNG khai báo autouse cleanup fixture ở conftest.py** — autouse module-level sẽ chạy cho mọi test integration (Phase 1 + Phase 3 v2.0 fixture chain) → flaky risk. Cleanup khai báo CỤC BỘ ở test_factor_hub_scoped.py (Task 2 — WRN-03 fix).

**Lý do append-only:**
- Phase 2 fixture mới KHÔNG share state với fixture Phase 3 v2.0 (test_factor_hub_scoped.py KHÔNG import auth_token).
- Sửa fixture cũ có thể break 166 unit test + 5 integration test Phase 1 (regression).
- `hub_app_factory` returns factory function (không fixture pure) → test gọi lúc nào tùy ý.

**Type hint:**
- Sửa import top file `conftest.py` thêm `from typing import Callable` nếu chưa có (verify qua read first). Nếu đã có thì skip.

**Linting note:**
- `import pytest as _pytest_phase2` alias để mypy --strict không phàn nàn về duplicate import (pytest đã import top file). Nếu ruff cảnh báo, dùng `# noqa: F811` hoặc kiểm tra import block trên file. Pattern alternative: chỉ dùng `pytest.fixture` qua import hiện có (skip alias).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run python -c "import sys; sys.path.insert(0, 'tests'); from tests.integration.conftest import hub_app_factory; print('hub_app_factory importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def hub_app_factory" Hub_All/api/tests/integration/conftest.py` ≥ 1
    - `grep -c "_phase2_build_dsn" Hub_All/api/tests/integration/conftest.py` ≥ 2 (function def + at least 1 call)
    - `grep -c "monkeypatch.setenv.\"HUB_NAME\"" Hub_All/api/tests/integration/conftest.py` ≥ 1
    - `grep -c "COCOINDEX_SKIP_SETUP" Hub_All/api/tests/integration/conftest.py` ≥ 1 (escape hatch consume)
    - `grep -c "medinet_hub_" Hub_All/api/tests/integration/conftest.py` ≥ 1 (DSN format docs)
    - `grep -c "get_settings.cache_clear" Hub_All/api/tests/integration/conftest.py` ≥ 1 (force re-parse env trong factory)
    - `grep -c "FACTOR-01\|FACTOR-02\|FACTOR-03\|D-V3-Phase2" Hub_All/api/tests/integration/conftest.py` ≥ 1 (traceability comment)
    - `grep -c "def postgres_container" Hub_All/api/tests/integration/conftest.py` == 1 (KHÔNG xoá fixture cũ)
    - `grep -c "def alembic_cfg" Hub_All/api/tests/integration/conftest.py` == 1 (KHÔNG xoá fixture cũ)
    - `grep -c "autouse=True" Hub_All/api/tests/integration/conftest.py | head -1` — KHÔNG có fixture autouse=True mới thêm bởi Plan 02-03 (verify NO autouse leak ra Phase 1/M2 test chain)
    - `cd Hub_All/api && uv run ruff check tests/integration/conftest.py` exit 0
    - `cd Hub_All/api && uv run mypy tests/integration/conftest.py` exit 0
  </acceptance_criteria>
  <done>
conftest.py extend append-only với `hub_app_factory` fixture + helper `_phase2_build_dsn`; KHÔNG khai báo autouse cleanup (chuyển sang test_factor_hub_scoped.py — WRN-03 fix); KHÔNG đụng fixture v2.0 / Phase 1 hiện hữu; ruff + mypy clean; fixture importable smoke OK.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo test_factor_hub_scoped.py — verify endpoint matrix 12 hub-scoped mount + 8 central-only strip với 404 envelope shape</name>
  <files>api/tests/integration/test_factor_hub_scoped.py</files>
  <read_first>
    - Hub_All/api/tests/integration/conftest.py (Task 1 hub_app_factory fixture vừa thêm)
    - Hub_All/api/app/main.py (Plan 02-01 create_app() conditional)
    - Hub_All/api/app/middleware/error_handler.py (M2 envelope wrap 404)
    - Hub_All/api/app/routers/sync.py (verify endpoint thực tế — KHÔNG có /run, có /stats)
    - Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md (D-V3-Phase2-D endpoint matrix + D-V3-Phase2-E envelope shape)
  </read_first>
  <behavior>
    - Test 1 (`test_central_mounts_all_endpoints`): hub_name=central → mỗi (method, path) trong HUB_SCOPED_ENDPOINTS + CENTRAL_ONLY_ENDPOINTS PHẢI status_code != 404 (mount đúng — không quan tâm 200/401/422)
    - Test 2 (`test_hub_strips_central_only`): parametrize hub_name in ["yte","duoc","hcns"] → mỗi (method, path) trong CENTRAL_ONLY_ENDPOINTS PHẢI status_code == 404 + envelope shape (success=False, error code in {"NOT_FOUND","ERROR"})
    - Test 3 (`test_hub_mounts_hub_scoped`): parametrize hub_name in ["yte","duoc","hcns"] → mỗi (method, path) trong HUB_SCOPED_ENDPOINTS PHẢI status_code != 404 (mount đúng)
    - Test 4 (`test_404_envelope_shape_hub_strip`): hub_name=yte + GET /api/rag-config → response.json() == {"success":False, "data":None, "error":{"code":...,"message":...}, "meta":None}
    - Test 5 (`test_404_envelope_unknown_route_central`): hub_name=central + GET /api/this-does-not-exist → 404 envelope shape (verify ErrorHandlerMiddleware wrap mọi 404, không chỉ FACTOR-02 strip)
    - Test 6 (`test_hub_yte_dsn_mismatch_raises`): HUB_NAME=yte + DSN trỏ /medinet_central → Settings raise ValidationError "DSN mismatch hub_name" (Phase 1 regression check)
    - Autouse cleanup fixture `_phase2_clear_settings_cache_after` (CỤC BỘ trong file này — KHÔNG ở conftest.py) gọi get_settings.cache_clear() trước + sau mỗi test để reset state singleton, KHÔNG affect Phase 1/M2 test fixture chain.
  </behavior>
  <action>
Tạo file `api/tests/integration/test_factor_hub_scoped.py`:

```python
"""Phase 2 FACTOR-02/03 — Integration test endpoint matrix hub-scoped vs central-only.

Verify:
- FACTOR-02: 8 router central-only strip ở hub con (yte/duoc/hcns) → 404 envelope
  shape (D-V3-Phase2-E).
- FACTOR-03: 12 endpoint hub-scoped (D-V3-Phase2-D matrix) mount ở hub con —
  status_code != 404 (mount đúng; dependency có thể reject 401/422 OK).
- Central giữ NGUYÊN 20 endpoint (12 + 8) — M2 backward-compat.
- Phase 1 _enforce_hub_dsn_match regression: HUB_NAME=yte + DSN central → raise.

Test in-process qua TestClient(app) — KHÔNG cần docker/testcontainers (Plan 02-04
sẽ smoke compose-level).

COCOINDEX_SKIP_SETUP=1 trong hub_app_factory bypass cocoindex Environment singleton.
Lifespan asyncpg.create_pool fail-soft (Phase 1 skeleton — log warning, KHÔNG raise).

Plan 02-03 Task 2.

CLEANUP NOTE (WRN-03 fix): autouse `_phase2_clear_settings_cache_after` khai báo
TRONG FILE NÀY (không ở conftest.py) — chỉ áp dụng các test Phase 2 trong file.
Tránh affect Phase 1 hub_isolation + Phase 3 v2.0 auth_client fixture chain (autouse
ở conftest.py sẽ chạy cho mọi test integration → flaky risk).
"""
from __future__ import annotations

import pytest
from fastapi.testclient import TestClient
from pydantic import ValidationError

# Marker — HARD-03 CI gate critical path + integration suite.
pytestmark = [pytest.mark.critical, pytest.mark.integration]


# ─── Endpoint matrix D-V3-Phase2-D ─────────────────────────────────────────
# 12 endpoint hub-scoped — MOUNT ở hub con (FACTOR-03)
HUB_SCOPED_ENDPOINTS = [
    ("POST", "/api/auth/login"),
    ("POST", "/api/auth/refresh"),
    ("POST", "/api/auth/logout"),
    ("GET",  "/api/auth/me"),
    ("GET",  "/api/profile"),
    ("PATCH", "/api/profile"),
    ("POST", "/api/documents"),
    ("GET",  "/api/documents"),
    ("DELETE", "/api/documents/00000000-0000-0000-0000-000000000000"),
    ("POST", "/api/search"),
    ("POST", "/api/ask"),
    ("GET",  "/api/usage"),
]

# 8 endpoint central-only — STRIP ở hub con (FACTOR-02)
# NOTE: sync_router thực tế (api/app/routers/sync.py M2 compat stub) chỉ có
# /stats, /batches, /batches/{id}, approve, reject (KHÔNG có /run legacy).
# Plan 02-03 dùng GET /api/sync/stats làm proxy verify FACTOR-02 strip semantic.
CENTRAL_ONLY_ENDPOINTS = [
    ("GET",  "/api/rag-config"),
    ("GET",  "/api/api-keys"),
    ("GET",  "/api/hubs"),
    ("GET",  "/api/users"),
    ("GET",  "/api/audit-logs"),
    ("GET",  "/api/system-settings"),
    ("GET",  "/api/sync/stats"),
    ("GET",  "/api/mcp/my-oauth-client"),
]


@pytest.fixture(autouse=True)
def _phase2_clear_settings_cache_after():
    """Cleanup lru_cache Settings sau MỖI test Phase 2 — CỤC BỘ trong file này.

    WRN-03 fix: KHÔNG khai báo ở conftest.py để tránh autouse chạy cho mọi test
    integration (Phase 1 hub_isolation + Phase 3 v2.0 auth_client chain) →
    có thể clear cache giữa assert → flaky.

    autouse=True ở module-level chỉ áp dụng test trong file này.
    """
    from app.config import get_settings

    get_settings.cache_clear()
    yield
    get_settings.cache_clear()


def _dispatch(client: TestClient, method: str, path: str):
    """Gọi TestClient với method động, body rỗng (không validate body — chỉ mount check)."""
    method_lower = method.lower()
    if method_lower == "get":
        return client.get(path)
    if method_lower == "post":
        return client.post(path, json={})
    if method_lower == "patch":
        return client.patch(path, json={})
    if method_lower == "delete":
        return client.delete(path)
    if method_lower == "put":
        return client.put(path, json={})
    raise ValueError(f"Method không support: {method}")


def _is_envelope_404(response) -> bool:
    """True nếu response là 404 envelope shape {success:false, data:null, error:{code,message}, meta:null}."""
    if response.status_code != 404:
        return False
    try:
        body = response.json()
    except ValueError:
        return False
    if body.get("success") is not False:
        return False
    if body.get("data") is not None:
        return False
    error = body.get("error")
    if not isinstance(error, dict):
        return False
    code = error.get("code")
    if code not in ("NOT_FOUND", "ERROR", "HTTP_404", "Not Found"):
        # ErrorHandlerMiddleware M2 dùng code "NOT_FOUND" hoặc "ERROR".
        # Khoảng dung sai accept khác value vẫn dict-shape OK.
        pass  # Không reject — chỉ verify shape.
    if "message" not in error:
        return False
    return True


def test_central_mounts_all_endpoints(hub_app_factory):
    """Central giữ NGUYÊN 20 endpoint (12 hub-scoped + 8 central-only)."""
    app = hub_app_factory("central")
    with TestClient(app) as client:
        for method, path in HUB_SCOPED_ENDPOINTS + CENTRAL_ONLY_ENDPOINTS:
            resp = _dispatch(client, method, path)
            assert resp.status_code != 404, (
                f"Central PHẢI mount {method} {path} — status: {resp.status_code} "
                f"body: {resp.text[:200]}"
            )


@pytest.mark.parametrize("hub_name", ["yte", "duoc", "hcns"])
def test_hub_strips_central_only(hub_app_factory, hub_name):
    """Hub con (yte/duoc/hcns) STRIP 8 endpoint central-only → 404 envelope (FACTOR-02)."""
    app = hub_app_factory(hub_name)
    with TestClient(app) as client:
        for method, path in CENTRAL_ONLY_ENDPOINTS:
            resp = _dispatch(client, method, path)
            assert resp.status_code == 404, (
                f"Hub {hub_name} PHẢI strip {method} {path} (404) — actual: "
                f"{resp.status_code} body: {resp.text[:200]}"
            )
            assert _is_envelope_404(resp), (
                f"Hub {hub_name} {method} {path} 404 PHẢI envelope shape "
                f"{{success:false, data:null, error:{{code,message}}, meta:null}} — "
                f"actual body: {resp.text[:300]}"
            )


@pytest.mark.parametrize("hub_name", ["yte", "duoc", "hcns"])
def test_hub_mounts_hub_scoped(hub_app_factory, hub_name):
    """Hub con (yte/duoc/hcns) MOUNT 12 endpoint hub-scoped — status != 404 (FACTOR-03)."""
    app = hub_app_factory(hub_name)
    with TestClient(app) as client:
        for method, path in HUB_SCOPED_ENDPOINTS:
            resp = _dispatch(client, method, path)
            assert resp.status_code != 404, (
                f"Hub {hub_name} PHẢI mount {method} {path} (FACTOR-03) — "
                f"actual: {resp.status_code} body: {resp.text[:200]}"
            )


def test_404_envelope_shape_hub_strip(hub_app_factory):
    """Hub yte + GET /api/rag-config → envelope shape verify chi tiết (D-V3-Phase2-E)."""
    app = hub_app_factory("yte")
    with TestClient(app) as client:
        resp = client.get("/api/rag-config")
        assert resp.status_code == 404, f"Expected 404, got {resp.status_code}"
        body = resp.json()
        assert body["success"] is False
        assert body["data"] is None
        assert isinstance(body["error"], dict)
        assert "code" in body["error"]
        assert "message" in body["error"]
        # meta key OPTIONAL — ErrorHandlerMiddleware M2 set None hoặc absent
        # tuỳ implementation. KHÔNG hard-assert.


def test_404_envelope_unknown_route_central(hub_app_factory):
    """Central + path không tồn tại → 404 envelope (verify ErrorHandlerMiddleware wrap mọi 404)."""
    app = hub_app_factory("central")
    with TestClient(app) as client:
        resp = client.get("/api/this-endpoint-does-not-exist")
        assert resp.status_code == 404
        assert _is_envelope_404(resp), (
            f"Central 404 unknown route PHẢI envelope shape — body: {resp.text[:300]}"
        )


def test_hub_yte_dsn_mismatch_raises(monkeypatch):
    """HUB_NAME=yte + DSN trỏ /medinet_central → Settings raise ValidationError.

    Phase 1 _enforce_hub_dsn_match validator carry forward — Plan 02-03 verify
    KHÔNG regress.
    """
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL", "postgresql+asyncpg://u:p@localhost:5432/medinet_central"
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
    monkeypatch.setenv("APP_ENV", "dev")

    from app.config import Settings, get_settings

    get_settings.cache_clear()

    with pytest.raises(ValidationError, match="DSN mismatch hub_name"):
        Settings()
```

**Note:**
- `TestClient(app)` từ `fastapi.testclient` boot lifespan synchronously — đủ cho test in-process. Cocoindex skip qua env. asyncpg.create_pool fail-soft (Phase 1 skeleton fail-soft pattern) — KHÔNG raise dù không có Postgres.
- HUB_SCOPED_ENDPOINTS endpoint DELETE dùng UUID dummy `00000000-...` để route resolve đúng pattern path param.
- Acceptance test cover 6 test định nghĩa + 6 parametrize (test_hub_strips × 3 hub + test_hub_mounts × 3 hub) = 12 PASS tổng.
- `pytestmark = [pytest.mark.critical, pytest.mark.integration]` mark mọi test trong file → HARD-03 CI gate.
- `_phase2_clear_settings_cache_after` autouse CỤC BỘ — WRN-03 fix.
- Nếu real-world endpoint prefix khác matrix (vd `/api/mcp/my-oauth-client` thực ra là `/api/mcp/oauth-client`), điều chỉnh CENTRAL_ONLY_ENDPOINTS list khi execute — verify qua grep router definition trước update. Đặc biệt `/api/sync/stats` đã verify trong sync.py line 39 — không cần đổi.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_factor_hub_scoped.py` exists
    - `grep -c "HUB_SCOPED_ENDPOINTS\s*=" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (12 endpoint matrix declared)
    - `grep -c "CENTRAL_ONLY_ENDPOINTS\s*=" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (8 endpoint matrix declared)
    - `grep -cE '\("POST",[ ]*"/api/auth/login"\)' Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -cE '\("GET",[ ]*"/api/rag-config"\)' Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -cE '\("GET",[ ]*"/api/sync/stats"\)' Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (BLK-01 fix — replace /run với /stats)
    - `grep -c '"/api/sync/run"' Hub_All/api/tests/integration/test_factor_hub_scoped.py` == 0 (BLK-01 fix — KHÔNG còn reference legacy endpoint)
    - `grep -c "def test_central_mounts_all_endpoints" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c "def test_hub_strips_central_only" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c "def test_hub_mounts_hub_scoped" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c "def test_404_envelope_shape_hub_strip" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c "def test_404_envelope_unknown_route_central" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c "def test_hub_yte_dsn_mismatch_raises" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c '@pytest.fixture(autouse=True)' Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (WRN-03 fix — autouse cleanup CỤC BỘ)
    - `grep -c "_phase2_clear_settings_cache_after" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1
    - `grep -c '@pytest.mark.parametrize("hub_name", \["yte", "duoc", "hcns"\]\)' Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 2 (parametrize 2 test strip + mount)
    - `grep -c "pytestmark.*critical.*integration\|@pytest.mark.critical" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (HARD-03 marker)
    - `grep -c "DSN mismatch hub_name" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (Phase 1 regression check)
    - `grep -c "TestClient" Hub_All/api/tests/integration/test_factor_hub_scoped.py` ≥ 1 (fastapi.testclient)
    - `cd Hub_All/api && uv run ruff check tests/integration/test_factor_hub_scoped.py` exit 0
    - `cd Hub_All/api && uv run mypy tests/integration/test_factor_hub_scoped.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS toàn bộ test trong file (1 central full + 3 parametrize strip + 3 parametrize mount + 1 envelope yte + 1 envelope central + 1 dsn mismatch — pytest đếm parametrize riêng → tổng 10 test result expected; tối thiểu hết test trong file đều PASS)
    - Plan 02-01 Task 2 unit test KHÔNG regress: `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py -v` PASS toàn bộ
    - Phase 1 integration test KHÔNG regress: `cd Hub_All/api && uv run pytest tests/integration/test_hub_isolation_db_level.py -v -m "critical and integration"` PASS 5/5 (cần Docker)
  </acceptance_criteria>
  <done>
test_factor_hub_scoped.py PASS toàn bộ test trong file (central full + hub strip × 3 + hub mount × 3 + envelope verify × 2 + dsn mismatch). FACTOR-02 strip semantic verify ở integration level. FACTOR-03 mount semantic verify. Envelope shape M2 ErrorHandlerMiddleware wrap. Phase 1 regression KHÔNG break. Autouse cleanup CỤC BỘ trong file (WRN-03 fix) — KHÔNG affect Phase 1/M2 fixture chain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test client → FastAPI app | TestClient in-process — KHÔNG có network/auth boundary thực tế; verify mount/strip semantic |
| Fixture monkeypatch.setenv → Settings singleton | get_settings.cache_clear() force re-parse env mỗi test — tránh state leak giữa parametrize |
| Test fixture hub_app_factory → create_app() | KHÔNG cần Postgres/Redis thật (lifespan fail-soft + COCOINDEX_SKIP_SETUP) — test isolation đơn vị unit-integration |
| Test → ErrorHandlerMiddleware envelope shape | Verify FACTOR-02 strip → 404 envelope shape D-V3-Phase2-E (KHÔNG 403 leak endpoint exist) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | Test_hub_strips_central_only fail → endpoint matrix CENTRAL_ONLY_ENDPOINTS sai → hub con thực tế leak endpoint admin | mitigate | Endpoint matrix HUB_SCOPED + CENTRAL_ONLY hardcoded literal trong test file — code review verify match D-V3-Phase2-D ở CONTEXT.md. `/api/sync/stats` verify từ sync.py source thực tế (BLK-01 fix). Test parametrize 3 hub → catch nếu router strip miss 1 hub. Phase 2 verifier (Plan 02-04) re-run test pre-closeout. |
| T-02-03-02 | Tampering | Test fixture hub_app_factory monkeypatch DSN trỏ DB thật → mutate test DB | accept | DSN format `postgresql+asyncpg://u:p@localhost:5432/medinet_hub_yte` — credentials u:p giả; nếu host thật có DB tên này + chấp nhận u:p → security issue local dev (out of scope Phase 2). Production CI runner KHÔNG có Postgres mở port localhost. |
| T-02-03-03 | DoS | TestClient(app) lifespan boot 12 lần × parametrize × test = ~24 boot → asyncpg.create_pool fail timeout chậm test suite | mitigate | Phase 1 lifespan asyncpg pool fail-soft (log warning, KHÔNG raise) → boot ms-fast khi không có Postgres. CocoIndex skip qua env. Test suite total estimate <30s với 12 test (TestClient boot ~1-2s/test × 12 = 12-24s). |
| T-02-03-04 | Repudiation | Test PASS nhưng route_paths không đầy đủ (vd /api/auth/login alias /api/auth/sign-in nội bộ) → false positive | mitigate | Test verify HTTP-level (request → response status code) thay vì internal route inspection. ErrorHandlerMiddleware wrap 404 envelope là contract observable. Alias endpoint thực sự nếu có sẽ vẫn pass nếu mount đúng (status != 404). |
| T-02-03-05 | Spoofing | Test hub_app_factory thiếu monkeypatch HUB_NAME → singleton state leak giữa parametrize → test PASS sai (test yte chạy với env central cached) | mitigate | hub_app_factory factory ALWAYS gọi `get_settings.cache_clear()` TRƯỚC `create_app()`. monkeypatch.setenv tự undo cuối test (pytest fixture lifecycle). `_phase2_clear_settings_cache_after` autouse fixture CỤC BỘ (WRN-03 fix) extra safety — chỉ áp dụng test Phase 2 trong file, KHÔNG nhiễu Phase 1/M2 chain. |
</threat_model>

<verification>
**Plan 02-03 verification chain:**

1. **File integrity (grep)**:
   - `api/tests/integration/conftest.py` extend (append-only) với hub_app_factory fixture
   - `api/tests/integration/test_factor_hub_scoped.py` mới tạo với endpoint matrix + 6 test định nghĩa + autouse cleanup CỤC BỘ

2. **Lint + type clean**:
   - `cd Hub_All/api && uv run ruff check tests/integration/conftest.py tests/integration/test_factor_hub_scoped.py` exit 0
   - `cd Hub_All/api && uv run mypy tests/integration/conftest.py tests/integration/test_factor_hub_scoped.py` exit 0

3. **Integration test PASS**:
   - `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS toàn bộ test trong file (1 central full + 3 hub strip parametrize + 3 hub mount parametrize + 1 envelope yte + 1 envelope central + 1 dsn mismatch)

4. **Regression KHÔNG break**:
   - Plan 02-01 Task 2 unit test PASS: `tests/unit/test_main_factory.py`
   - Phase 1 integration test PASS: `tests/integration/test_hub_isolation_db_level.py` (cần Docker — chạy nếu Docker available)
   - M2 fixture cũ (`app_with_auth`, `auth_client`, `admin_token`...) KHÔNG bị đụng (autouse cleanup KHÔNG ở conftest.py — WRN-03 fix)

5. **FACTOR-02 / FACTOR-03 satisfied at integration level**:
   - Hub con (yte/duoc/hcns) trả 404 envelope cho 8 endpoint central-only — verify ở 3 hub × 8 endpoint = 24 assertion
   - Hub con mount 12 endpoint hub-scoped — verify ở 3 hub × 12 endpoint = 36 assertion
   - Central giữ NGUYÊN 20 endpoint — 20 assertion
</verification>

<success_criteria>
- conftest.py extend với hub_app_factory fixture (Task 1) — KHÔNG có autouse cleanup ở conftest.py (WRN-03 fix)
- test_factor_hub_scoped.py PASS toàn bộ test trong file (Task 2)
- 8 endpoint central-only strip ở 3 hub con (24 assertion FACTOR-02 PASS) — bao gồm `/api/sync/stats` (BLK-01 fix — KHÔNG dùng `/api/sync/run` không tồn tại)
- 12 endpoint hub-scoped mount ở 3 hub con (36 assertion FACTOR-03 PASS)
- Central giữ NGUYÊN 20 endpoint (M2 backward-compat)
- 404 envelope shape verify (D-V3-Phase2-E satisfied)
- Phase 1 _enforce_hub_dsn_match regression check PASS (E-V3-3 carry forward)
- FACTOR-02 + FACTOR-03 satisfied at integration level (FACTOR-01 đã satisfy ở Plan 02-01 + 02-02 layer)
</success_criteria>

<output>
Tạo `.planning/phases/02-hub-con-codebase-factor/02-03-SUMMARY.md`:
- Endpoint matrix snapshot (12 hub-scoped + 8 central-only — D-V3-Phase2-D), note `/api/sync/stats` thay cho legacy `/run` (BLK-01 fix)
- Fixture hub_app_factory interface (callable hub_name → FastAPI app) — append-only ở conftest.py, autouse CỤC BỘ ở test file (WRN-03 fix)
- Test coverage breakdown: central full + 3 hub strip + 3 hub mount + envelope × 2 + dsn mismatch = 6 definition / parametrize-expanded
- 404 envelope shape verify (D-V3-Phase2-E satisfied)
- FACTOR-02/03 đóng integration level; Plan 02-04 sẽ smoke compose-level + closeout CLAUDE.md/STATE.md
</output>
</content>
