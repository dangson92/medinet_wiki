---
phase: 02-hub-con-codebase-factor
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - Hub_All/CLAUDE.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - FACTOR-01
  - FACTOR-02
  - FACTOR-03
tags:
  - closeout
  - claude-md-update
  - state-md-update
  - requirements-md-note
  - smoke-compose
  - phase-2-done
  - d-v3-phase2-i

must_haves:
  truths:
    - "Hub_All/CLAUDE.md section 2 update — Phase 2 status DONE + 4 service compose pattern + endpoint matrix reference (D-V3-Phase2-I)"
    - ".planning/STATE.md update — position Phase 2 DONE, completed_phases=2, completed_plans=9, percent ≈ 28%, next action Phase 3 Auth SSO"
    - "[BLOCKING smoke] make local docker compose up 2 service (central + yte) → curl matrix verify 12 hub-scoped mount + 8 central-only strip ở hub yte (real network, post-Plan 02-02 docker-compose 4 service)"
    - "Final regression: tests/unit/test_main_factory.py + tests/integration/test_factor_hub_scoped.py + tests/integration/test_hub_isolation_db_level.py (Phase 1 carry forward) PASS toàn bộ"
    - "Phase 2 success criteria 1-3 (ROADMAP) verified — 4 service spawn parallel; hub con /api/rag-config 404 + /api/health 200; smoke 12 endpoint hub-scoped"
    - ".planning/REQUIREMENTS.md FACTOR-03 + ROADMAP Phase 2 row có note giải thích chênh lệch '10 collective / 12 specific endpoints' (WRN-05 fix — tránh future confusion)"
  artifacts:
    - path: "Hub_All/CLAUDE.md"
      provides: "Project instruction update — section 2 Phase 2 DONE + section 6 pivot v3.0 progress update"
      contains: "Phase 2.*DONE\\|HUB_NAME.*central.*yte.*duoc.*hcns\\|python-api-(central|yte|duoc|hcns)"
    - path: ".planning/STATE.md"
      provides: "Project state update — Phase 2 DONE, completed_plans=9, next action Phase 3"
      contains: "Phase 2.*DONE\\|completed_phases: 2\\|completed_plans: 9\\|/gsd-discuss-phase 3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Note clarify FACTOR-03 '12 specific endpoint' implementation vs '10 collective' roadmap label"
      contains: "10 collective\\|12 specific\\|FACTOR-03"
  key_links:
    - from: "Hub_All/CLAUDE.md section 2"
      to: ".planning/STATE.md position"
      via: "Mirror status Phase 2 DONE + plan count + next action"
      pattern: "Phase 2"
    - from: ".planning/STATE.md"
      to: ".planning/phases/02-hub-con-codebase-factor/02-{01..04}-SUMMARY.md"
      via: "Phase 2 Results Summary table reference 4 plan + commit count + test count"
      pattern: "02-0[1-4]-SUMMARY"
    - from: "smoke verify"
      to: "docker-compose.yml Plan 02-02 + api/app/main.py Plan 02-01"
      via: "docker compose up postgres redis python-api-central python-api-yte → curl matrix 8180 vs 8181"
      pattern: "python-api-central\\|python-api-yte"
---

<objective>
Đóng Phase 2 với 4 deliverable:

1. **Update `Hub_All/CLAUDE.md`** section 2 (Milestone hiện tại) — ghi nhận Phase 2 DONE; thêm subsection v3.0 Phase 2 pattern (4 service compose + endpoint matrix + HUB_NAME) — D-V3-Phase2-I.
2. **Update `.planning/STATE.md`** — position Phase 2 DONE, completed_phases=2, completed_plans=9, percent ≈ 28%, next action `/gsd-discuss-phase 3`.
3. **Update `.planning/REQUIREMENTS.md` + ROADMAP Phase 2 row** (WRN-05 fix) — thêm note clarify "10 collective endpoint group / 12 specific endpoints" tránh future confusion về chênh lệch giữa ROADMAP label và implementation count.
4. **[BLOCKING smoke compose] make docker compose up 2 service (central + yte) + curl matrix verify endpoint mount/strip semantic ở runtime thật** — Plan 02-04 chạy SAU khi 02-01/02/03 ship file modify; verify rằng integration test in-process khớp với compose-level runtime behavior.

Plan 02-04 là Wave 3 closeout — chạy sau Plan 02-01 (Wave 1) + Plan 02-02 + 02-03 (Wave 2). KHÔNG modify code production — chỉ update docs + smoke verify.

Purpose: D-V3-Phase2-I closeout + verify FACTOR-01..03 satisfied ở runtime thật (compose-level) trước Phase 3.

Output: 3 file modify (`Hub_All/CLAUDE.md` + `.planning/STATE.md` + `.planning/REQUIREMENTS.md`) + 1 smoke verify checkpoint (runtime — không file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-01-PLAN.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-02-PLAN.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-03-PLAN.md
@Hub_All/docker-compose.yml

<interfaces>
<!-- Hub_All/CLAUDE.md section 2 hiện tại liệt kê M2 status. Phase 2 closeout update tham chiếu Phase 1 pattern (đã ship 2026-05-21). -->

`Hub_All/CLAUDE.md` section 2 hiện tại (line 21-49) liệt kê M2 v2.0 status + bảng 10 phase. Đoạn này KHÔNG đổi (M2 archive). Plan 02-04 thêm subsection v3.0 progress phía SAU section 6 (M2 closeout — pivot v3.0).

Section 6 hiện tại (line 111-145) — Plan 02-04 sửa: thêm note Phase 1 DONE + Phase 2 DONE.

`.planning/STATE.md` frontmatter (line 1-13) hiện tại:
```yaml
status: Phase 1 DONE 2026-05-21 ✅. 5 plans / 22 commits / 166 unit tests + 5 integration test PASS...
progress:
  total_phases: 7
  completed_phases: 1
  total_plans: 5
  completed_plans: 5
  percent: 14
```

→ Plan 02-04 update:
```yaml
status: Phase 2 DONE <date>. 4 plans / N commits / 8+ unit tests + 12+ integration test PASS. 4 service compose (central + yte + duoc + hcns)...
progress:
  total_phases: 7
  completed_phases: 2
  total_plans: 7 + N   (5 Phase 1 + 4 Phase 2 = 9)
  completed_plans: 9
  percent: 28
```

Phase 2 Results Summary table sẽ thêm vào STATE.md sau "Phase 1 Results Summary" — mirror pattern Phase 1.

`.planning/REQUIREMENTS.md` FACTOR-03 row hiện tại (WRN-05): label "10 endpoint hub-scoped" nhưng implementation thực tế là 12 endpoint (4 auth + 2 profile + 3 documents + search + ask + usage = 12). CONTEXT.md D-V3-Phase2-D đã giải thích chi tiết, nhưng REQUIREMENTS.md + ROADMAP chưa có note. Plan 02-04 Task 4 (mới) thêm note clarify.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING smoke compose] make docker compose up 2 service (central + yte) + curl matrix verify endpoint mount/strip ở runtime</name>
  <what-built>
Plan 02-01 + 02-02 + 02-03 đã ship: create_app() conditional router mount + docker-compose.yml 4 service dedicated + integration test endpoint matrix PASS in-process. Task 1 verify integration test khớp với runtime compose-level: chạy docker compose up 2 service (postgres + redis + python-api-central + python-api-yte) + curl matrix 12 hub-scoped + 8 central-only ở port 8180 (central) và 8181 (yte).

Đây là BLOCKING checkpoint — KHÔNG advance Phase 2 closeout nếu smoke fail. Smoke fail = real issue runtime KHÔNG visible qua in-process test (vd cocoindex Environment singleton conflict, port binding fail, container restart loop).
  </what-built>
  <how-to-verify>
Operator (user) chạy chuỗi command sau từ `Hub_All/` cwd. Mọi step expect exit 0; mọi curl expect đúng status code envelope shape.

**Step 0 — Đảm bảo Docker Desktop chạy + postgres healthy (Phase 1 schema push DONE):**

```bash
cd Hub_All
docker compose up -d postgres redis
sleep 10
docker compose ps postgres redis
# Expected: cả 2 service "Up (healthy)" status
```

**Step 1 — Build image API + up 2 service (central + yte):**

```bash
docker compose build python-api-central python-api-yte
docker compose up -d python-api-central python-api-yte
sleep 20  # wait lifespan boot (cocoindex setup_blocking + initial backfill)
docker compose ps python-api-central python-api-yte
# Expected: cả 2 container "Up" status (healthcheck Phase 4 v2.0 baseline)
```

**Step 2 — Verify container log hub yte có dòng `central_only_routers_skipped`:**

```bash
docker logs medinet-api-yte 2>&1 | grep central_only_routers_skipped
# Expected output: chứa "central_only_routers_skipped: hub_name=yte" hoặc tương tự
```

**Step 3 — Curl matrix HEALTH endpoint (universal — cả 2 hub):**

```bash
# Central
curl -sS http://localhost:8180/api/health | head -200
# Expected: 200 envelope {"success":true,"data":{"status":"ok"},"error":null,"meta":...}

# Hub yte
curl -sS http://localhost:8181/api/health | head -200
# Expected: 200 envelope tương tự
```

**Step 4 — Curl matrix CENTRAL-ONLY endpoint trên CENTRAL (port 8180):**

```bash
for path in /api/rag-config /api/hubs /api/users /api/api-keys /api/audit-logs /api/system-settings; do
    code=$(curl -sS -o /dev/null -w "%{http_code}" -H "Authorization: Bearer fake" http://localhost:8180$path)
    echo "Central $path → $code"
done
# Expected: mọi endpoint trả KHÁC 404 (200 / 401 / 403 / 422 đều OK — chỉ cần MOUNT)
```

**Step 5 — Curl matrix CENTRAL-ONLY endpoint trên HUB YTE (port 8181) — PHẢI 404 envelope:**

```bash
for path in /api/rag-config /api/hubs /api/users /api/api-keys /api/audit-logs /api/system-settings; do
    code=$(curl -sS -o /dev/null -w "%{http_code}" http://localhost:8181$path)
    body=$(curl -sS http://localhost:8181$path)
    echo "Hub yte $path → $code | body: ${body:0:120}"
done
# Expected: mọi endpoint trả 404; body có shape {"success":false,"data":null,"error":{"code":...,"message":...},"meta":...}
```

**Step 6 — Curl matrix HUB-SCOPED endpoint trên HUB YTE (port 8181) — PHẢI != 404:**

```bash
for path in /api/auth/me /api/profile /api/documents /api/usage; do
    code=$(curl -sS -o /dev/null -w "%{http_code}" http://localhost:8181$path)
    echo "Hub yte $path → $code"
done
# Expected: mọi endpoint trả KHÁC 404 (401 unauthorized / 422 validation OK — chỉ cần MOUNT đúng FACTOR-03)

# POST endpoint cần body
for path in /api/auth/login /api/search /api/ask; do
    code=$(curl -sS -o /dev/null -w "%{http_code}" -X POST -H "Content-Type: application/json" -d '{}' http://localhost:8181$path)
    echo "Hub yte POST $path → $code"
done
# Expected: mọi endpoint trả KHÁC 404
```

**Step 7 — Verify hub yte KHÔNG access được data hub khác (E-V3-3 carry forward — DB-level isolation Phase 1):**

```bash
# Phase 1 _enforce_hub_dsn_match: medinet-api-yte container ENV HUB_NAME=yte + DATABASE_URL trỏ medinet_hub_yte
# Verify qua docker exec inspect env:
docker exec medinet-api-yte env | grep -E "^HUB_NAME=|^DATABASE_URL=" 
# Expected:
#   HUB_NAME=yte
#   DATABASE_URL=postgresql+asyncpg://medinet:...@postgres:5432/medinet_hub_yte
```

**Step 8 — Cleanup (optional — nếu user muốn keep 2 service chạy thì skip):**

```bash
docker compose down  # KHÔNG -v (giữ volume — preserve M2 central data)
```

**Resume signal — user reply:**
- `"approved"` nếu mọi step PASS expected output → Plan 02-04 continue Task 2 (CLAUDE.md update).
- `"failed: <step>"` nếu step nào fail → STOP Phase 2 closeout, report root cause cho user, có thể cần re-execute Plan 02-01/02/03 fix.
- `"skip smoke"` nếu Docker Desktop không available local → accept risk; Plan 02-04 continue Task 2 + 3 + 4 dựa trên integration test in-process Plan 02-03 đã PASS.
  </how-to-verify>
  <resume-signal>
Type "approved" nếu smoke compose 7 step PASS expected output (4 service spawn parallel central + yte; curl matrix 12 hub-scoped mount + 8 central-only strip + envelope shape OK). HOẶC "skip smoke" nếu Docker Desktop không available local. HOẶC "failed: <step>" + paste output nếu step nào fail.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Hub_All/CLAUDE.md section 2 + section 6 — Phase 2 DONE + 4 service compose pattern + endpoint matrix reference</name>
  <files>Hub_All/CLAUDE.md</files>
  <read_first>
    - Hub_All/CLAUDE.md (file đang sửa — đặc biệt section 2 line 21-49 và section 6 line 111-145)
    - Hub_All/.planning/STATE.md (Phase 1 DONE summary làm template — line 5-13 frontmatter + Phase 1 Results Summary)
    - Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md (D-V3-Phase2-I — closeout pattern)
  </read_first>
  <action>
**Step 1: Update section 6 (M2 closeout — Pivot tới v3.0 Multi-Hub Split)** — thêm subsection "v3.0 progress" sau dòng kết section.

Mở `Hub_All/CLAUDE.md`. Tìm section 6 (line ~111 — bắt đầu `## 6. M2 closeout — Pivot tới v3.0 Multi-Hub Split`). Append (KHÔNG xoá nội dung cũ M2 closeout pivot description) subsection mới ở CUỐI section 6, NGAY TRƯỚC `---` separator + dòng cuối (`*Cập nhật: 2026-05-21 ...*`):

```markdown
### v3.0 progress (cập nhật khi mỗi Phase ship)

| Phase | Status | Plan count | Date | REQ-ID |
|-------|--------|------------|------|--------|
| 1 — Multi-DB Topology + Per-hub Alembic | ✅ DONE | 5 plan | 2026-05-21 | TOPO-01..04 (4) |
| 2 — Hub-con Codebase Factor | ✅ DONE | 4 plan | <DATE> | FACTOR-01..03 (3) |
| 3 — Auth SSO + hub_ids trong JWT | 📋 Next | — | — | SSO-01..04 |
| 4 — Cross-hub Data Sync | 📋 | — | — | SYNC-01..05 |
| 5 — Reverse Proxy + Frontend Subpath | 📋 | — | — | PROXY-01..04 |
| 6 — System Settings Sync | 📋 | — | — | SETTINGS-01..04 |
| 7 — Migration + Smoke E2E | 📋 | — | — | MIGRATE-01..05 |

**🚦 v3.0-a EXIT GATE** giữa Phase 3 và Phase 4 — demo 1 hub con (yte) + central + JWT SSO + golden path PASS → user accept là điều kiện tiếp tục v3.0-b (Phase 4-7).

### Phase 2 pattern (FACTOR-01..03 — 2026-<date>)

1 codebase deploy được nhiều lần với env `HUB_NAME=<central|yte|duoc|hcns>`:

- **`api/app/main.py::create_app()`** — factory no-arg đọc `settings.hub_name`, conditional mount:
  - **7 router universal** (mount mọi process): `auth_router`, `documents_router`, `profile_router`, `search_router`, `ask_router`, `usage_router`, `ai_chat_router`.
  - **9 router central-only** (chỉ mount khi `settings.hub_name == "central"`): `hubs_router`, `users_router`, `api_keys_router`, `audit_logs_router`, `rag_config_router`, `system_settings_router`, `sync_router`, `mcp_oauth_router`, `mcp_oauth_internal_router`.
- **`docker-compose.yml`** — 4 service dedicated FastAPI:
  - `python-api-central` (HUB_NAME=central, DB `medinet_central`, port host 8180 — M2 backward-compat).
  - `python-api-yte` (HUB_NAME=yte, DB `medinet_hub_yte`, port host 8181).
  - `python-api-duoc` (HUB_NAME=duoc, DB `medinet_hub_duoc`, port host 8182).
  - `python-api-hcns` (HUB_NAME=hcns, DB `medinet_hub_hcns`, port host 8183).
  - YAML anchor `x-api-template: &api-template` dedupe shared config (build / env_file / volumes keys ro / depends_on / network). Mỗi service inherit qua `<<: *api-template`.
  - Cocoindex LMDB volume per-hub (`medinet_cocoindex_<hub>`) — 1.0.3 Environment singleton isolation.
  - `mcp_service.MCP_API_BASE_URL` re-point `http://python-api-central:8080` (KHÔNG fan-out — LOCKED D-V3-02).
- **Endpoint contract:**
  - Hub con expose **12 endpoint hub-scoped specific** (FACTOR-03 — implementation count; ROADMAP/REQUIREMENTS label "10 collective endpoint group" gộp `/api/auth/*` + `/api/profile` + `/api/documents` thành nhóm): `/api/auth/{login,refresh,logout,me}`, `/api/profile` (GET/PATCH), `/api/documents` (POST/GET/DELETE), `/api/search`, `/api/ask`, `/api/usage`.
  - Hub con STRIP **8 endpoint central-only** (FACTOR-02) → 404 envelope `{success:false, data:null, error:{code, message}, meta:null}` (KHÔNG 403 — endpoint không exist mới đúng strip semantic).
- **Cross-hub alias defer:** `/api/ask/cross-hub` + `/api/search/answer` vẫn mount universal ở Phase 2 (router universal mount nguyên). Phase 4 SYNC-03 sẽ tách hoặc dùng dependency reject. Hub con runtime endpoint cross-hub trả 500/empty (DB không có data hub khác — KHÔNG leak vì E-V3-3 isolation Phase 1).
- **Phase 1 Settings._enforce_hub_dsn_match validator** carry forward — boot-time fail-fast nếu HUB_NAME ↔ DSN suffix mismatch (E-V3-3 enforce).

**Reference:**
- `.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md` — 9 D-V3-Phase2 decision LOCKED 2026-05-22.
- `.planning/phases/02-hub-con-codebase-factor/02-{01..04}-PLAN.md` — implementation chi tiết 4 plan.
- `.planning/phases/02-hub-con-codebase-factor/02-{01..04}-SUMMARY.md` — deliverable + commit + test count per plan.
```

**KHÔNG đổi nội dung section 1-5 + section 6 phần đầu (M2 closeout pivot description + 4 D-V3-01..04 LOCKED + GA-V3-A/B/C/D open question). Phase 3 sẽ update GA-V3-A status DONE.**

**Step 2: Verify changelog footer** — dòng cuối CLAUDE.md `*Cập nhật: 2026-05-21 ...*` cập nhật thành ngày Phase 2 closeout:

```markdown
*Cập nhật: <Phase 2 closeout date> (Phase 2 DONE — FACTOR-01..03 ship). Project: MEDWIKI. M2 v2.0 done; v3.0 Multi-Hub Split — Phase 1+2 DONE (9/~30 plan ≈ 28%), Next: `/gsd-discuss-phase 3` Auth SSO.*
```

**Lưu ý ngôn ngữ:** Toàn bộ subsection mới VIẾT TIẾNG VIỆT CÓ DẤU theo CLAUDE.md section 3. Chỉ giữ tiếng Anh cho: tên hàm/biến/path/REQ-ID/command. Bảng + bullet list dùng dấu tiếng Việt đầy đủ.
  </action>
  <verify>
    <automated>grep -c "Phase 2 — Hub-con Codebase Factor.*DONE\|FACTOR-01..03\|python-api-yte\|7 router universal\|9 router central-only" Hub_All/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v3.0 progress" Hub_All/CLAUDE.md` ≥ 1 (subsection header)
    - `grep -c "Phase 2 — Hub-con Codebase Factor.*DONE\|Phase 2 .* DONE" Hub_All/CLAUDE.md` ≥ 1 (Phase 2 status DONE)
    - `grep -c "FACTOR-01..03\|FACTOR-01, FACTOR-02, FACTOR-03" Hub_All/CLAUDE.md` ≥ 1 (REQ-ID traceability)
    - `grep -c "python-api-central\|python-api-yte\|python-api-duoc\|python-api-hcns" Hub_All/CLAUDE.md` ≥ 4 (4 service tên reference)
    - `grep -c "HUB_NAME=" Hub_All/CLAUDE.md` ≥ 1 (env var reference)
    - `grep -c "7 router universal\|9 router central-only" Hub_All/CLAUDE.md` ≥ 2 (router count reference)
    - `grep -c "12 endpoint hub-scoped" Hub_All/CLAUDE.md` ≥ 1 (FACTOR-03 contract)
    - `grep -c "8 endpoint central-only\|8 router central-only" Hub_All/CLAUDE.md` ≥ 1 (FACTOR-02 strip)
    - `grep -c "/gsd-discuss-phase 3" Hub_All/CLAUDE.md` ≥ 1 (next action pointer)
    - `grep -c "v3.0-a EXIT GATE" Hub_All/CLAUDE.md` ≥ 1 (gate reminder)
    - `grep -c "create_app()" Hub_All/CLAUDE.md` ≥ 1 (factory pattern reference)
    - `grep -c "x-api-template" Hub_All/CLAUDE.md` ≥ 1 (YAML anchor reference)
    - `grep -c "10 collective\|10 collective endpoint group" Hub_All/CLAUDE.md` ≥ 1 (WRN-05 traceability — clarify 10/12 chênh lệch)
    - `grep -c "## 1\|## 2\|## 3\|## 4\|## 5\|## 6" Hub_All/CLAUDE.md` == 6 (section heading KHÔNG bị đổi cấu trúc)
    - Verify CLAUDE.md vẫn parsing markdown đúng (KHÔNG broken table syntax)
  </acceptance_criteria>
  <done>
Hub_All/CLAUDE.md section 6 extend với subsection v3.0 progress (bảng 7 phase status) + Phase 2 pattern description (create_app() conditional + docker-compose 4 service + endpoint matrix); note clarify "10 collective / 12 specific endpoints" (WRN-05); changelog footer update date Phase 2 closeout; markdown structure preserve.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update .planning/STATE.md — Phase 2 DONE + Phase 2 Results Summary + next action /gsd-discuss-phase 3</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - Hub_All/.planning/STATE.md (file đang sửa — frontmatter line 1-13 + Phase 1 Results Summary làm template)
    - Hub_All/.planning/ROADMAP.md (Phase 3 description — next action prompt)
    - Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md (D-V3-Phase2 decision reference)
  </read_first>
  <action>
**Step 1: Update frontmatter (line 1-13).**

```yaml
---
gsd_state_version: 1.0
milestone: v3.0
milestone_name: Multi-Hub Split
status: Phase 2 DONE <DATE> ✅. 4 plans / <N> commits / 8+ unit tests + 12+ integration test PASS. create_app() factory conditional mount (7 universal + 9 central-only); docker-compose 4 service dedicated (central/yte/duoc/hcns) port 8180-8183 + cocoindex LMDB volume per-hub; endpoint matrix 12 hub-scoped mount + 8 central-only strip envelope shape. Phase 1+2 = 9/~30 plan ≈ 28%. Next phase 3 — Auth SSO + hub_ids JWT (GA-V3-A chốt).
last_updated: "<ISO-8601 timestamp>"
progress:
  total_phases: 7
  completed_phases: 2
  total_plans: 9
  completed_plans: 9
  percent: 28
---
```

Note: `total_plans` field gốc M2 reset = 5 (Phase 1) → Plan 02-04 update thành 9 (Phase 1 5 + Phase 2 4). `completed_plans` cũng 9. Percent ≈ 28% (9/30 estimate × 100; ROADMAP ước tính ~30 plan tổng cho v3.0).

**Step 2: Update Section "Current Position".**

Mở STATE.md, tìm `## Current Position` (line ~22). Thay block:

```markdown
## Current Position

- **Phase:** 2 — Hub-con Codebase Factor ✅ **DONE <DATE>**
- **Plan:** 4/4 plans complete (02-01..02-04) ở `.planning/phases/02-hub-con-codebase-factor/`
- **Status:** Phase 2 closed — FACTOR-01..03 satisfied. `create_app()` factory conditional router mount (7 universal + 9 central-only). Docker-compose mở rộng 4 service dedicated với YAML anchor `x-api-template`. Integration test endpoint matrix verify 12 hub-scoped mount + 8 central-only strip với 404 envelope shape M2 ErrorHandlerMiddleware wrap. Phase 1 `_enforce_hub_dsn_match` validator carry forward (E-V3-3 không regress).
- **Last activity:** <DATE> — `/gsd-execute-phase 2` wave-based execution complete. Wave 1 (Plan 02-01 BLOCKING — factory refactor) + Wave 2 (Plan 02-02 docker-compose + Plan 02-03 integration test parallel file-disjoint) + Wave 3 (Plan 02-04 closeout + smoke compose). N commits total. 8+ unit test (test_main_factory.py 4 hub mode) + 12+ integration test (test_factor_hub_scoped.py endpoint matrix) PASS. Phase 1 regression carry forward PASS (test_hub_isolation_db_level.py 5/5).
```

**Step 3: Append Section "Phase 2 Results Summary"** ngay sau bảng "Phase 1 Results Summary".

```markdown
## Phase 2 Results Summary

| Plan | Wave | Objective | Commits | Tests |
|------|------|-----------|---------|-------|
| 02-01 | 1 | Refactor create_app() conditional router mount (7 universal + 9 central-only) + unit test boot 4 hub mode | <N> | unit 8+/8+ PASS (test_main_factory.py) |
| 02-02 | 2 | Docker-compose 4 service FastAPI dedicated với YAML anchor + cocoindex LMDB volume per-hub + port 8180-8183 + MCP re-point central | <N> | docker compose config --quiet PASS; 8 service render |
| 02-03 | 2 | Integration test endpoint matrix — 12 hub-scoped mount + 8 central-only strip + envelope shape verify | <N> | integration 12+/12+ PASS (test_factor_hub_scoped.py) |
| 02-04 | 3 | Closeout — CLAUDE.md + STATE.md + REQUIREMENTS.md note update + smoke compose 2 service (central + yte) curl matrix | <N> | smoke compose verify (Task 1 BLOCKING checkpoint) |

**Phase 2 deliverable summary:**
- 4 service compose dedicated (central + yte + duoc + hcns) deploy được song song mỗi container kết nối DB riêng.
- Hub con (yte/duoc/hcns) strip 9 router central-only → 404 envelope shape (FACTOR-02).
- Hub con mount 12 endpoint hub-scoped specific (FACTOR-03 contract — ROADMAP label gộp thành "10 collective endpoint group"; xem note REQUIREMENTS.md FACTOR-03).
- Phase 1 DSN validator + E-V3-3 isolation carry forward — KHÔNG regress.

**v3.0-a progress: Phase 1+2 DONE (2/3 phase v3.0-a). Phase 3 Auth SSO sẽ trigger v3.0-a EXIT GATE.**
```

**Step 4: Update Section "Next Action".**

```markdown
## Next Action

1. **(Recommended) `/gsd-discuss-phase 3`** — Auth SSO + hub_ids trong JWT (GA-V3-A chốt). Gray areas: JWKS endpoint vs shared keypair vs cookie domain `.medinet.vn`; JWKS cache fallback fail-loud vs static keypair embedded; refresh token rotation contract.
2. (Optional) `/gsd-code-review 2` — advisory code review trên N commits Phase 2 (workflow.code_review gate).
3. (Optional) `/gsd-verify-work 2` — manual UAT 3 SC nếu user muốn extra verify ngoài automated test (compose-level smoke checkpoint Plan 02-04 đã cover căn bản).
```

**Step 5: KHÔNG đổi.**
- Section "Accumulated Context (carry forward từ v2.0)" — vẫn giữ v2.0 + v3.0 LOCKED decision history.
- Section "v3.0 open gray areas" — Phase 3 sẽ chốt GA-V3-A khi `/gsd-discuss-phase 3` done.
- Section "Carry-forward risks từ v2.0" + "v3.0 new risks" — KHÔNG đổi.
- Section "Project Reference" footer.

**Lưu ý:**
- `<DATE>` placeholder điền theo ngày thực tế khi execute Plan 02-04 (vd `2026-05-22`).
- `<N>` commits placeholder điền theo `git rev-list --count main -- .planning/phases/02-hub-con-codebase-factor/` hoặc estimate qua commit log Phase 2.
- `<ISO-8601 timestamp>` cho field `last_updated` dùng `date -u +"%Y-%m-%dT%H:%M:%S.000Z"`.
- Tiếng Việt có dấu xuyên suốt section mới.
  </action>
  <verify>
    <automated>grep -c "Phase 2 DONE\|completed_phases: 2\|completed_plans: 9\|percent: 28\|/gsd-discuss-phase 3\|Phase 2 Results Summary" Hub_All/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 2 DONE" Hub_All/.planning/STATE.md` ≥ 2 (frontmatter status + Current Position section)
    - `grep -c "completed_phases: 2" Hub_All/.planning/STATE.md` ≥ 1 (frontmatter)
    - `grep -c "completed_plans: 9" Hub_All/.planning/STATE.md` ≥ 1 (frontmatter)
    - `grep -c "total_plans: 9" Hub_All/.planning/STATE.md` ≥ 1 (frontmatter cập nhật Phase 1+2 cộng dồn)
    - `grep -c "percent: 28" Hub_All/.planning/STATE.md` ≥ 1 (frontmatter)
    - `grep -c "Phase 2 Results Summary" Hub_All/.planning/STATE.md` ≥ 1 (section mới)
    - `grep -cE "02-0[1-4] \| [123]" Hub_All/.planning/STATE.md` ≥ 4 (4 plan rows trong table Phase 2 Results)
    - `grep -c "/gsd-discuss-phase 3" Hub_All/.planning/STATE.md` ≥ 1 (next action pointer)
    - `grep -c "Hub-con Codebase Factor" Hub_All/.planning/STATE.md` ≥ 2 (Phase 2 name reference frontmatter + Current Position)
    - `grep -c "FACTOR-01..03\|FACTOR-01, FACTOR-02, FACTOR-03" Hub_All/.planning/STATE.md` ≥ 1
    - `grep -c "Phase 1 Results Summary" Hub_All/.planning/STATE.md` == 1 (KHÔNG xoá section Phase 1)
    - `grep -c "v3.0-a EXIT GATE" Hub_All/.planning/STATE.md` ≥ 1 (gate reminder Phase 3 trigger)
    - `grep -c "GA-V3-A" Hub_All/.planning/STATE.md` ≥ 1 (Phase 3 gray area reference)
    - `grep -c "10 collective endpoint group" Hub_All/.planning/STATE.md` ≥ 1 (WRN-05 traceability)
    - Verify markdown table syntax đúng + YAML frontmatter parse đúng (no broken delimiters)
  </acceptance_criteria>
  <done>
.planning/STATE.md frontmatter cập nhật progress (Phase 2 DONE, completed_plans=9, percent=28%, last_updated = now); Section Current Position cập nhật phase 2 closed; Phase 2 Results Summary table mới append với 4 plan + wave; Next Action pointer Phase 3 + GA-V3-A reference; markdown + YAML structure preserve; tiếng Việt có dấu xuyên suốt; note traceability 10/12 endpoint (WRN-05).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update .planning/REQUIREMENTS.md FACTOR-03 + ROADMAP Phase 2 row — note clarify "10 collective endpoint group / 12 specific endpoints" (WRN-05 fix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - Hub_All/.planning/REQUIREMENTS.md (file đang sửa — FACTOR-03 row hoặc bảng v3.0 requirements)
    - Hub_All/.planning/ROADMAP.md (Phase 2 row — verify wording hiện tại "10 endpoint" có thật sự xuất hiện hay không)
    - Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md (D-V3-Phase2-D matrix explain — 12 specific endpoint)
  </read_first>
  <action>
**Step 1: Đọc REQUIREMENTS.md** tìm row FACTOR-03 (hoặc table v3.0 requirements). Identify wording hiện tại có "10 endpoint" hay "10 hub-scoped endpoint" — đây là số ROADMAP label dùng từ initial seeding `/gsd-new-milestone v3.0` (2026-05-21).

**Step 2: Thêm note clarify ngay sau FACTOR-03 description** trong REQUIREMENTS.md. Format:

```markdown
**FACTOR-03 — Hub-scoped endpoint contract**

[... description hiện tại nếu có ...]

> **NOTE (Phase 2 closeout 2026-05-<date>):** Số "10 endpoint hub-scoped" trong label ROADMAP/REQUIREMENTS gốc là cách đếm **collective endpoint group** (gộp `/api/auth/*` thành 1 group, `/api/documents` GET/POST/DELETE thành 1 group, etc.). Implementation thực tế tính theo HTTP method × path = **12 specific endpoints**:
>
> 1. `POST /api/auth/login`
> 2. `POST /api/auth/refresh`
> 3. `POST /api/auth/logout`
> 4. `GET  /api/auth/me`
> 5. `GET  /api/profile`
> 6. `PATCH /api/profile`
> 7. `POST /api/documents`
> 8. `GET  /api/documents`
> 9. `DELETE /api/documents/{id}`
> 10. `POST /api/search`
> 11. `POST /api/ask`
> 12. `GET  /api/usage`
>
> Xem `.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md` D-V3-Phase2-D matrix để hiểu chi tiết breakdown. Integration test `tests/integration/test_factor_hub_scoped.py` verify đúng 12 endpoint specific.
```

**Step 3: Nếu ROADMAP.md row Phase 2 cũng wording "10 endpoint"** — cập nhật một dòng note nhỏ hoặc cập nhật wording thành "10 endpoint group / 12 specific HTTP method endpoints (xem REQUIREMENTS.md FACTOR-03 note)".

Lưu ý: KHÔNG sửa label gốc "10" thành "12" trong ROADMAP để giữ traceability với commit gốc v3.0 milestone seed. Chỉ thêm note clarify.

**Step 4: Verify markdown structure preserve.**
- Bảng requirements (nếu có) syntax đúng.
- Block quote `>` cho note phải đúng cú pháp markdown.

**Lưu ý ngôn ngữ:** Note viết tiếng Việt có dấu (kèm code identifier path tiếng Anh).

**Reject patterns:**
- KHÔNG sửa số "10" trong label gốc REQUIREMENTS.md FACTOR-03 thành "12" — giữ traceability commit gốc.
- KHÔNG xoá / thay description gốc FACTOR-03 — chỉ APPEND note clarify.
- KHÔNG động đến FACTOR-01 / FACTOR-02 rows — WRN-05 chỉ đặc biệt với FACTOR-03 (mâu thuẫn count label vs implementation).
  </action>
  <verify>
    <automated>grep -c "10 collective\|12 specific" Hub_All/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "10 collective endpoint group\|10 collective" Hub_All/.planning/REQUIREMENTS.md` ≥ 1 (WRN-05 fix — note traceability)
    - `grep -c "12 specific endpoint\|12 specific HTTP" Hub_All/.planning/REQUIREMENTS.md` ≥ 1 (clarify implementation count)
    - `grep -c "FACTOR-03" Hub_All/.planning/REQUIREMENTS.md` ≥ 1 (REQ-ID không xoá)
    - `grep -c "D-V3-Phase2-D" Hub_All/.planning/REQUIREMENTS.md` ≥ 1 (reference CONTEXT.md detail)
    - `grep -c "POST /api/auth/login\|GET  /api/usage" Hub_All/.planning/REQUIREMENTS.md` ≥ 1 (matrix listed trong note)
    - Verify markdown structure preserve (table/blockquote syntax đúng)
  </acceptance_criteria>
  <done>
.planning/REQUIREMENTS.md FACTOR-03 row có note clarify "10 collective endpoint group / 12 specific HTTP method endpoints" + matrix list 12 endpoint inline + reference D-V3-Phase2-D matrix. ROADMAP Phase 2 row có note nhỏ pointer tới REQUIREMENTS.md FACTOR-03 note. WRN-05 future confusion mitigated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (user) → Docker daemon | Smoke compose checkpoint chạy docker compose up — operator authorized (local Docker Desktop / CI runner) |
| Docs (CLAUDE.md + STATE.md + REQUIREMENTS.md) → future Claude session | Docs là project context auto-read session sau; PHẢI reflect Phase 2 status DONE chính xác để planner Phase 3 không re-do work; FACTOR-03 note clarify count tránh future planner confuse |
| Smoke compose curl → hub con port 8181 | Test direct port bypass Caddy (Phase 5 chưa lên) — acceptable dev/test stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | CLAUDE.md / STATE.md / REQUIREMENTS.md commit lưu DB credential hoặc hub identity nhạy cảm | accept | Plan 02-04 update CHỈ ghi tên hub (central/yte/duoc/hcns) + REQ-ID + plan number + endpoint path matrix — KHÔNG có credential / DSN thật / API key. Mọi tên hub đã public qua roadmap milestone v3.0 (2026-05-21). |
| T-02-04-02 | Repudiation | Smoke compose checkpoint operator skip "skip smoke" → không có evidence runtime PASS | mitigate | Plan 02-04 Task 1 resume signal cho phép "skip smoke" + STATE.md vẫn ghi nhận smoke chưa verify (recommend re-run khi Docker available). Integration test in-process Plan 02-03 đã cover semantic — KHÔNG ship-blocker nếu skip smoke 1 lần. |
| T-02-04-03 | Tampering | STATE.md frontmatter giả mạo (vd `completed_phases: 7` khi thực tế chỉ 2) → future planner skip Phase 3-7 | mitigate | Plan 02-04 acceptance criteria verify completed_phases=2, completed_plans=9, percent=28 — fail nếu khác. Future planner đọc REQ-ID coverage trong ROADMAP.md + Phase Results Summary table — cross-reference với plan file thực tế. Code review (Optional Task) catch nếu commit. |
| T-02-04-04 | DoS | Smoke compose up 4 service đồng thời → laptop dev resource exhaust (CPU/RAM) | accept | Task 1 chỉ up 2 service (postgres + redis + python-api-central + python-api-yte) — đủ smoke FACTOR-02/03. 4 service đồng thời defer Phase 7 migration smoke. |
| T-02-04-05 | Information Disclosure | docker logs medinet-api-yte print credential nếu structlog log raw env | mitigate | Phase 1 + M2 structlog config KHÔNG log raw env (HARD-01 Plan 10-01). Logger info `central_only_routers_skipped: hub_name=yte` chỉ chứa hub_name (public). Task 1 Step 2 verify grep target exact pattern → KHÔNG bị mislead. |
| T-02-04-06 | Elevation of Privilege | Smoke compose up dùng POSTGRES_PASSWORD default `medinet_dev_pwd` (M2 baseline) lên prod | accept | docker-compose.yml uses `${POSTGRES_PASSWORD:-medinet_dev_pwd}` fallback chỉ áp dụng dev/test. Production deploy ops set POSTGRES_PASSWORD qua secret manager / env file. Phase 2 KHÔNG cover production security — ROADMAP scope. |
| T-02-04-07 | Repudiation | Future planner Phase X re-implement "10 endpoint" theo label gốc REQUIREMENTS.md vì không thấy 12 specific implementation thực tế → mismatch contract | mitigate | Task 4 (WRN-05 fix) thêm note inline FACTOR-03 trong REQUIREMENTS.md + cross-reference D-V3-Phase2-D matrix + integration test list 12 endpoint. CLAUDE.md section 6 cũng note "10 collective / 12 specific" để planner Phase 3+ tham khảo. |
</threat_model>

<verification>
**Plan 02-04 verification chain (Phase 2 closeout):**

1. **Smoke compose verify [BLOCKING]** (Task 1):
   - 4 service compose render OK + 2 service spawn parallel (central 8180 + yte 8181)
   - curl matrix 8 central-only ở hub yte → 404 envelope
   - curl matrix 12 hub-scoped ở hub yte → != 404
   - docker logs medinet-api-yte có evidence central_only_routers_skipped

2. **File integrity (grep)** (Task 2 + 3 + 4):
   - Hub_All/CLAUDE.md section 6 mở rộng v3.0 progress + Phase 2 pattern + note 10/12 endpoint
   - .planning/STATE.md frontmatter + Phase 2 Results Summary + Next Action update + note 10/12
   - .planning/REQUIREMENTS.md FACTOR-03 note clarify "10 collective / 12 specific" + matrix list inline

3. **Markdown + YAML structure preserve**:
   - CLAUDE.md vẫn 6 section + table syntax đúng
   - STATE.md frontmatter YAML parse OK (yamllint nếu có)
   - REQUIREMENTS.md table/blockquote syntax đúng

4. **Phase 2 success criteria 1-3 (ROADMAP) verified**:
   - SC1: 4 service compose render đầy đủ + 2 service smoke up parallel (Task 1 step 1-2)
   - SC2: hub con `GET /api/rag-config` → 404 + `GET /api/health` → 200 (Task 1 step 3-5)
   - SC3: hub con 12 endpoint hub-scoped mount đúng (Task 1 step 6 + Plan 02-03 integration test 36 assertion)

5. **Final regression KHÔNG break**:
   - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py -v` PASS
   - `cd Hub_All/api && uv run pytest tests/integration/test_factor_hub_scoped.py -v -m "critical and integration"` PASS
   - `cd Hub_All/api && uv run pytest tests/integration/test_hub_isolation_db_level.py -v -m "critical and integration"` PASS (Phase 1 carry forward)
   - `cd Hub_All && docker compose config --quiet` exit 0

6. **Git status clean post-commit**:
   - 3 file modify committed (`Hub_All/CLAUDE.md` + `.planning/STATE.md` + `.planning/REQUIREMENTS.md`)
</verification>

<success_criteria>
- [BLOCKING] Smoke compose 7 step PASS hoặc "skip smoke" với rationale rõ (Task 1)
- Hub_All/CLAUDE.md section 6 mở rộng v3.0 progress + Phase 2 pattern + note 10/12 endpoint (Task 2)
- .planning/STATE.md frontmatter + Phase 2 Results Summary + Next Action update (Task 3)
- .planning/REQUIREMENTS.md FACTOR-03 note clarify "10 collective / 12 specific endpoints" inline matrix (Task 4 — WRN-05 fix)
- Phase 2 success criteria 1-3 (ROADMAP) verified ở runtime thật (Task 1)
- FACTOR-01..03 SATISFIED (verify chéo: unit Plan 02-01 + integration Plan 02-03 + compose smoke Plan 02-04 Task 1)
- Phase 1 regression test PASS (5 integration test Phase 1)
- Phase 2 closeout — ready cho `/gsd-discuss-phase 3` Auth SSO
- D-V3-Phase2-I (CLAUDE.md update pattern) satisfied
- v3.0-a progress: Phase 1+2 DONE (2/3 phase v3.0-a)
- WRN-05 future confusion mitigated — 3 file đều có note traceability "10/12"
</success_criteria>

<output>
Tạo `.planning/phases/02-hub-con-codebase-factor/02-04-SUMMARY.md`:
- Phase 2 closeout summary (4 plan + N commit + test count)
- Smoke compose checkpoint result (Task 1) — PASS / SKIP với reason
- CLAUDE.md update diff (section 6 extension + 10/12 note)
- STATE.md update diff (frontmatter progress + Phase 2 Results Summary + Next Action + 10/12 reference)
- REQUIREMENTS.md update diff (FACTOR-03 note + 12-endpoint matrix inline — WRN-05 fix)
- Phase 2 success criteria verification (ROADMAP SC1/2/3 PASS)
- v3.0-a progress: Phase 1+2 DONE; Phase 3 Auth SSO Next Action
- v3.0-a EXIT GATE preview (giữa Phase 3 và 4) — demo deliverable list
</output>
</content>
