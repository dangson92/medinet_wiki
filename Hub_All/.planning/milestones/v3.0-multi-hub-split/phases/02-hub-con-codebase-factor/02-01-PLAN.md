---
phase: 02-hub-con-codebase-factor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/main.py
  - api/tests/unit/test_main_factory.py
autonomous: true
requirements:
  - FACTOR-01
  - FACTOR-02
tags:
  - factory
  - router-conditional
  - hub-name
  - factor-01
  - factor-02
  - d-v3-phase2-a
  - d-v3-phase2-b
  - d-v3-phase2-e

must_haves:
  truths:
    - "create_app() đọc settings.hub_name (Phase 1 Literal) — KHÔNG đổi signature no-arg (D-V3-Phase2-A)"
    - "7 router universal (auth, documents, profile, search, ask, usage, ai_chat) mount ở MỌI process (central + hub con) — FACTOR-03 hub-scoped"
    - "9 router central-only (hubs, users, api_keys, audit_logs, rag_config, system_settings, sync, mcp_oauth, mcp_oauth_internal) mount CHỈ khi settings.hub_name == 'central' — FACTOR-02 strip"
    - "Logger info `central_only_routers_skipped: hub_name=<hub>` emit khi hub con boot (observability + R-V3-3 evidence)"
    - "Unit test boot 4 hub mode (central + yte + duoc + hcns) verify router list match expectation — KHÔNG cần docker"
    - "404 envelope shape `{success:false, data:null, error:{code, message}, meta:null}` giữ nguyên khi hub con nhận central-only endpoint (HTTPException handler M2 đã wrap — D-V3-Phase2-E)"
  artifacts:
    - path: "api/app/main.py"
      provides: "create_app() factory + conditional router mount block"
      contains: "if settings.hub_name == \"central\":"
    - path: "api/tests/unit/test_main_factory.py"
      provides: "Unit test boot 4 hub mode + verify router list"
      contains: "def test_factory_central_mounts_all_routers\\|def test_factory_yte_strips_central_only"
  key_links:
    - from: "api/app/main.py::create_app()"
      to: "api/app/config.py::Settings.hub_name (Phase 1 TOPO-04)"
      via: "settings = get_settings() rồi `if settings.hub_name == 'central'`"
      pattern: "settings\\.hub_name == \"central\""
    - from: "api/app/main.py::create_app()"
      to: "api/app/routers/__init__.py (7 universal + 9 central-only export)"
      via: "from app.routers import auth_router, documents_router, ..."
      pattern: "from app.routers import"
    - from: "api/tests/unit/test_main_factory.py"
      to: "api/app/main.py::create_app() factory"
      via: "monkeypatch.setenv HUB_NAME + DATABASE_URL + get_settings.cache_clear() + create_app()"
      pattern: "monkeypatch\\.setenv\\(\"HUB_NAME\""
---

<objective>
Refactor `create_app()` ở `api/app/main.py` để mount router CONDITIONAL theo `settings.hub_name` — 7 router universal mount ở mọi process (central + hub con), 9 router central-only chỉ mount khi `hub_name == "central"`. Thêm unit test `tests/unit/test_main_factory.py` boot app ở 4 mode (central/yte/duoc/hcns) verify router list match expectation.

Đây là plan BLOCKING (Wave 1) — Plan 02-02 (docker-compose) + Plan 02-03 (integration test) tiêu thụ output này.

Purpose: FACTOR-01 (1 codebase deploy nhiều lần với HUB_NAME) + FACTOR-02 (strip central-only router khi hub con) — đóng phần factory pattern Phase 2.

Output: 1 file modify (`api/app/main.py`) + 1 file mới (`tests/unit/test_main_factory.py`).

**Locked decisions consume (CONTEXT.md):**
- D-V3-Phase2-A: 1 `create_app()` factory no-arg (KHÔNG 2 file main_central + main_hub).
- D-V3-Phase2-B: Inline `if settings.hub_name == "central"` (KHÔNG feature flag config-driven).
- D-V3-Phase2-E: 404 shape (KHÔNG 403) — router không mount → FastAPI built-in 404 → ErrorHandlerMiddleware M2 wrap envelope.
- D-V3-Phase2-G: Lifespan universal (KHÔNG conditional skip step).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md
@Hub_All/.planning/phases/01-multi-db-topology/01-02-PLAN.md
@Hub_All/api/app/main.py
@Hub_All/api/app/config.py
@Hub_All/api/app/routers/__init__.py

<interfaces>
<!-- Phase 1 đã ship Settings.hub_name + DSN validator. Plan 02-01 consume nguyên xi. -->

Từ `api/app/config.py` (Phase 1 ship — KHÔNG sửa Plan 02-01):

```python
class Settings(BaseSettings):
    hub_name: Literal["central", "yte", "duoc", "hcns"] = "central"
    database_url: str = Field(...)
    # _enforce_hub_dsn_match validator: HUB_NAME=yte yêu cầu DSN trỏ /medinet_hub_yte

@lru_cache
def get_settings() -> Settings:
    return Settings()
```

Từ `api/app/routers/__init__.py` (M2 ship — KHÔNG sửa Plan 02-01):

```python
__all__ = [
    "ai_chat_router",           # universal
    "api_keys_router",          # central-only
    "ask_router",               # universal
    "audit_logs_router",        # central-only
    "documents_router",         # universal
    "hubs_router",              # central-only
    "mcp_oauth_internal_router",# central-only
    "mcp_oauth_router",         # central-only
    "profile_router",           # universal
    "rag_config_router",        # central-only
    "search_router",            # universal
    "sync_router",              # central-only
    "system_settings_router",   # central-only
    "usage_router",             # universal
    "users_router",             # central-only
]
# auth_router import từ app.auth (KHÔNG qua app.routers)
```

Từ `api/app/main.py` create_app() current (M2 line 325-460) — mount 15 router unconditional. Plan 02-01 wrap 9 trong `if settings.hub_name == "central":` block.

Test fixture pattern hiện hữu (Phase 1 test_config_hub_name.py):
- `monkeypatch.setenv("HUB_NAME", "yte")`
- `monkeypatch.setenv("DATABASE_URL", "postgresql+asyncpg://u:p@h:5432/medinet_hub_yte")`
- `get_settings.cache_clear()`  # force re-parse
- Then `from app.main import create_app; app = create_app()`
- Iterate `app.routes` → check path prefixes mount/strip.
</interfaces>
</context>

<notes>
**Cross-hub alias defer (WRN-02 — Plan 02-01 KHÔNG strip ask_router universal mount):**

Theo CONTEXT.md D-V3-Phase2-D matrix: `ask_router` thuộc nhóm universal — Plan 02-01 mount ở MỌI hub. CONTEXT.md cũng note rằng cross-hub variant `/api/ask/cross-hub` + `/api/search/answer` nên strip ở hub con, NHƯNG Plan 02-01 KHÔNG thực hiện strip này (mount nguyên `ask_router` + `search_router` universal).

Hệ quả Phase 2 (acknowledged, intentional):
- Hub con vẫn expose `/api/ask/cross-hub` + `/api/search/answer` ở runtime.
- Vì DB hub con KHÔNG chứa data của hub khác → endpoint cross-hub sẽ trả 500 (asyncpg query empty / FK miss) hoặc fallback local-only logic (tuỳ implementation cụ thể ở ask_router).
- KHÔNG có lỗ hổng cross-hub leak data (DB-level isolation Phase 1 E-V3-3 enforce — query KHÔNG access được medinet_hub_<khác>).
- Phase 2 chỉ refactor factory + strip 9 router central-only ở Tier 2.

**Deferred to Phase 4 SYNC-03:**
- Tách `ask_router` thành 2 router: `ask_local_router` (universal) + `ask_cross_hub_router` (central-only).
- Hoặc giữ 1 router + thêm dependency `require_central` reject 404 ở endpoint cross-hub khi hub_name != "central".
- Quyết định cụ thể chốt ở `/gsd-discuss-phase 4`.

→ Plan 02-03 integration test KHÔNG cover `/api/ask/cross-hub` ở hub con (defer Phase 4); chỉ verify base `/api/ask` mount đúng (FACTOR-03 hub-scoped contract).
</notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor api/app/main.py — wrap 9 central-only router với `if settings.hub_name == "central"` block + logger info strip</name>
  <files>api/app/main.py</files>
  <read_first>
    - Hub_All/api/app/main.py (file đang sửa — đặc biệt block mount router line 368-435)
    - Hub_All/api/app/config.py (Settings.hub_name Literal + _enforce_hub_dsn_match — KHÔNG sửa)
    - Hub_All/api/app/routers/__init__.py (15 router export — KHÔNG sửa)
    - Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md (D-V3-Phase2-A/B/E/G)
  </read_first>
  <action>
**Step 1: Reorganize router import + mount block trong `create_app()` thành 2 nhóm rõ ràng.**

Mở `api/app/main.py`, xác định block hiện tại từ line ~368 (sau `app.add_middleware(ErrorHandlerMiddleware)`) đến line ~435 (trước `from slowapi.errors import RateLimitExceeded`). Hiện block này import + mount 15 router rải rác qua 8 statement riêng. Plan 02-01 thay TOÀN BỘ block đó thành 2 nhóm:

**Tier 1 — Universal (7 router, mount ở mọi process):**

```python
    # ──────────────────────────────────────────────────────────────────────
    # Tier 1 — Universal routers (mount ở MỌI process: central + hub con)
    # FACTOR-03 hub-scoped: auth/profile/documents/search/ask/usage + ai_chat
    # ──────────────────────────────────────────────────────────────────────
    from app.auth import auth_router
    from app.routers import (
        ai_chat_router,
        ask_router,
        documents_router,
        profile_router,
        search_router,
        usage_router,
    )

    app.include_router(auth_router)
    app.include_router(documents_router)
    app.include_router(profile_router)
    app.include_router(search_router)
    app.include_router(ask_router)
    app.include_router(usage_router)
    app.include_router(ai_chat_router)
```

**Tier 2 — Central-only (9 router, mount CHỈ khi `hub_name == "central"`):**

```python
    # ──────────────────────────────────────────────────────────────────────
    # Tier 2 — Central-only routers (FACTOR-02 strip ở hub con)
    # 9 router admin/system mount CHỈ khi process = central.
    # Hub con (yte/duoc/hcns) nhận request central-only → 404 envelope
    # (ErrorHandlerMiddleware M2 wrap built-in 404 — D-V3-Phase2-E).
    # ──────────────────────────────────────────────────────────────────────
    if settings.hub_name == "central":
        from app.routers import (
            api_keys_router,
            audit_logs_router,
            hubs_router,
            mcp_oauth_internal_router,
            mcp_oauth_router,
            rag_config_router,
            sync_router,
            system_settings_router,
            users_router,
        )

        app.include_router(hubs_router)
        app.include_router(users_router)
        app.include_router(api_keys_router)
        app.include_router(audit_logs_router)
        app.include_router(rag_config_router)
        app.include_router(system_settings_router)
        app.include_router(sync_router)
        app.include_router(mcp_oauth_router)
        app.include_router(mcp_oauth_internal_router)
    else:
        logger.info(
            "central_only_routers_skipped: hub_name=%s — 9 routers stripped "
            "(FACTOR-02 enforce)",
            settings.hub_name,
        )
```

**Step 2: Giữ NGUYÊN lifespan + middleware + exception handler.**

- Lifespan M2 (line 58-322) KHÔNG sửa — universal cho mọi hub (D-V3-Phase2-G).
- Middleware order (CORS → SecurityHeaders → RequestId → Prometheus → ErrorHandler) KHÔNG đổi.
- HTTPException handler + HubIsolationError handler + healthz/readyz/metrics endpoint KHÔNG đổi.
- `app = create_app()` module-level KHÔNG đổi.

**Step 3: Comment block-level traceability.**

Thêm 1 docstring block đầu `create_app()` (sau dòng `settings = get_settings()`) giải thích pattern hub_name conditional:

```python
    # v3.0 Phase 2 FACTOR-01..03 (D-V3-Phase2-A/B):
    # Factory đọc settings.hub_name (Phase 1 TOPO-04 Literal central|yte|duoc|hcns)
    # → conditional mount router. KHÔNG đổi signature no-arg để giữ uvicorn
    # `app.main:app` entrypoint M2 compat. Test override qua monkeypatch.setenv
    # + get_settings.cache_clear() trước khi gọi create_app().
```

**KHÔNG xoá / sửa:**
- Lifespan asyncpg pool + redis + cocoindex + jwt + watchdog + audit + search_cache_task.
- Middleware add order.
- HTTPException handler + HubIsolationError handler.
- /healthz, /readyz, /metrics endpoint.
- Module-level `app = create_app()`.

**Reject patterns:**
- KHÔNG dùng `create_app(hub_name: str | None = None)` param — D-V3-Phase2-A reject.
- KHÔNG dùng config-driven feature flag `if settings.router_central_only:` — D-V3-Phase2-B reject (over-engineer).
- KHÔNG mount tất cả + 403 forbidden dependency — D-V3-Phase2-E reject (FACTOR-02 yêu cầu 404 strip semantic).
- KHÔNG bỏ rag_config router ở hub con bằng skip mount riêng — phải nằm trong block central-only chung.
- KHÔNG strip `/api/ask/cross-hub` hoặc `/api/search/answer` ở hub con — defer Phase 4 SYNC-03 (xem `<notes>` ở đầu plan).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run python -c "import os; os.environ['HUB_NAME']='central'; os.environ['DATABASE_URL']='postgresql+asyncpg://u:p@h:5432/medinet_central'; os.environ['COCOINDEX_DATABASE_URL']='postgresql://u:p@h:5432/medinet_cocoindex'; os.environ['REDIS_URL']='redis://localhost:6379/0'; os.environ['COCOINDEX_SKIP_SETUP']='1'; from app.config import get_settings; get_settings.cache_clear(); from app.main import create_app; app = create_app(); print(sorted({r.path.split('/')[2] for r in app.routes if r.path.startswith('/api/')}))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'if settings.hub_name == "central":' Hub_All/api/app/main.py` ≥ 1
    - `grep -c 'central_only_routers_skipped' Hub_All/api/app/main.py` ≥ 1 (logger info evidence)
    - `grep -c 'FACTOR-02\|FACTOR-01' Hub_All/api/app/main.py` ≥ 2 (REQ-ID traceability comment)
    - `grep -c 'D-V3-Phase2-A\|D-V3-Phase2-B' Hub_All/api/app/main.py` ≥ 1 (decision traceability comment)
    - `grep -c 'Tier 1 — Universal\|Tier 2 — Central-only' Hub_All/api/app/main.py` ≥ 2 (block comment markers)
    - `grep -cE '^\s*app\.include_router\(hubs_router\)' Hub_All/api/app/main.py` ≥ 1 (hubs_router vẫn mount khi central)
    - `grep -cE '^\s*app\.include_router\(rag_config_router\)' Hub_All/api/app/main.py` ≥ 1 (rag_config_router vẫn mount khi central)
    - `grep -cE '^\s*app\.include_router\(auth_router\)' Hub_All/api/app/main.py` ≥ 1 (auth_router universal mount)
    - `grep -cE '^\s*app\.include_router\(documents_router\)' Hub_All/api/app/main.py` ≥ 1 (documents_router universal mount)
    - `grep -cE '^\s*app\.include_router\(ai_chat_router\)' Hub_All/api/app/main.py` ≥ 1
    - `cd Hub_All/api && uv run ruff check app/main.py` exit 0
    - `cd Hub_All/api && uv run mypy app/main.py` exit 0
    - `cd Hub_All/api && COCOINDEX_SKIP_SETUP=1 HUB_NAME=central DATABASE_URL=postgresql+asyncpg://u:p@h:5432/medinet_central COCOINDEX_DATABASE_URL=postgresql://u:p@h:5432/medinet_cocoindex REDIS_URL=redis://localhost:6379/0 uv run python -c "from app.main import create_app; app=create_app(); paths=[r.path for r in app.routes]; assert any('/api/rag-config' in p for p in paths), 'rag-config phải mount khi central'; print('central OK', len(paths), 'routes')"` exit 0
    - `cd Hub_All/api && COCOINDEX_SKIP_SETUP=1 HUB_NAME=yte DATABASE_URL=postgresql+asyncpg://u:p@h:5432/medinet_hub_yte COCOINDEX_DATABASE_URL=postgresql://u:p@h:5432/medinet_cocoindex REDIS_URL=redis://localhost:6379/0 uv run python -c "from app.config import get_settings; get_settings.cache_clear(); from app.main import create_app; app=create_app(); paths=[r.path for r in app.routes]; assert not any('/api/rag-config' in p for p in paths), 'rag-config phải strip ở hub yte'; assert any('/api/auth' in p for p in paths), 'auth phải mount ở hub yte'; print('yte OK', len(paths), 'routes')"` exit 0
  </acceptance_criteria>
  <done>
api/app/main.py refactor xong: 7 universal router mount unconditional, 9 central-only router wrap trong `if settings.hub_name == "central":` block + logger info evidence; ruff + mypy clean; smoke create_app() ở 2 mode (central + yte) verify router list khác nhau đúng expectation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo unit test tests/unit/test_main_factory.py — boot app ở 4 hub mode + verify router list (FACTOR-01/02 unit-level)</name>
  <files>api/tests/unit/test_main_factory.py</files>
  <read_first>
    - Hub_All/api/tests/unit/test_config_hub_name.py (Phase 1 analog — monkeypatch.setenv + get_settings.cache_clear pattern)
    - Hub_All/api/app/main.py (Plan 02-01 Task 1 — factory đã refactor)
    - Hub_All/api/app/config.py (Settings.hub_name Literal + _enforce_hub_dsn_match)
  </read_first>
  <behavior>
    - Test 1 (`test_factory_central_mounts_all_routers`): HUB_NAME=central → app.routes có path prefix `/api/rag-config`, `/api/hubs`, `/api/users`, `/api/api-keys`, `/api/audit-logs`, `/api/system-settings`, `/api/sync`, `/api/mcp` (central-only) ĐỒNG THỜI có `/api/auth`, `/api/documents`, `/api/profile`, `/api/search`, `/api/ask`, `/api/usage`, `/api/ai/chat` (universal).
    - Test 2 (`test_factory_yte_strips_central_only`): HUB_NAME=yte + DSN trỏ medinet_hub_yte → app.routes KHÔNG có `/api/rag-config`, `/api/hubs`, `/api/users`, `/api/api-keys`, `/api/audit-logs`, `/api/system-settings`, `/api/sync`, `/api/mcp` ĐỒNG THỜI VẪN có `/api/auth`, `/api/documents`, `/api/profile`, `/api/search`, `/api/ask`, `/api/usage`, `/api/ai/chat`.
    - Test 3 (`test_factory_duoc_strips_central_only`): tương tự yte với HUB_NAME=duoc + DSN medinet_hub_duoc.
    - Test 4 (`test_factory_hcns_strips_central_only`): tương tự yte với HUB_NAME=hcns + DSN medinet_hub_hcns.
    - Test 5 (`test_factory_yte_logs_central_only_skip`): HUB_NAME=yte → caplog capture `logger.info` message chứa `central_only_routers_skipped: hub_name=yte` (evidence FACTOR-02 strip observable).
    - Test 6 (`test_factory_hub_name_mismatch_dsn_raises`): HUB_NAME=yte + DATABASE_URL trỏ medinet_central → Settings raise ValidationError "DSN mismatch hub_name" (Phase 1 validator carry forward, không regress).
  </behavior>
  <action>
Tạo file `api/tests/unit/test_main_factory.py`:

```python
"""Phase 2 FACTOR-01/02 — Unit test create_app() factory với 4 hub mode.

Verify conditional router mount đúng theo settings.hub_name:
- central: 7 universal + 9 central-only = 16 router (auth từ app.auth + 15 từ routers)
- yte/duoc/hcns: 7 universal CHỈ — 9 central-only strip (FACTOR-02)

Test KHÔNG cần Docker/testcontainers (unit-level — không hit lifespan
startup actually). FastAPI route registration ở module-import time qua
include_router → check app.routes list-as-is sau create_app() đủ.

Set COCOINDEX_SKIP_SETUP=1 để bypass cocoindex Environment singleton
(DEF-05-01 pattern M2 — Phase 4 test mode escape hatch).

Plan 02-01 Task 2.
"""
from __future__ import annotations

import logging

import pytest
from pydantic import ValidationError


# Path prefix expected mount cho từng hub mode (verify qua app.routes)
UNIVERSAL_PREFIXES = [
    "/api/auth",       # auth_router
    "/api/documents",  # documents_router
    "/api/profile",    # profile_router
    "/api/search",     # search_router
    "/api/ask",        # ask_router
    "/api/usage",      # usage_router
    "/api/ai/chat",    # ai_chat_router (note: prefix /api/ai/chat — không phải /api/ai)
]

CENTRAL_ONLY_PREFIXES = [
    "/api/rag-config",       # rag_config_router
    "/api/hubs",             # hubs_router
    "/api/users",            # users_router
    "/api/api-keys",         # api_keys_router
    "/api/audit-logs",       # audit_logs_router
    "/api/system-settings",  # system_settings_router
    "/api/sync",             # sync_router
    "/api/mcp",              # mcp_oauth_router (prefix /api/mcp/...)
    "/api/internal/mcp",     # mcp_oauth_internal_router
]


def _setup_env(monkeypatch, hub_name: str) -> None:
    """Set env vars chuẩn cho create_app() boot theo hub mode.

    DSN suffix phải khớp hub_name (Settings._enforce_hub_dsn_match — Phase 1).
    """
    if hub_name == "central":
        db = "medinet_central"
    else:
        db = f"medinet_hub_{hub_name}"
    monkeypatch.setenv("HUB_NAME", hub_name)
    monkeypatch.setenv(
        "DATABASE_URL", f"postgresql+asyncpg://u:p@localhost:5432/{db}"
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")
    # Force re-parse env mỗi test (lru_cache singleton)
    from app.config import get_settings

    get_settings.cache_clear()


def _route_paths(app) -> list[str]:
    """Trả danh sách path str từ app.routes (only có .path attr — Mount/Route)."""
    paths: list[str] = []
    for route in app.routes:
        path = getattr(route, "path", None)
        if isinstance(path, str):
            paths.append(path)
    return paths


def _has_prefix(paths: list[str], prefix: str) -> bool:
    """True nếu có ít nhất 1 path bắt đầu prefix."""
    return any(p.startswith(prefix) for p in paths)


@pytest.fixture(autouse=True)
def _clear_settings_cache():
    from app.config import get_settings

    get_settings.cache_clear()
    yield
    get_settings.cache_clear()


def test_factory_central_mounts_all_routers(monkeypatch):
    """HUB_NAME=central → mount cả 7 universal + 9 central-only."""
    _setup_env(monkeypatch, "central")
    from app.main import create_app

    app = create_app()
    paths = _route_paths(app)

    for prefix in UNIVERSAL_PREFIXES:
        assert _has_prefix(paths, prefix), (
            f"Universal router {prefix!r} PHẢI mount khi hub_name=central"
        )
    for prefix in CENTRAL_ONLY_PREFIXES:
        assert _has_prefix(paths, prefix), (
            f"Central-only router {prefix!r} PHẢI mount khi hub_name=central"
        )


@pytest.mark.parametrize("hub_name", ["yte", "duoc", "hcns"])
def test_factory_hub_strips_central_only(monkeypatch, hub_name):
    """HUB_NAME=yte|duoc|hcns → strip 9 central-only, giữ 7 universal."""
    _setup_env(monkeypatch, hub_name)
    from app.main import create_app

    app = create_app()
    paths = _route_paths(app)

    # 7 universal PHẢI có
    for prefix in UNIVERSAL_PREFIXES:
        assert _has_prefix(paths, prefix), (
            f"Universal router {prefix!r} PHẢI mount khi hub_name={hub_name}"
        )

    # 9 central-only PHẢI KHÔNG có
    for prefix in CENTRAL_ONLY_PREFIXES:
        assert not _has_prefix(paths, prefix), (
            f"Central-only router {prefix!r} PHẢI strip khi hub_name={hub_name} "
            f"— FACTOR-02 enforce"
        )


def test_factory_yte_strips_central_only(monkeypatch):
    """Explicit yte test — alias cho parametrize trên (test name traceability)."""
    _setup_env(monkeypatch, "yte")
    from app.main import create_app

    app = create_app()
    paths = _route_paths(app)
    assert not _has_prefix(paths, "/api/rag-config"), (
        "/api/rag-config PHẢI strip ở hub yte — FACTOR-02"
    )
    assert _has_prefix(paths, "/api/auth"), (
        "/api/auth PHẢI mount ở hub yte — FACTOR-03"
    )


def test_factory_duoc_strips_central_only(monkeypatch):
    """Explicit duoc test."""
    _setup_env(monkeypatch, "duoc")
    from app.main import create_app

    app = create_app()
    paths = _route_paths(app)
    assert not _has_prefix(paths, "/api/hubs"), (
        "/api/hubs PHẢI strip ở hub duoc — FACTOR-02"
    )


def test_factory_hcns_strips_central_only(monkeypatch):
    """Explicit hcns test."""
    _setup_env(monkeypatch, "hcns")
    from app.main import create_app

    app = create_app()
    paths = _route_paths(app)
    assert not _has_prefix(paths, "/api/users"), (
        "/api/users PHẢI strip ở hub hcns — FACTOR-02"
    )


def test_factory_yte_logs_central_only_skip(monkeypatch, caplog):
    """HUB_NAME=yte → logger.info emit central_only_routers_skipped với hub_name=yte."""
    _setup_env(monkeypatch, "yte")
    with caplog.at_level(logging.INFO, logger="app.main"):
        from app.main import create_app

        create_app()

    matches = [
        rec.getMessage() for rec in caplog.records
        if "central_only_routers_skipped" in rec.getMessage()
    ]
    assert matches, (
        "Phải log central_only_routers_skipped khi hub_name=yte"
    )
    assert any("hub_name=yte" in m for m in matches), (
        f"Log phải chứa 'hub_name=yte', got: {matches!r}"
    )


def test_factory_hub_name_mismatch_dsn_raises(monkeypatch):
    """HUB_NAME=yte + DSN trỏ medinet_central → Settings raise ValidationError.

    Phase 1 _enforce_hub_dsn_match validator carry forward — Plan 02-01 KHÔNG regress.
    """
    monkeypatch.setenv("HUB_NAME", "yte")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/medinet_central",
    )
    monkeypatch.setenv(
        "COCOINDEX_DATABASE_URL",
        "postgresql://u:p@localhost:5432/medinet_cocoindex",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("COCOINDEX_SKIP_SETUP", "1")

    from app.config import Settings, get_settings

    get_settings.cache_clear()

    with pytest.raises(ValidationError, match="DSN mismatch hub_name"):
        Settings()
```

Note: Test này KHÔNG dùng `@pytest.mark.critical` vì là unit test (không cần testcontainers); HARD-03 critical gate chỉ áp dụng integration test (Plan 02-03).

**Edge case**: nếu `/api/ai/chat` router thực tế có prefix khác (vd `/api/ai` chứ không `/api/ai/chat`), test có thể fail. Khi execute: kiểm tra prefix thực tế qua `grep "prefix=" Hub_All/api/app/routers/ai_chat.py` rồi điều chỉnh `UNIVERSAL_PREFIXES`. Tương tự `/api/mcp` vs `/api/mcp/my-oauth-client` cho mcp_oauth_router. Pattern `startswith` đã handle 2 dạng `/api/mcp` và `/api/mcp/...`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py -v</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/unit/test_main_factory.py` exists
    - `grep -c "def test_factory_central_mounts_all_routers" Hub_All/api/tests/unit/test_main_factory.py` ≥ 1
    - `grep -c "def test_factory_hub_strips_central_only\|def test_factory_yte_strips_central_only\|def test_factory_duoc_strips_central_only\|def test_factory_hcns_strips_central_only" Hub_All/api/tests/unit/test_main_factory.py` ≥ 4
    - `grep -c "def test_factory_yte_logs_central_only_skip" Hub_All/api/tests/unit/test_main_factory.py` ≥ 1
    - `grep -c "def test_factory_hub_name_mismatch_dsn_raises" Hub_All/api/tests/unit/test_main_factory.py` ≥ 1
    - `grep -c "COCOINDEX_SKIP_SETUP" Hub_All/api/tests/unit/test_main_factory.py` ≥ 1 (escape hatch test mode)
    - `grep -c "get_settings.cache_clear" Hub_All/api/tests/unit/test_main_factory.py` ≥ 2 (re-parse env pattern)
    - `grep -c "central_only_routers_skipped" Hub_All/api/tests/unit/test_main_factory.py` ≥ 1 (log verify)
    - `grep -c "DSN mismatch hub_name" Hub_All/api/tests/unit/test_main_factory.py` ≥ 1 (Phase 1 validator regression check)
    - `cd Hub_All/api && uv run ruff check tests/unit/test_main_factory.py` exit 0
    - `cd Hub_All/api && uv run mypy tests/unit/test_main_factory.py` exit 0
    - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py -v` PASS ≥ 8 test (1 central + 3 parametrize hub + 3 explicit hub + 1 log + 1 mismatch = 9; tối thiểu 8)
  </acceptance_criteria>
  <done>
Unit test test_main_factory.py PASS toàn bộ; verify factory mount đúng router theo hub_name; verify logger info central_only_routers_skipped emit; verify Phase 1 DSN validator KHÔNG regress; ruff + mypy clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → hub con FastAPI app | Request central-only endpoint (vd `GET /api/rag-config`) PHẢI nhận 404 envelope, KHÔNG 403 (FACTOR-02 strip semantic — endpoint không exist) |
| Env HUB_NAME → Settings | HUB_NAME = Literal["central","yte","duoc","hcns"] — pydantic-settings reject value khác ở startup (fail-fast) |
| Settings.hub_name → create_app() | Factory đọc singleton get_settings() — đảm bảo conditional mount idempotent (subsequent call KHÔNG đổi router list) |
| Unit test → create_app() | monkeypatch.setenv + get_settings.cache_clear() phải reset SẠCH giữa test — tránh state leak (lru_cache singleton) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | Hub con leak existence của central-only endpoint qua 403 thay vì 404 — attacker enumerate endpoint admin | mitigate | D-V3-Phase2-E LOCKED: router KHÔNG mount → FastAPI built-in 404 → ErrorHandlerMiddleware M2 wrap envelope shape `{success:false, data:null, error:{code:"NOT_FOUND", message}, meta:null}`. Acceptance criteria verify `/api/rag-config` ở hub yte NOT in `app.routes` (unit test) + integration test Plan 02-03 verify 404 envelope shape. |
| T-02-01-02 | Information Disclosure | Logger info `central_only_routers_skipped: hub_name=<hub>` leak hub identity ra log nếu log ship public | accept | Log info-level chỉ chứa hub_name (đã public qua env config + Caddy subpath Phase 5). KHÔNG chứa credential / DSN / user data. Ops log shipping qua structlog JSON tới Loki/Datadog internal — KHÔNG public. |
| T-02-01-03 | Tampering | Test HUB_NAME=yte + DSN central → bypass Settings validator nếu cache_clear() quên gọi | mitigate | Plan 02-01 Task 2 Test 6 (`test_factory_hub_name_mismatch_dsn_raises`) verify validator raise. `_clear_settings_cache` autouse fixture force `get_settings.cache_clear()` trước + sau mỗi test. Phase 1 validator `_enforce_hub_dsn_match` carry forward — KHÔNG regress. |
| T-02-01-04 | Elevation of Privilege | Hub con bypass FACTOR-02 strip bằng cách mount router trực tiếp ở runtime (vd qua plugin / monkey patch) | accept | Pattern KHÔNG enforce ở Python (dynamic language). Production deploy = uvicorn `app.main:app` chạy module-level `create_app()` → router mount cố định ở boot. KHÔNG có endpoint mount runtime. Threat thuộc internal compromise — không phải attack surface external. |
| T-02-01-05 | Denial of Service | Logger info emit mỗi request → log spam | mitigate | Logger info emit CHỈ 1 LẦN ở module-level `create_app()` (boot time), KHÔNG ở per-request middleware. Verify qua acceptance criteria `grep -c central_only_routers_skipped api/app/main.py` ≥ 1 — chỉ 1 statement. |
</threat_model>

<verification>
**Plan 02-01 verification chain:**

1. **File integrity (grep)**:
   - `api/app/main.py` modify line ~368-435 (mount router block) — 7 universal mount unconditional + 9 central-only wrap trong `if settings.hub_name == "central":`
   - `api/tests/unit/test_main_factory.py` mới tạo

2. **Lint + type clean**:
   - `cd Hub_All/api && uv run ruff check app/main.py tests/unit/test_main_factory.py` exit 0
   - `cd Hub_All/api && uv run mypy app/main.py tests/unit/test_main_factory.py` exit 0

3. **Unit test PASS**:
   - `cd Hub_All/api && uv run pytest tests/unit/test_main_factory.py -v` PASS ≥ 8 test

4. **Smoke create_app() 2 mode** (qua command verify acceptance):
   - HUB_NAME=central → app.routes có cả `/api/rag-config` và `/api/auth`
   - HUB_NAME=yte → app.routes KHÔNG có `/api/rag-config` nhưng VẪN có `/api/auth`

5. **Phase 1 regression KHÔNG break**:
   - `cd Hub_All/api && uv run pytest tests/unit/test_config_hub_name.py tests/unit/test_alembic_env_hub_arg.py tests/unit/test_flow_per_hub_naming.py -v` PASS toàn bộ
   - `cd Hub_All/api && uv run pytest tests/integration/test_hub_isolation_db_level.py -v -m "critical and integration"` PASS 5/5 (cần Docker)
</verification>

<success_criteria>
- create_app() refactor: 7 universal router mount unconditional, 9 central-only router wrap `if settings.hub_name == "central":` block + logger info evidence (Task 1)
- Unit test test_main_factory.py PASS ≥ 8 test (1 central full + 3 parametrize hub strip + 3 explicit hub strip + 1 log + 1 DSN mismatch) (Task 2)
- Phase 1 regression test KHÔNG fail (Settings.hub_name + DSN validator chain)
- FACTOR-01 satisfied: 1 codebase deploy 4 mode (central/yte/duoc/hcns) — verify qua unit test boot 4 mode
- FACTOR-02 satisfied: hub con KHÔNG mount 9 central-only router (verify qua app.routes assertion)
- D-V3-Phase2-A/B/E/G honored (factory no-arg, inline conditional, 404 shape, lifespan universal)
</success_criteria>

<output>
Tạo `.planning/phases/02-hub-con-codebase-factor/02-01-SUMMARY.md`:
- create_app() factory interface (no-arg, đọc settings.hub_name)
- Tier 1 (7 universal) + Tier 2 (9 central-only) router list + mount block diff
- Unit test coverage: 4 hub mode boot smoke + log evidence + Phase 1 validator regression
- FACTOR-01/02 đóng ở unit level — integration smoke ở Plan 02-03 (Wave 2)
- Next: Plan 02-02 (docker-compose 4 service) + Plan 02-03 (integration test) parallel Wave 2
</output>
</content>
