---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/Makefile
autonomous: true
requirements: [MIGRATE-01]
tags: [backend, alembic, integration-test, testcontainers, makefile, regression-verify]
must_haves:
  truths:
    - "api/Makefile có 2 target mới: test-integration (chạy tất cả pytest -m integration) + test-migration (chỉ test_migration_*.py)"
    - "Existing test_migration_0006_idempotent.py 5 test PASS clean qua make test-migration với testcontainers Postgres pgvector pg16"
    - "Existing test_migration_upgrade_downgrade.py 2 test PASS regression baseline (10 bang baseline + downgrade base clean)"
    - "alembic upgrade head 2 lần liên tiếp KHÔNG fail (idempotent guard sa.inspect introspect Plan 01-01 LOCKED carry forward)"
    - "alembic downgrade -1 rollback PASS — restore CHECK 3 value + drop user_hubs.role column + existing data preserve"
    - "Plan 04-01 KHÔNG sửa migration 0006_role_hub_admin.py (LOCKED Plan 01-01 ship — verify-only)"
    - "Plan 04-01 KHÔNG sửa downgrade() function (LOCKED Plan 01-01 ship với defensive RuntimeError E-V3.1-1)"
  artifacts:
    - path: "api/Makefile"
      provides: "test-integration + test-migration shortcut target Plan 04-02/Phase 4 closeout reuse"
      contains: "test-integration:"
  key_links:
    - from: "api/Makefile test-migration target"
      to: "api/tests/integration/test_migration_0006_idempotent.py + test_migration_upgrade_downgrade.py"
      via: "pytest pattern matching test_migration_*.py + -v --tb=short"
      pattern: "test_migration_\\*\\.py"
    - from: "make test-integration"
      to: "pytest -m integration"
      via: "pytest marker registered trong pyproject.toml [tool.pytest.ini_options].markers"
      pattern: "-m integration"
---

<objective>
Plan 04-01 v3.1 Phase 4 MIGRATE-01 — Wave 1 BLOCKING migration verify + Makefile shortcut: ADD `test-integration` + `test-migration` target vào `api/Makefile` + chạy test integration suite (7 test = 5 + 2) PASS clean live testcontainers Postgres pgvector pg16.

**Purpose:** Phase 1 Plan 01-01 ship migration `0006_role_hub_admin.py` với introspect `sa.inspect()` idempotent guard + downgrade() defensive RuntimeError E-V3.1-1 STOP (Plan 01-03 ship 5 integration test). Phase 4 Plan 04-01 chạy bắt buộc verify runtime live DB + ADD Makefile shortcut cho operator reuse (`make test-migration` + `make test-integration` thay manual `pytest tests/integration/...`).

**Output:**
- `api/Makefile` — APPEND 2 target mới: `test-integration` (chạy `pytest tests/integration -v -m integration --tb=short`) + `test-migration` (chỉ chạy 2 test file migration); update `.PHONY` line 2.
- KHÔNG code change cho `api/migrations/versions/0006_role_hub_admin.py` (LOCKED Plan 01-01).
- KHÔNG code change cho 2 test file existing — chỉ chạy verify.

**Scope:**
- Add 2 Makefile target inline.
- Verify 7 test PASS (5 + 2) qua `make test-migration` HOẶC `make test-integration` (subset matches).
- Document Docker Desktop prerequisite + testcontainers auto-spin.

**Pattern decisions Claude discretion:**
- Test marker filter: `-m integration` (registered trong pyproject.toml line 95).
- Makefile target naming: `test-integration` + `test-migration` (carry forward `test` + `test-cov` existing pattern line 31-35).
- Output format: `-v --tb=short` (verbose progress + short traceback summary).
- KHÔNG cần TEST_DATABASE_URL env set nếu test dùng `postgres_container` fixture (recommended) — fallback chỉ cho ad-hoc test_migration_0006_idempotent.py (Plan 01-03 pattern dùng TEST_DATABASE_URL env nếu set).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-CONTEXT.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-PATTERNS.md
@Hub_All/api/Makefile
@Hub_All/api/tests/integration/test_migration_0006_idempotent.py
@Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py
@Hub_All/api/pyproject.toml

<interfaces>
<!-- Pattern source-of-truth — Makefile target + pytest marker -->

From Hub_All/api/Makefile (line 1-5 + 31-35 hiện tại):
```makefile
# Medinet Wiki API — dev tasks
.PHONY: install dev keys keys-verify keys-verify-jwt lint test test-cov clean \
        migrate-up migrate-down migrate-check migrate-history cocoindex-setup \
        migrate-all migrate-status hub-init hub-add

UV ?= uv

# ... (line 8-30 install/dev/keys/lint)

test:
	$(UV) run pytest -v

test-cov:
	$(UV) run pytest --cov=app --cov-report=term-missing

clean:
	rm -rf .pytest_cache .mypy_cache .ruff_cache .venv

# === Alembic migration targets (Phase 2 Plan 05) ===
# Tien dieu kien: docker compose up -d postgres (Phase 1 Plan 02) + .env file day DSN.

migrate-up:
	uv run alembic upgrade head
```

From Hub_All/api/pyproject.toml [tool.pytest.ini_options] (line 89-97):
```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
markers = [
    "critical: critical path test (auth, hub isolation, ingest, search, ask)",
    "integration: integration test requires Postgres + Redis testcontainers",
]
addopts = "-ra --strict-markers"
```

From Hub_All/api/tests/integration/test_migration_0006_idempotent.py (5 test ship Plan 01-03):
- test_check_constraint_accepts_hub_admin
- test_re_run_upgrade_head_idempotent
- test_user_hubs_role_nullable_check_constraint
- test_audit_logs_migration_seed_row_inserted
- test_downgrade_restores_check_3_value

From Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py (2 test ship Phase 2 v2.0):
- test_upgrade_creates_10_tables
- test_downgrade_drops_all_clean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADD test-integration + test-migration Makefile target + update .PHONY</name>
  <read_first>
    - Hub_All/api/Makefile (TOÀN FILE — biết .PHONY line 2-4 + test target line 31-35 + Alembic targets line 40+)
    - Hub_All/api/pyproject.toml line 89-97 (pytest markers registered)
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-PATTERNS.md (section "Wave 1" — concrete APPEND pattern)
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-CONTEXT.md decisions D-V3.1-Phase4-A + D-V3.1-Phase4-C
  </read_first>
  <behavior>
    - Test 1 (Grep target exist): grep `^test-integration:` trong Hub_All/api/Makefile == 1.
    - Test 2 (Grep test-migration): grep `^test-migration:` trong Hub_All/api/Makefile == 1.
    - Test 3 (Grep .PHONY update): grep `test-integration test-migration` trong Hub_All/api/Makefile == 1.
    - Test 4 (Help target): `cd Hub_All/api && make -n test-migration` exits 0 (dry-run validate command syntax).
  </behavior>
  <action>
**Bước 1 — APPEND 2 target mới** TRƯỚC `# === Alembic migration targets ===` (line 40 area):

Tìm chỗ insert (sau `clean` target line 37-38, trước `# === Alembic migration targets ===` comment):

```makefile
clean:
	rm -rf .pytest_cache .mypy_cache .ruff_cache .venv

# === Integration test (Phase 4 v3.1 MIGRATE-01 + MIGRATE-02) ===
# Requires Docker Desktop running (testcontainers pgvector/pgvector:pg16 + redis).
# Auto-spin Postgres + Redis containers — KHÔNG cần TEST_DATABASE_URL env set
# nếu test dùng postgres_container fixture (recommended). Fallback TEST_DATABASE_URL
# nếu chạy ad-hoc test_migration_0006_idempotent.py (Plan 01-03 pattern).

test-integration:
	$(UV) run pytest tests/integration -v -m integration --tb=short

test-migration:
	$(UV) run pytest tests/integration/test_migration_0006_idempotent.py tests/integration/test_migration_upgrade_downgrade.py -v --tb=short

# === Alembic migration targets (Phase 2 Plan 05) ===
```

**Bước 2 — UPDATE `.PHONY` line 2-4** thêm 2 target mới:

REPLACE:
```makefile
.PHONY: install dev keys keys-verify keys-verify-jwt lint test test-cov clean \
        migrate-up migrate-down migrate-check migrate-history cocoindex-setup \
        migrate-all migrate-status hub-init hub-add
```

bằng:
```makefile
.PHONY: install dev keys keys-verify keys-verify-jwt lint test test-cov test-integration test-migration clean \
        migrate-up migrate-down migrate-check migrate-history cocoindex-setup \
        migrate-all migrate-status hub-init hub-add
```

**Bước 3 — Dry-run validate Makefile syntax:**

```bash
cd Hub_All/api && make -n test-migration
```

PHẢI exit 0 + print command `uv run pytest tests/integration/test_migration_0006_idempotent.py tests/integration/test_migration_upgrade_downgrade.py -v --tb=short` (KHÔNG execute).

```bash
cd Hub_All/api && make -n test-integration
```

PHẢI exit 0 + print command `uv run pytest tests/integration -v -m integration --tb=short`.

KHÔNG đụng:
- `test` + `test-cov` targets existing (line 31-35).
- Alembic migration targets (line 40+).
- Per-hub Alembic + hub-init + hub-add targets (line 55-100+).
- Cocoindex-setup target (line 102-107).
- UV variable line 6.
  </action>
  <verify>
    <automated>cd Hub_All/api && make -n test-migration && make -n test-integration && cd ../.. && grep -c "^test-integration:" Hub_All/api/Makefile && grep -c "^test-migration:" Hub_All/api/Makefile && grep -c "test-integration test-migration" Hub_All/api/Makefile</automated>
  </verify>
  <acceptance_criteria>
    - grep `^test-integration:` trong Hub_All/api/Makefile == 1 (target declared).
    - grep `^test-migration:` trong Hub_All/api/Makefile == 1 (target declared).
    - grep `test-integration test-migration` trong Hub_All/api/Makefile == 1 (.PHONY updated).
    - `cd Hub_All/api && make -n test-migration` exits 0 (dry-run validate Makefile syntax).
    - `cd Hub_All/api && make -n test-integration` exits 0.
    - test-integration command chứa `pytest tests/integration -v -m integration --tb=short`.
    - test-migration command chứa `pytest tests/integration/test_migration_0006_idempotent.py tests/integration/test_migration_upgrade_downgrade.py -v --tb=short`.
    - Comment block `# === Integration test (Phase 4 v3.1 MIGRATE-01 + MIGRATE-02) ===` đặt TRƯỚC target.
    - KHÔNG xoá hoặc đổi `test` + `test-cov` existing targets.
    - KHÔNG xoá hoặc đổi Alembic migration targets.
  </acceptance_criteria>
  <done>
    Makefile có 2 target mới + .PHONY updated; dry-run validate syntax PASS; Plan 04-02 + Plan 04-03 reuse `make test-integration` cho smoke E2E + final regression.
  </done>
</task>

<task type="auto">
  <name>Task 2: Chạy make test-migration verify 7 test PASS clean (5 + 2) live testcontainers Postgres pgvector pg16</name>
  <read_first>
    - Hub_All/api/tests/integration/test_migration_0006_idempotent.py (5 test list)
    - Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py (2 test list)
    - Hub_All/api/tests/integration/conftest.py (postgres_container fixture + alembic_cfg fixture)
    - Hub_All/.planning/phases/01-rbac-schema-migration/01-03-PLAN.md (DSN injection SAFETY pattern fix I-01)
  </read_first>
  <behavior>
    - Test 1 (Docker Desktop running): `docker ps` exit 0 (Docker engine available).
    - Test 2 (uv installed): `uv --version` exit 0.
    - Test 3 (Migration test PASS): `cd Hub_All/api && make test-migration` exit 0 + 7 test PASS.
    - Test 4 (Output sanity): output chứa `7 passed` HOẶC `5 passed` + `2 passed` count (per file).
  </behavior>
  <action>
**PRE-CONDITION:**
- Docker Desktop running (testcontainers auto-pull image `pgvector/pgvector:pg16` lần đầu — có thể chậm 30-60s).
- `uv` package manager installed (CLAUDE.md §3 stack pin).

**Bước 1 — Chạy migration test suite (7 test):**

```bash
cd Hub_All/api && make test-migration
```

Expected output sample:
```
uv run pytest tests/integration/test_migration_0006_idempotent.py tests/integration/test_migration_upgrade_downgrade.py -v --tb=short
...
tests/integration/test_migration_0006_idempotent.py::test_check_constraint_accepts_hub_admin PASSED
tests/integration/test_migration_0006_idempotent.py::test_re_run_upgrade_head_idempotent PASSED
tests/integration/test_migration_0006_idempotent.py::test_user_hubs_role_nullable_check_constraint PASSED
tests/integration/test_migration_0006_idempotent.py::test_audit_logs_migration_seed_row_inserted PASSED
tests/integration/test_migration_0006_idempotent.py::test_downgrade_restores_check_3_value PASSED
tests/integration/test_migration_upgrade_downgrade.py::test_upgrade_creates_10_tables PASSED
tests/integration/test_migration_upgrade_downgrade.py::test_downgrade_drops_all_clean PASSED

=========== 7 passed in XX.XXs ===========
```

PHẢI exit 0 + 7 test PASS.

**Bước 2 — Verify failure handling (sanity check):**

Nếu Docker KHÔNG running → testcontainers raise `docker.errors.DockerException` → pytest collect-time skip với clear message. Document trong SUMMARY.md nếu skip.

Nếu `TEST_DATABASE_URL` env var SET (operator override) → Plan 01-03 pattern fixture monkeypatch env + cache_clear PHẢI áp dụng. Nếu fail → check `test_migration_0006_idempotent.py` Iter 1 fix I-01 + I-02 comment.

**Bước 3 — Document live run trong SUMMARY.md:**

Output of `make test-migration` ghi vào SUMMARY.md (last 20 line + duration metric). Nếu fail, document failure root cause + remediation (KHÔNG mark plan complete cho tới khi PASS).

KHÔNG đụng test file (chỉ chạy verify-only).
KHÔNG đụng migration 0006 source code (LOCKED Plan 01-01).
  </action>
  <verify>
    <automated>cd Hub_All/api && make test-migration 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/api && make test-migration` exits 0.
    - Output chứa "7 passed" HOẶC "5 passed" + "2 passed" pattern.
    - KHÔNG có "failed" hoặc "error" trong test summary.
    - Nếu Docker KHÔNG running: document skip rationale trong SUMMARY.md + flag Plan 04-02 cũng cần Docker.
    - Nếu TEST_DATABASE_URL set ad-hoc → fixture pattern I-01 áp dụng PASS.
    - 7 test cover:
      - 5 test `test_migration_0006_idempotent.py` (4 success criteria ROADMAP Phase 1 + DSN SAFETY).
      - 2 test `test_migration_upgrade_downgrade.py` (10 bang baseline + downgrade clean).
  </acceptance_criteria>
  <done>
    7 integration test PASS live testcontainers (Postgres pgvector pg16 + alembic upgrade head + 2-run idempotent + downgrade rollback + audit seed + CHECK 3 value restore + 10 bang baseline + downgrade base clean); Plan 04-02 unblocked với infra verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Makefile target ↔ pytest command | Static text; user trigger `make test-migration` → shell exec. Trust boundary local dev env (KHÔNG production runtime). |
| testcontainers Docker engine ↔ pytest fixture | Docker socket access cần thiết — `postgres_container` fixture spawn container, expose random port. Trust boundary local dev (operator có Docker Desktop quyền). |
| Migration 0006 verify ↔ live Postgres | Test apply migration vào ephemeral testcontainer DB (KHÔNG production). Plan 01-03 DSN SAFETY pattern fix I-01 PHẢI giữ — fixture monkeypatch env + cache_clear, KHÔNG `cfg.set_main_option`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Operator chạy `make test-migration` accidentally với `TEST_DATABASE_URL=<production>` env set → migration apply vào prod DB | mitigate | Plan 01-03 Iter 1 fix I-01 carry forward — fixture monkeypatch env LOCAL scope; KHÔNG persistent env override. Document SUMMARY.md: "KHÔNG chạy `make test-migration` với `TEST_DATABASE_URL=<production>` — fixture postgres_container ưu tiên ephemeral testcontainer." Severity: LOW (dev convention). |
| T-04-01-02 | DoS | testcontainers pull image `pgvector/pgvector:pg16` lần đầu chậm 30-60s + network bandwidth | accept | One-time cost — sau pull cached locally. Operator có network local pull qua Docker Desktop. Severity: VERY LOW. |
| T-04-01-03 | Information Disclosure | Test output stdout chứa DSN passcode (testcontainer auto-gen password) | accept | Testcontainer password ephemeral random — KHÔNG re-use; container teardown clean. Output local dev terminal KHÔNG forward CI logs production. Severity: VERY LOW (dev-only). |
| T-04-01-04 | Repudiation | Plan 04-01 PASS report KHÔNG có audit trail run timestamp | mitigate | SUMMARY.md document output last 20 line + duration + commit SHA. Git log preserve audit chain. Severity: VERY LOW (low-stakes verify). |
</threat_model>

<verification>
- `cd Hub_All/api && make -n test-migration` exits 0 (dry-run syntax check).
- `cd Hub_All/api && make -n test-integration` exits 0.
- `cd Hub_All/api && make test-migration` exits 0 + 7 test PASS.
- `api/Makefile` có 2 target mới + .PHONY updated.
- KHÔNG đụng migration 0006 source code (LOCKED Plan 01-01).
- KHÔNG đụng 2 test file existing (verify-only).
- KHÔNG đụng existing Makefile targets (test, test-cov, migrate-*, hub-init, hub-add, cocoindex-setup).
</verification>

<success_criteria>
1. `api/Makefile` có target `test-integration` chạy `pytest tests/integration -v -m integration --tb=short`.
2. `api/Makefile` có target `test-migration` chạy 2 file migration test với `-v --tb=short`.
3. `.PHONY` line 2 update thêm 2 target mới.
4. `make -n test-migration` + `make -n test-integration` dry-run PASS (syntax valid).
5. `make test-migration` live PASS — 7 test (5 + 2) clean qua testcontainers pgvector pg16.
6. Migration 0006 idempotent verified runtime (upgrade head 2 lần PASS + downgrade -1 PASS + audit seed + CHECK 3 value restore).
7. 10 bang baseline + downgrade base clean verified (regression Phase 2 v2.0).
8. ROADMAP Phase 4 success criterion #1 satisfied — Migration script idempotent + rollback procedure verified.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/04-migration-smoke-e2e/04-01-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 1 file Makefile modify (2 target mới test-integration + test-migration + .PHONY updated)
- 0 file test modify (verify-only — Plan 01-01 + Phase 2 v2.0 LOCKED)
- 7 test PASS live testcontainers (5 + 2 — Migration 0006 idempotent + 10 bang baseline + downgrade clean)
- D-V3.1-Phase4-A + D-V3.1-Phase4-B verified runtime (sa.inspect introspect idempotent + downgrade full implement E-V3.1-1 STOP defensive)
- Plan 04-02 unblocked (`make test-integration` ready cho smoke E2E reuse)
- Document Docker Desktop prerequisite + testcontainers auto-spin pattern

Commit atomic: `feat(04-01): MIGRATE-01 Makefile test-integration + test-migration target + 7 test PASS verify` + body tiếng Việt nói tại sao (Phase 4 milestone closeout cần infra Makefile shortcut + verify migration 0006 idempotent + downgrade rollback PASS live testcontainers; carry forward Plan 01-01 LOCKED migration source code KHÔNG modify; carry forward Plan 01-03 DSN injection SAFETY fix I-01; 7 test cover 5 success criteria ROADMAP Phase 1 + 2 regression Phase 2 v2.0).
</output>
