---
phase: 04
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [MIGRATE-01, MIGRATE-02]  # closeout consolidate
tags: [closeout, docs-update, milestone-shipped, git-tag, v3-1-shipped]
must_haves:
  truths:
    - "STATE.md frontmatter completed_phases=4, completed_plans=15, percent=100, milestone_status=SHIPPED, milestone_shipped_date=2026-05-24, next_action /gsd-complete-milestone v3.1 hoặc /gsd-new-milestone v4.0"
    - "STATE.md body chứa section ## Phase 4 Results Summary (DONE 2026-05-24) với 3 plan bullet + carry forward + 🎉 v3.1 SHIPPED note"
    - "REQUIREMENTS.md 2 dòng [x] **MIGRATE-XX** marked + suffix (DONE 2026-05-24 — Plan 04-XX)"
    - "ROADMAP.md Phase 4 row status DONE + 3 plans checklist [x] + milestones table v3.1 ✅ Shipped 2026-05-24 + progress table v3.1 15/15 plan 15/15 REQ-ID"
    - "CLAUDE.md §6 subsection mới ### Phase 4 v3.1 Migration + Smoke E2E pattern (MIGRATE-01..02 — 2026-05-24) + bump trailing *Cập nhật:* line reflect v3.1 SHIPPED"
    - "Git tag annotated v3.1 LOCAL — git tag -a v3.1 -m '...' (KHÔNG push — operator decide)"
    - "Plan 04-03 SUMMARY.md ship documenting closeout"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "v3.1 milestone progress SHIPPED 100% (15/15 plan, 15/15 REQ-ID)"
      contains: "milestone_status: SHIPPED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MIGRATE-01..02 marked [x] — 15/15 REQ-ID v3.1 consumed"
      contains: "[x] **MIGRATE-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 table row DONE + plans checklist + milestone v3.1 Shipped marker + progress 100%"
      contains: "[x] 04-01-PLAN.md"
    - path: "CLAUDE.md"
      provides: "Phase 4 v3.1 pattern reference + 🎉 v3.1 MILESTONE CLOSED 2026-05-24 trailing line bump"
      contains: "Phase 4 v3.1 Migration + Smoke E2E pattern"
    - path: "GIT_TAG (annotated v3.1 local)"
      provides: "Milestone shipped marker — operator push manual qua git push origin v3.1"
      contains: "v3.1 RBAC hub_admin SHIPPED"
  key_links:
    - from: "STATE.md progress"
      to: "REQUIREMENTS.md MIGRATE-01..02 [x]"
      via: "Cross-reference REQ-ID consumed 15/15 + Phase 4 DONE + milestone SHIPPED"
      pattern: "completed_plans.*15.*percent.*100"
    - from: "CLAUDE.md §6 Phase 4 subsection"
      to: ".planning/phases/04-migration-smoke-e2e/ plan + SUMMARY files"
      via: "Reference paths trong subsection"
      pattern: "04-migration-smoke-e2e"
    - from: "Git tag v3.1"
      to: "CLAUDE.md trailing *Cập nhật:* line"
      via: "SHIPPED 2026-05-24 4 phase / 15 REQ-ID / 15 plan ship marker"
      pattern: "v3.1.*SHIPPED.*2026-05-24"
---

<objective>
Plan 04-03 v3.1 Phase 4 closeout — Wave 3 BLOCKING: 4 docs source-of-truth atomic update (STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md §6) + git tag annotated v3.1 local + Plan 04-03 SUMMARY.md ship documenting v3.1 milestone SHIPPED.

**Purpose:**
- 4 plan Phase 1-4 v3.1 đã ship 15/15 plan / 15/15 REQ-ID (ROLE-01..04 + DEP-01..05 + FE-01..04 + MIGRATE-01..02). Plan 04-03 closeout đánh dấu v3.1 milestone SHIPPED — 4 docs update atomic phản ánh state + git tag annotated cho operator review trước push.
- Carry forward pattern Plan 02-05 + Plan 03-04 v3.1 + Plan 04-07 + 05-06 + 06-05 + 07-05 v3.0 (4 docs + git tag — KHÔNG push).

**Output:**
- `Hub_All/.planning/STATE.md` — frontmatter + body update (Phase 4 Results Summary section + milestone SHIPPED markers).
- `Hub_All/.planning/REQUIREMENTS.md` — 2 dòng MIGRATE-01..02 mark `[x]` + suffix.
- `Hub_All/.planning/ROADMAP.md` — Phase 4 row DONE + 3 plans checklist [x] + Milestones section v3.1 ✅ Shipped + Progress table v3.1 100%.
- `Hub_All/CLAUDE.md` — §6 APPEND subsection mới `### Phase 4 v3.1 Migration + Smoke E2E pattern (MIGRATE-01..02 — 2026-05-24)` + 🎉 v3.1 MILESTONE CLOSED note + bump trailing `*Cập nhật:` line.
- Git tag annotated `v3.1` LOCAL (KHÔNG push — operator decide qua manual `git push origin v3.1` hoặc `/gsd-complete-milestone v3.1` future command).
- `Hub_All/.planning/phases/04-migration-smoke-e2e/04-03-SUMMARY.md` — document closeout.

**Scope:**
- 4 docs atomic update (chained commit chứa cả 4 file + SUMMARY).
- Git tag local (separate from commit — `git tag` operation).
- KHÔNG push tag — operator decide.

**Pattern decisions Claude discretion:**
- Tag message format: multi-line annotated với 4 phase summary + ROADMAP REQ-ID list + bug context reference (memory project_rbac_hub_admin_gap).
- CLAUDE.md trailing line update: APPEND v3.1 SHIPPED note SAU v3.0 SHIPPED note existing (giữ history, KHÔNG xoá).
- Milestones section ROADMAP: update v3.1 row từ `🚧 Started` → `✅ Shipped 2026-05-24, 4 phase / 15 REQ-ID`.
- Skip smoke checkpoint runtime manual visual carry forward Phase 1-3 v3.1 + v3.0 Plan 04-07 + 05-06 + 06-05 + 07-05 precedent (auto-fallback `--chain` mode active).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-CONTEXT.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-PATTERNS.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-01-SUMMARY.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-02-SUMMARY.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-04-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-05-PLAN.md

<interfaces>
<!-- Pattern source-of-truth — Phase 2 + Phase 3 closeout pattern carry forward -->

From Hub_All/.planning/STATE.md frontmatter hiện tại (sau Phase 3 Plan 03-04 ship):
```yaml
phase: 03-frontend-form-refactor (sẽ bump → 04-migration-smoke-e2e)
completed_phases: 3 → 4
completed_plans: 12 → 15
percent: 80 → 100
phase_4_context_status: GATHERED (existing) → DONE
phase_4_done_date: ADD 2026-05-24
milestone_status: STARTED → SHIPPED
milestone_shipped_date: ADD 2026-05-24
next_action: /gsd-plan-phase 4 → /gsd-complete-milestone v3.1 hoặc /gsd-new-milestone v4.0
```

From Hub_All/.planning/REQUIREMENTS.md (MIGRATE-XX hiện tại pattern):
```markdown
- [ ] **MIGRATE-01** Phase 4: Migration smoke test idempotent ...
- [ ] **MIGRATE-02** Phase 4: Smoke E2E 4 scenario qua pytest httpx ...
```

Update:
```markdown
- [x] **MIGRATE-01** Phase 4: Migration smoke test idempotent ... (DONE 2026-05-24 — Plan 04-01)
- [x] **MIGRATE-02** Phase 4: Smoke E2E 4 scenario qua pytest httpx ... (DONE 2026-05-24 — Plan 04-02)
```

From Hub_All/.planning/ROADMAP.md Milestones section line 8-15 (carry forward v3.0 Shipped pattern):
```markdown
- ✅ **v3.0 Multi-Hub Split** — Shipped 2026-05-23 (archive: [`milestones/v3.0-multi-hub-split/`](milestones/v3.0-multi-hub-split/))
- 🚧 **v3.1 RBAC hub_admin** — Started 2026-05-23, 4 phase / 15 REQ-ID, phase numbering reset về 1 (D-V3.1-04)
```

Update v3.1 row:
```markdown
- ✅ **v3.1 RBAC hub_admin** — Shipped 2026-05-24, 4 phase / 15 REQ-ID / 15 plan (defer archive `/gsd-complete-milestone v3.1`)
```

From Hub_All/.planning/ROADMAP.md Plans checklist Phase 4 (line 143-147 area):
```markdown
Plans:
- [ ] 04-01-PLAN.md — Migration idempotent verify + downgrade implement + Alembic test (MIGRATE-01)
- [ ] 04-02-PLAN.md — Smoke E2E 4 scenario pytest httpx + audit log inspect (MIGRATE-02)
- [ ] 04-03-PLAN.md — Closeout v3.1 — CLAUDE.md + STATE.md + ROADMAP.md + REQUIREMENTS.md mark v3.1 SHIPPED + git tag v3.1
```

Update:
```markdown
Plans:
- [x] 04-01-PLAN.md — Migration verify (Makefile test-migration + 7 test PASS testcontainers) (MIGRATE-01) ✅ DONE 2026-05-24
- [x] 04-02-PLAN.md — Smoke E2E 4 scenario pytest httpx + audit forensic chain (MIGRATE-02) ✅ DONE 2026-05-24
- [x] 04-03-PLAN.md — Closeout v3.1 SHIPPED — 4 docs update + git tag v3.1 ✅ DONE 2026-05-24
```

From Hub_All/.planning/ROADMAP.md Progress table (line 170-178 area):
```markdown
| **v3.1 RBAC hub_admin** | **4** | **12/~15** | **13/15** | 🚧 **Phase 3 DONE** | — |
```

Update:
```markdown
| **v3.1 RBAC hub_admin** | **4** | **15/15** | **15/15** | ✅ **SHIPPED 2026-05-24** | 2026-05-24 |
```

From Hub_All/CLAUDE.md §6 cuối file (line 501-503 area — trailing `*Cập nhật:` line v3.0):
```markdown
---

*Cập nhật: 2026-05-23 (Phase 7 DONE — MIGRATE-01..05 ship 5 plan; ... **🎉 v3.0 MILESTONE CLOSED 2026-05-23** ...)*
```

Append BEFORE trailing `---` (line 501) subsection mới `### Phase 4 v3.1 Migration + Smoke E2E pattern (MIGRATE-01..02 — 2026-05-24)` + bump trailing line append v3.1 SHIPPED note.

Git tag annotated v3.1 command:
```bash
git tag -a v3.1 -m "v3.1 RBAC hub_admin SHIPPED 2026-05-24

4 phase / 15 REQ-ID / 15 plan ship:
- Phase 1 ROLE: migration 0006 + helper get_effective_role (3 plan, ROLE-01..04)
- Phase 2 DEP: assert_hub_admin_for + GET /api/hubs filter + CRUD scope + audit (5 plan, DEP-01..05)
- Phase 3 FE: UserRole alias + form 3 option + HubSwitcher + Manage modal disabled (4 plan, FE-01..04)
- Phase 4 MIGRATE: idempotent verify + smoke E2E 4 scenario + audit forensic + closeout (3 plan, MIGRATE-01..02)

Proper fix bug user gán hub_admin vẫn vào central (memory project_rbac_hub_admin_gap).
KHÔNG push — operator decide qua git push origin v3.1 manual hoặc /gsd-complete-milestone v3.1."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 4 docs source-of-truth atomic (STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md §6)</name>
  <read_first>
    - Hub_All/.planning/STATE.md (TOÀN FILE — biết frontmatter + body Phase 1+2+3 Results sections + table v3.1 Planning Summary)
    - Hub_All/.planning/REQUIREMENTS.md (2 dòng `- [ ] **MIGRATE-XX**` cần mark [x] + suffix)
    - Hub_All/.planning/ROADMAP.md (Milestones section + Phase 4 row + plans checklist + Progress table)
    - Hub_All/CLAUDE.md §6 cuối file + trailing `*Cập nhật:` line (line 501-503)
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-01-SUMMARY.md + 04-02-SUMMARY.md (verify Plan 04-01 + 04-02 ship summary cho closeout text)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-05-PLAN.md (reference pattern closeout — file này mirror)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-04-PLAN.md (reference Plan 03-04 closeout pattern — file này mirror cùng cấu trúc)
  </read_first>
  <behavior>
    - Test 1 (STATE frontmatter): grep `completed_phases: 4` trong STATE.md == 1; grep `completed_plans: 15` == 1; grep `percent: 100` == 1; grep `milestone_status: "SHIPPED"` == 1.
    - Test 2 (STATE body): grep `Phase 4 Results Summary` trong STATE.md >= 1.
    - Test 3 (REQUIREMENTS MIGRATE): grep `\\[x\\] \\*\\*MIGRATE-01\\*\\*` trong REQUIREMENTS.md >= 1; grep `\\[x\\] \\*\\*MIGRATE-02\\*\\*` >= 1.
    - Test 4 (ROADMAP plans checklist): grep `\\[x\\] 04-01-PLAN.md` trong ROADMAP.md >= 1; grep `\\[x\\] 04-03-PLAN.md` >= 1.
    - Test 5 (ROADMAP milestone): grep `✅ \\*\\*v3.1 RBAC hub_admin\\*\\* — Shipped 2026-05-24` trong ROADMAP.md >= 1.
    - Test 6 (ROADMAP progress): grep `15/15 plan` HOẶC `15/15.*15/15` trong ROADMAP.md >= 1.
    - Test 7 (CLAUDE.md subsection): grep `### Phase 4 v3.1 Migration . Smoke E2E pattern` trong CLAUDE.md >= 1.
    - Test 8 (CLAUDE.md trailing line bump): grep `🎉 v3.1 MILESTONE CLOSED 2026-05-24` trong CLAUDE.md >= 1.
    - Test 9 (Preservation): grep `### Phase 1 v3.1 RBAC schema pattern` + `### Phase 2 v3.1 RBAC enforcement pattern` + `### Phase 3 v3.1 Frontend form refactor pattern` trong CLAUDE.md >= 3 (Phase 1+2+3 subsections preserve nguyên).
  </behavior>
  <action>
**Update 1: `Hub_All/.planning/STATE.md`** — frontmatter + body update:

1. **Frontmatter**:
   - `last_updated:` → `"2026-05-24T<HH:MM>:00.000Z"` (current timestamp).
   - `status:` → bump `"🎉 v3.1 SHIPPED 2026-05-24 — 4 phase / 15 REQ-ID / 15 plan ship. ..."` (rewrite reflect SHIPPED state).
   - `progress.completed_phases:` → `4` (bump 3 → 4).
   - `progress.completed_plans:` → `15` (bump 12 → 15: 3 Phase 1 + 5 Phase 2 + 4 Phase 3 + 3 Phase 4).
   - `progress.percent:` → `100` (15/15*100 = 100).
   - `milestone_status:` → `"SHIPPED"` (bump "STARTED" → "SHIPPED").
   - `milestone_shipped_date:` → ADD `"2026-05-24"`.
   - `phase_4_context_status:` → giữ `"GATHERED"` (đã set ở `/gsd-discuss-phase 4` 7d3218b).
   - `phase_4_plan_status:` → ADD `"DONE"` (sau Plan 04-03 ship).
   - `phase_4_plan_count:` → ADD `3`.
   - `phase_4_done_date:` → ADD `"2026-05-24"`.
   - `next_action:` → `"/gsd-complete-milestone v3.1 (archive .planning/milestones/v3.1-rbac-hub-admin-archive/) hoặc /gsd-new-milestone v4.0 (Production Hardening + Advanced RAG backlog)"`.

2. **Body table v3.1 Planning Summary** — update Phase 4 row:
   ```
   | 4 | Migration + smoke | Idempotent + rollback + smoke E2E 4 scenario + closeout v3.1 | MIGRATE-01..02 (2) | 2-3 | ✅ DONE 2026-05-24 (3 plan) |
   ```

3. **Body section MỚI** — APPEND SAU `## Phase 3 Results Summary (DONE 2026-05-24)` (TRƯỚC `## Open Question`):

```markdown
## Phase 4 Results Summary (DONE 2026-05-24)

3 plan ship 2 REQ-ID MIGRATE-01..02 — v3.1 milestone CLOSEOUT:

- **Plan 04-01** (MIGRATE-01): `api/Makefile` ADD 2 target mới `test-integration` + `test-migration` shortcut + chạy bắt buộc 7 integration test PASS clean live testcontainers (Postgres pgvector pg16 + Redis): 5 test `test_migration_0006_idempotent.py` (CHECK constraint 4 value + re-run upgrade head 2 lần idempotent + user_hubs.role nullable + audit_logs migration.role_seed + downgrade -1 restore CHECK 3 value) + 2 test `test_migration_upgrade_downgrade.py` regression baseline (10 bang baseline + downgrade base clean). D-V3.1-Phase4-A LOCKED `sa.inspect()` introspect pattern + D-V3.1-Phase4-B LOCKED downgrade() full implement với defensive RuntimeError E-V3.1-1 STOP — verified runtime, KHÔNG sửa migration source (LOCKED Plan 01-01).
- **Plan 04-02** (MIGRATE-02): File mới `api/tests/integration/test_smoke_e2e_v3_1_rbac.py` (~350 LOC) ship 4 scenario async test + `seed_v3_1_rbac_scenario` fixture (seed 2 hub thật dmd + tdt + 4 user qua direct asyncpg + pwdlib Argon2 hash) + 2 helper (`_login_get_token` + `_assert_audit_actor_metadata`). 4 scenario PASS clean: (1) super admin GET /api/hubs → ALL + POST user any hub → 201 + audit `actor_role='admin' + actor_hub_id=NULL`; (2) hub_admin dmd GET /api/hubs → CHỈ dmd + POST user dmd → 201 + POST user tdt → 403 HUB_ADMIN_REQUIRED envelope + audit `actor_role='hub_admin' + actor_hub_id=<dmd-uuid>`; (3) hub_admin tdt mirror (2) với hub tdt; (4) viewer GET /api/documents trong dmd → 200 + POST /api/users → 403 FORBIDDEN. Audit forensic chain D-V3.1-Phase4-E LOCKED verified runtime via direct asyncpg query `payload->>'actor_role'` + `payload->>'actor_hub_id'`. D-V3.1-Phase4-C LOCKED testcontainers in-process pattern carry forward Phase 1 + v3.0 conftest existing.
- **Plan 04-03** (closeout v3.1 SHIPPED): 4 docs source-of-truth atomic update (STATE.md frontmatter + body Phase 4 Results Summary section + REQUIREMENTS.md 2 dòng MIGRATE-XX `[x]` + ROADMAP.md Phase 4 row + plans checklist + Milestones v3.1 ✅ Shipped 2026-05-24 + Progress table 15/15 plan 15/15 REQ-ID + CLAUDE.md §6 APPEND subsection mới Phase 4 v3.1 + bump trailing `*Cập nhật:` line reflect 🎉 v3.1 MILESTONE CLOSED). Git tag annotated `v3.1` LOCAL (KHÔNG push — operator decide qua manual `git push origin v3.1` hoặc `/gsd-complete-milestone v3.1` future command archive `.planning/milestones/v3.1-rbac-hub-admin-archive/`). D-V3.1-Phase4-F LOCKED carry forward Plan 02-05 + 03-04 v3.1 + Plan 04-07/05-06/06-05/07-05 v3.0 closeout pattern.

**🎉 v3.1 MILESTONE SHIPPED 2026-05-24** — 4 phase / 15 REQ-ID / 15 plan ship · ROLE/DEP/FE/MIGRATE · proper fix bug user gán hub_admin vẫn vào central (memory project_rbac_hub_admin_gap 2026-05-23 trigger).

**Carry forward patterns (v3.1 milestone-level):**
- D-V3.1-01 LOCKED giữ tên enum `admin` = super-admin (KHÔNG rename `super_admin`) — carry forward v4.0 per-resource ACL nếu cần extend role enum.
- D-V3.1-02 LOCKED `user_hubs.role` nullable per-hub override pattern — carry forward v4.0 multi-role 1 user trong cùng hub (defer schema migration mới).
- assert_hub_admin_for inline validator pattern (Plan 02-01 D-V3.1-Phase2-D LOCKED) — carry forward future endpoint per-hub gate khi hub_id ở body POST/PATCH.
- 4 BE envelope mới Phase 2 (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + AUTH_STATE_INCONSISTENT) carry forward FE error handling future per-resource granular endpoint.
- Audit payload nest pattern (Plan 02-04 D-V3.1-Phase2-C LOCKED) — carry forward v4.0 per-resource ACL audit (KHÔNG schema migration audit_logs).
- Frontend UserRole single source-of-truth alias (Plan 03-01 D-V3.1-Phase3-D LOCKED) — carry forward v4.0 mọi role mới (vd `editor` per-resource scope).
- HubSwitcher inline component pattern (Plan 03-03) ~80 LOC keep inline; extract `frontend/src/components/HubSwitcher.tsx` defer khi v3.2+ scope phình lên.
- Migration introspect Plan 01-01 + integration test SAFETY DSN injection fix I-01 (Plan 01-03) — carry forward v4.0 future migration verify pattern.
- Smoke E2E 4 scenario testcontainers in-process pattern (Plan 04-02) — carry forward v4.0 future RBAC scenario extend (vd per-resource ACL scenario).
- Closeout 4 docs atomic + git tag annotated local (Plan 02-05 + 03-04 + 04-03) — carry forward v4.0 milestone close pattern.

**R-V3.1-1 HIGH + R-V3.1-2 MEDIUM mitigation chain (v3.1 final):**
- Migration rollback unsafe data loss: Plan 01-01 introspect idempotent + defensive RuntimeError E-V3.1-1 + Plan 04-01 verified runtime 7 test PASS testcontainers.
- Frontend role check bypass: backend filter authoritative (Plan 02-02 D-V3.1-Phase2-A) + assert_hub_admin_for inline (Plan 02-01) + T-02-02-E business logic block role escalation (Plan 02-03) + Manage modal disabled UX defense in depth (Plan 03-03) + Plan 04-02 smoke E2E 4 scenario verified runtime.
- Audit log incomplete: Plan 02-04 build_audit_payload helper + B7 iter 1 future-proof guard api_key_service grep == 0 + MANDATORY docstring + Plan 04-02 audit forensic chain verified runtime.

**Phase 4 backward compat (KHÔNG break M2/v3.0/v3.1 Phase 1+2+3):**
- 1 file mới `test_smoke_e2e_v3_1_rbac.py` ADD — KHÔNG modify existing test suite (200+ unit test regression preserve).
- 1 file Makefile MODIFY append 2 target — KHÔNG đụng `test` + `test-cov` + Alembic + hub-init + hub-add targets existing.
- Migration `0006_role_hub_admin.py` LOCKED — KHÔNG modify (verify-only).
- Backend source code LOCKED Phase 2 + Phase 3 v3.1 ship — Plan 04-02 verify-only via httpx ASGITransport.
- M2 envelope shape `{success, data, error, meta}` LOCKED carry forward v3.0 + v3.1 — Plan 04-02 verify envelope contract enforced.

**Next:** User decide:
- `/gsd-complete-milestone v3.1` — archive `.planning/milestones/v3.1-rbac-hub-admin-archive/` + reset ROADMAP.md cho v4.0 backlog (Production Hardening + Advanced RAG per memory `project_v3_multi_hub_split` seed + HA Redis cluster + OCR Vietnamese + streaming `/api/ask` + coverage >80% + per-resource ACL granular).
- `/gsd-new-milestone v4.0` — start v4.0 milestone (skip archive, fresh discuss-milestone).
- Push git tag `git push origin v3.1` manual (Plan 04-03 chỉ tag local).
```

**Update 2: `Hub_All/.planning/REQUIREMENTS.md`** — mark MIGRATE-01..02 [x]:

Tìm:
```markdown
- [ ] **MIGRATE-01** Phase 4: Migration smoke test idempotent ...
- [ ] **MIGRATE-02** Phase 4: Smoke E2E 4 scenario qua pytest httpx ...
```

REPLACE bằng:
```markdown
- [x] **MIGRATE-01** Phase 4: Migration smoke test idempotent — re-run Alembic upgrade head 2 lần → KHÔNG fail (CHECK constraint already exists detect + skip); rollback Alembic downgrade -1 → restore CHECK constraint cũ 3 value; existing data preserve. (DONE 2026-05-24 — Plan 04-01)
- [x] **MIGRATE-02** Phase 4: Smoke E2E 4 scenario qua pytest httpx — (1) super admin `admin@medinet.vn` thấy ALL hub + CRUD all user; (2) hub_admin assigned `dmd` thấy CHỈ `dmd` + CRUD user trong `dmd`, GET central → 403; (3) hub_admin assigned `tdt` không thấy `dmd` hoặc central; (4) viewer chỉ list documents trong hub được gán. Audit log inspect actor_role + actor_hub_id chính xác. Closeout docs CLAUDE.md + STATE.md + ROADMAP.md mark v3.1 SHIPPED. (DONE 2026-05-24 — Plan 04-02)
```

**Update 3: `Hub_All/.planning/ROADMAP.md`** — 4 thay đổi atomic:

1. **Milestones section** (line 12 area):
   - Tìm: `🚧 **v3.1 RBAC hub_admin** — Started 2026-05-23, 4 phase / 15 REQ-ID, phase numbering reset về 1 (D-V3.1-04)`
   - REPLACE: `✅ **v3.1 RBAC hub_admin** — Shipped 2026-05-24, 4 phase / 15 REQ-ID / 15 plan (defer archive qua /gsd-complete-milestone v3.1; D-V3.1-04 phase numbering reset về 1)`

2. **Phase 4 table row** (line 28 area):
   - Tìm: `| **4** | Migration + smoke E2E (MIGRATE) | ...`
   - REPLACE: `| **4** | Migration + smoke E2E (MIGRATE) ✅ DONE 2026-05-24 | ...`

3. **Phase 4 plans checklist** (line 143-147 area):
   - REPLACE 3 dòng `[ ] 04-0X-PLAN.md` bằng `[x] 04-0X-PLAN.md ... ✅ DONE 2026-05-24` (3 dòng).

4. **Progress table v3.1 row** (line 175 area):
   - Tìm: `| **v3.1 RBAC hub_admin** | **4** | **12/~15** | **13/15** | 🚧 **Phase 3 DONE** | — |`
   - REPLACE: `| **v3.1 RBAC hub_admin** | **4** | **15/15** | **15/15** | ✅ **SHIPPED** | 2026-05-24 |`

**Update 4: `Hub_All/CLAUDE.md` §6 + trailing line bump**:

1. **APPEND subsection mới** trước line 501 `---` separator (sau Phase 3 v3.1 subsection cuối):

```markdown
### Phase 4 v3.1 Migration + Smoke E2E pattern (MIGRATE-01..02 — 2026-05-24) 🎉 v3.1 MILESTONE CLOSED

3 plan ship 2 REQ-ID MIGRATE-01..02 — v3.1 milestone CLOSEOUT:

- **Plan 04-01 MIGRATE-01 — Makefile test-integration + test-migration target + 7 test PASS:** `api/Makefile` APPEND 2 target mới (`test-integration` chạy `pytest tests/integration -v -m integration --tb=short` + `test-migration` chạy 2 file migration test specific) + .PHONY update. Plan 01-01 LOCKED migration `0006_role_hub_admin.py` source code KHÔNG modify — Plan 04-01 verify-only chạy 7 integration test PASS clean live testcontainers Postgres pgvector pg16: 5 test `test_migration_0006_idempotent.py` (CHECK constraint 4 value + re-run upgrade head 2 lần idempotent + user_hubs.role nullable + audit_logs migration.role_seed + downgrade -1 restore CHECK 3 value) + 2 test `test_migration_upgrade_downgrade.py` regression baseline (10 bang baseline + downgrade base clean). D-V3.1-Phase4-A LOCKED `sa.inspect()` introspect pattern + D-V3.1-Phase4-B LOCKED downgrade() full implement với defensive RuntimeError E-V3.1-1 STOP. Reference: `.planning/phases/04-migration-smoke-e2e/04-01-PLAN.md` + `04-01-SUMMARY.md`.

- **Plan 04-02 MIGRATE-02 — Smoke E2E 4 scenario test mới + audit forensic chain:** File mới `api/tests/integration/test_smoke_e2e_v3_1_rbac.py` (~350 LOC) ship 4 scenario async test + `seed_v3_1_rbac_scenario` fixture (seed 2 hub thật `dmd` + `tdt` per memory `project_real_hubs_deployment` + 4 user qua direct asyncpg + `pwdlib.PasswordHash` Argon2 hash cross-compat M2 carry forward) + 2 helper (`_login_get_token` + `_assert_audit_actor_metadata` poll-based timeout 2s carry forward conftest.py `_wait_audit_row` line 778-806). 4 scenario PASS: (1) super admin GET /api/hubs → ALL + POST user → 201 + audit `actor_role='admin' + actor_hub_id=NULL`; (2) hub_admin dmd GET → CHỈ dmd + POST user dmd → 201 + POST user tdt → 403 HUB_ADMIN_REQUIRED envelope + audit `actor_role='hub_admin' + actor_hub_id=<dmd-uuid>`; (3) hub_admin tdt mirror (2); (4) viewer GET /api/documents trong dmd → 200 + POST /api/users → 403 FORBIDDEN. Audit forensic chain D-V3.1-Phase4-E LOCKED verified runtime via direct asyncpg query `SELECT payload->>'actor_role', payload->>'actor_hub_id' FROM audit_logs WHERE action=$1 AND target_type=$2 ORDER BY created_at DESC LIMIT 1`. D-V3.1-Phase4-C LOCKED testcontainers in-process pattern (PostgresContainer pgvector/pgvector:pg16 + RedisContainer + asgi_lifespan LifespanManager + httpx.AsyncClient ASGITransport) carry forward Phase 1 v3.1 + v3.0 conftest existing. Helper composite pattern carry forward `test_dep_hubs_scope.py` (Plan 02-02) + `test_audit_actor_metadata.py` (Plan 02-04). Reference: `04-02-PLAN.md` + `04-02-SUMMARY.md`.

- **Plan 04-03 closeout v3.1 SHIPPED — 4 docs atomic + git tag annotated:** STATE.md frontmatter `completed_phases: 4` + `completed_plans: 15` + `percent: 100` + `milestone_status: SHIPPED` + `milestone_shipped_date: 2026-05-24` + `next_action: /gsd-complete-milestone v3.1 hoặc /gsd-new-milestone v4.0` + body Phase 4 Results Summary section. REQUIREMENTS.md 2 dòng MIGRATE-XX mark `[x]` với suffix. ROADMAP.md Phase 4 row DONE + 3 plans checklist + Milestones v3.1 ✅ Shipped 2026-05-24 + Progress table 15/15 plan 15/15 REQ-ID. CLAUDE.md §6 APPEND subsection mới (file này) + bump trailing `*Cập nhật:` line. Git tag annotated `v3.1` LOCAL via `git tag -a v3.1 -m "v3.1 RBAC hub_admin SHIPPED 2026-05-24..."` (KHÔNG push — operator decide qua manual `git push origin v3.1` hoặc `/gsd-complete-milestone v3.1` future command archive). D-V3.1-Phase4-F LOCKED carry forward Plan 02-05 + 03-04 v3.1 + Plan 04-07 + 05-06 + 06-05 + 07-05 v3.0 closeout 4 docs + git tag pattern. Reference: `04-03-PLAN.md` + `04-03-SUMMARY.md`.

**Architecture insights (Phase 4 v3.1):**

1. **Makefile shortcut `test-integration` + `test-migration`** — DRY shortcut cho operator + CI reuse; `-v --tb=short` verbose + concise traceback; testcontainers auto-spin (KHÔNG cần TEST_DATABASE_URL env nếu dùng `postgres_container` fixture, fallback ad-hoc TEST_DATABASE_URL nếu chạy direct test file).
2. **Migration verify-only pattern** — Plan 04-01 KHÔNG re-implement migration 0006 (LOCKED Plan 01-01 ship 2026-05-23 với introspect + defensive RuntimeError E-V3.1-1) — chỉ chạy 7 test live verify runtime. Pattern aligns minimal scope philosophy v3.1 milestone closeout.
3. **Smoke E2E in-process testcontainers** — KHÔNG real docker compose. Reuse `postgres_container` + `redis_container` session-scoped fixture (1 lần spin-up cho cả test suite) + `hub_app_factory("central")` Phase 2 v3.0 ship + `asgi_lifespan` LifespanManager + `httpx.AsyncClient` ASGITransport. Real backend code path (Plan 02-01..04 + Plan 03-01..03) chạy thật KHÔNG mock.
4. **Audit forensic chain direct asyncpg query** — KHÔNG SQLAlchemy session overhead. `payload->>'actor_role'` + `payload->>'actor_hub_id'` JSONB query pattern Plan 02-04 D-V3.1-Phase2-C LOCKED carry forward. Poll-based timeout 2s xử lý BackgroundTask audit emit fire-and-forget timing (memory `project_fastapi_bgtask_commit` carry forward).
5. **Seed 2 hub thật dmd + tdt** — per memory `project_real_hubs_deployment` (yte/duoc/hcns decorative ghost trong spec/compose; chỉ dmd + tdt thực sự seed trong DB testcontainer). 4 user fixture với UUID runtime generate (KHÔNG hardcode — alembic seed central hub UUID khác nhau per run).
6. **`pwdlib.PasswordHash` Argon2 cross-compat** — M2 Phase 3 ship pwdlib pin params (m=65536, t=1, p=2, saltLen=16, keyLen=32) match Go alexedwards (P6 mitigation R6 cross-compat). Plan 04-02 reuse `PasswordHash.recommended()` singleton.
7. **Closeout 4 docs atomic + git tag local** — Pattern Plan 02-05 + 03-04 + v3.0 Plan 04-07/05-06/06-05/07-05 carry forward. Git tag annotated với multi-line message reference 4 phase + REQ-ID + bug context memory; KHÔNG push tag (operator decide qua manual hoặc future `/gsd-complete-milestone v3.1`).

**🎉 v3.1 MILESTONE CLOSED 2026-05-24** — 4 phase / 15 REQ-ID / 15 plan ship · ROLE/DEP/FE/MIGRATE · proper fix bug user gán hub_admin vẫn vào central (memory `project_rbac_hub_admin_gap` 2026-05-23 trigger). 4 phase × ~3.75 plan avg = 15 plan total — small scope KHÔNG cần anti-pivot split. v3.1-a/v3.1-b split KHÔNG cần (4 phase linear critical path 1 → 2 → 3 → 4 ship suôn sẻ trong 1.5 ngày 2026-05-23 → 2026-05-24).

**Backward compat (Phase 4 v3.1 KHÔNG break v3.0 + Phase 1+2+3):**
- 1 file mới `test_smoke_e2e_v3_1_rbac.py` ADD — KHÔNG modify existing 200+ unit test regression.
- 1 file `Makefile` MODIFY append 2 target — KHÔNG đụng `test` + `test-cov` + Alembic + hub-init + hub-add + cocoindex-setup targets existing.
- Migration `0006_role_hub_admin.py` LOCKED Plan 01-01 — Plan 04-01 verify-only KHÔNG modify.
- Backend source code (routers/users.py + routers/hubs.py + auth/dependencies.py + services/audit_service.py) LOCKED Phase 2 v3.1 ship — Plan 04-02 verify-only via httpx ASGITransport real code path.
- M2 envelope shape `{success, data, error, meta}` LOCKED carry forward v3.0 + v3.1 — Plan 04-02 verify envelope contract enforced (HUB_ADMIN_REQUIRED + FORBIDDEN scenarios).
- Frontend code (UserManagement.tsx + Layout.tsx HubSwitcher + Manage modal + api.ts UserRole alias) LOCKED Phase 3 v3.1 ship — Plan 04-02 KHÔNG đụng (backend smoke E2E scope).

**Next milestone:**
- `/gsd-complete-milestone v3.1` — archive `.planning/milestones/v3.1-rbac-hub-admin-archive/` + reset ROADMAP.md cho v4.0 backlog (Production Hardening + Advanced RAG per memory `project_v3_multi_hub_split` seed 2026-05-21 + HA Redis cluster + OCR Vietnamese + streaming `/api/ask` SSE + coverage >80% + per-resource ACL granular).
- `/gsd-new-milestone v4.0` — fresh milestone start skip archive (defer archive future cleanup).
- `git push origin v3.1` — manual push tag annotated cho remote reference (Plan 04-03 chỉ tag local).
```

2. **Bump trailing `*Cập nhật:` line** (line 503 area):
   - Tìm: `*Cập nhật: 2026-05-23 (Phase 7 DONE — MIGRATE-01..05 ship 5 plan; ... **🎉 v3.0 MILESTONE CLOSED 2026-05-23** ...)*`
   - REPLACE bằng: `*Cập nhật: 2026-05-24 (v3.1 Phase 4 DONE — MIGRATE-01..02 ship 3 plan; api/Makefile 2 target mới test-integration + test-migration + 7 integration test PASS live testcontainers + smoke E2E test mới test_smoke_e2e_v3_1_rbac.py 4 scenario + audit forensic chain payload->>'actor_role' + payload->>'actor_hub_id' verified runtime + closeout 4 docs atomic + git tag annotated v3.1 local. **🎉 v3.1 MILESTONE CLOSED 2026-05-24** — 4 phase / 15 REQ-ID / 15 plan ship · ROLE/DEP/FE/MIGRATE · proper fix bug user gán hub_admin vẫn vào central (memory project_rbac_hub_admin_gap). Project: MEDWIKI. v3.0 MILESTONE CLOSED 2026-05-23 (38/38 plan · 30/30 REQ-ID). M2 v2.0 closeout 2026-05-21 archived. Next: /gsd-complete-milestone v3.1 (archive .planning/milestones/v3.1-rbac-hub-admin-archive/) hoặc /gsd-new-milestone v4.0 (Production Hardening + Advanced RAG backlog per project_v3_multi_hub_split seed + HA Redis cluster + OCR Vietnamese + streaming /api/ask SSE + coverage >80% + per-resource ACL granular).*`

KHÔNG đụng các phần khác (Phase 1+2+3 v3.1 subsections preserve nguyên + v3.0 Phase 1-7 subsections preserve + M2 closeout note line 480-498 preserve).
  </action>
  <verify>
    <automated>grep -c "completed_phases: 4" Hub_All/.planning/STATE.md && grep -c "milestone_status: \"SHIPPED\"" Hub_All/.planning/STATE.md && grep -c "Phase 4 Results Summary" Hub_All/.planning/STATE.md && grep -c "\\[x\\] \\*\\*MIGRATE-01\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\\[x\\] \\*\\*MIGRATE-02\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\\[x\\] 04-01-PLAN.md" Hub_All/.planning/ROADMAP.md && grep -c "✅ \\*\\*v3.1 RBAC hub_admin\\*\\* — Shipped 2026-05-24" Hub_All/.planning/ROADMAP.md && grep -c "Phase 4 v3.1 Migration . Smoke E2E pattern" Hub_All/CLAUDE.md && grep -c "🎉 v3.1 MILESTONE CLOSED 2026-05-24" Hub_All/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter: `completed_phases: 4` + `completed_plans: 15` + `percent: 100` + `milestone_status: "SHIPPED"` + `milestone_shipped_date: "2026-05-24"` + `phase_4_done_date: "2026-05-24"` + `phase_4_plan_status: "DONE"` + `next_action` update reflect /gsd-complete-milestone hoặc /gsd-new-milestone v4.0.
    - STATE.md body: `## Phase 4 Results Summary (DONE 2026-05-24)` section + 3 plan bullet + carry forward + R-V3.1-1/-2 mitigation chain final + 🎉 v3.1 SHIPPED note.
    - REQUIREMENTS.md: 2 dòng `[x] **MIGRATE-XX** ... (DONE 2026-05-24 — Plan 04-XX)`.
    - ROADMAP.md: Milestones v3.1 → `✅ Shipped 2026-05-24`; Phase 4 table row DONE; 3 plans checklist `[x] 04-0X-PLAN.md ✅ DONE 2026-05-24`; Progress table v3.1 → `15/15 plan / 15/15 REQ-ID / ✅ SHIPPED / 2026-05-24`.
    - CLAUDE.md §6: APPEND subsection mới `### Phase 4 v3.1 Migration + Smoke E2E pattern (MIGRATE-01..02 — 2026-05-24) 🎉 v3.1 MILESTONE CLOSED` với 3 plan bullet + architecture insights 7 điểm + backward compat 6 điểm + Next milestone pointer.
    - CLAUDE.md trailing `*Cập nhật:` line bump → `2026-05-24 (v3.1 Phase 4 DONE ... 🎉 v3.1 MILESTONE CLOSED 2026-05-24 ...)`.
    - Phase 1+2+3 v3.1 subsections preserve nguyên (KHÔNG xoá / overwrite).
    - v3.0 Phase 1-7 subsections preserve nguyên.
    - M2 closeout note line 480-498 preserve.
  </acceptance_criteria>
  <done>
    4 docs source-of-truth atomic update phản ánh Phase 4 DONE + v3.1 MILESTONE SHIPPED. Frontmatter + body + table + subsection + trailing line đầy đủ + consistent. v3.1 milestone closeout marker ship cho operator review.
  </done>
</task>

<task type="auto">
  <name>Task 2: Git tag annotated v3.1 LOCAL (KHÔNG push)</name>
  <read_first>
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-CONTEXT.md `<specifics>` "Git tag command" section
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-PATTERNS.md section "9. Git tag annotated v3.1"
    - Hub_All/.planning/STATE.md (sau Task 1 ship — verify milestone_status: SHIPPED frontmatter)
  </read_first>
  <behavior>
    - Test 1 (Tag exists local): `git tag -l v3.1` exit 0 + output chứa `v3.1`.
    - Test 2 (Tag annotated): `git tag -ln v3.1` output chứa "v3.1 RBAC hub_admin SHIPPED 2026-05-24" prefix.
    - Test 3 (KHÔNG push): `git ls-remote --tags origin v3.1` 2>/dev/null KHÔNG có v3.1 (operator decide push manual).
  </behavior>
  <action>
**Bước 1 — Verify HEAD commit reflect Task 1 docs update + Plan 04-01 + Plan 04-02 ship:**

```bash
cd /c/Users/dangs/OneDrive/Máy\ tính/Code/medinet_wiki && git log --oneline -10
```

Expected: HEAD commit là `docs(04-03): closeout Phase 4 v3.1 ...` (sau Task 1 commit).

**Bước 2 — Tag annotated LOCAL** (KHÔNG push):

```bash
git tag -a v3.1 -m "v3.1 RBAC hub_admin SHIPPED 2026-05-24

4 phase / 15 REQ-ID / 15 plan ship:
- Phase 1 ROLE: migration 0006 + helper get_effective_role (3 plan, ROLE-01..04)
- Phase 2 DEP: assert_hub_admin_for + GET /api/hubs filter + CRUD scope + audit (5 plan, DEP-01..05)
- Phase 3 FE: UserRole alias + form 3 option + HubSwitcher + Manage modal disabled (4 plan, FE-01..04)
- Phase 4 MIGRATE: idempotent verify + smoke E2E 4 scenario + audit forensic + closeout (3 plan, MIGRATE-01..02)

Proper fix bug user gán hub_admin vẫn vào central (memory project_rbac_hub_admin_gap 2026-05-23 trigger).
KHÔNG push — operator decide qua git push origin v3.1 manual hoặc /gsd-complete-milestone v3.1 future command."
```

**Bước 3 — Verify tag local:**

```bash
git tag -l v3.1
# Expected output: v3.1

git tag -ln v3.1
# Expected output: v3.1 starts with "v3.1 RBAC hub_admin SHIPPED 2026-05-24..."

git show v3.1 --no-patch | head -20
# Expected: tagger info + multi-line annotated message
```

**Bước 4 — Verify KHÔNG push:**

```bash
git ls-remote --tags origin v3.1 2>/dev/null
# Expected: trống (KHÔNG có v3.1 remote).
```

Nếu output có v3.1 từ trước (pre-existing tag) → operator review trước, KHÔNG override.

**KHÔNG push tag** — operator decide:
- `git push origin v3.1` manual sau review.
- HOẶC defer `/gsd-complete-milestone v3.1` future command (auto archive + push tag).

Document tag command + verify output trong SUMMARY.md.
  </action>
  <verify>
    <automated>git tag -l v3.1 && git tag -ln v3.1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v3.1` exit 0 + output `v3.1`.
    - `git tag -ln v3.1` output chứa `v3.1` + `v3.1 RBAC hub_admin SHIPPED 2026-05-24` prefix.
    - `git show v3.1 --no-patch` output chứa multi-line tagger + annotated message với 4 phase summary.
    - `git ls-remote --tags origin v3.1` 2>/dev/null trống (KHÔNG push).
    - Tag pointer đúng HEAD commit (sau Task 1 docs(04-03) commit).
  </acceptance_criteria>
  <done>
    Git tag annotated v3.1 ship LOCAL + verify output PASS + KHÔNG push. Operator review qua `git tag -ln v3.1` rồi quyết định push manual hoặc defer `/gsd-complete-milestone v3.1`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4 docs source-of-truth ↔ git commit | Docs là markdown + YAML — KHÔNG executable; commit chain immutable preserve audit trail. Trust boundary local repo. |
| Git tag annotated v3.1 ↔ remote origin | Tag local only — KHÔNG push automatic. Operator quyết định push qua manual `git push origin v3.1` hoặc future `/gsd-complete-milestone v3.1` command. Trust boundary operator review. |
| CLAUDE.md §6 subsection mới ↔ Phase 1+2+3 subsections | APPEND-only pattern; KHÔNG xoá / overwrite existing subsections. Trust boundary content preservation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | 4 docs update KHÔNG atomic (commit chỉ 3 trong 4) → inconsistent state STATE/REQUIREMENTS/ROADMAP/CLAUDE | mitigate | Plan 04-03 commit atomic 5 file (4 docs + 04-03-SUMMARY.md) — pattern Plan 02-05 + 03-04 carry forward. Single commit, single rollback unit. Severity: LOW. |
| T-04-03-02 | Repudiation | Git tag annotated v3.1 KHÔNG có tagger info → KHÔNG audit ship author | mitigate | `git tag -a` annotated mode auto-include tagger name + email từ `git config user.name + user.email`. Verify qua `git show v3.1 --no-patch` post-tag. Severity: LOW. |
| T-04-03-03 | Tampering | Operator accidentally push tag v3.1 trước review | mitigate | Plan 04-03 KHÔNG `git push --tags` — chỉ `git tag -a` local. Document trong SUMMARY.md: "KHÔNG push automatic — operator review qua `git tag -ln v3.1` rồi `git push origin v3.1` manual hoặc defer `/gsd-complete-milestone v3.1`." Severity: VERY LOW (dev convention). |
| T-04-03-04 | Tampering | CLAUDE.md trailing line update accidental xóa v3.0 SHIPPED note → loss milestone history | mitigate | Plan 04-03 trailing line update PRESERVE v3.0 SHIPPED note (APPEND v3.1 SHIPPED + giữ v3.0 reference). Verify qua grep `v3.0 MILESTONE CLOSED 2026-05-23` trong CLAUDE.md sau update == 1. Severity: LOW. |
| T-04-03-05 | Information Disclosure | Git tag message expose memory file name `project_rbac_hub_admin_gap` → potential reveal internal note structure | accept | Memory file name public (Claude memory pattern documented). Tag message reference cho audit trail; KHÔNG expose memory content. Severity: VERY LOW. |
</threat_model>

<verification>
- 4 docs ship atomic — grep verify 9 criteria PASS (frontmatter + body + REQ-ID + plans + milestone + subsection + trailing line).
- Phase 1+2+3 v3.1 subsections preserve nguyên trong CLAUDE.md (KHÔNG xoá / overwrite).
- v3.0 Phase 1-7 subsections preserve nguyên.
- M2 closeout note (CLAUDE.md line 480-498) preserve nguyên.
- Git tag v3.1 ship LOCAL với annotated multi-line message.
- KHÔNG push tag — operator decide.
- KHÔNG đụng frontend source code (Plan 03-01..03 ship LOCKED).
- KHÔNG đụng backend source code (Plan 02-01..04 + migration 0006 LOCKED).
- KHÔNG đụng test file (Plan 04-01 + 04-02 ship LOCKED).
</verification>

<success_criteria>
1. STATE.md frontmatter + body update reflect Phase 4 DONE + v3.1 MILESTONE SHIPPED.
2. REQUIREMENTS.md 2 dòng MIGRATE-01..02 mark [x] với suffix Plan reference.
3. ROADMAP.md Phase 4 row + 3 plans checklist + Milestones v3.1 Shipped + Progress table 15/15 100%.
4. CLAUDE.md §6 APPEND subsection mới Phase 4 v3.1 + 7 architecture insights + 6 backward compat + Next milestone pointer + bump trailing *Cập nhật:* line reflect 🎉 v3.1 MILESTONE CLOSED.
5. Git tag annotated v3.1 LOCAL ship (KHÔNG push) — operator review qua git tag -ln v3.1 + push manual.
6. Plan 04-03 SUMMARY.md ship document closeout (4 docs file path + git tag verify output + carry forward Plan 02-05 + 03-04 pattern).
7. 🎉 v3.1 MILESTONE SHIPPED 2026-05-24 — 4 phase / 15 REQ-ID / 15 plan ship · ROLE/DEP/FE/MIGRATE · proper fix bug per memory project_rbac_hub_admin_gap.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/04-migration-smoke-e2e/04-03-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 4 docs source-of-truth atomic update (STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md §6)
- Git tag annotated v3.1 LOCAL — `git tag -ln v3.1` output verify (KHÔNG push, operator decide)
- 🎉 v3.1 MILESTONE SHIPPED 2026-05-24 — 4 phase / 15 REQ-ID / 15 plan ship
- D-V3.1-Phase4-F LOCKED closeout pattern Plan 02-05 + 03-04 v3.1 + Plan 04-07 + 05-06 + 06-05 + 07-05 v3.0 carry forward
- R-V3.1-1 HIGH + R-V3.1-2 MEDIUM mitigation chain final verified
- Phase 4 backward compat preserve M2/v3.0/v3.1 Phase 1+2+3 LOCKED
- Next: /gsd-complete-milestone v3.1 (archive) hoặc /gsd-new-milestone v4.0 (start fresh) HOẶC `git push origin v3.1` manual

Commit atomic: `docs(04-03): closeout Phase 4 v3.1 + 🎉 v3.1 MILESTONE SHIPPED 2026-05-24 + git tag v3.1 local` + body tiếng Việt nói tại sao (v3.1 milestone closeout 4 phase × 15 REQ-ID × 15 plan ship — proper fix bug user gán hub_admin vẫn vào central per memory project_rbac_hub_admin_gap; 4 docs atomic carry forward Plan 02-05 + 03-04 + v3.0 Plan 04-07 + 05-06 + 06-05 + 07-05 closeout pattern; git tag annotated local KHÔNG push, operator decide; v3.1 backward compat preserve v3.0 + Phase 1+2+3 LOCKED).
</output>
