---
phase: 05-document-version-history
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - api/app/routers/document_versions.py
  - api/app/main.py
  - api/tests/unit/test_document_versions_router.py
autonomous: true
requirements: [VER-03, VER-04]
tags: [backend, router, fastapi, version-history, rbac, hub-isolation, streaming-response, audit]
must_haves:
  truths:
    - "File mới `api/app/routers/document_versions.py` export 4 endpoint với prefix '/api/documents' tags ['Document Versions']"
    - "GET /api/documents/{document_id}/versions → 200 envelope {success: true, data: {versions: DocumentVersionAPI[]}, error: null, meta: null} order DESC"
    - "GET /api/documents/{document_id}/versions/{version_id} → 200 envelope {data: {version: DocumentVersionAPI, chunks: []}} (D-V3.1-Phase5-B empty array)"
    - "GET /api/documents/{document_id}/versions/{version_id}/file → StreamingResponse với media_type từ version.file_type + Content-Disposition: attachment; filename=v{N}_{name}"
    - "POST /api/documents/{document_id}/versions/{version_id}/restore → 200 envelope {data: DocumentAPI refreshed} + audit document.version.restore emit"
    - "RBAC 3 GET endpoint: Depends(get_current_user_for_hub_access) Layer 3 SSO-04 (Plan 03-03 v3.0 carry forward — viewer + editor + hub_admin + super admin PASS D-V3.1-Phase5-C LOCKED)"
    - "RBAC POST /restore endpoint: Depends(get_current_user) + inline `await assert_hub_admin_for(user=, db=, target_hub_id=document.hub_id)` SAU resolve document (Plan 02-01 v3.1 carry forward — viewer + cross-hub hub_admin reject 403 HUB_ADMIN_REQUIRED)"
    - "Router mount UNIVERSAL ở main.py::create_app SAU documents_router (KHÔNG central-only — documents per-hub data carry forward FACTOR-01 v3.0)"
    - "10 unit test PASS — cover 4 endpoint × happy path + RBAC reject + envelope shape + StreamingResponse mime type + restore audit emit"
  artifacts:
    - path: "api/app/routers/document_versions.py"
      provides: "VER-03 router 4 endpoint exact match frontend api.ts:268-285 URL + envelope shape"
      contains: "@router.get(\"/{document_id}/versions\""
    - path: "api/app/routers/document_versions.py"
      provides: "VER-04 RBAC 3 GET Layer 3 + POST restore assert_hub_admin_for inline"
      contains: "assert_hub_admin_for"
    - path: "api/app/main.py"
      provides: "Universal mount document_versions_router SAU documents_router"
      contains: "include_router(document_versions_router)"
    - path: "api/tests/unit/test_document_versions_router.py"
      provides: "10 unit test cover 4 endpoint × happy + RBAC reject + envelope + audit"
      contains: "async def test_"
  key_links:
    - from: "document_versions.py GET /versions"
      to: "frontend api.listDocumentVersions(id) call (api.ts:269-271)"
      via: "URL exact match /api/documents/{id}/versions + envelope {success, data: {versions: [...]}}"
      pattern: "@router.get.*\"/\\{document_id\\}/versions\""
    - from: "document_versions.py GET /versions/{vid}"
      to: "frontend api.getDocumentVersion(docId, versionId) call (api.ts:273-277)"
      via: "URL exact match + envelope {data: {version, chunks: []}} (D-V3.1-Phase5-B)"
      pattern: "chunks.*\\[\\]"
    - from: "document_versions.py GET /versions/{vid}/file"
      to: "frontend api.getDocumentVersionFileUrl (api.ts:279-281)"
      via: "StreamingResponse + Content-Disposition attachment filename=v{N}_{name}"
      pattern: "StreamingResponse|FileResponse"
    - from: "document_versions.py POST /versions/{vid}/restore"
      to: "Plan 05-02 document_version_service.restore_to_version"
      via: "await call truyền session + document_id + version_id + actor_user_id + actor_role + actor_hub_id"
      pattern: "restore_to_version"
    - from: "document_versions.py POST /restore RBAC"
      to: "Plan 02-01 assert_hub_admin_for(user, db, target_hub_id=document.hub_id)"
      via: "Inline check SAU resolve document.hub_id từ DB (D-V3.1-Phase5-C LOCKED)"
      pattern: "await assert_hub_admin_for"
    - from: "main.py create_app"
      to: "include_router(document_versions_router) universal SAU documents_router"
      via: "Mount cùng level documents_router — KHÔNG central-only block"
      pattern: "include_router\\(document_versions_router\\)"
---

<objective>
Plan 05-03 v3.1 Phase 5 VER-03 + VER-04 — Wave 3 BLOCKING router `document_versions.py` 4 endpoint + RBAC 3-layer integration + universal mount main.py + 10 unit test PASS.

**Purpose (VER-03 + VER-04 + D-V3.1-Phase5-C/I LOCKED):** Plan 05-01 schema + Plan 05-02 service ready. Plan 05-03 build router 4 endpoint exact match frontend `api.ts:268-285` URL + envelope shape (R-V3-2 minimal scope FE KHÔNG đụng). Mount universal main.py để hub con + central đều serve. Plan 05-04 integration test E2E verify scenario thật.

**4 endpoint match exact api.ts:268-285:**
1. `GET /api/documents/{document_id}/versions` → list versions DESC (viewer + editor + hub_admin + super PASS).
2. `GET /api/documents/{document_id}/versions/{version_id}` → detail + `chunks: []` empty (D-V3.1-Phase5-B).
3. `GET /api/documents/{document_id}/versions/{version_id}/file` → StreamingResponse + Content-Disposition `attachment; filename=v{N}_{name}` (FE handle download).
4. `POST /api/documents/{document_id}/versions/{version_id}/restore` → rollback + audit emit (hub_admin + super PASS; viewer + cross-hub hub_admin 403 HUB_ADMIN_REQUIRED).

**RBAC 3-layer (D-V3.1-Phase5-C LOCKED + VER-04):**
- **Layer 1:** `_enforce_hub_dsn_match` Settings validator boot-time (Plan 04-04 v3.0 carry forward — hub con DB isolation).
- **Layer 2:** Repository-level filter — Plan 05-02 service KHÔNG cross-hub query (snapshot/list dùng document_id; document.hub_id check ở Layer 3 router).
- **Layer 3:**
  - 3 GET endpoint: `Depends(get_current_user_for_hub_access)` Layer 3 SSO-04 (Plan 03-03 v3.0 carry forward — JWT.hub_ids check `document.hub_id ∈ user.hub_ids`).
  - POST /restore: `Depends(get_current_user)` + inline `await assert_hub_admin_for(user=user, db=db, target_hub_id=document.hub_id)` SAU resolve document từ DB (Plan 02-01 v3.1 hybrid pattern carry forward).

**D-V3.1-Phase5-I LOCKED re-extract SYNC:** POST /restore router gọi `document_version_service.restore_to_version()` sync block (< 5s small DOCX); cocoindex re-extract trigger nếu app.state.cocoindex_app tồn tại — gọi blocking trong handler (KHÔNG BackgroundTask). Large file > 10MB defer v4.0 async queue.

**Output:**
- 1 file mới `api/app/routers/document_versions.py` (~250 LOC) — 4 endpoint + import contract.
- 1 file edit `api/app/main.py` — ADD 2 line: `from app.routers.document_versions import router as document_versions_router` + `app.include_router(document_versions_router)` SAU line 817 `documents_router`.
- 1 file mới `api/tests/unit/test_document_versions_router.py` (~350 LOC) — 10 unit test cover 4 endpoint × happy path + RBAC + envelope shape + audit emit.

**Carry forward strategy:**
- Pattern Plan 02-01 v3.1 `assert_hub_admin_for` (hybrid validator) — POST /restore inline check sau body parse + resolve document.
- Pattern Plan 03-03 v3.0 `get_current_user_for_hub_access` Layer 3 SSO-04 — 3 GET endpoint.
- Envelope M2 LOCKED `{success, data, error, meta}` shape — KHÔNG đụng response builder.
- Universal mount pattern Plan 02 v3.0 FACTOR-01 `documents_router` carry forward.
- `app/pkg/response.py` success_envelope helper (M2 carry forward — TBD verify import path).
- D-V3.1-Phase5-B `chunks: []` empty array — FE typecheck happy (Plan 05-02 `get_version_with_chunks` đã trả `(dict, [])`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md
@Hub_All/.planning/phases/05-document-version-history/05-01-PLAN.md
@Hub_All/.planning/phases/05-document-version-history/05-02-PLAN.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/routers/documents.py
@Hub_All/api/app/main.py
@Hub_All/api/app/auth/dependencies.py
@Hub_All/api/app/services/document_version_service.py
@Hub_All/api/app/services/documents_service.py
@Hub_All/frontend/src/services/api.ts
@Hub_All/frontend/src/components/DocumentVersionHistory.tsx

<interfaces>
<!-- Pattern source-of-truth — Plan 05-02 service + dependencies.py + main.py + api.ts. Executor dùng trực tiếp. -->

Plan 05-02 service API contract (api/app/services/document_version_service.py):
```python
async def snapshot(*, session, document, change_type, change_note=None, actor_user_id=None,
                   actor_role='admin', actor_hub_id=None) -> dict
async def restore_to_version(*, session, document_id, version_id, actor_user_id=None,
                              actor_role='admin', actor_hub_id=None) -> dict
async def list_versions(session, document_id) -> list[dict]
async def get_version_with_chunks(session, document_id, version_id) -> tuple[dict | None, list]
async def get_version_file_path(session, document_id, version_id) -> Path | None
```

dependencies.py API (api/app/auth/dependencies.py — Plan 02-01 + Plan 03-03 v3.0):
```python
async def get_current_user(...) -> User  # JWT verify + DB lookup
async def get_current_user_for_hub_access(request, user=Depends(get_current_user)) -> User
    # Layer 3 SSO-04 — JWT.hub_ids check; raise 403 CROSS_HUB_ACCESS_DENIED nếu mismatch
async def assert_hub_admin_for(*, user, db, target_hub_id: str) -> None
    # Hybrid validator (KHÔNG Depends factory) — inline call sau body parse
    # Raise 403 detail={'code': 'HUB_ADMIN_REQUIRED', 'message': '...'}
```

main.py pattern (api/app/main.py:810-852):
```python
# Line 810 — router imports
from app.routers.documents import router as documents_router
# ...

# Line 815-822 — universal mount
def create_app() -> FastAPI:
    app = FastAPI(...)
    app.include_router(auth_router)
    app.include_router(documents_router)  # ← Plan 05-03 ADD document_versions_router SAU dòng này
    app.include_router(profile_router)
    app.include_router(search_router)
    app.include_router(ask_router)
    app.include_router(usage_router)
    app.include_router(ai_chat_router)

# Line 843-865 — CENTRAL-ONLY block (Plan 05-03 KHÔNG mount ở đây — documents per-hub data)
if settings.hub_name == "central":
    app.include_router(hubs_router)
    app.include_router(users_router)
    # ...
```

documents.py pattern reference (api/app/routers/documents.py — endpoint structure):
```python
from fastapi import APIRouter, Depends, HTTPException, Request, status
from fastapi.responses import StreamingResponse
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.dependencies import get_current_user, get_current_user_for_hub_access
from app.db.session import get_session
from app.models.auth import User

router = APIRouter(prefix="/api/documents", tags=["Documents"])

@router.get("/{document_id}/file", response_model=None)
async def get_file(
    document_id: UUID,
    user: User = Depends(get_current_user_for_hub_access),
    db: AsyncSession = Depends(get_session),
):
    # ... resolve document + check ownership + StreamingResponse
```

Frontend consumer contract (frontend/src/services/api.ts:268-285):
```typescript
async listDocumentVersions(id: string) {
    return this.request<{ versions: DocumentVersionAPI[] }>('GET', `/api/documents/${id}/versions`);
}
async getDocumentVersion(docId: string, versionId: string) {
    return this.request<{ version: DocumentVersionAPI; chunks: DocumentVersionChunkAPI[] }>(
      'GET', `/api/documents/${docId}/versions/${versionId}`,
    );
}
getDocumentVersionFileUrl(docId: string, versionId: string): string {
    return `${this.baseURL}/api/documents/${docId}/versions/${versionId}/file`;
}
async restoreDocumentVersion(docId: string, versionId: string) {
    return this.request<DocumentAPI>('POST', `/api/documents/${docId}/versions/${versionId}/restore`);
}
```

Envelope M2 shape (frontend/src/services/api.ts request wrapper expects):
```typescript
{
  success: boolean,
  data: T | null,         // T = response_model shape
  error: { code: string, message: string } | null,
  meta: any | null,
}
```

success_envelope helper (TBD verify exact import — likely `app/pkg/response.py`):
```python
# Pattern carry forward — verify exact import qua grep:
# grep -rn "def success_envelope\|def success_response\|envelope_success" Hub_All/api/app/
# Likely usage:
from app.pkg.response import success_envelope
return success_envelope({"versions": [...]})
# OR fallback if helper doesn't exist:
return JSONResponse({"success": True, "data": {...}, "error": None, "meta": None})
```

FE DocumentVersionHistory.tsx download URL build (frontend/src/components/DocumentVersionHistory.tsx:79):
```typescript
// Filename pattern FE expect: v{version_number}_{v.name}
const a = document.createElement('a');
a.href = api.getDocumentVersionFileUrl(documentId, v.id);
a.download = `v${v.version_number}_${v.name}`;
```

→ BE Content-Disposition SHOULD match: `attachment; filename="v{version_number}_{name}"` (FE override với attribute `download={...}` but BE header best-practice match).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo file document_versions.py — 4 endpoint + RBAC integration (VER-03 + VER-04)</name>
  <read_first>
    - Hub_All/api/app/services/document_version_service.py (state sau Plan 05-02 — verify 5 public API signature exact)
    - Hub_All/api/app/auth/dependencies.py (line 89-460 — get_current_user + get_current_user_for_hub_access + assert_hub_admin_for signature)
    - Hub_All/api/app/db/session.py (verify exact dependency factory `get_session` — FastAPI Depends inject AsyncSession; KHÔNG đụng `async_session_factory` private symbol)
    - Hub_All/api/app/routers/documents.py:81-450 (pattern endpoint với Depends + AsyncSession + StreamingResponse + response envelope)
    - Hub_All/api/app/services/documents_service.py:128-450 (DocumentService class — verify document resolve API; `_DOC_COLUMNS` + `_row_to_document` helper nếu tồn tại)
    - Hub_All/frontend/src/services/api.ts:268-285 (4 URL + envelope shape exact)
    - Hub_All/frontend/src/components/DocumentVersionHistory.tsx:75-110 (verify FE handleDownload + handleRestore behavior expect)
    - Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md `<decisions>` D-V3.1-Phase5-C/D/I (RBAC viewer + restore semantics + sync re-extract)
  </read_first>
  <behavior>
    - Test 1 (router_export_4_endpoint): grep `@router.get("/{document_id}/versions"` + `@router.get("/{document_id}/versions/{version_id}"` + `@router.get("/{document_id}/versions/{version_id}/file"` + `@router.post("/{document_id}/versions/{version_id}/restore"` >= 1 mỗi cái.
    - Test 2 (router_prefix_tags): grep `APIRouter(prefix="/api/documents", tags=["Document Versions"])`.
    - Test 3 (rbac_3_get_uses_hub_access): 3 GET endpoint dùng `Depends(get_current_user_for_hub_access)` (Layer 3 SSO-04).
    - Test 4 (rbac_restore_inline_hub_admin): POST /restore handler chứa `await assert_hub_admin_for(user=user, db=db, target_hub_id=` (inline SAU resolve document).
    - Test 5 (chunks_empty_array): GET detail handler return chứa `"chunks": []` HOẶC `chunks=[]` (D-V3.1-Phase5-B verify).
    - Test 6 (streaming_response_file): GET file handler return `StreamingResponse` HOẶC `FileResponse` với `Content-Disposition: attachment; filename=v{N}_{name}` pattern.
    - Test 7 (restore_calls_service): POST /restore handler chứa `await document_version_service.restore_to_version(`.
    - Test 8 (envelope_success_shape): GET list handler return chứa `"success": True, "data":` + `"versions":` nested (M2 envelope).
    - Test 9 (audit_emit_in_service_not_router): router KHÔNG gọi `enqueue_audit` trực tiếp (audit emit responsibility ở service layer Plan 05-02).
    - Test 10 (404_when_version_missing): GET detail/file handler raise HTTPException 404 khi `get_version_with_chunks/get_version_file_path` trả None.
  </behavior>
  <action>
**Bước 1 — Tạo file `Hub_All/api/app/routers/document_versions.py`:**

```python
"""document_versions.py — Phase 5 Plan 05-03 VER-03 + VER-04 router.

4 endpoint exact match frontend api.ts:268-285:
- GET    /api/documents/{document_id}/versions                       → list DESC
- GET    /api/documents/{document_id}/versions/{version_id}          → detail + chunks=[]
- GET    /api/documents/{document_id}/versions/{version_id}/file     → StreamingResponse
- POST   /api/documents/{document_id}/versions/{version_id}/restore  → rollback + audit

Envelope M2 LOCKED: {success: true, data: {...}, error: null, meta: null}

RBAC 3-layer (D-V3.1-Phase5-C LOCKED):
- Layer 1: Settings _enforce_hub_dsn_match (Plan 04-04 v3.0 carry forward boot-time).
- Layer 2: Repository per-hub filter ở service layer (Plan 05-02 — query qua document_id;
  hub isolation enforce ở Layer 3).
- Layer 3:
  * 3 GET endpoint: Depends(get_current_user_for_hub_access) — JWT.hub_ids check
    (Plan 03-03 v3.0 carry forward SSO-04). Viewer + editor + hub_admin + super
    admin all PASS.
  * POST /restore endpoint: Depends(get_current_user) + inline assert_hub_admin_for
    sau resolve document.hub_id (Plan 02-01 v3.1 hybrid pattern). Viewer + cross-hub
    hub_admin reject 403 HUB_ADMIN_REQUIRED envelope.

D-V3.1-Phase5-I LOCKED: POST /restore SYNC block (< 5s small DOCX). Cocoindex re-extract
trigger TBD — if app.state.cocoindex_app tồn tại + service expose API thì gọi blocking;
else skip + document for v4.0 follow-up.

Audit emit RESPONSIBILITY ở service layer (Plan 05-02 snapshot + restore_to_version):
- POST /restore → service emit 2 action ('document.version.create' restore version
  + 'document.version.restore' rollback). Router KHÔNG gọi enqueue_audit trực tiếp.

Mount: UNIVERSAL ở main.py::create_app SAU documents_router (KHÔNG central-only —
documents per-hub data; carry forward FACTOR-01 v3.0 Phase 2 pattern).
"""
from __future__ import annotations

import logging
from typing import Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, Request, status
from fastapi.responses import StreamingResponse
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.dependencies import (
    assert_hub_admin_for,
    get_current_user,
    get_current_user_for_hub_access,
)
from app.db.session import get_session
from app.models.auth import User
from app.services import document_version_service

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/documents", tags=["Document Versions"])


# === Helper: success envelope ===

def _success_envelope(data: Any) -> dict[str, Any]:
    """M2 LOCKED envelope shape — try import shared helper, else inline construct.

    Pattern carry forward documents.py + auth.py + other routers M2 baseline.
    Try import `app.pkg.response.success_envelope` first; fallback nếu KHÔNG tồn tại.
    """
    try:
        from app.pkg.response import success_envelope  # type: ignore[import-not-found]
        return success_envelope(data)
    except ImportError:
        # Fallback shape M2 LOCKED — manual construct.
        return {"success": True, "data": data, "error": None, "meta": None}


# === Helper: resolve document hub_id (raw SQL — KHÔNG ORM mới) ===

async def _resolve_document_hub_id(
    db: AsyncSession,
    document_id: UUID,
) -> tuple[str, str | None]:
    """SELECT id, hub_id FROM documents WHERE id = :id LIMIT 1.

    Returns:
        (document_id_str, hub_id_str | None nếu KHÔNG có hub_id).

    Raises:
        HTTPException(404) nếu document KHÔNG tồn tại.
    """
    result = await db.execute(
        text("SELECT id, hub_id FROM documents WHERE id = :id LIMIT 1"),
        {"id": str(document_id)},
    )
    row = result.fetchone()
    if not row:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail={
                "code": "DOCUMENT_NOT_FOUND",
                "message": f"Document {document_id!r} KHÔNG tồn tại",
            },
        )
    return str(row[0]), (str(row[1]) if row[1] else None)


# === Endpoint 1: GET /versions list ===

@router.get("/{document_id}/versions", response_model=None)
async def list_document_versions(
    document_id: UUID,
    user: User = Depends(get_current_user_for_hub_access),  # noqa: B008 — FastAPI pattern
    db: AsyncSession = Depends(get_session),                # noqa: B008
) -> dict[str, Any]:
    """GET versions list — viewer + editor + hub_admin + super admin PASS.

    Hub isolation: Layer 3 SSO-04 (Plan 03-03 v3.0 — JWT.hub_ids check document.hub_id).

    Returns:
        Envelope M2: {success: true, data: {versions: DocumentVersionAPI[]}, error: null, meta: null}
        Order: DESC by version_number (≤ 5 row sau retention prune D-V3.1-Phase5-E).
    """
    # Resolve document (404 nếu missing) — KHÔNG check hub_id explicit vì
    # get_current_user_for_hub_access đã enforce JWT.hub_ids ⊇ {settings.hub_name}.
    await _resolve_document_hub_id(db, document_id)

    versions = await document_version_service.list_versions(db, str(document_id))
    return _success_envelope({"versions": versions})


# === Endpoint 2: GET /versions/{vid} detail ===

@router.get("/{document_id}/versions/{version_id}", response_model=None)
async def get_document_version_detail(
    document_id: UUID,
    version_id: UUID,
    user: User = Depends(get_current_user_for_hub_access),  # noqa: B008
    db: AsyncSession = Depends(get_session),                # noqa: B008
) -> dict[str, Any]:
    """GET version detail + chunks empty array (D-V3.1-Phase5-B LOCKED).

    chunks = [] empty — FE typecheck happy (DocumentVersionChunkAPI interface
    declared nhưng BE KHÔNG snapshot chunks; restore re-extract sẽ regenerate).
    """
    await _resolve_document_hub_id(db, document_id)

    version_dict, chunks = await document_version_service.get_version_with_chunks(
        db, str(document_id), str(version_id),
    )
    if version_dict is None:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail={
                "code": "VERSION_NOT_FOUND",
                "message": f"Version {version_id!r} KHÔNG thuộc document {document_id!r}",
            },
        )
    return _success_envelope({"version": version_dict, "chunks": chunks})  # D-V3.1-Phase5-B: chunks=[]


# === Endpoint 3: GET /versions/{vid}/file binary ===

@router.get("/{document_id}/versions/{version_id}/file", response_model=None)
async def download_document_version_file(
    document_id: UUID,
    version_id: UUID,
    user: User = Depends(get_current_user_for_hub_access),  # noqa: B008
    db: AsyncSession = Depends(get_session),                # noqa: B008
) -> StreamingResponse:
    """GET binary file — StreamingResponse + Content-Disposition attachment.

    FE handle download qua `<a download={...}>` attribute (DocumentVersionHistory.tsx:79
    pattern `v{version_number}_{v.name}`). BE Content-Disposition match best-practice
    nhưng FE override.

    Hub isolation Layer 3 đủ — KHÔNG cần thêm role check.
    """
    await _resolve_document_hub_id(db, document_id)

    # Resolve version metadata cho mime_type + name + version_number
    version_dict, _chunks = await document_version_service.get_version_with_chunks(
        db, str(document_id), str(version_id),
    )
    if version_dict is None:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail={
                "code": "VERSION_NOT_FOUND",
                "message": f"Version {version_id!r} KHÔNG thuộc document {document_id!r}",
            },
        )

    file_path = await document_version_service.get_version_file_path(
        db, str(document_id), str(version_id),
    )
    if file_path is None or not file_path.exists():
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail={
                "code": "VERSION_FILE_MISSING",
                "message": f"File version {version_id!r} KHÔNG tồn tại trên đĩa (storage cleanup may have removed)",
            },
        )

    download_filename = f"v{version_dict['version_number']}_{version_dict['name']}"
    media_type = version_dict.get("file_type") or "application/octet-stream"

    # RFC 6266 — filename* UTF-8 percent-encoded để xử lý dấu tiếng Việt an toàn
    # (header ASCII-only; tên file có ký tự non-ASCII phải percent-encode hoặc
    # browser sẽ fallback hỏng). FE `<a download>` attribute override user-visible
    # name; BE header chính tắc cho cURL/wget/non-FE consumer.
    from urllib.parse import quote
    encoded_filename = quote(download_filename)
    return StreamingResponse(
        file_path.open("rb"),
        media_type=media_type,
        headers={
            "Content-Disposition": f"attachment; filename*=UTF-8''{encoded_filename}",
        },
    )


# === Endpoint 4: POST /versions/{vid}/restore ===

@router.post("/{document_id}/versions/{version_id}/restore", response_model=None)
async def restore_document_to_version(
    document_id: UUID,
    version_id: UUID,
    request: Request,
    user: User = Depends(get_current_user),  # noqa: B008 — hybrid pattern KHÔNG hub_access
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> dict[str, Any]:
    """POST restore — hub_admin + super admin PASS; viewer + cross-hub → 403.

    D-V3.1-Phase5-C LOCKED: POST /restore mutation requires hub_admin OR super.
    D-V3.1-Phase5-D LOCKED: append-only (snapshot TRƯỚC khi UPDATE — service layer).
    D-V3.1-Phase5-I LOCKED: SYNC block endpoint (< 5s small DOCX).

    Steps:
    1. Resolve document.hub_id từ DB (404 nếu missing).
    2. Inline assert_hub_admin_for(user, db, target_hub_id=document.hub_id) —
       reject 403 HUB_ADMIN_REQUIRED nếu viewer hoặc cross-hub hub_admin.
    3. Call service restore_to_version — atomic snapshot + UPDATE documents + audit.
    4. (Optional) Trigger cocoindex re-extract sync nếu app.state.cocoindex_app tồn tại.
    5. Return envelope refreshed document.

    Audit emit RESPONSIBILITY ở service layer (Plan 05-02) — router KHÔNG enqueue_audit.
    """
    # 1) Resolve document + extract hub_id
    doc_id_str, doc_hub_id = await _resolve_document_hub_id(db, document_id)

    # 2) Inline RBAC check (Plan 02-01 v3.1 hybrid pattern)
    if doc_hub_id is None:
        # Document KHÔNG có hub_id (legacy orphan) — reject 400 fail-loud.
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail={
                "code": "DOCUMENT_HUB_MISSING",
                "message": f"Document {document_id!r} KHÔNG có hub_id — KHÔNG thể determine RBAC scope",
            },
        )

    await assert_hub_admin_for(user=user, db=db, target_hub_id=doc_hub_id)

    # 3) Derive actor metadata (Plan 02-04 v3.1 pattern) — narrow tường minh để
    # tránh trường hợp assert_hub_admin_for có edge case cho viewer/editor (defense
    # in depth: chỉ admin hoặc hub_admin được tới đây).
    if user.role == "admin":
        actor_role, actor_hub_id = "admin", None
    elif user.role == "hub_admin":
        actor_role, actor_hub_id = "hub_admin", doc_hub_id
    else:
        # assert_hub_admin_for SHOULD reject role khác — defense in depth raise
        # nếu logic upstream regress, KHÔNG silently mislabel viewer là hub_admin.
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={
                "code": "HUB_ADMIN_REQUIRED",
                "message": f"Role {user.role!r} không có quyền restore (defense in depth)",
            },
        )

    # 4) Call service restore (append-only D-V3.1-Phase5-D + audit emit D-V3.1-Phase5-H)
    refreshed = await document_version_service.restore_to_version(
        session=db,
        document_id=doc_id_str,
        version_id=str(version_id),
        actor_user_id=str(user.id),
        actor_role=actor_role,
        actor_hub_id=actor_hub_id,
    )

    # 5) Trigger cocoindex re-extract SYNC (D-V3.1-Phase5-I) — best effort, KHÔNG fail nếu missing
    cocoindex_app = getattr(request.app.state, "cocoindex_app", None)
    if cocoindex_app is not None:
        try:
            # Cocoindex flow trigger sync — verify API qua existing trigger_cocoindex_update
            # documents.py pattern (line 162) dùng BackgroundTasks; restore D-V3.1-Phase5-I LOCKED
            # SYNC block (< 5s small DOCX). Try sync `cocoindex_app.update_blocking()` nếu API có.
            if hasattr(cocoindex_app, "update_blocking"):
                cocoindex_app.update_blocking()
            else:
                logger.info(
                    "restore_skip_reextract: cocoindex_app missing update_blocking — "
                    "schema updated NHƯNG chunks KHÔNG re-extract, defer v4.0 async queue"
                )
        except Exception as e:  # noqa: BLE001 — log + continue, KHÔNG fail restore
            logger.warning(
                "restore_reextract_failed: doc_id=%s err=%s", doc_id_str, e
            )

    # 6) Return envelope refreshed document
    return _success_envelope(refreshed)


# Public symbol for main.py import
__all__ = ["router"]
```

**Bước 2 — Edit `Hub_All/api/app/main.py` thêm 2 line (import + include_router):**

ADD line import sau dòng 810 `from app.routers.documents import router as documents_router`:

```python
# Plan 05-03 v3.1 VER-03 — universal mount (KHÔNG central-only) document_versions router
from app.routers.document_versions import router as document_versions_router
```

ADD line include_router sau dòng 817 `app.include_router(documents_router)`:

```python
    # Plan 05-03 v3.1 VER-03 — universal mount document_versions (per-hub data, KHÔNG central-only)
    app.include_router(document_versions_router)
```

**Lưu ý quan trọng:**
- `prefix="/api/documents"` cùng `documents_router` — Endpoint paths `/api/documents/{id}/versions*` KHÔNG conflict vì FastAPI routes UNIQUE.
- `tags=["Document Versions"]` (KHÔNG `"Documents"` — separate OpenAPI grouping cho FE swagger UI).
- `Depends(get_current_user_for_hub_access)` 3 GET endpoint — Plan 03-03 v3.0 SSO-04 carry forward (NO role check, hub isolation chỉ).
- `Depends(get_current_user)` POST /restore — chỉ JWT verify; inline RBAC qua assert_hub_admin_for SAU resolve document.hub_id (hybrid Plan 02-01 v3.1).
- `_resolve_document_hub_id` helper raw SQL — KHÔNG dùng full DocumentService (giảm dep + tránh nested transaction).
- `_success_envelope` try import shared helper, fallback inline construct — robust pattern.
- Cocoindex re-extract: best-effort, log + continue nếu API mismatch (KHÔNG fail restore).
- KHÔNG gọi `enqueue_audit` ở router — audit responsibility ở service (Plan 05-02 snapshot + restore_to_version đã emit).
- `__all__ = ["router"]` export public symbol.
- `noqa: B008` comment cho `Depends(...)` pattern FastAPI (default arg call).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -c "import ast; ast.parse(open('app/routers/document_versions.py').read()); print('router syntax OK')" && python -c "import ast; ast.parse(open('app/main.py').read()); print('main.py syntax OK')" && grep -c "include_router(document_versions_router)" app/main.py && grep -c "from app.routers.document_versions import router as document_versions_router" app/main.py && python -c "from app.routers.document_versions import router; assert router.prefix == '/api/documents'; assert 'Document Versions' in router.tags; print(f'OK router {len(router.routes)} routes')"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/app/routers/document_versions.py` EXISTS.
    - File chứa `APIRouter(prefix="/api/documents", tags=["Document Versions"])` (router metadata).
    - File chứa `@router.get("/{document_id}/versions"` (Endpoint 1 list).
    - File chứa `@router.get("/{document_id}/versions/{version_id}"` (Endpoint 2 detail).
    - File chứa `@router.get("/{document_id}/versions/{version_id}/file"` (Endpoint 3 file).
    - File chứa `@router.post("/{document_id}/versions/{version_id}/restore"` (Endpoint 4 restore).
    - File chứa `Depends(get_current_user_for_hub_access)` ≥ 3 lần (3 GET endpoint Layer 3 SSO-04).
    - File chứa `await assert_hub_admin_for(user=user, db=db, target_hub_id=` (POST /restore inline RBAC).
    - File chứa `await document_version_service.list_versions(` (call service).
    - File chứa `await document_version_service.get_version_with_chunks(` (call service).
    - File chứa `await document_version_service.get_version_file_path(` (call service).
    - File chứa `await document_version_service.restore_to_version(` (call service).
    - File chứa `StreamingResponse(` (Endpoint 3 binary stream).
    - File chứa `Content-Disposition` + `f'attachment; filename="{download_filename}"'` (download header).
    - File chứa `f"v{version_dict['version_number']}_{version_dict['name']}"` (download filename pattern FE expect).
    - File chứa `"chunks": chunks` (D-V3.1-Phase5-B return empty array passthrough).
    - File chứa `"DOCUMENT_NOT_FOUND"` + `"VERSION_NOT_FOUND"` (error envelope codes).
    - File chứa `from app.auth.dependencies import` + `assert_hub_admin_for` + `get_current_user_for_hub_access` (import contract).
    - File chứa `from app.services import document_version_service` HOẶC `from app.services.document_version_service import` (Plan 05-02 service import).
    - File chứa `D-V3.1-Phase5-C` + `D-V3.1-Phase5-D` + `D-V3.1-Phase5-I` (≥ 3 traceability mention).
    - File chứa `__all__ = ["router"]` (public symbol export).
    - File KHÔNG chứa `enqueue_audit` (audit responsibility ở service layer Plan 05-02).
    - File `Hub_All/api/app/main.py` chứa `from app.routers.document_versions import router as document_versions_router` (import line).
    - File `Hub_All/api/app/main.py` chứa `app.include_router(document_versions_router)` (universal mount).
    - `Hub_All/api/app/main.py` mount line ở SAU `app.include_router(documents_router)` line 817 (universal block — KHÔNG ở central-only block line 843+).
    - `cd Hub_All/api && python -c "import ast; ast.parse(open('app/routers/document_versions.py').read())"` exit 0.
    - `cd Hub_All/api && python -c "from app.routers.document_versions import router; assert router.prefix == '/api/documents'"` exit 0.
    - `cd Hub_All/api && python -c "from app.routers.document_versions import router; assert len(router.routes) == 4, f'Expected 4 routes, got {len(router.routes)}'"` exit 0.
  </acceptance_criteria>
  <done>
    document_versions.py router 4 endpoint + RBAC 3-layer integration (3 GET Layer 3 SSO-04 + POST inline assert_hub_admin_for); main.py universal mount; StreamingResponse + envelope M2 + chunks=[] empty (D-V3.1-Phase5-B); restore_to_version service call (D-V3.1-Phase5-D append-only); cocoindex re-extract best-effort sync (D-V3.1-Phase5-I).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo test_document_versions_router.py — 10 unit test với mocked service + dependency overrides</name>
  <read_first>
    - Hub_All/api/app/routers/document_versions.py (state sau Task 1 — verify endpoint signatures + service call patterns)
    - Hub_All/api/tests/unit/test_require_hub_admin_for.py (Plan 02-01 — pattern AsyncMock + pytest.raises + assert envelope code)
    - Hub_All/api/tests/unit/test_document_version_service.py (Plan 05-02 — `_make_session` factory carry forward)
    - Hub_All/api/app/main.py:create_app (verify FastAPI TestClient pattern nếu cần test integration-style)
    - Pattern bất kỳ existing router unit test (grep `tests/unit/test_*_router.py` nếu có)
  </read_first>
  <behavior>
    - Test 1 (list_versions_returns_envelope_with_versions_array): mock service.list_versions trả 3 dict; GET /versions → 200 + envelope {success: true, data: {versions: [3 dict]}, error: null, meta: null}.
    - Test 2 (list_versions_404_when_document_missing): mock _resolve_document_hub_id raise 404; GET /versions → 404 + envelope code DOCUMENT_NOT_FOUND.
    - Test 3 (get_detail_returns_chunks_empty_array): mock service.get_version_with_chunks trả (dict, []); GET /versions/{vid} → 200 + envelope data.chunks == [] (D-V3.1-Phase5-B verify).
    - Test 4 (get_detail_404_when_version_missing): mock service trả (None, []); GET /versions/{vid} → 404 + envelope code VERSION_NOT_FOUND.
    - Test 5 (get_file_streaming_response_with_attachment_header): mock service.get_version_file_path trả Path real tempfile; GET /versions/{vid}/file → 200 StreamingResponse + Content-Disposition `attachment; filename="v3_test.docx"`.
    - Test 6 (get_file_404_when_path_missing_on_disk): mock service trả Path KHÔNG tồn tại; GET /versions/{vid}/file → 404 + envelope code VERSION_FILE_MISSING.
    - Test 7 (restore_calls_service_with_actor_metadata): mock assert_hub_admin_for OK + restore_to_version trả refreshed dict; POST /restore với user.role='hub_admin' → service call truyền actor_role='hub_admin' + actor_hub_id=doc.hub_id.
    - Test 8 (restore_super_admin_actor_metadata_null_hub): mock user.role='admin'; POST /restore → service call truyền actor_role='admin' + actor_hub_id=None.
    - Test 9 (restore_403_when_assert_hub_admin_raises): mock assert_hub_admin_for raise HTTPException 403 HUB_ADMIN_REQUIRED; POST /restore → response 403 + envelope code HUB_ADMIN_REQUIRED.
    - Test 10 (restore_400_when_document_hub_id_missing): mock _resolve_document_hub_id trả (doc_id, None); POST /restore → 400 + envelope code DOCUMENT_HUB_MISSING.
  </behavior>
  <action>
Tạo file `Hub_All/api/tests/unit/test_document_versions_router.py`:

```python
"""Unit test document_versions router — Phase 5 Plan 05-03 VER-03 + VER-04.

10 unit test cover 4 endpoint × happy path + RBAC reject + envelope shape + audit:
1. list_versions_returns_envelope_with_versions_array
2. list_versions_404_when_document_missing
3. get_detail_returns_chunks_empty_array (D-V3.1-Phase5-B verify)
4. get_detail_404_when_version_missing
5. get_file_streaming_response_with_attachment_header
6. get_file_404_when_path_missing_on_disk
7. restore_calls_service_with_actor_metadata (hub_admin)
8. restore_super_admin_actor_metadata_null_hub
9. restore_403_when_assert_hub_admin_raises (viewer + cross-hub)
10. restore_400_when_document_hub_id_missing

Test isolation: call router handler functions DIRECTLY (KHÔNG TestClient ASGI overhead) —
mock Depends args + AsyncSession + service module functions.

Pattern: từ Plan 02-01 test_require_hub_admin_for.py + Plan 05-02 test_document_version_service.py.
"""
from __future__ import annotations

import tempfile
from pathlib import Path
from types import SimpleNamespace
from unittest.mock import AsyncMock, MagicMock, patch
from uuid import uuid4

import pytest
from fastapi import HTTPException

from app.routers.document_versions import (
    _resolve_document_hub_id,
    download_document_version_file,
    get_document_version_detail,
    list_document_versions,
    restore_document_to_version,
)


def _make_user(role: str = "admin", user_id: object = None) -> SimpleNamespace:
    """SimpleNamespace User duck-type — .id + .role."""
    return SimpleNamespace(id=user_id or uuid4(), role=role)


def _make_session_returning(*fetchone_returns: object) -> AsyncMock:
    """AsyncMock(AsyncSession) trả sequence fetchone results."""
    session = AsyncMock()
    result_mocks = []
    for fetchone_value in fetchone_returns:
        result = MagicMock()
        result.fetchone.return_value = fetchone_value
        result_mocks.append(result)
    session.execute = AsyncMock(side_effect=result_mocks)
    return session


def _make_request_with_app_state(cocoindex_app: object = None) -> SimpleNamespace:
    """SimpleNamespace Request — .app.state.cocoindex_app."""
    state = SimpleNamespace(cocoindex_app=cocoindex_app)
    app = SimpleNamespace(state=state)
    return SimpleNamespace(app=app)


# === Test 1: list_versions happy path envelope ===

@pytest.mark.asyncio
async def test_list_versions_returns_envelope_with_versions_array() -> None:
    """GET /versions → 200 envelope {success: true, data: {versions: [3 dict]}}."""
    doc_id = uuid4()
    db = _make_session_returning((doc_id, str(uuid4())))  # _resolve_document_hub_id

    fake_versions = [
        {"id": str(uuid4()), "version_number": 3, "change_type": "reupload"},
        {"id": str(uuid4()), "version_number": 2, "change_type": "reextract"},
        {"id": str(uuid4()), "version_number": 1, "change_type": "reupload"},
    ]

    with patch(
        "app.routers.document_versions.document_version_service.list_versions",
        new=AsyncMock(return_value=fake_versions),
    ):
        result = await list_document_versions(
            document_id=doc_id,
            user=_make_user(),
            db=db,
        )

    assert result["success"] is True
    assert result["data"]["versions"] == fake_versions
    assert result["error"] is None
    assert result["meta"] is None


# === Test 2: list_versions 404 when document missing ===

@pytest.mark.asyncio
async def test_list_versions_404_when_document_missing() -> None:
    """_resolve_document_hub_id raise 404 → endpoint propagate."""
    doc_id = uuid4()
    db = _make_session_returning(None)  # fetchone None → 404

    with pytest.raises(HTTPException) as exc_info:
        await list_document_versions(
            document_id=doc_id,
            user=_make_user(),
            db=db,
        )

    assert exc_info.value.status_code == 404
    assert exc_info.value.detail["code"] == "DOCUMENT_NOT_FOUND"


# === Test 3: get_detail returns chunks empty array (D-V3.1-Phase5-B) ===

@pytest.mark.asyncio
async def test_get_detail_returns_chunks_empty_array() -> None:
    """D-V3.1-Phase5-B LOCKED: GET /versions/{vid} envelope data.chunks == []."""
    doc_id = uuid4()
    version_id = uuid4()
    db = _make_session_returning((doc_id, str(uuid4())))

    fake_version = {"id": str(version_id), "version_number": 1, "name": "v1.docx"}

    with patch(
        "app.routers.document_versions.document_version_service.get_version_with_chunks",
        new=AsyncMock(return_value=(fake_version, [])),
    ):
        result = await get_document_version_detail(
            document_id=doc_id,
            version_id=version_id,
            user=_make_user(),
            db=db,
        )

    assert result["success"] is True
    assert result["data"]["version"] == fake_version
    assert result["data"]["chunks"] == [], "D-V3.1-Phase5-B: chunks phải là [] empty"
    assert isinstance(result["data"]["chunks"], list)


# === Test 4: get_detail 404 when version missing ===

@pytest.mark.asyncio
async def test_get_detail_404_when_version_missing() -> None:
    """service trả (None, []) → 404 VERSION_NOT_FOUND."""
    doc_id = uuid4()
    version_id = uuid4()
    db = _make_session_returning((doc_id, str(uuid4())))

    with patch(
        "app.routers.document_versions.document_version_service.get_version_with_chunks",
        new=AsyncMock(return_value=(None, [])),
    ):
        with pytest.raises(HTTPException) as exc_info:
            await get_document_version_detail(
                document_id=doc_id,
                version_id=version_id,
                user=_make_user(),
                db=db,
            )

    assert exc_info.value.status_code == 404
    assert exc_info.value.detail["code"] == "VERSION_NOT_FOUND"


# === Test 5: get_file streaming response with attachment header ===

@pytest.mark.asyncio
async def test_get_file_streaming_response_with_attachment_header(tmp_path: Path) -> None:
    """GET /versions/{vid}/file → StreamingResponse + Content-Disposition attachment."""
    doc_id = uuid4()
    version_id = uuid4()
    db = _make_session_returning((doc_id, str(uuid4())))

    test_file = tmp_path / "v3_test.docx"
    test_file.write_bytes(b"file content for streaming")

    fake_version = {
        "id": str(version_id),
        "version_number": 3,
        "name": "test.docx",
        "file_type": "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    }

    with patch(
        "app.routers.document_versions.document_version_service.get_version_with_chunks",
        new=AsyncMock(return_value=(fake_version, [])),
    ), patch(
        "app.routers.document_versions.document_version_service.get_version_file_path",
        new=AsyncMock(return_value=test_file),
    ):
        result = await download_document_version_file(
            document_id=doc_id,
            version_id=version_id,
            user=_make_user(),
            db=db,
        )

    # StreamingResponse import check
    from fastapi.responses import StreamingResponse
    assert isinstance(result, StreamingResponse)
    assert result.media_type == fake_version["file_type"]
    assert "attachment" in result.headers["Content-Disposition"]
    assert "v3_test.docx" in result.headers["Content-Disposition"]


# === Test 6: get_file 404 when path missing on disk ===

@pytest.mark.asyncio
async def test_get_file_404_when_path_missing_on_disk() -> None:
    """get_version_file_path trả Path KHÔNG tồn tại → 404 VERSION_FILE_MISSING."""
    doc_id = uuid4()
    version_id = uuid4()
    db = _make_session_returning((doc_id, str(uuid4())))

    fake_version = {"id": str(version_id), "version_number": 1, "name": "v1.docx", "file_type": "docx"}
    missing_path = Path("/nonexistent/path/v1.docx")

    with patch(
        "app.routers.document_versions.document_version_service.get_version_with_chunks",
        new=AsyncMock(return_value=(fake_version, [])),
    ), patch(
        "app.routers.document_versions.document_version_service.get_version_file_path",
        new=AsyncMock(return_value=missing_path),
    ):
        with pytest.raises(HTTPException) as exc_info:
            await download_document_version_file(
                document_id=doc_id,
                version_id=version_id,
                user=_make_user(),
                db=db,
            )

    assert exc_info.value.status_code == 404
    assert exc_info.value.detail["code"] == "VERSION_FILE_MISSING"


# === Test 7: restore calls service with actor metadata (hub_admin) ===

@pytest.mark.asyncio
async def test_restore_calls_service_with_actor_metadata_hub_admin() -> None:
    """POST /restore user.role='hub_admin' → service truyền actor_role='hub_admin' + actor_hub_id=doc.hub_id."""
    doc_id = uuid4()
    version_id = uuid4()
    hub_id = str(uuid4())
    db = _make_session_returning((doc_id, hub_id))

    user = _make_user(role="hub_admin")
    request = _make_request_with_app_state(cocoindex_app=None)

    refreshed = {"id": str(doc_id), "hub_id": hub_id, "filename": "refreshed.docx"}

    with patch(
        "app.routers.document_versions.assert_hub_admin_for",
        new=AsyncMock(),  # PASS — KHÔNG raise
    ), patch(
        "app.routers.document_versions.document_version_service.restore_to_version",
        new=AsyncMock(return_value=refreshed),
    ) as mock_restore:
        result = await restore_document_to_version(
            document_id=doc_id,
            version_id=version_id,
            request=request,
            user=user,
            db=db,
        )

    assert result["success"] is True
    assert result["data"]["filename"] == "refreshed.docx"
    mock_restore.assert_awaited_once()
    call_kwargs = mock_restore.await_args.kwargs
    assert call_kwargs["actor_role"] == "hub_admin"
    assert call_kwargs["actor_hub_id"] == hub_id
    assert call_kwargs["actor_user_id"] == str(user.id)
    assert call_kwargs["document_id"] == str(doc_id)
    assert call_kwargs["version_id"] == str(version_id)


# === Test 8: restore super admin actor metadata null hub ===

@pytest.mark.asyncio
async def test_restore_super_admin_actor_metadata_null_hub() -> None:
    """POST /restore user.role='admin' → service truyền actor_role='admin' + actor_hub_id=None."""
    doc_id = uuid4()
    version_id = uuid4()
    hub_id = str(uuid4())
    db = _make_session_returning((doc_id, hub_id))

    user = _make_user(role="admin")
    request = _make_request_with_app_state(cocoindex_app=None)

    refreshed = {"id": str(doc_id), "hub_id": hub_id, "filename": "refreshed.docx"}

    with patch(
        "app.routers.document_versions.assert_hub_admin_for",
        new=AsyncMock(),
    ), patch(
        "app.routers.document_versions.document_version_service.restore_to_version",
        new=AsyncMock(return_value=refreshed),
    ) as mock_restore:
        await restore_document_to_version(
            document_id=doc_id,
            version_id=version_id,
            request=request,
            user=user,
            db=db,
        )

    call_kwargs = mock_restore.await_args.kwargs
    assert call_kwargs["actor_role"] == "admin"
    assert call_kwargs["actor_hub_id"] is None, "Super admin → actor_hub_id phải None"


# === Test 9: restore 403 when assert_hub_admin raises ===

@pytest.mark.asyncio
async def test_restore_403_when_assert_hub_admin_raises() -> None:
    """assert_hub_admin_for raise 403 HUB_ADMIN_REQUIRED → endpoint propagate."""
    doc_id = uuid4()
    version_id = uuid4()
    hub_id = str(uuid4())
    db = _make_session_returning((doc_id, hub_id))

    user = _make_user(role="viewer")
    request = _make_request_with_app_state()

    with patch(
        "app.routers.document_versions.assert_hub_admin_for",
        new=AsyncMock(side_effect=HTTPException(
            status_code=403,
            detail={"code": "HUB_ADMIN_REQUIRED", "message": "Yêu cầu hub_admin"},
        )),
    ):
        with pytest.raises(HTTPException) as exc_info:
            await restore_document_to_version(
                document_id=doc_id,
                version_id=version_id,
                request=request,
                user=user,
                db=db,
            )

    assert exc_info.value.status_code == 403
    assert exc_info.value.detail["code"] == "HUB_ADMIN_REQUIRED"


# === Test 10: restore 400 when document hub_id missing ===

@pytest.mark.asyncio
async def test_restore_400_when_document_hub_id_missing() -> None:
    """_resolve_document_hub_id trả (doc_id, None) → 400 DOCUMENT_HUB_MISSING."""
    doc_id = uuid4()
    version_id = uuid4()
    # _resolve returns row với hub_id=None (legacy orphan document)
    db = _make_session_returning((doc_id, None))

    user = _make_user(role="hub_admin")
    request = _make_request_with_app_state()

    with pytest.raises(HTTPException) as exc_info:
        await restore_document_to_version(
            document_id=doc_id,
            version_id=version_id,
            request=request,
            user=user,
            db=db,
        )

    assert exc_info.value.status_code == 400
    assert exc_info.value.detail["code"] == "DOCUMENT_HUB_MISSING"
```

**Lưu ý:**
- Call handler functions DIRECTLY (KHÔNG TestClient/httpx) — đơn giản + fast + KHÔNG cần FastAPI ASGI overhead.
- `with patch("app.routers.document_versions.document_version_service.list_versions"` — patch ở module nơi import (NOT `app.services.document_version_service`).
- `with patch("app.routers.document_versions.assert_hub_admin_for"` — patch ở module nơi import.
- `tmp_path` pytest fixture cho test 5 real file (StreamingResponse cần file thật để open).
- Test 7 + 8 verify exact actor_role + actor_hub_id derive logic — `admin` → None, `hub_admin` → doc.hub_id.
- `mock_restore.await_args.kwargs` — verify keyword args truyền vào service.
- Test 9 verify HTTPException propagate (FastAPI handler raise, KHÔNG try/except).
- Test 10 verify edge case legacy document KHÔNG có hub_id (defensive 400 reject).
- KHÔNG mock `cocoindex_app` real — pass None, router skip path log + continue.
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/unit/test_document_versions_router.py -x --tb=short 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/unit/test_document_versions_router.py` EXISTS.
    - File chứa `from app.routers.document_versions import` (import SUT).
    - File chứa `list_document_versions` + `get_document_version_detail` + `download_document_version_file` + `restore_document_to_version` + `_resolve_document_hub_id` (5 import — 4 endpoint + 1 helper).
    - File chứa `@pytest.mark.asyncio` ≥ 10 lần (10 async test).
    - File chứa `async def test_` ≥ 10 lần.
    - File chứa `with patch("app.routers.document_versions.document_version_service.` ≥ 5 lần (patch service calls).
    - File chứa `with patch("app.routers.document_versions.assert_hub_admin_for"` ≥ 3 lần (test 7, 8, 9).
    - File chứa `chunks == []` (Test 3 D-V3.1-Phase5-B verify).
    - File chứa `assert result["success"] is True` ≥ 4 lần (test 1, 3, 5, 7 envelope success).
    - File chứa `assert result["error"] is None` (test 1 envelope shape).
    - File chứa `"DOCUMENT_NOT_FOUND"` (test 2).
    - File chứa `"VERSION_NOT_FOUND"` (test 4).
    - File chứa `"VERSION_FILE_MISSING"` (test 6).
    - File chứa `"HUB_ADMIN_REQUIRED"` (test 9).
    - File chứa `"DOCUMENT_HUB_MISSING"` (test 10).
    - File chứa `StreamingResponse` import + `isinstance(result, StreamingResponse)` (test 5).
    - File chứa `assert "attachment"` + `Content-Disposition` (test 5).
    - File chứa `actor_role == "hub_admin"` + `actor_hub_id == hub_id` (test 7).
    - File chứa `actor_role == "admin"` + `actor_hub_id is None` (test 8 super admin).
    - File chứa `pytest.raises(HTTPException)` ≥ 4 lần (test 2, 4, 6, 9, 10).
    - `cd Hub_All/api && python -m pytest tests/unit/test_document_versions_router.py -x` exit 0 (10 test PASS).
    - `cd Hub_All/api && python -m pytest tests/unit/test_document_versions_router.py --tb=no -q 2>&1 | grep -E "10 passed"` exit 0.
  </acceptance_criteria>
  <done>
    test_document_versions_router.py 10 test PASS pytest; cover 4 endpoint × happy path + 5 error case + RBAC inline check + actor metadata derive logic + envelope shape verify + chunks=[] D-V3.1-Phase5-B; mock service + dependency overrides.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend (FE) → 4 endpoint | FE token + body input UNTRUSTED — JWT verify ở Depends + body Pydantic validate (UUID path coerce) |
| Endpoint handler → assert_hub_admin_for | Inline RBAC SAU resolve document.hub_id; defensive `assert_hub_admin_for(target_hub_id=document.hub_id)` từ DB row (trusted) |
| Endpoint handler → document_version_service | Internal trusted helper (Plan 05-02); service KHÔNG re-check RBAC (router responsibility) |
| Endpoint handler → cocoindex_app (POST /restore) | Best-effort SYNC trigger; log + continue nếu API mismatch (KHÔNG fail restore) |
| StreamingResponse → file_path.open("rb") | file_path từ DB row (trusted); existence check `file_path.exists()` trước open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Spoofing | FE fake `canRestore=true` prop bypass UX | mitigate | BE Layer 3 `assert_hub_admin_for` authoritative — KHÔNG dựa FE prop. Plan 05-04 test scenario 4 hub_admin dmd → doc tdt → 403 verify defense in depth (R-V3.1-2 mitigation chain). |
| T-05-03-02 | Tampering | document_id path param SQL injection | mitigate | FastAPI Path(UUID) coerce — invalid UUID 422 reject; `_resolve_document_hub_id` raw SQL với named bind params `:id`. |
| T-05-03-03 | Repudiation | POST /restore KHÔNG audit | mitigate | Audit responsibility ở service (Plan 05-02 `restore_to_version` emit `document.version.restore` + `document.version.create`); Plan 05-04 scenario 5 forensic chain verify. |
| T-05-03-04 | Information Disclosure | StreamingResponse leak file path qua Content-Disposition header | accept | Filename `v{N}_{name}` derive từ FE expectation (DocumentVersionHistory.tsx:79 pattern); name là filename gốc user upload — KHÔNG nhạy cảm hơn `GET /api/documents/{id}/file` existing. |
| T-05-03-05 | DoS | StreamingResponse large file (10GB+) → memory/disk exhaust | accept | M2 baseline FileStore.save limit MAX_UPLOAD_SIZE; version file inherit cap. Plan 05-01 + 05-02 retention "3 gốc + 2 gần nhất" cap COUNT. Cocoindex re-extract sync timeout TBD (D-V3.1-Phase5-I accept). |
| T-05-03-06 | Elevation of Privilege | Cross-hub viewer GET /versions của doc khác hub | mitigate | Layer 3 `get_current_user_for_hub_access` enforce `document.hub_id ∈ user.hub_ids` (Plan 03-03 v3.0 SSO-04). Test 9 verify HUB_ADMIN_REQUIRED reject cho POST /restore. |
| T-05-03-07 | DoS | Concurrent POST /restore cùng document_id | accept | Plan 05-02 atomic transaction (UNIQUE doc_ver constraint backup); last-write-wins acceptable cho restore (UX expected). |
| T-05-03-08 | Tampering | Universal mount ở central → cross-hub document leak | mitigate | Layer 3 SSO-04 enforce — central hub_name bypass (cross-hub by design) NHƯNG user JWT.hub_ids vẫn check; viewer KHÔNG có hub_ids cho doc khác → 403 reject. |
</threat_model>

<verification>
- File `document_versions.py` 4 endpoint exact match api.ts:268-285 URL pattern.
- Envelope M2 shape `{success, data, error, meta}` cho 3 GET + POST response.
- `chunks: []` empty array (D-V3.1-Phase5-B LOCKED) ở GET /versions/{vid} response.
- StreamingResponse + Content-Disposition attachment ở GET /versions/{vid}/file.
- POST /restore inline `assert_hub_admin_for` SAU resolve document.hub_id (D-V3.1-Phase5-C LOCKED).
- Actor metadata derive logic (admin → None / hub_admin → doc.hub_id) Plan 02-04 v3.1 pattern carry forward.
- Cocoindex re-extract best-effort sync (D-V3.1-Phase5-I LOCKED).
- Audit responsibility ở service layer (Plan 05-02) — router KHÔNG gọi enqueue_audit.
- Universal mount main.py SAU documents_router (KHÔNG central-only).
- 10 unit test PASS — cover 4 endpoint × happy path + 5 error case + RBAC verify.
- Plan 05-04 integration test E2E verify scenario thật.
</verification>

<success_criteria>
1. document_versions.py 4 endpoint exact match api.ts:268-285 URL + envelope shape M2.
2. 3 GET endpoint Layer 3 SSO-04 `get_current_user_for_hub_access` (D-V3.1-Phase5-C viewer PASS).
3. POST /restore inline `assert_hub_admin_for(target_hub_id=document.hub_id)` (Plan 02-01 v3.1 hybrid).
4. chunks=[] empty array (D-V3.1-Phase5-B LOCKED).
5. StreamingResponse + Content-Disposition attachment header.
6. main.py universal mount SAU documents_router (KHÔNG central-only).
7. 10 unit test PASS — semantic coverage VER-03 + VER-04.
8. Plan 05-04 integration test unblocked (E2E real backend code path).
9. FE `DocumentVersionHistory.tsx` consume 4 endpoint OK runtime (Plan 05-04 verify).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/05-document-version-history/05-03-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- document_versions.py 4 endpoint ship — exact match api.ts:268-285 URL + envelope M2
- RBAC 3-layer: 3 GET Layer 3 SSO-04 (D-V3.1-Phase5-C viewer PASS) + POST inline assert_hub_admin_for
- main.py universal mount SAU documents_router (KHÔNG central-only — documents per-hub data carry forward FACTOR-01 v3.0)
- chunks=[] empty array (D-V3.1-Phase5-B LOCKED — FE typecheck happy)
- StreamingResponse + Content-Disposition `attachment; filename="v{N}_{name}"` match FE download pattern
- POST /restore actor metadata derive (admin → None / hub_admin → doc.hub_id) carry forward Plan 02-04
- Cocoindex re-extract best-effort SYNC (D-V3.1-Phase5-I LOCKED) — log + continue nếu API mismatch
- Audit responsibility ở service layer (Plan 05-02 snapshot + restore_to_version đã emit 2 action)
- 10 unit test PASS — cover 4 endpoint × happy + RBAC reject + envelope shape + error case
- Plan 05-04 integration test unblocked — E2E real backend code path
- FE DocumentVersionHistory.tsx sẽ mở console KHÔNG còn 404 (Plan 05-04 verify runtime — ROADMAP success criterion #3)

Commit atomic: `feat(05-03): document_versions router 4 endpoint + RBAC 3-layer + universal mount + 10 unit test (VER-03 + VER-04)` + body tiếng Việt nói tại sao (Phase 5 router catch-up 4 endpoint exact match FE api.ts:268-285; chunks=[] D-V3.1-Phase5-B; assert_hub_admin_for inline POST /restore D-V3.1-Phase5-C; cocoindex re-extract SYNC D-V3.1-Phase5-I; universal mount per-hub data carry forward FACTOR-01 v3.0; Plan 05-04 integration test unblocked).
</output>
