---
phase: 05-document-version-history
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05]
tags: [closeout, docs, state, requirements, roadmap, claude-md, milestone-reopen]
must_haves:
  truths:
    - "STATE.md frontmatter cập nhật: phase_5_status: DONE + phase_5_done_date: YYYY-MM-DD + progress.completed_phases: 5 + progress.completed_plans: 20 + next_action gợi ý 2 path (complete-milestone vs new-milestone v4.0)"
    - "STATE.md body APPEND section mới 'Phase 5 Results Summary (DONE YYYY-MM-DD)' với 5 plan summary + carry forward patterns + R-V3.1-2 mitigation chain Phase 5 + git tag decision"
    - "REQUIREMENTS.md APPEND section mới 'Phase 5 — Document Version History (v3.1 re-open)' với 5 dòng `- [x] **VER-XX** ... (DONE YYYY-MM-DD — Plan 05-XX)`; total v3.1 REQ count grow 15 → 20"
    - "ROADMAP.md Phase 5 row table chuyển `📋 PLANNED` → `✅ DONE YYYY-MM-DD` + Plans checklist 5 plan mark `[x]` + Progress table v3.1 4 phase → 5 phase / 15 plan → 20 plan / 15 REQ → 20 REQ"
    - "CLAUDE.md §6 APPEND subsection mới '### Phase 5 v3.1 Document version history pattern (VER-01..05 — YYYY-MM-DD)' + bump trailing `*Cập nhật:` line reflect Phase 5 done"
    - "KHÔNG tạo git tag v3.1.1 mặc định (D-V3.1-Phase5 Claude's discretion — semver clean, v3.1 đã tag 2026-05-24); document option `git tag -a v3.1.1` manual ở SUMMARY nếu operator muốn"
    - "1 commit atomic 4 file docs update — KHÔNG split (carry forward Plan 02-05 + 03-04 + 04-03 v3.1 pattern)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 5 DONE state — frontmatter + body Phase 5 Results Summary section"
      contains: "Phase 5 Results Summary (DONE"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VER-01..05 (5 REQ-ID) section mới với [x] mark + plan reference"
      contains: "VER-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 row table DONE + Plans checklist 5 [x] + Progress table v3.1 5 phase"
      contains: "5/5 plan ship"
    - path: "CLAUDE.md"
      provides: "§6 subsection mới Phase 5 v3.1 pattern + bump trailing Cập nhật line"
      contains: "Phase 5 v3.1 Document version history"
  key_links:
    - from: "STATE.md frontmatter"
      to: "phase_5_status + phase_5_done_date + progress.completed_phases + progress.completed_plans + next_action"
      via: "YAML frontmatter field update (carry forward Plan 04-03 pattern)"
      pattern: "phase_5_status: DONE"
    - from: "REQUIREMENTS.md APPEND"
      to: "5 dòng VER-XX [x] với plan reference"
      via: "APPEND section sau MIGRATE-02 — KHÔNG insert giữa file"
      pattern: "VER-01.*\\[x\\]"
    - from: "ROADMAP.md Phase 5 row"
      to: "Plans checklist 5 mark [x] + Progress table v3.1 5 phase"
      via: "Table edit row Phase 5 + Plans block edit checkbox + Progress table cập nhật"
      pattern: "✅ DONE.*5/5 plan"
    - from: "CLAUDE.md §6"
      to: "subsection APPEND + trailing line bump"
      via: "Pattern Plan 02-05 + 03-04 + 04-03 v3.1 carry forward"
      pattern: "Phase 5 v3.1 Document version history pattern"
---

<objective>
Plan 05-05 v3.1 Phase 5 closeout — Wave 5 BLOCKING atomic 4 docs update (STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md) + KHÔNG git tag mới mặc định (semver clean).

**Purpose (Phase 5 milestone closeout):** Plan 05-01 schema + Plan 05-02 service + Plan 05-03 router + Plan 05-04 integration test PASS. Plan 05-05 update 4 source-of-truth docs reflect Phase 5 DONE + 5 REQ-ID VER-01..05 consumed; v3.1 milestone re-opened scope (Phase 5 added 2026-05-26 sau v3.1 đã tag 2026-05-24) → progress 15 plan → 20 plan + 15 REQ → 20 REQ.

**4 docs atomic update (carry forward Plan 02-05 + 03-04 + 04-03 v3.1 pattern):**
1. **STATE.md** — Frontmatter (`phase_5_status: DONE` + `phase_5_done_date` + progress counters + `next_action` 2 option) + body APPEND Phase 5 Results Summary section.
2. **REQUIREMENTS.md** — APPEND section mới "Phase 5 — Document Version History (v3.1 re-open)" với 5 dòng `- [x] **VER-XX** ... (DONE YYYY-MM-DD — Plan 05-XX)`.
3. **ROADMAP.md** — Phase 5 row table `📋 PLANNED` → `✅ DONE YYYY-MM-DD` + Plans checklist 5 plan mark `[x]` + Progress table v3.1 cập nhật 5 phase / 20 plan / 20 REQ.
4. **CLAUDE.md** — §6 APPEND subsection mới + bump trailing `*Cập nhật:` line.

**Git tag decision (D-V3.1-Phase5 Claude's discretion):**
- **DEFAULT:** KHÔNG tạo tag mới — v3.1 đã tag 2026-05-24 (Plan 04-03 v3.1) + semver clean (Phase 5 = re-open scope, KHÔNG breaking change).
- **OPTIONAL:** Operator manual `git tag -a v3.1.1 -m "v3.1.1 Phase 5 Document version history"` nếu muốn distinguish post-v3.1 work (document trong SUMMARY.md).

**Smoke checkpoint runtime SKIP pre-resolved (carry forward Plan 02-05 + 03-04 + 04-03 v3.1):**
- Plan 05-04 integration test 5 scenario PASS clean runtime đã satisfy ROADMAP success criterion #2-5.
- Success criterion #1 (alembic upgrade head idempotent + downgrade) satisfied Plan 05-01 + integration test.
- Manual smoke FE `DocumentVersionHistory.tsx` open browser console KHÔNG còn 404 — defer ops handover (carry forward v3.0 + v3.1 closeout pattern).

**Output:**
- 4 file docs cập nhật atomic (single commit).
- 1 commit atomic `docs(05-05): closeout Phase 5 Document Version History — 5 plan / 5 REQ-ID VER-01..05 DONE`.

**Carry forward strategy:**
- Pattern Plan 02-05 + 03-04 + 04-03 v3.1 — closeout 4 docs atomic.
- Pattern Plan 04-03 v3.1 — git tag annotated local (NHƯNG Plan 05-05 default KHÔNG tag mới, semver clean).
- CLAUDE.md §6 APPEND subsection format match existing Phase 1-4 v3.1 subsection.
- ROADMAP.md table edit format match existing Phase rows (`✅ DONE YYYY-MM-DD` suffix).
- REQUIREMENTS.md APPEND format match existing REQ sections (5 dòng `- [x] **VER-XX**`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md
@Hub_All/.planning/phases/05-document-version-history/05-01-PLAN.md
@Hub_All/.planning/phases/05-document-version-history/05-02-PLAN.md
@Hub_All/.planning/phases/05-document-version-history/05-03-PLAN.md
@Hub_All/.planning/phases/05-document-version-history/05-04-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-05-PLAN.md
@Hub_All/.planning/phases/04-migration-smoke-e2e/04-03-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Pattern source-of-truth — Plan 02-05 + 03-04 + 04-03 v3.1 closeout pattern carry forward. -->

STATE.md frontmatter pattern (existing fields cần update Phase 5):
```yaml
---
gsd_state_version: 1.0
milestone: v3.1
milestone_name: RBAC hub_admin
status: "v3.1 SHIPPED 2026-05-24 ... + Phase 5 DONE YYYY-MM-DD (Document Version History 5 plan ship)"
last_updated: "YYYY-MM-DDTHH:MM:SS.000Z"
progress:
  total_phases: 5
  completed_phases: 5      # 4 → 5
  total_plans: 20          # 15 → 20
  completed_plans: 20      # 15 → 20
  percent: 100             # 80 → 100
milestone_status: "RE-OPENED → DONE"   # hoặc "SHIPPED" sau Phase 5
milestone_reopened_date: "2026-05-26"
phase_5_status: "DONE"     # PLANNED → DONE
phase_5_done_date: "YYYY-MM-DD"
phase_5_plan_count: 5
next_action: "Phase 5 DONE YYYY-MM-DD — chọn 1 trong 2 path: (a) `/gsd-complete-milestone v3.1` archive milestone hoặc (b) `/gsd-new-milestone v4.0` Production Hardening fresh discuss."
---
```

STATE.md body section pattern (Phase 4 Results Summary line 202-230 — exemplar):
```markdown
## Phase 4 Results Summary (DONE 2026-05-24) v3.1 SHIPPED

3 plan ship 2 REQ-ID MIGRATE-01..02 — v3.1 milestone CLOSEOUT:

- **Plan 04-01** (MIGRATE-01 — DONE_WITH_DEVIATION): ...
- **Plan 04-02** (MIGRATE-02): ...
- **Plan 04-03** (closeout v3.1 SHIPPED): ...

**Carry forward patterns:** ...

**R-V3.1-X mitigation chain:** ...
```

REQUIREMENTS.md pattern (verify exact location qua read full file — Plan 02-05/03-04/04-03 carry forward APPEND section, KHÔNG insert):
```markdown
## Phase 5 — Document Version History (v3.1 re-open 2026-05-26)

- [x] **VER-01** — Alembic migration `0007_document_versions` table (15 cột + UNIQUE + INDEX + CHECK change_type) idempotent + downgrade rollback (DONE YYYY-MM-DD — Plan 05-01).
- [x] **VER-02** — Service `document_version_service.py` 5 public API + 2 private helper: snapshot dedupe by hash + restore append-only + retention CTE "3 gốc + 2 gần nhất" + audit emit 2 action (DONE YYYY-MM-DD — Plan 05-02).
- [x] **VER-03** — Router `document_versions.py` 4 endpoint exact match api.ts:268-285 (GET list + GET detail chunks=[] + GET file StreamingResponse + POST restore) + universal mount main.py (DONE YYYY-MM-DD — Plan 05-03).
- [x] **VER-04** — RBAC 3-layer: 3 GET endpoint Layer 3 SSO-04 viewer PASS + POST /restore inline assert_hub_admin_for (hub_admin/super PASS, viewer/cross-hub 403 HUB_ADMIN_REQUIRED) (DONE YYYY-MM-DD — Plan 05-03).
- [x] **VER-05** — Integration test `test_document_versions.py` 5 scenario E2E PASS (create + list DESC + restore append-only + cross-hub 403 + audit forensic chain) (DONE YYYY-MM-DD — Plan 05-04).
```

ROADMAP.md Phase 5 row update pattern (line 29):
```markdown
| **5** | Document version history (VER) ✅ DONE YYYY-MM-DD | ... | VER-01..05 (5) | 5 | Phase 2 (RBAC) — independent của Phase 3+4 |
```

ROADMAP.md Plans checklist update (Phase 5 section — line 178 hiện tại "TBD"):
```markdown
Plans:
- [x] 05-01-PLAN.md — Alembic migration 0007 document_versions (15 cột + UNIQUE + INDEX + CHECK change_type) + 5 integration test idempotent (VER-01) ✅ DONE YYYY-MM-DD
- [x] 05-02-PLAN.md — document_version_service.py 5 API + 2 helper (snapshot dedupe + restore append-only + retention CTE + audit emit) + 8 unit test (VER-02) ✅ DONE YYYY-MM-DD
- [x] 05-03-PLAN.md — document_versions.py router 4 endpoint exact match api.ts:268-285 + RBAC 3-layer + universal mount + 10 unit test (VER-03 + VER-04) ✅ DONE YYYY-MM-DD
- [x] 05-04-PLAN.md — Integration test test_document_versions.py 5 scenario E2E (create + list DESC + restore append-only + cross-hub 403 + audit forensic) (VER-05) ✅ DONE YYYY-MM-DD
- [x] 05-05-PLAN.md — Closeout 4 docs atomic (STATE + REQUIREMENTS + ROADMAP + CLAUDE) ✅ DONE YYYY-MM-DD
```

ROADMAP.md Progress table update (line 207):
```markdown
| **v3.1 RBAC hub_admin** | **5** | **20/20** | **20/20** | ✅ **SHIPPED + Phase 5 DONE** | YYYY-MM-DD |
```

CLAUDE.md §6 APPEND pattern (verify exact line via grep §6 + check trailing pattern):
```markdown
### Phase 5 v3.1 Document version history pattern (VER-01..05 — YYYY-MM-DD)

- **Migration 0007 introspect carry forward Plan 01-01:** `sa.inspect()` 3-STEP guard (table existence + FK precondition); downgrade defensive COUNT log + DROP TABLE atomic.
- **Schema 15 cột exact match DocumentVersionAPI interface** (frontend/src/services/api.ts:599-615) — contract bridge BE → FE LOCKED.
- **Service layer 5 public API** (snapshot, restore_to_version, list_versions, get_version_with_chunks, get_version_file_path) + 2 private helper (_compute_file_hash, _enforce_retention).
- **D-V3.1-Phase5-A dedupe-by-hash:** trong cùng document_id, reupload exact same file → reference path cũ; cross-document dedupe defer v4.0.
- **D-V3.1-Phase5-B chunks per-version NO snapshot:** GET /versions/{vid} trả `chunks: []` empty array (FE typecheck happy); restore = re-index version file qua cocoindex.
- **D-V3.1-Phase5-D restore APPEND-ONLY:** snapshot(change_type='restore') TRƯỚC khi UPDATE documents (immutable history forensic preserve).
- **D-V3.1-Phase5-E retention "3 gốc + 2 gần nhất" enforce BE write-time CTE** window function ROW_NUMBER PARTITION (DEVIATION từ ROADMAP "FE filter client-side" per FE codebase audit).
- **D-V3.1-Phase5-H audit action codes:** `document.version.create` + `document.version.restore` với payload nest actor_role + actor_hub_id + document_id + version_number + change_type + restored_to.
- **D-V3.1-Phase5-I restore re-extract SYNC** block endpoint (< 5s small DOCX); large file defer v4.0 async queue.
- **Router universal mount main.py SAU documents_router** (per-hub data, KHÔNG central-only — carry forward FACTOR-01 v3.0).
- **RBAC 3-layer:** 3 GET Layer 3 SSO-04 viewer PASS (D-V3.1-Phase5-C) + POST /restore inline assert_hub_admin_for hybrid pattern (Plan 02-01 v3.1 carry forward).
- **Integration test pattern carry forward Plan 04-02 v3.1** + python-docx inline sample fixture (KHÔNG OpenAI API call).
- **R-V3.1-2 mitigation chain Phase 5:** BE Layer 3 authoritative defense in depth (KHÔNG dựa FE prop canRestore) + scenario 4 cross-hub 403 verified runtime.
- **Audit forensic chain pattern Plan 02-04 v3.1 carry forward:** `payload->>'actor_role'` + `payload->>'document_id'` + `payload->>'version_number'` + `payload->>'restored_to'` queryable cho incident review.
- **Storage explosion mitigation chain Phase 5 new:** dedupe-by-hash + retention 3+2 cap + file cleanup reference count = 0 (worst case 1 doc × 5 versions × 5MB = 25MB; 1000 docs = 25GB acceptable VPS).
```

CLAUDE.md trailing line bump pattern (existing exemplar):
```markdown
---

*Cập nhật: ... 🎉 v3.1 MILESTONE CLOSED 2026-05-24 → re-opened 2026-05-26 cho Phase 5 → Phase 5 DONE YYYY-MM-DD (Document Version History 5 plan / 5 REQ-ID VER-01..05).*
```

Pattern Plan 04-03 v3.1 (`feat/docs(04-03):` commit + 4 file docs atomic — verify exact format):
```bash
git add .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md CLAUDE.md
git commit -m "docs(05-05): closeout Phase 5 Document Version History — 5 plan / 5 REQ-ID VER-01..05 DONE

(body tiếng Việt detail Phase 5 re-open scope + 5 plan summary + R-V3.1-2 mitigation chain + KHÔNG tag mới mặc định semver clean)"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update STATE.md atomic — frontmatter Phase 5 DONE + body APPEND Phase 5 Results Summary section</name>
  <read_first>
    - Hub_All/.planning/STATE.md (TOÀN FILE — verify frontmatter structure + body Phase 1-4 Results Summary sections để pattern match)
    - Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md (decisions LOCKED 9 cái + R-V3.1-2 mitigation chain Phase 5)
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-03-PLAN.md `<context>` (verify Plan 04-03 closeout STATE.md update pattern carry forward)
  </read_first>
  <action>
**Bước 1 — Read STATE.md hiện tại để verify exact frontmatter structure.**

**Bước 2 — Update frontmatter STATE.md (chỉ field cần change):**

Resolve `${TODAY}` qua `date -u +%Y-%m-%d` runtime hoặc dùng `2026-05-26` (cùng ngày Phase 5 plan; nếu execute date khác → update).

Modify YAML frontmatter (Edit từng field — KHÔNG rewrite full file):
- `status:` — UPDATE message thêm Phase 5 DONE: `"v3.1 SHIPPED 2026-05-24 + Phase 5 DONE ${TODAY} (Document Version History 5 plan / 5 REQ-ID VER-01..05; re-open scope cho FE 404 bug catch-up)..."`.
- `last_updated:` — `"${TODAY}T00:00:00.000Z"` (UTC ISO 8601).
- `progress.completed_phases:` — `5` (4 → 5).
- `progress.total_plans:` — `20` (15 → 20).
- `progress.completed_plans:` — `20` (15 → 20).
- `progress.percent:` — `100` (80 → 100).
- `phase_5_status:` — `"DONE"` (PLANNED → DONE).
- ADD `phase_5_done_date:` — `"${TODAY}"`.
- ADD `phase_5_plan_count:` — `5`.
- `next_action:` — `"Phase 5 DONE ${TODAY} (Document Version History 5 plan ship). Chọn 1 trong 2 path: (a) /gsd-complete-milestone v3.1 archive milestone (v3.1 đã tag local 2026-05-24 — operator decide push git tag origin) hoặc (b) /gsd-new-milestone v4.0 Production Hardening fresh discuss."`.

**Bước 3 — APPEND body section mới "Phase 5 Results Summary" SAU Phase 4 Results Summary section (line ~230 hiện tại):**

```markdown
## Phase 5 Results Summary (DONE ${TODAY}) — v3.1 RE-OPEN SCOPE (Document Version History)

5 plan ship 5 REQ-ID VER-01..05 — v3.1 milestone re-opened 2026-05-26 cho FE catch-up scope (FE `DocumentVersionHistory.tsx` đã ship trước BE; user gặp 404 console khi mở tab):

- **Plan 05-01** (VER-01): Alembic migration `0007_document_versions` 15 cột exact match DocumentVersionAPI interface (frontend/src/services/api.ts:599-615) + UNIQUE (document_id, version_number) + INDEX (document_id) + CHECK constraint change_type IN 4 value (reupload|reextract|content_edit|restore). Idempotent introspect pattern Plan 01-01 v3.1 carry forward (sa.inspect 3-STEP: table guard + FK precondition documents + users). downgrade() defensive COUNT(*) log + DROP TABLE atomic (KHÔNG raise RuntimeError vì schema feature-additive — operator review responsibility). 5 integration test PASS (upgrade head idempotent re-run + downgrade rollback + 15 cột verify + UNIQUE enforce + CHECK reject invalid). DSN injection SAFETY pattern Plan 01-03 v3.1 carry forward (monkeypatch env DATABASE_URL + get_settings.cache_clear — KHÔNG Config.set_main_option vì env.py:185-191 runtime override bypass).
- **Plan 05-02** (VER-02): Service `api/app/services/document_version_service.py` 5 public API + 2 private helper. `snapshot(*, session, document, change_type, ...)` atomic 7 step: SHA-256 file_hash compute + dedupe-by-hash check trong cùng document_id (D-V3.1-Phase5-A LOCKED — reupload exact same → reference path cũ KHÔNG FileStore.save) + version_number monotonic SELECT MAX + 1 + INSERT row + _enforce_retention CTE window function ROW_NUMBER PARTITION (D-V3.1-Phase5-E LOCKED — "3 gốc + 2 gần nhất" enforce BE write-time, DEVIATION từ ROADMAP "FE filter client-side" per FE codebase audit) + _cleanup_orphan_files reference count = 0 + audit emit `document.version.create`. `restore_to_version()` append-only D-V3.1-Phase5-D LOCKED — snapshot TRƯỚC khi UPDATE documents.file_path|filename|mime_type|file_size_bytes (immutable history forensic). `list_versions` SELECT DESC. `get_version_with_chunks` trả `(dict, [])` empty array (D-V3.1-Phase5-B LOCKED — KHÔNG snapshot chunks defer v4.0). `get_version_file_path` Path object cho StreamingResponse. Raw SQL via sqlalchemy.text() + named bind params `:doc_id` `:hash` (T-05-02-01 SQL injection mitigation). Audit reuse Plan 02-04 v3.1 `build_audit_payload` + `enqueue_audit`. 8 unit test PASS (snapshot dedupe + monotonic + audit emit + list DESC + chunks empty + Path return + hash 64-char).
- **Plan 05-03** (VER-03 + VER-04): Router `api/app/routers/document_versions.py` 4 endpoint exact match `api.ts:268-285` URL + envelope M2 LOCKED `{success, data, error, meta}` shape. GET /versions list (DESC), GET /versions/{vid} (chunks=[] empty D-V3.1-Phase5-B), GET /versions/{vid}/file (StreamingResponse + Content-Disposition `attachment; filename="v{N}_{name}"` match FE download pattern DocumentVersionHistory.tsx:79), POST /versions/{vid}/restore. RBAC 3-layer: 3 GET endpoint `Depends(get_current_user_for_hub_access)` Layer 3 SSO-04 carry forward Plan 03-03 v3.0 (D-V3.1-Phase5-C LOCKED viewer PASS — read-only feature consistent với GET /api/documents/{id}/file existing); POST /restore `Depends(get_current_user)` + inline `await assert_hub_admin_for(user, db, target_hub_id=document.hub_id)` hybrid pattern Plan 02-01 v3.1 carry forward (D-V3.1-Phase5-C viewer + cross-hub hub_admin reject 403 HUB_ADMIN_REQUIRED). Actor metadata derive: admin → None / hub_admin → doc.hub_id (Plan 02-04 v3.1 pattern). Cocoindex re-extract best-effort SYNC (D-V3.1-Phase5-I LOCKED) — log + continue nếu API mismatch (KHÔNG fail restore). main.py UNIVERSAL mount SAU documents_router (KHÔNG central-only — documents per-hub data carry forward FACTOR-01 v3.0). 10 unit test PASS (4 endpoint × happy path + 5 error case + RBAC inline + actor metadata derive verify).
- **Plan 05-04** (VER-05): Integration test `api/tests/integration/test_document_versions.py` 5 scenario E2E PASS clean qua `make test-integration`. (1) create_version_via_reupload — service snapshot 2 lần với file khác hash → 2 row INSERT + version_number monotonic + dedupe verify. (2) list_returns_ordered_desc — 3 mutation → GET /versions trả [v3, v2, v1] DESC + envelope M2 shape. (3) restore_creates_new_version_append_only — POST /restore từ v1 → v_max+1 INSERT change_type='restore' + documents.file_path UPDATE = v1.file_path + total versions = 2 (KHÔNG xoá v1, D-V3.1-Phase5-D append-only LOCKED). (4) hub_admin_cross_hub_versions_403 — hub_admin dmd POST /restore doc tdt → 403 HUB_ADMIN_REQUIRED envelope (R-V3.1-2 mitigation chain Phase 5 verify defense in depth). (5) audit_forensic_chain — `_assert_audit_version_metadata` poll + assert 2 distinct action `document.version.create` + `document.version.restore` + payload nest actor_role + actor_hub_id + document_id + version_number + restored_to exact match (D-V3.1-Phase5-H LOCKED). Sample_docs fixture python-docx inline tempfile (M2 dep, KHÔNG OpenAI API call test env). Reuse postgres_container + redis_container + alembic_cfg + hub_app_factory + auth_client + admin_user + admin_token + _login_get_token + GO_SEED_HASH từ conftest.py — KHÔNG redeclare. Pattern carry forward test_smoke_e2e_v3_1_rbac.py (Plan 04-02) + test_audit_actor_metadata.py (Plan 02-04) + _wait_audit_row poll helper.
- **Plan 05-05** (closeout): 4 docs source-of-truth atomic update (STATE.md frontmatter + body Phase 5 Results Summary section; REQUIREMENTS.md APPEND 5 dòng `- [x] **VER-XX**`; ROADMAP.md Phase 5 row DONE + Plans checklist 5 plan `[x]` + Progress table v3.1 5 phase / 20 plan / 20 REQ; CLAUDE.md §6 APPEND subsection mới + bump trailing `*Cập nhật:` line). **KHÔNG tạo git tag mới mặc định** (D-V3.1-Phase5 Claude's discretion — v3.1 đã tag 2026-05-24 + semver clean, Phase 5 = re-open scope KHÔNG breaking change); operator option manual `git tag -a v3.1.1 -m "..."` document trong SUMMARY.

**Carry forward patterns (Phase 5 milestone-level — sẵn sàng cho v4.0 / v4.1):**
- Document version history schema 15 cột exact match FE interface (api.ts:599-615) — contract bridge BE → FE LOCKED.
- snapshot dedupe-by-hash trong cùng document_id (D-V3.1-Phase5-A) + retention CTE "3 gốc + 2 gần nhất" (D-V3.1-Phase5-E) + file cleanup reference count = 0 — storage explosion mitigation chain.
- Restore append-only (D-V3.1-Phase5-D) — immutable history pattern carry forward audit_logs M2 + v4.0 per-resource ACL future.
- chunks per-version NO snapshot (D-V3.1-Phase5-B) — FE typecheck happy; restore = re-index qua cocoindex deterministic; cross-version chunk snapshot defer v4.0 cùng dedup strategy.
- Audit action codes `document.version.{create,restore}` + payload nest extend Plan 02-04 v3.1 pattern.
- Router universal mount per-hub data (KHÔNG central-only) — pattern FACTOR-01 v3.0 carry forward.
- RBAC 3 GET Layer 3 SSO-04 viewer PASS + POST hub_admin inline assert_hub_admin_for hybrid — pattern Plan 02-01 v3.1 carry forward future per-resource POST endpoint.
- Integration test python-docx inline sample fixture + cocoindex skip pattern — Plan 04-02 v3.1 carry forward future feature E2E test KHÔNG OpenAI API call.

**R-V3.1-2 MEDIUM mitigation chain Phase 5:**
- BE Layer 3 `assert_hub_admin_for` authoritative — KHÔNG dựa FE `canRestore` prop. Plan 05-03 POST /restore enforce inline.
- Test Plan 05-04 scenario 4 hub_admin dmd → doc tdt → 403 HUB_ADMIN_REQUIRED verify defense in depth runtime.
- 3 GET endpoint Layer 3 SSO-04 enforce JWT.hub_ids ⊇ {settings.hub_name} (Plan 03-03 v3.0) — cross-hub viewer reject 403 CROSS_HUB_ACCESS_DENIED runtime.

**Phase 5 new constraints (carry forward future):**
- Storage explosion (D-V3.1-Phase5-A + D-V3.1-Phase5-E chain): dedupe-by-hash + retention cap COUNT(*) ≤ 5 per document + file cleanup reference count = 0; worst case 1 doc × 5 versions × 5MB = 25MB; 1000 docs = 25GB acceptable VPS.
- Cocoindex re-extract sync block (D-V3.1-Phase5-I): small file < 5s acceptable; large file > 10MB defer v4.0 async queue Celery/RQ/Postgres LISTEN/NOTIFY.

**Phase 5 backward compat (KHÔNG break v3.0 + v3.1 Phase 1-4):**
- FE `DocumentVersionHistory.tsx` UNCHANGED (R-V3-2 minimal scope — chỉ verify FE render OK sau BE ship; FE đã ship trước milestone trước).
- M2 LocalStorage + envelope M2 shape preserve LOCKED.
- Phase 2 v3.1 envelope codes (HUB_ADMIN_REQUIRED + ROLE-04 helper + Plan 02-04 audit nest) carry forward Phase 5 router POST /restore + service emit.
- KHÔNG đụng FE source code (frontend/src/services/api.ts + frontend/src/components/DocumentVersionHistory.tsx).
- KHÔNG đụng existing routers/services (documents.py + documents_service.py + file_store.py + audit_service.py + dependencies.py).
- KHÔNG đụng migration 0001-0006 source (chỉ ADD migration 0007 mới).

**Next:** User decide:
- `/gsd-complete-milestone v3.1` — archive `.planning/milestones/v3.1-rbac-hub-admin-archive/` + reset ROADMAP.md cho v4.0 backlog.
- `/gsd-new-milestone v4.0` — skip archive, fresh discuss-milestone (Production Hardening + Advanced RAG per memory `project_v3_multi_hub_split` seed + HA Redis cluster + OCR Vietnamese + streaming `/api/ask` SSE + coverage >80% + per-resource ACL granular).
- `git push origin v3.1` — manual push tag annotated nếu chưa push (Plan 04-03 chỉ tag local).
- (Optional) `git tag -a v3.1.1 -m "v3.1.1 Phase 5 Document Version History"` — operator quyết định tag mới distinguish post-v3.1 work (default Plan 05-05 KHÔNG tag, semver clean).
```

**Lưu ý quan trọng:**
- `${TODAY}` placeholder — executor resolve runtime qua `date -u +%Y-%m-%d` HOẶC fallback `2026-05-26` nếu execute cùng ngày plan.
- Edit qua Edit tool — KHÔNG rewrite full file (giảm risk break frontmatter/body).
- APPEND Phase 5 section SAU Phase 4 Results Summary (line ~230 hiện tại) — KHÔNG insert đầu file.
- Pattern carry forward Plan 04-03 v3.1 closeout exactly — verify before commit.
  </action>
  <verify>
    <automated>grep -c "phase_5_status: \"DONE\"" Hub_All/.planning/STATE.md && grep -c "phase_5_done_date" Hub_All/.planning/STATE.md && grep -c "Phase 5 Results Summary" Hub_All/.planning/STATE.md && grep -c "VER-01..05" Hub_All/.planning/STATE.md && grep -c "completed_plans: 20" Hub_All/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/.planning/STATE.md` chứa `phase_5_status: "DONE"` (frontmatter update).
    - File chứa `phase_5_done_date:` (ADD field mới).
    - File chứa `phase_5_plan_count: 5` (ADD field mới).
    - File chứa `completed_phases: 5` (4 → 5).
    - File chứa `total_plans: 20` (15 → 20).
    - File chứa `completed_plans: 20` (15 → 20).
    - File chứa `percent: 100` (80 → 100).
    - File chứa `next_action:` MENTION `Phase 5 DONE` + `complete-milestone` HOẶC `new-milestone v4.0`.
    - File chứa `## Phase 5 Results Summary` (body section APPEND).
    - File chứa `VER-01..05` (Phase 5 REQ-ID range).
    - File chứa `Plan 05-01` + `Plan 05-02` + `Plan 05-03` + `Plan 05-04` + `Plan 05-05` (5 plan summary).
    - File chứa `D-V3.1-Phase5-A` + `D-V3.1-Phase5-B` + `D-V3.1-Phase5-D` + `D-V3.1-Phase5-E` + `D-V3.1-Phase5-H` (decision traceability ≥ 5 mention).
    - File chứa `R-V3.1-2 MEDIUM mitigation chain Phase 5` (risk traceability).
    - File chứa `Carry forward patterns` cho Phase 5 (≥ 5 bullet).
    - File chứa "KHÔNG tạo git tag mới mặc định" HOẶC "KHÔNG tag" + "operator quyết định" (D-V3.1-Phase5 discretion).
    - Frontmatter YAML hợp lệ — `python -c "import yaml; print(yaml.safe_load(open('Hub_All/.planning/STATE.md').read().split('---')[1]))"` exit 0.
  </acceptance_criteria>
  <done>
    STATE.md frontmatter Phase 5 DONE + body APPEND Phase 5 Results Summary section đầy đủ; 5 plan summary + R-V3.1-2 mitigation chain + carry forward patterns + next_action 2 option.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md + ROADMAP.md atomic — VER-01..05 [x] mark + Phase 5 DONE + Progress table</name>
  <read_first>
    - Hub_All/.planning/REQUIREMENTS.md (TOÀN FILE — verify hiện tại MIGRATE-02 section cuối; identify location APPEND VER-XX)
    - Hub_All/.planning/ROADMAP.md (line 29 Phase 5 row + line 178 Plans checklist + line 207 Progress table — exact location edit)
    - Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md `<specifics>` Plan 05-05 (5 dòng VER-XX format expectation)
  </read_first>
  <action>
**Bước 1 — APPEND REQUIREMENTS.md section "Phase 5 — Document Version History":**

Read full REQUIREMENTS.md để identify section cuối (likely MIGRATE-02 v3.1 Phase 4 hoặc tương tự). APPEND SAU section cuối — KHÔNG insert giữa file.

Content APPEND:

```markdown

## Phase 5 — Document Version History (v3.1 re-open 2026-05-26)

**Trigger:** v3.1 milestone re-opened 2026-05-26 cho FE catch-up scope — frontend `DocumentVersionHistory.tsx` đã ship trước BE ở milestone trước; user gặp 404 console khi mở "Lịch sử phiên bản" tab. Backend hoàn toàn KHÔNG có `document_versions` table + 4 endpoint missing. Phase 5 backend catch-up bắt buộc (R-V3-2 minimal scope FE KHÔNG đụng).

- [x] **VER-01** — Alembic migration `0007_document_versions` table tạo schema 15 cột exact match `DocumentVersionAPI` interface (frontend/src/services/api.ts:599-615): id UUID PK + document_id UUID FK ON DELETE CASCADE + version_number INT + is_original BOOL + name TEXT + file_type TEXT + file_size BIGINT + file_path TEXT + file_hash TEXT NULL (SHA-256 hex 64-char dedupe key D-V3.1-Phase5-A) + extractor_used TEXT NULL + chunk_count INT DEFAULT 0 + change_type TEXT CHECK IN 4 value + change_note TEXT NULL + created_by UUID FK ON DELETE SET NULL + created_at TIMESTAMPTZ. UNIQUE (document_id, version_number) + INDEX (document_id). Idempotent introspect (sa.inspect) carry forward Plan 01-01 v3.1 pattern + downgrade() defensive COUNT log + DROP TABLE atomic. (DONE ${TODAY} — Plan 05-01).
- [x] **VER-02** — Service `api/app/services/document_version_service.py` 5 public API (`snapshot`, `restore_to_version`, `list_versions`, `get_version_with_chunks`, `get_version_file_path`) + 2 private helper (`_compute_file_hash` SHA-256, `_enforce_retention` CTE window function). Implement D-V3.1-Phase5-A dedupe-by-hash (trong cùng document_id) + D-V3.1-Phase5-D restore append-only (snapshot TRƯỚC UPDATE documents) + D-V3.1-Phase5-E retention "3 gốc + 2 gần nhất" CTE write-time enforce + D-V3.1-Phase5-H audit emit 2 action codes (`document.version.create` + `document.version.restore`). Raw SQL via sqlalchemy.text() + named bind params (T-05-02-01 mitigation). Reuse Plan 02-04 v3.1 `build_audit_payload` + `enqueue_audit`. 8 unit test PASS. (DONE ${TODAY} — Plan 05-02).
- [x] **VER-03** — Router `api/app/routers/document_versions.py` 4 endpoint exact match `frontend/src/services/api.ts:268-285` URL + envelope M2 LOCKED `{success, data, error, meta}` shape: `GET /api/documents/{id}/versions` (list DESC), `GET /api/documents/{id}/versions/{vid}` (detail + `chunks: []` empty D-V3.1-Phase5-B), `GET /api/documents/{id}/versions/{vid}/file` (StreamingResponse + `Content-Disposition: attachment; filename="v{N}_{name}"`), `POST /api/documents/{id}/versions/{vid}/restore` (rollback + audit emit). Universal mount `main.py::create_app` SAU `documents_router` (per-hub data, KHÔNG central-only — carry forward FACTOR-01 v3.0). Cocoindex re-extract best-effort SYNC (D-V3.1-Phase5-I LOCKED). 10 unit test PASS. (DONE ${TODAY} — Plan 05-03).
- [x] **VER-04** — RBAC 3-layer hub-scope: **Layer 1** Settings `_enforce_hub_dsn_match` boot-time (Plan 04-04 v3.0 carry forward); **Layer 2** repository per-hub filter ở service layer (Plan 05-02 query qua document_id); **Layer 3** dependency — 3 GET endpoint `Depends(get_current_user_for_hub_access)` SSO-04 viewer + editor + hub_admin + super admin PASS (D-V3.1-Phase5-C LOCKED — Plan 03-03 v3.0 carry forward) + POST `/restore` endpoint `Depends(get_current_user)` + inline `await assert_hub_admin_for(user, db, target_hub_id=document.hub_id)` hybrid pattern Plan 02-01 v3.1 carry forward (viewer + cross-hub hub_admin reject 403 `HUB_ADMIN_REQUIRED` envelope). Actor metadata derive: admin → None / hub_admin → doc.hub_id (Plan 02-04 v3.1 pattern). Audit emit 2 action `document.version.{create,restore}` qua service layer. (DONE ${TODAY} — Plan 05-03).
- [x] **VER-05** — Integration test `api/tests/integration/test_document_versions.py` 5 scenario E2E PASS clean qua `make test-integration`: (1) `create_version_via_reupload` 2 row INSERT + dedupe verify, (2) `list_returns_ordered_desc` GET DESC + envelope M2 shape, (3) `restore_creates_new_version_append_only` v_max+1 INSERT + documents UPDATE + total tăng 1 (D-V3.1-Phase5-D append-only), (4) `hub_admin_cross_hub_versions_403` HUB_ADMIN_REQUIRED envelope (R-V3.1-2 mitigation chain Phase 5 defense in depth), (5) `audit_forensic_chain` query `payload->>'actor_role'` + `payload->>'document_id'` + `payload->>'version_number'` + `payload->>'restored_to'` exact match (D-V3.1-Phase5-H). Sample_docs fixture python-docx inline tempfile (M2 dep, KHÔNG OpenAI API call). Reuse `postgres_container + redis_container + alembic_cfg + hub_app_factory + auth_client + admin_user + admin_token + _login_get_token + GO_SEED_HASH` từ conftest.py. Pattern carry forward Plan 04-02 v3.1 + Plan 02-04 v3.1 _wait_audit_row poll helper. (DONE ${TODAY} — Plan 05-04).

Total v3.1 REQ: 20 (Phase 1 ROLE-01..04 = 4 + Phase 2 DEP-01..05 = 5 + Phase 3 FE-01..04 = 4 + Phase 4 MIGRATE-01..02 = 2 + **Phase 5 VER-01..05 = 5**).
```

**Bước 2 — Update ROADMAP.md 3 location:**

**Edit 1 — Phase 5 row (line 29 hiện tại):**
```markdown
| **5** | Document version history (VER) ✅ DONE ${TODAY} | Build version-history feature backend (FE ship trước, user gặp 404). Alembic migration tạo `document_versions` table 15 cột match `DocumentVersionAPI` + service snapshot khi document mutate + 4 endpoint router + audit logging + RBAC hub-scope (carry forward Phase 2 `assert_hub_admin_for`). | VER-01..05 (5) | 5 | Phase 2 (RBAC) — independent của Phase 3+4 |
```

**Edit 2 — Plans checklist Phase 5 section (line 178 "TBD"):**

REPLACE line 178 `Plans: (TBD — sẽ điền sau /gsd-plan-phase 5)` với:

```markdown
**Plans:** 5 plans (Wave 1 BLOCKING migration + Wave 2 BLOCKING service + Wave 3 BLOCKING router + Wave 4 BLOCKING integration test + Wave 5 BLOCKING closeout — linear critical path).

Plans:
- [x] 05-01-PLAN.md — Alembic migration 0007 document_versions table (15 cột exact match DocumentVersionAPI + UNIQUE doc_ver + INDEX document_id + CHECK change_type) + 5 integration test idempotent với DSN injection SAFETY pattern Plan 01-03 carry forward (VER-01) ✅ DONE ${TODAY}
- [x] 05-02-PLAN.md — document_version_service.py 5 public API + 2 helper (snapshot dedupe by hash D-V3.1-Phase5-A + restore append-only D-V3.1-Phase5-D + retention CTE D-V3.1-Phase5-E + audit emit 2 action D-V3.1-Phase5-H) + 8 unit test (VER-02) ✅ DONE ${TODAY}
- [x] 05-03-PLAN.md — document_versions.py router 4 endpoint exact match api.ts:268-285 (envelope M2 + chunks=[] D-V3.1-Phase5-B + StreamingResponse) + RBAC 3-layer (Layer 3 SSO-04 GET + inline assert_hub_admin_for POST /restore) + universal mount main.py + 10 unit test (VER-03 + VER-04) ✅ DONE ${TODAY}
- [x] 05-04-PLAN.md — Integration test test_document_versions.py 5 scenario E2E (create + list DESC + restore append-only + cross-hub 403 + audit forensic chain) qua `make test-integration` (VER-05) ✅ DONE ${TODAY}
- [x] 05-05-PLAN.md — Closeout 4 docs atomic (STATE + REQUIREMENTS + ROADMAP + CLAUDE) + KHÔNG git tag mới mặc định (D-V3.1-Phase5 discretion semver clean) ✅ DONE ${TODAY}
```

**Edit 3 — Progress table row v3.1 (line 207):**
```markdown
| **v3.1 RBAC hub_admin** | **5** | **20/20** | **20/20** | ✅ **SHIPPED + Phase 5 DONE** | ${TODAY} |
```

**Edit 4 — Milestones bullet line 13 (cập nhật):**
```markdown
- ✅ **v3.1 RBAC hub_admin** — Shipped 2026-05-24, 4 phase / 15 REQ-ID / 15 plan + Phase 5 re-open DONE ${TODAY} (5 plan / 5 REQ VER-01..05 Document Version History; total 20 plan / 20 REQ); D-V3.1-04 phase numbering reset về 1.
```

**Lưu ý:**
- Edit qua Edit tool — KHÔNG rewrite full ROADMAP.md.
- 4 edit locations exact — verify trước khi commit.
- `${TODAY}` resolve runtime hoặc 2026-05-26 (cùng ngày plan).
- REQUIREMENTS.md APPEND SAU section cuối — verify exact location qua read full file trước APPEND.
  </action>
  <verify>
    <automated>grep -c "VER-01" Hub_All/.planning/REQUIREMENTS.md && grep -c "VER-05" Hub_All/.planning/REQUIREMENTS.md && grep -c "Phase 5 — Document Version History" Hub_All/.planning/REQUIREMENTS.md && grep -c "✅ DONE" Hub_All/.planning/ROADMAP.md && grep -c "20/20" Hub_All/.planning/ROADMAP.md && grep -c "05-01-PLAN.md" Hub_All/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/.planning/REQUIREMENTS.md` chứa `## Phase 5 — Document Version History (v3.1 re-open 2026-05-26)` section header.
    - File chứa `- [x] **VER-01**` + `- [x] **VER-02**` + `- [x] **VER-03**` + `- [x] **VER-04**` + `- [x] **VER-05**` (5 dòng REQ marked done).
    - File chứa `Total v3.1 REQ: 20` HOẶC `5 + 5 = 20` (cập nhật count).
    - File chứa `Plan 05-01` + `Plan 05-02` + `Plan 05-03` + `Plan 05-04` (4 plan reference; Plan 05-05 closeout KHÔNG xuất hiện cùng VER-XX vì chỉ docs).
    - File chứa `D-V3.1-Phase5-A` + `D-V3.1-Phase5-B` + `D-V3.1-Phase5-D` + `D-V3.1-Phase5-E` + `D-V3.1-Phase5-H` (≥ 5 decision traceability).
    - File `Hub_All/.planning/ROADMAP.md` Phase 5 row chứa `✅ DONE` (KHÔNG còn `📋 PLANNED`).
    - File chứa `05-01-PLAN.md` + `05-02-PLAN.md` + `05-03-PLAN.md` + `05-04-PLAN.md` + `05-05-PLAN.md` (5 plan reference).
    - File chứa `- [x] 05-01-PLAN.md` ≥ 5 lần (5 checkbox checked — 1 mỗi plan).
    - File chứa Progress table `**20/20**` hoặc tương đương (5 phase / 20 plan / 20 REQ).
    - File chứa `v3.1.*Phase 5 re-open DONE` HOẶC `Phase 5.*DONE` ở Milestones bullet (line ~13).
    - File KHÔNG còn `Plans: (TBD` ở Phase 5 section.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md APPEND Phase 5 section với 5 VER-XX [x] + plan reference; ROADMAP.md 4 edit (Phase 5 row DONE + Plans checklist 5 [x] + Progress table v3.1 20/20 + Milestones bullet update).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md §6 APPEND subsection mới Phase 5 + bump trailing Cập nhật line + git commit atomic 4 file docs</name>
  <read_first>
    - Hub_All/CLAUDE.md (TOÀN FILE — verify §6 location + existing Phase 1-4 v3.1 subsection format để mirror; verify trailing `*Cập nhật:` line vị trí)
    - Hub_All/.planning/phases/04-migration-smoke-e2e/04-03-PLAN.md (verify Plan 04-03 v3.1 CLAUDE.md §6 APPEND pattern carry forward)
    - Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md `<specifics>` Plan 05-05 (CLAUDE.md §6 subsection content expectation)
  </read_first>
  <action>
**Bước 1 — Read CLAUDE.md TOÀN FILE để identify §6 location + existing Phase 1-4 v3.1 subsection format.**

Grep `### Phase 4 v3.1` để identify ngày cuối Phase 1-4 v3.1 subsection. APPEND subsection mới SAU `### Phase 4 v3.1` subsection.

**Bước 2 — APPEND `### Phase 5 v3.1 Document version history pattern (VER-01..05 — ${TODAY})` subsection:**

```markdown

### Phase 5 v3.1 Document version history pattern (VER-01..05 — ${TODAY})

**Trigger:** v3.1 milestone re-opened 2026-05-26 (sau v3.1 SHIPPED 2026-05-24) — FE `DocumentVersionHistory.tsx` đã ship trước BE; user gặp 404 console khi mở "Lịch sử phiên bản" tab. Phase 5 backend catch-up — KHÔNG đụng FE (R-V3-2 minimal scope).

- **Alembic migration 0007 introspect carry forward Plan 01-01:** `sa.inspect()` 3-STEP guard (table existence + FK precondition documents + users); downgrade defensive COUNT log + DROP TABLE atomic (KHÔNG RuntimeError vì schema feature-additive).
- **Schema 15 cột exact match `DocumentVersionAPI` interface** (`frontend/src/services/api.ts:599-615`) — contract bridge BE → FE LOCKED. Field order match optional `?` (file_hash + extractor_used + change_note + created_by nullable).
- **Service layer 5 public API:** `snapshot`, `restore_to_version`, `list_versions`, `get_version_with_chunks`, `get_version_file_path`. 2 private helper: `_compute_file_hash` (SHA-256 hex 64-char), `_enforce_retention` (CTE window function ROW_NUMBER PARTITION).
- **D-V3.1-Phase5-A dedupe-by-hash:** trong cùng `document_id`, reupload exact same file → reference path cũ KHÔNG `FileStore.save` mới; cross-document dedupe defer v4.0 cần `file_store` schema migration thêm `reference_count`.
- **D-V3.1-Phase5-B chunks per-version NO snapshot:** `GET /versions/{vid}` trả `chunks: []` empty array (FE typecheck happy — `DocumentVersionChunkAPI` interface declared NHƯNG BE empty). Restore = re-index version file qua cocoindex deterministic.
- **D-V3.1-Phase5-D restore APPEND-ONLY:** `snapshot(change_type='restore')` TRƯỚC khi UPDATE `documents.file_path|filename|mime_type|file_size_bytes` — immutable history forensic preserve. KHÔNG xoá older version.
- **D-V3.1-Phase5-E retention "3 gốc + 2 gần nhất" enforce BE write-time CTE** — DEVIATION từ ROADMAP "FE filter client-side" per FE codebase audit (`DocumentVersionHistory.tsx:158` KHÔNG có filter logic; hint line 146 "Lưu tối đa 5 phiên bản" text-only). SQL CTE: `WITH ranked AS (SELECT ... ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number) AS asc_rank, ROW_NUMBER() OVER (... DESC) AS desc_rank FROM document_versions WHERE document_id = :doc_id) DELETE ... WHERE asc_rank > 3 AND desc_rank > 2 RETURNING file_path`. File cleanup chỉ khi `SELECT COUNT(*) WHERE file_path = $1 == 0` (cross-version dedupe safe).
- **D-V3.1-Phase5-H audit action codes:** `document.version.create` (snapshot mỗi trigger) + `document.version.restore` (POST /restore). Payload nest `actor_role` + `actor_hub_id` + `document_id` + `version_number` + `change_type` + `restored_to` (chỉ cho restore action). Reuse Plan 02-04 v3.1 `build_audit_payload` + `enqueue_audit`. Forensic query: `SELECT payload->>'document_id', payload->>'version_number', payload->>'restored_to' FROM audit_logs WHERE action='document.version.restore' ORDER BY created_at DESC LIMIT 10`.
- **D-V3.1-Phase5-I restore re-extract SYNC** block endpoint (< 5s small DOCX); cocoindex `update_blocking()` best-effort log + continue nếu API mismatch (KHÔNG fail restore). Large file > 10MB defer v4.0 async queue Celery/RQ/Postgres LISTEN/NOTIFY.
- **Router universal mount `main.py::create_app` SAU `documents_router`** — per-hub data (documents per-hub), KHÔNG central-only block; carry forward FACTOR-01 v3.0 Phase 2 pattern.
- **RBAC 3-layer (D-V3.1-Phase5-C LOCKED viewer PASS):**
  - **Layer 1:** Settings `_enforce_hub_dsn_match` boot-time (Plan 04-04 v3.0 carry forward).
  - **Layer 2:** Repository per-hub filter ở service (Plan 05-02 query qua document_id).
  - **Layer 3:** 3 GET endpoint `Depends(get_current_user_for_hub_access)` SSO-04 — viewer + editor + hub_admin + super admin PASS (read-only consistent với GET `/api/documents/{id}/file` existing). POST `/restore` endpoint `Depends(get_current_user)` + inline `await assert_hub_admin_for(user, db, target_hub_id=document.hub_id)` hybrid pattern Plan 02-01 v3.1 — viewer + cross-hub hub_admin reject 403 `HUB_ADMIN_REQUIRED` envelope.
- **Actor metadata derive:** admin → `actor_hub_id=None` / hub_admin → `actor_hub_id=document.hub_id` (Plan 02-04 v3.1 pattern carry forward).
- **Integration test pattern Plan 04-02 v3.1 carry forward** + python-docx inline `Document().add_paragraph + doc.save(tempfile_path)` (M2 dep, KHÔNG OpenAI API call test env). Reuse `postgres_container + redis_container + alembic_cfg + hub_app_factory + auth_client + admin_user + admin_token + _login_get_token + GO_SEED_HASH` từ conftest.py — KHÔNG redeclare. `_wait_audit_row` poll helper pattern test_audit_actor_metadata.py:92 carry forward.
- **R-V3.1-2 MEDIUM mitigation chain Phase 5:** BE Layer 3 `assert_hub_admin_for` authoritative — KHÔNG dựa FE prop `canRestore` (UX layer). Test Plan 05-04 scenario 4 hub_admin dmd → doc tdt → 403 verify defense in depth runtime.
- **Storage explosion mitigation chain Phase 5 new:** dedupe-by-hash (D-V3.1-Phase5-A) + retention "3 gốc + 2 gần nhất" cap COUNT(*) ≤ 5 per document (D-V3.1-Phase5-E) + file cleanup reference count = 0 (cross-version safe). Worst case 1 doc × 5 versions × 5MB = 25MB; 1000 docs = 25GB acceptable VPS.
- **Cocoindex re-extract sync block (D-V3.1-Phase5-I):** small file < 5s acceptable; large file > 10MB defer v4.0 async queue.
- **FE contract LOCKED — KHÔNG đụng frontend (R-V3-2 minimal scope):** Plan 05-01 schema 15 field exact match `DocumentVersionAPI` interface; Plan 05-03 4 endpoint URL + response envelope exact match `api.ts:268-285`; Plan 05-04 scenario 1 test giả hành verify FE typecheck `chunks: []` empty array runtime.
- **Closeout 4 docs atomic + KHÔNG git tag mới mặc định** (D-V3.1-Phase5 Claude's discretion — v3.1 đã tag 2026-05-24 + semver clean, Phase 5 = re-open scope KHÔNG breaking change). Operator option manual `git tag -a v3.1.1` document trong SUMMARY.
```

**Bước 3 — Bump trailing `*Cập nhật:` line:**

Grep `^\*Cập nhật:` trong CLAUDE.md để identify location (thường ở cuối file). Edit line cuối thêm Phase 5 mention:

```markdown
*Cập nhật: 🎉 v3.1 MILESTONE CLOSED 2026-05-24 → re-opened 2026-05-26 cho Phase 5 → Phase 5 DONE ${TODAY} (Document Version History 5 plan / 5 REQ-ID VER-01..05).*
```

**Bước 4 — Git commit atomic 4 file docs:**

```bash
git add Hub_All/.planning/STATE.md Hub_All/.planning/REQUIREMENTS.md Hub_All/.planning/ROADMAP.md Hub_All/CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(05-05): closeout Phase 5 Document Version History — 5 plan / 5 REQ-ID VER-01..05 DONE

Phase 5 v3.1 re-open scope (trigger: FE DocumentVersionHistory.tsx đã ship trước
BE; user gặp 404 console khi mở "Lịch sử phiên bản" tab). 5 plan ship 5 REQ-ID
VER-01..05 — backend catch-up bắt buộc (R-V3-2 minimal scope FE KHÔNG đụng):

- Plan 05-01 (VER-01): Alembic migration 0007 document_versions table 15 cột
  exact match DocumentVersionAPI interface (api.ts:599-615) + UNIQUE doc_ver +
  INDEX document_id + CHECK change_type. Idempotent introspect Plan 01-01 v3.1
  carry forward. 5 integration test PASS với DSN injection SAFETY Plan 01-03
  carry forward.
- Plan 05-02 (VER-02): document_version_service.py 5 public API + 2 helper —
  dedupe by hash D-V3.1-Phase5-A + restore append-only D-V3.1-Phase5-D +
  retention CTE D-V3.1-Phase5-E + audit emit 2 action D-V3.1-Phase5-H. 8 unit
  test PASS.
- Plan 05-03 (VER-03 + VER-04): Router 4 endpoint exact match api.ts:268-285 +
  RBAC 3-layer (Layer 3 SSO-04 GET viewer PASS D-V3.1-Phase5-C + inline
  assert_hub_admin_for POST /restore Plan 02-01 v3.1 hybrid pattern carry
  forward) + universal mount main.py FACTOR-01 v3.0 + chunks=[] empty
  D-V3.1-Phase5-B. 10 unit test PASS.
- Plan 05-04 (VER-05): Integration test test_document_versions.py 5 scenario E2E
  PASS (create dedupe + list DESC + restore append-only + cross-hub 403 +
  audit forensic chain). Pattern carry forward Plan 04-02 v3.1 + python-docx
  inline sample fixture KHÔNG OpenAI API call.
- Plan 05-05 (closeout): 4 docs atomic STATE + REQUIREMENTS + ROADMAP + CLAUDE.

R-V3.1-2 mitigation chain Phase 5 verified runtime — BE Layer 3 authoritative
defense in depth (scenario 4 cross-hub 403 HUB_ADMIN_REQUIRED).

KHÔNG git tag mới mặc định (D-V3.1-Phase5 discretion — v3.1 đã tag 2026-05-24
+ semver clean; operator option manual git tag -a v3.1.1 document SUMMARY).

Total v3.1: 5 phase / 20 plan / 20 REQ-ID (4 phase RBAC ship 2026-05-24 + Phase
5 Document Version History ship ${TODAY}).
EOF
)"
```

**Lưu ý:**
- 4 file path tuyệt đối (Windows): dùng `git add` với relative path từ git root.
- `$(cat <<'EOF' ... EOF)` heredoc pattern carry forward Plan 02-05 + 03-04 + 04-03 v3.1 (`HEREDOC` format multi-line commit message).
- `${TODAY}` resolve runtime hoặc 2026-05-26.
- KHÔNG dùng `git tag` (default — semver clean).
- KHÔNG `git push` (operator decide).
- KHÔNG dùng `--no-verify` (pre-commit hooks honor nếu có).
  </action>
  <verify>
    <automated>grep -c "Phase 5 v3.1 Document version history pattern" Hub_All/CLAUDE.md && grep -c "VER-01..05" Hub_All/CLAUDE.md && grep -E "^\*Cập nhật:" Hub_All/CLAUDE.md | head -1 && git log -1 --oneline | grep -c "05-05"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/CLAUDE.md` chứa `### Phase 5 v3.1 Document version history pattern (VER-01..05` (subsection header APPEND).
    - File chứa `D-V3.1-Phase5-A` + `D-V3.1-Phase5-B` + `D-V3.1-Phase5-D` + `D-V3.1-Phase5-E` + `D-V3.1-Phase5-H` (≥ 5 decision traceability).
    - File chứa `R-V3.1-2 MEDIUM mitigation chain Phase 5` (risk traceability).
    - File chứa `FACTOR-01 v3.0` (carry forward pattern reference).
    - File chứa `Plan 01-01 v3.1` + `Plan 02-01 v3.1` + `Plan 02-04 v3.1` + `Plan 03-03 v3.0` + `Plan 04-02 v3.1` (≥ 5 plan reference).
    - File chứa `KHÔNG git tag mới mặc định` HOẶC `KHÔNG tag` + `semver clean` (D-V3.1-Phase5 discretion mention).
    - File chứa trailing `*Cập nhật:` line mention `Phase 5 DONE` HOẶC `Phase 5.*Document Version History`.
    - Git commit log mới chứa `docs(05-05):` + `Phase 5 Document Version History` + `VER-01..05` (verify `git log -1`).
    - Git status clean SAU commit (verify `git status --short` empty).
    - 4 file docs đã commit cùng atomic: STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md.
    - KHÔNG có file ngoài 4 docs trong commit (verify `git show --name-only HEAD` chỉ chứa 4 file `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `CLAUDE.md`).
    - KHÔNG git tag v3.1.1 mới (verify `git tag --list v3.1.1` empty).
  </acceptance_criteria>
  <done>
    CLAUDE.md §6 APPEND Phase 5 subsection + bump trailing line; git commit atomic 4 file docs với message tiếng Việt detail Phase 5 re-open scope; KHÔNG git tag mới (semver clean default).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05-05 closeout → 4 source-of-truth docs | Markdown edit qua Edit tool — KHÔNG runtime user input; threat model giới hạn ở docs integrity + git commit atomic. |
| Git commit → repository state | Single commit atomic 4 file; KHÔNG split risk partial state; KHÔNG --no-verify (pre-commit hooks honor). |
| KHÔNG git tag mới | D-V3.1-Phase5 discretion — semver clean preserve; operator manual decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Frontmatter YAML invalid sau edit → workflow tooling fail parse | mitigate | Edit chỉ field cần update — KHÔNG rewrite full block; verify qua `yaml.safe_load` sau edit. |
| T-05-05-02 | Repudiation | 4 file docs split commit → partial state nếu fail giữa chừng | mitigate | Single atomic commit `git add 4 files + git commit` — git built-in atomicity. |
| T-05-05-03 | Integrity | Stale `${TODAY}` placeholder KHÔNG resolve → docs chứa literal `${TODAY}` | mitigate | Executor resolve runtime qua `date -u +%Y-%m-%d` HOẶC fallback `2026-05-26`; grep verify KHÔNG còn `${TODAY}` literal sau commit. |
| T-05-05-04 | DoS | 4 file docs lớn → git diff slow | accept | Tổng 4 file < 100KB; pre-commit hooks chạy < 5s acceptable. |
| T-05-05-05 | Elevation | Git tag v3.1.1 vô tình tạo break semver | mitigate | Default KHÔNG tag (D-V3.1-Phase5 discretion semver clean); operator manual `git tag -a v3.1.1` decide. |
</threat_model>

<verification>
- STATE.md frontmatter `phase_5_status: DONE` + body APPEND Phase 5 Results Summary section.
- REQUIREMENTS.md APPEND 5 dòng `VER-01..05 [x]` + Total count 20.
- ROADMAP.md 4 edit (Phase 5 row + Plans checklist + Progress table + Milestones bullet).
- CLAUDE.md §6 APPEND subsection mới + bump trailing line.
- Git commit atomic 4 file docs với message tiếng Việt detail.
- KHÔNG git tag mới (default semver clean).
- KHÔNG break v3.1 Phase 1-4 docs state.
- Plan 05-04 integration test PASS clean qua make test-integration (Wave 4 verification chain).
- ROADMAP success criterion #1-5 satisfied (Plan 05-01 + 05-02 + 05-03 + 05-04 ship runtime verified).
</verification>

<success_criteria>
1. 4 file docs cập nhật atomic — single commit.
2. STATE.md Phase 5 DONE + Results Summary section.
3. REQUIREMENTS.md APPEND 5 VER-XX [x] + plan reference.
4. ROADMAP.md Phase 5 row DONE + Plans checklist 5 [x] + Progress 20/20.
5. CLAUDE.md §6 subsection APPEND + trailing line bump.
6. Git commit message tiếng Việt detail Phase 5 re-open scope + 5 plan summary.
7. KHÔNG git tag mới (D-V3.1-Phase5 discretion semver clean default).
8. KHÔNG break v3.1 Phase 1-4 docs state (regression check).
9. v3.1 milestone state: 5 phase / 20 plan / 20 REQ-ID — re-open scope DONE.
10. User know next path: complete-milestone v3.1 archive HOẶC new-milestone v4.0.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/05-document-version-history/05-05-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 4 file docs cập nhật atomic (STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md)
- STATE.md frontmatter Phase 5 DONE + body APPEND Phase 5 Results Summary section đầy đủ (5 plan summary + R-V3.1-2 mitigation chain Phase 5 + carry forward patterns + next_action 2 option)
- REQUIREMENTS.md APPEND Phase 5 section với 5 VER-XX [x] + plan reference; total v3.1 REQ count 15 → 20
- ROADMAP.md 4 edit (Phase 5 row table DONE + Plans checklist 5 plan [x] + Progress table 20/20 + Milestones bullet line cập nhật)
- CLAUDE.md §6 APPEND subsection mới (~15 bullet point) Phase 5 v3.1 Document version history pattern + bump trailing *Cập nhật: line
- Git commit atomic single với message tiếng Việt detail Phase 5 re-open scope + 5 plan summary + R-V3.1-2 mitigation chain
- KHÔNG git tag mới (D-V3.1-Phase5 discretion semver clean — v3.1 đã tag 2026-05-24; operator option manual `git tag -a v3.1.1` document)
- KHÔNG `git push` (operator decide)
- v3.1 milestone re-open scope DONE — 5 phase / 20 plan / 20 REQ-ID consumed
- User next path: (a) `/gsd-complete-milestone v3.1` archive + reset ROADMAP cho v4.0 backlog HOẶC (b) `/gsd-new-milestone v4.0` Production Hardening fresh discuss

Commit atomic: `docs(05-05): closeout Phase 5 Document Version History — 5 plan / 5 REQ-ID VER-01..05 DONE` + body tiếng Việt detail Phase 5 re-open scope + 5 plan summary + R-V3.1-2 mitigation chain + KHÔNG tag mới mặc định semver clean.
</output>
