---
phase: 05-document-version-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/versions/0007_document_versions.py
  - api/tests/integration/test_migration_0007_idempotent.py
autonomous: true
requirements: [VER-01]
tags: [backend, alembic, migration, schema, version-history, introspect, idempotent]
must_haves:
  truths:
    - "Sau `alembic upgrade head`, bảng `document_versions` tồn tại với 15 cột match exact DocumentVersionAPI interface (api.ts:599-615) + 1 UNIQUE constraint (document_id, version_number) + 1 INDEX (document_id) + 1 CHECK constraint trên change_type"
    - "Re-run `alembic upgrade head` lần 2 KHÔNG fail (idempotent — introspect detect document_versions đã tồn tại + skip CREATE TABLE)"
    - "Sau `alembic downgrade -1`, bảng `document_versions` bị DROP + index `ix_document_versions_document_id` bị DROP; downgrade() print COUNT(*) trước drop để operator visibility"
    - "FK document_id REFERENCES documents(id) ON DELETE CASCADE — xoá document → cascade xoá tất cả versions; FK created_by REFERENCES users(id) ON DELETE SET NULL — xoá user → version row giữ nguyên với created_by=NULL"
    - "Migration revision='0007' + down_revision='0006' chain đúng (sau Plan 01-01 v3.1 ship 0006_role_hub_admin)"
    - "5 integration test PASS — cover upgrade head idempotent + downgrade rollback + 15 cột verify + UNIQUE constraint + INDEX exists"
  artifacts:
    - path: "api/migrations/versions/0007_document_versions.py"
      provides: "Alembic migration 0007 — CREATE TABLE document_versions với 15 cột + UNIQUE + INDEX + CHECK; introspect pattern carry forward 0006"
      contains: "def upgrade()"
    - path: "api/migrations/versions/0007_document_versions.py"
      provides: "downgrade() defensive với COUNT(*) log trước DROP"
      contains: "def downgrade()"
    - path: "api/tests/integration/test_migration_0007_idempotent.py"
      provides: "5 integration test — upgrade idempotent + downgrade + schema verify"
      contains: "async def test_"
  key_links:
    - from: "0007_document_versions.py upgrade()"
      to: "documents table (FK target)"
      via: "introspect check + sa.ForeignKey('documents.id', ondelete='CASCADE')"
      pattern: "documents.*ondelete.*CASCADE"
    - from: "0007_document_versions.py upgrade()"
      to: "users table (FK target)"
      via: "sa.ForeignKey('users.id', ondelete='SET NULL')"
      pattern: "users\\.id.*ondelete.*SET NULL"
    - from: "0007 schema columns"
      to: "frontend DocumentVersionAPI interface (api.ts:599-615)"
      via: "15 field name + type exact match (contract bridge BE → FE)"
      pattern: "version_number.*is_original.*file_hash.*chunk_count.*change_type"
    - from: "test_migration_0007_idempotent.py"
      to: "alembic_cfg fixture (conftest.py)"
      via: "DSN injection via monkeypatch + get_settings.cache_clear() (Plan 01-03 SAFETY pattern carry forward)"
      pattern: "monkeypatch.*DATABASE_URL"
---

<objective>
Plan 05-01 v3.1 Phase 5 VER-01 — Wave 1 BLOCKING Alembic migration `0007_document_versions` tạo bảng schema match exact frontend `DocumentVersionAPI` 15 cột + integration test idempotent.

**Purpose (VER-01 + D-V3.1-Phase5-G LOCKED):** Phase 5 backend catch-up cho FE component `DocumentVersionHistory.tsx` đã ship trước; user gặp 404 console khi mở "Lịch sử phiên bản" tab. Schema migration bước đầu tiên — enable Plan 05-02 service + Plan 05-03 router. Schema phải match EXACT 15 field của `DocumentVersionAPI` interface (frontend/src/services/api.ts:599-615) để FE typecheck pass khi BE response trả về.

**Output:**
- 1 file Alembic migration `0007_document_versions.py` idempotent (introspect pattern Plan 01-01 v3.1 carry forward) + downgrade() đầy đủ với defensive COUNT(*) log.
- 1 file integration test `test_migration_0007_idempotent.py` (~200 LOC) ship 5 test PASS — DSN injection SAFETY pattern Plan 01-03 v3.1 carry forward (monkeypatch env DATABASE_URL + get_settings.cache_clear() — KHÔNG dùng Config.set_main_option vì env.py:185-191 runtime override bypass).

**Carry forward strategy:**
- Pattern Plan 01-01 v3.1 (`0006_role_hub_admin.py`) — `sa.inspect()` introspect 3-STEP upgrade + defensive downgrade với RuntimeError E-V3.1-1.
- Pattern Plan 01-03 v3.1 (`test_migration_0006_idempotent.py`) — fixture monkeypatch `DATABASE_URL` env + `get_settings.cache_clear()` (I-01 + I-02 SAFETY-CRITICAL fix — bypass env.py runtime override).
- Schema 15 cột match contract `DocumentVersionAPI` (api.ts:599-615) — Plan 05-03 router serialize BE → FE response shape exact match.
- D-V3.1-Phase5-G LOCKED revision = "0007" + down_revision = "0006".
- D-V3.1-Phase5-A LOCKED — `file_hash TEXT NULL` (SHA-256 hex 64-char optional; dedupe key dùng bởi Plan 05-02 service).
- D-V3.1-Phase5-B LOCKED — KHÔNG có bảng `document_version_chunks` (defer v4.0); `chunk_count INT DEFAULT 0` cột metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md
@Hub_All/CLAUDE.md
@Hub_All/api/migrations/versions/0006_role_hub_admin.py
@Hub_All/api/migrations/versions/0001_initial_schema.py
@Hub_All/api/migrations/env.py
@Hub_All/api/tests/integration/conftest.py
@Hub_All/frontend/src/services/api.ts

<interfaces>
<!-- Pattern source-of-truth từ 0006 + 0001 + DocumentVersionAPI. Executor dùng trực tiếp — KHÔNG cần explore. -->

DocumentVersionAPI interface contract (frontend/src/services/api.ts:599-615) — 15 field:
```typescript
export interface DocumentVersionAPI {
  id: string;                    // UUID — server-generated
  document_id: string;           // UUID FK → documents.id
  version_number: number;        // INT monotonic per document_id (starts at 1)
  is_original: boolean;          // true khi version_number == 1
  name: string;                  // filename snapshot từ upload
  file_type: string;             // mime type or extension
  file_size: number;             // BIGINT bytes
  file_path: string;             // UUID-based FileStore path
  file_hash?: string;            // SHA-256 hex 64-char — D-V3.1-Phase5-A dedupe key
  extractor_used?: string;       // snapshot extractor name (docling, etc.)
  chunk_count: number;           // INT — KHÔNG snapshot chunks (D-V3.1-Phase5-B); only count
  change_type: 'reupload' | 'reextract' | 'content_edit' | 'restore';
  change_note?: string;          // optional human note
  created_by?: string;           // UUID FK → users.id ON DELETE SET NULL
  created_at: string;            // TIMESTAMPTZ ISO 8601
}
```

Plan 01-01 v3.1 introspect pattern (api/migrations/versions/0006_role_hub_admin.py — header + 3-STEP upgrade):
```python
"""role_hub_admin
Revision ID: 0006
Revises: 0005
Create Date: 2026-05-23 14:00:00.000000
"""
from __future__ import annotations
from typing import Sequence, Union  # noqa: UP035 — match baseline
import sqlalchemy as sa
from alembic import op

revision: str = "0006"
down_revision: Union[str, None] = "0005"  # noqa: UP007
branch_labels: Union[str, Sequence[str], None] = None  # noqa: UP007
depends_on: Union[str, Sequence[str], None] = None  # noqa: UP007

def upgrade() -> None:
    bind = op.get_bind()
    inspector = sa.inspect(bind)
    # STEP 1: introspect + idempotent guard
    # STEP 2: ALTER / CREATE statements
    # STEP 3: audit_logs seed (optional)
```

Plan 01-01 downgrade defensive pattern (0006_role_hub_admin.py):
```python
def downgrade() -> None:
    bind = op.get_bind()
    inspector = sa.inspect(bind)
    # Defensive count check trước rollback
    count = bind.execute(sa.text("SELECT COUNT(*) FROM ...")).scalar()
    if count and count > 0:
        # log warning hoặc raise RuntimeError nếu data loss risk
        ...
    # DROP statements với IF EXISTS guard
```

documents table schema (api/migrations/versions/0001_initial_schema.py — FK target):
```python
op.create_table(
    "documents",
    sa.Column("id", postgresql.UUID(as_uuid=True), primary_key=True, server_default=sa.text("gen_random_uuid()")),
    sa.Column("hub_id", postgresql.UUID(as_uuid=True), nullable=False),
    # ... → FK target document_id REFERENCES documents(id) ON DELETE CASCADE
)
```

users table schema (api/migrations/versions/0001_initial_schema.py — FK target):
```python
op.create_table(
    "users",
    sa.Column("id", postgresql.UUID(as_uuid=True), primary_key=True),
    # ... → FK target created_by REFERENCES users(id) ON DELETE SET NULL
)
```

DSN injection SAFETY pattern (Plan 01-03 v3.1 — `test_migration_0006_idempotent.py` Iter 1 fix):
```python
@pytest.fixture
def alembic_isolated_dsn(postgres_container, monkeypatch):
    """SAFETY: monkeypatch DATABASE_URL env + clear_cache get_settings.
    
    KHÔNG dùng Config.set_main_option("sqlalchemy.url", ...) — env.py:185-191
    runtime OVERRIDE từ get_settings().database_url → set_main_option BỊ IGNORE
    → test apply migration vào DB .env vd medinet_central, SAFETY BLOCKER.
    """
    dsn = postgres_container.get_connection_url().replace("postgresql+psycopg2://", "postgresql://")
    monkeypatch.setenv("DATABASE_URL", dsn)
    from app.config import get_settings
    get_settings.cache_clear()
    yield dsn
    get_settings.cache_clear()
```

env.py runtime override (api/migrations/env.py:185-191 — SAFETY-CRITICAL aware):
```python
# This block RUNTIME OVERRIDES sqlalchemy.url từ Settings — Config.set_main_option BỊ BYPASS.
config.set_main_option(
    "sqlalchemy.url", get_settings().database_url,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo file 0007_document_versions.py — header + upgrade() CREATE TABLE 15 cột + UNIQUE + INDEX + CHECK (VER-01)</name>
  <read_first>
    - Hub_All/api/migrations/versions/0006_role_hub_admin.py (TOÀN FILE — pattern Alembic introspect 3-STEP + downgrade defensive + docstring threat model + revision metadata)
    - Hub_All/api/migrations/versions/0001_initial_schema.py:46-200 (users + documents table baseline — verify FK target column types + naming convention)
    - Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md `<specifics>` Plan 05-01 (15 cột spec + UNIQUE + INDEX + CHECK constraint)
    - Hub_All/frontend/src/services/api.ts:599-615 (DocumentVersionAPI interface — verify 15 field name match exact)
    - Hub_All/CLAUDE.md §6 (stack pin Python 3.12 + SQLAlchemy 2 + Alembic + UUID gen_random_uuid())
  </read_first>
  <action>
Tạo file `Hub_All/api/migrations/versions/0007_document_versions.py` với cấu trúc:

**Header docstring (tiếng Việt có dấu) + revision metadata:**

```python
"""document_versions

Revision ID: 0007
Revises: 0006
Create Date: 2026-05-26 10:00:00.000000

Phase 5 Plan 05-01 VER-01 — Tạo bảng document_versions cho feature
version-history (frontend đã ship trước; BE catch-up gap).

Schema 15 cột match exact frontend DocumentVersionAPI interface
(frontend/src/services/api.ts:599-615) + 1 UNIQUE (document_id, version_number)
+ 1 INDEX (document_id) + 1 CHECK (change_type IN 4 value).

Idempotent strategy (D-V3.1-Phase5-G LOCKED — carry forward Plan 01-01 introspect):
- STEP 1: check document_versions table KHÔNG tồn tại (skip CREATE TABLE nếu re-run).
- STEP 2: check documents table tồn tại (precondition FK target — fail loud nếu schema baseline missing).
- STEP 3: check users table tồn tại (precondition FK target).
- STEP 4: CREATE TABLE document_versions với 15 cột + 1 UNIQUE + 1 INDEX + 1 CHECK.

Mitigations:
- T-05-01-01 Integrity — FK document_id ON DELETE CASCADE: xoá document → cascade
  xoá versions (KHÔNG orphan row). FK created_by ON DELETE SET NULL: xoá user →
  version row preserve forensic (created_by=NULL acceptable cho audit trail).
- T-05-01-02 Tampering — CHECK constraint change_type IN 4 value enforce schema
  (Plan 05-02 service raise nếu invalid; Plan 05-03 Pydantic Literal type validate).
- T-05-01-03 DoS — UNIQUE (document_id, version_number) prevent duplicate INSERT
  race condition (Plan 05-02 retry-safe atomic transaction).
- R-V3.1-1 mitigation — downgrade defensive COUNT(*) log + DROP TABLE atomic.

Carry forward Plan 01-01 v3.1 introspect + Plan 04-01 v3.0 introspect (idempotent
re-run safety + tên constraint introspect runtime).
"""
from __future__ import annotations

from typing import (  # noqa: UP035 — match Alembic baseline 0001-0006
    Sequence,
    Union,
)

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# revision identifiers, used by Alembic.
revision: str = "0007"
down_revision: Union[str, None] = "0006"  # noqa: UP007 — match baseline
branch_labels: Union[str, Sequence[str], None] = None  # noqa: UP007
depends_on: Union[str, Sequence[str], None] = None  # noqa: UP007
```

**upgrade() function — CREATE TABLE document_versions 15 cột + UNIQUE + INDEX + CHECK:**

```python
def upgrade() -> None:
    """VER-01 — CREATE TABLE document_versions với 15 cột + idempotent introspect."""
    bind = op.get_bind()
    inspector = sa.inspect(bind)

    existing_tables = set(inspector.get_table_names())

    # ============================================================
    # STEP 1 — Idempotent guard: skip nếu document_versions đã tồn tại.
    # ============================================================
    if "document_versions" in existing_tables:
        print(
            "[0007] SKIP CREATE TABLE document_versions — table đã tồn tại "
            "(idempotent re-run safe)"
        )
        return

    # ============================================================
    # STEP 2 — Precondition: documents table tồn tại (FK target).
    # ============================================================
    if "documents" not in existing_tables:
        raise RuntimeError(
            "Migration 0007 requires documents table (FK target). "
            "Run `alembic upgrade head` sequential từ 0001 baseline."
        )

    # ============================================================
    # STEP 3 — Precondition: users table tồn tại (FK target created_by).
    # ============================================================
    if "users" not in existing_tables:
        raise RuntimeError(
            "Migration 0007 requires users table (FK target created_by). "
            "Run `alembic upgrade head` sequential từ 0001 baseline."
        )

    # ============================================================
    # STEP 4 — CREATE TABLE document_versions (15 cột + UNIQUE + INDEX + CHECK).
    # ============================================================
    op.create_table(
        "document_versions",
        # 1) id UUID PK
        sa.Column(
            "id",
            postgresql.UUID(as_uuid=True),
            primary_key=True,
            server_default=sa.text("gen_random_uuid()"),
            nullable=False,
        ),
        # 2) document_id UUID FK ON DELETE CASCADE
        sa.Column(
            "document_id",
            postgresql.UUID(as_uuid=True),
            sa.ForeignKey("documents.id", ondelete="CASCADE"),
            nullable=False,
        ),
        # 3) version_number INT monotonic per document_id
        sa.Column("version_number", sa.Integer(), nullable=False),
        # 4) is_original BOOLEAN — true khi version_number == 1
        sa.Column(
            "is_original",
            sa.Boolean(),
            nullable=False,
            server_default=sa.text("false"),
        ),
        # 5) name TEXT — filename snapshot
        sa.Column("name", sa.Text(), nullable=False),
        # 6) file_type TEXT — mime type or extension
        sa.Column("file_type", sa.Text(), nullable=False),
        # 7) file_size BIGINT — bytes
        sa.Column("file_size", sa.BigInteger(), nullable=False),
        # 8) file_path TEXT — UUID-based FileStore path
        sa.Column("file_path", sa.Text(), nullable=False),
        # 9) file_hash TEXT NULL — SHA-256 hex 64-char (D-V3.1-Phase5-A dedupe key)
        sa.Column("file_hash", sa.Text(), nullable=True),
        # 10) extractor_used TEXT NULL — snapshot extractor name
        sa.Column("extractor_used", sa.Text(), nullable=True),
        # 11) chunk_count INT DEFAULT 0 — count only (D-V3.1-Phase5-B NO chunk snapshot)
        sa.Column(
            "chunk_count",
            sa.Integer(),
            nullable=False,
            server_default=sa.text("0"),
        ),
        # 12) change_type TEXT CHECK IN 4 value
        sa.Column("change_type", sa.Text(), nullable=False),
        # 13) change_note TEXT NULL — optional human note
        sa.Column("change_note", sa.Text(), nullable=True),
        # 14) created_by UUID FK ON DELETE SET NULL — forensic preserve
        sa.Column(
            "created_by",
            postgresql.UUID(as_uuid=True),
            sa.ForeignKey("users.id", ondelete="SET NULL"),
            nullable=True,
        ),
        # 15) created_at TIMESTAMPTZ DEFAULT NOW()
        sa.Column(
            "created_at",
            postgresql.TIMESTAMP(timezone=True),
            nullable=False,
            server_default=sa.text("NOW()"),
        ),
        # 16) UNIQUE (document_id, version_number) — prevent race duplicate
        sa.UniqueConstraint(
            "document_id",
            "version_number",
            name="uq_document_versions_doc_ver",
        ),
        # CHECK constraint change_type IN 4 value (D-V3.1-Phase5-H + Plan 05-02 contract)
        sa.CheckConstraint(
            "change_type IN ('reupload', 'reextract', 'content_edit', 'restore')",
            name="ck_document_versions_change_type",
        ),
    )

    # INDEX cho list query GET /versions WHERE document_id = $1 ORDER BY version_number DESC
    op.create_index(
        "ix_document_versions_document_id",
        "document_versions",
        ["document_id"],
    )

    print(
        "[0007] OK CREATE TABLE document_versions (15 cột + UNIQUE doc_ver + INDEX document_id + CHECK change_type)"
    )
```

**Lưu ý quan trọng:**
- KHÔNG tách upgrade() qua nhiều task — `op.create_table()` atomic, KHÔNG cần split.
- `server_default=sa.text("gen_random_uuid()")` — Postgres native UUID gen (M2 baseline carry forward 0001 — pgcrypto extension đã có).
- `postgresql.TIMESTAMP(timezone=True)` — TIMESTAMPTZ (memory `project_asyncpg_timestamptz_param` carry forward — Plan 05-02 query filter dùng `$N::text::timestamptz` nếu cần).
- `nullable=False` mặc định cho cột bắt buộc; `nullable=True` chỉ cho file_hash + extractor_used + change_note + created_by (theo DocumentVersionAPI optional fields).
- UNIQUE constraint name `uq_document_versions_doc_ver` (prefix `uq_` đồng nhất với `uq_*` baseline 0001-0006).
- CHECK constraint name `ck_document_versions_change_type` (prefix `ck_` đồng nhất với `ck_users_role_enum` + `ck_user_hubs_role_enum` từ 0001/0006).
- INDEX name `ix_document_versions_document_id` (prefix `ix_` Postgres standard).
- KHÔNG có `document_version_chunks` table (D-V3.1-Phase5-B LOCKED — Plan 05-03 router trả `chunks: []` empty array; defer v4.0).
- KHÔNG nest STEP 3 audit seed (Plan 01-01 có audit `migration.role_seed` vì là milestone-level RBAC; Plan 05-01 schema thuần — KHÔNG cần audit migration).
- `print()` log để operator visibility `alembic upgrade head` skip vs apply.
  </action>
  <verify>
    <automated>cd Hub_All/api && python -c "import ast; ast.parse(open('migrations/versions/0007_document_versions.py').read()); print('syntax OK')" && grep -c "revision: str = \"0007\"" migrations/versions/0007_document_versions.py && grep -c "down_revision.*\"0006\"" migrations/versions/0007_document_versions.py && grep -c "op.create_table" migrations/versions/0007_document_versions.py</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/migrations/versions/0007_document_versions.py` EXISTS.
    - File chứa `revision: str = "0007"` (revision metadata).
    - File chứa `down_revision: Union[str, None] = "0006"` (chain sau Plan 01-01 v3.1).
    - File chứa `op.create_table(` + `"document_versions",` (CREATE TABLE statement).
    - File chứa 15 lần `sa.Column(` (15 cột exact match DocumentVersionAPI).
    - File chứa `"id"` + `"document_id"` + `"version_number"` + `"is_original"` + `"name"` + `"file_type"` + `"file_size"` + `"file_path"` + `"file_hash"` + `"extractor_used"` + `"chunk_count"` + `"change_type"` + `"change_note"` + `"created_by"` + `"created_at"` (15 cột tên — 16th là implicit PK via UNIQUE).
    - File chứa `sa.ForeignKey("documents.id", ondelete="CASCADE")` (FK document_id cascade).
    - File chứa `sa.ForeignKey("users.id", ondelete="SET NULL")` (FK created_by SET NULL forensic).
    - File chứa `sa.UniqueConstraint(` + `"document_id"` + `"version_number"` (UNIQUE doc_ver).
    - File chứa `name="uq_document_versions_doc_ver"` (UNIQUE constraint name).
    - File chứa `sa.CheckConstraint(` + `"change_type IN ('reupload', 'reextract', 'content_edit', 'restore')"` (CHECK 4 value).
    - File chứa `name="ck_document_versions_change_type"` (CHECK name).
    - File chứa `op.create_index(` + `"ix_document_versions_document_id"` + `["document_id"]` (INDEX for list query).
    - File chứa `inspector = sa.inspect(bind)` + `if "document_versions" in existing_tables:` (idempotent introspect guard).
    - File chứa `RuntimeError(` + `"requires documents table"` HOẶC `"requires users table"` (FK precondition fail loud).
    - File chứa `server_default=sa.text("gen_random_uuid()")` (Postgres UUID default).
    - File chứa `postgresql.TIMESTAMP(timezone=True)` (TIMESTAMPTZ).
    - File chứa `server_default=sa.text("NOW()")` (created_at default).
    - File chứa `nullable=True` cho file_hash + extractor_used + change_note + created_by (optional fields).
    - File KHÔNG chứa `document_version_chunks` table (D-V3.1-Phase5-B LOCKED defer v4.0).
    - `cd Hub_All/api && python -c "import ast; ast.parse(open('migrations/versions/0007_document_versions.py').read())"` exit 0 (syntax valid).
    - `cd Hub_All/api && python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','migrations/versions/0007_document_versions.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.revision == '0007' and m.down_revision == '0006' and hasattr(m, 'upgrade'); print('OK metadata')"` exit 0.
  </acceptance_criteria>
  <done>
    File 0007 hợp lệ Python + revision 0007→0006 + CREATE TABLE 15 cột + UNIQUE doc_ver + INDEX document_id + CHECK change_type 4 value + idempotent introspect guard + FK precondition fail loud.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend 0007 — downgrade() defensive COUNT(*) log + DROP TABLE + DROP INDEX</name>
  <read_first>
    - Hub_All/api/migrations/versions/0007_document_versions.py (state sau Task 1 — extend file thêm downgrade())
    - Hub_All/api/migrations/versions/0006_role_hub_admin.py:downgrade() (pattern defensive count + RuntimeError E-V3.1-1 + IF EXISTS guard)
    - Hub_All/api/migrations/versions/0005_sync_outbox_per_hub.py:downgrade (pattern op.drop_table + op.drop_index thứ tự ngược upgrade)
  </read_first>
  <action>
Append downgrade() function vào cuối file `Hub_All/api/migrations/versions/0007_document_versions.py` (sau hàm `upgrade()` đã tạo từ Task 1):

```python
def downgrade() -> None:
    """Idempotent rollback — DROP TABLE document_versions + INDEX + log COUNT(*).

    Thứ tự ngược upgrade:
    1. Defensive log COUNT(*) document_versions hiện có (operator visibility — KHÔNG raise).
    2. DROP INDEX ix_document_versions_document_id (IF EXISTS).
    3. DROP TABLE document_versions (CASCADE FK auto-drop UNIQUE + CHECK).

    Lưu ý: KHÔNG raise nếu COUNT > 0 — schema 0007 là feature-additive (KHÔNG migrate
    data từ table cũ). Operator quyết định manual review trước rollback nếu data
    valuable (production deployment chưa nên rollback nếu user đã restore version).
    Pattern khác Plan 01-01 0006 — 0006 có defensive RuntimeError vì rollback CHECK
    constraint 3-value với row role='hub_admin' sẽ FAIL; 0007 DROP TABLE atomic OK.
    """
    bind = op.get_bind()
    inspector = sa.inspect(bind)

    existing_tables = set(inspector.get_table_names())

    # ============================================================
    # STEP 1 — Defensive log COUNT(*) (operator visibility).
    # ============================================================
    if "document_versions" in existing_tables:
        count_result = bind.execute(
            sa.text("SELECT COUNT(*) FROM document_versions")
        ).scalar()
        print(
            f"[0007 downgrade] document_versions table hiện có {count_result} row "
            f"— sẽ bị DROP. Nếu data valuable, ABORT downgrade + backup trước "
            f"khi re-run alembic downgrade."
        )
    else:
        print("[0007 downgrade] SKIP — document_versions table KHÔNG tồn tại")
        return

    # ============================================================
    # STEP 2 — DROP INDEX (IF EXISTS guard idempotent).
    # ============================================================
    op.execute(
        "DROP INDEX IF EXISTS ix_document_versions_document_id"
    )
    print("[0007 downgrade] OK DROP INDEX ix_document_versions_document_id")

    # ============================================================
    # STEP 3 — DROP TABLE (CASCADE auto-drop UNIQUE + CHECK + FK).
    # ============================================================
    op.drop_table("document_versions")
    print("[0007 downgrade] OK DROP TABLE document_versions (CASCADE auto-drop UNIQUE + CHECK + FK)")
```

**Lưu ý:**
- KHÔNG raise `RuntimeError` nếu COUNT > 0 (khác Plan 01-01 0006) — DROP TABLE atomic OK ngay cả khi có data; operator chịu trách nhiệm review trước rollback.
- `bind.execute().scalar()` để lấy COUNT result (op.execute return None — KHÔNG dùng).
- `DROP INDEX IF EXISTS` guard idempotent — re-run downgrade KHÔNG fail.
- `op.drop_table` SQLAlchemy helper auto-cascade FK + UNIQUE + CHECK (KHÔNG cần explicit DROP constraint).
- `print()` log để operator debug `alembic downgrade -1` thấy step nào skip/drop.
- KHÔNG dùng `IF EXISTS` ở `op.drop_table` (SQLAlchemy helper KHÔNG support — dùng introspect guard ở STEP 1 thay vì).
  </action>
  <verify>
    <automated>cd Hub_All/api && grep -c "def downgrade" migrations/versions/0007_document_versions.py && grep -c "DROP INDEX IF EXISTS ix_document_versions_document_id" migrations/versions/0007_document_versions.py && grep -c "op.drop_table" migrations/versions/0007_document_versions.py && grep -c "SELECT COUNT" migrations/versions/0007_document_versions.py && python -c "import ast; ast.parse(open('migrations/versions/0007_document_versions.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File 0007 chứa `def downgrade() -> None:` (function definition).
    - File 0007 chứa `SELECT COUNT(*) FROM document_versions` (defensive count log).
    - File 0007 chứa `bind.execute(` + `.scalar()` (count result extraction).
    - File 0007 chứa `DROP INDEX IF EXISTS ix_document_versions_document_id` (idempotent INDEX drop).
    - File 0007 chứa `op.drop_table("document_versions")` (table drop).
    - File 0007 chứa `inspector = sa.inspect(bind)` ≥ 2 lần (1 trong upgrade + 1 trong downgrade — introspect-based idempotent).
    - File 0007 chứa `if "document_versions" in existing_tables:` (idempotent guard ở downgrade).
    - File 0007 KHÔNG chứa `RuntimeError` ở downgrade() (khác 0006 — DROP TABLE atomic OK ngay cả khi có data).
    - `cd Hub_All/api && python -c "import ast; ast.parse(open('migrations/versions/0007_document_versions.py').read())"` exit 0.
    - `cd Hub_All/api && python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','migrations/versions/0007_document_versions.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m,'upgrade') and hasattr(m,'downgrade'); print('OK')"` exit 0.
  </acceptance_criteria>
  <done>
    File 0007 hoàn chỉnh: header + upgrade() CREATE TABLE 15 cột + downgrade() defensive COUNT log + DROP INDEX + DROP TABLE atomic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tạo test_migration_0007_idempotent.py — 5 test PASS với DSN injection SAFETY pattern Plan 01-03 carry forward</name>
  <read_first>
    - Hub_All/api/migrations/versions/0007_document_versions.py (state sau Task 1 + 2)
    - Hub_All/api/tests/integration/conftest.py:42-186 (postgres_container + alembic_cfg fixture signature)
    - Hub_All/api/tests/integration/test_smoke_e2e_v3_1_rbac.py (pattern testcontainers + asyncpg query — Plan 04-02 v3.1 carry forward)
    - Hub_All/.planning/phases/01-rbac-schema-migration/01-03-PLAN.md (DSN injection SAFETY pattern Iter 1 fix I-01 + I-02 — monkeypatch env DATABASE_URL + get_settings.cache_clear)
    - Hub_All/api/migrations/env.py:185-191 (verify env.py runtime override pattern — bypass Config.set_main_option)
  </read_first>
  <behavior>
    - Test 1 (upgrade_creates_table_with_15_columns): chạy `alembic upgrade head` → assert `document_versions` table tồn tại + 15 cột exact name match (id, document_id, version_number, is_original, name, file_type, file_size, file_path, file_hash, extractor_used, chunk_count, change_type, change_note, created_by, created_at — 15 explicit columns DocumentVersionAPI interface).
    - Test 2 (upgrade_idempotent_re_run): chạy `alembic upgrade head` 2 lần liên tiếp → lần 2 KHÔNG fail (introspect skip-guard); document_versions vẫn tồn tại; print log mention "SKIP".
    - Test 3 (downgrade_drops_table): chạy `alembic upgrade head` rồi `alembic downgrade -1` → document_versions table bị DROP; index `ix_document_versions_document_id` bị DROP; downgrade print log mention "OK DROP TABLE".
    - Test 4 (unique_constraint_enforces_doc_ver): sau upgrade, INSERT 2 row cùng (document_id, version_number) → second INSERT raise `UniqueViolationError` (asyncpg) hoặc `IntegrityError` (psycopg).
    - Test 5 (check_constraint_rejects_invalid_change_type): sau upgrade, INSERT row với `change_type='invalid_value'` → raise CHECK constraint violation error (asyncpg `CheckViolationError`).
  </behavior>
  <action>
Tạo file `Hub_All/api/tests/integration/test_migration_0007_idempotent.py` với 5 test PASS dùng DSN injection SAFETY pattern Plan 01-03 v3.1 carry forward.

```python
"""Integration test 0007_document_versions migration — Plan 05-01 VER-01.

5 test cover ROADMAP Phase 5 success criterion #1:
1. upgrade_creates_table_with_15_columns: alembic upgrade head + verify 15 cột exact.
2. upgrade_idempotent_re_run: alembic upgrade head × 2 lần PASS (introspect skip).
3. downgrade_drops_table: alembic downgrade -1 PASS + table + index bị DROP.
4. unique_constraint_enforces_doc_ver: INSERT duplicate (doc_id, ver_num) → fail.
5. check_constraint_rejects_invalid_change_type: INSERT change_type invalid → fail.

SAFETY-CRITICAL DSN injection pattern (Plan 01-03 v3.1 Iter 1 fix I-01 + I-02 carry forward):
- monkeypatch env DATABASE_URL + get_settings.cache_clear() — KHÔNG dùng
  Config.set_main_option("sqlalchemy.url", ...) vì env.py:185-191 runtime OVERRIDE
  → caller's set_main_option BỊ IGNORE → test apply migration vào DB .env vd
  medinet_central, SAFETY BLOCKER (data loss risk).

Reuse:
- postgres_container fixture (conftest.py:42-186 session-scoped).
- testcontainers.postgres + asyncpg (Plan 04-02 v3.1 carry forward).
"""
from __future__ import annotations

import asyncio
from pathlib import Path

import asyncpg
import pytest
from alembic import command
from alembic.config import Config
from testcontainers.postgres import PostgresContainer


# === SAFETY DSN injection fixture (Plan 01-03 v3.1 Iter 1 fix carry forward) ===

@pytest.fixture
def alembic_isolated_dsn(
    postgres_container: PostgresContainer,
    monkeypatch: pytest.MonkeyPatch,
) -> str:
    """SAFETY-CRITICAL: monkeypatch env DATABASE_URL + clear get_settings cache.

    KHÔNG dùng Config.set_main_option("sqlalchemy.url", ...) — env.py:185-191
    runtime OVERRIDE từ get_settings().database_url → caller's set_main_option
    BỊ IGNORE → test apply migration vào DB .env vd medinet_central, SAFETY
    BLOCKER (Plan 01-03 v3.1 Iter 1 fix I-01 + I-02).

    Returns:
        DSN postgresql:// (async-driver-compatible).
    """
    dsn = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    monkeypatch.setenv("DATABASE_URL", dsn)
    monkeypatch.setenv("ENV", "test")

    # Clear get_settings cache — force re-read DATABASE_URL từ env mới patch.
    from app.config import get_settings
    get_settings.cache_clear()

    yield dsn

    # Cleanup: clear cache lại để test sau KHÔNG bị stale.
    get_settings.cache_clear()


@pytest.fixture
def alembic_cfg_for_test(alembic_isolated_dsn: str) -> Config:
    """Alembic Config trỏ tới alembic.ini repo + DSN injection qua env.

    KHÔNG dùng `alembic_cfg` từ conftest.py vì conftest scope module-fixed
    apply head 1 lần; test idempotent + downgrade cần fresh Config per-test.
    """
    repo_root = Path(__file__).resolve().parents[2]  # tests/integration/ → api/
    alembic_ini = repo_root / "alembic.ini"
    assert alembic_ini.exists(), f"alembic.ini not found at {alembic_ini}"
    cfg = Config(str(alembic_ini))
    # KHÔNG set sqlalchemy.url ở đây — env.py:185-191 sẽ override từ env DATABASE_URL.
    return cfg


# === Helper: reset DB to base state trước mỗi test ===

async def _reset_db_to_base(dsn: str) -> None:
    """Drop alembic_version + tất cả schema table — fresh state cho test isolation."""
    conn = await asyncpg.connect(dsn)
    try:
        # Drop schema public CASCADE — fresh state (testcontainer dùng được vì DB ephemeral).
        await conn.execute("DROP SCHEMA IF EXISTS public CASCADE")
        await conn.execute("CREATE SCHEMA public")
        await conn.execute("GRANT ALL ON SCHEMA public TO public")
    finally:
        await conn.close()


# === Test 1: upgrade creates table với 15 cột exact ===

@pytest.mark.critical
@pytest.mark.integration
def test_upgrade_creates_table_with_15_columns(
    alembic_isolated_dsn: str,
    alembic_cfg_for_test: Config,
) -> None:
    """VER-01: alembic upgrade head → document_versions table tồn tại + 15 cột exact."""
    asyncio.run(_reset_db_to_base(alembic_isolated_dsn))

    # Apply migration head (sequential 0001 → 0007).
    command.upgrade(alembic_cfg_for_test, "head")

    # Verify table + 15 cột exact (async asyncpg query).
    async def _verify():
        conn = await asyncpg.connect(alembic_isolated_dsn)
        try:
            # Check table exists
            row = await conn.fetchrow(
                "SELECT to_regclass('document_versions') AS oid"
            )
            assert row["oid"] is not None, "document_versions table missing after upgrade"

            # Check 15 cột exact (information_schema query)
            cols = await conn.fetch(
                "SELECT column_name, data_type, is_nullable "
                "FROM information_schema.columns "
                "WHERE table_name = 'document_versions' "
                "ORDER BY ordinal_position"
            )
            col_names = [c["column_name"] for c in cols]
            expected = {
                "id", "document_id", "version_number", "is_original",
                "name", "file_type", "file_size", "file_path",
                "file_hash", "extractor_used", "chunk_count",
                "change_type", "change_note", "created_by", "created_at",
            }
            actual = set(col_names)
            missing = expected - actual
            assert not missing, f"document_versions missing columns: {missing}"
            assert len(col_names) == 15, (
                f"document_versions should have 15 explicit columns (DocumentVersionAPI interface), got {len(col_names)}: {col_names}"
            )

            # Check UNIQUE constraint exists
            uniq = await conn.fetchrow(
                "SELECT conname FROM pg_constraint "
                "WHERE conname = 'uq_document_versions_doc_ver'"
            )
            assert uniq is not None, "UNIQUE constraint uq_document_versions_doc_ver missing"

            # Check INDEX exists
            idx = await conn.fetchrow(
                "SELECT indexname FROM pg_indexes "
                "WHERE indexname = 'ix_document_versions_document_id'"
            )
            assert idx is not None, "INDEX ix_document_versions_document_id missing"

            # Check CHECK constraint exists
            chk = await conn.fetchrow(
                "SELECT conname FROM pg_constraint "
                "WHERE conname = 'ck_document_versions_change_type'"
            )
            assert chk is not None, "CHECK constraint ck_document_versions_change_type missing"
        finally:
            await conn.close()

    asyncio.run(_verify())


# === Test 2: upgrade idempotent — re-run 2 lần PASS ===

@pytest.mark.critical
@pytest.mark.integration
def test_upgrade_idempotent_re_run(
    alembic_isolated_dsn: str,
    alembic_cfg_for_test: Config,
) -> None:
    """VER-01: alembic upgrade head × 2 lần → lần 2 PASS (introspect skip-guard)."""
    asyncio.run(_reset_db_to_base(alembic_isolated_dsn))

    # Run 1 — apply migrations 0001 → 0007
    command.upgrade(alembic_cfg_for_test, "head")

    # Run 2 — re-run head; KHÔNG fail (Alembic detect already at head + 0007 introspect skip).
    command.upgrade(alembic_cfg_for_test, "head")

    # Verify table vẫn tồn tại
    async def _verify():
        conn = await asyncpg.connect(alembic_isolated_dsn)
        try:
            row = await conn.fetchrow(
                "SELECT to_regclass('document_versions') AS oid"
            )
            assert row["oid"] is not None, "document_versions table missing after re-run"
        finally:
            await conn.close()

    asyncio.run(_verify())


# === Test 3: downgrade drops table ===

@pytest.mark.critical
@pytest.mark.integration
def test_downgrade_drops_table(
    alembic_isolated_dsn: str,
    alembic_cfg_for_test: Config,
) -> None:
    """VER-01: alembic upgrade head + downgrade -1 → document_versions bị DROP + index DROP."""
    asyncio.run(_reset_db_to_base(alembic_isolated_dsn))

    # Apply migrations + verify table tồn tại
    command.upgrade(alembic_cfg_for_test, "head")

    # Downgrade -1 → revert 0007 → 0006
    command.downgrade(alembic_cfg_for_test, "-1")

    # Verify table + index bị DROP
    async def _verify():
        conn = await asyncpg.connect(alembic_isolated_dsn)
        try:
            row = await conn.fetchrow(
                "SELECT to_regclass('document_versions') AS oid"
            )
            assert row["oid"] is None, (
                "document_versions table should be DROPPED after downgrade -1"
            )

            idx = await conn.fetchrow(
                "SELECT indexname FROM pg_indexes "
                "WHERE indexname = 'ix_document_versions_document_id'"
            )
            assert idx is None, "INDEX should be DROPPED after downgrade -1"
        finally:
            await conn.close()

    asyncio.run(_verify())


# === Test 4: UNIQUE constraint enforces (document_id, version_number) ===

@pytest.mark.critical
@pytest.mark.integration
def test_unique_constraint_enforces_doc_ver(
    alembic_isolated_dsn: str,
    alembic_cfg_for_test: Config,
) -> None:
    """VER-01: UNIQUE (document_id, version_number) — INSERT duplicate → fail."""
    asyncio.run(_reset_db_to_base(alembic_isolated_dsn))
    command.upgrade(alembic_cfg_for_test, "head")

    async def _verify():
        conn = await asyncpg.connect(alembic_isolated_dsn)
        try:
            # Seed 1 hub + 1 document để FK satisfy
            hub_id = "00000000-0000-0000-0000-000000000001"
            doc_id = "00000000-0000-0000-0000-000000000002"
            # INSERT hubs: NOT NULL phải gồm id + slug + name + code + subdomain
            # (per migrations 0001 baseline + 0003 reconcile — subdomain server_default
            # dropped sau backfill row mới phải truyền tường minh).
            await conn.execute(
                "INSERT INTO hubs (id, slug, code, name, subdomain, status, is_active, created_at) "
                "VALUES ($1::uuid, 'test', 'test', 'Test Hub', 'test', 'active', TRUE, NOW()) "
                "ON CONFLICT DO NOTHING"
            , hub_id)
            await conn.execute(
                "INSERT INTO documents (id, hub_id, filename, file_path, file_size_bytes, "
                "mime_type, status, created_at, updated_at) "
                "VALUES ($1::uuid, $2::uuid, 'test.docx', '/tmp/test.docx', 1024, "
                "'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'pending', NOW(), NOW()) "
                "ON CONFLICT DO NOTHING"
            , doc_id, hub_id)

            # INSERT version 1 — OK
            await conn.execute(
                "INSERT INTO document_versions "
                "(document_id, version_number, is_original, name, file_type, file_size, file_path, change_type) "
                "VALUES ($1::uuid, 1, true, 'test.docx', 'docx', 1024, '/tmp/test.docx', 'reupload')"
            , doc_id)

            # INSERT version 1 lần 2 (cùng doc_id, version_number) → UniqueViolationError
            with pytest.raises(asyncpg.exceptions.UniqueViolationError):
                await conn.execute(
                    "INSERT INTO document_versions "
                    "(document_id, version_number, is_original, name, file_type, file_size, file_path, change_type) "
                    "VALUES ($1::uuid, 1, true, 'test.docx', 'docx', 1024, '/tmp/test.docx', 'reupload')"
                , doc_id)
        finally:
            await conn.close()

    asyncio.run(_verify())


# === Test 5: CHECK constraint rejects invalid change_type ===

@pytest.mark.critical
@pytest.mark.integration
def test_check_constraint_rejects_invalid_change_type(
    alembic_isolated_dsn: str,
    alembic_cfg_for_test: Config,
) -> None:
    """VER-01: CHECK change_type IN 4 value — INSERT invalid → fail."""
    asyncio.run(_reset_db_to_base(alembic_isolated_dsn))
    command.upgrade(alembic_cfg_for_test, "head")

    async def _verify():
        conn = await asyncpg.connect(alembic_isolated_dsn)
        try:
            hub_id = "00000000-0000-0000-0000-000000000003"
            doc_id = "00000000-0000-0000-0000-000000000004"
            # INSERT hubs: cùng schema bắt buộc id + slug + code + name + subdomain (0001 + 0003).
            await conn.execute(
                "INSERT INTO hubs (id, slug, code, name, subdomain, status, is_active, created_at) "
                "VALUES ($1::uuid, 'test2', 'test2', 'Test Hub 2', 'test2', 'active', TRUE, NOW()) "
                "ON CONFLICT DO NOTHING"
            , hub_id)
            await conn.execute(
                "INSERT INTO documents (id, hub_id, filename, file_path, file_size_bytes, "
                "mime_type, status, created_at, updated_at) "
                "VALUES ($1::uuid, $2::uuid, 'test.docx', '/tmp/test.docx', 1024, "
                "'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'pending', NOW(), NOW()) "
                "ON CONFLICT DO NOTHING"
            , doc_id, hub_id)

            # INSERT với change_type='invalid_value' → CheckViolationError
            with pytest.raises(asyncpg.exceptions.CheckViolationError):
                await conn.execute(
                    "INSERT INTO document_versions "
                    "(document_id, version_number, is_original, name, file_type, file_size, file_path, change_type) "
                    "VALUES ($1::uuid, 1, true, 'test.docx', 'docx', 1024, '/tmp/test.docx', 'invalid_value')"
                , doc_id)
        finally:
            await conn.close()

    asyncio.run(_verify())
```

**Lưu ý quan trọng:**
- **SAFETY-CRITICAL:** dùng `monkeypatch.setenv("DATABASE_URL", dsn)` + `get_settings.cache_clear()` — KHÔNG dùng `Config.set_main_option("sqlalchemy.url", ...)` (env.py:185-191 override bypass).
- `_reset_db_to_base()` drop schema public CASCADE — fresh state per-test (testcontainer DB ephemeral safe).
- Test dùng `asyncio.run()` wrap async function — pytest-asyncio default mode chưa enabled cho file này; pattern Plan 01-03 carry forward.
- INSERT test seed minimal (1 hub + 1 doc) trước INSERT version để FK satisfy.
- `asyncpg.exceptions.UniqueViolationError` + `CheckViolationError` exact exception class (KHÔNG generic `Exception`).
- KHÔNG dùng `alembic_cfg` từ conftest.py (module-scoped) — fresh Config per-test cần thiết cho idempotent + downgrade isolation.
- KHÔNG cần Argon2 password hash (test seed direct DB row — không qua auth flow).
- Schema seed documents row dùng cột `filename` + `file_path` + `file_size_bytes` + `mime_type` + `status` (verify khớp baseline 0001 `documents` schema khi viết); nếu tên cột khác → adjust theo introspect runtime.
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/integration/test_migration_0007_idempotent.py -v -m integration --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_migration_0007_idempotent.py` EXISTS.
    - File chứa `def alembic_isolated_dsn(` (DSN injection SAFETY fixture).
    - File chứa `monkeypatch.setenv("DATABASE_URL", dsn)` (env injection — KHÔNG Config.set_main_option).
    - File chứa `get_settings.cache_clear()` (Plan 01-03 SAFETY pattern carry forward).
    - File chứa `def test_upgrade_creates_table_with_15_columns(` (Test 1).
    - File chứa `def test_upgrade_idempotent_re_run(` (Test 2).
    - File chứa `def test_downgrade_drops_table(` (Test 3).
    - File chứa `def test_unique_constraint_enforces_doc_ver(` (Test 4).
    - File chứa `def test_check_constraint_rejects_invalid_change_type(` (Test 5).
    - File chứa `@pytest.mark.critical` ≥ 5 lần (5 test).
    - File chứa `@pytest.mark.integration` ≥ 5 lần (5 test).
    - File chứa `asyncpg.exceptions.UniqueViolationError` (Test 4 exact exception).
    - File chứa `asyncpg.exceptions.CheckViolationError` (Test 5 exact exception).
    - File chứa `command.upgrade(alembic_cfg_for_test, "head")` ≥ 4 lần (4 test apply migration).
    - File chứa `command.downgrade(alembic_cfg_for_test, "-1")` (Test 3 rollback).
    - File chứa `to_regclass('document_versions')` (table existence query).
    - File chứa expected column names check: `"document_id"` + `"version_number"` + `"is_original"` + `"change_type"` + `"file_hash"` + `"chunk_count"` (15 cột verify).
    - File chứa `uq_document_versions_doc_ver` + `ix_document_versions_document_id` + `ck_document_versions_change_type` (3 constraint name verify).
    - File KHÔNG chứa `Config.set_main_option("sqlalchemy.url"` (SAFETY blocker — Plan 01-03 fix carry forward).
    - `cd Hub_All/api && python -m pytest tests/integration/test_migration_0007_idempotent.py -v -m integration` exit 0 (5 test PASS).
    - Output chứa `5 passed` HOẶC `test_upgrade_creates_table_with_15_columns PASSED` + 4 test khác PASSED.
  </acceptance_criteria>
  <done>
    test_migration_0007_idempotent.py 5 test PASS với DSN injection SAFETY pattern; upgrade idempotent + downgrade rollback + 15 cột verify + UNIQUE enforce + CHECK enforce; ROADMAP Phase 5 success criterion #1 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration runner → Postgres DDL | Operator chạy `alembic upgrade head` qua DSN từ Settings — DDL privileged operation; KHÔNG nhận user input runtime |
| Migration upgrade() / downgrade() | Code-controlled, threat model giới hạn ở DB-level integrity + idempotent safety |
| Test fixture asyncpg seed | Direct INSERT bypass app layer — fixture trust local testcontainer (KHÔNG production) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Integrity | FK document_id missing CASCADE → orphan version row khi document bị xoá | mitigate | `sa.ForeignKey("documents.id", ondelete="CASCADE")` enforce — xoá document auto-cascade xoá versions. FK created_by `ondelete="SET NULL"` preserve forensic. |
| T-05-01-02 | Tampering | Plan 05-02 service INSERT change_type='invalid_value' qua bug | mitigate | CHECK constraint `change_type IN ('reupload','reextract','content_edit','restore')` reject ở DB layer. Test 5 verify runtime. |
| T-05-01-03 | DoS | Concurrent INSERT race condition duplicate (document_id, version_number) | mitigate | UNIQUE constraint `uq_document_versions_doc_ver` reject duplicate; Plan 05-02 service retry-safe trong transaction. Test 4 verify runtime. |
| T-05-01-04 | Repudiation | Migration apply KHÔNG có audit trail | accept | Plan 05-01 schema thuần — KHÔNG cần audit migration row như Plan 01-01 (0006 có audit `migration.role_seed` vì RBAC milestone-level; 0007 feature-additive schema). Operator `alembic history` đủ. |
| T-05-01-05 | Information Disclosure | `print()` log COUNT(*) downgrade leak data count | accept | Local dev terminal — KHÔNG production logs. Count chỉ là số nguyên KHÔNG PII. Severity: VERY LOW. |
| T-05-01-06 | Elevation of Privilege | Test fixture DSN injection SAFETY bypass → migration apply nhầm DB production | mitigate | Plan 01-03 SAFETY pattern carry forward: monkeypatch env DATABASE_URL + get_settings.cache_clear() — KHÔNG dùng Config.set_main_option vì env.py runtime override bypass. Test fixture testcontainer DSN only. |
</threat_model>

<verification>
- File `Hub_All/api/migrations/versions/0007_document_versions.py` tồn tại + syntax Python hợp lệ.
- revision='0007' + down_revision='0006' chain đúng sau Plan 01-01 v3.1 ship.
- upgrade() CREATE TABLE document_versions 15 cột + UNIQUE doc_ver + INDEX document_id + CHECK change_type 4 value.
- Idempotent guard ở mọi step (introspect-based via sa.inspect() + IF NOT EXISTS DDL).
- downgrade() defensive COUNT(*) log + DROP INDEX IF EXISTS + DROP TABLE atomic.
- 5 integration test PASS qua pytest — cover ROADMAP Phase 5 success criterion #1.
- DSN injection SAFETY pattern Plan 01-03 v3.1 carry forward (monkeypatch env + cache_clear — KHÔNG Config.set_main_option).
- 15 cột exact match `DocumentVersionAPI` interface (frontend/src/services/api.ts:599-615) — contract bridge BE → FE.
</verification>

<success_criteria>
1. File 0007_document_versions.py compile Python OK (ast.parse exit 0).
2. revision='0007', down_revision='0006' đúng chain sau Plan 01-01 v3.1.
3. upgrade() CREATE TABLE document_versions 15 cột + UNIQUE + INDEX + CHECK.
4. downgrade() DROP TABLE + DROP INDEX + defensive COUNT log.
5. 5 integration test PASS (upgrade idempotent + downgrade + 15 cột + UNIQUE enforce + CHECK enforce).
6. Schema exact match `DocumentVersionAPI` interface — Plan 05-03 router serialize BE → FE response OK.
7. Plan 05-02 service unblocked — có thể `SELECT|INSERT|UPDATE|DELETE FROM document_versions`.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/05-document-version-history/05-01-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- File 0007 đã tạo: upgrade() 3-STEP (idempotent guard + FK precondition + CREATE TABLE) + downgrade() defensive
- 15 cột exact match DocumentVersionAPI interface (api.ts:599-615) — contract bridge BE → FE LOCKED
- UNIQUE doc_ver + INDEX document_id + CHECK change_type 4 value
- Idempotent strategy GA-V3.1-C carry forward Plan 01-01 v3.1 (introspect via sa.inspect)
- 5 integration test PASS (upgrade + idempotent re-run + downgrade + UNIQUE enforce + CHECK enforce)
- DSN injection SAFETY pattern Plan 01-03 v3.1 carry forward (monkeypatch env DATABASE_URL — KHÔNG Config.set_main_option)
- Plan 05-02 service unblocked — schema ready cho snapshot() + retention CTE query

Commit atomic: `feat(05-01): Alembic migration 0007 document_versions (15 cột + UNIQUE + INDEX + CHECK) + 5 integration test (VER-01)` + body tiếng Việt nói tại sao (Phase 5 BE catch-up cho FE DocumentVersionHistory.tsx đã ship trước; 15 cột match exact DocumentVersionAPI interface api.ts:599-615 contract; introspect pattern Plan 01-01 v3.1 carry forward idempotent; DSN injection SAFETY Plan 01-03 carry forward; Plan 05-02 service + Plan 05-03 router unblocked).
</output>
