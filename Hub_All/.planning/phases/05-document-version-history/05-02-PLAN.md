---
phase: 05-document-version-history
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api/app/services/document_version_service.py
  - api/tests/unit/test_document_version_service.py
autonomous: true
requirements: [VER-02]
tags: [backend, service, version-history, snapshot, retention, dedupe-hash, audit-emit, file-store]
must_haves:
  truths:
    - "Sau khi merge, module mới `api/app/services/document_version_service.py` export 5 public API: snapshot, restore_to_version, list_versions, get_version_with_chunks, get_version_file_path"
    - "Hàm snapshot(*, session, document, change_type, change_note=None, actor_user_id=None, actor_role='admin', actor_hub_id=None) → DocumentVersion row INSERT atomic + audit_logs emit action='document.version.create' + retention cleanup CTE chạy cùng transaction"
    - "Dedupe-by-hash logic (D-V3.1-Phase5-A): _compute_file_hash SHA-256 hex; nếu hash đã tồn tại trong cùng document_id → file_path reference path cũ (KHÔNG FileStore.save); nếu mới → reference document.file_path hiện tại"
    - "Retention cleanup '3 gốc + 2 gần nhất' (D-V3.1-Phase5-E LOCKED): _enforce_retention CTE window function DELETE WHERE asc_rank > 3 AND desc_rank > 2; file_store.delete chỉ khi reference count = 0 (cross-version dedupe safe)"
    - "restore_to_version (D-V3.1-Phase5-D LOCKED append-only): snapshot(change_type='restore') TRƯỚC khi UPDATE documents.file_path|name|file_type|file_size = giá trị target version + audit emit action='document.version.restore' với payload nest restored_to=version_id"
    - "version_number monotonic per document_id qua SELECT COALESCE(MAX, 0) + 1 — UNIQUE constraint backup trong DB layer (Plan 05-01 ship)"
    - "is_original = (version_number == 1) derive — KHÔNG cần param"
    - "8 unit test PASS cover snapshot dedupe + version_number monotonic + retention 6→5 prune + restore append-only + audit emit verify + file cleanup reference count + change_type validation"
  artifacts:
    - path: "api/app/services/document_version_service.py"
      provides: "VER-02 service layer 5 public API + 2 private helper (_compute_file_hash, _enforce_retention) — Plan 05-03 router consume"
      contains: "async def snapshot"
    - path: "api/app/services/document_version_service.py"
      provides: "restore_to_version() append-only D-V3.1-Phase5-D + audit emit action='document.version.restore'"
      contains: "async def restore_to_version"
    - path: "api/app/services/document_version_service.py"
      provides: "_enforce_retention() CTE window function DELETE middle versions + file cleanup reference count"
      contains: "async def _enforce_retention"
    - path: "api/tests/unit/test_document_version_service.py"
      provides: "8 unit test pure Python — mock AsyncSession + FileStore + audit_service.enqueue_audit"
      contains: "async def test_"
  key_links:
    - from: "document_version_service.snapshot"
      to: "app.services.audit_service.enqueue_audit + build_audit_payload"
      via: "AuditEntry(action='document.version.create', payload=build_audit_payload(actor_role=, actor_hub_id=, extra={'document_id':, 'version_number':, 'change_type':}))"
      pattern: "action=\"document.version.create\""
    - from: "document_version_service.restore_to_version"
      to: "app.services.audit_service.enqueue_audit"
      via: "AuditEntry(action='document.version.restore', payload=build_audit_payload(extra={'restored_to':}))"
      pattern: "action=\"document.version.restore\""
    - from: "document_version_service._compute_file_hash"
      to: "hashlib.sha256(path.read_bytes()).hexdigest()"
      via: "Python stdlib SHA-256 hex 64-char"
      pattern: "hashlib\\.sha256"
    - from: "document_version_service._enforce_retention"
      to: "raw SQL CTE window function ROW_NUMBER asc + desc"
      via: "DELETE FROM document_versions WHERE id IN (SELECT id FROM ranked WHERE asc_rank > 3 AND desc_rank > 2) RETURNING file_path"
      pattern: "ROW_NUMBER.*PARTITION BY document_id.*ORDER BY version_number"
    - from: "document_version_service file cleanup"
      to: "app.services.file_store.FileStore.delete"
      via: "FileStore().delete(Path(path)) chỉ khi SELECT COUNT(*) WHERE file_path = $1 == 0"
      pattern: "FileStore\\(\\)\\.delete\\(Path"
---

<objective>
Plan 05-02 v3.1 Phase 5 VER-02 — Wave 2 BLOCKING service layer `document_version_service.py` core API + 8 unit test pure Python (mock AsyncSession + FileStore + audit_service).

**Purpose (VER-02 + D-V3.1-Phase5-A/D/E/H LOCKED):** Plan 05-01 schema ready; Plan 05-02 build service layer 5 public API + 2 private helper. Plan 05-03 router 4 endpoint consume service. Plan 05-04 integration test verify E2E.

**5 public API:**
1. `async def snapshot(*, session, document, change_type, change_note=None, actor_user_id=None, actor_role='admin', actor_hub_id=None) -> DocumentVersionRow` — INSERT version row atomic + audit emit + retention cleanup cùng transaction.
2. `async def restore_to_version(*, session, document_id, version_id, actor_user_id, actor_role='admin', actor_hub_id=None) -> dict` — append-only restore: snapshot current TRƯỚC → UPDATE documents → trigger re-extract sync → audit emit `document.version.restore`.
3. `async def list_versions(session, document_id) -> list[dict]` — SELECT * WHERE document_id ORDER BY version_number DESC; trả ≤ 5 row (sau retention prune).
4. `async def get_version_with_chunks(session, document_id, version_id) -> tuple[dict, list]` — SELECT 1 row + return `(version_dict, [])` empty array (D-V3.1-Phase5-B LOCKED NO chunks snapshot).
5. `async def get_version_file_path(session, document_id, version_id) -> Path` — SELECT file_path WHERE id; return Path object cho Plan 05-03 router StreamingResponse.

**2 private helper:**
1. `def _compute_file_hash(path: Path) -> str` — SHA-256 hex 64-char (D-V3.1-Phase5-A LOCKED dedupe key).
2. `async def _enforce_retention(session, document_id) -> list[str]` — CTE window function DELETE middle versions + return deleted file_path list cho cleanup.

**Output:**
- 1 file mới `api/app/services/document_version_service.py` (~400 LOC).
- 1 file mới `api/tests/unit/test_document_version_service.py` (~350 LOC) ship 8 unit test PASS.

**Carry forward strategy:**
- `audit_service.build_audit_payload` + `enqueue_audit` (Plan 02-04 v3.1 carry forward) — emit 2 action `document.version.{create,restore}`.
- `FileStore.save/load/delete` API (M2 carry forward) — KHÔNG đụng file_store.py.
- AsyncSession `text()` raw SQL pattern Plan 01-02 v3.1 — query CTE retention complex (window function ROW_NUMBER, KHÔNG SQLAlchemy ORM).
- `from app.config import get_settings` + `settings.hub_name` — Plan 05-03 hub isolation Layer 2 indirect (service KHÔNG biết hub_name, caller resolve document.hub_id qua Plan 05-03 dependency).
- D-V3.1-Phase5-I LOCKED — restore re-extract SYNC block endpoint (small DOCX < 5s acceptable); cocoindex flow trigger qua existing `documents_service` re-extract hook nếu tồn tại HOẶC inline `cocoindex_app.update_blocking()` (depend documents_service API).

**Note:** Phase 5 KHÔNG hook vào reupload/edit/reextract trigger điểm hiện tại của `documents_service.py` ở Plan 05-02 scope (theo CONTEXT.md `<domain>` VER-02 mention 4 trigger điểm nhưng documents_service.py grep KHÔNG có endpoint `reupload`/`edit_content`/`reextract` existing — FE `previewReuploadDocument` ở api.ts:287-296 ám chỉ endpoint chưa ship BE). Plan 05-02 chỉ build core service API; Plan 05-03 router POST /restore trigger snapshot(change_type='restore') trực tiếp. Trigger điểm reupload/edit/reextract sẽ wire sau khi BE ship router tương ứng (defer task wiring trong Plan 05-03 hoặc v4.0 nếu BE chưa ship). Service API ready cho future wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md
@Hub_All/.planning/phases/05-document-version-history/05-01-PLAN.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/services/file_store.py
@Hub_All/api/app/services/audit_service.py
@Hub_All/api/app/services/documents_service.py
@Hub_All/api/app/auth/role.py
@Hub_All/frontend/src/services/api.ts

<interfaces>
<!-- Pattern source-of-truth — FileStore + audit_service + documents_service. Executor dùng trực tiếp. -->

FileStore API (api/app/services/file_store.py):
```python
class FileStore:
    def __init__(self, base_dir: Path | None = None) -> None: ...
    def save(self, content: bytes, original_filename: str) -> Path: ...
    def load(self, path: Path) -> bytes: ...
    def delete(self, path: Path) -> bool: ...  # idempotent — False nếu KHÔNG tồn tại
```

audit_service API (api/app/services/audit_service.py):
```python
@dataclass
class AuditEntry:
    action: str
    user_id: str | None = None
    target_type: str | None = None
    target_id: str | None = None
    hub_id: str | None = None
    payload: dict[str, Any] | None = None
    request_id: str | None = None

def build_audit_payload(
    *,
    actor_role: str,        # 'admin' | 'hub_admin'
    actor_hub_id: str | None,  # None nếu super admin
    extra: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Nest actor + extra → {actor_role, actor_hub_id, **extra}"""

def enqueue_audit(entry: AuditEntry) -> None:
    """Non-blocking — queue full → drop + log warning. KHÔNG raise."""
```

DocumentVersionAPI shape contract (frontend/src/services/api.ts:599-615):
```typescript
export interface DocumentVersionAPI {
  id: string; document_id: string; version_number: number; is_original: boolean;
  name: string; file_type: string; file_size: number; file_path: string;
  file_hash?: string; extractor_used?: string; chunk_count: number;
  change_type: 'reupload' | 'reextract' | 'content_edit' | 'restore';
  change_note?: string; created_by?: string; created_at: string;
}
```

Schema 15 cột (api/migrations/versions/0007_document_versions.py — Plan 05-01 ship):
- id UUID PK | document_id UUID FK CASCADE | version_number INT | is_original BOOL
- name TEXT | file_type TEXT | file_size BIGINT | file_path TEXT
- file_hash TEXT NULL | extractor_used TEXT NULL | chunk_count INT DEFAULT 0
- change_type TEXT CHECK IN 4 value | change_note TEXT NULL
- created_by UUID FK SET NULL | created_at TIMESTAMPTZ DEFAULT NOW()
- UNIQUE (document_id, version_number) + INDEX (document_id)

documents row shape (caller document object passed vào snapshot — duck-type với attribute access):
- document.id (UUID str)
- document.hub_id (UUID str — Plan 05-03 dependency dùng để Layer 3 assert_hub_admin_for)
- document.filename (TEXT — copy vào version.name)
- document.file_path (TEXT — copy vào version.file_path nếu hash mới HOẶC reference path cũ nếu dedupe)
- document.mime_type (TEXT — copy vào version.file_type)
- document.file_size_bytes (BIGINT — copy vào version.file_size)
- document.extractor (TEXT optional — copy vào version.extractor_used nếu tồn tại)
- document.chunk_count (INT optional — copy vào version.chunk_count nếu tồn tại, fallback 0)

CTE window function pattern (D-V3.1-Phase5-E LOCKED — `_enforce_retention`):
```sql
WITH ranked AS (
    SELECT id, version_number, file_path,
           ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number) AS asc_rank,
           ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number DESC) AS desc_rank
    FROM document_versions
    WHERE document_id = :doc_id
)
DELETE FROM document_versions
WHERE id IN (SELECT id FROM ranked WHERE asc_rank > 3 AND desc_rank > 2)
RETURNING file_path;
```

Plan 05-03 router consumer signature (Wave 3 — Plan 05-02 must export compatible API):
```python
# Plan 05-03 GET /versions/{vid}/file:
file_path = await document_version_service.get_version_file_path(db, document_id, version_id)
return StreamingResponse(path.open("rb"), media_type=version.file_type, ...)

# Plan 05-03 POST /restore:
refreshed = await document_version_service.restore_to_version(
    session=db, document_id=document_id, version_id=version_id,
    actor_user_id=user.id, actor_role=..., actor_hub_id=...,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo file document_version_service.py — 2 helper private + snapshot() + list_versions() + get_version_with_chunks() + get_version_file_path() (VER-02 core)</name>
  <read_first>
    - Hub_All/api/app/services/file_store.py (TOÀN FILE — FileStore.save/load/delete API exact signature)
    - Hub_All/api/app/services/audit_service.py (TOÀN FILE — AuditEntry dataclass + build_audit_payload + enqueue_audit + AUDIT_ACTIONS frozenset NOTE: 'document.version.create' + 'document.version.restore' CHƯA có trong frozenset, cần ADD)
    - Hub_All/api/app/services/documents_service.py:128-450 (DocumentService class + _DOC_COLUMNS + reextract hook nếu tồn tại — verify trigger điểm)
    - Hub_All/api/migrations/versions/0007_document_versions.py (state sau Plan 05-01 — 15 cột schema verify)
    - Hub_All/.planning/phases/05-document-version-history/05-CONTEXT.md `<decisions>` D-V3.1-Phase5-A/B/D/E/H (dedupe + chunks=[] + restore append-only + retention CTE + audit action codes)
    - Hub_All/frontend/src/services/api.ts:599-615 (DocumentVersionAPI 15 field — verify serialization match)
  </read_first>
  <behavior>
    - Test 1 (snapshot_inserts_row_atomic): snapshot(doc, change_type='reupload') → INSERT 1 row document_versions với version_number=1, is_original=True; mock session.execute called 2+ lần (1 MAX query + 1 INSERT).
    - Test 2 (snapshot_dedupe_by_hash_reuses_path): doc đã có version v1 với file_hash='abc'; snapshot(doc) với cùng file_hash='abc' → version v2 INSERT với file_path = path cũ (KHÔNG FileStore.save mới).
    - Test 3 (snapshot_audit_emits_create_action): snapshot(doc, change_type='reupload', actor_role='hub_admin', actor_hub_id='dmd-uuid') → enqueue_audit gọi 1 lần với AuditEntry(action='document.version.create', payload contains actor_role='hub_admin' + document_id + version_number + change_type='reupload').
    - Test 4 (snapshot_version_number_monotonic): doc đã có v1, v2, v3 → snapshot trả v4 (SELECT MAX + 1).
    - Test 5 (list_versions_returns_desc): mock SELECT trả 3 row với version_number [3, 2, 1] DESC; list_versions trả list theo order DESC.
    - Test 6 (get_version_with_chunks_returns_empty_chunks): mock SELECT trả 1 row; get_version_with_chunks trả `(version_dict, [])` chunks empty array.
    - Test 7 (get_version_file_path_returns_path_object): mock SELECT file_path trả '/tmp/test.docx'; get_version_file_path trả Path('/tmp/test.docx').
    - Test 8 (compute_file_hash_returns_sha256_hex_64): _compute_file_hash(real tempfile bytes b'hello') trả string len 64 + match hashlib.sha256(b'hello').hexdigest().
  </behavior>
  <action>
**Bước 1 — Tạo file `Hub_All/api/app/services/document_version_service.py`:**

```python
"""document_version_service.py — Phase 5 Plan 05-02 VER-02 service layer.

5 public API:
- snapshot(*, session, document, change_type, change_note=None, actor_user_id=None,
           actor_role='admin', actor_hub_id=None) -> dict
- restore_to_version(*, session, document_id, version_id, actor_user_id,
                     actor_role='admin', actor_hub_id=None) -> dict
- list_versions(session, document_id) -> list[dict]
- get_version_with_chunks(session, document_id, version_id) -> tuple[dict, list]
- get_version_file_path(session, document_id, version_id) -> Path

2 private helper:
- _compute_file_hash(path: Path) -> str  # SHA-256 hex 64-char
- _enforce_retention(session, document_id) -> list[str]  # deleted file paths

Decisions LOCKED (05-CONTEXT.md):
- D-V3.1-Phase5-A: Dedupe-by-hash trong cùng document_id (reupload exact same file
  → reference path cũ; nếu hash mới → reference document.file_path hiện tại).
- D-V3.1-Phase5-B: chunks per-version NO snapshot — get_version_with_chunks trả
  `(version, [])` empty array (FE typecheck happy).
- D-V3.1-Phase5-D: Restore append-only — snapshot(change_type='restore') TRƯỚC
  khi UPDATE documents.
- D-V3.1-Phase5-E: Retention '3 gốc + 2 gần nhất' enforce BE write-time CTE.
- D-V3.1-Phase5-H: Audit action codes 'document.version.create' + 'document.version.restore'.
- D-V3.1-Phase5-I: Restore re-extract SYNC block endpoint (< 5s small DOCX).

Carry forward:
- audit_service.build_audit_payload + enqueue_audit (Plan 02-04 v3.1).
- FileStore.save/load/delete (M2).
- Raw SQL text() pattern Plan 01-02 v3.1 (CTE window function complex).
"""
from __future__ import annotations

import hashlib
import logging
from pathlib import Path
from typing import Any

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.services.audit_service import (
    AuditEntry,
    build_audit_payload,
    enqueue_audit,
)
from app.services.file_store import FileStore

logger = logging.getLogger(__name__)

# Action codes — D-V3.1-Phase5-H LOCKED.
# NOTE: AUDIT_ACTIONS frozenset trong audit_service.py KHÔNG include 2 action mới;
# enqueue_audit KHÔNG enforce hard (caller responsibility), nhưng để forensic
# query có thể chuẩn hóa qua hằng số.
ACTION_VERSION_CREATE = "document.version.create"
ACTION_VERSION_RESTORE = "document.version.restore"

# Retention policy hardcode (D-V3.1-Phase5-E LOCKED).
# Per-hub override defer v4.0 system_settings.
RETENTION_KEEP_FIRST_N = 3   # 3 gốc đầu
RETENTION_KEEP_LAST_N = 2    # 2 gần nhất


# === Helper: SHA-256 file hash ===

def _compute_file_hash(path: Path) -> str:
    """SHA-256 hex 64-char dedupe key (D-V3.1-Phase5-A LOCKED).

    Pattern khác chunks.content_hash BYTEA (M2 model layer) — VER table dùng
    TEXT hex 64-char cho query-able simple equality `WHERE file_hash = $1`.

    Args:
        path: Path tới file binary (FileStore UUID path).

    Returns:
        SHA-256 hex string len 64.

    Raises:
        FileNotFoundError nếu path KHÔNG tồn tại (caller defensive check).
    """
    return hashlib.sha256(path.read_bytes()).hexdigest()


# === Helper: retention cleanup CTE ===

async def _enforce_retention(
    session: AsyncSession,
    document_id: str,
) -> list[str]:
    """Retention '3 gốc + 2 gần nhất' — DELETE middle versions + return deleted file_path list.

    D-V3.1-Phase5-E LOCKED — BE write-time enforce (DEVIATION từ ROADMAP GA-V3.1-G
    "FE filter client-side" per FE codebase audit: DocumentVersionHistory.tsx line
    158 `versions.map(...)` KHÔNG có client-side filter; hint line 146 "Lưu tối đa
    5 phiên bản" text-only).

    SQL CTE window function (raw SQL via sqlalchemy.text — KHÔNG SQLAlchemy ORM
    vì complexity DELETE + RETURNING + ROW_NUMBER):
        WITH ranked AS (
            SELECT id, version_number, file_path,
                   ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number) AS asc_rank,
                   ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number DESC) AS desc_rank
            FROM document_versions
            WHERE document_id = :doc_id
        )
        DELETE FROM document_versions
        WHERE id IN (
            SELECT id FROM ranked
            WHERE asc_rank > :keep_first AND desc_rank > :keep_last
        )
        RETURNING file_path;

    Sau DELETE, return list file_path. Caller (snapshot) check reference count
    cho mỗi path: chỉ FileStore.delete khi `SELECT COUNT(*) WHERE file_path = $1 == 0`.

    Args:
        session: AsyncSession đang mở (caller manage transaction).
        document_id: UUID str của document parent.

    Returns:
        List file_path string của row vừa DELETE (có thể duplicate nếu 2 row reference
        cùng path qua dedupe). Caller dedupe + check ref count trước FileStore.delete.
    """
    result = await session.execute(
        text(
            """
            WITH ranked AS (
                SELECT id, version_number, file_path,
                       ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number) AS asc_rank,
                       ROW_NUMBER() OVER (PARTITION BY document_id ORDER BY version_number DESC) AS desc_rank
                FROM document_versions
                WHERE document_id = :doc_id
            )
            DELETE FROM document_versions
            WHERE id IN (
                SELECT id FROM ranked
                WHERE asc_rank > :keep_first AND desc_rank > :keep_last
            )
            RETURNING file_path
            """
        ),
        {
            "doc_id": str(document_id),
            "keep_first": RETENTION_KEEP_FIRST_N,
            "keep_last": RETENTION_KEEP_LAST_N,
        },
    )
    rows = result.fetchall()
    return [row[0] for row in rows]


async def _cleanup_orphan_files(
    session: AsyncSession,
    deleted_paths: list[str],
) -> int:
    """Sau retention prune, FileStore.delete file vật lý chỉ khi reference count = 0.

    Cross-version dedupe safe (D-V3.1-Phase5-A): nếu file_path còn reference từ
    version khác (v1 vẫn còn) → KHÔNG delete file vật lý.

    Args:
        session: AsyncSession đang mở.
        deleted_paths: list file_path string từ _enforce_retention return.

    Returns:
        Số file vật lý đã DELETE qua FileStore.delete.
    """
    if not deleted_paths:
        return 0

    store = FileStore()
    deleted_count = 0
    seen: set[str] = set()
    for path_str in deleted_paths:
        if path_str in seen:
            continue
        seen.add(path_str)

        # Check reference count còn lại
        result = await session.execute(
            text(
                "SELECT COUNT(*) FROM document_versions WHERE file_path = :path"
            ),
            {"path": path_str},
        )
        ref_count = result.scalar() or 0

        if ref_count == 0:
            try:
                if store.delete(Path(path_str)):
                    deleted_count += 1
            except OSError as e:
                # File system error — log + continue (idempotent FileStore.delete return False)
                logger.warning(
                    "file_cleanup_failed: path=%s err=%s", path_str, e
                )
    return deleted_count


# === Public API: snapshot ===

async def snapshot(
    *,
    session: AsyncSession,
    document: Any,  # duck-type: .id, .hub_id, .filename, .file_path, .mime_type, .file_size_bytes
    change_type: str,
    change_note: str | None = None,
    actor_user_id: str | None = None,
    actor_role: str = "admin",
    actor_hub_id: str | None = None,
) -> dict[str, Any]:
    """Tạo version snapshot cho document atomic + audit emit + retention cleanup.

    D-V3.1-Phase5-A LOCKED dedupe-by-hash:
    - Compute SHA-256 hash từ document.file_path hiện tại.
    - Query existing versions cùng (document_id, file_hash) → nếu match, reference
      file_path cũ (KHÔNG FileStore.save mới); nếu KHÔNG match → reference
      document.file_path (assume caller đã FileStore.save cho file mới trước khi
      gọi snapshot — Plan 05-03 router responsibility).

    D-V3.1-Phase5-E LOCKED retention: sau INSERT, gọi _enforce_retention CTE
    + _cleanup_orphan_files cùng transaction.

    D-V3.1-Phase5-H LOCKED audit: enqueue_audit action='document.version.create'
    payload nest actor_role + actor_hub_id + document_id + version_number + change_type.

    Args:
        session: AsyncSession đang mở (caller transaction).
        document: Document object duck-type với attributes id, hub_id, filename,
            file_path, mime_type, file_size_bytes (+ optional extractor, chunk_count).
        change_type: 'reupload' | 'reextract' | 'content_edit' | 'restore' (CHECK enforce DB).
        change_note: Optional human note.
        actor_user_id: UUID str của user trigger (None nếu system event — rare).
        actor_role: 'admin' (super) | 'hub_admin' — for audit forensic.
        actor_hub_id: None nếu super; hub_id nếu hub_admin.

    Returns:
        dict (DocumentVersionAPI shape) — id, document_id, version_number, ...

    Raises:
        ValueError nếu change_type invalid (CHECK constraint backup ở DB layer).
        FileNotFoundError nếu document.file_path KHÔNG tồn tại.
    """
    if change_type not in ("reupload", "reextract", "content_edit", "restore"):
        raise ValueError(
            f"Invalid change_type {change_type!r}; expected reupload|reextract|content_edit|restore"
        )

    # 1) Compute file hash từ current document.file_path
    current_path = Path(document.file_path)
    file_hash = _compute_file_hash(current_path)

    # 2) Dedupe-by-hash check (D-V3.1-Phase5-A) — query existing versions cùng (doc_id, hash)
    dedupe_result = await session.execute(
        text(
            "SELECT file_path FROM document_versions "
            "WHERE document_id = :doc_id AND file_hash = :hash "
            "LIMIT 1"
        ),
        {"doc_id": str(document.id), "hash": file_hash},
    )
    existing_path_row = dedupe_result.fetchone()
    if existing_path_row:
        # Reference path cũ — KHÔNG FileStore.save mới (Plan 05-03 router check trước
        # khi save nếu là reupload preview-confirm flow; snapshot reference path
        # mà document.file_path hiện tại đang trỏ tới).
        snapshot_file_path = existing_path_row[0]
    else:
        # Hash mới — reference document.file_path hiện tại (Plan 05-03 đã FileStore.save).
        snapshot_file_path = str(current_path)

    # 3) Derive version_number monotonic — SELECT MAX + 1
    max_result = await session.execute(
        text(
            "SELECT COALESCE(MAX(version_number), 0) FROM document_versions "
            "WHERE document_id = :doc_id"
        ),
        {"doc_id": str(document.id)},
    )
    next_version_number = (max_result.scalar() or 0) + 1
    is_original = (next_version_number == 1)

    # 4) INSERT version row
    extractor_used = getattr(document, "extractor", None) or getattr(document, "extractor_used", None)
    chunk_count = getattr(document, "chunk_count", 0) or 0
    name = getattr(document, "filename", None) or getattr(document, "name", "unknown")
    file_type = getattr(document, "mime_type", None) or getattr(document, "file_type", "application/octet-stream")
    file_size = getattr(document, "file_size_bytes", None) or getattr(document, "file_size", 0)

    insert_result = await session.execute(
        text(
            """
            INSERT INTO document_versions (
                document_id, version_number, is_original,
                name, file_type, file_size, file_path, file_hash,
                extractor_used, chunk_count, change_type, change_note, created_by
            ) VALUES (
                :doc_id, :version_number, :is_original,
                :name, :file_type, :file_size, :file_path, :file_hash,
                :extractor_used, :chunk_count, :change_type, :change_note, :created_by
            )
            RETURNING id, created_at
            """
        ),
        {
            "doc_id": str(document.id),
            "version_number": next_version_number,
            "is_original": is_original,
            "name": name,
            "file_type": file_type,
            "file_size": int(file_size),
            "file_path": snapshot_file_path,
            "file_hash": file_hash,
            "extractor_used": extractor_used,
            "chunk_count": int(chunk_count),
            "change_type": change_type,
            "change_note": change_note,
            "created_by": str(actor_user_id) if actor_user_id else None,
        },
    )
    inserted = insert_result.fetchone()
    version_id = str(inserted[0])
    created_at = inserted[1]

    # 5) Retention cleanup CTE cùng transaction (D-V3.1-Phase5-E)
    deleted_paths = await _enforce_retention(session, str(document.id))
    if deleted_paths:
        await _cleanup_orphan_files(session, deleted_paths)
        logger.info(
            "document_version_retention_prune: doc_id=%s deleted_versions=%d",
            document.id, len(deleted_paths),
        )

    # 6) Audit emit (D-V3.1-Phase5-H)
    payload = build_audit_payload(
        actor_role=actor_role,
        actor_hub_id=actor_hub_id,
        extra={
            "document_id": str(document.id),
            "version_number": next_version_number,
            "change_type": change_type,
        },
    )
    enqueue_audit(AuditEntry(
        action=ACTION_VERSION_CREATE,
        user_id=str(actor_user_id) if actor_user_id else None,
        target_type="document_version",
        target_id=version_id,
        hub_id=str(getattr(document, "hub_id", None)) if getattr(document, "hub_id", None) else None,
        payload=payload,
    ))

    # 7) Return DocumentVersionAPI shape dict
    return {
        "id": version_id,
        "document_id": str(document.id),
        "version_number": next_version_number,
        "is_original": is_original,
        "name": name,
        "file_type": file_type,
        "file_size": int(file_size),
        "file_path": snapshot_file_path,
        "file_hash": file_hash,
        "extractor_used": extractor_used,
        "chunk_count": int(chunk_count),
        "change_type": change_type,
        "change_note": change_note,
        "created_by": str(actor_user_id) if actor_user_id else None,
        "created_at": created_at.isoformat() if created_at else None,
    }


# === Public API: list_versions ===

async def list_versions(
    session: AsyncSession,
    document_id: str,
) -> list[dict[str, Any]]:
    """SELECT * FROM document_versions WHERE document_id ORDER BY version_number DESC.

    Trả ≤ 5 row sau retention prune (D-V3.1-Phase5-E LOCKED). KHÔNG paginate.
    """
    result = await session.execute(
        text(
            """
            SELECT id, document_id, version_number, is_original,
                   name, file_type, file_size, file_path, file_hash,
                   extractor_used, chunk_count, change_type, change_note,
                   created_by, created_at
            FROM document_versions
            WHERE document_id = :doc_id
            ORDER BY version_number DESC
            """
        ),
        {"doc_id": str(document_id)},
    )
    rows = result.fetchall()
    return [_row_to_dict(row) for row in rows]


# === Public API: get_version_with_chunks ===

async def get_version_with_chunks(
    session: AsyncSession,
    document_id: str,
    version_id: str,
) -> tuple[dict[str, Any] | None, list[Any]]:
    """SELECT 1 version + return (dict, []) — chunks empty (D-V3.1-Phase5-B LOCKED).

    Returns:
        (version_dict | None nếu KHÔNG tìm thấy, []).
    """
    result = await session.execute(
        text(
            """
            SELECT id, document_id, version_number, is_original,
                   name, file_type, file_size, file_path, file_hash,
                   extractor_used, chunk_count, change_type, change_note,
                   created_by, created_at
            FROM document_versions
            WHERE document_id = :doc_id AND id = :version_id
            LIMIT 1
            """
        ),
        {"doc_id": str(document_id), "version_id": str(version_id)},
    )
    row = result.fetchone()
    if not row:
        return None, []
    return _row_to_dict(row), []  # D-V3.1-Phase5-B LOCKED: chunks = [] empty


# === Public API: get_version_file_path ===

async def get_version_file_path(
    session: AsyncSession,
    document_id: str,
    version_id: str,
) -> Path | None:
    """SELECT file_path WHERE id — return Path object cho StreamingResponse Plan 05-03."""
    result = await session.execute(
        text(
            "SELECT file_path FROM document_versions "
            "WHERE document_id = :doc_id AND id = :version_id LIMIT 1"
        ),
        {"doc_id": str(document_id), "version_id": str(version_id)},
    )
    row = result.fetchone()
    if not row:
        return None
    return Path(row[0])


# === Public API: restore_to_version (D-V3.1-Phase5-D append-only) ===

async def restore_to_version(
    *,
    session: AsyncSession,
    document_id: str,
    version_id: str,
    actor_user_id: str | None = None,
    actor_role: str = "admin",
    actor_hub_id: str | None = None,
) -> dict[str, Any]:
    """Rollback document về trạng thái version target — APPEND-ONLY (D-V3.1-Phase5-D LOCKED).

    Atomic steps:
    1. Resolve target version từ document_versions (raise ValueError nếu KHÔNG tìm thấy).
    2. Resolve current document từ documents.
    3. snapshot(change_type='restore', change_note=f'Restore to v{N}') — version mới
       INSERT TRƯỚC khi UPDATE documents (append-only history preserve).
    4. UPDATE documents.file_path|filename|mime_type|file_size_bytes = giá trị target version.
    5. (D-V3.1-Phase5-I LOCKED SYNC) Re-extract trigger — cocoindex flow hoặc service
       layer existing nếu có. Plan 05-02 skip re-extract trigger trong service (caller
       Plan 05-03 router responsibility — gọi sau khi restore_to_version return).
    6. Audit emit action='document.version.restore' với payload nest restored_to=version_id.

    Args:
        session: AsyncSession đang mở (caller transaction — restore phải atomic).
        document_id: UUID str.
        version_id: UUID str của target version để rollback.
        actor_user_id: UUID str của user trigger.
        actor_role, actor_hub_id: forensic audit metadata.

    Returns:
        dict shape DocumentAPI (document refreshed sau UPDATE).

    Raises:
        ValueError nếu version_id KHÔNG thuộc document_id (cross-document attack).
        LookupError nếu document_id KHÔNG tồn tại.
    """
    # 1) Resolve target version
    target_result = await session.execute(
        text(
            """
            SELECT id, name, file_type, file_size, file_path, file_hash, version_number
            FROM document_versions
            WHERE document_id = :doc_id AND id = :version_id
            LIMIT 1
            """
        ),
        {"doc_id": str(document_id), "version_id": str(version_id)},
    )
    target = target_result.fetchone()
    if not target:
        raise ValueError(
            f"Version {version_id!r} KHÔNG thuộc document {document_id!r} — "
            f"cross-document restore reject"
        )

    target_name = target[1]
    target_file_type = target[2]
    target_file_size = target[3]
    target_file_path = target[4]
    target_version_number = target[6]

    # 2) Resolve current document (cho snapshot trước UPDATE)
    doc_result = await session.execute(
        text(
            "SELECT id, hub_id, filename, file_path, mime_type, file_size_bytes "
            "FROM documents WHERE id = :doc_id LIMIT 1"
        ),
        {"doc_id": str(document_id)},
    )
    current_doc_row = doc_result.fetchone()
    if not current_doc_row:
        raise LookupError(f"Document {document_id!r} KHÔNG tồn tại")

    # Build duck-type document object cho snapshot()
    class _CurrentDoc:
        id = str(current_doc_row[0])
        hub_id = str(current_doc_row[1]) if current_doc_row[1] else None
        filename = current_doc_row[2]
        file_path = current_doc_row[3]
        mime_type = current_doc_row[4]
        file_size_bytes = current_doc_row[5]

    # 3) Snapshot current state TRƯỚC khi UPDATE (append-only D-V3.1-Phase5-D)
    await snapshot(
        session=session,
        document=_CurrentDoc,
        change_type="restore",
        change_note=f"Restore to v{target_version_number}",
        actor_user_id=actor_user_id,
        actor_role=actor_role,
        actor_hub_id=actor_hub_id,
    )

    # 4) UPDATE documents = giá trị target version
    await session.execute(
        text(
            """
            UPDATE documents
            SET file_path = :file_path,
                filename = :filename,
                mime_type = :mime_type,
                file_size_bytes = :file_size,
                updated_at = NOW()
            WHERE id = :doc_id
            """
        ),
        {
            "file_path": target_file_path,
            "filename": target_name,
            "mime_type": target_file_type,
            "file_size": int(target_file_size),
            "doc_id": str(document_id),
        },
    )

    # 5) Audit emit restore action (D-V3.1-Phase5-H)
    restore_payload = build_audit_payload(
        actor_role=actor_role,
        actor_hub_id=actor_hub_id,
        extra={
            "document_id": str(document_id),
            "version_number": target_version_number,
            "restored_to": str(version_id),
        },
    )
    enqueue_audit(AuditEntry(
        action=ACTION_VERSION_RESTORE,
        user_id=str(actor_user_id) if actor_user_id else None,
        target_type="document",
        target_id=str(document_id),
        hub_id=str(_CurrentDoc.hub_id) if _CurrentDoc.hub_id else None,
        payload=restore_payload,
    ))

    # 6) Re-fetch document refreshed (return shape DocumentAPI)
    refreshed_result = await session.execute(
        text(
            "SELECT id, hub_id, filename, file_path, mime_type, file_size_bytes, "
            "status, created_at, updated_at "
            "FROM documents WHERE id = :doc_id LIMIT 1"
        ),
        {"doc_id": str(document_id)},
    )
    refreshed = refreshed_result.fetchone()
    return {
        "id": str(refreshed[0]),
        "hub_id": str(refreshed[1]) if refreshed[1] else None,
        "filename": refreshed[2],
        "file_path": refreshed[3],
        "mime_type": refreshed[4],
        "file_size_bytes": refreshed[5],
        "status": refreshed[6],
        "created_at": refreshed[7].isoformat() if refreshed[7] else None,
        "updated_at": refreshed[8].isoformat() if refreshed[8] else None,
    }


# === Helper: row → dict ===

def _row_to_dict(row: Any) -> dict[str, Any]:
    """Convert SQLAlchemy Row → dict shape DocumentVersionAPI (15 field explicit)."""
    return {
        "id": str(row[0]),
        "document_id": str(row[1]),
        "version_number": row[2],
        "is_original": row[3],
        "name": row[4],
        "file_type": row[5],
        "file_size": row[6],
        "file_path": row[7],
        "file_hash": row[8],
        "extractor_used": row[9],
        "chunk_count": row[10],
        "change_type": row[11],
        "change_note": row[12],
        "created_by": str(row[13]) if row[13] else None,
        "created_at": row[14].isoformat() if row[14] else None,
    }
```

**Lưu ý quan trọng:**
- KHÔNG đụng `file_store.py` source (M2 LOCKED).
- KHÔNG đụng `audit_service.py` source (Plan 02-04 v3.1 LOCKED) — `build_audit_payload` + `enqueue_audit` reuse exact signature.
- `enqueue_audit` KHÔNG enforce AUDIT_ACTIONS frozenset — action mới 'document.version.{create,restore}' KHÔNG cần add vào frozenset (caller responsibility comment).
- `_CurrentDoc` inline class duck-type — pattern Pythonic thay vì namedtuple/dataclass cho 1-shot use; snapshot() đọc attribute trực tiếp.
- Restore re-extract trigger SYNC (D-V3.1-Phase5-I LOCKED) — Plan 05-02 KHÔNG include re-extract call vì `documents_service.py` grep KHÔNG có `coco_flow.run_for_document` HOẶC public reextract API; Plan 05-03 router responsibility (xem note ở `<objective>`).
- Raw SQL via `sqlalchemy.text()` + named bind params `:doc_id` `:hash` `:version_number` — KHÔNG concat string trực tiếp (T-05-02-01 SQL injection mitigation).
- CTE window function ROW_NUMBER PARTITION BY + ORDER BY — Postgres standard SQL; KHÔNG cần special extension.
- `_cleanup_orphan_files` cross-version dedupe safe: nếu v1 vẫn còn reference file path cũ thì KHÔNG xoá vật lý.
- KHÔNG dùng `asyncio.create_task` cho audit emit (BackgroundTask flow tự thân `enqueue_audit` non-blocking — caller KHÔNG cần wrap thêm).
- KHÔNG dùng `getattr(document, ..., default)` cho field bắt buộc (id, file_path) — fail-loud nếu document object missing.
  </action>
  <verify>
    <automated>cd Hub_All/api && python -c "import ast; ast.parse(open('app/services/document_version_service.py').read()); print('syntax OK')" && python -c "from app.services.document_version_service import snapshot, restore_to_version, list_versions, get_version_with_chunks, get_version_file_path, _compute_file_hash, _enforce_retention; import inspect; assert all(inspect.iscoroutinefunction(f) for f in [snapshot, restore_to_version, list_versions, get_version_with_chunks, get_version_file_path, _enforce_retention]); print('5 public API + 1 async helper importable')"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/app/services/document_version_service.py` EXISTS.
    - File chứa `async def snapshot(` + `*,` keyword-only signature.
    - File chứa `async def restore_to_version(` + `*,` keyword-only signature.
    - File chứa `async def list_versions(` (2 positional args session, document_id).
    - File chứa `async def get_version_with_chunks(` + return `tuple` empty chunks.
    - File chứa `async def get_version_file_path(` + return `Path | None`.
    - File chứa `def _compute_file_hash(` (sync helper).
    - File chứa `async def _enforce_retention(` + raw SQL CTE.
    - File chứa `ACTION_VERSION_CREATE = "document.version.create"` (D-V3.1-Phase5-H).
    - File chứa `ACTION_VERSION_RESTORE = "document.version.restore"` (D-V3.1-Phase5-H).
    - File chứa `RETENTION_KEEP_FIRST_N = 3` + `RETENTION_KEEP_LAST_N = 2` (D-V3.1-Phase5-E).
    - File chứa `hashlib.sha256(` (SHA-256 hex dedupe).
    - File chứa `ROW_NUMBER() OVER (PARTITION BY document_id` (CTE window function D-V3.1-Phase5-E).
    - File chứa `RETURNING file_path` (CTE returning deleted paths).
    - File chứa `WHERE asc_rank > :keep_first AND desc_rank > :keep_last` (retention CTE filter).
    - File chứa `enqueue_audit(AuditEntry(` ≥ 2 lần (snapshot + restore).
    - File chứa `build_audit_payload(actor_role=` ≥ 2 lần.
    - File chứa `from app.services.audit_service import` (import audit reuse).
    - File chứa `from app.services.file_store import FileStore` (import FileStore reuse).
    - File chứa `"D-V3.1-Phase5-A"` + `"D-V3.1-Phase5-B"` + `"D-V3.1-Phase5-D"` + `"D-V3.1-Phase5-E"` + `"D-V3.1-Phase5-H"` (5 traceability mention).
    - File chứa `COALESCE(MAX(version_number), 0) + 1` (monotonic derive).
    - File chứa `is_original = (next_version_number == 1)` (derive logic).
    - File chứa `chunks = [] empty` HOẶC `return _row_to_dict(row), []` (D-V3.1-Phase5-B empty chunks).
    - File chứa `snapshot(...)` call inside `restore_to_version` (D-V3.1-Phase5-D append-only TRƯỚC UPDATE documents).
    - File chứa `raise ValueError(` cho cross-document restore reject (security T-05-02-02).
    - File chứa `sqlalchemy.text` import (raw SQL pattern Plan 01-02 carry forward).
    - File KHÔNG chứa SQL concat string (defense T-05-02-01 — only `:doc_id` `:hash` bind params).
    - `cd Hub_All/api && python -c "import ast; ast.parse(open('app/services/document_version_service.py').read())"` exit 0.
    - `cd Hub_All/api && python -c "from app.services.document_version_service import snapshot, restore_to_version, list_versions; import inspect; assert inspect.iscoroutinefunction(snapshot)"` exit 0.
  </acceptance_criteria>
  <done>
    document_version_service.py có 5 public API + 2 private helper; dedupe-by-hash (D-V3.1-Phase5-A) + CTE retention (D-V3.1-Phase5-E) + append-only restore (D-V3.1-Phase5-D) + chunks=[] empty (D-V3.1-Phase5-B) + audit 2 action (D-V3.1-Phase5-H); raw SQL bind params safe; importable runtime.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo test_document_version_service.py — 8 unit test pure Python (mock AsyncSession + FileStore + audit_service)</name>
  <read_first>
    - Hub_All/api/app/services/document_version_service.py (state sau Task 1 — verify 5 public API + 2 helper signature)
    - Hub_All/api/tests/unit/test_require_hub_admin_for.py (Plan 02-01 — `_make_session` AsyncMock factory pattern + `pytest.raises` + `with patch(` module-level patching)
    - Hub_All/api/tests/unit/test_role_helper.py:24-42 (AsyncMock factory `_make_session(*fetchone_returns)` carry forward)
    - Hub_All/api/app/services/audit_service.py (AuditEntry dataclass + enqueue_audit signature — verify mock target)
  </read_first>
  <behavior>
    - Test 1 (snapshot_inserts_row_atomic): snapshot(doc, change_type='reupload') → mock session.execute called >= 3 lần (dedupe query + MAX query + INSERT); return dict có `id` + `version_number=1` + `is_original=True`.
    - Test 2 (snapshot_dedupe_by_hash_reuses_path): mock dedupe SELECT trả ('/old/path',); snapshot trả dict.file_path = '/old/path' (KHÔNG dùng document.file_path mới).
    - Test 3 (snapshot_audit_emits_create_action): patch enqueue_audit; snapshot(doc, actor_role='hub_admin', actor_hub_id='dmd-uuid'); verify enqueue_audit gọi 1 lần với AuditEntry.action == 'document.version.create' + payload.actor_role == 'hub_admin' + payload.document_id + payload.version_number + payload.change_type.
    - Test 4 (snapshot_version_number_monotonic): mock MAX query trả 3 (3 versions existing); snapshot trả dict.version_number == 4 + is_original=False.
    - Test 5 (list_versions_returns_desc): mock SELECT fetchall trả 3 row với version_number [3, 2, 1] DESC; list_versions trả list 3 dict order [3, 2, 1].
    - Test 6 (get_version_with_chunks_returns_empty_chunks): mock SELECT fetchone trả 1 row; get_version_with_chunks trả `(dict, [])` chunks empty array (D-V3.1-Phase5-B verify).
    - Test 7 (get_version_file_path_returns_path_object): mock SELECT file_path trả '/tmp/test.docx'; get_version_file_path trả `Path('/tmp/test.docx')`.
    - Test 8 (compute_file_hash_sha256_hex_64): create real tempfile với bytes b'hello'; _compute_file_hash trả string len == 64 + match hashlib.sha256(b'hello').hexdigest() exact.
  </behavior>
  <action>
Tạo file `Hub_All/api/tests/unit/test_document_version_service.py`:

```python
"""Unit test document_version_service — Phase 5 Plan 05-02 VER-02.

8 test pure Python — mock AsyncSession.execute + patch enqueue_audit (KHÔNG hit Postgres/Redis).

Test cases (D-V3.1-Phase5-A/B/D/E/H LOCKED):
1. snapshot_inserts_row_atomic — dedupe query + MAX query + INSERT, version_number=1.
2. snapshot_dedupe_by_hash_reuses_path — D-V3.1-Phase5-A; mock dedupe match trả path cũ.
3. snapshot_audit_emits_create_action — D-V3.1-Phase5-H; AuditEntry action='document.version.create'.
4. snapshot_version_number_monotonic — MAX trả 3 → next = 4.
5. list_versions_returns_desc — SELECT DESC order verify.
6. get_version_with_chunks_returns_empty_chunks — D-V3.1-Phase5-B verify.
7. get_version_file_path_returns_path_object — Path object cho StreamingResponse.
8. compute_file_hash_sha256_hex_64 — SHA-256 hex 64-char exact match.

Carry forward patterns:
- `_make_session` AsyncMock factory (Plan 01-02 + 02-01 — test_role_helper.py + test_require_hub_admin_for.py).
- `with patch("app.services.document_version_service.enqueue_audit", ...)` — module-level patch.
- SimpleNamespace duck-type document object — KHÔNG full Document ORM.
"""
from __future__ import annotations

import hashlib
import tempfile
from datetime import datetime, timezone
from pathlib import Path
from types import SimpleNamespace
from unittest.mock import AsyncMock, MagicMock, patch
from uuid import uuid4

import pytest

from app.services.document_version_service import (
    ACTION_VERSION_CREATE,
    _compute_file_hash,
    get_version_file_path,
    get_version_with_chunks,
    list_versions,
    snapshot,
)


# === Helper: AsyncMock(AsyncSession) factory ===

def _make_session(*fetchone_returns: object, fetchall_returns: list | None = None, scalar_returns: list | None = None) -> AsyncMock:
    """Build AsyncMock(AsyncSession) trả về sequence fetchone + fetchall + scalar results.

    Carry forward pattern tests/unit/test_require_hub_admin_for.py + test_role_helper.py.

    Args:
        *fetchone_returns: Sequence row return cho mỗi session.execute().fetchone() call.
        fetchall_returns: List rows cho session.execute().fetchall() call (1 lần).
        scalar_returns: List scalar values cho session.execute().scalar() call.

    Example:
        >>> session = _make_session(None, (4,), ('uuid-1', datetime.now()))
        # 3 execute call: 1st fetchone None (no dedupe), 2nd scalar 4 (MAX), 3rd fetchone INSERT RETURNING
    """
    session = AsyncMock()
    result_mocks = []
    fetchone_iter = iter(fetchone_returns)
    fetchall_iter = iter([fetchall_returns] if fetchall_returns is not None else [])
    scalar_iter = iter(scalar_returns or [])

    # Determine how many execute calls expected (max of any iterator)
    expected_calls = max(
        len(fetchone_returns),
        1 if fetchall_returns is not None else 0,
        len(scalar_returns or []),
    )

    for _ in range(expected_calls + 5):  # Extra slack cho edge cases
        result = MagicMock()
        try:
            result.fetchone.return_value = next(fetchone_iter)
        except StopIteration:
            result.fetchone.return_value = None
        try:
            result.fetchall.return_value = next(fetchall_iter)
        except StopIteration:
            result.fetchall.return_value = []
        try:
            result.scalar.return_value = next(scalar_iter)
        except StopIteration:
            result.scalar.return_value = 0
        result_mocks.append(result)

    session.execute = AsyncMock(side_effect=result_mocks)
    return session


def _make_doc(file_path: str = "/tmp/test.docx", file_size: int = 1024) -> SimpleNamespace:
    """SimpleNamespace document duck-type — id, hub_id, filename, file_path, mime_type, file_size_bytes."""
    return SimpleNamespace(
        id=str(uuid4()),
        hub_id=str(uuid4()),
        filename="test.docx",
        file_path=file_path,
        mime_type="application/vnd.openxmlformats-officedocument.wordprocessingml.document",
        file_size_bytes=file_size,
    )


# === Test 1: snapshot inserts row atomic ===

@pytest.mark.asyncio
async def test_snapshot_inserts_row_atomic(tmp_path: Path) -> None:
    """snapshot(change_type='reupload') → dedupe query + MAX query + INSERT, version_number=1."""
    # Tạo real tempfile cho _compute_file_hash đọc bytes
    test_file = tmp_path / "test.docx"
    test_file.write_bytes(b"document content v1")
    doc = _make_doc(file_path=str(test_file))

    # Mock session: 1) dedupe None (no match), 2) MAX=0 → next=1, 3) INSERT RETURNING (uuid, datetime)
    inserted_id = uuid4()
    created_at = datetime.now(timezone.utc)
    session = _make_session(
        None,                           # dedupe fetchone
        (inserted_id, created_at),      # INSERT RETURNING fetchone
        fetchall_returns=[],            # _enforce_retention DELETE RETURNING empty
        scalar_returns=[0],             # MAX returns 0
    )

    with patch("app.services.document_version_service.enqueue_audit"):
        result = await snapshot(
            session=session,
            document=doc,
            change_type="reupload",
            actor_user_id=str(uuid4()),
            actor_role="admin",
            actor_hub_id=None,
        )

    assert session.execute.call_count >= 3, (
        f"Expect >= 3 execute calls (dedupe + MAX + INSERT + retention), got {session.execute.call_count}"
    )
    assert result["version_number"] == 1
    assert result["is_original"] is True
    assert result["change_type"] == "reupload"
    assert result["file_hash"] == hashlib.sha256(b"document content v1").hexdigest()
    assert result["document_id"] == doc.id


# === Test 2: snapshot dedupe by hash reuses path ===

@pytest.mark.asyncio
async def test_snapshot_dedupe_by_hash_reuses_path(tmp_path: Path) -> None:
    """D-V3.1-Phase5-A: existing version cùng file_hash → reference path cũ."""
    test_file = tmp_path / "test.docx"
    test_file.write_bytes(b"identical content")
    doc = _make_doc(file_path=str(test_file))

    old_path = "/old/storage/abc-uuid.docx"
    inserted_id = uuid4()
    created_at = datetime.now(timezone.utc)
    # Mock: 1) dedupe HIT → trả (old_path,), 2) MAX=0, 3) INSERT RETURNING
    session = _make_session(
        (old_path,),                    # dedupe fetchone HIT
        (inserted_id, created_at),      # INSERT RETURNING fetchone
        fetchall_returns=[],
        scalar_returns=[0],
    )

    with patch("app.services.document_version_service.enqueue_audit"):
        result = await snapshot(
            session=session,
            document=doc,
            change_type="reupload",
        )

    assert result["file_path"] == old_path, (
        f"Dedupe should reference old path, got {result['file_path']!r}"
    )


# === Test 3: snapshot audit emits create action with metadata nest ===

@pytest.mark.asyncio
async def test_snapshot_audit_emits_create_action(tmp_path: Path) -> None:
    """D-V3.1-Phase5-H: enqueue_audit action='document.version.create' + payload nest."""
    test_file = tmp_path / "test.docx"
    test_file.write_bytes(b"audit test content")
    doc = _make_doc(file_path=str(test_file))

    inserted_id = uuid4()
    created_at = datetime.now(timezone.utc)
    session = _make_session(
        None,
        (inserted_id, created_at),
        fetchall_returns=[],
        scalar_returns=[0],
    )

    hub_id = str(uuid4())
    actor_user_id = str(uuid4())

    with patch("app.services.document_version_service.enqueue_audit") as mock_enqueue:
        await snapshot(
            session=session,
            document=doc,
            change_type="reupload",
            actor_user_id=actor_user_id,
            actor_role="hub_admin",
            actor_hub_id=hub_id,
        )

    assert mock_enqueue.call_count == 1, "enqueue_audit phải gọi 1 lần"
    entry = mock_enqueue.call_args.args[0]
    assert entry.action == ACTION_VERSION_CREATE
    assert entry.action == "document.version.create"
    assert entry.payload["actor_role"] == "hub_admin"
    assert entry.payload["actor_hub_id"] == hub_id
    assert entry.payload["document_id"] == doc.id
    assert entry.payload["version_number"] == 1
    assert entry.payload["change_type"] == "reupload"
    assert entry.user_id == actor_user_id
    assert entry.target_type == "document_version"


# === Test 4: snapshot version_number monotonic increment ===

@pytest.mark.asyncio
async def test_snapshot_version_number_monotonic(tmp_path: Path) -> None:
    """SELECT MAX(version_number) trả 3 → next version = 4 + is_original=False."""
    test_file = tmp_path / "test.docx"
    test_file.write_bytes(b"v4 content")
    doc = _make_doc(file_path=str(test_file))

    inserted_id = uuid4()
    created_at = datetime.now(timezone.utc)
    # MAX trả 3 → next = 4
    session = _make_session(
        None,
        (inserted_id, created_at),
        fetchall_returns=[],
        scalar_returns=[3],     # MAX = 3
    )

    with patch("app.services.document_version_service.enqueue_audit"):
        result = await snapshot(
            session=session,
            document=doc,
            change_type="content_edit",
        )

    assert result["version_number"] == 4
    assert result["is_original"] is False


# === Test 5: list_versions returns DESC order ===

@pytest.mark.asyncio
async def test_list_versions_returns_desc() -> None:
    """SELECT * ORDER BY version_number DESC; trả 3 row order [3, 2, 1]."""
    doc_id = str(uuid4())
    now = datetime.now(timezone.utc)
    rows = [
        (uuid4(), doc_id, 3, False, "v3.docx", "docx", 3000, "/path/v3", "hash3", None, 5, "reupload", None, None, now),
        (uuid4(), doc_id, 2, False, "v2.docx", "docx", 2000, "/path/v2", "hash2", None, 4, "reextract", None, None, now),
        (uuid4(), doc_id, 1, True, "v1.docx", "docx", 1000, "/path/v1", "hash1", None, 3, "reupload", None, None, now),
    ]
    session = _make_session(fetchall_returns=rows)

    result = await list_versions(session, doc_id)

    assert len(result) == 3
    assert [r["version_number"] for r in result] == [3, 2, 1]
    assert result[2]["is_original"] is True
    assert result[0]["version_number"] == 3
    assert result[0]["change_type"] == "reupload"


# === Test 6: get_version_with_chunks returns empty chunks array ===

@pytest.mark.asyncio
async def test_get_version_with_chunks_returns_empty_chunks() -> None:
    """D-V3.1-Phase5-B LOCKED: chunks = [] empty array (FE typecheck happy)."""
    doc_id = str(uuid4())
    version_id = str(uuid4())
    now = datetime.now(timezone.utc)
    row = (uuid4(), doc_id, 1, True, "v1.docx", "docx", 1000, "/path/v1", "hash1", None, 3, "reupload", None, None, now)
    session = _make_session(row)

    version_dict, chunks = await get_version_with_chunks(session, doc_id, version_id)

    assert version_dict is not None
    assert version_dict["version_number"] == 1
    assert chunks == [], f"D-V3.1-Phase5-B: chunks phải là [], got {chunks!r}"
    assert isinstance(chunks, list)


# === Test 7: get_version_file_path returns Path object ===

@pytest.mark.asyncio
async def test_get_version_file_path_returns_path_object() -> None:
    """SELECT file_path → return Path object cho StreamingResponse Plan 05-03."""
    doc_id = str(uuid4())
    version_id = str(uuid4())
    session = _make_session(("/tmp/storage/abc.docx",))

    result = await get_version_file_path(session, doc_id, version_id)

    assert result == Path("/tmp/storage/abc.docx")
    assert isinstance(result, Path)


# === Test 8: _compute_file_hash returns SHA-256 hex 64-char ===

def test_compute_file_hash_returns_sha256_hex_64() -> None:
    """D-V3.1-Phase5-A: SHA-256 hex 64-char exact match hashlib stdlib."""
    with tempfile.NamedTemporaryFile(delete=False) as f:
        f.write(b"hello")
        tmp_path = Path(f.name)

    try:
        result = _compute_file_hash(tmp_path)
        expected = hashlib.sha256(b"hello").hexdigest()

        assert len(result) == 64, f"SHA-256 hex phải 64 char, got {len(result)}"
        assert result == expected
        # Verify hex chars only (0-9, a-f)
        assert all(c in "0123456789abcdef" for c in result)
    finally:
        tmp_path.unlink(missing_ok=True)
```

**Lưu ý:**
- 8 test pure Python — pytest-asyncio cho 7 async + 1 sync (test 8 `_compute_file_hash` là sync).
- `with patch("app.services.document_version_service.enqueue_audit", ...)` — patch ở module nơi import (NOT `app.services.audit_service.enqueue_audit`).
- `SimpleNamespace` duck-type document — đủ `.id .hub_id .filename .file_path .mime_type .file_size_bytes`.
- `_make_session` factory linh hoạt — accept fetchone sequence + fetchall + scalar (CTE retention return [] empty + MAX scalar).
- `tmp_path` pytest fixture cho test cần real file (cho _compute_file_hash).
- Test 1 assert `call_count >= 3` (dedupe + MAX + INSERT + retention CTE — total 4 nhưng allow slack cho refactor).
- Test 3 verify exact `entry.action == 'document.version.create'` + payload structure khớp D-V3.1-Phase5-H spec.
- KHÔNG import `from app.db.session` (pure mock — T-05-01-06 mitigation tránh hit real DB).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/unit/test_document_version_service.py -x --tb=short 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/unit/test_document_version_service.py` EXISTS.
    - File chứa `from app.services.document_version_service import` (import SUT).
    - File chứa `ACTION_VERSION_CREATE` (constant import verify).
    - File chứa `@pytest.mark.asyncio` ≥ 7 lần (7 async test — test 8 _compute_file_hash sync).
    - File chứa `async def test_` ≥ 7 lần.
    - File chứa `def test_compute_file_hash_returns_sha256_hex_64(` (sync test 8).
    - File chứa `def _make_session(` (helper factory carry forward Plan 01-02 + 02-01).
    - File chứa `def _make_doc(` (SimpleNamespace duck-type doc fixture).
    - File chứa `with patch("app.services.document_version_service.enqueue_audit"` ≥ 4 lần (test 1, 2, 3, 4).
    - File chứa `entry.action == "document.version.create"` (Test 3 verify action D-V3.1-Phase5-H).
    - File chứa `entry.payload["actor_role"] == "hub_admin"` (Test 3 verify payload nest).
    - File chứa `entry.payload["document_id"]` + `entry.payload["version_number"]` + `entry.payload["change_type"]` (Test 3 payload structure).
    - File chứa `result["version_number"] == 1` (Test 1 first version).
    - File chứa `result["is_original"] is True` (Test 1 derive).
    - File chứa `result["version_number"] == 4` + `result["is_original"] is False` (Test 4 monotonic).
    - File chứa `assert result["file_path"] == old_path` (Test 2 dedupe).
    - File chứa `[r["version_number"] for r in result] == [3, 2, 1]` (Test 5 DESC order).
    - File chứa `chunks == []` (Test 6 D-V3.1-Phase5-B verify).
    - File chứa `Path("/tmp/storage/abc.docx")` (Test 7 Path object).
    - File chứa `hashlib.sha256(b"hello").hexdigest()` (Test 8 exact match).
    - File chứa `len(result) == 64` (Test 8 hex 64-char verify).
    - `cd Hub_All/api && python -m pytest tests/unit/test_document_version_service.py -x` exit 0 (8 test PASS).
    - `cd Hub_All/api && python -m pytest tests/unit/test_document_version_service.py --tb=no -q 2>&1 | grep -E "8 passed"` exit 0.
    - File KHÔNG chứa `from app.db.session` (pure mock, T-05-01-06 mitigation).
  </acceptance_criteria>
  <done>
    test_document_version_service.py có 8 test PASS pytest; mock AsyncSession + patch enqueue_audit + tmp_path real file cho hash; cover snapshot dedupe + monotonic + audit emit + list DESC + chunks empty + Path return + hash 64-char; coverage core service API VER-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05-03 router → document_version_service.snapshot | document object từ resolved DB row (trusted); change_type từ caller body (UNTRUSTED — CHECK constraint + ValueError early reject) |
| document_version_service → audit_service.enqueue_audit | Internal trusted helper (Plan 02-04 v3.1) |
| document_version_service → FileStore.delete | Internal trusted helper (M2 LOCKED); cross-version dedupe safe check ref count = 0 |
| document_version_service → Postgres CTE DELETE | Raw SQL via sqlalchemy.text() + named bind params (T-05-02-01 SQL injection mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering (SQL injection) | document_id + version_id + hash từ body input → raw SQL `text()` execution | mitigate | All queries dùng named bind params `:doc_id` `:hash` `:version_id`; KHÔNG concat string trực tiếp. Plan 01-02 v3.1 carry forward. |
| T-05-02-02 | Elevation of Privilege | Cross-document restore (version_id thuộc document khác) | mitigate | restore_to_version() check `WHERE document_id = :doc_id AND id = :version_id` — fail ValueError nếu version_id KHÔNG thuộc document_id. Defense in depth ngoài Plan 05-03 RBAC. |
| T-05-02-03 | Information Disclosure | Audit emit leak document_id + version_number + file_hash | accept | `payload` audit_logs scope admin role qua audit_logs router require_role("admin") guard (M2 carry forward). Forensic feature by design. |
| T-05-02-04 | Integrity (storage explosion) | Reupload exact same file 1000x → 1000 versions × file copy | mitigate | D-V3.1-Phase5-A LOCKED dedupe-by-hash (reupload exact same → reference path cũ) + D-V3.1-Phase5-E retention cap COUNT(*) ≤ 5 per document. Worst case 1 doc × 5 versions × 5MB = 25MB. |
| T-05-02-05 | Tampering (file delete race) | _cleanup_orphan_files xoá file vẫn còn version reference | mitigate | _cleanup_orphan_files `SELECT COUNT(*) WHERE file_path = $1 == 0` check trước FileStore.delete; cross-version dedupe safe. |
| T-05-02-06 | DoS | CTE window function on large document_versions table (millions row) | accept | Retention CTE chạy CHỈ trên rows `WHERE document_id = :doc_id` (≤ 5 row sau prune); INDEX `ix_document_versions_document_id` (Plan 05-01) — query plan index-only scan. O(log n) per document. |
| T-05-02-07 | Repudiation | Restore action KHÔNG audit | mitigate | enqueue_audit action='document.version.restore' với payload nest restored_to=version_id + document_id + version_number. Plan 05-04 scenario 5 forensic chain verify. |
| T-05-02-08 | Tampering | Snapshot save file mới qua FileStore.save() race condition | accept | Plan 05-02 service KHÔNG gọi FileStore.save (Plan 05-03 router responsibility — caller save trước khi gọi snapshot). Snapshot chỉ reference document.file_path hiện tại. |

</threat_model>

<verification>
- File `document_version_service.py` export 5 public API + 2 private helper async/sync.
- snapshot() implement D-V3.1-Phase5-A dedupe-by-hash + D-V3.1-Phase5-E retention CTE + D-V3.1-Phase5-H audit emit.
- restore_to_version() implement D-V3.1-Phase5-D append-only (snapshot TRƯỚC UPDATE) + D-V3.1-Phase5-H audit emit.
- get_version_with_chunks() trả `(dict, [])` empty chunks (D-V3.1-Phase5-B LOCKED).
- _enforce_retention() CTE window function ROW_NUMBER PARTITION BY — Postgres standard SQL.
- _compute_file_hash() SHA-256 hex 64-char (Python stdlib hashlib).
- Raw SQL via `sqlalchemy.text()` + named bind params (T-05-02-01 mitigation).
- 8 unit test PASS pytest — mock AsyncSession + patch enqueue_audit + tmp_path real file cho hash.
- Plan 05-03 router 4 endpoint consume 5 public API exact signature.
- Plan 05-04 integration test E2E verify dedupe + retention + restore append-only + audit forensic.
</verification>

<success_criteria>
1. document_version_service.py có 5 public API + 2 private helper — syntax Python hợp lệ + importable.
2. snapshot() implement 7 step atomic — hash compute + dedupe + version_number monotonic + INSERT + retention CTE + file cleanup + audit emit.
3. restore_to_version() append-only D-V3.1-Phase5-D — snapshot TRƯỚC UPDATE documents.
4. _enforce_retention() CTE window function ROW_NUMBER PARTITION — D-V3.1-Phase5-E enforce.
5. 8 unit test PASS — cover semantic VER-02 core.
6. Plan 05-03 router unblocked với 5 API ready.
7. Plan 05-04 integration test sẽ verify E2E semantic.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/05-document-version-history/05-02-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- document_version_service.py 5 public API + 2 private helper ship
- D-V3.1-Phase5-A dedupe-by-hash trong cùng document_id (reupload exact same → reference path cũ)
- D-V3.1-Phase5-B chunks=[] empty (FE typecheck happy)
- D-V3.1-Phase5-D restore append-only (snapshot TRƯỚC UPDATE)
- D-V3.1-Phase5-E retention CTE window function "3 gốc + 2 gần nhất"
- D-V3.1-Phase5-H audit 2 action codes (document.version.create + restore)
- 8 unit test PASS — cover semantic VER-02 core
- Plan 05-03 router unblocked với 5 API ready
- Phase 5 trigger điểm reupload/edit/reextract wiring defer Plan 05-03 hoặc v4.0 (BE chưa ship endpoint tương ứng — note ở `<objective>`)
- Raw SQL bind params safe (T-05-02-01 mitigation)
- Audit reuse build_audit_payload + enqueue_audit (Plan 02-04 v3.1 LOCKED carry forward)

Commit atomic: `feat(05-02): document_version_service.py 5 API + 2 helper + 8 unit test (VER-02)` + body tiếng Việt nói tại sao (Phase 5 service layer — snapshot dedupe by hash + restore append-only + retention CTE 3 gốc 2 gần nhất + audit emit 2 action D-V3.1-Phase5-A/D/E/H LOCKED; chunks=[] empty D-V3.1-Phase5-B; raw SQL bind params safe; Plan 05-03 router 4 endpoint unblocked).
</output>
