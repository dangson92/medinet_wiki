---
plan: 03
phase: 4
wave: 2
type: execute
depends_on: [01, 02]
files_modified:
  - Hub_All/api/app/rag/flow.py
  - Hub_All/api/app/rag/setup.py
  - Hub_All/api/app/main.py
  - Hub_All/api/tests/unit/test_rag_flow.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]
must_haves:
  truths:
    - "cocoindex_app là coco.App đăng ký với name 'medinet_wiki_ingest' qua coco.App(coco.AppConfig(name=...), main_fn) — cocoindex 1.0.3 actual API (KHÔNG @cocoindex.flow_def, KHÔNG cocoindex.sources.Postgres notification — đều không tồn tại trong 1.0.3)."
    - "ChunkRow @dataclass 10 fields match Migration 0001 chunks schema; ChunkRow.vector dùng cocoindex.resources.schema.VectorSchema(dtype float32, size 1536) làm VectorSchemaProvider — pg.TableSchema.from_class(ChunkRow) build PASS với cột vector(1536) (KHÔNG raise VectorSpecProvider error)."
    - "stable_chunk_id(doc_id, idx) trả uuid5 deterministic — cùng (doc_id, idx) luôn ra cùng UUID (D-1/D-2 citation preservation qua re-index)."
    - "medinet_wiki_main orchestrator dùng pg.PgTableSource đọc documents + pg.mount_table_target chunks USER-managed (Decision B1 — Alembic owns ix_chunks_vector_hnsw) + coco.mount_each per-document processor; index_document extract → chunk VN → embed dim 1536 → table.declare_row."
    - "FastAPI lifespan gọi setup_cocoindex qua asyncio.to_thread (KHÔNG block event loop) FAIL-FAST (raise nếu setup lỗi — KHÔNG mask blocker) + lưu app.state.cocoindex_app cho A4 BackgroundTasks Plan 04-04; shutdown gọi stop_cocoindex."
    - "pytest tests/unit/test_rag_flow.py PASS — verify cocoindex_app registered + ChunkRow schema build no-raise + stable_chunk_id deterministic + 0 reference deprecated 0.x API."
  artifacts:
    - path: Hub_All/api/app/rag/flow.py
      provides: "coco.App medinet_wiki_ingest + ChunkRow dataclass + VectorSchema provider + 3 @coco.fn op + stable_chunk_id + medinet_wiki_main orchestrator."
      contains: "coco.App(coco.AppConfig(name=\"medinet_wiki_ingest\""
    - path: Hub_All/api/app/rag/setup.py
      provides: "setup_cocoindex + get_cocoindex_app + stop_cocoindex — cocoindex 1.0.3 lifespan + LMDB + update_blocking initial backfill."
      contains: "def setup_cocoindex"
    - path: Hub_All/api/app/main.py
      provides: "Lifespan step setup_cocoindex qua asyncio.to_thread fail-fast + app.state.cocoindex_app."
      contains: "setup_cocoindex"
    - path: Hub_All/api/tests/unit/test_rag_flow.py
      provides: "Unit test cocoindex 1.0.3 API surface + ChunkRow schema build + stable_chunk_id."
      contains: "def test_"
  key_links:
    - from: Hub_All/api/app/rag/flow.py
      to: Hub_All/api/app/services (file_extract + vn_chunker + embedder)
      via: "import extract_text, chunk_vietnamese, embed_text, EMBEDDING_DIM"
      pattern: "from app.services"
    - from: Hub_All/api/app/rag/setup.py
      to: Hub_All/api/app/rag/flow.py
      via: "import cocoindex_app + PG_POOL_KEY"
      pattern: "from app.rag.flow import"
    - from: Hub_All/api/app/main.py
      to: Hub_All/api/app/rag/setup.py
      via: "lifespan gọi setup_cocoindex qua asyncio.to_thread + get_cocoindex_app"
      pattern: "setup_cocoindex|get_cocoindex_app"
---

# Plan 04-03: CocoIndex 1.0.3 flow medinet_wiki_ingest + lifespan setup

<objective>
Định nghĩa cocoindex flow thực sự bằng **cocoindex 1.0.3 actual API** (xác minh trong 04-COCOINDEX-API-RESEARCH.md qua đọc source `.venv/Lib/site-packages/cocoindex/`).

**QUAN TRỌNG — API 0.x KHÔNG tồn tại:** `@cocoindex.flow_def`, `cocoindex.sources.Postgres(notification=...)`, `cocoindex.targets.Postgres`, `cocoindex.FlowLiveUpdater`, `cocoindex.init()`, `cocoindex.setup_flow()`, `cocoindex.VectorIndexDef` — TẤT CẢ không có trong cocoindex 1.0.3. INGEST-01 viết literal `@cocoindex.flow_def` + `PostgresNotification` là text từ API 0.x lỗi thời. Plan này hiện thực **INTENT** của INGEST-01 (auto-pickup ingest trong SLA) bằng cocoindex 1.0.3 App+lifespan+mount pattern + A4 BackgroundTasks (Plan 04-04 ship trigger).

Kiến trúc cocoindex 1.0.3:
- **App:** `coco.App(coco.AppConfig(name="medinet_wiki_ingest"), main_fn)` đăng ký tại import-time.
- **Source:** `pg.PgTableSource(pool, table_name="documents")` — one-shot SELECT qua `fetch_rows()`. Cocoindex 1.0.3 KHÔNG có Postgres LISTEN/NOTIFY (docstring `_source.py:3-5`: "Change notifications will be added later").
- **Target:** `pg.mount_table_target(PG_POOL_KEY, table_name="chunks", table_schema=..., managed_by=ManagedBy.USER)` — Decision B1: Alembic owns DDL + `ix_chunks_vector_hnsw`; cocoindex chỉ INSERT/UPSERT rows.
- **Trigger:** KHÔNG có notification → re-process qua `cocoindex_app.update_blocking()`. Plan 04-04 router add BackgroundTask gọi update_blocking sau response 202 (Decision A4).
- **VectorSchema:** `ChunkRow.vector` annotation dùng `cocoindex.resources.schema.VectorSchema(dtype=np.dtype(np.float32), size=1536)` — VectorSchema IS VectorSchemaProvider Protocol (verified isinstance). KHÔNG dùng `@coco.fn` callable làm provider (callable KHÔNG có `__coco_vector_schema__` → `TableSchema.from_class` raise ValueError — đây chính là blocker SC2 ở lần execute trước).

Purpose: INGEST-01/02/03 — flow đăng ký + transform extract/chunk/embed + target chunks pgvector.
Output: flow.py, setup.py, main.py lifespan, test_rag_flow.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-COCOINDEX-API-RESEARCH.md

<interfaces>
Plan 04-02 ship (Wave 1 — đã có khi Plan 04-03 chạy):
```python
# app/services/file_extract.py
def extract_text(file_path: Path) -> tuple[str, bool, dict[str, Any]]  # (text, is_scanned, meta)
# app/services/vn_chunker.py
@dataclass
class ChunkDraft:
    content: str
    heading_path: str | None
    page_start: int | None
    page_end: int | None
def chunk_vietnamese(text: str, *, chunk_size_chars=1200, overlap_chars=120) -> list[ChunkDraft]
# app/services/embedder.py
EMBEDDING_DIM: int = 1536
async def embed_text(text: str, *, model: str | None = None) -> list[float]
```

Plan 04-01 ship: Settings.cocoindex_lmdb_path (env COCOINDEX_DB).

cocoindex 1.0.3 API (verified — 04-COCOINDEX-API-RESEARCH.md Section 1-9):
```python
import cocoindex as coco
from cocoindex.connectors import postgres as pg
from cocoindex.connectorkits import target as ck_target
from cocoindex.resources import schema as coco_schema

VS = coco_schema.VectorSchema(dtype=np.dtype(np.float32), size=1536)  # IS VectorSchemaProvider

@coco.fn          # decorator — sync hoặc async
@coco.lifespan    # đăng ký lifespan generator trên default env

schema = await pg.TableSchema.from_class(ChunkRow, primary_key=["id"])   # async classmethod
table = await pg.mount_table_target(PG_POOL_KEY, table_name="chunks",
    table_schema=schema, pg_schema_name="public", managed_by=ck_target.ManagedBy.USER)
source = pg.PgTableSource(pool, table_name="documents", columns=[...])
await coco.mount_each(index_document, source.fetch_rows().items(key=lambda r: str(r["id"])), table)
table.declare_row(row=ChunkRow(...))

cocoindex_app = coco.App(coco.AppConfig(name="medinet_wiki_ingest"), main_fn)
coco.start_blocking(); cocoindex_app.update_blocking(); coco.stop_blocking()
```
LMDB: env `COCOINDEX_DB` = filesystem path (KHÔNG `COCOINDEX_DATABASE_URL`).
asyncpg DSN: SQLAlchemy `postgresql+asyncpg://` → asyncpg cần `postgresql://` (replace prefix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: flow.py — coco.App + ChunkRow + VectorSchema + 3 @coco.fn op</name>
  <read_first>
    - Hub_All/api/.venv/Lib/site-packages/cocoindex/connectors/postgres/_target.py (xác nhận signature mount_table_target + TableSchema.from_class là async classmethod + declare_row)
    - Hub_All/api/.venv/Lib/site-packages/cocoindex/resources/schema.py (xác nhận VectorSchema + VectorSchemaProvider Protocol)
    - Hub_All/api/app/services/__init__.py (Plan 04-02 — symbol thực tế embed_text)
    - Hub_All/api/migrations/versions/0001_initial_schema.py (chunks 10 cột chính xác)
  </read_first>
  <action>
  Tạo `Hub_All/api/app/rag/flow.py`.

  **Imports:** `import cocoindex as coco`, `from cocoindex.connectors import postgres as pg`, `from cocoindex.connectorkits import target as ck_target`, `from cocoindex.resources import schema as coco_schema`, `import numpy as np`, `from numpy.typing import NDArray`, `import asyncpg, hashlib, uuid`, `from typing import Annotated, Any`, `from pathlib import Path`, `from dataclasses import dataclass`, `from app.services.embedder import EMBEDDING_DIM, embed_text`, `from app.services.file_extract import extract_text`, `from app.services.vn_chunker import chunk_vietnamese`.

  **ContextKey:** `PG_POOL_KEY = coco.ContextKey[asyncpg.Pool]("medinet/pg_pool")`.

  **stable_chunk_id:** `CHUNK_ID_NAMESPACE = uuid.UUID("8f1a3c6e-0000-4000-a000-0000007a3d1c")`; `def stable_chunk_id(document_id: uuid.UUID, chunk_index: int) -> uuid.UUID: return uuid.uuid5(CHUNK_ID_NAMESPACE, f"{document_id}:{chunk_index}")`.

  **VectorSchema provider:** `_VECTOR_SCHEMA = coco_schema.VectorSchema(dtype=np.dtype(np.float32), size=EMBEDDING_DIM)` — module-level frozen msgspec.Struct, an toàn reuse. Đây là VectorSchemaProvider cho ChunkRow.vector. KHÔNG dùng `@coco.fn` callable làm provider.

  **ChunkRow @dataclass** — 10 fields match Migration 0001 chunks (KHÔNG khai `created_at` — server-side DEFAULT NOW()):
  ```python
  @dataclass
  class ChunkRow:
      id: uuid.UUID
      document_id: uuid.UUID
      hub_id: uuid.UUID
      content: str
      content_hash: bytes
      heading_path: str | None
      page_start: Annotated[int, pg.PgType("integer")] | None
      page_end: Annotated[int, pg.PgType("integer")] | None
      vector: Annotated[NDArray[np.float32], _VECTOR_SCHEMA]
      metadata: dict[str, Any]
  ```

  **`@coco.fn async def _embed_one(content: str) -> NDArray[np.float32]`** — gọi `await embed_text(content)`; guard `len != EMBEDDING_DIM → ValueError`; trả `np.asarray(vec, dtype=np.float32)`.

  **`@coco.fn async def index_document(doc_row: dict[str, Any], table: pg.TableTarget[ChunkRow]) -> None`** — per-document processor:
  1. `doc_id = uuid.UUID(str(doc_row["id"]))`, `hub_id = uuid.UUID(str(doc_row["hub_id"]))`, `file_path_str = doc_row["file_path"]`.
  2. `text, is_scanned, _meta = extract_text(Path(file_path_str))`; `if is_scanned: return` (defensive — router đã loại trước).
  3. `chunks = chunk_vietnamese(text)`; `if not chunks: return`.
  4. Per chunk (enumerate): `embedding = await _embed_one(chunk_draft.content)`; `content_hash = hashlib.sha256(chunk_draft.content.encode("utf-8")).digest()`; build `ChunkRow(id=stable_chunk_id(doc_id, idx), document_id=doc_id, hub_id=hub_id, content=..., content_hash=..., heading_path=..., page_start=..., page_end=..., vector=embedding, metadata={...})`; `table.declare_row(row=row)`.
  5. Thêm `logger.info("index_document_chunks: doc_id=%s chunks=%d", doc_id, len(chunks))` — debug log cần thiết để Plan 04-06 chẩn đoán gap zero-chunks.

  **`@coco.fn async def medinet_wiki_main() -> None`** — orchestrator:
  1. `pool = coco.use_context(PG_POOL_KEY)`.
  2. `chunks_schema = await pg.TableSchema.from_class(ChunkRow, primary_key=["id"])` — **PHẢI await** (async classmethod — verified inspect).
  3. `chunks_table = await pg.mount_table_target(PG_POOL_KEY, table_name="chunks", table_schema=chunks_schema, pg_schema_name="public", managed_by=ck_target.ManagedBy.USER)`.
  4. **Decision B1:** KHÔNG gọi method declare vector index — Alembic owns `ix_chunks_vector_hnsw`. Comment giải thích tiếng Việt, KHÔNG dùng literal `declare_vector_index` (acceptance grep =0).
  5. `pg_source = pg.PgTableSource(pool, table_name="documents", columns=["id", "hub_id", "file_path", "status"])`.
  6. `await coco.mount_each(index_document, pg_source.fetch_rows().items(key=lambda r: str(r["id"])), chunks_table)`.

  **App instance** (single-line — acceptance grep cần exact substring): `cocoindex_app = coco.App(coco.AppConfig(name="medinet_wiki_ingest"), medinet_wiki_main)`.

  **`__all__`** export: ChunkRow, PG_POOL_KEY, cocoindex_app, index_document, medinet_wiki_main, stable_chunk_id, EMBEDDING_DIM.

  **Docstring** tiếng Việt mô tả kiến trúc — KHÔNG chứa literal forbidden (`@cocoindex.flow_def`, `cocoindex.init`, `FlowLiveUpdater`, `cocoindex.sources.Postgres`, `cocoindex.targets.Postgres`). Nếu cần đề cập API cũ, viết "API 0.x lỗi thời" KHÔNG kèm symbol literal.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run python -c "import asyncio,uuid; from cocoindex.connectors import postgres as pg; from app.rag.flow import ChunkRow, cocoindex_app, stable_chunk_id; asyncio.run(pg.TableSchema.from_class(ChunkRow, primary_key=['id'])); d=uuid.uuid4(); assert stable_chunk_id(d,0)==stable_chunk_id(d,0); print('flow OK')" && uv run ruff check app/rag/flow.py && uv run mypy --strict app/rag/flow.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'coco.App(coco.AppConfig(name="medinet_wiki_ingest"' Hub_All/api/app/rag/flow.py` >= 1
    - `grep -c '@coco.fn' Hub_All/api/app/rag/flow.py` >= 3
    - `grep -c 'mount_table_target' Hub_All/api/app/rag/flow.py` >= 1
    - `grep -c 'PgTableSource' Hub_All/api/app/rag/flow.py` >= 1
    - `grep -c 'mount_each' Hub_All/api/app/rag/flow.py` >= 1
    - `grep -c 'declare_row' Hub_All/api/app/rag/flow.py` >= 1
    - `grep -c 'ManagedBy.USER' Hub_All/api/app/rag/flow.py` >= 1
    - `grep -c 'VectorSchema' Hub_All/api/app/rag/flow.py` >= 2
    - `grep -c 'stable_chunk_id' Hub_All/api/app/rag/flow.py` >= 2
    - `grep -c '@cocoindex.flow_def' Hub_All/api/app/rag/flow.py` == 0
    - `grep -c 'cocoindex.sources.Postgres' Hub_All/api/app/rag/flow.py` == 0
    - `grep -c 'FlowLiveUpdater' Hub_All/api/app/rag/flow.py` == 0
    - lệnh python in "flow OK" (TableSchema.from_class KHÔNG raise — vector(1536) build PASS); ruff + mypy --strict CLEAN
  </acceptance_criteria>
  <done>flow.py ship; coco.App registered; ChunkRow schema build PASS với vector(1536); 0 reference API 0.x.</done>
</task>

<task type="auto">
  <name>Task 2: setup.py — cocoindex 1.0.3 lifespan + LMDB + initial backfill</name>
  <read_first>
    - Hub_All/api/app/rag/flow.py (Task 1 — PG_POOL_KEY + cocoindex_app)
    - Hub_All/api/.venv/Lib/site-packages/cocoindex/_internal/environment.py (xác nhận @coco.lifespan + start_blocking + stop_blocking)
    - Hub_All/api/app/config.py (Settings.cocoindex_lmdb_path + database_url)
  </read_first>
  <action>
  Tạo `Hub_All/api/app/rag/setup.py`.

  - `def _to_asyncpg_dsn(sqlalchemy_dsn: str) -> str` — `return sqlalchemy_dsn.replace("postgresql+asyncpg://", "postgresql://", 1)`.
  - `def setup_cocoindex(settings: Settings) -> None` — đồng bộ (sync), caller wrap qua `asyncio.to_thread`:
    1. `os.environ.setdefault("COCOINDEX_DB", str(settings.cocoindex_lmdb_path))`.
    2. `from app.rag.flow import PG_POOL_KEY`; `asyncpg_dsn = _to_asyncpg_dsn(settings.database_url)`.
    3. Đăng ký `@coco.lifespan async def _lifespan(env_builder)` — tạo `asyncpg.create_pool(dsn=asyncpg_dsn, min_size=2, max_size=10)`, `env_builder.provide(PG_POOL_KEY, pool)`, `try: yield finally: await pool.close()`.
    4. `from app.rag.flow import cocoindex_app`; log app name (getattr `name` hoặc `_name`).
    5. `coco.start_blocking()`.
    6. `cocoindex_app.update_blocking()` — apply chunks schema diff + initial backfill cho pending documents.
  - `def get_cocoindex_app() -> Any` — `from app.rag.flow import cocoindex_app; return cocoindex_app`.
  - `def stop_cocoindex() -> None` — `try: coco.stop_blocking() except Exception: logger.warning(...)`.
  - Docstring tiếng Việt mô tả sequence — KHÔNG chứa literal `cocoindex.init` / `setup_flow` / `FlowLiveUpdater` (viết "API 0.x lỗi thời").
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run python -c "from app.rag.setup import setup_cocoindex, get_cocoindex_app, stop_cocoindex; print('setup helpers OK')" && uv run ruff check app/rag/setup.py && uv run mypy --strict app/rag/setup.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def setup_cocoindex' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c 'def get_cocoindex_app' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c 'def stop_cocoindex' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c '@coco.lifespan' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c 'start_blocking' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c 'update_blocking' Hub_All/api/app/rag/setup.py` >= 1
    - `grep -c 'cocoindex.init' Hub_All/api/app/rag/setup.py` == 0
    - `grep -c 'setup_flow' Hub_All/api/app/rag/setup.py` == 0
    - 3 helper import được (in "setup helpers OK"); ruff + mypy --strict CLEAN
  </acceptance_criteria>
  <done>setup.py ship 3 helper cocoindex 1.0.3; LMDB path từ env; 0 reference API 0.x.</done>
</task>

<task type="auto">
  <name>Task 3: main.py lifespan fail-fast + test_rag_flow.py</name>
  <read_first>
    - Hub_All/api/app/main.py (xác nhận lifespan hiện tại — step JWT/Redis/DB pool; middleware order P11; KHÔNG đụng phần khác)
    - Hub_All/api/app/rag/setup.py (Task 2)
    - Hub_All/api/tests/unit/test_rag_flow.py (file đã tồn tại — REWRITE đúng spec)
  </read_first>
  <action>
  **app/main.py — lifespan EXTEND (KHÔNG đụng middleware order, healthz/readyz, các step khác):**
  - Trong lifespan startup, thêm step cocoindex setup: `from app.rag.setup import setup_cocoindex, get_cocoindex_app`; `await asyncio.to_thread(setup_cocoindex, settings)`; `app.state.cocoindex_app = get_cocoindex_app()`.
  - **FAIL-FAST:** wrap try/except — nếu setup raise → `logger.error("cocoindex_setup_failed", exc_info=True)` rồi `raise` (KHÔNG nuốt lỗi, KHÔNG set cocoindex_app=None silent). Lý do: lần execute trước fail-soft mask blocker VectorSchemaProvider khiến SC2 fail âm thầm. Comment giải thích: "fail-fast — KHÔNG mask blocker; uvicorn crash startup nếu cocoindex setup lỗi".
  - Shutdown: `from app.rag.setup import stop_cocoindex`; `await asyncio.to_thread(stop_cocoindex)` trong try/except.
  - `readyz` nếu hiện check cocoindex thì giữ; nếu chưa, có thể thêm `cocoindex_ready = getattr(app.state, "cocoindex_app", None) is not None`.

  **tests/unit/test_rag_flow.py — REWRITE (pure Python, KHÔNG cần Postgres/cocoindex runtime):**
  - `test_cocoindex_app_registered` — `from app.rag.flow import cocoindex_app` KHÔNG raise; `assert cocoindex_app is not None`.
  - `test_cocoindex_app_name_snake_case` — app name == "medinet_wiki_ingest" (getattr `name` hoặc `_name`).
  - `test_chunk_row_dataclass_10_fields` — `dataclasses.fields(ChunkRow)` đủ 10 field tên đúng.
  - `test_chunk_row_vector_schema_build_no_raise` (`@pytest.mark.critical`) — `asyncio.run(pg.TableSchema.from_class(ChunkRow, primary_key=['id']))` KHÔNG raise; verify vector column type chứa "1536".
  - `test_stable_chunk_id_deterministic` — cùng (doc_id, idx) → cùng UUID; khác idx → khác UUID.
  - `test_flow_imports_services` — flow.py import được extract_text/chunk_vietnamese/embed_text.
  - `test_flow_no_deprecated_0x_api` — grep source flow.py: 0 occurrence của `@cocoindex.flow_def`, `cocoindex.sources.Postgres`, `cocoindex.targets.Postgres`, `FlowLiveUpdater`, `VectorIndexDef`.
  - `test_setup_helpers_importable` — `from app.rag.setup import setup_cocoindex, get_cocoindex_app, stop_cocoindex`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_rag_flow.py -v && uv run python -c "from app.main import create_app; create_app(); print('app factory OK')" && uv run ruff check app/main.py tests/unit/test_rag_flow.py && uv run mypy --strict app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setup_cocoindex' Hub_All/api/app/main.py` >= 1
    - `grep -c 'asyncio.to_thread' Hub_All/api/app/main.py` >= 1
    - `grep -c 'app.state.cocoindex_app' Hub_All/api/app/main.py` >= 1
    - `grep -c 'raise' Hub_All/api/app/main.py` >= 1 (fail-fast trong cocoindex setup block)
    - `grep -c 'def test_' Hub_All/api/tests/unit/test_rag_flow.py` >= 7
    - `pytest tests/unit/test_rag_flow.py` tất cả PASS gồm test_chunk_row_vector_schema_build_no_raise
    - `create_app()` KHÔNG raise; ruff + mypy --strict CLEAN
  </acceptance_criteria>
  <done>main.py lifespan fail-fast cocoindex setup + app.state.cocoindex_app; test_rag_flow.py PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documents row (file_path) → cocoindex flow | Cocoindex đọc file_path từ documents do router ghi; file trên local FS |
| cocoindex → asyncpg.Pool (full RW credentials) | Flow ghi chunks bằng DATABASE_URL credentials |
| cocoindex LMDB state | Internal state (memo cache, fingerprint) trên local FS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | D (DoS) | Flow stuck do LiteLLM rate limit → documents kẹt 'processing' forever | mitigate | Plan 04-05 watchdog 5 phút timeout NULL-guard flip 'processing' → 'failed'; Plan 04-04 trigger_cocoindex_update post-update count chunks → set status |
| T-04-03-02 | E (Elevation) | Cocoindex chạy với credentials full RW DATABASE_URL → có thể ghi rogue chunks | accept | Cocoindex 1.0.3 trusted layer; credentials .env gitignored; operator-only setup; Phase 10 DEPLOY.md document Postgres role least-privilege |
| T-04-03-03 | I (Info disclosure) | LMDB state (.cocoindex/) chứa file_path + memo cache → backup leak | accept | Local FS, .gitignored, operator-only; Phase 10 HARD-04 backup script document encrypt-at-rest |
| T-04-03-04 | R (Repudiation) | Lifespan shutdown KHÔNG stop cocoindex → asyncpg.Pool leak connection sau container stop | mitigate | stop_cocoindex (coco.stop_blocking) ở shutdown finally; try/except tolerate API drift |
| T-04-03-05 | D (DoS) | Flow INSERT chunks với hub_id NULL → constraint violation crash flow | mitigate | ChunkRow khai `hub_id: uuid.UUID` NOT NULL; index_document wire hub_id từ doc_row; unit test verify 10 field |
| T-04-03-06 | T (Tampering) | LMDB state corrupt giữa restart → memo cache lỗi → re-embed toàn corpus | accept | M2 0-100 docs re-embed cost chấp nhận được; Phase 10 HARD-04 LMDB backup/recovery script |
</threat_model>

<verification>
- `pg.TableSchema.from_class(ChunkRow, primary_key=['id'])` KHÔNG raise — vector(1536) schema build PASS (đây là blocker SC2 lần trước, plan này fix bằng VectorSchema provider).
- `cocoindex_app` registered name "medinet_wiki_ingest".
- `create_app()` KHÔNG raise; lifespan fail-fast khi cocoindex setup lỗi.
- `pytest tests/unit/test_rag_flow.py` PASS; `pytest tests/` full suite KHÔNG regression.
- ruff + mypy --strict CLEAN trên flow.py, setup.py, main.py.
- grep 0 reference deprecated 0.x API.
</verification>

<success_criteria>
- INGEST-01: cocoindex_app coco.App đăng ký name medinet_wiki_ingest (cocoindex 1.0.3 actual API).
- INGEST-02: index_document transform extract → chunk VN → embed dim 1536.
- INGEST-03: mount_table_target chunks USER-managed + ChunkRow content_hash + stable_chunk_id.
- ChunkRow schema build PASS (VectorSchemaProvider — fix blocker SC2 lần trước).
- Lifespan fail-fast + app.state.cocoindex_app cho A4.
- test_rag_flow.py PASS.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-03-SUMMARY.md` theo template summary.md.
</output>
