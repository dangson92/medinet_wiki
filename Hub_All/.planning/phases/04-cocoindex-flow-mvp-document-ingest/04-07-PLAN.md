---
phase: 04-cocoindex-flow-mvp-document-ingest
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - Hub_All/api/app/rag/flow.py
  - Hub_All/api/app/rag/__init__.py
  - Hub_All/api/app/main.py
  - Hub_All/api/app/services/documents_service.py
  - Hub_All/api/tests/integration/test_ingest_e2e.py
  - Hub_All/api/tests/unit/test_rag_flow.py
autonomous: true
gap_closure: true
requirements: [INGEST-01, INGEST-03]
tags: [phase-04, gap-closure, cocoindex-1.0.3, vectorschemaprovider, ingest]

must_haves:
  truths:
    - "SC2 cocoindex flow ship rows vào pgvector → documents.status pending→processing→completed trong <5s (KHÔNG còn STUCK pending vì VectorSchemaProvider blocker)"
    - "SC5 cocoindex memo content-hash incremental: upload 2 lần → chunks tuyến tính KHÔNG bloated >2x baseline"
    - "Lifespan setup_cocoindex fail-FAST: nếu schema build raise → uvicorn crash startup (KHÔNG silent fail-soft mask blocker)"
    - "Test E2E happy + dedup PASS thay SKIP — CI gate enforce architectural blocker recurrence"
    - "Docstring rag/__init__.py KHÔNG còn reference @cocoindex.flow_def (deprecated 0.x API)"
  artifacts:
    - path: "Hub_All/api/app/rag/flow.py"
      provides: "ChunkRow.vector annotation dùng VectorSchema(dtype=np.dtype(np.float32), size=1536) — implements VectorSchemaProvider Protocol"
      contains: "VectorSchema"
    - path: "Hub_All/api/app/main.py"
      provides: "Lifespan setup_cocoindex raise hard (KHÔNG fail-soft mask)"
      pattern: "raise"
    - path: "Hub_All/api/app/services/documents_service.py"
      provides: "trigger_cocoindex_update KHÔNG còn defensive cocoindex_app=None silent skip"
    - path: "Hub_All/api/tests/integration/test_ingest_e2e.py"
      provides: "Convert pytest.skip → assert cocoindex_app is not None"
      contains: "assert"
    - path: "Hub_All/api/app/rag/__init__.py"
      provides: "Docstring cocoindex 1.0.3 coco.App + main_fn (KHÔNG @cocoindex.flow_def)"
    - path: "Hub_All/api/tests/unit/test_rag_flow.py"
      provides: "Regression test schema build ChunkRow.vector — KHÔNG raise VectorSchemaProvider"
  key_links:
    - from: "app/rag/flow.py ChunkRow.vector"
      to: "cocoindex.resources.schema.VectorSchema(dtype=np.dtype(np.float32), size=1536)"
      via: "Annotated[NDArray[np.float32], VectorSchema(...)]"
      pattern: "Annotated\\[NDArray\\[np\\.float32\\], _VECTOR_SCHEMA\\]"
    - from: "app/main.py lifespan step 3"
      to: "setup_cocoindex"
      via: "raise hard on schema build failure (KHÔNG except + warning)"
      pattern: "raise"
    - from: "tests/integration/test_ingest_e2e.py"
      to: "app.state.cocoindex_app"
      via: "assert cocoindex_app is not None (replace pytest.skip)"
      pattern: "assert.*cocoindex_app.*is not None"
---

<objective>
**Gap closure Plan 04-07** — Sửa VectorSchemaProvider architectural blocker chặn SC2 + SC5 (Phase 4 ROADMAP M2a EXIT GATE) + remove fail-soft mask + convert pytest.skip → assert (CI gate enforce) + cleanup docstring stale.

**Purpose:** 04-VERIFICATION.md flag 2/5 success criteria FAILED + 2/8 INGEST PARTIAL cùng root cause: `app/rag/flow.py:132` `vector: Annotated[NDArray[np.float32], EMBEDDER]` — EMBEDDER là `@coco.fn _embed_one` KHÔNG implement `VectorSchemaProvider` Protocol mà cocoindex 1.0.3 yêu cầu cho NumPy ndarray field annotation trong `TableSchema.from_class`. Hậu quả cascade:

1. `pg.TableSchema.from_class(ChunkRow, primary_key=['id'])` raise `ValueError: VectorSpecProvider is required for NumPy ndarray type` (verified runtime probe).
2. `lifespan.setup_cocoindex` fail-soft (`app/main.py:113-114` `except Exception: logger.warning(...)`) → `app.state.cocoindex_app = None`.
3. Router `BackgroundTask trigger_cocoindex_update` detects `cocoindex_app=None` (`app/services/documents_service.py:397-399`) → silent skip + set status='failed' generic message.
4. test_e2e_upload_docx_to_chunks_completed + test_e2e_content_hash_incremental_dedup SKIP (`pytest.skip(...)` `tests/integration/test_ingest_e2e.py:192-193, 321-322`) — masking blocker trong CI.

**Output:** SC2 + SC5 PASS → 5/5 SC PASS → INGEST 8/8 SATISFIED → Phase 4 ready to mark complete. M2a EXIT GATE manual demo có thể PASS AC2 + AC5 với cocoindex thật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-VERIFICATION.md
@Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-COCOINDEX-API-RESEARCH.md

<!-- Existing implementation files this plan modifies — executor đọc trực tiếp -->
@Hub_All/api/app/rag/flow.py
@Hub_All/api/app/rag/__init__.py
@Hub_All/api/app/rag/setup.py
@Hub_All/api/app/main.py
@Hub_All/api/app/services/documents_service.py
@Hub_All/api/tests/integration/test_ingest_e2e.py
@Hub_All/api/tests/unit/test_rag_flow.py

<interfaces>
<!-- Cocoindex 1.0.3 VectorSchemaProvider Protocol — verified canonical pattern -->
<!-- Source: .venv/Lib/site-packages/cocoindex/resources/schema.py + ops/litellm.py -->

`cocoindex.resources.schema.VectorSchemaProvider` (Protocol):
```python
@typing.runtime_checkable
class VectorSchemaProvider(typing.Protocol):
    """Additional information for a vector column."""

    def __coco_vector_schema__(self) -> typing.Awaitable[VectorSchema]: ...
```

`cocoindex.resources.schema.VectorSchema` (concrete struct — itself a VectorSchemaProvider):
```python
class VectorSchema(msgspec.Struct, frozen=True, tag=True):
    dtype: np.dtype
    size: int

    async def __coco_vector_schema__(self) -> VectorSchema:
        return self
```

**Verified canonical fix** (runtime probe `isinstance(VectorSchema(dtype=np.dtype(np.float32), size=1536), VectorSchemaProvider) == True`):

```python
from cocoindex.resources import schema as _coco_schema

# Module-level constant — frozen msgspec.Struct, safe to reuse.
_VECTOR_SCHEMA = _coco_schema.VectorSchema(
    dtype=np.dtype(np.float32),
    size=1536,  # R1 pin pgvector 2000-dim limit; EMBEDDING_DIM Plan 04-02 constant.
)

@dataclass
class ChunkRow:
    # ... other fields ...
    vector: Annotated[NDArray[np.float32], _VECTOR_SCHEMA]  # ← VectorSchemaProvider Protocol satisfied
    # ... other fields ...
```

**KHÔNG dùng EMBEDDER (@coco.fn) làm provider** — `@coco.fn` decorator wraps callable, KHÔNG implement `__coco_vector_schema__` → fail Protocol check tại `TableSchema._columns_from_record_type` line 342-349 (`await res_schema.get_vector_schema(annot)` returns `None` → `_get_type_mapping` raise `ValueError`).

**Postgres connector error message dùng cụm "VectorSpecProvider"** (legacy naming) NHƯNG actual Protocol class là `VectorSchemaProvider`. Cùng concept.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix VectorSchemaProvider annotation (Gap #1 BLOCKER — SC2/SC5 root cause)</name>
  <files>Hub_All/api/app/rag/flow.py, Hub_All/api/tests/unit/test_rag_flow.py</files>
  <action>
**Mục đích:** Sửa `ChunkRow.vector` annotation để satisfy `cocoindex.resources.schema.VectorSchemaProvider` Protocol → `pg.TableSchema.from_class(ChunkRow, primary_key=['id'])` KHÔNG raise → flow schema build success → `setup_cocoindex` lifespan complete → `app.state.cocoindex_app` real (KHÔNG None) → BackgroundTask `trigger_cocoindex_update` chạy → status pending→processing→completed.

**File 1: `Hub_All/api/app/rag/flow.py`** — Apply 3 sửa đổi atomic:

**Sửa đổi A (line 1-44 docstring):** Cập nhật docstring section "Defensive carry-over từ revision 1:" thêm dòng:
```
- BLOCKER #1 (Plan 04-07 gap closure): ChunkRow.vector dùng VectorSchema(dtype=np.dtype(np.float32),
  size=1536) làm provider — implements cocoindex.resources.schema.VectorSchemaProvider Protocol.
  KHÔNG dùng EMBEDDER (@coco.fn _embed_one) — @coco.fn wraps callable, KHÔNG có
  __coco_vector_schema__ method → TableSchema.from_class raise ValueError.
```

**Sửa đổi B (line 54-66 imports):** Thêm import `cocoindex.resources.schema` SAU dòng `from cocoindex.connectors import postgres as pg`:
```python
from cocoindex.connectors import postgres as pg
from cocoindex.resources import schema as _coco_schema  # ← THÊM (Plan 04-07 fix)
```

**Sửa đổi C (line ~100-103 EMBEDDER constant):** Thay 4 dòng (line 100-103) hiện tại:
```python
# Stub VectorSchemaProvider cho ChunkRow.vector annotation — _embed_one làm provider.
# Cocoindex 1.0.3 dùng Annotated[NDArray, provider] để auto-resolve vector(N) Postgres type.
EMBEDDER = _embed_one
```
THÀNH:
```python
# === VectorSchemaProvider cho ChunkRow.vector annotation (Plan 04-07 gap closure) ===
# Cocoindex 1.0.3 yêu cầu VectorSchemaProvider Protocol (cocoindex.resources.schema)
# cho NumPy ndarray field annotation. VectorSchema itself IS VectorSchemaProvider
# (verified isinstance check) — frozen msgspec.Struct safe để reuse module-level.
# KHÔNG dùng @coco.fn _embed_one làm provider — @coco.fn wraps callable,
# KHÔNG có __coco_vector_schema__ method.
_VECTOR_SCHEMA = _coco_schema.VectorSchema(
    dtype=np.dtype(np.float32),
    size=EMBEDDING_DIM,  # 1536 — R1 mitigation pgvector 2000-dim limit
)
```

**Sửa đổi D (line 132 ChunkRow.vector annotation):** Thay dòng:
```python
    vector: Annotated[NDArray[np.float32], EMBEDDER]
```
THÀNH:
```python
    vector: Annotated[NDArray[np.float32], _VECTOR_SCHEMA]
```

**Sửa đổi E (line ~248-258 `__all__`):** Loại `"EMBEDDER"` khỏi `__all__` (KHÔNG còn dùng public). Thêm `"_VECTOR_SCHEMA"` nếu cần expose cho test (KHÔNG bắt buộc, prefix underscore). `__all__` final:
```python
__all__ = [
    "CHUNK_ID_NAMESPACE",
    "ChunkRow",
    "EMBEDDING_DIM",
    "PG_POOL_KEY",
    "cocoindex_app",
    "index_document",
    "medinet_wiki_main",
    "stable_chunk_id",
]
```

**File 2: `Hub_All/api/tests/unit/test_rag_flow.py`** — Thêm 1 regression test CUỐI file (sau test cuối cùng hiện có) verify schema build KHÔNG raise:

```python
import asyncio as _asyncio


def test_chunk_row_vector_schema_build_no_raise() -> None:
    """Plan 04-07 gap closure regression — ChunkRow.vector phải satisfy VectorSchemaProvider.

    BEFORE fix: `await pg.TableSchema.from_class(ChunkRow, primary_key=['id'])` raise
    `ValueError: VectorSpecProvider is required for NumPy ndarray type` vì
    `Annotated[NDArray, EMBEDDER]` — EMBEDDER là @coco.fn callable KHÔNG implement
    VectorSchemaProvider Protocol.

    AFTER fix: dùng cocoindex.resources.schema.VectorSchema(dtype=np.dtype(np.float32),
    size=1536) làm provider — itself implements VectorSchemaProvider (frozen msgspec.Struct
    với __coco_vector_schema__ method). Schema build PASS exit 0.

    KHÔNG cần Postgres pool — chỉ test schema build (sync compute từ annotation).
    """
    from cocoindex.connectors import postgres as pg
    from cocoindex.resources import schema as _coco_schema

    from app.rag.flow import ChunkRow

    # Build schema từ dataclass — async classmethod, run trong sync test qua asyncio.run.
    schema = _asyncio.run(pg.TableSchema.from_class(ChunkRow, primary_key=["id"]))

    # Verify vector column resolved thành pgvector type "vector(1536)".
    assert "vector" in schema.columns, "ChunkRow.vector phải thành column"
    vector_col = schema.columns["vector"]
    assert vector_col.type == "vector(1536)", (
        f"Vector dim mismatch: {vector_col.type} (expect 'vector(1536)' R1 pin)"
    )


def test_chunk_row_vector_uses_vector_schema_provider() -> None:
    """Plan 04-07 gap closure — VectorSchema constant phải implement VectorSchemaProvider."""
    import numpy as np
    from cocoindex.resources import schema as _coco_schema

    from app.rag import flow as flow_module

    # _VECTOR_SCHEMA module-level constant — must exist sau Plan 04-07.
    assert hasattr(flow_module, "_VECTOR_SCHEMA"), (
        "Plan 04-07 phải define _VECTOR_SCHEMA module-level constant"
    )
    vs = flow_module._VECTOR_SCHEMA
    assert isinstance(vs, _coco_schema.VectorSchema), (
        f"_VECTOR_SCHEMA phải là VectorSchema instance, got {type(vs).__name__}"
    )
    assert isinstance(vs, _coco_schema.VectorSchemaProvider), (
        "_VECTOR_SCHEMA phải satisfy VectorSchemaProvider Protocol (runtime_checkable)"
    )
    assert vs.size == 1536, f"Plan 04-02 R1 pin dim=1536, got {vs.size}"
    assert vs.dtype == np.dtype(np.float32), (
        f"R1 pin dtype float32, got {vs.dtype}"
    )


def test_flow_no_embedder_constant_for_vector_annotation() -> None:
    """Plan 04-07 gap closure — KHÔNG còn `EMBEDDER` annotation dùng cho vector field.

    BEFORE: `vector: Annotated[NDArray[np.float32], EMBEDDER]` (WRONG — EMBEDDER là
    @coco.fn callable).
    AFTER: `vector: Annotated[NDArray[np.float32], _VECTOR_SCHEMA]` (CORRECT —
    VectorSchema implements VectorSchemaProvider Protocol).
    """
    import app.rag.flow as flow_module

    with open(flow_module.__file__, encoding="utf-8") as f:
        source = f.read()
    assert "Annotated[NDArray[np.float32], EMBEDDER]" not in source, (
        "Plan 04-07 violated — flow.py vẫn dùng EMBEDDER (@coco.fn) làm vector provider"
    )
    assert "Annotated[NDArray[np.float32], _VECTOR_SCHEMA]" in source, (
        "Plan 04-07 phải dùng _VECTOR_SCHEMA (VectorSchema) làm vector provider"
    )
```

**Implementation notes:**
- VectorSchema là `frozen=True` msgspec.Struct — module-level constant safe (KHÔNG mutable state).
- `np.dtype(np.float32)` chuẩn — KHÔNG dùng `np.float32` raw (cocoindex check `vector_schema.dtype` so sánh với `np.half/np.float16` tại line 219 `_target.py`).
- Vẫn giữ `_embed_one` @coco.fn (per-chunk embed call vẫn dùng trong `index_document`). CHỈ tách provider vs embedder — provider là schema metadata (VectorSchema), embedder là runtime call (@coco.fn).
- EMBEDDER bị remove khỏi `__all__` NHƯNG có thể giữ trong module nếu code khác reference. Verify grep no other import.
  </action>
  <verify>
    <automated>cd Hub_All/api &amp;&amp; uv run python -c "from app.rag.flow import cocoindex_app, ChunkRow, _VECTOR_SCHEMA; from cocoindex.connectors import postgres as pg; import asyncio; s = asyncio.run(pg.TableSchema.from_class(ChunkRow, primary_key=['id'])); print('schema_ok:', s.columns['vector'].type)" &amp;&amp; cd Hub_All/api &amp;&amp; uv run pytest tests/unit/test_rag_flow.py -x -q</automated>
  </verify>
  <done>
- `app/rag/flow.py` line 132 chuyển từ `Annotated[NDArray[np.float32], EMBEDDER]` → `Annotated[NDArray[np.float32], _VECTOR_SCHEMA]`.
- `_VECTOR_SCHEMA = _coco_schema.VectorSchema(dtype=np.dtype(np.float32), size=EMBEDDING_DIM)` module-level constant tồn tại.
- Import `from cocoindex.resources import schema as _coco_schema` thêm vào flow.py.
- `await pg.TableSchema.from_class(ChunkRow, primary_key=['id'])` exit 0 + return schema với `columns['vector'].type == 'vector(1536)'`.
- 3 regression test mới trong test_rag_flow.py PASS (`test_chunk_row_vector_schema_build_no_raise`, `test_chunk_row_vector_uses_vector_schema_provider`, `test_flow_no_embedder_constant_for_vector_annotation`).
- Toàn bộ test_rag_flow.py suite (12 test cũ + 3 mới = 15) PASS exit 0.
- Grep `Annotated\[NDArray\[np\.float32\], EMBEDDER\]` trong flow.py = 0 match.
- Grep `_VECTOR_SCHEMA\|VectorSchema(` trong flow.py ≥ 2 match.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove fail-soft masks — lifespan + service raise hard (Gap #2 WARNING)</name>
  <files>Hub_All/api/app/main.py, Hub_All/api/app/services/documents_service.py</files>
  <action>
**Mục đích:** Sau Task 1 fix VectorSchemaProvider, lifespan setup_cocoindex sẽ thành công bình thường. NHƯNG hiện tại main.py line 113-114 fail-soft pattern (`except Exception as e: logger.warning(...)` + `cocoindex_ready=False`) sẽ MASK bất kỳ regression nào trong tương lai (e.g., Postgres không lên, schema sai). + documents_service.py line 396-416 defensive `cocoindex_app=None` skip silently set status='failed' generic message.

**Strategy:** Lifespan FAIL-FAST cho cocoindex setup. Nếu schema build / pool init / start_blocking fail → uvicorn crash + operator phải fix. Defensive cocoindex_app=None branch trong service vẫn giữ NHƯNG log ERROR level (KHÔNG WARNING) + raise exception trong BackgroundTask để cocoindex_ready=False phản ánh đúng. Đơn giản + safe + KHÔNG mask blocker tương lai.

**File 1: `Hub_All/api/app/main.py`** — Sửa 1 block (line 93-114) lifespan step 3 từ fail-soft → fail-fast:

**Sửa đổi A (line 93-114):** Thay block hiện tại:
```python
    # 3) Cocoindex 1.0.3 init + initial backfill (Phase 4 Plan 04-03 REVISION 2 — INGEST-01..03).
    #    ... (existing comments) ...
    app.state.cocoindex_app = None
    try:
        import asyncio

        from app.rag.setup import get_cocoindex_app, setup_cocoindex

        await asyncio.to_thread(setup_cocoindex, settings)
        logger.info("cocoindex_setup_ok")
        app.state.cocoindex_app = get_cocoindex_app()
        app.state.cocoindex_ready = True
        logger.info("cocoindex_app_attached_to_app_state")
    except Exception as e:  # noqa: BLE001 — Phase 1 fail-soft pattern
        logger.warning("cocoindex_init_failed: %s", e)
```

THÀNH:
```python
    # 3) Cocoindex 1.0.3 init + initial backfill (Phase 4 Plan 04-03 REVISION 2 — INGEST-01..03).
    #    setup_cocoindex(settings) → register @coco.lifespan + coco.start_blocking
    #    + cocoindex_app.update_blocking() initial backfill cho mọi pending documents.
    #    KHÔNG còn live updater pattern cocoindex 0.x (Decision A4 user accepted:
    #    per-document trigger qua FastAPI BackgroundTasks ở Plan 04-04).
    #
    #    setup_cocoindex SYNC blocking — wrap asyncio.to_thread để KHÔNG block
    #    FastAPI lifespan event loop.
    #
    #    Plan 04-07 gap closure: FAIL-FAST pattern thay fail-soft. Nếu setup_cocoindex
    #    raise (VectorSchemaProvider sai, Postgres không lên, schema build fail) →
    #    lifespan re-raise → uvicorn crash startup → operator phải fix root cause.
    #    KHÔNG mask architectural blocker bằng silent warning + cocoindex_app=None.
    app.state.cocoindex_app = None
    import asyncio

    from app.rag.setup import get_cocoindex_app, setup_cocoindex

    try:
        await asyncio.to_thread(setup_cocoindex, settings)
    except Exception as exc:
        logger.error("cocoindex_init_failed_fail_fast: %s", exc, exc_info=True)
        raise  # ← Plan 04-07: fail-fast — KHÔNG mask blocker
    logger.info("cocoindex_setup_ok")
    app.state.cocoindex_app = get_cocoindex_app()
    app.state.cocoindex_ready = True
    logger.info("cocoindex_app_attached_to_app_state")
```

**File 2: `Hub_All/api/app/services/documents_service.py`** — Sửa block defensive `cocoindex_app=None` (line ~396-416). KHÔNG xóa hoàn toàn (BackgroundTask vẫn cần defensive cho test isolation), NHƯNG đổi WARNING → ERROR + clarify message + raise sau khi update status.

**Sửa đổi A (line ~396-416):** Thay block hiện tại:
```python
    if cocoindex_app is None:
        logger.warning(
            "trigger_cocoindex_update_skip: cocoindex_app=None (lifespan setup failed?) doc_id=%s",
            doc_id,
        )
        # Set status='failed' để watchdog/UI phản ánh đúng.
        try:
            engine = get_engine()
            async with engine.begin() as conn:
                await conn.execute(
                    text(
                        "UPDATE documents SET status='failed', "
                        "error_message='cocoindex_app unavailable (lifespan failed)', "
                        "last_heartbeat=NOW(), updated_at=NOW() "
                        "WHERE id = :id"
                    ),
                    {"id": str(doc_id)},
                )
        except Exception as inner:  # noqa: BLE001
            logger.exception("trigger_cocoindex_update_status_set_failed: %s", inner)
        return
```

THÀNH:
```python
    if cocoindex_app is None:
        # Plan 04-07 gap closure: ERROR level thay WARNING. Sau Task 2 lifespan
        # fail-fast, cocoindex_app=None CHỈ xảy ra trong test isolation (test KHÔNG
        # setup cocoindex full) HOẶC architectural regression. ERROR log để alerting
        # bắt được. KHÔNG silent skip — set status='failed' rõ ràng.
        logger.error(
            "trigger_cocoindex_update_no_app: cocoindex_app=None doc_id=%s "
            "(Plan 04-07: lifespan fail-fast nên branch này CHỈ xảy ra trong test/regression)",
            doc_id,
        )
        try:
            engine = get_engine()
            async with engine.begin() as conn:
                await conn.execute(
                    text(
                        "UPDATE documents SET status='failed', "
                        "error_message='cocoindex_app unavailable — lifespan setup did not "
                        "complete (Plan 04-07: should fail-fast, check uvicorn startup logs)', "
                        "last_heartbeat=NOW(), updated_at=NOW() "
                        "WHERE id = :id"
                    ),
                    {"id": str(doc_id)},
                )
        except Exception as inner:  # noqa: BLE001
            logger.exception("trigger_cocoindex_update_status_set_failed: %s", inner)
        return
```

**Implementation notes:**
- Lifespan fail-fast — re-raise exception trong main.py lifespan generator. Uvicorn lifespan protocol: exception trong lifespan startup → app fail to start (ASGI lifespan.startup.failed event) → uvicorn process exit code != 0. Operator thấy traceback rõ ràng.
- Service defensive branch giữ NHƯNG ERROR level + message clarify "should fail-fast". Lý do: test isolation (test KHÔNG mount full lifespan — e.g., test_documents_upload.py với MockCocoindexApp fixture) vẫn pass; production setup fail = uvicorn crash trước khi router nhận request.
- Watchdog vẫn flip stuck rows nếu nào đó status='processing' lọt qua (defense-in-depth).
- KHÔNG sửa shutdown block (line 199-210) — defensive cocoindex_app None check OK cho shutdown path.
  </action>
  <verify>
    <automated>cd Hub_All/api &amp;&amp; uv run python -c "import inspect; from app.main import lifespan; src = inspect.getsource(lifespan); assert 'raise  # ← Plan 04-07: fail-fast' in src, 'Plan 04-07 fail-fast pattern missing'; print('main.py fail-fast verified')" &amp;&amp; cd Hub_All/api &amp;&amp; uv run python -c "import inspect; from app.services.documents_service import trigger_cocoindex_update; src = inspect.getsource(trigger_cocoindex_update); assert 'logger.error' in src and 'cocoindex_app=None' in src, 'service ERROR level missing'; assert 'should fail-fast' in src, 'clarification message missing'; print('service.py ERROR level verified')"</automated>
  </verify>
  <done>
- `app/main.py` lifespan step 3 (line ~93-114) chứa `raise  # ← Plan 04-07: fail-fast` sau `except` block — fail-fast lifespan.
- Block `except Exception as e: logger.warning("cocoindex_init_failed: %s", e)` KHÔNG còn tồn tại trong main.py — thay bằng `logger.error(...)` + `raise`.
- `app/services/documents_service.py::trigger_cocoindex_update` line ~396-416 defensive branch chuyển từ `logger.warning(...)` → `logger.error(...)` với message reference Plan 04-07 fail-fast.
- UPDATE error_message string SQL trong defensive branch reference "Plan 04-07: should fail-fast, check uvicorn startup logs".
- Toàn bộ unit test hiện có (test_documents_upload, test_rag_flow, test_watchdog, ...) PASS — defensive branch vẫn work cho test isolation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Convert pytest.skip → assert + remove fallback (Gap #3 WARNING — CI gate)</name>
  <files>Hub_All/api/tests/integration/test_ingest_e2e.py</files>
  <action>
**Mục đích:** Sau Task 1 + Task 2, lifespan fail-fast = `app.state.cocoindex_app` LUÔN không None khi test reach `_wait_until`. `pytest.skip("cocoindex_app KHÔNG setup được")` (line 192-193, 321-322) hiện tại MASKS architectural blocker recurrence trong CI — test SKIP đếm như PASS trong báo cáo. Convert thành `assert` → CI FAIL loud nếu blocker tái xuất hiện.

**File: `Hub_All/api/tests/integration/test_ingest_e2e.py`** — 2 sửa đổi:

**Sửa đổi A (line 190-193 trong `test_e2e_upload_docx_to_chunks_completed`):** Thay block:
```python
    # Verify lifespan đã setup cocoindex_app real (Plan 04-03 REVISION 2)
    cocoindex_app = getattr(app_with_auth.state, "cocoindex_app", None)
    if cocoindex_app is None:
        pytest.skip("cocoindex_app KHÔNG setup được (Docker postgres/redis không lên?)")
```

THÀNH:
```python
    # Plan 04-07 gap closure: assert thay pytest.skip — CI gate enforce architectural
    # blocker recurrence. Task 2 lifespan fail-fast: nếu cocoindex_app=None ở đây →
    # lifespan đã raise → app_with_auth fixture KHÔNG resolve được (test FAIL ở fixture
    # setup) HOẶC test isolation broken. Cả 2 trường hợp đều phải FAIL loud, KHÔNG SKIP.
    cocoindex_app = getattr(app_with_auth.state, "cocoindex_app", None)
    assert cocoindex_app is not None, (
        "Plan 04-07 architectural regression — app.state.cocoindex_app=None sau lifespan. "
        "Expected: Task 2 fail-fast pattern (uvicorn crash startup nếu setup_cocoindex fail) → "
        "test KHÔNG bao giờ reach point này với cocoindex_app=None. "
        "Check: uvicorn startup logs cho 'cocoindex_init_failed_fail_fast' ERROR trace."
    )
```

**Sửa đổi B (line 320-322 trong `test_e2e_content_hash_incremental_dedup`):** Thay block:
```python
    cocoindex_app = getattr(app_with_auth.state, "cocoindex_app", None)
    if cocoindex_app is None:
        pytest.skip("cocoindex_app KHÔNG setup được")
```

THÀNH:
```python
    # Plan 04-07 gap closure: assert thay pytest.skip — CI gate enforce.
    cocoindex_app = getattr(app_with_auth.state, "cocoindex_app", None)
    assert cocoindex_app is not None, (
        "Plan 04-07 architectural regression — app.state.cocoindex_app=None sau lifespan. "
        "Expected: Task 2 fail-fast pattern. Check uvicorn startup logs."
    )
```

**Implementation notes:**
- KHÔNG xóa `import pytest` (file vẫn dùng `pytest.fail` line 154 trong `_wait_until` + decorator markers).
- KHÔNG sửa test 2 (`test_e2e_pdf_scanned_failed_unsupported`) — test này KHÔNG có pytest.skip pattern (verified line 234-302).
- `_force_cocoindex_update` helper (line 159-168) — giữ nguyên, có guard `if cocoindex_app is not None` an toàn. KHÔNG cần sửa.
- 2 sửa đổi atomic — KHÔNG sửa logic _wait_until / _upload_docx / _create_hub / fixtures.
- Sau Task 1 (VectorSchemaProvider fix) + Task 2 (fail-fast lifespan), 2 test này expected PASS với Docker testcontainers postgres+redis. Nếu Docker không có sẵn trong CI runner, test sẽ FAIL ở fixture `app_with_auth` setup (NOT ở assert), với message rõ ràng.
  </action>
  <verify>
    <automated>cd Hub_All/api &amp;&amp; uv run python -c "
import re
with open('tests/integration/test_ingest_e2e.py', encoding='utf-8') as f:
    src = f.read()
# Plan 04-07: pytest.skip cocoindex_app patterns KHÔNG còn.
skip_count = len(re.findall(r'pytest\.skip\([\"\\'].*cocoindex_app', src))
assert skip_count == 0, f'pytest.skip cocoindex_app patterns vẫn còn: {skip_count}'
# Assert pattern thay vào ≥ 2.
assert_count = len(re.findall(r'assert cocoindex_app is not None', src))
assert assert_count >= 2, f'assert cocoindex_app is not None count = {assert_count} (expect ≥ 2)'
# Reference Plan 04-07 trong message ≥ 2.
ref_count = len(re.findall(r'Plan 04-07 architectural regression', src))
assert ref_count >= 2, f'Plan 04-07 reference count = {ref_count}'
print('test_ingest_e2e.py conversion verified: skip=0, assert={}, ref={}'.format(assert_count, ref_count))
" &amp;&amp; cd Hub_All/api &amp;&amp; uv run pytest tests/integration/test_ingest_e2e.py --collect-only -q</automated>
  </verify>
  <done>
- `grep -c "pytest.skip" tests/integration/test_ingest_e2e.py` = 0 (cả 2 occurrence line 193 + 322 đã convert).
- `grep -c "assert cocoindex_app is not None" tests/integration/test_ingest_e2e.py` ≥ 2.
- `grep -c "Plan 04-07 architectural regression" tests/integration/test_ingest_e2e.py` ≥ 2.
- pytest collect-only exit 0 — file vẫn parse được + 3 test markers `critical`/`integration`/`asyncio` giữ nguyên.
- `import pytest` còn tồn tại (file vẫn dùng `pytest.fail` + markers).
  </done>
</task>

<task type="auto">
  <name>Task 4: Cleanup stale docstring (Gap #4 INFO) + verify full E2E suite</name>
  <files>Hub_All/api/app/rag/__init__.py</files>
  <action>
**Mục đích:** `app/rag/__init__.py:7` docstring reference `@cocoindex.flow_def` (deprecated cocoindex 0.x API) — documentation drift mismatched với cocoindex 1.0.3 actual API (`coco.App + main_fn`). KHÔNG functional impact (docstring only) NHƯNG mislead executor/reader.

**File: `Hub_All/api/app/rag/__init__.py`** — Thay nguyên docstring (line 1-9):

**Sửa đổi A (line 1-9):** Thay:
```python
"""RAG package — cocoindex flow + ingest pipeline (Phase 4).

Public API:
    from app.rag import setup_cocoindex

Plan 04-01 ships scaffolding (setup helper).
Plan 04-02 thêm flow.py — @cocoindex.flow_def medinet_wiki_ingest.
Plan 04-03 thêm services (file_extract, vn_chunker, embedder).
"""
```

THÀNH:
```python
"""RAG package — cocoindex 1.0.3 flow + ingest pipeline (Phase 4).

Public API:
    from app.rag import setup_cocoindex

Plan 04-01 ships scaffolding (setup helper + alembic migration 0002 watchdog index).
Plan 04-02 ships services (file_extract, vn_chunker, embedder, file_store).
Plan 04-03 ships flow.py — cocoindex 1.0.3 `coco.App(coco.AppConfig(name="medinet_wiki_ingest"), main_fn)`
    pattern với mount_table_target + PgTableSource + ChunkRow dataclass schema.
    KHÔNG dùng deprecated @cocoindex.flow_def (cocoindex 0.x API — không tồn tại trong 1.0.3).
Plan 04-07 gap closure: ChunkRow.vector dùng VectorSchema (cocoindex.resources.schema)
    làm VectorSchemaProvider — fix architectural blocker SC2/SC5 ROADMAP.
"""
```

**Verification full E2E suite:** Sau khi all 4 task complete, chạy pytest full Phase 4 suite để xác nhận no regression + 2 test SC2/SC5 chuyển từ SKIP → PASS (yêu cầu testcontainers postgres+redis lên). Lệnh paste-ready cho operator:

```bash
cd Hub_All/api
uv run pytest -m "critical or integration" -v --tb=short 2>&1 | tail -50
# Expected:
# - tests/integration/test_ingest_e2e.py::test_e2e_upload_docx_to_chunks_completed PASS (NOT SKIP)
# - tests/integration/test_ingest_e2e.py::test_e2e_content_hash_incremental_dedup PASS (NOT SKIP)
# - tests/integration/test_ingest_e2e.py::test_e2e_pdf_scanned_failed_unsupported PASS
# - 29/29 critical tests PASS (Phase 3 + Phase 4 happy paths)
```

**Nếu testcontainers env không có sẵn (Docker daemon down):**
- Test E2E sẽ fail ở fixture `app_with_auth` setup (postgres_container/redis_container fail to start) — NOT ở assert cocoindex_app. Message rõ ràng từ testcontainers library.
- Operator phải khởi động Docker daemon trước khi run E2E suite. Đây là constraint Docker environment, KHÔNG phải gap Plan 04-07.

**M2a EXIT GATE manual demo verification:** Sau gap closure complete, operator chạy `Hub_All/docs/m2a-exit-gate-demo.md` 5 bước paste-ready. Expected:
- AC1 (SC1) PASS — đã verified Phase 4 plan 04-04
- AC2 (SC2) PASS (KHÔNG còn STUCK pending) — Plan 04-07 Task 1 fix
- AC3 (SC3) PASS — đã verified Phase 4 plan 04-04 BLOCKER #3
- AC4 (SC4) requires manual stress test (kill worker 5 min) — đã verified DB level
- AC5 (SC5) PASS (memo dedup work) — Plan 04-07 Task 1 fix

→ User accept M2a → tiếp tục M2b (Phase 5+).

**Implementation notes:**
- Docstring cleanup atomic — KHÔNG sửa logic file (chỉ docstring). Re-export `setup_cocoindex` giữ nguyên.
- KHÔNG cần test mới cho docstring change.
- Acceptance verification grep `@cocoindex.flow_def` trong file = 0.
  </action>
  <verify>
    <automated>cd Hub_All/api &amp;&amp; uv run python -c "
with open('app/rag/__init__.py', encoding='utf-8') as f:
    src = f.read()
# Plan 04-07: deprecated @cocoindex.flow_def reference KHÔNG còn.
assert '@cocoindex.flow_def' not in src, 'rag/__init__.py vẫn còn @cocoindex.flow_def'
# Cocoindex 1.0.3 actual API mentioned.
assert 'coco.App' in src, 'docstring phải mention coco.App pattern'
assert 'main_fn' in src, 'docstring phải mention main_fn pattern'
assert 'Plan 04-07' in src, 'docstring phải reference Plan 04-07 gap closure'
print('rag/__init__.py docstring cleanup verified')
" &amp;&amp; cd Hub_All/api &amp;&amp; uv run python -c "from app.rag import setup_cocoindex; print('public API intact:', setup_cocoindex)"</automated>
  </verify>
  <done>
- `grep -c "@cocoindex.flow_def" app/rag/__init__.py` = 0.
- Docstring reference `coco.App` + `main_fn` + `cocoindex 1.0.3` + `Plan 04-07`.
- Public API `from app.rag import setup_cocoindex` vẫn import được (KHÔNG break re-export).
- `app/rag/__init__.py` file size ~600-900 bytes (docstring expand từ 320 bytes baseline OK).
  </done>
</task>

</tasks>

<verification>
**Final verification suite — gap closure complete:**

```bash
cd Hub_All/api

# 1. Unit tests — schema build + Plan 04-07 regression
uv run pytest tests/unit/test_rag_flow.py -v --tb=short
# Expected: 15/15 PASS (12 existing + 3 Plan 04-07 regression)

# 2. Critical marker suite — Phase 3 + Phase 4 happy paths
uv run pytest -m critical -v --tb=short
# Expected: 29/29 PASS

# 3. E2E integration (REQUIRES Docker daemon + testcontainers postgres+redis)
uv run pytest tests/integration/test_ingest_e2e.py -v --tb=short
# Expected: 3/3 PASS (test 1 + test 3 chuyển từ SKIP → PASS, test 2 vẫn PASS)

# 4. Grep verification — Plan 04-07 acceptance criteria
grep -c "Annotated\[NDArray\[np.float32\], EMBEDDER\]" app/rag/flow.py    # = 0
grep -c "_VECTOR_SCHEMA" app/rag/flow.py                                   # ≥ 2
grep -c "raise  # ← Plan 04-07: fail-fast" app/main.py                     # = 1
grep -c "pytest.skip" tests/integration/test_ingest_e2e.py                 # = 0
grep -c "assert cocoindex_app is not None" tests/integration/test_ingest_e2e.py  # ≥ 2
grep -c "@cocoindex.flow_def" app/rag/__init__.py                          # = 0

# 5. Runtime probe — ChunkRow schema build KHÔNG raise
uv run python -c "
import asyncio
from cocoindex.connectors import postgres as pg
from app.rag.flow import ChunkRow
s = asyncio.run(pg.TableSchema.from_class(ChunkRow, primary_key=['id']))
assert s.columns['vector'].type == 'vector(1536)', f'vector type sai: {s.columns[\"vector\"].type}'
print('schema build OK:', s.columns['vector'].type)
"
```

**Architecture verification:**
- VectorSchemaProvider Protocol satisfied → `pg.TableSchema.from_class(ChunkRow)` exit 0
- Lifespan fail-fast → cocoindex_app=None branch CHỈ trong test isolation
- CI gate enforce → pytest.skip pattern eliminated cho cocoindex_app=None scenarios
- Documentation aligned cocoindex 1.0.3 actual API

**Re-verify expected after gap closure:**
- SC2 (Phase 4 ROADMAP success criterion) FAILED → PASS
- SC5 (Phase 4 ROADMAP success criterion) FAILED → PASS
- 5/5 ROADMAP success criteria PASS
- INGEST-01 PARTIAL → SATISFIED
- INGEST-03 PARTIAL → SATISFIED
- 8/8 INGEST requirements SATISFIED
- Phase 4 status: gaps_found → COMPLETE
- M2a EXIT GATE: ready for human accept decision
</verification>

<success_criteria>
**Gap closure plan complete khi:**

1. ✅ `app/rag/flow.py` ChunkRow.vector annotation: `Annotated[NDArray[np.float32], _VECTOR_SCHEMA]` (KHÔNG còn `EMBEDDER`).
2. ✅ `_VECTOR_SCHEMA = cocoindex.resources.schema.VectorSchema(dtype=np.dtype(np.float32), size=EMBEDDING_DIM)` module-level constant.
3. ✅ Runtime probe: `await pg.TableSchema.from_class(ChunkRow, primary_key=['id'])` exit 0 + vector column type = `"vector(1536)"`.
4. ✅ `app/main.py` lifespan step 3: fail-fast pattern (`raise` thay `logger.warning`).
5. ✅ `app/services/documents_service.py::trigger_cocoindex_update` defensive branch: ERROR level + Plan 04-07 reference message.
6. ✅ `tests/integration/test_ingest_e2e.py`: 0 occurrence `pytest.skip` cho cocoindex_app, ≥ 2 occurrence `assert cocoindex_app is not None`.
7. ✅ `app/rag/__init__.py`: docstring KHÔNG còn `@cocoindex.flow_def`, mention cocoindex 1.0.3 actual API.
8. ✅ Unit test suite `tests/unit/test_rag_flow.py`: 15/15 PASS (12 cũ + 3 mới regression).
9. ✅ Critical marker suite: 29/29 PASS (no regression).
10. ✅ E2E suite (với Docker testcontainers): 3/3 PASS (test 1 + 3 chuyển SKIP → PASS).

**Phase 4 re-verification expected outcome:**

| Success Criterion | Before Plan 04-07 | After Plan 04-07 |
|---|---|---|
| SC1 — upload 202 <500ms | ✅ PASS | ✅ PASS |
| SC2 — pending → completed <5s + chunks pgvector | ❌ FAILED (VectorSchemaProvider) | ✅ PASS |
| SC3 — scanned PDF 415 + failed_unsupported | ✅ PASS | ✅ PASS |
| SC4 — watchdog 5min timeout | ✅ PASS (DB level + manual stress) | ✅ PASS |
| SC5 — content-hash incremental dedup | ❌ FAILED (cùng root cause SC2) | ✅ PASS |

| INGEST Requirement | Before | After |
|---|---|---|
| INGEST-01 cocoindex flow đăng ký + Postgres source | ⚠️ PARTIAL | ✅ SATISFIED |
| INGEST-02 transform extract → chunk → embed | ✅ SATISFIED | ✅ SATISFIED |
| INGEST-03 target chunks + stable id + content_hash | ⚠️ PARTIAL | ✅ SATISFIED |
| INGEST-04 POST /upload admin → 202 | ✅ SATISFIED | ✅ SATISFIED |
| INGEST-05 GET /:id status + chunk_count | ✅ SATISFIED | ✅ SATISFIED |
| INGEST-06 watchdog flip stuck | ✅ SATISFIED | ✅ SATISFIED |
| INGEST-07 DELETE admin + cascade + audit | ✅ SATISFIED | ✅ SATISFIED |
| INGEST-08 GET list + pagination + filter | ✅ SATISFIED | ✅ SATISFIED |

**Total:** 8/8 INGEST SATISFIED + 5/5 SC PASS → Phase 4 ready to mark COMPLETE → M2a EXIT GATE ready cho user accept decision → tiếp tục M2b (Phase 5+).
</success_criteria>

<output>
After completion, create `.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-07-SUMMARY.md` documenting:
- Architectural fix: VectorSchemaProvider Protocol satisfaction via `cocoindex.resources.schema.VectorSchema`
- Fail-fast lifespan pattern: removes silent masking of cocoindex setup failures
- CI gate hardening: `pytest.skip` → `assert` conversion enforces blocker recurrence detection
- Documentation alignment: cocoindex 1.0.3 actual API (coco.App + main_fn) vs deprecated 0.x (@cocoindex.flow_def)
- Re-verification result: 5/5 SC PASS + 8/8 INGEST SATISFIED → Phase 4 COMPLETE
- M2a EXIT GATE: ready for user accept decision → continue M2b (Phase 5+)
</output>
