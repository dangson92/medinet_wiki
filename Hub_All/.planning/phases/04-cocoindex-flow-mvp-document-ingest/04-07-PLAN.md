---
plan: 07
phase: 4
wave: 4
type: execute
depends_on: [05, 06]
files_modified:
  - Hub_All/docs/m2a-exit-gate-demo.md
  - Hub_All/api/scripts/m2a_demo.sh
autonomous: false
requirements: [INGEST-04, INGEST-05, INGEST-06, INGEST-07, INGEST-08]
must_haves:
  truths:
    - "docs/m2a-exit-gate-demo.md là tài liệu tiếng Việt 5 bước paste-ready: Bước 1 docker compose up postgres redis; Bước 2 make migrate-up; Bước 3 uvicorn (lifespan auto setup_cocoindex) + seed admin; Bước 4 upload DOCX VN qua curl POST /api/documents/upload; Bước 5 poll GET /:id + psql SELECT chunks verify."
    - "scripts/m2a_demo.sh tự động hóa Bước 3-5 cho CI smoke — login admin → upload DOCX VN runtime-generate → poll status → psql verify chunks COUNT>0 + vector_dims=1536 + content_hash set; in marker M2A_DEMO_PASS."
    - "Demo md ánh xạ đủ 5 ROADMAP Phase 4 Success Criteria (SC1 upload<500ms, SC2 status→completed chunks pgvector, SC3 scanned PDF 415, SC4 watchdog 5min, SC5 content-hash dedup) — mỗi SC có bước verify cụ thể."
    - "Checkpoint human-verify — operator chạy demo với cocoindex + OPENAI_API_KEY thật, xác nhận M2a EXIT GATE: upload DOCX VN → SELECT chunks pgvector → accept thì tiếp tục M2b."
  artifacts:
    - path: Hub_All/docs/m2a-exit-gate-demo.md
      provides: "Manual demo script tiếng Việt 5 bước + AC1-5 checklist + quyết định gate."
      contains: "M2a EXIT GATE"
    - path: Hub_All/api/scripts/m2a_demo.sh
      provides: "Bash automation Bước 3-5 — login/upload/poll/SELECT verify chunks."
      contains: "M2A_DEMO_PASS"
  key_links:
    - from: Hub_All/api/scripts/m2a_demo.sh
      to: POST /api/documents/upload + GET /api/documents/:id
      via: "curl upload + poll loop status"
      pattern: "documents/upload"
    - from: Hub_All/docs/m2a-exit-gate-demo.md
      to: Hub_All/api/scripts/m2a_demo.sh
      via: "Bước 4-5 tham chiếu script automation"
      pattern: "m2a_demo.sh"
---

# Plan 04-07: M2a EXIT GATE — demo script + checkpoint xác nhận

<objective>
Đóng Phase 4 + M2a milestone — ship tài liệu demo + bash automation cho **M2a EXIT GATE**, rồi checkpoint để operator xác nhận với cocoindex + OPENAI_API_KEY thật.

ROADMAP M2a EXIT GATE (giữa Phase 4 và Phase 5): "Demo upload DOCX VN → chunks pgvector → SELECT verify content + hub_id + vector dimension đúng. User accept? Reject → STOP, không pivot 3."

Plan 04-06 đã verify SC2/SC5 qua E2E test automated (mock LiteLLM). Plan 04-07 ship artifact để operator chạy demo độc lập với LiteLLM thật — vì mock LiteLLM có thể trigger code path khác (mock aembedding KHÔNG check dim/content thật). Demo production thật là proxy chính xác cho quyết định tiếp tục M2b.

Purpose: M2a EXIT GATE — đóng Phase 4, gate sang Phase 5.
Output: docs/m2a-exit-gate-demo.md, scripts/m2a_demo.sh, checkpoint human-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/PROJECT.md

<interfaces>
Endpoints sẵn sàng sau Plan 04-04/05: POST /api/documents/upload (multipart, admin Bearer), GET /api/documents/:id, GET /api/documents, DELETE /api/documents/:id.
Auth: POST /api/auth/login → access_token. Seed admin: admin@medinet.vn / Admin@123 (Phase 3 seed).
Docker: docker compose up postgres redis (Phase 1 stack); make migrate-up (alembic); uvicorn app.main:app --port 8080.
Postgres container: medinet-postgres, DB medinet_central, user medinet.
chunks verify SQL: SELECT COUNT(*), MAX(vector_dims(vector)) AS dim, COUNT(content_hash) FROM chunks WHERE document_id=:id.
.env cần OPENAI_API_KEY thật cho demo (LiteLLM aembedding dim 1536).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/m2a-exit-gate-demo.md — manual demo 5 bước</name>
  <read_first>
    - Hub_All/docs/m2a-exit-gate-demo.md (file đã tồn tại — REWRITE đúng spec)
    - Hub_All/.planning/ROADMAP.md (Phase 4 — 5 Success Criteria + M2a EXIT GATE)
    - Hub_All/api/app/routers/documents.py (Plan 04-04/05 — endpoint shape)
  </read_first>
  <action>
  REWRITE `Hub_All/docs/m2a-exit-gate-demo.md` — tiếng Việt, paste-ready commands:

  - **Tiền điều kiện:** Docker Desktop chạy; `.env` có OPENAI_API_KEY thật; `make install` + `make keys` đã chạy.
  - **Bước 1 — Khởi động hạ tầng:** `cd Hub_All && docker compose up -d postgres redis` + `docker compose ps` (2 service healthy).
  - **Bước 2 — Migrate schema:** `cd Hub_All/api && make migrate-up && make migrate-check`.
  - **Bước 3 — Khởi động API + seed admin:** export OPENAI_API_KEY; `uv run uvicorn app.main:app --port 8080` (hoặc docker compose up api). Log PHẢI có `cocoindex_setup` thành công + `cocoindex_initial_backfill` complete (lifespan fail-fast Plan 04-03 — nếu setup lỗi uvicorn crash, KHÔNG mask). Seed admin SQL snippet.
  - **Bước 4 — Upload DOCX VN:** login lấy access_token; `curl POST /api/documents/upload -F file=@khám-bệnh-mẫu.docx -F hub_id=...` → 202 + document_id. Snippet runtime-generate DOCX VN qua python-docx (Mục 1 KHÁM TỔNG QUÁT + Mục 2 XÉT NGHIỆM).
  - **Bước 5 — Verify chunks pgvector:** poll loop GET /api/documents/:id tới status in {completed, failed, failed_unsupported} (POLL_TIMEOUT 300s — headroom cocoindex init lần đầu); `docker exec medinet-postgres psql -U medinet -d medinet_central -c "SELECT COUNT(*), MAX(vector_dims(vector)) AS dim, COUNT(content_hash) FROM chunks WHERE document_id='...'"` → expect COUNT>0, dim=1536, content_hash set.
  - **AC1-5 checklist** ánh xạ 5 ROADMAP Success Criteria:
    - AC1 (SC1, INGEST-04): upload → 202 + document_id <500ms + last_heartbeat bootstrap. Lưu ý SLA: <500ms là response của POST /upload (đo được tự động qua m2a_demo.sh / curl timing). SC2 <5s end-to-end pickup KHÔNG assert ở đây — xem AC2.
    - AC2 (SC2, INGEST-02/03): status pending → completed; chunks dim=1536 + content_hash set + hub_id đúng. <5s end-to-end pickup (cocoindex thấy row mới → flow chạy → chunks visible) được QUAN SÁT trong demo, KHÔNG assert cứng: lần chạy đầu cocoindex Rust core cold-start làm pickup chậm hơn SLA — operator ghi lại thời gian quan sát được, KHÔNG fail gate chỉ vì cold-start.
    - AC3 (SC3, R4): scanned PDF → 415 + status='failed_unsupported'.
    - AC4 (SC4, P8): kill cocoindex worker → sau 5 phút status flip 'failed' (manual stress optional).
    - AC5 (SC5, D-1): upload cùng file 2 lần → cocoindex memo + stable_chunk_id deterministic; quan sát OpenAI usage-log count KHÔNG tăng cho document đã embed (verification chính thức của SC5 — định tuyến từ Plan 04-06).
  - **Quyết định gate:** 5/5 AC PASS → accept M2a EXIT GATE, tiếp tục Phase 5. 3-4/5 → fix forward. ≤2/5 → trigger EXIT criteria E1 (PROJECT.md) — KHÔNG pivot lần 3, chỉ fix forward / ship M2a standalone.
  - **Cảnh báo bảo mật:** KHÔNG commit .env; đổi password admin Admin@123 trước production.
  </action>
  <verify>
    <automated>cd Hub_All && test -f docs/m2a-exit-gate-demo.md && grep -c 'M2a EXIT GATE' docs/m2a-exit-gate-demo.md && grep -c 'AC2' docs/m2a-exit-gate-demo.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'M2a EXIT GATE' Hub_All/docs/m2a-exit-gate-demo.md` >= 2
    - `grep -c 'Bước' Hub_All/docs/m2a-exit-gate-demo.md` >= 5
    - `grep -c 'vector_dims' Hub_All/docs/m2a-exit-gate-demo.md` >= 1
    - `grep -c 'AC1\|AC2\|AC3\|AC4\|AC5' Hub_All/docs/m2a-exit-gate-demo.md` >= 5
    - file tồn tại, viết tiếng Việt
  </acceptance_criteria>
  <done>Demo md 5 bước paste-ready + AC1-5 checklist + quyết định gate.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/m2a_demo.sh — bash automation</name>
  <read_first>
    - Hub_All/api/scripts/m2a_demo.sh (file đã tồn tại — REWRITE đúng spec)
    - Hub_All/docs/m2a-exit-gate-demo.md (Task 1 — Bước 3-5)
  </read_first>
  <action>
  REWRITE `Hub_All/api/scripts/m2a_demo.sh` — bash strict mode `set -euo pipefail`:
  - ENV override-able: API_URL, ADMIN_EMAIL, ADMIN_PASSWORD, HUB_SLUG, PG_CONTAINER, PG_USER, PG_DB, POLL_TIMEOUT (default 300).
  - 7 step: (1) health check /healthz + /readyz; (2) login → access_token (validate length>100); (3) resolve hub_id từ slug qua docker exec psql; (4) runtime-generate DOCX VN qua python3 + python-docx; (5) POST /api/documents/upload multipart → DOC_ID; (6) poll loop seq 1..POLL_TIMEOUT đợi status in {completed, failed, failed_unsupported}; (7) psql verify chunks: COUNT>0 + vector_dims=1536 + hub_id match + content_hash IS NOT NULL.
  - In marker `M2A_DEMO_PASS chunks=N dim=1536` khi PASS cho CI grep; exit 1 nếu FAIL.
  - python3 inline JSON parser (KHÔNG phụ thuộc jq).
  - `bash -n scripts/m2a_demo.sh` syntax check phải PASS.
  </action>
  <verify>
    <automated>cd Hub_All/api && bash -n scripts/m2a_demo.sh && grep -c 'M2A_DEMO_PASS' scripts/m2a_demo.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'set -euo pipefail' Hub_All/api/scripts/m2a_demo.sh` >= 1
    - `grep -c 'M2A_DEMO_PASS' Hub_All/api/scripts/m2a_demo.sh` >= 1
    - `grep -c 'documents/upload' Hub_All/api/scripts/m2a_demo.sh` >= 1
    - `grep -c 'vector_dims' Hub_All/api/scripts/m2a_demo.sh` >= 1
    - `bash -n scripts/m2a_demo.sh` exit 0 (syntax OK)
  </acceptance_criteria>
  <done>m2a_demo.sh ship; 7 step automation; bash -n PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — M2a EXIT GATE manual demo verify</name>
  <what-built>
  Phase 4 hoàn tất pipeline ingest: cocoindex 1.0.3 flow medinet_wiki_ingest + A4 BackgroundTasks trigger + extract/chunk VN/embed dim 1536 → chunks pgvector + watchdog + DELETE/LIST. Plan 04-06 đã verify SC2 qua E2E test automated (mock LiteLLM). Plan 04-07 ship docs/m2a-exit-gate-demo.md + scripts/m2a_demo.sh để operator demo với cocoindex + LiteLLM thật.
  </what-built>
  <how-to-verify>
  1. Đảm bảo `.env` có `OPENAI_API_KEY` thật.
  2. Chạy theo `Hub_All/docs/m2a-exit-gate-demo.md` Bước 1-5 (hoặc `cd Hub_All/api && bash scripts/m2a_demo.sh` cho Bước 3-5).
  3. Xác nhận:
     - AC1: `POST /api/documents/upload` DOCX VN → 202 + document_id (<500ms).
     - AC2: poll `GET /api/documents/:id` → status chuyển `pending → completed`; `psql SELECT COUNT(*), vector_dims(vector), content_hash FROM chunks WHERE document_id=...` → COUNT>0, dim=1536, content_hash set, hub_id đúng.
     - AC3: upload scanned PDF → 415 + status='failed_unsupported'.
     - AC4 (optional stress): kill cocoindex worker giữa flow → sau 5 phút status flip 'failed'.
     - AC5: upload cùng file 2 lần → KHÔNG bloat chunks (cocoindex memo); quan sát OpenAI usage-log count KHÔNG tăng cho document đã embed.
  4. Quyết định: 5/5 (hoặc 4/5 với AC4 optional) PASS → accept M2a EXIT GATE → tiếp tục Phase 5. Nếu AC2 FAIL (0 chunks) → báo lại, KHÔNG đóng phase — cần debug forward (Plan 04-06 Task 3 đã có debug hook log index_document_chunks).
  </how-to-verify>
  <acceptance_criteria>
  - AC1: POST /api/documents/upload DOCX VN → 202 + document_id. SLA: <500ms upload-response là phần TỰ ĐỘNG KIỂM (đo được qua m2a_demo.sh / curl timing).
  - AC2: status pending → completed; chunks COUNT>0, dim=1536, content_hash set, hub_id đúng. SLA <5s end-to-end pickup (cocoindex thấy row → flow chạy → chunks visible) được QUAN SÁT (KHÔNG assert) trong demo production — cocoindex Rust cold-start làm lần chạy đầu chậm hơn SLA; operator ghi lại thời gian quan sát được, KHÔNG fail gate chỉ vì cold-start.
  - AC3: scanned PDF → 415 + status='failed_unsupported'.
  - AC4 (optional stress): kill cocoindex worker → sau 5 phút status flip 'failed'.
  - AC5: upload cùng file 2 lần → KHÔNG bloat chunks; OpenAI usage-log count KHÔNG tăng cho document đã embed (verification chính thức SC5 — định tuyến từ Plan 04-06).
  - Quyết định gate: 5/5 (hoặc 4/5 với AC4 optional) PASS → accept M2a EXIT GATE.
  </acceptance_criteria>
  <resume-signal>Gõ "approved" nếu M2a EXIT GATE PASS, hoặc mô tả AC nào fail.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo script → live API | curl gọi endpoint với access_token admin |
| operator → .env OPENAI_API_KEY | Secret thật trong .env (gitignored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | I (Info disclosure) | Demo script in OPENAI_API_KEY / access_token ra terminal → leak qua screenshot/log | mitigate | Script KHÔNG echo env vars / token đầy đủ; demo md cảnh báo KHÔNG screenshot terminal |
| T-04-07-02 | E (Elevation) | Demo seed admin password Admin@123 plaintext → operator quên đổi production | mitigate | Demo md cảnh báo in đậm đổi password trước production; Phase 5 USER-02 reset endpoint |
| T-04-07-03 | R (Repudiation) | M2a EXIT GATE accept qua chat reply KHÔNG audit trail | accept | M2 single-user; quyết định ghi vào STATE.md + SUMMARY |
</threat_model>

<verification>
- docs/m2a-exit-gate-demo.md tồn tại, 5 bước, AC1-5 checklist.
- scripts/m2a_demo.sh `bash -n` PASS, có M2A_DEMO_PASS marker.
- Checkpoint operator xác nhận M2a EXIT GATE (5 SC).
</verification>

<success_criteria>
- docs/m2a-exit-gate-demo.md + scripts/m2a_demo.sh ship.
- M2a EXIT GATE checkpoint — operator accept → Phase 4 đóng, Phase 5 unblocked.
- 5 ROADMAP Phase 4 Success Criteria verified (automated Plan 04-06 SC2 + manual demo Plan 04-07 SC1/SC3/SC4/SC5).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-07-SUMMARY.md` theo template summary.md.
</output>
</content>
</invoke>
