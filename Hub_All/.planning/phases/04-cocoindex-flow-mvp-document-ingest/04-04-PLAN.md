---
plan: 04
phase: 4
wave: 2
depends_on: [01, 02]
files_modified:
  - Hub_All/api/app/schemas/__init__.py
  - Hub_All/api/app/schemas/documents.py
  - Hub_All/api/app/services/documents_service.py
  - Hub_All/api/app/services/file_extract.py
  - Hub_All/api/app/routers/__init__.py
  - Hub_All/api/app/routers/documents.py
  - Hub_All/api/app/main.py
  - Hub_All/api/tests/integration/test_documents_upload.py
autonomous: true
requirements: [INGEST-04, INGEST-05]
revision: 2
revision_reason: |
  Plan 04-03 REVISION 2 đã rewrite flow theo cocoindex 1.0.3 actual API. Cocoindex
  1.0.3 KHÔNG support source LISTEN/NOTIFY natively — Plan 04-04 revision 1 logic
  `pg_notify('documents_notify', :doc_id)` sẽ KHÔNG trigger cocoindex flow.

  User decision A4 BLOCKING (confirmed): Router synchronous trigger qua FastAPI
  BackgroundTasks. Plan 04-04 REVISION 2:
  - REMOVE `pg_notify('documents_notify', :doc_id)` SQL.
  - ADD FastAPI `BackgroundTasks` parameter vào router endpoint upload.
  - ADD helper `trigger_cocoindex_update(cocoindex_app, doc_id)` — chạy
    `await asyncio.to_thread(cocoindex_app.update_blocking)` rồi count chunks
    WHERE document_id=:doc_id → UPDATE documents SET status='completed'
    chunk_count=:count nếu >0, status='failed' error_message='no chunks generated'
    nếu =0. Helper này set status sau update_blocking vì cocoindex 1.0.3 KHÔNG
    expose per-row callback hook.
  - Router sau service.create() thành công gọi
    `background_tasks.add_task(trigger_cocoindex_update, request.app.state.cocoindex_app, doc_id)`.
  - Service.create GIỮ INSERT documents row với last_heartbeat=NOW() bootstrap
    (WARNING #7 — watchdog Plan 04-05 NULL guard query vẫn cần).
  - Service.create GIỮ early-detect scanned PDF synchronous (BLOCKER #3) +
    KHÔNG add BackgroundTask cho scanned (status='failed_unsupported' final).
  - Service.create GIỮ SQL NOW() cho timestamps (BLOCKER #4 — datetime.utcnow
    deprecated Python 3.12).
  - Settings.cocoindex_lmdb_path field đã add ở Plan 04-03 task 02 (Q5 NO-OP cho
    Plan 04-04).
must_haves:
  truths:
    - "POST /api/documents/upload multipart (file + hub_id form field) với Bearer admin token → 202 envelope {success:true, data:{document_id, status:'pending'}, error:null, meta:null}. File lưu file_store/<uuid>.<ext>, row documents INSERT status='pending' + last_heartbeat=NOW() bootstrap (WARNING #7), FastAPI BackgroundTasks add_task(trigger_cocoindex_update, app.state.cocoindex_app, doc_id) — KHÔNG còn pg_notify (A4 user BLOCKING confirmed)."
    - "POST upload với extension không trong whitelist (.exe, .png...) → 415 envelope {error:{code:'UNSUPPORTED_FORMAT', message:'Định dạng ... không hỗ trợ. Khuyến nghị: chuyển sang DOCX.'}} (R4 mitigation). KHÔNG INSERT documents row, KHÔNG add BackgroundTask."
    - "POST upload PDF scanned (detect synchronously qua app/services/file_extract.detect_scanned_pdf) → 415 envelope {error:{code:'UNSUPPORTED_FORMAT', message:'PDF scan chưa hỗ trợ trong M2. Khuyến nghị: chuyển sang DOCX.'}} + row documents INSERT status='failed_unsupported' + KHÔNG add BackgroundTask cocoindex (BLOCKER #3 strategy A — R4 mitigation early-detect)."
    - "POST upload viewer/editor (KHÔNG admin) → 403 envelope {error:{code:'FORBIDDEN'}} (RBAC require_role('admin') từ Phase 3 Plan 03-05)."
    - "GET /api/documents/:id Bearer → 200 envelope {data:{id, hub_id, filename, status, chunk_count, last_heartbeat, attempts, error_message, created_at, updated_at}} với status enum 5 giá trị (pending|processing|completed|failed|failed_unsupported)."
    - "trigger_cocoindex_update helper sau cocoindex_app.update_blocking() → SELECT COUNT(*) FROM chunks WHERE document_id=:id → UPDATE documents SET status='completed' chunk_count=:count nếu >0 hoặc status='failed' nếu =0; refresh last_heartbeat=NOW() (cho watchdog Plan 04-05 NULL guard)."
    - "pytest tests/integration/test_documents_upload.py -v PASS — testcontainers Postgres pgvector pg16 + Redis 7-alpine, upload DOCX VN → 202 + status pending + BackgroundTask added (mock cocoindex_app.update_blocking), upload .exe → 415, upload scanned PDF → 415 + status failed_unsupported + KHÔNG add task, GET sau upload → status pending."
  artifacts:
    - path: Hub_All/api/app/schemas/__init__.py
      provides: "Package init schemas."
      exports: ["DocumentResponse", "DocumentStatus"]
    - path: Hub_All/api/app/schemas/documents.py
      provides: "Pydantic v2 schemas — DocumentResponse + DocumentStatus Literal."
      contains: "class DocumentResponse"
    - path: Hub_All/api/app/services/documents_service.py
      provides: "DocumentService REVISION 2 — early-detect scanned PDF (BLOCKER #3) + save file qua FileStore + INSERT documents row với last_heartbeat=NOW() bootstrap (WARNING #7) + SQL NOW() timestamps (BLOCKER #4 — KHÔNG datetime.utcnow). KHÔNG còn pg_notify (A4). Module-level helper trigger_cocoindex_update(cocoindex_app, doc_id) chạy update_blocking rồi count chunks → set status."
      contains: "class DocumentService"
    - path: Hub_All/api/app/services/file_extract.py
      provides: "EXTEND (Plan 04-02 baseline đã expose detect_scanned_pdf public — verify only)."
      contains: "def detect_scanned_pdf"
    - path: Hub_All/api/app/routers/__init__.py
      provides: "Package init routers."
      exports: ["documents_router"]
    - path: Hub_All/api/app/routers/documents.py
      provides: "REVISION 2 APIRouter(prefix='/api/documents') — POST /upload (Content-Length pre-check WARNING #6 + BackgroundTasks A4 trigger) + GET /:id."
      contains: "background_tasks: BackgroundTasks"
    - path: Hub_All/api/app/main.py
      provides: "EXTEND — include_router(documents_router) trong create_app()."
      contains: "include_router(documents_router)"
    - path: Hub_All/api/tests/integration/test_documents_upload.py
      provides: "Integration test 8 case REVISION 2: happy upload DOCX VN + assert BackgroundTask added (mock cocoindex_app), 415 .exe, 415 scanned PDF + failed_unsupported + KHÔNG add task, 403 viewer (assert error.code FORBIDDEN — KHÔNG message text — WARNING #4), 401 missing auth, 404 GET unknown id, GET sau upload với last_heartbeat NOT NULL (WARNING #7)."
      contains: "def test_upload_happy_path"
  key_links:
    - from: Hub_All/api/app/routers/documents.py POST /upload
      to: Hub_All/api/app/services/documents_service.py::DocumentService.create
      via: "service.create(hub_id, uploaded_by, file_content, original_filename) → 202 hoặc 415 (scanned PDF)"
      pattern: "service.create"
    - from: Hub_All/api/app/services/documents_service.py::create
      to: Hub_All/api/app/services/file_extract.py::detect_scanned_pdf (Plan 04-02 expose)
      via: "Nếu ext=='.pdf' → detect_scanned_pdf(saved_path); is_scanned=True → INSERT row status='failed_unsupported' + KHÔNG return doc_id cho BackgroundTask + raise UnsupportedFormatError"
      pattern: "detect_scanned_pdf|is_scanned check.*status..failed_unsupported"
    - from: Hub_All/api/app/services/documents_service.py::create
      to: Hub_All/api/app/services/file_store.py::FileStore.save
      via: "fs.save(content, filename) → Path"
      pattern: "FileStore"
    - from: Hub_All/api/app/services/documents_service.py::create
      to: Postgres INSERT documents row + last_heartbeat=NOW() bootstrap (WARNING #7)
      via: "INSERT documents (..., last_heartbeat=NOW(), created_at=NOW(), updated_at=NOW()) — bootstrap last_heartbeat NOT NULL trước khi cocoindex pick up; watchdog Plan 04-05 chỉ flip stale (NOT NULL + > 5 min). KHÔNG còn pg_notify (A4)."
      pattern: "last_heartbeat.*NOW|INSERT INTO documents"
    - from: Hub_All/api/app/routers/documents.py POST /upload (sau service.create OK)
      to: trigger_cocoindex_update helper qua FastAPI BackgroundTasks (A4)
      via: "background_tasks.add_task(trigger_cocoindex_update, request.app.state.cocoindex_app, doc_id) — Plan 04-03 lưu app.state.cocoindex_app trong lifespan"
      pattern: "background_tasks.add_task|trigger_cocoindex_update"
    - from: Hub_All/api/app/services/documents_service.py::trigger_cocoindex_update
      to: cocoindex_app.update_blocking + chunks COUNT → UPDATE documents status
      via: "await asyncio.to_thread(cocoindex_app.update_blocking) → SELECT COUNT(*) FROM chunks WHERE document_id=:id → UPDATE documents SET status='completed' chunk_count=:count last_heartbeat=NOW() nếu >0; status='failed' nếu =0"
      pattern: "update_blocking|UPDATE documents.*status.*completed"
    - from: Hub_All/api/app/routers/documents.py
      to: Hub_All/api/app/auth/dependencies.py::require_role
      via: "Depends(require_role('admin'))"
      pattern: "require_role"
---

# Plan 04: Documents router POST /upload + GET /:id + BackgroundTasks A4 (REVISION 2)

## Objective

Implement REST endpoint upload document (INGEST-04) + GET status (INGEST-05). Router validate format (R4 mitigation whitelist 4 ext + Content-Length DoS guard WARNING #6), gọi DocumentService.create() → **early-detect scanned PDF synchronously** (BLOCKER #3) → FileStore + Postgres INSERT (với `last_heartbeat=NOW()` bootstrap — WARNING #7) → **FastAPI BackgroundTasks add_task(trigger_cocoindex_update, app.state.cocoindex_app, doc_id)** để cocoindex 1.0.3 flow Plan 04-03 chạy `update_blocking()` synchronously trong background thread (Decision A4 user BLOCKING confirmed).

**Endpoints:**

| Endpoint | Method | Auth | Purpose |
|---|---|---|---|
| `/api/documents/upload` | POST | Bearer admin | Multipart file + hub_id → 202 với document_id (HOẶC 415 nếu ext sai / PDF scan) |
| `/api/documents/:id` | GET | Bearer (any role có hub access) | Trả status + chunk_count + heartbeat |

**Tại sao plan riêng (tách khỏi Plan 04-05 list/delete/watchdog):**
- Upload + GET là core path M2a EXIT GATE — phải hoạt động trước list/delete.
- Plan 04-04 dùng FileStore + DocumentService — Plan 04-05 sẽ extend service với list/delete query + watchdog task.
- Test integration upload có infrastructure phức tạp (testcontainers + httpx multipart + mock cocoindex_app) — tách riêng dễ debug.

**REVISION 2 — A4 BackgroundTasks trigger replace pg_notify (cocoindex 1.0.3 architectural — user BLOCKING confirmed):**

- Cocoindex 1.0.3 `PgTableSource.fetch_rows()` là one-shot SELECT, KHÔNG support change notification natively (RESEARCH.md Section 4.4 + Section 9 Open Question Q1). LISTEN/NOTIFY pg_notify('documents_notify', :doc_id) sẽ KHÔNG trigger cocoindex flow → chunks KHÔNG được generate → user thấy status='pending' forever.
- Decision A4 user BLOCKING accepted: Router upload sau INSERT documents → FastAPI BackgroundTasks `add_task(trigger_cocoindex_update, request.app.state.cocoindex_app, doc_id)`. BackgroundTask chạy SAU response 202 trả về client → KHÔNG block upload latency. Helper `trigger_cocoindex_update`:
  ```python
  async def trigger_cocoindex_update(cocoindex_app: Any, doc_id: UUID) -> None:
      try:
          await asyncio.to_thread(cocoindex_app.update_blocking)
          # Post-update: count chunks for this doc, set status accordingly
          async with get_engine().begin() as conn:
              count = await conn.scalar(
                  text("SELECT COUNT(*) FROM chunks WHERE document_id = :id"),
                  {"id": str(doc_id)},
              )
              if count > 0:
                  await conn.execute(
                      text(
                          "UPDATE documents SET status='completed', "
                          "chunk_count=:count, last_heartbeat=NOW(), updated_at=NOW() "
                          "WHERE id = :id"
                      ),
                      {"count": count, "id": str(doc_id)},
                  )
              else:
                  await conn.execute(
                      text(
                          "UPDATE documents SET status='failed', "
                          "error_message='cocoindex flow generated 0 chunks', "
                          "last_heartbeat=NOW(), updated_at=NOW() "
                          "WHERE id = :id"
                      ),
                      {"id": str(doc_id)},
                  )
      except Exception as exc:
          # On exception → status='failed' + error_message
          async with get_engine().begin() as conn:
              await conn.execute(
                  text(
                      "UPDATE documents SET status='failed', "
                      "error_message=:err, last_heartbeat=NOW(), updated_at=NOW() "
                      "WHERE id = :id"
                  ),
                  {"err": f"cocoindex update failed: {exc}", "id": str(doc_id)},
              )
  ```

**Multi-source coverage audit (revision 2):**

| Source | Item | Plan covering | Status |
|---|---|---|---|
| GOAL (ROADMAP Phase 4) | Upload endpoint trigger ingest pipeline | 04-04 task 02-03 (service + router + BackgroundTasks A4) | covered |
| REQ INGEST-04 | POST /api/documents/upload multipart admin → 202 + INSERT pending | 04-04 task 01-03 | covered |
| REQ INGEST-05 | GET /api/documents/:id → status + chunk_count + heartbeat | 04-04 task 01-03 | covered |
| RESEARCH | Cocoindex 1.0.3 KHÔNG support LISTEN/NOTIFY → A4 BackgroundTasks | 04-04 task 02 (trigger_cocoindex_update helper) + task 03 (router add_task) | covered |
| CONTEXT R4 | Whitelist 4 ext + scanned PDF early-detect (BLOCKER #3) | 04-04 task 02 (service.create early-detect + raise + INSERT failed_unsupported) | covered |
| CONTEXT BLOCKER #4 | Datetime utcnow deprecated → SQL NOW() | 04-04 task 02 (INSERT timestamps NOW() server-side) | covered |
| CONTEXT WARNING #6 | DoS upload large file → Content-Length pre-check | 04-04 task 03 (router pre-check trước file.read()) | covered |
| CONTEXT WARNING #7 | Watchdog NULL bug → bootstrap last_heartbeat=NOW() | 04-04 task 02 (INSERT last_heartbeat=NOW() cả 2 path scanned + non-scanned) | covered |
| Decision A4 (BLOCKING) | Router BackgroundTasks trigger thay LISTEN/NOTIFY | 04-04 task 02 (trigger_cocoindex_update helper) + task 03 (router signature + add_task) | covered |
| Decision Q5 (NEW) | Settings.cocoindex_lmdb_path | 04-03 task 02 đã ship — Plan 04-04 NO-OP | covered (cross-plan) |

KHÔNG có item missing — INGEST-04..05 + R4 + BLOCKER #3/#4 + WARNING #6/#7 + Decision A4 đều covered.

## Must-Haves

**Truths (7 mục, observable):**

- `curl -X POST http://localhost:8080/api/documents/upload -H "Authorization: Bearer <admin_token>" -F "file=@Khám.docx" -F "hub_id=<uuid>"` → status 202 + body `{success:true, data:{document_id, status:'pending', filename:'Khám.docx'}, error:null, meta:null}` trong <500ms.
- `curl -X POST .../upload -F "file=@malware.exe" -F "hub_id=<uuid>"` → 415 + body `{success:false, error:{code:'UNSUPPORTED_FORMAT', ...}, meta:null}`.
- `curl -X POST .../upload -F "file=@scan.pdf" -F "hub_id=<uuid>"` (scanned PDF heuristic) → 415 envelope `error.code='UNSUPPORTED_FORMAT'`; SELECT `status` qua psql → `failed_unsupported` (BLOCKER #3); KHÔNG add BackgroundTask cho row scanned.
- `curl -X POST .../upload -H "Authorization: Bearer <viewer_token>"` → 403 + `error.code='FORBIDDEN'` (KHÔNG assert message text — WARNING #4).
- `curl http://localhost:8080/api/documents/<unknown-uuid>` Bearer admin → 404 + `error.code='NOT_FOUND'`.
- Sau response 202, BackgroundTask `trigger_cocoindex_update` chạy → `cocoindex_app.update_blocking()` → count chunks > 0 → `UPDATE documents SET status='completed'`. Test integration mock cocoindex_app verify add_task được call với đúng args.
- `cd Hub_All/api && uv run pytest tests/integration/test_documents_upload.py -v` PASS ≥ 8 test (testcontainers Postgres pgvector + Redis).

**Artifacts (8 file):**

- `Hub_All/api/app/schemas/__init__.py` (NEW) — package init schemas.
- `Hub_All/api/app/schemas/documents.py` (NEW) — Pydantic models + DocumentStatus Literal.
- `Hub_All/api/app/services/documents_service.py` (NEW) — `DocumentService` class với early-detect scanned PDF (BLOCKER #3) + bootstrap last_heartbeat (WARNING #7) + SQL `NOW()` cho timestamps (BLOCKER #4) + module-level helper `trigger_cocoindex_update` (A4).
- `Hub_All/api/app/services/file_extract.py` (VERIFY only — Plan 04-02 đã expose `detect_scanned_pdf` public).
- `Hub_All/api/app/routers/__init__.py` (NEW) — package init routers.
- `Hub_All/api/app/routers/documents.py` (NEW) — `APIRouter(prefix="/api/documents")` 2 endpoint với Content-Length pre-check (WARNING #6) + BackgroundTasks A4 trigger.
- `Hub_All/api/app/main.py` (EXTEND) — `include_router(documents_router)`.
- `Hub_All/api/tests/integration/test_documents_upload.py` (NEW) — 8 test integration với mock cocoindex_app.

**Key links (6 connection):**

- `routers/documents.py::upload` → Content-Length pre-check (WARNING #6) → `Depends(require_role("admin"))` (Phase 3) → service.create() → 202 hoặc 415; nếu 202 → `background_tasks.add_task(trigger_cocoindex_update, request.app.state.cocoindex_app, doc_id)` (A4).
- `services/documents_service.py::create` → nếu ext='.pdf' → `app.services.file_extract.detect_scanned_pdf(saved_path)` → is_scanned=True → INSERT row status='failed_unsupported' + KHÔNG return doc_id cho BackgroundTask + raise UnsupportedFormatError(scanned=True) → router trả 415 (BLOCKER #3).
- `services/documents_service.py::create` → `FileStore().save(content, filename)` → INSERT documents row với `last_heartbeat=NOW()` bootstrap (WARNING #7) + SQL `NOW()` cho timestamps (BLOCKER #4). KHÔNG còn `SELECT pg_notify(...)` (A4 — Plan 04-03 đã document cocoindex 1.0.3 không support source notification).
- `services/documents_service.py::trigger_cocoindex_update` (module-level helper) → `await asyncio.to_thread(cocoindex_app.update_blocking)` → SELECT COUNT chunks → UPDATE documents status='completed'/'failed' + last_heartbeat=NOW() (refresh cho watchdog NULL guard).
- `routers/documents.py::get_by_id` → `Depends(get_current_user)` (Phase 3) → service.get(id) → 200 with DocumentResponse.
- `app/main.py::create_app()` → `from app.routers import documents_router; app.include_router(documents_router)`.

## Tasks

<task id="01">
<action>
Tạo `Hub_All/api/app/schemas/documents.py` + `__init__.py` (KHÔNG đổi từ revision 1 — schemas KHÔNG bị ảnh hưởng A4).

**File 1: `Hub_All/api/app/schemas/__init__.py`**

```python
"""Schemas package — Pydantic v2 request/response models cho REST API."""
from __future__ import annotations

from app.schemas.documents import (
    DOCUMENT_STATUS_VALUES,
    DocumentListItem,
    DocumentResponse,
    DocumentStatus,
    DocumentUploadResponse,
)

__all__ = [
    "DOCUMENT_STATUS_VALUES",
    "DocumentListItem",
    "DocumentResponse",
    "DocumentStatus",
    "DocumentUploadResponse",
]
```

**File 2: `Hub_All/api/app/schemas/documents.py`**

```python
"""Document schemas — Pydantic v2.

Plan 04-04 ship:
- DocumentStatus Literal — match documents.status CHECK enum (R4 + P8).
- DocumentResponse — GET /api/documents/:id payload.
- DocumentListItem — GET /api/documents list item (Plan 04-05).
- DocumentUploadResponse — POST upload 202 response.

INGEST-05: status enum đúng 5 giá trị: pending | processing | completed |
failed | failed_unsupported.
"""
from __future__ import annotations

from datetime import datetime
from typing import Literal

from pydantic import BaseModel

DocumentStatus = Literal[
    "pending",
    "processing",
    "completed",
    "failed",
    "failed_unsupported",
]

DOCUMENT_STATUS_VALUES: tuple[DocumentStatus, ...] = (
    "pending",
    "processing",
    "completed",
    "failed",
    "failed_unsupported",
)


class DocumentResponse(BaseModel):
    """Single document detail — GET /api/documents/:id response data."""

    id: str  # UUID string
    hub_id: str  # UUID string
    uploaded_by: str | None  # UUID string hoặc None nếu user bị xoá (FK SET NULL)
    filename: str
    file_path: str
    mime_type: str | None
    file_size_bytes: int | None
    status: DocumentStatus
    error_message: str | None
    last_heartbeat: datetime | None
    attempts: int
    chunk_count: int
    created_at: datetime
    updated_at: datetime | None


class DocumentListItem(BaseModel):
    """List item — GET /api/documents (Plan 04-05). Compact subset."""

    id: str
    hub_id: str
    filename: str
    status: DocumentStatus
    chunk_count: int
    created_at: datetime
    updated_at: datetime | None


class DocumentUploadResponse(BaseModel):
    """POST /api/documents/upload response data — 202 Accepted."""

    document_id: str
    status: DocumentStatus
    filename: str
```
</action>
<read_first>
- Hub_All/api/app/models/document.py
- Hub_All/api/app/auth/schemas.py (Phase 3 reference style)
- Hub_All/api/migrations/versions/0001_initial_schema.py (status CHECK constraint line 287)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/schemas/__init__.py` tồn tại.
- File `Hub_All/api/app/schemas/documents.py` tồn tại.
- `grep -c 'DocumentStatus = Literal' Hub_All/api/app/schemas/documents.py` ≥ 1.
- `grep -c '"failed_unsupported"' Hub_All/api/app/schemas/documents.py` ≥ 2 (Literal + tuple).
- `grep -c 'class DocumentResponse' Hub_All/api/app/schemas/documents.py` ≥ 1.
- `grep -c 'class DocumentListItem' Hub_All/api/app/schemas/documents.py` ≥ 1.
- `grep -c 'class DocumentUploadResponse' Hub_All/api/app/schemas/documents.py` ≥ 1.
- `grep -c 'last_heartbeat' Hub_All/api/app/schemas/documents.py` ≥ 1.
- `grep -c 'chunk_count' Hub_All/api/app/schemas/documents.py` ≥ 2 (Response + ListItem).
- `cd Hub_All/api && uv run python -c "from app.schemas import DocumentResponse, DocumentStatus, DOCUMENT_STATUS_VALUES, DocumentUploadResponse; print(DOCUMENT_STATUS_VALUES)"` exit 0.
- `cd Hub_All/api && uv run ruff check app/schemas` exit 0.
- `cd Hub_All/api && uv run mypy app/schemas` exit 0.
</acceptance_criteria>
</task>

<task id="02">
<action>
Tạo `Hub_All/api/app/services/documents_service.py` — `DocumentService` class với 2 method `create` + `get` + module-level helper `trigger_cocoindex_update` (A4 user BLOCKING confirmed).

**Prerequisite — verify Plan 04-02 đã expose `detect_scanned_pdf`:**

Plan 04-02 SUMMARY confirm `app/services/file_extract.py` đã ship `detect_scanned_pdf(file_path: Path) -> bool` PUBLIC. Nếu Plan 04-04 executor verify thấy chưa có (regression), thêm function này (xem revision 1 paste-ready code line 270-292). Acceptance criteria sẽ check.

**API contract DocumentService:**

```python
class DocumentService:
    def __init__(self, db: AsyncSession, file_store: FileStore | None = None) -> None: ...

    async def create(
        self,
        *,
        hub_id: UUID,
        uploaded_by: UUID | None,
        file_content: bytes,
        original_filename: str,
        mime_type: str | None = None,
    ) -> DocumentUploadResponse:
        """Save file → early-detect scanned PDF → INSERT documents (last_heartbeat=NOW()) → 202.

        Hoặc nếu PDF scanned: INSERT row status='failed_unsupported' + raise
        UnsupportedFormatError(scanned=True) để router return 415 + KHÔNG add
        BackgroundTask cho cocoindex (A4 — scanned row final).

        Caller (router) sau khi nhận DocumentUploadResponse OK → gọi
        background_tasks.add_task(trigger_cocoindex_update, app.state.cocoindex_app,
        UUID(result.document_id)) (A4).

        Raises:
            UnsupportedFormatError: ext không trong ALLOWED_EXTENSIONS HOẶC PDF scanned.
        """

    async def get(self, document_id: UUID) -> DocumentResponse | None:
        """SELECT 1 row qua id. Return None nếu không tồn tại."""
```

**Logic create() REVISION 2 — A4 (KHÔNG còn pg_notify) + carry-over BLOCKER #3 + #4 + WARNING #7:**

1. Validate extension qua `Path(original_filename).suffix.lower() in ALLOWED_EXTENSIONS`. KHÔNG match → raise `UnsupportedFormatError` (router catch trả 415).
2. `file_store.save(file_content, original_filename)` → `file_path: Path`.
3. **BLOCKER #3 strategy A** — Nếu ext='.pdf': gọi `detect_scanned_pdf(saved_path)` SYNCHRONOUSLY. Nếu `is_scanned=True`:
   - INSERT documents row với `status='failed_unsupported'`, `error_message='PDF scan chưa hỗ trợ trong M2. Khuyến nghị: chuyển sang DOCX.'`, `last_heartbeat=NOW()` (bootstrap WARNING #7), `created_at=NOW()`, `updated_at=NOW()`.
   - **A4: KHÔNG return doc_id cho router add_task** — raise UnsupportedFormatError trước khi return → router catch + trả 415 (KHÔNG add BackgroundTask).
   - Raise `UnsupportedFormatError` với attribute `scanned=True`.
4. Else (ext non-PDF hoặc PDF text-only): INSERT documents row với `status='pending'`, `last_heartbeat=NOW()` bootstrap (WARNING #7), timestamps dùng SQL `NOW()` server-side (BLOCKER #4 — KHÔNG truyền Python timestamp).
5. **A4: KHÔNG SQL `SELECT pg_notify(:channel, :payload)`** — cocoindex 1.0.3 source `PgTableSource.fetch_rows()` KHÔNG listen NOTIFY (RESEARCH.md Section 4.4). Trigger qua FastAPI BackgroundTasks ở router level (Plan 04-04 task 03).
6. Commit transaction (auto qua FastAPI `get_session` Depends).
7. Return `DocumentUploadResponse(document_id=str(doc_id), status="pending", filename=original_filename)`.

**Helper `trigger_cocoindex_update` (A4 module-level — router gọi qua background_tasks.add_task):**

Sau khi `cocoindex_app.update_blocking()` xong, count chunks WHERE document_id=:id; >0 → status='completed' + chunk_count=:count + last_heartbeat=NOW(); =0 → status='failed' + error_message='cocoindex flow generated 0 chunks' + last_heartbeat=NOW(). Exception → status='failed' + error_message='cocoindex update failed: {exc}' + last_heartbeat=NOW().

**Paste-ready (REVISION 2 — replace toàn bộ revision 1 paste-ready):**

```python
"""Document service — Plan 04-04 REVISION 2 (INGEST-04, INGEST-05).

Layer between router và DB / FileStore. Hành vi chính:
- Validate ext whitelist (R4).
- save file via FileStore.
- BLOCKER #3 (R4 early-detect strategy A): nếu .pdf → detect_scanned_pdf
  SYNCHRONOUSLY; scanned → INSERT status='failed_unsupported' + raise
  UnsupportedFormatError(scanned=True). Router catch + KHÔNG add BackgroundTask
  cho row scanned (A4 — scanned final).
- INSERT documents với last_heartbeat=NOW() bootstrap (WARNING #7 — watchdog
  Plan 04-05 chỉ flip nếu NOT NULL + stale; tránh false-flip mọi processing row).
- Timestamps dùng SQL NOW() server-side (BLOCKER #4 — KHÔNG datetime.utcnow
  deprecated Python 3.12).
- A4 (REVISION 2): KHÔNG còn pg_notify('documents_notify', :doc_id) — cocoindex
  1.0.3 source `PgTableSource.fetch_rows()` KHÔNG listen NOTIFY natively
  (RESEARCH.md Section 4.4 + Open Question Q1). Trigger qua FastAPI BackgroundTasks
  ở router level (Plan 04-04 task 03 router) gọi
  `add_task(trigger_cocoindex_update, app.state.cocoindex_app, doc_id)`.
- GET single row qua id.

Module-level helper trigger_cocoindex_update (A4):
- Chạy await asyncio.to_thread(cocoindex_app.update_blocking) trong background
  (sau response 202 client trả về).
- Sau update_blocking() xong, count chunks WHERE document_id=:id → UPDATE
  documents SET status='completed' chunk_count=:count last_heartbeat=NOW()
  nếu >0; status='failed' nếu =0.
- Exception → status='failed' + error_message + last_heartbeat=NOW().

Plan 04-05 EXTEND: list + delete + watchdog logic.
"""
from __future__ import annotations

import asyncio
import logging
from pathlib import Path
from typing import Any
from uuid import UUID, uuid4

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_engine
from app.schemas.documents import DocumentResponse, DocumentUploadResponse
from app.services.file_extract import (
    ALLOWED_EXTENSIONS,
    UnsupportedFormatError,
    detect_scanned_pdf,
)
from app.services.file_store import FileStore

logger = logging.getLogger(__name__)

SCANNED_PDF_MESSAGE = "PDF scan chưa hỗ trợ trong M2. Khuyến nghị: chuyển sang DOCX."


class DocumentService:
    """Plan 04-04 REVISION 2 ship create + get. Plan 04-05 sẽ extend list + delete."""

    def __init__(
        self,
        db: AsyncSession,
        file_store: FileStore | None = None,
    ) -> None:
        self.db = db
        self.file_store = file_store or FileStore()

    async def create(
        self,
        *,
        hub_id: UUID,
        uploaded_by: UUID | None,
        file_content: bytes,
        original_filename: str,
        mime_type: str | None = None,
    ) -> DocumentUploadResponse:
        """Save file + early-detect scanned PDF + INSERT documents.

        REVISION 2 A4: KHÔNG còn pg_notify — caller (router) trigger cocoindex
        qua BackgroundTasks add_task(trigger_cocoindex_update, app, doc_id).

        Raises:
            UnsupportedFormatError: ext không trong ALLOWED_EXTENSIONS HOẶC PDF scanned (BLOCKER #3).
        """
        ext = Path(original_filename).suffix.lower()
        if ext not in ALLOWED_EXTENSIONS:
            raise UnsupportedFormatError(ext)

        # 1) Save file → file_path UUID
        saved_path = self.file_store.save(file_content, original_filename)
        file_size = len(file_content)

        # 2) BLOCKER #3 — early-detect scanned PDF SYNCHRONOUSLY (strategy A).
        is_scanned = False
        if ext == ".pdf":
            try:
                is_scanned = detect_scanned_pdf(saved_path)
            except Exception as e:  # noqa: BLE001 — pypdf parse fail
                logger.warning(
                    "detect_scanned_pdf_failed: %s — treat as non-scanned (defensive)",
                    e,
                )
                is_scanned = False

        doc_id = uuid4()

        if is_scanned:
            # PDF scanned → INSERT status='failed_unsupported'.
            # Timestamps dùng SQL NOW() server-side (BLOCKER #4).
            # last_heartbeat=NOW() bootstrap để consistent với non-scanned path (WARNING #7).
            # A4: KHÔNG add BackgroundTask cho row scanned (router catch raise → 415).
            await self.db.execute(
                text(
                    "INSERT INTO documents "
                    "(id, hub_id, uploaded_by, filename, file_path, mime_type, "
                    "file_size_bytes, status, error_message, attempts, chunk_count, "
                    "last_heartbeat, created_at, updated_at) "
                    "VALUES (:id, :hub, :uploader, :name, :path, :mime, :size, "
                    "'failed_unsupported', :err, 0, 0, NOW(), NOW(), NOW())"
                ),
                {
                    "id": str(doc_id),
                    "hub": str(hub_id),
                    "uploader": str(uploaded_by) if uploaded_by else None,
                    "name": original_filename,
                    "path": str(saved_path),
                    "mime": mime_type,
                    "size": file_size,
                    "err": SCANNED_PDF_MESSAGE,
                },
            )
            logger.info(
                "document_failed_unsupported_scanned_pdf: id=%s hub_id=%s filename=%s",
                doc_id,
                hub_id,
                original_filename,
            )
            # Raise để router trả 415 envelope (KHÔNG add BackgroundTask cho row scanned).
            err = UnsupportedFormatError(".pdf")
            err.args = (SCANNED_PDF_MESSAGE,)
            err.ext = ".pdf"  # type: ignore[attr-defined]
            err.scanned = True  # type: ignore[attr-defined]
            raise err

        # 3) Non-scanned path — INSERT row status='pending' + last_heartbeat=NOW() bootstrap.
        #    Timestamps dùng SQL NOW() server-side (BLOCKER #4 — KHÔNG datetime.utcnow).
        #    last_heartbeat=NOW() bootstrap (WARNING #7 — watchdog query chỉ flip nếu
        #    NOT NULL + stale; tránh false-flip mọi processing row).
        #    A4 REVISION 2: KHÔNG còn pg_notify — caller (router) add BackgroundTask.
        await self.db.execute(
            text(
                "INSERT INTO documents "
                "(id, hub_id, uploaded_by, filename, file_path, mime_type, "
                "file_size_bytes, status, attempts, chunk_count, last_heartbeat, "
                "created_at, updated_at) "
                "VALUES (:id, :hub, :uploader, :name, :path, :mime, :size, "
                "'pending', 0, 0, NOW(), NOW(), NOW())"
            ),
            {
                "id": str(doc_id),
                "hub": str(hub_id),
                "uploader": str(uploaded_by) if uploaded_by else None,
                "name": original_filename,
                "path": str(saved_path),
                "mime": mime_type,
                "size": file_size,
            },
        )

        # FastAPI get_session() auto-commit on success.
        logger.info(
            "document_created: id=%s hub_id=%s filename=%s size=%d",
            doc_id,
            hub_id,
            original_filename,
            file_size,
        )

        return DocumentUploadResponse(
            document_id=str(doc_id),
            status="pending",
            filename=original_filename,
        )

    async def get(self, document_id: UUID) -> DocumentResponse | None:
        """SELECT 1 document qua id. None nếu không tồn tại."""
        row = (
            await self.db.execute(
                text(
                    "SELECT id, hub_id, uploaded_by, filename, file_path, mime_type, "
                    "file_size_bytes, status, error_message, last_heartbeat, "
                    "attempts, chunk_count, created_at, updated_at "
                    "FROM documents WHERE id = :id"
                ),
                {"id": str(document_id)},
            )
        ).fetchone()
        if row is None:
            return None
        return DocumentResponse(
            id=str(row[0]),
            hub_id=str(row[1]),
            uploaded_by=str(row[2]) if row[2] else None,
            filename=row[3],
            file_path=row[4],
            mime_type=row[5],
            file_size_bytes=row[6],
            status=row[7],
            error_message=row[8],
            last_heartbeat=row[9],
            attempts=row[10],
            chunk_count=row[11],
            created_at=row[12],
            updated_at=row[13],
        )


# === A4 helper module-level — router gọi qua FastAPI BackgroundTasks add_task ===

async def trigger_cocoindex_update(cocoindex_app: Any, doc_id: UUID) -> None:
    """A4 helper — chạy cocoindex update_blocking + set documents status sau khi xong.

    Args:
        cocoindex_app: coco.App instance (Plan 04-03 lưu ở app.state.cocoindex_app).
        doc_id: document_id vừa INSERT (router pass qua add_task args).

    Sequence:
    1. await asyncio.to_thread(cocoindex_app.update_blocking) — chạy cocoindex
       flow blocking trong background thread (KHÔNG block FastAPI event loop).
       Cocoindex re-fetch ALL documents rows + memo skip rows unchanged + process
       row mới (status='pending').
    2. SELECT COUNT(*) FROM chunks WHERE document_id=:id.
    3. Nếu count > 0 → UPDATE documents SET status='completed' chunk_count=:count
       last_heartbeat=NOW() (refresh cho watchdog NULL guard).
    4. Nếu count = 0 → UPDATE documents SET status='failed'
       error_message='cocoindex flow generated 0 chunks' last_heartbeat=NOW().
    5. Exception → UPDATE documents SET status='failed'
       error_message='cocoindex update failed: {exc}' last_heartbeat=NOW().

    Cocoindex 1.0.3 KHÔNG expose per-row callback hook → CẦN strategy này
    (post-update count chunks → set status). Documented inter-plan dependency với
    Plan 04-05 watchdog (5min timeout NULL guard).

    KHÔNG raise — exception swallowed + log + UPDATE failed status (BackgroundTask
    runs sau response client → exception KHÔNG return được lên user).
    """
    if cocoindex_app is None:
        logger.warning(
            "trigger_cocoindex_update_skip: cocoindex_app=None (lifespan setup failed?) doc_id=%s",
            doc_id,
        )
        # Set status='failed' để watchdog/UI phản ánh đúng.
        try:
            engine = get_engine()
            async with engine.begin() as conn:
                await conn.execute(
                    text(
                        "UPDATE documents SET status='failed', "
                        "error_message='cocoindex_app unavailable (lifespan failed)', "
                        "last_heartbeat=NOW(), updated_at=NOW() "
                        "WHERE id = :id"
                    ),
                    {"id": str(doc_id)},
                )
        except Exception as inner:  # noqa: BLE001
            logger.exception("trigger_cocoindex_update_status_set_failed: %s", inner)
        return

    try:
        # 1) Run cocoindex update blocking trong thread executor.
        await asyncio.to_thread(cocoindex_app.update_blocking)
        logger.info("trigger_cocoindex_update_blocking_complete: doc_id=%s", doc_id)

        # 2-4) Count chunks → set status.
        engine = get_engine()
        async with engine.begin() as conn:
            count_row = (
                await conn.execute(
                    text("SELECT COUNT(*) FROM chunks WHERE document_id = :id"),
                    {"id": str(doc_id)},
                )
            ).fetchone()
            count = int(count_row[0]) if count_row else 0

            if count > 0:
                await conn.execute(
                    text(
                        "UPDATE documents SET status='completed', "
                        "chunk_count=:count, last_heartbeat=NOW(), updated_at=NOW() "
                        "WHERE id = :id"
                    ),
                    {"count": count, "id": str(doc_id)},
                )
                logger.info(
                    "trigger_cocoindex_update_completed: doc_id=%s chunks=%d",
                    doc_id,
                    count,
                )
            else:
                await conn.execute(
                    text(
                        "UPDATE documents SET status='failed', "
                        "error_message='cocoindex flow generated 0 chunks', "
                        "last_heartbeat=NOW(), updated_at=NOW() "
                        "WHERE id = :id"
                    ),
                    {"id": str(doc_id)},
                )
                logger.warning(
                    "trigger_cocoindex_update_zero_chunks: doc_id=%s",
                    doc_id,
                )
    except Exception as exc:  # noqa: BLE001 — BackgroundTask exception swallowed
        logger.exception(
            "trigger_cocoindex_update_failed: doc_id=%s exc=%s",
            doc_id,
            exc,
        )
        try:
            engine = get_engine()
            async with engine.begin() as conn:
                await conn.execute(
                    text(
                        "UPDATE documents SET status='failed', "
                        "error_message=:err, last_heartbeat=NOW(), updated_at=NOW() "
                        "WHERE id = :id"
                    ),
                    {
                        "err": f"cocoindex update failed: {exc!s}"[:500],  # truncate long traces
                        "id": str(doc_id),
                    },
                )
        except Exception as inner:  # noqa: BLE001
            logger.exception(
                "trigger_cocoindex_update_status_set_failed: doc_id=%s inner=%s",
                doc_id,
                inner,
            )
```

**Lưu ý:**
- Dùng raw `text()` SQL thay vì SQLAlchemy ORM `Document(...)` để KHÔNG phụ thuộc model init signature — flexible cho Plan 04-05 join.
- **BLOCKER #4**: Timestamps dùng SQL `NOW()` server-side ở TẤT CẢ INSERT/UPDATE (`created_at`, `updated_at`, `last_heartbeat`) — KHÔNG `datetime.utcnow()`.
- **WARNING #7**: `last_heartbeat = NOW()` bootstrap CẢ 2 path (scanned + non-scanned) cho consistency. `trigger_cocoindex_update` cũng refresh `last_heartbeat=NOW()` sau update_blocking.
- **BLOCKER #3**: `is_scanned` path raise `UnsupportedFormatError` với `scanned=True` attribute → router check `getattr(err, 'scanned', False)` để return 415.
- **A4**: KHÔNG `SELECT pg_notify(:channel, :payload)` — REMOVED. Cocoindex 1.0.3 source KHÔNG listen NOTIFY (RESEARCH.md Section 4.4 + Open Question Q1).
- `trigger_cocoindex_update` open NEW connection qua `get_engine().begin()` — KHÔNG share `self.db` AsyncSession (BackgroundTask runs sau response → AsyncSession đã close).
- `trigger_cocoindex_update` KHÔNG raise — exception swallowed + log + UPDATE failed status (BackgroundTask exception KHÔNG return được lên user).
</action>
<read_first>
- Hub_All/api/app/models/document.py
- Hub_All/api/app/services/file_extract.py (Plan 04-02 — verify detect_scanned_pdf public exposed)
- Hub_All/api/app/services/file_store.py (Plan 04-02)
- Hub_All/api/app/schemas/documents.py (Task 01 output)
- Hub_All/api/app/db/session.py (verify get_engine signature)
- Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-COCOINDEX-API-RESEARCH.md (Section 4.4 + Section 9 Q1 LISTEN/NOTIFY confirm)
- Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-03-PLAN.md (Plan 04-03 task 02 — verify get_cocoindex_app + main.py app.state.cocoindex_app pattern)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/services/documents_service.py` tồn tại.
- `grep -c 'class DocumentService' Hub_All/api/app/services/documents_service.py` ≥ 1.
- `grep -c 'async def create' Hub_All/api/app/services/documents_service.py` ≥ 1.
- `grep -c 'async def get' Hub_All/api/app/services/documents_service.py` ≥ 1.
- `grep -c 'UnsupportedFormatError' Hub_All/api/app/services/documents_service.py` ≥ 1.
- `grep -c 'ALLOWED_EXTENSIONS' Hub_All/api/app/services/documents_service.py` ≥ 1.
- `grep -c "'pending'" Hub_All/api/app/services/documents_service.py` ≥ 1.
- **A4 REVISION 2 — KHÔNG còn pg_notify:**
  - `grep -c 'pg_notify' Hub_All/api/app/services/documents_service.py` = 0 (REMOVED revision 1).
  - `grep -c 'NOTIFY_CHANNEL' Hub_All/api/app/services/documents_service.py` = 0 (REMOVED revision 1).
  - `grep -c "documents_notify" Hub_All/api/app/services/documents_service.py` = 0.
- **A4 REVISION 2 — trigger_cocoindex_update helper:**
  - `grep -c 'async def trigger_cocoindex_update' Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c 'cocoindex_app.update_blocking' Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c 'asyncio.to_thread' Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c "status='completed'" Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c "status='failed'" Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c 'SELECT COUNT.*FROM chunks WHERE document_id' Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c 'last_heartbeat=NOW()' Hub_All/api/app/services/documents_service.py` ≥ 2 (UPDATE completed + UPDATE failed).
- **BLOCKER #3 — early-detect scanned PDF (carry-over):**
  - `grep -c 'detect_scanned_pdf' Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c "from app.services.file_extract import" Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c "'failed_unsupported'" Hub_All/api/app/services/documents_service.py` ≥ 1.
  - `grep -c 'SCANNED_PDF_MESSAGE' Hub_All/api/app/services/documents_service.py` ≥ 2 (define + use).
  - `grep -c 'scanned = True' Hub_All/api/app/services/documents_service.py` ≥ 1.
- **BLOCKER #4 — SQL NOW() KHÔNG datetime.utcnow:**
  - `grep -c 'datetime.utcnow' Hub_All/api/app/services/documents_service.py` = 0.
  - `grep -c 'NOW()' Hub_All/api/app/services/documents_service.py` ≥ 5 (INSERT scanned + INSERT pending + UPDATE completed + UPDATE failed + UPDATE exception).
- **WARNING #7 — last_heartbeat bootstrap + refresh:**
  - `grep -c 'last_heartbeat' Hub_All/api/app/services/documents_service.py` ≥ 4 (column + bootstrap scanned + bootstrap pending + refresh trigger_cocoindex_update).
- **Plan 04-02 prerequisite** — `detect_scanned_pdf` expose:
  - `grep -c 'def detect_scanned_pdf' Hub_All/api/app/services/file_extract.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.services.documents_service import DocumentService, SCANNED_PDF_MESSAGE, trigger_cocoindex_update; print(SCANNED_PDF_MESSAGE[:20], callable(trigger_cocoindex_update))"` exit 0.
- `cd Hub_All/api && uv run python -c "from app.services.file_extract import detect_scanned_pdf; print(callable(detect_scanned_pdf))"` exit 0 output `True`.
- `cd Hub_All/api && uv run ruff check app/services/documents_service.py app/services/file_extract.py` exit 0.
- `cd Hub_All/api && uv run mypy app/services/documents_service.py app/services/file_extract.py` exit 0.
</acceptance_criteria>
</task>

<task id="03">
<action>
Tạo `Hub_All/api/app/routers/documents.py` + `__init__.py` + EXTEND `Hub_All/api/app/main.py` mount router. Ship WARNING #6 Content-Length pre-check + BLOCKER #3 415 handler cho scanned PDF + **A4 BackgroundTasks trigger** (REVISION 2 user BLOCKING confirmed).

**File 1: `Hub_All/api/app/routers/__init__.py`**

```python
"""Routers package — APIRouter modular cho mỗi domain.

Phase 4 ship documents_router (INGEST-04, INGEST-05).
Phase 5 sẽ thêm hubs_router, users_router, audit_router.
"""
from __future__ import annotations

from app.routers.documents import router as documents_router

__all__ = ["documents_router"]
```

**File 2: `Hub_All/api/app/routers/documents.py`** (REVISION 2 — A4 BackgroundTasks)

```python
"""Documents router — Plan 04-04 REVISION 2 (INGEST-04, INGEST-05).

2 endpoint:
    POST /api/documents/upload   — multipart + admin → 202 document_id
                                   + BackgroundTask trigger_cocoindex_update (A4)
    GET  /api/documents/:id      — Bearer → 200 DocumentResponse / 404

Plan 04-05 EXTEND: GET / (list) + DELETE /:id.

Auth pattern:
- POST upload: require_role("admin") — chỉ admin upload (D6 frontend admin-only).
- GET :id: get_current_user — viewer/editor/admin đều xem được (RBAC khi list
  filter hub_id sẽ enforce ở Plan 04-05 — Phase 5 chính thức HUB-02).

Mitigations:
- WARNING #6 DoS: Content-Length header pre-check TRƯỚC file.read() để chống
  attacker upload 10GB buffer toàn bộ.
- BLOCKER #3 R4: UnsupportedFormatError với scanned=True attribute → 415 với
  message rõ "PDF scan chưa hỗ trợ trong M2".
- A4 (REVISION 2 — user BLOCKING confirmed): Sau service.create() OK (KHÔNG
  raise UnsupportedFormatError) → background_tasks.add_task(
  trigger_cocoindex_update, request.app.state.cocoindex_app, doc_id). Cocoindex
  1.0.3 KHÔNG support source LISTEN/NOTIFY → must trigger update_blocking
  synchronously per upload (Plan 04-03 lifespan đã setup app.state.cocoindex_app).
"""
from __future__ import annotations

import logging
from uuid import UUID

from fastapi import (
    APIRouter,
    BackgroundTasks,
    Depends,
    File,
    Form,
    Request,
    UploadFile,
    status,
)
from fastapi.responses import JSONResponse
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.dependencies import get_current_user, require_role
from app.db.session import get_session
from app.models.auth import User
from app.pkg import response as resp
from app.services.documents_service import (
    DocumentService,
    trigger_cocoindex_update,
)
from app.services.file_extract import UnsupportedFormatError

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/api/documents", tags=["documents"])

# Limit upload size = 50MB (DoS mitigation T-04-02-03 + T-04-04-01 + WARNING #6).
MAX_UPLOAD_SIZE_BYTES = 50 * 1024 * 1024


def get_document_service(
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> DocumentService:
    return DocumentService(db=db)


@router.post("/upload")
async def upload(
    request: Request,
    background_tasks: BackgroundTasks,
    file: UploadFile = File(...),  # noqa: B008
    hub_id: str = Form(...),
    user: User = Depends(require_role("admin")),  # noqa: B008
    service: DocumentService = Depends(get_document_service),  # noqa: B008
) -> JSONResponse:
    """POST /api/documents/upload — multipart file + hub_id → 202.

    R4 mitigation: ext không trong whitelist → 415 UNSUPPORTED_FORMAT.
    BLOCKER #3: PDF scanned → 415 UNSUPPORTED_FORMAT + row failed_unsupported
    + KHÔNG add BackgroundTask cho row scanned (A4 — final).
    WARNING #6: Content-Length > 50MB → 413 trước khi file.read().
    A4 (REVISION 2): Sau service.create() OK → background_tasks.add_task(
    trigger_cocoindex_update, request.app.state.cocoindex_app, doc_id) — chạy
    cocoindex_app.update_blocking() trong background thread sau response 202.
    """
    # === WARNING #6 — Content-Length pre-check TRƯỚC file.read() ===
    content_length_header = request.headers.get("content-length")
    if content_length_header:
        try:
            content_length = int(content_length_header)
            if content_length > MAX_UPLOAD_SIZE_BYTES:
                return resp.bad_request(
                    message=f"File vượt quá giới hạn {MAX_UPLOAD_SIZE_BYTES // (1024*1024)}MB",
                    code="FILE_TOO_LARGE",
                )
        except (TypeError, ValueError):
            pass  # invalid header → tiếp tục, sẽ check sau read

    # Validate hub_id UUID format
    try:
        hub_uuid = UUID(hub_id)
    except ValueError:
        return resp.bad_request(
            message=f"hub_id không phải UUID hợp lệ: {hub_id!r}",
            code="INVALID_HUB_ID",
        )

    # Read file content (đã pre-check Content-Length — buffer ≤ 50MB).
    content = await file.read()
    if len(content) > MAX_UPLOAD_SIZE_BYTES:
        # Fallback nếu client KHÔNG gửi Content-Length header (defensive).
        return resp.bad_request(
            message=f"File vượt quá giới hạn {MAX_UPLOAD_SIZE_BYTES // (1024*1024)}MB",
            code="FILE_TOO_LARGE",
        )
    if len(content) == 0:
        return resp.bad_request(
            message="File rỗng — không thể upload",
            code="EMPTY_FILE",
        )

    original_filename = file.filename or "unnamed"
    mime_type = file.content_type

    try:
        result = await service.create(
            hub_id=hub_uuid,
            uploaded_by=user.id,
            file_content=content,
            original_filename=original_filename,
            mime_type=mime_type,
        )
    except UnsupportedFormatError as e:
        # BLOCKER #3 — scanned PDF: service đã INSERT row failed_unsupported.
        # A4: KHÔNG add BackgroundTask cho row scanned.
        # Trả 415 với message phù hợp (SCANNED_PDF_MESSAGE nếu scanned, else default).
        return resp.unsupported_format(message=str(e))

    # A4 REVISION 2 — sau service.create OK (status='pending'), trigger cocoindex
    # qua FastAPI BackgroundTasks. Plan 04-03 lifespan đã setup app.state.cocoindex_app.
    cocoindex_app = getattr(request.app.state, "cocoindex_app", None)
    if cocoindex_app is None:
        logger.warning(
            "upload_no_cocoindex_app_in_state: doc_id=%s — BackgroundTask sẽ set status=failed",
            result.document_id,
        )
    background_tasks.add_task(
        trigger_cocoindex_update,
        cocoindex_app,
        UUID(result.document_id),
    )

    return resp.accepted(data=result.model_dump())


@router.get("/{document_id}")
async def get_by_id(
    document_id: str,
    user: User = Depends(get_current_user),  # noqa: B008
    service: DocumentService = Depends(get_document_service),  # noqa: B008
) -> JSONResponse:
    """GET /api/documents/:id — Bearer → DocumentResponse.

    Plan 04-04 ship: bất kỳ user authenticated đều xem được.
    Plan 04-05 / Phase 5 HUB-02 sẽ enforce hub isolation (user.hub_assignments
    intersect document.hub_id).
    """
    try:
        doc_uuid = UUID(document_id)
    except ValueError:
        return resp.bad_request(
            message=f"document_id không phải UUID hợp lệ: {document_id!r}",
            code="INVALID_DOCUMENT_ID",
        )

    doc = await service.get(doc_uuid)
    if doc is None:
        return resp.not_found(
            message=f"Document {document_id} không tồn tại",
            code="NOT_FOUND",
        )
    return resp.ok(data=doc.model_dump(mode="json"))
```

**File 3: `Hub_All/api/app/main.py`** (EXTEND `create_app`)

Sau dòng `app.include_router(auth_router)` (line ~207), thêm:

```python
    # Mount documents router (Plan 04-04 INGEST-04..05).
    from app.routers import documents_router

    app.include_router(documents_router)
```

**KHÔNG đổi** middleware order, lifespan (Plan 04-03 đã ship cocoindex setup), exception handler, healthz/readyz.

**Note Plan 04-05 forward reference (WARNING #5):** Plan 04-05 sẽ APPEND-ONLY thêm DELETE + LIST endpoint vào `documents.py` + watchdog task vào lifespan main.py. Plan 04-04 chỉ ship POST + GET; KHÔNG modify gì Plan 04-05 đụng.
</action>
<read_first>
- Hub_All/api/app/auth/router.py (Phase 3 reference style)
- Hub_All/api/app/auth/dependencies.py (require_role + get_current_user)
- Hub_All/api/app/pkg/response.py (verify resp.unsupported_format + bad_request + accepted signatures)
- Hub_All/api/app/main.py (vị trí include_router)
- Hub_All/api/app/services/documents_service.py (Task 02 output — trigger_cocoindex_update + DocumentService)
- Hub_All/api/app/services/file_extract.py (UnsupportedFormatError + detect_scanned_pdf)
- Hub_All/api/app/schemas/documents.py (Task 01 output)
- Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-03-PLAN.md (verify Plan 04-03 task 03 đã wire app.state.cocoindex_app trong lifespan)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/routers/__init__.py` tồn tại.
- File `Hub_All/api/app/routers/documents.py` tồn tại.
- `grep -c 'router = APIRouter(prefix="/api/documents"' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c '@router.post("/upload")' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c '@router.get("/{document_id}")' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c 'require_role("admin")' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c 'MAX_UPLOAD_SIZE_BYTES' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c 'unsupported_format' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c 'UnsupportedFormatError' Hub_All/api/app/routers/documents.py` ≥ 1.
- **WARNING #6 — Content-Length pre-check:**
  - `grep -c 'content-length' Hub_All/api/app/routers/documents.py` ≥ 1.
  - `grep -c 'FILE_TOO_LARGE' Hub_All/api/app/routers/documents.py` ≥ 1.
  - `grep -c 'request.headers.get' Hub_All/api/app/routers/documents.py` ≥ 1.
- **A4 REVISION 2 — BackgroundTasks trigger:**
  - `grep -c 'from fastapi import' Hub_All/api/app/routers/documents.py` ≥ 1.
  - `grep -c 'BackgroundTasks' Hub_All/api/app/routers/documents.py` ≥ 2 (import + parameter).
  - `grep -c 'background_tasks: BackgroundTasks' Hub_All/api/app/routers/documents.py` ≥ 1.
  - `grep -c 'background_tasks.add_task' Hub_All/api/app/routers/documents.py` ≥ 1.
  - `grep -c 'trigger_cocoindex_update' Hub_All/api/app/routers/documents.py` ≥ 2 (import + add_task).
  - `grep -c 'request.app.state.cocoindex_app\|app.state.cocoindex_app' Hub_All/api/app/routers/documents.py` ≥ 1.
- `grep -c 'from app.routers import documents_router' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'app.include_router(documents_router)' Hub_All/api/app/main.py` ≥ 1.
- 4 path mới mount: `cd Hub_All/api && uv run python -c "from app.main import create_app; app = create_app(); paths = [r.path for r in app.routes]; assert '/api/documents/upload' in paths; assert '/api/documents/{document_id}' in paths; print('paths OK')"` exit 0.
- `cd Hub_All/api && uv run ruff check app/routers app/main.py` exit 0.
- `cd Hub_All/api && uv run mypy app/routers app/main.py` exit 0.
</acceptance_criteria>
</task>

<task id="04">
<action>
Tạo `Hub_All/api/tests/integration/test_documents_upload.py` — 8 integration test (REVISION 2 — A4 BackgroundTasks pattern + mock cocoindex_app + carry-over BLOCKER #3 + WARNING #4 + #7).

**Test plan REVISION 2:**

1. `test_upload_happy_path_docx` — admin upload DOCX VN runtime-generated → 202 + document_id + status='pending' + row INSERT với last_heartbeat NOT NULL + **mock cocoindex_app.update_blocking → assert add_task được call** (A4).
2. `test_upload_rejects_unsupported_format` — upload .exe → 415 UNSUPPORTED_FORMAT + KHÔNG INSERT row.
3. `test_upload_rejects_scanned_pdf` — upload PDF scanned (mock detect_scanned_pdf return True) → 415 UNSUPPORTED_FORMAT + row status='failed_unsupported' + **mock cocoindex_app.update_blocking KHÔNG được call** (A4 + BLOCKER #3).
4. `test_upload_rejects_viewer_role` — viewer token → 403 FORBIDDEN. **CHỈ assert `body["error"]["code"] == "FORBIDDEN"`** (WARNING #4).
5. `test_upload_rejects_missing_auth` — no Bearer → 401 MISSING_AUTHORIZATION.
6. `test_upload_rejects_invalid_hub_id` — hub_id='not-uuid' → 400 INVALID_HUB_ID.
7. `test_upload_rejects_empty_file` — file rỗng → 400 EMPTY_FILE.
8. `test_get_document_by_id_after_upload` — POST upload → 202; GET với cùng document_id → 200 + status='pending' + chunk_count=0 + last_heartbeat NOT NULL (WARNING #7).

**Strategy mock cocoindex_app:**
- Plan 04-03 lifespan setup `app.state.cocoindex_app = get_cocoindex_app()`. Test override qua `app_with_auth.state.cocoindex_app = MockCocoindexApp()` — Mock có `.update_blocking()` sync method (KHÔNG raise).
- Verify add_task được call qua mock `update_blocking` count call sau response.

**Paste-ready REVISION 2:**

```python
"""Integration test Phase 4 Plan 04-04 REVISION 2 — POST /api/documents/upload + GET /:id.

Sử dụng fixtures Phase 3 Plan 03-05 từ tests/integration/conftest.py:
- postgres_container (scope=module), redis_container, app_with_auth, auth_client
- admin_user, viewer_user, admin_token, viewer_token

KHÔNG cần cocoindex flow chạy thật — mock app.state.cocoindex_app với
.update_blocking() sync method. Test focus vào router + service layer
(INGEST-04 + INGEST-05) + A4 BackgroundTasks pattern. Plan 04-06 sẽ test E2E
ingest pipeline với cocoindex thật.

REVISION 2 (A4 user BLOCKING confirmed):
- test 1 happy: assert mock cocoindex_app.update_blocking đã được call sau response.
- test 3 scanned PDF: assert mock cocoindex_app.update_blocking KHÔNG được call
  (BLOCKER #3 — scanned row final, KHÔNG add BackgroundTask).
- test 4 viewer 403: assert chỉ error.code (KHÔNG message text — WARNING #4).
- test 8: assert last_heartbeat IS NOT NULL sau upload (WARNING #7 bootstrap).
"""
from __future__ import annotations

import io
import uuid
from pathlib import Path
from typing import Any

import httpx
import pytest
from docx import Document as DocxDocument
from sqlalchemy import text


class MockCocoindexApp:
    """Mock coco.App cho test — count update_blocking calls + thread-safe."""

    def __init__(self) -> None:
        self.update_blocking_calls: int = 0

    def update_blocking(self) -> None:
        # Sync method để asyncio.to_thread chạy được.
        self.update_blocking_calls += 1


def _make_docx_vn(tmp_path: Path, name: str = "Khám-bệnh.docx") -> bytes:
    """Tạo DOCX VN sample → bytes."""
    doc = DocxDocument()
    doc.add_paragraph("Mục 1. KHÁM TỔNG QUÁT")
    doc.add_paragraph("Bệnh nhân được khám lâm sàng tỉ mỉ.")
    path = tmp_path / name
    doc.save(str(path))
    return path.read_bytes()


async def _create_hub(app_with_auth: Any, hub_id: uuid.UUID | None = None) -> uuid.UUID:
    """INSERT 1 hub row trực tiếp (Phase 4 chưa có /api/hubs endpoint)."""
    from app.db.session import get_engine

    hid = hub_id or uuid.uuid4()
    engine = get_engine()
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO hubs (id, slug, name, description, is_active, created_at, updated_at) "
                "VALUES (:id, :slug, :name, :desc, TRUE, NOW(), NOW())"
            ),
            {
                "id": str(hid),
                "slug": f"test-hub-{hid.hex[:8]}",
                "name": "Test Hub",
                "desc": "Phase 4 test",
            },
        )
    return hid


@pytest.fixture
def mock_cocoindex_app(app_with_auth: Any) -> MockCocoindexApp:
    """Mock app.state.cocoindex_app cho A4 test — verify update_blocking gọi đúng."""
    mock = MockCocoindexApp()
    app_with_auth.state.cocoindex_app = mock
    return mock


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_happy_path_docx(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    admin_user: dict[str, str],
    app_with_auth: Any,
    tmp_path: Path,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """Admin upload DOCX VN → 202 + envelope shape D6. last_heartbeat bootstrap (WARNING #7).

    REVISION 2 A4: assert mock cocoindex_app.update_blocking đã được call qua
    BackgroundTask sau response. FastAPI BackgroundTasks chạy SAU response trả
    về client → cần await client.post() return rồi mới count.
    """
    hub_id = await _create_hub(app_with_auth)
    content = _make_docx_vn(tmp_path)

    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("Khám-bệnh.docx", io.BytesIO(content), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 202, r.text
    body = r.json()
    assert body["success"] is True
    assert body["error"] is None
    data = body["data"]
    assert "document_id" in data
    assert data["status"] == "pending"
    assert data["filename"] == "Khám-bệnh.docx"

    # Verify row trong DB — last_heartbeat NOT NULL (WARNING #7 bootstrap).
    from app.db.session import get_engine
    engine = get_engine()
    async with engine.begin() as conn:
        row = (
            await conn.execute(
                text(
                    "SELECT status, hub_id, uploaded_by, filename, last_heartbeat "
                    "FROM documents WHERE id = :id"
                ),
                {"id": data["document_id"]},
            )
        ).fetchone()
    assert row is not None
    # Status có thể đã chuyển sang 'completed' hoặc 'failed' nếu BackgroundTask
    # đã chạy xong qua mock (mock generate 0 chunks → 'failed' với message).
    # Test core happy path chỉ verify row tồn tại + last_heartbeat bootstrap.
    assert row[0] in ("pending", "completed", "failed")
    assert str(row[1]) == str(hub_id)
    assert str(row[2]) == admin_user["id"]
    assert row[3] == "Khám-bệnh.docx"
    # WARNING #7: last_heartbeat bootstrap = NOW() — KHÔNG NULL.
    assert row[4] is not None, (
        "WARNING #7 violated — last_heartbeat phải bootstrap=NOW() lúc INSERT, KHÔNG NULL"
    )

    # A4 REVISION 2: verify BackgroundTask đã trigger update_blocking.
    # FastAPI BackgroundTasks chạy SAU response — chờ ngắn để task complete.
    import asyncio
    await asyncio.sleep(0.5)
    assert mock_cocoindex_app.update_blocking_calls >= 1, (
        f"A4 violated — update_blocking phải được call qua BackgroundTask, "
        f"got count={mock_cocoindex_app.update_blocking_calls}"
    )


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_rejects_unsupported_format(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """Upload .exe → 415 UNSUPPORTED_FORMAT (R4 mitigation). A4: KHÔNG trigger cocoindex."""
    hub_id = await _create_hub(app_with_auth)
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("malware.exe", io.BytesIO(b"MZ\x90\x00"), "application/octet-stream")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 415, r.text
    body = r.json()
    assert body["success"] is False
    assert body["error"]["code"] == "UNSUPPORTED_FORMAT"
    # WARNING #4: KHÔNG assert message text cụ thể (envelope có thể đổi).
    # Chỉ verify ext trong message để đảm bảo error contextual.
    assert ".exe" in body["error"]["message"]

    # A4: ext rejected SỚM (trước service.create) → KHÔNG add BackgroundTask.
    import asyncio
    await asyncio.sleep(0.3)
    assert mock_cocoindex_app.update_blocking_calls == 0, (
        f"A4 violated — .exe rejection KHÔNG được trigger cocoindex, "
        f"got count={mock_cocoindex_app.update_blocking_calls}"
    )


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_rejects_scanned_pdf(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    monkeypatch: pytest.MonkeyPatch,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """BLOCKER #3 — Upload scanned PDF → 415 UNSUPPORTED_FORMAT + status='failed_unsupported'.

    A4 REVISION 2: KHÔNG trigger cocoindex cho row scanned (final state).
    """
    hub_id = await _create_hub(app_with_auth)

    # Mock detect_scanned_pdf → return True (PDF scanned).
    # Patch module-level reference TRƯỚC khi service import-resolve.
    from app.services import documents_service, file_extract

    def _fake_detect(path: Path) -> bool:
        return True

    monkeypatch.setattr(file_extract, "detect_scanned_pdf", _fake_detect)
    monkeypatch.setattr(documents_service, "detect_scanned_pdf", _fake_detect)

    # Minimal PDF magic header (router save file, service detect mock → True).
    pdf_bytes = b"%PDF-1.4\n1 0 obj\n<<>>\nendobj\nxref\n0 1\n0000000000 65535 f \ntrailer<<>>\n%%EOF"
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("scan.pdf", io.BytesIO(pdf_bytes), "application/pdf")},
        data={"hub_id": str(hub_id)},
    )
    # BLOCKER #3 — service detect scanned → INSERT failed_unsupported + raise → router 415.
    assert r.status_code == 415, r.text
    body = r.json()
    assert body["success"] is False
    assert body["error"]["code"] == "UNSUPPORTED_FORMAT"

    # Verify DB — phải có row status='failed_unsupported' (service INSERT trước khi raise).
    from app.db.session import get_engine

    engine = get_engine()
    async with engine.connect() as conn:
        rows = (
            await conn.execute(
                text(
                    "SELECT status, filename, error_message FROM documents "
                    "WHERE hub_id = :hub AND filename = 'scan.pdf'"
                ),
                {"hub": str(hub_id)},
            )
        ).fetchall()
    assert len(rows) == 1, f"Phải có đúng 1 row scan.pdf trong DB, got {len(rows)}"
    assert rows[0][0] == "failed_unsupported", (
        f"BLOCKER #3 violated — status phải 'failed_unsupported', got {rows[0][0]}"
    )

    # A4 REVISION 2: scanned row final — router catch UnsupportedFormatError TRƯỚC
    # khi đến block add_task → KHÔNG trigger cocoindex.
    import asyncio
    await asyncio.sleep(0.3)
    assert mock_cocoindex_app.update_blocking_calls == 0, (
        f"A4 violated — scanned PDF KHÔNG được trigger cocoindex, "
        f"got count={mock_cocoindex_app.update_blocking_calls}"
    )


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_rejects_viewer_role(
    auth_client: httpx.AsyncClient,
    viewer_token: str,
    app_with_auth: Any,
    tmp_path: Path,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """Viewer KHÔNG được upload → 403 FORBIDDEN (RBAC require_role admin).

    WARNING #4: CHỈ assert error.code — KHÔNG assert message text cụ thể.
    """
    hub_id = await _create_hub(app_with_auth)
    content = _make_docx_vn(tmp_path)
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {viewer_token}"},
        files={"file": ("ok.docx", io.BytesIO(content), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 403, r.text
    body = r.json()
    assert body["success"] is False
    assert body["error"]["code"] == "FORBIDDEN"
    # WARNING #4: KHÔNG assert message text "Không đủ quyền..." — Phase 3 envelope format có thể đổi.


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_rejects_missing_auth(
    auth_client: httpx.AsyncClient,
    app_with_auth: Any,
    tmp_path: Path,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """No Bearer → 401 MISSING_AUTHORIZATION."""
    hub_id = await _create_hub(app_with_auth)
    content = _make_docx_vn(tmp_path)
    r = await auth_client.post(
        "/api/documents/upload",
        files={"file": ("ok.docx", io.BytesIO(content), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 401, r.text
    body = r.json()
    assert body["error"]["code"] == "MISSING_AUTHORIZATION"


@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_rejects_invalid_hub_id(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    tmp_path: Path,
    app_with_auth: Any,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """hub_id KHÔNG phải UUID → 400 INVALID_HUB_ID."""
    content = _make_docx_vn(tmp_path)
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("x.docx", io.BytesIO(content), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")},
        data={"hub_id": "not-a-uuid"},
    )
    assert r.status_code == 400, r.text
    body = r.json()
    assert body["error"]["code"] == "INVALID_HUB_ID"


@pytest.mark.integration
@pytest.mark.asyncio
async def test_upload_rejects_empty_file(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """File rỗng → 400 EMPTY_FILE."""
    hub_id = await _create_hub(app_with_auth)
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("empty.txt", io.BytesIO(b""), "text/plain")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 400, r.text
    body = r.json()
    assert body["error"]["code"] == "EMPTY_FILE"


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_get_document_by_id_after_upload(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    tmp_path: Path,
    mock_cocoindex_app: MockCocoindexApp,
) -> None:
    """POST upload → 202; GET cùng id → 200 status='pending' + chunk_count=0 + last_heartbeat NOT NULL."""
    hub_id = await _create_hub(app_with_auth)
    content = _make_docx_vn(tmp_path)
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("test.docx", io.BytesIO(content), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 202
    doc_id = r.json()["data"]["document_id"]

    r2 = await auth_client.get(
        f"/api/documents/{doc_id}",
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r2.status_code == 200, r2.text
    body = r2.json()
    assert body["success"] is True
    data = body["data"]
    assert data["id"] == doc_id
    # Status có thể đã chuyển sang 'failed' nếu BackgroundTask mock đã chạy
    # (mock generate 0 chunks → status='failed'). Accept cả 2 state.
    assert data["status"] in ("pending", "failed")
    assert data["attempts"] == 0
    assert data["filename"] == "test.docx"
    # WARNING #7 bootstrap last_heartbeat NOT NULL — serialize qua API.
    assert data["last_heartbeat"] is not None, (
        "WARNING #7 violated — last_heartbeat phải bootstrap=NOW() lúc INSERT"
    )


@pytest.mark.integration
@pytest.mark.asyncio
async def test_get_document_404_unknown_id(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
) -> None:
    """GET unknown UUID → 404 NOT_FOUND."""
    r = await auth_client.get(
        f"/api/documents/{uuid.uuid4()}",
        headers={"Authorization": f"Bearer {admin_token}"},
    )
    assert r.status_code == 404, r.text
    assert r.json()["error"]["code"] == "NOT_FOUND"
```

**Lưu ý executor:**
- `MockCocoindexApp.update_blocking()` sync method để asyncio.to_thread chạy được — KHÔNG dùng async def.
- `mock_cocoindex_app` fixture override `app_with_auth.state.cocoindex_app` với mock — Plan 04-03 lifespan setup có thể không chạy trong test (testcontainer Postgres chưa lên cocoindex thật).
- Test 1 + 2 + 3 verify call count update_blocking — happy path ≥1, .exe rejection =0, scanned PDF =0 (A4 + BLOCKER #3).
- `await asyncio.sleep(0.5)` cho FastAPI BackgroundTasks complete sau response. 0.5s đủ cho mock task instant + UPDATE chunks=0 → status='failed'.
- Test 1 + 8 status accept cả "pending" + "completed"/"failed" vì BackgroundTask có thể đã set status sau response (mock 0 chunks → 'failed'). E2E Plan 04-06 sẽ test full flow với cocoindex thật.
- `_make_docx_vn` runtime-generate DOCX qua python-docx (KHÔNG commit binary).
- Marker `@pytest.mark.critical` cho 5 test core (happy + 415 ext + 415 scanned + 403 + GET after upload).
</action>
<read_first>
- Hub_All/api/tests/integration/conftest.py (fixtures Plan 03-05)
- Hub_All/api/tests/integration/test_auth_login.py (test style reference)
- Hub_All/api/tests/integration/test_rbac_dependency.py (RBAC test pattern)
- Hub_All/api/app/routers/documents.py (Task 03 output)
- Hub_All/api/app/services/documents_service.py (Task 02 output — trigger_cocoindex_update)
- Hub_All/api/app/services/file_extract.py (detect_scanned_pdf — Task 02 prerequisite)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/test_documents_upload.py` tồn tại.
- `grep -c 'async def test_upload_happy_path_docx' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_upload_rejects_unsupported_format' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- **BLOCKER #3 verify test:**
  - `grep -c 'async def test_upload_rejects_scanned_pdf' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
  - `grep -c "failed_unsupported" Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
  - `grep -c "monkeypatch.setattr" Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- **A4 REVISION 2 verify test (mock cocoindex_app):**
  - `grep -c 'class MockCocoindexApp' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
  - `grep -c 'update_blocking_calls' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 3 (happy ≥1 + ext rejection =0 + scanned =0).
  - `grep -c 'def mock_cocoindex_app' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
  - `grep -c 'app_with_auth.state.cocoindex_app' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_upload_rejects_viewer_role' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_upload_rejects_missing_auth' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_upload_rejects_invalid_hub_id' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_upload_rejects_empty_file' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_get_document_by_id_after_upload' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c 'async def test_get_document_404_unknown_id' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 1.
- `grep -c '@pytest.mark.critical' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 5.
- `grep -c 'last_heartbeat' Hub_All/api/tests/integration/test_documents_upload.py` ≥ 2 (assert NOT NULL test 1 + test 8).
- `cd Hub_All/api && uv run pytest tests/integration/test_documents_upload.py -v` PASS ≥ 8 test (Docker Desktop yêu cầu).
- `cd Hub_All/api && uv run ruff check tests/integration/test_documents_upload.py` exit 0.
</acceptance_criteria>
</task>

## Verification

- `cd Hub_All/api && uv run pytest tests/integration/test_documents_upload.py -v` PASS ≥ 8 test.
- `cd Hub_All/api && uv run python -c "from app.services.documents_service import DocumentService, trigger_cocoindex_update, SCANNED_PDF_MESSAGE; print(callable(trigger_cocoindex_update))"` exit 0.
- `cd Hub_All/api && uv run ruff check app/routers app/services/documents_service.py app/main.py tests/integration/test_documents_upload.py` exit 0.
- `cd Hub_All/api && uv run mypy app/routers app/services/documents_service.py app/main.py` exit 0.
- Re-run Phase 3 full pytest suite NO REGRESS: `cd Hub_All/api && uv run pytest tests/ -v -m critical` PASS toàn bộ critical (29 Phase 3 + Plan 04-04 ≥ 5).

## Threat Model

**Trust boundaries:**

| Boundary | Description |
|---|---|
| HTTP multipart upload (UploadFile) → file_store + asyncpg INSERT | Untrusted user input. R4 mitigation: ext whitelist + scanned PDF early-detect. WARNING #6: Content-Length pre-check chống DoS 10GB buffer. |
| service.create → DocumentService → asyncpg INSERT | Trusted layer. SQL parametrize qua text() + bind params (KHÔNG SQL injection). last_heartbeat=NOW() bootstrap (WARNING #7). |
| router success → BackgroundTasks.add_task(trigger_cocoindex_update, app, doc_id) → cocoindex_app.update_blocking | A4 user BLOCKING confirmed. Plan 04-03 setup app.state.cocoindex_app. asyncio.to_thread wrap → KHÔNG block FastAPI event loop. Cocoindex 1.0.3 trusted. |
| trigger_cocoindex_update → asyncpg.Pool (engine.begin()) → UPDATE documents status | Open NEW connection (KHÔNG share AsyncSession đã close). Trusted. |

**STRIDE Threat Register:**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-04-01 | D (Denial of service) | Upload 10GB file → buffer toàn bộ vào memory → OOM | mitigate | WARNING #6 fix — `request.headers.get('content-length')` pre-check trước `await file.read()`. Fallback len(content) check sau read. MAX_UPLOAD_SIZE_BYTES = 50MB. |
| T-04-04-02 | T (Tampering) | Attacker upload .exe rename .docx → bypass whitelist | accept | Whitelist check theo `Path(filename).suffix.lower()` — ext-based. Magic byte check defer Phase 10 HARD-04. M2 admin-only upload (require_role admin) → trusted user. |
| T-04-04-03 | I (Information disclosure) | error_message leak file_path absolute → reveal server filesystem layout | accept | error_message từ exception (e.g. "PDF parse failed: /tmp/file_store/abc.pdf"). Truncate length 500 chars. M2 admin-only access GET endpoint → trusted. Phase 10 HARD-04 redact paths. |
| T-04-04-04 | E (Elevation of privilege) | Viewer call POST /upload → bypass admin RBAC | mitigate | `Depends(require_role("admin"))` Phase 3 enforce 403. Test `test_upload_rejects_viewer_role` verify. |
| T-04-04-05 | R (Repudiation) | Upload không ghi audit log → admin có thể chối | accept | Plan 5 AUX-01 sẽ implement audit logger async. M2a accept gap. |
| T-04-04-06 (REVISION 2 NEW) | D (Denial of service) | BackgroundTask trigger_cocoindex_update tốn lâu → multiple concurrent uploads → cocoindex_app.update_blocking() race | accept | M2 single uvicorn worker. asyncio.to_thread serialize qua thread pool default size. Multi-worker race documented Plan 10 hardening. |
| T-04-04-07 (REVISION 2 NEW) | I (Information disclosure) | trigger_cocoindex_update exception → error_message ghi traceback đầy đủ vào documents.error_message → reveal stack | mitigate | Truncate `f"cocoindex update failed: {exc!s}"[:500]` — giới hạn 500 chars. Stack trace KHÔNG leak vào DB. Full traceback chỉ trong logger.exception (server log). |

## Output

Sau khi complete tạo `Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-04-SUMMARY.md` ghi rõ:
- 8 file modified/created + ≥4 commit hash atomic.
- **REVISION 2 — A4 BackgroundTasks** (user BLOCKING confirmed): KHÔNG còn `pg_notify('documents_notify', :doc_id)` — replaced bằng FastAPI BackgroundTasks `add_task(trigger_cocoindex_update, app.state.cocoindex_app, doc_id)`.
- POST /api/documents/upload (admin) → 202 + INSERT documents row + last_heartbeat=NOW() bootstrap (WARNING #7) + BackgroundTask trigger cocoindex (A4).
- GET /api/documents/:id (any auth) → 200 DocumentResponse / 404.
- Module-level helper `trigger_cocoindex_update` ship: `await asyncio.to_thread(cocoindex_app.update_blocking)` → SELECT COUNT chunks → UPDATE documents status='completed'/'failed' + last_heartbeat=NOW() refresh (cho watchdog NULL guard).
- BLOCKER #3 (R4 scanned PDF): synchronous early-detect + INSERT failed_unsupported + raise UnsupportedFormatError + KHÔNG add BackgroundTask. Test 3 PASS.
- BLOCKER #4 (datetime): SQL NOW() server-side cho mọi INSERT/UPDATE timestamps.
- WARNING #6 (DoS): Content-Length pre-check trước file.read().
- WARNING #7 (NULL heartbeat): bootstrap NOW() + refresh trong trigger_cocoindex_update.
- Test integration 8/8 PASS với mock MockCocoindexApp.
- Sẵn sàng cho Plan 04-05 REVISION 2 (watchdog 5min timeout NULL guard + status helper đã có ở Plan 04-04).
</content>
</invoke>