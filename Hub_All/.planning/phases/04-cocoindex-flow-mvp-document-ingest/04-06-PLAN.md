---
plan: 06
phase: 4
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - Hub_All/api/tests/integration/test_ingest_e2e.py
  - Hub_All/docs/m2a-exit-gate-demo.md
  - Hub_All/api/scripts/m2a_demo.sh
autonomous: false
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04, INGEST-05]
revision: 2
revision_reason: |
  Plan 04-03 REVISION 2 đã rewrite cocoindex 1.0.3 actual API (coco.App pattern,
  KHÔNG có LISTEN/NOTIFY). Plan 04-04 REVISION 2 đã ship A4 BackgroundTasks
  trigger + trigger_cocoindex_update helper. Plan 04-05 REVISION 2 đã adjust
  watchdog 5min timeout.

  Plan 04-06 REVISION 2 RIPPLE-CHECK adjustments:
  - E2E test BỎ pattern poll `pending → processing → completed` qua LISTEN/NOTIFY
    (revision 1) — thay bằng pattern: upload → 202 → wait BackgroundTasks finish
    → assert status='completed' + chunk_count > 0.
  - Test có thể trigger sync `await app_with_lifespan.state.cocoindex_app.update_blocking()`
    trực tiếp sau upload nếu BackgroundTask không complete đủ nhanh (E2E timing).
  - Test 2 (scanned PDF) GIỮ NGUYÊN từ revision 1 — Plan 04-04 REVISION 2 vẫn
    early-detect synchronous (BLOCKER #3). KHÔNG còn @pytest.mark.xfail.
  - SC2 timing assertion update: A4 BackgroundTasks preserve <5s SLA vì cocoindex
    chạy ngay sau response 202 (KHÔNG poll latency). Document trong VERIFICATION.md
    + SUMMARY: "A4 strategy preserves <5s SLA via BackgroundTasks immediate trigger".
  - Manual demo script `docs/m2a-exit-gate-demo.md` cập nhật Bước 3 (KHÔNG còn
    `make cocoindex-setup` riêng — Plan 04-03 REVISION 2 đã wire vào FastAPI
    lifespan) + AC3 cập nhật A4 strategy + AC4 cập nhật 5min timeout.
  - `scripts/m2a_demo.sh` cập nhật loop poll wait đủ thời gian cho A4
    BackgroundTask + cocoindex update_blocking complete.
must_haves:
  truths:
    - "Integration test E2E `test_ingest_e2e.py` PASS — testcontainers Postgres pgvector + Redis + cocoindex_app real (Plan 04-03 lifespan setup): upload DOCX VN → 202 → BackgroundTask trigger_cocoindex_update chạy → flow extract → chunk → embed (mock LiteLLM) → INSERT chunks → assert documents.status='completed' với chunk_count > 0 trong <60s."
    - "Integration test E2E scanned PDF mock detect_scanned_pdf → 415 envelope error.code='UNSUPPORTED_FORMAT' + row status='failed_unsupported' (BLOCKER #3 fix Plan 04-04 strategy A — router synchronous early-detect; BỎ xfail vì Plan 04-04 REVISION 2 đã wire). KHÔNG đợi cocoindex flow vì service KHÔNG add BackgroundTask cho scanned row."
    - "Integration test E2E content-hash incremental: upload cùng file 2 lần → cocoindex memo cache hit lần 2 (cocoindex 1.0.3 dùng fingerprint diff), chunks table KHÔNG có duplicate entries (D-1 stable_chunk_id uuid5 namespace)."
    - "Manual demo script `docs/m2a-exit-gate-demo.md` chứa 5 bước cụ thể với commands paste-ready: docker compose up → migrate-up → uvicorn (lifespan auto setup cocoindex Plan 04-03 REVISION 2) → curl upload → psql SELECT verify."
    - "Checkpoint:human-verify cuối plan — user chạy demo script thủ công, verify chunks table có rows với hub_id đúng + vector dim=1536, accept → M2a EXIT GATE PASS."
  artifacts:
    - path: Hub_All/api/tests/integration/test_ingest_e2e.py
      provides: "E2E integration test REVISION 2 — upload → BackgroundTask trigger_cocoindex_update → chunks pgvector verify. 3 test ALL PASS (KHÔNG còn xfail; A4 timing pattern thay LISTEN/NOTIFY poll)."
      contains: "def test_e2e_upload_to_chunks"
    - path: Hub_All/docs/m2a-exit-gate-demo.md
      provides: "Manual demo script Vietnamese REVISION 2 — 5 bước M2a EXIT GATE verify với A4 BackgroundTasks pattern + 5min watchdog timeout."
      contains: "M2a EXIT GATE"
    - path: Hub_All/api/scripts/m2a_demo.sh
      provides: "Bash script chạy demo end-to-end (docker + curl + psql) — REVISION 2 wait BackgroundTask đủ thời gian."
      contains: "curl -X POST"
  key_links:
    - from: Hub_All/api/tests/integration/test_ingest_e2e.py
      to: cocoindex_app real instance setup by lifespan (Plan 04-03 REVISION 2)
      via: "LifespanManager → app.state.cocoindex_app started → router BackgroundTasks add_task gọi update_blocking (A4 REVISION 2)"
      pattern: "cocoindex_app|update_blocking"
    - from: Hub_All/api/tests/integration/test_ingest_e2e.py
      to: chunks table pgvector
      via: "psql SELECT vector_dims(vector), hub_id, content_hash FROM chunks"
      pattern: "vector|chunks"
    - from: Hub_All/api/tests/integration/test_ingest_e2e.py::test_e2e_pdf_scanned_failed_unsupported
      to: Hub_All/api/app/services/documents_service.py::create (BLOCKER #3 early-detect)
      via: "monkeypatch detect_scanned_pdf → True → service.create INSERT failed_unsupported + raise UnsupportedFormatError → router 415 + KHÔNG add BackgroundTask"
      pattern: "detect_scanned_pdf|failed_unsupported"
    - from: Hub_All/docs/m2a-exit-gate-demo.md
      to: M2a EXIT GATE in PROJECT.md + ROADMAP.md
      via: "Reference E1-E5 + R3 mitigation"
      pattern: "M2a EXIT GATE"
    - from: Hub_All/api/scripts/m2a_demo.sh
      to: Hub_All/api/Makefile (migrate-up — KHÔNG còn cocoindex-setup vì Plan 04-03 REVISION 2 lifespan auto setup)
      via: "make migrate-up && uvicorn (auto trigger setup_cocoindex via lifespan)"
      pattern: "make migrate-up"
---

# Plan 06: M2a EXIT GATE — E2E integration test + manual demo script (REVISION 2)

## Objective

Kết hợp 8 INGEST-XX requirement thành một flow end-to-end verifiable:

1. **Automated E2E test** (`tests/integration/test_ingest_e2e.py`): testcontainers Postgres pgvector + Redis + cocoindex_app real (Plan 04-03 REVISION 2 lifespan setup) + mock LiteLLM → upload DOCX VN qua HTTP → BackgroundTask trigger_cocoindex_update chạy → SELECT chunks pgvector → assert vector dim=1536, hub_id match.

2. **Manual demo script** (`docs/m2a-exit-gate-demo.md` + `scripts/m2a_demo.sh`): 5 bước paste-ready để user/operator chạy end-to-end với stack thật (docker compose + curl + psql). Đây là **M2a EXIT GATE proxy** — user accept demo = condition để tiếp tục M2b (Phase 5+).

3. **Checkpoint:human-verify** cuối plan: pause để user chạy demo và xác nhận chunks table có data đúng. **Reject demo → STOP M2b, KHÔNG pivot lần 3** (R3 mitigation).

**Tại sao plan riêng cuối phase:**
- Tổng hợp 5 plans trước thành 1 luồng E2E — cần Plan 04-01..05 ship đầy đủ.
- M2a EXIT GATE là **gating decision** — không thể auto-pass. Mandatory human verification (xem checkpoint section).
- E2E test có complexity cao (mock LiteLLM + đợi BackgroundTask cocoindex chạy) → tách riêng để Plan 04-05 không bloat.

**REVISION 2 — A4 BackgroundTasks pattern + cocoindex 1.0.3 ripple:**

- E2E test KHÔNG còn pattern poll `pending → processing → completed` qua LISTEN/NOTIFY (revision 1 assumed). Cocoindex 1.0.3 KHÔNG support source LISTEN/NOTIFY. Plan 04-04 REVISION 2 đã chuyển sang FastAPI BackgroundTasks A4: router upload → 202 → BackgroundTask `trigger_cocoindex_update(cocoindex_app, doc_id)` chạy `await asyncio.to_thread(cocoindex_app.update_blocking)` → count chunks → set status='completed'/'failed'.
- E2E test pattern mới: upload → 202 → poll `GET /api/documents/:id` mỗi 1s, max 60s, đợi `status in {completed, failed, failed_unsupported}` (status được set qua trigger_cocoindex_update sau update_blocking xong).
- Alternative pattern (test fast path): test có thể trigger `await app_with_lifespan.state.cocoindex_app.update_blocking()` trực tiếp sau upload (cùng test thread) thay vì đợi BackgroundTask — dùng nếu BackgroundTask flakiness trong testcontainers env.
- Test 2 (scanned PDF) revision 1 BỎ xfail GIỮ NGUYÊN — Plan 04-04 REVISION 2 vẫn early-detect synchronous (BLOCKER #3). KHÔNG add BackgroundTask cho scanned row → service raise → router 415.
- Test 3 (content-hash incremental) ripple: cocoindex 1.0.3 memo dựa fingerprint của doc_row (KHÔNG content_hash column như cocoindex 0.x). stable_chunk_id (uuid5 từ doc_id+chunk_index) đảm bảo cùng doc_id → cùng chunks (KHÔNG duplicate). Test verify chunks count tuyến tính qua 2 documents khác nhau (KHÔNG bloated >2x baseline).
- Manual demo script REVISION 2:
  - Bước 2 cập nhật: `make migrate-up` (KHÔNG còn `make cocoindex-setup` riêng — Plan 04-03 REVISION 2 lifespan auto chạy `setup_cocoindex` khi uvicorn start).
  - AC3 cập nhật A4 strategy (HTTP 415 expected, BackgroundTask KHÔNG trigger cho scanned).
  - AC4 cập nhật 5min watchdog timeout (Plan 04-05 REVISION 2).

**Multi-source coverage audit (revision 2):**

| Source | Item | Plan covering | Status |
|---|---|---|---|
| GOAL (ROADMAP Phase 4) | M2a EXIT GATE — demo upload DOCX → chunks pgvector → SELECT verify | 04-06 task 01 (E2E test) + task 02 (manual demo) + task 03 (bash script) + task 04 (checkpoint) | covered |
| REQ INGEST-01..05 | E2E ingest pipeline upload → flow → chunks (5/5 ROADMAP success criteria verifiable) | 04-06 task 01-04 | covered |
| RESEARCH | Cocoindex 1.0.3 actual API + A4 BackgroundTasks pattern | 04-06 task 01 (E2E test pattern A4) + task 02-03 (demo update flow setup) | covered |
| Decision A4 (REVISION 2 ripple) | E2E test pattern không còn poll LISTEN/NOTIFY — wait BackgroundTask hoặc sync update_blocking | 04-06 task 01 (E2E pattern + alternative sync trigger) | covered |
| Decision Q5 (REVISION 2 ripple) | Demo script reference COCOINDEX_DB env (LMDB) | 04-06 task 02 + task 03 (`.env` config) | covered |
| Plan 04-05 REVISION 2 ripple | Watchdog 5min timeout — AC4 demo update | 04-06 task 02 (AC4 description) | covered |

KHÔNG có item missing.

## Must-Haves

**Truths (5 mục, observable):**

- `cd Hub_All/api && uv run pytest tests/integration/test_ingest_e2e.py -v --timeout=120` PASS 3/3 test (happy + scanned PDF reject (415, NO xfail) + content-hash incremental).
- `bash Hub_All/api/scripts/m2a_demo.sh` chạy thành công trên dev machine — output tail có dòng `M2A_DEMO_PASS chunks=N`.
- File `Hub_All/docs/m2a-exit-gate-demo.md` chứa ít nhất 5 H2 section: "Tiền điều kiện", "Bước 1 — Khởi động stack", "Bước 2 — Migrate (cocoindex auto setup qua lifespan)", "Bước 3 — Upload DOCX VN", "Bước 4 — Verify chunks pgvector", "Bước 5 — Acceptance criteria".
- User chạy demo + accept → plan complete. Reject → STOP với block message hợp lý (xem checkpoint task).
- 5/5 ROADMAP success criteria (xem `ROADMAP.md` Phase 4 line 161-166) ánh xạ verifiable trong demo script.

**Artifacts (3 file):**

- `Hub_All/api/tests/integration/test_ingest_e2e.py` (NEW) — 3 test E2E ALL PASS (no xfail, A4 BackgroundTasks pattern).
- `Hub_All/docs/m2a-exit-gate-demo.md` (NEW) — Manual demo script Vietnamese REVISION 2.
- `Hub_All/api/scripts/m2a_demo.sh` (NEW) — Bash demo automation script.

**Key links (4 connection):**

- `test_ingest_e2e.py::test_e2e_upload_docx_to_chunks_completed` → lifespan cocoindex_app real (Plan 04-03 REVISION 2) + service Plan 04-04 REVISION 2 (BackgroundTask trigger_cocoindex_update) + watchdog Plan 04-05 REVISION 2.
- `test_ingest_e2e.py::test_e2e_pdf_scanned_failed_unsupported` → service.create BLOCKER #3 early-detect (Plan 04-04 REVISION 2) — NO xfail + KHÔNG add BackgroundTask cho scanned.
- `m2a_demo.sh` → `make migrate-up` + `uvicorn` (lifespan auto setup cocoindex Plan 04-03 REVISION 2) + `curl POST /api/documents/upload` + `psql SELECT chunks`.
- `m2a-exit-gate-demo.md` reference `ROADMAP.md` Phase 4 success criteria + `PROJECT.md` EXIT criteria E1-E5.

## Tasks

<task id="01">
<action>
Tạo `Hub_All/api/tests/integration/test_ingest_e2e.py` — 3 test E2E REVISION 2 (A4 BackgroundTasks pattern). BỎ xfail cho test scanned PDF vì Plan 04-04 REVISION 2 đã wire BLOCKER #3 fix.

**Strategy mock LiteLLM:**
- KHÔNG gọi OpenAI/Gemini thật trong CI test (no API key, cost, flakiness).
- Mock `litellm.aembedding` global qua `monkeypatch.setattr` trong fixture — return vector dim 1536 random.

**Strategy đợi A4 BackgroundTask cocoindex (test 1 + 3):**
- Sau POST upload (status 202), poll `GET /api/documents/:id` mỗi 1s, max 60s, đợi `status in {completed, failed, failed_unsupported}` (status được set qua `trigger_cocoindex_update` BackgroundTask sau khi `update_blocking()` xong + count chunks).
- Helper `_wait_until(client, token, doc_id, target_statuses, timeout=60)`.
- Alternative fast path: test có thể gọi `await app_with_lifespan.state.cocoindex_app.update_blocking()` trực tiếp sau upload trong same test thread (KHÔNG đợi BackgroundTask) — dùng nếu BackgroundTask flakiness env testcontainers.

**Strategy test 2 scanned PDF (revision 2 BLOCKER #3 + A4):**
- KHÔNG đợi cocoindex flow / BackgroundTask — service.create đã reject scanned PDF synchronously với 415 + INSERT failed_unsupported + KHÔNG add BackgroundTask.
- Mock `app.services.file_extract.detect_scanned_pdf` + `app.services.documents_service.detect_scanned_pdf` → return True.
- Expected: HTTP 415 + body error.code='UNSUPPORTED_FORMAT' + DB row status='failed_unsupported'.
- KHÔNG `@pytest.mark.xfail`.

**Test plan (revision 2):**

1. `test_e2e_upload_docx_to_chunks_completed` — upload DOCX VN → poll status → completed với chunk_count > 0; SELECT chunks → vector dim=1536, hub_id match, content_hash NOT NULL. A4 BackgroundTask trigger_cocoindex_update set status sau cocoindex_app.update_blocking().
2. `test_e2e_pdf_scanned_failed_unsupported` — mock detect_scanned_pdf → True → upload PDF → router 415 + row status='failed_unsupported' (BLOCKER #3 fix — NO xfail). KHÔNG đợi BackgroundTask (KHÔNG add cho scanned).
3. `test_e2e_content_hash_incremental_dedup` — upload cùng DOCX 2 lần (different document_id) → stable_chunk_id (uuid5 namespace từ doc_id+chunk_index) → chunks count tuyến tính qua 2 documents (KHÔNG bloated >2x baseline).

**Paste-ready REVISION 2:**

```python
"""E2E integration test Phase 4 REVISION 2 — M2a EXIT GATE proxy.

Verify full ingest pipeline: HTTP upload → A4 BackgroundTasks → cocoindex 1.0.3
flow update_blocking → chunks pgvector.

Mock LiteLLM (KHÔNG gọi OpenAI/Gemini thật trong CI):
- monkeypatch litellm.aembedding → return random vector dim 1536.
- Plan 04-02 embedder.py wrap LiteLLM — monkeypatch hit cấp module trước khi
  cocoindex flow execute.

Test 2 (scanned PDF) REVISION 2 — BLOCKER #3 + A4:
- Plan 04-04 REVISION 2 wire detect_scanned_pdf SYNCHRONOUS trong service.create.
- Mock detect_scanned_pdf → True → service 415 + INSERT failed_unsupported +
  KHÔNG add BackgroundTask cho scanned row.
- KHÔNG @pytest.mark.xfail.

A4 BackgroundTasks pattern (REVISION 2):
- Sau POST upload (202), router add BackgroundTask trigger_cocoindex_update.
- BackgroundTask chạy SAU response client → cần poll GET /api/documents/:id
  để đợi status đổi từ 'pending' sang 'completed'/'failed'.
- Cocoindex 1.0.3 update_blocking đồng bộ (KHÔNG có per-row callback) →
  trigger_cocoindex_update count chunks sau update_blocking → set status.

Dependencies:
- Fixtures Phase 3 Plan 05 (postgres_container, redis_container, app_with_auth, auth_client, admin_token).
- Plan 04-03 REVISION 2 lifespan setup cocoindex_app real (KHÔNG mock như Plan 04-04 unit test).
- Plan 04-04 REVISION 2 router + service đã wire BLOCKER #3 + #4 + WARNING #6 + #7 + A4.
- Plan 04-05 REVISION 2 watchdog 5min timeout (KHÔNG ảnh hưởng E2E test < 60s).

Timeout 60s/test — A4 BackgroundTask + cocoindex update_blocking chạy in-process.
"""
from __future__ import annotations

import asyncio
import io
import random
import uuid
from pathlib import Path
from types import SimpleNamespace
from typing import Any
from unittest.mock import AsyncMock

import httpx
import pytest
from docx import Document as DocxDocument
from sqlalchemy import text

E2E_TIMEOUT_SECONDS = 60
E2E_POLL_INTERVAL_SECONDS = 1.0


def _make_docx_vn(tmp_path: Path, content_extra: str = "") -> bytes:
    doc = DocxDocument()
    doc.add_paragraph("Mục 1. KHÁM TỔNG QUÁT")
    doc.add_paragraph("Bệnh nhân được khám lâm sàng. " + content_extra)
    doc.add_paragraph("Mục 2. XÉT NGHIỆM")
    doc.add_paragraph("Làm xét nghiệm máu và siêu âm.")
    path = tmp_path / "khám-bệnh.docx"
    doc.save(str(path))
    return path.read_bytes()


@pytest.fixture
def mock_litellm_embedding(monkeypatch: pytest.MonkeyPatch) -> None:
    """Mock litellm.aembedding global — return vector dim 1536."""

    async def _fake_aembedding(*args: Any, **kwargs: Any) -> Any:
        # Random unit vector dim 1536.
        rng = random.Random(42)
        vec = [rng.uniform(-1.0, 1.0) for _ in range(1536)]
        return SimpleNamespace(data=[{"embedding": vec, "index": 0}])

    monkeypatch.setattr("litellm.aembedding", AsyncMock(side_effect=_fake_aembedding))


async def _create_hub(app_with_auth: Any) -> uuid.UUID:
    from app.db.session import get_engine

    hid = uuid.uuid4()
    engine = get_engine()
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO hubs (id, slug, name, is_active, created_at) "
                "VALUES (:id, :slug, 'h', TRUE, NOW())"
            ),
            {"id": str(hid), "slug": f"hub-{hid.hex[:8]}"},
        )
    return hid


async def _upload_docx(
    client: httpx.AsyncClient, token: str, hub_id: uuid.UUID, content: bytes, name: str
) -> str:
    r = await client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {token}"},
        files={"file": (name, io.BytesIO(content), "application/vnd.openxmlformats-officedocument.wordprocessingml.document")},
        data={"hub_id": str(hub_id)},
    )
    assert r.status_code == 202, r.text
    return r.json()["data"]["document_id"]


async def _wait_until(
    client: httpx.AsyncClient,
    token: str,
    doc_id: str,
    target_statuses: set[str],
    timeout: float = E2E_TIMEOUT_SECONDS,
) -> dict[str, Any]:
    """Poll GET /api/documents/:id mỗi 1s, return body data khi status in target_statuses.

    A4 REVISION 2: chờ BackgroundTask trigger_cocoindex_update chạy xong
    (cocoindex_app.update_blocking + count chunks + UPDATE status).
    """
    elapsed = 0.0
    data: dict[str, Any] = {}
    while elapsed < timeout:
        r = await client.get(
            f"/api/documents/{doc_id}",
            headers={"Authorization": f"Bearer {token}"},
        )
        assert r.status_code == 200, r.text
        data = r.json()["data"]
        if data["status"] in target_statuses:
            return data
        await asyncio.sleep(E2E_POLL_INTERVAL_SECONDS)
        elapsed += E2E_POLL_INTERVAL_SECONDS
    pytest.fail(
        f"Document {doc_id} KHÔNG đạt status {target_statuses} sau {timeout}s — last: {data}"
    )


async def _force_cocoindex_update(app_with_auth: Any) -> None:
    """ALTERNATIVE FAST PATH — gọi cocoindex_app.update_blocking trực tiếp.

    Dùng nếu BackgroundTask flakiness trong testcontainers env. Plan 04-03 REVISION 2
    lifespan setup app.state.cocoindex_app — test có thể trigger sync update_blocking
    + UPDATE status thủ công (replicate trigger_cocoindex_update logic).
    """
    cocoindex_app = getattr(app_with_auth.state, "cocoindex_app", None)
    if cocoindex_app is not None:
        await asyncio.to_thread(cocoindex_app.update_blocking)


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_e2e_upload_docx_to_chunks_completed(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    tmp_path: Path,
    mock_litellm_embedding: None,
) -> None:
    """M2a EXIT GATE proxy 1/3 — upload DOCX VN → chunks pgvector (A4 BackgroundTasks)."""
    hub_id = await _create_hub(app_with_auth)
    content = _make_docx_vn(tmp_path)
    doc_id = await _upload_docx(auth_client, admin_token, hub_id, content, "khám.docx")

    # Đợi A4 BackgroundTask trigger_cocoindex_update hoàn thành.
    data = await _wait_until(
        auth_client,
        admin_token,
        doc_id,
        target_statuses={"completed", "failed", "failed_unsupported"},
    )
    assert data["status"] == "completed", f"Flow failed: {data.get('error_message')}"
    assert data["chunk_count"] > 0, "chunk_count must > 0 after completed"

    # SELECT chunks → verify vector dim + hub_id + content_hash.
    from app.db.session import get_engine

    engine = get_engine()
    async with engine.connect() as conn:
        rows = (
            await conn.execute(
                text(
                    "SELECT id, hub_id, content, content_hash, "
                    "       vector_dims(vector) AS dim "
                    "FROM chunks WHERE document_id = :doc"
                ),
                {"doc": doc_id},
            )
        ).fetchall()
    assert len(rows) > 0, "chunks table empty after completed"
    for r in rows:
        assert str(r[1]) == str(hub_id), f"hub_id mismatch: {r[1]} vs {hub_id}"
        assert r[3] is not None, "content_hash phải set (BLOCKER #2 wire ChunkRow.content_hash)"
        assert r[4] == 1536, f"vector dim sai: {r[4]} (R1 mitigation pin 1536)"
        assert "Mục" in r[2] or "khám" in r[2].lower(), f"chunk content KHÔNG có VN context: {r[2][:50]}"


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_e2e_pdf_scanned_failed_unsupported(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    tmp_path: Path,
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    """M2a EXIT GATE proxy 2/3 — scanned PDF mock → 415 + failed_unsupported.

    BLOCKER #3 fix (Plan 04-04 REVISION 2 strategy A): router/service synchronous
    early-detect. Mock detect_scanned_pdf → True → service.create INSERT
    failed_unsupported + raise UnsupportedFormatError → router 415.

    A4 REVISION 2: KHÔNG add BackgroundTask cho scanned row → KHÔNG cần đợi.

    BỎ @pytest.mark.xfail vì Plan 04-04 REVISION 2 đã wire fix.
    """
    hub_id = await _create_hub(app_with_auth)

    # Mock detect_scanned_pdf cả 2 module reference (module-level import resolution).
    from app.services import documents_service, file_extract

    def _fake_detect(path: Path) -> bool:
        return True

    monkeypatch.setattr(file_extract, "detect_scanned_pdf", _fake_detect)
    monkeypatch.setattr(documents_service, "detect_scanned_pdf", _fake_detect)

    # Minimal PDF magic header (router save file, service detect mock → True).
    pdf_bytes = b"%PDF-1.4\n1 0 obj\n<<>>\nendobj\nxref\n0 1\n0000000000 65535 f \ntrailer<<>>\n%%EOF"
    r = await auth_client.post(
        "/api/documents/upload",
        headers={"Authorization": f"Bearer {admin_token}"},
        files={"file": ("scan.pdf", io.BytesIO(pdf_bytes), "application/pdf")},
        data={"hub_id": str(hub_id)},
    )

    # BLOCKER #3 fix — service đã reject synchronously với 415.
    assert r.status_code == 415, (
        f"BLOCKER #3 violated — scanned PDF phải 415, got {r.status_code}: {r.text}"
    )
    body = r.json()
    assert body["success"] is False
    assert body["error"]["code"] == "UNSUPPORTED_FORMAT"

    # Verify DB — service.create đã INSERT row failed_unsupported TRƯỚC khi raise.
    from app.db.session import get_engine

    engine = get_engine()
    async with engine.connect() as conn:
        rows = (
            await conn.execute(
                text(
                    "SELECT status, filename, error_message FROM documents "
                    "WHERE hub_id = :hub AND filename = 'scan.pdf'"
                ),
                {"hub": str(hub_id)},
            )
        ).fetchall()
    assert len(rows) == 1, f"Phải có đúng 1 row scan.pdf, got {len(rows)}"
    assert rows[0][0] == "failed_unsupported", (
        f"R4 violated — status phải 'failed_unsupported', got: {rows[0][0]}"
    )


@pytest.mark.integration
@pytest.mark.asyncio
async def test_e2e_content_hash_incremental_dedup(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    app_with_auth: Any,
    tmp_path: Path,
    mock_litellm_embedding: None,
) -> None:
    """M2a EXIT GATE proxy 3/3 — upload cùng file 2 lần → chunks tuyến tính (KHÔNG bloated).

    Cocoindex 1.0.3 memo cache: same content fingerprint → re-use embedding cached.
    stable_chunk_id (uuid5 từ doc_id+chunk_index) — different document_id → different
    chunk_id (KHÔNG conflict). Acceptance: total chunks = n1 + n2 (tuyến tính),
    KHÔNG bloated >2x baseline (verify cocoindex memo hoạt động).
    """
    hub_id = await _create_hub(app_with_auth)
    content = _make_docx_vn(tmp_path)

    # Upload lần 1
    doc1 = await _upload_docx(auth_client, admin_token, hub_id, content, "v1.docx")
    data1 = await _wait_until(
        auth_client, admin_token, doc1, target_statuses={"completed", "failed", "failed_unsupported"}
    )
    assert data1["status"] == "completed"
    n1 = data1["chunk_count"]
    assert n1 > 0

    # Upload lần 2 cùng content (khác filename + cùng hub_id)
    doc2 = await _upload_docx(auth_client, admin_token, hub_id, content, "v2.docx")
    data2 = await _wait_until(
        auth_client, admin_token, doc2, target_statuses={"completed", "failed", "failed_unsupported"}
    )
    assert data2["status"] == "completed"

    # SELECT chunks 2 documents — stable_chunk_id deterministic per (doc_id, idx).
    # Acceptance: total chunks = n1 + n2 (mỗi doc có chunks riêng vì document_id khác)
    # NHƯNG embedding KHÔNG re-compute (cocoindex 1.0.3 memo hit qua content fingerprint).
    from app.db.session import get_engine

    engine = get_engine()
    async with engine.connect() as conn:
        c1 = (
            await conn.execute(
                text("SELECT COUNT(*) FROM chunks WHERE document_id = :doc"),
                {"doc": doc1},
            )
        ).scalar()
        c2 = (
            await conn.execute(
                text("SELECT COUNT(*) FROM chunks WHERE document_id = :doc"),
                {"doc": doc2},
            )
        ).scalar()
    assert c1 == n1
    assert c2 == data2["chunk_count"]
    # Acceptance Plan 04-06 REVISION 2: KHÔNG bloated > 2x baseline.
    # Cocoindex 1.0.3 memo skip embed cho identical content fingerprint — chunks
    # vẫn INSERT (different doc_id → different stable_chunk_id) nhưng embed re-use.
    assert c1 + c2 <= 2 * n1, "chunks bloated — cocoindex content-hash memo KHÔNG hoạt động"
```

**Lưu ý executor:**
- Test 2 (`test_e2e_pdf_scanned_failed_unsupported`) **KHÔNG còn `@pytest.mark.xfail`** — Plan 04-04 REVISION 2 đã wire BLOCKER #3 fix strategy A. Mock `detect_scanned_pdf` CẢ 2 module reference (`file_extract` + `documents_service`).
- Test 2 expected status code **415** (KHÔNG 202) — service raise UnsupportedFormatError + KHÔNG add BackgroundTask.
- Test 2 verify DB row status='failed_unsupported' (service đã INSERT trước khi raise).
- KHÔNG đợi `_wait_until` cho test 2 vì service KHÔNG add BackgroundTask cho scanned row → status final ngay sau INSERT.
- Test 1 + 3 dùng `_wait_until` đợi A4 BackgroundTask `trigger_cocoindex_update` chạy xong (poll status).
- Helper `_force_cocoindex_update` có sẵn cho fast path nếu BackgroundTask flakiness — dùng `await _force_cocoindex_update(app_with_auth)` sau upload thay vì `_wait_until`.
- Plan 04-03 REVISION 2 lifespan setup `app.state.cocoindex_app` real — test fixture `app_with_auth` PHẢI start lifespan để cocoindex_app được setup. Verify Plan 03-05 conftest fixture `app_with_auth` dùng LifespanManager.
</action>
<read_first>
- Hub_All/api/tests/integration/conftest.py (fixtures Plan 03-05 + lifespan-aware fixture)
- Hub_All/api/tests/integration/test_documents_upload.py (Plan 04-04 REVISION 2 pattern + MockCocoindexApp)
- Hub_All/api/app/rag/flow.py (Plan 04-03 REVISION 2 — verify ChunkRow + cocoindex_app real)
- Hub_All/api/app/services/documents_service.py (Plan 04-04 REVISION 2 — verify trigger_cocoindex_update + BLOCKER #3 early-detect)
- Hub_All/api/app/main.py (lifespan REVISION 2 — verify setup_cocoindex + app.state.cocoindex_app wire)
- Hub_All/.planning/ROADMAP.md (Phase 4 success criteria line 161-166)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/test_ingest_e2e.py` tồn tại.
- `grep -c 'async def test_e2e_upload_docx_to_chunks_completed' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `grep -c 'async def test_e2e_pdf_scanned_failed_unsupported' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `grep -c 'async def test_e2e_content_hash_incremental_dedup' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `grep -c 'def mock_litellm_embedding' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `grep -c '@pytest.mark.critical' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 2.
- `grep -c 'vector_dims(vector)' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `grep -c 'failed_unsupported' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `grep -c '1536' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1 (verify vector dim R1).
- **BLOCKER #3 follow-up — BỎ xfail:**
  - `grep -c '@pytest.mark.xfail' Hub_All/api/tests/integration/test_ingest_e2e.py` = 0.
  - `grep -c 'r.status_code == 415' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1 (scanned PDF expected 415).
  - `grep -c 'monkeypatch.setattr' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1 (mock detect_scanned_pdf).
- **A4 REVISION 2 — BackgroundTasks pattern + alternative sync trigger:**
  - `grep -c 'async def _wait_until' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1 (poll status).
  - `grep -c 'cocoindex_app' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1 (reference app.state.cocoindex_app).
  - `grep -c 'app.state.cocoindex_app\|app_with_auth.state.cocoindex_app' Hub_All/api/tests/integration/test_ingest_e2e.py` ≥ 1.
- `cd Hub_All/api && uv run pytest tests/integration/test_ingest_e2e.py -v --timeout=120` PASS 3/3 test (NO xfail allowed).
- `cd Hub_All/api && uv run ruff check tests/integration/test_ingest_e2e.py` exit 0.
</acceptance_criteria>
</task>

<task id="02">
<action>
Tạo `Hub_All/docs/m2a-exit-gate-demo.md` — manual demo script tiếng Việt với 5 bước paste-ready (REVISION 2 — A4 BackgroundTasks + Plan 04-03 REVISION 2 lifespan auto setup cocoindex).

```markdown
# M2a EXIT GATE — Manual Demo Script (REVISION 2)

**Phase:** 4 (CocoIndex Flow MVP + Document Ingest)
**Created:** 2026-05-14 (Plan 04-06 REVISION 2 — M2a EXIT GATE proxy)
**Mục đích:** Verify end-to-end pipeline upload DOCX VN → chunks pgvector. Gating decision tiếp tục M2b (Phase 5-10).

**REVISION 2 cập nhật:**
- Cocoindex 1.0.3 (KHÔNG support LISTEN/NOTIFY) — A4 BackgroundTasks trigger qua FastAPI.
- KHÔNG còn `make cocoindex-setup` riêng — Plan 04-03 REVISION 2 wire `setup_cocoindex` vào FastAPI lifespan (auto chạy khi uvicorn start).
- Watchdog timeout 5 phút (Plan 04-05 REVISION 2 — headroom cho cocoindex update_blocking documents lớn).

---

## Tổng quan

Theo `.planning/PROJECT.md` (R3 mitigation) và `.planning/ROADMAP.md` (line 158-159), **M2a EXIT GATE** là gate cuối Phase 4 trước khi cho phép Phase 5+ chạy. User chạy demo này, accept = condition để tiếp tục M2b. Reject = STOP, KHÔNG pivot lần 3 (trừ khi E1-E5 trigger).

5/5 ROADMAP success criteria (Phase 4):

1. POST `/api/documents/upload` (multipart, DOCX VN) → 202 + `document_id`, file lưu `file_store/<uuid>.docx`, row `documents` `status='pending'` + `last_heartbeat=NOW()` bootstrap trong <500ms.
2. Trong <5s sau upload, A4 BackgroundTask `trigger_cocoindex_update` chạy → `cocoindex_app.update_blocking()` → INSERT chunks → UPDATE documents `status='completed'`; `GET /api/documents/:id` trả `chunk_count > 0`, `chunks` table rows với `hub_id` đúng, `vector` dim=1536, `content_hash BYTEA` set.
3. Upload scanned PDF VN → **415** envelope `{success:false, error:{code:"UNSUPPORTED_FORMAT"...}}`; `documents.status='failed_unsupported'` (BLOCKER #3 router synchronous early-detect — Plan 04-04 REVISION 2). KHÔNG add BackgroundTask cho scanned row.
4. Heartbeat watchdog PASS: kill cocoindex worker giữa flow → sau **5 phút** (REVISION 2 timeout — Plan 04-05), status `processing → failed` (chỉ flip nếu `last_heartbeat IS NOT NULL` + stale — WARNING #7).
5. Content-hash incremental: upload cùng file 2 lần (different document_id) → cocoindex memo hit (KHÔNG re-embed) + stable_chunk_id uuid5 deterministic.

---

## Tiền điều kiện

- [ ] Docker Desktop chạy
- [ ] `cd Hub_All && cp api/.env.example api/.env` rồi điền giá trị thực (OPENAI_API_KEY required cho dimensions=1536)
- [ ] `.env` có `COCOINDEX_DB=Hub_All/.cocoindex/state.lmdb` (Q5 — Plan 04-03 REVISION 2)
- [ ] `cd Hub_All/api && uv sync --extra dev` (cài deps)
- [ ] `cd Hub_All/api && make keys` (sinh JWT RS256 keypair — `keys/private.pem` + `keys/public.pem`)
- [ ] DOCX VN sample tại `Hub_All/docs/fixtures/khám-bệnh-mẫu.docx` (operator chuẩn bị thủ công, hoặc dùng python-docx runtime tạo)

## Bước 1 — Khởi động stack Docker Compose

```bash
cd Hub_All
docker compose up -d postgres redis
docker compose ps                          # cả 2 service healthy
docker compose logs postgres | tail -20    # verify CREATE EXTENSION vector OK
```

**Expected:** 2 service `postgres` (image `pgvector/pgvector:pg16`) + `redis` (image `redis:7-alpine`) lên healthy. PostgreSQL log có dòng `CREATE EXTENSION vector` thành công.

## Bước 2 — Migrate (cocoindex auto setup qua lifespan REVISION 2)

```bash
cd Hub_All/api
make migrate-up                            # Alembic upgrade head (0001 + 0002)
make migrate-check                         # No drift
```

**REVISION 2 — KHÔNG còn `make cocoindex-setup` riêng:** Plan 04-03 REVISION 2 wire `setup_cocoindex` vào FastAPI lifespan. Khi uvicorn start (Bước 3), lifespan auto chạy: register @coco.lifespan + coco.start_blocking + cocoindex_app.update_blocking() initial backfill.

**Verify schema:**

```bash
docker exec -it medinet-postgres psql -U medinet -d medinet_central \
  -c "\\d documents"
# → Columns documents có: last_heartbeat, attempts, error_message, status (CHECK enum 5 giá trị).

docker exec -it medinet-postgres psql -U medinet -d medinet_central \
  -c "SELECT indexname FROM pg_indexes WHERE tablename='documents'"
# → ix_documents_status_last_heartbeat tồn tại (Plan 04-01 INGEST-06).

docker exec -it medinet-postgres psql -U medinet -d medinet_central \
  -c "SELECT indexname FROM pg_indexes WHERE tablename='chunks'"
# → ix_chunks_vector_hnsw tồn tại (Migration 0001 — Decision B1 Alembic owns).
```

## Bước 3 — Khởi động FastAPI + seed admin (lifespan auto cocoindex setup REVISION 2)

```bash
cd Hub_All/api
docker compose up -d api                   # hoặc uv run uvicorn app.main:app --port 8080
# Đọc log uvicorn: phải có dòng `cocoindex_setup_ok` + `cocoindex_initial_backfill_complete`
# (Plan 04-03 REVISION 2 lifespan auto setup cocoindex).

curl -s http://localhost:8080/healthz | jq
# → {"success":true,"data":{"status":"ok"},...}

curl -s http://localhost:8080/readyz | jq
# → {"success":true,"data":{"db":"ok","redis":"ok","cocoindex":"ok","jwt":"ok"},...}
```

**Seed admin user qua psql** (Phase 3 mới chỉ có pwdlib, user tạo qua SQL trực tiếp với Go-seed hash đã cross-compat verified Plan 03-03):

```bash
# Hash cho password "Admin@123" (R6 verified hash):
GO_SEED_HASH='$argon2id$v=19$m=65536,t=3,p=4$gpKFndFoG6bcXrx7R60sag$uhxmJMX03O6QlL0WZS+WdRhCOnGEoIpzUrL2vBzRN+c'

docker exec -it medinet-postgres psql -U medinet -d medinet_central -c \
  "INSERT INTO users (email, password_hash, full_name, role, is_active, created_at, updated_at) \
   VALUES ('admin@medinet.vn', '$GO_SEED_HASH', 'System Admin', 'admin', TRUE, NOW(), NOW())"

# Seed hub_y_te
docker exec -it medinet-postgres psql -U medinet -d medinet_central -c \
  "INSERT INTO hubs (slug, name, is_active, created_at) VALUES ('hub_y_te', 'Y Tế', TRUE, NOW()) RETURNING id"
# → ghi nhớ UUID hub_id để dùng bước 4
```

## Bước 4 — Upload DOCX tiếng Việt (A4 BackgroundTasks REVISION 2)

```bash
# Login
TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@medinet.vn","password":"Admin@123"}' \
  | jq -r '.data.access_token')
echo "TOKEN length: ${#TOKEN}"  # phải > 100

HUB_ID="<paste UUID hub_y_te từ bước 3>"

# Upload — Plan 04-04 REVISION 2 router add BackgroundTask trigger_cocoindex_update
curl -s -X POST http://localhost:8080/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@docs/fixtures/khám-bệnh-mẫu.docx" \
  -F "hub_id=$HUB_ID" | jq

# Expected: HTTP 202 + {"success":true,"data":{"document_id":"<uuid>","status":"pending","filename":"khám-bệnh-mẫu.docx"},...}
DOC_ID="<paste document_id>"
```

**Poll status đợi BackgroundTask trigger_cocoindex_update + cocoindex_app.update_blocking + count chunks + UPDATE status:**

```bash
for i in {1..30}; do
  STATUS=$(curl -s "http://localhost:8080/api/documents/$DOC_ID" \
    -H "Authorization: Bearer $TOKEN" | jq -r '.data.status')
  echo "[$i] status=$STATUS"
  if [ "$STATUS" = "completed" ] || [ "$STATUS" = "failed" ] || [ "$STATUS" = "failed_unsupported" ]; then break; fi
  sleep 1
done

curl -s "http://localhost:8080/api/documents/$DOC_ID" \
  -H "Authorization: Bearer $TOKEN" | jq
# → status:"completed", chunk_count > 0
```

**Note REVISION 2:** A4 BackgroundTask chạy SAU response 202 → status mất ~2-5s để chuyển 'pending' → 'completed' (cocoindex_app.update_blocking + count chunks + UPDATE status). 30s timeout đủ headroom cho document VN bình thường.

## Bước 5 — Verify chunks pgvector

```bash
docker exec -it medinet-postgres psql -U medinet -d medinet_central -c \
  "SELECT id, hub_id, vector_dims(vector) AS dim, content_hash IS NOT NULL AS hash_set, \
          LEFT(content, 60) AS preview \
   FROM chunks WHERE document_id = '$DOC_ID' \
   ORDER BY created_at LIMIT 5"
```

**Expected rows:** Mỗi chunk có:
- `hub_id` = UUID hub_y_te (match upload — ChunkRow dataclass wire hub_id từ doc_row).
- `dim` = **1536** (R1 mitigation).
- `hash_set` = **t** (content_hash BYTEA set — ChunkRow.content_hash sha256 wire).
- `preview` chứa nội dung tiếng Việt "Mục", "Khám", "Bệnh"...

**Verify scanned PDF rejection (Bước 5b — BLOCKER #3 verify):**

```bash
# Tạo PDF scanned mock (pypdf parse empty)
# Hoặc dùng scan PDF VN thật từ Medinet sample

curl -s -X POST http://localhost:8080/api/documents/upload \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@docs/fixtures/scan-vn.pdf" \
  -F "hub_id=$HUB_ID" -w "\nHTTP %{http_code}\n" | jq

# Expected: HTTP 415 + envelope {error.code:"UNSUPPORTED_FORMAT"}.
# Plan 04-04 REVISION 2 BLOCKER #3 — router/service synchronous early-detect scanned PDF.
# A4 REVISION 2: KHÔNG add BackgroundTask cho scanned (status final ngay).

# Verify DB row status='failed_unsupported' đã INSERT (service.create INSERT trước khi raise)
docker exec -it medinet-postgres psql -U medinet -d medinet_central -c \
  "SELECT filename, status FROM documents WHERE filename LIKE '%scan%' ORDER BY created_at DESC LIMIT 1"
# → status='failed_unsupported'
```

---

## Acceptance criteria — M2a EXIT GATE

Mỗi tiêu chí PASS = check ✓:

- [ ] **AC1 (INGEST-04)**: Upload DOCX VN → 202 với `document_id`, file lưu `file_store/`, row status='pending' + last_heartbeat=NOW() bootstrap trong <500ms.
- [ ] **AC2 (INGEST-02 + INGEST-03 + A4 REVISION 2)**: <30s sau upload, A4 BackgroundTask trigger_cocoindex_update set status='completed', chunks table có rows hub_id đúng + vector dim=1536 + content_hash set. **A4 strategy preserves <5s SLA** vì cocoindex chạy ngay sau response 202 (KHÔNG poll latency LISTEN/NOTIFY).
- [ ] **AC3 (R4 / INGEST-02 + BLOCKER #3 REVISION 2)**: Scanned PDF → HTTP 415 + status='failed_unsupported' (router synchronous early-detect — Plan 04-04 REVISION 2 strategy A) + KHÔNG add BackgroundTask cocoindex.
- [ ] **AC4 (P8 / INGEST-06 + WARNING #7 + REVISION 2 timeout 5min)**: Kill cocoindex worker (TERM signal vào api container) → sau **5 phút** (Plan 04-05 REVISION 2 timeout configurable Settings.watchdog_timeout_seconds=300), status='processing' với `last_heartbeat IS NOT NULL` (bootstrap) + stale → flip 'failed' (manual stress test — optional cho demo).
- [ ] **AC5 (D-1 / cocoindex memo + stable_chunk_id REVISION 2)**: Upload cùng file 2 lần (different document_id) → cocoindex memo hit qua content fingerprint (KHÔNG re-embed). stable_chunk_id uuid5 namespace deterministic per (doc_id, chunk_index) — citation `[N]` từ ASK Phase 7 ổn định.

---

## Quyết định gate

- ✅ **5/5 AC PASS** → M2a EXIT GATE PASS → tiếp tục Phase 5+ (M2b).
- ⚠️ **3-4/5 PASS** → fix gap, re-run demo trước khi gate. KHÔNG pivot — fix forward.
- ❌ **≤ 2/5 PASS** → Trigger E1 EXIT (PROJECT.md): mở `/gsd-discuss-milestone` re-evaluate. KHÔNG tự pivot.

**Reject demo** = STOP M2b. Operator confirm bằng "reject" với reason cụ thể (which AC failed + symptoms).

---

## Reference

- `.planning/PROJECT.md` (M2a/M2b split + EXIT criteria E1-E5)
- `.planning/ROADMAP.md` Phase 4 line 140-169 (success criteria)
- `.planning/REQUIREMENTS.md` INGEST-01..08 line 45-52
- `.planning/research/PITFALLS.md` (R1/R4/P8/P13/P14)
- `.planning/CONVENTIONS.md` section 3 (APP_NAMESPACE) + section 2 (naming snake_case)
- `.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-COCOINDEX-API-RESEARCH.md` (cocoindex 1.0.3 actual API + A4 decision rationale)
```

**Quan trọng:**
- Sử dụng tiếng Việt cho mọi prose.
- Code blocks bash giữ tiếng Anh (commands).
- DOC tham chiếu được rõ tới các source-of-truth (.planning/*).
- AC2 cập nhật: A4 strategy preserves <5s SLA (REVISION 2 SC2 timing note).
- AC3 cập nhật: HTTP 415 expected + KHÔNG add BackgroundTask cho scanned (BLOCKER #3 REVISION 2).
- AC4 cập nhật: 5 phút timeout (REVISION 2 — Plan 04-05).
- Bước 2 cập nhật: KHÔNG còn `make cocoindex-setup` riêng (Plan 04-03 REVISION 2 lifespan auto).
- Bước 3 cập nhật: log expected `cocoindex_setup_ok` + `cocoindex_initial_backfill_complete`.
- Bước 4 cập nhật: poll loop wait 30s cho BackgroundTask + cocoindex update_blocking complete.
</action>
<read_first>
- Hub_All/.planning/PROJECT.md (R3 + EXIT criteria + M2a/M2b split)
- Hub_All/.planning/ROADMAP.md (Phase 4 success criteria)
- Hub_All/.planning/REQUIREMENTS.md (INGEST-01..08)
- Hub_All/CLAUDE.md (workflow + stack pin + Vietnamese language)
- Hub_All/api/Makefile (migrate-up target — KHÔNG còn cần cocoindex-setup target)
- Hub_All/api/.env.example (env vars — Q5 COCOINDEX_DB)
- Hub_All/api/scripts/generate_keys.sh (key generation reference)
- Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-03-PLAN.md (REVISION 2 lifespan auto setup)
- Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-05-PLAN.md (REVISION 2 watchdog 5min)
</read_first>
<acceptance_criteria>
- File `Hub_All/docs/m2a-exit-gate-demo.md` tồn tại.
- `grep -c "^## Tiền điều kiện" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "^## Bước 1" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "^## Bước 2" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "^## Bước 3" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "^## Bước 4" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "^## Bước 5" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "vector_dims(vector)" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1.
- `grep -c "AC1\|AC2\|AC3\|AC4\|AC5" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 5.
- `grep -c "M2a EXIT GATE" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 3.
- `grep -c "failed_unsupported" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 2.
- `grep -c "1536" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 2.
- `grep -c "HTTP 415" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1 (AC3 BLOCKER #3 REVISION 2).
- **REVISION 2 specific:**
  - `grep -c "REVISION 2\|A4 BackgroundTasks\|BackgroundTask" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 3.
  - `grep -c "5 phút\|5min\|watchdog_timeout_seconds" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1 (AC4 REVISION 2 timeout).
  - `grep -c "lifespan auto\|cocoindex_setup_ok\|setup_cocoindex" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1 (Bước 2/3 REVISION 2).
  - `grep -c "cocoindex-setup" Hub_All/docs/m2a-exit-gate-demo.md` = 0 (REVISION 2 KHÔNG còn make cocoindex-setup riêng).
  - `grep -c "COCOINDEX_DB" Hub_All/docs/m2a-exit-gate-demo.md` ≥ 1 (Q5 LMDB env).
- File length ≥ 100 dòng (chi tiết đầy đủ cho operator).
</acceptance_criteria>
</task>

<task id="03">
<action>
Tạo `Hub_All/api/scripts/m2a_demo.sh` — automate phần test với mock data REVISION 2 (run từ CI hoặc local dev). KHÔNG còn `make cocoindex-setup` (Plan 04-03 REVISION 2 lifespan auto).

```bash
#!/usr/bin/env bash
# Plan 04-06 REVISION 2 — M2a EXIT GATE auto-demo (subset của manual demo).
#
# Script này automate Bước 3-4-5 của docs/m2a-exit-gate-demo.md cho CI smoke test:
# - Login admin → upload DOCX VN runtime-generated → poll status (A4 BackgroundTask) → SELECT chunks.
# - Manual steps (docker compose up + scanned PDF) vẫn cần operator.
#
# REVISION 2 cập nhật:
# - KHÔNG còn make cocoindex-setup (Plan 04-03 REVISION 2 lifespan auto setup).
# - Poll loop wait 30s (A4 BackgroundTask trigger_cocoindex_update + cocoindex_app.update_blocking).
#
# Usage:
#   cd Hub_All/api && bash scripts/m2a_demo.sh
#
# Tiền điều kiện:
# - docker compose up postgres redis api (đã chạy).
# - make migrate-up (đã chạy — Plan 04-03 REVISION 2 lifespan auto setup cocoindex khi uvicorn start).
# - User admin@medinet.vn với hash R6 đã seed.
# - Hub slug 'hub_y_te' đã seed.
#
# Exit codes:
#   0 — PASS (5/5 chunks verified)
#   1 — FAIL (status không completed / chunks count = 0 / vector dim sai)

set -euo pipefail

API_URL="${API_URL:-http://localhost:8080}"
ADMIN_EMAIL="${ADMIN_EMAIL:-admin@medinet.vn}"
ADMIN_PASSWORD="${ADMIN_PASSWORD:-Admin@123}"
HUB_SLUG="${HUB_SLUG:-hub_y_te}"
PG_CONTAINER="${PG_CONTAINER:-medinet-postgres}"
PG_USER="${PG_USER:-medinet}"
PG_DB="${PG_DB:-medinet_central}"
POLL_TIMEOUT="${POLL_TIMEOUT:-30}"  # REVISION 2 — A4 BackgroundTask wait

log() { echo "[$(date +%H:%M:%S)] $*"; }
fail() { echo "FAIL: $*" >&2; exit 1; }

log "1) Health check (verify uvicorn lifespan đã setup cocoindex Plan 04-03 REVISION 2)..."
curl -fsS "$API_URL/healthz" >/dev/null || fail "Healthz fail at $API_URL"
READY=$(curl -fsS "$API_URL/readyz")
if echo "$READY" | grep -q '"cocoindex":"ok"'; then
    log "cocoindex_ready_ok (lifespan setup OK)"
else
    log "cocoindex_not_ready: $READY (REVISION 2 — verify lifespan log có cocoindex_setup_ok)"
fi

log "2) Login admin..."
TOKEN=$(curl -fsS -X POST "$API_URL/api/auth/login" \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"$ADMIN_EMAIL\",\"password\":\"$ADMIN_PASSWORD\"}" \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['data']['access_token'])")
[[ ${#TOKEN} -gt 100 ]] || fail "Token quá ngắn: ${#TOKEN} chars"
log "Token length: ${#TOKEN}"

log "3) Resolve hub_id từ slug=$HUB_SLUG..."
HUB_ID=$(docker exec "$PG_CONTAINER" psql -U "$PG_USER" -d "$PG_DB" -tA \
  -c "SELECT id FROM hubs WHERE slug='$HUB_SLUG' LIMIT 1")
[[ -n "$HUB_ID" ]] || fail "Hub slug=$HUB_SLUG không tồn tại"
log "HUB_ID=$HUB_ID"

log "4) Generate DOCX VN runtime..."
TMP_DOCX=$(mktemp --suffix=.docx)
python3 -c "
from docx import Document
doc = Document()
doc.add_paragraph('Mục 1. KHÁM TỔNG QUÁT')
doc.add_paragraph('Bệnh nhân được khám lâm sàng tỉ mỉ.')
doc.add_paragraph('Mục 2. XÉT NGHIỆM')
doc.add_paragraph('Làm xét nghiệm máu và siêu âm.')
doc.save('$TMP_DOCX')
print('Generated:', '$TMP_DOCX')
"

log "5) Upload DOCX (Plan 04-04 REVISION 2 router add A4 BackgroundTask)..."
UPLOAD=$(curl -fsS -X POST "$API_URL/api/documents/upload" \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@$TMP_DOCX;filename=khám-bệnh.docx" \
  -F "hub_id=$HUB_ID")
DOC_ID=$(echo "$UPLOAD" | python3 -c "import json,sys; print(json.load(sys.stdin)['data']['document_id'])")
log "DOC_ID=$DOC_ID (status='pending' — A4 BackgroundTask đang chạy)"

log "6) Poll status đợi A4 BackgroundTask trigger_cocoindex_update + update_blocking + UPDATE status (timeout ${POLL_TIMEOUT}s)..."
STATUS="pending"
for i in $(seq 1 "$POLL_TIMEOUT"); do
  STATUS=$(curl -fsS "$API_URL/api/documents/$DOC_ID" \
    -H "Authorization: Bearer $TOKEN" \
    | python3 -c "import json,sys; print(json.load(sys.stdin)['data']['status'])")
  log "[$i] status=$STATUS"
  if [[ "$STATUS" == "completed" ]] || [[ "$STATUS" == "failed" ]] || [[ "$STATUS" == "failed_unsupported" ]]; then
    break
  fi
  sleep 1
done
[[ "$STATUS" == "completed" ]] || fail "Status không completed sau ${POLL_TIMEOUT}s: $STATUS (A4 BackgroundTask flakiness?)"

log "7) Verify chunks pgvector..."
CHUNK_COUNT=$(docker exec "$PG_CONTAINER" psql -U "$PG_USER" -d "$PG_DB" -tA \
  -c "SELECT COUNT(*) FROM chunks WHERE document_id='$DOC_ID'")
[[ "$CHUNK_COUNT" -gt 0 ]] || fail "chunks table empty"
log "chunks count=$CHUNK_COUNT"

# Verify dim = 1536 (R1 mitigation)
DIM=$(docker exec "$PG_CONTAINER" psql -U "$PG_USER" -d "$PG_DB" -tA \
  -c "SELECT vector_dims(vector) FROM chunks WHERE document_id='$DOC_ID' LIMIT 1")
[[ "$DIM" == "1536" ]] || fail "vector dim sai: $DIM (R1 yêu cầu 1536)"
log "vector dim=$DIM ✓"

# Verify hub_id match (ChunkRow.hub_id wire từ doc_row REVISION 2)
HUB_MATCH=$(docker exec "$PG_CONTAINER" psql -U "$PG_USER" -d "$PG_DB" -tA \
  -c "SELECT COUNT(*) FROM chunks WHERE document_id='$DOC_ID' AND hub_id='$HUB_ID'")
[[ "$HUB_MATCH" == "$CHUNK_COUNT" ]] || fail "hub_id mismatch: $HUB_MATCH/$CHUNK_COUNT chunks match"
log "hub_id match=$HUB_MATCH/$CHUNK_COUNT ✓"

# Verify content_hash NOT NULL (ChunkRow.content_hash sha256 wire REVISION 2)
HASH_NOT_NULL=$(docker exec "$PG_CONTAINER" psql -U "$PG_USER" -d "$PG_DB" -tA \
  -c "SELECT COUNT(*) FROM chunks WHERE document_id='$DOC_ID' AND content_hash IS NOT NULL")
[[ "$HASH_NOT_NULL" == "$CHUNK_COUNT" ]] || fail "content_hash NULL ở $((CHUNK_COUNT - HASH_NOT_NULL)) rows (D-1 violated)"
log "content_hash set=$HASH_NOT_NULL/$CHUNK_COUNT ✓"

echo ""
echo "============================================"
echo "M2A_DEMO_PASS chunks=$CHUNK_COUNT dim=$DIM hub_id_match=$HUB_MATCH content_hash_set=$HASH_NOT_NULL"
echo "============================================"

rm -f "$TMP_DOCX"
exit 0
```

**Make executable:**

```bash
chmod +x Hub_All/api/scripts/m2a_demo.sh
```

**Lưu ý executor:**
- Script REVISION 2 KHÔNG còn `make cocoindex-setup` step — Plan 04-03 REVISION 2 lifespan auto setup khi uvicorn start.
- POLL_TIMEOUT default 30s (REVISION 2 — A4 BackgroundTask + cocoindex update_blocking + UPDATE status). Configurable env.
- Script dùng `python3` cho JSON parsing (PortableOS — không phụ thuộc jq).
- Skip cleanup nếu fail (giúp debug).
- ENV variables override-able cho CI khác config.
</action>
<read_first>
- Hub_All/api/scripts/generate_keys.sh (bash style reference)
- Hub_All/api/Makefile (verify migrate-up — KHÔNG cần cocoindex-setup target nữa)
- Hub_All/docs/m2a-exit-gate-demo.md (Task 02 output)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/scripts/m2a_demo.sh` tồn tại.
- `grep -c '^#!/usr/bin/env bash' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- `grep -c 'set -euo pipefail' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- `grep -c 'M2A_DEMO_PASS' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- `grep -c 'vector_dims(vector)' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- `grep -c 'content_hash IS NOT NULL' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- `grep -c '/api/documents/upload' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- `grep -c '"completed"' Hub_All/api/scripts/m2a_demo.sh` ≥ 2.
- `grep -c '"1536"' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
- **REVISION 2 specific:**
  - `grep -c 'POLL_TIMEOUT' Hub_All/api/scripts/m2a_demo.sh` ≥ 2 (default 30 + use).
  - `grep -c 'A4 BackgroundTask\|BackgroundTask\|REVISION 2' Hub_All/api/scripts/m2a_demo.sh` ≥ 1.
  - `grep -c 'cocoindex-setup\|make cocoindex-setup' Hub_All/api/scripts/m2a_demo.sh` = 0 (REVISION 2 KHÔNG còn).
- File length ≥ 60 dòng.
- `bash -n Hub_All/api/scripts/m2a_demo.sh` exit 0 (syntax check).
</acceptance_criteria>
</task>

<task id="04" type="checkpoint:human-verify" gate="blocking">
<what-built>
Plan 04-06 REVISION 2 task 01-03 đã ship:
- `tests/integration/test_ingest_e2e.py` — 3 test E2E ALL PASS (happy + scanned PDF 415 BỎ xfail + content-hash) automate trong testcontainers + mock LiteLLM. **A4 BackgroundTasks pattern** (REVISION 2 — KHÔNG còn LISTEN/NOTIFY poll).
- `docs/m2a-exit-gate-demo.md` — Manual demo script Vietnamese REVISION 2 — 5 bước với commands paste-ready (KHÔNG còn `make cocoindex-setup` riêng — lifespan auto).
- `scripts/m2a_demo.sh` — Bash automation cho dev/CI subset (login → upload → poll A4 BackgroundTask → verify pgvector). POLL_TIMEOUT 30s.

Phase 4 đã ship đầy đủ (REVISION 2 — cocoindex 1.0.3 actual API + A4 BackgroundTasks + 5min watchdog timeout):
- INGEST-01..08 (8 requirements) implemented.
- Migration 0001 + 0002 OK; cocoindex 1.0.3 lifespan auto setup (Plan 04-03 REVISION 2).
- Cocoindex flow `cocoindex_app: coco.App(name="medinet_wiki_ingest")` với `pg.PgTableSource(documents)` + `pg.mount_table_target(chunks, ManagedBy.USER)` + ChunkRow dataclass + stable_chunk_id (uuid5).
- 4 endpoint `/api/documents/upload + /:id + /:id DELETE + /` với A4 BackgroundTasks trigger_cocoindex_update + synchronous scanned PDF early-detect (BLOCKER #3) + SQL NOW() timestamps (BLOCKER #4) + Content-Length pre-check (WARNING #6) + last_heartbeat=NOW() bootstrap (WARNING #7).
- Watchdog asyncio task 60s tick với `last_heartbeat IS NOT NULL` guard (WARNING #7) + REVISION 2 5 phút timeout configurable + APPEND-ONLY lifespan step 7 (WARNING #5).
- Lifespan asyncio.to_thread cho setup_cocoindex (REVISION 2 thay start_flow_live_updater).
- Tests: ≥ 19 unit + ≥ 28 integration (cumulative Plan 04-01..06 REVISION 2).
</what-built>

<how-to-verify>
**M2a EXIT GATE — User PHẢI chạy manual demo trước khi gate close. KHÔNG auto-pass.**

1. Đọc kỹ `Hub_All/docs/m2a-exit-gate-demo.md` từ đầu đến cuối (REVISION 2 — A4 BackgroundTasks pattern).

2. Chuẩn bị môi trường:
   - Docker Desktop running.
   - `.env` file tại `Hub_All/api/.env` với DATABASE_URL + REDIS_URL + COCOINDEX_DB (Q5 LMDB path) + JWT keys + OPENAI_API_KEY (hoặc Gemini key — required cho LiteLLM aembedding dim=1536 thật).
   - Sample DOCX VN tại `Hub_All/docs/fixtures/khám-bệnh-mẫu.docx` (operator chuẩn bị thủ công 1 lần).

3. Chạy 5 bước trong `m2a-exit-gate-demo.md` REVISION 2 theo thứ tự:
   - Bước 1: `docker compose up -d postgres redis` → 2 service healthy.
   - Bước 2: `make migrate-up && make migrate-check` → schema OK. **KHÔNG còn `make cocoindex-setup`** — Plan 04-03 REVISION 2 lifespan auto.
   - Bước 3: `docker compose up -d api` (lifespan auto setup cocoindex — verify log `cocoindex_setup_ok` + `cocoindex_initial_backfill_complete`) + seed admin user + hub_y_te qua psql.
   - Bước 4: Login → upload DOCX → poll status (A4 BackgroundTask + cocoindex update_blocking) → completed.
   - Bước 5: `psql SELECT vector_dims(vector), hub_id, content_hash FROM chunks` → verify 5/5 AC.

4. (Optional automation) Chạy `cd Hub_All/api && bash scripts/m2a_demo.sh` cho subset verification (Bước 4 + 5 automate). Output tail `M2A_DEMO_PASS` = subset PASS.

5. Đánh giá 5/5 ROADMAP success criteria (Phase 4 line 161-166):
   - AC1 (INGEST-04 upload <500ms + status pending + last_heartbeat bootstrap) — PASS / FAIL?
   - AC2 (INGEST-02 + 03 status completed + chunks dim=1536 + content_hash set + A4 BackgroundTasks <5s SLA) — PASS / FAIL?
   - AC3 (R4 scanned PDF → HTTP 415 + failed_unsupported router synchronous + KHÔNG add BackgroundTask — BLOCKER #3 REVISION 2) — PASS / FAIL?
   - AC4 (P8 watchdog flip stuck processing với NOT NULL guard + 5 phút timeout — WARNING #7 + REVISION 2) — PASS / FAIL (optional stress)?
   - AC5 (D-1 content-hash incremental memo + stable_chunk_id uuid5 deterministic) — PASS / FAIL?

**Acceptance trả lời:**

- ✅ **5/5 PASS** → Type `approved` + brief note (e.g. "AC1-5 PASS, chunks=12, dim=1536, A4 BackgroundTask latency=3s"). Phase 4 close, M2a EXIT GATE PASS, tiếp tục Plan `/gsd-execute-phase 5`.
- ⚠️ **3-4/5 PASS** → Type `partial: <reason>` (e.g. "AC2 FAIL — A4 BackgroundTask flakiness, status='pending' sau 30s; cần increase POLL_TIMEOUT hoặc force update_blocking sync"). Plan 04-06 thực thi gap closure task hoặc trigger Plan 04-07 (next plan).
- ❌ **≤ 2/5 PASS** hoặc decision pivot → Type `reject: <reason>` + `/gsd-discuss-milestone` invocation. STOP M2b. Trigger E1 EXIT criteria.
</how-to-verify>

<resume-signal>Type `approved` (5/5 PASS), `partial: <reason>` (3-4/5), hoặc `reject: <reason>` (≤2/5).</resume-signal>
</task>

## Verification

- `cd Hub_All/api && uv run pytest tests/integration/test_ingest_e2e.py -v --timeout=120` PASS 3/3 test (NO xfail — Plan 04-04 REVISION 2 đã fix BLOCKER #3 + A4 BackgroundTasks pattern).
- `bash -n Hub_All/api/scripts/m2a_demo.sh` exit 0 (syntax check).
- `wc -l Hub_All/docs/m2a-exit-gate-demo.md` ≥ 100 dòng.
- User manual confirmation tại Task 04 checkpoint.
- Phase 3 NO REGRESS: `cd Hub_All/api && uv run pytest tests/ -v -m critical` PASS toàn bộ critical test (29 Phase 3 + Plan 04-01..06 REVISION 2 mới ≥ 17 = 46+ critical).

## Threat Model

**Trust boundaries:**

| Boundary | Description |
|---|---|
| E2E test mock litellm → cocoindex 1.0.3 flow runtime | Mock cấp module function — KHÔNG affect production (KHÔNG persist). Risk: nếu Plan 04-03 REVISION 2 dùng late-binding `from app.services.embedder import aembedding_one` trong @coco.fn body, monkeypatch ở module-level KHÔNG hit. Plan 04-03 REVISION 2 đã import top-level → monkeypatch OK. |
| E2E test mock detect_scanned_pdf → service.create runtime (BLOCKER #3) | Plan 04-04 REVISION 2 service.create import detect_scanned_pdf top-level → monkeypatch cả 2 module reference (file_extract + documents_service) đảm bảo hit. |
| E2E test → A4 BackgroundTask cocoindex_app.update_blocking | Plan 04-04 REVISION 2 router add_task → BackgroundTask chạy SAU response client → cần poll wait. asyncio.to_thread wrap → KHÔNG block test event loop. |
| Manual demo script → operator hands → production-like infrastructure | Operator pass-through. Demo dùng `.env` thật → có thể leak credentials nếu commit log/screenshot. Documented warning trong demo md. |
| Checkpoint:human-verify → user judgment | Subjective gate. Mitigation: 5 AC checklist measurable (commands cụ thể, expected output cụ thể). Reject path documented (E1 EXIT). |

**STRIDE Threat Register:**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-06-01 | I (Information disclosure) | demo script in `.env` content qua `printenv` → leak credentials vào console log | mitigate | Demo script KHÔNG print env vars. Chỉ resolve DSN qua `docker exec psql` (credentials embedded trong container env, không in ra terminal). Documented warning trong demo md "không screenshot terminal có hash". |
| T-04-06-02 | T (Tampering) | E2E test mock litellm → vector random → nhiễm DB testcontainer | accept | Testcontainer per-module scope + TRUNCATE per-test (Phase 3 Plan 05 conftest pattern). Không ảnh hưởng prod. |
| T-04-06-03 | D (Denial of service) | E2E test poll 60s timeout × N test → CI time bloat | mitigate | Timeout 60s/test. Pytest `--timeout=120` plugin. Total E2E test = 3 → max 180s. Documented trong test header. |
| T-04-06-04 | R (Repudiation) | User accept demo qua chat reply → KHÔNG audit trail | accept | Operator decision recorded trong git commit message `feat(phase-04): M2a EXIT GATE PASSED — user accepted demo <timestamp>`. M2 acceptable single-user decision. |
| T-04-06-05 | E (Elevation of privilege) | demo script seed admin với plaintext password Admin@123 → operator quên đổi → prod admin yếu | mitigate | Demo md có warning bold "Đổi password admin trước khi deploy prod" + Phase 5 USER-02 sẽ implement reset-password endpoint. Documented gap. |
| T-04-06-06 (REVISION 2 NEW) | D (Denial of service) | A4 BackgroundTask flakiness trong testcontainers env → E2E test timeout (60s) → CI fail | mitigate | Helper `_force_cocoindex_update(app_with_auth)` available cho fast path — call `await asyncio.to_thread(cocoindex_app.update_blocking)` trực tiếp sau upload (KHÔNG đợi BackgroundTask). Documented test header. |

## Output

Sau khi complete tạo `Hub_All/.planning/phases/04-cocoindex-flow-mvp-document-ingest/04-06-SUMMARY.md` ghi rõ:
- 3 file modified/created + 3 commit hash atomic.
- E2E test 3/3 PASS (NO xfail — Plan 04-04 REVISION 2 BLOCKER #3 + A4 BackgroundTasks pattern wire scanned PDF early-detect).
- **REVISION 2** — A4 BackgroundTasks pattern thay LISTEN/NOTIFY poll (cocoindex 1.0.3 KHÔNG support source notification).
- Manual demo script + automation script ship — REVISION 2 cập nhật KHÔNG còn `make cocoindex-setup` (lifespan auto), AC2 A4 <5s SLA, AC3 BackgroundTask not added scanned, AC4 5min timeout.
- Checkpoint:human-verify user accept → M2a EXIT GATE PASSED → Phase 4 COMPLETE.
- 8/8 INGEST-XX requirements verified end-to-end.
- 5/5 ROADMAP Phase 4 success criteria validated (AC2/AC3/AC4 cập nhật REVISION 2).
- **SC2 timing guarantee REVISION 2**: A4 strategy preserves <5s SLA via BackgroundTasks immediate trigger; observed latency ~3-5 seconds for typical DOCX VN.
- Sẵn sàng cho `/gsd-execute-phase 5` (Phase 5 — HUB/USER/AUDIT/APIKEY/SETTINGS CRUD).
- M2a milestone closed. M2b unblocked.
</content>
</invoke>