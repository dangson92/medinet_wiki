---
plan: 03
phase: 3
wave: 2
depends_on: [02]
files_modified:
  - Hub_All/api/app/auth/__init__.py
  - Hub_All/api/app/auth/password.py
  - Hub_All/api/tests/unit/test_password.py
  - Hub_All/api/tests/integration/test_argon2_cross_compat.py
autonomous: true
requirements: [AUTH-05]
---

# Plan 03: Argon2 password module + cross-compat test (Go alexedwards ↔ pwdlib)

## Objective

Port Argon2id password hashing từ Go `backend/internal/pkg/hash/argon2.go` sang Python `pwdlib[argon2]`. Pin params chuẩn `m=65536, t=3, p=4, saltLen=16, keyLen=32` — **lấy từ Go SOURCE CODE** (constants ở line 13-19), KHÔNG từ REQUIREMENTS.md (doc nói `t=1, p=2` là sai). Cross-compat test bắt buộc PASS: pwdlib `verify()` chấp nhận hash do Go sinh trong seed.sql (5 sample) — R6 mitigation.

**Document doc-bug discrepancy** trong SUMMARY: REQUIREMENTS.md / ROADMAP.md AUTH-05 và Pitfall P6 ghi `t=1, p=2` nhưng Go source dùng `t=3, p=4`. Phase 3 chọn theo Go source (single source of truth = code đang chạy + seed hash production). Suggest follow-up doc fix.

**Tại sao plan riêng:** AUTH-05 cross-compat test là quality gate critical (R6) + cần testcontainers Postgres KHÔNG cần thiết (test pure Python pwdlib verify hash string). Tách riêng để không bloat Plan 03-04 router.

## Scope Note — AC4 Coverage

ROADMAP AC4 yêu cầu "5 sample hash do Go alexedwards/argon2id sinh + bi-directional Python↔Go verify". Plan 03-03 currently:
- ✅ 1 Go production hash (admin@medinet.vn từ seed.sql) verify qua pwdlib.
- ✅ 5 Python-sinh hash round-trip verify (pwdlib hash → pwdlib verify).
- ⏸ 4 thêm Go-sinh hash + ngược chiều "Python-sinh → Go verify": defer Phase 5 HUB-01 (cần `backend/cmd/hashgen` CLI hoặc thêm test Go-side — phụ thuộc backend/ tồn tại đến Phase 8 TEARDOWN-01).
- Risk: nếu Go production phát hành hash với tham số khác (e.g., bump iterations) trong tương lai, Plan 03-03 sẽ break — mitigation: AUTH-05 acceptance test sẽ FAIL fast, trigger re-pin.

Plan 03-03 accept current scope (1 Go hash + 5 Python round-trip) là đủ proof concept cross-compat. Full 5+5 bi-directional verify sẽ ship trong Phase 5/8 khi backend/ Go runtime còn available.

## Must-Haves

**Truths (4 mục):**
- `password.hash_password("Admin@123")` sinh hash string với prefix `$argon2id$v=19$m=65536,t=3,p=4$...` (cùng format Go).
- `password.verify_password("Admin@123", "$argon2id$v=19$m=65536,t=3,p=4$gpKFndFoG6bcXrx7R60sag$uhxmJMX03O6QlL0WZS+WdRhCOnGEoIpzUrL2vBzRN+c")` → `True` (R6 — cross-compat Go seed.sql hash).
- `password.verify_password("WrongPassword", "<go_seed_hash>")` → `False`.
- Hash do Python sinh ra → có thể được verify lại bằng Python (round-trip).

**Artifacts (4 file):**
- `Hub_All/api/app/auth/password.py` — wrap `pwdlib.PasswordHash` cấu hình Argon2id `memory_cost=65536, time_cost=3, parallelism=4, salt_len=16, hash_len=32`; expose `hash_password(plaintext) -> str` + `verify_password(plaintext, hash) -> bool`.
- `Hub_All/api/app/auth/__init__.py` (EXTEND) — re-export `hash_password`, `verify_password`.
- `Hub_All/api/tests/unit/test_password.py` — round-trip + reject wrong password + format prefix check.
- `Hub_All/api/tests/integration/test_argon2_cross_compat.py` — verify hash do Go `alexedwards/argon2id` sinh (5 sample từ seed.sql / seed_demo.sql).

**Key links (3 connection):**
- `app/auth/password.py` → `pwdlib.PasswordHash` + `pwdlib.hashers.Argon2Hasher(memory_cost=65536, time_cost=3, parallelism=4, salt_len=16, hash_len=32)` — pwdlib 0.3.0 API.
- Test fixtures → hash strings từ `Hub_All/backend/scripts/seed.sql` line 13 + `Hub_All/backend/scripts/seed_demo.sql` (admin user `Admin@123` + 5 demo user cùng plaintext).
- Plan 03-04 service login → `verify_password(req.password, user.password_hash)` để check credential.

## Tasks

<task id="01">
<action>
Tạo `Hub_All/api/app/auth/password.py` — wrap `pwdlib[argon2]` với params **MATCH Go source** (NOT REQUIREMENTS.md doc — đã verify bằng đọc `backend/internal/pkg/hash/argon2.go` line 13-19).

**Params (LẤY TỪ GO SOURCE, KHÔNG từ doc):**

| Param | Go const (argon2.go) | Python pwdlib name |
|---|---|---|
| memory | `argonMemory = 64 * 1024 = 65536` | `memory_cost=65536` |
| iterations | `argonIterations = 3` | `time_cost=3` |
| parallelism | `argonParallel = 4` | `parallelism=4` |
| salt length | `argonSaltLen = 16` | `salt_len=16` |
| hash length | `argonKeyLen = 32` | `hash_len=32` |

**`pwdlib[argon2]==0.3.0` API:**

```python
from pwdlib import PasswordHash
from pwdlib.hashers.argon2 import Argon2Hasher

pwd_context = PasswordHash((
    Argon2Hasher(
        memory_cost=65536,
        time_cost=3,
        parallelism=4,
        salt_len=16,
        hash_len=32,
    ),
))

hash_str = pwd_context.hash("plaintext")  # -> "$argon2id$v=19$m=65536,t=3,p=4$..."
ok = pwd_context.verify("plaintext", hash_str)  # -> True
```

**Lưu ý quan trọng:**
1. Nếu `pwdlib.hashers.argon2.Argon2Hasher` API constructor khác (kiểm tra version 0.3.0 thật), điều chỉnh dùng `pwdlib.hashers.argon2.Argon2Hasher` keyword args đúng tên — đọc package metadata `python -c "from pwdlib.hashers.argon2 import Argon2Hasher; help(Argon2Hasher.__init__)"` trước.
2. `pwdlib` 0.3.0 dùng backend `argon2-cffi` (đã pin trong pyproject) — output hash string compatible với Go `golang.org/x/crypto/argon2` format `$argon2id$v=19$m=...$t=...$p=...$salt$hash`. Verify bằng cross-compat test Task 03.
3. **KHÔNG dùng** `passlib` (legacy, không maintain, params API khác).

**Constant + docstring document doc-bug:**

Paste-ready:

```python
"""Argon2id password hashing — port từ Go `internal/pkg/hash/argon2.go`.

R6 mitigation: cross-compat với Go `golang.org/x/crypto/argon2` để hash do
Go cũ sinh (seed.sql production) verify được bằng Python pwdlib không cần
re-hash.

PARAMS — LẤY TỪ GO SOURCE (KHÔNG từ REQUIREMENTS.md doc):
    memory_cost  = 65536   (Go argonMemory     = 64 * 1024)
    time_cost    = 3       (Go argonIterations = 3)
    parallelism  = 4       (Go argonParallel   = 4)
    salt_len     = 16      (Go argonSaltLen    = 16)
    hash_len     = 32      (Go argonKeyLen     = 32)

⚠ DOC-BUG: REQUIREMENTS.md AUTH-05 + PITFALLS.md P6 ghi `t=1, p=2`. Source code
Go (backend/internal/pkg/hash/argon2.go line 13-19) là single source of truth.
Seed hash production `$argon2id$v=19$m=65536,t=3,p=4$...` confirm Go source
đúng. Sẽ tạo follow-up commit để fix REQUIREMENTS.md + PITFALLS.md sau Plan 03-03
ship.

Hash string format (cùng cả Go + Python):
    $argon2id$v=19$m=65536,t=3,p=4$<salt_b64>$<hash_b64>
"""
from __future__ import annotations

from pwdlib import PasswordHash
from pwdlib.hashers.argon2 import Argon2Hasher

# Params bake constant — sửa = re-hash toàn bộ user (R6 risk re-emerge).
ARGON2_MEMORY_COST = 65_536  # KB → 64 MB
ARGON2_TIME_COST = 3
ARGON2_PARALLELISM = 4
ARGON2_SALT_LEN = 16
ARGON2_HASH_LEN = 32

_password_hash = PasswordHash(
    (
        Argon2Hasher(
            memory_cost=ARGON2_MEMORY_COST,
            time_cost=ARGON2_TIME_COST,
            parallelism=ARGON2_PARALLELISM,
            salt_len=ARGON2_SALT_LEN,
            hash_len=ARGON2_HASH_LEN,
        ),
    )
)


def hash_password(plaintext: str) -> str:
    """Hash plaintext → string format $argon2id$v=19$m=65536,t=3,p=4$...

    Cùng format Go alexedwards/argon2id → verify được giữa 2 stack.
    """
    return _password_hash.hash(plaintext)


def verify_password(plaintext: str, hashed: str) -> bool:
    """Verify plaintext vs hash. Trả False thay vì raise nếu hash format sai.

    pwdlib `verify()` raise `pwdlib.exceptions.UnknownHashError` nếu prefix
    KHÔNG phải Argon2id — wrap catch để KHÔNG leak chi tiết format cho caller
    (Plan 03-04 router chỉ cần True/False).
    """
    try:
        return _password_hash.verify(plaintext, hashed)
    except Exception:  # noqa: BLE001 — wrap mọi parse error thành False
        return False
```

**Update `Hub_All/api/app/auth/__init__.py`** (đã tạo Plan 03-02) — thêm export `hash_password` + `verify_password` + 5 ARGON2_* constants:

```python
"""Auth package — Phase 3 (AUTH-01..06)."""
from __future__ import annotations

from app.auth.jwt import (
    JWT_ALGORITHM,
    JWT_ISSUER,
    JWTClaims,
    JWTError,
    JWTManager,
    TokenPair,
)
from app.auth.password import (
    ARGON2_HASH_LEN,
    ARGON2_MEMORY_COST,
    ARGON2_PARALLELISM,
    ARGON2_SALT_LEN,
    ARGON2_TIME_COST,
    hash_password,
    verify_password,
)

__all__ = [
    "ARGON2_HASH_LEN",
    "ARGON2_MEMORY_COST",
    "ARGON2_PARALLELISM",
    "ARGON2_SALT_LEN",
    "ARGON2_TIME_COST",
    "JWT_ALGORITHM",
    "JWT_ISSUER",
    "JWTClaims",
    "JWTError",
    "JWTManager",
    "TokenPair",
    "hash_password",
    "verify_password",
]
```
</action>
<read_first>
- Hub_All/api/pyproject.toml
- Hub_All/backend/internal/pkg/hash/argon2.go
- Hub_All/api/app/auth/__init__.py (đã có Plan 03-02)
- Hub_All/api/app/auth/jwt.py (Plan 03-02 — để hiểu __init__ pattern)
- Hub_All/.planning/REQUIREMENTS.md (line 28 — AUTH-05 doc bug)
- Hub_All/.planning/research/PITFALLS.md (P6 doc bug)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/app/auth/password.py` tồn tại.
- `grep -c 'ARGON2_MEMORY_COST = 65_536' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'ARGON2_TIME_COST = 3' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'ARGON2_PARALLELISM = 4' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'ARGON2_SALT_LEN = 16' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'ARGON2_HASH_LEN = 32' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'def hash_password' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'def verify_password' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'DOC-BUG' Hub_All/api/app/auth/password.py` ≥ 1.
- `grep -c 'hash_password' Hub_All/api/app/auth/__init__.py` ≥ 1.
- `grep -c 'verify_password' Hub_All/api/app/auth/__init__.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.auth import hash_password, verify_password; h = hash_password('test'); print(h[:40]); assert h.startswith('\$argon2id\$v=19\$m=65536,t=3,p=4\$'); assert verify_password('test', h) is True; assert verify_password('wrong', h) is False; print('round-trip OK')"` exits 0 với output bắt đầu `$argon2id$v=19$m=65536,t=3,p=4$` và kết thúc `round-trip OK`.
- `cd Hub_All/api && uv run ruff check app/auth/password.py app/auth/__init__.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth/password.py` exits 0.
</acceptance_criteria>
</task>

<task id="02">
<action>
Tạo `Hub_All/api/tests/unit/test_password.py` — unit test pure Python (KHÔNG cần Postgres) verify hash/verify happy + reject + format.

**Test bắt buộc:**

1. `test_hash_password_returns_argon2id_prefix` — hash → assert prefix `$argon2id$v=19$m=65536,t=3,p=4$`.
2. `test_round_trip_verify_true` — `verify_password(plain, hash_password(plain))` is `True`.
3. `test_verify_rejects_wrong_password` — `verify_password("wrong", hash_password("right"))` is `False`.
4. `test_verify_returns_false_on_garbage_hash` — `verify_password("anything", "not-a-hash-at-all")` is `False` (KHÔNG raise).
5. `test_hash_is_different_each_call` — `hash_password("x") != hash_password("x")` (salt random).
6. `test_params_constants_match_go_source` — assert `ARGON2_MEMORY_COST == 65_536, ARGON2_TIME_COST == 3, ARGON2_PARALLELISM == 4, ARGON2_SALT_LEN == 16, ARGON2_HASH_LEN == 32` — guard regression vô tình đổi giá trị.

Paste-ready:

```python
"""Unit test password hashing — Plan 03-03 (AUTH-05).

Test pure Python, KHÔNG cần Postgres. Cross-compat với hash Go sinh sẽ test
ở `tests/integration/test_argon2_cross_compat.py`.
"""
from __future__ import annotations

import pytest

from app.auth import (
    ARGON2_HASH_LEN,
    ARGON2_MEMORY_COST,
    ARGON2_PARALLELISM,
    ARGON2_SALT_LEN,
    ARGON2_TIME_COST,
    hash_password,
    verify_password,
)


def test_hash_password_returns_argon2id_prefix() -> None:
    h = hash_password("Admin@123")
    assert h.startswith("$argon2id$v=19$m=65536,t=3,p=4$"), (
        f"prefix sai — không match Go format. Got: {h[:50]}"
    )


def test_round_trip_verify_true() -> None:
    plain = "Mật khẩu Tiếng Việt 1234!@#"
    h = hash_password(plain)
    assert verify_password(plain, h) is True


def test_verify_rejects_wrong_password() -> None:
    h = hash_password("right-password")
    assert verify_password("wrong-password", h) is False


def test_verify_returns_false_on_garbage_hash() -> None:
    # KHÔNG raise — caller (Plan 03-04 router) chỉ cần True/False.
    assert verify_password("anything", "not-a-hash-at-all") is False
    assert verify_password("anything", "") is False
    assert verify_password("anything", "$bcrypt$v=2b$10$...") is False


def test_hash_is_different_each_call() -> None:
    """Salt random — 2 hash của cùng 1 plaintext PHẢI khác nhau."""
    a = hash_password("same-plain")
    b = hash_password("same-plain")
    assert a != b


def test_params_constants_match_go_source() -> None:
    """Guard regression — đổi 1 const = re-hash toàn bộ user → R6 trigger."""
    assert ARGON2_MEMORY_COST == 65_536
    assert ARGON2_TIME_COST == 3
    assert ARGON2_PARALLELISM == 4
    assert ARGON2_SALT_LEN == 16
    assert ARGON2_HASH_LEN == 32
```
</action>
<read_first>
- Hub_All/api/app/auth/password.py
- Hub_All/api/tests/conftest.py
- Hub_All/api/tests/unit/test_jwt.py (Plan 03-02 — style reference)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/unit/test_password.py` tồn tại.
- `grep -c 'def test_hash_password_returns_argon2id_prefix' Hub_All/api/tests/unit/test_password.py` ≥ 1.
- `grep -c 'def test_round_trip_verify_true' Hub_All/api/tests/unit/test_password.py` ≥ 1.
- `grep -c 'def test_verify_rejects_wrong_password' Hub_All/api/tests/unit/test_password.py` ≥ 1.
- `grep -c 'def test_verify_returns_false_on_garbage_hash' Hub_All/api/tests/unit/test_password.py` ≥ 1.
- `grep -c 'def test_hash_is_different_each_call' Hub_All/api/tests/unit/test_password.py` ≥ 1.
- `grep -c 'def test_params_constants_match_go_source' Hub_All/api/tests/unit/test_password.py` ≥ 1.
- `cd Hub_All/api && uv run pytest tests/unit/test_password.py -v` exits 0 với 6 test PASS.
- `cd Hub_All/api && uv run ruff check tests/unit/test_password.py` exits 0.
</acceptance_criteria>
</task>

<task id="03">
<action>
Tạo `Hub_All/api/tests/integration/test_argon2_cross_compat.py` — test CRITICAL (R6 mitigation, AUTH-05 success criterion 4 + 5). Verify hash do Go `alexedwards/argon2id` sinh ra (production seed.sql) được pwdlib `verify_password()` accept.

**Fixture hash thật từ Go seed:**

Tất cả seed user (admin@medinet.vn + 19 demo user nva@medinet.vn..lvm@medinet.vn) trong `Hub_All/backend/scripts/seed.sql` line 13 và `seed_demo.sql` line 63, 67-88 dùng **CÙNG 1 hash** cho **CÙNG 1 plaintext `Admin@123`** (đây là test convenience). Hash thật:

```
$argon2id$v=19$m=65536,t=3,p=4$gpKFndFoG6bcXrx7R60sag$uhxmJMX03O6QlL0WZS+WdRhCOnGEoIpzUrL2vBzRN+c
```

Test sẽ verify 1 hash này + sinh thêm 4 hash khác **bằng Python** với cùng params nhưng plaintext khác, xác nhận tất cả round-trip. Để mô phỏng "5 sample Go hash" trong ROADMAP success criterion 4, plan này:
- 1 sample hash THẬT từ Go seed.sql (admin@medinet.vn, `Admin@123`).
- 4 sample Python sinh cùng params (verify pwdlib format đúng + Go format compat).

Nếu Phase 3 cần thêm hash thật từ Go runtime (paranoid R6), executor có thể chạy:
```bash
cd Hub_All/backend && go run ./cmd/hashgen "User@Pass1" "User@Pass2" ...
```
nhưng KHÔNG bắt buộc trong Plan 03-03 — 1 hash production từ seed.sql đã đủ proof concept (cùng Go lib alexedwards, cùng params).

**Test marker `@pytest.mark.critical`** — đây là critical path R6/AUTH-05.

**Test bắt buộc:**

1. `test_pwdlib_verify_go_seed_admin_hash` — Hash từ `Hub_All/backend/scripts/seed.sql` line 13, plaintext `Admin@123` → `verify_password("Admin@123", <go_hash>)` is `True`. (R6 critical proof)
2. `test_pwdlib_reject_go_seed_with_wrong_password` — Cùng hash, plaintext `"WrongPassword"` → `False`. (Phản chứng để loại false-positive verify always True)
3. `test_round_trip_python_to_python_5_samples` — Sinh 5 hash Python với 5 plaintext khác nhau → verify lại cả 5 PASS.
4. `test_python_generated_hash_format_matches_go` — Hash Python sinh phải có prefix EXACT `$argon2id$v=19$m=65536,t=3,p=4$` byte-identical Go.

**Lưu ý:** Test này KHÔNG cần testcontainers Postgres (pure cryptography). Đặt trong `tests/integration/` để chạy chung với `pytest -m critical` (CI gate HARD-03). Marker `integration` + `critical` cả 2.

Paste-ready:

```python
"""Integration test Argon2 cross-compat — Plan 03-03 (AUTH-05 / R6).

Mục đích: verify hash do Go `alexedwards/argon2id` sinh trong seed.sql production
được pwdlib Python verify lại bằng `verify_password()` mà KHÔNG cần re-hash
toàn bộ user.

R6 critical proof: 1 hash thật từ seed.sql + 4 hash Python sinh = đủ confirm
pwdlib + argon2-cffi cùng format binary với golang.org/x/crypto/argon2 khi
params (m,t,p,saltLen,keyLen) trùng nhau.

KHÔNG cần testcontainers Postgres — chỉ cần Python pwdlib + hash string literal.
Marker `critical` cho CI gate HARD-03 `pytest -m critical`.
"""
from __future__ import annotations

import pytest

from app.auth import hash_password, verify_password

# Hash thật từ Go seed.sql (admin@medinet.vn) — KHÔNG sửa, KHÔNG paraphrase.
# Plaintext: "Admin@123" (xem comment line 3-7 của seed.sql).
# Sinh bởi: backend/internal/pkg/hash/argon2.go::HashPassword qua Go alexedwards.
GO_SEED_HASH = (
    "$argon2id$v=19$m=65536,t=3,p=4"
    "$gpKFndFoG6bcXrx7R60sag"
    "$uhxmJMX03O6QlL0WZS+WdRhCOnGEoIpzUrL2vBzRN+c"
)
GO_SEED_PLAINTEXT = "Admin@123"


@pytest.mark.critical
@pytest.mark.integration
def test_pwdlib_verify_go_seed_admin_hash() -> None:
    """R6 CORE — pwdlib verify Go-generated hash production."""
    assert verify_password(GO_SEED_PLAINTEXT, GO_SEED_HASH) is True, (
        "Cross-compat FAIL — pwdlib KHÔNG verify được hash do Go sinh. "
        "Kiểm tra lại ARGON2_* params trong app/auth/password.py có match "
        "backend/internal/pkg/hash/argon2.go line 13-19 không."
    )


@pytest.mark.critical
@pytest.mark.integration
def test_pwdlib_reject_go_seed_with_wrong_password() -> None:
    """Phản chứng — verify KHÔNG always-True."""
    assert verify_password("WrongPassword!@#", GO_SEED_HASH) is False
    assert verify_password("admin@123", GO_SEED_HASH) is False  # case-sensitive
    assert verify_password("", GO_SEED_HASH) is False


@pytest.mark.critical
@pytest.mark.integration
def test_round_trip_python_to_python_5_samples() -> None:
    """Sinh 5 hash Python với 5 plaintext khác → verify lại cả 5."""
    samples = [
        "Editor@Pass123",
        "Viewer@2026",
        "Mật khẩu có dấu tiếng Việt",
        "P@ssw0rd!#$%^&*()",
        "x" * 64,  # max length stress
    ]
    for plain in samples:
        h = hash_password(plain)
        assert h.startswith("$argon2id$v=19$m=65536,t=3,p=4$"), (
            f"Format prefix wrong for plaintext {plain!r}: {h[:50]}"
        )
        assert verify_password(plain, h) is True, f"Round-trip fail for {plain!r}"
        assert verify_password(plain + "x", h) is False


@pytest.mark.critical
@pytest.mark.integration
def test_python_generated_hash_format_matches_go() -> None:
    """Hash Python sinh PHẢI có prefix byte-identical Go format."""
    h = hash_password("any-plain")
    # Expected: $argon2id$v=19$m=65536,t=3,p=4$<salt>$<hash>
    parts = h.split("$")
    assert len(parts) == 6, f"Hash phải có 6 segment (split bằng $), nhận {len(parts)}: {parts}"
    assert parts[0] == ""  # leading $
    assert parts[1] == "argon2id"
    assert parts[2] == "v=19"
    assert parts[3] == "m=65536,t=3,p=4"
    # parts[4] = salt b64, parts[5] = hash b64 — chỉ check không rỗng
    assert len(parts[4]) > 0
    assert len(parts[5]) > 0
```
</action>
<read_first>
- Hub_All/api/app/auth/password.py
- Hub_All/backend/scripts/seed.sql
- Hub_All/backend/scripts/seed_demo.sql
- Hub_All/backend/internal/pkg/hash/argon2.go
- Hub_All/api/pyproject.toml (line 95-99 — marker `critical` đã đăng ký)
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/test_argon2_cross_compat.py` tồn tại.
- `grep -c 'GO_SEED_HASH' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1.
- `grep -c 'gpKFndFoG6bcXrx7R60sag' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1 (hash thật bake nguyên).
- `grep -c 'GO_SEED_PLAINTEXT = "Admin@123"' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1.
- `grep -c '@pytest.mark.critical' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 4.
- `grep -c 'def test_pwdlib_verify_go_seed_admin_hash' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1.
- `grep -c 'def test_pwdlib_reject_go_seed_with_wrong_password' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1.
- `grep -c 'def test_round_trip_python_to_python_5_samples' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1.
- `grep -c 'def test_python_generated_hash_format_matches_go' Hub_All/api/tests/integration/test_argon2_cross_compat.py` ≥ 1.
- `cd Hub_All/api && uv run pytest tests/integration/test_argon2_cross_compat.py -v` exits 0 với 4 test PASS.
- `cd Hub_All/api && uv run pytest tests/integration/test_argon2_cross_compat.py -m critical -v` exits 0 với 4 test PASS (verify marker `critical` apply đúng).
- `cd Hub_All/api && uv run ruff check tests/integration/test_argon2_cross_compat.py` exits 0.
</acceptance_criteria>
</task>

## Verification

- `cd Hub_All/api && uv run pytest tests/unit/test_password.py tests/integration/test_argon2_cross_compat.py -v` PASS ≥ 10 test.
- `cd Hub_All/api && uv run pytest -m critical -v` lọc đúng 4 test cross-compat + bất kỳ critical test khác đã có.
- `cd Hub_All/api && uv run python -c "from app.auth import verify_password; assert verify_password('Admin@123', '\$argon2id\$v=19\$m=65536,t=3,p=4\$gpKFndFoG6bcXrx7R60sag\$uhxmJMX03O6QlL0WZS+WdRhCOnGEoIpzUrL2vBzRN+c') is True; print('R6 cross-compat OK')"` exits 0 với output `R6 cross-compat OK`.
- `cd Hub_All/api && uv run ruff check app/auth tests/unit/test_password.py tests/integration/test_argon2_cross_compat.py` exits 0.
- `cd Hub_All/api && uv run mypy app/auth` exits 0.

## Threat Model

**Trust boundaries:**

| Boundary | Description |
|---|---|
| Client `POST /api/auth/login` → service `verify_password` | Plaintext password chỉ tồn tại trong RAM scope của verify call. KHÔNG log, KHÔNG persist. |
| `users.password_hash` DB column → backup/log leak | Hash đã hash 1 chiều — leak hash KHÔNG = leak password (chỉ giảm bảo vệ qua brute-force time cost). R6 + T-03-pw-timing mitigated. |
| Go seed `alexedwards/argon2id` → Python pwdlib verify | Cross-stack interop — chỉ secure khi params + binary format hoàn toàn match. Test cross-compat là gate verify hai stack đồng bộ. |

**STRIDE Threat Register:**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-pw-timing | I (Information disclosure) | `verify_password` runtime khác nhau giữa user tồn tại vs không tồn tại → attacker dùng timing oracle enumerate email | mitigate | pwdlib + argon2-cffi dùng `constant_time_compare` internal khi compare hash bytes. Plan 03-04 service `login` PHẢI luôn gọi `verify_password()` ngay cả khi user không tồn tại (compare với dummy hash) để response time tương tự — sẽ bake trong Plan 03-04. Phase 3 Plan 03-03 chỉ chịu trách nhiệm primitive an toàn. |
| T-03-pw-param-downgrade | E (Elevation) | Attacker với DB access đổi `password_hash` column thành hash params yếu (`m=8, t=1, p=1`) rồi crack offline | accept | Mitigation đòi hỏi DB-level constraint (CHECK regex) — phức tạp + brittle. M2 chấp nhận risk DB-level (attacker đã có DB write access = compromised toàn bộ). Documented Out of Scope. |
| T-03-pw-pepper-missing | T (Tampering) | Hash KHÔNG có server-side pepper (app secret) → leak DB = đủ để brute force | accept | Go cũ KHÔNG dùng pepper. Adding pepper sẽ break R6 cross-compat (existing user phải re-hash). Defer v4.0. Mitigation interim: Argon2id m=64MB + t=3 + p=4 đủ chống GPU brute force trong scope M2. |
| T-03-pw-log-leak | I | `logger.error("login failed", password=plain)` leak password vào log | mitigate | `app/auth/password.py` `verify_password` KHÔNG log gì cả. Plan 03-04 service login PHẢI: `log.info("auth_attempt", email=email)` — KHÔNG log password kể cả ở debug level. Convention bake trong CONVENTIONS.md section 5 (logging DON'T). |
| T-03-pw-doc-bug | T (process tampering) | Future contributor đọc REQUIREMENTS.md (`t=1, p=2`) rồi sửa code params → R6 regress | mitigate | (a) `password.py` docstring DOC-BUG note bake rõ. (b) `test_params_constants_match_go_source` guard regression — sửa const = test fail. (c) SUMMARY note tạo follow-up commit fix REQUIREMENTS.md + PITFALLS.md (out of plan scope nhưng document hint). |

## Output

Sau khi complete tạo `Hub_All/.planning/phases/03-auth-port-rbac-response-envelope/03-03-SUMMARY.md`:
- 4 file created/updated.
- 10+ test PASS (6 unit + 4 critical cross-compat).
- R6 mitigation verified: pwdlib `verify("Admin@123", <go_seed_hash>)` → True.
- DOC-BUG note: REQUIREMENTS.md AUTH-05 + PITFALLS.md P6 ghi `t=1, p=2` nhưng implementation theo Go source `t=3, p=4`. Suggest follow-up doc fix commit.
- Plan 03-04 có thể `from app.auth import verify_password` để check credential.
