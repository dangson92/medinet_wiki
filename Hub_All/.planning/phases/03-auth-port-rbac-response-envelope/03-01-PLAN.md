---
plan: 01
phase: 3
wave: 1
depends_on: []
files_modified:
  - Hub_All/api/app/middleware/__init__.py
  - Hub_All/api/app/middleware/request_id.py
  - Hub_All/api/app/middleware/security_headers.py
  - Hub_All/api/app/middleware/error_handler.py
  - Hub_All/api/app/pkg/response.py
  - Hub_All/api/app/config.py
  - Hub_All/api/app/main.py
  - Hub_All/api/tests/test_middleware.py
autonomous: true
requirements: [AUTH-01, AUTH-04]
---

# Plan 01: Middleware infra + envelope error helpers + CORS production validator

## Objective

Dựng nền móng request middleware cho toàn bộ Phase 3 + chuẩn hoá envelope error helpers byte-identical với Go `pkg/response/response.go`. Đây là "Wave 1 — Define contracts" cho Plan 03-04 (auth router) và Plan 03-05 (RBAC tests) — mọi handler auth sẽ phụ thuộc:
1. `response.unauthorized()/forbidden()/bad_request()` có code = `UPPER_SNAKE_CASE` match Go (`UNAUTHORIZED`, `FORBIDDEN` — KHÔNG `unauthorized` lowercase) để frontend D6 tương thích shape cũ.
2. `RequestIdMiddleware` propagate `X-Request-Id` (sinh UUID4 nếu thiếu) — auth handlers log với `request_id` field.
3. `SecurityHeadersMiddleware` set X-Content-Type-Options + X-Frame-Options + Referrer-Policy match Go.
4. `ErrorHandlerMiddleware` catch mọi exception → return envelope `{success:false, error:{code, message}}` (KHÔNG raw stack trace leak).
5. `cors_allowed_origins` pydantic validator REJECT LAN origin trong production (P12).
6. Wire 5 middleware vào `app.main:create_app()` theo thứ tự P11-correct (add CUỐI = outermost).

**Tại sao plan riêng:** Plan 03-04 (router) cần `response.bad_request()`, `response.unauthorized()` ổn định; nếu plan 03-04 phải vừa định nghĩa helper vừa wire router, một thay đổi ở response shape sẽ phải re-do router code. Tách layer infra → 03-04 chỉ gọi helpers đã commit.

## Must-Haves

**Truths (5 mục, người dùng/Claude executor quan sát được):**
- Plan 03-04 có thể `from app.pkg import response as resp` rồi `return resp.unauthorized(...)` mà response body match Go shape `{success:false, data:null, error:{code:"UNAUTHORIZED", message:"..."}, meta:null}` byte-identical.
- Mỗi HTTP request đi qua app có header response `X-Request-Id` (UUID4 sinh nếu client không gửi, echo lại nếu client gửi).
- Response của mọi endpoint có header bảo mật: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`.
- Exception trong handler → response 500 envelope `{success:false, error:{code:"INTERNAL_ERROR", message:"..."}}` KHÔNG leak stack trace.
- `Settings(app_env="production", cors_allowed_origins=["http://192.168.1.1:5173"])` raise `ValidationError` ngay startup (P12 mitigation).

**Artifacts (8 file):**
- `Hub_All/api/app/middleware/__init__.py` — re-export 3 middleware class.
- `Hub_All/api/app/middleware/request_id.py` — `RequestIdMiddleware(BaseHTTPMiddleware)` set `request.state.request_id` + header echo.
- `Hub_All/api/app/middleware/security_headers.py` — `SecurityHeadersMiddleware(BaseHTTPMiddleware)` set 5 header.
- `Hub_All/api/app/middleware/error_handler.py` — `ErrorHandlerMiddleware(BaseHTTPMiddleware)` catch Exception, return envelope, log structured.
- `Hub_All/api/app/pkg/response.py` (EXTEND) — đổi error code lowercase → UPPER_SNAKE_CASE match Go (`UNAUTHORIZED`, `FORBIDDEN`, `BAD_REQUEST`, `NOT_FOUND`, `CONFLICT`, `INTERNAL_ERROR`, `RATE_LIMIT_EXCEEDED`); thêm `validation_error()` helper 422.
- `Hub_All/api/app/config.py` (EXTEND) — pydantic `field_validator` reject LAN origin khi `app_env=production`.
- `Hub_All/api/app/main.py` (EXTEND) — wire 3 middleware mới + giữ CORS, theo order REVERSED P11.
- `Hub_All/api/tests/test_middleware.py` — unit test verify shape envelope error + header X-Request-Id + CORS production validator reject.

**Key links (4 connection):**
- `app/middleware/error_handler.py` → `app.pkg.response.internal_error()` — pattern: try/await call_next/except → return envelope.
- `app/main.py` → `app.middleware.*` — pattern: `app.add_middleware(ErrorHandlerMiddleware)` LAST (outermost) sau `add_middleware(CORSMiddleware)`.
- `app/config.py` field_validator → `app_env` + `cors_allowed_origins` (P12 — chỉ check khi `app_env=="production"`).
- `app/pkg/response.py` error code → match Go `pkg/response/response.go` line 60-103 (`UPPER_SNAKE_CASE`).

## Tasks

<task id="01">
<action>
Tạo 3 file middleware mới trong `Hub_All/api/app/middleware/`. Pattern dùng `starlette.middleware.base.BaseHTTPMiddleware` (KHÔNG dùng `@app.middleware("http")` decorator vì decorator function-style không re-usable + khó test).

**File 1: `Hub_All/api/app/middleware/request_id.py`** — Sinh hoặc echo header `X-Request-Id` (UUID4). Match Go `internal/middleware/request_id.go`. KHÔNG validate format UUID của client (proxy/gateway có thể inject `traceparent`-style). Lưu vào `request.state.request_id` để downstream handler/log dùng được.

Paste-ready:

```python
"""RequestId middleware — sinh hoặc echo X-Request-Id header.

Match Go internal/middleware/request_id.go. Đặt sớm trong chain (gần outermost)
để mọi log downstream + error response có request_id field.

Phase 3: chỉ gắn vào request.state. Phase 10 (HARD-01) sẽ propagate vào structlog
context vars cho cocoindex flow logs.
"""
from __future__ import annotations

import uuid

from starlette.middleware.base import BaseHTTPMiddleware, RequestResponseEndpoint
from starlette.requests import Request
from starlette.responses import Response

REQUEST_ID_HEADER = "X-Request-Id"


class RequestIdMiddleware(BaseHTTPMiddleware):
    """Set request.state.request_id + echo header response.

    Behavior:
    - Client gửi `X-Request-Id: <bất kỳ>` → echo nguyên trị (KHÔNG validate format).
    - Client không gửi → sinh UUID4 mới.
    - Response luôn có header `X-Request-Id`.
    """

    async def dispatch(
        self, request: Request, call_next: RequestResponseEndpoint
    ) -> Response:
        rid = request.headers.get(REQUEST_ID_HEADER) or str(uuid.uuid4())
        request.state.request_id = rid
        response = await call_next(request)
        response.headers[REQUEST_ID_HEADER] = rid
        return response
```

**File 2: `Hub_All/api/app/middleware/security_headers.py`** — Set 5 header bảo mật match Go `internal/middleware/security.go` line 7-13.

Paste-ready:

```python
"""SecurityHeaders middleware — set 5 header bảo mật cơ bản.

Match Go internal/middleware/security.go (line 7-13):
  - X-Content-Type-Options: nosniff
  - X-Frame-Options: DENY
  - X-XSS-Protection: 1; mode=block
  - Referrer-Policy: strict-origin-when-cross-origin
  - Permissions-Policy: camera=(), microphone=(), geolocation=()

CSP + HSTS defer Phase 10 hardening (HARD-04) — production nginx layer set
HSTS preload + CSP report-only đầu tiên trước khi enforce.
"""
from __future__ import annotations

from starlette.middleware.base import BaseHTTPMiddleware, RequestResponseEndpoint
from starlette.requests import Request
from starlette.responses import Response


class SecurityHeadersMiddleware(BaseHTTPMiddleware):
    """Inject 5 security header cố định vào mọi response."""

    async def dispatch(
        self, request: Request, call_next: RequestResponseEndpoint
    ) -> Response:
        response = await call_next(request)
        response.headers["X-Content-Type-Options"] = "nosniff"
        response.headers["X-Frame-Options"] = "DENY"
        response.headers["X-XSS-Protection"] = "1; mode=block"
        response.headers["Referrer-Policy"] = "strict-origin-when-cross-origin"
        response.headers["Permissions-Policy"] = (
            "camera=(), microphone=(), geolocation=()"
        )
        return response
```

**File 3: `Hub_All/api/app/middleware/error_handler.py`** — Catch mọi exception unhandled trong handler → return envelope 500 `{success:false, error:{code:"INTERNAL_ERROR", message:"..."}}`. Log structured (level=error) với request_id + stack trace cho ops debug. KHÔNG leak stack vào client response body. ĐỪNG catch `HTTPException` của FastAPI (Starlette tự handle → envelope sẽ KHÔNG match → để FastAPI `exception_handler` lo nếu cần Phase sau).

Paste-ready:

```python
"""ErrorHandler middleware — catch exception, return envelope chuẩn.

Outermost middleware (add LAST trong app.add_middleware). Mọi exception
không-HTTPException từ handler/downstream middleware sẽ bị catch ở đây →
log structured (level=error) + return envelope `{success:false, error:{code,
message}, meta:null}` 500 status.

P11 mitigation: PHẢI add CUỐI cùng để wrap toàn bộ chain (CORS, security_headers,
request_id). Nếu add ĐẦU → exception từ CORS middleware không được catch.

KHÔNG leak stack trace vào response body — chỉ log server-side.
"""
from __future__ import annotations

import logging

from starlette.exceptions import HTTPException as StarletteHTTPException
from starlette.middleware.base import BaseHTTPMiddleware, RequestResponseEndpoint
from starlette.requests import Request
from starlette.responses import Response

from app.pkg import response as resp

logger = logging.getLogger(__name__)


class ErrorHandlerMiddleware(BaseHTTPMiddleware):
    """Catch unhandled exception → log + return envelope 500.

    KHÔNG catch starlette.exceptions.HTTPException (FastAPI tự handle qua
    exception_handler) — nếu catch ở đây thì shape envelope sẽ KHÔNG match
    với raise HTTPException ở handler.
    """

    async def dispatch(
        self, request: Request, call_next: RequestResponseEndpoint
    ) -> Response:
        try:
            return await call_next(request)
        except Exception as exc:  # noqa: BLE001 — catch-all là chủ đích của middleware này.
            # HTTPException pass-through — để FastAPI exception_handler (Plan 03-05)
            # render envelope shape khớp với endpoint raise HTTPException trực tiếp.
            # Nếu catch ở đây thì envelope SẼ KHÔNG match (T-03-http-exception-mask).
            if isinstance(exc, StarletteHTTPException):
                raise
            request_id = getattr(request.state, "request_id", "unknown")
            logger.error(
                "unhandled_exception",
                exc_info=exc,
                extra={
                    "request_id": request_id,
                    "method": request.method,
                    "path": request.url.path,
                },
            )
            return resp.internal_error(message="Lỗi máy chủ nội bộ")
```

**File 4: `Hub_All/api/app/middleware/__init__.py`** — re-export 3 class:

```python
"""Middleware package — request_id, security_headers, error_handler.

Sử dụng trong app/main.py (theo P11 — add CUỐI = outermost):
    from app.middleware import (
        ErrorHandlerMiddleware,
        RequestIdMiddleware,
        SecurityHeadersMiddleware,
    )
"""
from __future__ import annotations

from app.middleware.error_handler import ErrorHandlerMiddleware
from app.middleware.request_id import REQUEST_ID_HEADER, RequestIdMiddleware
from app.middleware.security_headers import SecurityHeadersMiddleware

__all__ = [
    "REQUEST_ID_HEADER",
    "ErrorHandlerMiddleware",
    "RequestIdMiddleware",
    "SecurityHeadersMiddleware",
]
```
</action>
<read_first>
- Hub_All/api/app/main.py
- Hub_All/api/app/pkg/response.py
- Hub_All/backend/internal/middleware/request_id.go
- Hub_All/backend/internal/middleware/security.go
- Hub_All/backend/internal/middleware/recovery.go
- Hub_All/.planning/CONVENTIONS.md
- Hub_All/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- 4 file tồn tại: `Hub_All/api/app/middleware/__init__.py`, `request_id.py`, `security_headers.py`, `error_handler.py`.
- `grep -c 'class RequestIdMiddleware(BaseHTTPMiddleware)' Hub_All/api/app/middleware/request_id.py` ≥ 1.
- `grep -c 'class SecurityHeadersMiddleware(BaseHTTPMiddleware)' Hub_All/api/app/middleware/security_headers.py` ≥ 1.
- `grep -c 'class ErrorHandlerMiddleware(BaseHTTPMiddleware)' Hub_All/api/app/middleware/error_handler.py` ≥ 1.
- `grep -c 'X-Content-Type-Options' Hub_All/api/app/middleware/security_headers.py` ≥ 1.
- `grep -c 'X-Frame-Options' Hub_All/api/app/middleware/security_headers.py` ≥ 1.
- `grep -c 'Referrer-Policy' Hub_All/api/app/middleware/security_headers.py` ≥ 1.
- `grep -c 'REQUEST_ID_HEADER = "X-Request-Id"' Hub_All/api/app/middleware/request_id.py` ≥ 1.
- `grep -c 'resp.internal_error' Hub_All/api/app/middleware/error_handler.py` ≥ 1.
- `grep -c 'isinstance(exc, StarletteHTTPException)' Hub_All/api/app/middleware/error_handler.py` ≥ 1 (HTTPException pass-through — Plan 03-05 exception_handler render envelope).
- `grep -c 'from starlette.exceptions import HTTPException' Hub_All/api/app/middleware/error_handler.py` ≥ 1.
- `cd Hub_All/api && uv run python -c "from app.middleware import RequestIdMiddleware, SecurityHeadersMiddleware, ErrorHandlerMiddleware, REQUEST_ID_HEADER; print('imports OK')"` exits 0 với output `imports OK`.
- `cd Hub_All/api && uv run ruff check app/middleware` exits 0.
- `cd Hub_All/api && uv run mypy app/middleware` exits 0.
</acceptance_criteria>
</task>

<task id="02">
<action>
Cập nhật `Hub_All/api/app/pkg/response.py` để error code = `UPPER_SNAKE_CASE` match Go `backend/internal/pkg/response/response.go` line 60-103. Lý do: D6 quy định envelope `{success, data, error, meta}` "shape-identical Go cũ" — frontend React 19 hiện hữu (`frontend/src/services/api.ts`) có thể check `error.code === "UNAUTHORIZED"` để hiển thị thông báo. Nếu Python trả lowercase `"unauthorized"`, frontend không match → modal lỗi không hiện đúng.

**Sửa các hàm sau** (KHÔNG đổi signature, chỉ đổi default value của tham số `code`):

| Hàm | code cũ (lowercase) | code mới (Go-compat UPPER) |
|---|---|---|
| `bad_request` | `"bad_request"` | `"BAD_REQUEST"` |
| `unauthorized` | `"unauthorized"` | `"UNAUTHORIZED"` |
| `forbidden` | `"forbidden"` | `"FORBIDDEN"` |
| `not_found` | `"not_found"` | `"NOT_FOUND"` |
| `conflict` | `"conflict"` | `"CONFLICT"` |
| `unsupported_format` | `"unsupported_format"` | `"UNSUPPORTED_FORMAT"` |
| `too_many_requests` | `"rate_limited"` | `"RATE_LIMIT_EXCEEDED"` (match Go line 95) |
| `internal_error` | `"internal_error"` | `"INTERNAL_ERROR"` |
| `service_unavailable` | `"service_unavailable"` | `"SERVICE_UNAVAILABLE"` |

**Thêm hàm mới `validation_error`** (Pydantic validation 422):

```python
def validation_error(
    message: str = "Dữ liệu không hợp lệ",
    code: str = "VALIDATION_ERROR",
    details: dict[str, Any] | None = None,
) -> JSONResponse:
    """422 Unprocessable Entity — Pydantic v2 validation fail.

    Phase 3 router login/refresh dùng để trả lỗi validation request body.
    """
    return _error(code, message, status.HTTP_422_UNPROCESSABLE_ENTITY, details)
```

**KHÔNG đổi** `ok()`, `created()`, `accepted()`, `paginated()` — success helpers giữ nguyên.

**Lý do KHÔNG đổi response shape Go-compat `omitempty`:**  Python `JSONResponse(content={"data": None})` emit `"data": null` (KHÔNG omit) — frontend D6 đã chấp nhận shape có `null` từ Phase 1. Giữ envelope 4-field cứng `{success, data, error, meta}` mọi response.

**Lưu ý docstring thêm note phía trên `bad_request`:**

```python
# NOTE: Error code convention = UPPER_SNAKE_CASE match Go pkg/response (Phase 3
# AUTH-01). Frontend React 19 `services/api.ts` switch trên `error.code` —
# đổi sang lowercase sẽ break D6.
```
</action>
<read_first>
- Hub_All/api/app/pkg/response.py
- Hub_All/backend/internal/pkg/response/response.go
- Hub_All/api/app/main.py
</read_first>
<acceptance_criteria>
- `grep -c 'code: str = "BAD_REQUEST"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'code: str = "UNAUTHORIZED"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'code: str = "FORBIDDEN"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'code: str = "NOT_FOUND"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'code: str = "CONFLICT"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'code: str = "RATE_LIMIT_EXCEEDED"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'code: str = "INTERNAL_ERROR"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'def validation_error' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c '"VALIDATION_ERROR"' Hub_All/api/app/pkg/response.py` ≥ 1.
- `grep -c 'UPPER_SNAKE_CASE match Go' Hub_All/api/app/pkg/response.py` ≥ 1.
- Lowercase regression check: `grep -Pc '"(unauthorized|forbidden|bad_request|not_found|conflict|internal_error|rate_limited|service_unavailable)"' Hub_All/api/app/pkg/response.py` = 0 (KHÔNG còn lowercase mặc định).
- `cd Hub_All/api && uv run python -c "from app.pkg import response as r; import json; body = json.loads(r.unauthorized().body); assert body['error']['code']=='UNAUTHORIZED', body; print('OK')"` exits 0 với output `OK`.
- `cd Hub_All/api && uv run python -c "from app.pkg.response import validation_error; r = validation_error(); import json; body = json.loads(r.body); assert body['error']['code']=='VALIDATION_ERROR' and body['success'] is False; print('OK')"` exits 0.
- `cd Hub_All/api && uv run ruff check app/pkg/response.py` exits 0.
- `cd Hub_All/api && uv run mypy app/pkg/response.py` exits 0.
</acceptance_criteria>
</task>

<task id="03">
<action>
Cập nhật `Hub_All/api/app/config.py` thêm pydantic `field_validator` cho `cors_allowed_origins` — REJECT LAN origin khi `app_env=="production"` (P12 mitigation).

**LAN pattern reject (regex):** `localhost`, `127\.0\.0\.1`, `0\.0\.0\.0`, `192\.168\.`, `10\.`, `172\.(1[6-9]|2[0-9]|3[01])\.`. Match cả http/https/ws/wss scheme — chỉ check phần host.

**Validator phải dùng `mode="after"`** (KHÔNG `mode="before"`) để `info.data["app_env"]` đã được populate. Đặt SAU validator `_parse_csv` (mode=before) đã có sẵn.

Paste-ready (THÊM vào class `Settings`, KHÔNG xóa validator `_parse_csv` cũ):

```python
import re  # add import ở đầu file nếu chưa có


# ... bên trong class Settings, sau validator _parse_csv hiện có:

@field_validator("cors_allowed_origins", mode="after")
@classmethod
def _no_lan_in_prod(cls, v: list[str], info: ValidationInfo) -> list[str]:
    """Reject LAN/localhost origin trong production env (P12 mitigation).

    Nếu deploy production mà CORS list lọt `http://192.168.x.x` hoặc `localhost`,
    attacker trong cùng LAN có thể bypass CORS gọi API → leak credentials.
    Fail-fast ngay startup, KHÔNG defer runtime.

    Dev/staging chấp nhận localhost — chỉ reject khi `app_env=="production"`.
    """
    if info.data.get("app_env") != "production":
        return v
    forbidden_patterns = [
        r"localhost",
        r"127\.0\.0\.1",
        r"0\.0\.0\.0",
        r"192\.168\.",
        r"\b10\.",
        r"172\.(1[6-9]|2[0-9]|3[01])\.",
    ]
    for origin in v:
        for pat in forbidden_patterns:
            if re.search(pat, origin):
                raise ValueError(
                    f"Production cấm CORS origin LAN/localhost: {origin!r} "
                    f"(match pattern {pat!r}). P12 mitigation."
                )
    return v
```

**Cần thêm import `ValidationInfo`:** Sửa dòng import `from pydantic import Field, field_validator` thành `from pydantic import Field, ValidationInfo, field_validator`.

**KHÔNG đổi** các field khác (jwt_*, redis_url, database_url, app_namespace...) — giữ nguyên Phase 1 contract.
</action>
<read_first>
- Hub_All/api/app/config.py
- Hub_All/.planning/CONVENTIONS.md
- Hub_All/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- `grep -c '_no_lan_in_prod' Hub_All/api/app/config.py` ≥ 1.
- `grep -c 'mode="after"' Hub_All/api/app/config.py` ≥ 1.
- `grep -F '192.168.' Hub_All/api/app/config.py | wc -l` ≥ 1 (fixed-string match — regex escaping bypass cho IP pattern).
- `grep -F '127.0.0.1' Hub_All/api/app/config.py | wc -l` ≥ 1 (fixed-string match).
- `grep -c 'ValidationInfo' Hub_All/api/app/config.py` ≥ 1.
- `grep -c 'P12 mitigation' Hub_All/api/app/config.py` ≥ 1.
- `cd Hub_All/api && APP_ENV=production DATABASE_URL=postgresql+asyncpg://x:x@localhost/x COCOINDEX_DATABASE_URL=postgresql://x:x@localhost/x REDIS_URL=redis://localhost CORS_ALLOWED_ORIGINS='http://192.168.1.1:5173' uv run python -c "from app.config import get_settings; get_settings.cache_clear(); get_settings()" 2>&1 | grep -c 'Production cấm CORS origin LAN'` ≥ 1.
- `cd Hub_All/api && APP_ENV=dev DATABASE_URL=postgresql+asyncpg://x:x@localhost/x COCOINDEX_DATABASE_URL=postgresql://x:x@localhost/x REDIS_URL=redis://localhost CORS_ALLOWED_ORIGINS='http://localhost:5173' uv run python -c "from app.config import get_settings; get_settings.cache_clear(); print(get_settings().cors_allowed_origins)"` exits 0 (dev chấp nhận localhost).
- `cd Hub_All/api && uv run ruff check app/config.py` exits 0.
- `cd Hub_All/api && uv run mypy app/config.py` exits 0.
</acceptance_criteria>
</task>

<task id="04">
<action>
Cập nhật `Hub_All/api/app/main.py` để wire 3 middleware mới (RequestId, SecurityHeaders, ErrorHandler) vào `create_app()`. **Tuân thủ thứ tự P11** — FastAPI add CUỐI = outermost (chạy ĐẦU cho incoming request).

**Final outer→inner order (request flow ngoài vào trong):**
1. ErrorHandler (outermost = add LAST) — catch mọi exception bên trong
2. RequestId — gắn `X-Request-Id` sớm để middleware downstream + handler có ID
3. SecurityHeaders — set header bảo mật
4. CORS (đã có Phase 1) — handle preflight + Access-Control-Allow-*
5. (Phase 3: rate_limit placeholder — KHÔNG wire trong plan này; Plan 03-04 hoặc Phase 5 AUX-03 wire slowapi đầy đủ. Phase 3 giữ stub trong comment.)
6. (Innermost = router handler)

**Code add_middleware ngược thứ tự:** Inner (chạy gần handler) → add ĐẦU. Outer (chạy đầu request) → add SAU.

**Sửa block `create_app()`** — giữ CORSMiddleware Phase 1 cũ, thêm 3 middleware mới ở đúng vị trí, cập nhật comment P11.

Paste-ready (thay block từ `# Middleware Phase 1` đến hết `app.add_middleware(CORSMiddleware, ...)` hiện có):

```python
    # Middleware order — P11 PITFALL (FastAPI executes last-added FIRST cho
    # incoming request). Order REVERSED từ Go Gin. Outer-to-inner cho REQUEST:
    #   1) ErrorHandler   (outermost — catch mọi exception kể cả CORS leak)
    #   2) RequestId      (gắn X-Request-Id sớm để log downstream có ID)
    #   3) SecurityHeaders (X-Content-Type-Options, X-Frame-Options, ...)
    #   4) CORS           (preflight OPTIONS + Access-Control-Allow-*)
    #   5) [Phase 5 AUX-03] rate_limit (slowapi — placeholder stub Phase 3,
    #      Plan 03-04 KHÔNG wire; full enable Phase 5)
    #   6) router handler (innermost)
    #
    # Add order = REVERSED (innermost trước, outermost sau):
    #   add CORS → add SecurityHeaders → add RequestId → add ErrorHandler
    app.add_middleware(
        CORSMiddleware,
        allow_origins=settings.cors_allowed_origins,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )
    app.add_middleware(SecurityHeadersMiddleware)
    app.add_middleware(RequestIdMiddleware)
    app.add_middleware(ErrorHandlerMiddleware)  # LAST add = OUTERMOST
```

**Thêm import** ở đầu file (sau `from fastapi.middleware.cors import CORSMiddleware`):

```python
from app.middleware import (
    ErrorHandlerMiddleware,
    RequestIdMiddleware,
    SecurityHeadersMiddleware,
)
```

**KHÔNG đổi** `lifespan`, `healthz`, `readyz` — giữ nguyên Phase 1 contract.
</action>
<read_first>
- Hub_All/api/app/main.py
- Hub_All/api/app/middleware/__init__.py
- Hub_All/.planning/CONVENTIONS.md (section 4 — Middleware Order)
- Hub_All/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- `grep -c 'from app.middleware import' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'app.add_middleware(ErrorHandlerMiddleware)' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'app.add_middleware(RequestIdMiddleware)' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'app.add_middleware(SecurityHeadersMiddleware)' Hub_All/api/app/main.py` ≥ 1.
- `grep -c 'LAST add = OUTERMOST' Hub_All/api/app/main.py` ≥ 1.
- Order check: trong file, dòng `app.add_middleware(CORSMiddleware` PHẢI xuất hiện TRƯỚC `app.add_middleware(SecurityHeadersMiddleware)`, và `app.add_middleware(ErrorHandlerMiddleware)` phải là dòng `add_middleware` cuối cùng. Verify: `cd Hub_All/api && uv run python -c "import re; lines=open('app/main.py').read(); cors=lines.find('add_middleware(CORSMiddleware'); sec=lines.find('add_middleware(SecurityHeadersMiddleware'); rid=lines.find('add_middleware(RequestIdMiddleware'); err=lines.find('add_middleware(ErrorHandlerMiddleware'); assert cors < sec < rid < err, f'order wrong: {cors} {sec} {rid} {err}'; print('order OK')"` exits 0.
- `cd Hub_All/api && uv run python -c "from app.main import create_app; app = create_app(); print(len(app.user_middleware))"` exits 0 và output ≥ 4 (CORS + Security + RequestId + ErrorHandler).
- `cd Hub_All/api && uv run ruff check app/main.py` exits 0.
- `cd Hub_All/api && uv run mypy app/main.py` exits 0.
</acceptance_criteria>
</task>

<task id="05">
<action>
Tạo `Hub_All/api/tests/test_middleware.py` — unit test verify 4 hành vi quan trọng nhất của middleware infra. KHÔNG cần testcontainers Postgres (test ở mức HTTP layer, dùng httpx AsyncClient + asgi-lifespan in-process).

**4 test bắt buộc:**

1. `test_request_id_generated_when_missing` — Client KHÔNG gửi `X-Request-Id` → response header có `X-Request-Id` UUID4-format.
2. `test_request_id_echoed_when_present` — Client gửi `X-Request-Id: abc-123` → response echo NGUYÊN trị `abc-123` (KHÔNG validate format).
3. `test_security_headers_set` — Mọi response GET /healthz có 5 header: X-Content-Type-Options=nosniff, X-Frame-Options=DENY, X-XSS-Protection=1; mode=block, Referrer-Policy=strict-origin-when-cross-origin, Permissions-Policy chứa camera=().
4. `test_error_handler_returns_envelope_500` — Tạo route stub `/test/boom` raise `RuntimeError` → response 500, body match `{"success": false, "data": null, "error": {"code": "INTERNAL_ERROR", "message": "Lỗi máy chủ nội bộ"}, "meta": null}`.
5. `test_response_envelope_unauthorized_code_uppercase` — Call helper `response.unauthorized()` parse body → assert `body["error"]["code"] == "UNAUTHORIZED"` (Go-compat).
6. `test_cors_production_rejects_lan_origin` — Set `app_env=production`, `cors_allowed_origins=["http://192.168.1.1:5173"]` → `Settings()` raise `ValidationError` chứa "P12".
7. `test_http_exception_passes_through` — Tạo route stub `/test/raise-http` raise `HTTPException(401, detail="custom")` → ErrorHandlerMiddleware KHÔNG catch (pass-through tới FastAPI default handler). Verify response status=401 và body KHÔNG có shape `INTERNAL_ERROR` (nếu Plan 03-05 đã wire exception_handler thì shape sẽ là envelope `{success:false, error:{code, message}}`; Plan 03-01 không assert shape envelope vì exception_handler thuộc Plan 03-05 — chỉ assert status code đúng 401 và `INTERNAL_ERROR` KHÔNG xuất hiện trong response body).

Paste-ready (skeleton):

```python
"""Unit test middleware infra Phase 3 Plan 01.

Test in-process (httpx AsyncClient + asgi-lifespan) — KHÔNG cần Postgres/Redis.
Lifespan Phase 1 cho phép DB/Redis fail không crash, app vẫn start được.
"""
from __future__ import annotations

import json
import re

import pytest
from asgi_lifespan import LifespanManager
from fastapi import FastAPI
from httpx import ASGITransport, AsyncClient

from app.main import create_app
from app.pkg import response as resp


@pytest.fixture
async def client() -> AsyncClient:
    app = create_app()
    async with LifespanManager(app):
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as c:
            yield c


@pytest.mark.asyncio
async def test_request_id_generated_when_missing(client: AsyncClient) -> None:
    r = await client.get("/healthz")
    assert "x-request-id" in r.headers
    # UUID4 format pattern
    assert re.fullmatch(
        r"[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}",
        r.headers["x-request-id"],
    ), r.headers["x-request-id"]


@pytest.mark.asyncio
async def test_request_id_echoed_when_present(client: AsyncClient) -> None:
    r = await client.get("/healthz", headers={"X-Request-Id": "custom-trace-abc-123"})
    assert r.headers["x-request-id"] == "custom-trace-abc-123"


@pytest.mark.asyncio
async def test_security_headers_set(client: AsyncClient) -> None:
    r = await client.get("/healthz")
    assert r.headers["x-content-type-options"] == "nosniff"
    assert r.headers["x-frame-options"] == "DENY"
    assert r.headers["x-xss-protection"] == "1; mode=block"
    assert r.headers["referrer-policy"] == "strict-origin-when-cross-origin"
    assert "camera=()" in r.headers["permissions-policy"]


@pytest.mark.asyncio
async def test_error_handler_returns_envelope_500() -> None:
    """Mount route stub raise RuntimeError → envelope 500 KHÔNG leak stack."""
    app = create_app()

    @app.get("/test/boom")
    async def boom() -> None:
        raise RuntimeError("intentional explosion for test")

    async with LifespanManager(app):
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as c:
            r = await c.get("/test/boom")
    assert r.status_code == 500
    body = r.json()
    assert body["success"] is False
    assert body["data"] is None
    assert body["error"]["code"] == "INTERNAL_ERROR"
    assert body["error"]["message"] == "Lỗi máy chủ nội bộ"
    assert body["meta"] is None
    # Stack trace KHÔNG leak vào body
    assert "RuntimeError" not in r.text
    assert "intentional explosion" not in r.text


def test_response_envelope_unauthorized_code_uppercase() -> None:
    """AUTH-01 D6 — error code = UPPER_SNAKE_CASE match Go."""
    r = resp.unauthorized()
    body = json.loads(r.body)
    assert body["success"] is False
    assert body["error"]["code"] == "UNAUTHORIZED"
    assert r.status_code == 401


def test_response_envelope_forbidden_code_uppercase() -> None:
    r = resp.forbidden()
    body = json.loads(r.body)
    assert body["error"]["code"] == "FORBIDDEN"
    assert r.status_code == 403


def test_cors_production_rejects_lan_origin(monkeypatch: pytest.MonkeyPatch) -> None:
    """P12 mitigation — production env reject LAN origin ngay startup."""
    monkeypatch.setenv("APP_ENV", "production")
    monkeypatch.setenv("CORS_ALLOWED_ORIGINS", "http://192.168.1.5:5173")
    from app.config import get_settings

    get_settings.cache_clear()
    with pytest.raises(Exception) as exc_info:  # pydantic.ValidationError
        get_settings()
    assert "192.168" in str(exc_info.value) or "P12" in str(exc_info.value)


def test_cors_dev_allows_localhost(monkeypatch: pytest.MonkeyPatch) -> None:
    """Dev env chấp nhận localhost — chỉ production strict."""
    monkeypatch.setenv("APP_ENV", "dev")
    monkeypatch.setenv("CORS_ALLOWED_ORIGINS", "http://localhost:5173")
    from app.config import get_settings

    get_settings.cache_clear()
    s = get_settings()
    assert "http://localhost:5173" in s.cors_allowed_origins


@pytest.mark.asyncio
async def test_http_exception_passes_through() -> None:
    """ErrorHandlerMiddleware KHÔNG catch HTTPException → pass-through tới
    FastAPI exception_handler (Plan 03-05). Plan 03-01 chỉ verify status code
    đúng 401 và response body KHÔNG bị mask thành INTERNAL_ERROR envelope.
    """
    from fastapi import HTTPException
    app = create_app()

    @app.get("/test/raise-http")
    async def raise_http() -> None:
        raise HTTPException(status_code=401, detail={"code": "TEST_CUSTOM", "message": "test"})

    async with LifespanManager(app):
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as c:
            r = await c.get("/test/raise-http")
    assert r.status_code == 401, r.text
    # ErrorHandlerMiddleware KHÔNG được mask HTTPException thành INTERNAL_ERROR
    assert "INTERNAL_ERROR" not in r.text, f"HTTPException bị catch nhầm — body: {r.text}"
```
</action>
<read_first>
- Hub_All/api/tests/conftest.py
- Hub_All/api/app/main.py
- Hub_All/api/app/pkg/response.py
- Hub_All/api/app/middleware/__init__.py
- Hub_All/api/app/config.py
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/test_middleware.py` tồn tại.
- `grep -c 'def test_request_id_generated_when_missing' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `grep -c 'def test_request_id_echoed_when_present' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `grep -c 'def test_security_headers_set' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `grep -c 'def test_error_handler_returns_envelope_500' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `grep -c 'def test_response_envelope_unauthorized_code_uppercase' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `grep -c 'def test_cors_production_rejects_lan_origin' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `grep -c 'def test_http_exception_passes_through' Hub_All/api/tests/test_middleware.py` ≥ 1.
- `cd Hub_All/api && uv run pytest tests/test_middleware.py -v` exits 0 với ≥ 8 test PASS (7 cộng `test_http_exception_passes_through`).
- `cd Hub_All/api && uv run ruff check tests/test_middleware.py` exits 0.
</acceptance_criteria>
</task>

## Verification

- `cd Hub_All/api && uv run pytest tests/test_middleware.py -v` PASS ≥ 7 test (request_id 2 + security 1 + error_handler 1 + envelope code 2 + cors validator 2).
- `cd Hub_All/api && uv run ruff check app/middleware app/pkg/response.py app/main.py app/config.py tests/test_middleware.py` exits 0.
- `cd Hub_All/api && uv run mypy app/middleware app/pkg/response.py app/main.py app/config.py` exits 0.
- `cd Hub_All/api && uv run python -c "from app.main import create_app; app = create_app(); names = [m.cls.__name__ for m in app.user_middleware]; assert 'ErrorHandlerMiddleware' in names and 'RequestIdMiddleware' in names and 'SecurityHeadersMiddleware' in names and 'CORSMiddleware' in names; print('middleware wired:', names)"` exits 0 với output liệt kê 4 middleware.

## Threat Model

**Trust boundaries:**

| Boundary | Description |
|---|---|
| Browser/curl → FastAPI middleware chain | Untrusted input (request headers, body, query params) cross qua chain. Trust set sau JWT verify (Plan 03-04 dependency `get_current_user`). |
| Handler → log/Sentry (Phase 10) | Exception payload có thể chứa PII (email, token). ErrorHandlerMiddleware log structured (request_id + path + method) NHƯNG KHÔNG log full body request — tránh leak PII vào log file. |
| Production CORS list → browser | Origin sai (LAN) → cross-site request từ malicious page trong cùng LAN có thể bypass CORS. P12 mitigation: pydantic validator reject startup time. |

**STRIDE Threat Register:**

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | I (Information disclosure) | ErrorHandlerMiddleware leak stack trace vào response body | mitigate | Catch generic `Exception`, log server-side qua `logger.error(exc_info=exc)`, response body CHỈ chứa `{"code":"INTERNAL_ERROR", "message":"Lỗi máy chủ nội bộ"}` — KHÔNG include `str(exc)` hoặc traceback. Test `test_error_handler_returns_envelope_500` assert `"RuntimeError" not in r.text`. |
| T-03-02 | T (Tampering) | Client gửi `X-Request-Id` chứa CRLF injection (header splitting) | accept | Starlette `Response.headers[k]=v` đã encode safely (raise nếu chứa `\r\n`). Pattern này được Starlette test cover. Risk thấp + mitigation đã built-in. Documented không có CRLF validation thủ công trong RequestIdMiddleware. |
| T-03-03 | E (Elevation of privilege) | CORS production list chứa LAN origin → cross-origin leak credential | mitigate | P12 mitigation: pydantic `field_validator(mode="after")` reject `192.168.*`, `10.*`, `172.16-31.*`, `localhost`, `127.0.0.1`, `0.0.0.0` khi `app_env=="production"`. Fail-fast startup. Test `test_cors_production_rejects_lan_origin` verify raise. |
| T-03-04 | S (Spoofing) | Client spoof `X-Forwarded-For`/`Host` → log sai source IP | accept | Phase 3 chưa đụng `X-Forwarded-For` (rate limit dùng Redis key `rl:<ip>` — Phase 5 AUX-03). Khi Phase 5 wire slowapi, sẽ set `TrustedHostMiddleware` + `ProxyHeadersMiddleware` correct trust list. Phase 3 accept với rationale (chưa expose rate limit). |
| T-03-05 | I (Information disclosure) | `SecurityHeadersMiddleware` thiếu CSP → XSS reflected | accept | M2 chấp nhận risk CSP defer. Phase 10 HARD-04 sẽ add CSP report-only đầu tiên rồi enforce. Mitigation interim: `X-Content-Type-Options: nosniff` + `X-Frame-Options: DENY` đã giảm bề mặt tấn công cơ bản. Documented Out of Scope Phase 3. |

## Output

Sau khi complete tạo `Hub_All/.planning/phases/03-auth-port-rbac-response-envelope/03-01-SUMMARY.md` ghi rõ: 8 file modified/created, error code Go-compat UPPER_SNAKE bake xong, middleware order P11 verify đúng, test 7/7 PASS, sẵn sàng cho Plan 03-04 wire auth router.
