---
phase: 08.2-mcp-service-standalone-process
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Hub_All/mcp_service/pyproject.toml
  - Hub_All/mcp_service/README.md
  - Hub_All/mcp_service/.env.example
  - Hub_All/mcp_service/mcp_app/__init__.py
  - Hub_All/mcp_service/mcp_app/config.py
  - Hub_All/mcp_service/mcp_app/schemas.py
  - Hub_All/mcp_service/mcp_app/api_client.py
autonomous: true
requirements: [MCP-01, MCP-02]
must_haves:
  truths:
    - "Thư mục Hub_All/mcp_service/ tồn tại như một Python package độc lập, dependency chỉ gồm mcp, httpx, pydantic, pydantic-settings — KHÔNG có app.* hay fastapi"
    - "ApiClient gọi được API Service qua HTTP httpx, forward header X-API-Key, unwrap envelope {success,data,error}"
    - "Cấu hình base URL API Service đọc từ env var, có validate scheme http/https chống SSRF"
  artifacts:
    - path: "Hub_All/mcp_service/pyproject.toml"
      provides: "Khai báo package độc lập + dependency tối thiểu"
      contains: "httpx"
    - path: "Hub_All/mcp_service/mcp_app/api_client.py"
      provides: "ApiClient httpx — gọi API Service, unwrap envelope, forward X-API-Key"
      min_lines: 60
    - path: "Hub_All/mcp_service/mcp_app/schemas.py"
      provides: "6 Pydantic output model cho 3 tool MCP"
      contains: "class AskAnswer"
    - path: "Hub_All/mcp_service/mcp_app/config.py"
      provides: "Settings đọc env — API base URL + validate scheme"
      contains: "api_base_url"
  key_links:
    - from: "Hub_All/mcp_service/mcp_app/api_client.py"
      to: "API Service qua HTTP"
      via: "httpx.AsyncClient"
      pattern: "httpx\\.AsyncClient"
---

<objective>
Dựng nền móng cho MCP Service độc lập: tạo cây thư mục `Hub_All/mcp_service/` như một Python package tách hẳn khỏi `api/`, với dependency tối thiểu (`mcp`, `httpx`, `pydantic`, `pydantic-settings`) — KHÔNG import `app.*`. Plan này tạo 3 mảnh nền: (1) `config.py` đọc base URL API Service từ env có validate chống SSRF, (2) `schemas.py` — 6 Pydantic output model cho 3 tool, (3) `api_client.py` — lớp `ApiClient` dùng `httpx.AsyncClient` gọi API Service, forward `X-API-Key`, unwrap envelope `{success,data,error}`.

Mục đích: Đảo decision D-04 của Phase 8.1 — MCP tool sẽ gọi API qua HTTP thay vì import service layer in-process. Plan 03 (MCP server + tools) build trên các interface plan này tạo ra.
Output: Package `mcp_service/` có khung + 3 module nền (`config`, `schemas`, `api_client`) — chưa có MCP server, chưa có entrypoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-CONTEXT.md

<interfaces>
<!-- Output schema MCP hiện tại (Phase 8.1) — TÁI DÙNG NGUYÊN cho mcp_service. Trích từ api/app/mcp/schemas.py -->
6 Pydantic BaseModel cần port sang `mcp_service/mcp_app/schemas.py`:
```python
class CitationItem(BaseModel):
    chunk_id: str
    document_name: str
    hub_name: str
    snippet: str
    score: float

class AskAnswer(BaseModel):
    answer: str            # giữ nguyên marker [N]
    citations: list[CitationItem]

class SearchResultItem(BaseModel):
    chunk_id: str
    content: str
    score: float
    document_name: str
    hub_name: str
    hub_id: str

class SearchResult(BaseModel):
    results: list[SearchResultItem]
    total: int

class HubItem(BaseModel):
    id: str
    name: str
    description: str | None = None

class HubList(BaseModel):
    hubs: list[HubItem]
```

<!-- Envelope API Service trả về (api/app/pkg/response.py) — ApiClient phải unwrap -->
Mọi endpoint API Service trả JSON shape:
```json
{"success": true, "data": {...}, "error": null, "meta": {...}}
```
Lỗi:
```json
{"success": false, "data": null, "error": {"code": "INVALID_API_KEY", "message": "..."}, "meta": null}
```
HTTP status: 200 thành công, 400 INVALID_QUERY, 401 INVALID_API_KEY, 403 FORBIDDEN (hub isolation), 500 LLM_FAILED/EMBEDDING_FAILED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo khung package mcp_service/ + pyproject.toml + README + .env.example</name>
  <read_first>
    - Hub_All/api/pyproject.toml — tham chiếu cấu trúc [project], [build-system] hatchling, [tool.ruff], [tool.mypy], [tool.pytest.ini_options] để giữ nhất quán convention dự án.
    - .planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-CONTEXT.md — quyết định kiến trúc D-01..D-14 (mcp_service đảo D-04).
  </read_first>
  <action>
    Tạo cây thư mục `Hub_All/mcp_service/` với:

    1. `Hub_All/mcp_service/pyproject.toml` — package độc lập tên `medinet-wiki-mcp-service`, `requires-python = ">=3.11,<3.13"`. Dependency `[project].dependencies`:
       - `"mcp>=1.27.0,<1.28"` (cùng pin với api/pyproject.toml — tránh divergence)
       - `"httpx>=0.27,<1"`
       - `"pydantic>=2.7.0,<3"`
       - `"pydantic-settings>=2.6,<3"`
       TUYỆT ĐỐI KHÔNG có `fastapi`, `cocoindex`, `asyncpg`, `sqlalchemy`, hay bất kỳ dep nào của `api/`.
       `[project.optional-dependencies].dev`: `"pytest>=8,<9"`, `"pytest-asyncio>=0.24,<1"`, `"ruff>=0.6,<1"`, `"mypy>=1.11,<2"`, `"respx>=0.21,<1"` (mock httpx cho test).
       `[build-system]`: `requires = ["hatchling"]`, `build-backend = "hatchling.build"`.
       `[tool.hatch.build.targets.wheel]`: `packages = ["mcp_app"]`.
       `[tool.ruff]`: `line-length = 100`, `target-version = "py312"`. `[tool.ruff.lint]`: `select = ["E", "F", "I", "B", "UP", "N", "C90", "W"]`, `ignore = ["E501"]`.
       `[tool.mypy]`: `python_version = "3.12"`, `strict = true`, `ignore_missing_imports = true`.
       `[tool.pytest.ini_options]`: `asyncio_mode = "auto"`, `testpaths = ["tests"]`, `python_files = ["test_*.py"]`, `addopts = "-ra --strict-markers"`.

    2. `Hub_All/mcp_service/mcp_app/__init__.py` — file rỗng với `from __future__ import annotations`.

    3. `Hub_All/mcp_service/README.md` (tiếng Việt có dấu) — mô tả: MCP Service là process độc lập tách khỏi API Service (Phase 8.2, đảo D-04 Phase 8.1). Kiến trúc: `LLM Agent ─► MCP Service ─► API Service (HTTP)`. Hướng dẫn chạy: `cd Hub_All/mcp_service && uv run python -m mcp_app.server` (hoặc `python -m mcp_app.server`). Port mặc định 8190. Biến môi trường: `MCP_API_BASE_URL`, `MCP_SERVICE_PORT`, `MCP_SERVICE_HOST`. Ghi rõ: MCP Service KHÔNG cần truy cập DB/Redis — mọi data đi qua API Service.

    4. `Hub_All/mcp_service/.env.example` — các key: `MCP_API_BASE_URL=http://localhost:8180` (base URL API Service, KHÔNG kèm `/api`), `MCP_SERVICE_HOST=0.0.0.0`, `MCP_SERVICE_PORT=8190`, `MCP_HTTP_TIMEOUT=30`.
  </action>
  <verify>
    <automated>test -f Hub_All/mcp_service/pyproject.toml && test -f Hub_All/mcp_service/mcp_app/__init__.py && test -f Hub_All/mcp_service/README.md && test -f Hub_All/mcp_service/.env.example && ! grep -qE "fastapi|cocoindex|asyncpg|sqlalchemy" Hub_All/mcp_service/pyproject.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/mcp_service/pyproject.toml` tồn tại, chứa chuỗi `httpx`, `mcp>=1.27.0,<1.28`, `pydantic`.
    - `Hub_All/mcp_service/pyproject.toml` KHÔNG chứa chuỗi `fastapi`, `cocoindex`, `asyncpg`, `sqlalchemy` (grep -E trả rỗng).
    - `Hub_All/mcp_service/mcp_app/__init__.py` tồn tại.
    - `Hub_All/mcp_service/README.md` tồn tại, viết tiếng Việt có dấu, chứa chuỗi `MCP Service` và `8190`.
    - `Hub_All/mcp_service/.env.example` chứa các key `MCP_API_BASE_URL`, `MCP_SERVICE_PORT`.
  </acceptance_criteria>
  <done>Package skeleton mcp_service/ tồn tại với pyproject.toml dependency tối thiểu (không có dep của api/), README + .env.example đầy đủ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config.py — Settings đọc env + schemas.py — 6 Pydantic output model</name>
  <read_first>
    - Hub_All/api/app/mcp/schemas.py — 6 Pydantic model nguồn (CitationItem, AskAnswer, SearchResultItem, SearchResult, HubItem, HubList) — port NGUYÊN sang mcp_service.
    - Hub_All/api/app/config.py (dòng 17, 105-145) — pattern pydantic-settings BaseSettings + field_validator để tham chiếu (mcp_service tự viết bản tối giản, KHÔNG import).
  </read_first>
  <behavior>
    - Test 1: `Settings(api_base_url="http://localhost:8180")` khởi tạo OK, `api_base_url` được giữ nguyên.
    - Test 2: `Settings(api_base_url="ftp://evil")` raise ValidationError — scheme không phải http/https.
    - Test 3: `Settings(api_base_url="http://localhost:8180/")` — trailing slash bị strip, `api_base_url == "http://localhost:8180"`.
    - Test 4: `AskAnswer(answer="x [1]", citations=[])` khởi tạo OK; `SearchResult(results=[], total=0)` khởi tạo OK; `HubList(hubs=[])` khởi tạo OK.
  </behavior>
  <action>
    Tạo `Hub_All/mcp_service/mcp_app/config.py`:
    - `from __future__ import annotations`.
    - Class `Settings(BaseSettings)` (pydantic-settings) với `model_config = SettingsConfigDict(env_prefix="MCP_", env_file=".env", extra="ignore")`.
    - Field:
      - `api_base_url: str` — env `MCP_API_BASE_URL`, default `"http://localhost:8180"`.
      - `service_host: str` — env `MCP_SERVICE_HOST`, default `"0.0.0.0"`.
      - `service_port: int` — env `MCP_SERVICE_PORT`, default `8190`.
      - `http_timeout: float` — env `MCP_HTTP_TIMEOUT`, default `30.0`.
    - `@field_validator("api_base_url", mode="after")` — hàm `_validate_base_url`: (a) strip trailing `/`; (b) parse bằng `urllib.parse.urlparse`, nếu `scheme not in ("http", "https")` → `raise ValueError("MCP_API_BASE_URL phải dùng scheme http hoặc https — chống SSRF/misconfiguration")`; (c) nếu `netloc` rỗng → `raise ValueError("MCP_API_BASE_URL thiếu host")`. Trả về URL đã strip.
    - Hàm `get_settings() -> Settings` với `functools.lru_cache` — singleton.

    Tạo `Hub_All/mcp_service/mcp_app/schemas.py`:
    - `from __future__ import annotations` + `from pydantic import BaseModel`.
    - Port NGUYÊN 6 model từ `api/app/mcp/schemas.py`: `CitationItem`, `AskAnswer`, `SearchResultItem`, `SearchResult`, `HubItem`, `HubList` (xem block `<interfaces>` ở `<context>` — copy chính xác field + type).
    - Docstring tiếng Việt có dấu cho mỗi model.

    Tạo `Hub_All/mcp_service/tests/__init__.py` (rỗng) + `Hub_All/mcp_service/tests/test_config.py`:
    - Test 4 behavior ở trên dùng `pytest` + `pytest.raises(ValidationError)` cho Test 2.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && python -m pytest tests/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/mcp_service/mcp_app/config.py` chứa chuỗi `api_base_url` và `field_validator`.
    - `python -c "from mcp_app.config import Settings; Settings(api_base_url='ftp://x')"` raise lỗi (scheme reject).
    - `Hub_All/mcp_service/mcp_app/schemas.py` chứa `class AskAnswer`, `class SearchResult`, `class HubList`, `class CitationItem`, `class SearchResultItem`, `class HubItem`.
    - `pytest tests/test_config.py` exit code 0, 4 test PASS.
  </acceptance_criteria>
  <done>config.py validate base URL scheme http/https + strip trailing slash; schemas.py có đủ 6 model port từ Phase 8.1; test_config.py 4 test PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: api_client.py — ApiClient httpx gọi API Service, unwrap envelope, forward X-API-Key</name>
  <read_first>
    - Hub_All/api/app/routers/ask.py — shape response endpoint /api/ask + map lỗi (INVALID_QUERY, LLM_FAILED, EMBEDDING_FAILED).
    - Hub_All/api/app/routers/search.py — shape response /api/search, /api/search/cross-hub.
    - Hub_All/api/app/auth/dependencies.py (dòng 222-290) — get_api_key_or_jwt: API Service nhận X-API-Key qua header, trả 401 INVALID_API_KEY nếu sai.
    - Hub_All/mcp_service/mcp_app/config.py — Settings tạo ở Task 2 (api_base_url, http_timeout).
  </read_first>
  <behavior>
    - Test 1: response 200 envelope `{"success":true,"data":{"results":[]},"error":null}` → `ApiClient.post()` trả `{"results":[]}` (đã unwrap `data`).
    - Test 2: response 401 envelope `{"success":false,"error":{"code":"INVALID_API_KEY","message":"..."}}` → raise `ApiUnauthorizedError`.
    - Test 3: response 403 envelope `error.code == "FORBIDDEN"` → raise `ApiForbiddenError`.
    - Test 4: response 400 `error.code == "INVALID_QUERY"` → raise `ApiBadRequestError` mang message.
    - Test 5: response 500 `error.code == "LLM_FAILED"` → raise `ApiServerError`.
    - Test 6: header `X-API-Key` được gửi đúng giá trị truyền vào; KHÔNG log giá trị key (kiểm tra qua respx route assertion).
  </behavior>
  <action>
    Tạo `Hub_All/mcp_service/mcp_app/api_client.py`:
    - `from __future__ import annotations`, `import logging`, `from typing import Any`, `import httpx`.
    - Định nghĩa cây exception (kế thừa `Exception`):
      - `ApiClientError` — base.
      - `ApiUnauthorizedError(ApiClientError)` — 401, key thiếu/sai.
      - `ApiForbiddenError(ApiClientError)` — 403, hub isolation.
      - `ApiBadRequestError(ApiClientError)` — 400, query không hợp lệ.
      - `ApiServerError(ApiClientError)` — 5xx hoặc LLM_FAILED/EMBEDDING_FAILED.
    - Class `ApiClient`:
      - `__init__(self, base_url: str, timeout: float = 30.0)` — lưu `base_url`, tạo `self._client = httpx.AsyncClient(base_url=base_url, timeout=timeout)`.
      - `async def aclose(self) -> None` — `await self._client.aclose()`.
      - Hỗ trợ `async with` qua `__aenter__`/`__aexit__`.
      - `async def _request(self, method: str, path: str, *, api_key: str, json_body: dict[str, Any] | None = None, params: dict[str, Any] | None = None) -> Any`:
        - Header: `{"X-API-Key": api_key}`.
        - Gọi `self._client.request(method, path, headers=..., json=json_body, params=params)`.
        - QUAN TRỌNG — KHÔNG bao giờ `logger.info`/`logger.error` chứa `api_key` hay full URL có query string. Khi log lỗi chỉ log `method`, `path`, `status_code`, `error.code` — KHÔNG log header, KHÔNG log key.
        - Bắt `httpx.RequestError` (network/timeout) → `raise ApiServerError(f"Không kết nối được API Service: {type(e).__name__}")`.
        - Parse JSON response. Lấy `envelope = resp.json()`.
        - Nếu `resp.status_code == 200` và `envelope.get("success") is True` → `return envelope.get("data")`.
        - Ngược lại: đọc `err = envelope.get("error") or {}`, `code = err.get("code", "ERROR")`, `message = err.get("message", "Lỗi không xác định")`. Map:
          - `status_code == 401` → `raise ApiUnauthorizedError(message)`.
          - `status_code == 403` → `raise ApiForbiddenError(message)`.
          - `status_code == 400` → `raise ApiBadRequestError(message)`.
          - còn lại (5xx hoặc code `LLM_FAILED`/`EMBEDDING_FAILED`) → `raise ApiServerError(f"{code}: {message}")`.
        - Nếu JSON parse fail → `raise ApiServerError(f"API Service trả response không hợp lệ (status {resp.status_code})")`.
      - `async def get(self, path, *, api_key, params=None)` → `_request("GET", ...)`.
      - `async def post(self, path, *, api_key, json_body=None)` → `_request("POST", ...)`.
    - Mọi message tiếng Việt có dấu.

    Tạo `Hub_All/mcp_service/tests/test_api_client.py` — dùng `respx` mock httpx, phủ 6 behavior test ở trên. Test 6 dùng `respx` route assertion kiểm tra request header `X-API-Key`.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && python -m pytest tests/test_api_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/mcp_service/mcp_app/api_client.py` chứa chuỗi `httpx.AsyncClient` và `class ApiClient`.
    - File chứa định nghĩa 5 exception: `ApiClientError`, `ApiUnauthorizedError`, `ApiForbiddenError`, `ApiBadRequestError`, `ApiServerError`.
    - File KHÔNG chứa chuỗi `logger.*api_key` hay bất kỳ statement log header (grep `logger` không có dòng nào reference `api_key` hoặc `headers`).
    - File chứa header dict `{"X-API-Key": api_key}` (grep `X-API-Key` khớp).
    - `pytest tests/test_api_client.py` exit code 0, 6 test PASS.
  </acceptance_criteria>
  <done>ApiClient gọi API Service qua httpx, unwrap envelope data, map 401/403/400/5xx sang exception riêng, forward X-API-Key, không leak key vào log; 6 test PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP Service → API Service (HTTP) | Boundary HTTP mới sinh ra do tách process. `X-API-Key` đi qua đây — header phải bảo mật, không leak vào log. |
| env config → ApiClient | `MCP_API_BASE_URL` từ env không tin cậy nếu deploy sai — cần validate scheme. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-01-I | Information Disclosure | api_client.py — log statement | mitigate | `_request` chỉ log `method`/`path`/`status_code`/`error.code` — KHÔNG log header, KHÔNG log `api_key`, KHÔNG log URL có query string. AC kiểm tra grep. |
| T-08.2-01-T | Tampering / SSRF | config.py — `MCP_API_BASE_URL` | mitigate | `field_validator` reject scheme khác `http`/`https`, reject netloc rỗng → chống trỏ tới `file://`/`ftp://` hay địa chỉ nội bộ ngoài ý muốn do misconfig. |
| T-08.2-01-D | Denial of Service | ApiClient — request treo | mitigate | `httpx.AsyncClient(timeout=...)` đặt timeout từ config (default 30s) — request tới API Service không treo vô hạn. |
| T-08.2-01-S | Spoofing | ApiClient gửi key | accept | MCP Service forward key client cung cấp; API Service mới là bên verify. MCP không tự cấp quyền — rủi ro thấp, để API-side enforce. |
</threat_model>

<verification>
- `cd Hub_All/mcp_service && python -m pytest tests/ -q` — toàn bộ test Plan 01 PASS.
- `grep -rE "fastapi|cocoindex|asyncpg|import app\." Hub_All/mcp_service/mcp_app/` — KHÔNG có kết quả (package độc lập, không phụ thuộc api/).
- `cd Hub_All/mcp_service && python -m ruff check mcp_app/` — All checks passed.
</verification>

<success_criteria>
- Package `mcp_service/` tồn tại độc lập, dependency tối thiểu, không import `app.*`.
- `ApiClient` gọi được API Service qua httpx, unwrap envelope, forward `X-API-Key` an toàn (không leak log).
- `config.py` validate base URL chống SSRF.
- Toàn bộ test Plan 01 PASS.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08.2-mcp-service-standalone-process/08.2-01-SUMMARY.md`.
</output>
