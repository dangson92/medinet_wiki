---
status: partial
phase: 08.2-mcp-service-standalone-process
source: [08.2-VERIFICATION.md]
started: 2026-05-19T00:00:00Z
updated: 2026-05-19T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. SC4 — usage_events ghi row sau ask_wiki thành công (DB thật)
expected: Chạy API Service + MCP Service + Postgres thật; `SELECT count(*) FROM usage_events` trước/sau khi gọi `ask_wiki` qua MCP client với `X-API-Key` hợp lệ → số tăng đúng 1 (BackgroundTasks chạy sau response). Bước UAT chi tiết ghi trong `mcp_service/README.md` mục "UAT thủ công — SC4 usage_events".
result: [pending]

### 2. MCP Service chạy standalone end-to-end qua python -m mcp_app.server
expected: Khởi động MCP Service trên port 8190 (`python -m mcp_app.server`), kết nối MCP client thật (Claude/ChatGPT) qua transport Streamable HTTP, gọi 3 tool `list_hubs`/`search_wiki`/`ask_wiki` → tool trả kết quả; gọi với key thiếu/sai → `ToolError MCP_UNAUTHORIZED`.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
