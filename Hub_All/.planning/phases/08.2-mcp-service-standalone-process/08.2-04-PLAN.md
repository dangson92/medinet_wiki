---
phase: 08.2-mcp-service-standalone-process
plan: "04"
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - Hub_All/api/app/main.py
  - Hub_All/api/pyproject.toml
  - Hub_All/api/app/mcp/__init__.py
  - Hub_All/api/app/mcp/server.py
  - Hub_All/api/app/mcp/auth.py
  - Hub_All/api/app/mcp/schemas.py
  - Hub_All/api/tests/unit/mcp/__init__.py
  - Hub_All/api/tests/unit/mcp/test_mcp_auth.py
  - Hub_All/api/tests/unit/mcp/test_mcp_tools.py
  - Hub_All/api/tests/unit/mcp/test_mcp_mount.py
autonomous: true
requirements: [MCP-01, MCP-02]
must_haves:
  truths:
    - "api/app/main.py không còn mount /mcp, không còn _composed_lifespan, không còn _mcp_app, không còn inject mcp_set_pool"
    - "Thư mục api/app/mcp/ và api/tests/unit/mcp/ bị xoá hoàn toàn"
    - "api/pyproject.toml không còn dependency mcp"
    - "API Service vẫn khởi động + chạy bình thường sau khi gỡ MCP: regression test PASS"
  artifacts:
    - path: "Hub_All/api/app/main.py"
      provides: "App factory + lifespan đã gỡ sạch mọi wiring MCP"
      contains: "create_app"
  key_links:
    - from: "Hub_All/api/app/main.py"
      to: "lifespan (không còn MCP)"
      via: "lifespan=lifespan trực tiếp, không compose"
      pattern: "lifespan=lifespan"
---

<objective>
Gỡ MCP server in-process khỏi API Service. Sau khi Plan 03 chứng minh MCP Service standalone chạy được, plan này: (1) gỡ `app.mount("/mcp", ...)`, `_composed_lifespan`, `_mcp_app`, `_mcp_lifespan` khỏi `api/app/main.py`; (2) gỡ block inject `mcp_set_pool` trong `lifespan`; (3) xoá package `api/app/mcp/` (`__init__.py`, `server.py`, `auth.py`, `schemas.py`); (4) xoá thư mục test `api/tests/unit/mcp/`; (5) gỡ dependency `mcp>=1.27.0,<1.28` khỏi `api/pyproject.toml`.

Mục đích: Hoàn tất đảo D-04 — API Service không còn biết gì về MCP; MCP là process riêng. SC1 của phase: `api/app/main.py` không còn `/mcp` + `_composed_lifespan`.
Output: API Service sạch MCP, vẫn khởi động + chạy bình thường, regression test PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- api/app/main.py — các điểm MCP cần GỠ (Phase 8.1 thêm vào): -->
<!-- 1. lifespan() dòng ~93-101: block "Phase 8.1 MCP — inject pool" gọi mcp_set_pool. -->
<!-- 2. create_app() dòng ~330-367: import _mcp_server, _mcp_app = _mcp_server.streamable_http_app(), -->
<!--    _mcp_lifespan, @asynccontextmanager _composed_lifespan, FastAPI(lifespan=_composed_lifespan). -->
<!-- 3. create_app() dòng ~577-580: app.mount("/mcp", _mcp_app). -->
<!-- Sau khi gỡ: FastAPI(..., lifespan=lifespan) trực tiếp; lifespan() không còn block MCP. -->

<!-- api/pyproject.toml dòng ~37: "mcp>=1.27.0,<1.28", — GỠ dòng này. httpx GIỮ (dùng cho ai_chat router). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gỡ wiring MCP khỏi api/app/main.py + dependency khỏi pyproject.toml</name>
  <read_first>
    - Hub_All/api/app/main.py — file đang sửa: xem block MCP trong `lifespan()` (sau bước 2 redis init), và trong `create_app()` (TRƯỚC `FastAPI(...)` — `_mcp_server`/`_mcp_app`/`_composed_lifespan`; SAU include_router — `app.mount("/mcp", ...)`).
    - Hub_All/api/pyproject.toml — dòng dependency `"mcp>=1.27.0,<1.28",`.
  </read_first>
  <action>
    Sửa `Hub_All/api/app/main.py` — gỡ TẤT CẢ wiring MCP, KHÔNG đụng phần khác:

    1. Trong hàm `lifespan()`: XOÁ block "Phase 8.1 MCP — inject pool + redis vào module singleton" (đoạn comment + `if app.state.db_pool is not None: from app.mcp.server import set_pool as mcp_set_pool ... mcp_set_pool(...) ... logger.info("mcp_pool_injected")`). Giữ nguyên các bước 2.5, 3, 4... còn lại.

    2. Trong hàm `create_app()`:
       - XOÁ comment block "Phase 8.1 MCP — tạo ASGI sub-app TRƯỚC FastAPI init" + dòng `from app.mcp.server import mcp as _mcp_server` + `_mcp_app = _mcp_server.streamable_http_app()`.
       - XOÁ comment block "Compose lifespans" + dòng `_mcp_lifespan = _mcp_app.router.lifespan_context` + toàn bộ `@asynccontextmanager async def _composed_lifespan(app): ...`.
       - Đổi `app = FastAPI(title=..., version=..., lifespan=_composed_lifespan)` → `lifespan=lifespan` (dùng trực tiếp hàm `lifespan` module-level). Giữ `title`/`version`. Cập nhật/gỡ comment "CRITICAL: _composed_lifespan BẮT BUỘC...".
       - XOÁ comment block "Phase 8.1 MCP — mount ASGI sub-app tại /mcp" + dòng `app.mount("/mcp", _mcp_app)` (gần cuối `create_app()`, trước `return app`).

    3. Kiểm tra import: nếu `asynccontextmanager` chỉ còn dùng cho `lifespan` (vẫn dùng) → giữ import. Không gỡ import nào còn được dùng.

    Sửa `Hub_All/api/pyproject.toml`:
    - XOÁ dòng `"mcp>=1.27.0,<1.28",` trong `[project].dependencies`.
    - GIỮ `"httpx>=0.27,<1"` (router `ai_chat` dùng) và mọi dependency khác.

    Mọi comment mới (nếu có) viết tiếng Việt có dấu.
  </action>
  <verify>
    <automated>cd Hub_All/api && ! grep -qE "/mcp|_composed_lifespan|_mcp_app|mcp_set_pool|app\.mcp" app/main.py && ! grep -q "mcp>=1.27" pyproject.toml && python -c "import ast; ast.parse(open('app/main.py').read()); print('main.py syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/api/app/main.py` KHÔNG còn chuỗi `/mcp`, `_composed_lifespan`, `_mcp_app`, `_mcp_lifespan`, `mcp_set_pool`, `app.mcp` (grep -E trả rỗng).
    - `Hub_All/api/app/main.py` chứa `lifespan=lifespan` trong constructor `FastAPI(...)`.
    - `Hub_All/api/pyproject.toml` KHÔNG còn chuỗi `mcp>=1.27` (grep trả rỗng); VẪN còn `httpx`.
    - `python -c "import ast; ast.parse(open('app/main.py').read())"` không lỗi syntax.
  </acceptance_criteria>
  <done>api/app/main.py sạch mọi wiring MCP (mount, composed lifespan, pool inject); pyproject.toml gỡ dependency mcp; httpx giữ lại.</done>
</task>

<task type="auto">
  <name>Task 2: Xoá package api/app/mcp/ + thư mục test api/tests/unit/mcp/ + verify regression</name>
  <read_first>
    - Hub_All/api/app/mcp/ — 4 file sắp xoá (`__init__.py`, `server.py`, `auth.py`, `schemas.py`) + thư mục `__pycache__`.
    - Hub_All/api/tests/unit/mcp/ — 4 file test sắp xoá (`__init__.py`, `test_mcp_auth.py`, `test_mcp_tools.py`, `test_mcp_mount.py`).
    - Hub_All/api/app/main.py — file vừa sửa ở Task 1 (xác nhận không còn import `app.mcp`).
  </read_first>
  <action>
    1. Xoá toàn bộ thư mục `Hub_All/api/app/mcp/` — gồm `__init__.py`, `server.py`, `auth.py`, `schemas.py`, và `__pycache__/`. Dùng lệnh xoá thư mục (`rm -rf Hub_All/api/app/mcp`).

    2. Xoá toàn bộ thư mục `Hub_All/api/tests/unit/mcp/` — gồm `__init__.py`, `test_mcp_auth.py`, `test_mcp_tools.py`, `test_mcp_mount.py`, và `__pycache__/`.

    3. Quét toàn bộ `Hub_All/api/` tìm import còn sót tới `app.mcp` — `grep -rn "app.mcp\|app\.mcp\|from app import mcp" Hub_All/api/app Hub_All/api/tests` — nếu còn bất kỳ reference nào (ngoài 2 thư mục đã xoá), sửa/gỡ nó. Dự kiến KHÔNG còn (Task 1 đã gỡ ở main.py).

    4. Chạy regression test API: `cd Hub_All/api && python -m pytest tests/unit/ -q` — toàn bộ unit test PASS (không còn test MCP, các test khác không vỡ).

    5. Verify API Service khởi động được: `cd Hub_All/api && python -c "from app.main import create_app; app = create_app(); print('create_app OK', len(app.routes), 'routes')"` — `create_app()` chạy không lỗi, KHÔNG còn route `/mcp`.

    6. Verify không còn route /mcp: trong cùng lệnh hoặc lệnh riêng, assert không có route nào có path bắt đầu `/mcp`.
  </action>
  <verify>
    <automated>cd Hub_All/api && test ! -d app/mcp && test ! -d tests/unit/mcp && python -m pytest tests/unit/ -q && python -c "from app.main import create_app; app = create_app(); assert not any(getattr(r,'path','').startswith('/mcp') for r in app.routes), 'route /mcp vẫn tồn tại'; print('no /mcp route OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Thư mục `Hub_All/api/app/mcp/` KHÔNG còn tồn tại.
    - Thư mục `Hub_All/api/tests/unit/mcp/` KHÔNG còn tồn tại.
    - `grep -rn "app.mcp" Hub_All/api/app Hub_All/api/tests` — không có kết quả.
    - `pytest tests/unit/ -q` exit code 0 — regression unit PASS.
    - `create_app()` chạy không lỗi; không có route nào path bắt đầu `/mcp`.
  </acceptance_criteria>
  <done>Package api/app/mcp/ + test api/tests/unit/mcp/ xoá hoàn toàn; không còn import app.mcp; API Service khởi động bình thường, regression unit PASS, không còn route /mcp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Service process | Sau khi gỡ MCP, API Service chỉ phục vụ Frontend + MCP Service (qua HTTP) — không còn endpoint MCP nội bộ. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-04-D | Denial of Service | gỡ wiring làm API không khởi động | mitigate | Task 2 verify `create_app()` chạy + `pytest tests/unit/` PASS trước khi đóng plan — bắt lỗi gỡ nhầm. |
| T-08.2-04-T | Tampering | còn import app.mcp gây ImportError runtime | mitigate | Task 2 bước 3 grep toàn `api/` tìm reference sót; verify `create_app()` import-time không lỗi. |
| T-08.2-04-I | Information Disclosure | endpoint /mcp cũ còn lộ | mitigate | Verify command assert không còn route path `/mcp` — surface MCP cũ trong API Service bị gỡ sạch. |
</threat_model>

<verification>
- `test ! -d Hub_All/api/app/mcp && test ! -d Hub_All/api/tests/unit/mcp` — 2 thư mục đã xoá.
- `grep -rE "/mcp|_composed_lifespan|app\.mcp|mcp_set_pool" Hub_All/api/app/main.py` — không có kết quả.
- `cd Hub_All/api && python -m pytest tests/unit/ -q` — regression PASS.
- `cd Hub_All/api && python -c "from app.main import create_app; create_app()"` — khởi động không lỗi.
- `grep "mcp>=1.27" Hub_All/api/pyproject.toml` — không có kết quả.
</verification>

<success_criteria>
- `api/app/main.py` không còn `/mcp`, `_composed_lifespan`, `_mcp_app`, `mcp_set_pool`.
- `api/app/mcp/` và `api/tests/unit/mcp/` xoá hoàn toàn.
- `api/pyproject.toml` không còn dependency `mcp`.
- API Service khởi động + regression unit PASS.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08.2-mcp-service-standalone-process/08.2-04-SUMMARY.md`.
</output>
