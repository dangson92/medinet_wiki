---
phase: 08.2-mcp-service-standalone-process
verified: 2026-05-19T00:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "SC4 — usage_events ghi row sau ask_wiki thành công (DB thật)"
    expected: "Chạy API Service + MCP Service + Postgres thật; SELECT count(*) FROM usage_events trước/sau khi gọi ask_wiki qua MCP client với X-API-Key hợp lệ → số tăng đúng 1 (BackgroundTasks chạy sau response)"
    why_human: "Test tự động dùng respx mock HTTP — KHÔNG có Postgres trong vòng test. SC4 phủ gián tiếp API-side (unit Plan 02 Task 3 verify background_tasks.add_task gọi log_usage_event) nhưng row DB thật chỉ verify được qua UAT thủ công. Đã định trước ở giai đoạn plan (Plan 05 objective + threat T-08.2-05-R); bước UAT ghi trong mcp_service/README.md mục 'UAT thủ công — SC4 usage_events'."
  - test: "MCP Service chạy standalone end-to-end qua python -m mcp_app.server"
    expected: "Khởi động MCP Service trên port 8190, kết nối MCP client (Claude/ChatGPT), gọi 3 tool list_hubs/search_wiki/ask_wiki qua transport Streamable HTTP thật → tool trả kết quả; auth fail trả ToolError MCP_UNAUTHORIZED"
    why_human: "build_asgi_app() tạo ASGI app thành công (verify tự động) nhưng hành vi runtime với MCP client thật + transport Streamable HTTP + uvicorn server cần kiểm thử thủ công — không verify được bằng grep/unit test."
---

# Phase 8.2: MCP Service — Tách Thành Process Độc Lập — Báo cáo Xác minh

**Mục tiêu phase:** Tách MCP server khỏi process FastAPI thành một service độc lập (`Hub_All/mcp_service/` — process/runtime + port riêng) gọi API Service qua HTTP — đảo decision D-04 của Phase 8.1. Kiến trúc đích: Frontend ─► API Service; LLM Agent ─► MCP Service ─► API Service.

**Verified:** 2026-05-19
**Status:** human_needed
**Re-verification:** Không — xác minh lần đầu

## Đạt mục tiêu

### Các Observable Truth (5 Success Criteria từ ROADMAP)

| #   | Truth (Success Criteria)                                                                                                  | Trạng thái  | Bằng chứng |
| --- | ------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------- |
| 1   | MCP Service chạy process riêng (`Hub_All/mcp_service/`, port riêng), KHÔNG còn mount trong FastAPI; `main.py` không còn `/mcp` + `_composed_lifespan` | ✓ VERIFIED  | `grep -E "/mcp\|_composed_lifespan\|_mcp_app\|mcp_set_pool\|app.mcp" api/app/main.py` → rỗng (exit 1). `api/app/main.py:326` chứa `lifespan=lifespan` trực tiếp. Thư mục `api/app/mcp/` và `api/tests/unit/mcp/` đã xoá (test -d → GONE). `mcp_service/mcp_app/server.py:291-309` có `main()` + `uvicorn.run(host=service_host, port=service_port)`; `mcp_service/mcp_app/config.py:32` default port 8190. |
| 2   | 3 tool `list_hubs`/`search_wiki`/`ask_wiki` gọi API qua HTTP, forward `X-API-Key`, unwrap envelope `{success,data,error}`   | ✓ VERIFIED  | `server.py` — 3 tool decorate `@mcp.tool()` gọi `_get_client().get/post` (`/api/hubs`, `/api/search`, `/api/search/cross-hub`, `/api/ask`, `/api/search/answer`). `api_client.py:79` header `{"X-API-Key": api_key}`; `api_client.py:103-104` unwrap `envelope.get("data")` khi `success is True`. API-side: `search.py`/`ask.py` 6 endpoint dùng `Depends(get_api_key_or_jwt_with_hubs)`, `hubs.py:58` `GET /api/hubs` dùng `Depends(get_api_key_or_jwt)`. `test_integration_tools.py` 9 test respx PASS. |
| 3   | Auth fail (thiếu/sai key) → API trả 401 → MCP map sang `ToolError MCP_UNAUTHORIZED`; hub isolation enforce API-side          | ✓ VERIFIED  | `auth.py:62-66` `require_api_key` raise `ToolError("MCP_UNAUTHORIZED: ...")` khi thiếu key. `api_client.py:117-118` 401 → `ApiUnauthorizedError`; `server.py:82-83` `_map_api_error` → `ToolError("MCP_UNAUTHORIZED")`; `:84-85` `ApiForbiddenError` (403) → `ToolError("HUB_ACCESS_DENIED")`. Hub isolation API-side: `get_api_key_or_jwt_with_hubs` (`dependencies.py:292`) load `hub_ids` từ `user_hubs`. `test_integration_tools.py` Test 6/7/8 PASS. |
| 4   | `usage_events` vẫn có row sau mỗi `ask_wiki` thành công (do `/api/ask` BackgroundTasks ghi — MCP không tự ghi)               | ? UNCERTAIN | API-side ĐÚNG: `ask.py:139-140` + `:286-287` `background_tasks.add_task(log_usage_event, ...)` GIỮ NGUYÊN, không phụ thuộc kiểu auth; `test_apikey_search_ask_auth.py:214-216` verify `add_task` gọi với `log_usage_event` qua X-API-Key path (Test 6 PASS). MCP-side: `server.py` KHÔNG import `log_usage_event` (xác nhận grep rỗng). **Row DB thật không verify được tự động (respx mock không có Postgres) → human_needed — đã định trước ở plan.** |
| 5   | Test suite `mcp_service/` mock httpx call tới API; regression API không vỡ khi gỡ mount `/mcp`                              | ✓ VERIFIED  | `mcp_service/tests/` — **33/33 PASS** (4 config + 6 api_client + 5 auth + 9 server + 9 integration_tools); `test_integration_tools.py` chứa `respx`. API regression `tests/unit/` — **119/119 PASS** (gồm `test_hubs_apikey_list.py` 3 + `test_apikey_search_ask_auth.py` 7). |

**Score:** 5/5 truths verified (SC4 verify đúng bản chất: API-side đạt — verified; row DB thật là `human_needed` đã định trước, không tính là gap).

### Required Artifacts

| Artifact | Mong đợi | Trạng thái | Chi tiết |
| -------- | -------- | ---------- | -------- |
| `mcp_service/pyproject.toml` | Package độc lập + dep tối thiểu | ✓ VERIFIED | Chứa `mcp>=1.27.0,<1.28`, `httpx>=0.27,<1`, `pydantic`, `pydantic-settings`; KHÔNG có `fastapi`/`cocoindex`/`asyncpg`/`sqlalchemy`. |
| `mcp_service/mcp_app/config.py` | Settings + validate scheme chống SSRF | ✓ VERIFIED | `Settings(BaseSettings)` env prefix `MCP_`; `field_validator` `_validate_base_url` ép scheme http/https + reject netloc rỗng + strip trailing slash. |
| `mcp_service/mcp_app/schemas.py` | 6 Pydantic output model | ✓ VERIFIED | 6 model (`CitationItem`, `AskAnswer`, `SearchResultItem`, `SearchResult`, `HubItem`, `HubList`) — import thành công ở `server.py`. |
| `mcp_service/mcp_app/api_client.py` | ApiClient httpx — unwrap envelope, forward X-API-Key | ✓ VERIFIED | 144 dòng; `httpx.AsyncClient`, 5 exception, header `X-API-Key`, map 401/403/400/5xx; log chỉ `method/path/status_code/error_code` — không leak key. |
| `mcp_service/mcp_app/auth.py` | Đọc X-API-Key từ HTTP header MCP | ✓ VERIFIED | `extract_api_key` đọc `ctx.request_context.request.headers.get("x-api-key")`; `require_api_key` raise `ToolError MCP_UNAUTHORIZED`; KHÔNG verify DB. |
| `mcp_service/mcp_app/server.py` | FastMCP + 3 tool + entrypoint standalone | ✓ VERIFIED | 314 dòng; `streamable_http_app`, `uvicorn`, 3 tool `@mcp.tool()`, `build_asgi_app()` + `main()` + `if __name__=="__main__"`. `build_asgi_app()` chạy → `Starlette` app OK. |
| `api/app/auth/dependencies.py` | `get_api_key_or_jwt_with_hubs` | ✓ VERIFIED | `dependencies.py:292` `async def get_api_key_or_jwt_with_hubs` — verify X-API-Key HOặC JWT, load `hub_ids` từ `user_hubs`. |
| `api/app/routers/hubs.py` | GET /api/hubs nhận X-API-Key | ✓ VERIFIED | `hubs.py:58` `GET /api/hubs` dùng `Depends(get_api_key_or_jwt)`; 5 endpoint mutate vẫn `require_role("admin")` (5 dòng). |
| `api/app/routers/search.py` | 3 endpoint search nhận X-API-Key | ✓ VERIFIED | 3 endpoint dùng `Depends(get_api_key_or_jwt_with_hubs)`; grep `get_current_user_with_hubs` → 0. |
| `api/app/routers/ask.py` | 3 endpoint ask nhận X-API-Key | ✓ VERIFIED | 3 endpoint dùng `Depends(get_api_key_or_jwt_with_hubs)`; grep `get_current_user_with_hubs` → 0. |
| `api/app/services/hub_service.py` | `HubService.list_for_hubs` | ✓ VERIFIED | `hub_service.py:171` `async def list_for_hubs` — scope theo `hub_ids`, guard rỗng → `([], 0)`. |
| `api/app/main.py` | App factory sạch wiring MCP | ✓ VERIFIED | Không còn `/mcp`/`_composed_lifespan`/`_mcp_app`/`mcp_set_pool`/`app.mcp`; `:326` `lifespan=lifespan`. |
| `mcp_service/tests/test_integration_tools.py` | Test 3 tool qua respx | ✓ VERIFIED | 9 test PASS, chứa `respx`. |

### Key Link Verification

| From | To | Via | Trạng thái | Chi tiết |
| ---- | -- | --- | ---------- | -------- |
| `mcp_service/mcp_app/api_client.py` | API Service qua HTTP | `httpx.AsyncClient` | ✓ WIRED | `api_client.py:49` `httpx.AsyncClient(base_url=base_url, timeout=timeout)`. |
| `mcp_service/mcp_app/server.py` | `api_client.py` | `ApiClient` gọi `/api/hubs`, `/api/search`, `/api/ask` | ✓ WIRED | `server.py:28-35` import `ApiClient`; `_get_client()` tạo singleton; 3 tool gọi `get/post`. |
| `mcp_service/mcp_app/server.py` | `uvicorn` | entrypoint `__main__` chạy `streamable_http_app` | ✓ WIRED | `server.py:297` `import uvicorn`; `:309` `uvicorn.run(app, host=..., port=...)`. |
| `api/app/routers/search.py` → `ask.py` | `get_api_key_or_jwt_with_hubs` | `Depends` | ✓ WIRED | 6 endpoint search/ask đều inject dependency mới. |
| `api/app/routers/hubs.py` | `get_api_key_or_jwt` | `Depends` | ✓ WIRED | `hubs.py:58` `list_hubs` dùng dependency X-API-Key. |
| `get_api_key_or_jwt_with_hubs` | `user_hubs` | `select(UserHub.hub_id)` load hub_ids | ✓ WIRED | `dependencies.py` load `hub_ids` từ bảng `user_hubs` — nguồn hub isolation HUB-02. |
| `api/app/main.py` | lifespan (không còn MCP) | `lifespan=lifespan` trực tiếp | ✓ WIRED | `main.py:326` dùng `lifespan` module-level, không compose. |
| `test_integration_tools.py` | `server.py` | gọi tool thật + respx mock endpoint API | ✓ WIRED | 9 test gọi 3 tool thật qua respx, PASS. |

### Data-Flow Trace (Level 4)

| Artifact | Biến dữ liệu | Nguồn | Dữ liệu thật | Trạng thái |
| -------- | ------------ | ----- | ------------ | ---------- |
| `server.py` 3 tool | `data` (envelope.data) | `ApiClient.get/post` → API Service HTTP qua `httpx` | API Service thật trả dữ liệu RAG/search/hub (test phủ qua respx mock đúng shape) | ✓ FLOWING (qua boundary HTTP — verify gián tiếp bằng integration test; runtime với API thật là human_needed item 2) |
| `hubs.py` `list_hubs` | hub list | `HubService.list` / `list_for_hubs` (raw SQL `hubs` table) | DB query thật — `WHERE id = ANY(:hub_ids)` named bind param | ✓ FLOWING |
| `ask.py` usage log | `usage_events` row | `background_tasks.add_task(log_usage_event, pool, ...)` | BackgroundTasks ghi DB thật sau response | ⚠️ verify API-side (add_task gọi đúng) — row DB thật là human_needed item 1 |

### Behavioral Spot-Checks

| Behavior | Command | Kết quả | Trạng thái |
| -------- | ------- | ------- | ---------- |
| MCP Service tạo ASGI app + import 3 tool | `python -c "from mcp_app.server import build_asgi_app, list_hubs, search_wiki, ask_wiki; build_asgi_app()"` | `ASGI app OK: Starlette; tools OK` | ✓ PASS |
| Test suite mcp_service/ | `python -m pytest tests/ -q` | `33 passed in 4.36s` | ✓ PASS |
| Regression API unit | `python -m pytest tests/unit/ -q` | `119 passed, 7 warnings` | ✓ PASS |
| MCP Service chạy standalone với MCP client thật | (cần uvicorn server + MCP client) | — | ? SKIP → human verification |

### Requirements Coverage

| Requirement | Source Plan | Mô tả | Trạng thái | Bằng chứng |
| ----------- | ----------- | ----- | ---------- | -------- |
| MCP-01 | 01,02,03,04,05 | MCP Server expose RAG/Wiki cho Claude/ChatGPT agent | ✓ SATISFIED | MCP Service standalone (`mcp_service/`) chạy được qua `python -m mcp_app.server`, expose 3 tool qua Streamable HTTP gọi API qua HTTP. SC1/SC2/SC3/SC5 verified. |
| MCP-02 | 01,02,03,04,05 | Tool exposure: search, ask, list_documents, get_document — scoped theo hub_id | ⚠️ PARTIAL (3/4 tool, defer có chủ ý) | Phase 8.2 expose 3 tool (`list_hubs`/`search_wiki`/`ask_wiki`) — scope hub_id enforce API-side. `list_documents`/`get_document` **DEFER** sang phase sau (quyết định user 2026-05-19, ghi rõ ở Plan 03 `<notes>` + 08.2-03-SUMMARY.md "Deferred / Nợ kỹ thuật" — Phase 8.2 là re-architecture thuần, không thêm tool mới). |

**Lưu ý MCP-02:** Defer 2 tool là quyết định user được ghi nhận rõ ràng trong plan và summary — KHÔNG phải gap phát sinh ngoài ý muốn. Phase 8.2 đảm bảo đúng phạm vi đã chốt (port 3 tool sẵn có từ Phase 8.1). Nợ kỹ thuật `list_documents`/`get_document` chưa được phase cụ thể nào trong ROADMAP claim — cần đảm bảo nó được lên lịch ở một phase MCP bổ sung trước khi đóng milestone v4.0.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Tác động |
| ---- | ---- | ------- | -------- | -------- |
| `mcp_service/mcp_app/api_client.py` | 103-123 | Map lỗi thuần theo HTTP status, bỏ qua `error.code` khi status 200 + `success=false` | ⚠️ Warning (WR-01 REVIEW) | Giả định ngầm dễ vỡ nếu API/proxy trả 200 + envelope lỗi — hiện chưa lộ vì endpoint API trả đúng status. Không chặn release. |
| `mcp_service/mcp_app/server.py` | 157-252 | Tool `search_wiki`/`ask_wiki` không guard `query` rỗng trước khi gọi HTTP | ⚠️ Warning (WR-02 REVIEW) | Tốn 1 round-trip HTTP thừa; hành vi cuối vẫn đúng (API trả 400 → `ToolError INVALID_QUERY`). |
| `mcp_service/mcp_app/server.py` | 258-278 | Cleanup `atexit` chạy `asyncio.run(aclose())` trên event loop mới | ⚠️ Warning (WR-03 REVIEW) | Connection có thể không giải phóng sạch lúc shutdown; đã bọc try/except — known-limitation, rủi ro thấp với process sống lâu. |

Không có anti-pattern Blocker. 3 Warning đều đã được code review (08.2-REVIEW.md) phân loại non-blocking. Không phát hiện stub/placeholder/TODO chặn mục tiêu — toàn bộ tool có thân hàm thực, gọi API thật, có test phủ.

### Human Verification Required

#### 1. SC4 — usage_events ghi row sau ask_wiki thành công (DB thật)

**Test:** Chạy API Service + MCP Service + Postgres thật. `SELECT count(*) FROM usage_events;` ghi số trước. Gọi `ask_wiki` qua MCP client với X-API-Key hợp lệ → trả answer. Đợi vài giây (BackgroundTasks chạy sau response), `SELECT count(*) FROM usage_events;` lại.
**Expected:** Số row tăng đúng 1.
**Why human:** Test tự động dùng respx mock HTTP — không có Postgres trong vòng test. API-side đã verify (`background_tasks.add_task(log_usage_event,...)` gọi đúng — unit test Plan 02 Task 3 PASS), nhưng row DB thật chỉ verify được qua UAT thủ công. Đã định trước ở giai đoạn plan; bước UAT ghi trong `mcp_service/README.md` mục "UAT thủ công — SC4 usage_events".

#### 2. MCP Service chạy standalone end-to-end với MCP client thật

**Test:** Khởi động `python -m mcp_app.server` (port 8190). Kết nối MCP client (Claude/ChatGPT) qua transport Streamable HTTP. Gọi 3 tool `list_hubs`/`search_wiki`/`ask_wiki` với X-API-Key hợp lệ; thử thêm 1 lần thiếu/sai key.
**Expected:** 3 tool trả kết quả thật từ API Service; thiếu/sai key → `ToolError MCP_UNAUTHORIZED`; hub ngoài quyền → `ToolError HUB_ACCESS_DENIED`.
**Why human:** `build_asgi_app()` tạo ASGI app thành công (verify tự động) nhưng hành vi runtime với MCP client thật + uvicorn server cần kiểm thử thủ công.

### Gaps Summary

Không có gap chặn mục tiêu. Cả 5 Success Criteria đều đạt ở tầng verify tự động được:

- **SC1/SC2/SC3/SC5** — VERIFIED đầy đủ qua grep codebase + 33 test mcp_service PASS + 119 test regression API PASS.
- **SC4** — API-side đạt (BackgroundTasks `log_usage_event` giữ nguyên, unit test verify `add_task` gọi đúng); row DB thật là `human_needed` — bản chất đã được nhận diện và chốt từ giai đoạn plan (respx mock không có DB), KHÔNG phải gap phát sinh.

Phase chuyển trạng thái `human_needed` (KHÔNG `passed`) vì có 2 mục cần kiểm thử thủ công: SC4 row DB thật + chạy MCP Service standalone end-to-end với MCP client thật. Hai mục này đều thuộc loại "không verify được bằng grep/unit test" — cần UAT.

**Nợ kỹ thuật cần truy vết:** MCP-02 yêu cầu 4 tool; Phase 8.2 expose 3 (`list_documents`/`get_document` defer theo quyết định user). Cần đảm bảo 2 tool còn lại được lên lịch ở một phase MCP bổ sung trước khi đóng milestone v4.0 — hiện chưa có phase ROADMAP nào claim chúng.

---

_Verified: 2026-05-19_
_Verifier: Claude (gsd-verifier)_
