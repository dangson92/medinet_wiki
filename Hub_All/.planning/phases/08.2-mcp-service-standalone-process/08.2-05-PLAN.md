---
phase: 08.2-mcp-service-standalone-process
plan: "05"
type: execute
wave: 4
depends_on: ["03", "04"]
files_modified:
  - Hub_All/mcp_service/tests/test_integration_tools.py
  - Hub_All/mcp_service/tests/conftest.py
  - Hub_All/mcp_service/README.md
autonomous: true
requirements: [MCP-01, MCP-02]
must_haves:
  truths:
    - "Test suite mcp_service/ mock httpx call tới API Service — verify 3 tool end-to-end qua boundary HTTP, không cần API Service thật"
    - "Test verify auth fail → ToolError MCP_UNAUTHORIZED; hub isolation 403 → ToolError HUB_ACCESS_DENIED"
    - "Toàn bộ test mcp_service/ + regression unit API PASS — phase đóng được"
  artifacts:
    - path: "Hub_All/mcp_service/tests/test_integration_tools.py"
      provides: "Test 3 tool qua boundary HTTP với respx mock API Service"
      contains: "respx"
  key_links:
    - from: "Hub_All/mcp_service/tests/test_integration_tools.py"
      to: "Hub_All/mcp_service/mcp_app/server.py"
      via: "gọi tool thật với respx mock endpoint API"
      pattern: "respx"
---

<objective>
Hoàn tất Phase 8.2 với test suite tích hợp cho MCP Service: dùng `respx` mock các endpoint API Service (`/api/hubs`, `/api/search`, `/api/search/cross-hub`, `/api/ask`, `/api/search/answer`) và gọi 3 tool thật (`list_hubs`, `search_wiki`, `ask_wiki`) end-to-end qua boundary HTTP — verify tool gửi đúng request, unwrap envelope, map lỗi. Khác với test ở Plan 03 (mock ở tầng `ApiClient`), plan này mock ở tầng HTTP transport (`respx`) — verify cả `ApiClient` + tool cùng nhau.

Mục đích: Đáp ứng SC5 — "Test suite mcp_service/ mock httpx call tới API; regression API không vỡ". Đóng phase.
Output: `test_integration_tools.py` + `conftest.py` fixture; README cập nhật hướng dẫn test; toàn bộ test PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 3 tool — mcp_service/mcp_app/server.py (Plan 03): -->
<!--   list_hubs(ctx) -> HubList -->
<!--   search_wiki(ctx, query, hub_id=None, top_k=10) -> SearchResult -->
<!--   ask_wiki(ctx, query, hub_id=None) -> AskAnswer -->

<!-- ApiClient — mcp_service/mcp_app/api_client.py: dùng httpx.AsyncClient(base_url=...) -->
<!--   → respx mock theo base_url + path. -->

<!-- Endpoint API Service + envelope shape (xem chi tiết Plan 03 <interfaces>): -->
<!--   GET  /api/hubs              → {"success":true,"data":{"items":[{id,name,description}],...}} -->
<!--   POST /api/search            → {"success":true,"data":{"results":[{id,hub_id,hub_name,title,snippet,content,score}]}} -->
<!--   POST /api/search/cross-hub  → như /api/search -->
<!--   POST /api/ask               → {"success":true,"data":{"answer","citations":[{chunk_id,document_name,hub_name,content_snippet,score}],...}} -->
<!--   POST /api/search/answer     → {"success":true,"data":{"answer","citations":[{id,document_name,hub_name,snippet,score}],...}} -->
<!--   Lỗi 401 → {"success":false,"error":{"code":"INVALID_API_KEY","message":"..."}} -->
<!--   Lỗi 403 → {"success":false,"error":{"code":"FORBIDDEN","message":"..."}} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: conftest.py — fixture mock ctx + reset ApiClient singleton</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/server.py — module-level `_api_client` singleton + `_get_client()`; tool nhận `ctx: Context`.
    - Hub_All/mcp_service/mcp_app/auth.py — `extract_api_key(ctx)` đọc `ctx.request_context.request.headers`.
    - Hub_All/mcp_service/tests/test_server.py — pattern mock `ctx` đã dùng ở Plan 03.
  </read_first>
  <action>
    Tạo `Hub_All/mcp_service/tests/conftest.py`:
    - `from __future__ import annotations`, `import pytest`, `from unittest.mock import MagicMock`.
    - Fixture `mock_ctx(api_key: str = "test-key-123")` — factory tạo `ctx` giả:
      - `ctx = MagicMock()`; `ctx.request_context.request.headers` là một object có `.get(name)` trả `api_key` khi `name == "x-api-key"`, ngược lại `None`. Dùng dict thật `{"x-api-key": api_key}` hoặc `MagicMock` với `side_effect`.
      - Trả factory hàm `_make(api_key=...)` để test tạo ctx có/không có key.
    - Fixture `reset_api_client(autouse=True)` — trước mỗi test, set `mcp_app.server._api_client = None` để singleton ApiClient tạo mới (tránh leak base_url giữa test). Sau test cũng reset về `None`.
    - Fixture `api_base_url` — trả `"http://testserver-api"` (base URL dùng cho respx + override config). Set env `MCP_API_BASE_URL` qua `monkeypatch` HOẶC patch `get_settings` để `api_base_url` khớp.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && python -m pytest tests/conftest.py --co -q 2>/dev/null; test -f tests/conftest.py && echo "conftest OK"</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/mcp_service/tests/conftest.py` tồn tại, chứa fixture `mock_ctx` (hoặc factory tương đương) và fixture reset `_api_client` singleton.
    - Fixture reset đặt `mcp_app.server._api_client = None` (grep `_api_client` khớp).
    - `pytest tests/ -q --co` (collect-only) không lỗi import conftest.
  </acceptance_criteria>
  <done>conftest.py cung cấp fixture mock ctx + reset ApiClient singleton cho test tích hợp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_integration_tools.py — 3 tool end-to-end qua respx mock API Service</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/server.py — 3 tool (Plan 03).
    - Hub_All/mcp_service/mcp_app/api_client.py — ApiClient (Plan 01) — base_url + path để respx route khớp.
    - Hub_All/mcp_service/tests/conftest.py — fixture Task 1.
    - Hub_All/api/app/routers/ask.py + search.py + hubs.py — envelope shape thật của API Service (đảm bảo mock data khớp shape thật).
  </read_first>
  <behavior>
    - Test 1 (list_hubs happy): respx mock `GET {base}/api/hubs` → 200 envelope `data.items=[{id,name,description}]` → `list_hubs(ctx)` trả `HubList` đúng số hub; request gửi header `X-API-Key` đúng giá trị.
    - Test 2 (search_wiki single-hub): respx mock `POST {base}/api/search` → 200 → `search_wiki(ctx, "q", hub_id="h1")` trả `SearchResult`; verify request body chứa `hub_ids: ["h1"]`.
    - Test 3 (search_wiki cross-hub): respx mock `POST {base}/api/search/cross-hub` → 200 → `search_wiki(ctx, "q")` (hub_id None) trả `SearchResult`; verify gọi đúng path cross-hub.
    - Test 4 (ask_wiki single-hub): respx mock `POST {base}/api/ask` → 200 envelope `data.answer="... [1]"` + `citations` → `ask_wiki(ctx, "q", hub_id="h1")` trả `AskAnswer` — answer giữ marker, citations map đúng.
    - Test 5 (ask_wiki cross-hub): respx mock `POST {base}/api/search/answer` → 200 → `ask_wiki(ctx, "q")` (hub_id None) trả `AskAnswer`; verify gọi `/api/search/answer` (KHÔNG `/api/ask/cross-hub`).
    - Test 6 (auth fail — thiếu key): `ctx` không có header `x-api-key` → tool raise `ToolError` chứa `MCP_UNAUTHORIZED`; respx KHÔNG nhận request nào (tool fail trước khi gọi HTTP).
    - Test 7 (API 401): respx mock endpoint → 401 envelope `error.code=INVALID_API_KEY` → tool raise `ToolError` chứa `MCP_UNAUTHORIZED`.
    - Test 8 (hub isolation 403): respx mock `POST /api/search` → 403 envelope `error.code=FORBIDDEN` → `search_wiki` raise `ToolError` chứa `HUB_ACCESS_DENIED`.
    - Test 9 (API 5xx): respx mock `POST /api/ask` → 500 envelope `error.code=LLM_FAILED` → `ask_wiki` raise `ToolError` — message KHÔNG chứa stack trace.
  </behavior>
  <action>
    Tạo `Hub_All/mcp_service/tests/test_integration_tools.py`:
    - Docstring tiếng Việt: "Test tích hợp Phase 8.2 — 3 tool MCP gọi API Service qua boundary HTTP. Dùng respx mock httpx — KHÔNG cần API Service thật chạy. Verify request shape + envelope unwrap + map lỗi."
    - `from __future__ import annotations`, `import pytest`, `import respx`, `import httpx`.
    - Import 3 tool từ `mcp_app.server`, schema từ `mcp_app.schemas`, `ToolError` từ `mcp.server.fastmcp.exceptions`.
    - Dùng fixture `mock_ctx` + `reset_api_client` + `api_base_url` từ conftest.
    - 9 test behavior ở trên. Mỗi test dùng `@respx.mock` decorator; mock endpoint qua `respx.get(...)` / `respx.post(...)` trả `httpx.Response(status_code, json={...envelope...})`.
    - Test verify request body / header dùng `route.calls.last.request` của respx.
    - Đánh dấu `@pytest.mark.asyncio` cho test async.
    - Test 1, 7, 8 (auth/hub isolation) đánh thêm marker `@pytest.mark.critical` (nhất quán convention `pyproject.toml` — nếu marker chưa khai báo trong mcp_service pyproject thì thêm vào `[tool.pytest.ini_options].markers`).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && python -m pytest tests/test_integration_tools.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/mcp_service/tests/test_integration_tools.py` tồn tại, chứa ≥ 9 hàm test, chứa chuỗi `respx`.
    - Test phủ: 3 tool happy path (single + cross-hub) + auth fail thiếu key + API 401 + hub isolation 403 + API 5xx.
    - Test verify request body cross-hub gọi đúng path (`/api/search/cross-hub`, `/api/search/answer`).
    - `pytest tests/test_integration_tools.py` exit code 0, 9 test PASS.
  </acceptance_criteria>
  <done>test_integration_tools.py verify 3 tool end-to-end qua respx mock API Service — happy path + auth fail + hub isolation + lỗi server; 9 test PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Verify toàn bộ test suite + regression API + cập nhật README</name>
  <read_first>
    - Hub_All/mcp_service/README.md — file đang sửa: thêm mục hướng dẫn test.
    - Hub_All/mcp_service/tests/ — toàn bộ test Plan 01/03/05.
    - Hub_All/api/tests/ — regression API sau khi gỡ MCP (Plan 04).
  </read_first>
  <action>
    1. Chạy toàn bộ test MCP Service: `cd Hub_All/mcp_service && python -m pytest tests/ -q` — TẤT CẢ test PASS (config + api_client + auth + server + integration_tools).

    2. Chạy ruff lint MCP Service: `cd Hub_All/mcp_service && python -m ruff check mcp_app/ tests/` — All checks passed. Sửa lỗi lint nếu có.

    3. Chạy regression API (xác nhận gỡ MCP Plan 04 không vỡ): `cd Hub_All/api && python -m pytest tests/unit/ -q` — PASS. (Nếu test integration testcontainers khả dụng và nhanh → chạy thêm `tests/integration/` nhánh search/ask; nếu hạ tầng không sẵn → ghi chú trong SUMMARY, không block.)

    4. Cập nhật `Hub_All/mcp_service/README.md` — thêm mục "## Chạy test" (tiếng Việt có dấu):
       - `cd Hub_All/mcp_service && python -m pytest tests/ -q` — chạy toàn bộ test.
       - Ghi rõ test dùng `respx` mock API Service — KHÔNG cần API Service thật chạy.
       - Thêm mục "## Kiến trúc sau Phase 8.2": sơ đồ `Frontend ─► API Service` và `LLM Agent ─► MCP Service ─► API Service (HTTP)`; ghi MCP Service đảo D-04 Phase 8.1 (gọi HTTP thay import service layer).
       - Thêm mục "## Biến môi trường" liệt kê `MCP_API_BASE_URL`, `MCP_SERVICE_HOST`, `MCP_SERVICE_PORT`, `MCP_HTTP_TIMEOUT` với mô tả + giá trị mặc định.

    5. Trong SUMMARY ghi rõ kết quả: số test PASS mcp_service, số test PASS regression API, trạng thái lint.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && python -m pytest tests/ -q && python -m ruff check mcp_app/ tests/ && grep -q "Chạy test" README.md && echo ALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/mcp_service && python -m pytest tests/ -q` exit code 0 — toàn bộ test MCP Service PASS.
    - `cd Hub_All/mcp_service && python -m ruff check mcp_app/ tests/` — All checks passed.
    - `cd Hub_All/api && python -m pytest tests/unit/ -q` exit code 0 — regression API PASS.
    - `Hub_All/mcp_service/README.md` chứa mục `Chạy test`, `Kiến trúc sau Phase 8.2`, `Biến môi trường`; viết tiếng Việt có dấu.
  </acceptance_criteria>
  <done>Toàn bộ test mcp_service/ PASS + ruff clean + regression API unit PASS; README cập nhật hướng dẫn test + kiến trúc + env var.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mock → tool thật | Test verify tool xử lý đúng cả response thành công lẫn lỗi auth/isolation/server từ API Service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-05-S | Spoofing | xác minh auth fail được chặn | mitigate | Test 6/7 verify thiếu key / API 401 → `ToolError MCP_UNAUTHORIZED`; Test 6 verify tool fail TRƯỚC khi gọi HTTP (không request rác). |
| T-08.2-05-E | Elevation of Privilege | xác minh hub isolation | mitigate | Test 8 verify API 403 → `ToolError HUB_ACCESS_DENIED` — hub isolation API-side được tool tôn trọng, không bypass. |
| T-08.2-05-I | Information Disclosure | xác minh lỗi không leak | mitigate | Test 9 verify `ToolError` từ API 5xx có message sạch — không stack trace, không chi tiết nội bộ. |
| T-08.2-05-T | Tampering | regression API sau gỡ MCP | mitigate | Task 3 chạy `pytest tests/unit/` của API — bắt regression nếu Plan 04 gỡ MCP làm vỡ API Service. |
</threat_model>

<verification>
- `cd Hub_All/mcp_service && python -m pytest tests/ -q` — toàn bộ test MCP Service PASS.
- `cd Hub_All/mcp_service && python -m ruff check mcp_app/ tests/` — All checks passed.
- `cd Hub_All/api && python -m pytest tests/unit/ -q` — regression API PASS.
- `Hub_All/mcp_service/README.md` có đủ mục hướng dẫn test + kiến trúc.
</verification>

<success_criteria>
- Test suite `mcp_service/` mock httpx (`respx`) verify 3 tool end-to-end qua boundary HTTP.
- Test phủ auth fail (`MCP_UNAUTHORIZED`), hub isolation (`HUB_ACCESS_DENIED`), lỗi server (message sạch).
- Toàn bộ test MCP Service + regression API unit PASS.
- README cập nhật đầy đủ.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08.2-mcp-service-standalone-process/08.2-05-SUMMARY.md`.
</output>
