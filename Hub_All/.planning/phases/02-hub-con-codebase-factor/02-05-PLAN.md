---
phase: 02-hub-con-codebase-factor
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - api/app/config.py
  - api/tests/unit/test_config_hub_name.py
  - api/tests/unit/test_config_hub_name_dynamic.py
  - api/scripts/hub-add.sh
  - docker-compose.override.yml.template
  - Hub_All/Makefile
  - api/Makefile
  - Hub_All/README.md
  - Hub_All/CLAUDE.md
  - .planning/STATE.md
autonomous: false
requirements:
  - FACTOR-04
tags:
  - dynamic-hub-registration
  - settings-str-validator
  - regex-validation
  - reserved-name-blacklist
  - docker-compose-override
  - make-hub-add
  - factor-04
  - user-direction-b

must_haves:
  truths:
    - "Settings.hub_name đổi từ Literal['central','yte','duoc','hcns'] sang str (FACTOR-04 enable dynamic add)"
    - "field_validator regex `^[a-z][a-z0-9_]{1,15}$` reject uppercase, hyphen, starting digit/underscore, > 16 char (Postgres identifier safe, DB name medinet_hub_<name> < 32 char limit)"
    - "Reserved name blacklist: postgres, cocoindex, template0, template1, public, medinet — collision với system DB / role medinet (security)"
    - "'central' aggregator + 4 hub gốc (yte, duoc, hcns) PHẢI accept regression (KHÔNG break Phase 1 + Plan 02-01..04)"
    - "scripts/hub-add.sh wrap quanh hub-init.sh (DB-level Phase 1 Plan 01-05 carry forward) + tự sinh service block từ template append docker-compose.override.yml"
    - "Auto-detect port `8180 + N` nếu user không truyền explicit PORT — scan docker-compose.yml + docker-compose.override.yml hiện hữu"
    - "docker-compose.override.yml.template với placeholder {{HUB}} + {{PORT}} (sed substitute) + inherit `<<: *api-template` anchor từ base"
    - "Smoke checkpoint user-action: `make hub-add HUB=tmp_test PORT=8189` → docker compose up -d python-api-tmp_test → curl localhost:8189/api/health 200 → cleanup"
    - "Phase 1 _enforce_hub_dsn_match validator dùng self.hub_name dynamic — KHÔNG sửa (đã accept str từ trước qua self.hub_name reference)"
    - "Hub_All/README.md thêm section 'Add a new hub' với 3-step quick start cho operator"
    - "10/10 unit test_config_hub_name_dynamic.py PASS — accept 4 hub gốc + 3 hub mới (phap_che, marketing, dev_test) + reject 5 invalid pattern + reject 6 reserved name"
    - "Plan 02-04 closeout đã ship trước — Plan 02-05 cập nhật STATE.md tiếp tục từ completed_plans=9 → 10, percent ≈ 31%"
  artifacts:
    - path: "api/app/config.py"
      provides: "Settings.hub_name str + field_validator regex + reserved blacklist"
      contains: "hub_name: str\\|@field_validator..hub_name.\\|reserved.*hub_name\\|^\\[a-z\\]\\[a-z0-9_\\]"
    - path: "api/scripts/hub-add.sh"
      provides: "Wrap hub-init.sh + auto-gen docker-compose.override.yml service block"
      contains: "hub-init.sh\\|docker-compose.override.yml\\|x-api-template\\|sed"
    - path: "docker-compose.override.yml.template"
      provides: "Service block template với placeholder {{HUB}} {{PORT}}"
      contains: "\\{\\{HUB\\}\\}\\|\\{\\{PORT\\}\\}\\|x-api-template\\|HUB_NAME:"
    - path: "api/tests/unit/test_config_hub_name_dynamic.py"
      provides: "10 test scenario — accept dynamic + reject invalid pattern + reject reserved"
      contains: "def test_accept_dynamic\\|def test_reject_uppercase\\|def test_reject_reserved\\|def test_accept_central\\|def test_regression_4_hubs"
    - path: "Hub_All/README.md"
      provides: "Section 'Add a new hub' với 3-step quick start"
      contains: "make hub-add\\|hub mới\\|FACTOR-04"
  key_links:
    - from: "api/app/config.py::Settings.hub_name (str + validator)"
      to: "api/scripts/hub-add.sh (regex match cùng pattern)"
      via: "Cùng regex `^[a-z][a-z0-9_]{1,15}$` — Settings validate runtime + hub-add.sh validate trước CREATE DATABASE để fail-fast trước khi tạo DB orphan"
      pattern: "\\^\\[a-z\\]\\[a-z0-9_\\]"
    - from: "api/scripts/hub-add.sh"
      to: "api/scripts/hub-init.sh (Phase 1 Plan 01-05 carry forward)"
      via: "hub-add.sh call hub-init.sh để tạo DB + ext vector + alembic upgrade head (DB layer reuse)"
      pattern: "hub-init\\.sh"
    - from: "docker-compose.override.yml.template"
      to: "docker-compose.yml::x-api-template anchor (Plan 02-02 ship)"
      via: "Template inherit `<<: *api-template` từ base compose qua docker compose merge khi up"
      pattern: "<<:\\s*\\*api-template"
    - from: "Hub_All/Makefile + api/Makefile"
      to: "api/scripts/hub-add.sh"
      via: "make target `hub-add HUB=<name> [PORT=<port>]` proxy bash script"
      pattern: "hub-add"
---

<objective>
Generalize hub list từ hardcoded 4 hub (central/yte/duoc/hcns) sang **dynamic registration** — operator thêm hub mới (vd `phap_che`, `marketing`, `quality_assurance`) bằng 1 lệnh `make hub-add HUB=<name> [PORT=<port>]` mà KHÔNG sửa code Python / `docker-compose.yml` base.

3 changes:
1. **Settings layer** (`api/app/config.py`) — đổi `hub_name: Literal["central","yte","duoc","hcns"]` → `hub_name: str` với `@field_validator` regex strict + reserved-name blacklist (security).
2. **Compose layer** (`docker-compose.override.yml.template` + `scripts/hub-add.sh`) — auto-gen service block từ template, append vào `docker-compose.override.yml` (docker compose auto-merge với base). Auto-detect port nếu user không truyền.
3. **Make target** (`Hub_All/Makefile` + `api/Makefile`) — `make hub-add HUB=<name> [PORT=<port>]` proxy `scripts/hub-add.sh`.

Plan 02-05 là Wave 4 extension — chạy SAU Plan 02-04 closeout (FACTOR-01..03 đã satisfied). FACTOR-04 = REQ-ID thêm vào Phase 2 retroactively theo user direction B 2026-05-22.

Purpose: Mở rộng v3.0 architectural model — hub list KHÔNG còn hardcoded ở source. Operator chạy 1 lệnh là thêm hub mới (no code edit, no compose edit base, no PR). Đặt nền cho Phase 6 SETTINGS-04 `hub_registry` table sau này — Settings hiện chỉ validate format; full registry source-of-truth defer Phase 6.

Output: 1 file refactor (`api/app/config.py`) + 1 file extend test (`tests/unit/test_config_hub_name.py`) + 5 file mới (`tests/unit/test_config_hub_name_dynamic.py`, `api/scripts/hub-add.sh`, `docker-compose.override.yml.template`, `Hub_All/Makefile` + `api/Makefile` target append, `Hub_All/README.md` section) + 2 file update doc (`Hub_All/CLAUDE.md` section 6 v3.0 progress, `.planning/STATE.md` frontmatter).

**Locked decisions (CONTEXT.md + this plan):**
- D-V3-Phase2-Dynamic-A: regex `^[a-z][a-z0-9_]{1,15}$` (max 16 char vì Postgres identifier 63 char total — `medinet_hub_` prefix 12 char + name ≤ 16 → 28 char total, có headroom). KHÔNG cho phép hyphen (Postgres identifier cần quote nếu non-alphanumeric).
- D-V3-Phase2-Dynamic-B: Reserved blacklist 6 name = `{postgres, cocoindex, template0, template1, public, medinet}`. "central" KHÔNG trong blacklist (aggregator special accept).
- D-V3-Phase2-Dynamic-C: Compose layering = base `docker-compose.yml` (KHÔNG sửa, giữ 4 hub gốc) + auto-gen `docker-compose.override.yml` (gitignored — operator-local). Hub mới chỉ live trong override.
- D-V3-Phase2-Dynamic-D: Port auto-detect = scan max port hiện hữu (8180 central + 8181 yte + 8182 duoc + 8183 hcns + bất kỳ override) + 1. User có thể override qua `PORT=<port>` arg.
- D-V3-Phase2-Dynamic-E: KHÔNG ship `hub_registry` table integration ở Plan 02-05 — defer Phase 6 SETTINGS-04. Plan 02-05 chỉ validate format Settings + sinh compose block. Source-of-truth long-term sẽ là `hub_registry` table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-CONTEXT.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-01-PLAN.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-02-PLAN.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-04-PLAN.md
@Hub_All/api/app/config.py
@Hub_All/api/tests/unit/test_config_hub_name.py
@Hub_All/api/scripts/init-db.sh
@Hub_All/api/scripts/hub-init.sh
@Hub_All/docker-compose.yml
@Hub_All/Makefile
@Hub_All/api/Makefile

<interfaces>
<!-- Phase 1 đã ship resolve_database_url + _enforce_hub_dsn_match dùng self.hub_name dynamic; chỉ field type Literal[...] cần đổi sang str. -->

Từ `api/app/config.py` hiện tại (Plan 01-02 ship — Phase 1):

```python
class Settings(BaseSettings):
    hub_name: Literal["central", "yte", "duoc", "hcns"] = "central"

    @model_validator(mode="after")
    def _enforce_hub_dsn_match(self) -> Settings:
        dsn_path = self.database_url.split("?", 1)[0].rstrip("/")
        expected_db = (
            "medinet_central"
            if self.hub_name == "central"
            else f"medinet_hub_{self.hub_name}"
        )
        if not dsn_path.endswith(f"/{expected_db}"):
            ...
```

→ Plan 02-05 đổi:

```python
RESERVED_HUB_NAMES = frozenset({
    "postgres", "cocoindex", "template0", "template1", "public", "medinet",
})

class Settings(BaseSettings):
    hub_name: str = "central"

    @field_validator("hub_name", mode="after")
    @classmethod
    def _validate_hub_name(cls, v: str) -> str:
        if not re.fullmatch(r"^[a-z][a-z0-9_]{0,15}$", v):
            raise ValueError(
                f"hub_name invalid: {v!r}. Pattern bắt buộc "
                f"`^[a-z][a-z0-9_]{{0,15}}$` (lowercase a-z bắt đầu, "
                f"max 16 char, underscore/digit ok, KHÔNG hyphen/uppercase)."
            )
        if v in RESERVED_HUB_NAMES:
            raise ValueError(
                f"hub_name reserved: {v!r}. Reserved names collide với "
                f"Postgres system DB / role: {sorted(RESERVED_HUB_NAMES)}"
            )
        return v

    # _enforce_hub_dsn_match KHÔNG sửa — đã dùng self.hub_name dynamic
```

Note regex `{0,15}` thay vì `{1,15}` để cho phép single-char name (vd `a`) — Postgres identifier accept 1 char. Min total length = 1, max = 16. Verify hub-init.sh regex hiện tại `{1,30}` — Plan 02-05 STRICTER xuống `{0,15}` để khớp Settings (15+1 first char = 16 max). hub-init.sh sẽ được sync regex cùng Plan 02-05 (Task 2).

Từ `api/scripts/hub-init.sh` hiện tại (Plan 01-05 ship — Phase 1):

```bash
if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{1,30}$ ]]; then
    echo "[hub-init] ERROR: hub name '$HUB' invalid. Pattern: ^[a-z][a-z0-9_]{1,30}$"
    exit 2
fi
```

→ Plan 02-05 đổi pattern thành `^[a-z][a-z0-9_]{0,15}$` (sync với Settings).

Từ `Hub_All/docker-compose.yml` hiện tại (Plan 02-02 ship):

```yaml
x-api-template: &api-template
  build: ...
  env_file: [./api/.env]
  environment: { ... }
  volumes: [...]
  depends_on: ...
  networks: [medinet_net]

services:
  python-api-central: { <<: *api-template, ..., ports: ["8180:8080"], volumes: [..., medinet_cocoindex_central:...] }
  python-api-yte:     { <<: *api-template, ..., ports: ["8181:8080"], volumes: [..., medinet_cocoindex_yte:...] }
  python-api-duoc:    { <<: *api-template, ..., ports: ["8182:8080"], volumes: [..., medinet_cocoindex_duoc:...] }
  python-api-hcns:    { <<: *api-template, ..., ports: ["8183:8080"], volumes: [..., medinet_cocoindex_hcns:...] }
```

→ Plan 02-05 KHÔNG sửa docker-compose.yml base. Tất cả hub mới sẽ đi vào `docker-compose.override.yml` (gitignored — operator-local).

Note: docker compose tự động merge `docker-compose.yml` + `docker-compose.override.yml` khi `docker compose up` mà không cần `-f` flag. Anchor `*api-template` từ base file VẪN reference được trong override file qua `docker compose config` (verify Task 2 acceptance).

⚠️ Pitfall: YAML anchor (`&api-template`) định nghĩa ở base file CHỈ visible trong cùng file YAML. Khi merge cross-file, override KHÔNG thấy anchor base. → Workaround: override file PHẢI tự define lại `environment` + `volumes` đầy đủ (giống pattern Plan 02-02 đã làm với từng service). Template snippet sẽ inline toàn bộ keys thay vì cố `<<: *api-template` cross-file.

Verify pitfall qua Task 2 step: `docker compose config --quiet` (config render merge) + `docker compose config --services | wc -l` (count expanded service).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Settings.hub_name Literal → str + regex validator + reserved blacklist + update test_config_hub_name.py + add test_config_hub_name_dynamic.py</name>
  <files>api/app/config.py, api/tests/unit/test_config_hub_name.py, api/tests/unit/test_config_hub_name_dynamic.py</files>
  <read_first>
    - Hub_All/api/app/config.py (file đang sửa — line 38-43 Literal field + line 158-191 _enforce_hub_dsn_match validator KHÔNG sửa)
    - Hub_All/api/tests/unit/test_config_hub_name.py (file đang sửa — test 5 "test_invalid_hub_name_raises" cần update để match ValueError thay vì Literal error)
    - Hub_All/api/scripts/hub-init.sh (Phase 1 ship — regex hiện hữu `^[a-z][a-z0-9_]{1,30}$`; Task 1 sync với Settings nhưng KHÔNG sửa script ở Task 1 — defer Task 2)
    - Hub_All/.planning/phases/01-multi-db-topology/01-02-PLAN.md (Phase 1 _enforce_hub_dsn_match validator pattern — Plan 02-05 reuse)
  </read_first>
  <action>
**Step 1: Refactor `api/app/config.py`** — replace Literal field với str + validator.

Mở `api/app/config.py`. Tại đầu file (sau imports), thêm constant:

```python
# Plan 02-05 FACTOR-04 — Reserved hub names blacklist.
# Postgres có 4 template DB hệ thống (postgres/template0/template1) + schema mặc
# định "public" + role "medinet" (M2 carry forward — postgres_user/owner cho mọi
# DB nghiep vu) + DB internal cocoindex (R5 + P7 carry forward — separate khỏi
# user DB). Reserve 6 name này tránh hub_name=postgres làm CREATE DATABASE
# medinet_hub_postgres OK nhưng confuse + reserved "medinet" tránh hub_name=medinet
# làm DB name medinet_hub_medinet redundant + privilege escalation theory nếu
# someone alias trùng role. "central" KHÔNG trong blacklist — central là aggregator
# special-case mapping sang medinet_central (KHÔNG prefix medinet_hub_).
RESERVED_HUB_NAMES = frozenset({
    "postgres",    # Postgres default superuser DB
    "cocoindex",   # Internal cocoindex state DB (M2 ship)
    "template0",   # Postgres template (read-only system)
    "template1",   # Postgres template (writable system default)
    "public",      # Default schema name — collision với cocoindex_db_schema
    "medinet",     # Postgres role name (M2 init-db.sh OWNER) — privilege confuse
})
```

Tại field `hub_name`, đổi:

```python
# CŨ:
hub_name: Literal["central", "yte", "duoc", "hcns"] = "central"

# MỚI:
# Plan 02-05 FACTOR-04 — Dynamic hub registration.
# Đổi từ Literal[4 hub] sang str regex validator để operator thêm hub mới
# (vd phap_che, marketing) qua `make hub-add HUB=<name>` mà KHÔNG sửa code.
# Validator enforce Postgres identifier safe (lowercase a-z first, a-z0-9_
# rest, max 16 char total — Postgres identifier 63 char limit minus
# "medinet_hub_" prefix 12 char = 51 char headroom, nhưng 16 char cho dễ
# nhớ + URL prefix Phase 5 Caddy gọn). KHÔNG cho phép hyphen (Postgres
# identifier quote requirement) hoặc uppercase (case-sensitivity confuse).
hub_name: str = "central"
```

Sau model_config + trước `database_url` field, thêm `field_validator`:

```python
@field_validator("hub_name", mode="after")
@classmethod
def _validate_hub_name(cls, v: str) -> str:
    """Regex format + reserved blacklist (FACTOR-04 Plan 02-05).

    Pattern `^[a-z][a-z0-9_]{0,15}$`:
    - lowercase a-z bắt đầu (KHÔNG digit/underscore — Postgres identifier
      khuyến nghị start với letter để khỏi quote)
    - body 0-15 char a-z0-9 underscore
    - max total = 16 char (medinet_hub_<name> = 12 + 16 = 28 char < 63 limit)

    Reserved blacklist: 6 name collide Postgres system DB / role (xem
    RESERVED_HUB_NAMES docstring).

    Reject:
    - empty string → ValueError (regex không match)
    - uppercase ("Yte") → ValueError
    - hyphen ("phap-che") → ValueError
    - starting digit ("1hub") → ValueError
    - starting underscore ("_hub") → ValueError
    - > 16 char ("very_long_hub_name") → ValueError
    - reserved ("postgres", "cocoindex", ...) → ValueError
    """
    if not re.fullmatch(r"^[a-z][a-z0-9_]{0,15}$", v):
        raise ValueError(
            f"hub_name invalid format: {v!r}. Pattern required: "
            f"^[a-z][a-z0-9_]{{0,15}}$ (lowercase a-z first char, max 16 "
            f"char total, a-z0-9_ rest — KHÔNG hyphen/uppercase/start-digit)."
        )
    if v in RESERVED_HUB_NAMES:
        raise ValueError(
            f"hub_name reserved: {v!r}. 6 reserved names collide Postgres "
            f"system DB / role medinet: {sorted(RESERVED_HUB_NAMES)}."
        )
    return v
```

**Step 2: KHÔNG sửa `_enforce_hub_dsn_match` validator** — đã dùng `self.hub_name` dynamic, accept str.

**Step 3: Update test cases** `api/tests/unit/test_config_hub_name.py` — Test 5 `test_invalid_hub_name_raises`:

Test 5 hiện check `ValidationError` (Literal reject). Sau Plan 02-05, "invalid_hub" (snake_case 11 char OK) sẽ PASS regex → Settings load OK. Plan 02-05 PHẢI đổi test 5 thành scenario reject thực sự (vd uppercase, hyphen, reserved). Hoặc better: rename test 5 thành `test_invalid_hub_name_pattern_raises` với input `"Invalid_Hub"` (uppercase reject).

```python
# === Test 5: hub_name vi phạm regex format → ValidationError ===
def test_invalid_hub_name_pattern_raises(monkeypatch: pytest.MonkeyPatch) -> None:
    """Plan 02-05 FACTOR-04 — Regex validator reject hub_name uppercase /
    hyphen / starting digit. Plan 01-02 ship Literal[4 hub] đã đổi sang str
    + regex (Plan 02-05). Test này verify regex enforce KHÔNG bị bypass.
    """
    _set_env(
        monkeypatch,
        hub_name="Invalid_Hub",  # uppercase → regex reject
        database_url="postgresql+asyncpg://u:p@h:5432/medinet_central",
    )
    with pytest.raises(ValidationError, match="hub_name invalid format"):
        Settings()
```

Verify 7 test khác trong file `test_config_hub_name.py` KHÔNG cần đổi (tất cả input đều satisfy regex mới).

**Step 4: Tạo file mới `api/tests/unit/test_config_hub_name_dynamic.py`** — full coverage FACTOR-04 validator.

```python
"""Unit tests cho `Settings.hub_name` str validator (FACTOR-04 Plan 02-05).

Plan 02-05 đổi field `hub_name` từ Literal[4 hub] → str + regex + blacklist.
Test này verify validator chấp nhận hub mới (dynamic registration) đồng thời
reject 5 invalid pattern + 6 reserved name + edge case empty/single-char.

Threat model cover:
- T-02-05-01 Tampering: hub_name attacker-controlled env injection special char
  → regex reject pre-DB-create
- T-02-05-02 Privilege confuse: hub_name=medinet / postgres collision Postgres
  role/system DB → blacklist reject startup
- T-02-05-03 DoS: hub_name = 100-char string blow up DB identifier limit →
  regex max 16 char reject

Pattern theo test_config_hub_name.py (Phase 1 Plan 01-02) — monkeypatch.setenv +
get_settings.cache_clear → Settings() fresh instantiate.
"""
from __future__ import annotations

import pytest
from pydantic import ValidationError

from app.config import RESERVED_HUB_NAMES, Settings, get_settings


def _set_env(
    monkeypatch: pytest.MonkeyPatch,
    *,
    hub_name: str | None = None,
    database_url: str | None = None,
) -> None:
    if hub_name is not None:
        monkeypatch.setenv("HUB_NAME", hub_name)
    if database_url is not None:
        monkeypatch.setenv("DATABASE_URL", database_url)
    get_settings.cache_clear()


# ═══════════════════════════════════════════════════════════════════════════
# ACCEPT scenarios — 4 hub gốc (regression) + 3 hub mới (dynamic)
# ═══════════════════════════════════════════════════════════════════════════


@pytest.mark.parametrize("hub", ["central", "yte", "duoc", "hcns"])
def test_accept_4_original_hubs_regression(
    monkeypatch: pytest.MonkeyPatch, hub: str
) -> None:
    """Regression — 4 hub gốc PHẢI accept sau khi đổi Literal → str + regex."""
    dsn_target = (
        "medinet_central" if hub == "central" else f"medinet_hub_{hub}"
    )
    _set_env(
        monkeypatch,
        hub_name=hub,
        database_url=f"postgresql+asyncpg://u:p@h:5432/{dsn_target}",
    )
    s = Settings()
    assert s.hub_name == hub


@pytest.mark.parametrize("hub", ["phap_che", "marketing", "dev_test"])
def test_accept_dynamic_hub_names(
    monkeypatch: pytest.MonkeyPatch, hub: str
) -> None:
    """Hub mới (vd phap_che, marketing) match regex → accept (FACTOR-04 enable)."""
    _set_env(
        monkeypatch,
        hub_name=hub,
        database_url=f"postgresql+asyncpg://u:p@h:5432/medinet_hub_{hub}",
    )
    s = Settings()
    assert s.hub_name == hub


def test_accept_single_char_hub(monkeypatch: pytest.MonkeyPatch) -> None:
    """Edge case — 1-char hub name (vd 'a') OK qua regex `{0,15}` (min total 1)."""
    _set_env(
        monkeypatch,
        hub_name="a",
        database_url="postgresql+asyncpg://u:p@h:5432/medinet_hub_a",
    )
    s = Settings()
    assert s.hub_name == "a"


def test_accept_max_length_16_char(monkeypatch: pytest.MonkeyPatch) -> None:
    """Edge case — 16-char hub name OK (boundary inclusive)."""
    hub_16 = "a" + "b" * 15  # 16 char total
    assert len(hub_16) == 16
    _set_env(
        monkeypatch,
        hub_name=hub_16,
        database_url=f"postgresql+asyncpg://u:p@h:5432/medinet_hub_{hub_16}",
    )
    s = Settings()
    assert s.hub_name == hub_16


# ═══════════════════════════════════════════════════════════════════════════
# REJECT pattern — uppercase / hyphen / starting-digit / starting-underscore /
# too-long / empty
# ═══════════════════════════════════════════════════════════════════════════


@pytest.mark.parametrize(
    "hub,reason",
    [
        ("Yte", "uppercase"),
        ("YTE", "all-uppercase"),
        ("phap-che", "hyphen-disallowed"),
        ("1hub", "starting-digit"),
        ("_underscore", "starting-underscore"),
        ("17chars_name_long", "exceeds-16-char-max"),  # 17 char
        ("", "empty-string"),
        ("hub.dot", "dot-disallowed"),
        ("hub space", "whitespace-disallowed"),
        ("hub$dollar", "special-char-disallowed"),
    ],
)
def test_reject_invalid_pattern(
    monkeypatch: pytest.MonkeyPatch, hub: str, reason: str
) -> None:
    """T-02-05-01 / T-02-05-03 — Regex validator reject 10 invalid pattern."""
    _set_env(
        monkeypatch,
        hub_name=hub,
        database_url="postgresql+asyncpg://u:p@h:5432/medinet_central",
    )
    with pytest.raises(ValidationError, match="hub_name invalid format"):
        Settings()


# ═══════════════════════════════════════════════════════════════════════════
# REJECT reserved blacklist — 6 name collide Postgres system DB / role
# ═══════════════════════════════════════════════════════════════════════════


@pytest.mark.parametrize("hub", sorted(RESERVED_HUB_NAMES))
def test_reject_reserved_hub_names(
    monkeypatch: pytest.MonkeyPatch, hub: str
) -> None:
    """T-02-05-02 — 6 reserved name (postgres/cocoindex/template0/template1/
    public/medinet) reject để tránh collision Postgres system / role medinet.
    """
    _set_env(
        monkeypatch,
        hub_name=hub,
        database_url="postgresql+asyncpg://u:p@h:5432/medinet_central",
    )
    with pytest.raises(ValidationError, match="hub_name reserved"):
        Settings()


def test_central_not_in_reserved_blacklist() -> None:
    """'central' KHÔNG trong blacklist — aggregator special-case (medinet_central
    DB mapping, KHÔNG prefix medinet_hub_).
    """
    assert "central" not in RESERVED_HUB_NAMES


def test_reserved_blacklist_size_is_6() -> None:
    """Lock blacklist size — thêm reserved name mới phải update test này +
    docstring RESERVED_HUB_NAMES.
    """
    assert len(RESERVED_HUB_NAMES) == 6


# ═══════════════════════════════════════════════════════════════════════════
# DSN match validator regression — Phase 1 _enforce_hub_dsn_match dùng
# self.hub_name dynamic, accept str từ trước → Plan 02-05 KHÔNG break.
# ═══════════════════════════════════════════════════════════════════════════


def test_dynamic_hub_dsn_match_ok(monkeypatch: pytest.MonkeyPatch) -> None:
    """Hub mới phap_che + DSN /medinet_hub_phap_che → _enforce_hub_dsn_match
    pass (Phase 1 validator dùng self.hub_name dynamic, KHÔNG hardcode 4 hub).
    """
    _set_env(
        monkeypatch,
        hub_name="phap_che",
        database_url="postgresql+asyncpg://u:p@h:5432/medinet_hub_phap_che",
    )
    s = Settings()
    assert s.hub_name == "phap_che"
    assert s.database_url.endswith("/medinet_hub_phap_che")


def test_dynamic_hub_dsn_mismatch_raises(monkeypatch: pytest.MonkeyPatch) -> None:
    """Hub mới phap_che nhưng DSN /medinet_hub_marketing → fail-fast (E-V3-3
    enforce hub con KHÔNG truy cập data hub khác).
    """
    _set_env(
        monkeypatch,
        hub_name="phap_che",
        database_url="postgresql+asyncpg://u:p@h:5432/medinet_hub_marketing",
    )
    with pytest.raises(ValidationError, match="DSN mismatch hub_name"):
        Settings()
```

**Step 5: Run test verify** (sẽ chạy ở `<verify>`).
  </action>
  <verify>
    <automated>cd Hub_All/api &amp;&amp; uv run pytest tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hub_name: str" Hub_All/api/app/config.py` ≥ 1 (Literal → str)
    - `grep -c "Literal\[\"central\"" Hub_All/api/app/config.py` == 0 (Literal[hub] đã xoá)
    - `grep -c "RESERVED_HUB_NAMES" Hub_All/api/app/config.py` ≥ 2 (constant + usage)
    - `grep -c "_validate_hub_name\|@field_validator..hub_name." Hub_All/api/app/config.py` ≥ 1 (validator function)
    - `grep -c '\^\[a-z\]\[a-z0-9_\]' Hub_All/api/app/config.py` ≥ 1 (regex literal)
    - `grep -c "postgres.*cocoindex.*template\|template0\|template1\|public.*medinet" Hub_All/api/app/config.py` ≥ 1 (blacklist content)
    - `grep -c "_enforce_hub_dsn_match" Hub_All/api/app/config.py` ≥ 1 (Phase 1 validator KHÔNG xoá)
    - pytest unit `test_config_hub_name_dynamic.py` ≥ 20 test PASS (4 regression + 3 dynamic + 2 boundary + 10 reject pattern + 6 reject reserved + 1 'central' not reserved + 1 size lock + 2 DSN match dynamic — gộp parametrize)
    - pytest unit `test_config_hub_name.py` 11/11 PASS (10 original + 1 đổi test 5 thành test_invalid_hub_name_pattern_raises)
    - `ruff check api/app/config.py api/tests/unit/test_config_hub_name.py api/tests/unit/test_config_hub_name_dynamic.py` exit 0
    - `mypy --strict api/app/config.py` exit 0 (str type không break mypy)
    - Phase 1 regression: pytest `tests/unit/test_config_hub_name.py` 11/11 PASS (sau khi đổi test 5)
    - Plan 02-01 regression: pytest `tests/unit/test_main_factory.py` 9/9 PASS (create_app() KHÔNG break)
  </acceptance_criteria>
  <done>
api/app/config.py refactor: Literal["central","yte","duoc","hcns"] → str + field_validator regex `^[a-z][a-z0-9_]{0,15}$` + RESERVED_HUB_NAMES blacklist 6 name; _enforce_hub_dsn_match KHÔNG sửa (dùng self.hub_name dynamic); test_config_hub_name.py test 5 đổi sang `test_invalid_hub_name_pattern_raises` với uppercase input; tạo test_config_hub_name_dynamic.py với ≥ 20 test PASS (regression + dynamic + reject + DSN match dynamic). ruff + mypy + Phase 1 regression PASS.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo scripts/hub-add.sh + docker-compose.override.yml.template + Makefile target hub-add — sync hub-init.sh regex với Settings</name>
  <files>api/scripts/hub-add.sh, docker-compose.override.yml.template, Hub_All/Makefile, api/Makefile, api/scripts/hub-init.sh</files>
  <read_first>
    - Hub_All/api/scripts/hub-init.sh (Phase 1 ship — regex `{1,30}` cần sync xuống `{0,15}` match Settings Plan 02-05)
    - Hub_All/api/scripts/init-db.sh (pattern bash script idempotent reference)
    - Hub_All/docker-compose.yml (Plan 02-02 ship — service block template inline cho python-api-yte/duoc/hcns; Plan 02-05 sao chép pattern vào template)
    - Hub_All/Makefile + Hub_All/api/Makefile (hub-init target hiện hữu — pattern make proxy bash script)
    - Hub_All/.gitignore (verify docker-compose.override.yml đã gitignored hay chưa — nếu chưa cần add)
  </read_first>
  <action>
**Step 1: Sync hub-init.sh regex** — `api/scripts/hub-init.sh` đổi pattern từ `^[a-z][a-z0-9_]{1,30}$` thành `^[a-z][a-z0-9_]{0,15}$` để match Settings Plan 02-05.

```bash
# api/scripts/hub-init.sh line 40 — đổi:
# CŨ:
if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{1,30}$ ]]; then
    echo "[hub-init] ERROR: hub name '$HUB' invalid. Pattern: ^[a-z][a-z0-9_]{1,30}$"

# MỚI (sync Plan 02-05):
if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{0,15}$ ]]; then
    echo "[hub-init] ERROR: hub name '$HUB' invalid. Pattern: ^[a-z][a-z0-9_]{0,15}$ (sync Settings Plan 02-05 FACTOR-04)"
```

Verify Phase 1 hub gốc (yte, duoc, hcns) đều match `{0,15}` (3-4 char OK). Phase 1 dynamic test cũng dùng "phap_che" (8 char OK).

**Step 2: Tạo `docker-compose.override.yml.template`** — service block snippet cho hub mới.

Vì YAML anchor (`*api-template`) cross-file KHÔNG hoạt động (xem `<interfaces>` pitfall), template phải inline đầy đủ:

```yaml
# Plan 02-05 FACTOR-04 — Template service block cho hub mới (`make hub-add HUB=<name>`).
# Đây là TEMPLATE — script hub-add.sh sed substitute {{HUB}} + {{PORT}} rồi
# append vào docker-compose.override.yml (gitignored — operator-local).
# Docker compose tự merge docker-compose.yml + override khi `docker compose up`.
# YAML anchor `*api-template` từ base file KHÔNG visible cross-file → inline đầy đủ.

  python-api-{{HUB}}:
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - ./api/.env
    container_name: medinet-api-{{HUB}}
    environment:
      HUB_NAME: {{HUB}}
      DATABASE_URL: postgresql+asyncpg://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_hub_{{HUB}}
      COCOINDEX_DATABASE_URL: postgresql://medinet:${POSTGRES_PASSWORD:-medinet_dev_pwd}@postgres:5432/medinet_cocoindex
      REDIS_URL: redis://redis:6379/0
      APP_NAMESPACE: medinet_prod
      COCOINDEX_DB_SCHEMA: cocoindex
      JWT_PRIVATE_KEY_PATH: /keys/private.pem
      JWT_PUBLIC_KEY_PATH: /keys/public.pem
      FILE_STORE_DIR: /file_store
      APP_ENV: ${APP_ENV:-dev}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      COCOINDEX_DB: /app/.cocoindex/state.lmdb
      COCOINDEX_LMDB_PATH: /app/.cocoindex/state.lmdb
    volumes:
      - ./api/keys:/keys:ro
      - ./file_store:/file_store
      - medinet_cocoindex_{{HUB}}:/app/.cocoindex
    ports:
      - "{{PORT}}:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks: [medinet_net]
```

Note placeholder format `{{HUB}}` + `{{PORT}}` — sed substitute `s/{{HUB}}/<name>/g` + `s/{{PORT}}/<port>/g` trong hub-add.sh.

**Step 3: Tạo `api/scripts/hub-add.sh`** — wrap quanh hub-init.sh + auto-gen compose block.

```bash
#!/usr/bin/env bash
# Medinet Wiki API — Dynamic hub registration (Plan 02-05 FACTOR-04).
#
# Tao 1 hub moi end-to-end (DB + compose service) bang 1 lenh:
#   1. Validate hub_name format regex (sync Settings Plan 02-05 + hub-init.sh)
#   2. Validate hub_name reserved blacklist (sync Settings RESERVED_HUB_NAMES)
#   3. Validate hub chua co trong docker-compose.yml base + override
#   4. Auto-detect port (8180 + N hien huu + 1) neu khong truyen explicit
#   5. Call hub-init.sh <HUB> -> CREATE DATABASE + ext + HNSW + alembic upgrade
#   6. Sed substitute docker-compose.override.yml.template -> append override
#   7. In huong dan next step (docker compose up -d python-api-<HUB>)
#
# Usage:
#   bash api/scripts/hub-add.sh <hub_name> [<port>]
#   HUB=<name> [PORT=<port>] bash api/scripts/hub-add.sh
#
# Vd:
#   bash api/scripts/hub-add.sh phap_che           # auto-detect port
#   bash api/scripts/hub-add.sh phap_che 8189     # explicit port
#
# Yeu cau prereq:
#   - Postgres container dang chay (docker compose up -d postgres)
#   - api/.env DATABASE_URL tro medinet_central
#   - Working directory = repo root (chua docker-compose.yml) hoac Hub_All/
#
# Validation chain (fail-fast cac error pre-DB-create):
#   - Regex format -> reject pre-CREATE DATABASE (orphan DB cleanup deu phai)
#   - Reserved blacklist -> reject pre-CREATE DATABASE
#   - Duplicate service detect -> reject pre-CREATE DATABASE

set -euo pipefail

# ──────────────────────────────────────────────────────────────────────────
# (1/7) Parse args
# ──────────────────────────────────────────────────────────────────────────

HUB=${1:-${HUB:-}}
PORT=${2:-${PORT:-}}

if [ -z "$HUB" ]; then
    echo "[hub-add] ERROR: thieu hub name."
    echo "Usage: bash api/scripts/hub-add.sh <hub_name> [<port>]"
    echo "       HUB=<name> [PORT=<port>] bash api/scripts/hub-add.sh"
    exit 2
fi

# ──────────────────────────────────────────────────────────────────────────
# (2/7) Validate hub_name regex format (sync Settings Plan 02-05)
# ──────────────────────────────────────────────────────────────────────────

if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{0,15}$ ]]; then
    echo "[hub-add] ERROR: hub name '$HUB' invalid format."
    echo "  Pattern required: ^[a-z][a-z0-9_]{0,15}$"
    echo "  Reject: uppercase / hyphen / starting-digit / starting-underscore / > 16 char."
    echo "  (sync Settings app/config.py FACTOR-04 Plan 02-05)"
    exit 2
fi

# ──────────────────────────────────────────────────────────────────────────
# (3/7) Validate hub_name reserved blacklist (sync RESERVED_HUB_NAMES)
# ──────────────────────────────────────────────────────────────────────────

RESERVED_NAMES=("postgres" "cocoindex" "template0" "template1" "public" "medinet")
for reserved in "${RESERVED_NAMES[@]}"; do
    if [ "$HUB" = "$reserved" ]; then
        echo "[hub-add] ERROR: hub name '$HUB' reserved."
        echo "  6 reserved names: ${RESERVED_NAMES[@]}"
        echo "  (Postgres system DB / role medinet collision)"
        exit 2
    fi
done

# 'central' KHONG reserved (aggregator special) nhung tao hub_central qua hub-add
# se gay confusion - reject explicit.
if [ "$HUB" = "central" ]; then
    echo "[hub-add] ERROR: 'central' la aggregator special-case, da co san."
    echo "  Hub mới phai khac 'central'."
    exit 2
fi

# ──────────────────────────────────────────────────────────────────────────
# (4/7) Determine repo root + check docker-compose.yml exist
# ──────────────────────────────────────────────────────────────────────────

# Find compose root: ưu tien cwd nếu có docker-compose.yml, fallback Hub_All/
if [ -f "docker-compose.yml" ]; then
    COMPOSE_ROOT="$(pwd)"
elif [ -f "Hub_All/docker-compose.yml" ]; then
    COMPOSE_ROOT="$(pwd)/Hub_All"
elif [ -f "../docker-compose.yml" ]; then
    COMPOSE_ROOT="$(cd .. && pwd)"
else
    echo "[hub-add] ERROR: KHONG tim thay docker-compose.yml."
    echo "  Chay tu repo root hoac Hub_All/ directory."
    exit 2
fi

TEMPLATE_PATH="$COMPOSE_ROOT/docker-compose.override.yml.template"
OVERRIDE_PATH="$COMPOSE_ROOT/docker-compose.override.yml"
BASE_PATH="$COMPOSE_ROOT/docker-compose.yml"

if [ ! -f "$TEMPLATE_PATH" ]; then
    echo "[hub-add] ERROR: KHONG tim thay $TEMPLATE_PATH"
    echo "  Plan 02-05 phai ship docker-compose.override.yml.template cùng commit."
    exit 2
fi

# ──────────────────────────────────────────────────────────────────────────
# (5/7) Detect duplicate service in base + override
# ──────────────────────────────────────────────────────────────────────────

if grep -q "^  python-api-${HUB}:" "$BASE_PATH"; then
    echo "[hub-add] ERROR: service python-api-${HUB} da ton tai trong docker-compose.yml base."
    echo "  Hub gốc Phase 2: central, yte, duoc, hcns. Hub trùng KHONG cho phep."
    exit 2
fi

if [ -f "$OVERRIDE_PATH" ] && grep -q "^  python-api-${HUB}:" "$OVERRIDE_PATH"; then
    echo "[hub-add] ERROR: service python-api-${HUB} da ton tai trong $OVERRIDE_PATH."
    echo "  Hub da add truoc - xoa thu cong neu muon re-create."
    exit 2
fi

# ──────────────────────────────────────────────────────────────────────────
# (6/7) Auto-detect port nếu chưa truyền (max port hien huu + 1)
# ──────────────────────────────────────────────────────────────────────────

if [ -z "$PORT" ]; then
    # Scan max port "NNNN:8080" trong base + override
    MAX_PORT=0
    while IFS= read -r line; do
        if [[ "$line" =~ \"([0-9]{4,5}):8080\" ]]; then
            port="${BASH_REMATCH[1]}"
            if [ "$port" -gt "$MAX_PORT" ]; then
                MAX_PORT=$port
            fi
        fi
    done < <(cat "$BASE_PATH" "$OVERRIDE_PATH" 2>/dev/null || true)

    if [ "$MAX_PORT" -eq 0 ]; then
        PORT=8184  # fallback nếu base parse fail
    else
        PORT=$((MAX_PORT + 1))
    fi
    echo "[hub-add] Auto-detect port: $PORT (max hien huu = $MAX_PORT)"
fi

# Validate port range 1024-65535 (privileged port reject)
if ! [[ "$PORT" =~ ^[0-9]+$ ]] || [ "$PORT" -lt 1024 ] || [ "$PORT" -gt 65535 ]; then
    echo "[hub-add] ERROR: port '$PORT' invalid. Range 1024-65535."
    exit 2
fi

# Detect port conflict trong base + override
if grep -qE "\"${PORT}:8080\"" "$BASE_PATH" 2>/dev/null; then
    echo "[hub-add] ERROR: port $PORT da xai trong docker-compose.yml base."
    exit 2
fi
if [ -f "$OVERRIDE_PATH" ] && grep -qE "\"${PORT}:8080\"" "$OVERRIDE_PATH"; then
    echo "[hub-add] ERROR: port $PORT da xai trong $OVERRIDE_PATH."
    exit 2
fi

# ──────────────────────────────────────────────────────────────────────────
# (7/7) Execute: hub-init.sh (DB) + sed template (compose) + register volume
# ──────────────────────────────────────────────────────────────────────────

echo "[hub-add] === Tao hub moi '$HUB' (port=$PORT, DB=medinet_hub_$HUB) ==="

# Step 7a: Call hub-init.sh (DB-level Phase 1)
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "[hub-add] (a) Goi $SCRIPT_DIR/hub-init.sh $HUB ..."
bash "$SCRIPT_DIR/hub-init.sh" "$HUB"

# Step 7b: Sed substitute template -> append override
echo "[hub-add] (b) Generate service block tu template..."
if [ ! -f "$OVERRIDE_PATH" ]; then
    # First-time: write header
    cat > "$OVERRIDE_PATH" <<EOF
# Plan 02-05 FACTOR-04 — Dynamic hub registration override (auto-generated).
# Generate by: scripts/hub-add.sh
# Edit thu cong KHONG khuyen nghi - re-run hub-add.sh se append/conflict.
# .gitignored - hub mới la operator-local, KHONG commit.

services:
EOF
fi

# Append service block (sed substitute placeholder)
sed "s/{{HUB}}/$HUB/g; s/{{PORT}}/$PORT/g" "$TEMPLATE_PATH" >> "$OVERRIDE_PATH"

# Append volume declaration (cocoindex LMDB per-hub) nếu chưa có volumes: section
if ! grep -q "^volumes:" "$OVERRIDE_PATH"; then
    cat >> "$OVERRIDE_PATH" <<EOF

volumes:
  medinet_cocoindex_$HUB:
EOF
else
    # Volumes section đã có — append volume mới sau marker
    sed -i.bak "/^volumes:/a\\
  medinet_cocoindex_$HUB:
" "$OVERRIDE_PATH" && rm -f "${OVERRIDE_PATH}.bak"
fi

# Step 7c: Verify docker compose config merge OK
echo "[hub-add] (c) Verify docker compose config merge OK..."
cd "$COMPOSE_ROOT"
if ! docker compose config --quiet 2>&1; then
    echo "[hub-add] ERROR: docker compose config merge FAIL sau append."
    echo "  Override file: $OVERRIDE_PATH"
    echo "  Xoa thu cong block python-api-$HUB neu can rollback."
    exit 3
fi

# ──────────────────────────────────────────────────────────────────────────
# DONE
# ──────────────────────────────────────────────────────────────────────────

echo "[hub-add] DONE — hub '$HUB' ready."
echo ""
echo "Next steps:"
echo "  1. Build + up service:"
echo "     docker compose up -d python-api-$HUB"
echo ""
echo "  2. Verify health:"
echo "     curl http://localhost:$PORT/api/health"
echo "     Expected: 200 {\"success\":true,\"data\":{\"status\":\"ok\"},...}"
echo ""
echo "  3. Logs:"
echo "     docker compose logs -f python-api-$HUB"
echo ""
echo "  4. Frontend route (Phase 5 Caddy chua ship):"
echo "     Direct: http://localhost:$PORT/api/..."
echo "     Caddy subpath (Phase 5): http://wiki.domain.com/$HUB/api/..."
echo ""
echo "Override file: $OVERRIDE_PATH"
echo "Hub_registry persistence: defer Phase 6 SETTINGS-04 hub_registry table."
```

**Step 4: Update `api/scripts/hub-add.sh` permissions** — `chmod +x`.

**Step 5: Update `Hub_All/Makefile`** — append target `hub-add`:

```makefile
# ═══════════════════════════════════════════════════════════════════════════
# Plan 02-05 FACTOR-04 — Dynamic hub registration end-to-end
# ═══════════════════════════════════════════════════════════════════════════
# Tien dieu kien giong hub-init: postgres dang chay + .env DATABASE_URL central.
# Khac biet: hub-add wrap quanh hub-init + auto-gen docker-compose.override.yml
# service block + volume declaration. Operator chay 1 lenh la tao xong hub moi.
#
# Usage:
#   make hub-add HUB=phap_che              # auto-detect port (8184 trở lên)
#   make hub-add HUB=marketing PORT=8200   # explicit port
#
# Sau khi DONE:
#   docker compose up -d python-api-phap_che
#   curl http://localhost:<port>/api/health

hub-add:
	@if [ -z "$(HUB)" ]; then echo "Usage: make hub-add HUB=<name> [PORT=<port>]"; exit 2; fi
	@bash Hub_All/api/scripts/hub-add.sh $(HUB) $(PORT)
```

Append vào `.PHONY` declaration line 11-13: thêm `hub-add`.

Append vào help text:

```
@echo "  hub-add HUB=<name> [PORT=<port>] - them hub moi end-to-end (DB + ext vector + alembic + compose service block)"
```

**Step 6: Update `api/Makefile`** — append target `hub-add`:

```makefile
# === Dynamic hub registration end-to-end (Plan 02-05 v3.0 FACTOR-04) ===
# Wrap quanh hub-init + auto-gen docker-compose.override.yml service block.
# Goi: make hub-add HUB=<name> [PORT=<port>]
# Sau khi DONE: docker compose up -d python-api-<name>

hub-add:
	@if [ -z "$(HUB)" ]; then echo "Usage: make hub-add HUB=<name> [PORT=<port>]"; exit 2; fi
	@bash scripts/hub-add.sh $(HUB) $(PORT)
```

Append vào `.PHONY` line 4: thêm `hub-add`.

**Step 7: Verify `.gitignore` exclude `docker-compose.override.yml`** — if chưa có, add line:

```
# Plan 02-05 FACTOR-04 — Dynamic hub registration override (operator-local).
docker-compose.override.yml
```

Note: Template (`docker-compose.override.yml.template`) PHẢI commit (source-of-truth). Override file (`docker-compose.override.yml`) operator-local KHÔNG commit.

**Lưu ý ngôn ngữ:** Comment trong bash + Makefile dùng tiếng Việt không dấu (encoding bash an toàn). Help text + error message dùng tiếng Việt có dấu OK (file utf-8). Template YAML dùng tiếng Anh comment vì là spec file.
  </action>
  <verify>
    <automated>cd Hub_All &amp;&amp; bash -n api/scripts/hub-add.sh &amp;&amp; docker compose config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Hub_All/api/scripts/hub-add.sh && test -x Hub_All/api/scripts/hub-add.sh` PASS (file exist + executable)
    - `test -f Hub_All/docker-compose.override.yml.template` PASS (template file exist)
    - `grep -c "{{HUB}}" Hub_All/docker-compose.override.yml.template` ≥ 5 (placeholder used)
    - `grep -c "{{PORT}}" Hub_All/docker-compose.override.yml.template` ≥ 1 (placeholder used)
    - `grep -c "python-api-{{HUB}}\|HUB_NAME: {{HUB}}\|medinet_hub_{{HUB}}" Hub_All/docker-compose.override.yml.template` ≥ 3 (key positions placeholder)
    - `grep -c "hub-add\:" Hub_All/Makefile` ≥ 1 (Makefile target exist)
    - `grep -c "hub-add\:" Hub_All/api/Makefile` ≥ 1 (api/Makefile target exist)
    - `grep -c '\^\[a-z\]\[a-z0-9_\]' Hub_All/api/scripts/hub-add.sh` ≥ 1 (regex same as Settings)
    - `grep -c '\^\[a-z\]\[a-z0-9_\]{0,15}' Hub_All/api/scripts/hub-init.sh` ≥ 1 (hub-init.sh regex synced)
    - `grep -c "0,15" Hub_All/api/scripts/hub-init.sh` ≥ 1 (sync regex max 16 char Plan 02-05)
    - `grep -c "0,15" Hub_All/api/scripts/hub-add.sh` ≥ 1
    - `grep -c "postgres.*cocoindex.*template\|RESERVED_NAMES=" Hub_All/api/scripts/hub-add.sh` ≥ 1 (reserved blacklist)
    - `bash -n Hub_All/api/scripts/hub-add.sh` exit 0 (bash syntax OK)
    - `grep -c "docker-compose.override.yml" Hub_All/.gitignore` ≥ 1 (gitignored)
    - `docker compose -f Hub_All/docker-compose.yml config --quiet` exit 0 (base compose vẫn parse OK)
  </acceptance_criteria>
  <done>
api/scripts/hub-add.sh: bash script wrap hub-init.sh + sed substitute template + auto-detect port + reserved blacklist + duplicate detect + post-write verify `docker compose config`. docker-compose.override.yml.template: service block inline với placeholder {{HUB}} {{PORT}}. Hub_All/Makefile + api/Makefile: target hub-add proxy script + help text. api/scripts/hub-init.sh: regex sync `{0,15}` (giảm từ {1,30}). .gitignore: exclude docker-compose.override.yml.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING smoke] make hub-add HUB=tmp_test PORT=8189 → docker compose up + curl health → cleanup</name>
  <what-built>
Plan 02-05 Task 1 (Settings str + validator) + Task 2 (hub-add.sh + template + Makefile) đã ship. Task 3 verify end-to-end runtime: `make hub-add HUB=tmp_test PORT=8189` tạo hub mới hoàn chỉnh, container boot 200/health, cleanup không leak.

Đây là BLOCKING checkpoint — KHÔNG advance Task 4 (docs update) nếu smoke fail. Smoke fail = real issue runtime KHÔNG visible qua unit test (vd Postgres permission gốc role 'medinet' không có CREATEDB, alembic env.py không resolve dynamic DSN, docker compose merge override syntax invalid).
  </what-built>
  <how-to-verify>
Operator (user) chạy chuỗi command sau từ `Hub_All/` cwd. Mọi step expect exit 0; curl expect 200/health envelope.

**Step 0 — Đảm bảo prereq:**

```bash
cd Hub_All
docker compose up -d postgres redis
sleep 10
docker compose ps postgres redis
# Expected: cả 2 service "Up (healthy)"

# .env file chứa DATABASE_URL trỏ medinet_central + HUB_NAME=central
cat api/.env | grep -E "^DATABASE_URL=|^HUB_NAME=" || true
```

**Step 1 — Test reject invalid pattern (regex + reserved):**

```bash
# Reject uppercase
make hub-add HUB=Yte_test PORT=8189 2>&1 | grep -i "invalid format" || echo "FAIL: must reject uppercase"

# Reject hyphen
make hub-add HUB=phap-che PORT=8189 2>&1 | grep -i "invalid format" || echo "FAIL: must reject hyphen"

# Reject reserved
make hub-add HUB=postgres PORT=8189 2>&1 | grep -i "reserved" || echo "FAIL: must reject postgres reserved"
make hub-add HUB=medinet PORT=8189 2>&1 | grep -i "reserved" || echo "FAIL: must reject medinet reserved"

# Reject 'central' explicit
make hub-add HUB=central PORT=8189 2>&1 | grep -i "central.*aggregator\|aggregator.*special" || echo "FAIL: must reject central"
```

**Step 2 — Test accept hub mới `tmp_test`:**

```bash
make hub-add HUB=tmp_test PORT=8189
# Expected DONE message:
#   [hub-add] DONE — hub 'tmp_test' ready.
# Verify side effects:
ls -la docker-compose.override.yml  # File tồn tại
cat docker-compose.override.yml | grep "python-api-tmp_test"  # Service block append
cat docker-compose.override.yml | grep "medinet_cocoindex_tmp_test"  # Volume append
psql -h localhost -U medinet -d postgres -c "SELECT 1 FROM pg_database WHERE datname='medinet_hub_tmp_test'" -tA  # → "1"
```

**Step 3 — Verify docker compose merge OK:**

```bash
docker compose config --services | grep python-api-tmp_test
# Expected: dòng "python-api-tmp_test"
docker compose config | grep -A 3 "python-api-tmp_test:" | head -20
# Expected: service block render với HUB_NAME=tmp_test + port 8189
```

**Step 4 — Build + up service mới:**

```bash
docker compose up -d python-api-tmp_test
sleep 20  # Wait cocoindex setup_blocking + lifespan boot
docker compose ps python-api-tmp_test
# Expected: container "Up" status
```

**Step 5 — Curl health endpoint:**

```bash
curl -sS http://localhost:8189/api/health | head -200
# Expected: 200 envelope {"success":true,"data":{"status":"ok"},"error":null,"meta":...}

curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8189/api/health
# Expected: 200
```

**Step 6 — Verify hub isolation (E-V3-3 carry forward) — `tmp_test` KHÔNG access được data hub khác:**

```bash
docker exec medinet-api-tmp_test env | grep -E "^HUB_NAME=|^DATABASE_URL="
# Expected:
#   HUB_NAME=tmp_test
#   DATABASE_URL=postgresql+asyncpg://medinet:...@postgres:5432/medinet_hub_tmp_test
```

**Step 7 — Verify central-only router STRIP ở hub `tmp_test` (FACTOR-02 carry forward):**

```bash
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8189/api/rag-config
# Expected: 404 (hub con strip rag_config router)

curl -sS http://localhost:8189/api/rag-config | head -200
# Expected: envelope {"success":false,"data":null,"error":{"code":"NOT_FOUND",...},"meta":...}
```

**Step 8 — Cleanup hub `tmp_test`:**

```bash
# Stop + remove container
docker compose stop python-api-tmp_test
docker compose rm -f python-api-tmp_test

# Drop DB (operator authorize)
psql -h localhost -U medinet -d postgres -c "DROP DATABASE IF EXISTS medinet_hub_tmp_test"

# Remove override block (manual — script reverse defer v3.0-b)
# Operator edit docker-compose.override.yml: xoá block python-api-tmp_test + volume medinet_cocoindex_tmp_test

# Remove volume (operator)
docker volume rm Hub_All_medinet_cocoindex_tmp_test 2>/dev/null || true

# Verify cleanup
docker compose ps python-api-tmp_test 2>&1 | grep -i "no such" || true
psql -h localhost -U medinet -d postgres -c "SELECT 1 FROM pg_database WHERE datname='medinet_hub_tmp_test'" -tA
# Expected: empty result
```

**Resume signal — user reply:**
- `"approved"` nếu mọi step PASS expected output → Plan 02-05 continue Task 4 (docs update).
- `"failed: <step>"` nếu step nào fail → STOP Plan 02-05, report root cause cho user, có thể cần re-execute Task 1 hoặc 2 fix.
- `"skip smoke"` nếu Docker Desktop không available local → accept risk; Plan 02-05 continue Task 4 dựa trên unit test PASS Task 1 + bash syntax PASS Task 2.
  </how-to-verify>
  <resume-signal>
Type "approved" nếu smoke 8 step PASS expected output (reject 5 invalid + accept tmp_test + docker compose merge OK + container boot + curl /api/health 200 + 404 envelope strip + cleanup OK). HOẶC "skip smoke" nếu Docker Desktop không available local. HOẶC "failed: <step>" + paste output nếu step nào fail.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update Hub_All/README.md + Hub_All/CLAUDE.md section 6 + .planning/STATE.md — FACTOR-04 deliverable note</name>
  <files>Hub_All/README.md, Hub_All/CLAUDE.md, .planning/STATE.md</files>
  <read_first>
    - Hub_All/README.md (file đang sửa — thêm section "Add a new hub")
    - Hub_All/CLAUDE.md (Plan 02-04 closeout có thể đã đụng section 6 — Plan 02-05 thêm note FACTOR-04 dynamic)
    - Hub_All/.planning/STATE.md (Plan 02-04 closeout đã set completed_plans=9, Plan 02-05 update tiếp → 10)
  </read_first>
  <action>
**Step 1: Update `Hub_All/README.md`** — thêm section "Add a new hub (dynamic)" sau section deployment hoặc cuối.

```markdown
## Add a new hub (dynamic registration — FACTOR-04 Plan 02-05)

Thêm hub mới (vd `phap_che`, `marketing`) hoàn chỉnh bằng 1 lệnh — KHÔNG sửa code Python / `docker-compose.yml` base:

```bash
# 1. Đảm bảo postgres + redis up
docker compose up -d postgres redis

# 2. Tạo hub mới (DB + ext vector + alembic + compose service block)
make hub-add HUB=phap_che              # auto-detect port (8184 trở lên)
# HOẶC explicit port:
make hub-add HUB=phap_che PORT=8200

# 3. Build + up service mới
docker compose up -d python-api-phap_che

# 4. Verify health
curl http://localhost:<port>/api/health
# Expected: {"success":true,"data":{"status":"ok"},...}
```

**Validation rules:**
- Pattern hub name: `^[a-z][a-z0-9_]{0,15}$` (lowercase a-z bắt đầu, max 16 char, a-z0-9_ rest — KHÔNG hyphen/uppercase).
- Reserved name reject: `postgres`, `cocoindex`, `template0`, `template1`, `public`, `medinet` (Postgres system collision).
- `central` reject (aggregator special-case đã có sẵn).

**Phía sau hậu trường:**
- `make hub-add` → `scripts/hub-add.sh` validate format + reserved + duplicate.
- Call `scripts/hub-init.sh` (Phase 1 ship) — `CREATE DATABASE medinet_hub_<name>` + `CREATE EXTENSION vector` + HNSW verify + `alembic upgrade head`.
- Sed substitute `docker-compose.override.yml.template` → append `docker-compose.override.yml` (gitignored — operator-local).
- Docker compose tự merge `docker-compose.yml` base + override khi `up`.

**Cleanup hub mới:**
```bash
docker compose stop python-api-<name>
docker compose rm -f python-api-<name>
psql -h localhost -U medinet -d postgres -c "DROP DATABASE IF EXISTS medinet_hub_<name>"
# Manual edit docker-compose.override.yml — xoá block python-api-<name> + volume medinet_cocoindex_<name>
docker volume rm medinet_cocoindex_<name>
```

Reverse script (`hub-remove.sh`) defer v3.0-b (Phase 7 MIGRATE-03 sẽ ship tooling truncate central skeleton).

**Hub registry source-of-truth (long-term):** Phase 6 SETTINGS-04 sẽ ship `hub_registry` table ở `medinet_central` — central admin CRUD; hub con đọc TTL cache. Hiện Plan 02-05 chỉ validate format Settings + sinh compose block — operator phải manual track danh sách hub đã add.

---
```

**Step 2: Update `Hub_All/CLAUDE.md` section 6** — note FACTOR-04 added vào v3.0 progress table.

Sau Plan 02-04 closeout đã thêm bảng "v3.0 progress" trong CLAUDE.md section 6 (xem 02-04-PLAN Task 2 spec). Plan 02-05 update bảng:

```markdown
| Phase | Status | Plan count | Date | REQ-ID |
|-------|--------|------------|------|--------|
| 1 — Multi-DB Topology + Per-hub Alembic | ✅ DONE | 5 plan | 2026-05-21 | TOPO-01..04 (4) |
| 2 — Hub-con Codebase Factor | ✅ DONE | 5 plan | <DATE> | FACTOR-01..04 (4 — FACTOR-04 added 2026-05-22 Plan 02-05) |
| 3 — Auth SSO + hub_ids trong JWT | 📋 Next | — | — | SSO-01..04 |
...
```

Và thêm subsection "Phase 2 FACTOR-04 pattern" sau "Phase 2 pattern":

```markdown
### Phase 2 FACTOR-04 dynamic hub registration (added 2026-05-22 — user direction B)

Operator thêm hub mới không sửa code:

- `Settings.hub_name` đổi `Literal[4]` → `str` + regex `^[a-z][a-z0-9_]{0,15}$` + reserved blacklist 6 name (postgres, cocoindex, template0, template1, public, medinet).
- `make hub-add HUB=<name> [PORT=<port>]` → `scripts/hub-add.sh` wrap `hub-init.sh` (DB) + sed substitute `docker-compose.override.yml.template` → append `docker-compose.override.yml` (gitignored, operator-local).
- Auto-detect port = max hiện hữu + 1 nếu user không truyền.
- Quick start operator: xem `Hub_All/README.md` section "Add a new hub".
- Hub registry source-of-truth defer Phase 6 SETTINGS-04.

Reference: `.planning/phases/02-hub-con-codebase-factor/02-05-PLAN.md`.
```

**Step 3: Update `.planning/STATE.md`** — frontmatter + Phase 2 Results Summary table thêm row 02-05.

```yaml
# Frontmatter — sau Plan 02-04 closeout completed_plans=9, percent=28. Plan 02-05 update:
status: Phase 2 DONE <DATE> ✅ (5 plans total — Plan 02-05 ship 2026-05-22 FACTOR-04 dynamic hub registration; user direction B sau Plan 02-04 closeout). ...
progress:
  total_phases: 7
  completed_phases: 2
  total_plans: 10  # 5 Phase 1 + 5 Phase 2
  completed_plans: 10
  percent: 31  # 10 / ~32 estimate
```

Phase 2 Results Summary table thêm row:

```markdown
| 02-05 | 4 | Dynamic hub registration FACTOR-04 — Settings str + regex + reserved blacklist + scripts/hub-add.sh + docker-compose.override.yml.template + Makefile target + README quick start | <N> | unit 20+/20+ PASS + smoke `make hub-add HUB=tmp_test` checkpoint |
```

Cập nhật "Phase 2 deliverable summary" thêm bullet:

```markdown
- **FACTOR-04 dynamic hub registration (added 2026-05-22 user direction B):** operator add hub mới (vd `phap_che`, `marketing`) bằng `make hub-add HUB=<name> [PORT=<port>]` mà KHÔNG sửa code/compose base. Settings regex + reserved blacklist + scripts/hub-add.sh wrap hub-init.sh + docker-compose.override.yml.template sed-substitute. Hub registry source-of-truth defer Phase 6 SETTINGS-04.
```

**Step 4: KHÔNG đổi.**
- Phase 1 Results Summary section.
- v3.0 LOCKED decisions section.
- Phase 3 Next Action section (Plan 02-04 đã set).
- Carry-forward risks section.

**Lưu ý:**
- `<DATE>` = ngày thực tế execute Plan 02-05.
- `<N>` commits = đếm git log của Plan 02-05.
- Tiếng Việt có dấu xuyên suốt.
  </action>
  <verify>
    <automated>grep -c "FACTOR-04\|hub-add\|dynamic hub registration\|02-05" Hub_All/README.md Hub_All/CLAUDE.md Hub_All/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Add a new hub\|Add a new hub (dynamic" Hub_All/README.md` ≥ 1 (section header)
    - `grep -c "make hub-add HUB=" Hub_All/README.md` ≥ 2 (CLI quick start examples)
    - `grep -c '\^\[a-z\]\[a-z0-9_\]' Hub_All/README.md` ≥ 1 (regex doc)
    - `grep -c "postgres.*cocoindex.*template\|template0.*template1.*public" Hub_All/README.md` ≥ 1 (reserved doc)
    - `grep -c "FACTOR-04" Hub_All/CLAUDE.md` ≥ 1 (CLAUDE.md note FACTOR-04 covered)
    - `grep -c "make hub-add\|dynamic hub registration" Hub_All/CLAUDE.md` ≥ 1
    - `grep -c "FACTOR-04" Hub_All/.planning/STATE.md` ≥ 1 (STATE traceability)
    - `grep -c "completed_plans: 10" Hub_All/.planning/STATE.md` ≥ 1 (frontmatter progress update)
    - `grep -c "total_plans: 10" Hub_All/.planning/STATE.md` ≥ 1
    - `grep -c "02-05" Hub_All/.planning/STATE.md` ≥ 1 (Plan 02-05 trong table)
    - `grep -c "user direction B\|hướng B" Hub_All/.planning/STATE.md` ≥ 1 (decision provenance)
    - Markdown structure preserve — table/blockquote syntax OK
  </acceptance_criteria>
  <done>
README.md có section "Add a new hub" với 3-step quick start + validation rules + cleanup procedure. CLAUDE.md section 6 v3.0 progress bảng phase 2 đổi `FACTOR-01..04 (4 — FACTOR-04 added Plan 02-05)` + subsection "Phase 2 FACTOR-04 dynamic hub registration" mô tả pattern. STATE.md frontmatter `completed_plans=10, percent=31` + Phase 2 Results Summary row 02-05 + deliverable summary bullet FACTOR-04. Tiếng Việt có dấu xuyên suốt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (user) → `make hub-add` | Operator authorized chạy script đổi DB + compose state. Script kiểm tra tiền điều kiện (postgres up, .env DATABASE_URL central) trước khi action. |
| `make hub-add` → Postgres CREATE DATABASE | Script call `hub-init.sh` (Phase 1) call `psql -U medinet` — role `medinet` cần CREATEDB privilege (M2 carry forward). Threat: nếu role bị strip CREATEDB → hub-add fail step 7a với rõ error. |
| `hub-add.sh` → `docker-compose.override.yml` (file system) | Script write file local — KHÔNG remote. Threat: file injection nếu hub_name không sanitize → đã validate regex pre-write. |
| Settings runtime → env HUB_NAME (attacker-controlled?) | env var có thể bị set externally (operator typo, CI env, malicious if compromised). Settings validator fail-fast startup nếu format invalid hoặc reserved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | env `HUB_NAME` attacker-controlled với special char (`'; DROP TABLE` SQL injection through DSN) | mitigate | Regex `^[a-z][a-z0-9_]{0,15}$` reject special char ở Settings + hub-add.sh. Phase 1 `_enforce_hub_dsn_match` validator strip DSN query string + check suffix — DSN không thể inject. Postgres identifier quoted internally bởi asyncpg/SQLAlchemy parameterized query. |
| T-02-05-02 | Elevation of Privilege | `HUB_NAME=postgres` → DSN trỏ `medinet_hub_postgres` (DB attacker-creatable), tạo DB OWNER=medinet (M2 role) → escalation theo path lateral DB. HOẶC `HUB_NAME=medinet` → role name confusion | mitigate | Reserved blacklist 6 name (`RESERVED_HUB_NAMES`) reject `postgres`, `medinet`, `template0`, `template1`, `public`, `cocoindex` ở Settings + hub-add.sh. Unit test `test_reject_reserved_hub_names` lock blacklist parametrize 6 name. |
| T-02-05-03 | Denial of Service | `HUB_NAME=very_long_name_> 16 char` → Postgres identifier 63 char limit có thể fit nhưng `medinet_hub_<name>` + cocoindex `medinet_<hub>_<table>` các phối hợp khả năng > 63 → CREATE TABLE fail runtime. | mitigate | Regex max 16 char hard cap. Settings validator + hub-add.sh validator reject pre-DB-create. Test `test_reject_too_long` parametrize 17-char input. |
| T-02-05-04 | Information Disclosure | `docker-compose.override.yml` commit lên repo có thể leak operator-local hub list (vd `tmp_test`, `dev_<engineer>`) — internal info exposure | mitigate | `.gitignore` exclude `docker-compose.override.yml`. Template file (`docker-compose.override.yml.template`) commit OK vì là spec không chứa hub name cụ thể. README.md document gitignore rule. |
| T-02-05-05 | Tampering | Operator manual edit `docker-compose.override.yml` rồi `hub-add` lần 2 — race condition append với existing hub trùng tên | mitigate | hub-add.sh Step 5 `grep -q "^  python-api-${HUB}:" $OVERRIDE_PATH` detect duplicate, exit 2 với rõ error. Operator manual xoá block nếu muốn re-create. |
| T-02-05-06 | Repudiation | Skip smoke checkpoint (Task 3) — KHÔNG có evidence end-to-end FACTOR-04 pass | mitigate | Task 3 resume signal "skip smoke" được phép NHƯNG rationale phải rõ (vd Docker không available). Unit test Task 1 (≥ 20 test) cover Settings validator + DSN match dynamic. Bash syntax check Task 2 + `docker compose config --quiet` parse OK = static verify compose layer. Smoke chỉ cover runtime startup — 90% rủi ro covered by static. |
| T-02-05-07 | Elevation of Privilege | Operator chạy `make hub-add` với role role không có CREATEDB privilege → `hub-init.sh` step 1 CREATE DATABASE fail nhưng compose override đã append → state inconsistent | accept | hub-init.sh chạy TRƯỚC compose append trong hub-add.sh sequence (Step 7a → Step 7b). Nếu hub-init.sh fail set -euo pipefail abort → compose chưa append. State consistent. Test bằng Task 3 smoke (Postgres up healthy nghĩa là role medinet có CREATEDB từ M2). |
| T-02-05-08 | Tampering | `docker-compose.override.yml.template` bị modify malicious (vd thêm `command: rm -rf /`) → mọi hub mới run command đó | mitigate | Template commit vào git → code review catch. Threat scope tương đương docker-compose.yml base bị tamper — không khác. Branch protection (defer v4.0) sẽ enforce review trước merge. |
| T-02-05-09 | DoS | Operator add 100 hub → 100 service container → resource exhaust laptop dev | accept | Phase 2 KHÔNG cover production capacity — operator self-aware. Phase 6 SETTINGS-04 `hub_registry` sẽ thêm capacity guard config-driven. |
</threat_model>

<verification>
**Plan 02-05 verification chain (FACTOR-04 extension):**

1. **Unit test Settings validator** (Task 1):
   - `pytest tests/unit/test_config_hub_name.py` 11/11 PASS (10 original + 1 đổi test 5)
   - `pytest tests/unit/test_config_hub_name_dynamic.py` ≥ 20 test PASS (4 regression + 3 dynamic + 2 boundary + 10 reject pattern + 6 reject reserved + 1 'central' not reserved + 1 size lock + 2 DSN match dynamic — gộp parametrize)
   - `mypy --strict api/app/config.py` exit 0
   - `ruff check api/` exit 0

2. **Static verify bash + compose** (Task 2):
   - `bash -n api/scripts/hub-add.sh` exit 0 (syntax OK)
   - `bash -n api/scripts/hub-init.sh` exit 0 (sync regex OK)
   - `docker compose config --quiet` exit 0 (base compose still parses with template file present)
   - `test -f docker-compose.override.yml.template && test -f api/scripts/hub-add.sh && test -x api/scripts/hub-add.sh` PASS
   - `grep` acceptance criteria Task 2 đầy đủ

3. **Smoke runtime [BLOCKING]** (Task 3):
   - Reject 5 invalid input — 4 regex + 6 reserved → exit 2 với error message
   - Accept `tmp_test` hub mới end-to-end: DB created + override appended + container boot + curl 200 + 404 envelope strip FACTOR-02
   - Hub isolation: `tmp_test` container env HUB_NAME=tmp_test + DSN /medinet_hub_tmp_test (E-V3-3 carry forward)
   - Cleanup: stop + rm container + DROP DATABASE + manual rm override block + rm volume

4. **Docs integrity** (Task 4):
   - README.md có section "Add a new hub" với 3-step + validation rules + cleanup
   - CLAUDE.md section 6 update FACTOR-04 covered + Phase 2 plan count 5 + Phase 2 FACTOR-04 pattern subsection
   - STATE.md frontmatter completed_plans=10, percent=31, Phase 2 Results Summary row 02-05

5. **Regression check** (Task 1 + 2 implicit):
   - Phase 1 Plan 01-02 test (`test_config_hub_name.py`) — 4 hub gốc accept (regression không break)
   - Plan 02-01 test (`test_main_factory.py`) — 9 test PASS create_app() 4 hub mode
   - Plan 02-03 integration test (`test_factor_hub_scoped.py`) — 10/10 PASS endpoint matrix
   - Plan 02-02 docker compose base — `docker compose config --services` 8 service render OK

6. **Git status clean post-commit**:
   - File modified committed: `api/app/config.py`, `api/tests/unit/test_config_hub_name.py`, `api/scripts/hub-init.sh`, `Hub_All/Makefile`, `api/Makefile`, `Hub_All/README.md`, `Hub_All/CLAUDE.md`, `.planning/STATE.md`
   - File new committed: `api/tests/unit/test_config_hub_name_dynamic.py`, `api/scripts/hub-add.sh`, `Hub_All/docker-compose.override.yml.template`
   - File gitignored (KHÔNG commit): `Hub_All/docker-compose.override.yml`
</verification>

<success_criteria>
- Settings.hub_name đổi Literal → str + regex `^[a-z][a-z0-9_]{0,15}$` + reserved blacklist 6 name (Task 1)
- 20+ unit test PASS test_config_hub_name_dynamic.py + 11/11 test_config_hub_name.py (Task 1)
- scripts/hub-add.sh + docker-compose.override.yml.template + Makefile target hub-add ship (Task 2)
- scripts/hub-init.sh regex sync `{0,15}` (Task 2)
- [BLOCKING] Smoke `make hub-add HUB=tmp_test PORT=8189` PASS hoặc "skip smoke" với rationale rõ (Task 3)
- README.md section "Add a new hub" + CLAUDE.md FACTOR-04 covered + STATE.md update completed_plans=10 (Task 4)
- Phase 1 regression: 4 hub gốc accept KHÔNG break (test_config_hub_name.py 11/11)
- Plan 02-01 regression: test_main_factory.py 9/9 PASS
- Plan 02-03 regression: test_factor_hub_scoped.py 10/10 PASS
- ruff + mypy --strict PASS toàn bộ file modified
- FACTOR-04 SATISFIED — operator thêm hub mới bằng 1 lệnh, KHÔNG sửa code/compose base
- D-V3-Phase2-Dynamic-A..E (5 decision LOCKED Plan 02-05) verified
- `docker-compose.override.yml` trong .gitignore
- Hub registry source-of-truth defer Phase 6 SETTINGS-04 documented
</success_criteria>

<output>
Tạo `.planning/phases/02-hub-con-codebase-factor/02-05-SUMMARY.md`:
- Plan 02-05 FACTOR-04 dynamic hub registration ship — file modified + new list
- Test count: 20+ unit test test_config_hub_name_dynamic.py + 11 test_config_hub_name.py
- Smoke checkpoint Task 3 result (PASS / SKIP với reason)
- D-V3-Phase2-Dynamic-A..E decision LOCKED — rationale rõ
- Settings refactor diff (Literal[4] → str + validator + blacklist)
- scripts/hub-add.sh design highlight (validate → hub-init.sh → sed template → docker compose config verify)
- docker-compose.override.yml.template snippet inline
- Makefile target + README quick start + CLAUDE.md note
- Threat model 9 STRIDE threat — mitigation summary
- Phase 2 5 plan ship — FACTOR-01..04 satisfied
- Hub registry source-of-truth defer Phase 6 SETTINGS-04 — future plan reference
- Next: Phase 2 fully DONE (5 plan); /gsd-discuss-phase 3 Auth SSO sẵn sàng start
</output>
</content>
