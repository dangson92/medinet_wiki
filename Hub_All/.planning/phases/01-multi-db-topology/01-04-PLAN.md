---
phase: 01-multi-db-topology
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - api/app/rag/flow.py
  - api/app/rag/setup.py
autonomous: true
requirements:
  - TOPO-03
tags:
  - cocoindex
  - app-namespace
  - per-hub
  - r5-carry-forward

must_haves:
  truths:
    - "Cocoindex App name resolve per-hub: medinet_<hub>_ingest (medinet_central_ingest cho hub central, medinet_yte_ingest cho yte, ...)"
    - "APP_NAMESPACE resolve per-hub: medinet_<hub>_prod thay cho cố định medinet_prod (R5 scale per-hub)"
    - "cocoindex_db_schema giữ cố định 'cocoindex' (P7 carry forward — internal tables tách schema)"
    - "Setup function setup_cocoindex(settings) set APP_NAMESPACE env trước coco.start_blocking() — Q5 lifecycle"
  artifacts:
    - path: "api/app/rag/flow.py"
      provides: "Factory function tạo cocoindex_app theo settings.hub_name; module-level cocoindex_app instance vẫn export cho backward compat (build từ get_settings)"
      contains: "medinet_.*_ingest\\|hub_name"
    - path: "api/app/rag/setup.py"
      provides: "setup_cocoindex set os.environ['APP_NAMESPACE'] = f'medinet_{hub_name}_prod' trước start_blocking"
      contains: "APP_NAMESPACE.*medinet_\\|hub_name"
  key_links:
    - from: "Settings.hub_name (Plan 02)"
      to: "flow.py App name resolver"
      via: "f'medinet_{settings.hub_name}_ingest'"
      pattern: "medinet_.*_ingest"
    - from: "Settings.hub_name (Plan 02)"
      to: "setup.py APP_NAMESPACE env"
      via: "os.environ['APP_NAMESPACE'] = f'medinet_{settings.hub_name}_prod'"
      pattern: "APP_NAMESPACE.*medinet_"
---

<objective>
Refactor `api/app/rag/flow.py` để cocoindex App name resolve theo `settings.hub_name`: `medinet_central_ingest` cho central, `medinet_yte_ingest` cho yte, etc. (thay vì hardcode `medinet_wiki_ingest` M2). Refactor `api/app/rag/setup.py` để set `os.environ["APP_NAMESPACE"] = f"medinet_{hub_name}_prod"` per-hub thay vì cố định `medinet_prod`. Giữ `cocoindex_db_schema="cocoindex"` cố định (P7 — internal tables ở schema riêng).

Purpose: TOPO-03 — Cocoindex internal tables (`cocoindex.medinet_<hub>_prod__*`) không đụng nhau giữa các DB hub khi 4 process cùng instance chạy. APP_NAMESPACE là R5 mitigation v2.0; v3.0 scale per-hub theo decision GA-Phase1-B LOCKED ở planner.

Output: 2 file Python refactor. KHÔNG sửa flow logic (extract/chunk/embed/declare_row) — chỉ đổi tên app + APP_NAMESPACE. KHÔNG sửa Alembic migrations (chunks table public schema giữ nguyên).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/01-multi-db-topology/01-02-PLAN.md
@Hub_All/api/app/rag/flow.py
@Hub_All/api/app/rag/setup.py
@Hub_All/api/app/config.py

<interfaces>
<!-- Snapshot flow.py M2 — relevant block App registration (line 268) -->

`api/app/rag/flow.py` hiện tại (M2):
```python
# Line 268 (M2):
cocoindex_app = coco.App(coco.AppConfig(name="medinet_wiki_ingest"), medinet_wiki_main)

__all__ = [
    "CHUNK_ID_NAMESPACE",
    "ChunkRow",
    "EMBEDDING_DIM",
    "PG_POOL_KEY",
    "cocoindex_app",
    "index_document",
    "medinet_wiki_main",
    "stable_chunk_id",
]
```

KEY: App name hiện hardcode `medinet_wiki_ingest`. Plan 04 refactor sang `medinet_<hub>_ingest` resolve từ `Settings.hub_name`.

**Tradeoff:** App instance registered module-level (side-effect import). Settings load qua `get_settings()` ở module-level OK (lru_cache singleton). Nhưng test cần thay đổi hub_name → reset singleton. Test ở plan này MOCK level — full integration ở Plan 05.

`api/app/rag/setup.py` hiện tại (M2 — line 44-72 setup_cocoindex):
```python
def setup_cocoindex(settings: Settings) -> None:
    # 1) Set COCOINDEX_DB (LMDB path)
    os.environ["COCOINDEX_DB"] = str(settings.cocoindex_lmdb_path)

    # 2) Register lifespan (asyncpg.Pool provide)
    from app.rag.flow import PG_POOL_KEY
    asyncpg_dsn = _to_asyncpg_dsn(settings.database_url)
    @coco.lifespan
    async def _lifespan(env_builder): ...

    # 3) Import flow → register coco.App
    from app.rag.flow import cocoindex_app

    # 4) Start env + apply schema
    coco.start_blocking()

    # 5) Initial backfill
    cocoindex_app.update_blocking()
```

KEY: APP_NAMESPACE env hiện KHÔNG được set tường minh trong setup.py — phụ thuộc Settings.app_namespace = "medinet_prod" + cocoindex SDK đọc qua env. Cần set tường minh `os.environ["APP_NAMESPACE"]` trước `coco.start_blocking()`.

Verify ở grep `os.environ\["APP_NAMESPACE"` trong codebase hiện tại — nếu chưa có → Plan 04 thêm.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor flow.py — App name resolve per-hub qua Settings.hub_name</name>
  <files>api/app/rag/flow.py, api/tests/unit/test_flow_per_hub_naming.py</files>
  <read_first>
    - api/app/rag/flow.py (file đang sửa — line 268 App instantiation)
    - api/app/config.py (Settings.hub_name field Plan 02 đã ship)
  </read_first>
  <behavior>
    - Test 1: `resolve_cocoindex_app_name("central")` trả "medinet_central_ingest"
    - Test 2: `resolve_cocoindex_app_name("yte")` trả "medinet_yte_ingest"
    - Test 3: `resolve_cocoindex_app_name("duoc")` trả "medinet_duoc_ingest"
    - Test 4: `resolve_cocoindex_app_name("hcns")` trả "medinet_hcns_ingest"
    - Test 5: `resolve_cocoindex_app_name("invalid")` raise ValueError chứa "không hợp lệ"
    - Test 6: Module-level `cocoindex_app` instance có attr `name` (hoặc `_name`) chứa "medinet_" + suffix "_ingest" (smoke check không kiểm hub cụ thể vì phụ thuộc env runtime)
  </behavior>
  <action>
**Step 1: Refactor `api/app/rag/flow.py`** — thay block App registration (line 264-269):

```python
# === App instance — registered on import (cocoindex 1.0.3 App pattern) ===
# v3.0 TOPO-03: App name resolve per-hub qua Settings.hub_name.
# medinet_<hub>_ingest = medinet_central_ingest | medinet_yte_ingest | ...

_VALID_HUBS_FLOW = {"central", "yte", "duoc", "hcns"}


def resolve_cocoindex_app_name(hub_name: str) -> str:
    """Resolve cocoindex App name theo hub.

    Format: medinet_<hub>_ingest. R5 carry forward — snake_case name registration
    để acceptance criteria grep `medinet_<hub>_ingest` xác định đúng app instance.

    Raises:
        ValueError: hub_name không thuộc 4 hub hợp lệ.
    """
    if hub_name not in _VALID_HUBS_FLOW:
        raise ValueError(
            f"hub_name={hub_name!r} không hợp lệ. "
            f"Hợp lệ: {sorted(_VALID_HUBS_FLOW)}."
        )
    return f"medinet_{hub_name}_ingest"


# Module-level App registration — settings.hub_name resolve ở import time.
# get_settings() lru_cache → 1 process = 1 hub = 1 app instance (KHÔNG re-register).
from app.config import get_settings as _get_settings  # noqa: E402 — import sau dataclass def
_settings = _get_settings()
_app_name = resolve_cocoindex_app_name(_settings.hub_name)
cocoindex_app = coco.App(coco.AppConfig(name=_app_name), medinet_wiki_main)
```

**Step 2: Đổi tên function `medinet_wiki_main`** — vẫn giữ tên function (Python identifier OK) vì caller mount qua reference object, không qua name string. KHÔNG đổi cho gọn diff.

**Step 3: Update `__all__`** — thêm `"resolve_cocoindex_app_name"`:

```python
__all__ = [
    "CHUNK_ID_NAMESPACE",
    "ChunkRow",
    "EMBEDDING_DIM",
    "PG_POOL_KEY",
    "cocoindex_app",
    "index_document",
    "medinet_wiki_main",
    "resolve_cocoindex_app_name",
    "stable_chunk_id",
]
```

**Step 4: Tạo `api/tests/unit/test_flow_per_hub_naming.py`** — 6 test theo behavior block:

```python
import pytest
from app.rag.flow import resolve_cocoindex_app_name, cocoindex_app

@pytest.mark.parametrize("hub,expected", [
    ("central", "medinet_central_ingest"),
    ("yte", "medinet_yte_ingest"),
    ("duoc", "medinet_duoc_ingest"),
    ("hcns", "medinet_hcns_ingest"),
])
def test_resolve_name(hub, expected):
    assert resolve_cocoindex_app_name(hub) == expected

def test_resolve_invalid_raises():
    with pytest.raises(ValueError, match="không hợp lệ"):
        resolve_cocoindex_app_name("invalid")

def test_module_app_name_contains_prefix_suffix():
    """Smoke check module-level instance — name có pattern medinet_<x>_ingest.
    KHÔNG check hub cụ thể vì phụ thuộc HUB_NAME env runtime."""
    name = getattr(cocoindex_app, "name", None) or getattr(cocoindex_app, "_name", "")
    assert name.startswith("medinet_") and name.endswith("_ingest"), \
        f"App name {name!r} không match pattern medinet_<hub>_ingest"
```

Lưu ý: Test 6 import `cocoindex_app` module-level → trigger `_get_settings()` runtime, cần test env có DATABASE_URL hợp lệ. Conftest existing `tests/conftest.py` (unit level) thường stub env qua `monkeypatch.setenv` ở fixture. Nếu test fail do thiếu env → wrap test 6 trong `try-except ValidationError` skip hoặc dùng `pytest.importorskip("app.rag.flow")`. Recommended: tạo fixture `_minimal_env_for_settings` ở test file set HUB_NAME=central + DATABASE_URL=...medinet_central + redis_url + cocoindex_database_url.

Sample fixture:
```python
@pytest.fixture(autouse=True)
def _minimal_env(monkeypatch):
    monkeypatch.setenv("HUB_NAME", "central")
    monkeypatch.setenv("DATABASE_URL", "postgresql+asyncpg://u:p@h:5432/medinet_central")
    monkeypatch.setenv("COCOINDEX_DATABASE_URL", "postgresql://u:p@h:5432/medinet_cocoindex")
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    # Reset lru_cache để Settings re-load với env mới
    from app.config import get_settings
    get_settings.cache_clear()
    yield
    get_settings.cache_clear()
```
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest tests/unit/test_flow_per_hub_naming.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def resolve_cocoindex_app_name" Hub_All/api/app/rag/flow.py` ≥ 1
    - `grep -c 'f"medinet_{' Hub_All/api/app/rag/flow.py` ≥ 1 (f-string với hub_name)
    - `grep -c "_VALID_HUBS_FLOW" Hub_All/api/app/rag/flow.py` ≥ 1
    - `grep -c "cocoindex_app = coco.App" Hub_All/api/app/rag/flow.py` ≥ 1 (App registration intact)
    - `grep -c "name=_app_name" Hub_All/api/app/rag/flow.py` ≥ 1 (dynamic name)
    - `grep -c 'name="medinet_wiki_ingest"' Hub_All/api/app/rag/flow.py` == 0 (M2 hardcode đã bỏ)
    - `grep -c "resolve_cocoindex_app_name" Hub_All/api/app/rag/flow.py` ≥ 2 (def + export __all__ + usage)
    - File `Hub_All/api/tests/unit/test_flow_per_hub_naming.py` exists, `grep -c "def test_\|@pytest.mark.parametrize" Hub_All/api/tests/unit/test_flow_per_hub_naming.py` ≥ 3
    - `cd Hub_All/api && uv run pytest tests/unit/test_flow_per_hub_naming.py -v` PASS toàn bộ
    - `cd Hub_All/api && uv run mypy app/rag/flow.py` exit 0
    - `cd Hub_All/api && uv run ruff check app/rag/flow.py tests/unit/test_flow_per_hub_naming.py` exit 0
  </acceptance_criteria>
  <done>
flow.py App name dynamic per-hub; resolve helper export; unit tests PASS; mypy + ruff clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor setup.py — set APP_NAMESPACE env per-hub trước coco.start_blocking</name>
  <files>api/app/rag/setup.py</files>
  <read_first>
    - api/app/rag/setup.py (file đang sửa — setup_cocoindex function)
    - api/app/config.py (Settings.app_namespace field hiện tại + Settings.hub_name)
  </read_first>
  <action>
Mở `api/app/rag/setup.py`. Sửa function `setup_cocoindex(settings: Settings)` thêm step set APP_NAMESPACE per-hub trước step 1 hiện tại (COCOINDEX_DB):

Block hiện tại (line 67-72):
```python
def setup_cocoindex(settings: Settings) -> None:
    # 1) Set COCOINDEX_DB (LMDB path) — Q5.
    os.environ["COCOINDEX_DB"] = str(settings.cocoindex_lmdb_path)
    logger.info(...)
```

Sửa thành (chèn step 0 mới):

```python
def setup_cocoindex(settings: Settings) -> None:
    # 0) Set APP_NAMESPACE per-hub — v3.0 TOPO-03 (R5 scale per-hub).
    #    Format: medinet_<hub_name>_prod. Trước v3.0 cố định "medinet_prod" cho mọi env;
    #    v3.0 mỗi hub có namespace riêng để cocoindex internal tables
    #    `cocoindex.medinet_<hub>_prod__*` không đụng nhau giữa các DB hub.
    #    P7 carry forward: cocoindex_db_schema vẫn "cocoindex" (không đổi schema).
    namespaced = f"medinet_{settings.hub_name}_prod"
    os.environ["APP_NAMESPACE"] = namespaced
    logger.info(
        "cocoindex_app_namespace_set: hub=%s namespace=%s",
        settings.hub_name, namespaced,
    )

    # 1) Set COCOINDEX_DB (LMDB path) — Q5.
    os.environ["COCOINDEX_DB"] = str(settings.cocoindex_lmdb_path)
    logger.info(
        "cocoindex_setup_start: lmdb=%s asyncpg_dsn=%s",
        settings.cocoindex_lmdb_path,
        "<redacted>",
    )

    # 2) Register lifespan ... (giữ nguyên các step còn lại)
```

Lưu ý:
- `Settings.app_namespace` field hiện tại default `"medinet_prod"` — KHÔNG xoá field (backward compat: nếu deployer set env APP_NAMESPACE manually thì pydantic load vào Settings.app_namespace, nhưng setup_cocoindex GHI ĐÈ qua `os.environ["APP_NAMESPACE"]` ngay trước start_blocking → cocoindex SDK đọc giá trị mới khi parse env). Document trade-off trong docstring.
- Renumber step 1 → 2, 2 → 3, etc.? KHÔNG cần — chỉ comment header "0)" thêm vào, các step 1-5 giữ nguyên số.

KHÔNG sửa các phần khác của setup.py (`_to_asyncpg_dsn`, `get_cocoindex_app`, `stop_cocoindex`, `@coco.lifespan _lifespan`).

KHÔNG sửa Settings.app_namespace default value ở config.py — backward compat M2 deploy đơn giữ `medinet_prod`. Plan 04 chỉ override RUNTIME qua setup_cocoindex.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run mypy app/rag/setup.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'os.environ\["APP_NAMESPACE"\]' Hub_All/api/app/rag/setup.py` ≥ 1
    - `grep -c 'f"medinet_{' Hub_All/api/app/rag/setup.py` ≥ 1 (f-string với hub_name → namespaced)
    - `grep -c "settings.hub_name" Hub_All/api/app/rag/setup.py` ≥ 2 (sử dụng + log)
    - `grep -c "namespaced = " Hub_All/api/app/rag/setup.py` ≥ 1
    - `grep -c "cocoindex_app_namespace_set" Hub_All/api/app/rag/setup.py` ≥ 1 (structured log event)
    - `grep -c "TOPO-03" Hub_All/api/app/rag/setup.py` ≥ 1 (REQ ID reference cho traceability)
    - `cd Hub_All/api && uv run mypy app/rag/setup.py` exit 0
    - `cd Hub_All/api && uv run ruff check app/rag/setup.py` exit 0
    - `grep -c "def setup_cocoindex" Hub_All/api/app/rag/setup.py` ≥ 1 (function signature intact)
    - `grep -c "coco.start_blocking" Hub_All/api/app/rag/setup.py` ≥ 1 (step 4 intact, APP_NAMESPACE đặt TRƯỚC)
  </acceptance_criteria>
  <done>
setup.py set APP_NAMESPACE per-hub trước start_blocking; structured log event "cocoindex_app_namespace_set"; mypy + ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process startup → cocoindex SDK | APP_NAMESPACE set qua os.environ — cocoindex SDK đọc lazy ở start_blocking() |
| flow.py import → Settings load | lru_cache singleton — 1 process = 1 hub_name = 1 App instance |
| Different hub processes → cùng Postgres cocoindex internal schema | namespace tách table prefix `medinet_<hub>_prod__` → no clash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | Caller set APP_NAMESPACE env trước setup_cocoindex (vd ops misconfig) | mitigate | setup_cocoindex GHI ĐÈ qua os.environ["APP_NAMESPACE"] — caller env bị override deterministic theo settings.hub_name. Log event "cocoindex_app_namespace_set" cho audit. |
| T-01-04-02 | Information Disclosure | App name (medinet_yte_ingest) leak ra error log nếu cocoindex SDK crash | accept | App name không phải PII / secret; hub identity là public info trong topology. |
| T-01-04-03 | Tampering | flow.py module-level `_get_settings()` cache stale nếu test thay env runtime | mitigate | Test fixture _minimal_env trong test_flow_per_hub_naming.py gọi `get_settings.cache_clear()` trước + sau. Production runtime: 1 process = 1 hub deploy → cache OK. |
| T-01-04-04 | Spoofing | Hub yte cố đăng ký App name "medinet_central_ingest" để consume central state | mitigate | resolve_cocoindex_app_name lấy hub_name từ Settings (đã validated qua Plan 02 model_validator khớp DATABASE_URL suffix). Hub yte process không thể đặt hub_name="central" mà không đổi DATABASE_URL → fail-fast startup. |
| T-01-04-05 | Information Disclosure | cocoindex internal state table `cocoindex.medinet_<hub>_prod__*` cùng DB → query cross-namespace có thể đọc | accept | Phase 1 v3.0 TOPO-03 chỉ scope process layer naming; cross-namespace SQL access cần Postgres-level role grant (defer Phase 7 production hardening). Hub con KHÔNG có quyền connect cocoindex internal DB từ outside — chỉ qua coco.App SDK. |
</threat_model>

<verification>
- Task 1 unit test PASS (resolve helper + module App name pattern)
- Task 2 grep verify APP_NAMESPACE env set + hub_name interpolation
- mypy + ruff PASS toàn bộ 2 file
- Integration smoke (recommend Plan 05 / Phase 2): set HUB_NAME=yte + run lifespan → log "cocoindex_app_registered: medinet_yte_ingest" + "cocoindex_app_namespace_set: hub=yte namespace=medinet_yte_prod"
</verification>

<success_criteria>
- `resolve_cocoindex_app_name(hub_name)` deterministic 4 giá trị + invalid raise
- Module-level `cocoindex_app.name` (hoặc `_name`) match pattern `medinet_<hub>_ingest`
- `setup_cocoindex` set `os.environ["APP_NAMESPACE"] = f"medinet_{hub_name}_prod"` TRƯỚC start_blocking
- Structured log event `cocoindex_app_namespace_set` xuất hiện trong setup runtime
- `cocoindex_db_schema="cocoindex"` field Settings KHÔNG đổi (P7 carry forward)
- mypy + ruff PASS 2 file
</success_criteria>

<output>
Tạo `.planning/phases/01-multi-db-topology/01-04-SUMMARY.md`:
- Per-hub app naming convention `medinet_<hub>_ingest` (4 giá trị enum)
- APP_NAMESPACE per-hub `medinet_<hub>_prod` (R5 scale)
- P7 carry forward: cocoindex_db_schema="cocoindex" cố định
- Module-level App instance build từ Settings.hub_name (lru_cache singleton 1-process-1-hub)
</output>
