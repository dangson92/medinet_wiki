---
phase: 06-search-api-single-cross-hub
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - Hub_All/api/app/services/search_cache.py
  - Hub_All/api/app/services/search_service.py
  - Hub_All/api/app/services/documents_service.py
  - Hub_All/api/app/routers/documents.py
  - Hub_All/api/app/main.py
autonomous: true
requirements: [SEARCH-04]
must_haves:
  truths:
    - "Document upload/delete trong hub publish Redis Pub/Sub channel hub:{hub_id}:invalidate"
    - "Subscriber asyncio task trong lifespan lắng nghe channel hub:*:invalidate và xoá cache key của hub đó"
    - "Search cache key namespaced theo hub để invalidation 1 hub không xoá hub khác"
    - "Sau khi document mới upload, search lần kế tiếp KHÔNG trả kết quả stale (cache miss)"
  artifacts:
    - path: "Hub_All/api/app/services/search_cache.py"
      provides: "cache key scheme hub-tagged + publish_invalidate + invalidate_hub + subscriber loop"
      contains: "async def search_cache_subscriber"
    - path: "Hub_All/api/app/services/documents_service.py"
      provides: "publish hub:{hub_id}:invalidate sau create + delete"
      contains: "hub_invalidate"
  key_links:
    - from: "app/services/documents_service.py"
      to: "Redis Pub/Sub channel hub:{hub_id}:invalidate"
      via: "redis.publish"
      pattern: "publish.*invalidate"
    - from: "app/main.py lifespan"
      to: "app/services/search_cache.py search_cache_subscriber"
      via: "asyncio.create_task"
      pattern: "search_cache_subscriber"
---

<objective>
Wire cache invalidation cho search (SEARCH-04, D-12): khi document upload/delete trong 1 hub → publish Redis Pub/Sub channel `hub:{hub_id}:invalidate`; một subscriber asyncio task trong lifespan lắng nghe và xoá cache key của hub đó. Đảm bảo upload document mới → search lần kế tiếp không trả kết quả cũ.

**Quyết định scope có chủ đích (D-12 vs codebase M2):** D-12 liệt kê upload/edit/delete; codebase M2 `documents_service.py` CHỈ có `create()` + `delete()` — KHÔNG có content-edit/reupload path (xác nhận PATTERNS.md §documents_service). Wire publish vào 2 path tồn tại; edit-invalidation sẽ tự đủ khi edit path được thêm ở phase tương lai (chỉ cần thêm 1 dòng `publish_invalidate` vào path đó — `publish_invalidate` đã là helper sẵn). Đây là gap được nhận diện và chấp nhận có chủ đích, KHÔNG phải thiếu sót.

Purpose: Hoàn tất SEARCH-04 (Redis cache có TTL từ 06-01 + invalidation theo event). Không có invalidation, cache 300s sẽ trả stale 5 phút sau mỗi upload — fail Success Criteria #4 ROADMAP.
Output: `app/services/search_cache.py` (mới — cache key scheme + publish + subscriber), refactor cache key trong `search_service.py` sang hub-tagged scheme, wire publish vào `documents_service.py` (create + delete) + `documents.py` router (truyền redis), wire subscriber task vào `main.py` lifespan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/06-search-api-single-cross-hub/06-CONTEXT.md
@Hub_All/.planning/phases/06-search-api-single-cross-hub/06-PATTERNS.md
@Hub_All/api/app/services/search_service.py

<interfaces>
<!-- Contracts từ codebase. -->

Từ app/services/search_service.py (06-01/06-02):
```python
def _cache_key(query, hub_ids, top_k, min_score) -> str       # prefix "search:"
def _cross_cache_key(query, hub_ids, top_k, min_score) -> str  # prefix "search:cross:"
# SearchService.search() + search_cross_hub() đọc/ghi 2 key này.
```

Từ app/services/documents_service.py:
```python
class DocumentService:
    def __init__(self, db: AsyncSession, file_store: FileStore | None = None) -> None: ...
    async def create(self, *, hub_id: UUID, uploaded_by, file_content, original_filename, mime_type=None) -> DocumentResponse:
        # ... INSERT documents ... await self.db.commit()  (dòng 219) ... return DocumentResponse
    async def delete(self, document_id: UUID, *, actor: User, actor_hub_ids: Sequence[str], request_id=None) -> bool:
        # ... DELETE FROM documents (dòng 383) ... INSERT audit_logs ... return True
        # hub_id resource có sẵn biến local `hub_id` (dòng 352)
# LƯU Ý: M2 documents_service.py CHỈ có create() + delete() — KHÔNG có edit/reupload method.
```

Từ app/routers/documents.py:
```python
def get_documents_service(db: AsyncSession = Depends(get_session)) -> DocumentService:
    return DocumentService(db=db)   # dòng 71 — KHÔNG truyền redis hiện tại
```

Redis client: `request.app.state.redis` (redis.asyncio, decode_responses=True, có thể None).
Lifespan asyncio task pattern (main.py:214-222 audit task, shutdown 241-255):
```python
app.state.audit_task = asyncio.create_task(audit_flush_loop())
# shutdown: task.cancel(); await task (except CancelledError pass)
```
Lifespan có biến `app.state.redis` sẵn sau step 2 (main.py:84-89).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo app/services/search_cache.py — hub-tagged cache scheme + publish + subscriber + refactor search_service</name>
  <read_first>
    - Hub_All/api/app/services/search_service.py (06-01/06-02 — _cache_key/_cross_cache_key + search/search_cross_hub đọc ghi cache)
    - Hub_All/api/app/services/audit_service.py (analog asyncio loop pattern — audit_flush_loop)
    - Hub_All/.planning/phases/06-search-api-single-cross-hub/06-CONTEXT.md §D-11 §D-12
    - Hub_All/.planning/phases/06-search-api-single-cross-hub/06-PATTERNS.md §No Analog Found (Pub/Sub subscriber)
  </read_first>
  <files>Hub_All/api/app/services/search_cache.py, Hub_All/api/app/services/search_service.py</files>
  <action>
**Cache key scheme — hub-tagged set (D-12).** Vấn đề: cache key sha256 không chứa hub_id nên không xoá theo hub được. Giải pháp: với MỖI cache key search, ngoài SET value còn thêm key đó vào 1 Redis SET `search:hubtag:{hub_id}` cho mỗi hub trong query. Khi invalidate hub X → đọc SET `search:hubtag:{X}`, `DEL` mọi member + `DEL` chính SET đó.

Tạo MỚI `Hub_All/api/app/services/search_cache.py`:

Imports: `from __future__ import annotations`, `import asyncio`, `import logging`, `from typing import Any`. `logger = logging.getLogger(__name__)`.

Constants:
```python
INVALIDATE_CHANNEL_PATTERN = "hub:*:invalidate"
HUBTAG_PREFIX = "search:hubtag:"
HUBTAG_TTL = 600  # SET tự hết hạn nếu subscriber miss — phòng rò key
```

Helper `def invalidate_channel(hub_id: str) -> str` → trả `f"hub:{hub_id}:invalidate"`.

Async `async def tag_cache_key(redis: Any, cache_key: str, hub_ids: list[str]) -> None` — gọi sau khi search ghi cache:
```python
if redis is None:
    return
try:
    for hub_id in hub_ids:
        tag = f"{HUBTAG_PREFIX}{hub_id}"
        await redis.sadd(tag, cache_key)
        await redis.expire(tag, HUBTAG_TTL)
except Exception as e:  # noqa: BLE001 — best-effort, KHÔNG raise
    logger.warning("search_cache_tag_failed: %s", e)
```

Async `async def invalidate_hub(redis: Any, hub_id: str) -> int` — xoá mọi cache key gắn hub:
```python
if redis is None:
    return 0
try:
    tag = f"{HUBTAG_PREFIX}{hub_id}"
    members = await redis.smembers(tag)
    deleted = 0
    if members:
        deleted = await redis.delete(*members)
    await redis.delete(tag)
    logger.info("search_cache_invalidated: hub=%s keys=%d", hub_id, deleted)
    return int(deleted)
except Exception as e:  # noqa: BLE001
    logger.warning("search_cache_invalidate_failed: hub=%s err=%s", hub_id, e)
    return 0
```

Async `async def publish_invalidate(redis: Any, hub_id: str) -> None` — best-effort publish:
```python
if redis is None:
    return
try:
    await redis.publish(invalidate_channel(hub_id), "1")
    logger.info("hub_invalidate_published: hub=%s", hub_id)
except Exception as e:  # noqa: BLE001 — best-effort, KHÔNG raise (precedent fail-open)
    logger.warning("hub_invalidate_publish_failed: hub=%s err=%s", hub_id, e)
```

Async `async def search_cache_subscriber(redis: Any) -> None` — subscriber loop chạy trong lifespan task:
```python
if redis is None:
    logger.warning("search_cache_subscriber: redis None — subscriber không khởi động")
    return
pubsub = redis.pubsub()
try:
    await pubsub.psubscribe(INVALIDATE_CHANNEL_PATTERN)
    logger.info("search_cache_subscriber_started: pattern=%s", INVALIDATE_CHANNEL_PATTERN)
    async for message in pubsub.listen():
        if message is None or message.get("type") != "pmessage":
            continue
        channel = message.get("channel", "")
        # channel dạng "hub:{hub_id}:invalidate" — tách hub_id (phần giữa).
        parts = channel.split(":")
        if len(parts) == 3 and parts[0] == "hub" and parts[2] == "invalidate":
            await invalidate_hub(redis, parts[1])
except asyncio.CancelledError:
    logger.info("search_cache_subscriber_cancelled")
    raise
except Exception as e:  # noqa: BLE001
    logger.warning("search_cache_subscriber_error: %s", e)
finally:
    try:
        await pubsub.aclose()
    except Exception:  # noqa: BLE001
        pass
```
LƯU Ý `decode_responses=True` (main.py:85) → `message["channel"]` là `str`, KHÔNG bytes — split trực tiếp OK.

**Refactor `search_service.py` — chèn `tag_cache_key` vào CẢ HAI method.** Import `from app.services.search_cache import tag_cache_key` ở đầu file. Phải thêm `await tag_cache_key(self.redis, cache_key, hub_ids)` vào:

  (a) `search()` — NGAY SAU lệnh `await self.redis.set(cache_key, ...)` ghi cache single-hub, BÊN TRONG cùng block try/except fail-open đang bọc lệnh `redis.set` đó. Dùng `hub_ids` đã intersect (cùng list dùng để build `cache_key`).

  (b) `search_cross_hub()` — NGAY SAU lệnh `await self.redis.set(cross_cache_key, ...)` ghi cache cross-hub, BÊN TRONG cùng block try/except fail-open đang bọc lệnh `redis.set` đó. Dùng đúng `hub_ids` đã intersect của cross-hub query và đúng biến `cross_cache_key`.

  FALLBACK: nếu cache-write của method nào CHƯA có try/except riêng (06-01/06-02 không bọc), thì bọc thêm 1 try/except fail-open quanh CẢ `redis.set` LẪN `tag_cache_key` của method đó — `except Exception` chỉ `logger.warning`, KHÔNG raise. Mục tiêu: `tag_cache_key` luôn nằm trong cùng phạm vi fail-open với `redis.set` tương ứng, ở cả hai method.

KHÔNG đổi logic search khác (embed, vector query, intersect, response shape).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/search_cache.py app/services/search_service.py && uv run mypy --strict app/services/search_cache.py app/services/search_service.py && uv run python -c "from app.services.search_cache import search_cache_subscriber, publish_invalidate, invalidate_hub, tag_cache_key, invalidate_channel; assert invalidate_channel('abc')=='hub:abc:invalidate'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async def search_cache_subscriber" Hub_All/api/app/services/search_cache.py` match
    - `grep "async def publish_invalidate" Hub_All/api/app/services/search_cache.py` match
    - `grep "async def invalidate_hub" Hub_All/api/app/services/search_cache.py` match
    - `grep "psubscribe" Hub_All/api/app/services/search_cache.py` match
    - `grep -c "tag_cache_key" Hub_All/api/app/services/search_service.py` trả >= 3 (1 import + 1 call trong search() + 1 call trong search_cross_hub())
    - `uv run python -c "from app.services.search_cache import invalidate_channel; assert invalidate_channel('h1')=='hub:h1:invalidate'"` exit 0
    - ruff + mypy --strict exit 0
  </acceptance_criteria>
  <done>search_cache.py có hub-tagged scheme + publish + invalidate + subscriber loop; search_service ghi cache có tag_cache_key TRONG cả search() và search_cross_hub(), mỗi call nằm trong block fail-open của method đó. ruff + mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire publish_invalidate vào documents_service create+delete + subscriber task vào lifespan</name>
  <read_first>
    - Hub_All/api/app/services/documents_service.py dòng 100-110 (__init__), 214-226 (create commit), 382-411 (delete)
    - Hub_All/api/app/routers/documents.py dòng 68-72 (get_documents_service DI factory)
    - Hub_All/api/app/main.py dòng 214-222 (audit task pattern), 239-255 (shutdown cancel pattern)
    - Hub_All/api/app/services/search_cache.py (vừa tạo Task 1 — publish_invalidate, search_cache_subscriber)
  </read_first>
  <files>Hub_All/api/app/services/documents_service.py, Hub_All/api/app/routers/documents.py, Hub_All/api/app/main.py</files>
  <action>
**Phạm vi wiring có chủ đích (D-12):** D-12 liệt kê upload/edit/delete; codebase M2 `documents_service.py` CHỈ có `create()` + `delete()` — KHÔNG có content-edit/reupload method (xác nhận PATTERNS.md §documents_service). Task này wire `publish_invalidate` vào ĐÚNG 2 path tồn tại (`create` + `delete`). Edit-invalidation KHÔNG bị bỏ sót: khi edit/reupload path được thêm ở phase tương lai, chỉ cần thêm 1 dòng `await publish_invalidate(self.redis, str(hub_id))` vào path đó — `publish_invalidate` đã là helper dùng chung. Đây là gap được nhận diện và chấp nhận có chủ đích.

**A. `documents_service.py` — thêm redis param + publish.**

`DocumentService.__init__`: thêm param `redis: Any = None` (import `from typing import Any` nếu chưa có — file đã có). Lưu `self.redis = redis`.

Trong `create()`: NGAY SAU `await self.db.commit()` (dòng 219) + logger.info document_created, thêm publish:
```python
from app.services.search_cache import publish_invalidate
await publish_invalidate(self.redis, str(hub_id))
```
(`publish_invalidate` đã best-effort fail-open — KHÔNG raise nếu redis None/down.)

Trong `delete()`: SAU bước 4 (INSERT audit_logs dòng 403) + TRƯỚC bước 5 (xoá file), thêm publish dùng biến local `hub_id` đã có (dòng 352):
```python
from app.services.search_cache import publish_invalidate
if hub_id is not None:
    await publish_invalidate(self.redis, str(hub_id))
```
Đặt import `publish_invalidate` ở đầu file (import block) thay vì inline — gọn hơn; HOẶC inline trong method nếu lo circular import. KHÔNG có circular (search_cache chỉ import asyncio/logging/typing) → đặt ở đầu file.

**B. `documents.py` router — truyền redis vào DI factory.**

`get_documents_service` hiện chỉ nhận `db`. Đổi để đọc redis từ `request.app.state`:
```python
def get_documents_service(
    request: Request,
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> DocumentService:
    redis = getattr(request.app.state, "redis", None)
    return DocumentService(db=db, redis=redis)
```
Đảm bảo `Request` đã import từ `fastapi` trong documents.py (kiểm tra import block — thêm nếu thiếu).

**C. `main.py` lifespan — subscriber asyncio task.**

Trong lifespan, SAU step 8 audit task (dòng ~222), thêm step 9:
```python
# 9) Search cache invalidation subscriber (Phase 6 SEARCH-04 — D-12).
#    Lắng nghe Redis Pub/Sub hub:*:invalidate → xoá cache key của hub đó.
app.state.search_cache_task = None
if app.state.redis is not None:
    try:
        from app.services.search_cache import search_cache_subscriber

        app.state.search_cache_task = asyncio.create_task(
            search_cache_subscriber(app.state.redis)
        )
        logger.info("search_cache_subscriber_task_started")
    except Exception as e:  # noqa: BLE001
        logger.warning("search_cache_subscriber_start_failed: %s", e)
```

Trong shutdown `finally` block, SAU audit task cancel (dòng ~255) + TRƯỚC `dispose_engine`, thêm cancel cho search_cache_task:
```python
# Search cache subscriber task (Phase 6 SEARCH-04) — cancel TRƯỚC redis aclose.
if getattr(app.state, "search_cache_task", None) is not None:
    app.state.search_cache_task.cancel()
    try:
        await app.state.search_cache_task
    except asyncio.CancelledError:
        pass
    except Exception as e:  # noqa: BLE001
        logger.warning("search_cache_task_stop_failed: %s", e)
    app.state.search_cache_task = None
```
LƯU Ý: subscriber task phải cancel TRƯỚC `app.state.redis.aclose()` (dòng ~277-281) — subscriber dùng redis connection. Đặt block cancel này TRƯỚC dispose_engine là an toàn vì redis aclose ở cuối.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/documents_service.py app/routers/documents.py app/main.py && uv run mypy --strict app/services/documents_service.py app/routers/documents.py app/main.py && uv run python -c "from app.main import create_app; app=create_app(); print('create_app OK with search cache wiring')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "publish_invalidate" Hub_All/api/app/services/documents_service.py` match (xuất hiện ở create + delete — ít nhất 2 lần ngoài import, hoặc 1 import + 2 call)
    - `grep "redis" Hub_All/api/app/services/documents_service.py` match trong __init__ (self.redis)
    - `grep "DocumentService(db=db, redis=redis)" Hub_All/api/app/routers/documents.py` match
    - `grep "search_cache_subscriber" Hub_All/api/app/main.py` match
    - `grep "search_cache_task" Hub_All/api/app/main.py` match (start + cancel)
    - `uv run python -c "from app.main import create_app; create_app()"` exit 0
    - ruff + mypy --strict exit 0
  </acceptance_criteria>
  <done>documents create+delete publish hub:{hub_id}:invalidate (edit/reupload path không tồn tại ở M2 — wiring sẽ thêm khi path đó ra đời); documents router truyền redis; lifespan chạy + cancel subscriber task. ruff + mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documents_service → Redis Pub/Sub | publish message với hub_id (từ DB, trusted) |
| Redis Pub/Sub channel → subscriber | channel name parse để lấy hub_id |
| subscriber → Redis cache | DEL cache key của hub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | subscriber channel parse | mitigate | Subscriber chỉ `psubscribe("hub:*:invalidate")` + validate `len(parts)==3 and parts[0]=="hub" and parts[2]=="invalidate"` trước khi invalidate — channel rác bị bỏ qua |
| T-06-03-02 | Information Disclosure | hub-tagged cache invalidation | mitigate | Cache key gắn SET `search:hubtag:{hub_id}` — invalidate hub X CHỈ DEL key của X, KHÔNG đụng cache hub khác (không cross-hub cache poisoning) |
| T-06-03-03 | Denial of Service | publish_invalidate trong upload/delete path | mitigate | `publish_invalidate` best-effort fail-open — Redis down KHÔNG raise, upload/delete vẫn thành công (precedent documents_service.py:407-410) |
| T-06-03-04 | Denial of Service | subscriber loop crash | accept | Subscriber bọc try/except — lỗi không crash lifespan; nếu loop chết, cache TTL 300s + HUBTAG_TTL 600s vẫn tự hết hạn (stale tối đa 5 phút). Chấp nhận M2 |
| T-06-03-05 | Repudiation | cache invalidation event | accept | Invalidation log qua `logger.info` (không audit_logs). Chấp nhận — cache là tối ưu hiệu năng, không phải security control cần forensic trail |
</threat_model>

<verification>
- `uv run ruff check app/services/search_cache.py app/services/search_service.py app/services/documents_service.py app/routers/documents.py app/main.py` exit 0
- `uv run mypy --strict` trên 5 file đó exit 0
- `create_app()` chạy không lỗi
- Wire điểm: `publish_invalidate` trong create + delete; `tag_cache_key` trong cả search() và search_cross_hub(); `search_cache_subscriber` task start + cancel trong lifespan
- Integration verify (cache invalidation thực sự) — plan 06-04 test
</verification>

<success_criteria>
- Document upload/delete publish channel `hub:{hub_id}:invalidate` (edit/reupload path không có ở M2 — gap chấp nhận có chủ đích, D-12)
- Subscriber asyncio task lắng nghe `hub:*:invalidate` → `invalidate_hub` xoá cache key
- Cache key hub-tagged — invalidate 1 hub không xoá hub khác
- `tag_cache_key` chèn vào cả search() và search_cross_hub() trong cùng block fail-open với cache-write
- Subscriber task cancel sạch ở shutdown TRƯỚC redis aclose
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-search-api-single-cross-hub/06-03-SUMMARY.md` theo template.
</output>
</content>
</invoke>
