---
phase: 06-search-api-single-cross-hub
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - Hub_All/api/app/services/search_service.py
  - Hub_All/api/app/routers/search.py
  - Hub_All/api/app/routers/__init__.py
  - Hub_All/api/app/main.py
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03]
must_haves:
  truths:
    - "POST /api/search trả top-k chunks union các hub cho user, envelope {success,data,error,meta}"
    - "POST /api/search/cross-hub query song song mỗi hub qua asyncio.gather rồi aggregate + re-rank theo score"
    - "POST /api/search/similar nhận {content,hub_id?,threshold?} trả SimilarResponse"
    - "Cả 3 endpoint decorate @limiter.limit(SEARCH_LIMIT) 100/min/user"
    - "search router mounted vào create_app() — 3 endpoint reachable"
    - "Hub user không được assign bị loại ở service trước SQL (defense in depth)"
  artifacts:
    - path: "Hub_All/api/app/routers/search.py"
      provides: "3 POST endpoint /api/search, /api/search/cross-hub, /api/search/similar"
      contains: "router = APIRouter(prefix=\"/api/search\""
    - path: "Hub_All/api/app/services/search_service.py"
      provides: "search_cross_hub() fan-out + find_similar()"
      contains: "async def search_cross_hub"
  key_links:
    - from: "app/main.py create_app()"
      to: "app/routers/search.py"
      via: "app.include_router(search_router)"
      pattern: "search_router"
    - from: "app/routers/search.py"
      to: "app/services/search_service.py"
      via: "SearchService(pool=..., redis=...)"
      pattern: "SearchService"
---

<objective>
Hoàn thiện search endpoint layer: thêm `search_cross_hub()` (fan-out asyncio.gather + re-rank — SEARCH-03) và `find_similar()` vào SearchService, tạo `app/routers/search.py` với 3 POST endpoint khớp `api.ts`, mount router vào `create_app()`.

Purpose: Plan này đưa search API thành endpoint reachable. Sau plan, 3 endpoint frontend gọi (`search`, `crossHubSearch`, `findSimilar`) hoạt động. Cache invalidation Pub/Sub là 06-03.
Output: `app/services/search_service.py` (thêm 2 method), `app/routers/search.py` (mới), `app/routers/__init__.py` + `app/main.py` (mount).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/06-search-api-single-cross-hub/06-CONTEXT.md
@Hub_All/.planning/phases/06-search-api-single-cross-hub/06-PATTERNS.md
@Hub_All/api/app/services/search_service.py
@Hub_All/api/app/schemas/search.py

<interfaces>
<!-- Contracts từ codebase + plan 06-01. -->

Từ app/services/search_service.py (06-01 đã tạo):
```python
DEFAULT_TOP_K = 10; MAX_TOP_K = 50; SNIPPET_MAX_CHARS = 300; CACHE_TTL_SECONDS = 300
class SearchService:
    def __init__(self, *, pool: Any, redis: Any = None) -> None: ...
    async def search(self, *, body: SearchRequest, user: UserWithHubs) -> dict[str, Any]: ...
    # _run_vector_query trả TUPLE (rows, hubs_queried_count) — phần tử [1] là số hub thực sự query.
    async def _run_vector_query(self, *, query_vector, hub_ids, top_k, all_hubs) -> tuple[list[Any], int]: ...
def intersect_hubs(requested, user_hub_ids, role) -> list[str]: ...
def _to_pgvector_literal(vec) -> str: ...
def _truncate_snippet(content, limit=300) -> str: ...
def _cache_key(query, hub_ids, top_k, min_score) -> str: ...
def _resolve_top_k(raw) -> int: ...
def _row_to_item(row) -> SearchResultItem: ...
```

Router pattern (analog app/routers/audit_logs.py — slowapi decoration):
```python
@router.post("")
@limiter.limit(SEARCH_LIMIT)               # decorator SÁT def — slowapi yêu cầu
async def search_endpoint(
    request: Request,                       # BẮT BUỘC param request: Request (KHÔNG Depends)
    body: SearchRequest,
    user: UserWithHubs = Depends(get_current_user_with_hubs),  # noqa: B008
    service: SearchService = Depends(get_search_service),       # noqa: B008
) -> JSONResponse: ...
```

asyncpg pool: `request.app.state.db_pool`. Redis: `request.app.state.redis` (có thể None).
SEARCH_LIMIT đã export ở `app/middleware/__init__.py` = `"{rate_limit_search_per_minute}/minute"` (mặc định 100/min).
`get_current_user_with_hubs` (app/auth/dependencies.py) trả `UserWithHubs(user, hub_ids)` — `user.role` str, `hub_ids` list[str] từ DB user_hubs.

resp envelope helpers (app/pkg/response.py):
```python
resp.ok(data) -> 200      resp.bad_request(message, code="BAD_REQUEST") -> 400
resp.internal_error(message, code="INTERNAL_ERROR") -> 500
```
HubIsolationError handler + RateLimitExceeded handler đã wire main.py — KHÔNG cần thêm.

create_app() mount pattern (main.py:328-349): `from app.routers import documents_router` rồi `app.include_router(documents_router)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm search_cross_hub() + find_similar() vào SearchService</name>
  <read_first>
    - Hub_All/api/app/services/search_service.py (06-01 — search() + helper dùng lại; _run_vector_query trả tuple)
    - Hub_All/api/app/services/documents_service.py dòng 33 (asyncio import precedent)
    - Hub_All/.planning/phases/06-search-api-single-cross-hub/06-CONTEXT.md §D-06 §D-04 §D-11
    - Hub_All/.planning/phases/06-search-api-single-cross-hub/06-PATTERNS.md §Cross-hub fan-out
  </read_first>
  <files>Hub_All/api/app/services/search_service.py</files>
  <action>
EXTEND `app/services/search_service.py` (KHÔNG ghi đè — append method vào class `SearchService` + bổ sung import). Thêm `import asyncio` vào import block. Bổ sung import schema: `from app.schemas.search import SearchRequest, SearchResponse, SearchResultItem, SimilarMatch, SimilarRequest, SimilarResponse` (mở rộng dòng import hiện có).

LƯU Ý CONTRACT `_run_vector_query`: method này (06-01) trả TUPLE `(rows, hubs_queried_count)`. Mọi call site trong plan này PHẢI unpack tuple — lấy `rows` (phần tử [0]) để map item, KHÔNG dùng trực tiếp giá trị trả về như list.

Thêm helper module-level `_cross_cache_key(query: str, hub_ids: list[str], top_k: int, min_score: float | None) -> str` — trả `"search:cross:" + hashlib.sha256(f"{query}|{sorted(hub_ids)}|{top_k}|{min_score}".encode()).hexdigest()` (tách namespace cache cross-hub khỏi single-hub để invalidation 06-03 xử lý chung prefix `search:`).

Thêm method `async def search_cross_hub(self, *, body: SearchRequest, user: "UserWithHubs") -> dict[str, Any]` (SEARCH-03, D-06):
  1. `t0 = time.perf_counter()`; `top_k = _resolve_top_k(body.top_k)`.
  2. `hub_ids = intersect_hubs(body.hub_ids, user.hub_ids, user.user.role)`.
  3. admin + không filter: nếu `user.user.role == "admin" and not body.hub_ids`: cross-hub cần danh sách hub cụ thể để fan-out — query tất cả hub active. PHẢI dùng predicate `is_active = TRUE` — NHẤT QUÁN với branch admin-all của `_run_vector_query` trong 06-01 (cũng đếm `count(*) FROM hubs WHERE is_active = TRUE` và filter SQL `h.is_active = TRUE`). TUYỆT ĐỐI KHÔNG dùng `status = 'active'`:
     ```python
     async with self.pool.acquire() as conn:
         rows = await conn.fetch("SELECT id FROM hubs WHERE is_active = TRUE")
     hub_ids = [str(r["id"]) for r in rows]
     ```
  4. Nếu `hub_ids` rỗng → trả `SearchResponse(results=[], total_hubs_searched=0, query_time_ms=int((time.perf_counter()-t0)*1000), cache_hit=False).model_dump(mode="json")` sớm.
  5. Cache đọc (D-11 — cache cross-hub TOO): `cache_key = _cross_cache_key(body.query, hub_ids, top_k, body.min_score)`. Nếu `self.redis is not None`: try `cached = await self.redis.get(cache_key)`; nếu cached → `result = json.loads(cached); result["cache_hit"] = True; return result`. Bọc try/except — fail-open, `logger.warning`.
  6. Embed query 1 lần: `try: query_vector = await embed_text(body.query)` — `except ValueError`/`except EmbedderError` re-raise (router map).
  7. Fan-out per-hub qua `asyncio.gather`. `_run_vector_query` trả tuple `(rows, count)` — `_search_one` CHỈ lấy phần tử [0] (`rows`):
     ```python
     async def _search_one(hub_id: str) -> list[Any]:
         rows, _ = await self._run_vector_query(
             query_vector=query_vector, hub_ids=[hub_id], top_k=top_k, all_hubs=False,
         )
         return rows
     per_hub = await asyncio.gather(*[_search_one(h) for h in hub_ids])
     ```
  8. Aggregate + re-rank: `all_rows = [r for rows in per_hub for r in rows]`; `all_rows.sort(key=lambda r: float(r["score"]), reverse=True)`.
  9. `items = [_row_to_item(r) for r in all_rows[:top_k]]`; min_score filter: nếu `body.min_score is not None` → `items = [it for it in items if it.score >= body.min_score]`.
  10. `result = SearchResponse(results=items, total_hubs_searched=len(hub_ids), query_time_ms=int((time.perf_counter()-t0)*1000), cache_hit=False).model_dump(mode="json")`. (`len(hub_ids)` = số hub fan-out thực sự; với admin-all `hub_ids` đã = danh sách hub `is_active = TRUE` ở bước 3, NHẤT QUÁN predicate 06-01.)
  11. Cache ghi: nếu `self.redis is not None`: try `await self.redis.set(cache_key, json.dumps(result), ex=CACHE_TTL_SECONDS)`; try/except fail-open.
  12. `logger.info("cross_hub_search_completed", extra={"hub_count": len(hub_ids), "result_count": len(items)})`. `return result`.

Thêm method `async def find_similar(self, *, body: SimilarRequest, user: "UserWithHubs") -> dict[str, Any]` (D-04 — không có REQ-ID, KHÔNG cache D-11):
  1. `top_k = DEFAULT_TOP_K` (frontend không gửi top_k cho similar).
  2. hub scope: `requested = [body.hub_id] if body.hub_id else None`; `hub_ids = intersect_hubs(requested, user.hub_ids, user.user.role)`.
  3. `is_admin_all = user.user.role == "admin" and not body.hub_id`.
  4. Nếu KHÔNG `is_admin_all` và `hub_ids` rỗng → trả `SimilarResponse(matches=[]).model_dump(mode="json")`.
  5. Embed `body.content`: `try: query_vector = await embed_text(body.content)` — `except ValueError`/`EmbedderError` re-raise.
  6. `_run_vector_query` trả tuple — unpack lấy `rows`: `rows, _ = await self._run_vector_query(query_vector=query_vector, hub_ids=hub_ids, top_k=top_k, all_hubs=is_admin_all)`.
  7. threshold filter: `threshold = body.threshold if body.threshold is not None else 0.0`; lọc `rows` giữ row có `float(r["score"]) >= threshold`.
  8. Map → `SimilarMatch`: `page_id=str(r["document_id"])`, `page_title=r["filename"]`, `similarity_score=float(r["score"])`, `hub_name=r["hub_name"]`.
  9. `return SimilarResponse(matches=matches).model_dump(mode="json")`.

KHÔNG cache `/similar` (D-11). KHÔNG mount router (Task 2).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/search_service.py && uv run mypy --strict app/services/search_service.py && uv run python -c "from app.services.search_service import SearchService; assert hasattr(SearchService,'search_cross_hub') and hasattr(SearchService,'find_similar')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async def search_cross_hub" Hub_All/api/app/services/search_service.py` match
    - `grep "async def find_similar" Hub_All/api/app/services/search_service.py` match
    - `grep "asyncio.gather" Hub_All/api/app/services/search_service.py` match
    - `grep "reverse=True" Hub_All/api/app/services/search_service.py` match (re-rank theo score desc)
    - `grep "SELECT id FROM hubs WHERE is_active = TRUE" Hub_All/api/app/services/search_service.py` match (admin-all fan-out — predicate NHẤT QUÁN 06-01)
    - `grep -c "status = 'active'" Hub_All/api/app/services/search_service.py` trả 0 (KHÔNG dùng predicate status — chỉ is_active)
    - `grep "SimilarResponse" Hub_All/api/app/services/search_service.py` match
    - ruff + mypy --strict exit 0
  </acceptance_criteria>
  <done>SearchService có search_cross_hub() fan-out asyncio.gather + re-rank và find_similar(); cả hai unpack tuple `(rows, _)` từ `_run_vector_query`; admin-all branch dùng predicate `is_active = TRUE` nhất quán 06-01; ruff + mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo app/routers/search.py — 3 POST endpoint + mount vào create_app()</name>
  <read_first>
    - Hub_All/api/app/routers/audit_logs.py (analog — slowapi @limiter.limit decoration + envelope + DI factory)
    - Hub_All/api/app/routers/documents.py dòng 68-72 (get_*_service DI factory pattern)
    - Hub_All/api/app/routers/__init__.py (barrel export pattern)
    - Hub_All/api/app/main.py dòng 322-354 (create_app — router mount block)
    - Hub_All/api/app/services/search_service.py (SearchService — vừa hoàn thiện Task 1)
    - Hub_All/api/app/pkg/response.py (resp.ok, resp.bad_request, resp.internal_error)
  </read_first>
  <files>Hub_All/api/app/routers/search.py, Hub_All/api/app/routers/__init__.py, Hub_All/api/app/main.py</files>
  <action>
Tạo MỚI `Hub_All/api/app/routers/search.py`:

Imports: `from __future__ import annotations`, `import logging`, `from fastapi import APIRouter, Depends, Request`, `from fastapi.responses import JSONResponse`, `from app.auth.dependencies import UserWithHubs, get_current_user_with_hubs`, `from app.middleware import SEARCH_LIMIT, limiter`, `from app.pkg import response as resp`, `from app.schemas.search import SearchRequest, SimilarRequest`, `from app.services.embedder import EmbedderError`, `from app.services.search_service import SearchService`.

`logger = logging.getLogger(__name__)` · `router = APIRouter(prefix="/api/search", tags=["search"])`.

DI factory — service cần asyncpg pool + redis từ `request.app.state` (search dùng raw SQL, KHÔNG AsyncSession):
```python
def get_search_service(request: Request) -> SearchService:
    pool = getattr(request.app.state, "db_pool", None)
    redis = getattr(request.app.state, "redis", None)
    if pool is None:
        raise RuntimeError("db_pool chưa sẵn sàng — search không khả dụng")
    return SearchService(pool=pool, redis=redis)
```
LƯU Ý: `RuntimeError` được ErrorHandlerMiddleware bắt → 500 envelope (pool down = service unavailable thực sự — chấp nhận M2).

3 endpoint POST — mỗi cái `@router.post(...)` ở TRÊN, `@limiter.limit(SEARCH_LIMIT)` ở DƯỚI (sát def). Endpoint function PHẢI có param `request: Request` (KHÔNG Depends — slowapi đọc):

```python
@router.post("")
@limiter.limit(SEARCH_LIMIT)
async def search_endpoint(
    request: Request,
    body: SearchRequest,
    user: UserWithHubs = Depends(get_current_user_with_hubs),  # noqa: B008
    service: SearchService = Depends(get_search_service),       # noqa: B008
) -> JSONResponse:
    _ = request  # slowapi đọc; auth gate qua get_current_user_with_hubs.
    try:
        result = await service.search(body=body, user=user)
    except ValueError as e:
        return resp.bad_request(message=str(e), code="INVALID_QUERY")
    except EmbedderError as e:
        return resp.internal_error(message=f"Embedding lỗi: {e}", code="EMBEDDING_FAILED")
    return resp.ok(data=result)


@router.post("/cross-hub")
@limiter.limit(SEARCH_LIMIT)
async def cross_hub_search_endpoint(
    request: Request,
    body: SearchRequest,
    user: UserWithHubs = Depends(get_current_user_with_hubs),  # noqa: B008
    service: SearchService = Depends(get_search_service),       # noqa: B008
) -> JSONResponse:
    _ = request
    try:
        result = await service.search_cross_hub(body=body, user=user)
    except ValueError as e:
        return resp.bad_request(message=str(e), code="INVALID_QUERY")
    except EmbedderError as e:
        return resp.internal_error(message=f"Embedding lỗi: {e}", code="EMBEDDING_FAILED")
    return resp.ok(data=result)


@router.post("/similar")
@limiter.limit(SEARCH_LIMIT)
async def find_similar_endpoint(
    request: Request,
    body: SimilarRequest,
    user: UserWithHubs = Depends(get_current_user_with_hubs),  # noqa: B008
    service: SearchService = Depends(get_search_service),       # noqa: B008
) -> JSONResponse:
    _ = request
    try:
        result = await service.find_similar(body=body, user=user)
    except ValueError as e:
        return resp.bad_request(message=str(e), code="INVALID_QUERY")
    except EmbedderError as e:
        return resp.internal_error(message=f"Embedding lỗi: {e}", code="EMBEDDING_FAILED")
    return resp.ok(data=result)
```

Cập nhật `app/routers/__init__.py`: thêm `from app.routers.search import router as search_router` (giữ alphabet) và `"search_router"` vào `__all__`.

Cập nhật `app/main.py` `create_app()`: trong block mount router (sau `app.include_router(rag_config_router)` dòng ~349), thêm:
```python
# Mount search router (Phase 6 SEARCH-01..04 — 3 endpoint POST).
from app.routers import search_router
app.include_router(search_router)
```
KHÔNG sửa middleware order, KHÔNG thêm exception handler (HubIsolationError + RateLimitExceeded đã wire Phase 5).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/routers/search.py app/routers/__init__.py app/main.py && uv run mypy --strict app/routers/search.py && uv run python -c "from app.main import create_app; app=create_app(); paths={r.path for r in app.routes}; assert '/api/search' in paths and '/api/search/cross-hub' in paths and '/api/search/similar' in paths, paths; print('3 search endpoint mounted OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'prefix="/api/search"' Hub_All/api/app/routers/search.py` match
    - `grep -c "@limiter.limit(SEARCH_LIMIT)" Hub_All/api/app/routers/search.py` trả 3
    - `grep -c "request: Request" Hub_All/api/app/routers/search.py` trả ít nhất 4 (3 endpoint + 1 DI factory)
    - `grep "search_router" Hub_All/api/app/routers/__init__.py` match (import + __all__)
    - `grep "search_router" Hub_All/api/app/main.py` match
    - `uv run python -c "from app.main import create_app; app=create_app(); p={r.path for r in app.routes}; assert {'/api/search','/api/search/cross-hub','/api/search/similar'} <= p"` exit 0
    - ruff + mypy --strict exit 0
  </acceptance_criteria>
  <done>3 endpoint POST search reachable qua create_app(), decorate @limiter.limit, envelope response; ruff + mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → search router | JWT bắt buộc (viewer+); request body `hub_ids` untrusted |
| router → SearchService | UserWithHubs.hub_ids từ DB (trusted); body từ client (untrusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Spoofing | 3 search endpoint | mitigate | `get_current_user_with_hubs` verify Bearer JWT — anonymous → 401; hub_ids lấy từ DB user_hubs KHÔNG payload |
| T-06-02-02 | Information Disclosure | cross-hub search | mitigate | `search_cross_hub` fan-out CHỈ trên `hub_ids` đã intersect — viewer Hub A request `[A,B,C]` chỉ fan-out hub được assign |
| T-06-02-03 | Elevation of Privilege | admin all-hubs branch | mitigate | Branch `all_hubs` / `SELECT id FROM hubs WHERE is_active = TRUE` CHỈ kích hoạt khi `user.role == "admin"` — editor/viewer luôn đi qua intersect |
| T-06-02-04 | Denial of Service | 3 search endpoint | mitigate | `@limiter.limit(SEARCH_LIMIT)` 100/min/user trên cả 3 — vượt → 429 envelope (D-13) |
| T-06-02-05 | Denial of Service | cross-hub fan-out | accept | `asyncio.gather` N hub song song — N giới hạn bởi số hub user assign (thực tế nhỏ, ≤10). Chấp nhận M2; phân trang/giới hạn hub-count defer nếu hub registry phình to |
</threat_model>

<verification>
- `uv run ruff check app/services/search_service.py app/routers/search.py app/routers/__init__.py app/main.py` exit 0
- `uv run mypy --strict app/services/search_service.py app/routers/search.py` exit 0
- `create_app()` mount 3 path: `/api/search`, `/api/search/cross-hub`, `/api/search/similar`
- 3 endpoint đều có `@limiter.limit(SEARCH_LIMIT)` + param `request: Request`
- `search_cross_hub` admin-all branch dùng `is_active = TRUE` (KHÔNG `status = 'active'`) — nhất quán 06-01
</verification>

<success_criteria>
- POST `/api/search` union search, POST `/api/search/cross-hub` fan-out + re-rank, POST `/api/search/similar` find-similar — cả 3 reachable
- Cả 3 decorate `@limiter.limit(SEARCH_LIMIT)` (D-13)
- Response envelope `{success,data,error,meta}` mọi path
- ValueError → 400 INVALID_QUERY, EmbedderError → 500 EMBEDDING_FAILED
- `search_cross_hub` admin-all dùng predicate `is_active = TRUE` cho tập hub fan-out — NHẤT QUÁN với branch admin-all của `_run_vector_query` trong 06-01
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-search-api-single-cross-hub/06-02-SUMMARY.md` theo template.
</output>
