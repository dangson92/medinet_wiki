---
phase: 01-rbac-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/migrations/versions/0006_role_hub_admin.py
autonomous: true
requirements: [ROLE-01, ROLE-02, ROLE-03]
must_haves:
  truths:
    - "Sau `alembic upgrade head`, CHECK constraint trên `users.role` chấp nhận 4 giá trị: admin, hub_admin, editor, viewer"
    - "Sau `alembic upgrade head`, column `user_hubs.role` tồn tại, kiểu TEXT, nullable, default NULL"
    - "Existing rows `users` (role='admin') preserve nguyên semantic — KHÔNG bị reject CHECK constraint mới"
    - "Existing rows `user_hubs` preserve nguyên — column `role` mới = NULL (inherit global)"
    - "Sau migration apply, 1 row audit_logs tồn tại với action='migration.role_seed', payload chứa count + timestamp"
    - "Re-run `alembic upgrade head` lần 2 KHÔNG fail (idempotent — introspect detect already-applied state)"
    - "Sau `alembic downgrade -1`, CHECK constraint trở về 3 giá trị cũ + column `user_hubs.role` bị drop; existing data preserve"
  artifacts:
    - path: "api/migrations/versions/0006_role_hub_admin.py"
      provides: "Alembic migration revision 0006 — mở rộng CHECK constraint users.role + thêm user_hubs.role nullable column + seed audit log"
      contains: "def upgrade()"
    - path: "api/migrations/versions/0006_role_hub_admin.py"
      provides: "downgrade() function khôi phục CHECK 3 value + drop user_hubs.role column"
      contains: "def downgrade()"
  key_links:
    - from: "0006_role_hub_admin.py upgrade()"
      to: "users table CHECK constraint role_enum"
      via: "DROP + ADD CHECK constraint via op.execute raw SQL (idempotent introspect via sa.inspect)"
      pattern: "DROP CONSTRAINT.*role_enum.*ADD CONSTRAINT.*role IN.*'hub_admin'"
    - from: "0006_role_hub_admin.py upgrade()"
      to: "user_hubs table"
      via: "op.add_column với IF NOT EXISTS guard raw SQL"
      pattern: "ALTER TABLE user_hubs ADD COLUMN IF NOT EXISTS role TEXT"
    - from: "0006_role_hub_admin.py upgrade()"
      to: "audit_logs INSERT row"
      via: "op.execute INSERT với jsonb_build_object payload + SELECT count subquery"
      pattern: "INSERT INTO audit_logs.*action.*migration.role_seed"
---

<objective>
Tạo Alembic migration revision 0006 mở rộng schema RBAC theo D-V3.1-01/02 LOCKED:
- ROLE-01: CHECK constraint `users.role` chấp nhận thêm `'hub_admin'` (4 value tổng: `admin | hub_admin | editor | viewer`).
- ROLE-02: Thêm column `user_hubs.role` TEXT nullable default NULL (per-hub override; NULL = inherit `users.role` global).
- ROLE-03: Migration seed — audit_logs INSERT row `action='migration.role_seed'` ghi count + timestamp. Existing `users.role='admin'` GIỮ semantic super-admin (user_hubs.role=NULL toàn bộ — không cần backfill vì column mới mặc định NULL).

**Purpose:** Đóng gap RBAC role-per-hub được defer v4.0 trong M2. User report 2026-05-23 — tạo user gán hub `dmd` nhưng vẫn vào được central vì role `admin` global bypass user_hubs filter. Phase 1 đặt nền schema; Phase 2 add backend enforcement; Phase 3 add frontend; Phase 4 verify E2E.

**Output:** 1 file Alembic migration idempotent + downgrade impl + audit log seed.

**Carry forward strategy:**
- Pattern Plan 04-01 v3.0 (`0005_sync_outbox_per_hub.py`) — explicit raw SQL `op.execute()` cho CHECK constraint manipulation (Alembic `op.create_check_constraint` tên constraint không reliable across Postgres versions; introspect via `sa.inspect()` để verify post-apply).
- Idempotent strategy GA-V3.1-C khuyến nghị: introspect Python pre-condition (KHÔNG PL/pgSQL DO block) — đối xứng với Plan 04-01 skip guard `current_database()` check.
- audit_logs schema preserve: KHÔNG add column mới (payload jsonb đủ chứa count + timestamp event).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/CLAUDE.md
@Hub_All/api/migrations/versions/0005_sync_outbox_per_hub.py
@Hub_All/api/migrations/versions/0001_initial_schema.py
@Hub_All/api/app/models/auth.py

<interfaces>
<!-- Schema source-of-truth từ migration 0001 + model auth.py. Executor dùng trực tiếp — KHÔNG cần explore. -->

CHECK constraint hiện tại trên users.role (từ 0001_initial_schema.py:71-74):
```python
sa.CheckConstraint(
    "role IN ('admin', 'editor', 'viewer')",
    name="ck_users_role_enum",  # <-- TÊN CONSTRAINT THỰC TẾ TRONG DB
)
```

Model SQLAlchemy (api/app/models/auth.py:51-55) có discrepancy:
```python
CheckConstraint(
    "role IN ('admin', 'editor', 'viewer')",
    name="role_enum",  # <-- TÊN KHÁC; model KHÔNG được auto-sync với DB
)
```

→ Source-of-truth = migration 0001. Executor PHẢI introspect runtime để confirm tên thực tế:
```python
from sqlalchemy import inspect
inspector = inspect(bind)
constraints = inspector.get_check_constraints("users")
# Tìm constraint chứa "role IN" trong sqltext → lấy name thực tế
```

user_hubs schema hiện tại (từ 0001:140-163) — composite PK (user_id, hub_id), KHÔNG có column `role`:
```python
op.create_table(
    "user_hubs",
    sa.Column("user_id", postgresql.UUID(as_uuid=True), nullable=False),
    sa.Column("hub_id", postgresql.UUID(as_uuid=True), nullable=False),
    sa.Column("assigned_at", sa.DateTime(timezone=True), nullable=False,
              server_default=sa.text("NOW()")),
    # ... FK + PK
)
```

audit_logs schema (từ 0001:366-411) — payload JSONB nullable, không cần migration column mới:
```python
sa.Column("payload", postgresql.JSONB(astext_type=sa.Text()), nullable=True),
sa.Column("action", sa.Text(), nullable=False),
sa.Column("created_at", sa.DateTime(timezone=True), nullable=False,
          server_default=sa.text("NOW()")),
```

Alembic revision header convention (từ 0005:62-69):
```python
revision: str = "0006"
down_revision: Union[str, None] = "0005"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None
```

Skip-guard pattern carry forward Plan 04-01 (0005:81-86) — sync_outbox per-hub-only. 
KHÔNG áp dụng cho migration 0006 (RBAC schema áp dụng CẢ central + 3 hub con — schema users + user_hubs đồng nhất mọi DB theo D-V3-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo file 0006_role_hub_admin.py — header + upgrade() phần CHECK constraint mở rộng (ROLE-01)</name>
  <read_first>
    - Hub_All/api/migrations/versions/0005_sync_outbox_per_hub.py (toàn bộ — pattern Alembic + explicit raw SQL + threat model docstring)
    - Hub_All/api/migrations/versions/0001_initial_schema.py:46-77 (users table + CHECK constraint definition source-of-truth)
    - Hub_All/CLAUDE.md (stack pin Python 3.12 + SQLAlchemy 2 async + Alembic; raw SQL via op.execute pattern)
  </read_first>
  <files>api/migrations/versions/0006_role_hub_admin.py</files>
  <action>
Tạo file `api/migrations/versions/0006_role_hub_admin.py` với cấu trúc:

**Header docstring (tiếng Việt có dấu):**
```python
"""role_hub_admin

Revision ID: 0006
Revises: 0005
Create Date: 2026-05-23 14:00:00.000000

Phase 1 Plan 01-01 (ROLE-01 + ROLE-02 + ROLE-03) — Mở rộng schema RBAC v3.1:
- ROLE-01: CHECK constraint users.role + 'hub_admin' (4 value: admin|hub_admin|editor|viewer).
- ROLE-02: user_hubs.role TEXT nullable default NULL (per-hub override).
- ROLE-03: audit_logs INSERT row action='migration.role_seed' (audit trail).

Áp dụng cho CẢ central + 3 hub con (schema users + user_hubs đồng nhất mọi DB
theo D-V3-01 multi-DB topology). KHÔNG skip-guard như Plan 04-01.

Idempotent strategy (GA-V3.1-C — introspect):
- CHECK constraint: introspect users CHECK constraints qua sa.inspect(),
  detect tên thực tế (M2 có thể là 'ck_users_role_enum' từ 0001 hoặc
  'role_enum' từ model auth.py discrepancy), skip nếu đã có 'hub_admin' trong sqltext.
- user_hubs.role column: op.add_column với introspect get_columns() check,
  skip nếu column đã tồn tại.
- audit_logs seed row: INSERT ... WHERE NOT EXISTS guard tránh duplicate khi re-run.

Mitigations:
- T-01-01-01 Tampering — CHECK constraint mở rộng KHÔNG xoá data; DROP + ADD
  atomic trong cùng transaction (Alembic wrap upgrade qua BEGIN/COMMIT).
- T-01-01-02 Integrity — user_hubs.role nullable default NULL → existing rows
  preserve semantic inherit global (D-V3.1-02 LOCKED).
- R-V3.1-1 HIGH mitigation — downgrade impl đầy đủ + idempotent re-run safety
  (test ở Phase 4 MIGRATE-01).
"""
from __future__ import annotations

from typing import (  # noqa: UP035 — match Alembic template baseline 0001-0005
    Sequence,
    Union,
)

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "0006"
down_revision: Union[str, None] = "0005"  # noqa: UP007 — match baseline
branch_labels: Union[str, Sequence[str], None] = None  # noqa: UP007
depends_on: Union[str, Sequence[str], None] = None  # noqa: UP007
```

**upgrade() function — STEP 1 CHECK constraint mở rộng:**

```python
def upgrade() -> None:
    """ROLE-01 CHECK constraint mở rộng + ROLE-02 user_hubs.role + ROLE-03 seed audit."""
    bind = op.get_bind()
    inspector = sa.inspect(bind)

    # ============================================================
    # 1) ROLE-01 — CHECK constraint users.role mở rộng (4 value).
    # ============================================================
    # Introspect tên constraint thực tế (M2 có 2 candidate name do model/migration
    # discrepancy: 'ck_users_role_enum' từ 0001 vs 'role_enum' từ model).
    check_constraints = inspector.get_check_constraints("users")
    role_constraint_name: str | None = None
    role_constraint_sqltext: str | None = None
    for cc in check_constraints:
        sqltext = cc.get("sqltext", "")
        # Detect constraint chứa role IN (...) — phân biệt với user_status_enum.
        if "role" in sqltext and "admin" in sqltext and "status" not in sqltext:
            role_constraint_name = cc.get("name")
            role_constraint_sqltext = sqltext
            break

    if role_constraint_name is None:
        raise RuntimeError(
            "Không tìm thấy CHECK constraint users.role trong DB — "
            "schema baseline 0001 có thể chưa apply hoặc đã bị drop manual"
        )

    # Idempotent: skip nếu đã có 'hub_admin' trong sqltext.
    if "hub_admin" in (role_constraint_sqltext or ""):
        print(
            f"[0006] SKIP CHECK constraint users.role — đã có 'hub_admin' "
            f"trong sqltext của {role_constraint_name!r}"
        )
    else:
        # DROP cũ + ADD mới — atomic trong cùng transaction.
        op.execute(f'ALTER TABLE users DROP CONSTRAINT "{role_constraint_name}"')
        op.execute(
            f'ALTER TABLE users ADD CONSTRAINT "{role_constraint_name}" '
            f"CHECK (role IN ('admin', 'hub_admin', 'editor', 'viewer'))"
        )
        print(
            f"[0006] OK CHECK constraint {role_constraint_name!r} mở rộng → "
            f"4 value (admin|hub_admin|editor|viewer)"
        )

    # ... (Task 2 sẽ thêm STEP 2 user_hubs.role + Task 3 thêm STEP 3 audit seed)
```

**Lưu ý quan trọng:**
- Giữ tên constraint cũ (DROP + ADD cùng tên) để Phase 4 downgrade restore chính xác.
- `print()` log để operator debug `alembic upgrade head` thấy step nào skip/apply.
- KHÔNG dùng `op.create_check_constraint(...)` SQLAlchemy helper — name không reliable across Postgres versions; explicit raw SQL kiểm soát hoàn toàn.
- KHÔNG hardcode tên `ck_users_role_enum` — introspect runtime (chống discrepancy model vs migration phát hiện ở context window).

**KHÔNG implement đầy đủ ở task này** — chỉ phần header + STEP 1 CHECK constraint. Task 2 + 3 sẽ extend cùng file.
  </action>
  <verify>
    <automated>cd Hub_All/api && python -c "from migrations.versions import _0006_role_hub_admin; print('imported OK')" 2>&1 || python -c "import importlib.util; spec = importlib.util.spec_from_file_location('m', 'migrations/versions/0006_role_hub_admin.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.revision == '0006' and m.down_revision == '0005'; print('OK revision metadata')"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/migrations/versions/0006_role_hub_admin.py` EXISTS.
    - File chứa `revision: str = "0006"` + `down_revision: Union[str, None] = "0005"`.
    - File chứa string `"role IN ('admin', 'hub_admin', 'editor', 'viewer')"` (4 value ADD constraint SQL).
    - File chứa `inspector.get_check_constraints("users")` (introspect runtime).
    - File chứa `if "hub_admin" in (role_constraint_sqltext or "")` (idempotent guard).
    - File chứa `ALTER TABLE users DROP CONSTRAINT` + `ALTER TABLE users ADD CONSTRAINT` (raw SQL DROP+ADD).
    - File KHÔNG hardcode tên `ck_users_role_enum` hoặc `role_enum` (introspect-only).
    - Grep `grep -c "noqa: UP" Hub_All/api/migrations/versions/0006_role_hub_admin.py` ≥ 2 (match baseline 0001-0005 typing imports).
  </acceptance_criteria>
  <done>
    File 0006 hợp lệ Python + có revision metadata 0006→0005 + STEP 1 CHECK constraint mở rộng với introspect idempotent guard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend 0006 — STEP 2 user_hubs.role column nullable (ROLE-02)</name>
  <read_first>
    - api/migrations/versions/0006_role_hub_admin.py (state sau Task 1 — extend file)
    - api/migrations/versions/0005_sync_outbox_per_hub.py:130-134 (pattern `ADD COLUMN IF NOT EXISTS` raw SQL — analog cho documents.sync_status)
    - api/migrations/versions/0001_initial_schema.py:140-163 (user_hubs table baseline)
  </read_first>
  <files>api/migrations/versions/0006_role_hub_admin.py</files>
  <action>
Thêm STEP 2 vào hàm `upgrade()` đã có từ Task 1, NGAY SAU phần STEP 1 CHECK constraint:

```python
    # ============================================================
    # 2) ROLE-02 — user_hubs.role TEXT nullable default NULL.
    # ============================================================
    # Per-hub role override (NULL = inherit users.role global — D-V3.1-02 LOCKED).
    # Idempotent: introspect user_hubs columns, skip nếu 'role' đã tồn tại.
    user_hubs_columns = {col["name"] for col in inspector.get_columns("user_hubs")}
    if "role" in user_hubs_columns:
        print("[0006] SKIP user_hubs.role — column đã tồn tại")
    else:
        # Raw SQL `ADD COLUMN IF NOT EXISTS` — defensive double-guard (introspect
        # check ở Python level + IF NOT EXISTS ở DDL level đề phòng concurrent
        # migration race condition extreme edge case).
        op.execute("""
            ALTER TABLE user_hubs
            ADD COLUMN IF NOT EXISTS role TEXT NULL DEFAULT NULL
        """)
        # Optional CHECK constraint trên user_hubs.role — chấp nhận cùng 4 value
        # NHƯNG cho phép NULL. PostgreSQL CHECK constraint auto-pass NULL (3-value
        # logic), nên syntax `CHECK (role IS NULL OR role IN (...))` explicit.
        op.execute("""
            ALTER TABLE user_hubs
            ADD CONSTRAINT ck_user_hubs_role_enum
            CHECK (role IS NULL OR role IN ('admin', 'hub_admin', 'editor', 'viewer'))
        """)
        print(
            "[0006] OK user_hubs.role nullable column added (4 value CHECK + NULL allowed)"
        )
```

**Lưu ý:**
- `DEFAULT NULL` redundant với `NULL` (Postgres default behavior), GIỮ explicit để document intent → existing rows backfill NULL khi ADD COLUMN.
- CHECK constraint tên `ck_user_hubs_role_enum` (prefix `ck_` đồng nhất với `ck_users_role_enum` baseline 0001).
- KHÔNG cần data backfill — ALTER TABLE ADD COLUMN với default NULL = O(1) instant trên Postgres ≥11 (metadata-only operation).
- Idempotent guard kiểm tra column EXISTS qua introspect; KHÔNG kiểm tra constraint exists (constraint chỉ tạo cùng column → atomic).
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS role TEXT" Hub_All/api/migrations/versions/0006_role_hub_admin.py && grep -c "ck_user_hubs_role_enum" Hub_All/api/migrations/versions/0006_role_hub_admin.py && grep -c "role IS NULL OR role IN" Hub_All/api/migrations/versions/0006_role_hub_admin.py</automated>
  </verify>
  <acceptance_criteria>
    - File 0006 chứa `ALTER TABLE user_hubs ADD COLUMN IF NOT EXISTS role TEXT` (idempotent DDL).
    - File 0006 chứa `ck_user_hubs_role_enum` (CHECK constraint name).
    - File 0006 chứa `CHECK (role IS NULL OR role IN ('admin', 'hub_admin', 'editor', 'viewer'))` (NULL-aware CHECK).
    - File 0006 chứa `inspector.get_columns("user_hubs")` (introspect Python-level guard).
    - File 0006 chứa `if "role" in user_hubs_columns:` (idempotent skip branch).
    - Câu lệnh `python -c "import ast; ast.parse(open('Hub_All/api/migrations/versions/0006_role_hub_admin.py').read())"` exit 0 (syntax valid).
  </acceptance_criteria>
  <done>
    STEP 2 user_hubs.role nullable + CHECK NULL-aware có trong upgrade(); idempotent guard introspect; syntax Python hợp lệ.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend 0006 — STEP 3 audit_logs seed + downgrade() đầy đủ (ROLE-03 + rollback)</name>
  <read_first>
    - api/migrations/versions/0006_role_hub_admin.py (state sau Task 2)
    - api/migrations/versions/0005_sync_outbox_per_hub.py:211-225 (pattern downgrade() IF EXISTS thứ tự ngược upgrade)
    - api/migrations/versions/0001_initial_schema.py:366-411 (audit_logs table schema source-of-truth)
  </read_first>
  <files>api/migrations/versions/0006_role_hub_admin.py</files>
  <action>
Thêm STEP 3 vào upgrade() + impl downgrade() đầy đủ.

**STEP 3 audit_logs seed (ROLE-03) — append sau STEP 2:**

```python
    # ============================================================
    # 3) ROLE-03 — Migration seed audit log (action='migration.role_seed').
    # ============================================================
    # Ghi nhận count user role='admin' (giữ semantic super-admin global) +
    # count user_hubs row hiện tại (preserve nguyên — role=NULL inherit).
    # Idempotent: WHERE NOT EXISTS guard tránh duplicate khi re-run.
    op.execute("""
        INSERT INTO audit_logs (action, target_type, payload, created_at)
        SELECT
            'migration.role_seed',
            'schema',
            jsonb_build_object(
                'migration_revision', '0006',
                'admin_count', (SELECT COUNT(*) FROM users WHERE role = 'admin'),
                'user_hubs_count', (SELECT COUNT(*) FROM user_hubs),
                'timestamp_utc', NOW()::text,
                'note', 'ROLE-03 seed: existing users.role=admin giữ super-admin semantic; user_hubs.role mặc định NULL (inherit global)'
            ),
            NOW()
        WHERE NOT EXISTS (
            SELECT 1 FROM audit_logs
            WHERE action = 'migration.role_seed'
              AND payload->>'migration_revision' = '0006'
        )
    """)
    print("[0006] OK audit_logs seed row inserted (action='migration.role_seed')")
```

**downgrade() function — rollback đầy đủ (thứ tự NGƯỢC upgrade):**

```python
def downgrade() -> None:
    """Idempotent rollback — restore CHECK 3 value + drop user_hubs.role + keep audit.

    Thứ tự ngược upgrade:
    1. KHÔNG xoá audit_logs seed row (audit trail forensic — D-V3-Phase4-C2 carry forward).
    2. Drop user_hubs CHECK constraint ck_user_hubs_role_enum.
    3. Drop column user_hubs.role.
    4. Restore CHECK constraint users.role về 3 value (drop + add).

    Lưu ý: Nếu downgrade chạy mà users đã có row role='hub_admin' → CHECK constraint
    3-value sẽ FAIL khi ADD. Defensive: log warning nếu count > 0, KHÔNG auto-delete
    (operator phải xử lý manual — E-V3.1-1 trigger).
    """
    bind = op.get_bind()
    inspector = sa.inspect(bind)

    # Defensive check — count user role='hub_admin' trước khi rollback CHECK.
    hub_admin_count = bind.execute(
        sa.text("SELECT COUNT(*) FROM users WHERE role = 'hub_admin'")
    ).scalar()
    if hub_admin_count and hub_admin_count > 0:
        raise RuntimeError(
            f"Cannot downgrade 0006 → 0005: {hub_admin_count} user(s) hiện có role='hub_admin' "
            f"sẽ FAIL CHECK constraint 3-value. Vui lòng UPDATE users SET role='viewer' "
            f"hoặc xoá user role='hub_admin' trước khi downgrade (E-V3.1-1 carry forward)."
        )

    # 1) Drop user_hubs.role CHECK constraint (nếu tồn tại).
    op.execute("ALTER TABLE user_hubs DROP CONSTRAINT IF EXISTS ck_user_hubs_role_enum")

    # 2) Drop user_hubs.role column (nếu tồn tại).
    user_hubs_columns = {col["name"] for col in inspector.get_columns("user_hubs")}
    if "role" in user_hubs_columns:
        op.execute("ALTER TABLE user_hubs DROP COLUMN role")
        print("[0006 downgrade] OK user_hubs.role column dropped")
    else:
        print("[0006 downgrade] SKIP user_hubs.role — column không tồn tại")

    # 3) Restore CHECK constraint users.role về 3 value — introspect tên thật.
    check_constraints = inspector.get_check_constraints("users")
    role_constraint_name: str | None = None
    for cc in check_constraints:
        sqltext = cc.get("sqltext", "")
        if "role" in sqltext and "admin" in sqltext and "status" not in sqltext:
            role_constraint_name = cc.get("name")
            break

    if role_constraint_name is None:
        print("[0006 downgrade] SKIP users.role CHECK — constraint không tồn tại")
    else:
        op.execute(f'ALTER TABLE users DROP CONSTRAINT "{role_constraint_name}"')
        op.execute(
            f'ALTER TABLE users ADD CONSTRAINT "{role_constraint_name}" '
            f"CHECK (role IN ('admin', 'editor', 'viewer'))"
        )
        print(
            f"[0006 downgrade] OK CHECK constraint {role_constraint_name!r} restored → 3 value"
        )

    # KHÔNG xoá audit_logs seed row — audit trail forensic giữ nguyên dù rollback schema.
    # (Operator có thể manual DELETE nếu cần — out of scope migration.)
```

**Lưu ý quan trọng:**
- downgrade() PHẢI dùng `bind.execute().scalar()` để check count (KHÔNG dùng `op.execute()` vì op.execute return None — không lấy được result row).
- `RuntimeError` ngăn rollback khi có dữ liệu role='hub_admin' tồn tại → E-V3.1-1 STOP trigger.
- Restore CHECK constraint dùng tên gốc (introspect) — đảm bảo schema identical với pre-0006 state.
- KHÔNG xoá audit row — pattern carry forward Plan 04-06 audit trail D-V3-Phase4-C2 (audit forensic preserve dù schema rollback).
  </action>
  <verify>
    <automated>grep -c "migration.role_seed" Hub_All/api/migrations/versions/0006_role_hub_admin.py && grep -c "def downgrade" Hub_All/api/migrations/versions/0006_role_hub_admin.py && grep -c "WHERE NOT EXISTS" Hub_All/api/migrations/versions/0006_role_hub_admin.py && grep -c "RuntimeError" Hub_All/api/migrations/versions/0006_role_hub_admin.py && python -c "import ast; ast.parse(open('Hub_All/api/migrations/versions/0006_role_hub_admin.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - File 0006 chứa `'migration.role_seed'` string (audit action constant).
    - File 0006 chứa `WHERE NOT EXISTS` (idempotent seed guard).
    - File 0006 chứa `jsonb_build_object` + `'admin_count'` + `'user_hubs_count'` + `'migration_revision'` (payload fields).
    - File 0006 chứa `def downgrade() -> None:` (function definition).
    - File 0006 chứa `SELECT COUNT(*) FROM users WHERE role = 'hub_admin'` (defensive count check).
    - File 0006 chứa `RuntimeError` với message mention `hub_admin` + `E-V3.1-1` (rollback guard).
    - File 0006 chứa `ALTER TABLE user_hubs DROP COLUMN role` (column drop).
    - File 0006 chứa `CHECK (role IN ('admin', 'editor', 'viewer'))` (3-value restore CHECK).
    - `python -c "import ast; ast.parse(open('Hub_All/api/migrations/versions/0006_role_hub_admin.py').read())"` exit 0.
    - `python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','Hub_All/api/migrations/versions/0006_role_hub_admin.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m,'upgrade') and hasattr(m,'downgrade'); print('OK')"` exit 0.
  </acceptance_criteria>
  <done>
    File 0006 hoàn chỉnh: header + upgrade() 3 STEP (CHECK extend + user_hubs.role + audit seed) + downgrade() rollback đầy đủ với defensive check role='hub_admin' count.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration runner → Postgres DDL | Operator chạy `alembic upgrade head` qua psql connection (DSN từ Settings) — DDL privileged operation |
| Migration upgrade() / downgrade() | Code-controlled, KHÔNG nhận user input — threat model giới hạn ở DB-level integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | upgrade() CHECK constraint DROP+ADD | mitigate | Atomic transaction (Alembic wrap BEGIN/COMMIT); introspect tên constraint runtime (KHÔNG hardcode); nếu DB state corrupt → RuntimeError early fail |
| T-01-01-02 | Integrity | user_hubs.role ADD COLUMN với default NULL | mitigate | ALTER TABLE ADD COLUMN NULL DEFAULT NULL → existing rows backfill NULL atomic (Postgres ≥11 metadata-only O(1)); CHECK constraint NULL-aware allow inherit semantic |
| T-01-01-03 | Repudiation | Migration không trace operator action | mitigate | audit_logs seed row action='migration.role_seed' với jsonb_build_object payload: migration_revision + admin_count + user_hubs_count + timestamp_utc; idempotent WHERE NOT EXISTS guard |
| T-01-01-04 | DoS | Re-run upgrade head fail/loop | mitigate | Introspect-based idempotent guard ở 3 STEP (CHECK / user_hubs.role / audit seed) — re-run an toàn; print() log operator visibility |
| T-01-01-05 | Elevation of Privilege | Downgrade phá CHECK với row role='hub_admin' tồn tại | mitigate | Defensive RuntimeError ở downgrade() nếu COUNT(*) WHERE role='hub_admin' > 0 → E-V3.1-1 STOP trigger; operator phải clean manual |
| T-01-01-06 | Information Disclosure | Audit seed leak count toàn DB | accept | admin_count + user_hubs_count chỉ là số đếm, KHÔNG PII; payload chỉ visible cho admin role qua audit_logs API (Phase 5 require_role("admin") guard carry forward) |
</threat_model>

<verification>
- File `Hub_All/api/migrations/versions/0006_role_hub_admin.py` tồn tại + syntax Python hợp lệ.
- upgrade() 3 STEP: CHECK constraint mở rộng 4 value + user_hubs.role nullable + audit_logs seed.
- downgrade() 3 STEP ngược + defensive check role='hub_admin' count.
- Idempotent guard ở mọi step (introspect-based + IF NOT EXISTS + WHERE NOT EXISTS).
- Tên constraint introspect runtime (KHÔNG hardcode tránh model/migration discrepancy).
- Pattern carry forward 0005 Plan 04-01 (explicit raw SQL + skip-guard introspect + docstring threat model).
</verification>

<success_criteria>
1. File 0006_role_hub_admin.py compile Python OK (ast.parse exit 0).
2. revision='0006', down_revision='0005' đúng chain.
3. upgrade() chứa 3 STEP rõ ràng + comment Vietnamese.
4. downgrade() chứa 3 STEP ngược + RuntimeError guard role='hub_admin'.
5. Idempotent verify chạy được ở Phase 4 MIGRATE-01 (re-run head 2 lần PASS).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/01-rbac-schema-migration/01-01-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- File 0006 đã tạo với 3 STEP upgrade() + 3 STEP downgrade()
- Idempotent strategy GA-V3.1-C: introspect (KHÔNG PL/pgSQL DO block)
- CHECK constraint name introspect runtime (model/migration discrepancy resolved)
- audit_logs seed pattern carry forward (KHÔNG migration column mới)
- Phase 4 MIGRATE-01 sẽ verify runtime upgrade/downgrade idempotent

Commit atomic: `feat(01-01): Alembic migration 0006 role_hub_admin + user_hubs.role + audit seed` + body tiếng Việt nói tại sao (mở rộng RBAC role-per-hub gap M2 defer v4.0; idempotent introspect + downgrade defensive).
</output>
