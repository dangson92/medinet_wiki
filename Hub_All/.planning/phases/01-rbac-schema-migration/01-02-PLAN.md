---
phase: 01-rbac-schema-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/auth/role.py
  - api/tests/unit/test_role_helper.py
autonomous: true
requirements: [ROLE-04]
must_haves:
  truths:
    - "Helper `get_effective_role(session, user_id, hub_id) -> str` tồn tại, return role string"
    - "Khi user_hubs.role IS NOT NULL cho (user_id, hub_id) → return user_hubs.role (override)"
    - "Khi user_hubs.role IS NULL cho (user_id, hub_id) → return users.role (inherit global)"
    - "Khi không có user_hubs row cho (user_id, hub_id) → return users.role (fallback — no membership)"
    - "Khi không có users row → raise UserNotFoundError (defensive — caller phải handle)"
    - "Unit test pytest 4 case + 1 error case PASS — KHÔNG cần DB runtime (mock AsyncSession.execute)"
  artifacts:
    - path: "api/app/auth/role.py"
      provides: "Module mới chứa get_effective_role async helper + UserNotFoundError exception"
      contains: "async def get_effective_role"
    - path: "api/tests/unit/test_role_helper.py"
      provides: "pytest 5 case test cover semantic helper (4-case + 1 error)"
      contains: "async def test_"
  key_links:
    - from: "api/app/auth/role.py::get_effective_role"
      to: "user_hubs.role column + users.role column"
      via: "SQLAlchemy text() query với named bind params (T-01-02-01 SQL injection mitigation)"
      pattern: "SELECT .* FROM user_hubs.*WHERE user_id.*hub_id"
    - from: "test_role_helper.py"
      to: "get_effective_role"
      via: "AsyncMock(spec=AsyncSession) + side_effect return row tuple"
      pattern: "AsyncMock.*spec=AsyncSession"
---

<objective>
Tạo module `api/app/auth/role.py` mới chứa async helper `get_effective_role(session, user_id, hub_id) -> str` + unit test pytest 5 case cover semantic ROLE-04.

**Purpose:** Phase 2 sẽ dùng helper này trong `require_hub_admin_for(hub_id)` dependency (DEP-01). Đặt helper ở module riêng `role.py` (KHÔNG extend `dependencies.py`) — separation of concerns: `role.py` chứa business logic role resolution, `dependencies.py` chỉ chứa FastAPI dependency wrappers. Phase 2 import `from app.auth.role import get_effective_role`.

**Logic (D-V3.1-02 LOCKED carry forward):**
1. SELECT user_hubs.role WHERE user_id=$1 AND hub_id=$2 → nếu non-NULL → return.
2. Nếu user_hubs row tồn tại nhưng role=NULL → SELECT users.role WHERE id=$1 → return (inherit global).
3. Nếu user_hubs row KHÔNG tồn tại (no membership) → SELECT users.role WHERE id=$1 → return (fallback global).
4. Nếu users row KHÔNG tồn tại → raise `UserNotFoundError` (defensive).

**Output:** 2 file mới (role.py module + test_role_helper.py) + pytest pass.

**Lưu ý carry forward CLAUDE.md:**
- Raw SQL via `sqlalchemy.text()` + named bind params `:user_id`, `:hub_id` (T-XX SQL injection mitigation — CLAUDE.md section 3 stack pin).
- Timestamp KHÔNG cần (helper read-only).
- Async function với AsyncSession (carry forward Phase 3+5+6 pattern).
- Tiếng Việt có dấu cho docstring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/auth/dependencies.py
@Hub_All/api/app/models/auth.py
@Hub_All/api/tests/unit/test_require_role.py

<interfaces>
<!-- Pattern source-of-truth từ dependencies.py + models/auth.py. Executor dùng trực tiếp. -->

User model (api/app/models/auth.py:31-60):
```python
class User(UUIDMixin, TimestampMixin, Base):
    __tablename__ = "users"
    email: Mapped[str]
    role: Mapped[str]  # 'admin' | 'editor' | 'viewer' M2; v3.1 thêm 'hub_admin'
    is_active: Mapped[bool]
    # ... (UUIDMixin cung cấp id: Mapped[uuid.UUID])
```

UserHub model (api/app/models/auth.py:91-114) — composite PK (user_id, hub_id):
```python
class UserHub(Base):
    __tablename__ = "user_hubs"
    user_id: Mapped[uuid.UUID]
    hub_id: Mapped[uuid.UUID]
    assigned_at: Mapped[datetime]
    # v3.1 Plan 01-01 sẽ ADD COLUMN role: Mapped[str | None] qua migration 0006
```

Sau Plan 01-01 áp dụng migration 0006, DB sẽ có column `user_hubs.role TEXT NULL`. Plan 01-02 này KHÔNG cần update model SQLAlchemy (model có thể lệch DB cho tới khi cần đọc qua ORM; helper dùng raw SQL `text()` nên KHÔNG depend model attribute).

AsyncSession query pattern (carry forward dependencies.py:236-239 + api_keys verify):
```python
from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession

result = await session.execute(
    text("SELECT col FROM table WHERE id = :id"),
    {"id": str(some_uuid)},
)
row = result.fetchone()  # tuple hoặc None
```

Unit test pattern (carry forward test_require_role.py:1-43) — pytest async + AsyncMock:
```python
import pytest
from unittest.mock import AsyncMock, MagicMock

@pytest.mark.asyncio
async def test_xxx():
    session = AsyncMock()
    result_mock = MagicMock()
    result_mock.fetchone.return_value = ('admin',)  # tuple — row của SELECT
    session.execute.return_value = result_mock
    # ... assert helper output
```

pytest-asyncio đã cài (carry forward Phase 3+ test pattern). Marker `@pytest.mark.asyncio` hoặc fixture auto-detect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo module api/app/auth/role.py — get_effective_role helper + UserNotFoundError</name>
  <read_first>
    - Hub_All/api/app/auth/dependencies.py:1-50 (import style + module docstring tiếng Việt + async function pattern)
    - Hub_All/api/app/auth/dependencies.py:236-248 (SQLAlchemy text() + named bind params pattern + AsyncSession.execute)
    - Hub_All/CLAUDE.md (raw SQL `sqlalchemy.text()` + named bind params convention)
  </read_first>
  <files>api/app/auth/role.py</files>
  <action>
Tạo file `api/app/auth/role.py` với nội dung:

```python
"""Role resolution helper — Phase 1 Plan 01-02 (ROLE-04).

Module mới tách khỏi `dependencies.py` (separation of concerns):
- `dependencies.py`: FastAPI dependency wrappers (Depends-injected).
- `role.py`: business logic role resolution thuần (KHÔNG FastAPI dep).

Phase 2 sẽ import `from app.auth.role import get_effective_role` để build
dependency `require_hub_admin_for(hub_id)` (DEP-01).

Logic ROLE-04 (D-V3.1-02 LOCKED):
1. SELECT user_hubs.role WHERE user=$1 AND hub=$2 → non-NULL → return (override).
2. user_hubs row tồn tại + role=NULL → return users.role (inherit global).
3. KHÔNG có user_hubs row → return users.role (fallback no-membership).
4. KHÔNG có users row → raise UserNotFoundError (defensive — caller handle).

Mitigations:
- T-01-02-01 SQL injection — raw SQL via sqlalchemy.text() + named bind params
  `:user_id`, `:hub_id` (CLAUDE.md stack pin convention).
- T-01-02-02 Type coercion — user_id + hub_id chấp nhận UUID hoặc str, convert
  về str(uuid) trước khi bind (asyncpg auto-cast text→uuid trong Postgres).
- T-01-02-03 Race condition — async function single-shot read, KHÔNG transaction
  scope (caller chịu trách nhiệm session lifecycle); idempotent read-only.
"""
from __future__ import annotations

from uuid import UUID

from sqlalchemy import text
from sqlalchemy.ext.asyncio import AsyncSession


class UserNotFoundError(Exception):
    """User_id không tồn tại trong table users — defensive exception.

    Caller (Phase 2 dependency) phải handle riêng — KHÔNG silent return string
    rỗng / role mặc định (tránh privilege escalation case stale user_id token).
    """


async def get_effective_role(
    session: AsyncSession,
    user_id: UUID | str,
    hub_id: UUID | str,
) -> str:
    """Trả về effective role của user trong hub cụ thể (per-hub override aware).

    Logic ROLE-04 (D-V3.1-02 LOCKED — D-V3.1-02 "user_hubs.role nullable;
    NULL = inherit users.role global"):

    1. SELECT user_hubs.role WHERE user_id=:uid AND hub_id=:hid
       - Row tồn tại + role IS NOT NULL → return user_hubs.role (per-hub override).
       - Row tồn tại + role IS NULL → fallthrough STEP 2.
       - Row KHÔNG tồn tại → fallthrough STEP 2 (no-membership fallback global).
    2. SELECT users.role WHERE id=:uid
       - Row tồn tại → return users.role (inherit global).
       - Row KHÔNG tồn tại → raise UserNotFoundError.

    Args:
        session: AsyncSession đang mở (caller manage lifecycle).
        user_id: UUID hoặc str — converted về str(uuid) trước bind.
        hub_id: UUID hoặc str — converted về str(uuid) trước bind.

    Returns:
        Role string: 'admin' | 'hub_admin' | 'editor' | 'viewer' (theo CHECK
        constraint Plan 01-01 migration 0006).

    Raises:
        UserNotFoundError: Nếu user_id không tồn tại trong table users.

    Example:
        >>> # super admin (users.role='admin', user_hubs.role=NULL)
        >>> role = await get_effective_role(db, super_admin_id, dmd_hub_id)
        >>> assert role == 'admin'

        >>> # hub_admin per-hub (users.role='editor', user_hubs.role='hub_admin')
        >>> role = await get_effective_role(db, hub_admin_user_id, dmd_hub_id)
        >>> assert role == 'hub_admin'

        >>> # viewer per-hub (users.role='admin', user_hubs.role='viewer')
        >>> role = await get_effective_role(db, demoted_admin_id, dmd_hub_id)
        >>> assert role == 'viewer'

        >>> # no membership fallback (users.role='admin', không có user_hubs row)
        >>> role = await get_effective_role(db, super_admin_id, unassigned_hub_id)
        >>> assert role == 'admin'  # fallback global
    """
    user_id_str = str(user_id)
    hub_id_str = str(hub_id)

    # STEP 1 — Check user_hubs.role override.
    result = await session.execute(
        text(
            "SELECT role FROM user_hubs "
            "WHERE user_id = :user_id AND hub_id = :hub_id"
        ),
        {"user_id": user_id_str, "hub_id": hub_id_str},
    )
    row = result.fetchone()
    if row is not None and row[0] is not None:
        # Per-hub override → return ngay (KHÔNG cần check users.role).
        return str(row[0])

    # STEP 2 — Inherit global (row IS NULL hoặc row KHÔNG tồn tại).
    result = await session.execute(
        text("SELECT role FROM users WHERE id = :user_id"),
        {"user_id": user_id_str},
    )
    user_row = result.fetchone()
    if user_row is None:
        raise UserNotFoundError(
            f"User {user_id_str!r} không tồn tại trong table users — "
            f"defensive guard (caller phải handle stale user_id token)"
        )
    return str(user_row[0])
```

**Lưu ý quan trọng:**
- KHÔNG dùng `User` / `UserHub` SQLAlchemy ORM model — raw text() chống dependency cycle (Phase 2 dependency có thể tạo cycle nếu helper import model).
- `str(user_id)` + `str(hub_id)` convert UUID → str cho asyncpg bind. Postgres auto-cast text → uuid khi compare với column `user_id UUID`.
- `from __future__ import annotations` để hỗ trợ `UUID | str` union syntax Python 3.10+ (CLAUDE.md pin 3.12).
- Exception class `UserNotFoundError` declare ở top module — Phase 2 sẽ import nếu cần handle (hiện tại sẽ propagate qua FastAPI → 500 INTERNAL_ERROR — acceptable defensive default).
- KHÔNG cache result — caller chịu trách nhiệm session lifecycle (FastAPI Depends scope).
  </action>
  <verify>
    <automated>cd Hub_All && python -c "import sys; sys.path.insert(0, 'api'); from app.auth.role import get_effective_role, UserNotFoundError; import inspect; assert inspect.iscoroutinefunction(get_effective_role); assert issubclass(UserNotFoundError, Exception); print('OK helper imports + async signature')"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/app/auth/role.py` EXISTS.
    - File chứa `async def get_effective_role(session: AsyncSession, user_id: UUID | str, hub_id: UUID | str) -> str:` (đúng signature).
    - File chứa `class UserNotFoundError(Exception):` (defensive exception).
    - File chứa `text(` import + `from sqlalchemy import text` (raw SQL pattern).
    - File chứa `:user_id` + `:hub_id` (named bind params — T-01-02-01 mitigation).
    - File chứa `SELECT role FROM user_hubs` (STEP 1 query).
    - File chứa `SELECT role FROM users WHERE id` (STEP 2 query).
    - File chứa `row[0] is not None` (NULL-aware check).
    - File chứa `raise UserNotFoundError` (defensive raise).
    - File KHÔNG import `User` hoặc `UserHub` (raw SQL only, chống cycle).
    - `python -c "import sys; sys.path.insert(0, 'Hub_All/api'); from app.auth.role import get_effective_role"` exit 0.
  </acceptance_criteria>
  <done>
    Module role.py tồn tại với async helper + exception + raw SQL named bind params; signature match đặc tả D-V3.1-02.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tạo unit test test_role_helper.py — 5 case cover semantic ROLE-04</name>
  <read_first>
    - Hub_All/api/app/auth/role.py (vừa tạo ở Task 1 — public API + signature)
    - Hub_All/api/tests/unit/test_require_role.py (pattern pytest async unit test KHÔNG cần DB)
    - Hub_All/CLAUDE.md (testing convention pytest + AsyncMock)
  </read_first>
  <files>api/tests/unit/test_role_helper.py</files>
  <action>
Tạo file `api/tests/unit/test_role_helper.py` với 5 test case:

```python
"""Unit test get_effective_role helper — Phase 1 Plan 01-02 (ROLE-04).

Pure Python test, KHÔNG cần Postgres/Redis runtime. Mock AsyncSession.execute
trả về MagicMock result với fetchone() side_effect — cover 4 case semantic +
1 defensive case.

Test cases (ROLE-04 acceptance):
1. super_admin: users.role='admin', user_hubs.role=NULL → return 'admin'.
2. hub_admin per-hub: users.role='editor', user_hubs.role='hub_admin' → return 'hub_admin'.
3. viewer per-hub override: users.role='admin', user_hubs.role='viewer' → return 'viewer'.
4. no membership fallback: users.role='admin', user_hubs row KHÔNG tồn tại → return 'admin'.
5. user_not_found defensive: users row KHÔNG tồn tại → raise UserNotFoundError.
"""
from __future__ import annotations

from unittest.mock import AsyncMock, MagicMock
from uuid import uuid4

import pytest

from app.auth.role import UserNotFoundError, get_effective_role


def _make_session(*fetchone_returns: object) -> AsyncMock:
    """Build AsyncMock(AsyncSession) trả về sequence fetchone results.

    Args:
        *fetchone_returns: Tuple sequence — mỗi item là row return cho lần
            execute() tiếp theo. None = row không tồn tại; tuple = row tồn tại.

    Example:
        >>> session = _make_session(('hub_admin',))  # 1 query trả về row role='hub_admin'
        >>> session = _make_session(None, ('admin',))  # 2 queries: 1 None, 2 row
    """
    session = AsyncMock()
    result_mocks = []
    for fetchone_value in fetchone_returns:
        result = MagicMock()
        result.fetchone.return_value = fetchone_value
        result_mocks.append(result)
    session.execute = AsyncMock(side_effect=result_mocks)
    return session


@pytest.mark.asyncio
async def test_super_admin_no_user_hubs_row_returns_admin() -> None:
    """Case 4: users.role='admin', user_hubs row KHÔNG tồn tại (no membership)
    → return 'admin' (fallback global).

    STEP 1: user_hubs query trả None.
    STEP 2: users query trả ('admin',).
    """
    user_id = uuid4()
    hub_id = uuid4()
    session = _make_session(None, ('admin',))

    role = await get_effective_role(session, user_id, hub_id)

    assert role == 'admin'
    assert session.execute.call_count == 2


@pytest.mark.asyncio
async def test_super_admin_user_hubs_role_null_returns_admin() -> None:
    """Case 1: users.role='admin', user_hubs.role=NULL → return 'admin' (inherit).

    STEP 1: user_hubs query trả (None,) — row tồn tại NHƯNG role IS NULL.
    STEP 2: users query trả ('admin',).
    """
    user_id = uuid4()
    hub_id = uuid4()
    session = _make_session((None,), ('admin',))

    role = await get_effective_role(session, user_id, hub_id)

    assert role == 'admin'
    assert session.execute.call_count == 2


@pytest.mark.asyncio
async def test_hub_admin_per_hub_override_returns_hub_admin() -> None:
    """Case 2: users.role='editor', user_hubs.role='hub_admin' → return 'hub_admin'.

    Per-hub override path — STEP 1 đủ, KHÔNG cần STEP 2.
    """
    user_id = uuid4()
    hub_id = uuid4()
    session = _make_session(('hub_admin',))

    role = await get_effective_role(session, user_id, hub_id)

    assert role == 'hub_admin'
    assert session.execute.call_count == 1  # STEP 2 skip


@pytest.mark.asyncio
async def test_viewer_per_hub_override_returns_viewer() -> None:
    """Case 3: users.role='admin' (super), user_hubs.role='viewer' (demote per-hub)
    → return 'viewer' (override semantic — D-V3.1-02 LOCKED).

    Demonstration semantic: per-hub override CÓ THỂ demote super-admin xuống viewer
    cho hub cụ thể (forward compat case "super admin xem hub này dạng viewer").
    """
    user_id = uuid4()
    hub_id = uuid4()
    session = _make_session(('viewer',))

    role = await get_effective_role(session, user_id, hub_id)

    assert role == 'viewer'
    assert session.execute.call_count == 1


@pytest.mark.asyncio
async def test_user_not_found_raises_user_not_found_error() -> None:
    """Case 5: users row KHÔNG tồn tại → raise UserNotFoundError (defensive).

    STEP 1: user_hubs query trả None (no membership).
    STEP 2: users query trả None (user không tồn tại).
    → Raise UserNotFoundError với message mention user_id.
    """
    user_id = uuid4()
    hub_id = uuid4()
    session = _make_session(None, None)

    with pytest.raises(UserNotFoundError, match=str(user_id)):
        await get_effective_role(session, user_id, hub_id)

    assert session.execute.call_count == 2


@pytest.mark.asyncio
async def test_get_effective_role_accepts_str_uuid_args() -> None:
    """Defensive: helper chấp nhận user_id/hub_id dạng str (KHÔNG chỉ UUID object).

    Coverage carry forward Phase 3 SSO pattern — JWT claims.sub là str, KHÔNG UUID.
    """
    user_id_str = str(uuid4())
    hub_id_str = str(uuid4())
    session = _make_session(('admin',))

    role = await get_effective_role(session, user_id_str, hub_id_str)

    assert role == 'admin'
    # Verify session.execute() được gọi với param value là str (không phải UUID).
    call_kwargs = session.execute.call_args_list[0]
    # call_args_list[0] là tuple (args, kwargs); args[1] là dict params.
    assert isinstance(call_kwargs[0][1]['user_id'], str)
    assert isinstance(call_kwargs[0][1]['hub_id'], str)
```

**Lưu ý:**
- `@pytest.mark.asyncio` marker REQUIRED (pytest-asyncio đã cài Phase 3).
- `_make_session(*fetchone_returns)` helper factory — 1-liner build mock với multiple execute() call sequence.
- 6 test case (5 ROLE-04 case + 1 str args defensive) — đủ semantic cover.
- KHÔNG cần fixture conftest.py (test self-contained).
- Path import `from app.auth.role import ...` đúng convention pytest qua `pyproject.toml` root path (carry forward Phase 3 test_require_role.py:12 `from app.auth import require_role`).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/unit/test_role_helper.py -x -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/unit/test_role_helper.py` EXISTS.
    - File chứa 6 test function (`async def test_...`) — verify qua `grep -c "async def test_" Hub_All/api/tests/unit/test_role_helper.py` ≥ 6.
    - File chứa `from app.auth.role import UserNotFoundError, get_effective_role` (import test target).
    - File chứa `@pytest.mark.asyncio` decorator ≥ 6 lần.
    - File chứa `pytest.raises(UserNotFoundError` (case 5 defensive).
    - File chứa 4 distinct return value assert: `'admin'`, `'hub_admin'`, `'viewer'`, `UserNotFoundError`.
    - `cd Hub_All/api && python -m pytest tests/unit/test_role_helper.py -x` exit 0 (tất cả test PASS).
    - Test KHÔNG cần Postgres runtime (mock-only — verify qua KHÔNG có import `from app.db.session` hoặc fixture DB).
  </acceptance_criteria>
  <done>
    test_role_helper.py có 6 test PASS pytest; mock AsyncSession; cover 4-case ROLE-04 + 1 defensive UserNotFoundError + 1 str args coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 2 dependency → get_effective_role | Phase 2 `require_hub_admin_for(hub_id)` sẽ gọi helper với user_id từ JWT (verified upstream) + hub_id từ URL path (untrusted query param) |
| get_effective_role → Postgres SELECT | Raw SQL named bind params — asyncpg layer escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering (SQL injection) | get_effective_role SQL execution | mitigate | Raw SQL via `sqlalchemy.text()` + named bind params `:user_id`, `:hub_id` (CLAUDE.md stack pin convention); KHÔNG f-string concat user input |
| T-01-02-02 | Information Disclosure | Helper expose existence của user_id | accept | UserNotFoundError raise → Phase 2 dependency wrap thành 403 FORBIDDEN (KHÔNG 404) — KHÔNG leak user_id existence; defer Phase 2 enforcement |
| T-01-02-03 | Elevation of Privilege | Stale user_id token → bypass role check | mitigate | UserNotFoundError raise (KHÔNG silent return string rỗng/role mặc định); caller (Phase 2) phải handle explicit |
| T-01-02-04 | Repudiation | Helper KHÔNG audit | accept | Helper read-only — Phase 2 dependency layer sẽ audit ở `actor_role` payload (DEP-05) khi user thực hiện action; helper KHÔNG cần duplicate audit |
</threat_model>

<verification>
- File `Hub_All/api/app/auth/role.py` tồn tại + import OK qua python -c.
- `get_effective_role` async signature đúng: `(session: AsyncSession, user_id: UUID | str, hub_id: UUID | str) -> str`.
- `UserNotFoundError` subclass Exception, raise đúng case (users row missing).
- File `Hub_All/api/tests/unit/test_role_helper.py` chứa 6 test, pytest exit 0.
- 4-case ROLE-04 cover đầy đủ + 1 defensive + 1 str args = 6 case mạnh hơn yêu cầu 4-case minimum.
- KHÔNG test cần Postgres runtime — pure mock.
</verification>

<success_criteria>
1. Module role.py compile + import OK (python -c smoke).
2. Test pytest 6/6 PASS.
3. Helper signature match D-V3.1-02 LOCKED (per-hub override + inherit global fallback).
4. Raw SQL named bind params (T-01-02-01 mitigation).
5. UserNotFoundError defensive raise (T-01-02-03 mitigation).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/01-rbac-schema-migration/01-02-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- File role.py + test_role_helper.py đã tạo
- 6 test PASS (vượt 4-case minimum ROLE-04)
- Raw SQL named bind params + AsyncMock pattern carry forward
- Helper placement decision: `api/app/auth/role.py` module mới (separation of concerns, KHÔNG extend dependencies.py)
- Phase 2 sẽ import `from app.auth.role import get_effective_role` để build `require_hub_admin_for(hub_id)` (DEP-01)

Commit atomic: `feat(01-02): get_effective_role helper + 6 unit test (ROLE-04)` + body tiếng Việt nói tại sao (per-hub role override + inherit global fallback theo D-V3.1-02 LOCKED; module riêng tách khỏi dependencies.py).
</output>
