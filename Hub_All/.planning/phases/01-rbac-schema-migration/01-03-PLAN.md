---
phase: 01-rbac-schema-migration
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - api/tests/integration/test_migration_0006_idempotent.py
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - CLAUDE.md
autonomous: true
requirements: [ROLE-01, ROLE-02, ROLE-03, ROLE-04]
must_haves:
  truths:
    - "Integration test `test_migration_0006_idempotent.py` PASS: alembic upgrade head 2 lần liên tiếp KHÔNG fail"
    - "Integration test verify CHECK constraint sau upgrade chấp nhận INSERT user role='hub_admin'"
    - "Integration test verify user_hubs.role nullable cho phép INSERT row với role=NULL"
    - "Integration test verify audit_logs có row action='migration.role_seed' (count ≥ 1)"
    - "Integration test fixture monkeypatch DATABASE_URL + clear get_settings cache để env.py pick up TEST_DATABASE_URL (KHÔNG fallback Settings dev/prod)"
    - "STATE.md cập nhật progress Phase 1: completed_plans = 3 + Phase 1 status DONE"
    - "REQUIREMENTS.md mark `[x]` cho ROLE-01, ROLE-02, ROLE-03, ROLE-04"
    - "CLAUDE.md section 6 thêm subsection 'v3.1 Phase 1 RBAC schema (ROLE-01..04)' ghi nhận pattern"
  artifacts:
    - path: "api/tests/integration/test_migration_0006_idempotent.py"
      provides: "Integration test idempotent upgrade/downgrade + CHECK constraint verify + audit log verify"
      contains: "alembic upgrade"
    - path: ".planning/STATE.md"
      provides: "v3.1 milestone progress cập nhật Phase 1 DONE"
      contains: "Phase 1.*DONE"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ROLE-01..04 marked [x]"
      contains: "[x] **ROLE-01**"
    - path: "CLAUDE.md"
      provides: "Phase 1 pattern reference cho v3.1 Phase 2+ developer"
      contains: "v3.1 Phase 1"
  key_links:
    - from: "test_migration_0006_idempotent.py"
      to: "api/migrations/versions/0006_role_hub_admin.py"
      via: "alembic.command.upgrade + alembic.command.downgrade + 2 lần liên tiếp re-run"
      pattern: "command.upgrade.*head.*command.upgrade.*head"
    - from: "test_migration_0006_idempotent.py alembic_cfg fixture"
      to: "api/migrations/env.py runtime DSN resolution"
      via: "monkeypatch DATABASE_URL env + get_settings.cache_clear() — env.py:185-191 đọc Settings.database_url runtime override sqlalchemy.url ⇒ test PHẢI inject env var (KHÔNG dùng set_main_option vì bị override)"
      pattern: "monkeypatch.setenv.*DATABASE_URL.*get_settings.cache_clear"
    - from: ".planning/STATE.md progress"
      to: ".planning/REQUIREMENTS.md ROLE-01..04 [x]"
      via: "Cross-reference REQ-ID consumed count + status DONE"
      pattern: "completed_plans.*3"
---

<objective>
Closeout Phase 1 v3.1 RBAC schema migration — verify idempotent migration runtime + update 3 docs source-of-truth.

**Purpose:** Phase 1 đã tạo migration 0006 (Plan 01-01) + helper get_effective_role (Plan 01-02). Plan 01-03 verify runtime + closeout:
1. Integration test chạy thật Alembic upgrade/downgrade trên DB test → verify idempotent + acceptance criteria 4 success criteria của ROADMAP Phase 1.
2. STATE.md cập nhật progress (1/4 phase DONE).
3. REQUIREMENTS.md mark [x] ROLE-01..04.
4. CLAUDE.md section 6 thêm subsection v3.1 Phase 1 pattern reference (carry forward Phase 2+).

**Pattern carry forward:** Plan 04-07 v3.0 (smoke checkpoint runtime SKIP pre-resolved — defer Phase 7 MIGRATE-05). KHÁC: Phase 1 v3.1 RUN integration test runtime ngay (KHÔNG defer Phase 4 MIGRATE-01) vì migration schema critical + R-V3.1-1 HIGH severity.

**Output:** 1 file test mới + 3 file docs update.

**Note về integration test environment:** Test sẽ dùng pytest fixture `tmp_path` + asyncpg connect trực tiếp tới Postgres test DB (carry forward Phase 4 v3.0 pattern). KHÔNG cần testcontainers (Phase 1 setup chưa có); dùng env var `TEST_DATABASE_URL` override hoặc xài DB local `medinet_test` (nếu chưa có thì test sẽ skip với pytest.skip marker — operator chạy manual `make test-integration` Phase 4 MIGRATE-01).

**SAFETY-CRITICAL — DSN injection pattern (Iter 1 revision fix I-01 + I-02):** `migrations/env.py:185-191` runtime OVERRIDE `sqlalchemy.url` từ `get_settings().database_url` — IGNORE caller's `cfg.set_main_option("sqlalchemy.url", ...)`. Hệ quả nếu fixture set_main_option: test sẽ apply migration vào DB lấy từ Settings (vd `medinet_central` từ `.env`), KHÔNG phải `TEST_DATABASE_URL` → có thể vô tình modify DB dev/prod. **MITIGATION:** fixture monkeypatch env var `DATABASE_URL=<test_db_url>` + `get_settings.cache_clear()` TRƯỚC mỗi `command.upgrade()` / `command.downgrade()` — env.py sẽ re-read Settings và pick up test DSN. Reference pattern: `tests/unit/test_alembic_env_hub_arg.py` (đã test parse_hub_x_arg + resolve_env_database_url qua sys.path inject). Bỏ `sync_url` conversion — env.py xử lý asyncpg URL natively qua `resolve_env_database_url` + `async_engine_from_config`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/CLAUDE.md
@Hub_All/api/migrations/versions/0006_role_hub_admin.py
@Hub_All/api/migrations/env.py
@Hub_All/api/app/config.py
@Hub_All/api/app/auth/role.py
@Hub_All/api/tests/unit/test_role_helper.py
@Hub_All/api/tests/unit/test_alembic_env_hub_arg.py

<interfaces>
<!-- Alembic command API pattern (Python standard) + SAFETY pattern monkeypatch DSN -->

```python
# ❌ SAI — cfg.set_main_option BỊ OVERRIDE bởi env.py:185-191 runtime.
# cfg = Config("alembic.ini")
# cfg.set_main_option("sqlalchemy.url", test_db_url)  # env.py overwrite!
# command.upgrade(cfg, "head")  # → apply vào DB Settings.database_url (vd medinet_central) — SAFETY BUG!

# ✅ ĐÚNG — monkeypatch env var DATABASE_URL + clear get_settings cache.
from alembic.config import Config
from alembic import command

def test_upgrade(monkeypatch, test_db_url):
    # 1. Override env var TRƯỚC get_settings() đọc.
    monkeypatch.setenv("DATABASE_URL", test_db_url)  # format gốc postgresql+asyncpg://...
    # 2. Clear lru_cache để get_settings() re-parse env.
    from app.config import get_settings
    get_settings.cache_clear()
    # 3. Create Config — env.py sẽ đọc Settings.database_url MỚI.
    cfg = Config("alembic.ini")  # path từ Hub_All/api/
    # 4. Run command — env.py:185-191 override sqlalchemy.url = Settings.database_url (test DSN).
    command.upgrade(cfg, "head")
    command.downgrade(cfg, "-1")
    command.upgrade(cfg, "head")  # re-run idempotent verify
```

asyncpg connect pattern test (carry forward 04-04 integration):
```python
import asyncpg
# Note: asyncpg KHÔNG hiểu "postgresql+asyncpg://" prefix — strip prefix khi connect trực tiếp.
asyncpg_dsn = test_db_url.replace("postgresql+asyncpg://", "postgresql://")
conn = await asyncpg.connect(dsn=asyncpg_dsn)
row = await conn.fetchrow("SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname = $1", "ck_users_role_enum")
assert "hub_admin" in row[0]
```

pytest skip pattern khi DB không sẵn:
```python
import pytest
@pytest.fixture
def test_db_url():
    url = os.environ.get("TEST_DATABASE_URL")
    if not url:
        pytest.skip("TEST_DATABASE_URL chưa set — chạy manual với DB test")
    # Format gốc postgresql+asyncpg://... — env.py + Settings expect prefix này.
    return url
```

STATE.md cấu trúc cập nhật (carry forward v3.0 pattern):
```yaml
progress:
  total_phases: 4
  completed_phases: 1  # ← bump từ 0 → 1
  total_plans: 15
  completed_plans: 3   # ← bump từ 0 → 3 (Plan 01-01, 01-02, 01-03)
  percent: 20          # ← recompute (3/15 = 20%)
next_action: "/gsd-discuss-phase 2 — DEP backend RBAC enforcement..."
```

REQUIREMENTS.md mark pattern (search/replace):
- `- [ ] **ROLE-01**` → `- [x] **ROLE-01**`
- `- [ ] **ROLE-02**` → `- [x] **ROLE-02**`
- `- [ ] **ROLE-03**` → `- [x] **ROLE-03**`
- `- [ ] **ROLE-04**` → `- [x] **ROLE-04**`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo integration test test_migration_0006_idempotent.py (SAFETY DSN injection pattern)</name>
  <read_first>
    - Hub_All/api/migrations/versions/0006_role_hub_admin.py (target migration — verify upgrade/downgrade behavior)
    - Hub_All/api/migrations/env.py (**ROOT CAUSE I-01** — lines 185-191 runtime OVERRIDE `sqlalchemy.url` từ `get_settings().database_url`; caller's `set_main_option` BỊ IGNORE)
    - Hub_All/api/app/config.py (`get_settings()` function — verify `@lru_cache` decorator để biết `.cache_clear()` available; line 629-632)
    - Hub_All/api/tests/unit/test_alembic_env_hub_arg.py (**ANALOG PATTERN** — sys.path inject test helpers env.py; reference cho fixture structure)
    - Hub_All/api/tests/unit/test_role_helper.py (Plan 01-02 pattern — pytest async + mock fixture style)
  </read_first>
  <files>api/tests/integration/test_migration_0006_idempotent.py</files>
  <action>
Tạo file `api/tests/integration/test_migration_0006_idempotent.py` với 5 integration test.

**CRITICAL — DSN injection pattern (Iter 1 fix I-01 + I-02):** Fixture `alembic_cfg` PHẢI dùng `monkeypatch.setenv("DATABASE_URL", test_db_url)` + `get_settings.cache_clear()` TRƯỚC khi tạo `Config(...)` — KHÔNG dùng `cfg.set_main_option("sqlalchemy.url", ...)` vì `migrations/env.py:185-191` runtime OVERWRITE từ `get_settings().database_url`. Nếu fixture set_main_option: test sẽ apply migration vào DB lấy từ `.env` (vd `medinet_central`), KHÔNG phải `TEST_DATABASE_URL` → SAFETY BLOCKER (vô tình modify dev/prod DB). Bỏ `sync_url = test_db_url.replace("postgresql+asyncpg://", "postgresql://")` — env.py xử lý asyncpg URL natively (async_engine_from_config). CHỈ asyncpg.connect() trực tiếp cần strip prefix.

```python
"""Integration test migration 0006 idempotent — Phase 1 Plan 01-03 (ROLE-01..04).

Verify 4 success criteria của ROADMAP Phase 1:
1. `alembic upgrade head` 2 lần PASS idempotent; CHECK constraint accept 4 value.
2. user_hubs.role nullable default NULL; existing rows preserved.
3. audit_logs có row action='migration.role_seed' với count + timestamp.
4. `alembic downgrade -1` rollback PASS — restore CHECK 3 value cũ, drop user_hubs.role.

PRE-CONDITION: TEST_DATABASE_URL env var trỏ tới Postgres test DB CÓ schema
0001-0005 đã apply (KHÔNG bao gồm 0006). Test sẽ apply 0006 + verify + rollback.

Nếu TEST_DATABASE_URL không set → pytest.skip ("manual run only" — Phase 4
MIGRATE-01 sẽ chạy đầy đủ qua make test-integration).

SAFETY-CRITICAL DSN injection (Iter 1 revision fix I-01):
- `migrations/env.py:185-191` runtime OVERRIDE `sqlalchemy.url` từ
  `get_settings().database_url` → IGNORE caller's `cfg.set_main_option(...)`.
- Fixture PHẢI monkeypatch env var `DATABASE_URL` + `get_settings.cache_clear()`
  TRƯỚC `command.upgrade()` — env.py re-read Settings và pick up TEST_DATABASE_URL.
- KHÔNG dùng `set_main_option("sqlalchemy.url", ...)` (bị overwrite).
- KHÔNG convert sync_url — env.py xử lý asyncpg natively (async_engine_from_config).
- Reference pattern: tests/unit/test_alembic_env_hub_arg.py (sys.path inject env.py helpers).
"""
from __future__ import annotations

import os
import uuid

import asyncpg
import pytest
from alembic import command
from alembic.config import Config


@pytest.fixture
def test_db_url() -> str:
    """Lấy TEST_DATABASE_URL hoặc skip test.

    Format gốc `postgresql+asyncpg://user:pass@host:5432/medinet_test` — KHÔNG
    convert sang `postgresql://`. env.py + Settings expect prefix asyncpg
    (async_engine_from_config). Chỉ khi asyncpg.connect() trực tiếp mới strip
    prefix (xem helper `_asyncpg_dsn` dưới).
    """
    url = os.environ.get("TEST_DATABASE_URL")
    if not url:
        pytest.skip(
            "TEST_DATABASE_URL chưa set — integration test chỉ chạy manual. "
            "Set TEST_DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/medinet_test "
            "trước khi chạy `pytest tests/integration/test_migration_0006_idempotent.py`. "
            "Phase 4 MIGRATE-01 sẽ chạy bắt buộc qua `make test-integration`."
        )
    return url


def _asyncpg_dsn(test_db_url: str) -> str:
    """Strip `+asyncpg` prefix cho asyncpg.connect() trực tiếp.

    asyncpg KHÔNG hiểu prefix `postgresql+asyncpg://` (SQLAlchemy driver hint).
    Chỉ cần dùng khi connect direct ngoài SQLAlchemy engine (vd verify CHECK
    constraint sau migration). env.py vẫn xài format gốc qua async_engine_from_config.
    """
    return test_db_url.replace("postgresql+asyncpg://", "postgresql://")


@pytest.fixture
def alembic_cfg(test_db_url: str, monkeypatch: pytest.MonkeyPatch) -> Config:
    """Tạo Alembic Config với DSN injection qua monkeypatch env + cache_clear.

    SAFETY-CRITICAL (Iter 1 fix I-01 + I-02):
    - `migrations/env.py:185-191` runtime OVERRIDE `sqlalchemy.url` từ
      `get_settings().database_url`. Caller's `cfg.set_main_option("sqlalchemy.url", ...)`
      BỊ IGNORE → nếu dùng set_main_option, migration apply vào DB lấy từ .env
      (vd `medinet_central`) — SAFETY BLOCKER.
    - Pattern ĐÚNG: monkeypatch.setenv("DATABASE_URL", test_db_url) + clear
      `get_settings` lru_cache → env.py re-read Settings và pick up test DSN.
    - Format DSN giữ nguyên `postgresql+asyncpg://` (KHÔNG convert sync).
    - Reference: tests/unit/test_alembic_env_hub_arg.py (analog pattern test
      env.py helpers).
    """
    # 1. Override env var TRƯỚC khi get_settings() được gọi.
    monkeypatch.setenv("DATABASE_URL", test_db_url)

    # 2. Clear lru_cache get_settings (api/app/config.py:629 @lru_cache decorator).
    #    Settings instance trước đó (boot từ .env) đã cache → cache_clear() force
    #    re-parse env vars mới.
    from app.config import get_settings
    get_settings.cache_clear()

    # 3. Optional cleanup teardown: restore cache state sau test.
    #    monkeypatch.setenv() tự restore env var sau test scope (pytest builtin).
    #    Nhưng lru_cache PHẢI clear lại để test khác KHÔNG kế thừa Settings test DSN.
    def _cleanup_cache() -> None:
        get_settings.cache_clear()

    # pytest finalizer (request-scoped cleanup).
    # Note: monkeypatch fixture là function-scoped → tự revert env vars.
    # Manual cache_clear cuối test để Settings fresh cho next test.
    import atexit
    atexit.register(_cleanup_cache)  # safety net — không rely (test order dependency).

    # 4. Tạo Config — env.py sẽ đọc Settings.database_url MỚI khi load.
    #    alembic.ini ở Hub_All/api/alembic.ini (carry forward 0001-0005 layout).
    cfg_path = os.path.join(os.path.dirname(__file__), "..", "..", "alembic.ini")
    cfg_path = os.path.abspath(cfg_path)
    cfg = Config(cfg_path)
    # KHÔNG set_main_option("sqlalchemy.url", ...) — bị env.py override (I-01 fix).
    return cfg


def _reset_settings_cache() -> None:
    """Helper clear get_settings cache giữa test (defensive — test isolation).

    Gọi sau mỗi command.upgrade()/downgrade() để đảm bảo Settings KHÔNG cache
    state cũ (test_db_url khác giữa các test session hoặc parallel run).
    """
    from app.config import get_settings
    get_settings.cache_clear()


@pytest.mark.asyncio
async def test_upgrade_head_then_check_constraint_accepts_hub_admin(
    test_db_url: str, alembic_cfg: Config,
) -> None:
    """Success criteria 1 — sau upgrade head, CHECK constraint accept 'hub_admin'.

    Steps:
    1. command.upgrade(cfg, 'head') → 0006 apply (DSN inject qua monkeypatch ở fixture).
    2. INSERT 1 user với role='hub_admin' → PHẢI thành công (CHECK pass).
    3. INSERT 1 user với role='invalid' → PHẢI fail (CHECK reject).
    """
    # Fixture đã monkeypatch DATABASE_URL + cache_clear → env.py pick up test DSN.
    command.upgrade(alembic_cfg, "head")

    conn = await asyncpg.connect(dsn=_asyncpg_dsn(test_db_url))
    try:
        # Insert user hub_admin — PHẢI thành công.
        user_id = uuid.uuid4()
        await conn.execute(
            """
            INSERT INTO users (id, email, password_hash, role)
            VALUES ($1, $2, $3, 'hub_admin')
            """,
            user_id, f"hubadmin_{user_id}@test.local", "dummy_hash",
        )

        # Verify row inserted với role='hub_admin'.
        row = await conn.fetchrow(
            "SELECT role FROM users WHERE id = $1", user_id,
        )
        assert row is not None
        assert row["role"] == "hub_admin"

        # Insert user role='invalid' — PHẢI fail CHECK constraint.
        with pytest.raises(asyncpg.CheckViolationError):
            await conn.execute(
                """
                INSERT INTO users (id, email, password_hash, role)
                VALUES ($1, $2, $3, 'invalid_role')
                """,
                uuid.uuid4(), f"invalid_{uuid.uuid4()}@test.local", "dummy_hash",
            )
    finally:
        # Cleanup test users — defensive (KHÔNG leak test data).
        await conn.execute(
            "DELETE FROM users WHERE email LIKE 'hubadmin_%@test.local' OR email LIKE 'invalid_%@test.local'"
        )
        await conn.close()


@pytest.mark.asyncio
async def test_upgrade_head_idempotent_runs_twice_without_error(
    test_db_url: str, alembic_cfg: Config,
) -> None:
    """Success criteria 1 (idempotent) — re-run upgrade head 2 lần KHÔNG fail.

    Steps:
    1. command.upgrade(cfg, 'head') → 0006 apply (lần 1; fixture DSN inject).
    2. _reset_settings_cache() giữa 2 lần để env.py re-read Settings (defensive).
    3. command.upgrade(cfg, 'head') → 0006 SKIP via introspect guard (lần 2 no-op).
    Cả 2 lần KHÔNG raise exception.

    Note: fixture đã monkeypatch + cache_clear lần đầu. Giữa 2 command.upgrade
    Settings có thể cached lại (lru_cache) — defensive clear thêm.
    """
    command.upgrade(alembic_cfg, "head")
    # Defensive — clear cache giữa 2 upgrade để env.py re-read Settings fresh.
    _reset_settings_cache()
    # Re-run — introspect guard ở 3 STEP của 0006 PHẢI skip toàn bộ.
    command.upgrade(alembic_cfg, "head")
    # Nếu không raise → PASS.


@pytest.mark.asyncio
async def test_user_hubs_role_nullable_accepts_null_and_value(
    test_db_url: str, alembic_cfg: Config,
) -> None:
    """Success criteria 2 — user_hubs.role nullable + NULL-aware CHECK + value CHECK.

    Steps:
    1. Tạo user + hub fixture.
    2. INSERT user_hubs row với role=NULL → PASS.
    3. INSERT user_hubs row với role='hub_admin' → PASS.
    4. INSERT user_hubs row với role='invalid' → FAIL CHECK.
    """
    # Fixture đã monkeypatch DATABASE_URL — env.py pick up test DSN.
    command.upgrade(alembic_cfg, "head")
    conn = await asyncpg.connect(dsn=_asyncpg_dsn(test_db_url))
    try:
        # Setup user + hub.
        user_id = uuid.uuid4()
        hub_id = uuid.uuid4()
        await conn.execute(
            "INSERT INTO users (id, email, password_hash, role) VALUES ($1, $2, $3, 'viewer')",
            user_id, f"role_test_{user_id}@test.local", "dummy_hash",
        )
        await conn.execute(
            "INSERT INTO hubs (id, slug, name) VALUES ($1, $2, $3)",
            hub_id, f"test-hub-{hub_id}", f"Test Hub {hub_id}",
        )

        # INSERT user_hubs role=NULL — inherit global.
        await conn.execute(
            "INSERT INTO user_hubs (user_id, hub_id, role) VALUES ($1, $2, NULL)",
            user_id, hub_id,
        )
        row = await conn.fetchrow(
            "SELECT role FROM user_hubs WHERE user_id = $1 AND hub_id = $2",
            user_id, hub_id,
        )
        assert row is not None
        assert row["role"] is None  # NULL preserved.

        # UPDATE role='hub_admin' — per-hub override.
        await conn.execute(
            "UPDATE user_hubs SET role = 'hub_admin' WHERE user_id = $1 AND hub_id = $2",
            user_id, hub_id,
        )
        row = await conn.fetchrow(
            "SELECT role FROM user_hubs WHERE user_id = $1 AND hub_id = $2",
            user_id, hub_id,
        )
        assert row["role"] == "hub_admin"

        # UPDATE role='invalid' — FAIL CHECK.
        with pytest.raises(asyncpg.CheckViolationError):
            await conn.execute(
                "UPDATE user_hubs SET role = 'invalid' WHERE user_id = $1 AND hub_id = $2",
                user_id, hub_id,
            )
    finally:
        # Cleanup.
        await conn.execute(
            "DELETE FROM users WHERE email LIKE 'role_test_%@test.local'"
        )
        await conn.execute(
            "DELETE FROM hubs WHERE slug LIKE 'test-hub-%'"
        )
        await conn.close()


@pytest.mark.asyncio
async def test_audit_logs_seed_row_inserted_after_upgrade(
    test_db_url: str, alembic_cfg: Config,
) -> None:
    """Success criteria 3 — audit_logs có row action='migration.role_seed'.

    Verify:
    - Row tồn tại với action='migration.role_seed'.
    - payload chứa 'migration_revision' = '0006'.
    - payload chứa 'admin_count' (≥ 0 — có thể 0 nếu DB test rỗng).
    - payload chứa 'timestamp_utc' (string).
    """
    # Fixture monkeypatch DATABASE_URL — env.py pick up test DSN.
    command.upgrade(alembic_cfg, "head")
    conn = await asyncpg.connect(dsn=_asyncpg_dsn(test_db_url))
    try:
        row = await conn.fetchrow(
            """
            SELECT payload
            FROM audit_logs
            WHERE action = 'migration.role_seed'
              AND payload->>'migration_revision' = '0006'
            ORDER BY created_at DESC
            LIMIT 1
            """,
        )
        assert row is not None, "Migration seed row KHÔNG tồn tại sau upgrade"
        import json
        payload = json.loads(row["payload"]) if isinstance(row["payload"], str) else row["payload"]
        assert payload.get("migration_revision") == "0006"
        assert "admin_count" in payload
        assert "user_hubs_count" in payload
        assert "timestamp_utc" in payload
    finally:
        await conn.close()


@pytest.mark.asyncio
async def test_downgrade_restores_check_3_value_and_drops_user_hubs_role(
    test_db_url: str, alembic_cfg: Config,
) -> None:
    """Success criteria 4 — downgrade -1 rollback PASS, restore CHECK 3 value + drop column.

    Steps:
    1. Upgrade head (0006 apply; fixture monkeypatch DSN).
    2. Verify user_hubs.role column tồn tại + CHECK accept 'hub_admin'.
    3. _reset_settings_cache() defensive trước downgrade (giữ DSN test fresh).
    4. Downgrade -1 (rollback 0006 → 0005; env.py re-read Settings).
    5. Verify user_hubs.role column DROPPED.
    6. Verify CHECK constraint users.role REJECT 'hub_admin' (3 value cũ).
    7. _reset_settings_cache() trước restore head (final state cho test order).
    8. Restore head state (KHÔNG để DB ở 0005 sau test).
    """
    # Setup state — apply head trước (fixture monkeypatch DSN).
    command.upgrade(alembic_cfg, "head")

    # Defensive — clear cache trước downgrade để env.py re-read Settings fresh.
    _reset_settings_cache()
    command.downgrade(alembic_cfg, "-1")

    conn = await asyncpg.connect(dsn=_asyncpg_dsn(test_db_url))
    try:
        # Verify user_hubs.role DROPPED.
        col_row = await conn.fetchrow(
            """
            SELECT column_name FROM information_schema.columns
            WHERE table_name = 'user_hubs' AND column_name = 'role'
            """,
        )
        assert col_row is None, "user_hubs.role column KHÔNG bị drop sau downgrade"

        # Verify CHECK constraint users.role REJECT 'hub_admin'.
        with pytest.raises(asyncpg.CheckViolationError):
            await conn.execute(
                """
                INSERT INTO users (id, email, password_hash, role)
                VALUES ($1, $2, $3, 'hub_admin')
                """,
                uuid.uuid4(), f"downgrade_test_{uuid.uuid4()}@test.local", "dummy_hash",
            )
    finally:
        # Cleanup + restore head cho test khác.
        await conn.execute(
            "DELETE FROM users WHERE email LIKE 'downgrade_test_%@test.local'"
        )
        await conn.close()
        # Defensive clear trước restore head — env.py re-read Settings.
        _reset_settings_cache()
        # Restore head state (KHÔNG để DB ở downgrade state — tránh phá test sau).
        command.upgrade(alembic_cfg, "head")
```

**Lưu ý:**
- Test có thể skip nếu TEST_DATABASE_URL chưa set — operator chạy manual lúc cần (Phase 4 MIGRATE-01 sẽ chạy bắt buộc).
- 5 test PASS = đủ verify 4 success criteria ROADMAP Phase 1.
- Cleanup defensive (DELETE WHERE email LIKE '...test.local') tránh leak test data.
- Last test restore head state (KHÔNG để DB ở downgrade state — tránh phá test sau).
- `asyncpg.CheckViolationError` là exception specific (KHÔNG dùng generic `asyncpg.PostgresError`).
- **DSN injection (Iter 1 fix I-01 + I-02):** Fixture monkeypatch `DATABASE_URL` env var + `get_settings.cache_clear()` thay vì `cfg.set_main_option("sqlalchemy.url", ...)`. Lý do: `migrations/env.py:185-191` OVERRIDE sqlalchemy.url runtime từ `get_settings().database_url` → caller's set_main_option BỊ IGNORE. Nếu fixture cũ: test sẽ apply vào DB Settings (vd medinet_central .env) — SAFETY BUG.
- **Format DSN:** Giữ nguyên `postgresql+asyncpg://` cho env.py + Settings (async_engine_from_config). Chỉ `_asyncpg_dsn()` helper strip prefix khi asyncpg.connect() trực tiếp (asyncpg KHÔNG hiểu SQLAlchemy driver hint).
- **Pattern reference:** `tests/unit/test_alembic_env_hub_arg.py` test env.py helpers qua sys.path inject — analog cho lý do tại sao phải monkeypatch env (env.py KHÔNG phải module Python chuẩn — Alembic load qua exec_module + runtime DSN injection).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -c "import ast; ast.parse(open('tests/integration/test_migration_0006_idempotent.py').read()); print('syntax OK')" && grep -c "async def test_" Hub_All/api/tests/integration/test_migration_0006_idempotent.py && grep -c "monkeypatch.setenv(\"DATABASE_URL\"" Hub_All/api/tests/integration/test_migration_0006_idempotent.py && grep -c "get_settings.cache_clear()" Hub_All/api/tests/integration/test_migration_0006_idempotent.py</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_migration_0006_idempotent.py` EXISTS.
    - File chứa ≥ 5 `async def test_` function (verify qua grep).
    - File chứa `command.upgrade(alembic_cfg, "head")` ≥ 2 lần (test idempotent re-run).
    - File chứa `command.downgrade(alembic_cfg, "-1")` (test rollback).
    - File chứa `asyncpg.CheckViolationError` (CHECK constraint verify).
    - File chứa `migration.role_seed` (audit seed verify).
    - File chứa `pytest.skip` cho fixture `test_db_url` (skip-if-no-DB pattern).
    - File chứa `information_schema.columns` (column drop verify post-downgrade).
    - **[Iter 1 fix I-01]** File chứa `monkeypatch.setenv("DATABASE_URL"` ≥ 1 lần (fixture inject env var thay vì set_main_option).
    - **[Iter 1 fix I-01]** File chứa `get_settings.cache_clear()` ≥ 2 lần (fixture init + defensive between upgrade/downgrade).
    - **[Iter 1 fix I-01]** File KHÔNG chứa `cfg.set_main_option("sqlalchemy.url"` (anti-pattern bị env.py override).
    - **[Iter 1 fix I-02]** File KHÔNG chứa `sync_url = test_db_url.replace("postgresql+asyncpg://", "postgresql://")` ở level fixture (chỉ helper `_asyncpg_dsn()` cho asyncpg.connect direct).
    - **[Iter 1 fix I-02]** File chứa `from app.config import get_settings` (import để clear cache).
    - File chứa comment reference `migrations/env.py:185-191` HOẶC `Iter 1 fix I-01` (traceability cho future reviewer hiểu pattern).
    - `python -c "import ast; ast.parse(open('Hub_All/api/tests/integration/test_migration_0006_idempotent.py').read())"` exit 0.
  </acceptance_criteria>
  <done>
    Integration test file tồn tại với 5 test cover 4 success criteria Phase 1; skip-if-no-DB pattern; **fixture DSN injection qua monkeypatch DATABASE_URL + get_settings.cache_clear() (Iter 1 fix I-01 SAFETY BLOCKER)**; KHÔNG dùng cfg.set_main_option bị env.py override; KHÔNG convert sync_url ở level fixture (Iter 1 fix I-02); syntax Python hợp lệ.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật STATE.md + REQUIREMENTS.md + CLAUDE.md (closeout docs)</name>
  <read_first>
    - .planning/STATE.md (toàn bộ — biết section nào cần update progress + next_action)
    - .planning/REQUIREMENTS.md (tìm 4 dòng `- [ ] **ROLE-01..04**` → mark `[x]`)
    - CLAUDE.md (section 6 cuối file — append subsection v3.1 Phase 1)
    - Hub_All/.planning/phases/01-rbac-schema-migration/01-01-PLAN.md (tham chiếu artifact list)
    - Hub_All/.planning/phases/01-rbac-schema-migration/01-02-PLAN.md (tham chiếu artifact list)
  </read_first>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, CLAUDE.md</files>
  <action>
**Update 1: `Hub_All/.planning/STATE.md`** — cập nhật frontmatter + body progress:

Trong YAML frontmatter (đầu file):
```yaml
last_updated: "2026-05-23T18:00:00.000Z"  # ← cập nhật timestamp ship Phase 1
progress:
  total_phases: 4
  completed_phases: 1               # ← bump 0 → 1
  total_plans: 15
  completed_plans: 3                # ← bump 0 → 3 (Plan 01-01 + 01-02 + 01-03)
  percent: 20                       # ← (3 / 15) * 100 = 20
next_action: "/gsd-discuss-phase 2 — DEP backend RBAC enforcement (require_hub_admin_for dependency + GET /api/hubs filter + CRUD scope)"
```

Trong body, section "## v3.1 Planning Summary" table — bump Phase 1 row status:
```
| 1 | DB schema | role_enum mở rộng + user_hubs.role column + helper + seed migration | ROLE-01..04 (4) | 3-4 | ✅ DONE 2026-05-23 (3 plan) |
```

THÊM section MỚI sau "## Key Decisions (LOCKED 2026-05-23)" (trước "## Open Question"):

```markdown
## Phase 1 Results Summary (DONE 2026-05-23)

3 plan ship 4 REQ-ID ROLE-01..04:

- **Plan 01-01** (ROLE-01 + ROLE-02 + ROLE-03): Alembic migration `0006_role_hub_admin.py` mở rộng CHECK constraint `users.role` thêm `'hub_admin'` (4 value) + thêm column `user_hubs.role TEXT NULL` (per-hub override; NULL = inherit global) + audit_logs INSERT row `action='migration.role_seed'` với jsonb_build_object payload (migration_revision + admin_count + user_hubs_count + timestamp_utc). Idempotent qua introspect (`sa.inspect()`) — 3 STEP có guard skip-if-applied. downgrade() đầy đủ với defensive `RuntimeError` nếu có row `role='hub_admin'` tồn tại (E-V3.1-1 STOP trigger).
- **Plan 01-02** (ROLE-04): Module mới `api/app/auth/role.py` chứa `async def get_effective_role(session, user_id, hub_id) -> str` + `UserNotFoundError` defensive exception. Logic: SELECT user_hubs.role override → fallback users.role inherit global. Raw SQL via `sqlalchemy.text()` + named bind params (T-01-02-01 SQL injection mitigation). 6 unit test pytest PASS (4-case ROLE-04 + 1 defensive + 1 str args coverage). Phase 2 sẽ import để build `require_hub_admin_for(hub_id)` dependency (DEP-01).
- **Plan 01-03** (closeout): Integration test `tests/integration/test_migration_0006_idempotent.py` (5 test) cover 4 success criteria ROADMAP Phase 1 — runtime skip-if-no-DB pattern (Phase 4 MIGRATE-01 sẽ chạy bắt buộc). **SAFETY-CRITICAL DSN injection pattern (Iter 1 revision fix I-01 + I-02):** fixture monkeypatch `DATABASE_URL` env + `get_settings.cache_clear()` thay vì `cfg.set_main_option("sqlalchemy.url", ...)` (bị env.py:185-191 runtime OVERRIDE từ get_settings().database_url → caller's set_main_option BỊ IGNORE → test apply migration vào DB .env vd medinet_central, SAFETY BLOCKER). STATE.md + REQUIREMENTS.md + CLAUDE.md update.

**Carry forward patterns:**
- Alembic explicit raw SQL `op.execute()` + introspect guard (pattern Plan 04-01 v3.0).
- Audit_logs payload nest (KHÔNG migration column mới) — D-V3-Phase4-C2 v3.0 carry forward.
- Module separation: `api/app/auth/role.py` business logic role thuần; `dependencies.py` FastAPI wrapper (Phase 2 sẽ extend).
- Tên CHECK constraint introspect runtime (chống model/migration discrepancy `ck_users_role_enum` vs `role_enum`).
- **Alembic integration test DSN injection pattern (Iter 1 fix I-01):** monkeypatch env `DATABASE_URL` + `get_settings.cache_clear()` thay vì `Config.set_main_option("sqlalchemy.url", ...)` — bypass env.py:185-191 runtime override. Áp dụng cho mọi integration test Alembic Phase 4+ MIGRATE-01.
```

**Update 2: `Hub_All/.planning/REQUIREMENTS.md`** — mark 4 REQ:

Tìm 4 dòng:
```markdown
- [ ] **ROLE-01** Phase 1: Mở rộng CHECK constraint...
- [ ] **ROLE-02** Phase 1: Thêm column user_hubs.role...
- [ ] **ROLE-03** Phase 1: Migration script seed...
- [ ] **ROLE-04** Phase 1: Helper function get_effective_role...
```

→ Đổi `[ ]` → `[x]` cho cả 4 dòng. Append cuối mỗi dòng ` (DONE 2026-05-23 — Plan 01-XX)`:
```markdown
- [x] **ROLE-01** Phase 1: Mở rộng CHECK constraint... (DONE 2026-05-23 — Plan 01-01)
- [x] **ROLE-02** Phase 1: Thêm column user_hubs.role... (DONE 2026-05-23 — Plan 01-01)
- [x] **ROLE-03** Phase 1: Migration script seed... (DONE 2026-05-23 — Plan 01-01)
- [x] **ROLE-04** Phase 1: Helper function get_effective_role... (DONE 2026-05-23 — Plan 01-02)
```

**Update 3: `Hub_All/CLAUDE.md`** — append cuối section 6 subsection mới:

Tìm cuối file (sau section "Phase 4 Cross-hub Data Sync pattern..." block) — append subsection mới:

```markdown
### Phase 1 v3.1 RBAC schema pattern (ROLE-01..04 — 2026-05-23)

3 plan đóng 4 REQ-ID ROLE schema migration + helper:

- **Plan 01-01 ROLE-01 + ROLE-02 + ROLE-03 — Alembic migration 0006_role_hub_admin.py:** CHECK constraint `users.role` mở rộng 4 value (admin|hub_admin|editor|viewer) qua DROP + ADD raw SQL với introspect `sa.inspect().get_check_constraints("users")` tìm tên constraint thật (chống discrepancy `ck_users_role_enum` migration 0001 vs `role_enum` model auth.py). user_hubs.role TEXT NULL DEFAULT NULL + CHECK NULL-aware `(role IS NULL OR role IN (...))` per-hub override (D-V3.1-02 LOCKED). audit_logs INSERT seed `action='migration.role_seed'` với jsonb_build_object payload (migration_revision + admin_count + user_hubs_count + timestamp_utc) + WHERE NOT EXISTS idempotent guard. downgrade() defensive RuntimeError nếu COUNT(*) role='hub_admin' > 0 (E-V3.1-1 STOP). Reference: `.planning/phases/01-rbac-schema-migration/01-01-PLAN.md` + `01-01-SUMMARY.md`.

- **Plan 01-02 ROLE-04 — Module mới api/app/auth/role.py:** `async def get_effective_role(session: AsyncSession, user_id: UUID | str, hub_id: UUID | str) -> str` + `UserNotFoundError(Exception)`. Logic: SELECT user_hubs.role WHERE (user, hub) → non-NULL return (override); NULL hoặc no-row fallthrough → SELECT users.role return (inherit global); no user → UserNotFoundError raise. Raw SQL via `sqlalchemy.text()` + named bind params `:user_id`/`:hub_id` (T-01-02-01 SQL injection mitigation; CLAUDE.md stack pin). KHÔNG import `User`/`UserHub` ORM model (chống cycle Phase 2). Test pytest 6 case + AsyncMock — KHÔNG cần Postgres runtime. Phase 2 import qua `from app.auth.role import get_effective_role` build `require_hub_admin_for(hub_id)` (DEP-01). Reference: `01-02-PLAN.md` + `01-02-SUMMARY.md`.

- **Plan 01-03 closeout — Integration test + docs:** `tests/integration/test_migration_0006_idempotent.py` 5 test cover 4 success criteria ROADMAP Phase 1: (1) CHECK accept hub_admin sau upgrade; (2) re-run upgrade head idempotent KHÔNG fail; (3) user_hubs.role nullable + NULL-aware CHECK; (4) audit_logs seed row tồn tại; (5) downgrade -1 restore CHECK 3 value + drop column. Skip-if-no-DB pattern (`TEST_DATABASE_URL` env var); Phase 4 MIGRATE-01 sẽ chạy bắt buộc. **SAFETY-CRITICAL DSN injection (Iter 1 revision fix I-01 + I-02):** fixture monkeypatch `DATABASE_URL` env + `get_settings.cache_clear()` thay vì `cfg.set_main_option("sqlalchemy.url", ...)` — env.py:185-191 runtime OVERRIDE sqlalchemy.url từ get_settings().database_url, IGNORE caller's set_main_option → nếu dùng set_main_option, test apply migration vào DB .env (vd medinet_central) thay vì TEST_DATABASE_URL (SAFETY BLOCKER). Format DSN giữ `postgresql+asyncpg://` (env.py xử lý async natively); chỉ helper `_asyncpg_dsn()` strip prefix khi asyncpg.connect() direct. STATE.md + REQUIREMENTS.md + CLAUDE.md update. Reference: `01-03-PLAN.md` + `01-03-SUMMARY.md`.

**Backward compat (Phase 1 KHÔNG break v3.0):**
- Schema 0001-0005 carry forward unchanged — chỉ EXTEND CHECK constraint + ADD COLUMN nullable (zero data migration).
- Existing user `role='admin'` GIỮ semantic super-admin (D-V3.1-01 LOCKED — KHÔNG rename `super_admin`).
- M2 + v3.0 JWT claim schema KHÔNG đụng (defer Phase 2-3 cho `actor_role` payload nest, KHÔNG schema migration).
- Frontend hiện tại tiếp tục dùng `users.role` global cho tới Phase 3 FE-04 type extend.

**R-V3.1-1 HIGH mitigation chain Phase 1:**
- Idempotent introspect ở 3 STEP upgrade() — re-run safety (Plan 01-01 + GA-V3.1-C).
- downgrade() đầy đủ với defensive RuntimeError chặn rollback unsafe khi có row `role='hub_admin'` (Plan 01-01 + E-V3.1-1).
- Integration test 5 case verify cả upgrade idempotent + downgrade rollback (Plan 01-03).
- **Integration test DSN injection SAFETY pattern (Iter 1 fix I-01):** monkeypatch env var thay vì set_main_option — chống test vô tình apply migration vào DB dev/prod (env.py:185-191 override sqlalchemy.url runtime).
- Phase 4 MIGRATE-01 sẽ verify runtime full E2E với DB live + 4 hub scenario.

**Next:** Phase 2 `/gsd-discuss-phase 2` DEP backend RBAC enforcement (require_hub_admin_for + GET /api/hubs filter + CRUD scope).
```

**Lưu ý:**
- 3 file update atomic trong cùng 1 commit (Plan 01-03 closeout).
- KHÔNG đụng các section khác của STATE.md / REQUIREMENTS.md / CLAUDE.md (preserve M2 + v3.0 content nguyên).
- Tiếng Việt có dấu cho mọi description text (CLAUDE.md convention).
  </action>
  <verify>
    <automated>grep -c "completed_plans: 3" Hub_All/.planning/STATE.md && grep -c "Phase 1 Results Summary" Hub_All/.planning/STATE.md && grep -c "\[x\] \*\*ROLE-01\*\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\[x\] \*\*ROLE-04\*\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "Phase 1 v3.1 RBAC schema pattern" Hub_All/CLAUDE.md
  </automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter chua "completed_plans: 3" (bumped tu 0).
    - STATE.md body chua section "## Phase 1 Results Summary (DONE 2026-05-23)" voi 3 bullet plan.
    - STATE.md table v3.1 Planning Summary row Phase 1 chua "DONE 2026-05-23".
    - STATE.md Phase 1 Results Summary co bullet Plan 01-03 mention "Iter 1 revision fix I-01" (traceability checker round).
    - REQUIREMENTS.md co 4 dong "- [x] **ROLE-XX**" (X = 01, 02, 03, 04).
    - REQUIREMENTS.md moi dong ROLE da mark co suffix "(DONE 2026-05-23 — Plan 01-XX)".
    - CLAUDE.md chua subsection "### Phase 1 v3.1 RBAC schema pattern (ROLE-01..04 — 2026-05-23)".
    - CLAUDE.md subsection moi chua 3 bullet plan (01-01, 01-02, 01-03) + Backward compat + R-V3.1-1 mitigation + Next pointer.
    - CLAUDE.md subsection moi chua mention "Iter 1 revision fix I-01" trong bullet Plan 01-03 va R-V3.1-1 mitigation chain (DSN injection SAFETY pattern doc).
    - CLAUDE.md subsection moi KHONG de len section Phase 4 Cross-hub Data Sync v3.0 (preserve).
  </acceptance_criteria>
  <done>
    3 file docs update atomic (STATE.md + REQUIREMENTS.md + CLAUDE.md); progress reflect Phase 1 DONE 3/15 plan; 4 REQ-ID ROLE-01..04 marked [x]; CLAUDE.md co pattern reference cho Phase 2+ INCLUDING Iter 1 fix I-01 SAFETY pattern (DSN injection monkeypatch env + cache_clear) cho future integration test Alembic.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test runner -> Postgres test DB | Test chay alembic upgrade/downgrade DDL — privileged operation; isolated toi DB test KHONG anh huong production |
| Docs update (STATE.md / REQUIREMENTS.md / CLAUDE.md) | Local repo files — KHONG co trust boundary external |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | Integration test pha DB test schema | mitigate | Cleanup defensive trong fixture finally (DELETE WHERE email LIKE test pattern); last test restore head state |
| T-01-03-02 | Repudiation | Docs update khong trace ai update | accept | Git commit history du trace (commit prefix "feat(01-03): closeout Phase 1") |
| T-01-03-03 | DoS | Test chay voi DB production accidentally | mitigate | **[Iter 1 fix I-01 SAFETY BLOCKER]** Fixture monkeypatch `DATABASE_URL` env + `get_settings.cache_clear()` thay vi `cfg.set_main_option("sqlalchemy.url", ...)` (bi env.py:185-191 OVERRIDE tu get_settings().database_url → caller's set_main_option BỊ IGNORE → test apply migration vao DB .env vd medinet_central). TEST_DATABASE_URL env var REQUIRED (pytest.skip neu unset); KHONG hardcode production DSN. |
| T-01-03-04 | Integrity | STATE.md / REQUIREMENTS.md merge conflict voi parallel plan | accept | Plan 01-03 o wave 2 (sequential sau Plan 01-01 + 01-02); KHONG co parallel writer cung file |
| T-01-03-05 | Tampering | Test vo tinh apply migration vao DB dev/prod do env.py runtime override sqlalchemy.url | mitigate | **[Iter 1 fix I-01 SAFETY BLOCKER]** Fixture pattern `monkeypatch.setenv("DATABASE_URL", test_db_url)` + `from app.config import get_settings; get_settings.cache_clear()` TRƯỚC moi `command.upgrade()` / `command.downgrade()` — env.py:185-191 doc Settings.database_url MOI (re-parse env vi lru_cache cleared) thay vi cached .env value. Acceptance criteria verify qua grep `monkeypatch.setenv("DATABASE_URL"` + `get_settings.cache_clear()` present trong test file. |
</threat_model>

<verification>
- Integration test file syntax Python hop le + 5 test pattern.
- **[Iter 1 fix I-01 + I-02]** Fixture pattern monkeypatch DATABASE_URL + get_settings.cache_clear() present (grep verify); KHONG dung cfg.set_main_option("sqlalchemy.url"); KHONG convert sync_url o level fixture.
- STATE.md: completed_plans=3, Phase 1 DONE, Results Summary section moi + Iter 1 fix mention.
- REQUIREMENTS.md: 4 dong "[x] **ROLE-XX**".
- CLAUDE.md: subsection "### Phase 1 v3.1 RBAC schema pattern" append cuoi section 6 + Iter 1 SAFETY pattern doc.
- KHONG de len section khac (M2 + v3.0 content preserve).
</verification>

<success_criteria>
1. test_migration_0006_idempotent.py co 5 test, syntax hop le, skip-if-no-DB.
2. **[Iter 1 fix I-01]** Fixture DSN injection qua monkeypatch DATABASE_URL + get_settings.cache_clear() (KHONG set_main_option bi env.py override).
3. **[Iter 1 fix I-02]** Format DSN giu `postgresql+asyncpg://` o fixture (chi helper `_asyncpg_dsn` strip prefix cho asyncpg.connect direct).
4. STATE.md progress phan anh Phase 1 DONE 3/15 plan.
5. REQUIREMENTS.md mark 4 REQ-ID ROLE-01..04 [x] + suffix Plan.
6. CLAUDE.md subsection v3.1 Phase 1 pattern reference day du + Iter 1 SAFETY pattern doc.
7. Phase 2 san sang start qua "/gsd-discuss-phase 2".
</success_criteria>

<output>
Sau khi hoan thanh, tao "Hub_All/.planning/phases/01-rbac-schema-migration/01-03-SUMMARY.md" (tieng Viet co dau) ghi nhan:
- Integration test 5 case (skip-if-no-DB pattern)
- **[Iter 1 revision fix I-01 + I-02]** Fixture DSN injection qua monkeypatch DATABASE_URL env + get_settings.cache_clear() (bypass env.py:185-191 runtime override) — SAFETY BLOCKER mitigation chong test apply migration vao DB dev/prod
- 3 docs file update atomic (STATE.md progress + REQUIREMENTS.md 4 REQ [x] + CLAUDE.md subsection moi)
- Phase 1 DONE 100% — 3 plan ship 4 REQ-ID ROLE-01..04
- Carry forward Plan 04-07 v3.0 closeout pattern (KHAC: Phase 1 RUN integration test ngay thay vi defer)
- Phase 2 "/gsd-discuss-phase 2" DEP backend RBAC enforcement (require_hub_admin_for + GET /api/hubs filter + CRUD scope)

Commit atomic: "chore(01-03): closeout Phase 1 v3.1 ROLE schema migration" + body tieng Viet noi tai sao (integration test verify idempotent + DSN injection SAFETY pattern Iter 1 fix I-01 + 3 docs update + Phase 1 DONE 4 REQ-ID).
</output>
</content>
</invoke>