---
plan: 03
phase: 2
wave: 3
depends_on: [01, 02]
files_modified:
  - Hub_All/api/alembic.ini
  - Hub_All/api/migrations/__init__.py
  - Hub_All/api/migrations/env.py
  - Hub_All/api/migrations/script.py.mako
  - Hub_All/api/migrations/versions/.gitkeep
autonomous: true
requirements: [CORE-02]
---

# Plan 03: Alembic init + async env.py + `include_object` filter cho schema `cocoindex` (P7) + drift-detection config (P20)

## Objective
Khởi tạo Alembic 1.18.4 với async template trong `Hub_All/api/migrations/`, configure `env.py` để: (a) đọc DSN từ `app.config.get_settings()` thay vì hardcode trong `alembic.ini`; (b) `target_metadata = Base.metadata` (import từ Plan 01 + Plan 02); (c) `include_object` callback exclude bất kỳ object thuộc schema `cocoindex` (P7 mitigation); (d) `compare_type=True` + `compare_server_default=True` cho autogenerate detect drift chính xác (P20 mitigation). Đây là foundation cho Plan 04 (sinh migration script).

**Tại sao plan riêng:** Alembic config tách rời khỏi việc viết migration thực — env.py là static infrastructure chạy MỌI lần `alembic upgrade/downgrade/check`. Sai env.py = mọi migration sau đều sai. Plan 04 tập trung 100% vào schema content.

## Must-Haves

**Truths (4 mục):**
- `cd Hub_All/api && uv run alembic --help` exits 0 (alembic CLI hoạt động trong project context).
- `cd Hub_All/api && uv run alembic current` exits 0 trên DB rỗng — trả `(empty)` (chưa có version nào, đúng Plan 03 chưa có migration).
- `cd Hub_All/api && uv run alembic check` không crash khi DB chưa migrate (Plan 04 sẽ là người chạy first migration).
- Alembic autogenerate (Plan 04) sẽ pick up đủ 10 table từ `app.models.*` qua `target_metadata`.

**Artifacts (5 file):**
- `Hub_All/api/alembic.ini` — config file (script_location, file_template, logging — KHÔNG hardcode sqlalchemy.url, env.py đọc từ settings).
- `Hub_All/api/migrations/__init__.py` — empty package marker.
- `Hub_All/api/migrations/env.py` — async runtime (`run_migrations_online` dùng `async_engine_from_config`); import `Base.metadata`; bake `include_object` + `compare_type` + `compare_server_default`.
- `Hub_All/api/migrations/script.py.mako` — template cho migration files (chuẩn Alembic).
- `Hub_All/api/migrations/versions/.gitkeep` — keep directory dù chưa có version (Git track).

**Key links (3 connection):**
- `env.py` → `app.config.get_settings()` — pattern: lấy DSN runtime, KHÔNG đụng `alembic.ini` (cho phép DSN khác giữa dev/test/prod).
- `env.py` → `app.db.base.Base` + `app.models.*` — pattern: `from app.db.base import Base; from app.models import *; target_metadata = Base.metadata`.
- `env.py.include_object` → exclude `cocoindex` schema — pattern: `if hasattr(obj, 'schema') and obj.schema == 'cocoindex': return False` (P7 mitigation).

## Tasks

<task id="01" tdd="false">
<action>
Tạo `Hub_All/api/alembic.ini` — config tối giản, KHÔNG hardcode `sqlalchemy.url` (env.py đọc từ settings).

**Quan trọng:**
- `script_location = migrations` (relative tới `alembic.ini` location, tức `Hub_All/api/migrations/`).
- `file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(hour).2d%%(minute).2d-%%(rev)s_%%(slug)s` — file migration có timestamp + rev + slug dễ đọc.
- KHÔNG set `sqlalchemy.url` — env.py sẽ inject runtime qua `config.set_main_option('sqlalchemy.url', settings.database_url)`.
- Logging section copy chuẩn Alembic template.

Nội dung paste-ready:

```ini
# Alembic configuration cho Medinet Wiki API M2
# Tham chiếu: https://alembic.sqlalchemy.org/en/latest/tutorial.html
#
# LƯU Ý: sqlalchemy.url KHÔNG set ở đây — env.py runtime đọc từ
# app.config.get_settings().database_url để mỗi env (dev/test/prod) dùng DSN
# riêng mà không cần sửa file ini.

[alembic]
script_location = migrations
file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(hour).2d%%(minute).2d-%%(rev)s_%%(slug)s
prepend_sys_path = .
version_path_separator = os
output_encoding = utf-8
truncate_slug_length = 40

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
```
</action>
<read_first>
- Hub_All/api/pyproject.toml
- Hub_All/.planning/research/STACK.md
- Hub_All/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/alembic.ini` tồn tại.
- `grep -c '^script_location = migrations$' Hub_All/api/alembic.ini` ≥ 1.
- `grep -c '^prepend_sys_path = .$' Hub_All/api/alembic.ini` ≥ 1.
- `grep -c '^file_template = ' Hub_All/api/alembic.ini` ≥ 1.
- `grep -cE '^sqlalchemy.url' Hub_All/api/alembic.ini` trả về `0` (KHÔNG hardcode DSN).
- `grep -c '\[loggers\]' Hub_All/api/alembic.ini` ≥ 1.
- `grep -c '\[logger_alembic\]' Hub_All/api/alembic.ini` ≥ 1.
</acceptance_criteria>
</task>

<task id="02" tdd="false">
<action>
Tạo `Hub_All/api/migrations/__init__.py` (empty package marker, cho phép `from migrations import ...` nếu sau cần) và `Hub_All/api/migrations/versions/.gitkeep` (file rỗng để git track thư mục versions trống cho đến Plan 04).

```bash
# Hub_All/api/migrations/__init__.py — empty
# Hub_All/api/migrations/versions/.gitkeep — empty
```

Trên Windows, dùng PowerShell `New-Item -ItemType File -Path Hub_All/api/migrations/__init__.py` và tương tự cho `.gitkeep`. Hoặc executor có thể dùng Write tool tạo file rỗng.
</action>
<read_first>
- Hub_All/api/alembic.ini
</read_first>
<acceptance_criteria>
- File `Hub_All/api/migrations/__init__.py` tồn tại (có thể rỗng).
- File `Hub_All/api/migrations/versions/.gitkeep` tồn tại (có thể rỗng).
- Directory `Hub_All/api/migrations/versions/` tồn tại.
</acceptance_criteria>
</task>

<task id="03" tdd="false">
<action>
Tạo `Hub_All/api/migrations/script.py.mako` — Alembic migration template chuẩn (lấy nguyên từ Alembic async template). File này được dùng KHI sinh migration mới (`alembic revision`) — Plan 04 sẽ generate `0001_initial_schema.py` từ template này.

Nội dung paste-ready:

```mako
"""${message}

Revision ID: ${up_revision}
Revises: ${down_revision | comma,n}
Create Date: ${create_date}

"""
from __future__ import annotations

from typing import Sequence, Union

from alembic import op
import sqlalchemy as sa
${imports if imports else ""}

# revision identifiers, used by Alembic.
revision: str = ${repr(up_revision)}
down_revision: Union[str, None] = ${repr(down_revision)}
branch_labels: Union[str, Sequence[str], None] = ${repr(branch_labels)}
depends_on: Union[str, Sequence[str], None] = ${repr(depends_on)}


def upgrade() -> None:
    ${upgrades if upgrades else "pass"}


def downgrade() -> None:
    ${downgrades if downgrades else "pass"}
```
</action>
<read_first>
- Hub_All/api/alembic.ini
- Hub_All/.planning/research/STACK.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/migrations/script.py.mako` tồn tại.
- `grep -c '"""${message}"""' Hub_All/api/migrations/script.py.mako` trả về `0` (template syntax `${message}` không có dấu nháy kép bao).
- `grep -c '\${message}' Hub_All/api/migrations/script.py.mako` ≥ 1.
- `grep -c '\${up_revision}' Hub_All/api/migrations/script.py.mako` ≥ 1.
- `grep -c '\${down_revision' Hub_All/api/migrations/script.py.mako` ≥ 1.
- `grep -c '\${upgrades' Hub_All/api/migrations/script.py.mako` ≥ 1.
- `grep -c '\${downgrades' Hub_All/api/migrations/script.py.mako` ≥ 1.
- `grep -c 'from alembic import op' Hub_All/api/migrations/script.py.mako` ≥ 1.
- `grep -c 'import sqlalchemy as sa' Hub_All/api/migrations/script.py.mako` ≥ 1.
</acceptance_criteria>
</task>

<task id="04" tdd="false">
<action>
Tạo `Hub_All/api/migrations/env.py` — Alembic environment runtime với 4 đặc trưng chủ chốt cho M2:

1. **Async runtime** — dùng `async_engine_from_config` + `asyncio.run` để Alembic chạy native async khớp stack SQLAlchemy 2 + asyncpg. Pattern theo Alembic async template (1.18+).
2. **DSN từ `app.config.get_settings().database_url`** — KHÔNG hardcode trong `alembic.ini` (Plan 03 task 01 đã skip `sqlalchemy.url` cố ý). `env.py` inject runtime qua `config.set_main_option`.
3. **`target_metadata = Base.metadata`** — import `Base` từ `app.db.base` + `from app.models import *` để mọi model class register vào metadata trước khi autogenerate run.
4. **`include_object` filter exclude schema `cocoindex`** (P7 mitigation — Pitfall #P7 + ROADMAP success criteria #5):
   - Filter: nếu `type_ in ('table', 'index') and hasattr(object, 'schema') and object.schema == 'cocoindex'`: return `False`.
   - Đảm bảo `alembic revision --autogenerate` KHÔNG sinh ra `op.drop_table('__cocoindex_*')` ngay cả khi DB có schema cocoindex (do cocoindex auto-create trong Phase 4).

5. **`compare_type=True` + `compare_server_default=True`** (P20 mitigation — detect drift chính xác cho type changes như `Text` ↔ `Varchar(N)`, server defaults như `'pending'` ↔ `NULL`).

Nội dung paste-ready:

```python
"""Alembic environment runtime — Medinet Wiki API M2.

Đặc trưng:
- Async runtime (asyncio + async_engine_from_config) — match stack SQLAlchemy 2 + asyncpg.
- DSN inject từ app.config.get_settings() — alembic.ini KHÔNG hardcode (mỗi env dùng DSN riêng).
- target_metadata = Base.metadata (import từ app.db.base + app.models.*).
- include_object filter: exclude schema 'cocoindex' (Pitfall #P7 — KHÔNG touch internal state).
- compare_type=True + compare_server_default=True (Pitfall #P20 — drift detection chính xác).
"""
from __future__ import annotations

import asyncio
from logging.config import fileConfig

from alembic import context
from sqlalchemy import pool
from sqlalchemy.engine import Connection
from sqlalchemy.ext.asyncio import async_engine_from_config

# Import models để Base.metadata pick up đủ 10 table TRƯỚC khi autogenerate run.
from app.config import get_settings
from app.db.base import Base
from app.models import *  # noqa: F401, F403 — register tất cả model vào metadata


# Alembic Config object — read alembic.ini
config = context.config

# Setup logging từ alembic.ini
if config.config_file_name is not None:
    fileConfig(config.config_file_name)

# Inject DSN runtime — KHÔNG hardcode trong alembic.ini.
_settings = get_settings()
config.set_main_option("sqlalchemy.url", _settings.database_url)

# Target metadata cho autogenerate
target_metadata = Base.metadata


def include_object(
    object_: object,
    name: str | None,
    type_: str,
    reflected: bool,
    compare_to: object | None,
) -> bool:
    """Filter autogenerate diff — exclude schema 'cocoindex' (Pitfall #P7).

    CocoIndex tự quản schema `cocoindex` (auto-create internal state tables ở
    Phase 4). Alembic KHÔNG được drop/alter/touch các bảng đó — nếu không,
    `alembic upgrade head` có thể xóa state cocoindex → re-index toàn bộ corpus.
    """
    if hasattr(object_, "schema") and object_.schema == "cocoindex":
        return False
    return True


def run_migrations_offline() -> None:
    """Offline mode — sinh SQL string KHÔNG cần connect DB (vd để review)."""
    url = config.get_main_option("sqlalchemy.url")
    context.configure(
        url=url,
        target_metadata=target_metadata,
        literal_binds=True,
        dialect_opts={"paramstyle": "named"},
        include_object=include_object,
        compare_type=True,
        compare_server_default=True,
    )

    with context.begin_transaction():
        context.run_migrations()


def do_run_migrations(connection: Connection) -> None:
    """Cấu hình context cho 1 transaction migration (sync wrapper)."""
    context.configure(
        connection=connection,
        target_metadata=target_metadata,
        include_object=include_object,
        compare_type=True,
        compare_server_default=True,
    )

    with context.begin_transaction():
        context.run_migrations()


async def run_async_migrations() -> None:
    """Online async — tạo async engine + run migrations qua run_sync."""
    connectable = async_engine_from_config(
        config.get_section(config.config_ini_section, {}),
        prefix="sqlalchemy.",
        poolclass=pool.NullPool,
    )

    async with connectable.connect() as connection:
        await connection.run_sync(do_run_migrations)

    await connectable.dispose()


def run_migrations_online() -> None:
    """Online mode entry — chạy async migration qua asyncio.run."""
    asyncio.run(run_async_migrations())


if context.is_offline_mode():
    run_migrations_offline()
else:
    run_migrations_online()
```
</action>
<read_first>
- Hub_All/api/alembic.ini
- Hub_All/api/app/config.py
- Hub_All/api/app/db/base.py
- Hub_All/api/app/db/__init__.py
- Hub_All/api/app/models/__init__.py
- Hub_All/.planning/research/STACK.md
- Hub_All/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/migrations/env.py` tồn tại.
- `grep -c 'from app.config import get_settings' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c 'from app.db.base import Base' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c 'from app.models import \*' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c 'target_metadata = Base.metadata' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c 'def include_object' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c "object_.schema == \"cocoindex\"" Hub_All/api/migrations/env.py` ≥ 1 (P7 filter).
- `grep -c 'compare_type=True' Hub_All/api/migrations/env.py` ≥ 2 (online + offline mode).
- `grep -c 'compare_server_default=True' Hub_All/api/migrations/env.py` ≥ 2.
- `grep -c 'async_engine_from_config' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c 'asyncio.run' Hub_All/api/migrations/env.py` ≥ 1.
- `grep -c 'config.set_main_option' Hub_All/api/migrations/env.py` ≥ 1 (DSN inject runtime).
- `grep -c 'include_object=include_object' Hub_All/api/migrations/env.py` ≥ 2.
- `cd Hub_All/api && uv run python -c "import app.config; import sys; sys.path.insert(0, 'migrations'); print('env.py import-safe')"` exits 0 (verify module-level import KHÔNG crash khi load).
</acceptance_criteria>
</task>

<task id="05" tdd="false">
<action>
Smoke verify Alembic config hoàn chỉnh — chạy `alembic --help` + `alembic current` để confirm:
1. `alembic` CLI tìm được `alembic.ini` trong `Hub_All/api/`.
2. `env.py` load không crash (import models OK, settings load OK).
3. Trên DB rỗng, `alembic current` không exception, output `(empty)` hoặc chuỗi tương đương (chưa có version).

**Yêu cầu prereq:** docker compose phải up từ Plan 02 Phase 1 (postgres healthy với 2 DB `medinet_central` + `medinet_cocoindex`). Plan 04 sẽ test thực hơn với `alembic upgrade head`.

Tạo helper script `Hub_All/api/scripts/alembic-smoke.sh` (nếu chưa có thư mục `scripts/`, đã có từ Phase 1). Script chạy 3 lệnh kiểm tra cơ bản:

```bash
#!/usr/bin/env bash
# Smoke test Alembic config — Plan 03 verify env.py + alembic.ini hoạt động.
# Yêu cầu prereq: docker compose up -d postgres (Phase 1 Plan 02) + .env file đủ DSN.

set -euo pipefail

cd "$(dirname "$0")/.."

echo "[alembic-smoke] (1/3) Verify alembic CLI accessible..."
uv run alembic --help > /dev/null

echo "[alembic-smoke] (2/3) Verify env.py load (no migration apply)..."
uv run alembic current 2>&1 | tee /tmp/alembic_current.log

# Alembic current trên DB rỗng có thể trả về 1 trong các pattern:
# - "(empty)" — chưa có alembic_version table
# - "INFO  [alembic.runtime.migration] Context impl PostgresqlImpl." rồi blank
# Đều OK miễn KHÔNG exception traceback.
if grep -qE '(Traceback|Error|FATAL)' /tmp/alembic_current.log; then
    echo "FAIL: alembic current có exception/error."
    exit 1
fi

echo "[alembic-smoke] (3/3) Verify target_metadata pick up 10 tables..."
uv run python -c "
from app.db.base import Base
from app.models import *
tables = sorted(Base.metadata.tables.keys())
assert len(tables) == 10, f'Expected 10 tables, got {len(tables)}: {tables}'
print(f'OK — {len(tables)} tables registered: {tables}')
"

echo "[alembic-smoke] DONE — Alembic config sẵn sàng cho Plan 04 (sinh migration)."
```
</action>
<read_first>
- Hub_All/api/alembic.ini
- Hub_All/api/migrations/env.py
- Hub_All/api/app/models/__init__.py
- Hub_All/.planning/research/STACK.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/scripts/alembic-smoke.sh` tồn tại.
- `head -1 Hub_All/api/scripts/alembic-smoke.sh` trả `#!/usr/bin/env bash`.
- `grep -c 'uv run alembic --help' Hub_All/api/scripts/alembic-smoke.sh` ≥ 1.
- `grep -c 'uv run alembic current' Hub_All/api/scripts/alembic-smoke.sh` ≥ 1.
- `grep -c "len(tables) == 10" Hub_All/api/scripts/alembic-smoke.sh` ≥ 1.
- `cd Hub_All/api && uv run alembic --help` exits 0 (CLI hoạt động).
- (Yêu cầu Phase 1 docker up) `cd Hub_All/api && DATABASE_URL=postgresql+asyncpg://medinet:medinet_dev_pwd@localhost:5432/medinet_central COCOINDEX_DATABASE_URL=postgresql://medinet:medinet_dev_pwd@localhost:5432/medinet_cocoindex REDIS_URL=redis://localhost:6379/0 uv run alembic current` exits 0 và output KHÔNG có `Traceback` (DB rỗng trả `(empty)` hoặc tương đương).
</acceptance_criteria>
</task>

## Verification

- `cd Hub_All/api && uv run alembic --help` exits 0 và output có dòng `usage: alembic`.
- `cd Hub_All/api && uv run python -c "from app.db.base import Base; from app.models import *; assert len(Base.metadata.tables) == 10"` exits 0 (10 table register vào metadata trước khi env.py load).
- `cd Hub_All/api && uv run python -c "import importlib.util; spec = importlib.util.spec_from_file_location('env_smoke', 'migrations/env.py'); print('env.py path resolves' if spec else 'FAIL')"` exits 0 và output `env.py path resolves`.
- (Prereq Phase 1 docker up) `cd Hub_All/api && DATABASE_URL=postgresql+asyncpg://medinet:medinet_dev_pwd@localhost:5432/medinet_central COCOINDEX_DATABASE_URL=postgresql://medinet:medinet_dev_pwd@localhost:5432/medinet_cocoindex REDIS_URL=redis://localhost:6379/0 uv run alembic current` exits 0.
- `cd Hub_All/api && uv run ruff check migrations/env.py` exits 0.
- `ls Hub_All/api/migrations/` liệt kê đủ 4 mục: `__init__.py`, `env.py`, `script.py.mako`, `versions/`.
- `ls Hub_All/api/migrations/versions/` liệt kê duy nhất `.gitkeep` (chưa có migration script — Plan 04 sẽ tạo).
