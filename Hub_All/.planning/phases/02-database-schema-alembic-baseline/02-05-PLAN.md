---
plan: 05
phase: 2
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - Hub_All/api/Makefile
  - Hub_All/api/pyproject.toml
  - Hub_All/api/tests/integration/__init__.py
  - Hub_All/api/tests/integration/conftest.py
  - Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py
  - Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py
  - Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py
autonomous: true
requirements: [CORE-02]
---

# Plan 05: Verify suite — `make migrate-up/down/check` targets + 3 pytest integration tests (testcontainers Postgres) verify 5 ROADMAP success criteria

## Objective
Bake verify hardening cho schema baseline: (a) thêm 3 Makefile target tiện dụng (`migrate-up`, `migrate-down`, `migrate-check`) trong `Hub_All/api/Makefile`; (b) 3 pytest integration test critical-path dùng testcontainers Postgres ext pgvector — verify migration upgrade clean, HNSW `vector_cosine_ops` build OK trên `vector(1536)`, và `include_object` filter Plan 03 thực sự ignore schema `cocoindex`. Đây là điểm chốt 5 success criteria của ROADMAP Phase 2, đồng thời mở đường cho HARD-03 Phase 10 (integration test ≥50% critical path).

**Tại sao plan riêng:** Plan 04 viết migration; Plan 05 verify migration đúng spec qua test tự động — tách giúp ai chạy regression sau Phase 2 không phải nhớ list lệnh psql manual. Test integration runs trên CI sau này.

## Must-Haves

**Truths (6 mục — verify match 5 ROADMAP success criteria + R1 dim pin):**
- `make -C Hub_All/api migrate-up` chạy `alembic upgrade head` thành công, tạo 10 bảng (Criteria #1).
- pytest `test_migration_upgrade_downgrade.py::test_upgrade_creates_10_tables` PASS — verify đủ 10 bảng + `alembic_version` (Criteria #1).
- pytest `test_migration_upgrade_downgrade.py::test_downgrade_drops_all_clean` PASS — `alembic downgrade base` sau `upgrade head` → `public` schema rỗng (Criteria #3).
- pytest `test_chunks_hnsw_cosine_ops.py::test_chunks_vector_hnsw_uses_cosine_ops` PASS — verify HNSW index dùng `vector_cosine_ops` (Criteria #2, P17 mitigation).
- pytest `test_chunks_vector_column_is_1536_dim` PASS — verify `chunks.vector` type là `vector(1536)` (R1 pin dim 1536 — chống pgvector 2000-dim index limit).
- pytest `test_chunks_hnsw_cosine_ops.py::test_documents_status_enum_includes_failed_unsupported` PASS — CHECK constraint chứa `failed_unsupported` (Criteria #4, R4 mitigation).
- pytest `test_alembic_ignores_cocoindex_schema.py::test_upgrade_does_not_touch_cocoindex_schema` PASS — sau khi tạo schema `cocoindex` + bảng giả + chạy `alembic upgrade head`, bảng cocoindex vẫn nguyên (Criteria #5, P7 mitigation).

**Artifacts (5 file):**
- `Hub_All/api/Makefile` — thêm 3 target migrate-* (giữ nguyên target cũ Phase 1 nếu có).
- `Hub_All/api/tests/integration/__init__.py` — empty package marker (chưa tồn tại từ Phase 1).
- `Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` — 2 test (upgrade tạo 10 bảng + downgrade clean).
- `Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` — 2 test (HNSW vector_cosine_ops build OK trên `vector(1536)` + documents.status CHECK enum).
- `Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` — 1 test (P7 filter verify).

**Key links (3 connection):**
- pytest `@pytest.fixture(scope="module") postgres_container` → `testcontainers.postgres.PostgresContainer("pgvector/pgvector:pg16")` — pattern: real Postgres + ext vector enable trong fixture, share giữa tests trong cùng module để giảm overhead.
- pytest test → `alembic.command.upgrade/downgrade/check` programmatic API — pattern: KHÔNG dùng subprocess, dùng `alembic.config.Config` + `alembic.command.upgrade(cfg, "head")` để control DSN per-test.
- Makefile target → `uv run alembic upgrade head` etc. — pattern: developer 1-letter command, CI script reuse.

## Tasks

<task id="01" tdd="false">
<action>
Thêm 3 target `migrate-up`, `migrate-down`, `migrate-check` vào `Hub_All/api/Makefile`. Nếu Makefile chưa tồn tại (Phase 1 có thể chưa tạo), tạo mới với 5 target cơ bản:
- `install` — `uv sync --extra dev`
- `lint` — `uv run ruff check . && uv run mypy app`
- `test` — `uv run pytest`
- `migrate-up` — `uv run alembic upgrade head`
- `migrate-down` — `uv run alembic downgrade base`
- `migrate-check` — `uv run alembic check`

Nếu Makefile đã tồn tại (kiểm tra `ls Hub_All/api/Makefile` trước), append 3 target migrate-* mới — KHÔNG overwrite target có sẵn từ Phase 1.

Nội dung paste-ready (full Makefile mới hoặc append):

```makefile
# Medinet Wiki API — Makefile (Phase 2 mở rộng với migrate-* targets)
# Tiền điều kiện: docker compose up -d postgres (Phase 1 Plan 02) + .env file đủ DSN.

.PHONY: install lint test migrate-up migrate-down migrate-check migrate-history

install:
	uv sync --extra dev

lint:
	uv run ruff check .
	uv run mypy app

test:
	uv run pytest

# === Alembic migration targets (Phase 2) ===

migrate-up:
	uv run alembic upgrade head

migrate-down:
	uv run alembic downgrade base

migrate-check:
	uv run alembic check

migrate-history:
	uv run alembic history --verbose
```

Nếu file đã có (Phase 1 đã tạo), executor cần read file cũ và ADD 4 dòng migrate-* targets vào cuối, KHÔNG xóa các target sẵn có.
</action>
<read_first>
- Hub_All/api/Makefile (nếu tồn tại; có thể chưa)
- Hub_All/api/pyproject.toml
- Hub_All/.planning/research/STACK.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/Makefile` tồn tại.
- `grep -c '^migrate-up:$' Hub_All/api/Makefile` ≥ 1.
- `grep -c '^migrate-down:$' Hub_All/api/Makefile` ≥ 1.
- `grep -c '^migrate-check:$' Hub_All/api/Makefile` ≥ 1.
- `grep -c 'uv run alembic upgrade head' Hub_All/api/Makefile` ≥ 1.
- `grep -c 'uv run alembic downgrade base' Hub_All/api/Makefile` ≥ 1.
- `grep -c 'uv run alembic check' Hub_All/api/Makefile` ≥ 1.
- `grep -c '\.PHONY' Hub_All/api/Makefile` ≥ 1.
- (Prereq docker compose up + .env) `cd Hub_All/api && make migrate-up` exits 0.
- (Prereq) `cd Hub_All/api && make migrate-check` exits 0 (no drift).
- (Prereq) `cd Hub_All/api && make migrate-down` exits 0.
</acceptance_criteria>
</task>

<task id="02" tdd="false">
<action>
Tạo `Hub_All/api/tests/integration/__init__.py` (empty marker) và `Hub_All/api/tests/integration/conftest.py` chứa 2 shared fixture cho cả 3 file test integration ở task 03/04/05. **Đặt fixture ở conftest.py từ ĐẦU — KHÔNG embed trong test file rồi move sau (tránh duplicate / shadowing module-scope giữa 3 test file).**

**Fixture spec:**

1. **`postgres_container` (scope=module)** — start testcontainers PostgresContainer image `pgvector/pgvector:pg16`, enable ext `vector` + `pgcrypto` qua `CREATE EXTENSION IF NOT EXISTS`, yield container. Scope module để share giữa tests trong cùng module (tránh start 5s/lần).
2. **`alembic_cfg` (scope=function)** — tạo `alembic.config.Config` trỏ về `alembic.ini`, inject env vars `DATABASE_URL`/`COCOINDEX_DATABASE_URL`/`REDIS_URL`/`APP_ENV` qua `monkeypatch.setenv`, clear `get_settings.cache_clear()` để app config pick up DSN testcontainer.

**Lưu ý dependency:** Plan này dùng `testcontainers-python` + `psycopg2-binary` (driver sync cho SQLAlchemy `create_engine` trong fixture). 2 lib này CHƯA có trong `pyproject.toml` của Phase 1 Plan 01. Task này phải Edit `Hub_All/api/pyproject.toml` thêm vào section `[project.optional-dependencies].dev`:
- `testcontainers[postgres]>=4.7,<5`
- `psycopg2-binary>=2.9,<3`

Rồi rerun `uv sync --extra dev` trong `Hub_All/api/`.

Nội dung paste-ready `Hub_All/api/tests/integration/conftest.py`:

```python
"""Shared fixtures cho integration tests Phase 2 (Plan 05).

Định nghĩa source-of-truth cho 2 fixture dùng chung giữa 3 file test:
- test_migration_upgrade_downgrade.py
- test_chunks_hnsw_cosine_ops.py
- test_alembic_ignores_cocoindex_schema.py

KHÔNG redeclare fixture trong test file — pytest auto-discover qua conftest.
"""
from __future__ import annotations

from pathlib import Path

import pytest
from alembic.config import Config
from sqlalchemy import create_engine, text
from testcontainers.postgres import PostgresContainer


@pytest.fixture(scope="module")
def postgres_container() -> PostgresContainer:
    """Postgres 16 + pgvector ext + pgcrypto ext (scope module — share giữa test cùng module)."""
    with PostgresContainer("pgvector/pgvector:pg16", driver=None) as pg:
        sync_url = pg.get_connection_url().replace(
            "postgresql+psycopg2://", "postgresql://"
        )
        eng = create_engine(sync_url)
        with eng.begin() as conn:
            conn.execute(text("CREATE EXTENSION IF NOT EXISTS vector"))
            conn.execute(text("CREATE EXTENSION IF NOT EXISTS pgcrypto"))
        eng.dispose()
        yield pg


@pytest.fixture
def alembic_cfg(
    postgres_container: PostgresContainer,
    monkeypatch: pytest.MonkeyPatch,
) -> Config:
    """Alembic Config + inject env vars cho app.config.get_settings()."""
    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    async_url = sync_url.replace("postgresql://", "postgresql+asyncpg://")

    monkeypatch.setenv("DATABASE_URL", async_url)
    monkeypatch.setenv("COCOINDEX_DATABASE_URL", sync_url)
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.setenv("APP_ENV", "dev")

    from app.config import get_settings
    get_settings.cache_clear()

    cfg_path = Path(__file__).resolve().parents[2] / "alembic.ini"
    return Config(str(cfg_path))
```
</action>
<read_first>
- Hub_All/api/pyproject.toml
- Hub_All/api/app/config.py
- Hub_All/api/alembic.ini
- Hub_All/.planning/CONVENTIONS.md
- Hub_All/.planning/research/STACK.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/__init__.py` tồn tại (có thể rỗng).
- File `Hub_All/api/tests/integration/conftest.py` tồn tại.
- `grep -c '@pytest.fixture(scope="module")' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c 'def postgres_container' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c 'def alembic_cfg' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c 'PostgresContainer("pgvector/pgvector:pg16"' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c 'CREATE EXTENSION IF NOT EXISTS vector' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c 'CREATE EXTENSION IF NOT EXISTS pgcrypto' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c 'get_settings.cache_clear' Hub_All/api/tests/integration/conftest.py` ≥ 1.
- `grep -c '"testcontainers\[postgres\]' Hub_All/api/pyproject.toml` ≥ 1 (dev dep add OK).
- `grep -c '"psycopg2-binary' Hub_All/api/pyproject.toml` ≥ 1.
- `cd Hub_All/api && uv sync --extra dev` exits 0 (lock + install OK).
- `cd Hub_All/api && uv run python -c "from testcontainers.postgres import PostgresContainer; print('import OK')"` exits 0.
</acceptance_criteria>
</task>

<task id="03" tdd="false">
<action>
Tạo `Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` — 2 integration test critical-path. Test KHÔNG khai báo fixture — chỉ inject `postgres_container` + `alembic_cfg` qua dependency injection từ `conftest.py` (đã tạo ở task 02).

**Tests:**
1. **`test_upgrade_creates_10_tables`**: chạy `alembic upgrade head` programmatically, verify `information_schema.tables` có đủ 11 dòng (10 schema + `alembic_version`).
2. **`test_downgrade_drops_all_clean`**: sau upgrade, chạy `alembic downgrade base`, verify `public.tables` rỗng (alembic_version cũng drop).

Nội dung paste-ready:

```python
"""Integration tests: Alembic migration upgrade/downgrade trên Postgres thực.

Sử dụng testcontainers pgvector/pgvector:pg16 — KHÔNG mock asyncpg.
Verify ROADMAP Phase 2 success criteria #1 + #3.

Fixture `postgres_container` + `alembic_cfg` lấy từ tests/integration/conftest.py.
"""
from __future__ import annotations

import pytest
from alembic import command
from alembic.config import Config
from sqlalchemy import create_engine, text
from testcontainers.postgres import PostgresContainer


@pytest.mark.critical
@pytest.mark.integration
def test_upgrade_creates_10_tables(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """upgrade head → public schema có đúng 10 bảng app + alembic_version."""
    command.upgrade(alembic_cfg, "head")

    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    eng = create_engine(sync_url)
    with eng.connect() as conn:
        rows = conn.execute(text(
            "SELECT table_name FROM information_schema.tables "
            "WHERE table_schema='public' AND table_type='BASE TABLE' "
            "ORDER BY table_name"
        )).all()
    eng.dispose()

    table_names = sorted(r[0] for r in rows)
    expected = sorted([
        "alembic_version",
        "api_keys",
        "audit_logs",
        "chunks",
        "documents",
        "hubs",
        "refresh_tokens",
        "settings",
        "usage_events",
        "user_hubs",
        "users",
    ])
    assert table_names == expected, f"Schema mismatch. Got: {table_names}"


@pytest.mark.critical
@pytest.mark.integration
def test_downgrade_drops_all_clean(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """downgrade base sau upgrade head → public schema rỗng (alembic_version cũng drop)."""
    command.upgrade(alembic_cfg, "head")
    command.downgrade(alembic_cfg, "base")

    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    eng = create_engine(sync_url)
    with eng.connect() as conn:
        rows = conn.execute(text(
            "SELECT table_name FROM information_schema.tables "
            "WHERE table_schema='public' AND table_type='BASE TABLE'"
        )).all()
    eng.dispose()

    assert len(rows) == 0, f"Downgrade không clean. Còn lại: {[r[0] for r in rows]}"
```

**LƯU Ý:** Test file KHÔNG được redeclare fixture `postgres_container` hoặc `alembic_cfg` — fixture lấy nguyên qua DI từ `conftest.py`. Nếu paste lại fixture vào file này sẽ shadow module-scope conftest và start 2 container redundant.
</action>
<read_first>
- Hub_All/api/tests/integration/conftest.py
- Hub_All/api/migrations/versions/0001_initial_schema.py
- Hub_All/api/migrations/env.py
- Hub_All/.planning/CONVENTIONS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` tồn tại.
- `grep -c 'def test_upgrade_creates_10_tables' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` ≥ 1.
- `grep -c 'def test_downgrade_drops_all_clean' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` ≥ 1.
- `grep -c '@pytest.mark.critical' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` ≥ 2.
- `grep -c '@pytest.mark.integration' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` ≥ 2.
- `grep -c 'command.upgrade(alembic_cfg' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` ≥ 2.
- `grep -c 'command.downgrade(alembic_cfg' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` ≥ 1.
- `grep -c '@pytest.fixture' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` trả về `0` (KHÔNG redeclare fixture — phải lấy từ conftest.py).
- `grep -c 'def postgres_container' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` trả về `0` (cùng lý do).
- `grep -c 'def alembic_cfg' Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py` trả về `0`.
- (Yêu cầu Docker available) `cd Hub_All/api && uv run pytest tests/integration/test_migration_upgrade_downgrade.py -v -m critical` exits 0 và output có `2 passed`.
</acceptance_criteria>
</task>

<task id="04" tdd="false">
<action>
Tạo `Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` — 2 test verify R1 + R4 + P17 mitigations:

1. **`test_chunks_vector_hnsw_uses_cosine_ops`**: Sau `alembic upgrade head`, query `pg_indexes` confirm index `ix_chunks_vector_hnsw` exist và `pg_get_indexdef()` output có `USING hnsw` + `vector_cosine_ops`. Đây là P17 mitigation hard verify.
2. **`test_documents_status_enum_includes_failed_unsupported`**: Query `pg_constraint.consrc` (hoặc `pg_get_constraintdef`) cho CHECK constraint `ck_documents_status_enum` — verify text chứa `'failed_unsupported'`. R4 mitigation hard verify.

Fixture `postgres_container` + `alembic_cfg` lấy nguyên từ `Hub_All/api/tests/integration/conftest.py` (đã tạo ở task 02 — source-of-truth). Test file KHÔNG được redeclare fixture.

Nội dung paste-ready `Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py`:

```python
"""Integration tests: chunks HNSW vector_cosine_ops + documents.status enum.

Verify ROADMAP Phase 2 success criteria #2 (HNSW vector_cosine_ops) + #4
(documents.status enum bao gồm failed_unsupported).

Mitigations: R1 (pgvector dim 1536), R4 (status enum), P17 (cosine ops).
"""
from __future__ import annotations

import pytest
from alembic import command
from alembic.config import Config
from sqlalchemy import create_engine, text
from testcontainers.postgres import PostgresContainer


@pytest.mark.critical
@pytest.mark.integration
def test_chunks_vector_hnsw_uses_cosine_ops(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """Index ix_chunks_vector_hnsw PHẢI dùng vector_cosine_ops (P17 + R1)."""
    command.upgrade(alembic_cfg, "head")

    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    eng = create_engine(sync_url)
    with eng.connect() as conn:
        # pg_indexes.indexdef trả về full CREATE INDEX DDL string
        row = conn.execute(text(
            "SELECT indexdef FROM pg_indexes "
            "WHERE schemaname='public' AND indexname='ix_chunks_vector_hnsw'"
        )).first()
    eng.dispose()

    assert row is not None, "Index ix_chunks_vector_hnsw KHÔNG tồn tại"
    indexdef = row[0]
    assert "USING hnsw" in indexdef, f"Index không dùng HNSW: {indexdef}"
    assert "vector_cosine_ops" in indexdef, (
        f"P17 violation: index KHÔNG dùng vector_cosine_ops: {indexdef}"
    )


@pytest.mark.critical
@pytest.mark.integration
def test_chunks_vector_column_is_1536_dim(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """chunks.vector PHẢI là vector(1536) (R1 pin dim)."""
    command.upgrade(alembic_cfg, "head")

    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    eng = create_engine(sync_url)
    with eng.connect() as conn:
        # pg_attribute + format_type cho vector(N) output
        row = conn.execute(text(
            "SELECT format_type(atttypid, atttypmod) "
            "FROM pg_attribute "
            "WHERE attrelid = 'public.chunks'::regclass "
            "AND attname = 'vector'"
        )).first()
    eng.dispose()

    assert row is not None, "Column chunks.vector KHÔNG tồn tại"
    type_str = row[0]
    assert "vector(1536)" in type_str, f"R1 violation: type không phải vector(1536): {type_str}"


@pytest.mark.critical
@pytest.mark.integration
def test_documents_status_enum_includes_failed_unsupported(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """CHECK constraint documents.status PHẢI bao gồm 'failed_unsupported' (R4)."""
    command.upgrade(alembic_cfg, "head")

    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    eng = create_engine(sync_url)
    with eng.connect() as conn:
        row = conn.execute(text(
            "SELECT pg_get_constraintdef(oid) "
            "FROM pg_constraint "
            "WHERE conname = 'ck_documents_status_enum'"
        )).first()
    eng.dispose()

    assert row is not None, "CHECK constraint ck_documents_status_enum KHÔNG tồn tại"
    constraint_def = row[0]
    assert "failed_unsupported" in constraint_def, (
        f"R4 violation: CHECK constraint KHÔNG có failed_unsupported: {constraint_def}"
    )
    # Bonus: verify cả 5 status values
    for status_val in ("pending", "processing", "completed", "failed", "failed_unsupported"):
        assert status_val in constraint_def, f"Status enum thiếu '{status_val}': {constraint_def}"
```
</action>
<read_first>
- Hub_All/api/tests/integration/conftest.py
- Hub_All/api/tests/integration/test_migration_upgrade_downgrade.py
- Hub_All/api/migrations/versions/0001_initial_schema.py
- Hub_All/.planning/research/PITFALLS.md
- Hub_All/.planning/CONVENTIONS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` tồn tại.
- `grep -c '@pytest.fixture' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` trả về `0` (fixture phải lấy từ conftest, KHÔNG redeclare).
- `grep -c 'def postgres_container' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` trả về `0`.
- `grep -c 'def alembic_cfg' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` trả về `0`.
- `grep -c 'def test_chunks_vector_hnsw_uses_cosine_ops' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 1.
- `grep -c 'def test_chunks_vector_column_is_1536_dim' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 1.
- `grep -c 'def test_documents_status_enum_includes_failed_unsupported' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 1.
- `grep -c 'vector_cosine_ops' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 1.
- `grep -c 'vector(1536)' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 1.
- `grep -c 'failed_unsupported' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 2 (test name + assert).
- `grep -c 'ck_documents_status_enum' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 1.
- `grep -c '@pytest.mark.critical' Hub_All/api/tests/integration/test_chunks_hnsw_cosine_ops.py` ≥ 3.
- (Yêu cầu Docker) `cd Hub_All/api && uv run pytest tests/integration/test_chunks_hnsw_cosine_ops.py -v -m critical` exits 0 và output có `3 passed`.
</acceptance_criteria>
</task>

<task id="05" tdd="false">
<action>
Tạo `Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` — 1 test verify P7 mitigation (Plan 03 env.py `include_object` filter thực sự work).

**Test design:**
1. Setup: `alembic upgrade head` trước (schema `public` 10 bảng có sẵn).
2. Manual create schema `cocoindex` + 1 bảng giả `cocoindex.fake_state(id int)` (mô phỏng cocoindex Phase 4 tự tạo).
3. Chạy `alembic check` — KHÔNG được report drift đề xuất drop `cocoindex.fake_state` (vì env.py `include_object` filter exclude schema cocoindex).
4. Verify: `alembic check` exits 0 hoặc nếu có drift report thì content KHÔNG được chứa `cocoindex.fake_state`.
5. Cleanup: drop `cocoindex.fake_state` + schema cocoindex sau test (để fixture container không leak).

Nội dung paste-ready:

```python
"""Integration test: Alembic include_object filter ignore schema 'cocoindex' (P7).

Verify ROADMAP Phase 2 success criteria #5 — Alembic KHÔNG touch schema cocoindex
(do cocoindex Phase 4 tự quản, KHÔNG cho Alembic drop/alter).

Mitigations: P7 (cocoindex shared schema), P20 (Alembic drift detection).
"""
from __future__ import annotations

from io import StringIO
from unittest.mock import patch

import pytest
from alembic import command
from alembic.config import Config
from sqlalchemy import create_engine, text
from testcontainers.postgres import PostgresContainer


@pytest.mark.critical
@pytest.mark.integration
def test_upgrade_does_not_touch_cocoindex_schema(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """Sau upgrade head, schema cocoindex giả lập VẪN nguyên (env.py filter P7)."""
    sync_url = postgres_container.get_connection_url().replace(
        "postgresql+psycopg2://", "postgresql://"
    )
    eng = create_engine(sync_url)

    # 1) Manual setup schema cocoindex giả lập (mô phỏng Phase 4 cocoindex auto-create)
    with eng.begin() as conn:
        conn.execute(text("CREATE SCHEMA IF NOT EXISTS cocoindex"))
        conn.execute(text(
            "CREATE TABLE cocoindex.fake_state (id int primary key, val text)"
        ))
        conn.execute(text(
            "INSERT INTO cocoindex.fake_state (id, val) VALUES (1, 'baseline')"
        ))

    # 2) Chạy alembic upgrade head — KHÔNG được touch schema cocoindex
    command.upgrade(alembic_cfg, "head")

    # 3) Verify bảng cocoindex.fake_state vẫn nguyên + row vẫn còn
    with eng.connect() as conn:
        row = conn.execute(text(
            "SELECT val FROM cocoindex.fake_state WHERE id=1"
        )).first()

        # Verify schema 'cocoindex' KHÔNG có entry trong alembic_version (Alembic không tham gia)
        cocoindex_tables = conn.execute(text(
            "SELECT table_name FROM information_schema.tables "
            "WHERE table_schema='cocoindex'"
        )).all()

    assert row is not None, "P7 violation: cocoindex.fake_state bị xóa bởi Alembic"
    assert row[0] == "baseline", "P7 violation: cocoindex.fake_state.val bị thay đổi"
    table_names = [r[0] for r in cocoindex_tables]
    assert "fake_state" in table_names, f"P7 violation: bảng missing: {table_names}"

    eng.dispose()


@pytest.mark.critical
@pytest.mark.integration
def test_alembic_check_no_drift_after_upgrade(
    postgres_container: PostgresContainer,
    alembic_cfg: Config,
) -> None:
    """Sau upgrade head, alembic check KHÔNG báo drift (P20 baseline correct).

    Defense: nếu naming convention sai hoặc model khác schema thực, alembic check
    sẽ exit code 1 với CommandError. P20 mitigation: convention từ Plan 01 + autogenerate
    Plan 04 sinh đúng → check PASS.
    """
    command.upgrade(alembic_cfg, "head")

    # alembic check raise CommandError nếu có drift; exit code 0 nếu clean.
    # Wrap try/except để có message rõ ràng.
    try:
        command.check(alembic_cfg)
    except Exception as e:  # noqa: BLE001
        pytest.fail(f"P20 drift detected sau upgrade head: {e}")
```
</action>
<read_first>
- Hub_All/api/migrations/env.py
- Hub_All/api/migrations/versions/0001_initial_schema.py
- Hub_All/api/tests/integration/conftest.py
- Hub_All/.planning/research/PITFALLS.md
</read_first>
<acceptance_criteria>
- File `Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` tồn tại.
- `grep -c 'def test_upgrade_does_not_touch_cocoindex_schema' Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` ≥ 1.
- `grep -c 'def test_alembic_check_no_drift_after_upgrade' Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` ≥ 1.
- `grep -c 'CREATE SCHEMA IF NOT EXISTS cocoindex' Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` ≥ 1.
- `grep -c 'cocoindex.fake_state' Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` ≥ 2.
- `grep -c 'command.check' Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` ≥ 1.
- `grep -c '@pytest.mark.critical' Hub_All/api/tests/integration/test_alembic_ignores_cocoindex_schema.py` ≥ 2.
- (Yêu cầu Docker) `cd Hub_All/api && uv run pytest tests/integration/test_alembic_ignores_cocoindex_schema.py -v -m critical` exits 0 và output có `2 passed`.
- (Yêu cầu Docker) `cd Hub_All/api && uv run pytest tests/integration/ -v -m critical` exits 0 và output có `7 passed` (2 + 3 + 2 từ 3 file test).
</acceptance_criteria>
</task>

## Verification

**Prereq:** Docker Desktop running (testcontainers tự pull image `pgvector/pgvector:pg16`). KHÔNG cần docker compose Phase 1 up — testcontainers tạo container riêng cho test isolation.

- `ls Hub_All/api/tests/integration/` liệt kê đủ 5 mục: `__init__.py`, `conftest.py`, `test_alembic_ignores_cocoindex_schema.py`, `test_chunks_hnsw_cosine_ops.py`, `test_migration_upgrade_downgrade.py`.
- `cd Hub_All/api && uv run pytest tests/integration/ -v -m critical` exits 0 với output có dòng `7 passed in <time>s`:
  - `test_migration_upgrade_downgrade.py`: 2 test (upgrade tạo 10 bảng + downgrade clean).
  - `test_chunks_hnsw_cosine_ops.py`: 3 test (HNSW cosine ops + vector dim 1536 + status enum failed_unsupported).
  - `test_alembic_ignores_cocoindex_schema.py`: 2 test (P7 filter + P20 no drift).
- `cd Hub_All/api && make migrate-up && make migrate-check && make migrate-down` (full cycle với docker compose `postgres` Phase 1 up) exits 0 mọi lệnh.
- `cd Hub_All/api && uv run ruff check tests/integration` exits 0.
- `grep -rE '(failed_unsupported|vector_cosine_ops|ix_chunks_vector_hnsw|cocoindex)' Hub_All/api/tests/integration/ | wc -l` ≥ 8 (mọi keyword verify đều cover).

## Source Audit

| Source | Item | Plan | Status |
|---|---|---|---|
| GOAL (ROADMAP P2) | "Alembic migration baseline khởi tạo schema Postgres cho toàn bộ entity M2 (9 table) với chunks pgvector + HNSW index sẵn sàng" | P01 (db infra) + P02 (10 model) + P03 (alembic init) + P04 (migration script) + P05 (verify suite) | COVERED |
| REQ (REQUIREMENTS.md) | CORE-02 (phần Alembic baseline tách từ Phase 1) | P01-P05 đều có `requirements: [CORE-02]` trong frontmatter | COVERED |
| ROADMAP success criteria #1 | `alembic upgrade head` tạo 9 table + settings = 10 bảng spec đúng | P04 (migration) + P05 test_upgrade_creates_10_tables | COVERED |
| ROADMAP success criteria #2 | `\d chunks` cho thấy vector(1536), HNSW vector_cosine_ops, B-tree (hub_id, document_id), content_hash BYTEA | P02 model + P04 migration + P05 test_chunks_vector_hnsw_uses_cosine_ops + test_chunks_vector_column_is_1536_dim | COVERED |
| ROADMAP success criteria #3 | `alembic check` no drift + `alembic downgrade base` clean | P03 env.py compare_type=True + P05 test_downgrade_drops_all_clean + test_alembic_check_no_drift_after_upgrade | COVERED |
| ROADMAP success criteria #4 | chunks FK đúng + documents.status enum includes failed_unsupported (R4) | P02 model + P04 migration + P05 test_documents_status_enum_includes_failed_unsupported | COVERED |
| ROADMAP success criteria #5 | Schema cocoindex tồn tại trên medinet_cocoindex, Alembic KHÔNG touch (include_object filter) | P03 env.py include_object + P05 test_upgrade_does_not_touch_cocoindex_schema | COVERED |
| RESEARCH (STACK.md) | alembic==1.18.4, sqlalchemy>=2.0.36,<2.1, asyncpg==0.30.0, pgvector==0.4.2, python-docx==1.2.0 (pinned versions) | Phase 1 Plan 01 đã pin trong pyproject.toml; Plan 05 add testcontainers>=4.7 + psycopg2-binary>=2.9 | COVERED |
| RESEARCH (ARCHITECTURE.md) | Module layout `app/db/{base, session, models}`, `migrations/{env.py, versions/}` | P01 (app/db/) + P02 (app/models/, gom theo domain) + P03 (migrations/) | COVERED (CHOICE: split app/models/ theo domain thay vì 1 file models.py — cleaner cho mypy + reading) |
| RESEARCH (PITFALLS.md P1) | pgvector 2000-dim limit — pin 1536 | P02 model Vector(1536) + P04 migration + P05 test_chunks_vector_column_is_1536_dim | COVERED |
| RESEARCH (PITFALLS.md P7) | cocoindex shared schema — Alembic include_object filter | P03 env.py + P05 test_upgrade_does_not_touch_cocoindex_schema | COVERED |
| RESEARCH (PITFALLS.md P17) | HNSW vector_cosine_ops (NOT L2/IP) | P02 model postgresql_ops + P04 migration raw SQL + P05 test_chunks_vector_hnsw_uses_cosine_ops | COVERED |
| RESEARCH (PITFALLS.md P20) | Alembic baseline drift — compare_type, first autogenerate on empty DB | P03 env.py compare_type=True + compare_server_default=True + P05 test_alembic_check_no_drift_after_upgrade | COVERED |
| RESEARCH (PITFALLS.md P8) | Stuck processing status — heartbeat column | P02 Document.last_heartbeat + P04 migration | COVERED |
| RESEARCH (FEATURES.md) | chunks table required columns (content_hash, heading_path, page_start/end, metadata JSONB) | P02 model + P04 migration đầy đủ | COVERED |
| CONTEXT | Không có CONTEXT.md (Phase 2 chưa qua /gsd-discuss-phase) | — | N/A — planner discretion áp dụng theo PROJECT.md + research |
| Threat Model T-02-01 (chunks.hub_id NULL bypass) | NOT NULL + FK CASCADE | P02 + P04 | COVERED |
| Threat Model T-02-02 (cross-hub leak ON DELETE SET NULL) | documents.hub_id ON DELETE CASCADE | P02 + P04 | COVERED |
| Threat Model T-02-03 (refresh_tokens plaintext) | token_hash + UNIQUE | P02 + P04 | COVERED |
| Threat Model T-02-04 (api_keys replay) | key_hash + key_prefix UX | P02 + P04 | COVERED |
| Threat Model T-02-05 (audit log tampering) | accept (M2 schema-level đủ) | P01 threat_model | DOCUMENTED |

**Kết luận audit:** 0 gap, 0 item missing. Mọi source artifact item được cover bởi ít nhất 1 plan task.
