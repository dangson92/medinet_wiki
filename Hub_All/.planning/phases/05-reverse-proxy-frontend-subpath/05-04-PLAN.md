---
phase: 5
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - frontend/src/pages/Login.tsx
  - frontend/src/Layout.tsx
  - frontend/src/services/api.ts
  - frontend/src/pages/__tests__/Login.spec.tsx
  - frontend/src/__tests__/Layout.spec.tsx
autonomous: true
requirements: [PROXY-02, PROXY-04]
tags: [frontend, login, layout, branding, sso-redirect, cross-hub, v3.0, phase5]

must_haves:
  truths:
    - "Login.tsx mount detect: hub con (CURRENT_HUB !== 'central') → useEffect window.location.replace tới central /login?return=/<hub> + skeleton render state C (UI-SPEC §2.3)"
    - "Login.tsx parse ?return param + validate ∈ KNOWN_HUBS (T-5-04 open redirect mitigation) + render branding của returnHub (state A/B/D — UI-SPEC §2)"
    - "Login.tsx render branding logo + title + tagline + themeColor inline CSS var --hub-theme + submit button gradient (D-V3-Phase5-D2 LOCKED)"
    - "Layout.tsx sidebar header logo bg = HUB_BRANDING.themeColor + title = HUB_BRANDING.title (thay hardcode 'Medinet Wiki' + bg-brand-indigo)"
    - "api.ts crossHubSearch method dùng ABSOLUTE path '/api/search/cross-hub' (KHÔNG ${API_BASE} prefix) — D-V3-Phase4-D3 + RESEARCH §7 Option A"
    - "api.ts tryRefresh() fetch options explicit `redirect: 'follow'` — D-V3-Phase5-C4 LOCKED, preserve POST body qua 307 từ hub con → central"
    - "vitest test PASS cover 4 state Login (A/B/C/D) + Layout branding render + crossHubSearch absolute + tryRefresh redirect option"
  artifacts:
    - path: "frontend/src/pages/Login.tsx"
      provides: "4 UX state (A: central no-return, B: central with return, C: hub redirect, D: invalid return fallback) + branding render + themeColor inline"
      min_lines: 200
    - path: "frontend/src/Layout.tsx"
      provides: "Sidebar header replace bg-brand-indigo + 'Medinet Wiki' → HUB_BRANDING (logo + title + themeColor inline) + hover ring themeColor"
    - path: "frontend/src/services/api.ts"
      provides: "crossHubSearch method ABSOLUTE path override + requestAbsolute private helper + tryRefresh redirect: 'follow' explicit audit (D-V3-Phase5-C4)"
      contains: "requestAbsolute"
    - path: "frontend/src/pages/__tests__/Login.spec.tsx"
      provides: "vitest 4 state coverage + return param validation + branding render"
    - path: "frontend/src/__tests__/Layout.spec.tsx"
      provides: "vitest sidebar branding title + logo render từ getBranding(CURRENT_HUB)"
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "getBranding from src/branding + CURRENT_HUB from src/services/api"
      via: "import { getBranding } + import { CURRENT_HUB } from '../services/api'"
      pattern: "import.*getBranding.*from.*branding"
    - from: "frontend/src/Layout.tsx"
      to: "getBranding(CURRENT_HUB) module-level"
      via: "const HUB_BRANDING = getBranding(CURRENT_HUB)"
      pattern: "getBranding\\(CURRENT_HUB\\)"
    - from: "frontend/src/services/api.ts crossHubSearch"
      to: "Caddy /api/* central handle (no strip)"
      via: "fetch absolute '/api/search/cross-hub'"
      pattern: "'/api/search/cross-hub'"
    - from: "frontend/src/services/api.ts tryRefresh"
      to: "Hub con 307 → central /api/auth/refresh (Plan 03-04 SSO-02 carry forward)"
      via: "fetch(url, { method: 'POST', redirect: 'follow', body: ... })"
      pattern: "redirect:\\s*['\"]follow['\"]"
---

<objective>
Wave 3 — Wire branding + SSO redirect + cross-hub absolute path vào Login.tsx + Layout.tsx + api.ts. Consume `getBranding()` + `CURRENT_HUB` + `APP_BASE` từ Plan 05-02 + 05-03. Implement 4 UX state Login (A/B/C/D per UI-SPEC §2) + Layout sidebar swap + api.crossHubSearch ABSOLUTE path override (D-V3-Phase4-D3 + RESEARCH §7 Option A) + audit `tryRefresh()` explicit `redirect: 'follow'` (D-V3-Phase5-C4 LOCKED — preserve POST body qua 307 redirect Plan 03-04 SSO-02 carry forward). 2 vitest test file mới + 1 file extend (api.spec.ts).

Purpose: PROXY-02 + PROXY-04 satisfied — per-hub login branding render + redirect chain hub con → central; carry forward Phase 4 cross-hub strip semantic + Phase 3 SSO 307 chain. T-5-04 open redirect mitigated qua return param validation strict regex + KNOWN_HUBS allowlist check.

Output: 3 file edit + 2 file mới test; 17+ vitest test new (15 + 2 tryRefresh); cumulative full suite ~42 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-02-PLAN.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-03-PLAN.md

<interfaces>
<!-- Exports từ Plan 05-02 (api.ts) sẵn sàng consume -->
From frontend/src/services/api.ts (Plan 05-02 output):
```typescript
export const PREFIX: string | null;
export const API_BASE: string;
export const APP_BASE: string;
export const CURRENT_HUB: string;  // 'central' | <hub_name>
export const api: APIClient;
```

<!-- Existing tryRefresh M2 carry forward — Plan 05-04 Task 4 audits redirect option (D-V3-Phase5-C4) -->
From frontend/src/services/api.ts (M2 line 51-71, BEFORE Plan 05-04 edit):
```typescript
private async tryRefresh(): Promise<boolean> {
  const refreshToken = localStorage.getItem('refresh_token');
  if (!refreshToken) return false;
  try {
    const res = await fetch(`${this.baseURL}/api/auth/refresh`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ refresh_token: refreshToken }),
      // ⚠ MISSING redirect: 'follow' explicit — Plan 05-04 Task 4 ADDS
    });
    ...
  }
}
```

<!-- Exports từ Plan 05-03 (branding) sẵn sàng consume -->
From frontend/src/branding/index.ts (Plan 05-03 output):
```typescript
export interface BrandingConfig { logo, title, tagline, themeColor }
export function getBranding(hub: string): BrandingConfig;
export function getContrastTextColor(themeColor: string): 'white' | 'slate-900';
```

<!-- Existing Login.tsx M2 structure giữ shell, đổi branding inline + add useEffect mount redirect -->
<!-- Existing Layout.tsx M2 sidebar header line ~155-167 — đổi logo bg + title text -->

<!-- T-5-04 open redirect requirement: return param phải validate strict -->
- Strip leading `/` → returnHub
- Validate regex `^[a-z][a-z0-9_]{0,15}$` (Settings hub format Plan 02-05)
- Validate ∈ KNOWN_HUBS (window.__HUB_CONFIG__.allowlist || fallback hardcode)
- Reject absolute URL (`http://`, `//`, `https://`) — would be open redirect

<!-- Cross-hub endpoint contract from Phase 4 D-V3-Phase4-D3 LOCKED -->
- /api/search/cross-hub mount CENTRAL-ONLY (hub con strip → 404 envelope D6)
- Frontend Phase 5: api.crossHubSearch() phải gọi ABSOLUTE path không prefix
- Caddy /api/* handle block (Plan 05-01) → reverse_proxy python-api-central (no strip)

<!-- 307 redirect chain contract from Phase 3 Plan 03-04 SSO-02 LOCKED -->
- Hub con POST /api/auth/refresh → 307 RedirectResponse Location: ${CENTRAL_URL}/api/auth/refresh
- RFC 7231 §6.4.7: 307 MUST preserve method + body (browser auto-follow with body intact)
- Frontend fetch default redirect mode is 'follow' but D-V3-Phase5-C4 mandates EXPLICIT for clarity + audit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Login.tsx wire 4 UX state + branding render + useEffect mount redirect (PROXY-02 + PROXY-04 + T-5-04)</name>
  <files>frontend/src/pages/Login.tsx, frontend/src/pages/__tests__/Login.spec.tsx</files>
  <read_first>
    - frontend/src/pages/Login.tsx (full file — đọc M2 shell hiện tại để giữ form + features + error handling)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md §2 (4 state machine A/B/C/D) + §1.4 (theme color usage rules) + §10.1 (VN copywriting strings)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md §"Implementation Decisions C1 + B4" (LOCKED)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"Pattern 4" lines 345-400 (verbatim snippet)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-04-01 (T-5-04 open redirect mitigation requirement)
  </read_first>
  <behavior>
    - Test State A: URL `/login` (CURRENT_HUB='central', no return) → render Medinet central branding + submit → window.location.replace('/')
    - Test State B: URL `/login?return=/yte` (CURRENT_HUB='central', valid return) → render yte branding + chip "Sau đăng nhập sẽ vào Hub Y tế Medinet" + submit → window.location.replace('/yte/dashboard')
    - Test State C: URL `/yte/login` (CURRENT_HUB='yte') → useEffect fires window.location.replace('http://localhost/login?return=/yte') + render skeleton (KHÔNG render form local — avoid flicker per UI-SPEC §2.3)
    - Test State D: URL `/login?return=/INVALID_HUB` → fallback central branding, no chip, console.warn (T-5-04 mitigation — invalid return rejected)
    - Test T-5-04: URL `/login?return=//evil.com` → fallback central (absolute URL injection rejected)
    - Test T-5-04: URL `/login?return=/postgres` → fallback central (reserved name fails KNOWN_HUBS check)
  </behavior>
  <action>
**Step 1 — EDIT `frontend/src/pages/Login.tsx`:** Giữ phần form M2 (email, password, validation, error handling, account locked state — line ~50-300 M2) NGUYÊN; thêm/đổi 4 phần dưới:

**(A) Imports thêm ở top file (sau dòng `import React, { useState }` hoặc tương đương):**
```typescript
import { useEffect, useMemo } from 'react';
import { useSearchParams } from 'react-router-dom';
import { CURRENT_HUB, APP_BASE } from '../services/api';
import { getBranding, getContrastTextColor, type BrandingConfig } from '../branding';
```

**(B) Component body — thêm logic sau khi đọc existing state hooks (email, password, etc.):**
```typescript
const [searchParams] = useSearchParams();

// T-5-04 mitigation — validate return param strict
const returnHub = useMemo<string | null>(() => {
  const raw = searchParams.get('return');
  if (!raw) return null;
  // Strip leading slash
  const candidate = raw.replace(/^\//, '');
  // 1) Reject absolute URL injection (//evil.com, http://, https://)
  if (raw.startsWith('//') || raw.includes('://')) return null;
  // 2) Validate hub format Settings regex (Plan 02-05 FACTOR-04)
  if (!/^[a-z][a-z0-9_]{0,15}$/.test(candidate)) return null;
  // 3) Validate ∈ KNOWN_HUBS (runtime config or fallback hardcode)
  const knownHubs: readonly string[] =
    (typeof window !== 'undefined' && window.__HUB_CONFIG__?.allowlist) ?? ['yte', 'duoc', 'hcns'];
  if (!knownHubs.includes(candidate)) {
    if (import.meta.env?.DEV) console.warn('[login] Invalid return hub param rejected:', candidate);
    return null;
  }
  return candidate;
}, [searchParams]);

// State C: hub con direct visit → redirect tới central /login?return=/<hub>
useEffect(() => {
  if (CURRENT_HUB !== 'central') {
    // W-05 fix: Use origin (not host) to inherit protocol — safer against HTTP/HTTPS mismatch in dev environment.
    // window.location.origin includes protocol (https://localhost), while window.location.host is host-only (localhost).
    const target = `${window.location.origin}/login?return=/${CURRENT_HUB}`;
    window.location.replace(target);
  }
}, []);

// Branding resolution: hub con state C dùng CURRENT_HUB; central state A/B/D dùng returnHub || 'central'
const branding: BrandingConfig = useMemo(() => {
  if (CURRENT_HUB !== 'central') {
    return getBranding(CURRENT_HUB); // State C — render skeleton với theme của hub đang ở
  }
  return getBranding(returnHub || 'central'); // State A (no return) / B (valid return) / D (invalid → central fallback)
}, [returnHub]);

const contrastText = getContrastTextColor(branding.themeColor);
```

**(C) Early return cho State C — skeleton trước khi redirect resolve (UI-SPEC §2.3):**
```tsx
if (CURRENT_HUB !== 'central') {
  return (
    <div
      className="min-h-screen flex flex-col items-center justify-center"
      style={{
        ['--hub-theme' as string]: branding.themeColor,
        background: `linear-gradient(135deg, ${branding.themeColor}, color-mix(in srgb, ${branding.themeColor} 60%, black))`,
      } as React.CSSProperties}
    >
      <div className="animate-spin rounded-full h-10 w-10 border-4 border-white border-t-transparent" />
      <p className="mt-4 text-sm font-medium text-white/80">Đang chuyển đến trang đăng nhập trung tâm...</p>
    </div>
  );
}
```

**(D) Render branding ở existing Login form shell — wrap outermost container thêm style `--hub-theme` CSS var + thay hero gradient indigo cứng:**

Outermost container thêm:
```tsx
<div
  className="relative flex min-h-screen overflow-x-hidden font-sans"
  style={{
    ['--hub-theme' as string]: branding.themeColor,
  } as React.CSSProperties}
>
```

Hero left column background (replace indigo gradient cứng M2):
```tsx
<div
  className="hidden lg:flex lg:w-1/2 ..."
  style={{
    background: `linear-gradient(135deg, var(--hub-theme) 0%, color-mix(in srgb, var(--hub-theme) 80%, black) 52%, color-mix(in srgb, var(--hub-theme) 60%, black) 100%)`,
  }}
>
  {/* Top-left logo + title + tagline (UI-SPEC §1.2 LOCKED) */}
  <div className="flex items-center gap-3 mb-12">
    <div className="flex h-11 w-11 items-center justify-center rounded-[14px] bg-white shadow-lg">
      <img src={branding.logo} alt={branding.title} className="w-7 h-7 object-contain" />
    </div>
    <div>
      <h1 className="text-lg font-bold leading-tight tracking-tight text-white">{branding.title}</h1>
      <p className="text-[12px] text-white/60">{branding.tagline}</p>
    </div>
  </div>
  {/* M2 hero headline + subtagline + features list giữ NGUYÊN */}
</div>
```

Form panel logo bubble (replace `bg-indigo-100` M2 với inline themeColor 10%):
```tsx
<div
  className="mx-auto mb-5 flex h-16 w-16 items-center justify-center rounded-2xl"
  style={{ backgroundColor: `color-mix(in srgb, var(--hub-theme) 10%, white)` }}
>
  <img src={branding.logo} alt={branding.title} className="w-10 h-10 object-contain"
    onError={(e) => { (e.currentTarget as HTMLImageElement).style.display = 'none'; }} />
</div>
```

Form panel subline (dynamic title):
```tsx
<p className="text-sm text-slate-500 dark:text-slate-400">
  Chào mừng bạn trở lại {branding.title}
</p>
```

State B chip (chỉ render khi returnHub valid):
```tsx
{returnHub && (
  <div
    className="mx-auto mt-3 inline-flex items-center gap-2 px-3 py-1.5 rounded-full text-xs font-medium"
    style={{
      backgroundColor: `color-mix(in srgb, var(--hub-theme) 12%, white)`,
      color: 'var(--hub-theme)',
    }}
  >
    <span>Sau đăng nhập sẽ vào {branding.title}</span>
  </div>
)}
```

Submit button — replace M2 indigo gradient với themeColor:
```tsx
<button
  type="submit"
  className="w-full rounded-xl py-3 px-4 font-semibold transition-all"
  style={{
    background: `linear-gradient(135deg, var(--hub-theme), color-mix(in srgb, var(--hub-theme) 85%, white))`,
    color: contrastText === 'slate-900' ? '#0f172a' : 'white',
    boxShadow: `0 10px 30px -8px color-mix(in srgb, var(--hub-theme) 55%, transparent)`,
  }}
  disabled={isLoading}
>
  {isLoading ? 'Đang đăng nhập...' : 'Đăng nhập'}
</button>
```

**(E) handleSubmit success redirect cross-prefix qua window.location.replace:**
```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  // ... existing validation
  const result = await login(email, password);
  if (result.success) {
    // Cross-prefix redirect — window.location reset basename + module-level api.ts re-compute
    // (KHÔNG dùng react-router navigate — basename scope hiện tại)
    const dest = returnHub ? `/${returnHub}/dashboard` : '/';
    window.location.replace(dest);
    return;
  }
  // ... existing error handling
};
```

GIỮ NGUYÊN M2: email + password input fields, validation logic, account locked state, Google button shell, footer "Chưa có tài khoản?", copyright text. KHÔNG đụng features list, hero headline VN.

**Step 2 — Tạo `frontend/src/pages/__tests__/Login.spec.tsx` mới:**

```tsx
// Phase 5 PROXY-02 + PROXY-04 unit test — Login 4 state machine + T-5-04 mitigation
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-04-01
//         .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md §2

import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

// Mock react-router useSearchParams — dynamic per test via window.location.search
async function renderLoginAtUrl(opts: { pathname: string; search?: string }) {
  vi.resetModules();
  Object.defineProperty(window, 'location', {
    value: {
      ...window.location,
      pathname: opts.pathname,
      search: opts.search ?? '',
      href: `http://localhost${opts.pathname}${opts.search ?? ''}`,
      origin: 'http://localhost',
      replace: vi.fn(), // spy on window.location.replace
    },
    writable: true,
  });

  // Mock AuthContext - login function returns success
  vi.doMock('../../contexts/AuthContext', () => ({
    useAuth: () => ({
      login: vi.fn().mockResolvedValue({ success: true }),
      isLoading: false,
      isAuthenticated: false,
    }),
    AuthProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  }));

  const { default: Login } = await import('../Login');
  return render(
    <MemoryRouter initialEntries={[`${opts.pathname}${opts.search ?? ''}`]}>
      <Login />
    </MemoryRouter>
  );
}

describe('Phase 5 — Login.tsx UX state machine', () => {
  beforeEach(() => {
    vi.resetModules();
    delete (window as unknown as { __HUB_CONFIG__?: unknown }).__HUB_CONFIG__;
    localStorage.clear();
  });

  it('State A: central no-return → render Medinet central branding', async () => {
    await renderLoginAtUrl({ pathname: '/login' });
    await waitFor(() => {
      expect(screen.queryByText(/Medinet Wiki/i)).toBeInTheDocument();
    });
  });

  it('State B: central with valid return=/yte → render yte branding + chip', async () => {
    await renderLoginAtUrl({ pathname: '/login', search: '?return=/yte' });
    await waitFor(() => {
      expect(screen.queryByText(/Hub Y tế Medinet/i)).toBeInTheDocument();
      expect(screen.queryByText(/Sau đăng nhập sẽ vào/i)).toBeInTheDocument();
    });
  });

  it('State D: invalid return=/INVALID_HUB → silent fallback central + no chip', async () => {
    await renderLoginAtUrl({ pathname: '/login', search: '?return=/INVALID_HUB' });
    await waitFor(() => {
      expect(screen.queryByText(/Medinet Wiki/i)).toBeInTheDocument();
      expect(screen.queryByText(/Sau đăng nhập sẽ vào/i)).not.toBeInTheDocument();
    });
  });

  it('T-5-04: return=//evil.com (absolute URL injection) → rejected, central fallback', async () => {
    await renderLoginAtUrl({ pathname: '/login', search: '?return=//evil.com' });
    await waitFor(() => {
      expect(screen.queryByText(/Medinet Wiki/i)).toBeInTheDocument();
      expect(screen.queryByText(/Sau đăng nhập sẽ vào/i)).not.toBeInTheDocument();
    });
  });

  it('T-5-04: return=/postgres (reserved name) → rejected (not in KNOWN_HUBS), central fallback', async () => {
    await renderLoginAtUrl({ pathname: '/login', search: '?return=/postgres' });
    await waitFor(() => {
      expect(screen.queryByText(/Medinet Wiki/i)).toBeInTheDocument();
    });
  });

  it('State C placeholder: hub con direct visit useEffect should trigger window.location.replace', async () => {
    // Note: jsdom KHÔNG fully simulate window.location.pathname trong React-router context;
    // testing useEffect side-effect spy on window.location.replace mock.
    // Full state-C test reality verify ở manual smoke Plan 05-06.
    await renderLoginAtUrl({ pathname: '/yte/login' });
    // Skeleton text "Đang chuyển đến trang đăng nhập trung tâm..." render before redirect
    // (in real browser, replace would unmount component; in jsdom replace is spy stub)
    // This test verifies the skeleton text exists in render output OR window.location.replace was called
    await waitFor(() => {
      const replaceMock = window.location.replace as ReturnType<typeof vi.fn>;
      // Either redirect was scheduled OR skeleton renders
      const hasRedirect = (replaceMock as any).mock.calls.length > 0;
      const hasSkeleton = !!screen.queryByText(/Đang chuyển đến trang đăng nhập trung tâm/i);
      expect(hasRedirect || hasSkeleton).toBe(true);
    });
  });
});
```

**Threat mitigation:**
- T-5-04 (Login ?return open redirect): Strict validation chain — strip slash + reject `//` + reject `://` + regex hub format + KNOWN_HUBS allowlist. 3 test (T-5-04 explicit) verify behavior.
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npx vitest run src/pages/__tests__/Login.spec.tsx --reporter=verbose &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { useEffect, useMemo } from 'react'" Hub_All/frontend/src/pages/Login.tsx` exit 0
    - `grep -q "import { CURRENT_HUB, APP_BASE }" Hub_All/frontend/src/pages/Login.tsx` exit 0
    - `grep -q "import { getBranding" Hub_All/frontend/src/pages/Login.tsx` exit 0
    - `grep -q "useSearchParams" Hub_All/frontend/src/pages/Login.tsx` exit 0
    - `grep -q "window.location.replace" Hub_All/frontend/src/pages/Login.tsx` exit 0 (useEffect redirect + submit success)
    - `grep -q "window.location.origin" Hub_All/frontend/src/pages/Login.tsx` exit 0 (W-05 fix — origin not host, inherit protocol safe HTTP/HTTPS dev)
    - `! grep -q "window.location.host[^a-zA-Z_]" Hub_All/frontend/src/pages/Login.tsx` exit 0 (W-05 negation — KHÔNG dùng host bare, chỉ origin)
    - `grep -q "Use origin (not host)" Hub_All/frontend/src/pages/Login.tsx` exit 0 (W-05 code comment present)
    - `grep -q "Đang chuyển đến trang đăng nhập trung tâm" Hub_All/frontend/src/pages/Login.tsx` exit 0 (State C skeleton)
    - `grep -q "Sau đăng nhập sẽ vào" Hub_All/frontend/src/pages/Login.tsx` exit 0 (State B chip)
    - `grep -q "color-mix(in srgb" Hub_All/frontend/src/pages/Login.tsx` exit 0 (themeColor blend)
    - `grep -q "'--hub-theme'" Hub_All/frontend/src/pages/Login.tsx` exit 0 (CSS variable inline)
    - `grep -q "knownHubs.includes(candidate)" Hub_All/frontend/src/pages/Login.tsx` exit 0 (T-5-04 allowlist check)
    - `grep -q "raw.startsWith('//') || raw.includes('://')" Hub_All/frontend/src/pages/Login.tsx` exit 0 (T-5-04 absolute URL reject)
    - `test -f Hub_All/frontend/src/pages/__tests__/Login.spec.tsx` exit 0
    - `cd Hub_All/frontend && npx vitest run src/pages/__tests__/Login.spec.tsx` exit 0; 6+ test PASS
    - `cd Hub_All/frontend && npm run lint` PASS
  </acceptance_criteria>
  <done>
    Login.tsx 4 state machine wire + branding themeColor inline CSS var + T-5-04 strict return validation + W-05 origin (not host) protocol-safe. 6+ vitest test PASS. M2 form shell + features + error handling preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Layout.tsx sidebar header swap branding + vitest test (PROXY-04 + UI-SPEC §3)</name>
  <files>frontend/src/Layout.tsx, frontend/src/__tests__/Layout.spec.tsx</files>
  <read_first>
    - frontend/src/Layout.tsx (full file — đọc sidebar header ~line 150-170 hiện M2 hardcode "Medinet Wiki" + bg-brand-indigo logo)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md §3 (sidebar header contract + collapse behavior + hover ring themeColor)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md §"Implementation Decisions D2" (scope LOCKED — chỉ sidebar header touch, KHÔNG cascade)
  </read_first>
  <behavior>
    - Test 1: CURRENT_HUB='central' → Layout sidebar render text "Medinet Wiki" + logo bg indigo
    - Test 2: CURRENT_HUB='yte' → Layout sidebar render text "Hub Y tế Medinet" + logo bg emerald
    - Test 3: Logo img src match `/branding/<hub>/logo.svg` schema
  </behavior>
  <action>
**Step 1 — EDIT `frontend/src/Layout.tsx`:**

**(A) Imports thêm top file:**
```typescript
import { getBranding, getContrastTextColor } from './branding';
import { CURRENT_HUB } from './services/api';
import { Database } from 'lucide-react'; // existing M2 import, ensure available
```

**(B) Module-level branding resolution (sau imports, trước component):**
```typescript
// Phase 5 PROXY-04 — branding resolution module-level (compute 1 lần)
const HUB_BRANDING = getBranding(CURRENT_HUB);
const HUB_CONTRAST = getContrastTextColor(HUB_BRANDING.themeColor);
```

**(C) Outermost wrapper inject CSS variable `--hub-theme` (UI-SPEC §8.2):**
```tsx
return (
  <div
    className="flex h-screen bg-slate-50 dark:bg-slate-950 overflow-hidden relative"
    style={{ ['--hub-theme' as string]: HUB_BRANDING.themeColor } as React.CSSProperties}
  >
    {/* ... existing children */}
  </div>
);
```

**(D) Sidebar header — locate line ~155-167 M2 với logo bg-brand-indigo + "Medinet Wiki" text → REPLACE:**

Find existing M2 block tương tự:
```tsx
<div className="w-8 h-8 bg-brand-indigo rounded-xl ...">
  <Database size={18} className="text-white" />
</div>
<span className="font-bold text-slate-900 dark:text-white tracking-tight text-lg">Medinet Wiki</span>
```

REPLACE bằng:
```tsx
<div
  className="w-8 h-8 rounded-xl flex items-center justify-center shadow-lg shrink-0"
  style={{ backgroundColor: HUB_BRANDING.themeColor }}
>
  <img
    src={HUB_BRANDING.logo}
    alt={HUB_BRANDING.title}
    className="w-5 h-5 object-contain"
    onError={(e) => {
      (e.currentTarget as HTMLImageElement).style.display = 'none';
      const fallback = (e.currentTarget as HTMLImageElement).nextElementSibling as HTMLElement | null;
      if (fallback) fallback.style.display = 'block';
    }}
    style={{ filter: HUB_CONTRAST === 'slate-900' ? 'invert(1)' : 'none' }}
  />
  <Database
    size={18}
    className="hidden"
    style={{ color: HUB_CONTRAST === 'slate-900' ? '#0f172a' : 'white' }}
  />
</div>
<span className="font-bold text-slate-900 dark:text-white tracking-tight text-lg truncate max-w-[160px]">
  {HUB_BRANDING.title}
</span>
```

**(E) Profile avatar hover ring (UI-SPEC §3.4) — find existing `hover:ring-brand-indigo/30` → replace inline:**
```tsx
<div
  className="hover:ring-2 transition-all"
  style={{ ['--tw-ring-color' as string]: `color-mix(in srgb, ${HUB_BRANDING.themeColor} 30%, transparent)` } as React.CSSProperties}
>
  {/* avatar */}
</div>
```

GIỮ NGUYÊN M2 (UI-SPEC §1.4 LOCKED scope minimal):
- Sidebar nav active item `bg-brand-indigo` cứng (defer Tailwind cascade v4.0).
- Sidebar nav hover state.
- Header bell, theme toggle, search field.
- 11 trang nội dung route children (Dashboard, Documents, etc).
- Sidebar collapse behavior (motion.aside animate width).

**Step 2 — Tạo `frontend/src/__tests__/Layout.spec.tsx` mới:**

```tsx
// Phase 5 PROXY-04 unit test — Layout sidebar branding render
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-04-02
//         .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md §3

import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

async function renderLayoutWithCurrentHub(hub: string) {
  vi.resetModules();
  Object.defineProperty(window, 'location', {
    value: {
      ...window.location,
      pathname: hub === 'central' ? '/' : `/${hub}/`,
      href: `http://localhost/${hub === 'central' ? '' : hub + '/'}`,
      origin: 'http://localhost',
    },
    writable: true,
  });

  vi.doMock('../contexts/AuthContext', () => ({
    useAuth: () => ({
      isAuthenticated: true,
      user: { id: 'u1', name: 'Test User', email: 'test@medinet.vn', role: 'admin' },
      logout: vi.fn(),
      isLoading: false,
    }),
    AuthProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  }));

  vi.doMock('../contexts/ThemeContext', () => ({
    useTheme: () => ({ theme: 'light', toggleTheme: vi.fn() }),
    ThemeProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  }));

  const { default: Layout } = await import('../Layout');
  return render(
    <MemoryRouter>
      <Layout />
    </MemoryRouter>
  );
}

describe('Phase 5 PROXY-04 — Layout sidebar branding', () => {
  beforeEach(() => {
    vi.resetModules();
    delete (window as unknown as { __HUB_CONFIG__?: unknown }).__HUB_CONFIG__;
  });

  it('CURRENT_HUB=central → renders "Medinet Wiki" title in sidebar', async () => {
    await renderLayoutWithCurrentHub('central');
    expect(screen.queryByText(/Medinet Wiki/i)).toBeInTheDocument();
  });

  it('CURRENT_HUB=yte → renders "Hub Y tế Medinet" title in sidebar', async () => {
    await renderLayoutWithCurrentHub('yte');
    expect(screen.queryByText(/Hub Y tế Medinet/i)).toBeInTheDocument();
  });

  it('CURRENT_HUB=duoc → renders "Hub Dược Medinet" title in sidebar', async () => {
    await renderLayoutWithCurrentHub('duoc');
    expect(screen.queryByText(/Hub Dược Medinet/i)).toBeInTheDocument();
  });

  it('Logo img src matches /branding/<hub>/logo.svg schema', async () => {
    await renderLayoutWithCurrentHub('yte');
    const logo = document.querySelector('img[src*="/branding/"]');
    expect(logo).not.toBeNull();
    expect((logo as HTMLImageElement).src).toMatch(/\/branding\/yte\/logo\.svg$/);
  });
});
```

**Threat mitigation:**
- KHÔNG mới risk vector (Layout consume branding registry locked schema từ Plan 05-03 Task 1).
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npx vitest run src/__tests__/Layout.spec.tsx --reporter=verbose &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { getBranding" Hub_All/frontend/src/Layout.tsx` exit 0
    - `grep -q "import { CURRENT_HUB } from './services/api'" Hub_All/frontend/src/Layout.tsx` exit 0
    - `grep -q "const HUB_BRANDING = getBranding(CURRENT_HUB)" Hub_All/frontend/src/Layout.tsx` exit 0
    - `grep -q "HUB_BRANDING.title" Hub_All/frontend/src/Layout.tsx` exit 0 (dynamic title)
    - `grep -q "backgroundColor: HUB_BRANDING.themeColor" Hub_All/frontend/src/Layout.tsx` exit 0 (logo bg inline)
    - `grep -q "src={HUB_BRANDING.logo}" Hub_All/frontend/src/Layout.tsx` exit 0 (img src)
    - `grep -q "'--hub-theme'" Hub_All/frontend/src/Layout.tsx` exit 0 (CSS var outermost wrapper)
    - KHÔNG còn hardcode "Medinet Wiki" trong sidebar header span: regex check `grep -c "Medinet Wiki" Hub_All/frontend/src/Layout.tsx` ≤ 1 (chỉ còn ở fallback nếu có)
    - `test -f Hub_All/frontend/src/__tests__/Layout.spec.tsx` exit 0
    - `cd Hub_All/frontend && npx vitest run src/__tests__/Layout.spec.tsx` exit 0; 4 test PASS
    - `cd Hub_All/frontend && npm run lint` PASS
  </acceptance_criteria>
  <done>
    Layout.tsx sidebar header logo bg + title swap qua HUB_BRANDING từ getBranding(CURRENT_HUB) module-level. 4 vitest test PASS. M2 nav items + active state + collapse behavior preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: api.ts crossHubSearch absolute path override + requestAbsolute helper (PROXY-02 + D-V3-Phase4-D3 + RESEARCH §7 Option A)</name>
  <files>frontend/src/services/api.ts, frontend/src/services/__tests__/api.spec.ts</files>
  <read_first>
    - frontend/src/services/api.ts (sau Plan 05-02 edit — đọc class APIClient hiện tại + tìm existing crossHubSearch method M2)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"Cross-hub UI Redirect Handling" lines 826-893 (Option A FE absolute path)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md §5.1 (api.crossHubSearch absolute path contract)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-04-03 (assert fetch called với /api/search/cross-hub không prefix)
  </read_first>
  <behavior>
    - Test 1: api.crossHubSearch(data) gọi fetch với absolute path '/api/search/cross-hub' (KHÔNG '${API_BASE}/search/cross-hub')
    - Test 2: requestAbsolute helper auth header Bearer token nếu localStorage có access_token
    - Test 3: 401 handling — request retry sau refresh
  </behavior>
  <action>
**Step 1 — EDIT `frontend/src/services/api.ts`:**

**(A) Thêm private helper `requestAbsolute<T>` trong class APIClient (sibling của private `request`):**

```typescript
class APIClient {
  // ... existing private baseURL, getToken, request, tryRefresh

  /**
   * Phase 5 D-V3-Phase4-D3 + RESEARCH §7 Option A — gọi central absolute path
   * KHÔNG dùng this.baseURL prefix. Dùng cho endpoint central-only như /api/search/cross-hub
   * mà hub con strip (FACTOR-02 extend Phase 4).
   *
   * Caddy /api/* handle block (Plan 05-01) → reverse_proxy python-api-central:8080 (no strip).
   */
  private async requestAbsolute<T>(method: string, absolutePath: string, body?: unknown): Promise<APIResponse<T>> {
    const headers: Record<string, string> = { 'Content-Type': 'application/json' };
    const token = this.getToken();
    if (token) headers['Authorization'] = `Bearer ${token}`;

    const res = await fetch(absolutePath, {
      method,
      headers,
      body: body ? JSON.stringify(body) : undefined,
    });

    if (res.status === 401) {
      const refreshed = await this.tryRefresh();
      if (refreshed) {
        headers['Authorization'] = `Bearer ${this.getToken()}`;
        const retry = await fetch(absolutePath, { method, headers, body: body ? JSON.stringify(body) : undefined });
        return retry.json();
      }
      localStorage.removeItem('access_token');
      localStorage.removeItem('refresh_token');
      window.location.href = '/login';
      throw new Error('Session expired');
    }

    return res.json();
  }
}
```

**(B) REPLACE existing `crossHubSearch` method M2 (nếu tồn tại) — hoặc ADD nếu chưa có:**

```typescript
class APIClient {
  // ... existing methods ...

  /**
   * Cross-hub search — central-only endpoint (D-V3-Phase4-D3 LOCKED Phase 4 + RESEARCH §7 Option A).
   *
   * CRITICAL: Always uses ABSOLUTE path '/api/search/cross-hub' (KHÔNG ${this.baseURL} prefix).
   * Lý do: hub con strip endpoint (FACTOR-02 extend) → 404 envelope D6. Frontend phải bypass
   * API_BASE prefix để đảm bảo reach central qua Caddy /api/* handle (Plan 05-01).
   */
  async crossHubSearch(data: { query: string; hub_ids?: string[]; limit?: number }): Promise<APIResponse<{ results: unknown[]; total_hubs_searched: number; query_time_ms: number; cache_hit: boolean }>> {
    return this.requestAbsolute('POST', '/api/search/cross-hub', data);
  }
}
```

Nếu M2 đã có method `crossHubSearch` dùng `this.request` → REPLACE để dùng `requestAbsolute`.

**Step 2 — Extend `frontend/src/services/__tests__/api.spec.ts` (file Plan 05-02 đã tạo) thêm describe block:**

```typescript
// ... existing tests Plan 05-02 ...

describe('Phase 5 PROXY-02 — api.crossHubSearch ABSOLUTE path override', () => {
  beforeEach(() => {
    vi.resetModules();
    delete (window as unknown as { __HUB_CONFIG__?: unknown }).__HUB_CONFIG__;
  });

  it('Test 8: crossHubSearch fetches ABSOLUTE /api/search/cross-hub (NOT ${API_BASE}/search/cross-hub)', async () => {
    // Mock window.location to hub con yte → API_BASE would be /yte/api
    Object.defineProperty(window, 'location', {
      value: {
        ...window.location,
        pathname: '/yte/dashboard',
        href: 'http://localhost/yte/dashboard',
        origin: 'http://localhost',
      },
      writable: true,
    });

    const fetchSpy = vi.spyOn(global, 'fetch').mockResolvedValue({
      status: 200,
      json: async () => ({ success: true, data: { results: [], total_hubs_searched: 0, query_time_ms: 10, cache_hit: false } }),
    } as Response);

    vi.resetModules();
    const { api } = await import('../api');
    await api.crossHubSearch({ query: 'test', hub_ids: ['yte'] });

    expect(fetchSpy).toHaveBeenCalled();
    const calledUrl = fetchSpy.mock.calls[0][0];
    // Critical assertion: absolute path /api/search/cross-hub (NOT /yte/api/search/cross-hub)
    expect(calledUrl).toBe('/api/search/cross-hub');
    expect(calledUrl).not.toMatch(/^\/yte\//);

    fetchSpy.mockRestore();
  });

  it('Test 9: crossHubSearch attaches Bearer token if access_token in localStorage', async () => {
    localStorage.setItem('access_token', 'fake-jwt-token');

    const fetchSpy = vi.spyOn(global, 'fetch').mockResolvedValue({
      status: 200,
      json: async () => ({ success: true }),
    } as Response);

    vi.resetModules();
    const { api } = await import('../api');
    await api.crossHubSearch({ query: 'test' });

    const fetchOptions = fetchSpy.mock.calls[0][1] as RequestInit;
    expect((fetchOptions.headers as Record<string, string>)['Authorization']).toBe('Bearer fake-jwt-token');

    fetchSpy.mockRestore();
    localStorage.removeItem('access_token');
  });
});
```

**Threat mitigation:**
- KHÔNG mới risk vector — same-origin same auth pattern M2 carry forward.
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npx vitest run src/services/__tests__/api.spec.ts --reporter=verbose &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "requestAbsolute" Hub_All/frontend/src/services/api.ts` exit 0 (private helper exist)
    - `grep -q "async crossHubSearch" Hub_All/frontend/src/services/api.ts` exit 0 (method exist)
    - `grep -q "'/api/search/cross-hub'" Hub_All/frontend/src/services/api.ts` exit 0 (absolute path literal)
    - `grep -q "D-V3-Phase4-D3" Hub_All/frontend/src/services/api.ts` exit 0 (comment reference)
    - `cd Hub_All/frontend && npx vitest run src/services/__tests__/api.spec.ts` exit 0; 9+ test PASS (7 Plan 05-02 + 2 new)
    - `cd Hub_All/frontend && npm run lint` PASS
  </acceptance_criteria>
  <done>
    api.ts crossHubSearch dùng absolute '/api/search/cross-hub' qua requestAbsolute helper bypass API_BASE prefix. 9+ vitest test PASS verify behavior. D-V3-Phase4-D3 (hub con strip) carry forward semantic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Audit api.ts tryRefresh() explicit `redirect: 'follow'` (B-02 fix — D-V3-Phase5-C4 LOCKED)</name>
  <files>frontend/src/services/api.ts, frontend/src/services/__tests__/api.spec.ts</files>
  <read_first>
    - frontend/src/services/api.ts (line 51-71 — M2 tryRefresh method carried forward through Plan 05-02 + Task 3)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md §"Implementation Decisions C4" (D-V3-Phase5-C4 LOCKED — verify `redirect: 'follow'` explicit khi build fetch options)
    - .planning/phases/03-auth-sso-hub-ids-jwt/03-04-PLAN.md (Plan 03-04 SSO-02 — hub con `/api/auth/refresh` 307 RedirectResponse Location: ${CENTRAL_URL}/api/auth/refresh)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md §"Pitfall 3" / RFC 7231 §6.4.7 (307 MUST preserve POST + body)
  </read_first>
  <behavior>
    - Test 10: tryRefresh() fetch is invoked with options object containing `redirect: 'follow'` explicit (audit assertion via fetchSpy.toHaveBeenCalledWith expect.objectContaining({ redirect: 'follow' }))
    - Test 11 (optional regression): grep regex check `redirect:\s*['"]follow['"]` matches inside api.ts tryRefresh region
  </behavior>
  <action>
**Step 1 — Audit existing `tryRefresh()` method in `frontend/src/services/api.ts` (line ~51-71):**

Read current fetch call inside `tryRefresh()`:
```typescript
const res = await fetch(`${this.baseURL}/api/auth/refresh`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ refresh_token: refreshToken }),
});
```

**If the fetch options object does NOT have `redirect: 'follow'` explicit → ADD it.**
**If it already does → leave it but ENSURE the D-V3-Phase5-C4 comment is present.**

**Step 2 — REPLACE the tryRefresh fetch call with:**

```typescript
private async tryRefresh(): Promise<boolean> {
  const refreshToken = localStorage.getItem('refresh_token');
  if (!refreshToken) return false;
  try {
    // D-V3-Phase5-C4 LOCKED — preserve POST body through 307 redirect from hub con → central
    // Plan 03-04 SSO-02: hub con POST /api/auth/refresh → 307 RedirectResponse Location: ${CENTRAL_URL}/api/auth/refresh
    // RFC 7231 §6.4.7: 307 MUST preserve method + body. Browser fetch default redirect mode is 'follow'
    // but we set EXPLICIT for clarity + audit trail (B-02 fix per plan-checker iteration 1).
    const res = await fetch(`${this.baseURL}/api/auth/refresh`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ refresh_token: refreshToken }),
      redirect: 'follow',
    });
    if (!res.ok) return false;
    const data = await res.json();
    if (data.success && data.data) {
      localStorage.setItem('access_token', data.data.access_token);
      localStorage.setItem('refresh_token', data.data.refresh_token);
      return true;
    }
    return false;
  } catch {
    return false;
  }
}
```

**Step 3 — Extend `frontend/src/services/__tests__/api.spec.ts` thêm describe block (sau Task 3 crossHubSearch tests):**

```typescript
describe('Phase 5 D-V3-Phase5-C4 — api.tryRefresh redirect: follow audit', () => {
  beforeEach(() => {
    vi.resetModules();
    delete (window as unknown as { __HUB_CONFIG__?: unknown }).__HUB_CONFIG__;
    localStorage.setItem('refresh_token', 'fake-refresh-token');
  });

  afterEach(() => {
    localStorage.removeItem('refresh_token');
  });

  it('Test 10: tryRefresh() fetch is called with explicit `redirect: "follow"` option', async () => {
    // Mock to hub con yte → tryRefresh would POST /yte/api/auth/refresh → backend 307 → central
    Object.defineProperty(window, 'location', {
      value: {
        ...window.location,
        pathname: '/yte/dashboard',
        href: 'http://localhost/yte/dashboard',
        origin: 'http://localhost',
      },
      writable: true,
    });

    const fetchSpy = vi.spyOn(global, 'fetch').mockResolvedValue({
      ok: true,
      status: 200,
      json: async () => ({
        success: true,
        data: { access_token: 'new-access', refresh_token: 'new-refresh' },
      }),
    } as Response);

    vi.resetModules();
    const { api } = await import('../api');

    // Indirect invocation: tryRefresh is private; trigger via 401 path on api.request
    // Use direct spy approach — invoke tryRefresh via a 401-triggering call
    // Alternative: call any authenticated endpoint, fetch returns 401, retry triggers tryRefresh

    // Simpler: mock first fetch 401, second fetch (tryRefresh) success
    fetchSpy.mockResolvedValueOnce({
      status: 401,
      json: async () => ({ success: false }),
    } as Response);
    fetchSpy.mockResolvedValueOnce({
      ok: true,
      status: 200,
      json: async () => ({
        success: true,
        data: { access_token: 'new-access', refresh_token: 'new-refresh' },
      }),
    } as Response);
    fetchSpy.mockResolvedValueOnce({
      status: 200,
      json: async () => ({ success: true }),
    } as Response);

    // Trigger via a stub authenticated call — e.g., api.refreshUser() or any GET that hits 401
    try {
      await api.refreshUser?.();
    } catch {
      // ignore; we only care about fetch call assertion
    }

    // Assert: at least one fetch call to /api/auth/refresh had redirect: 'follow'
    const refreshCall = fetchSpy.mock.calls.find((call) =>
      String(call[0]).includes('/api/auth/refresh')
    );
    expect(refreshCall).toBeDefined();
    expect(refreshCall![1]).toEqual(
      expect.objectContaining({ redirect: 'follow' })
    );

    fetchSpy.mockRestore();
  });

  it('Test 11: api.ts source contains `redirect: "follow"` in tryRefresh region (regex grep equivalent)', async () => {
    // Smoke regression — verify code-level presence via source string match.
    // Equivalent to: grep -q 'redirect:\s*['\"]follow['\"]' src/services/api.ts
    const apiSource = await import('../api?raw').catch(() => null);
    // If ?raw import unsupported in vitest config, fallback to fs read in test
    if (apiSource && typeof (apiSource as any).default === 'string') {
      expect((apiSource as any).default).toMatch(/redirect:\s*['"]follow['"]/);
    } else {
      // Fallback: file system read via node:fs
      const fs = await import('node:fs/promises');
      const src = await fs.readFile('src/services/api.ts', 'utf-8');
      expect(src).toMatch(/redirect:\s*['"]follow['"]/);
    }
  });
});
```

**Threat mitigation:**
- T-5-12 (basename mismatch infinite redirect): explicit `redirect: 'follow'` ensures browser auto-follows 307 chain without manual handling — KHÔNG infinite loop possible because Plan 03-04 backend issues 307 ONLY once (hub con → central, central handles directly).
- KHÔNG mới risk vector (D-V3-Phase5-C4 LOCKED carry forward Phase 3 307 semantic).
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npx vitest run src/services/__tests__/api.spec.ts --reporter=verbose &amp;&amp; npm run lint &amp;&amp; grep -qE "redirect:\s*['\"]follow['\"]" frontend/src/services/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE "redirect:\s*['\"]follow['\"]" Hub_All/frontend/src/services/api.ts` exit 0 (B-02 core fix — explicit redirect option present)
    - `grep -q "D-V3-Phase5-C4" Hub_All/frontend/src/services/api.ts` exit 0 (decision reference comment in tryRefresh region)
    - `grep -q "preserve POST body through 307 redirect" Hub_All/frontend/src/services/api.ts` exit 0 (B-02 code comment present)
    - `cd Hub_All/frontend && npx vitest run src/services/__tests__/api.spec.ts -t "redirect"` exit 0; Test 10 + 11 PASS (vitest assertion on fetch options + source-level regex check)
    - Full api.spec.ts suite: `cd Hub_All/frontend && npx vitest run src/services/__tests__/api.spec.ts` exit 0; 11+ test PASS cumulative (7 prefix detect + 2 crossHubSearch + 2 tryRefresh redirect audit)
    - `cd Hub_All/frontend && npm run lint` PASS (tsc --noEmit — RequestInit type accepts `redirect: 'follow' | 'error' | 'manual'`)
  </acceptance_criteria>
  <done>
    api.ts tryRefresh() fetch options explicit `redirect: 'follow'` per D-V3-Phase5-C4 LOCKED. Code comment references decision + Plan 03-04 SSO-02 307 chain + RFC 7231 §6.4.7. 2 vitest test PASS verify (fetch spy assertion + source regex grep). B-02 BLOCKER closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL ?return param → Login.tsx parse | Untrusted user input (URL query); strict validation chain required |
| getBranding(CURRENT_HUB) → Login + Layout | Trusted compile-time registry from Plan 05-03 |
| api.crossHubSearch → Caddy /api/* central | Same-origin fetch; Caddy handle routing authoritative |
| api.tryRefresh → hub con backend → 307 central (Plan 03-04 SSO-02) | Same-origin fetch; backend 307 trusted (Plan 03-04 LOCKED); `redirect: 'follow'` explicit per D-V3-Phase5-C4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-04 | Tampering / Open redirect | Login.tsx `?return=` param | mitigate | 4-layer validation: (1) strip leading `/`; (2) reject `//` and `://` (absolute URL injection); (3) regex `^[a-z][a-z0-9_]{0,15}$` (Settings hub format); (4) ∈ KNOWN_HUBS allowlist. 3 explicit test cases (T-5-04 prefix) verify behavior. Dev `console.warn` log invalid attempts. |
| T-5-06 | XSS | themeColor inline `style` attribute | mitigate | themeColor sourced from compile-time registry (Plan 05-03 hardcoded TS const), NOT user input. React `style={{}}` auto-escape CSS values. |
| T-5-09 | Tampering | Layout title from CURRENT_HUB | mitigate | CURRENT_HUB from api.ts (Plan 05-02) — validated via KNOWN_HUBS at module level. Fallback central if unknown. Cannot inject arbitrary text. |
| T-5-12 | Spoofing / infinite redirect | tryRefresh 307 chain explicit redirect option | mitigate | `redirect: 'follow'` explicit per D-V3-Phase5-C4 (B-02 fix). Backend 307 chain bounded — hub con → central (1 hop), central handles locally (no further redirect). RFC 7231 preserves POST body intact. |
</threat_model>

<verification>
- 3 file edit (Login.tsx + Layout.tsx + api.ts cumulative across 4 tasks) + 2 file mới test (Login.spec.tsx + Layout.spec.tsx) + 1 file extend (api.spec.ts)
- 14+ vitest test PASS (6 Login + 4 Layout + 2 crossHubSearch + 2 tryRefresh redirect audit)
- Full suite cumulative ~41 test PASS
- tsc --noEmit PASS
- T-5-04 strict return validation explicit test
- T-5-12 tryRefresh `redirect: 'follow'` explicit (B-02 BLOCKER fix)
- W-05 origin (not host) explicit comment (Login.tsx Task 1)
- M2 form/sidebar/nav/active state preserved (R-V3-2 mitigation scope minimal)
</verification>

<success_criteria>
Wave 3 ship — Login.tsx 4 state machine + branding render; Layout.tsx sidebar swap; api.crossHubSearch absolute override; api.tryRefresh explicit `redirect: 'follow'` audit (D-V3-Phase5-C4 carry forward Plan 03-04 SSO-02). PROXY-02 + PROXY-04 satisfied semantic; T-5-04 + T-5-12 mitigated; Plan 05-06 manual smoke có structure để verify 4 hub × 11 trang.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reverse-proxy-frontend-subpath/05-04-SUMMARY.md` với:
- 4 task commits (atomic): feat(05-04) Login branding+redirect + feat(05-04) Layout sidebar branding + feat(05-04) api crossHubSearch absolute + fix(05-04) api tryRefresh redirect: follow audit (B-02)
- 14+ vitest test PASS table (Login 6 state + Layout 4 + crossHub 2 + tryRefresh 2)
- Full suite total count
- Acceptance criteria PASS table (18+ grep + tsc + vitest including new B-02 grep + W-05 grep)
- Deviations log (nếu MemoryRouter conflict với window.location mock → adapt test pattern; nếu `?raw` import unsupported → fallback fs.readFile)
- Next plan handoff: Plan 05-05 chỉ depend 05-01 (Caddyfile env) — có thể chạy parallel Wave 4 với 05-04; Plan 05-06 Wave 5 depend tất cả
</output>
</output>
