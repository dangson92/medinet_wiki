---
phase: 5
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/branding/index.ts
  - frontend/src/branding/central/index.ts
  - frontend/src/branding/yte/index.ts
  - frontend/src/branding/duoc/index.ts
  - frontend/src/branding/hcns/index.ts
  - frontend/public/branding/central/logo.svg
  - frontend/public/branding/yte/logo.svg
  - frontend/public/branding/duoc/logo.svg
  - frontend/public/branding/hcns/logo.svg
  - frontend/src/branding/__tests__/registry.spec.ts
autonomous: true
requirements: [PROXY-04]
tags: [frontend, branding, vite-glob, asset-svg, v3.0, phase5]

must_haves:
  truths:
    - "`getBranding(hub)` tráº£ config Ä‘Ãºng cho 4 hub initial (central/yte/duoc/hcns)"
    - "`getBranding(unknown_hub)` fallback central (Medinet indigo `#6366f1`)"
    - "4 SVG placeholder asset tá»“n táº¡i táº¡i `frontend/public/branding/<hub>/logo.svg`"
    - "`getContrastTextColor(themeColor)` helper tráº£ 'slate-900' cho hcns amber (`#f59e0b` luminance > 0.55), 'white' cho 3 hub cÃ²n láº¡i"
  artifacts:
    - path: "frontend/src/branding/index.ts"
      provides: "Vite glob registry + BrandingConfig type + getBranding() + getContrastTextColor() helper"
      exports: ["BrandingConfig", "getBranding", "getContrastTextColor"]
    - path: "frontend/src/branding/central/index.ts"
      provides: "Medinet indigo #6366f1 branding"
      contains: "themeColor: '#6366f1'"
    - path: "frontend/src/branding/yte/index.ts"
      provides: "Hub Y táº¿ emerald #10b981 branding"
      contains: "themeColor: '#10b981'"
    - path: "frontend/src/branding/duoc/index.ts"
      provides: "Hub DÆ°á»£c sky #0ea5e9 branding"
      contains: "themeColor: '#0ea5e9'"
    - path: "frontend/src/branding/hcns/index.ts"
      provides: "Hub HCNS amber #f59e0b branding"
      contains: "themeColor: '#f59e0b'"
    - path: "frontend/public/branding/central/logo.svg"
      provides: "Placeholder text-M monochrome SVG"
    - path: "frontend/public/branding/yte/logo.svg"
      provides: "Placeholder text-Y monochrome SVG"
    - path: "frontend/public/branding/duoc/logo.svg"
      provides: "Placeholder text-D monochrome SVG"
    - path: "frontend/public/branding/hcns/logo.svg"
      provides: "Placeholder text-H monochrome SVG"
  key_links:
    - from: "frontend/src/branding/index.ts"
      to: "branding/<hub>/index.ts Ã— 4"
      via: "import.meta.glob('./*/index.ts', { eager: true })"
      pattern: "import.meta.glob"
    - from: "branding/<hub>/index.ts"
      to: "public/branding/<hub>/logo.svg"
      via: "logo path '/branding/<hub>/logo.svg' (Vite copy public â†’ dist â†’ Caddy serve)"
      pattern: "/branding/.*/logo.svg"
---

<objective>
Wave 2 PARALLEL (file-disjoint vá»›i Plan 05-02) â€” Per-hub branding registry static + 4 SVG placeholder assets. Táº¡o `frontend/src/branding/index.ts` Vite glob registry (`import.meta.glob('./*/index.ts', { eager: true })`) + 4 hub config (central indigo / yte emerald / duoc sky / hcns amber theo Specific Ideas + UI-SPEC Â§1) + helper `getBranding()` fallback central + `getContrastTextColor()` cho WCAG mitigation hcns amber. Táº¡o 4 SVG placeholder text-only initial letter (M/Y/D/H) á»Ÿ `frontend/public/branding/<hub>/logo.svg` (Vite copy public â†’ dist â†’ Caddy file_server).

Purpose: PROXY-04 â€” per-hub login branding satisfied (D-V3-Phase5-D1/D3 LOCKED + UI-SPEC Â§1 contract). Plan 05-04 sáº½ consume `getBranding()` á»Ÿ Login.tsx + Layout.tsx; designer hoÃ¡n Ä‘á»•i SVG file later mÃ  KHÃ”NG cáº§n rebuild FE.

Output: 5 TypeScript source file + 4 SVG file + 1 vitest test file. PROXY-04 registry + asset layer ready cho Plan 05-04 wire UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md
@.planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md

<interfaces>
<!-- Branding catalog tá»« UI-SPEC.md Â§1.1 (LOCKED) -->
4 hub initial:
| Hub key | title (VN) | tagline (VN) | themeColor (hex) | logo path |
|---------|-----------|---------------|-------------------|-----------|
| central | "Medinet Wiki" | "Tri thá»©c ná»™i bá»™ Medinet" | #6366f1 indigo | /branding/central/logo.svg |
| yte | "Hub Y táº¿ Medinet" | "Tri thá»©c y táº¿ cho má»i nhÃ¢n viÃªn" | #10b981 emerald | /branding/yte/logo.svg |
| duoc | "Hub DÆ°á»£c Medinet" | "HÆ°á»›ng dáº«n dÆ°á»£c lÃ¢m sÃ ng" | #0ea5e9 sky | /branding/duoc/logo.svg |
| hcns | "Hub HCNS Medinet" | "ChÃ­nh sÃ¡ch nhÃ¢n sá»± Medinet" | #f59e0b amber | /branding/hcns/logo.svg |

<!-- BrandingConfig type contract tá»« UI-SPEC.md Â§1.1 -->
```typescript
export interface BrandingConfig {
  readonly logo: string;        // Public asset URL absolute, vd "/branding/yte/logo.svg"
  readonly title: string;       // VN hub title, â‰¤ 24 chars (sidebar truncate max-w-[160px])
  readonly tagline: string;     // VN subtitle Login hero, â‰¤ 48 chars
  readonly themeColor: string;  // Hex 7 chars (kÃ¨m #), dÃ¹ng lÃ m CSS var --hub-theme
}
```

<!-- getContrastTextColor helper tá»« UI-SPEC.md Â§1.5 -->
Helper return 'white' | 'slate-900' dá»±a trÃªn relative luminance themeColor:
- yte emerald + duoc sky + central indigo â†’ 'white' (ratio â‰¥ 3:1)
- hcns amber â†’ 'slate-900' (white-on-amber 2.07:1 FAIL WCAG 1.4.11)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Táº¡o `frontend/src/branding/index.ts` registry + BrandingConfig type + getBranding + getContrastTextColor (PROXY-04 + UI-SPEC Â§1.1/1.5)</name>
  <files>frontend/src/branding/index.ts</files>
  <read_first>
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Implementation Decisions D1" (LOCKED â€” static glob)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md Â§1.1 (BrandingConfig type) + Â§1.5 (getContrastTextColor luminance helper hcns amber)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md Â§"Pattern 5" lines 402-463 (eager glob verified)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md Â§"Pitfall 8" (tsconfig types ["vite/client"] cáº§n verify)
    - frontend/tsconfig.json (verify types: ["vite/client"] cÃ³ sáºµn)
  </read_first>
  <action>
Táº O file má»›i `frontend/src/branding/index.ts` vá»›i ná»™i dung VERBATIM:

```typescript
// Phase 5 PROXY-04 â€” Per-hub branding registry (D-V3-Phase5-D1 LOCKED)
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Decisions D"
//         .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md Â§1
//         .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md Pattern 5

// Type contract â€” UI-SPEC Â§1.1 LOCKED
export interface BrandingConfig {
  readonly logo: string;       // Public asset absolute path '/branding/<hub>/logo.svg'
  readonly title: string;      // VN hub title (â‰¤ 24 chars â€” sidebar truncate max-w-[160px])
  readonly tagline: string;    // VN subtitle (â‰¤ 48 chars â€” Login hero column)
  readonly themeColor: string; // Hex 7 chars '#xxxxxx' â€” dÃ¹ng lÃ m CSS variable --hub-theme
}

// Vite eager glob â€” compile-time scan + bundle Táº¤T Cáº¢ branding modules.
// Eager mode â†’ static imports inlined, KHÃ”NG lazy chunk, KHÃ”NG async penalty.
// Reference: https://vite.dev/guide/features.html#glob-import
const modules = import.meta.glob<{ branding: BrandingConfig }>('./*/index.ts', {
  eager: true,
});

// Build registry keyed by hub name. Path pattern './<hub>/index.ts'
// Regex constrain: hub name match Settings format ^[a-z][a-z0-9_]{0,15}$ (Plan 02-05 FACTOR-04)
// T-5-03 mitigation: path schema locked â€” KHÃ”NG accept user input cho asset path
const registry: Record<string, BrandingConfig> = {};
for (const path in modules) {
  const match = path.match(/^\.\/([a-z][a-z0-9_]{0,15})\/index\.ts$/);
  if (match) {
    registry[match[1]] = modules[path].branding;
  }
}

// Fallback Medinet central (M2 brand) â€” silent fallback per UI-SPEC Â§1.1
const FALLBACK: BrandingConfig = registry['central'] ?? {
  logo: '/branding/central/logo.svg',
  title: 'Medinet Wiki',
  tagline: 'Tri thá»©c ná»™i bá»™ Medinet',
  themeColor: '#6366f1', // indigo-500 M2 brand
};

/**
 * Tráº£ vá» branding config cho hub. Fallback silent vá» central náº¿u hub khÃ´ng cÃ³.
 *
 * @param hub - hub name (vd 'yte', 'central', hoáº·c 'unknown')
 * @returns BrandingConfig (luÃ´n defined â€” fallback central)
 */
export function getBranding(hub: string): BrandingConfig {
  const cfg = registry[hub];
  if (cfg) return cfg;
  // Dev-only warning, silent UX cho user (UI-SPEC Â§1.1 fallback contract)
  if (typeof import.meta !== 'undefined' && import.meta.env?.DEV) {
    console.warn(`[branding] Unknown hub "${hub}" â€” fallback to central`);
  }
  return FALLBACK;
}

/**
 * Helper WCAG mitigation â€” return 'white' | 'slate-900' dá»±a trÃªn relative luminance.
 *
 * hcns amber #f59e0b vs white = 2.07:1 â†’ FAIL WCAG 1.4.11 (3:1 cho UI icon).
 * Sá»­ dá»¥ng cho sidebar logo icon + Login submit button text khi themeColor light.
 *
 * Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md Â§1.5
 *
 * @param themeColor - hex string '#xxxxxx'
 * @returns 'white' náº¿u themeColor Ä‘á»§ tá»‘i (luminance â‰¤ 0.55), else 'slate-900'
 */
export function getContrastTextColor(themeColor: string): 'white' | 'slate-900' {
  // Strip '#' + parse RGB 6 hex digits
  const hex = themeColor.replace(/^#/, '');
  if (hex.length !== 6) return 'white'; // defensive fallback

  const r = parseInt(hex.slice(0, 2), 16) / 255;
  const g = parseInt(hex.slice(2, 4), 16) / 255;
  const b = parseInt(hex.slice(4, 6), 16) / 255;

  // Relative luminance (sRGB) â€” simplified linear approx, Ä‘á»§ accuracy cho threshold 0.55
  // Reference: https://www.w3.org/TR/WCAG21/#dfn-relative-luminance (full version uses gamma correction)
  const luminance = 0.2126 * r + 0.7152 * g + 0.0722 * b;

  return luminance > 0.55 ? 'slate-900' : 'white';
}

// Export registry cho test introspection (KHÃ”NG dÃ¹ng á»Ÿ production component)
export const _registry_internal_for_test = registry;
```

**T-5-03 mitigation note (path traversal):** Regex `^\.\/([a-z][a-z0-9_]{0,15})\/index\.ts$` constrain hub name strict â€” KHÃ”NG cho phÃ©p `../`, `/`, hoáº·c special chars. Path asset hardcode `/branding/<hub>/logo.svg` schema, hub component register lock trong file riÃªng KHÃ”NG accept user input.
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "export interface BrandingConfig" Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "export function getBranding" Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "export function getContrastTextColor" Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "import.meta.glob" Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "eager: true" Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "FALLBACK" Hub_All/frontend/src/branding/index.ts` exit 0
    - `grep -q "themeColor: '#6366f1'" Hub_All/frontend/src/branding/index.ts` exit 0 (central FALLBACK hardcode)
    - `cd Hub_All/frontend && npm run lint` PASS (tsc --noEmit â€” import.meta.glob generic infer OK vá»›i vite/client types)
  </acceptance_criteria>
  <done>
    Registry helper file vá»›i BrandingConfig type + getBranding fallback + getContrastTextColor WCAG helper + Vite eager glob 4 hub. tsc lint PASS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Táº¡o 4 file `branding/<hub>/index.ts` Ã— 4 hub initial (PROXY-04 + UI-SPEC Â§1.1 + CONTEXT Specific Ideas)</name>
  <files>frontend/src/branding/central/index.ts, frontend/src/branding/yte/index.ts, frontend/src/branding/duoc/index.ts, frontend/src/branding/hcns/index.ts</files>
  <read_first>
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Specific Ideas" (4 hub tone + title VN + tagline VN â€” copy verbatim)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md Â§1.1 (catalog 4 hub initial)
  </read_first>
  <action>
Táº O 4 file má»›i vá»›i ná»™i dung verbatim (má»—i file 1 hub config â€” designer hoÃ¡n Ä‘á»•i sau):

**`frontend/src/branding/central/index.ts`:**
```typescript
// Phase 5 PROXY-04 â€” Central Medinet Wiki branding (M2 brand carry forward)
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Specific Ideas"
//         .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md Â§1.1
export const branding = {
  logo: '/branding/central/logo.svg',
  title: 'Medinet Wiki',
  tagline: 'Tri thá»©c ná»™i bá»™ Medinet',
  themeColor: '#6366f1', // indigo-500 â€” M2 brand color carry forward
} as const;
```

**`frontend/src/branding/yte/index.ts`:**
```typescript
// Phase 5 PROXY-04 â€” Hub Y táº¿ Medinet branding
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Specific Ideas"
//         Health = green semantic
export const branding = {
  logo: '/branding/yte/logo.svg',
  title: 'Hub Y táº¿ Medinet',
  tagline: 'Tri thá»©c y táº¿ cho má»i nhÃ¢n viÃªn',
  themeColor: '#10b981', // emerald-500
} as const;
```

**`frontend/src/branding/duoc/index.ts`:**
```typescript
// Phase 5 PROXY-04 â€” Hub DÆ°á»£c Medinet branding
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Specific Ideas"
//         Clinical pharmacy = blue semantic
export const branding = {
  logo: '/branding/duoc/logo.svg',
  title: 'Hub DÆ°á»£c Medinet',
  tagline: 'HÆ°á»›ng dáº«n dÆ°á»£c lÃ¢m sÃ ng',
  themeColor: '#0ea5e9', // sky-500
} as const;
```

**`frontend/src/branding/hcns/index.ts`:**
```typescript
// Phase 5 PROXY-04 â€” Hub HCNS (HÃ nh chÃ­nh NhÃ¢n sá»±) Medinet branding
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md Â§"Specific Ideas"
//         HR = warm amber semantic
// WCAG note: amber #f59e0b vs white = 2.07:1 FAIL â€” UI-SPEC Â§1.5 getContrastTextColor return 'slate-900'
export const branding = {
  logo: '/branding/hcns/logo.svg',
  title: 'Hub HCNS Medinet',
  tagline: 'ChÃ­nh sÃ¡ch nhÃ¢n sá»± Medinet',
  themeColor: '#f59e0b', // amber-500
} as const;
```

Má»—i file dÃ¹ng `as const` Ä‘á»ƒ TypeScript infer readonly + literal types match BrandingConfig contract.
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 4 file tá»“n táº¡i: `test -f Hub_All/frontend/src/branding/central/index.ts && test -f Hub_All/frontend/src/branding/yte/index.ts && test -f Hub_All/frontend/src/branding/duoc/index.ts && test -f Hub_All/frontend/src/branding/hcns/index.ts` exit 0
    - `grep -q "themeColor: '#6366f1'" Hub_All/frontend/src/branding/central/index.ts` exit 0
    - `grep -q "themeColor: '#10b981'" Hub_All/frontend/src/branding/yte/index.ts` exit 0
    - `grep -q "themeColor: '#0ea5e9'" Hub_All/frontend/src/branding/duoc/index.ts` exit 0
    - `grep -q "themeColor: '#f59e0b'" Hub_All/frontend/src/branding/hcns/index.ts` exit 0
    - `grep -q "title: 'Hub Y táº¿ Medinet'" Hub_All/frontend/src/branding/yte/index.ts` exit 0 (VN diacritics preserve)
    - `grep -q "title: 'Hub DÆ°á»£c Medinet'" Hub_All/frontend/src/branding/duoc/index.ts` exit 0
    - `grep -q "title: 'Hub HCNS Medinet'" Hub_All/frontend/src/branding/hcns/index.ts` exit 0
    - `grep -q "title: 'Medinet Wiki'" Hub_All/frontend/src/branding/central/index.ts` exit 0
    - Má»—i file `as const`: `grep -q "as const" Hub_All/frontend/src/branding/{central,yte,duoc,hcns}/index.ts` (4 files exit 0 â€” readonly type infer)
    - `cd Hub_All/frontend && npm run lint` PASS
  </acceptance_criteria>
  <done>
    4 hub config TS file vá»›i title VN cÃ³ dáº¥u + tagline VN + themeColor hex 7-char + logo path absolute. Vite glob registry (Task 1) auto-import + tsc PASS.
  </done>
</task>

<task type="auto">
  <name>Task 3: Táº¡o 4 SVG placeholder logo + vitest test registry behavior (PROXY-04 + UI-SPEC Â§6.2 + VALIDATION 5-03-01..03)</name>
  <files>frontend/public/branding/central/logo.svg, frontend/public/branding/yte/logo.svg, frontend/public/branding/duoc/logo.svg, frontend/public/branding/hcns/logo.svg, frontend/src/branding/__tests__/registry.spec.ts</files>
  <read_first>
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md Â§6 (SVG specs: viewBox 0 0 64 64, monochrome white fill, text-only initial letter placeholder)
    - .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-03-01..03 (registry spec + asset existence)
  </read_first>
  <action>
**Step 1 â€” Táº¡o 4 SVG file vá»›i content monochrome text-only initial letter (M/Y/D/H) trÃªn fill white, viewBox 0 0 64 64:**

`frontend/public/branding/central/logo.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-label="Medinet Wiki logo">
  <text x="32" y="46" text-anchor="middle" font-family="Inter, -apple-system, system-ui, sans-serif" font-weight="700" font-size="38" fill="#FFFFFF">M</text>
</svg>
```

`frontend/public/branding/yte/logo.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-label="Hub Y táº¿ logo">
  <text x="32" y="46" text-anchor="middle" font-family="Inter, -apple-system, system-ui, sans-serif" font-weight="700" font-size="38" fill="#FFFFFF">Y</text>
</svg>
```

`frontend/public/branding/duoc/logo.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-label="Hub DÆ°á»£c logo">
  <text x="32" y="46" text-anchor="middle" font-family="Inter, -apple-system, system-ui, sans-serif" font-weight="700" font-size="38" fill="#FFFFFF">D</text>
</svg>
```

`frontend/public/branding/hcns/logo.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-label="Hub HCNS logo">
  <text x="32" y="46" text-anchor="middle" font-family="Inter, -apple-system, system-ui, sans-serif" font-weight="700" font-size="38" fill="#FFFFFF">H</text>
</svg>
```

Má»—i SVG: viewBox 64Ã—64 monochrome white fill text-only initial letter (designer hoÃ¡n Ä‘á»•i sau â€” Vite copy `public/` verbatim â†’ `dist/branding/<hub>/logo.svg` â†’ Caddy file_server). File size budget â‰¤ 4 KB per UI-SPEC Â§6.1.

**Step 2 â€” Táº¡o `frontend/src/branding/__tests__/registry.spec.ts`:**

```typescript
// Phase 5 PROXY-04 unit test â€” branding registry behavior
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-VALIDATION.md task 5-03-01..03

import { describe, it, expect } from 'vitest';
import { getBranding, getContrastTextColor, type BrandingConfig } from '../index';

describe('Phase 5 PROXY-04 â€” branding registry', () => {
  describe('getBranding() â€” 4 hub initial', () => {
    it('returns Medinet central config for hub="central"', () => {
      const cfg = getBranding('central');
      expect(cfg.title).toBe('Medinet Wiki');
      expect(cfg.tagline).toBe('Tri thá»©c ná»™i bá»™ Medinet');
      expect(cfg.themeColor).toBe('#6366f1');
      expect(cfg.logo).toBe('/branding/central/logo.svg');
    });

    it('returns yte config for hub="yte" (emerald #10b981)', () => {
      const cfg = getBranding('yte');
      expect(cfg.title).toBe('Hub Y táº¿ Medinet');
      expect(cfg.tagline).toBe('Tri thá»©c y táº¿ cho má»i nhÃ¢n viÃªn');
      expect(cfg.themeColor).toBe('#10b981');
      expect(cfg.logo).toBe('/branding/yte/logo.svg');
    });

    it('returns duoc config for hub="duoc" (sky #0ea5e9)', () => {
      const cfg = getBranding('duoc');
      expect(cfg.title).toBe('Hub DÆ°á»£c Medinet');
      expect(cfg.tagline).toBe('HÆ°á»›ng dáº«n dÆ°á»£c lÃ¢m sÃ ng');
      expect(cfg.themeColor).toBe('#0ea5e9');
      expect(cfg.logo).toBe('/branding/duoc/logo.svg');
    });

    it('returns hcns config for hub="hcns" (amber #f59e0b)', () => {
      const cfg = getBranding('hcns');
      expect(cfg.title).toBe('Hub HCNS Medinet');
      expect(cfg.tagline).toBe('ChÃ­nh sÃ¡ch nhÃ¢n sá»± Medinet');
      expect(cfg.themeColor).toBe('#f59e0b');
      expect(cfg.logo).toBe('/branding/hcns/logo.svg');
    });
  });

  describe('getBranding() â€” fallback behavior', () => {
    it('fallbacks to central for unknown hub "phap_che"', () => {
      const cfg = getBranding('phap_che');
      expect(cfg.title).toBe('Medinet Wiki');
      expect(cfg.themeColor).toBe('#6366f1');
    });

    it('fallbacks to central for empty string ""', () => {
      const cfg = getBranding('');
      expect(cfg.title).toBe('Medinet Wiki');
    });

    it('fallbacks to central for hub with special chars (T-5-03 path traversal mitigation)', () => {
      // Regex constrain ^[a-z][a-z0-9_]{0,15}$ â€” ../, /, etc rejected by registry build regex
      const cfg = getBranding('../etc/passwd');
      expect(cfg.title).toBe('Medinet Wiki');
    });
  });

  describe('getContrastTextColor() â€” WCAG 1.4.11 mitigation', () => {
    it('returns "white" for indigo central #6366f1 (luminance ~0.30)', () => {
      expect(getContrastTextColor('#6366f1')).toBe('white');
    });

    it('returns "white" for emerald yte #10b981 (luminance ~0.50)', () => {
      expect(getContrastTextColor('#10b981')).toBe('white');
    });

    it('returns "white" for sky duoc #0ea5e9 (luminance ~0.49)', () => {
      expect(getContrastTextColor('#0ea5e9')).toBe('white');
    });

    it('returns "slate-900" for amber hcns #f59e0b (luminance ~0.62 > 0.55 threshold)', () => {
      expect(getContrastTextColor('#f59e0b')).toBe('slate-900');
    });

    it('defensive fallback "white" for malformed hex', () => {
      expect(getContrastTextColor('not-a-hex')).toBe('white');
      expect(getContrastTextColor('#abc')).toBe('white'); // 3-char hex not supported
    });
  });

  describe('BrandingConfig type shape', () => {
    it('all 4 hub configs have required fields (logo + title + tagline + themeColor)', () => {
      const hubs = ['central', 'yte', 'duoc', 'hcns'];
      for (const hub of hubs) {
        const cfg: BrandingConfig = getBranding(hub);
        expect(cfg.logo).toMatch(/^\/branding\/[a-z][a-z0-9_]*\/logo\.svg$/);
        expect(cfg.title.length).toBeGreaterThan(0);
        expect(cfg.title.length).toBeLessThanOrEqual(24);
        expect(cfg.tagline.length).toBeLessThanOrEqual(48);
        expect(cfg.themeColor).toMatch(/^#[0-9a-f]{6}$/i);
      }
    });
  });
});
```

**Threat mitigation:**
- T-5-03 (logo asset path traversal): SVG asset path locked schema `/branding/<hub>/logo.svg` qua regex registry build (Task 1). Hub key strict regex ^[a-z][a-z0-9_]{0,15}$ â€” path traversal `../`, `/`, special chars rejected. Test 7 verify behavior.
  </action>
  <verify>
    <automated>cd Hub_All/frontend &amp;&amp; npx vitest run src/branding/__tests__/registry.spec.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - 4 SVG file tá»“n táº¡i: `test -f Hub_All/frontend/public/branding/central/logo.svg && test -f Hub_All/frontend/public/branding/yte/logo.svg && test -f Hub_All/frontend/public/branding/duoc/logo.svg && test -f Hub_All/frontend/public/branding/hcns/logo.svg` exit 0
    - Má»—i SVG file size â‰¤ 4 KB: `find Hub_All/frontend/public/branding -name "logo.svg" -size -4k | wc -l` returns 4
    - SVG content monochrome white fill text: `grep -l 'fill="#FFFFFF"' Hub_All/frontend/public/branding/*/logo.svg | wc -l` returns 4
    - `grep -l 'viewBox="0 0 64 64"' Hub_All/frontend/public/branding/*/logo.svg | wc -l` returns 4
    - `test -f Hub_All/frontend/src/branding/__tests__/registry.spec.ts` exit 0
    - `cd Hub_All/frontend && npx vitest run src/branding/__tests__/registry.spec.ts` exit 0; 17+ test PASS (4 hub + 3 fallback + 5 contrast + 1 shape)
    - VN diacritic preserve trong test assertions: `grep -q "Hub Y táº¿ Medinet" Hub_All/frontend/src/branding/__tests__/registry.spec.ts` exit 0
    - `cd Hub_All/frontend && npx vitest run` (full suite) â€” 27+ test PASS (10 Plan 05-02 + 17+ Plan 05-03)
  </acceptance_criteria>
  <done>
    4 SVG placeholder asset (M/Y/D/H initial letter) + vitest registry test 17+ scenarios PASS. T-5-03 path traversal mitigated qua regex constrain + test verify.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Branding registry â†’ public asset path | Static config compile-time â€” KHÃ”NG accept user input |
| public/branding/*/logo.svg â†’ Caddy file_server | Static asset serve â€” no execution context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03 | Tampering / path traversal | branding registry asset path | mitigate | Regex constrain `^\.\/([a-z][a-z0-9_]{0,15})\/index\.ts$` registry build (Task 1) â€” `../`, `/`, special chars rejected. Asset path schema hardcoded `/branding/<hub>/logo.svg`. Test 7 in registry.spec.ts verify behavior. |
| T-5-06 | XSS | themeColor inline CSS variable | mitigate | themeColor hardcoded TS const trong 4 hub config â€” NOT user input. Plan 05-04 sáº½ render qua React `style={{...}}` (CSS escaping built-in). |
| T-5-13 | Info disclosure | SVG content reveal hub presence | accept | SVG placeholder text-only initial letter, no sensitive data. Designer hoÃ¡n Ä‘á»•i sau cÅ©ng pháº£i tuÃ¢n schema viewBox 64Ã—64 monochrome. |
</threat_model>

<verification>
- 5 TS source file + 4 SVG file + 1 vitest test file create
- 17+ vitest test PASS cover 4 hub config + fallback behavior + WCAG contrast helper + path traversal mitigation
- tsc --noEmit PASS (Vite import.meta.glob generic infer + `as const` literal types match BrandingConfig)
- Full suite `npx vitest run` PASS (Plan 05-02 + 05-03 cumulative)
</verification>

<success_criteria>
Wave 2 parallel ship â€” Branding registry helper + 4 hub config + 4 SVG asset + WCAG mitigation helper. PROXY-04 layer 1 (registry) ready. Plan 05-04 Wave 3 sáº½ consume `getBranding()` + `getContrastTextColor()` á»Ÿ Login.tsx + Layout.tsx render.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reverse-proxy-frontend-subpath/05-03-SUMMARY.md` vá»›i:
- 3 task commits (atomic): feat(05-03) branding registry helper + feat(05-03) 4 hub config + chore(05-03) 4 SVG placeholder + test
- 17+ vitest test PASS table (test groups + count)
- Acceptance criteria PASS table (15+ grep checks + test file existence + asset size)
- Deviations log (náº¿u import.meta.glob generic infer fail â†’ fallback manual narrow per Pitfall 8)
- Next plan handoff: Plan 05-04 Wave 3 depend cáº£ 05-02 + 05-03; designer sau Phase 5 ship cÃ³ thá»ƒ swap SVG file mÃ  KHÃ”NG rebuild FE
</output>
