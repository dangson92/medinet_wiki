---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Hub_All/mcp_service/mcp_app/api_client.py
  - Hub_All/mcp_service/mcp_app/oauth/provider.py
  - Hub_All/mcp_service/mcp_app/oauth/login.py
  - Hub_All/mcp_service/mcp_app/oauth/__init__.py
  - Hub_All/mcp_service/mcp_app/server.py
  - Hub_All/mcp_service/tests/test_oauth_provider.py
  - Hub_All/mcp_service/tests/test_oauth_login.py
  - Hub_All/mcp_service/tests/test_api_client.py
autonomous: true
requirements: [MCP-01]
must_haves:
  truths:
    - "MCP Service expose OAuth metadata route (/.well-known/oauth-authorization-server, /.well-known/oauth-protected-resource)"
    - "Claude web có thể đăng ký client động qua DCR (POST /register) — client lưu vào OAuthStore"
    - "Bước login OAuth hiển thị HTML form, xác thực credential bằng POST /api/auth/login của API Service (D-02)"
    - "Login đúng → phát authorization code bind (user, downstream JWT access+refresh); login sai → render lại form kèm lỗi"
    - "POST /token exchange code → OAuth access token; load_access_token verify token mỗi tool call"
    - "OAuthStore schema được khởi tạo trong Starlette app lifespan startup — bảng sẵn sàng trước request đầu tiên"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/oauth/provider.py"
      provides: "MedinetOAuthProvider — OAuthAuthorizationServerProvider 9 method"
      contains: "class MedinetOAuthProvider"
      min_lines: 150
    - path: "Hub_All/mcp_service/mcp_app/oauth/login.py"
      provides: "Route GET /login + POST /login/callback — verify credential Medinet"
      contains: "login"
    - path: "Hub_All/mcp_service/mcp_app/server.py"
      provides: "FastMCP wired với auth_server_provider + AuthSettings + route /login + lifespan init_schema"
      contains: "auth_server_provider"
  key_links:
    - from: "Hub_All/mcp_service/mcp_app/server.py"
      to: "MedinetOAuthProvider"
      via: "FastMCP(auth_server_provider=...)"
      pattern: "auth_server_provider"
    - from: "Hub_All/mcp_service/mcp_app/oauth/login.py"
      to: "/api/auth/login"
      via: "api_client.login()"
      pattern: "api_client.*login"
    - from: "Hub_All/mcp_service/mcp_app/oauth/provider.py"
      to: "OAuthStore"
      via: "constructor injection"
      pattern: "OAuthStore|store"
---

<objective>
Implement lớp OAuth Authorization Server thật cho MCP Service: lớp `MedinetOAuthProvider` (kế thừa `OAuthAuthorizationServerProvider` của SDK `mcp` 1.27 — 9 method), route HTML form login xác thực bằng tài khoản Medinet (D-02), và wire tất cả vào `FastMCP` để SDK tự mount route metadata/`/authorize`/`/token`/`/register`. Thêm method `api_client.login()` gọi `POST /api/auth/login`. Khởi tạo schema `OAuthStore` trong lifespan startup của Starlette app.

Purpose: Đây là lõi của phase — MCP-01. SDK `mcp` 1.27 lo phần OAuth "đúng spec" (metadata RFC 8414/9728, PKCE S256, DCR RFC 7591, code flow); việc của plan này là 3 mảng glue: persistence (qua `OAuthStore` của Plan 01 — gồm bảng `oauth_pending` cho authorize transaction), login form xác thực Medinet, và wiring.
Output: Claude web hoàn tất được OAuth discovery + DCR + authorize + token exchange. Việc forward JWT downstream xuống tool (D-03) là Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-CONTEXT.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-01-SUMMARY.md

<interfaces>
<!-- Hợp đồng từ SDK mcp 1.27 + Plan 01. Executor dùng trực tiếp, KHÔNG khám phá lại. -->

OAuthStore (Plan 01 — mcp_app/oauth/store.py) — đã có sẵn (4 bảng, gồm oauth_pending):
```python
class OAuthStore:
    def __init__(self, db_path: str) -> None: ...
    async def init_schema(self) -> None: ...
    async def save_client(self, client_id, client_metadata: dict) -> None: ...
    async def get_client(self, client_id) -> dict | None: ...
    async def save_auth_code(self, code, client_id, code_payload: dict,
        downstream_jwt, downstream_refresh_token, user_payload: dict, expires_at: int) -> None: ...
    async def load_auth_code(self, code) -> dict | None: ...
    async def delete_auth_code(self, code) -> None: ...
    async def save_token(self, access_token, refresh_token, client_id, scopes: list,
        downstream_jwt, downstream_refresh_token, user_payload: dict, expires_at: int) -> None: ...
    async def load_token(self, access_token) -> dict | None: ...
    async def load_token_by_refresh(self, refresh_token) -> dict | None: ...
    async def update_oauth_token(self, old_access, new_access, new_refresh, expires_at) -> None: ...
    async def delete_token(self, access_token) -> None: ...
    # --- pending-authorize transaction (Plan 01 — bảng oauth_pending) ---
    async def save_pending(self, txn, client_id, redirect_uri, code_challenge,
        code_challenge_method, client_state, scopes: list, created_at: int) -> None: ...
    async def load_pending(self, txn) -> dict | None: ...
        # trả {client_id, redirect_uri, code_challenge, code_challenge_method, client_state, scopes, created_at}
    async def delete_pending(self, txn) -> None: ...
    async def cleanup_expired(self, now: int) -> None: ...
```
> Bảng `oauth_pending` đã được Plan 01 author — Plan 02 chỉ CONSUME. KHÔNG Edit `store.py` để thêm bảng/cột mới ở plan này.

conftest.py (Plan 01) — đã có fixture + helper:
```python
@pytest.fixture
async def oauth_store(): ...   # OAuthStore :memory:, schema sẵn

async def fake_pending_authorize(store, *, txn="txn-test", client_id="client-test",
    redirect_uri="https://claude.ai/api/mcp/auth_callback", code_challenge="challenge-test",
    code_challenge_method="S256", client_state="client-state-test",
    scopes=None, created_at=None) -> str:
    """Seed 1 bản ghi oauth_pending; trả txn. Dùng cho test login callback."""
```

SDK mcp 1.27 — interface AS provider (from mcp.server.auth.provider):
```python
from mcp.server.auth.provider import (
    OAuthAuthorizationServerProvider, AuthorizationParams,
    AuthorizationCode, RefreshToken, AccessToken,
)
from mcp.shared.auth import OAuthClientInformationFull, OAuthToken

# 9 method bắt buộc override (chữ ký chính xác executor đọc từ provider.py của SDK):
#   get_client(client_id) -> OAuthClientInformationFull | None
#   register_client(client_info: OAuthClientInformationFull) -> None
#   authorize(client, params: AuthorizationParams) -> str   # trả URL redirect
#   load_authorization_code(client, authorization_code) -> AuthorizationCode | None
#   exchange_authorization_code(client, authorization_code) -> OAuthToken
#   load_refresh_token(client, refresh_token) -> RefreshToken | None
#   exchange_refresh_token(client, refresh_token, scopes) -> OAuthToken
#   load_access_token(token) -> AccessToken | None
#   revoke_token(token) -> None
```

SDK mcp 1.27 — wiring (from mcp.server.auth.settings + mcp.server.fastmcp):
```python
from mcp.server.auth.settings import AuthSettings, ClientRegistrationOptions, RevocationOptions
from mcp.server.fastmcp import FastMCP
from pydantic import AnyHttpUrl
# FastMCP(..., auth_server_provider=provider, auth=AuthSettings(...))
# → SDK tự gọi create_auth_routes() bên trong streamable_http_app() khi provider non-None.
```

API Service POST /api/auth/login — body {email, password}, trả envelope:
  data = {access_token, refresh_token, expires_at, user}   (LƯU Ý: expires_at — Unix ts, KHÔNG expires_in)
  401 nếu credential sai.

server.py hiện tại — điểm gắn:
  line 53: `mcp = FastMCP("Medinet Wiki MCP Service", stateless_http=True)`
  lines 58-73: `_get_client()` — mẫu singleton lười
  lines 281-288: `build_asgi_app()` → `mcp.streamable_http_app()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm api_client.login() gọi POST /api/auth/login</name>
  <files>Hub_All/mcp_service/mcp_app/api_client.py, Hub_All/mcp_service/tests/test_api_client.py</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/api_client.py (bản hiện tại — `_request`, `get`, `post`, cây exception)
    - Hub_All/mcp_service/tests/test_api_client.py (mẫu test respx async)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Gọi /api/auth/login từ ApiClient")
  </read_first>
  <behavior>
    - `login("a@b.com", "pwd")` với API trả 200 envelope success → trả dict `{access_token, refresh_token, expires_at, user}`.
    - `login("a@b.com", "sai")` với API trả 401 → trả `None` (KHÔNG raise — credential sai là kết quả hợp lệ của login form).
    - `login(...)` với API 500 → raise `ApiServerError` (lỗi hạ tầng, khác credential sai).
  </behavior>
  <action>
    Trong `api_client.py`, thêm method vào class `ApiClient` (SAU `post`, GIỮ nguyên `_request`/`get`/`post`):
    ```python
    async def login(self, email: str, password: str) -> dict | None:
        """Xác thực credential Medinet qua API Service (D-02). None nếu credential sai.

        Gọi POST /api/auth/login. KHÔNG verify password tại MCP Service — ủy thác
        hoàn toàn API Service (Argon2). Bảo mật: KHÔNG log email/password.
        """
        try:
            resp = await self._client.post(
                "/api/auth/login", json={"email": email, "password": password}
            )
        except httpx.RequestError as e:
            logger.error("API Service không phản hồi khi login: %s", type(e).__name__)
            raise ApiServerError(f"Không kết nối được API Service: {type(e).__name__}") from e
        try:
            envelope = resp.json()
        except ValueError as e:
            raise ApiServerError(
                f"API Service trả response không hợp lệ (status {resp.status_code})"
            ) from e
        if resp.status_code == 200 and envelope.get("success") is True:
            return envelope.get("data")  # {access_token, refresh_token, expires_at, user}
        if resp.status_code == 401:
            logger.info("Login thất bại — credential sai (status 401)")
            return None
        err = envelope.get("error") or {}
        raise ApiServerError(f"{err.get('code', 'ERROR')}: {err.get('message', 'lỗi không xác định')}")
    ```
    Cập nhật module docstring `api_client.py` — thêm dòng nói Phase 8.3 thêm `login()` cho OAuth flow.
    Trong `test_api_client.py` (MODIFY) — GIỮ 6 test cũ. Thêm (mẫu respx lines 28-40):
    - `test_login_success` — respx mock `POST /api/auth/login` trả 200 envelope → `login()` trả dict đủ field.
    - `test_login_wrong_credential` — respx mock trả 401 → `login()` trả `None`.
    - `test_login_server_error` — respx mock trả 500 → `login()` raise `ApiServerError`.
    - Verify request body bằng `json.loads(route.calls.last.request.content)` đúng `{email, password}`.
    Đánh `@pytest.mark.critical` cho `test_login_success` + `test_login_wrong_credential`.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_api_client.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async def login" Hub_All/mcp_service/mcp_app/api_client.py` tồn tại.
    - `grep "/api/auth/login" Hub_All/mcp_service/mcp_app/api_client.py` tồn tại.
    - `grep -E "test_login_success|test_login_wrong_credential|test_login_server_error" Hub_All/mcp_service/tests/test_api_client.py` — cả 3 test.
    - `cd Hub_All/mcp_service && uv run pytest tests/test_api_client.py` — PASS.
  </acceptance_criteria>
  <done>ApiClient.login() gọi /api/auth/login, trả dict/None/raise đúng; test PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MedinetOAuthProvider — OAuthAuthorizationServerProvider 9 method</name>
  <files>Hub_All/mcp_service/mcp_app/oauth/provider.py, Hub_All/mcp_service/mcp_app/oauth/__init__.py, Hub_All/mcp_service/tests/test_oauth_provider.py</files>
  <read_first>
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Code Examples → Khung lớp OAuth provider"; §"Pattern 1")
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§provider.py — quy ước docstring/logging/constructor)
    - Hub_All/mcp_service/mcp_app/api_client.py (sau Task 1 — có method login())
    - Hub_All/mcp_service/mcp_app/oauth/store.py (Plan 01 — interface OAuthStore, gồm save_pending/load_pending/delete_pending)
    - Hub_All/mcp_service/tests/conftest.py (Plan 01 — fixture oauth_store + helper fake_pending_authorize)
    - Hub_All/mcp_service/tests/test_api_client.py (mẫu test async)
    - SDK source: `mcp/server/auth/provider.py` trong .venv của mcp_service — đọc chữ ký chính xác 9 method + model AuthorizationCode/RefreshToken/AccessToken/OAuthToken/OAuthClientInformationFull
  </read_first>
  <behavior>
    - `register_client(client_info)` → lưu client vào store; `get_client(client_id)` đọc lại trả `OAuthClientInformationFull` (DCR roundtrip).
    - `get_client("không-tồn-tại")` → None.
    - `load_access_token(token_hợp_lệ)` → trả `AccessToken` với scopes đúng.
    - `load_access_token(token_hết_hạn)` (expires_at < now) → trả None.
    - `load_access_token(token_sai_scope)` / token không có trong store → None.
    - `exchange_authorization_code` với code hợp lệ → trả `OAuthToken` có `access_token` + `refresh_token`; code bị xoá khỏi store sau exchange (single-use).
    - `exchange_refresh_token` rotate OAuth token — refresh cũ không dùng lại được.
    - `authorize(client, params)` → SINH `txn` ngẫu nhiên, lưu pending-authorize params vào `oauth_pending` (qua `store.save_pending`), trả URL redirect `f"{issuer_url}/login?txn={txn}"`.
  </behavior>
  <action>
    **`mcp_app/oauth/provider.py`** (NEW) — module docstring tiếng Việt (Phase 8.3, MCP-01, vai trò AS, KHÁC 8.2: MCP Service nay tự phát token OAuth, lưu ý KHÔNG log token). `from __future__ import annotations`. `logger = logging.getLogger(__name__)`.

    Class `MedinetOAuthProvider(OAuthAuthorizationServerProvider)`:
    - `__init__(self, store: OAuthStore, api_client: ApiClient, issuer_url: str, access_token_ttl: int, refresh_token_ttl: int)` — dependency injected tường minh (KHÔNG tự khởi tạo singleton). Lưu reference.
    - Implement 9 method theo interface SDK (đọc chữ ký CHÍNH XÁC từ `mcp/server/auth/provider.py` trong .venv — chữ ký dưới là mô tả hành vi, executor khớp type thật của SDK 1.27):
      - `get_client(client_id)` → `await store.get_client(...)` → dựng lại `OAuthClientInformationFull` từ JSON; None nếu không có.
      - `register_client(client_info)` → serialize `client_info` (dùng `.model_dump()` hoặc tương đương) → `store.save_client(client_info.client_id, ...)`. DCR mở (Claude's Discretion) — KHÔNG từ chối client; gate thật là login.
      - `authorize(client, params)` → SINH `txn = secrets.token_urlsafe(32)`. Lưu pending-authorize transaction qua `store.save_pending(txn=txn, client_id=client.client_id, redirect_uri=str(params.redirect_uri), code_challenge=params.code_challenge, code_challenge_method=<lấy từ params, mặc định "S256">, client_state=params.state, scopes=list(params.scopes or []), created_at=self._now())`. Trả URL `f"{self._issuer_url}/login?txn={txn}"`. Login callback (Task 3) dùng `txn` để `load_pending` nối lại flow. KHÔNG tự thêm bảng — `oauth_pending` đã do Plan 01 author. (Tên field chính xác của `AuthorizationParams` — `redirect_uri`, `code_challenge`, `state`, `scopes` — executor đọc từ SDK `provider.py`; nếu lệch, theo SDK.)
      - `load_authorization_code(client, authorization_code)` → `store.load_auth_code(code)` → dựng `AuthorizationCode`; None nếu không có / hết hạn (kiểm `expires_at`).
      - `exchange_authorization_code(client, authorization_code)` → đọc code từ store (kèm downstream JWT/refresh + user) → sinh `access_token` + `refresh_token` OAuth (token_urlsafe, opaque — RESEARCH.md A3) → `store.save_token(...)` với `expires_at = now + access_token_ttl`, bind downstream JWT/refresh + user → `store.delete_auth_code(code)` (single-use) → trả `OAuthToken(access_token=..., refresh_token=..., token_type="Bearer", expires_in=access_token_ttl, scope=...)`.
      - `load_refresh_token(client, refresh_token)` → `store.load_token_by_refresh(...)` → dựng `RefreshToken`; None nếu không có.
      - `exchange_refresh_token(client, refresh_token, scopes)` → đọc token theo refresh → sinh access+refresh OAuth mới → `store.update_oauth_token(old_access, new_access, new_refresh, new_expires_at)` → trả `OAuthToken` mới. (Downstream JWT giữ nguyên — refresh downstream JWT là Plan 03.)
      - `load_access_token(token)` → `store.load_token(token)` → None nếu không có; kiểm `expires_at < now()` → None (token hết hạn); dựng `AccessToken` với scopes. Đây là method verify mỗi tool call.
      - `revoke_token(token)` → `store.delete_token(...)` (hoặc xoá theo refresh nếu token là refresh).
    - Method bổ trợ `complete_authorization(txn: str, login_data: dict) -> tuple[str, str, str | None]` — wiring nội bộ cho login callback (Task 3) dùng. Logic:
      - `pending = await store.load_pending(txn)` → None → raise `OAuthStoreError` ("phiên authorize hết hạn/không hợp lệ").
      - Sinh authorization `code = secrets.token_urlsafe(32)`.
      - Dựng `code_payload` (scopes, redirect_uri, code_challenge, code_challenge_method từ `pending`).
      - `store.save_auth_code(code=code, client_id=pending["client_id"], code_payload=code_payload, downstream_jwt=login_data["access_token"], downstream_refresh_token=login_data["refresh_token"], user_payload=login_data["user"], expires_at=self._now() + 600)`.
      - `store.delete_pending(txn)` (pending dùng 1 lần).
      - Trả `(code, pending["redirect_uri"], pending["client_state"])`.
    - Helper nội bộ: `_now() -> int` trả `int(time.time())`; `_new_opaque() -> str` sinh token bằng `secrets.token_urlsafe(32)`.
    - Logging: chỉ log `client_id` / loại thao tác / `expired`/`not_found` — KHÔNG log giá trị token/code/JWT/txn.

    **`mcp_app/oauth/__init__.py`** (MODIFY) — thêm export `MedinetOAuthProvider`:
    ```python
    from mcp_app.oauth.provider import MedinetOAuthProvider
    from mcp_app.oauth.store import OAuthStore
    __all__ = ["MedinetOAuthProvider", "OAuthStore"]
    ```

    **`tests/test_oauth_provider.py`** (NEW) — module docstring tiếng Việt liệt kê behavior. Dùng fixture `oauth_store` (Plan 01) + `ApiClient` mock qua respx (hoặc MagicMock cho method không gọi API). Test phủ (RESEARCH.md §"Validation Architecture → Test Map"):
    - `test_dcr_roundtrip` — `register_client` → `get_client` trả đúng (MCP-01).
    - `test_get_client_missing` — `get_client` id lạ → None.
    - `test_token_valid` — lưu token hợp lệ qua store → `load_access_token` trả `AccessToken` (`@pytest.mark.critical`).
    - `test_token_expired` — token `expires_at` quá khứ → `load_access_token` trả None (`@pytest.mark.critical`).
    - `test_token_wrong_scope` — token không có hoặc scope không hợp lệ → reject/None.
    - `test_authorize_persists_pending` — gọi `authorize(client, params)` → URL trả có `?txn=`; `store.load_pending(txn)` trả bản ghi đủ `redirect_uri`/`code_challenge`/`client_state`.
    - `test_complete_authorization` — `fake_pending_authorize(oauth_store)` seed pending → `complete_authorization(txn, login_data)` trả `(code, redirect_uri, client_state)`; `store.load_auth_code(code)` trả record bind downstream JWT; `store.load_pending(txn)` trả None (pending đã xoá).
    - `test_exchange_authorization_code` — lưu auth code → `exchange_authorization_code` trả `OAuthToken` có refresh_token; code bị xoá sau exchange.
    - `test_exchange_refresh_token_rotates` — refresh → token mới khác token cũ; refresh cũ không dùng lại được.
    - `test_revoke_token` — `revoke_token` → `load_access_token` trả None.
    Lưu ý chữ ký method SDK: executor PHẢI khớp đúng type SDK 1.27 (đọc `.venv` provider.py). Nếu chữ ký lệch mô tả trên, theo SDK — KHÔNG bịa.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class MedinetOAuthProvider" Hub_All/mcp_service/mcp_app/oauth/provider.py` tồn tại.
    - `grep -E "def (get_client|register_client|authorize|load_authorization_code|exchange_authorization_code|load_refresh_token|exchange_refresh_token|load_access_token|revoke_token)" Hub_All/mcp_service/mcp_app/oauth/provider.py` — đủ 9 method.
    - `grep "save_pending" Hub_All/mcp_service/mcp_app/oauth/provider.py` tồn tại (authorize lưu pending qua store).
    - `grep "complete_authorization" Hub_All/mcp_service/mcp_app/oauth/provider.py` tồn tại.
    - `grep "MedinetOAuthProvider" Hub_All/mcp_service/mcp_app/oauth/__init__.py` — export có mặt.
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py` — toàn bộ PASS (gồm test_token_valid, test_token_expired, test_token_wrong_scope, test_dcr_roundtrip, test_authorize_persists_pending, test_complete_authorization).
    - `grep -rE "logger.*(token|code|jwt|txn)" Hub_All/mcp_service/mcp_app/oauth/provider.py` — KHÔNG log giá trị secret.
  </acceptance_criteria>
  <done>MedinetOAuthProvider implement đủ 9 method SDK + complete_authorization, DCR + token lifecycle + pending-authorize persistence hoạt động, test PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Login form route + wire FastMCP với auth_server_provider + lifespan init_schema</name>
  <files>Hub_All/mcp_service/mcp_app/oauth/login.py, Hub_All/mcp_service/mcp_app/server.py, Hub_All/mcp_service/tests/test_oauth_login.py</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/server.py (bản hiện tại — line 53 FastMCP, 58-73 singleton, 281-288 build_asgi_app)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Pattern 1", §"Pattern 2")
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§login.py, §server.py — 5 điểm gắn)
    - Hub_All/mcp_service/tests/test_integration_tools.py (mẫu respx + helper _envelope_ok/_envelope_err)
    - Hub_All/mcp_service/tests/conftest.py (Plan 01 — fixture oauth_store + helper fake_pending_authorize)
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (sau Task 2 — có complete_authorization)
    - SDK source: `mcp/server/auth/settings.py` trong .venv — chữ ký AuthSettings/ClientRegistrationOptions/RevocationOptions
    - SDK source: `mcp/server/fastmcp/server.py` trong .venv — đọc `streamable_http_app()` để biết app trả về thuộc loại `Starlette` và có attribute `lifespan` (session-manager lifespan của SDK)
  </read_first>
  <behavior>
    - GET `/login?txn=<txn>` → trả HTML 200 chứa `<form method="post">` với input email + password.
    - POST `/login/callback` với credential đúng → gọi `api_client.login()` → `provider.complete_authorization(txn, login_data)` → redirect 302 về `redirect_uri` kèm `?code=...&state=...`.
    - POST `/login/callback` với credential sai → render lại HTML form 200 kèm thông báo "Sai tài khoản hoặc mật khẩu".
    - `build_asgi_app()` trả app có route `/.well-known/oauth-authorization-server` (SDK mount tự động khi provider non-None) + route `/login`.
    - **OAuthStore schema được khởi tạo tự động trong lifespan `startup` của Starlette app** — `init_schema()` chạy đúng 1 lần khi app khởi động, TRƯỚC khi nhận request đầu tiên. KHÔNG dùng `asyncio.run` thủ công, KHÔNG để "executor chọn".
  </behavior>
  <action>
    **`mcp_app/oauth/login.py`** (NEW) — module docstring tiếng Việt (Phase 8.3, D-02, vai trò login form, lưu ý KHÔNG log password). `from __future__ import annotations`. Implement:
    - `def render_login_form(txn: str, error: str | None = None) -> HTMLResponse` — trả HTML tối giản: `<form method="post" action="/login/callback">` với hidden input `txn`, input `email` (type email), input `password` (type password), nút submit. Nếu `error` → hiển thị dòng đỏ. Label tiếng Việt ("Email", "Mật khẩu", "Đăng nhập"). Lưu ý: API Service `LoginRequest` dùng field `email` — form gửi `email` (RESEARCH.md A6).
    - `async def login_get(request)` — Starlette handler GET `/login`: đọc query `txn`, trả `render_login_form(txn)`. Nếu thiếu `txn` → 400.
    - `async def login_callback(request)` — Starlette handler POST `/login/callback`: đọc form `txn`, `email`, `password`. Logic:
      (a) `login_data = await api_client.login(email, password)`;
      (b) nếu None → `render_login_form(txn, error="Sai tài khoản hoặc mật khẩu")` (HTTP 200);
      (c) nếu OK → `code, redirect_uri, client_state = await provider.complete_authorization(txn, login_data)` → `RedirectResponse(f"{redirect_uri}?code={code}&state={client_state}", status_code=302)` (nếu `client_state` là None thì bỏ phần `&state=`).
    - `def get_login_routes(provider, api_client) -> list[Route]` — trả list `Route` Starlette để mount: `Route("/login", login_get, methods=["GET"])`, `Route("/login/callback", login_callback, methods=["POST"])`. Dùng closure để inject `provider`/`api_client` vào handler.
    - Bảo mật: KHÔNG log password/email/JWT; lỗi `ApiServerError` → render form kèm thông báo chung "Lỗi hệ thống, thử lại sau" (KHÔNG stack trace — mẫu `_map_api_error`); `OAuthStoreError` (txn hết hạn) → render form kèm "Phiên đăng nhập hết hạn, vui lòng kết nối lại".

    **`mcp_app/server.py`** (MODIFY) — mở rộng, KHÔNG rewrite:
    1. Import: `from contextlib import asynccontextmanager`, `from mcp.server.auth.settings import AuthSettings, ClientRegistrationOptions, RevocationOptions`, `from pydantic import AnyHttpUrl`, `from mcp_app.oauth import MedinetOAuthProvider, OAuthStore`, `from mcp_app.oauth.login import get_login_routes`.
    2. Thêm singleton lười cho `_store` + `_provider` (mẫu `_get_client` lines 58-73):
       ```python
       _oauth_store: OAuthStore | None = None
       _oauth_provider: MedinetOAuthProvider | None = None

       def _get_oauth_store() -> OAuthStore: ...   # khởi tạo từ settings.oauth_state_db_path
       def _get_oauth_provider() -> MedinetOAuthProvider: ...  # inject store + _get_client() + settings
       ```
    3. **LOCKED — khởi tạo schema qua lifespan startup (KHÔNG asyncio.run, KHÔNG "executor chọn"):**
       `streamable_http_app()` trả về một Starlette app và GẮN SẴN lifespan riêng của SDK (session-manager lifespan). Để chạy `init_schema()` lúc startup mà KHÔNG ghi đè lifespan SDK, dùng cách COMPOSE lifespan như sau trong `build_asgi_app()`:
       ```python
       app = mcp.streamable_http_app()        # Starlette app — đã có lifespan SDK
       _sdk_lifespan = app.router.lifespan_context   # giữ lifespan SDK gốc

       @asynccontextmanager
       async def _composed_lifespan(app_):
           # startup: khởi tạo schema OAuth store TRƯỚC khi nhận request
           await _get_oauth_store().init_schema()
           # rồi chạy lifespan SDK (session manager) lồng bên trong
           async with _sdk_lifespan(app_):
               yield
           # shutdown: đóng store best-effort
           await _get_oauth_store().aclose()

       app.router.lifespan_context = _composed_lifespan
       ```
       Cách này bảo toàn lifespan session-manager của SDK (RESEARCH.md ghi `streamable_http_app()` tự gắn lifespan riêng) đồng thời thêm bước `init_schema()` tất định lúc startup. Nếu API attribute lệch (vd SDK 1.27 đặt lifespan ở `app.router.lifespan_context` hay `app.lifespan` khác) — executor đọc Starlette/SDK source xác nhận đúng attribute, GIỮ nguyên ý đồ compose (KHÔNG vứt lifespan SDK). Tuyệt đối KHÔNG dùng `asyncio.run(init_schema())` ở module-level hay trong `build_asgi_app()` ngoài lifespan.
    4. Build `FastMCP(...)` trong `build_asgi_app()` để provider sẵn sàng:
       ```python
       def build_asgi_app() -> Any:
           settings = get_settings()
           provider = _get_oauth_provider()
           mcp = FastMCP(
               "Medinet Wiki MCP Service",
               stateless_http=True,
               auth_server_provider=provider,
               auth=AuthSettings(
                   issuer_url=AnyHttpUrl(settings.oauth_issuer_url),
                   resource_server_url=AnyHttpUrl(settings.oauth_issuer_url),
                   client_registration_options=ClientRegistrationOptions(
                       enabled=True, valid_scopes=["wiki"], default_scopes=["wiki"],
                   ),
                   revocation_options=RevocationOptions(enabled=True),
                   required_scopes=["wiki"],
               ),
           )
           # đăng ký lại 3 tool lên instance mcp này (xem lưu ý dưới)
           app = mcp.streamable_http_app()
           # ... compose lifespan như mục 3 ...
           for route in get_login_routes(provider, _get_client()):
               app.router.routes.append(route)
           return app
       ```
       LƯU Ý QUAN TRỌNG về 3 tool: hiện 3 tool dùng decorator `@mcp.tool()` ở module-level gắn vào `mcp` module-level. Nếu chuyển `FastMCP(...)` vào `build_asgi_app()`, executor PHẢI giữ 3 tool đăng ký được — 2 cách: (a) giữ `mcp` module-level NHƯNG truyền `auth_server_provider` ngay khi khởi tạo module-level (provider phải sẵn sàng lúc import — chấp nhận được nếu provider lazy-safe); HOẶC (b) chuyển định nghĩa 3 tool thành hàm `register_tools(mcp)` gọi trong `build_asgi_app()`. Executor chọn cách ÍT thay đổi nhất, GIỮ logic 3 tool body nguyên vẹn, ghi rõ trong SUMMARY. GIỮ `stateless_http=True` (Pitfall 2).
    5. Cập nhật `main()` log — thêm `oauth_issuer_url` (URL public — an toàn log); KHÔNG log signing key/token.
    6. Cập nhật `_close_client_atexit` — thêm đóng `_oauth_store` best-effort (lifespan shutdown đã đóng, atexit là lớp phòng vệ thêm).
    7. Cập nhật module docstring `server.py` — thêm dòng Phase 8.3: OAuth AS + login form + lifespan init_schema.

    **`tests/test_oauth_login.py`** (NEW) — module docstring tiếng Việt. Dùng Starlette `TestClient` (hoặc httpx ASGITransport) trên app từ `build_asgi_app()`, respx mock `/api/auth/login`, fixture `oauth_store` + override config issuer URL. Test phủ:
    - `test_login_get_renders_form` — GET `/login?txn=x` → 200 + HTML chứa `<form` + input `email` + `password`.
    - `test_login_callback_wrong_credential` — POST `/login/callback` (respx `/api/auth/login` trả 401) → 200 + HTML chứa "Sai tài khoản hoặc mật khẩu".
    - `test_login_callback_success_redirects` — seed pending bằng helper `fake_pending_authorize` (từ conftest Plan 01) vào store mà provider dùng → POST callback với `txn` đã seed (respx `/api/auth/login` trả 200 envelope JWT pair) → 302 redirect kèm `code=` + `state=`. (Helper `fake_pending_authorize` đã có sẵn — KHÔNG tự viết lại seeding.)
    - `test_lifespan_inits_schema` — khởi động app qua TestClient (kích hoạt lifespan startup) → query trực tiếp store đã dùng cho thấy bảng `oauth_clients`/`oauth_pending` tồn tại (vd `save_client` + `get_client` chạy không lỗi sau khi app start). Chứng minh `init_schema()` chạy trong lifespan.
    - `test_metadata_endpoint` — GET `/.well-known/oauth-authorization-server` → 200 + JSON có `issuer`, `authorization_endpoint`, `token_endpoint`, `registration_endpoint` (`@pytest.mark.critical` — chứng minh SDK mount route AS).
    - `test_protected_resource_metadata` — GET `/.well-known/oauth-protected-resource` → 200 JSON.
    Đánh `@pytest.mark.critical` cho `test_metadata_endpoint` + `test_login_callback_wrong_credential`.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_login.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "auth_server_provider" Hub_All/mcp_service/mcp_app/server.py` tồn tại.
    - `grep "AuthSettings" Hub_All/mcp_service/mcp_app/server.py` tồn tại.
    - `grep "get_login_routes" Hub_All/mcp_service/mcp_app/server.py` tồn tại (route /login mount).
    - `grep -E "lifespan|init_schema" Hub_All/mcp_service/mcp_app/server.py` — lifespan compose + init_schema có mặt.
    - `grep -c "asyncio.run" Hub_All/mcp_service/mcp_app/server.py` — KHÔNG có `asyncio.run` cho init_schema (init_schema chỉ chạy trong lifespan).
    - `grep "stateless_http=True" Hub_All/mcp_service/mcp_app/server.py` vẫn tồn tại (Pitfall 2).
    - `grep -E "login_get|login_callback" Hub_All/mcp_service/mcp_app/oauth/login.py` tồn tại.
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_login.py` — PASS, gồm `test_metadata_endpoint` + `test_lifespan_inits_schema` + `test_login_callback_success_redirects`.
    - `cd Hub_All/mcp_service && uv run pytest` — regression toàn bộ suite PASS.
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app tests` — clean.
  </acceptance_criteria>
  <done>FastMCP wired với OAuth provider, SDK mount metadata route, login form xác thực Medinet hoạt động, OAuthStore schema init trong lifespan startup, regression không vỡ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude web → MCP Service `/authorize` `/token` `/register` | Untrusted OAuth client crosses here — DCR mở, mọi input cần SDK validate |
| User browser → `/login` `/login/callback` | Credential Medinet (email/password) crosses here — pass-through tới API Service, KHÔNG verify tại MCP |
| MCP Service → API Service `/api/auth/login` | Internal HTTP — credential ủy thác cho Argon2 verify của API Service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-05 | Spoofing/Tampering | `/authorize` + `/token` PKCE | mitigate | PKCE S256 do SDK `mcp` 1.27 xử lý — KHÔNG hand-roll; `create_auth_routes` validate `code_challenge`. Code single-use: `exchange_authorization_code` gọi `store.delete_auth_code` sau exchange. |
| T-08.3-06 | DoS/Tampering | DCR `/register` mở | accept | Open DCR là Claude's Discretion (CONTEXT D-01) — gate bảo mật thật là bước login credential Medinet. Authorization code chỉ cấp SAU login OK. Rate-limit `/register` defer (low-value: client rác không truy cập được tool nếu không login). |
| T-08.3-07 | Spoofing | Credential brute-force qua `/login/callback` | mitigate | `/api/auth/login` của API Service đã có anti-timing oracle (Phase 3 AUTH). MCP `login()` trả None đồng nhất cho mọi credential sai — KHÔNG lộ user tồn tại hay không. |
| T-08.3-08 | Information Disclosure | Log password/JWT trong login flow | mitigate | `login.py` + `api_client.login()` KHÔNG log email/password/JWT — chỉ log status. Acceptance criteria kiểm grep. |
| T-08.3-09 | Elevation of Privilege | confused deputy — OAuth token dùng quyền sai user | mitigate | Mỗi authorization code/token bind đúng 1 downstream JWT của đúng user đã login (`store.save_token`/`save_auth_code` lưu `user_payload` + `downstream_jwt` cùng record). KHÔNG service key chung (D-03). |
| T-08.3-10 | Tampering | Issuer/redirect spoofing | mitigate | `issuer_url` cố định từ config (validated Plan 01). PKCE + state do SDK. `redirect_uri` lấy từ `oauth_pending` của chính client đã DCR (lưu lúc `authorize()`) — KHÔNG nhận redirect tuỳ ý lúc callback. |
</threat_model>

<verification>
- `cd Hub_All/mcp_service && uv run pytest` — toàn bộ suite PASS (test cũ + test_oauth_provider.py + test_oauth_login.py + test_api_client.py mở rộng).
- `cd Hub_All/mcp_service && uv run ruff check .` — clean.
- `cd Hub_All/mcp_service && uv run python -c "from mcp_app.server import build_asgi_app; app=build_asgi_app(); print('app ok')"` — app build không lỗi.
- Manual probe trong test: GET `/.well-known/oauth-authorization-server` trả JSON có `registration_endpoint` (chứng minh DCR mount).
</verification>

<success_criteria>
- `MedinetOAuthProvider` implement đủ 9 method `OAuthAuthorizationServerProvider` SDK 1.27 + `complete_authorization`.
- `FastMCP` wired `auth_server_provider` + `AuthSettings` → SDK mount metadata/`/authorize`/`/token`/`/register`.
- `OAuthStore.init_schema()` chạy trong lifespan startup của Starlette app — bảng sẵn sàng trước request đầu tiên, lifespan SDK được bảo toàn.
- Login form xác thực bằng `POST /api/auth/login` (D-02) — credential sai render lỗi, đúng phát code bind JWT.
- `api_client.login()` gọi `/api/auth/login`, trả dict/None/raise đúng.
- 3 tool cũ vẫn đăng ký + chạy (regression PASS); `stateless_http=True` giữ.
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-02-SUMMARY.md`
</output>
</content>
</invoke>
