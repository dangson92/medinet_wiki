---
phase: 08.3-mcp-oauth-deploy-public-https
verified: 2026-05-19T00:00:00Z
status: gaps_found
score: 4/5 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Token OAuth hợp lệ → MCP tool gọi API Service với danh tính/hub scope đúng; token thiếu/sai → từ chối (SC3)"
    status: partial
    reason: >-
      Đường happy path + token thiếu/sai/hết hạn đã đúng và có test phủ (78/78
      PASS). Nhưng 2 lỗi đúng đắn OAuth (08.3-REVIEW.md CR-01, CR-02) khiến
      authorization code / refresh token KHÔNG single-use chắc chắn — vi phạm
      RFC 6749 §10.4/§10.5. Đây là lõi của SC3 "token sai → từ chối": một code
      đã dùng / một refresh token đã rotate vẫn có thể bị tái dùng (replay /
      double-spend). Test suite hiện chỉ phủ đường tuần tự, không phát hiện.
    artifacts:
      - path: "Hub_All/mcp_service/mcp_app/oauth/provider.py"
        issue: >-
          exchange_authorization_code (dòng 201-241): save_token() chạy TRƯỚC
          delete_auth_code() — nếu save_token lỗi giữa chừng, code không bị xoá
          và còn hạn → replay. exchange_refresh_token (dòng 257-290): rotate qua
          update_oauth_token định vị bản ghi theo access_token đã hết hạn, không
          kiểm rowcount → 2 request cùng refresh token cũ đều rotate được; nếu
          access_token bị đổi giữa chừng, UPDATE khớp 0 dòng nhưng provider vẫn
          trả token mới không tồn tại trong store (user bị đá ra im lặng).
      - path: "Hub_All/mcp_service/mcp_app/oauth/store.py"
        issue: >-
          Thiếu thao tác claim nguyên tử (DELETE ... RETURNING) cho auth code;
          update_oauth_token (dòng 314-322) WHERE access_token = ? và không trả
          rowcount. oauth_tokens.refresh_token thiếu ràng buộc UNIQUE (WR-05).
    missing:
      - "Xoá/claim authorization code TRƯỚC khi save_token (DELETE ... RETURNING) — code single-use kể cả khi save_token lỗi (CR-01)"
      - "exchange_refresh_token rotate định vị theo refresh_token (giá trị caller cầm), kiểm cursor.rowcount == 1 — phát hiện refresh-token reuse, raise lỗi thay vì trả token mồ côi (CR-02)"
      - "Thêm test phủ đường đồng thời / replay: dùng lại code đã exchange, dùng lại refresh token đã rotate → phải bị từ chối"
deferred:
  - truth: "cleanup_expired chạy định kỳ dọn code/pending hết hạn (08.3-REVIEW WR-06)"
    addressed_in: "Phase 10"
    evidence: "Phase 10 goal: Hardening + Observability — background task/scheduler là phạm vi hardening; pending có rủi ro hạn (WR-02) cần vá cùng CR nhưng việc gọi cleanup định kỳ thuộc hardening."
human_verification:
  - test: "SC4 — kết nối thật từ Claude web qua 'Add custom connector'"
    expected: >-
      curl https://<domain>/.well-known/oauth-authorization-server trả 200 JSON;
      Claude web hoàn tất OAuth flow + login Medinet; 3 tool list_hubs/
      search_wiki/ask_wiki xuất hiện và gọi được; sau >15 phút tool vẫn chạy
      (downstream JWT tự refresh).
    why_human: >-
      Cần domain public HTTPS thật + DNS A record + tài khoản Claude web +
      thao tác UI trên dialog connector — không auto-verify được. Quy trình
      7 bước đầy đủ trong 08.3-HUMAN-UAT.md.
---

# Phase 8.3: MCP OAuth 2.0 + Deploy Public HTTPS — Báo cáo Xác minh

**Phase Goal:** Cho phép Claude web "Add custom connector" kết nối tới MeWiki MCP Service. Thêm lớp OAuth 2.0 cho MCP Service và đưa service ra public qua HTTPS. Kiến trúc đích: Claude web ─(OAuth)─► MCP Service (public HTTPS) ─(JWT RS256 nội bộ theo user)─► API Service.
**Verified:** 2026-05-19
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria ROADMAP)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | MCP Service expose endpoint OAuth 2.0 đúng MCP authorization spec; Claude web "Add custom connector" hoàn tất OAuth flow | ✓ VERIFIED | `build_asgi_app()` build OK, SDK mount đủ route: `/.well-known/oauth-authorization-server`, `/.well-known/oauth-protected-resource`, `/authorize`, `/token`, `/register`, `/revoke` + `/login`, `/login/callback`. `MedinetOAuthProvider` implement đủ 9 method `OAuthAuthorizationServerProvider` SDK 1.27 + `complete_authorization`. `test_metadata_endpoint` (critical) PASS. Phần "hoàn tất OAuth flow" thật từ Claude web → human (SC4). |
| 2 | MCP Service chạy public qua HTTPS (Dockerfile + reverse proxy TLS + domain) | ✓ VERIFIED | `mcp_service/Dockerfile` multi-stage uv, non-root `mcpuser`, `EXPOSE 8190`, `CMD uvicorn ... --factory`, HEALTHCHECK probe well-known. `Caddyfile` auto-TLS forward nguyên host (KHÔNG `handle_path`/`uri strip`). `docker-compose.yml` có `mcp_service` + `caddy` + 3 named volume; mcp_service KHÔNG map port host (chỉ Caddy expose 80/443). `docker compose config` hợp lệ (orchestrator + SUMMARY xác nhận). |
| 3 | Token OAuth hợp lệ → tool gọi API đúng danh tính/hub scope; token thiếu/sai → từ chối, hub isolation enforce API-side | ✗ FAILED (partial) | Happy path + reject token thiếu/sai/hết hạn đúng: `_resolve_credential` → `load_access_token` verify, `_call_api` forward `Bearer <downstream JWT>`, retry-on-401 refresh + rotation. Test `test_token_valid/expired/wrong_scope`, `test_tool_oauth_token_invalid`, `test_tool_jwt_refresh_retry/fails` PASS. **NHƯNG** CR-01 (auth code không single-use khi save_token lỗi) + CR-02 (refresh token double-spend / token mồ côi) vi phạm RFC 6749 — token "đã dùng" vẫn có thể tái dùng. Xem Gaps. |
| 4 | Kết nối thật từ Claude web: 3 tool list_hubs/search_wiki/ask_wiki gọi được qua connector | ? UNCERTAIN | Cần domain public + DNS + tài khoản Claude web — `human_needed`. Quy trình UAT 7 bước trong `08.3-HUMAN-UAT.md`. |
| 5 | Test suite phủ OAuth flow (token hợp lệ/hết hạn/sai scope); regression mcp_service + API không vỡ | ✓ VERIFIED | `uv run pytest` mcp_service: **78/78 PASS**. API unit regression: **119/119 PASS**. OAuth flow có test phủ token valid/expired/wrong-scope, DCR roundtrip, authorize-persists-pending, complete_authorization, exchange code/refresh, metadata endpoint. Lưu ý: test KHÔNG phủ đường replay/đồng thời (liên quan CR-01/CR-02) — xem Gaps missing. |

**Score:** 4/5 truths verified (SC3 FAILED, SC4 uncertain → human)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | `cleanup_expired` chạy định kỳ (scheduler/background task) — WR-06 | Phase 10 | Phase 10 = Hardening + Observability; background task dọn state hết hạn thuộc phạm vi hardening. (Lưu ý: kiểm tra hạn pending ở runtime — WR-02 — nên vá cùng cụm CR ở SC3, không defer.) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mcp_service/mcp_app/oauth/store.py` | OAuthStore — SQLite 4 bảng | ✓ VERIFIED | 402 dòng; 4 bảng `oauth_clients/oauth_auth_codes/oauth_tokens/oauth_pending`; CRUD đầy đủ + parametrized query. |
| `mcp_service/mcp_app/oauth/provider.py` | MedinetOAuthProvider 9 method | ✓ VERIFIED | 326 dòng; đủ 9 method SDK + `complete_authorization`. (Có lỗi đúng đắn — xem Gaps.) |
| `mcp_service/mcp_app/oauth/login.py` | Route /login + /login/callback | ✓ VERIFIED | 139 dòng; `render_login_form` (escape XSS), `login_get`, `login_callback`, `get_login_routes`. |
| `mcp_service/mcp_app/server.py` | FastMCP wired auth_server_provider + lifespan init_schema | ✓ VERIFIED | 552 dòng; `auth_server_provider`, `AuthSettings`, `get_login_routes`, `_composed_lifespan` chạy `init_schema()` startup, `_resolve_credential`/`_call_api`, `stateless_http=True` giữ. |
| `mcp_service/mcp_app/auth.py` | extract_oauth_token + Credential | ✓ VERIFIED | 125 dòng; `extract_oauth_token` + dataclass `Credential`; `extract_api_key` giữ song song. |
| `mcp_service/mcp_app/api_client.py` | forward Bearer JWT + login + refresh_jwt | ✓ VERIFIED | 278 dòng; `Authorization: Bearer`, `login()` → `/api/auth/login`, `refresh_jwt()` → `/api/auth/refresh`. |
| `mcp_service/mcp_app/config.py` | Settings OAuth fields | ✓ VERIFIED | `oauth_issuer_url` (validated http/https, default an toàn), `oauth_state_db_path`, token TTL. |
| `mcp_service/Dockerfile` | Image multi-stage uv non-root factory CMD | ✓ VERIFIED | 41 dòng; builder/runtime, `mcpuser`, `--factory`, EXPOSE 8190, HEALTHCHECK well-known. |
| `Hub_All/Caddyfile` | Reverse proxy auto-TLS → mcp_service:8190 | ✓ VERIFIED | `reverse_proxy medinet-mcp:8190`, KHÔNG strip path. |
| `Hub_All/docker-compose.yml` | Service mcp_service + caddy + volume | ✓ VERIFIED | `mcp_service` + `caddy` + `medinet_mcp_oauth_state` volume; mcp_service không map port host. |
| `08.3-HUMAN-UAT.md` | Quy trình UAT SC4 | ✓ VERIFIED | Tiền điều kiện domain/DNS + 7 bước SC4 + fallback Cloudflare Tunnel. |
| `mcp_service/uv.lock` | Lockfile cho Dockerfile | ✓ VERIFIED | 745 dòng; `aiosqlite 0.22.1`, `uvicorn 0.47.0`, `mcp 1.27.1` (pin giữ <1.28). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| server.py | MedinetOAuthProvider | `FastMCP(auth_server_provider=...)` | ✓ WIRED | `auth_server_provider=_get_oauth_provider()` dòng 472. |
| login.py | `/api/auth/login` | `api_client.login()` | ✓ WIRED | `login_callback` gọi `api_client.login(email, password)` dòng 108. |
| provider.py | OAuthStore | constructor injection | ✓ WIRED | `MedinetOAuthProvider(store, api_client, ...)` — store dùng xuyên 9 method. |
| server.py | downstream JWT từ OAuthStore | `load_token → downstream_jwt` | ✓ WIRED | `_resolve_credential` → `store.load_token` → `Credential(kind="jwt", ...)`. |
| api_client.py | `/api/auth/refresh` | `refresh_jwt on 401` | ✓ WIRED | `refresh_jwt` POST `/api/auth/refresh`; `_call_api` bắt `ApiUnauthorizedError` → refresh → `update_downstream_jwt` → retry. |
| server.py | OAuthStore.init_schema | lifespan startup | ✓ WIRED | `_composed_lifespan` chạy `init_schema()` trước lifespan SDK; KHÔNG `asyncio.run`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| 3 tool (list_hubs/search_wiki/ask_wiki) | `data` từ `_call_api` | API Service qua httpx (`/api/hubs`, `/api/search`, `/api/ask`) | Yes — gọi HTTP thật, forward credential | ✓ FLOWING (verify qua respx integration test) |
| OAuth flow | downstream JWT/refresh | `api_client.login()` → API Service `/api/auth/login` envelope `data` | Yes — JWT thật bind vào `oauth_tokens`/`oauth_auth_codes` | ✓ FLOWING |
| login form | HTML | `render_login_form(txn, error)` | Yes — form thật, escape XSS | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| App build + mount route OAuth | `python -c "build_asgi_app()"` | 9 route gồm `/.well-known/oauth-authorization-server`, `/authorize`, `/token`, `/register`, `/revoke`, `/login` | ✓ PASS |
| Test suite mcp_service | `uv run pytest -q` | 78 passed | ✓ PASS |
| Regression API unit | `uv run pytest tests/unit -q` | 119 passed | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MCP-01 | 08.3-01, 08.3-02, 08.3-04 | MCP Server expose RAG/Wiki cho Claude/ChatGPT agent (OAuth surface) | ✓ SATISFIED | OAuth AS đủ 9 method + metadata route + DCR + login form; deploy public HTTPS. Bullet v4.0 — không có checkbox riêng trong REQUIREMENTS.md. |
| MCP-02 | 08.3-03, 08.3-04 | Tool exposure scoped theo hub_id | ◑ PARTIAL | 3 tool resolve OAuth token → forward downstream JWT đúng user; hub isolation enforce API-side. Đường happy path đúng, nhưng đúng đắn token lifecycle (CR-01/CR-02) còn lỗ — ảnh hưởng tính "scoped đúng" khi token bị replay. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| oauth/provider.py | 201-241 | save_token TRƯỚC delete_auth_code — code không single-use khi save_token lỗi | 🛑 Blocker | CR-01 — authorization code replay, vi phạm RFC 6749 §10.5. |
| oauth/provider.py | 257-290 | exchange_refresh_token rotate theo access_token, không kiểm rowcount | 🛑 Blocker | CR-02 — refresh token double-spend / token mồ côi, vi phạm RFC 6749 §10.4. |
| oauth/provider.py | 132-176 | complete_authorization không kiểm hạn pending transaction | ⚠️ Warning | WR-02 — txn lộ sống vô thời hạn (cleanup_expired không được gọi định kỳ). |
| oauth/provider.py | 314-326 | revoke_token không dọn token family nhất quán | ⚠️ Warning | WR-03 — revoke bằng access token cũ (đã rotate) khớp 0 dòng. |
| oauth/provider.py / server.py | 166-168 / 224-233 | `KeyError` chưa xử lý khi payload downstream thiếu field | ⚠️ Warning | WR-04 — lỗi 500 + stack trace, vi phạm T-08.3-08. |
| oauth/store.py | 55-66 | `oauth_tokens.refresh_token` thiếu UNIQUE | ⚠️ Warning | WR-05 — `load_token_by_refresh` nhập nhằng (rủi ro thấp). |
| oauth/store.py / server.py | 392-402 | `cleanup_expired` định nghĩa nhưng không bao giờ gọi | ⚠️ Warning | WR-06 — code/pending hết hạn tích tụ (deferred Phase 10). |
| oauth/login.py | 130-132 | `client_state` không URL-encode khi ghép redirect | ℹ️ Info | IN-05 — URL redirect méo nếu state chứa ký tự đặc biệt. |

> Ghi nhận tích cực: mọi truy vấn SQLite parametrized (không SQL injection); không log token/JWT/code/password thô; `render_login_form` escape XSS; Dockerfile non-root + `.dockerignore` loại `.env`/`.oauth`. Xem 08.3-REVIEW.md.

### Human Verification Required

**SC4 — Kết nối thật từ Claude web qua "Add custom connector"** (`human_needed`)

- **Test:** Theo `08.3-HUMAN-UAT.md` 7 bước — operator chuẩn bị domain public HTTPS + DNS A record + port 80/443 (hoặc Cloudflare Tunnel) + tài khoản Medinet active; `docker compose up -d`; probe well-known; "Add custom connector" trên Claude web; login Medinet; gọi 3 tool; chờ >15 phút gọi lại tool.
- **Expected:** OAuth discovery trả 200 JSON; connector kết nối thành công; 3 tool xuất hiện và trả kết quả đúng role-scope (D-04); tool vẫn chạy sau khi JWT downstream tự refresh.
- **Why human:** Cần domain public + DNS + tài khoản Claude web + thao tác UI — không auto-verify được.

### Gaps Summary

Bốn trên năm Success Criteria đã đạt và verify được tự động (SC1, SC2, SC5 ✓; SC4 chờ human UAT). Toàn bộ artifact tồn tại, đủ thực chất, wired đúng; 78/78 test mcp_service + 119/119 test API regression PASS.

**Gap chặn SC3:** Code review 08.3-REVIEW.md phát hiện 2 lỗi Nghiêm trọng về tính đúng đắn OAuth — `exchange_authorization_code` xoá code SAU khi `save_token` (CR-01, replay khi save lỗi) và `exchange_refresh_token` rotate định vị sai theo `access_token` không kiểm rowcount (CR-02, double-spend / token mồ côi). SC3 yêu cầu "token thiếu/sai → từ chối"; một authorization code đã dùng hoặc refresh token đã rotate là "token sai" theo RFC 6749 §10.4/§10.5 nhưng hệ thống hiện không từ chối chắc chắn. Test suite chỉ phủ đường tuần tự nên không bắt được — cần vá CR-01/CR-02 (claim code nguyên tử + rotate theo refresh_token có kiểm rowcount) kèm test replay/đồng thời.

6 Warning + 5 Info còn lại không chặn goal nhưng nên xử lý: WR-02 (kiểm hạn pending) nên vá cùng cụm CR; WR-06 (gọi `cleanup_expired` định kỳ) defer Phase 10 (hardening); WR-03/WR-04/WR-05 và các Info là cải thiện độ bền.

---

_Verified: 2026-05-19_
_Verifier: Claude (gsd-verifier)_
