---
phase: 08.3-mcp-oauth-deploy-public-https
verified: 2026-05-19T00:00:00Z
status: human_needed
score: 4/5 must-haves verified (SC4 chờ human UAT)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "SC3 — Authorization code single-use: claim_auth_code (DELETE ... RETURNING) claim nguyên tử TRƯỚC save_token; code đã dùng / save_token lỗi giữa chừng đều không replay được (CR-01)"
    - "SC3 — Refresh token single-use: rotate_token định vị theo refresh_token caller cầm + kiểm cursor.rowcount == 1; refresh đã rotate → raise OAuthStoreError reuse (CR-02)"
    - "SC3 — Code/pending hết hạn bị từ chối: guard expires_at TẠI exchange_authorization_code + kiểm _PENDING_TTL trong complete_authorization"
    - "SC5 — Test suite phủ đường replay/reuse: 11 test mới (5 store + 6 provider) chứng minh code replay / refresh reuse / code+pending hết hạn bị từ chối; regression 78 → 89 PASS"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "cleanup_expired chạy định kỳ dọn code/pending hết hạn (08.3-REVIEW WR-06)"
    addressed_in: "Phase 10"
    evidence: "Phase 10 goal: Hardening + Observability — background task/scheduler dọn state hết hạn thuộc phạm vi hardening."
human_verification:
  - test: "SC1/SC2/SC4 — kết nối thật từ Claude web qua 'Add custom connector' trên public HTTPS"
    expected: >-
      curl https://<domain>/.well-known/oauth-authorization-server và
      /.well-known/oauth-protected-resource trả 200 JSON; Claude web hoàn tất
      OAuth flow + login Medinet; 3 tool list_hubs/search_wiki/ask_wiki xuất
      hiện và gọi được; sau >15 phút tool vẫn chạy (downstream JWT tự refresh).
    why_human: >-
      Cần domain public HTTPS thật + DNS A record + tài khoản Claude web +
      thao tác UI trên dialog connector — không auto-verify được. Quy trình
      7 bước đầy đủ trong 08.3-HUMAN-UAT.md.
---

# Phase 8.3: MCP OAuth 2.0 + Deploy Public HTTPS — Báo cáo Xác minh (Re-verification)

**Phase Goal:** Cho phép Claude web "Add custom connector" kết nối tới MeWiki MCP Service. Thêm lớp OAuth 2.0 cho MCP Service (`Hub_All/mcp_service/`, port 8190) và đưa service ra public qua HTTPS. Kiến trúc đích: Claude web ─(OAuth)─► MCP Service (public HTTPS) ─(JWT RS256 nội bộ theo user)─► API Service.
**Verified:** 2026-05-19
**Status:** human_needed
**Re-verification:** Yes — sau gap closure Plan 05 + 06 (vá CR-01/CR-02 token lifecycle + thêm test replay/reuse)

## Tóm tắt Re-verification

Verification trước (status `gaps_found`, 4/5) tìm thấy SC3 FAILED partial — 2 lỗi Critical RFC 6749 (CR-01 authorization code replay, CR-02 refresh token double-spend). Plan 05 vá code, Plan 06 thêm 11 test. Re-verification này tập trung kiểm SC3 + SC5:

- **CR-01 đã vá đúng đắn:** `store.claim_auth_code` (store.py:215-242) dùng `DELETE FROM oauth_auth_codes WHERE code = ? RETURNING ...` — đọc + xoá code trong MỘT câu lệnh nguyên tử. `provider.exchange_authorization_code` (provider.py:223) gọi `claim_auth_code` TRƯỚC `save_token`; code bị xoá trước khi save → nếu save lỗi, code không còn để replay.
- **CR-02 đã vá đúng đắn:** `store.rotate_token` (store.py:355-376) `UPDATE ... WHERE refresh_token = ?` + trả `cursor.rowcount == 1`. UNIQUE INDEX `idx_oauth_tokens_refresh` (store.py:80-82) đảm bảo rowcount không bao giờ > 1. `provider.exchange_refresh_token` (provider.py:293-307) raise `OAuthStoreError` khi `not ok` (rowcount != 1 = reuse).
- **Guard hạn:** `exchange_authorization_code` (provider.py:233) reject code hết hạn ngay sau claim; `complete_authorization` (provider.py:156) reject pending quá `_PENDING_TTL` (600s).
- **Test chứng minh:** 11 test mới — chạy lại `pytest -q` cho **89 passed** (78 → 89), `-m critical` **17 passed**. Các test replay/reuse genuinely thực thi đường tấn công (gọi lại exchange với cùng `AuthorizationCode`, dùng lại `refresh_obj` cũ, fault-injection patch `save_token`) → `pytest.raises(OAuthStoreError)`.

**Kết luận:** Gap SC3 đã đóng — xác minh trực tiếp trên codebase + chạy test thật. Status chuyển `gaps_found` → `human_needed` vì SC1/SC2/SC4 (kết nối thật Claude web + public HTTPS deploy) là item human-UAT hợp lệ; toàn bộ artifact/config code đã tồn tại và đúng.

## Goal Achievement

### Observable Truths (Success Criteria ROADMAP)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | MCP Service expose endpoint OAuth 2.0 đúng MCP authorization spec; Claude web "Add custom connector" hoàn tất OAuth flow | ✓ VERIFIED (code) / ? human (live) | SDK mcp 1.27 mount đủ route well-known + `/authorize` + `/token` + `/register` + `/revoke`; `MedinetOAuthProvider` implement đủ 9 method `OAuthAuthorizationServerProvider` + `complete_authorization`. `test_metadata_endpoint` (critical) PASS. Phần "hoàn tất flow thật từ Claude web" → human (SC4). |
| 2 | MCP Service chạy public qua HTTPS (Dockerfile + reverse proxy TLS + domain) | ✓ VERIFIED (code) / ? human (deploy) | `mcp_service/Dockerfile` multi-stage uv non-root `mcpuser` `--factory` EXPOSE 8190 HEALTHCHECK; `Caddyfile` auto-TLS forward nguyên host; `docker-compose.yml` có `mcp_service` + `caddy` + named volume, mcp_service không map port host. Deploy public thật → human. |
| 3 | Token OAuth hợp lệ → tool gọi API đúng danh tính/hub scope; token thiếu/sai → từ chối; vòng đời token: code/refresh single-use, code/pending hết hạn bị từ chối (RFC 6749 §10.4/§10.5) | ✓ VERIFIED | **Gap đã đóng.** `claim_auth_code` DELETE...RETURNING nguyên tử TRƯỚC `save_token` (CR-01); `rotate_token` rowcount check + UNIQUE INDEX (CR-02); guard `expires_at` tại `exchange_authorization_code`; `_PENDING_TTL` check tại `complete_authorization`. 11 test mới chứng minh replay code / reuse refresh / code+pending hết hạn / save_token-fail-no-replay đều bị từ chối. Happy path + reject token thiếu/sai/hết hạn vẫn đúng (test cũ PASS). |
| 4 | Kết nối thật từ Claude web: 3 tool list_hubs/search_wiki/ask_wiki gọi được qua connector | ? UNCERTAIN (human) | Cần domain public + DNS + tài khoản Claude web — `human_needed`. Quy trình UAT 7 bước trong `08.3-HUMAN-UAT.md`. Code 3 tool + `_resolve_credential`/`_call_api` đã wired và có integration test (respx). |
| 5 | Test suite phủ OAuth flow (token hợp lệ/hết hạn/sai scope); regression mcp_service + API không vỡ | ✓ VERIFIED | `uv run pytest -q` mcp_service: **89/89 PASS** (verifier chạy lại — xác nhận); `-m critical`: **17/17 PASS**. Test mới phủ replay/reuse/expired/PKCE-persist. API regression 222 passed, 1 PRE-EXISTING fail (`test_auth_refresh_race.py`, KeyError 'email') trong `api/` — phase 08.3 KHÔNG chạm `api/` (0 file api/ đổi); KHÔNG phải regression do phase này. |

**Score:** 4/5 truths VERIFIED (SC1/SC2/SC3/SC5 ✓); SC4 → human. SC1/SC2 phần code đã verified, phần live-deploy gộp vào item human cùng SC4.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | `cleanup_expired` chạy định kỳ (scheduler/background task) — WR-06 | Phase 10 | Phase 10 = Hardening + Observability; background task dọn state hết hạn thuộc phạm vi hardening. Lưu ý: kiểm hạn pending/code TẠI runtime (WR-02) đã được vá ở Plan 05 — chỉ việc gọi `cleanup_expired` định kỳ mới defer. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `mcp_service/mcp_app/oauth/store.py` | OAuthStore + claim_auth_code + rotate_token + unique index | ✓ VERIFIED | 459 dòng; `claim_auth_code` (DELETE...RETURNING dòng 215-242), `rotate_token` (rowcount dòng 355-376), `idx_oauth_tokens_refresh` UNIQUE INDEX trong `_SCHEMA_STATEMENTS` (dòng 79-83). `delete_auth_code`/`update_oauth_token` cũ giữ nguyên. Query parametrized. |
| `mcp_service/mcp_app/oauth/provider.py` | exchange code/refresh đúng đắn single-use + guard hạn + validate payload | ✓ VERIFIED | 358 dòng; `_PENDING_TTL=600`; `exchange_authorization_code` dùng `claim_auth_code` + guard `expires_at` (dòng 223-234); `exchange_refresh_token` dùng `rotate_token` + raise reuse (dòng 293-307); `complete_authorization` kiểm `_PENDING_TTL` + validate shape `login_data` (dòng 155-165); `revoke_token` tra cứu cả access lẫn refresh. KHÔNG còn gọi `update_oauth_token`/`delete_auth_code(authorization_code.code)`. |
| `mcp_service/mcp_app/oauth/login.py` | login_callback bắt KeyError/TypeError | ✓ VERIFIED | `except (KeyError, TypeError)` (dòng 129) render `render_login_form` lỗi hệ thống chung. |
| `mcp_service/mcp_app/server.py` | _call_api validate shape new_pair | ✓ VERIFIED | `if not all(new_pair.get(k) for k in ("access_token", "refresh_token"))` (dòng 228) → raise `ApiUnauthorizedError` có kiểm soát thay vì KeyError 500. |
| `mcp_service/tests/test_oauth_store.py` | test claim_auth_code + rotate_token | ✓ VERIFIED | 15 test (10 cũ + 5 mới): `test_claim_auth_code_single_use/_missing`, `test_rotate_token_success/_reuse_returns_false/_unknown_refresh`. |
| `mcp_service/tests/test_oauth_provider.py` | test replay/reuse/expired/PKCE | ✓ VERIFIED | 17 test (11 cũ + 6 mới): `test_exchange_code_replay_rejected`, `test_exchange_code_save_token_failure_no_replay` (fault-injection patch), `test_exchange_refresh_token_reuse_rejected`, `test_complete_authorization_pending_expired`, `test_exchange_code_expired_rejected`, `test_pkce_challenge_persisted`. |
| `mcp_service/Dockerfile` / `Caddyfile` / `docker-compose.yml` | Deploy public HTTPS | ✓ VERIFIED | Không đổi từ verification trước — multi-stage non-root, auto-TLS, named volume. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| provider.exchange_authorization_code | store.claim_auth_code | claim TRƯỚC save_token | ✓ WIRED | provider.py:223 `claim_auth_code(authorization_code.code)` rồi mới `save_token` dòng 239. |
| provider.exchange_refresh_token | store.rotate_token | rotate theo refresh_token + kiểm rowcount | ✓ WIRED | provider.py:293 `rotate_token(old_refresh_token=refresh_token.token, ...)`; `if not ok` → raise. |
| store.rotate_token | UNIQUE INDEX idx_oauth_tokens_refresh | rowcount đáng tin | ✓ WIRED | `_SCHEMA_STATEMENTS` có `CREATE UNIQUE INDEX IF NOT EXISTS` — `init_schema` chạy ở lifespan startup. |
| login_callback | render_login_form (KeyError/TypeError) | except branch | ✓ WIRED | login.py:129-132. |
| server._call_api | ApiUnauthorizedError | validate new_pair shape | ✓ WIRED | server.py:228-231. |
| test_oauth_provider.py | provider.exchange_* | pytest.raises(OAuthStoreError) | ✓ WIRED | 6 test mới gọi thẳng provider, kỳ vọng raise — verifier chạy lại: 32/32 OAuth test PASS. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| OAuth store + provider test (gồm replay/reuse mới) | `uv run pytest -q tests/test_oauth_store.py tests/test_oauth_provider.py` | 32 passed in 0.74s | ✓ PASS |
| Toàn bộ suite mcp_service | `uv run pytest -q` | 89 passed in 1.76s | ✓ PASS |
| Test critical | `uv run pytest -q -m critical` | 17 passed, 72 deselected | ✓ PASS |

> Spot-check do verifier tự chạy lại — KHÔNG dựa vào claim SUMMARY. Con số 89/89 + 17/17 critical khớp claim Plan 06.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MCP-01 | 08.3-01..06 | MCP Server expose OAuth surface cho Claude/agent | ✓ SATISFIED | OAuth AS đủ 9 method + metadata route + DCR + login; deploy config public HTTPS. **Lưu ý:** MCP-01 KHÔNG có trong REQUIREMENTS.md canonical (38 REQ-ID v1) — phase 08.3 là phase chèn thêm; không fail verification vì thiếu traceability. |
| MCP-02 | 08.3-03..06 | Tool exposure scoped theo hub_id | ✓ SATISFIED | 3 tool resolve OAuth token → forward downstream JWT đúng user; hub isolation enforce API-side. Token lifecycle (CR-01/CR-02) nay đúng đắn → "scoped đúng" không còn bị replay phá. Trước đây ◑ PARTIAL — nay nâng lên SATISFIED sau gap closure. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| oauth/provider.py | 293-307 | exchange_refresh_token phát hiện reuse nhưng KHÔNG thu hồi cả token family | ⚠️ Warning | 08.3-REVIEW WR-01 — chặn được double-spend cùng refresh token, nhưng phiên bị đánh cắp (chain đã rotate trước) vẫn sống. Cần `family_id` để thu hồi family. Không chặn goal SC3 (single-use đã đảm bảo) — cải thiện độ bền, cân nhắc Phase 10. |
| oauth/store.py | 252-282 / 355-376 | `save_token` INSERT OR REPLACE / `rotate_token` UPDATE không bắt `aiosqlite.IntegrityError` | ⚠️ Warning | 08.3-REVIEW WR-02 — va chạm PK access_token (xác suất cực thấp, 256-bit entropy) rò 500. Nên bọc thành OAuthStoreError. Không chặn goal. |
| oauth/provider.py | 287-308 | `exchange_refresh_token` đọc record 2 lần (`load_token_by_refresh` rồi `rotate_token`) | ⚠️ Warning | 08.3-REVIEW WR-03 — TOCTOU nhỏ; KHÔNG nguy hiểm về đúng đắn (rowcount vẫn bắt reuse), `record["scopes"]` có thể cũ. Code-smell. |
| oauth/store.py | 339-353 | `update_oauth_token` thành dead code sau Plan 05 (provider chuyển sang `rotate_token`) | ℹ️ Info | 08.3-REVIEW IN-01 — giữ lại có rủi ro tái mở CR-02 nếu ai gọi lại. Plan 05 cố ý giữ để không vỡ test cũ; nên xoá hoặc đánh dấu DEPRECATED. |
| oauth/store.py | 140 | `import time` trong thân `save_client` | ℹ️ Info | 08.3-REVIEW IN-02 — không nhất quán, nên đưa lên module-level. |

> Ghi nhận tích cực: mọi truy vấn SQLite parametrized; không log token/JWT/code/password thô; `claim_auth_code` claim-then-use nguyên tử; `rotate_token` reuse-detection qua rowcount; UNIQUE INDEX đảm bảo rowcount đáng tin. 08.3-REVIEW gap-closure kết luận **0 Critical** — các bản vá CR-01/CR-02 ĐÚNG ĐẮN và đủ test. 5 Warning/Info còn lại KHÔNG chặn goal.

### Human Verification Required

**SC1 / SC2 / SC4 — Kết nối thật từ Claude web qua "Add custom connector" trên public HTTPS** (`human_needed`)

- **Test:** Theo `08.3-HUMAN-UAT.md` 7 bước — operator chuẩn bị domain public HTTPS + DNS A record + port 80/443 (hoặc Cloudflare Tunnel) + tài khoản Medinet active; `docker compose up -d`; probe `/.well-known/oauth-authorization-server` + `/.well-known/oauth-protected-resource`; "Add custom connector" trên Claude web; login Medinet; gọi 3 tool `list_hubs`/`search_wiki`/`ask_wiki`; chờ >15 phút gọi lại tool.
- **Expected:** OAuth discovery trả 200 JSON; connector kết nối thành công; 3 tool xuất hiện và trả kết quả đúng role-scope (D-04); tool vẫn chạy sau khi JWT downstream tự refresh.
- **Why human:** Cần domain public HTTPS thật + DNS + tài khoản Claude web + thao tác UI trên dialog connector — không auto-verify được. Toàn bộ artifact code/config (Dockerfile, Caddyfile, docker-compose.yml, OAuth provider, 3 tool) đã verified tồn tại và đúng; chỉ phần kết nối live cần human.

### Gaps Summary

**Không còn gap chặn goal.** Gap SC3 từ verification trước (2 lỗi Critical RFC 6749) đã được đóng hoàn toàn:

- **CR-01 (replay authorization code)** — `claim_auth_code` dùng `DELETE ... RETURNING` đọc+xoá nguyên tử; code bị xoá TRƯỚC `save_token`. Verifier xác minh trực tiếp trên store.py + provider.py và chạy `test_exchange_code_save_token_failure_no_replay` (fault-injection patch `save_token` raise) — PASS, chứng minh code không replay được kể cả khi save lỗi.
- **CR-02 (refresh token double-spend)** — `rotate_token` định vị theo `refresh_token` + kiểm `cursor.rowcount == 1`; UNIQUE INDEX đảm bảo rowcount tin cậy; provider raise `OAuthStoreError` khi reuse. `test_exchange_refresh_token_reuse_rejected` + `test_rotate_token_reuse_returns_false` PASS.
- **Code/pending hết hạn** — guard `expires_at` tại `exchange_authorization_code` + `_PENDING_TTL` tại `complete_authorization`. `test_exchange_code_expired_rejected` + `test_complete_authorization_pending_expired` PASS.
- **SC5** — regression 78 → 89 PASS; verifier chạy lại xác nhận 89/89 + 17/17 critical.

Phase đạt 4/5 Success Criteria verify được tự động (SC1/SC2 phần code, SC3, SC5). SC4 (cùng phần live của SC1/SC2) là item human-UAT hợp lệ — cần domain public + Claude web thật. Status `human_needed`: tự động hoá đã xanh hết, chờ human UAT theo `08.3-HUMAN-UAT.md`.

5 Warning/Info từ 08.3-REVIEW gap-closure (WR-01 family revocation, WR-02 IntegrityError, WR-03 đọc 2 lần, IN-01 dead code, IN-02 import) KHÔNG chặn goal — là cải thiện độ bền, cân nhắc Phase 10. WR-06 (`cleanup_expired` định kỳ) defer Phase 10.

**Lưu ý API regression:** `api/` có 1 test fail PRE-EXISTING (`test_auth_refresh_race.py::test_refresh_happy_returns_new_pair`, KeyError 'email') — phase 08.3 KHÔNG chạm file nào trong `api/`; đây KHÔNG phải regression do phase này gây ra; user đã xác nhận và chọn tiếp tục.

---

_Verified: 2026-05-19 (Re-verification sau gap closure Plan 05 + 06)_
_Verifier: Claude (gsd-verifier)_
