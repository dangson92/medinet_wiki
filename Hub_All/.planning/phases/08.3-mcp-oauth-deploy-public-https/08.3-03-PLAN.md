---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - Hub_All/mcp_service/mcp_app/auth.py
  - Hub_All/mcp_service/mcp_app/api_client.py
  - Hub_All/mcp_service/mcp_app/server.py
  - Hub_All/mcp_service/tests/test_auth.py
  - Hub_All/mcp_service/tests/test_api_client.py
  - Hub_All/mcp_service/tests/test_server.py
  - Hub_All/mcp_service/tests/test_integration_tools.py
  - Hub_All/mcp_service/tests/conftest.py
autonomous: true
requirements: [MCP-02]
must_haves:
  truths:
    - "Tool call mang OAuth token → MCP Service resolve user + downstream JWT → forward Authorization: Bearer <JWT> xuống API Service"
    - "X-API-Key local vẫn dùng song song được — client Claude Code/Desktop không vỡ"
    - "Downstream JWT hết hạn (API trả 401) → MCP Service refresh JWT → retry 1 lần thành công"
    - "Refresh token rotation: lưu đè CẢ access + refresh mới — lần refresh sau không fail"
    - "Token OAuth thiếu/sai/hết hạn → tool trả JSON-RPC error MCP_UNAUTHORIZED, hub isolation vẫn enforce API-side"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/auth.py"
      provides: "extract_oauth_token + resolve credential cho tool"
      contains: "extract_oauth_token"
    - path: "Hub_All/mcp_service/mcp_app/api_client.py"
      provides: "forward Bearer JWT + refresh_jwt() + retry-on-401"
      contains: "refresh_jwt"
  key_links:
    - from: "Hub_All/mcp_service/mcp_app/server.py"
      to: "downstream JWT từ OAuthStore"
      via: "resolve OAuth token → load_token → downstream_jwt"
      pattern: "downstream_jwt|resolve"
    - from: "Hub_All/mcp_service/mcp_app/api_client.py"
      to: "/api/auth/refresh"
      via: "refresh_jwt on 401"
      pattern: "/api/auth/refresh"
---

<objective>
Hoàn tất D-03/D-04 — đường danh tính từ OAuth token tới API Service: mỗi tool call resolve OAuth token → lấy downstream JWT của đúng user → `ApiClient` forward `Authorization: Bearer <JWT>` (thay `X-API-Key`). Xử lý JWT downstream hết hạn giữa phiên OAuth dài (Claude's Discretion): API trả 401 → `refresh_jwt()` đổi JWT mới → lưu rotation → retry 1 lần. GIỮ `X-API-Key` song song cho client local (Claude's Discretion — khuyến nghị giữ).

Purpose: MCP-02 — 3 tool gọi API Service scoped theo hub_id. Plan 02 đã làm OAuth flow; plan này nối OAuth token → JWT → tool call, và đóng pitfall JWT downstream hết hạn (Pitfall 4/5).
Output: 3 tool chạy được qua cả OAuth token lẫn X-API-Key; hub isolation enforce API-side; refresh-on-401 hoạt động.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-CONTEXT.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-02-SUMMARY.md

<interfaces>
<!-- Hợp đồng từ codebase + Plan 01/02. -->

auth.py hiện tại — pattern đọc header từ MCP Context:
```python
def extract_api_key(ctx: object) -> str | None:
    try:
        request = ctx.request_context.request
        if request is None:
            return None
        return request.headers.get("x-api-key")
    except (AttributeError, LookupError, TypeError):
        return None

def require_api_key(ctx: object) -> str:   # raise ToolError nếu thiếu
    ...
```

api_client.py hiện tại — _request forward X-API-Key (line 79):
```python
async def _request(self, method, path, *, api_key: str, json_body=None, params=None) -> Any:
    headers = {"X-API-Key": api_key}
    ...
    if resp.status_code == 401: raise ApiUnauthorizedError(message)
async def get(self, path, *, api_key, params=None) -> Any: ...
async def post(self, path, *, api_key, json_body=None) -> Any: ...
async def login(self, email, password) -> dict | None: ...   # thêm ở Plan 02
```

OAuthStore (Plan 01) — load_token / update_downstream_jwt:
```python
async def load_token(self, access_token) -> dict | None: ...
    # trả {downstream_jwt, downstream_refresh_token, user_payload, scopes, expires_at, ...}
async def update_downstream_jwt(self, access_token, new_jwt, new_refresh) -> None: ...
```

API Service POST /api/auth/refresh — body {refresh_token}, trả envelope:
  data = {access_token, refresh_token, expires_at, user}
  REFRESH CÓ ROTATION (AUTH-02) — refresh cũ bị blacklist; PHẢI lưu đè cả 2 token mới.
  JWT TTL: access 900s, refresh 604800s.

server.py — 3 tool gọi `require_api_key(ctx)` đầu handler (lines 132, 174, 230);
  body tool dùng `_get_client().get(...)` / `.post(...)` với `api_key=`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract_oauth_token + ApiClient forward Bearer JWT + refresh_jwt()</name>
  <files>Hub_All/mcp_service/mcp_app/auth.py, Hub_All/mcp_service/mcp_app/api_client.py, Hub_All/mcp_service/tests/test_auth.py, Hub_All/mcp_service/tests/test_api_client.py</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/auth.py (bản hiện tại — `extract_api_key`/`require_api_key`)
    - Hub_All/mcp_service/mcp_app/api_client.py (bản hiện tại + sau Plan 02 — `_request`/`get`/`post`/`login`)
    - Hub_All/mcp_service/tests/test_auth.py (test hiện có cho extract_api_key)
    - Hub_All/mcp_service/tests/test_api_client.py (mẫu test respx; verify header forward)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Pattern 3", §"Đọc Bearer token trong tool", §Pitfall 4/5)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§auth.py, §api_client.py — điểm sửa 1)
  </read_first>
  <behavior>
    - `extract_oauth_token(ctx)` với header `Authorization: Bearer abc` → trả `"abc"`.
    - `extract_oauth_token(ctx)` không header / header sai format → trả `None` (KHÔNG raise).
    - `ApiClient.get/post` với param `jwt="X"` → request có header `Authorization: Bearer X`, KHÔNG có `X-API-Key`.
    - `ApiClient.get/post` với param `api_key="K"` (không jwt) → request có header `X-API-Key: K` (nhánh cũ giữ nguyên).
    - `refresh_jwt("old_refresh")` với API trả 200 → trả dict `{access_token, refresh_token, ...}`.
    - `refresh_jwt("hết_hạn")` với API trả 401 → trả `None`.
  </behavior>
  <action>
    **`auth.py`** (MODIFY) — GIỮ nguyên `extract_api_key` + `require_api_key`. Thêm:
    ```python
    def extract_oauth_token(ctx: object) -> str | None:
        """Đọc 'Authorization: Bearer <token>' từ MCP Context. None nếu không có/sai format.

        KHÔNG verify token — chỉ trích. Verify do MedinetOAuthProvider.load_access_token.
        Bảo mật: KHÔNG log giá trị token.
        """
        try:
            request = ctx.request_context.request  # type: ignore[attr-defined]
            if request is None:
                return None
            header = request.headers.get("authorization", "")
        except (AttributeError, LookupError, TypeError):
            return None
        parts = header.split(" ", 1)
        if len(parts) == 2 and parts[0].lower() == "bearer" and parts[1]:
            return parts[1]
        return None
    ```
    Cập nhật module docstring `auth.py` — thêm dòng Phase 8.3: hỗ trợ OAuth Bearer token song song X-API-Key.

    **`api_client.py`** (MODIFY) — sửa `_request` để hỗ trợ CẢ jwt lẫn api_key:
    1. Đổi chữ ký `_request` — `api_key` thành optional, thêm `jwt: str | None = None`:
       ```python
       async def _request(self, method, path, *, api_key: str | None = None,
                          jwt: str | None = None, json_body=None, params=None) -> Any:
           if jwt:
               headers = {"Authorization": f"Bearer {jwt}"}
           elif api_key:
               headers = {"X-API-Key": api_key}
           else:
               raise ApiClientError("Thiếu credential — cần jwt hoặc api_key")
           ...
       ```
       GIỮ nguyên phần unwrap envelope + map status → exception. KHÔNG log header.
    2. Đổi chữ ký `get` + `post` — thêm `jwt: str | None = None`, `api_key` thành optional, pass-through xuống `_request`.
    3. Thêm method `refresh_jwt`:
       ```python
       async def refresh_jwt(self, refresh_token: str) -> dict | None:
           """Đổi JWT mới bằng refresh token API Service (POST /api/auth/refresh).

           REFRESH CÓ ROTATION (AUTH-02) — caller PHẢI lưu đè cả access + refresh
           mới (Pitfall 5). None nếu refresh token hết hạn/blacklist.
           """
           try:
               resp = await self._client.post(
                   "/api/auth/refresh", json={"refresh_token": refresh_token}
               )
           except httpx.RequestError as e:
               raise ApiServerError(f"Không kết nối được API Service: {type(e).__name__}") from e
           try:
               envelope = resp.json()
           except ValueError as e:
               raise ApiServerError(
                   f"API Service trả response không hợp lệ (status {resp.status_code})"
               ) from e
           if resp.status_code == 200 and envelope.get("success") is True:
               return envelope.get("data")
           if resp.status_code == 401:
               return None
           err = envelope.get("error") or {}
           raise ApiServerError(f"{err.get('code', 'ERROR')}: {err.get('message', '')}")
       ```
    Cập nhật module docstring `api_client.py` — Phase 8.3: forward Bearer JWT + refresh_jwt.

    **`test_auth.py`** (MODIFY) — GIỮ test cũ. Thêm: `test_extract_oauth_token_present` (header Bearer → token), `test_extract_oauth_token_missing` (không header → None), `test_extract_oauth_token_malformed` (header không "Bearer " → None).

    **`test_api_client.py`** (MODIFY) — GIỮ test cũ + 3 test login từ Plan 02. Thêm:
    - `test_forward_bearer_jwt` — `get/post` với `jwt=` → respx verify request header `Authorization: Bearer ...`, KHÔNG có `X-API-Key`.
    - `test_forward_api_key_still_works` — `get/post` với `api_key=` → header `X-API-Key` (nhánh cũ).
    - `test_request_no_credential_raises` — gọi không jwt không api_key → raise `ApiClientError`.
    - `test_refresh_jwt_success` — respx `/api/auth/refresh` 200 → dict.
    - `test_refresh_jwt_expired` — respx 401 → None.
    Đánh `@pytest.mark.critical` cho `test_forward_bearer_jwt` + `test_refresh_jwt_success`.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_auth.py tests/test_api_client.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def extract_oauth_token" Hub_All/mcp_service/mcp_app/auth.py` tồn tại.
    - `grep "Authorization.*Bearer" Hub_All/mcp_service/mcp_app/api_client.py` tồn tại.
    - `grep "async def refresh_jwt" Hub_All/mcp_service/mcp_app/api_client.py` tồn tại.
    - `grep "/api/auth/refresh" Hub_All/mcp_service/mcp_app/api_client.py` tồn tại.
    - `grep "def extract_api_key" Hub_All/mcp_service/mcp_app/auth.py` vẫn tồn tại (X-API-Key song song giữ).
    - `cd Hub_All/mcp_service && uv run pytest tests/test_auth.py tests/test_api_client.py` — PASS.
  </acceptance_criteria>
  <done>extract_oauth_token + ApiClient forward Bearer JWT song song X-API-Key + refresh_jwt() hoạt động, test PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tool resolve OAuth token → forward downstream JWT + retry-on-401 refresh</name>
  <files>Hub_All/mcp_service/mcp_app/server.py, Hub_All/mcp_service/tests/test_server.py, Hub_All/mcp_service/tests/test_integration_tools.py, Hub_All/mcp_service/tests/conftest.py</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/server.py (bản sau Plan 02 — 3 tool, `_get_client`, provider/store singleton)
    - Hub_All/mcp_service/mcp_app/auth.py (sau Task 1 — `extract_oauth_token`)
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (Plan 02 — `load_access_token`)
    - Hub_All/mcp_service/mcp_app/oauth/store.py (Plan 01 — `load_token`, `update_downstream_jwt`)
    - Hub_All/mcp_service/tests/test_server.py (9 test mock `_get_client`, helper `_patch_client`)
    - Hub_All/mcp_service/tests/test_integration_tools.py (9 test respx; verify header lines 75-77)
    - Hub_All/mcp_service/tests/conftest.py (sau Plan 01/02 — fixture mock_ctx, oauth_store)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Pattern 3", §Pitfall 4/5)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§server.py điểm gắn 3)
  </read_first>
  <behavior>
    - Tool call với header `Authorization: Bearer <oauth_token_hợp_lệ>` → resolve token → lấy downstream JWT → tool gọi API forward `Bearer <JWT>` → trả kết quả.
    - Tool call với header `X-API-Key` (không OAuth) → nhánh cũ: forward `X-API-Key` (client local không vỡ).
    - Tool call không credential nào → `ToolError` MCP_UNAUTHORIZED.
    - Tool call OAuth token hết hạn / không trong store → `ToolError` MCP_UNAUTHORIZED.
    - Tool call downstream JWT 401 (API trả 401) → MCP refresh JWT (`/api/auth/refresh`) → lưu rotation vào store → retry 1 lần → thành công.
    - Refresh JWT cũng fail (refresh token hết hạn) → `ToolError` MCP_UNAUTHORIZED yêu cầu re-connect.
  </behavior>
  <action>
    **`server.py`** (MODIFY) — mở rộng, GIỮ logic 3 tool body nguyên vẹn:
    1. Thêm helper resolve credential — hàm async dùng đầu mỗi tool thay cho `require_api_key(ctx)` đơn thuần:
       ```python
       async def _resolve_credential(ctx: Context) -> _Credential:
           """Resolve credential cho tool call — ưu tiên OAuth token, fallback X-API-Key.

           - Có Authorization: Bearer → verify qua provider.load_access_token → lấy
             downstream JWT từ store → trả _Credential(kind="jwt", ...).
           - Có X-API-Key (không OAuth) → trả _Credential(kind="api_key", ...).
           - Không có gì / OAuth token sai → raise ToolError MCP_UNAUTHORIZED.
           """
       ```
       `_Credential` là dataclass nhỏ giữ `kind` ("jwt"|"api_key"), `value` (JWT hoặc api_key), `oauth_token` (str|None — cần cho refresh-rotation update store). Định nghĩa trong `server.py` hoặc `auth.py` — executor chọn, ưu tiên `auth.py` cho gọn.
       Logic resolve:
       - `token = extract_oauth_token(ctx)` → nếu có: `access = await _get_oauth_provider().load_access_token(token)`; None → `raise ToolError("MCP_UNAUTHORIZED: token OAuth thiếu/sai/hết hạn — kết nối lại connector")`; có → đọc `store.load_token(token)` lấy `downstream_jwt` → `_Credential(kind="jwt", value=downstream_jwt, oauth_token=token)`.
       - else `key = extract_api_key(ctx)` → có: `_Credential(kind="api_key", value=key, oauth_token=None)`.
       - else: `raise ToolError("MCP_UNAUTHORIZED: thiếu Authorization Bearer hoặc X-API-Key")`.
    2. Thêm helper gọi API có retry-on-401 (chỉ cho nhánh JWT):
       ```python
       async def _call_api(cred, method, path, *, json_body=None, params=None) -> Any:
           """Gọi API Service với credential. Nhánh JWT: 401 → refresh → retry 1 lần."""
       ```
       - `kind == "api_key"` → gọi `_get_client().get/post(..., api_key=cred.value)` thẳng (KHÔNG retry — X-API-Key không refresh được).
       - `kind == "jwt"` → gọi với `jwt=cred.value`; bắt `ApiUnauthorizedError` lần đầu:
         - đọc `store.load_token(cred.oauth_token)` lấy `downstream_refresh_token`;
         - `new_pair = await _get_client().refresh_jwt(downstream_refresh_token)`;
         - None → re-raise `ApiUnauthorizedError` (→ tool map ToolError MCP_UNAUTHORIZED);
         - có → `await store.update_downstream_jwt(cred.oauth_token, new_pair["access_token"], new_pair["refresh_token"])` (Pitfall 5 — lưu đè cả 2) → retry gọi API với JWT mới 1 lần.
    3. Sửa 3 tool (`list_hubs`, `search_wiki`, `ask_wiki`) — thay `api_key = require_api_key(ctx)` + `_get_client().get/post(..., api_key=api_key)` bằng:
       ```python
       cred = await _resolve_credential(ctx)
       ...
       data = await _call_api(cred, "GET", "/api/hubs", params={"per_page": 100})
       ```
       GIỮ NGUYÊN toàn bộ logic dựng path/body + parse response của 3 tool. Map exception giữ `_map_api_error`.
    4. Cập nhật module docstring `server.py` — Phase 8.3: tool forward OAuth-bound JWT, retry-on-401 refresh.

    **`conftest.py`** (MODIFY) — thêm fixture/helper: `mock_ctx_oauth` — biến thể `mock_ctx` set header `authorization: Bearer <token>` thay `x-api-key`. Nếu `server.py` thêm singleton `_oauth_store`/`_oauth_provider`, mở rộng autouse `reset_api_client` (hoặc thêm fixture autouse mới) reset cả `_oauth_store`/`_oauth_provider` về `None` trước+sau mỗi test.

    **`test_server.py`** (MODIFY) — GIỮ 9 test. Thêm:
    - `test_tool_resolves_oauth_token` — ctx có Bearer token hợp lệ (seed store + provider) → tool chạy, forward JWT.
    - `test_tool_oauth_token_invalid` — Bearer token không trong store → `ToolError` MCP_UNAUTHORIZED.
    - `test_tool_no_credential` — không header → `ToolError`.

    **`test_integration_tools.py`** (MODIFY) — GIỮ 9 test (X-API-Key path vẫn pass). Thêm:
    - `test_tool_forwards_bearer_jwt` — ctx OAuth → respx verify request tới API có header `Authorization: Bearer <JWT>` (KHÔNG `X-API-Key`). Mẫu verify header lines 75-77 (đổi `X-API-Key` → `Authorization`).
    - `test_tool_jwt_refresh_retry` — respx: lần 1 `/api/search` trả 401, `/api/auth/refresh` trả 200 JWT mới, lần 2 `/api/search` trả 200 → tool trả kết quả; verify store có downstream JWT mới (`@pytest.mark.critical`).
    - `test_tool_jwt_refresh_fails` — respx: `/api/search` 401, `/api/auth/refresh` 401 → tool raise `ToolError` MCP_UNAUTHORIZED.
    Đánh `@pytest.mark.critical` cho `test_tool_forwards_bearer_jwt` + `test_tool_jwt_refresh_retry`.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_server.py tests/test_integration_tools.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "_resolve_credential|_call_api" Hub_All/mcp_service/mcp_app/server.py` — cả 2 helper.
    - `grep "update_downstream_jwt" Hub_All/mcp_service/mcp_app/server.py` tồn tại (retry-on-401 lưu rotation).
    - `grep "extract_api_key" Hub_All/mcp_service/mcp_app/server.py` vẫn tồn tại (X-API-Key fallback).
    - `grep -c "@mcp.tool()" Hub_All/mcp_service/mcp_app/server.py` trả về 3 (3 tool giữ nguyên).
    - `grep -E "test_tool_forwards_bearer_jwt|test_tool_jwt_refresh_retry" Hub_All/mcp_service/tests/test_integration_tools.py` — cả 2 test.
    - `cd Hub_All/mcp_service && uv run pytest` — toàn bộ suite PASS.
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app tests` — clean.
  </acceptance_criteria>
  <done>3 tool resolve OAuth token → forward downstream JWT, retry-on-401 refresh + rotation, X-API-Key song song; toàn bộ test PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude web → MCP Service tool call (`POST /mcp`) | OAuth token crosses here — verify mỗi call qua `load_access_token` |
| MCP Service → API Service tool endpoints | Forward `Authorization: Bearer <downstream JWT>` — danh tính theo từng user |
| MCP Service → API Service `/api/auth/refresh` | Refresh downstream JWT — refresh token nhạy cảm, có rotation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-11 | Spoofing | Tool call với OAuth token giả/hết hạn | mitigate | `_resolve_credential` gọi `provider.load_access_token` verify mỗi call (Plan 02 kiểm `expires_at`); token sai → `ToolError` MCP_UNAUTHORIZED, tool KHÔNG chạy logic. |
| T-08.3-12 | Elevation of Privilege | confused deputy — tool dùng JWT của user khác | mitigate | OAuth token bind đúng 1 downstream JWT trong `oauth_tokens` (Plan 01/02). `_resolve_credential` lấy JWT theo đúng `oauth_token` của request — KHÔNG service key chung (D-03). Hub isolation + RBAC enforce API-side mỗi call (D-04, HUB-02). |
| T-08.3-13 | Information Disclosure | Log downstream JWT / Authorization header | mitigate | `api_client._request` KHÔNG log header; `_resolve_credential`/`_call_api` chỉ log `kind`/`path`/`status`. Acceptance criteria + verification kiểm grep. |
| T-08.3-14 | Spoofing | Refresh token cũ bị tái dùng sau rotation | mitigate | `refresh_jwt` 401 → `_call_api` re-raise → ToolError. `store.update_downstream_jwt` lưu đè CẢ access + refresh (Pitfall 5) — lần refresh sau dùng token mới hợp lệ. Test `test_tool_jwt_refresh_retry` regression. |
| T-08.3-15 | Tampering | Tool nhận credential rỗng "đi vòng" auth | mitigate | `_resolve_credential` raise `ToolError` khi không có cả OAuth lẫn X-API-Key; `_request` raise `ApiClientError` nếu thiếu credential — không có đường gọi API không credential. |
</threat_model>

<verification>
- `cd Hub_All/mcp_service && uv run pytest` — toàn bộ suite PASS (test cũ + test mới Plan 03).
- `cd Hub_All/mcp_service && uv run ruff check .` — clean.
- `cd Hub_All/mcp_service && uv run mypy mcp_app` — không lỗi type mới.
- `grep -rE "logger.*(jwt|token|authorization)" Hub_All/mcp_service/mcp_app/server.py Hub_All/mcp_service/mcp_app/api_client.py` — KHÔNG log giá trị secret.
- Per-wave merge: `cd Hub_All/api && uv run pytest` — regression API không vỡ (D-03 không sửa API nhưng JWT path dùng chung).
</verification>

<success_criteria>
- Tool call OAuth → resolve token → forward downstream JWT `Bearer` xuống API Service.
- X-API-Key local song song hoạt động — client Claude Code/Desktop không vỡ.
- Downstream JWT 401 → refresh → lưu rotation (cả access + refresh) → retry thành công.
- Token OAuth thiếu/sai/hết hạn → `ToolError` MCP_UNAUTHORIZED; hub isolation enforce API-side.
- 3 tool body logic giữ nguyên; regression mcp_service + API PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-03-SUMMARY.md`
</output>
