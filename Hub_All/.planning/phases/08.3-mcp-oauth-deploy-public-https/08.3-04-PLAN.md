---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - Hub_All/mcp_service/Dockerfile
  - Hub_All/mcp_service/.dockerignore
  - Hub_All/Caddyfile
  - Hub_All/docker-compose.yml
  - Hub_All/mcp_service/README.md
  - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md
autonomous: true
requirements: [MCP-01, MCP-02]
user_setup:
  - service: domain-dns
    why: "OAuth issuer URL bắt buộc public HTTPS — Claude web connector cần domain truy cập được"
    dashboard_config:
      - task: "Trỏ DNS A record domain (vd mcp.medinet.vn) tới IP host chạy MCP Service; mở port 80+443 cho Caddy ACME — HOẶC dùng Cloudflare Tunnel nếu không mở được port"
        location: "Nhà cung cấp DNS / firewall của host"
must_haves:
  truths:
    - "MCP Service build được Docker image multi-stage uv"
    - "docker-compose up khởi động cả mcp_service + reverse proxy, mcp_service gọi API qua docker network"
    - "OAuth state SQLite file nằm trên named volume — sống sót container restart"
    - "Reverse proxy terminate TLS + forward nguyên host tới mcp_service (well-known route ở root không bị strip)"
    - "Đổi reverse proxy (Caddy ↔ Cloudflare Tunnel) chỉ cần đổi MCP_OAUTH_ISSUER_URL + compose, KHÔNG sửa mcp_app"
  artifacts:
    - path: "Hub_All/mcp_service/Dockerfile"
      provides: "Image MCP Service multi-stage uv, non-root, factory CMD"
      contains: "uvicorn"
    - path: "Hub_All/Caddyfile"
      provides: "Reverse proxy auto-TLS → mcp_service:8190"
      contains: "reverse_proxy"
    - path: "Hub_All/docker-compose.yml"
      provides: "Service mcp_service + caddy + volume OAuth state"
      contains: "mcp_service"
    - path: "Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md"
      provides: "Quy trình UAT thủ công SC4 — kết nối thật Claude web"
  key_links:
    - from: "Hub_All/docker-compose.yml"
      to: "mcp_service Dockerfile"
      via: "build context"
      pattern: "context:.*mcp_service"
    - from: "Hub_All/Caddyfile"
      to: "mcp_service:8190"
      via: "reverse_proxy docker network"
      pattern: "mcp_service:8190|medinet-mcp:8190"
---

<objective>
Đưa MCP Service ra public qua HTTPS để Claude web "Add custom connector" kết nối được: viết `Dockerfile` multi-stage `uv` cho `mcp_service/`, thêm service `mcp_service` + reverse proxy `caddy` (auto-TLS) vào `docker-compose.yml`, tạo `Caddyfile`. Đồng thời viết `08.3-HUMAN-UAT.md` — quy trình UAT thủ công cho SC4 (kết nối thật từ Claude web — `human_needed`, không auto-verify được).

Purpose: SC2 (public HTTPS) — hoàn tất MCP-01/MCP-02 ở tầng triển khai. Reverse proxy chọn **Caddy** (RESEARCH.md recommendation: auto-TLS Let's Encrypt, zero-config, ít ma sát nhất). Thiết kế deploy để đổi proxy (Caddy ↔ Cloudflare Tunnel) KHÔNG đụng `mcp_app` — chỉ `MCP_OAUTH_ISSUER_URL` + compose.
Output: `docker-compose up` chạy được stack đầy đủ; operator chỉ cần cấp domain + DNS để hoàn tất UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-CONTEXT.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-03-SUMMARY.md

<interfaces>
<!-- Mẫu deploy từ codebase. Executor copy + điều chỉnh, KHÔNG khám phá lại. -->

api/Dockerfile — multi-stage uv (mẫu cho mcp_service/Dockerfile):
```dockerfile
# syntax=docker/dockerfile:1.7
FROM python:3.12-slim-bookworm AS builder
ENV UV_LINK_MODE=copy UV_COMPILE_BYTECODE=1 UV_PROJECT_ENVIRONMENT=/app/.venv PYTHONDONTWRITEBYTECODE=1
WORKDIR /app
RUN pip install --no-cache-dir uv==0.4.30
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

FROM python:3.12-slim-bookworm AS runtime
ENV PYTHONUNBUFFERED=1 PATH="/app/.venv/bin:$PATH" PYTHONPATH=/app
RUN groupadd ... apiuser && useradd ... apiuser && apt-get install curl ...
WORKDIR /app
COPY --from=builder --chown=apiuser:apiuser /app/.venv /app/.venv
COPY --chown=apiuser:apiuser app ./app
RUN mkdir -p /app/.cocoindex && chown apiuser:apiuser /app/.cocoindex
USER apiuser
EXPOSE 8080
HEALTHCHECK --interval=10s --timeout=5s --start-period=15s --retries=3 \
    CMD curl -fsS http://localhost:8080/healthz || exit 1
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
```

docker-compose.yml — service python-api (mẫu cho mcp_service) + named volume:
```yaml
python-api:
  build: { context: ./api, dockerfile: Dockerfile }
  container_name: medinet-api
  env_file: [./api/.env]
  environment: { ... }
  ports: ["8180:8080"]
  depends_on: { postgres: { condition: service_healthy } }
  networks: [medinet_net]
volumes:
  medinet_cocoindex_state:
networks:
  medinet_net: { driver: bridge }
```

mcp_app entrypoint — `build_asgi_app()` là factory function trả Starlette app (Plan 02 đã đưa FastMCP build vào trong factory).
  → CMD uvicorn cần cờ `--factory` (KHÁC api dùng object trực tiếp).
MCP Service lắng nghe port 8190 (config default service_port).

OAuth issuer default (Plan 01) — `Settings.oauth_issuer_url` CÓ default `http://localhost:8190`.
  → `Settings()` KHÔNG raise khi thiếu env → route `/.well-known/oauth-authorization-server`
    do SDK mount LUÔN trả 200 ngay từ lúc app start, kể cả trước khi operator set domain thật.
  → HEALTHCHECK probe well-known route là TẤT ĐỊNH (xem Task 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile multi-stage uv cho mcp_service + .dockerignore</name>
  <files>Hub_All/mcp_service/Dockerfile, Hub_All/mcp_service/.dockerignore</files>
  <read_first>
    - Hub_All/api/Dockerfile (mẫu multi-stage uv — copy gần 1:1)
    - Hub_All/mcp_service/pyproject.toml (sau Plan 01 — có aiosqlite + uvicorn, uv.lock đã sinh)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§Dockerfile — khác biệt cần áp)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Runtime State Inventory" — SQLite volume)
  </read_first>
  <action>
    **`mcp_service/Dockerfile`** (NEW) — copy cấu trúc `api/Dockerfile`, áp các khác biệt:
    - Builder stage: GIỮ nguyên `api/Dockerfile` lines 1-11 (`COPY pyproject.toml uv.lock ./` — `uv.lock` đã sinh ở Plan 01).
    - Runtime stage:
      - Đổi user `apiuser` → `mcpuser` (groupadd/useradd/chown).
      - `COPY --chown=mcpuser:mcpuser mcp_app ./mcp_app` (thay `app ./app`).
      - BỎ dòng `COPY alembic.ini` + `COPY migrations` (MCP Service KHÔNG có DB chung — không migration).
      - Thay `RUN mkdir -p /app/.cocoindex ...` bằng `RUN mkdir -p /app/.oauth && chown mcpuser:mcpuser /app/.oauth` — thư mục SQLite OAuth state, tạo + chown TRƯỚC `USER mcpuser`.
      - `USER mcpuser`.
      - `EXPOSE 8190` (thay 8080).
      - `HEALTHCHECK` — **probe route `/.well-known/oauth-authorization-server`. Đây là route TẤT ĐỊNH:** Plan 01 cho `oauth_issuer_url` một default (`http://localhost:8190`) nên `Settings()` KHÔNG raise khi thiếu env, và SDK `mcp` mount well-known route ngay khi `auth_server_provider` non-None → route trả 200 từ lúc app start, KỂ CẢ trước khi operator set domain thật. Dùng:
        ```dockerfile
        HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=3 \
            CMD curl -fsS http://localhost:8190/.well-known/oauth-authorization-server || exit 1
        ```
        KHÔNG để executor "xác nhận route khi test" — route này đã được chứng minh tất định bởi default issuer của Plan 01 + cơ chế mount của SDK (RESEARCH.md Pattern 1). `start-period=20s` cho app + lifespan `init_schema()` (Plan 02) kịp khởi động.
      - `CMD ["uvicorn", "mcp_app.server:build_asgi_app", "--factory", "--host", "0.0.0.0", "--port", "8190"]` — `build_asgi_app` là factory → BẮT BUỘC cờ `--factory`.

    **`mcp_service/.dockerignore`** (NEW) — loại trừ khỏi build context: `.venv`, `__pycache__`, `.pytest_cache`, `.ruff_cache`, `tests`, `.env`, `*.db`, `.oauth`, `.git`. (Mẫu: nếu `api/.dockerignore` tồn tại thì copy, không thì viết theo danh sách trên.)
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run python -c "import mcp_app.server" && docker build -t medinet-mcp:test . && docker run --rm medinet-mcp:test python -c "import mcp_app.server; print('image ok')"</automated>
  </verify>
  <acceptance_criteria>
    - **Fast pre-check:** `cd Hub_All/mcp_service && uv run python -c "import mcp_app.server"` thành công (exit 0) — bắt regression import/syntax TRƯỚC khi build image multi-phút. Chạy bước này TRƯỚC `docker build`; nếu fail → sửa code, KHÔNG tốn thời gian build.
    - File `Hub_All/mcp_service/Dockerfile` tồn tại.
    - `grep "mcp_app.server:build_asgi_app" Hub_All/mcp_service/Dockerfile` tồn tại.
    - `grep -- "--factory" Hub_All/mcp_service/Dockerfile` tồn tại.
    - `grep "EXPOSE 8190" Hub_All/mcp_service/Dockerfile` tồn tại.
    - `grep "/app/.oauth" Hub_All/mcp_service/Dockerfile` tồn tại (thư mục SQLite state).
    - `grep "well-known/oauth-authorization-server" Hub_All/mcp_service/Dockerfile` tồn tại (HEALTHCHECK route tất định).
    - `grep -c "alembic" Hub_All/mcp_service/Dockerfile` trả về 0 (không migration).
    - `docker build -t medinet-mcp:test Hub_All/mcp_service` thành công (exit 0).
  </acceptance_criteria>
  <done>Dockerfile multi-stage uv build được image MCP Service, non-root mcpuser, factory CMD port 8190, HEALTHCHECK probe well-known route tất định; fast pre-check import chạy trước build.</done>
</task>

<task type="auto">
  <name>Task 2: Caddyfile + service mcp_service + caddy trong docker-compose.yml</name>
  <files>Hub_All/Caddyfile, Hub_All/docker-compose.yml</files>
  <read_first>
    - Hub_All/docker-compose.yml (bản hiện tại — 3 service + volumes + networks)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§docker-compose.yml, §Caddyfile)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§Pitfall 6/7 — issuer URL + proxy không strip path)
  </read_first>
  <action>
    **`Hub_All/Caddyfile`** (NEW) — reverse proxy auto-TLS. Dùng biến môi trường cho domain để đổi domain không sửa file:
    ```
    # MCP Service public HTTPS — Phase 8.3.
    # Domain truyền qua biến MCP_PUBLIC_DOMAIN (đọc từ .env của compose).
    # Caddy tự xin + gia hạn cert Let's Encrypt (auto-TLS) — cần port 80+443 mở.
    {$MCP_PUBLIC_DOMAIN} {
        # Forward NGUYÊN host + path tới MCP Service — KHÔNG strip prefix.
        # Route /.well-known/... phải ở root (Pitfall 7); SDK mcp mount sẵn ở root.
        reverse_proxy medinet-mcp:8190
    }
    ```
    Nếu cần dev local không cert: thêm comment hướng dẫn dùng `MCP_PUBLIC_DOMAIN=:80` hoặc `localhost` để Caddy cấp cert nội bộ.

    **`docker-compose.yml`** (MODIFY) — GIỮ nguyên 3 service hiện có. Thêm:
    1. Service `mcp_service` (copy cấu trúc `python-api`):
       ```yaml
       mcp_service:
         build:
           context: ./mcp_service
           dockerfile: Dockerfile
         container_name: medinet-mcp
         env_file:
           - ./mcp_service/.env
         environment:
           # Gọi API Service qua docker network — KHÔNG localhost.
           MCP_API_BASE_URL: http://python-api:8080
           # Issuer = domain public HTTPS. Đổi proxy chỉ cần đổi biến này.
           MCP_OAUTH_ISSUER_URL: ${MCP_OAUTH_ISSUER_URL:-http://localhost:8190}
           # SQLite OAuth state trong volume — đường dẫn tuyệt đối.
           MCP_OAUTH_STATE_DB_PATH: /app/.oauth/state.db
         volumes:
           - medinet_mcp_oauth_state:/app/.oauth
         depends_on:
           python-api:
             condition: service_healthy
         networks: [medinet_net]
       ```
       LƯU Ý: KHÔNG map port host ra ngoài cho `mcp_service` — chỉ Caddy expose public. Service nói HTTP nội bộ docker network.
    2. Service `caddy`:
       ```yaml
       caddy:
         image: caddy:2-alpine
         container_name: medinet-caddy
         restart: unless-stopped
         environment:
           MCP_PUBLIC_DOMAIN: ${MCP_PUBLIC_DOMAIN:-localhost}
         ports:
           - "80:80"
           - "443:443"
         volumes:
           - ./Caddyfile:/etc/caddy/Caddyfile:ro
           - medinet_caddy_data:/data
           - medinet_caddy_config:/config
         depends_on:
           - mcp_service
         networks: [medinet_net]
       ```
    3. Thêm vào `volumes:` — `medinet_mcp_oauth_state:`, `medinet_caddy_data:`, `medinet_caddy_config:`.
    Đảm bảo `MCP_OAUTH_ISSUER_URL` và `MCP_PUBLIC_DOMAIN` nhất quán (issuer = `https://${MCP_PUBLIC_DOMAIN}` trong prod) — thêm comment giải thích operator phải set 2 biến này trong `.env` gốc compose.
  </action>
  <verify>
    <automated>cd Hub_All && docker compose config --quiet && echo "compose valid"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/Caddyfile` tồn tại; `grep "reverse_proxy medinet-mcp:8190" Hub_All/Caddyfile` tồn tại.
    - `grep "mcp_service:" Hub_All/docker-compose.yml` tồn tại.
    - `grep "caddy:" Hub_All/docker-compose.yml` tồn tại.
    - `grep "MCP_API_BASE_URL: http://python-api:8080" Hub_All/docker-compose.yml` tồn tại (gọi qua docker network).
    - `grep "medinet_mcp_oauth_state" Hub_All/docker-compose.yml` xuất hiện ở cả service mount lẫn block `volumes:`.
    - `grep -A2 "mcp_service:" Hub_All/docker-compose.yml | grep -c "ports:"` — KHÔNG có map port host cho mcp_service (chỉ caddy expose 80/443).
    - `cd Hub_All && docker compose config --quiet` — exit 0 (compose hợp lệ).
  </acceptance_criteria>
  <done>Caddyfile + service mcp_service/caddy trong compose, OAuth state trên named volume, mcp_service gọi API qua docker network, compose validate OK.</done>
</task>

<task type="auto">
  <name>Task 3: Cập nhật README + viết 08.3-HUMAN-UAT.md (SC4 human_needed)</name>
  <files>Hub_All/mcp_service/README.md, Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md</files>
  <read_first>
    - Hub_All/mcp_service/README.md (bản hiện tại — kiến trúc + cách chạy standalone)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Open Questions" — domain, SC4; §"Environment Availability" — Cloudflare Tunnel fallback)
    - Hub_All/.planning/phases/08.2-mcp-service-standalone-process/08.2-HUMAN-UAT.md (mẫu format UAT thủ công của phase trước)
  </read_first>
  <action>
    **`mcp_service/README.md`** (MODIFY) — GIỮ nội dung cũ. Thêm section "Phase 8.3 — OAuth 2.0 + Deploy public HTTPS" tiếng Việt:
    - Kiến trúc đích: `Claude web ─(OAuth)─► MCP Service (public HTTPS) ─(JWT nội bộ)─► API Service`.
    - Cách chạy có OAuth: `docker compose up` (toàn stack) HOẶC native `uvicorn mcp_app.server:build_asgi_app --factory --port 8190`.
    - Config OAuth mới: `MCP_OAUTH_ISSUER_URL`, `MCP_OAUTH_STATE_DB_PATH`, `MCP_OAUTH_ACCESS_TOKEN_TTL`, `MCP_OAUTH_REFRESH_TOKEN_TTL`.
    - 2 cách xác thực: OAuth 2.0 (Claude web connector) + X-API-Key (Claude Code/Desktop local — vẫn dùng được song song).
    - Reverse proxy: Caddy auto-TLS (mặc định); fallback Cloudflare Tunnel — chỉ cần đổi `MCP_OAUTH_ISSUER_URL` + thay service `caddy`, KHÔNG sửa code `mcp_app`.
    - Cảnh báo: issuer URL prod PHẢI là `https://` domain thật (Pitfall 6).

    **`08.3-HUMAN-UAT.md`** (NEW) — quy trình UAT thủ công, format theo `08.2-HUMAN-UAT.md`. Tiếng Việt. Nội dung:
    - **Tiền điều kiện (operator chuẩn bị):**
      1. Domain công khai trỏ DNS A record tới host (vd `mcp.medinet.vn`).
      2. Port 80 + 443 mở ra Internet (cho Caddy ACME) — HOẶC dùng Cloudflare Tunnel nếu không mở được port.
      3. Set `.env` gốc compose: `MCP_PUBLIC_DOMAIN=mcp.medinet.vn`, `MCP_OAUTH_ISSUER_URL=https://mcp.medinet.vn`.
      4. API Service (`python-api`) đang chạy + có ít nhất 1 tài khoản Medinet (email/password) còn active để login.
      5. `docker compose up -d` — chờ `medinet-mcp` + `medinet-caddy` healthy.
    - **SC4 — kết nối thật từ Claude web (`human_needed` — KHÔNG auto-verify được):**
      - Bước 1: `curl -fsS https://<domain>/.well-known/oauth-authorization-server` → JSON có `issuer`, `authorization_endpoint`, `token_endpoint`, `registration_endpoint`. KỲ VỌNG: 200 + JSON hợp lệ.
      - Bước 2: Vào Claude web → Settings → Connectors → "Add custom connector" → nhập URL `https://<domain>/mcp` (hoặc URL MCP endpoint — xác nhận path khi test).
      - Bước 3: Claude web mở luồng OAuth → hiển thị form login Medinet → nhập email + password tài khoản Medinet → submit.
      - Bước 4: KỲ VỌNG: Claude web báo connector kết nối thành công; 3 tool `list_hubs` / `search_wiki` / `ask_wiki` xuất hiện.
      - Bước 5: Trong Claude chat, yêu cầu gọi `list_hubs` → KỲ VỌNG: trả danh sách hub đúng theo role tài khoản (admin → mọi hub; user thường → hub được gán — D-04).
      - Bước 6: Gọi `search_wiki` + `ask_wiki` → KỲ VỌNG: trả kết quả từ Medinet Wiki.
      - Bước 7 (regression JWT refresh): để connector kết nối > 15 phút rồi gọi lại tool → KỲ VỌNG: vẫn chạy (downstream JWT đã tự refresh — Pitfall 4).
    - **Ô đánh dấu kết quả:** PASS / FAIL + ghi chú; chỗ ghi domain thật đã dùng.
    - **Lưu ý:** SC1/SC2/SC3/SC5 đã auto-verify qua test suite (Plan 01-03). SC4 đây là phần duy nhất cần con người.
  </action>
  <verify>
    <automated>test -f Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md && grep -q "SC4" Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md && echo "uat doc ok"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md` tồn tại.
    - `grep -E "SC4|human_needed|Add custom connector" Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md` — có mặt.
    - `grep "well-known/oauth-authorization-server" Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md` tồn tại (bước probe).
    - `grep "Cloudflare Tunnel" Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-HUMAN-UAT.md` tồn tại (fallback proxy).
    - `grep "Phase 8.3" Hub_All/mcp_service/README.md` tồn tại (section mới).
    - `grep -E "MCP_OAUTH_ISSUER_URL" Hub_All/mcp_service/README.md` tồn tại.
  </acceptance_criteria>
  <done>README cập nhật section OAuth + deploy; 08.3-HUMAN-UAT.md có quy trình SC4 đầy đủ với tiền điều kiện domain + fallback Cloudflare Tunnel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Caddy (port 80/443) | Public traffic crosses here — TLS termination, untrusted |
| Caddy → mcp_service:8190 | Internal docker network — HTTP, trusted segment |
| mcp_service container → SQLite volume `/app/.oauth` | OAuth state persistence — file chứa secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-16 | Tampering/Disclosure | MITM trên kênh public | mitigate | Caddy auto-TLS Let's Encrypt — HTTPS bắt buộc cho mọi traffic public. HTTP chỉ tồn tại trong docker network nội bộ (Caddy → mcp_service). `MCP_OAUTH_ISSUER_URL` ép `https` ở prod (validator Plan 01). |
| T-08.3-17 | Tampering | Reverse proxy strip path `/.well-known/...` → discovery 404 | mitigate | `Caddyfile` dùng `reverse_proxy medinet-mcp:8190` forward NGUYÊN host + path — KHÔNG `handle_path`/`uri strip` (Pitfall 7). UAT bước 1 `curl` well-known route xác nhận. |
| T-08.3-18 | Elevation of Privilege | Container chạy root | mitigate | Dockerfile tạo non-root `mcpuser`, `USER mcpuser` trước CMD. Thư mục `/app/.oauth` chown `mcpuser` trước khi hạ quyền. |
| T-08.3-19 | Information Disclosure | OAuth state file (downstream JWT) lộ qua volume | accept | SQLite file nằm trên named volume `medinet_mcp_oauth_state` — bảo vệ ở tầng Docker volume + host filesystem permissions. Không mã hoá at-rest ở phase này (low-value khi host trusted; T-08.3-03 Plan 01). |
| T-08.3-20 | DoS | mcp_service map port host ra Internet trực tiếp (bypass TLS) | mitigate | `docker-compose.yml` KHÔNG map port host cho `mcp_service` — chỉ Caddy expose 80/443. Mọi truy cập public buộc đi qua TLS termination. Acceptance criteria kiểm không có `ports:` cho mcp_service. |
</threat_model>

<verification>
- `cd Hub_All/mcp_service && uv run python -c "import mcp_app.server"` — fast pre-check import (chạy TRƯỚC docker build).
- `cd Hub_All/mcp_service && docker build -t medinet-mcp:test .` — build thành công.
- `cd Hub_All && docker compose config --quiet` — compose hợp lệ.
- `docker run --rm medinet-mcp:test python -c "import mcp_app.server"` — image import được package.
- Đọc lại `Caddyfile` — KHÔNG có `handle_path` / `uri strip_prefix` (forward nguyên path).
- `08.3-HUMAN-UAT.md` tồn tại với quy trình SC4 đầy đủ.
</verification>

<success_criteria>
- `mcp_service/Dockerfile` build được image multi-stage uv, non-root, factory CMD port 8190, HEALTHCHECK probe well-known route tất định.
- Fast native pre-check `import mcp_app.server` chạy trước `docker build` — bắt regression syntax không cần build multi-phút.
- `docker-compose.yml` có service `mcp_service` + `caddy`; OAuth state trên named volume; mcp_service gọi API qua docker network.
- `Caddyfile` reverse proxy auto-TLS forward nguyên host (well-known route không bị strip).
- Đổi reverse proxy chỉ cần đổi `MCP_OAUTH_ISSUER_URL` + service compose — KHÔNG sửa `mcp_app`.
- `08.3-HUMAN-UAT.md` có quy trình SC4 thủ công đầy đủ + tiền điều kiện domain/DNS + fallback Cloudflare Tunnel.
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-04-SUMMARY.md`
</output>
</content>
</invoke>
