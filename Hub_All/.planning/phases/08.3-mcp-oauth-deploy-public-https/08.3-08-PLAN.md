---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 08
type: execute
wave: 2
depends_on: [07]
gap_closure: true
files_modified:
  - Hub_All/mcp_service/mcp_app/oauth/store.py
  - Hub_All/mcp_service/mcp_app/oauth/provider.py
  - Hub_All/mcp_service/mcp_app/server.py
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "oauth_tokens có cột family_id TEXT — gán lúc exchange_authorization_code, giữ nguyên qua mỗi rotate_token"
    - "rotate_token rowcount=0 (reuse detected) → provider gọi delete_token_family(family_id) → mọi token cùng chain (cùng family_id) bị xoá khỏi store (RFC 6749 §10.4)"
    - "Token chain đã rotate trước đó (kẻ tấn công giữ refresh mới) cũng mất hiệu lực sau khi nạn nhân reuse refresh cũ → both sides bị logged out (đúng spec)"
    - "Token cũ trước migration (family_id IS NULL) coi như family riêng (mỗi token 1 family) — back-compat, không xoá nhầm"
    - "_call_api refresh JWT downstream race: 2 tool song song cùng OAuth token chỉ có 1 lần refresh thực sự (asyncio.Lock per access_token), tool còn lại đọc record mới từ store"
    - "Lock pool cleanup wire qua late import — revoke_token gọi server._release_refresh_lock(access_token) sau khi delete_token → không leak ~136 byte / user concurrent active"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/oauth/store.py"
      provides: "schema oauth_tokens thêm family_id NULL; save_token nhận family_id; rotate_token PRESERVE family_id; method delete_token_family; load_token / load_token_by_refresh trả family_id"
      contains: "family_id"
    - path: "Hub_All/mcp_service/mcp_app/oauth/provider.py"
      provides: "exchange_authorization_code sinh family_id mới + truyền save_token; exchange_refresh_token rowcount=0 → load record cũ → delete_token_family; revoke_token gọi _release_refresh_lock(access_token) qua late import sau khi delete"
      contains: "delete_token_family"
    - path: "Hub_All/mcp_service/mcp_app/server.py"
      provides: "_call_api dùng asyncio.Lock per OAuth access_token cho khối refresh_jwt + update_downstream_jwt — chặn race; lock pool toàn cục với cleanup hook _release_refresh_lock được wire từ provider.revoke_token"
      contains: "_refresh_locks"
  key_links:
    - from: "provider.exchange_refresh_token"
      to: "store.delete_token_family"
      via: "rowcount=0 → load record để lấy family_id → xoá family"
      pattern: "delete_token_family"
    - from: "provider.exchange_authorization_code"
      to: "store.save_token (family_id mới)"
      via: "family_id = self._new_opaque() lúc phát token đầu tiên"
      pattern: "family_id"
    - from: "_call_api"
      to: "asyncio.Lock per access_token"
      via: "_refresh_locks[oauth_token]"
      pattern: "_refresh_locks"
    - from: "provider.revoke_token"
      to: "server._release_refresh_lock"
      via: "late import sau delete_token"
      pattern: "_release_refresh_lock"
---

<objective>
Vá HIGH-02 (refresh token family revocation, RFC 6749 §10.4) + HIGH-03 (refresh
JWT downstream concurrency race) từ `08.3-MCP-AUDIT-2026-05-21.md`. Đây là 2
High còn lại nhóm "vòng đời token + đồng thời" sau khi Plan 07 đã đóng client_id
mismatch + CORS + CSRF + DCR redirect.

HIGH-02 lý do quan trọng: bản vá CR-02 (Plan 05) chỉ chặn double-spend CÙNG
refresh token, nhưng phiên bị đánh cắp (chain đã rotate trước) vẫn sống. RFC
6749 §10.4 yêu cầu thu hồi cả token family. Cần cột `family_id` trên
`oauth_tokens`, gán cố định lúc phát token đầu tiên (`exchange_authorization_code`),
giữ nguyên qua mỗi `rotate_token` — khi reuse phát hiện → `delete_token_family`.

HIGH-03 lý do quan trọng: 2 tool call song song dùng cùng OAuth token + downstream
JWT vừa hết hạn → tool 1 refresh JWT (lưu rotation), tool 2 song song refresh
với refresh_token đã rotate ở tool 1 → API trả 401 → tool 2 fail "MCP_UNAUTHORIZED"
không deterministic. Cần `asyncio.Lock` per access_token đảm bảo refresh JWT
single-flight; tool 2 chờ tool 1 xong, đọc lại record từ store.

HIGH-03 cleanup follow-up: lock pool memory leak khi user revoke token. Wire
cleanup qua late import (~5 dòng) trong `provider.revoke_token` — pattern này
đã được dùng nhiều chỗ (vd `server.py:439 import asyncio` trong
`_close_client_atexit`). Không defer Phase 10 (per checker feedback Blocker #4).

Purpose: Đóng 2 High vòng đời token + concurrency + cleanup follow-up. Sau plan
này, tổng các fix Plan 07 + 08 = 3 Critical (trừ CRIT-01 defer) + 6 High đã vá
ở code. Plan 09 sẽ thêm test phủ.

Output: store.py có family_id + delete_token_family; provider.py exchange_auth
sinh family_id + exchange_refresh gọi delete_token_family khi reuse +
revoke_token wire _release_refresh_lock cleanup; server.py _call_api có lock
per access_token + helper _release_refresh_lock cleanup pool entry.

PHỤ THUỘC Plan 07 (wave 1) — Plan 08 đụng provider.py + store.py (cùng file
Plan 07), bắt buộc Wave 2 sau Plan 07 commit. Regression test Plan 06 hiện có
(89/89) PHẢI vẫn xanh sau Plan 08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-07-PLAN.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-05-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Chữ ký method hiện có sau Plan 05+06+07 — executor dùng trực tiếp. -->

oauth/store.py — schema oauth_tokens hiện tại (sau Plan 05 + 07 — sẽ thêm family_id):
```python
CREATE TABLE IF NOT EXISTS oauth_tokens (
    access_token TEXT PRIMARY KEY,
    refresh_token TEXT,
    client_id TEXT NOT NULL,
    scopes TEXT NOT NULL,
    downstream_jwt TEXT NOT NULL,
    downstream_refresh_token TEXT NOT NULL,
    user_payload TEXT NOT NULL,
    expires_at INTEGER NOT NULL
    -- HIGH-02 Plan 08 sẽ thêm: family_id TEXT (NULLABLE — back-compat DB cũ)
)
CREATE UNIQUE INDEX IF NOT EXISTS idx_oauth_tokens_refresh ON oauth_tokens(refresh_token)
```

oauth/store.py — chữ ký method liên quan:
```python
async def save_token(self, access_token, refresh_token, client_id, scopes,
                     downstream_jwt, downstream_refresh_token, user_payload,
                     expires_at) -> None
async def rotate_token(self, old_refresh_token, new_access_token,
                       new_refresh_token, expires_at) -> bool
async def load_token(self, access_token) -> dict | None
async def load_token_by_refresh(self, refresh_token) -> dict | None
async def delete_token(self, access_token) -> None
_TOKEN_COLUMNS = "access_token, refresh_token, client_id, scopes, downstream_jwt, downstream_refresh_token, user_payload, expires_at"
@staticmethod _row_to_token(row: tuple) -> dict   # map 8 cột → dict 8 khoá
```

oauth/provider.py — chữ ký method liên quan (SAU Plan 07 commit):
```python
async def exchange_authorization_code(self, client, authorization_code) -> OAuthToken
    # Plan 07: claim_auth_code + record["client_id"] != client.client_id check + expires_at; save_token
async def exchange_refresh_token(self, client, refresh_token, scopes) -> OAuthToken
    # Plan 07: load_token_by_refresh + record["client_id"] != client.client_id check + rotate_token
async def revoke_token(self, token) -> None
    # Plan 07: record["client_id"] != token.client_id check + delete_token + silent no-op
@staticmethod _new_opaque() -> str
```

server.py — _call_api (server.py:193-243):
```python
async def _call_api(cred, method, path, *, params=None, json_body=None) -> Any
    # kind=jwt: thử với JWT hiện tại, 401 → refresh → retry 1 lần
    # KHỐI CRITICAL HIGH-03 (dòng 220-243):
    #   except ApiUnauthorizedError:
    #       record = await store.load_token(cred.oauth_token)
    #       new_pair = await client.refresh_jwt(record["downstream_refresh_token"])
    #       await store.update_downstream_jwt(...)
    #       return await _do(new_pair["access_token"])
```
asyncio.Lock pattern cho HIGH-03 cần đặt ở module-level server.py.

server.py late-import pattern hiện có (chứng minh không circular):
```python
# server.py:439 — _close_client_atexit
def _close_client_atexit() -> None:
    global _api_client, _oauth_store
    import asyncio   # ← late import standard library trong function body
    ...
```
provider.py KHÔNG import server.py top-level → wire `_release_refresh_lock`
qua late import `from mcp_app.server import _release_refresh_lock` trong
revoke_token body — KHÔNG tạo circular import.

aiosqlite — `cursor.rowcount` đáng tin sau Plan 05 UNIQUE INDEX trên refresh_token.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: store.py — schema family_id + save_token + rotate_token preserve + load_token expose + delete_token_family (HIGH-02)</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/store.py (file đang sửa — _SCHEMA_STATEMENTS oauth_tokens dòng 55-66, save_token 252-282, rotate_token 355-376, _row_to_token 284-296, _TOKEN_COLUMNS 298-301, init_schema 99-126 sau migration csrf Plan 07)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (HIGH-02 section II)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-07-PLAN.md (Task 2 init_schema migration pattern — dùng PRAGMA table_info cho cột mới)
  </read_first>
  <action>
Sửa `Hub_All/mcp_service/mcp_app/oauth/store.py` — 5 thay đổi:

(1) Schema oauth_tokens: thêm cột `family_id TEXT` (NULLABLE — back-compat DB
cũ; token cũ NULL coi như family riêng). Sửa `_SCHEMA_STATEMENTS` oauth_tokens
CREATE TABLE (dòng 55-66) thêm cột TRƯỚC `expires_at`:

```python
    """
    CREATE TABLE IF NOT EXISTS oauth_tokens (
        access_token TEXT PRIMARY KEY,
        refresh_token TEXT,
        client_id TEXT NOT NULL,
        scopes TEXT NOT NULL,
        downstream_jwt TEXT NOT NULL,
        downstream_refresh_token TEXT NOT NULL,
        user_payload TEXT NOT NULL,
        family_id TEXT,
        expires_at INTEGER NOT NULL
    )
    """,
```

(2) Migration back-compat: sửa `init_schema` (sau Plan 07 đã có khối PRAGMA
table_info cho `oauth_pending.csrf_token`). Thêm khối tương tự cho
`oauth_tokens.family_id`. SAU khối migration csrf của Plan 07:

```python
        # HIGH-02 Plan 08 migration — DB cũ chưa có family_id; coi như NULL
        # (mỗi token 1 family riêng).
        async with self._conn.execute(
            "PRAGMA table_info(oauth_tokens)"
        ) as cursor:
            cols_tokens = {row[1] for row in await cursor.fetchall()}
        if "family_id" not in cols_tokens:
            await self._conn.execute(
                "ALTER TABLE oauth_tokens ADD COLUMN family_id TEXT"
            )
        await self._conn.commit()
```

(3) Sửa `save_token` (store.py:252-282): thêm tham số `family_id: str | None = None`
TRƯỚC `expires_at` (default `None` để back-compat test cũ truyền thiếu). Sửa
INSERT có thêm cột `family_id`:

```python
    async def save_token(
        self,
        access_token: str,
        refresh_token: str | None,
        client_id: str,
        scopes: list,
        downstream_jwt: str,
        downstream_refresh_token: str,
        user_payload: dict,
        family_id: str | None = None,
        expires_at: int = 0,
    ) -> None:
        """Lưu OAuth token kèm downstream JWT + refresh + user payload + family_id.

        HIGH-02 (audit 2026-05-21): family_id gán lúc exchange_authorization_code
        (token đầu tiên trong chain), giữ nguyên qua mỗi rotate. Khi reuse phát
        hiện → delete_token_family xoá cả chain (RFC 6749 §10.4).

        Default family_id=None — back-compat test cũ; provider mới truyền non-None.
        """
        conn = self._require_conn()
        await conn.execute(
            "INSERT OR REPLACE INTO oauth_tokens "
            "(access_token, refresh_token, client_id, scopes, downstream_jwt, "
            "downstream_refresh_token, user_payload, family_id, expires_at) "
            "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)",
            (
                access_token,
                refresh_token,
                client_id,
                json.dumps(scopes),
                downstream_jwt,
                downstream_refresh_token,
                json.dumps(user_payload),
                family_id,
                expires_at,
            ),
        )
        await conn.commit()
        logger.info("OAuthStore lưu OAuth token cho client_id=%s", client_id)
```

LƯU Ý: `expires_at` có default 0 chỉ để cho phép default sau dấu phẩy của
`family_id` — caller production LUÔN truyền expires_at non-zero (kwarg). Nếu
prefer giữ expires_at không default, ĐẶT `family_id` SAU `expires_at`. Cách
trên giữ tương thích column order trong INSERT (8 cột giữ thứ tự gốc, family_id
chèn trước expires_at để khớp logic schema).

(4) Sửa `_TOKEN_COLUMNS` + `_row_to_token` — thêm `family_id` vào danh sách
cột select + map. Sửa hằng số + staticmethod:

```python
    @staticmethod
    def _row_to_token(row: tuple) -> dict:
        """Map row oauth_tokens → dict."""
        return {
            "access_token": row[0],
            "refresh_token": row[1],
            "client_id": row[2],
            "scopes": json.loads(row[3]),
            "downstream_jwt": row[4],
            "downstream_refresh_token": row[5],
            "user_payload": json.loads(row[6]),
            "family_id": row[7],
            "expires_at": row[8],
        }

    _TOKEN_COLUMNS = (
        "access_token, refresh_token, client_id, scopes, downstream_jwt, "
        "downstream_refresh_token, user_payload, family_id, expires_at"
    )
```

(5) Sửa `rotate_token` (store.py:355-376) — PRESERVE family_id (KHÔNG đụng cột
family_id). UPDATE hiện chỉ set 3 cột (access_token, refresh_token, expires_at)
— family_id KHÔNG bị đụng → tự động preserve. KHÔNG đổi hành vi UPDATE; CẬP
NHẬT docstring giải thích family_id preserve:

```python
    async def rotate_token(
        self,
        old_refresh_token: str,
        new_access_token: str,
        new_refresh_token: str,
        expires_at: int,
    ) -> bool:
        """Rotate token OAuth định vị theo refresh_token (giá trị caller cầm).

        Trả True nếu UPDATE khớp đúng 1 dòng; False nếu refresh token đã bị dùng/
        rotate (rowcount != 1) — caller PHẢI coi đây là refresh-token reuse và từ
        chối (CR-02, RFC 6749 §10.4). Khác update_oauth_token: định vị theo
        refresh_token + kiểm rowcount thay vì WHERE access_token đã hết hạn.

        HIGH-02 Plan 08: KHÔNG đụng cột family_id — preserve nguyên giá trị
        gán lúc exchange_authorization_code. Provider exchange_refresh_token
        khi rowcount=0 sẽ gọi delete_token_family để thu hồi cả chain.
        """
        # ... body GIỮ NGUYÊN — UPDATE 3 cột, family_id preserve tự động.
```

(6) THÊM method `delete_token_family` SAU `delete_token`:

```python
    async def delete_token_family(self, family_id: str) -> int:
        """Xoá tất cả OAuth token thuộc cùng family (chain rotate cũ + mới).

        HIGH-02 (audit 2026-05-21, RFC 6749 §10.4): refresh token reuse phát
        hiện → AS PHẢI thu hồi cả family vì không phân biệt được kẻ tấn công
        và client thật. Trả số dòng xoá (chỉ dùng để log).

        Lưu ý: family_id NULL (DB cũ) → KHÔNG xoá (mỗi token NULL coi như
        family riêng — back-compat). family_id chuỗi rỗng cũng treat như NULL.
        """
        if not family_id:
            return 0
        conn = self._require_conn()
        cursor = await conn.execute(
            "DELETE FROM oauth_tokens WHERE family_id = ?", (family_id,)
        )
        await conn.commit()
        return cursor.rowcount
```

KHÔNG sửa method khác. Mọi query parametrized.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/store.py</automated>
  </verify>
  <acceptance_criteria>
    - `store.py` `_SCHEMA_STATEMENTS` chứa chuỗi `family_id TEXT` trong CREATE TABLE oauth_tokens
    - `store.py` `init_schema` chứa chuỗi `PRAGMA table_info(oauth_tokens)` và `ALTER TABLE oauth_tokens ADD COLUMN family_id TEXT`
    - `store.py` `save_token` signature có tham số `family_id: str | None = None`
    - `store.py` `save_token` INSERT chứa cột `family_id` và 9 placeholder
    - `store.py` `_TOKEN_COLUMNS` chứa chuỗi `family_id`
    - `store.py` `_row_to_token` trả dict có key `family_id`
    - `store.py` chứa `async def delete_token_family(self, family_id: str) -> int:`
    - `store.py` `delete_token_family` chứa `DELETE FROM oauth_tokens WHERE family_id = ?`
    - `store.py` `delete_token_family` chứa guard `if not family_id: return 0`
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/store.py` exits 0
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_store.py -q` exits 0 (regression — default family_id=None back-compat)
  </acceptance_criteria>
  <done>store.py có family_id NULLABLE + migration ALTER TABLE; save_token nhận family_id default None (back-compat); rotate_token preserve family_id (KHÔNG đụng); delete_token_family xoá theo family_id (NULL/rỗng → no-op); _row_to_token + _TOKEN_COLUMNS update; ruff sạch; regression store PASS.</done>
</task>

<task type="auto">
  <name>Task 2: provider.py — sinh family_id + delete_token_family khi reuse (HIGH-02)</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (file đang sửa — POST-Plan 07 commit; verify exchange_authorization_code đã có client_id mismatch guard; exchange_refresh_token đã có guard sau load_token_by_refresh; save_token call format hiện tại không drift)
    - Hub_All/mcp_service/mcp_app/oauth/store.py (chữ ký save_token + delete_token_family sau Task 1)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (HIGH-02 code mẫu fix `delete_token_family`)
  </read_first>
  <action>
**Warning #5 (read_first drift check):** TRƯỚC khi patch, đọc `provider.py` SAU
khi Plan 07 đã commit. Verify cả 2 vùng patch:

(a) `exchange_authorization_code` save_token call format hiện tại (post-Plan 07
KHÔNG đụng vùng save_token, chỉ thêm `record["client_id"] != client.client_id`
guard ở đầu). Format save_token kwarg list dự kiến:
```python
await self._store.save_token(
    access_token=access_token,
    refresh_token=refresh_token,
    client_id=record["client_id"],
    scopes=scopes,
    downstream_jwt=record["downstream_jwt"],
    downstream_refresh_token=record["downstream_refresh_token"],
    user_payload=record["user_payload"],
    expires_at=self._now() + self._access_token_ttl,
)
```
Nếu format khác (vd Plan 07 thêm kwarg) → adapt insertion point `family_id=family_id`.

(b) `exchange_refresh_token` đoạn `if not ok:` hiện tại (post-Plan 07 chỉ thêm
guard sau load_token_by_refresh, KHÔNG đụng nhánh sau rotate_token).

Sửa `Hub_All/mcp_service/mcp_app/oauth/provider.py` — 2 thay đổi:

(1) Sửa `exchange_authorization_code` — sinh `family_id` mới + truyền vào
`save_token`. Sau Plan 07, đoạn save_token trông như block ở (a) trên.

Sửa thành:

```python
        access_token = self._new_opaque()
        refresh_token = self._new_opaque()
        family_id = self._new_opaque()  # HIGH-02: token đầu chain — family_id mới
        scopes = list(authorization_code.scopes)
        await self._store.save_token(
            access_token=access_token,
            refresh_token=refresh_token,
            client_id=record["client_id"],
            scopes=scopes,
            downstream_jwt=record["downstream_jwt"],
            downstream_refresh_token=record["downstream_refresh_token"],
            user_payload=record["user_payload"],
            family_id=family_id,
            expires_at=self._now() + self._access_token_ttl,
        )
```

(2) Sửa `exchange_refresh_token` — khi `rotate_token` trả False (reuse detected),
gọi `delete_token_family` để xoá cả chain.

**Warning #6 — BEFORE/AFTER block tường minh cho executor copy-thay-thế (chống drift):**

BEFORE (sau Plan 07 commit — vùng từ Plan 07 guard đến raise reuse):

```python
        if record["client_id"] != client.client_id:  # Plan 07 HIGH-08
            logger.warning(
                "exchange_refresh_token client_id mismatch — "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                client.client_id,
            )
            raise OAuthStoreError(
                "Refresh token không thuộc client này"
            )

        new_access = self._new_opaque()
        new_refresh = self._new_opaque()
        ok = await self._store.rotate_token(
            old_refresh_token=refresh_token.token,
            new_access_token=new_access,
            new_refresh_token=new_refresh,
            expires_at=self._now() + self._access_token_ttl,
        )
        if not ok:
            logger.info(
                "MedinetOAuthProvider refresh token reuse — KHÔNG rotate"
            )
            raise OAuthStoreError(
                "Refresh token đã dùng — vui lòng kết nối lại"
            )
        result_scopes = scopes or record["scopes"]
```

AFTER (Plan 08 patch — guard Plan 07 GIỮ NGUYÊN, chỉ thay nhánh `if not ok:`):

```python
        if record["client_id"] != client.client_id:  # Plan 07 — GIỮ NGUYÊN
            logger.warning(
                "exchange_refresh_token client_id mismatch — "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                client.client_id,
            )
            raise OAuthStoreError(
                "Refresh token không thuộc client này"
            )

        new_access = self._new_opaque()
        new_refresh = self._new_opaque()
        ok = await self._store.rotate_token(
            old_refresh_token=refresh_token.token,
            new_access_token=new_access,
            new_refresh_token=new_refresh,
            expires_at=self._now() + self._access_token_ttl,
        )
        if not ok:
            # rowcount != 1 — refresh token đã bị dùng/rotate (reuse detected).
            # HIGH-02 (audit 2026-05-21, RFC 6749 §10.4): KHÔNG phân biệt được
            # kẻ tấn công và client thật → AS PHẢI thu hồi cả family. record
            # đã load ở đầu hàm (load_token_by_refresh) — nếu record có
            # family_id non-NULL thì xoá cả family; family_id NULL (token cũ
            # trước migration HIGH-02) → delete_token_family no-op.
            family_id = record.get("family_id") or ""           # NEW
            if family_id:                                        # NEW
                deleted = await self._store.delete_token_family(family_id)  # NEW
                logger.warning(                                  # NEW
                    "exchange_refresh_token reuse — xoá token family "
                    "client_id=%s family_size=%d",
                    record["client_id"],
                    deleted,
                )
            else:                                                # NEW
                logger.info(                                     # NEW
                    "MedinetOAuthProvider refresh token reuse — token cũ "
                    "không có family_id, KHÔNG thu hồi family (back-compat)"
                )
            raise OAuthStoreError(
                "Refresh token đã dùng — vui lòng kết nối lại"
            )
        result_scopes = scopes or record["scopes"]
```

Executor copy nguyên block "AFTER" thay block "BEFORE". Guard Plan 07 + new_access
+ rotate_token call GIỮ NGUYÊN; CHỈ nhánh `if not ok:` thêm 7 dòng mới
(comment `# NEW` để executor dễ chỉ ra phần thay đổi — KHÔNG cần giữ comment
trong code final).

(Đoạn `record = await self._store.load_token_by_refresh(refresh_token.token)`
ở đầu hàm sau Plan 07 — record đã có `family_id` vì `_row_to_token` Task 1
include — KHÔNG cần re-load.)

KHÔNG đụng method khác. KHÔNG log family_id giá trị thô (chỉ log
`family_size=deleted_rowcount` và `client_id`).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/provider.py</automated>
  </verify>
  <acceptance_criteria>
    - `provider.py` `exchange_authorization_code` chứa chuỗi `family_id = self._new_opaque()`
    - `provider.py` `save_token` call chứa kwarg `family_id=family_id`
    - `provider.py` `exchange_refresh_token` đoạn `if not ok:` chứa chuỗi `delete_token_family(family_id)` và `record.get("family_id")`
    - `provider.py` log warning chứa chuỗi `xoá token family` (HIGH-02 visibility)
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth/provider.py` exits 0
    - `cd Hub_All/mcp_service && uv run python -c "import mcp_app.oauth.provider"` exits 0
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py -q` exits 0 (regression — test cũ Plan 06 vẫn PASS vì family_id propagate tự động; test reuse cũ sẽ thấy family bị xoá luôn — chấp nhận được vì hành vi đúng đắn hơn)
  </acceptance_criteria>
  <done>provider.py exchange_authorization_code sinh family_id mới truyền save_token; exchange_refresh_token khi reuse load family_id từ record gọi delete_token_family + log warning số token bị xoá; ruff sạch; regression test PASS hoặc cập nhật kỳ vọng test cũ.</done>
</task>

<task type="auto">
  <name>Task 3: server.py — asyncio.Lock per access_token cho refresh JWT downstream (HIGH-03) + wire cleanup vào provider.revoke_token (late import)</name>
  <read_first>
    - Hub_All/mcp_service/mcp_app/server.py (file đang sửa — _call_api dòng 193-243, đặc biệt khối except ApiUnauthorizedError 222-243; pattern late import `_close_client_atexit:439 import asyncio`)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (HIGH-03 section II)
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (POST-Plan 07: revoke_token đã có client_id check + delete_token — Task 3 thêm wire _release_refresh_lock qua late import sau delete)
  </read_first>
  <action>
Sửa `Hub_All/mcp_service/mcp_app/server.py` + `Hub_All/mcp_service/mcp_app/oauth/provider.py` — 4 thay đổi:

(1) Thêm `import asyncio` ở đầu file server.py (kiểm — có thể đã có; nếu chưa, thêm sau
`import logging`).

(2) Thêm lock pool module-level + helper trong server.py, ĐẶT NGAY TRÊN
`async def _call_api`:

```python
# HIGH-03 (audit 2026-05-21): asyncio.Lock per OAuth access_token cho khối
# refresh JWT downstream. 2 tool song song cùng OAuth token + downstream JWT
# vừa hết hạn — không lock thì tool 1 refresh OK, tool 2 refresh với token
# đã rotate → API 401 → tool 2 fail không deterministic. Lock đảm bảo
# single-flight refresh; tool sau đọc lại record (đã update từ tool trước).
#
# Memory profile: 1 Lock ~ 56 bytes (Python 3.12 CPython), key string ~80
# byte → 1 token entry ~ 136 byte. 1000 user concurrent ~ 136 KB — chấp
# nhận được. Cleanup wire từ provider.revoke_token qua late import.
_refresh_locks: dict[str, asyncio.Lock] = {}


def _get_refresh_lock(oauth_token: str) -> asyncio.Lock:
    """Trả asyncio.Lock cho oauth_token — lazy create. Single-flight refresh JWT."""
    lock = _refresh_locks.get(oauth_token)
    if lock is None:
        lock = asyncio.Lock()
        _refresh_locks[oauth_token] = lock
    return lock


def _release_refresh_lock(oauth_token: str) -> None:
    """Xoá lock entry khỏi pool — gọi từ provider.revoke_token (qua late import)
    để tránh memory leak khi user revoke token.

    Best-effort: lock đang held → caller chờ ở `async with` ngoài → pop chỉ xoá
    reference trong dict; lock object tự GC khi không còn ref ngoài. KHÔNG raise
    nếu key không tồn tại (pop default).

    HIGH-03 cleanup: wire qua provider.revoke_token với pattern late import
    `from mcp_app.server import _release_refresh_lock` (xem
    `_close_client_atexit:439 import asyncio` cho pattern tương tự).
    """
    _refresh_locks.pop(oauth_token, None)
```

(3) Sửa `_call_api` trong server.py — bọc khối refresh JWT trong
`async with lock`. Đoạn `except ApiUnauthorizedError:` từ dòng 222 đổi:

```python
    try:
        return await _do(cred.value)
    except ApiUnauthorizedError:
        logger.info("Downstream JWT 401 — thử refresh JWT rồi retry")
        store = _get_oauth_store()
        oauth_token = cred.oauth_token or ""

        # HIGH-03: single-flight refresh per access_token. Tool 1 vào lock,
        # refresh + lưu; tool 2 chờ lock, đọc lại record (đã update) → thấy
        # downstream JWT mới → KHÔNG refresh lại lần 2.
        async with _get_refresh_lock(oauth_token):
            record = await store.load_token(oauth_token)
            if record is None:
                raise

            # Re-check: nếu tool trước đã refresh, downstream_jwt khác cred.value
            # → retry với JWT mới ngay (skip refresh).
            if record["downstream_jwt"] != cred.value:
                logger.info(
                    "Downstream JWT đã được tool song song refresh — retry "
                    "với JWT mới"
                )
                return await _do(record["downstream_jwt"])

            new_pair = await client.refresh_jwt(record["downstream_refresh_token"])
            if new_pair is None:
                # Refresh token cũng hết hạn → user phải re-connect.
                raise
            if not all(new_pair.get(k) for k in ("access_token", "refresh_token")):
                logger.error("refresh_jwt trả payload thiếu access/refresh token")
                raise ApiUnauthorizedError(
                    "Phản hồi refresh JWT thiếu trường bắt buộc"
                ) from None
            # Pitfall 5 — lưu đè CẢ downstream JWT + refresh token mới.
            await store.update_downstream_jwt(
                oauth_token,
                new_pair["access_token"],
                new_pair["refresh_token"],
            )
            return await _do(new_pair["access_token"])
```

Logic chính của fix:
- `async with lock` đảm bảo tối đa 1 task chạy khối refresh tại 1 thời điểm
  với cùng `oauth_token`.
- Sau khi vào lock, `load_token` lại để đọc record fresh. Nếu
  `record["downstream_jwt"]` đã KHÁC `cred.value` (tool trước đã refresh xong)
  → skip refresh, retry với JWT mới luôn.
- Nếu vẫn cùng JWT cũ → tool đầu tiên vào lock thật → refresh + update.

(4) **Blocker #4 — Wire cleanup vào `provider.revoke_token`** (POST-Plan 07).
Sửa `Hub_All/mcp_service/mcp_app/oauth/provider.py` `revoke_token` — thêm
late import + call `_release_refresh_lock(access_token)` SAU `delete_token`.

Pattern late import (KHÔNG circular vì provider.py KHÔNG import server.py
top-level — verify trước khi sửa):

```bash
cd Hub_All && grep -n "from mcp_app.server" mcp_service/mcp_app/oauth/provider.py
# Expected: 0 matches (no top-level import of server)
```

BEFORE (sau Plan 07 commit revoke_token body):

```python
        if record["client_id"] != token.client_id:
            logger.warning(
                "revoke_token client_id mismatch — silent no-op "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                token.client_id,
            )
            return
        await self._store.delete_token(record["access_token"])
        logger.info(
            "MedinetOAuthProvider revoke token, client_id=%s", token.client_id
        )
```

AFTER (Plan 08 Task 3 — thêm 4 dòng cleanup wire qua late import sau delete):

```python
        if record["client_id"] != token.client_id:
            logger.warning(
                "revoke_token client_id mismatch — silent no-op "
                "record_client_id=%s requesting_client_id=%s",
                record["client_id"],
                token.client_id,
            )
            return
        access_token_revoked = record["access_token"]            # NEW
        await self._store.delete_token(access_token_revoked)
        # HIGH-03 cleanup (audit 2026-05-21): xoá lock entry để tránh leak.
        # Late import chống circular — provider.py không import server.py
        # top-level. Pattern tương tự server.py:439 `_close_client_atexit`.
        from mcp_app.server import _release_refresh_lock          # NEW
        _release_refresh_lock(access_token_revoked)               # NEW
        logger.info(
            "MedinetOAuthProvider revoke token, client_id=%s", token.client_id
        )
```

(5) Lint + regression:

```
cd Hub_All/mcp_service && uv run ruff check mcp_app/server.py mcp_app/oauth/provider.py
cd Hub_All/mcp_service && uv run pytest -q
```

Test cũ trong test_server.py / test_integration_tools.py KHÔNG mock _call_api
khối refresh (chỉ test happy path / 401 không refresh). Sau fix, behavior happy
path KHÔNG đổi (cred.value vẫn dùng); behavior 401 cũ KHÔNG đổi (refresh
single-flight cùng kết quả với refresh trực tiếp khi không có concurrent).

Test cũ revoke_token (test_oauth_provider.py) — sau wire cleanup, revoke vẫn
xoá token đúng; thêm side-effect là _refresh_locks.pop — no-op nếu chưa có
entry → KHÔNG vỡ test cũ.

Test mới HIGH-03 concurrency sẽ ở Plan 09. KHÔNG thêm test trong plan này.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `server.py` chứa chuỗi `_refresh_locks: dict[str, asyncio.Lock] = {}`
    - `server.py` chứa chuỗi `def _get_refresh_lock(oauth_token: str) -> asyncio.Lock:`
    - `server.py` chứa chuỗi `def _release_refresh_lock(oauth_token: str) -> None:`
    - `server.py` chứa chuỗi `async with _get_refresh_lock(oauth_token):`
    - `server.py` `_call_api` chứa re-check `record["downstream_jwt"] != cred.value`
    - `server.py` import `asyncio` ở đầu file
    - `provider.py` `revoke_token` chứa chuỗi `from mcp_app.server import _release_refresh_lock` (late import — Blocker #4 wire)
    - `provider.py` `revoke_token` chứa chuỗi `_release_refresh_lock(access_token_revoked)` (cleanup call)
    - `cd Hub_All && grep -n "from mcp_app.server" mcp_service/mcp_app/oauth/provider.py | grep -v "def\|async def"` chỉ trả late import bên trong revoke_token (KHÔNG có top-level)
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/server.py mcp_app/oauth/provider.py` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q` exits 0 (regression — happy path + 401 single-tool + revoke đều PASS)
    - `cd Hub_All/mcp_service && uv run pytest -q -m critical` exits 0
  </acceptance_criteria>
  <done>server.py có lock pool module-level + helper _get/_release; _call_api khối refresh JWT bọc async with lock + re-check downstream_jwt mới; provider.revoke_token wire _release_refresh_lock qua late import (Blocker #4 closed — HIGH-03 cleanup follow-up đóng đủ); ruff sạch; regression 89/89 PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|----------|-------|
| Kẻ tấn công đoạt 1 refresh token chain → giữ chain mới sau khi rotate; nạn nhân reuse refresh cũ phát hiện | Phiên đánh cắp vẫn sống nếu không thu hồi family (HIGH-02) |
| 2+ tool song song cùng OAuth token + downstream JWT vừa hết hạn | Race refresh — tool 1 rotate, tool 2 dùng refresh cũ → 401 ngẫu nhiên (HIGH-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-39 | Elevation | refresh token chain attack — kẻ tấn công giữ refresh mới sau khi nạn nhân reuse phát hiện | mitigate | Task 1+2: family_id NULLABLE gán lúc exchange_auth_code, preserve qua rotate; reuse → delete_token_family xoá cả chain → kẻ tấn công + nạn nhân đều mất phiên (RFC 6749 §10.4). |
| T-08.3-40 | Denial of Service | 2 tool song song refresh JWT downstream — tool 2 nhận 401 không deterministic | mitigate | Task 3: asyncio.Lock per access_token + re-check `record["downstream_jwt"]` trong critical section → tool 2 thấy JWT mới, KHÔNG refresh lần 2 → cả 2 tool succeed. HIGH-03. |
| T-08.3-41 | Information Disclosure | family_id NULL (token cũ trước migration) coi như family riêng — KHÔNG bị xoá nhầm | accept | Back-compat: token cũ NULL → delete_token_family no-op. Audit ghi rõ giới hạn — chấp nhận được vì user re-connect tự nhiên rotate token sang chain mới (có family_id non-NULL). |
| T-08.3-42 | Repudiation | Log family revocation | accept | Chỉ log `client_id` + `family_size=N` — KHÔNG log family_id giá trị thô (T-08.3-08). Đủ để debug. |
| T-08.3-43 | Information Disclosure | _refresh_locks memory leak khi user revoke token | mitigate | Task 3: wire `_release_refresh_lock(access_token)` vào `provider.revoke_token` qua late import — pop entry sau delete. Pattern tương tự `_close_client_atexit` late import asyncio. Không leak. |
</threat_model>

<verification>
- `store.py` `_SCHEMA_STATEMENTS` chứa cột `family_id TEXT` trong oauth_tokens.
- `store.py` `init_schema` có migration ALTER TABLE oauth_tokens family_id (PRAGMA check).
- `store.py` `save_token` có tham số `family_id: str | None = None`; INSERT 9 cột.
- `store.py` chứa method `delete_token_family` với guard `if not family_id: return 0`.
- `store.py` `_TOKEN_COLUMNS` + `_row_to_token` include `family_id`.
- `provider.py` `exchange_authorization_code` chứa `family_id = self._new_opaque()` + truyền `save_token(..., family_id=family_id, ...)`.
- `provider.py` `exchange_refresh_token` đoạn reuse chứa `delete_token_family` + `record.get("family_id")`.
- `provider.py` `revoke_token` chứa late import `from mcp_app.server import _release_refresh_lock` + call `_release_refresh_lock(access_token_revoked)`.
- `server.py` chứa `_refresh_locks` dict + `_get_refresh_lock` helper + `_release_refresh_lock` helper + `async with _get_refresh_lock` trong `_call_api`.
- `cd Hub_All/mcp_service && uv run ruff check mcp_app/` exits 0.
- `cd Hub_All/mcp_service && uv run pytest -q` exits 0 — regression 89/89.
</verification>

<success_criteria>
- HIGH-02: refresh token reuse phát hiện → xoá cả token family qua `family_id` (RFC 6749 §10.4). Token cũ NULL family_id back-compat — không xoá nhầm.
- HIGH-03: 2 tool song song refresh JWT downstream — chỉ 1 lần `client.refresh_jwt` thực sự chạy (asyncio.Lock per access_token + re-check downstream_jwt).
- HIGH-03 cleanup follow-up: lock pool cleanup wire vào `provider.revoke_token` qua late import — không leak khi user revoke. Blocker #4 closed.
- Regression mcp_service pytest 89/89 PASS; test critical PASS.
- Plan 09 sẽ thêm test phủ HIGH-02 family revocation + HIGH-03 concurrency.

LƯU Ý WAL mode (MED-09, Warning #10): defer Phase 10 — KHÔNG bắt buộc trong
Plan 08. SUMMARY plan 08 acknowledge defer (theo audit Section IX defer list).
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-08-SUMMARY.md`.
Commit atomic 3 task riêng:
- `feat(08.3-08): thêm family_id + delete_token_family vào OAuthStore (HIGH-02 schema)`
- `fix(08.3-08): exchange refresh reuse → xoá cả token family (HIGH-02 RFC 6749 §10.4)`
- `fix(08.3-08): asyncio.Lock per access_token cho refresh JWT downstream + wire cleanup (HIGH-03)`
</output>
</content>
</invoke>