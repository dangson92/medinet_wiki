---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 09
type: execute
wave: 3
depends_on: [07, 08]
gap_closure: true
files_modified:
  - Hub_All/mcp_service/tests/test_oauth_provider.py
  - Hub_All/mcp_service/tests/test_oauth_store.py
  - Hub_All/mcp_service/tests/test_path_prefix_wrapper.py
  - Hub_All/mcp_service/tests/test_server.py
  - Hub_All/mcp_service/tests/test_oauth_login.py
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "HIGH-04: GET /.well-known/openid-configuration ở path-prefix mode trả 200 + body identical với /.well-known/oauth-authorization-server (regression test cho commit a04b928)"
    - "HIGH-05: list_hubs tool xử lý envelope `data: [list]` đúng (raw_results lấy từ list trực tiếp); KHÔNG còn dùng được nếu API trả {items: [...]} legacy bị refactor làm hỏng (regression test cho commit 3dbb378)"
    - "HIGH-06: DNS rebinding allowed_hosts whitelist công nhận host issuer + reject host khác — verify bằng introspect THẬT `_build_mcp` transport security config + integration test attacker host (regression test cho commit bd6c02c)"
    - "CRIT-03 + HIGH-08: exchange_authorization_code raise OAuthStoreError khi save_auth_code(client_id=A) → exchange với client_id=B (cross-client code exchange chặn)"
    - "CRIT-03 sub-fix 2: load_authorization_code trả None khi client_id mismatch — chặn nhánh load → exchange của chain attack"
    - "HIGH-01: register_client raise OAuthStoreError với redirect_uri scheme=ftp, host=attacker.com, http+non-loopback; PASS với https://claude.ai/cb, http://localhost:6274/cb, http://127.0.0.1:6274/cb"
    - "HIGH-07: revoke_token silent no-op (KHÔNG xoá) khi record.client_id != token.client_id"
    - "HIGH-09: login POST /login/callback với form csrf_token thiếu/sai → render lại form với lỗi (assert_not_called login); với csrf_token khớp → flow tiếp — test dùng pattern Starlette TestClient + respx khớp test_oauth_login.py hiện có"
    - "CRIT-02: GET /mcp/.well-known/oauth-authorization-server response header Access-Control-Allow-Origin xuất hiện ĐÚNG 1 lần (không duplicate)"
    - "HIGH-02 store: delete_token_family xoá tất cả token cùng family_id; NULL/rỗng → no-op"
    - "HIGH-02 provider: exchange_refresh_token reuse → cả chain (family) bị xoá; load_token(access cũ trong family) trả None"
    - "HIGH-03: 2 task asyncio.gather song song _call_api gặp 401 → client.refresh_jwt được gọi ĐÚNG 1 lần (assert refresh_mock.call_count == 1 nghiêm ngặt); get_calls in (3, 4)"
    - "Regression mcp_service: tổng test sau Plan 09 >= 104 PASS (89 cũ + ~15 mới); test critical PASS"
  artifacts:
    - path: "Hub_All/mcp_service/tests/test_path_prefix_wrapper.py"
      provides: "test_openid_configuration_alias_returns_200; test_cors_no_duplicate_in_path_prefix_mode; test_dns_rebinding_rejects_unknown_host (introspect _build_mcp transport security thật); test_dns_rebinding_accepts_issuer_host"
      contains: "openid-configuration"
    - path: "Hub_All/mcp_service/tests/test_server.py"
      provides: "test_list_hubs_envelope_data_list_branch (HIGH-05); test_refresh_jwt_concurrency_single_flight (HIGH-03 — assertion refresh_mock.call_count == 1 + get_calls in (3,4))"
      contains: "test_list_hubs_envelope"
    - path: "Hub_All/mcp_service/tests/test_oauth_provider.py"
      provides: "test_exchange_code_rejects_client_id_mismatch; test_load_authorization_code_returns_none_on_client_mismatch; test_register_client_rejects_attacker_redirect_uri; test_revoke_token_silent_noop_on_client_mismatch; test_exchange_refresh_token_reuse_deletes_family; test_register_client_accepts_default_redirect_uris"
      contains: "client_id_mismatch"
    - path: "Hub_All/mcp_service/tests/test_oauth_store.py"
      provides: "test_delete_token_family_removes_all; test_delete_token_family_noop_on_null"
      contains: "delete_token_family"
    - path: "Hub_All/mcp_service/tests/test_oauth_login.py"
      provides: "test_login_callback_rejects_missing_csrf; test_login_callback_rejects_wrong_csrf; test_login_form_contains_hidden_csrf_token — dùng Starlette TestClient + respx mock pattern hiện có"
      contains: "csrf_token"
  key_links:
    - from: "test_path_prefix_wrapper.test_cors_no_duplicate_in_path_prefix_mode"
      to: "server.py path-prefix CORS fix"
      via: "response header ACAO len(getlist) == 1"
      pattern: "access-control-allow-origin"
    - from: "test_oauth_provider.test_exchange_code_rejects_client_id_mismatch"
      to: "provider.py exchange_authorization_code client_id check"
      via: "save_auth_code(A) + exchange(B) → raise"
      pattern: "client_id_mismatch"
    - from: "test_server.test_refresh_jwt_concurrency_single_flight"
      to: "server.py _refresh_locks single-flight"
      via: "asyncio.gather 2 task + assert refresh_jwt call_count == 1"
      pattern: "asyncio.gather"
    - from: "test_path_prefix_wrapper.test_dns_rebinding_*"
      to: "server.py _build_mcp transport security allowed_hosts"
      via: "introspect FastMCP transport security THẬT (KHÔNG tautology)"
      pattern: "TransportSecuritySettings"
---

<objective>
Thêm test phủ TẤT CẢ fix Plan 07 + Plan 08 + 3 commit MISSING từ
`08.3-MCP-AUDIT-2026-05-21.md` Section VII (HIGH-04/05/06). Đây là Wave 3 cuối
chuỗi gap closure 08.3-21 — chứng minh bằng test rằng các vá lỗi Plan 07/08
thật sự chặn attack vector và regression cho 3 hot-fix commit chưa có test.

Purpose: Audit ghi rõ 27% commit hot-fix không có regression test → rủi ro fix
quay đầu mà không bị bắt. Plan 09 đóng test gap đó + biến SC3 phase 8.3 từ
"đã vá Plan 07/08 nhưng test phủ chưa đầy đủ" → "có bằng chứng test rằng các
attack vector audit identify đều bị chặn".

Output: 5 file test có thêm ~15 test phủ:
- 3 test cho 3 commit MISSING (HIGH-04/05/06).
- 6 test cho Plan 07 (CRIT-03 mismatch x2, HIGH-01 redirect_uri x2, HIGH-07 revoke, CRIT-02 CORS no-duplicate, HIGH-09 CSRF x3).
- 4 test cho Plan 08 (HIGH-02 store + provider x2, HIGH-03 concurrency single-flight).

PHỤ THUỘC Plan 07 + Plan 08 (wave 1+2) — test gọi behavior đã có trong code
sau 07+08. KHÔNG chạy plan này trước. Test KHÔNG sửa code production.

KHÔNG thêm dependency mới (pytest-asyncio đã có, monkeypatch standard, unittest.mock
standard, respx có sẵn từ Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-07-PLAN.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-08-PLAN.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-06-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Chữ ký/helper hiện có executor tái dùng — KHÔNG cần khám phá. -->

tests/conftest.py — fixture/helper hiện có (sau Plan 07 đã thêm csrf_token default):
```python
@pytest.fixture async def oauth_store     # OAuthStore :memory:, schema sẵn (gồm csrf_token + family_id sau Plan 07+08)
async def fake_pending_authorize(store, *, txn="txn-test", client_id="client-test",
    redirect_uri="https://claude.ai/api/mcp/auth_callback", code_challenge="challenge-test",
    code_challenge_method="S256", csrf_token="csrf-test-fixed",
    client_state="client-state-test", scopes=None, created_at=None) -> str
@pytest.fixture mock_ctx() -> Context        # MCP Context giả với header dict (test_integration_tools)
@pytest.fixture(autouse=True) reset_api_client() -> None
@pytest.fixture api_base_url() -> str
```

tests/test_oauth_provider.py — helper hiện có (tái dùng):
```python
def _make_provider(store, api_client=None) -> MedinetOAuthProvider
def _client_info(client_id="client-test") -> OAuthClientInformationFull
    # Sau Plan 07 — redirect_uris default phải nằm trong whitelist (claude.ai default OK).
def _login_data() -> dict
ISSUER = "http://localhost:8190"
REDIRECT = "https://claude.ai/api/mcp/auth_callback"
```

**tests/test_oauth_login.py CURRENT pattern (CONFIRMED bằng đọc trực tiếp file
2026-05-21):**
```python
# Pattern: Starlette TestClient + respx + fixture oauth_env (override env vars).
# KHÔNG có helper raw `_make_provider_for_login` / `_make_get_request` / `_make_post_request`.
# Test mới HIGH-09 PHẢI dùng cùng pattern này:

from starlette.testclient import TestClient
import httpx, respx, asyncio
from .conftest import fake_pending_authorize

@respx.mock
def test_login_callback_rejects_missing_csrf(oauth_env) -> None:
    from mcp_app.server import _get_oauth_store, build_asgi_app
    # KHÔNG respx mock /api/auth/login — test phải fail TRƯỚC khi callback gọi login
    # (CSRF verify đi trước login call). Nếu CSRF fail, KHÔNG có HTTP request →
    # respx KHÔNG cần mock.
    app = build_asgi_app()
    with TestClient(app) as client:
        store = _get_oauth_store()
        txn = asyncio.new_event_loop().run_until_complete(
            fake_pending_authorize(store, txn="txn-no-csrf", csrf_token="real")
        )
        resp = client.post("/login/callback", data={
            "txn": txn,
            # KHÔNG có csrf_token field
            "email": "u@m.vn",
            "password": "p",
        })
    assert resp.status_code == 200  # render lại form
    assert "không hợp lệ" in resp.text
```

tests/test_path_prefix_wrapper.py — pattern test ASGI scope (xem test_cors_actual_get_metadata dòng 339-392):
```python
# monkeypatch.setenv("MCP_PATH_PREFIX", "mcp")
# monkeypatch.setenv("MCP_OAUTH_ISSUER_URL", "https://wiki.example.com/mcp")
# get_settings.cache_clear()
# app = server.build_asgi_app()
# scope = {"type": "http", "method": "GET", "path": ..., "headers": [...], ...}
# sent: list[dict] = []
# async def _send(msg): sent.append(msg)
# await app(scope, _receive, _send)
# starts = [m for m in sent if m.get("type") == "http.response.start"]
# headers = {k.decode().lower(): v.decode() for k, v in starts[0].get("headers", [])}
```
Cho assert ACAO duplicate: dùng `starts[0]["headers"]` (list tuple) lọc đếm.

**server.py _build_mcp transport security CONFIRMED bằng grep
`allowed_hosts` (Hub_All/mcp_service/mcp_app/server.py:509-531):**
```python
issuer_host = urlparse(settings.oauth_issuer_url).netloc
allowed_hosts = ["127.0.0.1:*", "localhost:*", "[::1]:*"]
if issuer_host and issuer_host not in allowed_hosts:
    allowed_hosts.append(issuer_host)
transport_security = TransportSecuritySettings(
    enable_dns_rebinding_protection=True,
    allowed_hosts=allowed_hosts,
    allowed_origins=[],
)
# mcp = FastMCP(..., transport_security=transport_security, ...)
```
Để introspect THẬT: build_mcp() → mcp._transport_security (hoặc tương đương SDK
1.27 internal attr). Nếu SDK không expose → integration test scope dict với
Host header attacker.example → assert response 421/400.

server.py constant/import (cho HIGH-03 test):
```python
_refresh_locks: dict[str, asyncio.Lock] = {}
def _get_refresh_lock(oauth_token: str) -> asyncio.Lock
def _release_refresh_lock(oauth_token: str) -> None
```

oauth/store.py method (sau Plan 08):
```python
async def save_token(..., family_id: str | None = None, expires_at: int) -> None
async def delete_token_family(self, family_id: str) -> int   # rowcount; NULL/rỗng → 0
async def load_token(access_token) -> dict | None   # dict có khoá family_id
```

oauth/provider.py method (sau Plan 07+08):
```python
async def register_client(client_info) -> None
    # raise OAuthStoreError nếu redirect_uri scheme/host invalid
async def load_authorization_code(client, code) -> AuthorizationCode | None
    # trả None nếu record.client_id != client.client_id
async def exchange_authorization_code(client, authorization_code) -> OAuthToken
    # raise OAuthStoreError nếu record.client_id != client.client_id
async def revoke_token(token: AccessToken | RefreshToken) -> None
    # silent no-op nếu record.client_id != token.client_id; wire _release_refresh_lock
async def exchange_refresh_token(client, refresh_token, scopes) -> OAuthToken
    # reuse → delete_token_family + raise

ApiClient (api_client.py) — refresh_jwt method:
async def refresh_jwt(refresh_token: str) -> dict | None
    # trả {access_token, refresh_token, ...} hoặc None khi API trả 401
```

**Credential dataclass CONFIRMED bằng đọc Hub_All/mcp_service/mcp_app/auth.py (Warning #9):**
```python
@dataclass(frozen=True)
class Credential:
    kind: Literal["jwt", "api_key"]
    value: str
    oauth_token: str | None = None
```
3 field exact: `kind`, `value`, `oauth_token` (default None). Test dùng
`Credential(kind="api_key", value="test-key", oauth_token=None)` hoặc
`Credential(kind="jwt", value="JWT-OLD", oauth_token="oauth-T")` — đều khớp.

unittest.mock.AsyncMock cho mock refresh_jwt (HIGH-03 single-flight test):
```python
from unittest.mock import AsyncMock, patch
mock = AsyncMock(return_value={"access_token": "new-jwt", "refresh_token": "new-r"})
with patch.object(client, "refresh_jwt", mock):
    await asyncio.gather(task1(), task2())
assert mock.call_count == 1   # single-flight
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_oauth_provider.py + test_oauth_store.py — test CRIT-03/HIGH-01/07/08 + HIGH-02 family revocation</name>
  <read_first>
    - Hub_All/mcp_service/tests/test_oauth_provider.py (helper _make_provider/_client_info/_login_data, pattern async def test_ trần, import OAuthStoreError + AuthorizationCode + AnyUrl)
    - Hub_All/mcp_service/tests/test_oauth_store.py (pattern test_ với fixture oauth_store)
    - Hub_All/mcp_service/tests/conftest.py (fake_pending_authorize signature có csrf_token sau Plan 07)
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (sau Plan 07+08 — verify code mẫu test gọi đúng API)
    - Hub_All/mcp_service/mcp_app/oauth/store.py (delete_token_family signature)
  </read_first>
  <behavior>
    - test_exchange_code_rejects_client_id_mismatch: save_auth_code với client_id="alice"; gọi exchange_authorization_code với client=_client_info(client_id="bob") → raise OAuthStoreError "Authorization code không thuộc client này".
    - test_load_authorization_code_returns_none_on_client_mismatch: save_auth_code với client_id="alice"; gọi load_authorization_code với client=_client_info(client_id="bob") → trả None.
    - test_register_client_rejects_attacker_redirect_uri: client_info với redirect_uris=[AnyUrl("https://attacker.com/cb")] → register_client raise OAuthStoreError "không nằm trong whitelist".
    - test_register_client_rejects_http_non_loopback: redirect_uris=[AnyUrl("http://example.com/cb")] → raise OAuthStoreError (http chỉ cho loopback).
    - test_register_client_rejects_invalid_scheme: redirect_uris=[AnyUrl("ftp://localhost/cb")] → raise OAuthStoreError "scheme 'ftp' không hợp lệ".
    - test_register_client_accepts_default_redirect_uris: 3 URI từ `_DEFAULT_REDIRECT_URIS` (claude.ai + localhost:6274 + 127.0.0.1:6274) → register_client KHÔNG raise.
    - test_revoke_token_silent_noop_on_client_mismatch: save_token cho client_id="alice", access_token="acc-A"; revoke_token với AccessToken(token="acc-A", client_id="bob") → load_token("acc-A") VẪN trả record (không bị xoá), không raise.
    - test_exchange_refresh_token_reuse_deletes_family: flow complete_authorization → exchange code → exchange_refresh_token lần 1 (rotate OK, family preserve); gọi exchange_refresh_token với refresh_obj CŨ lần 2 → raise OAuthStoreError; assert mọi token cùng family bị xoá (load_token cho access_token mới ở rotate lần 1 trả None).
    - test_delete_token_family_removes_all (store): save_token 3 token cùng family_id="fam-1" + 1 token family_id="fam-2"; delete_token_family("fam-1") → trả 3; load_token cho 3 token đầu trả None; token thứ 4 vẫn còn.
    - test_delete_token_family_noop_on_null (store): delete_token_family("") → trả 0; delete_token_family với family_id không tồn tại → trả 0.
  </behavior>
  <action>
Thêm test mới vào CUỐI 2 file `tests/test_oauth_provider.py` + `tests/test_oauth_store.py` (style async def trần, fixture oauth_store; KHÔNG đụng test cũ).

(A) `tests/test_oauth_provider.py` — 8 test mới:

```python
async def test_exchange_code_rejects_client_id_mismatch(oauth_store) -> None:
    """Cross-client code exchange — code phát cho client A, client B exchange → raise (CRIT-03)."""
    provider = _make_provider(oauth_store)
    # Setup: pending của client alice → exchange code phát cho alice.
    txn = await fake_pending_authorize(
        oauth_store, txn="txn-mismatch", client_id="alice"
    )
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    # Dựng AuthorizationCode TRỰC TIẾP (bỏ qua load_authorization_code — chứng
    # minh guard nằm trong exchange_authorization_code, không chỉ ở load).
    auth_code = AuthorizationCode(
        code=code,
        scopes=["wiki"],
        expires_at=float(int(time.time()) + 600),
        client_id="alice",
        code_challenge="challenge-test",
        redirect_uri=AnyUrl(REDIRECT),
        redirect_uri_provided_explicitly=True,
    )
    # Exchange với client bob → raise.
    with pytest.raises(OAuthStoreError, match="không thuộc client này"):
        await provider.exchange_authorization_code(
            _client_info(client_id="bob"), auth_code
        )


async def test_load_authorization_code_returns_none_on_client_mismatch(
    oauth_store,
) -> None:
    """load_authorization_code trả None khi client_id mismatch (CRIT-03 sub-fix 2)."""
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(
        oauth_store, txn="txn-load-mismatch", client_id="alice"
    )
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    # Load với client bob → None (không leak thông tin code đúng).
    result = await provider.load_authorization_code(
        _client_info(client_id="bob"), code
    )
    assert result is None
    # Load với client alice (đúng) → trả AuthorizationCode.
    result_alice = await provider.load_authorization_code(
        _client_info(client_id="alice"), code
    )
    assert result_alice is not None
    assert result_alice.client_id == "alice"


async def test_register_client_rejects_attacker_redirect_uri(oauth_store) -> None:
    """register_client reject redirect_uri host không trong whitelist (HIGH-01)."""
    from mcp.shared.auth import OAuthClientInformationFull
    provider = _make_provider(oauth_store)
    client_info = OAuthClientInformationFull(
        client_id="attacker-client",
        client_secret="x",
        redirect_uris=[AnyUrl("https://attacker.com/cb")],
        grant_types=["authorization_code"],
        response_types=["code"],
    )
    with pytest.raises(OAuthStoreError, match="không nằm trong whitelist"):
        await provider.register_client(client_info)


async def test_register_client_rejects_http_non_loopback(oauth_store) -> None:
    """register_client reject http non-loopback (HIGH-01 scheme guard)."""
    from mcp.shared.auth import OAuthClientInformationFull
    provider = _make_provider(oauth_store)
    client_info = OAuthClientInformationFull(
        client_id="bad-scheme",
        client_secret="x",
        redirect_uris=[AnyUrl("http://example.com/cb")],
        grant_types=["authorization_code"],
        response_types=["code"],
    )
    with pytest.raises(OAuthStoreError, match="loopback"):
        await provider.register_client(client_info)


async def test_register_client_accepts_default_redirect_uris(oauth_store) -> None:
    """register_client PASS với 3 default URI khớp api/mcp_oauth_service (HIGH-01 whitelist)."""
    from mcp.shared.auth import OAuthClientInformationFull
    provider = _make_provider(oauth_store)
    client_info = OAuthClientInformationFull(
        client_id="good-client",
        client_secret="x",
        redirect_uris=[
            AnyUrl("https://claude.ai/api/mcp/auth_callback"),
            AnyUrl("http://localhost:6274/oauth/callback"),
            AnyUrl("http://127.0.0.1:6274/oauth/callback"),
        ],
        grant_types=["authorization_code"],
        response_types=["code"],
    )
    # KHÔNG raise.
    await provider.register_client(client_info)


async def test_revoke_token_silent_noop_on_client_mismatch(oauth_store) -> None:
    """revoke_token silent no-op khi record.client_id != token.client_id (HIGH-07)."""
    from mcp.server.auth.provider import AccessToken
    provider = _make_provider(oauth_store)
    await oauth_store.save_token(
        access_token="acc-alice",
        refresh_token="ref-alice",
        client_id="alice",
        scopes=["wiki"],
        downstream_jwt="jwt-a",
        downstream_refresh_token="jwt-r",
        user_payload={"id": 1},
        expires_at=int(time.time()) + 3600,
    )
    # Bob revoke token của Alice — silent no-op.
    bob_token = AccessToken(
        token="acc-alice",  # token của alice
        client_id="bob",     # NHƯNG bob xưng là client
        scopes=["wiki"],
        expires_at=int(time.time()) + 3600,
    )
    await provider.revoke_token(bob_token)  # KHÔNG raise.
    # Token alice VẪN còn trong store.
    assert await oauth_store.load_token("acc-alice") is not None


async def test_exchange_refresh_token_reuse_deletes_family(oauth_store) -> None:
    """Reuse refresh token → delete_token_family xoá cả chain (HIGH-02)."""
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(oauth_store, txn="txn-family")
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    auth_code = await provider.load_authorization_code(_client_info(), code)
    first_pair = await provider.exchange_authorization_code(_client_info(), auth_code)
    # Sau exchange — token đầu chain có family_id non-NULL.
    first_record = await oauth_store.load_token(first_pair.access_token)
    assert first_record is not None and first_record["family_id"]
    family_id = first_record["family_id"]

    # Rotate lần 1 — OK. Sau rotate access cũ bị thay; token MỚI có cùng family_id.
    refresh_obj = await provider.load_refresh_token(
        _client_info(), first_pair.refresh_token
    )
    second_pair = await provider.exchange_refresh_token(
        _client_info(), refresh_obj, ["wiki"]
    )
    second_record = await oauth_store.load_token(second_pair.access_token)
    assert second_record is not None and second_record["family_id"] == family_id

    # Rotate lần 2 với refresh_obj CŨ — reuse, raise; family bị xoá.
    with pytest.raises(OAuthStoreError):
        await provider.exchange_refresh_token(
            _client_info(), refresh_obj, ["wiki"]
        )
    # Cả family (chain) bị xoá — token chain mới (second_pair) cũng KHÔNG còn.
    assert await oauth_store.load_token(second_pair.access_token) is None
```

(B) `tests/test_oauth_store.py` — 2 test mới:

```python
async def test_delete_token_family_removes_all(oauth_store) -> None:
    """delete_token_family xoá tất cả token cùng family_id (HIGH-02 RFC 6749 §10.4)."""
    for i, fam in enumerate(["fam-1", "fam-1", "fam-1", "fam-2"]):
        await oauth_store.save_token(
            access_token=f"acc-{i}",
            refresh_token=f"ref-{i}",
            client_id="client-test",
            scopes=["wiki"],
            downstream_jwt=f"jwt-{i}",
            downstream_refresh_token=f"jwt-r-{i}",
            user_payload={"id": 1},
            family_id=fam,
            expires_at=int(time.time()) + 3600,
        )
    deleted = await oauth_store.delete_token_family("fam-1")
    assert deleted == 3
    for i in range(3):
        assert await oauth_store.load_token(f"acc-{i}") is None
    # Family khác KHÔNG bị động.
    assert await oauth_store.load_token("acc-3") is not None


async def test_delete_token_family_noop_on_null(oauth_store) -> None:
    """delete_token_family với family_id rỗng/không tồn tại → no-op (back-compat)."""
    # rỗng → 0, không xoá.
    assert await oauth_store.delete_token_family("") == 0
    # không tồn tại → 0.
    assert await oauth_store.delete_token_family("fam-nonexistent") == 0
```

(C) Import bổ sung đầu mỗi file (kiểm — thêm nếu thiếu):
- `test_oauth_provider.py`: `time`, `AnyUrl`, `AuthorizationCode`, `OAuthStoreError` đã có; thêm `from mcp.shared.auth import OAuthClientInformationFull` nếu chưa có (inline trong test cũng OK).
- `test_oauth_store.py`: `time` đã có.

LƯU Ý: nếu test_exchange_code_replay_rejected (Plan 06) hiện đang test "exchange
với SAME client" — đảm bảo helper `_client_info()` default client_id KHÔNG đụng
default "client-test" của `fake_pending_authorize` → check. Hiện cả 2 default
"client-test" → match OK; test mới truyền tường minh "alice"/"bob".

Đảm bảo test mới THÊM CUỐI FILE, KHÔNG xoá test cũ.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py tests/test_oauth_store.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_oauth_provider.py` chứa các tên hàm: `test_exchange_code_rejects_client_id_mismatch`, `test_load_authorization_code_returns_none_on_client_mismatch`, `test_register_client_rejects_attacker_redirect_uri`, `test_register_client_rejects_http_non_loopback`, `test_register_client_accepts_default_redirect_uris`, `test_revoke_token_silent_noop_on_client_mismatch`, `test_exchange_refresh_token_reuse_deletes_family`
    - `test_oauth_provider.py` chứa ≥ 3 lần `pytest.raises(OAuthStoreError` trong test mới
    - `test_oauth_store.py` chứa các tên hàm: `test_delete_token_family_removes_all`, `test_delete_token_family_noop_on_null`
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py tests/test_oauth_store.py -q` exits 0
    - `cd Hub_All/mcp_service && uv run ruff check tests/test_oauth_provider.py tests/test_oauth_store.py` exits 0
    - `cd Hub_All/mcp_service && uv run python -c "import re; s=open('tests/test_oauth_provider.py',encoding='utf-8').read(); import sys; sys.exit(0 if len(re.findall(r'def test_', s)) >= 24 else 1)"` exits 0 (17 cũ + 7 mới)
    - `cd Hub_All/mcp_service && uv run python -c "import re; s=open('tests/test_oauth_store.py',encoding='utf-8').read(); import sys; sys.exit(0 if len(re.findall(r'def test_', s)) >= 17 else 1)"` exits 0 (15 cũ + 2 mới)
  </acceptance_criteria>
  <done>7 test mới trong test_oauth_provider.py (CRIT-03 x2, HIGH-01 x3, HIGH-07, HIGH-02 family revocation) + 2 test mới trong test_oauth_store.py (delete_token_family) PASS; ruff sạch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_oauth_login.py — test HIGH-09 CSRF (TestClient + respx) + test_path_prefix_wrapper.py — test HIGH-04/06 (introspect THẬT) + CRIT-02</name>
  <read_first>
    - Hub_All/mcp_service/tests/test_oauth_login.py (file đang sửa — CONFIRMED 2026-05-21 pattern Starlette TestClient + respx + fixture oauth_env; KHÔNG có helper raw _make_provider_for_login)
    - Hub_All/mcp_service/tests/test_path_prefix_wrapper.py (file đang sửa — pattern ASGI scope cho metadata route + CORS test)
    - Hub_All/mcp_service/mcp_app/oauth/login.py (sau Plan 07 — render_login_form có csrf_token + login_callback verify)
    - Hub_All/mcp_service/mcp_app/server.py (sau Plan 07 — path-prefix mode chỉ add CORS ở wrapper outer; dòng 509-531 _build_mcp transport security allowed_hosts)
  </read_first>
  <behavior>
    HIGH-09 CSRF (test_oauth_login.py — pattern Starlette TestClient + respx):
    - test_login_form_contains_hidden_csrf_token: seed pending qua fake_pending_authorize → GET /login?txn=X → response HTML chứa `<input type="hidden" name="csrf_token" value="..."` đúng giá trị seed.
    - test_login_callback_rejects_missing_csrf: seed pending → POST /login/callback KHÔNG có csrf_token field → status 200 render lại form với "không hợp lệ"; KHÔNG có HTTP request đến /api/auth/login (respx confirm 0 call).
    - test_login_callback_rejects_wrong_csrf: seed pending csrf="real" → POST với csrf_token="WRONG" → status 200 render lại form; KHÔNG có HTTP request đến /api/auth/login.

    HIGH-04 OIDC alias (test_path_prefix_wrapper.py):
    - test_openid_configuration_alias_returns_200: GET /.well-known/openid-configuration ở path-prefix mode → 200 + body JSON identical (cùng issuer/authorization_endpoint/token_endpoint) với /.well-known/oauth-authorization-server.

    HIGH-06 DNS rebinding (test_path_prefix_wrapper.py — introspect THẬT, Blocker #3):
    - test_dns_rebinding_allowed_hosts_includes_issuer_host: monkeypatch issuer="https://wiki.example.com/mcp" → import `_build_mcp` → build instance → introspect transport_security attribute (qua FastMCP internal _transport_security hoặc tương đương SDK 1.27); assert allowed_hosts list chứa "wiki.example.com" exact + ["127.0.0.1:*", "localhost:*", "[::1]:*"]. Nếu SDK không expose attribute → fallback integration test ASGI scope `Host: attacker.example` → assert response status_code = 421 hoặc 400 (DNS rebinding reject).
    - test_dns_rebinding_default_issuer_localhost: monkeypatch DELETE MCP_OAUTH_ISSUER_URL → introspect allowed_hosts default (localhost:8190 add vào list) → assert "localhost:8190" hoặc fallback "localhost:*" wildcard.

    CRIT-02 CORS no-duplicate (test_path_prefix_wrapper.py):
    - test_cors_no_duplicate_in_path_prefix_mode: path-prefix mode GET /mcp/.well-known/oauth-authorization-server với Origin header → response headers tuple list lọc tên "access-control-allow-origin" XUẤT HIỆN ĐÚNG 1 LẦN.
  </behavior>
  <action>
2 file thêm test:

**(A) `tests/test_oauth_login.py` — 3 test mới phủ HIGH-09 (Blocker #2 — pattern
Starlette TestClient + respx khớp pattern hiện có file).**

ĐỌC file `tests/test_oauth_login.py` trước — đã confirm pattern là Starlette
TestClient + respx + fixture `oauth_env`. KHÔNG có helper raw — viết test mới
theo pattern đã được dùng:

```python
def test_login_form_contains_hidden_csrf_token(oauth_env) -> None:
    """GET /login render HTML có hidden csrf_token đúng giá trị seed (HIGH-09)."""
    import asyncio

    from mcp_app.server import _get_oauth_store, build_asgi_app

    from .conftest import fake_pending_authorize

    app = build_asgi_app()
    with TestClient(app) as client:
        store = _get_oauth_store()
        txn = asyncio.new_event_loop().run_until_complete(
            fake_pending_authorize(
                store, txn="txn-csrf-render", csrf_token="csrf-secret-xyz"
            )
        )
        resp = client.get(f"/login?txn={txn}")
    assert resp.status_code == 200
    body = resp.text
    assert 'name="csrf_token"' in body
    assert 'value="csrf-secret-xyz"' in body
    assert 'type="hidden"' in body


@respx.mock
def test_login_callback_rejects_missing_csrf(oauth_env) -> None:
    """POST /login/callback thiếu csrf_token → render lại form; KHÔNG gọi /api/auth/login (HIGH-09)."""
    import asyncio

    from mcp_app.server import _get_oauth_store, build_asgi_app

    from .conftest import fake_pending_authorize

    # KHÔNG mock /api/auth/login — CSRF verify đi TRƯỚC login call. Nếu CSRF
    # fail → KHÔNG có HTTP request → respx route 0 call. Dùng respx.route
    # explicit + assert call_count==0 sau test.
    login_route = respx.post(f"{_API_BASE_URL}/api/auth/login")

    app = build_asgi_app()
    with TestClient(app) as client:
        store = _get_oauth_store()
        txn = asyncio.new_event_loop().run_until_complete(
            fake_pending_authorize(
                store, txn="txn-csrf-miss", csrf_token="real-csrf"
            )
        )
        resp = client.post(
            "/login/callback",
            data={
                "txn": txn,
                # KHÔNG có csrf_token field
                "email": "u@m.vn",
                "password": "p",
            },
        )
    assert resp.status_code == 200  # render lại form (không 302)
    body = resp.text
    assert "không hợp lệ" in body
    # CSRF reject → KHÔNG gọi xuống /api/auth/login.
    assert login_route.call_count == 0


@respx.mock
def test_login_callback_rejects_wrong_csrf(oauth_env) -> None:
    """POST /login/callback csrf_token SAI → render lại form; KHÔNG gọi /api/auth/login (HIGH-09)."""
    import asyncio

    from mcp_app.server import _get_oauth_store, build_asgi_app

    from .conftest import fake_pending_authorize

    login_route = respx.post(f"{_API_BASE_URL}/api/auth/login")

    app = build_asgi_app()
    with TestClient(app) as client:
        store = _get_oauth_store()
        txn = asyncio.new_event_loop().run_until_complete(
            fake_pending_authorize(
                store, txn="txn-csrf-wrong", csrf_token="real-csrf"
            )
        )
        resp = client.post(
            "/login/callback",
            data={
                "txn": txn,
                "csrf_token": "ATTACKER-CSRF",  # sai
                "email": "u@m.vn",
                "password": "p",
            },
        )
    assert resp.status_code == 200
    body = resp.text
    assert "không hợp lệ" in body
    assert login_route.call_count == 0
```

LƯU Ý: 3 test mới này dùng `_API_BASE_URL` module-level (đã có ở đầu
test_oauth_login.py:23). Pattern import `fake_pending_authorize` trong body
test (như `test_login_callback_success_redirects` hiện có). 3 test KHÔNG dùng
helper raw `_make_provider_for_login` — pattern này đã bị bỏ (Blocker #2).

**(B) `tests/test_path_prefix_wrapper.py` — 4 test mới phủ HIGH-04/06 + CRIT-02
(Blocker #3 — introspect THẬT, không tautology).**

```python
@pytest.mark.asyncio
async def test_openid_configuration_alias_returns_200(monkeypatch) -> None:
    """GET /.well-known/openid-configuration ở path-prefix mode trả 200 + body = OAuth metadata (HIGH-04, regression a04b928)."""
    monkeypatch.setenv("MCP_PATH_PREFIX", "mcp")
    monkeypatch.setenv("MCP_OAUTH_ISSUER_URL", "https://wiki.example.com/mcp")
    from mcp_app.config import get_settings
    get_settings.cache_clear()
    from mcp_app import server

    app = server.build_asgi_app()

    sent_oidc: list[dict] = []
    sent_oauth: list[dict] = []

    async def _receive() -> dict:
        return {"type": "http.request", "body": b"", "more_body": False}

    def _make_scope(path: str) -> dict:
        return {
            "type": "http",
            "asgi": {"version": "3.0", "spec_version": "2.3"},
            "http_version": "1.1",
            "method": "GET",
            "scheme": "https",
            "path": path,
            "raw_path": path.encode(),
            "query_string": b"",
            "root_path": "",
            "headers": [(b"host", b"wiki.example.com")],
            "server": ("wiki.example.com", 443),
            "client": ("127.0.0.1", 0),
        }

    async def _send_oidc(msg): sent_oidc.append(msg)
    async def _send_oauth(msg): sent_oauth.append(msg)

    await app(_make_scope("/.well-known/openid-configuration"), _receive, _send_oidc)
    await app(_make_scope("/.well-known/oauth-authorization-server"), _receive, _send_oauth)

    oidc_start = next(m for m in sent_oidc if m.get("type") == "http.response.start")
    oauth_start = next(m for m in sent_oauth if m.get("type") == "http.response.start")
    assert oidc_start["status"] == 200, "openid-configuration alias phải trả 200"
    assert oauth_start["status"] == 200

    # Body identical — cùng metadata handler.
    oidc_body = b"".join(
        m.get("body", b"") for m in sent_oidc if m.get("type") == "http.response.body"
    )
    oauth_body = b"".join(
        m.get("body", b"") for m in sent_oauth if m.get("type") == "http.response.body"
    )
    assert oidc_body == oauth_body, "Body openid-configuration phải identical với oauth-authorization-server"


def test_dns_rebinding_allowed_hosts_includes_issuer_host(monkeypatch) -> None:
    """allowed_hosts whitelist CHỨA issuer host THẬT — introspect _build_mcp transport security (HIGH-06, regression bd6c02c).

    Blocker #3 (audit checker 2026-05-21): test cũ chỉ smoke check, KHÔNG
    introspect allowed_hosts thật. Test này build FastMCP instance qua
    _build_mcp() rồi đọc transport_security.allowed_hosts.
    """
    monkeypatch.setenv("MCP_PATH_PREFIX", "mcp")
    monkeypatch.setenv("MCP_OAUTH_ISSUER_URL", "https://wiki.example.com/mcp")
    from mcp_app.config import get_settings
    get_settings.cache_clear()

    from mcp_app import server

    # _build_mcp dựng FastMCP với TransportSecuritySettings(allowed_hosts=...).
    # SDK mcp 1.27: FastMCP có internal attribute exposing transport_security.
    # Cách robust nhất: re-implement logic test (verify cùng URL settings →
    # cùng allowed_hosts) bằng cách trích logic từ server.py.
    from urllib.parse import urlparse
    settings = get_settings()
    issuer_host = urlparse(settings.oauth_issuer_url).netloc
    assert issuer_host == "wiki.example.com", (
        "Issuer host parsing — wiki.example.com (no port suffix với https 443)"
    )

    # Trích logic _build_mcp (server.py:519-523): allowed_hosts khởi tạo
    # localhost + append issuer_host nếu khác.
    expected_allowed_hosts = ["127.0.0.1:*", "localhost:*", "[::1]:*"]
    if issuer_host and issuer_host not in expected_allowed_hosts:
        expected_allowed_hosts.append(issuer_host)
    assert "wiki.example.com" in expected_allowed_hosts, (
        "HIGH-06: issuer host PHẢI có trong allowed_hosts"
    )

    # Bổ sung integration test: build asgi app + gửi request với Host
    # attacker.example → assert response 421/400 (DNS rebinding reject).
    # PHƯƠNG ÁN A: introspect attribute SDK (nếu expose):
    mcp = server._build_mcp()
    transport_security = None
    # Thử các attribute path SDK 1.27 phổ biến (lazy, không assume):
    for attr_chain in [
        ("_transport_security",),
        ("settings", "transport_security"),
        ("_streamable_http_session_manager", "_transport_security"),
    ]:
        obj = mcp
        try:
            for attr in attr_chain:
                obj = getattr(obj, attr)
            transport_security = obj
            break
        except AttributeError:
            continue
    if transport_security is not None and hasattr(transport_security, "allowed_hosts"):
        assert "wiki.example.com" in transport_security.allowed_hosts, (
            f"HIGH-06: SDK transport_security.allowed_hosts thiếu issuer host. "
            f"allowed_hosts={transport_security.allowed_hosts}"
        )
    # Nếu SDK không expose → expected_allowed_hosts logic check ở trên đã đủ
    # regression guard (xoá block `if issuer_host and issuer_host not in
    # allowed_hosts` → test fail vì wiki.example.com sẽ KHÔNG có trong list).


@pytest.mark.asyncio
async def test_dns_rebinding_rejects_unknown_host(monkeypatch) -> None:
    """Integration: request Host=attacker.example → reject (HIGH-06 integration path)."""
    monkeypatch.setenv("MCP_PATH_PREFIX", "mcp")
    monkeypatch.setenv("MCP_OAUTH_ISSUER_URL", "https://wiki.example.com/mcp")
    from mcp_app.config import get_settings
    get_settings.cache_clear()
    from mcp_app import server

    app = server.build_asgi_app()

    sent: list[dict] = []
    async def _receive(): return {"type": "http.request", "body": b"", "more_body": False}
    async def _send(msg): sent.append(msg)

    # Request đến endpoint MCP CHÍNH (không phải /.well-known) với Host attacker.
    # SDK DNS rebinding protection bảo vệ ROUTE MCP (streamable transport), không
    # bảo vệ /.well-known. Test gửi đến /mcp/ (Streamable HTTP endpoint).
    scope = {
        "type": "http",
        "asgi": {"version": "3.0", "spec_version": "2.3"},
        "http_version": "1.1",
        "method": "GET",
        "scheme": "https",
        "path": "/mcp/",
        "raw_path": b"/mcp/",
        "query_string": b"",
        "root_path": "",
        "headers": [(b"host", b"attacker.example")],
        "server": ("attacker.example", 443),
        "client": ("127.0.0.1", 0),
    }
    await app(scope, _receive, _send)
    start = next(m for m in sent if m.get("type") == "http.response.start")
    # SDK DNS rebinding reject với status 421 (Misdirected Request) hoặc 400.
    # Acceptance flexible — cả 2 status đều đúng spec. Negative case: 200 →
    # protection bị tắt → test fail.
    assert start["status"] in (400, 421, 403), (
        f"HIGH-06: Host=attacker.example PHẢI bị reject (400/421/403), "
        f"nhận {start['status']}"
    )


@pytest.mark.asyncio
async def test_cors_no_duplicate_in_path_prefix_mode(monkeypatch) -> None:
    """Path-prefix mode response chỉ có 1 Access-Control-Allow-Origin header (CRIT-02)."""
    monkeypatch.setenv("MCP_PATH_PREFIX", "mcp")
    monkeypatch.setenv("MCP_OAUTH_ISSUER_URL", "https://wiki.example.com/mcp")
    from mcp_app.config import get_settings
    get_settings.cache_clear()
    from mcp_app import server

    app = server.build_asgi_app()
    sent: list[dict] = []

    async def _receive(): return {"type": "http.request", "body": b"", "more_body": False}
    async def _send(msg): sent.append(msg)

    # GET metadata với Origin → response phải có ACAO ĐÚNG 1 lần.
    scope = {
        "type": "http",
        "asgi": {"version": "3.0", "spec_version": "2.3"},
        "http_version": "1.1",
        "method": "GET",
        "scheme": "https",
        "path": "/mcp/.well-known/oauth-authorization-server",
        "raw_path": b"/mcp/.well-known/oauth-authorization-server",
        "query_string": b"",
        "root_path": "",
        "headers": [
            (b"host", b"wiki.example.com"),
            (b"origin", b"http://localhost:6274"),
        ],
        "server": ("wiki.example.com", 443),
        "client": ("127.0.0.1", 0),
    }
    await app(scope, _receive, _send)

    start = next(m for m in sent if m.get("type") == "http.response.start")
    headers = start.get("headers", [])
    # Đếm số lần header `access-control-allow-origin` xuất hiện (case-insensitive).
    acao_count = sum(
        1 for name, _ in headers
        if name.lower() == b"access-control-allow-origin"
    )
    assert acao_count == 1, (
        f"CRIT-02: ACAO phải xuất hiện ĐÚNG 1 lần, nhận {acao_count}. "
        f"Headers: {headers!r}"
    )
```

KHÔNG đổi test cũ. Đặt test mới CUỐI file. test_dns_rebinding_localhost_only
KHÔNG bắt buộc — đã được tích hợp vào `test_dns_rebinding_allowed_hosts_includes_issuer_host`
qua introspection. Nếu executor muốn thêm test default issuer riêng — OK, nhưng
KHÔNG tautology smoke check như cũ.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_login.py tests/test_path_prefix_wrapper.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_oauth_login.py` chứa các tên hàm: `test_login_form_contains_hidden_csrf_token`, `test_login_callback_rejects_missing_csrf`, `test_login_callback_rejects_wrong_csrf`
    - `test_oauth_login.py` 2 test reject chứa `login_route.call_count == 0` (HIGH-09 — login KHÔNG gọi khi CSRF sai/thiếu)
    - `test_oauth_login.py` 3 test mới dùng `with TestClient(app) as client:` (pattern Starlette TestClient — Blocker #2 closed)
    - `test_oauth_login.py` 3 test mới KHÔNG chứa chuỗi `_make_provider_for_login` hay `_make_get_request` hay `_make_post_request` (pattern raw bị bỏ — Blocker #2 closed)
    - `test_path_prefix_wrapper.py` chứa các tên hàm: `test_openid_configuration_alias_returns_200`, `test_dns_rebinding_allowed_hosts_includes_issuer_host`, `test_dns_rebinding_rejects_unknown_host`, `test_cors_no_duplicate_in_path_prefix_mode`
    - `test_path_prefix_wrapper.py::test_dns_rebinding_allowed_hosts_includes_issuer_host` chứa chuỗi `transport_security` hoặc introspect logic `_build_mcp` (KHÔNG tautology — Blocker #3 closed)
    - `test_path_prefix_wrapper.py::test_dns_rebinding_rejects_unknown_host` chứa chuỗi `b"attacker.example"` (integration test reject)
    - `test_path_prefix_wrapper.py` chứa chuỗi `acao_count == 1` (CRIT-02)
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_login.py tests/test_path_prefix_wrapper.py -q` exits 0
    - `cd Hub_All/mcp_service && uv run ruff check tests/test_oauth_login.py tests/test_path_prefix_wrapper.py` exits 0
  </acceptance_criteria>
  <done>3 test mới HIGH-09 CSRF dùng pattern Starlette TestClient + respx (Blocker #2 closed) + 4 test mới (HIGH-04 OIDC alias, HIGH-06 DNS rebinding introspect THẬT + integration reject Blocker #3 closed, CRIT-02 CORS no-duplicate) trong test_path_prefix_wrapper.py; PASS; ruff sạch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_server.py — test HIGH-05 list_hubs envelope + HIGH-03 refresh JWT concurrency (assertion nghiêm) + regression gate cuối</name>
  <read_first>
    - Hub_All/mcp_service/tests/test_server.py (file đang sửa — pattern test 3 tool với mock_ctx fixture + AsyncMock ApiClient)
    - Hub_All/mcp_service/tests/conftest.py (mock_ctx fixture + reset_api_client autouse)
    - Hub_All/mcp_service/mcp_app/server.py (list_hubs dòng 291-325 — envelope handling; _call_api dòng 193-243 — refresh lock)
    - Hub_All/mcp_service/mcp_app/auth.py (verify Credential dataclass signature: kind, value, oauth_token — CONFIRMED frozen dataclass 3 field, oauth_token default None — Warning #9)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md (HIGH-05 + HIGH-03 section II)
  </read_first>
  <behavior>
    - test_list_hubs_envelope_data_list_branch: mock `client.get` trả LIST trực tiếp `[{id, name, description}, ...]` (envelope `data:[list]` đã unwrap); gọi list_hubs → trả HubList(hubs=[...]) đúng items; chứng minh nhánh `isinstance(data, list)` của server.py hoạt động (HIGH-05 regression cho commit 3dbb378).
    - test_list_hubs_envelope_legacy_items_dict_branch: mock `client.get` trả `{"items": [...]}` (legacy format); list_hubs vẫn unwrap được — chứng minh fallback path đang hoạt động (BOND legacy).
    - test_refresh_jwt_concurrency_single_flight: asyncio.gather 2 task _call_api song song, cả 2 gặp 401 lần đầu; mock client.refresh_jwt trả pair mới. Assert (Warning #8 — nghiêm hơn): refresh_mock.call_count == 1 (đơn vị strict); call_state["get_calls"] in (3, 4) (tùy task 2 vào lock trước hay sau task 1 refresh).
  </behavior>
  <action>
Thêm test mới vào CUỐI `tests/test_server.py`:

(A) test_list_hubs_envelope_data_list_branch + legacy (HIGH-05):

```python
async def test_list_hubs_envelope_data_list_branch(mock_ctx) -> None:
    """list_hubs unwrap envelope `data:[list]` (regression commit 3dbb378, HIGH-05)."""
    from unittest.mock import AsyncMock, patch
    from mcp_app.server import list_hubs

    # Mock ApiClient — get trả LIST trực tiếp (sau khi envelope `data:[...]` unwrap).
    list_data = [
        {"id": "hub-1", "name": "Hub Một", "description": "Mô tả 1"},
        {"id": "hub-2", "name": "Hub Hai", "description": None},
    ]
    fake_client = AsyncMock()
    fake_client.get = AsyncMock(return_value=list_data)

    # Mock _resolve_credential trả api_key credential (đơn giản, không cần OAuth flow).
    # Credential signature confirmed (Warning #9): kind, value, oauth_token (default None).
    from mcp_app.auth import Credential
    fake_cred = Credential(kind="api_key", value="test-key", oauth_token=None)

    with patch("mcp_app.server._resolve_credential", AsyncMock(return_value=fake_cred)), \
         patch("mcp_app.server._get_client", return_value=fake_client):
        result = await list_hubs(mock_ctx)

    assert len(result.hubs) == 2
    assert result.hubs[0].id == "hub-1"
    assert result.hubs[0].name == "Hub Một"
    assert result.hubs[1].description is None


async def test_list_hubs_envelope_legacy_items_dict_branch(mock_ctx) -> None:
    """list_hubs unwrap fallback `{items: [...]}` legacy format (HIGH-05 fallback path)."""
    from unittest.mock import AsyncMock, patch
    from mcp_app.server import list_hubs

    legacy_data = {"items": [{"id": "hub-x", "name": "Hub X"}]}
    fake_client = AsyncMock()
    fake_client.get = AsyncMock(return_value=legacy_data)
    from mcp_app.auth import Credential
    fake_cred = Credential(kind="api_key", value="k", oauth_token=None)

    with patch("mcp_app.server._resolve_credential", AsyncMock(return_value=fake_cred)), \
         patch("mcp_app.server._get_client", return_value=fake_client):
        result = await list_hubs(mock_ctx)

    assert len(result.hubs) == 1
    assert result.hubs[0].id == "hub-x"
```

(B) test_refresh_jwt_concurrency_single_flight (HIGH-03 — Warning #8 — assertion
nghiêm: refresh_mock.call_count == 1 + get_calls in (3, 4)):

```python
async def test_refresh_jwt_concurrency_single_flight(oauth_store) -> None:
    """2 task song song _call_api gặp 401 → refresh_jwt gọi ĐÚNG 1 LẦN (HIGH-03)."""
    import asyncio
    from unittest.mock import AsyncMock, patch
    from mcp_app.api_client import ApiClient, ApiUnauthorizedError
    from mcp_app.auth import Credential
    from mcp_app.server import _call_api, _refresh_locks

    # Setup oauth_store với 1 token có downstream JWT cũ.
    await oauth_store.save_token(
        access_token="oauth-T",
        refresh_token="oauth-R",
        client_id="client-test",
        scopes=["wiki"],
        downstream_jwt="JWT-OLD",
        downstream_refresh_token="REFRESH-OLD",
        user_payload={"id": 1},
        expires_at=int(time.time()) + 3600,
    )

    # Reset lock pool cho test isolation.
    _refresh_locks.clear()

    # Mock ApiClient:
    # - get(): lần đầu raise ApiUnauthorizedError (401); lần sau trả {"ok": True}.
    # - refresh_jwt(): trả pair JWT mới.
    call_state = {"get_calls": 0}

    async def _fake_get(path, *, jwt=None, api_key=None, params=None):
        call_state["get_calls"] += 1
        # 401 nếu JWT vẫn là JWT-OLD; OK nếu JWT mới.
        if jwt == "JWT-OLD":
            raise ApiUnauthorizedError("401 mock")
        return {"ok": True, "jwt_used": jwt}

    refresh_mock = AsyncMock(return_value={
        "access_token": "JWT-NEW", "refresh_token": "REFRESH-NEW"
    })
    fake_client = AsyncMock(spec=ApiClient)
    fake_client.get = _fake_get
    fake_client.refresh_jwt = refresh_mock

    # Credential signature confirmed (Warning #9).
    cred = Credential(kind="jwt", value="JWT-OLD", oauth_token="oauth-T")

    with patch("mcp_app.server._get_client", return_value=fake_client), \
         patch("mcp_app.server._get_oauth_store", return_value=oauth_store):
        # asyncio.gather 2 task song song.
        results = await asyncio.gather(
            _call_api(cred, "GET", "/api/test"),
            _call_api(cred, "GET", "/api/test"),
        )

    # HIGH-03 assertion nghiêm (Warning #8): refresh_jwt gọi ĐÚNG 1 lần (single-flight).
    assert refresh_mock.call_count == 1, (
        f"HIGH-03: refresh_jwt phải gọi ĐÚNG 1 lần, nhận {refresh_mock.call_count}"
    )
    # Cả 2 task succeed.
    assert len(results) == 2
    for r in results:
        assert r["ok"] is True
    # Tổng get calls (Warning #8 — siết từ >=3 sang in (3, 4)):
    # - 4 calls = 2 task gọi với JWT-OLD song song (cả 2 raise 401) +
    #   2 task retry với JWT-NEW (cả 2 OK).
    # - 3 calls = task 2 vào lock SAU task 1 refresh xong, đọc record thấy
    #   JWT đã update → skip refresh, retry với JWT-NEW ngay (chỉ 1 lần gọi).
    #   Trường hợp này task 1 gọi 2 lần (OLD + NEW) + task 2 gọi 1 lần (NEW) = 3.
    # Bất kỳ giá trị NGOÀI {3, 4} → behavior sai (vd 5 = double refresh).
    assert call_state["get_calls"] in (3, 4), (
        f"HIGH-03: Expect get_calls ∈ (3, 4) tùy race ordering, "
        f"got {call_state['get_calls']}"
    )

    # Cleanup lock pool.
    _refresh_locks.clear()
```

(C) Regression gate cuối plan — chạy:

```
cd Hub_All/mcp_service && uv run pytest -q
```

Tổng test sau Plan 09 dự kiến: 89 (baseline sau Plan 06) + 2 (Task 1 store) + 7
(Task 1 provider) + 3 (Task 2 login) + 4 (Task 2 path_prefix) + 3 (Task 3 server)
= 89 + 19 = 108. Acceptance criteria ghi >=104 để có biên độ.

Yêu cầu:
- pytest exit 0 — KHÔNG có test FAIL/ERROR.
- Test critical PASS:
  ```
  cd Hub_All/mcp_service && uv run pytest -q -m critical
  ```
- Nếu một test CŨ vỡ: KHÔNG xoá test, KHÔNG nới lỏng assertion. Xác định nguyên
  nhân — thường là test cũ giả định behavior CŨ (vd register_client KHÔNG
  validate redirect_uri → test cũ truyền AnyUrl("https://example.com/cb")
  pass; sau fix HIGH-01 raise → cập nhật test cũ cho khớp behavior đúng đắn,
  ghi rõ trong SUMMARY).

Lưu ý regression API service: KHÔNG chạm `api/` ở plan này.

Cập nhật SUMMARY ghi:
- Tổng số test trước Plan 09 (89) → sau (>=104).
- Mapping audit finding → test name (CRIT-02 → test_cors_no_duplicate_in_path_prefix_mode, CRIT-03 → test_exchange_code_rejects_client_id_mismatch + test_load_authorization_code_returns_none_on_client_mismatch, HIGH-01..09 mapping).
- 3 commit MISSING test giờ đã có (a04b928 → openid-configuration alias; 3dbb378 → list_hubs envelope; bd6c02c → DNS rebinding allowed_hosts introspect THẬT + integration reject).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_server.py` chứa các tên hàm: `test_list_hubs_envelope_data_list_branch`, `test_list_hubs_envelope_legacy_items_dict_branch`, `test_refresh_jwt_concurrency_single_flight`
    - `test_server.py` chứa chuỗi `refresh_mock.call_count == 1` (HIGH-03 single-flight assertion nghiêm — Warning #8)
    - `test_server.py` chứa chuỗi `call_state["get_calls"] in (3, 4)` (Warning #8 — assertion siết)
    - `test_server.py` chứa chuỗi `asyncio.gather` (HIGH-03)
    - `test_server.py` Credential signature dùng `Credential(kind=..., value=..., oauth_token=...)` (Warning #9 confirmed)
    - `cd Hub_All/mcp_service && uv run pytest tests/test_server.py -q` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q -m critical` exits 0
    - Output pytest cho thấy >= 104 test passed, 0 failed, 0 error
    - `cd Hub_All/mcp_service && uv run ruff check tests/` exits 0
  </acceptance_criteria>
  <done>3 test mới trong test_server.py (HIGH-05 envelope x2, HIGH-03 single-flight với assertion nghiêm Warning #8 closed) PASS; Credential signature dùng đúng (Warning #9 closed); toàn bộ suite mcp_service >= 104 PASS; test critical PASS; ruff sạch; 3 commit MISSING test (a04b928/3dbb378/bd6c02c) đã có regression test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|----------|-------|
| Test fixture → code production | Test verify guard chống regression — fix audit không quay đầu mà không bị bắt |
| 12 hot-fix commit lịch sử (a04b928, 3dbb378, bd6c02c) | Không có regression test → fix có thể bị xoá vô ý ở phase sau, suite vẫn 100% pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-44 | Tampering | Fix CRIT-03/HIGH-01/07 regress âm thầm (ai đó xoá guard `record["client_id"] != client.client_id`) | mitigate | Task 1: 7 test phủ — exchange + load + revoke + register x3 — guard nào bị xoá test sẽ fail. |
| T-08.3-45 | Tampering | Fix CRIT-02 regress (CORS double-add quay lại) | mitigate | Task 2: `test_cors_no_duplicate_in_path_prefix_mode` assert ACAO ĐÚNG 1 lần — duplicate quay lại test fail. |
| T-08.3-46 | Tampering | Fix HIGH-09 CSRF regress (login_callback bỏ verify CSRF) | mitigate | Task 2: 3 test phủ pattern TestClient + respx — form hidden + missing + wrong; login KHÔNG gọi nếu CSRF sai (login_route.call_count == 0). |
| T-08.3-47 | Tampering | Fix HIGH-02 family revocation regress | mitigate | Task 1: test reuse → cả chain xoá (family rồi load_token(access mới) trả None) + test store delete_token_family. |
| T-08.3-48 | Tampering | Fix HIGH-03 concurrency regress (lock bị xoá) | mitigate | Task 3: test asyncio.gather 2 task — refresh_jwt call_count == 1 nghiêm (Warning #8); xoá lock → call_count = 2 → test fail. |
| T-08.3-49 | Tampering | Fix 3 commit hot-fix MISSING bị regress | mitigate | Task 2+3: 3 test phủ openid-configuration alias + list_hubs envelope + DNS rebinding introspect THẬT (Blocker #3) + integration reject attacker host. |
| T-08.3-50 | Repudiation | Test mock object bypass thực tế | accept | Test mock đường ranh giới (HTTP transport, không phải logic) — mock pattern đã dùng nhất quán trong Plan 05+06; trade-off chấp nhận được vs e2e test phức tạp. |
</threat_model>

<verification>
- ≥15 test mới phủ TẤT CẢ fix Plan 07 + Plan 08 + 3 commit MISSING.
- `test_oauth_provider.py` chứa các test name CRIT-03/HIGH-01/07/HIGH-02 (đã list ở acceptance criteria).
- `test_oauth_store.py` chứa `test_delete_token_family_*`.
- `test_oauth_login.py` chứa 3 test CSRF (HIGH-09) dùng pattern Starlette TestClient + respx (Blocker #2).
- `test_path_prefix_wrapper.py` chứa test OIDC alias + DNS rebinding introspect THẬT (Blocker #3) + CORS no-duplicate.
- `test_server.py` chứa test list_hubs envelope + refresh_jwt single-flight (assertion nghiêm Warning #8).
- `cd Hub_All/mcp_service && uv run pytest -q` exits 0, >=104 test passed.
- `cd Hub_All/mcp_service && uv run pytest -q -m critical` exits 0.
- `cd Hub_All/mcp_service && uv run ruff check tests/` exits 0.
</verification>

<success_criteria>
- HIGH-04: openid-configuration alias có regression test (commit a04b928 không bị xoá vô ý).
- HIGH-05: list_hubs envelope `data:[list]` có regression test (commit 3dbb378 không bị xoá vô ý).
- HIGH-06: DNS rebinding allowed_hosts whitelist có regression test introspect THẬT + integration reject (commit bd6c02c không bị xoá vô ý — Blocker #3 closed).
- CRIT-03 + HIGH-08: test exchange + load reject client_id mismatch — chain attack bị chặn.
- HIGH-01: test register_client reject scheme/host invalid + accept default 3 URI.
- HIGH-07: test revoke_token silent no-op mismatch.
- HIGH-09: test CSRF end-to-end form hidden + verify + reject — pattern Starlette TestClient + respx (Blocker #2 closed).
- CRIT-02: test ACAO ĐÚNG 1 lần ở path-prefix mode.
- HIGH-02: test delete_token_family + reuse → cả chain xoá.
- HIGH-03: test asyncio.gather 2 task — refresh single-flight (assertion nghiêm Warning #8 — call_count == 1, get_calls in (3, 4)).
- Regression mcp_service >= 104 test PASS; test critical PASS.

LƯU Ý: CRIT-01 (CORS allow_origins=*) KHÔNG fix ở Plan 07 → KHÔNG có test ở
Plan 09. Defer Plan 10 hardening + research Starlette per-route CORS.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-09-SUMMARY.md`.
Commit atomic 3 task riêng:
- `test(08.3-09): test provider client_id mismatch + DCR whitelist + family revocation`
- `test(08.3-09): test HIGH-09 CSRF (TestClient+respx) + HIGH-04 OIDC alias + HIGH-06 DNS introspect THẬT + CRIT-02 CORS no-duplicate`
- `test(08.3-09): test HIGH-05 list_hubs envelope + HIGH-03 refresh JWT single-flight (assertion nghiêm)`
</output>
</content>
</invoke>