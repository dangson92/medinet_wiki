---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 06
type: execute
wave: 2
depends_on: [05]
gap_closure: true
files_modified:
  - Hub_All/mcp_service/tests/test_oauth_provider.py
  - Hub_All/mcp_service/tests/test_oauth_store.py
autonomous: true
requirements: [MCP-01, MCP-02]

must_haves:
  truths:
    - "Dùng lại authorization code đã exchange → exchange thất bại (raise OAuthStoreError)"
    - "save_token lỗi giữa chừng (mock/patch) → authorization code KHÔNG còn để replay"
    - "Dùng lại refresh token đã rotate → rotate_token trả False / provider raise lỗi reuse"
    - "Pending transaction hết hạn (created_at quá _PENDING_TTL) → complete_authorization từ chối"
    - "PKCE code_verifier sai → exchange thất bại (xác nhận SDK mcp 1.27 verify PKCE, hoặc provider tự verify)"
    - "Regression mcp_service 78/78 cũ vẫn PASS sau khi thêm test mới"
  artifacts:
    - path: "Hub_All/mcp_service/tests/test_oauth_provider.py"
      provides: "test replay code, refresh reuse, pending hết hạn, save_token lỗi, PKCE sai"
      contains: "def test_"
    - path: "Hub_All/mcp_service/tests/test_oauth_store.py"
      provides: "test claim_auth_code nguyên tử + rotate_token rowcount"
      contains: "claim_auth_code"
  key_links:
    - from: "test_oauth_provider.py"
      to: "provider.exchange_authorization_code / exchange_refresh_token"
      via: "test replay/reuse"
      pattern: "pytest.raises\\(OAuthStoreError\\)"
---

<objective>
Thêm test phủ đường replay / đồng thời / PKCE — gap `missing` #3 trong
08.3-VERIFICATION.md ("Thêm test phủ đường đồng thời / replay"). Plan 05 đã vá
code (CR-01/CR-02/WR-02..05); plan này CHỨNG MINH bằng test rằng các fix thật sự
chặn replay/reuse — đồng thời xác nhận SDK mcp 1.27 có verify PKCE (WR-01).

Purpose: Test suite hiện chỉ phủ đường tuần tự nên không bắt được CR-01/CR-02.
Không có test replay, một fix có thể regress âm thầm. Plan này đóng gap đó —
biến SC3 từ FAILED → VERIFIED có bằng chứng test.

Output: test_oauth_store.py + test_oauth_provider.py có test mới phủ:
claim_auth_code nguyên tử, rotate_token rowcount, code replay, save_token lỗi
giữa chừng, refresh token reuse, pending hết hạn, PKCE code_verifier sai.

PHỤ THUỘC Plan 05 (wave 1) — test gọi `claim_auth_code`/`rotate_token` và kỳ
vọng hành vi raise của provider đã vá. KHÔNG chạy plan này trước Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-VERIFICATION.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md
@.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-05-PLAN.md
@Hub_All/CLAUDE.md

<interfaces>
<!-- Pattern test + fixture hiện có — executor dùng trực tiếp, không cần khám phá. -->

tests/conftest.py — fixture/helper:
```python
@pytest.fixture async def oauth_store     # OAuthStore trên SQLite :memory:, schema sẵn
async def fake_pending_authorize(store, *, txn="txn-test", client_id="client-test",
    redirect_uri="https://claude.ai/api/mcp/auth_callback", code_challenge="challenge-test",
    code_challenge_method="S256", client_state="client-state-test",
    scopes=None, created_at=None) -> str   # seed 1 bản ghi oauth_pending, trả txn
```
- `fake_pending_authorize` nhận `created_at` — truyền giá trị quá khứ để test pending hết hạn.

tests/test_oauth_provider.py — helper hiện có (tái dùng, KHÔNG định nghĩa lại):
```python
def _make_provider(store, api_client=None) -> MedinetOAuthProvider  # access_token_ttl=3600
def _client_info(client_id="client-test") -> OAuthClientInformationFull
def _login_data() -> dict   # {access_token, refresh_token, expires_at, user}
ISSUER = "http://localhost:8190"
REDIRECT = "https://claude.ai/api/mcp/auth_callback"
```
- Test async không cần `@pytest.mark.asyncio` (pytest-asyncio auto mode đã cấu hình — các test hiện có là `async def` trần).

Chữ ký method Plan 05 vừa thêm (test gọi tới):
```python
# store.py
async def claim_auth_code(self, code: str) -> dict | None   # DELETE RETURNING, None nếu đã claim
async def rotate_token(self, old_refresh_token, new_access_token,
                       new_refresh_token, expires_at) -> bool   # False nếu rowcount != 1
# provider.py
exchange_authorization_code  -> raise OAuthStoreError nếu code đã dùng
exchange_refresh_token       -> raise OAuthStoreError nếu refresh token reuse
complete_authorization       -> raise OAuthStoreError nếu pending hết hạn (created_at < now - 600)
_PENDING_TTL = 600           # hằng số module provider.py
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_oauth_store.py — test claim_auth_code nguyên tử + rotate_token rowcount</name>
  <read_first>
    - Hub_All/mcp_service/tests/test_oauth_store.py (file đang sửa — pattern test, import fake_pending_authorize, style async def trần)
    - Hub_All/mcp_service/mcp_app/oauth/store.py (chữ ký claim_auth_code / rotate_token sau Plan 05)
    - Hub_All/mcp_service/tests/conftest.py (fixture oauth_store)
  </read_first>
  <behavior>
    - test_claim_auth_code_single_use: save_auth_code → claim_auth_code trả dict đủ 6 field downstream; gọi claim_auth_code LẦN 2 cùng code → None (đã claim, không còn trong store); load_auth_code sau đó cũng None.
    - test_claim_auth_code_missing: claim_auth_code với code không tồn tại → None.
    - test_rotate_token_success: save_token (access=A, refresh=R) → rotate_token(old_refresh_token=R, new_access=A2, new_refresh=R2, ...) trả True; load_token(A2) trả record; load_token(A) trả None; load_token_by_refresh(R2) trả record.
    - test_rotate_token_reuse_returns_false: sau rotate lần 1 (R→R2), rotate_token lần 2 với old_refresh_token=R (refresh cũ) → trả False (rowcount 0); store không đổi.
    - test_rotate_token_unknown_refresh: rotate_token với old_refresh_token không tồn tại → False.
  </behavior>
  <action>
Thêm 5 test vào CUỐI `Hub_All/mcp_service/tests/test_oauth_store.py` (giữ style
hiện có: `async def test_...(oauth_store)` trần, không marker asyncio; dùng
fixture `oauth_store`). Cập nhật docstring đầu file thêm dòng mô tả test mới.

Mẫu cụ thể (executor copy + điều chỉnh tên biến cho nhất quán):

```python
async def test_claim_auth_code_single_use(oauth_store) -> None:
    """claim_auth_code đọc + xoá code nguyên tử — lần 2 trả None (CR-01)."""
    await oauth_store.save_auth_code(
        code="code-claim",
        client_id="client-abc",
        code_payload={"redirect_uri": "https://claude.ai/cb", "scopes": ["wiki"]},
        downstream_jwt="jwt-a",
        downstream_refresh_token="jwt-r",
        user_payload={"id": 1, "email": "u@medinet.vn"},
        expires_at=int(time.time()) + 600,
    )
    first = await oauth_store.claim_auth_code("code-claim")
    assert first is not None
    assert first["downstream_jwt"] == "jwt-a"
    assert first["downstream_refresh_token"] == "jwt-r"
    # Claim lần 2 — code đã bị xoá nguyên tử.
    assert await oauth_store.claim_auth_code("code-claim") is None
    assert await oauth_store.load_auth_code("code-claim") is None


async def test_claim_auth_code_missing(oauth_store) -> None:
    """claim_auth_code với code không tồn tại → None."""
    assert await oauth_store.claim_auth_code("code-khong-ton-tai") is None


async def test_rotate_token_success(oauth_store) -> None:
    """rotate_token định vị theo refresh_token → True; access cũ mất hiệu lực (CR-02)."""
    await oauth_store.save_token(
        access_token="acc-A",
        refresh_token="ref-R",
        client_id="client-abc",
        scopes=["wiki"],
        downstream_jwt="jwt",
        downstream_refresh_token="jwt-r",
        user_payload={"id": 1},
        expires_at=int(time.time()) + 3600,
    )
    ok = await oauth_store.rotate_token(
        old_refresh_token="ref-R",
        new_access_token="acc-A2",
        new_refresh_token="ref-R2",
        expires_at=int(time.time()) + 3600,
    )
    assert ok is True
    assert await oauth_store.load_token("acc-A2") is not None
    assert await oauth_store.load_token("acc-A") is None
    assert await oauth_store.load_token_by_refresh("ref-R2") is not None


async def test_rotate_token_reuse_returns_false(oauth_store) -> None:
    """rotate_token với refresh token đã rotate → False (rowcount 0, reuse)."""
    await oauth_store.save_token(
        access_token="acc-B",
        refresh_token="ref-S",
        client_id="client-abc",
        scopes=["wiki"],
        downstream_jwt="jwt",
        downstream_refresh_token="jwt-r",
        user_payload={"id": 1},
        expires_at=int(time.time()) + 3600,
    )
    assert await oauth_store.rotate_token(
        old_refresh_token="ref-S",
        new_access_token="acc-B2",
        new_refresh_token="ref-S2",
        expires_at=int(time.time()) + 3600,
    ) is True
    # Dùng lại refresh token CŨ — phải trả False.
    assert await oauth_store.rotate_token(
        old_refresh_token="ref-S",
        new_access_token="acc-B3",
        new_refresh_token="ref-S3",
        expires_at=int(time.time()) + 3600,
    ) is False
    assert await oauth_store.load_token("acc-B3") is None


async def test_rotate_token_unknown_refresh(oauth_store) -> None:
    """rotate_token với refresh token không tồn tại → False."""
    assert await oauth_store.rotate_token(
        old_refresh_token="ref-khong-ton-tai",
        new_access_token="acc-x",
        new_refresh_token="ref-x",
        expires_at=int(time.time()) + 3600,
    ) is False
```

`import time` đã có sẵn đầu file — không thêm.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_store.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_oauth_store.py` chứa các tên hàm: `test_claim_auth_code_single_use`, `test_claim_auth_code_missing`, `test_rotate_token_success`, `test_rotate_token_reuse_returns_false`, `test_rotate_token_unknown_refresh`
    - `cd Hub_All/mcp_service && uv run python -c "import re; s=open('tests/test_oauth_store.py',encoding='utf-8').read(); import sys; sys.exit(0 if len(re.findall(r'def test_', s)) >= 15 else 1)"` exits 0 (10 test cũ + 5 mới)
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_store.py -q` exits 0
    - `cd Hub_All/mcp_service && uv run ruff check tests/test_oauth_store.py` exits 0
  </acceptance_criteria>
  <done>5 test mới cho claim_auth_code (single-use + missing) và rotate_token (success + reuse False + unknown False) PASS; ruff sạch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_oauth_provider.py — test replay code, save_token lỗi, refresh reuse, pending hết hạn, PKCE sai</name>
  <read_first>
    - Hub_All/mcp_service/tests/test_oauth_provider.py (file đang sửa — helper _make_provider/_client_info/_login_data, pattern test, import fake_pending_authorize + OAuthStoreError)
    - Hub_All/mcp_service/mcp_app/oauth/provider.py (sau Plan 05 — exchange_authorization_code dùng claim_auth_code, exchange_refresh_token raise khi reuse, complete_authorization kiểm _PENDING_TTL)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-REVIEW.md (WR-01 — bối cảnh PKCE; quyết định fallback nếu SDK không verify)
  </read_first>
  <behavior>
    - test_exchange_code_replay_rejected: complete_authorization → load_authorization_code → exchange_authorization_code (lần 1 OK); gọi exchange_authorization_code LẦN 2 cùng AuthorizationCode → raise OAuthStoreError (code đã claim).
    - test_exchange_code_save_token_failure_no_replay: patch store.save_token raise lỗi; gọi exchange_authorization_code → raise; sau đó load_auth_code(code) trả None (code đã bị claim, KHÔNG replay được) — chứng minh CR-01.
    - test_exchange_refresh_token_reuse_rejected: exchange code → exchange_refresh_token lần 1 OK (rotate); gọi exchange_refresh_token LẦN 2 với refresh_obj CŨ → raise OAuthStoreError (reuse detected) — chứng minh CR-02.
    - test_complete_authorization_pending_expired: fake_pending_authorize với created_at = now - 700 (quá _PENDING_TTL 600) → complete_authorization raise OAuthStoreError.
    - test_pkce_wrong_verifier_rejected: chứng minh PKCE verify (WR-01). Xem hướng dẫn trong action.
  </behavior>
  <action>
Thêm 5 test vào CUỐI `Hub_All/mcp_service/tests/test_oauth_provider.py` (style:
`async def test_...(oauth_store)` trần; tái dùng helper `_make_provider`,
`_client_info`, `_login_data`, `fake_pending_authorize`). Cập nhật docstring đầu
file thêm mô tả test mới.

(A) test_exchange_code_replay_rejected:
```python
async def test_exchange_code_replay_rejected(oauth_store) -> None:
    """Dùng lại authorization code đã exchange → raise OAuthStoreError (CR-01)."""
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(oauth_store, txn="txn-replay")
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    auth_code = await provider.load_authorization_code(_client_info(), code)
    await provider.exchange_authorization_code(_client_info(), auth_code)
    # Lần 2 cùng code — code đã bị claim nguyên tử.
    with pytest.raises(OAuthStoreError):
        await provider.exchange_authorization_code(_client_info(), auth_code)
```

(B) test_exchange_code_save_token_failure_no_replay — dùng
`unittest.mock.patch` (thêm `from unittest.mock import patch` ở đầu file nếu
chưa có) patch `oauth_store.save_token` raise lỗi, xác nhận code KHÔNG còn replay:
```python
async def test_exchange_code_save_token_failure_no_replay(oauth_store) -> None:
    """save_token lỗi giữa chừng → code đã bị claim, KHÔNG replay được (CR-01)."""
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(oauth_store, txn="txn-savefail")
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    auth_code = await provider.load_authorization_code(_client_info(), code)

    async def _boom(*args, **kwargs):
        raise OAuthStoreError("save_token lỗi mô phỏng")

    with patch.object(oauth_store, "save_token", side_effect=_boom):
        with pytest.raises(OAuthStoreError):
            await provider.exchange_authorization_code(_client_info(), auth_code)
    # Code đã bị claim_auth_code xoá TRƯỚC save_token — không còn để replay.
    assert await oauth_store.load_auth_code(code) is None
    with pytest.raises(OAuthStoreError):
        await provider.exchange_authorization_code(_client_info(), auth_code)
```

(C) test_exchange_refresh_token_reuse_rejected:
```python
async def test_exchange_refresh_token_reuse_rejected(oauth_store) -> None:
    """Dùng lại refresh token đã rotate → raise OAuthStoreError reuse (CR-02)."""
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(oauth_store, txn="txn-reuse")
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    auth_code = await provider.load_authorization_code(_client_info(), code)
    first = await provider.exchange_authorization_code(_client_info(), auth_code)
    refresh_obj = await provider.load_refresh_token(
        _client_info(), first.refresh_token
    )
    # Rotate lần 1 — OK.
    await provider.exchange_refresh_token(_client_info(), refresh_obj, ["wiki"])
    # Rotate lần 2 với refresh_obj CŨ — reuse, phải raise.
    with pytest.raises(OAuthStoreError):
        await provider.exchange_refresh_token(_client_info(), refresh_obj, ["wiki"])
```

(D) test_complete_authorization_pending_expired:
```python
async def test_complete_authorization_pending_expired(oauth_store) -> None:
    """Pending transaction quá _PENDING_TTL → complete_authorization từ chối (WR-02)."""
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(
        oauth_store, txn="txn-expired", created_at=int(time.time()) - 700
    )
    with pytest.raises(OAuthStoreError):
        await provider.complete_authorization(txn, _login_data())
```

(E) test_pkce_wrong_verifier_rejected — WR-01. Mục tiêu: chứng minh PKCE thật
sự được verify. SDK `mcp` 1.27 verify PKCE ở route `/token` của SDK (provider
glue KHÔNG verify). Vì test provider gọi `exchange_authorization_code` trực
tiếp (bỏ qua route SDK), test này xác nhận TÍNH NHẤT QUÁN của `code_challenge`
end-to-end và đóng vai trò guard cho fallback. Triển khai:

1. Trong test, tạo cặp PKCE thật: `verifier = secrets.token_urlsafe(32)`,
   `challenge = base64.urlsafe_b64encode(hashlib.sha256(verifier.encode()).digest()).rstrip(b"=").decode()`
   (thêm `import base64, hashlib, secrets` đầu file nếu chưa có).
2. Seed pending với `code_challenge=challenge` qua `fake_pending_authorize(...,
   code_challenge=challenge)`; complete_authorization → load_authorization_code.
3. Khẳng định `auth_code.code_challenge == challenge` (challenge lưu nhất quán
   từ pending → code — guard chống regress mất code_challenge).
4. Kiểm tra hành vi SDK: gọi
   `cd Hub_All/mcp_service && uv run python -c "import mcp.server.auth.handlers.token as t; import inspect; src=inspect.getsource(t); print('VERIFY' if 'code_verifier' in src and 'sha256' in src else 'NOVERIFY')"`.
   - Nếu in `VERIFY` → SDK tự verify PKCE; test chỉ cần assert ở bước 3 +
     thêm comment ghi rõ "SDK mcp 1.27 verify PKCE — xác nhận qua inspect
     mcp.server.auth.handlers.token (chứa sha256+code_verifier)". Đặt tên test
     `test_pkce_challenge_persisted_and_sdk_verifies`.
   - Nếu in `NOVERIFY` → SDK KHÔNG verify: provider PHẢI tự verify. Trường hợp
     này KHÔNG hoàn tất bằng test đơn thuần — DỪNG và escalate: thêm vào SUMMARY
     mục "PKCE GAP — cần fix code" mô tả rằng `exchange_authorization_code` phải
     so khớp `base64url(sha256(code_verifier)) == code_challenge` và reject
     `code_challenge_method` != "S256" ở `authorize()` (provider.py:121 hard-code
     "S256"), rồi báo orchestrator cần plan bổ sung. (Theo RESEARCH.md SDK mcp
     1.27 verify PKCE — kỳ vọng nhánh VERIFY; nhánh NOVERIFY là phòng vệ.)

Test (E) viết theo nhánh xác nhận được — pattern khung:
```python
async def test_pkce_challenge_persisted_and_sdk_verifies(oauth_store) -> None:
    """code_challenge lưu nhất quán pending→code; xác nhận SDK verify PKCE (WR-01)."""
    verifier = secrets.token_urlsafe(32)
    challenge = (
        base64.urlsafe_b64encode(hashlib.sha256(verifier.encode()).digest())
        .rstrip(b"=")
        .decode()
    )
    provider = _make_provider(oauth_store)
    txn = await fake_pending_authorize(
        oauth_store, txn="txn-pkce", code_challenge=challenge
    )
    code, _r, _s = await provider.complete_authorization(txn, _login_data())
    auth_code = await provider.load_authorization_code(_client_info(), code)
    # code_challenge phải đi nguyên vẹn từ pending sang authorization code.
    assert auth_code.code_challenge == challenge
```
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_oauth_provider.py` chứa các tên hàm: `test_exchange_code_replay_rejected`, `test_exchange_code_save_token_failure_no_replay`, `test_exchange_refresh_token_reuse_rejected`, `test_complete_authorization_pending_expired`, và một test PKCE (`test_pkce_challenge_persisted_and_sdk_verifies` hoặc tương đương)
    - `test_oauth_provider.py` chứa ít nhất 4 lần `pytest.raises(OAuthStoreError)` trong các test mới
    - `cd Hub_All/mcp_service && uv run python -c "import re; s=open('tests/test_oauth_provider.py',encoding='utf-8').read(); import sys; sys.exit(0 if len(re.findall(r'def test_', s)) >= 16 else 1)"` exits 0 (11 test cũ + 5 mới)
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_provider.py -q` exits 0
    - `cd Hub_All/mcp_service && uv run ruff check tests/test_oauth_provider.py` exits 0
  </acceptance_criteria>
  <done>5 test mới phủ code replay, save_token lỗi không-replay, refresh token reuse, pending hết hạn, PKCE challenge nhất quán + xác nhận SDK verify; tất cả PASS. Nếu nhánh NOVERIFY phát hiện SDK không verify PKCE → ghi mục escalate trong SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Regression gate đầy đủ — toàn bộ suite mcp_service xanh</name>
  <read_first>
    - Hub_All/mcp_service/tests/ (toàn thư mục test — kiểm số test trước/sau)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-VERIFICATION.md (mốc baseline 78/78 PASS)
  </read_first>
  <action>
Chạy regression gate cuối cùng cho gap closure Phase 8.3:

```
cd Hub_All/mcp_service && uv run pytest -q
```

Baseline trước gap closure: 78 test PASS. Sau Plan 05 + Plan 06: dự kiến
78 + 10 (5 store + 5 provider) = 88 test, tất cả PASS.

Yêu cầu:
- pytest exit 0 — KHÔNG có test FAIL/ERROR.
- Nếu một test CŨ vỡ: KHÔNG xoá test, KHÔNG nới lỏng assertion để "qua". Xác
  định nguyên nhân: nếu test cũ kiểm chính lỗi đang vá (ví dụ test cũ giả định
  `update_oauth_token` rotate được mọi lúc) → cập nhật test cũ cho khớp hành vi
  đúng đắn RFC, ghi rõ trong SUMMARY. Nếu vỡ vì lý do khác → sửa code cho đúng.
- Chạy `cd Hub_All/mcp_service && uv run pytest -q -m critical` xác nhận test
  critical (`test_token_valid/expired/wrong_scope`, `test_metadata_endpoint`)
  vẫn PASS.

Lưu ý regression API service: gap closure này CHỈ chạm `mcp_service/` — KHÔNG
sửa file nào trong `api/`. API regression (119/119) không thể bị ảnh hưởng nên
KHÔNG cần chạy lại; ghi nhận điều này trong SUMMARY.

Cập nhật SUMMARY ghi: số test trước (78) → sau (kỳ vọng 88); xác nhận SC3
chuyển FAILED → VERIFIED (code replay + refresh reuse + pending hết hạn nay có
test chứng minh bị từ chối).
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/mcp_service && uv run pytest -q` exits 0
    - `cd Hub_All/mcp_service && uv run pytest -q -m critical` exits 0
    - Output pytest cho thấy >= 88 test passed, 0 failed, 0 error
  </acceptance_criteria>
  <done>Toàn bộ suite mcp_service PASS (>= 88 test); test critical PASS; SC3 có bằng chứng test chuyển sang VERIFIED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Mô tả |
|----------|-------|
| Claude web (OAuth client) → MCP Service `/token` | Code/refresh token client gửi — test mô phỏng kẻ tấn công replay/reuse |
| OAuth client → MCP Service `/authorize` (PKCE) | code_challenge/code_verifier — test xác nhận liên kết PKCE không bị mất |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-27 | Spoofing/Elevation | `exchange_authorization_code` — authorization code replay | mitigate | Task 1+2: test `test_claim_auth_code_single_use`, `test_exchange_code_replay_rejected`, `test_exchange_code_save_token_failure_no_replay` chứng minh code single-use kể cả khi save_token lỗi — bằng chứng test cho fix CR-01 |
| T-08.3-28 | Elevation/Tampering | `exchange_refresh_token` — refresh token double-spend | mitigate | Task 1+2: test `test_rotate_token_reuse_returns_false`, `test_exchange_refresh_token_reuse_rejected` chứng minh refresh token reuse bị phát hiện qua rowcount → raise — bằng chứng test cho fix CR-02 |
| T-08.3-29 | Elevation | `complete_authorization` — pending txn replay window | mitigate | Task 2: test `test_complete_authorization_pending_expired` chứng minh pending quá _PENDING_TTL bị từ chối — bằng chứng test cho fix WR-02 |
| T-08.3-30 | Spoofing | PKCE bypass — `code_verifier` không được verify | mitigate | Task 2 test (E): inspect `mcp.server.auth.handlers.token` xác nhận SDK mcp 1.27 verify PKCE (sha256 + code_verifier); test assert code_challenge đi nguyên vẹn pending→code (guard chống regress). Nhánh NOVERIFY → escalate báo cần fix provider (WR-01) |
| T-08.3-31 | Tampering | Test giả định sai → fix regress âm thầm | mitigate | Task 3: regression gate đầy đủ + test critical; cấm nới lỏng assertion để "qua" — test cũ vỡ phải sửa cho đúng RFC |
</threat_model>

<verification>
- 5 test mới trong test_oauth_store.py (claim_auth_code x2, rotate_token x3) PASS.
- 5 test mới trong test_oauth_provider.py (code replay, save_token lỗi, refresh reuse, pending hết hạn, PKCE) PASS.
- `cd Hub_All/mcp_service && uv run pytest -q` exits 0, >= 88 test passed.
- `cd Hub_All/mcp_service && uv run pytest -q -m critical` exits 0.
- `cd Hub_All/mcp_service && uv run ruff check tests/` exits 0.
</verification>

<success_criteria>
- Gap `missing` #3 trong VERIFICATION.md (test replay/đồng thời) đã đóng — có test phủ dùng lại code đã exchange + dùng lại refresh token đã rotate.
- save_token lỗi giữa chừng có test chứng minh code không replay được.
- Pending hết hạn có test chứng minh bị từ chối.
- PKCE: xác nhận SDK verify (hoặc escalate nếu không).
- Regression mcp_service >= 88 test PASS; test critical PASS.
- SC3 chuyển FAILED → VERIFIED có bằng chứng test.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-06-SUMMARY.md`.
Commit atomic: `test(08.3-06): thêm test replay/reuse/PKCE đóng gap SC3 token lifecycle`.
</output>
