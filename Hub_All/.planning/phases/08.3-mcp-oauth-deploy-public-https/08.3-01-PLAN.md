---
phase: 08.3-mcp-oauth-deploy-public-https
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Hub_All/mcp_service/pyproject.toml
  - Hub_All/mcp_service/mcp_app/config.py
  - Hub_All/mcp_service/.env.example
  - Hub_All/mcp_service/mcp_app/oauth/__init__.py
  - Hub_All/mcp_service/mcp_app/oauth/store.py
  - Hub_All/mcp_service/tests/conftest.py
  - Hub_All/mcp_service/tests/test_oauth_store.py
autonomous: true
requirements: [MCP-01]
must_haves:
  truths:
    - "MCP Service đọc được config MCP_OAUTH_ISSUER_URL và MCP_OAUTH_STATE_DB_PATH"
    - "Issuer URL bị ép scheme http/https — localhost http vẫn được cho dev, không có scheme bị reject"
    - "OAuth state store khởi tạo 4 bảng SQLite (oauth_clients, oauth_auth_codes, oauth_tokens, oauth_pending) lúc startup"
    - "Store ghi rồi đọc lại được client / authorization code / token / pending-authorize transaction (CRUD roundtrip qua SQLite file)"
    - "Test fixture OAuth store dùng SQLite :memory: sẵn sàng cho các plan sau; có helper fake_pending_authorize seed bản ghi oauth_pending"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/oauth/store.py"
      provides: "OAuthStore — SQLite persistence cho clients/auth_codes/tokens/pending"
      contains: "class OAuthStore"
      min_lines: 140
    - path: "Hub_All/mcp_service/mcp_app/config.py"
      provides: "Settings mở rộng — oauth_issuer_url, oauth_state_db_path"
      contains: "oauth_issuer_url"
    - path: "Hub_All/mcp_service/mcp_app/oauth/__init__.py"
      provides: "package init cho mcp_app.oauth"
    - path: "Hub_All/mcp_service/tests/test_oauth_store.py"
      provides: "test CRUD roundtrip cho OAuthStore"
      contains: "OAuthStore"
  key_links:
    - from: "Hub_All/mcp_service/mcp_app/oauth/store.py"
      to: "aiosqlite"
      via: "async SQLite connection"
      pattern: "import aiosqlite"
    - from: "Hub_All/mcp_service/mcp_app/config.py"
      to: "MCP_OAUTH_STATE_DB_PATH"
      via: "pydantic-settings env field"
      pattern: "oauth_state_db_path"
---

<objective>
Dựng nền tảng cho lớp OAuth của MCP Service: thêm dependency (`aiosqlite`, `uvicorn`), mở rộng `Settings` với field OAuth, và tạo `OAuthStore` — lớp persistence SQLite cho toàn bộ OAuth state (registered clients DCR, authorization codes, OAuth tokens kèm downstream JWT, pending-authorize transactions). Đồng thời tạo Wave 0 test scaffolding (fixture OAuth store + helper seed pending + `test_oauth_store.py`).

Purpose: Phase 8.2 cố ý KHÔNG cho MCP Service chạm DB/Redis chung. OAuth cần state bền (sống sót restart — RESEARCH.md Pitfall 3) → SQLite file cục bộ là lựa chọn ít-ma-sát. Plan 02 (provider) và Plan 03 (forward JWT) đều dựng trên store này, nên store + config phải xong trước. Đặc biệt: bảng `oauth_pending` lưu tham số của bước `authorize()` giữa lúc Claude gọi `/authorize` và lúc user submit login form — Plan 02 Task 2/3 consume bảng này, nên schema PHẢI được author tại Plan 01 (KHÔNG để Plan 02 Edit ngược lại plan Wave 1 đã merge).
Output: `mcp_app/oauth/` package với `store.py`, `Settings` mở rộng, deps pinned, fixture test + helper seed pending.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-CONTEXT.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md

<interfaces>
<!-- Quy ước codebase mcp_app — executor phải replicate, KHÔNG khám phá lại. -->

config.py hiện tại — Settings (pydantic-settings, env_prefix "MCP_"):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="MCP_", env_file=".env", ...)
    api_base_url: str = "http://localhost:8180"
    service_host: str = "0.0.0.0"
    service_port: int = 8190
    http_timeout: float = 30.0

    @field_validator("api_base_url", mode="after")
    @classmethod
    def _validate_base_url(cls, value: str) -> str:
        stripped = value.rstrip("/")
        parsed = urlparse(stripped)
        if parsed.scheme not in ("http", "https"):
            raise ValueError("MCP_API_BASE_URL phải dùng scheme http hoặc https ...")
        if not parsed.netloc:
            raise ValueError("MCP_API_BASE_URL thiếu host")
        return stripped

@functools.lru_cache
def get_settings() -> Settings: ...
```

api_client.py — mẫu class async độc lập + cây exception + lifecycle:
```python
class ApiClientError(Exception): ...        # exception gốc
async def aclose(self) -> None: ...          # đóng resource
async def __aenter__(self) -> ApiClient: ...
async def __aexit__(self, exc_type, exc, tb) -> None: ...
```

conftest.py hiện tại có 3 fixture: `mock_ctx`, `reset_api_client` (autouse), `api_base_url`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm dependency aiosqlite + uvicorn, verify pin, tạo uv.lock</name>
  <files>Hub_All/mcp_service/pyproject.toml</files>
  <read_first>
    - Hub_All/mcp_service/pyproject.toml (bản hiện tại — thấy block [project].dependencies + [project.optional-dependencies].dev)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Standard Stack" — version verification + §Pitfall 1)
  </read_first>
  <behavior>
    - `uv pip index versions mcp` xác nhận resolve trong khoảng `>=1.27.0,<1.28` (KHÔNG để lên 1.28+).
    - `uv.lock` tồn tại sau `uv lock` và pin `aiosqlite` + `uvicorn` ở version cụ thể trong khoảng cho phép.
  </behavior>
  <action>
    1. Chạy `cd Hub_All/mcp_service && uv pip index versions mcp aiosqlite uvicorn` — ghi lại version mới nhất của `aiosqlite` và `uvicorn` trong khoảng cho phép.
    2. Sửa `[project].dependencies` của `pyproject.toml` — thêm 2 dòng (GIỮ nguyên `mcp>=1.27.0,<1.28`, `httpx`, `pydantic`, `pydantic-settings`):
       - `"aiosqlite>=0.20,<1",  # OAuth state store cục bộ — KHÔNG chạm DB chung (Phase 8.2 boundary)`
       - `"uvicorn>=0.30,<1",  # ASGI server — khai báo tường minh cho Dockerfile CMD --factory`
       Nếu `uv pip index versions` cho thấy version mới nhất ngoài khoảng `<1` (vd aiosqlite ra 1.x), điều chỉnh upper bound cho khớp version thật + ghi lý do trong comment.
    3. KHÔNG thêm package `fastmcp` (gofastmcp.com — RESEARCH.md Pitfall 1). KHÔNG bump `mcp` (P19).
    4. Chạy `cd Hub_All/mcp_service && uv lock` để sinh `uv.lock` (file này CHƯA tồn tại — cần cho Dockerfile `COPY ... uv.lock` ở Plan 04).
    5. Chạy `cd Hub_All/mcp_service && uv sync` để cài deps.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv lock --check && python -c "import aiosqlite, uvicorn; print('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "aiosqlite" Hub_All/mcp_service/pyproject.toml` trả về dòng trong `[project].dependencies`.
    - `grep "uvicorn" Hub_All/mcp_service/pyproject.toml` trả về dòng trong `[project].dependencies`.
    - `grep 'mcp>=1.27.0,<1.28' Hub_All/mcp_service/pyproject.toml` vẫn tồn tại (pin KHÔNG đổi).
    - File `Hub_All/mcp_service/uv.lock` tồn tại.
    - `grep -c "fastmcp" Hub_All/mcp_service/pyproject.toml` trả về 0 (KHÔNG có package fastmcp bên thứ ba).
  </acceptance_criteria>
  <done>aiosqlite + uvicorn pinned trong dependencies, uv.lock sinh ra, mcp pin giữ nguyên, deps cài được.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mở rộng Settings với field OAuth + validator issuer URL</name>
  <files>Hub_All/mcp_service/mcp_app/config.py, Hub_All/mcp_service/.env.example</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/config.py (bản hiện tại — `Settings` + `_validate_base_url` + `get_settings`)
    - Hub_All/mcp_service/.env.example (bản hiện tại)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§config.py — pattern field + validator)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§Pitfall 6 — issuer URL localhost làm hỏng discovery)
  </read_first>
  <behavior>
    - `MCP_OAUTH_ISSUER_URL=https://mcp.example.com` → `Settings().oauth_issuer_url == "https://mcp.example.com"` (trailing slash strip).
    - `MCP_OAUTH_ISSUER_URL=ftp://x` → raise ValueError (scheme không phải http/https).
    - `MCP_OAUTH_ISSUER_URL=https://` (thiếu host) → raise ValueError.
    - `oauth_state_db_path` default = `.oauth/state.db` (tương đối — native dev); override tuyệt đối qua env cho Docker.
    - `oauth_access_token_ttl` default 3600 (1h), `oauth_refresh_token_ttl` default 2592000 (30 ngày) — RESEARCH.md Open Question 2.
    - `oauth_issuer_url` có DEFAULT `http://localhost:8190` → `Settings()` KHÔNG raise khi thiếu env. Đây là quyết định cố ý: route `/.well-known/oauth-authorization-server` trả 200 ngay cả khi operator chưa set domain thật → cho phép Dockerfile HEALTHCHECK (Plan 04) probe well-known route một cách tất định.
  </behavior>
  <action>
    Trong `config.py`, thêm vào class `Settings` (SAU `http_timeout`, GIỮ nguyên field cũ + validator `_validate_base_url`):
    ```python
    # --- OAuth (Phase 8.3) ---
    # URL public HTTPS mà Claude web truy cập — dùng làm issuer trong OAuth metadata.
    # Dev có thể để http://localhost:8190; prod PHẢI https (Pitfall 6).
    # CÓ DEFAULT cố ý: Settings() không raise khi thiếu env → well-known route trả 200
    # ngay từ lúc start (Dockerfile HEALTHCHECK Plan 04 dựa vào tính tất định này).
    oauth_issuer_url: str = "http://localhost:8190"
    # Đường dẫn file SQLite lưu OAuth state. Tương đối cho native dev,
    # override tuyệt đối (vd /app/.oauth/state.db) cho Docker volume.
    oauth_state_db_path: str = ".oauth/state.db"
    # Lifetime token OAuth do MCP Service tự phát (giây).
    oauth_access_token_ttl: int = 3600       # 1 giờ
    oauth_refresh_token_ttl: int = 2592000   # 30 ngày
    ```
    Thêm validator (copy cấu trúc `_validate_base_url`, mode="after"):
    ```python
    @field_validator("oauth_issuer_url", mode="after")
    @classmethod
    def _validate_issuer_url(cls, value: str) -> str:
        """Validate issuer URL: strip trailing slash, ép scheme http/https, bắt buộc host.

        OAuth metadata `issuer` PHẢI là URL Claude web truy cập được — issuer sai
        làm discovery + redirect hỏng (Pitfall 6).
        """
        stripped = value.rstrip("/")
        parsed = urlparse(stripped)
        if parsed.scheme not in ("http", "https"):
            raise ValueError(
                "MCP_OAUTH_ISSUER_URL phải dùng scheme http hoặc https"
            )
        if not parsed.netloc:
            raise ValueError("MCP_OAUTH_ISSUER_URL thiếu host")
        return stripped
    ```
    Cập nhật `.env.example` — thêm sau block hiện có:
    ```
    # --- OAuth (Phase 8.3) ---
    # URL public HTTPS để Claude web kết nối connector. Prod PHẢI https.
    MCP_OAUTH_ISSUER_URL=http://localhost:8190
    # File SQLite lưu OAuth state (clients/codes/tokens/pending). Docker: override /app/.oauth/state.db.
    MCP_OAUTH_STATE_DB_PATH=.oauth/state.db
    # Lifetime token OAuth (giây).
    MCP_OAUTH_ACCESS_TOKEN_TTL=3600
    MCP_OAUTH_REFRESH_TOKEN_TTL=2592000
    ```
    Cập nhật module docstring của `config.py` — thêm dòng nói Phase 8.3 thêm field OAuth.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run python -c "from mcp_app.config import Settings; s=Settings(); assert s.oauth_issuer_url and s.oauth_state_db_path; print('config ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "oauth_issuer_url" Hub_All/mcp_service/mcp_app/config.py` xuất hiện trong field + validator.
    - `grep "oauth_state_db_path" Hub_All/mcp_service/mcp_app/config.py` tồn tại.
    - `grep "_validate_issuer_url" Hub_All/mcp_service/mcp_app/config.py` tồn tại.
    - `grep "MCP_OAUTH_ISSUER_URL" Hub_All/mcp_service/.env.example` tồn tại.
    - `cd Hub_All/mcp_service && uv run python -c "from mcp_app.config import Settings; Settings()"` chạy KHÔNG lỗi khi không set MCP_OAUTH_ISSUER_URL (default kích hoạt — exit 0).
    - `cd Hub_All/mcp_service && MCP_OAUTH_ISSUER_URL=ftp://x uv run python -c "from mcp_app.config import Settings; Settings()"` thoát với lỗi ValueError (exit code != 0).
  </acceptance_criteria>
  <done>Settings có 4 field OAuth, validator ép scheme http/https issuer, oauth_issuer_url có default an toàn, .env.example cập nhật.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tạo package oauth/ + OAuthStore SQLite (4 bảng) + Wave 0 test scaffolding</name>
  <files>Hub_All/mcp_service/mcp_app/oauth/__init__.py, Hub_All/mcp_service/mcp_app/oauth/store.py, Hub_All/mcp_service/tests/conftest.py, Hub_All/mcp_service/tests/test_oauth_store.py</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/api_client.py (analog cho store — class async + cây exception + aclose + __aenter__/__aexit__)
    - Hub_All/mcp_service/mcp_app/__init__.py (mẫu package init tối giản)
    - Hub_All/mcp_service/tests/conftest.py (bản hiện tại — 3 fixture, pattern monkeypatch + cache_clear)
    - Hub_All/mcp_service/tests/test_api_client.py (mẫu test async + respx; ở đây chỉ cần phần async, không cần respx)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-RESEARCH.md (§"Runtime State Inventory" — bảng OAuth; §Pitfall 3)
    - Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-PATTERNS.md (§store.py — cây exception, lifecycle async, schema startup)
  </read_first>
  <behavior>
    - `OAuthStore.init_schema()` tạo 4 bảng nếu chưa có; gọi 2 lần không lỗi (CREATE TABLE IF NOT EXISTS).
    - `save_client(client)` rồi `get_client(client_id)` trả lại đúng client (DCR roundtrip).
    - `get_client("không-tồn-tại")` trả None.
    - `save_auth_code(...)` rồi `load_auth_code(code)` trả record kèm downstream JWT + refresh đã bind; `delete_auth_code(code)` xoá → load lại trả None (single-use).
    - `save_token(...)` rồi `load_token(token)` trả record; record chứa `downstream_jwt` + `downstream_refresh_token` + `expires_at`.
    - `update_downstream_jwt(oauth_token, new_access, new_refresh)` ghi đè cả 2 field (Pitfall 5 — refresh rotation).
    - `delete_token(token)` xoá → load trả None (hỗ trợ revoke).
    - `save_pending(txn, ...)` rồi `load_pending(txn)` trả lại đủ tham số authorize (`code_challenge`, `code_challenge_method`, `redirect_uri`, `client_state`, `client_id`, `scopes`); `delete_pending(txn)` xoá → load lại None (pending dùng 1 lần khi login callback nối lại flow).
  </behavior>
  <action>
    **`mcp_app/oauth/__init__.py`** — package init tối giản, export `OAuthStore`:
    ```python
    """Package OAuth cho MCP Service — Phase 8.3 (MCP-01)."""
    from mcp_app.oauth.store import OAuthStore

    __all__ = ["OAuthStore"]
    ```

    **`mcp_app/oauth/store.py`** — lớp `OAuthStore`. Mở đầu bằng module docstring tiếng Việt (mẫu `api_client.py` lines 1-12): nói Phase 8.3, vai trò persistence OAuth, KHÁC Phase 8.2 ở chỗ MCP Service nay có store cục bộ SQLite (KHÔNG chạm DB chung), lưu ý bảo mật KHÔNG log token/JWT/code. `from __future__ import annotations`. `logger = logging.getLogger(__name__)`.

    Cây exception riêng:
    ```python
    class OAuthStoreError(Exception):
        """Lỗi gốc khi thao tác OAuth state store."""
    ```

    Class `OAuthStore`:
    - `__init__(self, db_path: str)` — lưu `db_path`, KHÔNG mở connection ngay (lazy).
    - `async def init_schema(self) -> None` — mở `aiosqlite.connect(db_path)`, chạy 4 `CREATE TABLE IF NOT EXISTS`:
      ```sql
      CREATE TABLE IF NOT EXISTS oauth_clients (
        client_id TEXT PRIMARY KEY,
        client_metadata TEXT NOT NULL,   -- JSON OAuthClientInformationFull
        created_at INTEGER NOT NULL
      );
      CREATE TABLE IF NOT EXISTS oauth_auth_codes (
        code TEXT PRIMARY KEY,
        client_id TEXT NOT NULL,
        code_payload TEXT NOT NULL,      -- JSON AuthorizationCode (scopes, redirect_uri, challenge...)
        downstream_jwt TEXT NOT NULL,    -- JWT RS256 access token API Service
        downstream_refresh_token TEXT NOT NULL,
        user_payload TEXT NOT NULL,      -- JSON user info từ /api/auth/login
        expires_at INTEGER NOT NULL      -- Unix ts — code TTL ngắn ~600s
      );
      CREATE TABLE IF NOT EXISTS oauth_tokens (
        access_token TEXT PRIMARY KEY,
        refresh_token TEXT,
        client_id TEXT NOT NULL,
        scopes TEXT NOT NULL,            -- JSON list
        downstream_jwt TEXT NOT NULL,
        downstream_refresh_token TEXT NOT NULL,
        user_payload TEXT NOT NULL,
        expires_at INTEGER NOT NULL      -- Unix ts OAuth access token
      );
      -- Lưu tham số authorize() giữa lúc /authorize và lúc user submit login form.
      -- `txn` là state ngẫu nhiên do authorize() sinh; login callback (Plan 02 Task 3)
      -- dùng txn này để nối lại flow + biết redirect_uri/code_challenge của client.
      CREATE TABLE IF NOT EXISTS oauth_pending (
        txn TEXT PRIMARY KEY,
        client_id TEXT NOT NULL,
        redirect_uri TEXT NOT NULL,           -- redirect_uri client đã DCR khai
        code_challenge TEXT NOT NULL,         -- PKCE challenge
        code_challenge_method TEXT NOT NULL,  -- thường "S256"
        client_state TEXT,                    -- OAuth `state` param của client (Claude web)
        scopes TEXT NOT NULL,                 -- JSON list scope yêu cầu
        created_at INTEGER NOT NULL           -- Unix ts — TTL ngắn để cleanup pending bỏ dở
      );
      ```
      Đảm bảo thư mục cha của `db_path` tồn tại (`os.makedirs(os.path.dirname(...), exist_ok=True)` nếu có dirname).
    - Methods CRUD (mọi method async, dùng `aiosqlite`, parametrized query — KHÔNG string interpolation):
      - `save_client(client_id: str, client_metadata: dict) -> None` — INSERT OR REPLACE; serialize metadata bằng `json.dumps`.
      - `get_client(client_id: str) -> dict | None` — SELECT; `json.loads` metadata; None nếu không có.
      - `save_auth_code(code, client_id, code_payload: dict, downstream_jwt, downstream_refresh_token, user_payload: dict, expires_at: int) -> None`.
      - `load_auth_code(code: str) -> dict | None` — trả dict với keys `client_id, code_payload, downstream_jwt, downstream_refresh_token, user_payload, expires_at`.
      - `delete_auth_code(code: str) -> None` — DELETE (single-use).
      - `save_token(access_token, refresh_token, client_id, scopes: list, downstream_jwt, downstream_refresh_token, user_payload: dict, expires_at: int) -> None`.
      - `load_token(access_token: str) -> dict | None`.
      - `load_token_by_refresh(refresh_token: str) -> dict | None` — SELECT WHERE refresh_token = ?.
      - `update_downstream_jwt(access_token: str, new_jwt: str, new_refresh: str) -> None` — UPDATE ghi đè CẢ `downstream_jwt` và `downstream_refresh_token` (Pitfall 5).
      - `update_oauth_token(old_access, new_access, new_refresh, expires_at) -> None` — dùng khi exchange_refresh_token rotate token OAuth.
      - `delete_token(access_token: str) -> None` — DELETE (revoke).
      - `save_pending(txn: str, client_id: str, redirect_uri: str, code_challenge: str, code_challenge_method: str, client_state: str | None, scopes: list, created_at: int) -> None` — INSERT vào `oauth_pending`; serialize `scopes` bằng `json.dumps`.
      - `load_pending(txn: str) -> dict | None` — SELECT từ `oauth_pending`; trả dict keys `client_id, redirect_uri, code_challenge, code_challenge_method, client_state, scopes, created_at` (`scopes` đã `json.loads`); None nếu không có.
      - `delete_pending(txn: str) -> None` — DELETE từ `oauth_pending` (pending dùng 1 lần sau khi login callback nối lại flow).
      - `cleanup_expired(now: int) -> None` — DELETE từ `oauth_auth_codes` WHERE expires_at < now; ĐỒNG THỜI DELETE từ `oauth_pending` WHERE created_at < now - 600 (dọn pending bỏ dở quá 10 phút).
    - `async def aclose(self) -> None` — đóng connection nếu đang mở.
    - `__aenter__` / `__aexit__` — copy mẫu `ApiClient`.
    - Logging: chỉ log `client_id` / số bản ghi / lỗi loại exception — KHÔNG log token/JWT/code (T-08.2-01-I).
    Đề xuất giữ 1 connection mở sau `init_schema()` (SQLite async, single-file) — hoặc mở/đóng mỗi method nếu đơn giản hơn. Executor chọn cách nhất quán, KHÔNG block event loop.

    **`tests/conftest.py`** (MODIFY) — GIỮ 3 fixture cũ. Thêm fixture + helper:
    ```python
    @pytest.fixture
    async def oauth_store():
        """OAuthStore tạm trên SQLite :memory: — schema khởi tạo sẵn."""
        from mcp_app.oauth.store import OAuthStore
        store = OAuthStore(db_path=":memory:")
        await store.init_schema()
        yield store
        await store.aclose()


    async def fake_pending_authorize(store, *, txn="txn-test", client_id="client-test",
                                     redirect_uri="https://claude.ai/api/mcp/auth_callback",
                                     code_challenge="challenge-test",
                                     code_challenge_method="S256",
                                     client_state="client-state-test",
                                     scopes=None, created_at=None):
        """Seed 1 bản ghi oauth_pending để test login callback nối lại flow.

        Trả về `txn` đã seed. Dùng ở Plan 02 Task 3 (test_login_callback_success_redirects)
        và bất kỳ test nào cần pending-authorize params có sẵn trong store.
        """
        import time
        await store.save_pending(
            txn=txn, client_id=client_id, redirect_uri=redirect_uri,
            code_challenge=code_challenge, code_challenge_method=code_challenge_method,
            client_state=client_state, scopes=scopes or ["wiki"],
            created_at=created_at if created_at is not None else int(time.time()),
        )
        return txn
    ```
    Lưu ý: `:memory:` SQLite cần GIỮ 1 connection xuyên suốt (đóng connection = mất DB). Nếu `OAuthStore` mở/đóng connection mỗi method, fixture phải dùng `tmp_path` file thật thay vì `:memory:`. Executor chọn phù hợp với thiết kế store ở trên — nếu store mở connection 1 lần ở `init_schema()` và giữ, `:memory:` chạy được.

    **`tests/test_oauth_store.py`** (NEW) — module docstring tiếng Việt liệt kê behavior test. Test phủ (dùng fixture `oauth_store`):
    - `test_init_schema_idempotent` — gọi `init_schema()` lần 2 không lỗi.
    - `test_client_roundtrip` — `save_client` → `get_client` trả đúng; `get_client` id lạ → None.
    - `test_auth_code_roundtrip_single_use` — `save_auth_code` → `load_auth_code` trả đủ field downstream JWT + refresh; `delete_auth_code` → `load_auth_code` trả None.
    - `test_token_roundtrip` — `save_token` → `load_token` trả record với `downstream_jwt`.
    - `test_load_token_by_refresh` — lưu token có refresh → `load_token_by_refresh` tìm được.
    - `test_update_downstream_jwt_rotates_both` — sau `update_downstream_jwt`, CẢ `downstream_jwt` và `downstream_refresh_token` đổi giá trị mới (Pitfall 5 regression).
    - `test_delete_token` — `delete_token` → `load_token` trả None.
    - `test_pending_roundtrip` — `save_pending` → `load_pending` trả đủ field (`code_challenge`, `redirect_uri`, `client_state`, `scopes`...); `delete_pending` → `load_pending` trả None.
    - `test_pending_helper_seeds` — gọi `fake_pending_authorize(oauth_store)` → `load_pending("txn-test")` trả bản ghi đúng (chứng minh helper conftest hoạt động cho Plan 02).
    - `test_cleanup_expired` — lưu 1 code expires_at quá khứ + 1 tương lai + 1 pending created_at quá cũ (>600s) → `cleanup_expired(now)` → chỉ code tương lai còn, pending cũ bị xoá.
    Đánh `@pytest.mark.critical` cho `test_token_roundtrip` + `test_update_downstream_jwt_rotates_both`.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_oauth_store.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/mcp_service/mcp_app/oauth/__init__.py` và `oauth/store.py` tồn tại.
    - `grep "class OAuthStore" Hub_All/mcp_service/mcp_app/oauth/store.py` tồn tại.
    - `grep -E "oauth_clients|oauth_auth_codes|oauth_tokens|oauth_pending" Hub_All/mcp_service/mcp_app/oauth/store.py` trả về cả 4 tên bảng.
    - `grep -E "save_pending|load_pending|delete_pending" Hub_All/mcp_service/mcp_app/oauth/store.py` — đủ 3 method pending.
    - `grep "update_downstream_jwt" Hub_All/mcp_service/mcp_app/oauth/store.py` tồn tại.
    - `grep "oauth_store" Hub_All/mcp_service/tests/conftest.py` — fixture mới có mặt.
    - `grep "fake_pending_authorize" Hub_All/mcp_service/tests/conftest.py` — helper seed pending có mặt.
    - `cd Hub_All/mcp_service && uv run pytest tests/test_oauth_store.py` — toàn bộ test PASS.
    - `cd Hub_All/mcp_service && uv run pytest` — regression: toàn bộ test cũ vẫn PASS.
    - `cd Hub_All/mcp_service && uv run ruff check mcp_app/oauth tests/test_oauth_store.py` — clean.
  </acceptance_criteria>
  <done>Package oauth/ tồn tại, OAuthStore CRUD 4 bảng SQLite (gồm oauth_pending) hoạt động + test PASS, fixture oauth_store + helper fake_pending_authorize sẵn sàng cho Plan 02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP Service process → SQLite file | OAuth state (clients/codes/tokens/pending kèm downstream JWT) được ghi xuống file cục bộ — file chứa secret nhạy cảm |
| env / .env → Settings | Config (issuer URL) là input cần validate trước khi dùng làm OAuth metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-01 | Information Disclosure | `oauth/store.py` log | mitigate | KHÔNG log giá trị token/JWT/code — chỉ log client_id/status/loại exception (T-08.2-01-I pattern). Acceptance criteria kiểm grep. |
| T-08.3-02 | Tampering | `config.py` issuer URL | mitigate | `_validate_issuer_url` ép scheme http/https + bắt buộc host — issuer sai làm discovery/redirect hỏng (Pitfall 6). |
| T-08.3-03 | Information Disclosure | SQLite state file chứa downstream JWT | accept | File nằm trong volume mount/host filesystem của operator; bảo vệ ở tầng OS/Docker volume permissions. MCP Service container chạy non-root (Plan 04 chown thư mục `.oauth`). Không mã hoá at-rest ở phase này — low-value khi host đã được trust. |
| T-08.3-04 | Tampering | `oauth/store.py` SQL queries | mitigate | Mọi query dùng parametrized placeholder `?` — KHÔNG string interpolation → chống SQL injection từ client_id/code/txn do client cung cấp. |
| T-08.3-04b | Tampering | `oauth_pending` lưu redirect_uri/PKCE challenge | mitigate | `redirect_uri` + `code_challenge` lưu lúc `authorize()` (từ client đã DCR) — login callback đọc lại từ pending, KHÔNG nhận redirect tuỳ ý. `txn` là `secrets.token_urlsafe` ngẫu nhiên — khó đoán. `cleanup_expired` dọn pending bỏ dở. |
</threat_model>

<verification>
- `cd Hub_All/mcp_service && uv run pytest` — toàn bộ suite PASS (test cũ + test_oauth_store.py mới).
- `cd Hub_All/mcp_service && uv run ruff check .` — clean.
- `uv lock --check` — lockfile đồng bộ với pyproject.
- `grep -rE "logger.*(token|jwt|code|password)" Hub_All/mcp_service/mcp_app/oauth/store.py` — KHÔNG có dòng log giá trị secret (chỉ tên biến trong message tĩnh được phép, không log giá trị).
</verification>

<success_criteria>
- `aiosqlite` + `uvicorn` pinned, `uv.lock` tồn tại, `mcp` pin giữ `>=1.27.0,<1.28`.
- `Settings` có `oauth_issuer_url` (validated http/https, có default an toàn), `oauth_state_db_path`, `oauth_access_token_ttl`, `oauth_refresh_token_ttl`.
- `OAuthStore` khởi tạo 4 bảng SQLite (gồm `oauth_pending`) + CRUD roundtrip clients/codes/tokens/pending hoạt động.
- `update_downstream_jwt` ghi đè cả access + refresh (Pitfall 5).
- Fixture `oauth_store` + helper `fake_pending_authorize` sẵn sàng cho Plan 02/03.
- Regression: test mcp_service cũ không vỡ.
</success_criteria>

<output>
After completion, create `.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-01-SUMMARY.md`
</output>
</content>
</invoke>
