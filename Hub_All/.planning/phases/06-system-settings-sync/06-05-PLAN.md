---
phase: 06
plan: 05
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - Hub_All/CLAUDE.md
  - Hub_All/.planning/STATE.md
  - Hub_All/.planning/REQUIREMENTS.md
  - Hub_All/.planning/ROADMAP.md
  - Hub_All/README.md
autonomous: false
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04]
tags: [closeout, docs, smoke-checkpoint]

must_haves:
  truths:
    - "CLAUDE.md §6 v3.0 progress row Phase 6 ✅ DONE 2026-05-23 + REQ-ID list SETTINGS-01..04"
    - "CLAUDE.md §6 thêm subsection mới 'Phase 6 System Settings Sync pattern' 5-7 bullet point (carry forward Phase 4+5 style)"
    - "STATE.md frontmatter Phase 6 DONE + Current Position + Phase 6 Planning + Results Summary"
    - "REQUIREMENTS.md SETTINGS-01..04 mark `[x]` + NOTE Phase 6 closeout 5-plan list"
    - "ROADMAP.md Phase 6 row ✅ DONE 2026-05-23 + Progress table update"
    - "README.md NEW section 'System Settings Sync Deploy Notes (Phase 6 v3.0)' 5-step deploy + rollback procedure"
    - "Smoke checkpoint auto-fallback `skip smoke` per v3.0-b precedent (Plan 03-05 + 04-07 + 05-06)"
  artifacts:
    - path: "Hub_All/CLAUDE.md"
      provides: "§6 progress row + Phase 6 pattern subsection"
      contains: "Phase 6"
    - path: "Hub_All/.planning/STATE.md"
      provides: "frontmatter + Phase 6 Results Summary"
      contains: "Phase 6 status"
    - path: "Hub_All/.planning/REQUIREMENTS.md"
      provides: "SETTINGS-01..04 mark [x] + NOTE"
      contains: "[x] **SETTINGS-01**"
    - path: "Hub_All/.planning/ROADMAP.md"
      provides: "Phase 6 row DONE"
      contains: "✅ DONE"
    - path: "Hub_All/README.md"
      provides: "System Settings Sync Deploy Notes section"
      contains: "Phase 6"
  key_links:
    - from: "CLAUDE.md §6 progress row"
      to: "Phase 6 pattern subsection (cùng file)"
      via: "markdown internal anchor"
      pattern: "Phase 6 System Settings Sync pattern"
    - from: "STATE.md frontmatter"
      to: "Next phase trigger /gsd-discuss-phase 7"
      via: "current_position field"
      pattern: "Phase 7 next"
    - from: "README.md deploy notes"
      to: "Operator deploy procedure"
      via: "5-step checklist"
      pattern: "openssl rand -hex 32"
---

<objective>
Wave 5 closeout Phase 6 — Update 5 doc file (CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md) phản ánh Phase 6 DONE + SETTINGS-01..04 closed + 5-plan list + backward compat notes + deploy procedure. Smoke checkpoint manual smoke (compose runtime + central PUT rag_config + yte cache flush observe) — auto-fallback `skip smoke` per v3.0-b precedent (Plan 03-05 + 04-07 + 05-06 pre-resolved).

**Purpose:** Closeout pattern carry forward 3 plan v3.0-b — đảm bảo project knowledge base luôn up-to-date, next phase planner đọc STATE/CLAUDE biết Phase 6 đã ship. Operator deploy biết gen `SETTINGS_PROXY_SECRET` + restart 4 service procedure.

**Output:** 5 file docs updated + 1 smoke checkpoint (skip auto-fallback) + 1 SUMMARY.md cuối Phase 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/06-system-settings-sync/06-CONTEXT.md
@Hub_All/.planning/phases/06-system-settings-sync/06-PATTERNS.md
@Hub_All/.planning/phases/06-system-settings-sync/06-01-PLAN.md
@Hub_All/.planning/phases/06-system-settings-sync/06-02-PLAN.md
@Hub_All/.planning/phases/06-system-settings-sync/06-03-PLAN.md
@Hub_All/.planning/phases/06-system-settings-sync/06-04-PLAN.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-05-PLAN.md
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-07-PLAN.md
@Hub_All/.planning/phases/05-reverse-proxy-frontend-subpath/05-06-PLAN.md
@Hub_All/CLAUDE.md
@Hub_All/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md §6 progress row + Phase 6 pattern subsection</name>
  <files>Hub_All/CLAUDE.md</files>
  <read_first>
    - Hub_All/CLAUDE.md (đọc §6 — v3.0 progress table + Phase 4 pattern subsection + Phase 5 pattern subsection style để copy structure)
    - Hub_All/.planning/phases/06-system-settings-sync/06-CONTEXT.md (extract 4 D-V3-Phase6-A..D decisions cho subsection)
    - Hub_All/.planning/phases/06-system-settings-sync/06-{01..04}-PLAN.md (extract artifact + test count + deviation cho summary)
  </read_first>
  <action>
    **CLAUDE.md §6 progress table** — Update row Phase 6:
    ```markdown
    | 6 — System Settings Sync | ✅ **DONE** | **5 plan** | **2026-05-23** | **SETTINGS-01..04 (4)** |
    ```

    **CLAUDE.md §6 thêm subsection mới (đặt SAU Phase 5 pattern subsection):**

    ```markdown
    ### Phase 6 System Settings Sync pattern (SETTINGS-01..04 — 2026-05-23)

    5 plan đóng 4 REQ-ID System Settings Sync + 4 D-V3-Phase6-A/B/C/D LOCKED + 6 Prometheus metric infrastructure (HTTP pull + Redis pub/sub invalidate mechanism, R-V3-6 LOW mitigation):

    - **Plan 06-01 SETTINGS-01..04 — Wave 1 BLOCKING infra (D-V3-Phase6-A/B/D):** Module mới `api/app/settings_sync/` (3 file scaffold — `__init__.py` + `keys.py` 4 constant + `make_apikey_cache_key` SHA-256 hash helper T-06-04-02 + `metrics.py` 6 Prometheus collector module-level label `hub_name` + `key_type` enum bounded cardinality W7 carry forward). Settings 5 field mới (`settings_proxy_secret` + 3 TTL field + reconnect_seconds) + 1 model_validator `_enforce_settings_proxy_secret` length ≥ 32 char BOTH central + hub con (T-06-04-01 entropy 128-bit). docker-compose 4 service env wire `SETTINGS_PROXY_SECRET` fail-loud `${VAR:?msg}` syntax + docker-compose.override.yml.template FACTOR-04 placeholder + .env.example document 4 env mới với `openssl rand -hex 32` hint. 22 unit test PASS + regression Phase 1..5 unit suite KHÔNG break.

    - **Plan 06-02 SETTINGS-01/02/04 — Wave 2 client + subscriber + Pydantic schema (D-V3-Phase6-A/C):** Module `api/app/settings_sync/client.py` (~300 LOC) 3 HTTP client class — `RagConfigClient` fetch `GET /api/rag-config` + cache Redis TTL 60s + `SettingsUnavailableError` fail-loud + Prometheus emit hit/miss/latency/stale; `HubRegistryClient` fetch `GET /api/hubs` singleton cache TTL 300s; `ApiKeyVerifyClient` POST `/api/api-keys/verify` body `{api_key}` header `X-Internal-Auth=<secret>` (D-V3-Phase6-D) + cache hash key TTL 60s + KHÔNG cache negative 401 + 3 enum result `valid|invalid|cached`. Module `subscriber.py` (~180 LOC) `settings_subscriber_loop(redis, hub_name, reconnect_seconds=5)` async + outer `while True:` reconnect retry on ConnectionError + Pydantic `InvalidateMessage(BaseModel)` schema `config_key: Literal[3] + hub: str + key_id: str | None + timestamp: int` validate payload reject invalid (T-06-02-03 carry forward) + 3 config_key flush branch (rag_config per-hub / hub_registry singleton / apikey SCAN+DEL pattern) + CancelledError graceful shutdown. 18 unit test PASS.

    - **Plan 06-03 SETTINGS-02/03 — Wave 3 refactor + extend (D-V3-Phase6-A/C/D):** `api/app/auth/api_key.py::require_api_key` REFACTOR — thêm `request: Request` param + branch verify path theo `settings.hub_name` (central=local M2 `ApiKeyService.verify_key` AES-GCM carry forward; hub con=`app.state.api_key_verify_client.verify` HTTP proxy + 503 `APIKEY_VERIFY_CLIENT_UNAVAILABLE` nếu client missing). Dependency mới `api/app/auth/dependencies.py::require_internal_auth` — Header `X-Internal-Auth` constant-time `hmac.compare_digest` với `settings.settings_proxy_secret` → 401 `INTERNAL_AUTH_FAIL` nếu mismatch (T-06-04-01 mitigation timing attack). `api/app/routers/rag_config.py::update_rag_config` PUT extend publish best-effort fail-open `redis.publish("settings:invalidate", json({"config_key":"rag_config","hub":"*","timestamp":<unix>}))` sau service.update_config success (pattern carry forward search_cache.py M2 publish_invalidate fail-open). Endpoint MỚI `api/app/routers/api_keys.py POST /verify` central-only mount (FACTOR-02 carry forward — api_keys_router central-only) với `require_internal_auth` dep + `VerifyApiKeyRequest` schema + return raw dict `{valid: bool, principal: dict | null}` (KHÔNG envelope — hub con ApiKeyVerifyClient parse raw). 22 unit test PASS.

    - **Plan 06-04 SETTINGS-01..04 — Wave 4 lifespan integration BLOCKING:** `api/app/main.py::lifespan` thêm block hub con (`if settings.hub_name != "central":`) SAU sync_worker spawn — init 4 `app.state.<X>` default None defensive + escape hatch `SETTINGS_SKIP_FETCH=1` env (pattern song song JWKS_SKIP_FETCH + SYNC_SKIP_CENTRAL_POOL + COCOINDEX_SKIP_SETUP) + 3 client instantiate + `await rag_client.fetch_initial()` blocking 5s + `await hub_client.fetch_initial()` blocking 5s (ApiKeyVerifyClient lazy KHÔNG fetch_initial — verify on demand) + boot fail-loud raise → uvicorn exit 1 (R-V3-6 LOW pattern carry forward Plan 03-02 JWKSCache + Plan 04-04 central_sync_pool) + spawn `app.state.settings_subscriber_task = asyncio.create_task(settings_subscriber_loop(...))` nếu app.state.redis present. Shutdown graceful `task.cancel() + asyncio.wait_for(timeout=10s)` sau yield. 6 integration test ASGI lifespan PASS (escape hatch + populate + spawn + shutdown + fail-loud + central skip); pubsub e2e defer Phase 7 MIGRATE-05 nếu fakeredis async pubsub limit.

    - **Plan 06-05 closeout — docs + smoke checkpoint (file này, 2026-05-23):** CLAUDE.md §6 D-V3-Phase6 lockedin + §6 progress row + pattern subsection (file này) + STATE.md frontmatter + Phase 6 Planning + Results Summary + REQUIREMENTS.md SETTINGS-01..04 mark `[x]` + NOTE Phase 6 closeout 5-plan list + ROADMAP.md Phase 6 DONE + Progress table + README.md NEW section "System Settings Sync Deploy Notes (Phase 6 v3.0)". Smoke checkpoint manual smoke 4 hub × `PUT /api/rag-config` central + verify yte/duoc/hcns cache flush < 30s + central /api/api-keys/verify proxy 200 — Task 5 auto-fallback `skip smoke` per v3.0-b precedent (Plan 03-05 + 04-07 + 05-06 pre-resolved skip pattern + `--auto --chain` mode active); evidence chain 62+ unit + 6 integration test in-process cover semantic; manual visual smoke runtime defer Phase 7 MIGRATE-05 full E2E.

    **Architecture insights (Phase 6):**

    1. **HTTP pull + Redis pub/sub invalidate hybrid (D-V3-Phase6-A LOCKED):** Pull pattern proven Phase 3 JWKSCache carry forward; pub/sub bổ sung "TTL window stale" mitigation — đổi config ở central → publish ngay → hub con flush < 1s thực tế (E-V3-4 < 30s threshold thừa margin). Best-effort fail-open Redis down KHÔNG block admin write (TTL natural fallback 60s).
    2. **1 channel `settings:invalidate` cho cả 3 category (D-V3-Phase6-C):** Pydantic `InvalidateMessage` schema `config_key: Literal[3]` + `hub: str` + `key_id: str | None` + `timestamp: int` validate payload reject injection (T-06-02-03). Subscriber phân nhánh 3 config_key flush đúng cache key (rag_config per-hub broadcast / hub_registry singleton / apikey SCAN+DEL full flush).
    3. **Shared secret `X-Internal-Auth` (D-V3-Phase6-D):** Simpler hơn JWT internal-only / mTLS cho v3.0-b scope. 32-char min entropy 128-bit + hmac.compare_digest constant-time. POST /api/api-keys/verify central-only mount (FACTOR-02 carry forward) + endpoint mới KHÔNG đụng M2 `ApiKeyService.verify_key` AES-GCM at-rest private logic (AUX-02 unchanged).
    4. **TTL strategy per category (D-V3-Phase6-B):** rag_config 60s + apikey 60s (hot revoke window M2 AUX-02 baseline) + hub_registry 300s (rare-change FACTOR-04 `make hub-add` infrequent). Pub/sub primary; TTL fallback Redis down.
    5. **Cache key hashing T-06-04-02 mitigation:** `make_apikey_cache_key()` SHA-256 hex prefix 16 char — KHÔNG plaintext trong Redis key/value. Subscriber `apikey` flush dùng SCAN+DEL pattern (KHÔNG cần plaintext compute key).
    6. **Lifespan fail-loud boot pattern carry forward:** rag_client + hub_client `fetch_initial()` blocking 5s raise → uvicorn exit 1 (operator catch boot error). Pattern song song Phase 3 JWKSCache + Phase 4 central_sync_pool — consistency v3.0 milestone.
    7. **6 Prometheus metric mới (Phase 6 observability):** `settings_cache_hit_total{hub_name, key_type}` + `settings_cache_miss_total` + `settings_pull_latency_seconds{hub_name, endpoint}` histogram + `settings_invalidate_received_total{hub_name, key_type}` + `settings_stale_seconds{hub_name, key_type}` gauge + `apikey_verify_total{hub_name, result}` — label cardinality bounded W7 carry forward Phase 4.

    **T-06-01..04 STRIDE coverage:**
    - T-06-01-01 Tampering `.env` SETTINGS_PROXY_SECRET: Plan 06-01 fail-loud `${VAR:?msg}` + Settings validator length ≥ 32 char.
    - T-06-02-01 Information Disclosure apikey plaintext Redis key: Plan 06-01 `make_apikey_cache_key` SHA-256 hash prefix.
    - T-06-02-03 Tampering pub/sub payload injection: Plan 06-02 Pydantic Literal enum reject invalid + JSON parse fail log warning skip.
    - T-06-02-05 DoS subscriber crash: Plan 06-02 best-effort fail-open + outer reconnect loop + CancelledError graceful.
    - T-06-03-01 Tampering X-Internal-Auth secret leak: Plan 06-03 hmac.compare_digest constant-time + secret entropy 128-bit.
    - T-06-03-04 DoS publish_invalidate hang: Plan 06-03 try/except wrap + log warning (KHÔNG raise) — best-effort fail-open.
    - T-06-04-01 DoS lifespan blocking hang: Plan 06-04 timeout 5s connect 2s read; fail-loud raise → uvicorn exit 1.
    - T-06-04-03 Resource leak subscriber sau shutdown: Plan 06-04 `task.cancel() + asyncio.wait_for(timeout=10s)` graceful.

    **Backward compat (Phase 6 KHÔNG break M2):**
    - `require_api_key` signature mở rộng thêm `request: Request` — FastAPI Depends auto-inject; consumer endpoint M2 KHÔNG cần update.
    - `update_rag_config` PUT signature mở rộng thêm `request: Request` — backward compat.
    - M2 `ApiKeyService.verify_key` AES-GCM at-rest LOCKED unchanged (AUX-02 carry forward).
    - M2 `rag_config_service.py` `update_config()` LOCKED unchanged — Phase 6 chỉ wrap publish best-effort sau success.
    - Hub con M2 cũ thiếu `SETTINGS_PROXY_SECRET` env → Settings validator fail boot → operator phải set TRƯỚC deploy (xem README.md Phase 6 deploy 5-step).

    **R-V3-6 LOW mitigation chain:**
    - HTTP pull on-demand + Redis cache TTL 60s baseline.
    - Pub/sub invalidate broadcast `settings:invalidate` channel — < 1s propagate thực tế.
    - Subscriber reconnect retry 5s interval on ConnectionError — KHÔNG fail-loud (TTL natural fallback).
    - Boot fail-loud rag_client + hub_client fetch_initial 5s timeout — operator catch (R-V3-5 pattern carry forward).
    - Idempotent flush — multi pub/sub event same key OK.

    **E-V3-4 propagate < 30s carry forward:**
    - Default TTL 60s (rag_config + apikey) overlap pub/sub natural propagate.
    - Pub/sub broadcast hub="*" tới all hub con đồng thời.
    - Live measure defer Phase 7 MIGRATE-05 runtime smoke E2E.

    **v3.0-b progress:** Phase 6 hoàn tất 4/4 REQ-ID SETTINGS. v3.0-b 3/4 phase DONE (Phase 4 + 5 + 6 — 33/~37 plan ≈ 89%). Next: Phase 7 MIGRATE (MIGRATE-01..05 — pg_dump snapshot per hub_id + blue/green restore + MCP re-point + smoke E2E 3 hub con + central + cross-hub p95 < 1.5s live measure).

    **Reference:**
    - `.planning/phases/06-system-settings-sync/06-CONTEXT.md` — 4 D-V3-Phase6-A..D LOCKED 2026-05-23.
    - `.planning/phases/06-system-settings-sync/06-PATTERNS.md` — Pattern map 14 file analog + 5 Wave grouping.
    - `.planning/phases/06-system-settings-sync/06-{01..05}-PLAN.md` — implementation chi tiết 5 plan.
    - `.planning/phases/06-system-settings-sync/06-{01..05}-SUMMARY.md` — deliverable + commit + test count per plan.
    ```
  </action>
  <verify>
    <automated>cd Hub_All && grep -c "Phase 6 System Settings Sync pattern" CLAUDE.md && grep -c "SETTINGS-01..04" CLAUDE.md && grep -c "Plan 06-01\|Plan 06-02\|Plan 06-03\|Plan 06-04\|Plan 06-05" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Phase 6 — System Settings Sync" CLAUDE.md | grep -c "DONE"` >= 1 (progress row updated).
    - `grep -c "Phase 6 System Settings Sync pattern" CLAUDE.md >= 1` (subsection header).
    - `grep -c "Plan 06-01\|Plan 06-02\|Plan 06-03\|Plan 06-04\|Plan 06-05" CLAUDE.md >= 5` (5 plan referenced).
    - `grep -c "D-V3-Phase6" CLAUDE.md >= 4` (4 decision marker).
    - `grep -c "SETTINGS_PROXY_SECRET\|settings:invalidate\|make_apikey_cache_key" CLAUDE.md >= 3`.
    - `grep -c "R-V3-6 LOW mitigation chain" CLAUDE.md >= 1`.
    - `grep -c "E-V3-4 propagate" CLAUDE.md >= 1`.
    - `grep -c "T-06-01\|T-06-02\|T-06-03\|T-06-04" CLAUDE.md >= 5` (STRIDE threat coverage list).
    - Subsection structure khớp Phase 4+5 pattern (4-7 bullet detail + Architecture insights + STRIDE coverage + Backward compat + R mitigation + E carry forward + progress + Reference).
  </acceptance_criteria>
  <done>CLAUDE.md §6 progress row + Phase 6 pattern subsection ship + grep acceptance criteria PASS.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md frontmatter + Phase 6 Planning + Results Summary</name>
  <files>Hub_All/.planning/STATE.md</files>
  <read_first>
    - Hub_All/.planning/STATE.md (đọc current frontmatter status + Current Position + Phase 5 Results Summary structure để copy/extend)
    - Hub_All/.planning/phases/06-system-settings-sync/06-{01..04}-PLAN.md (extract metrics + commit count + test count cho Results Summary table)
  </read_first>
  <action>
    **STATE.md frontmatter** — update fields:
    - `status` → câu mô tả mới: "Phase 6 DONE 2026-05-23 ✅ — System Settings Sync 5 plan ship SETTINGS-01..04. HTTP pull on-demand + Redis cache TTL 60s/300s/60s + pub/sub invalidate channel `settings:invalidate` (E-V3-4 < 30s propagate) + api_keys verify proxy central `X-Internal-Auth` shared secret + hub_registry read-only TTL 5 phút. ... [extend chi tiết per Phase 5 + Phase 4 frontmatter style]"
    - `last_updated` → "2026-05-23T..."
    - `progress.completed_phases` → 6
    - `progress.total_plans` → 33 (28 + 5)
    - `progress.completed_plans` → 33
    - `progress.percent` → 89 (33/37)
    - `phase_6_status` → "DONE 2026-05-23 ✅ — 5 plan/4 wave shipped..."

    **STATE.md Current Position section** — update Phase 6 ✅ DONE 2026-05-23, brief description.

    **STATE.md thêm "Phase 6 Planning Summary" table** — Đặt SAU Phase 5 Planning Summary table:

    ```markdown
    ## Phase 6 Planning Summary

    | Plan | Wave | Objective | Tasks | Files modified | REQ | Status |
    |------|------|-----------|-------|----------------|-----|--------|
    | 06-01 | 1 BLOCKING | Wave 1 infra: settings_sync/ module scaffold (3 file) + Settings 5 field + 1 validator + docker-compose 4 service env + override.yml.template + .env.example document | 3 (auto) | `api/app/settings_sync/{__init__,keys,metrics}.py`, `api/app/config.py`, `api/tests/unit/test_settings_sync_keys.py`, `test_settings_sync_metrics.py`, `test_config_settings_proxy_secret.py`, `docker-compose.yml`, `docker-compose.override.yml.template`, `.env.example` | SETTINGS-01..04 | ✅ **DONE 2026-05-23** |
    | 06-02 | 2 | Wave 2 client + subscriber + Pydantic schema: 3 HTTP client class (RagConfigClient + HubRegistryClient + ApiKeyVerifyClient) + SettingsUnavailableError + settings_subscriber_loop async + InvalidateMessage schema + 18 unit test | 2 (tdd) | `api/app/settings_sync/{client,subscriber,__init__}.py`, `api/tests/unit/test_settings_sync_client.py`, `test_settings_sync_subscriber.py` | SETTINGS-01, SETTINGS-02, SETTINGS-04 | ✅ **DONE 2026-05-23** |
    | 06-03 | 3 | Wave 3 refactor + extend: require_api_key branch hub_name + require_internal_auth dep + update_rag_config publish best-effort + POST /api/api-keys/verify endpoint mới + 22 unit test | 2 (tdd) | `api/app/auth/api_key.py`, `api/app/auth/dependencies.py`, `api/app/routers/rag_config.py`, `api/app/routers/api_keys.py`, `api/app/schemas/api_keys.py`, 4 test file mới | SETTINGS-02, SETTINGS-03 | ✅ **DONE 2026-05-23** |
    | 06-04 | 4 | Wave 4 lifespan integration BLOCKING: hub con block init 3 client + spawn subscriber task + shutdown graceful + SETTINGS_SKIP_FETCH escape hatch + 6 integration test ASGI lifespan | 2 (1 auto + 1 tdd) | `api/app/main.py`, `api/tests/integration/test_settings_sync_lifespan_integration.py`, `test_settings_sync_pubsub_e2e.py` (skip module-level defer Phase 7) | SETTINGS-01..04 | ✅ **DONE 2026-05-23** |
    | 06-05 | 5 closeout | Docs update CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md + smoke checkpoint manual (skip auto-fallback per v3.0-b precedent) | 2 (1 auto + 1 checkpoint:human-action) | `Hub_All/CLAUDE.md`, `.planning/STATE.md` (file này), `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `Hub_All/README.md` | SETTINGS-01..04 closeout | ✅ **DONE 2026-05-23** (Task 5b smoke `skip smoke` auto-fallback per --auto chain + v3.0-b precedent) |

    **Coverage:** 4/4 REQ (SETTINGS-01..04) covered ≥ 1 plan/REQ.

    **Critical path:** 06-01 ✅ (Wave 1 BLOCKING) → 06-02 (Wave 2 depend 06-01) → 06-03 (Wave 3 depend 06-01 + 06-02 — sửa router consume client) → 06-04 (Wave 4 BLOCKING depend 06-01..03 — lifespan wire) → 06-05 (Wave 5 closeout depend all).

    **Auto-chain pause resolved:** Plan 06-05 Task 2b (`checkpoint:human-action gate=blocking`) — manual smoke compose runtime 4 hub × PUT central rag_config + verify hub con cache flush < 30s + central POST /api/api-keys/verify proxy 200. Auto-fallback `skip smoke` per --auto chain mode active + v3.0-b precedent (Plan 03-05 + 04-07 + 05-06 pre-resolved skip pattern); evidence chain in-process 62+ unit + 6 integration test PASS cover semantic. Manual smoke runtime defer Phase 7 MIGRATE-05 full E2E (3 hub con + central + golden path + JWT SSO live + cross-hub search live data + per-hub branding visual diff + settings sync pub/sub live propagate).
    ```

    **STATE.md thêm "Phase 6 Results Summary" table** — Đặt SAU Phase 6 Planning Summary:

    ```markdown
    ## Phase 6 Results Summary

    | Plan | Wave | Objective | Commits | Tests |
    |------|------|-----------|---------|-------|
    | 06-01 | 1 | settings_sync/ scaffold + Settings + docker-compose env wire | TBD (3-4 commit expected) | 22/22 unit (9 config + 6 keys + 7 metrics) + docker compose config exit 0 + ruff/mypy clean |
    | 06-02 | 2 | client + subscriber + Pydantic schema | TBD (2-3 commit) | 18/18 unit (10 client + 8 subscriber) + ruff/mypy clean |
    | 06-03 | 3 | require_api_key branch + require_internal_auth + rag_config publish + /verify endpoint | TBD (2-4 commit) | 22/22 unit (6 require_api_key + 6 require_internal_auth + 4 rag_config publish + 6 verify endpoint) + regression M2 auth + Phase 3 SSO PASS |
    | 06-04 | 4 | lifespan integration + integration test ASGI | TBD (2 commit) | 6/6 integration ASGI lifespan + mypy clean + pubsub e2e defer Phase 7 |
    | 06-05 | 5 | Closeout docs + smoke checkpoint | TBD (5 task commit + 1 SUMMARY) | grep acceptance 5 file PASS + Task 5b smoke `skip smoke` auto-fallback |

    **Phase 6 deliverable summary:**
    - Hub con `rag_config` HTTP pull + Redis cache TTL 60s + pub/sub invalidate < 1s thực tế (E-V3-4 < 30s threshold thừa margin) — SETTINGS-01/02 + RagConfigClient + subscriber `rag_config` flush branch + central PUT publish best-effort fail-open.
    - API key verify proxy: hub con `require_api_key` branch → POST /api/api-keys/verify central (X-Internal-Auth header 32-char secret hmac.compare_digest) + cache Redis TTL 60s hash key — SETTINGS-03 + ApiKeyVerifyClient + require_internal_auth dep.
    - Hub registry read-only: HubRegistryClient HTTP pull `GET /api/hubs` (M2 endpoint hiện hữu) + cache TTL 5 phút singleton key — SETTINGS-04.
    - settings_sync/ module 5 file (~700 LOC source + ~600 LOC test) — pattern proven Phase 3 JWKSCache + Phase 4 sync module carry forward.
    - 6 Prometheus metric mới observability — settings_cache_hit/miss_total + pull_latency + invalidate_received + stale_seconds + apikey_verify_total label `hub_name` + `key_type` enum bounded W7.
    - 4 D-V3-Phase6 LOCKED consumed (A=HTTP pull + pub/sub; B=TTL 60s/300s/60s; C=1 channel + Pydantic enum payload; D=shared secret 32-char hmac compare).
    - 5 plan ship in-process 62+ unit + 6 integration test PASS — manual smoke runtime defer Phase 7 MIGRATE-05.

    **v3.0-b progress: Phase 4+5+6 DONE (3/4 phase v3.0-b — 33/~37 plan ≈ 89%). Next: Phase 7 MIGRATE (MIGRATE-01..05 — pg_dump snapshot per hub_id + blue/green restore + MCP re-point + smoke E2E full v3.0).**
    ```
  </action>
  <verify>
    <automated>cd Hub_All && grep -c "Phase 6 Planning Summary" .planning/STATE.md && grep -c "Phase 6 Results Summary" .planning/STATE.md && grep -c "completed_phases: 6" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "completed_phases:" .planning/STATE.md | grep -c "6" >= 1` (frontmatter update).
    - `grep "total_plans:" .planning/STATE.md | grep -c "33" >= 1`.
    - `grep "completed_plans:" .planning/STATE.md | grep -c "33" >= 1`.
    - `grep "percent:" .planning/STATE.md | grep -c "89" >= 1`.
    - `grep -c "phase_6_status" .planning/STATE.md >= 1`.
    - `grep -c "Phase 6 Planning Summary" .planning/STATE.md >= 1`.
    - `grep -c "Phase 6 Results Summary" .planning/STATE.md >= 1`.
    - `grep -c "06-01\|06-02\|06-03\|06-04\|06-05" .planning/STATE.md >= 5`.
    - `grep -c "SETTINGS-01\|SETTINGS-02\|SETTINGS-03\|SETTINGS-04" .planning/STATE.md >= 4`.
    - YAML frontmatter parse OK (Python yaml.safe_load test optional).
  </acceptance_criteria>
  <done>STATE.md frontmatter + Phase 6 Planning + Results Summary table ship. grep acceptance criteria PASS.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md SETTINGS-01..04 mark [x] + NOTE Phase 6 closeout</name>
  <files>Hub_All/.planning/REQUIREMENTS.md</files>
  <read_first>
    - Hub_All/.planning/REQUIREMENTS.md (đọc section "SETTINGS — System Settings Sync (4 REQ — GA-V3-B chốt ở /gsd-discuss-phase 6)" — copy spec hiện hữu vào checkbox update + thêm NOTE 5-plan closeout)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-05-PLAN.md HOẶC Phase 4+5 plan tương ứng REQUIREMENTS.md NOTE style để copy structure
  </read_first>
  <action>
    **REQUIREMENTS.md SETTINGS section** — Cho mỗi REQ-ID SETTINGS-01..04, đổi `- [ ] **SETTINGS-XX**` → `- [x] **SETTINGS-XX** ✅ Phase 6:` + append note inline `_Closed 2026-05-23 Plan 06-XX (...)._`:

    ```markdown
    ### SETTINGS — System Settings Sync (4 REQ — GA-V3-B chốt 2026-05-23 — SETTINGS-01..04 satisfied 2026-05-23 Plan 06-01..05)

    - [x] **SETTINGS-01** ✅ Phase 6: Hub con đọc `rag_config` ... _Closed 2026-05-23 Plan 06-01 (settings_sync/ module scaffold + Settings 5 field + 6 Prometheus collector) + Plan 06-02 (RagConfigClient httpx async + Redis cache TTL 60s + SettingsUnavailableError fail-loud + Prometheus emit) + Plan 06-04 (lifespan hub con fetch_initial blocking 5s + boot fail-loud)._
    - [x] **SETTINGS-02** ✅ Phase 6: Push invalidate qua Redis pub/sub channel ... _Closed 2026-05-23 Plan 06-02 (settings_subscriber_loop async + Pydantic InvalidateMessage schema + 3 config_key flush branch + reconnect retry + CancelledError graceful) + Plan 06-03 (update_rag_config PUT publish best-effort fail-open settings:invalidate channel) + Plan 06-04 (lifespan spawn subscriber task + shutdown graceful)._
    - [x] **SETTINGS-03** ✅ Phase 6: API key verification proxy ... _Closed 2026-05-23 Plan 06-02 (ApiKeyVerifyClient httpx POST + X-Internal-Auth header + Redis cache TTL 60s hash key T-06-04-02) + Plan 06-03 (require_api_key REFACTOR branch hub_name central vs hub con + require_internal_auth dep hmac.compare_digest + POST /api/api-keys/verify endpoint mới central-only mount FACTOR-02 carry forward)._
    - [x] **SETTINGS-04** ✅ Phase 6: `hub_registry` read-only ở hub con ... _Closed 2026-05-23 Plan 06-02 (HubRegistryClient httpx GET /api/hubs + Redis cache TTL 300s singleton key + fetch_initial blocking 5s) + Plan 06-04 (lifespan hub con init). Hub con POST/PUT/PATCH /api/hubs* đã 404 do FACTOR-02 Phase 2 strip hubs_router central-only — KHÔNG cần thay đổi._
    ```

    **Thêm NOTE Phase 6 closeout (style Phase 4+5 NOTE):**

    ```markdown
    > **NOTE Phase 6 closeout (Plan 06-05 SETTINGS-01..04 — 2026-05-23):** 5 plan ship đầy đủ 4 REQ-ID + 4 D-V3-Phase6-A/B/C/D LOCKED consumed + 6 Prometheus metric infrastructure + ~14 STRIDE threat addressed:
    >
    > 1. Plan 06-01 — Wave 1 BLOCKING infra: settings_sync/ module scaffold 3 file + Settings 5 field + 1 validator + docker-compose 4 service env + override.yml.template + .env.example.
    > 2. Plan 06-02 — Wave 2 client + subscriber: 3 HTTP client class (RagConfigClient + HubRegistryClient + ApiKeyVerifyClient) + SettingsUnavailableError + settings_subscriber_loop + Pydantic InvalidateMessage schema + 18 unit test.
    > 3. Plan 06-03 — Wave 3 refactor + extend: require_api_key branch + require_internal_auth + update_rag_config publish + POST /api/api-keys/verify endpoint + 22 unit test.
    > 4. Plan 06-04 — Wave 4 lifespan integration: main.py hub con block init 3 client + spawn subscriber + shutdown graceful + SETTINGS_SKIP_FETCH escape hatch + 6 integration test.
    > 5. Plan 06-05 — Wave 5 closeout: docs update CLAUDE.md + STATE.md + REQUIREMENTS.md (file này) + ROADMAP.md + README.md + smoke checkpoint runtime SKIP pre-resolved per v3.0-b precedent.
    >
    > **Backward compat:** require_api_key + update_rag_config signature mở rộng thêm `request: Request` param (FastAPI Depends auto-inject — consumer endpoint KHÔNG cần update). M2 `ApiKeyService.verify_key` AES-GCM at-rest + `RagConfigService.update_config()` LOCKED unchanged. Hub con M2 cũ thiếu `SETTINGS_PROXY_SECRET` env → Settings validator fail boot → operator phải set TRƯỚC deploy (xem README.md "System Settings Sync Deploy Notes (Phase 6 v3.0)" section deploy 5 step + rollback procedure).
    >
    > **R-V3-6 LOW mitigation chain:** HTTP pull on-demand + Redis cache TTL 60s/300s/60s + pub/sub invalidate broadcast `settings:invalidate` channel — < 1s propagate thực tế (E-V3-4 < 30s threshold thừa margin) + subscriber reconnect retry 5s + boot fail-loud rag_client + hub_client fetch_initial 5s timeout (R-V3-5 pattern carry forward Plan 03-02 JWKSCache).
    >
    > **v3.0-b progress:** Phase 6 hoàn tất 4/4 REQ-ID SETTINGS. v3.0-b 3/4 phase DONE (Phase 4+5+6 — 33/~37 plan ≈ 89%). Next: `/gsd-discuss-phase 7` Migration + Smoke E2E (MIGRATE-01..05 — pg_dump snapshot per hub_id + blue/green restore + MCP re-point central + smoke E2E 3 hub con + central + golden path mỗi hub PASS + cross-hub p95 < 1.5s live measure + hub isolation enforce E-V3-3).
    ```
  </action>
  <verify>
    <automated>cd Hub_All && grep -c "\\[x\\] \\*\\*SETTINGS-0[1-4]\\*\\*" .planning/REQUIREMENTS.md && grep -c "NOTE Phase 6 closeout" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*SETTINGS-01\*\*" .planning/REQUIREMENTS.md >= 1`.
    - `grep -c "\[x\] \*\*SETTINGS-02\*\*" .planning/REQUIREMENTS.md >= 1`.
    - `grep -c "\[x\] \*\*SETTINGS-03\*\*" .planning/REQUIREMENTS.md >= 1`.
    - `grep -c "\[x\] \*\*SETTINGS-04\*\*" .planning/REQUIREMENTS.md >= 1`.
    - `grep -c "NOTE Phase 6 closeout" .planning/REQUIREMENTS.md >= 1`.
    - `grep -c "Plan 06-01\|Plan 06-02\|Plan 06-03\|Plan 06-04\|Plan 06-05" .planning/REQUIREMENTS.md >= 5`.
    - `grep -c "D-V3-Phase6" .planning/REQUIREMENTS.md >= 4` (4 decision marker).
    - `grep -c "Closed 2026-05-23" .planning/REQUIREMENTS.md >= 4` (4 REQ closeout marker).
    - Markdown structure preserve — section "SETTINGS — System Settings Sync" header unchanged.
  </acceptance_criteria>
  <done>4 SETTINGS REQ mark [x] + NOTE Phase 6 closeout 5-plan list ship. grep acceptance PASS.</done>
</task>

<task type="auto">
  <name>Task 4: ROADMAP.md Phase 6 row DONE + Progress table</name>
  <files>Hub_All/.planning/ROADMAP.md</files>
  <read_first>
    - Hub_All/.planning/ROADMAP.md (đọc Phase 6 row hiện tại + Phase 5 row pattern + Progress table v3.0 row để update)
  </read_first>
  <action>
    **ROADMAP.md Phase 6 row** — Update:
    ```markdown
    | ✅ **6** | System settings sync | rag-config HTTP pull + Redis cache 60s + pub/sub invalidate < 30s; api_keys verify proxy central; hub_registry read-only | SETTINGS-01..04 (4) | 4 | M2 shipped — **DONE 2026-05-23** (5 plans / ~13 commits / 62+ unit + 6 integration test PASS) |
    ```

    **ROADMAP.md Phase 6 detail section** — Update header `### Phase 6 — System Settings Sync (GA-V3-B) ✅ DONE 2026-05-23` và update Plans list:

    ```markdown
    Plans:
    - [x] 06-01-PLAN.md — Wave 1 BLOCKING settings_sync/ scaffold + Settings 5 field + docker-compose env wire (SETTINGS-01..04 — D-V3-Phase6-A/B/D) — **DONE 2026-05-23**
    - [x] 06-02-PLAN.md — Wave 2 client (RagConfigClient + HubRegistryClient + ApiKeyVerifyClient) + subscriber + Pydantic schema + 18 unit test (SETTINGS-01/02/04 — D-V3-Phase6-A/C) — **DONE 2026-05-23**
    - [x] 06-03-PLAN.md — Wave 3 require_api_key branch + require_internal_auth + update_rag_config publish + /api/api-keys/verify endpoint + 22 unit test (SETTINGS-02/03 — D-V3-Phase6-A/C/D) — **DONE 2026-05-23**
    - [x] 06-04-PLAN.md — Wave 4 lifespan integration BLOCKING + ASGI integration test + SETTINGS_SKIP_FETCH escape hatch + 6 integration test (SETTINGS-01..04) — **DONE 2026-05-23**
    - [x] 06-05-PLAN.md — Wave 5 closeout CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md (file này) + README.md + smoke checkpoint (skip auto-fallback) — **DONE 2026-05-23**
    ```

    **Thêm Decisions block (style Phase 4+5):**

    ```markdown
    **Decisions (chốt 2026-05-23 theo planner seed defaults Auto Mode `--auto --chain`, KHÔNG `/gsd-discuss-phase 6` interactive — 4 D-V3-Phase6-A..D LOCKED):**
    - D-V3-Phase6-A: HTTP pull on-demand + Redis pub/sub invalidate hybrid (REJECT push webhook / env var local).
    - D-V3-Phase6-B: TTL strategy per category — rag_config 60s + hub_registry 300s + apikey 60s.
    - D-V3-Phase6-C: 1 channel `settings:invalidate` cho cả 3 category + Pydantic Literal enum payload validate.
    - D-V3-Phase6-D: Shared secret header `X-Internal-Auth: <SETTINGS_PROXY_SECRET>` 32-char min + hmac.compare_digest constant-time.
    ```

    **ROADMAP.md Progress table v3.0 row** — update:
    ```markdown
    | **v3.0 Multi-Hub Split** | **7** | **33/~37** | **21/30** | 🔄 **Phase 1+2+3+4+5+6 DONE 2026-05-23 (33/~37 ≈ 89%) — v3.0-b 3/4 phase complete** | — |
    ```

    **Last updated footer** — update đến cuối file:
    ```markdown
    *Last updated: 2026-05-23 sau `/gsd-execute-phase 6` Plan 06-05 closeout — Phase 6 System Settings Sync DONE 5 plans (Wave 1 BLOCKING + Wave 2 + Wave 3 + Wave 4 BLOCKING + Wave 5 closeout). 4 D-V3-Phase6-A..D consumed; HTTP pull + Redis pub/sub invalidate + api_keys proxy verify central + hub_registry read-only + 6 Prometheus metric infrastructure. SETTINGS-01..04 fully shipped. 62+ unit + 6 integration PASS in-process. v3.0-b 3/4 phase complete (33/~37 plan ≈ 89%). Next: `/gsd-discuss-phase 7` Migration + Smoke E2E (MIGRATE-01..05 — pg_dump per hub_id + blue/green restore + MCP re-point + smoke E2E full v3.0).*
    ```
  </action>
  <verify>
    <automated>cd Hub_All && grep -c "✅ \*\*6\*\*" .planning/ROADMAP.md && grep -c "Phase 6 — System Settings Sync" .planning/ROADMAP.md && grep -c "06-01-PLAN\|06-02-PLAN\|06-03-PLAN\|06-04-PLAN\|06-05-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "✅ \*\*6\*\*" .planning/ROADMAP.md >= 1` (Phase 6 row DONE marker).
    - `grep -c "06-01-PLAN\|06-02-PLAN\|06-03-PLAN\|06-04-PLAN\|06-05-PLAN" .planning/ROADMAP.md >= 5`.
    - `grep -c "\[x\] 06-01-PLAN" .planning/ROADMAP.md >= 1`.
    - `grep -c "D-V3-Phase6-A\|D-V3-Phase6-B\|D-V3-Phase6-C\|D-V3-Phase6-D" .planning/ROADMAP.md >= 4`.
    - `grep -c "33/~37" .planning/ROADMAP.md >= 1` (Progress table updated).
    - `grep -c "21/30" .planning/ROADMAP.md >= 1` (REQ-ID count updated 17+4).
    - `grep -c "89%" .planning/ROADMAP.md >= 1`.
    - `grep -c "Phase 6 System Settings Sync DONE 5 plans" .planning/ROADMAP.md >= 1` (footer updated).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 6 row DONE + plans list [x] + Decisions block + Progress table updated + footer.</done>
</task>

<task type="auto">
  <name>Task 5a: README.md NEW section "System Settings Sync Deploy Notes (Phase 6 v3.0)"</name>
  <files>Hub_All/README.md</files>
  <read_first>
    - Hub_All/README.md (đọc section hiện tại — locate vị trí insert sau "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)" section Plan 05-06 carry forward; copy structure 5-step + rollback)
    - Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-05-PLAN.md (Plan 03-05 SSO Backward Incompat section style để copy structure)
  </read_first>
  <action>
    **README.md** — Thêm section MỚI cuối file (sau "Reverse Proxy Subpath Deploy Notes (Phase 5 v3.0)" section):

    ```markdown
    ## System Settings Sync Deploy Notes (Phase 6 v3.0)

    **Phase 6 ship 2026-05-23 — SETTINGS-01..04.** Hub con HTTP pull `rag_config` + `hub_registry` từ central qua Redis cache TTL 60s/300s + pub/sub invalidate `settings:invalidate` channel < 1s propagate. API key verify proxy hub con → central `POST /api/api-keys/verify` với shared secret `X-Internal-Auth: <SETTINGS_PROXY_SECRET>` 32-char min entropy.

    ### Backward Incompat Warning (TRIPLE cumulative — Phase 3 + Phase 5 + Phase 6)

    Trước khi deploy Phase 6, operator phải xử lý:

    1. **SETTINGS_PROXY_SECRET env (NEW Phase 6):** Hub con M2 cũ thiếu env → Settings validator `_enforce_settings_proxy_secret` raise `ValidationError` (length < 32 char) → uvicorn FAIL boot. Operator phải `openssl rand -hex 32` + paste vào `.env` TRƯỚC khi `docker compose up -d`.
    2. **CENTRAL_URL env (carry forward Phase 3 Plan 03-04):** Hub con cần CENTRAL_URL → lifespan settings_sync init dùng để fetch_initial.
    3. **CENTRAL_JWKS_URL env (carry forward Phase 3 Plan 03-02):** Hub con cần để verify JWT.

    Nếu thiếu bất kỳ env nào → docker compose interpolation `${VAR:?msg}` fail TRƯỚC khi container start (fail-loud expected).

    ### Deploy Procedure (5 step)

    **Step 1 — Generate shared secret:**

    ```bash
    # Generate 32-byte hex secret (64 char) — entropy 128-bit
    openssl rand -hex 32
    # Example output: a3f8b2c1d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1
    ```

    **Step 2 — Update `.env`:**

    ```bash
    # Append vào .env (production) hoặc .env.local (dev)
    cat >> .env <<EOF

    # Phase 6 System Settings Sync (SETTINGS-01..04 — 2026-05-23)
    SETTINGS_PROXY_SECRET=<paste-secret-from-step-1>
    SETTINGS_CACHE_TTL_RAG_CONFIG=60
    SETTINGS_CACHE_TTL_HUB_REGISTRY=300
    SETTINGS_CACHE_TTL_APIKEY=60
    SETTINGS_SUBSCRIBER_RECONNECT_SECONDS=5
    EOF

    # Security: chmod 600 .env (operator-only read)
    chmod 600 .env
    ```

    **Step 3 — Broadcast operator notice:**

    Vì cần restart cả 4 service (central + 3 hub con) để load env mới, expect ~15-30s downtime. Broadcast Slack/Email TRƯỚC khi deploy:

    > "Medinet Wiki deploy v3.0 Phase 6 lúc HH:MM. Expect 15-30s downtime. Sau deploy, mọi user phải re-login (nếu JWT M2 cũ KHÔNG có `kid`/`aud`/`hub_ids` claim) + settings cache Redis flush."

    **Step 4 — Stop + start docker-compose 4 service:**

    ```bash
    docker compose down  # graceful shutdown — subscriber_task.cancel() + wait_for(10s)
    docker compose up -d  # boot 4 service (central + 3 hub con) đồng thời
    ```

    Wait ~10-20s cho lifespan hub con `rag_client.fetch_initial()` blocking 5s + `hub_client.fetch_initial()` blocking 5s. Nếu central down hoặc network fail → hub con uvicorn exit 1 (boot fail-loud — operator catch).

    **Step 5 — Verify deployment:**

    ```bash
    # Check 4 service healthy
    curl -s http://localhost:8180/api/health  # central
    curl -s http://localhost:8181/api/health  # yte
    curl -s http://localhost:8182/api/health  # duoc
    curl -s http://localhost:8183/api/health  # hcns

    # Verify settings sync subscriber task running (qua docker logs)
    docker compose logs python-api-yte 2>&1 | grep "settings_subscriber_task_started"
    # Expected: "settings_subscriber_task_started: hub=yte"

    # Verify rag_config fetch_initial ready
    docker compose logs python-api-yte 2>&1 | grep "lifespan_settings_sync_ready"
    # Expected: "lifespan_settings_sync_ready: hub=yte central_url=http://python-api-central:8080"

    # End-to-end test pub/sub propagate (manual smoke):
    # 1. Đăng nhập admin ở central
    # 2. PUT /api/rag-config với body mới (vd đổi temperature 0.7 → 0.5)
    # 3. Trong < 2s, hub yte cache settings:rag_config:yte sẽ bị flush
    # 4. Yêu cầu next /api/ask ở yte sẽ re-fetch rag_config mới
    ```

    ### Rollback Procedure

    Nếu Phase 6 deploy fail (vd central /api/api-keys/verify endpoint trả 500, hub con boot fail-loud lifespan):

    ```bash
    # Option A: Hot fallback dùng escape hatch (testing/staging only)
    docker compose down
    export SETTINGS_SKIP_FETCH=1
    docker compose up -d python-api-yte python-api-duoc python-api-hcns
    # → hub con boot OK nhưng SETTINGS_UNAVAILABLE cho rag_config + apikey verify

    # Option B: Revert git commit Phase 6 + redeploy
    git revert <phase-6-commits>  # 5 plan = 5 commit cluster
    docker compose down
    docker compose build
    docker compose up -d
    # → fall back to Phase 5 v3.0-b state
    ```

    ### Smoke Defer Phase 7 MIGRATE-05

    Manual smoke runtime full (PUT central → all 3 hub con cache flush < 30s + apikey verify proxy → central round-trip + hub_registry pull) defer Phase 7 MIGRATE-05 full E2E (3 hub con + central + golden path + JWT SSO live + cross-hub search live + per-hub branding visual diff + settings sync pub/sub live propagate).

    Plan 06-05 Task 5b smoke checkpoint `skip smoke` auto-fallback per --auto chain + v3.0-b precedent (Plan 03-05 + 04-07 + 05-06 pre-resolved skip pattern). Evidence chain in-process 62+ unit + 6 integration test PASS cover semantic SETTINGS-01..04.

    ### Phase 6 Architecture Reference

    Chi tiết implementation:
    - `Hub_All/CLAUDE.md` §6 "Phase 6 System Settings Sync pattern" — 7 architecture insight + STRIDE coverage T-06-01..04.
    - `.planning/phases/06-system-settings-sync/06-CONTEXT.md` — 4 D-V3-Phase6-A..D LOCKED 2026-05-23.
    - `.planning/phases/06-system-settings-sync/06-PATTERNS.md` — Pattern map 14 file analog + 5 Wave grouping.
    - `.planning/phases/06-system-settings-sync/06-{01..05}-SUMMARY.md` — deliverable + commit + test count per plan.
    ```
  </action>
  <verify>
    <automated>cd Hub_All && grep -c "System Settings Sync Deploy Notes" README.md && grep -c "openssl rand -hex 32" README.md && grep -c "SETTINGS_PROXY_SECRET" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "System Settings Sync Deploy Notes" README.md >= 1` (section header).
    - `grep -c "openssl rand -hex 32" README.md >= 1` (gen secret hint).
    - `grep -c "SETTINGS_PROXY_SECRET" README.md >= 2` (deploy step + env doc).
    - `grep -c "Deploy Procedure (5 step)" README.md >= 1`.
    - `grep -c "Rollback Procedure" README.md >= 1`.
    - `grep -c "SETTINGS_SKIP_FETCH=1" README.md >= 1` (escape hatch documented).
    - `grep -c "settings:invalidate" README.md >= 1` (pub/sub channel mentioned).
    - `grep -c "Phase 6" README.md >= 3` (section header + multiple references).
  </acceptance_criteria>
  <done>README.md "System Settings Sync Deploy Notes (Phase 6 v3.0)" section ship với 5-step + rollback + escape hatch + smoke defer.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5b: Smoke checkpoint manual smoke 4 hub × PUT rag_config + apikey verify proxy</name>
  <what-built>
    Phase 6 đã ship 5 plan in-process verify:
    - Plan 06-01: 22 unit test (config + settings_sync/keys + metrics) + docker compose config exit 0.
    - Plan 06-02: 18 unit test (client + subscriber) + Pydantic schema validate.
    - Plan 06-03: 22 unit test (require_api_key branch + require_internal_auth + rag_config publish + /verify endpoint) + regression M2 auth PASS.
    - Plan 06-04: 6 integration test ASGI lifespan + main.py mypy strict clean + module import resolve OK.
    - Plan 06-05: Docs update CLAUDE.md + STATE.md + REQUIREMENTS.md + ROADMAP.md + README.md (Tasks 1-5a above).

    Evidence chain in-process: 62+ unit + 6 integration PASS cover SETTINGS-01..04 semantic. Manual smoke runtime full Phase 6 (compose up 4 service + PUT central rag_config + verify yte/duoc/hcns cache flush < 30s observed + apikey verify proxy 200 ↔ central round-trip + hub_registry singleton pull) cần Docker desktop running + 4 service boot + admin login + Redis observe.
  </what-built>
  <how-to-verify>
    **Manual smoke procedure (nếu user resume signal `approved`):**

    1. Generate SETTINGS_PROXY_SECRET + update .env per README.md "System Settings Sync Deploy Notes" Step 1-2.
    2. `docker compose down && docker compose up -d` (4 service boot ~15-20s — lifespan blocking fetch_initial 5s × 2 client).
    3. Verify boot:
       ```bash
       curl -s http://localhost:8180/api/health  # central 200
       curl -s http://localhost:8181/api/health  # yte 200
       docker compose logs python-api-yte 2>&1 | grep "lifespan_settings_sync_ready\|settings_subscriber_task_started"
       ```
    4. Admin login central → PUT /api/rag-config (đổi 1 field, vd `temperature: 0.7 → 0.5`):
       ```bash
       curl -X PUT http://localhost:8180/api/rag-config \
         -H "Authorization: Bearer <admin_jwt>" \
         -H "Content-Type: application/json" \
         -d '{"provider":"openai","model":"gpt-4","temperature":0.5,"dimensions":1536}'
       ```
    5. Verify central publish event:
       ```bash
       docker compose logs python-api-central 2>&1 | tail -10 | grep "settings_invalidate_published"
       ```
    6. Verify yte subscriber receive + flush trong < 2s:
       ```bash
       docker compose logs python-api-yte 2>&1 | tail -10 | grep "settings_invalidate_rag_config"
       ```
    7. Verify yte cache empty (Redis):
       ```bash
       docker compose exec redis redis-cli GET "settings:rag_config:yte"
       # Expected: (nil) — flushed
       ```
    8. Next call /api/ask ở yte → re-fetch rag_config → assert temperature=0.5 reflected.
    9. Test apikey verify proxy:
       ```bash
       curl -H "X-API-Key: <valid_api_key>" http://localhost:8181/api/usage
       # Expected: 200 — hub con dùng ApiKeyVerifyClient → central proxy verify
       docker compose logs python-api-yte 2>&1 | grep "apikey_verify"
       ```

    **Resume signal options:**
    - `approved` — operator verified all 9 steps PASS → proceed close Phase 6.
    - `skip smoke` — AUTO-FALLBACK default per v3.0-b precedent (Plan 03-05 + 04-07 + 05-06 pre-resolved skip pattern + --auto --chain mode active) → evidence chain in-process 62+ unit + 6 integration cover semantic; manual visual smoke runtime defer Phase 7 MIGRATE-05 full E2E.
    - `failed [details]` — operator hit issue → rollback per README.md Rollback Procedure + open issue ticket + revert plan.
  </how-to-verify>
  <resume-signal>Type `approved`, `skip smoke`, or `failed [details]`. Default per --auto --chain + v3.0-b precedent: `skip smoke`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs source-of-truth → Next planner consume | CLAUDE.md + STATE.md must reflect current state cho `/gsd-discuss-phase 7` consume; outdated → planner gen plan sai. |
| README.md operator procedure → Production deploy | Operator follow 5-step procedure; rollback procedure phải tested (skip smoke runtime — accept Plan 06-05). |
| Smoke checkpoint signal → Auto-chain control | `skip smoke` resume signal phải explicit (KHÔNG silent fall-through) — Plan 03-05 + 04-07 + 05-06 precedent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Repudiation | Docs claim Phase 6 DONE nhưng smoke chưa runtime | mitigate | Task 5b smoke `skip smoke` rationale rõ ràng (evidence chain in-process + defer Phase 7) — KHÔNG silently mark DONE; SUMMARY.md ghi cuối Phase 6 explicit "smoke runtime defer Phase 7 MIGRATE-05". |
| T-06-05-02 | Information Disclosure | README.md operator step expose secret in example | mitigate | README.md hướng dẫn `openssl rand -hex 32` + `chmod 600 .env` + KHÔNG hardcode secret thật; placeholder `<paste-secret-from-step-1>` rõ ràng. |
| T-06-05-03 | Tampering | STATE.md frontmatter format break next planner parse | mitigate | YAML frontmatter preserve structure Phase 5 + verify `yaml.safe_load` parse OK; gsd-sdk frontmatter.validate post-write. |
| T-06-05-04 | Repudiation | Rollback procedure chưa tested live | accept | Pattern carry forward Plan 03-05 + 04-07 + 05-06 — rollback procedure documented per `git revert <plan-N>` workflow; live runtime test defer Phase 7. |
| T-06-05-05 | Spoofing | `skip smoke` resume signal misuse production | mitigate | Resume signal `skip smoke` only valid trong --auto --chain context dev/staging; production deploy operator manual run smoke step + verify before mark DONE. |
</threat_model>

<verification>
**Wave 5 closeout verify khi cả 6 task PASS:**

```bash
cd Hub_All
# Verify 5 doc file updated
grep -c "Phase 6" CLAUDE.md  # >= 3
grep -c "Phase 6 Planning Summary" .planning/STATE.md  # >= 1
grep -c "\[x\] \*\*SETTINGS-0[1-4]\*\*" .planning/REQUIREMENTS.md  # >= 4
grep -c "✅ \*\*6\*\*" .planning/ROADMAP.md  # >= 1
grep -c "System Settings Sync Deploy Notes" README.md  # >= 1

# Frontmatter validate
python -c "import yaml; data = yaml.safe_load(open('.planning/STATE.md').read().split('---')[1]); print(data['progress'])"
# Expected: {'total_phases': 7, 'completed_phases': 6, 'total_plans': 33, 'completed_plans': 33, 'percent': 89}
```
</verification>

<success_criteria>
- CLAUDE.md §6 progress row Phase 6 ✅ DONE + Phase 6 pattern subsection (5 plan detail + 7 architecture insight + STRIDE coverage T-06-01..04 + Backward compat + R-V3-6 LOW mitigation + E-V3-4 carry forward + progress + Reference).
- STATE.md frontmatter (completed_phases=6 + total_plans=33 + completed_plans=33 + percent=89 + phase_6_status) + Current Position + Phase 6 Planning + Results Summary table.
- REQUIREMENTS.md SETTINGS-01..04 mark `[x]` ✅ Phase 6 + inline Closed 2026-05-23 Plan 06-XX note + NOTE Phase 6 closeout 5-plan list.
- ROADMAP.md Phase 6 row ✅ DONE 2026-05-23 + Plans list [x] + Decisions block 4 D-V3-Phase6 LOCKED + Progress table 33/~37 89%.
- README.md NEW section "System Settings Sync Deploy Notes (Phase 6 v3.0)" 5-step deploy + rollback procedure + escape hatch + smoke defer Phase 7.
- Smoke checkpoint Task 5b `skip smoke` auto-fallback per v3.0-b precedent (--auto --chain mode active).
- SUMMARY.md cuối Phase 6 ghi tổng kết 5 plan + decision + evidence chain.
</success_criteria>

<output>
After completion, create `.planning/phases/06-system-settings-sync/06-05-SUMMARY.md` + `.planning/phases/06-system-settings-sync/06-PHASE-CLOSEOUT-SUMMARY.md` (tổng kết Phase 6 5 plan):
- 5 doc file modified line count.
- Task 5b smoke `skip smoke` rationale rõ ràng.
- Evidence chain 62+ unit + 6 integration test PASS in-process.
- Backward incompat warning operator (TRIPLE Phase 3 + 5 + 6).
- Phase 6 deliverable summary + Backward compat + R-V3-6 mitigation + E-V3-4 carry forward.
- v3.0-b progress: 3/4 phase DONE (Phase 4+5+6 — 33/~37 plan ≈ 89%).
- Next: `/gsd-discuss-phase 7` Migration + Smoke E2E (MIGRATE-01..05 — final v3.0 phase).
</output>
