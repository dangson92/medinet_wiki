---
phase: 04-cross-hub-data-sync
plan: 07
type: execute
wave: 6
depends_on:
  - 04-06
files_modified:
  - Hub_All/CLAUDE.md
  - Hub_All/.planning/STATE.md
  - Hub_All/.planning/REQUIREMENTS.md
  - Hub_All/README.md
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
tags:
  - closeout
  - docs
  - phase4
  - smoke-checkpoint
user_setup: []

must_haves:
  truths:
    - "CLAUDE.md section 6 update Phase 4 DONE row + Phase 4 pattern subsection (outbox + worker + checksum + admin replay + cross-hub refactor)"
    - "STATE.md frontmatter v3.0 progress update — completed_phases 3→4, total_plans 15→22, percent ≈ 69%"
    - "REQUIREMENTS.md SYNC-01..05 mark [x] với note Phase 4 closeout 6 plan implementation + 9 D-V3-Phase4-A1..D3 LOCKED + 6 Prometheus metric"
    - "README.md add section 'Cross-hub Sync Deploy Notes (Phase 4 v3.0)' với env list mới (HUB_ID + CENTRAL_SYNC_DSN + CHECKSUM_HUB_DSNS_JSON + SYNC_BATCH_SIZE + ...) + admin replay curl example"
    - "Smoke checkpoint: human-verify hoặc skip pre-resolved per user decision — defer Phase 7 MIGRATE-05 full E2E runtime (carry forward pattern Plan 02-04 + 03-05)"
  artifacts:
    - path: "Hub_All/CLAUDE.md"
      provides: "Section 6 update v3.0 progress table + Phase 4 pattern subsection"
      contains: "Phase 4 — Cross-hub Data Sync"
      contains_alt: "outbox + worker"
    - path: "Hub_All/.planning/STATE.md"
      provides: "Frontmatter + Current Position + Phase 4 Planning Summary + Results Summary + Next Action update"
      contains: "Phase 4"
      contains_alt: "completed_phases: 4"
    - path: "Hub_All/.planning/REQUIREMENTS.md"
      provides: "SYNC-01..05 mark [x] + Phase 4 closeout note"
      contains: "[x] **SYNC-01**"
      contains_alt: "[x] **SYNC-05**"
    - path: "Hub_All/README.md"
      provides: "Cross-hub Sync Deploy Notes section mới"
      contains: "Cross-hub Sync"
      contains_alt: "HUB_ID="
  key_links:
    - from: "CLAUDE.md section 6"
      to: ".planning/phases/04-cross-hub-data-sync/04-*-SUMMARY.md"
      via: "Reference link 6 plan SUMMARY"
      pattern: "04-cross-hub-data-sync"
    - from: "STATE.md Results Summary"
      to: "5 SYNC REQ-ID + 6 Prometheus metric + Phase 4 deliverable"
      via: "Inline table 6 plan x objective x commits x tests"
      pattern: "SYNC-01..05"
---

<objective>
Wave 6 CLOSEOUT — docs update + smoke checkpoint cho Phase 4. Pattern carry forward Plan 02-04 + 03-05 (3 docs + 1 README + 1 checkpoint). Closeout 6 plan ship SYNC-01..05 + 9 D-V3-Phase4 decision + 6 Prometheus metric infrastructure.

Purpose:
- **Traceability:** CLAUDE.md section 6 progress table + Phase 4 pattern subsection cho dev tương lai onboard.
- **State integrity:** STATE.md frontmatter v3.0 progress (4/7 phase ≈ 57% nếu strict per phase / 22/~32 plan ≈ 69%).
- **REQUIREMENTS verifiable:** SYNC-01..05 mark `[x]` + cross-reference 6 plan SUMMARY.
- **Operator UX:** README.md section deploy notes cho Phase 4 env mới + admin replay example.
- **Smoke runtime defer:** Plan 02-04 + 03-05 pattern — Task 5 skip pre-resolved per user decision (evidence chain in-process unit + integration test PASS; runtime E2E defer Phase 7 MIGRATE-05).

Output:
- 4 docs update (CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md)
- 1 smoke checkpoint (human-verify hoặc skip)
- 1 SUMMARY.md tổng kết Phase 4
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/README.md

# Reference closeout pattern
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-05-PLAN.md
@Hub_All/.planning/phases/03-auth-sso-hub-ids-jwt/03-05-SUMMARY.md
@Hub_All/.planning/phases/02-hub-con-codebase-factor/02-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md section 6 update — v3.0 progress table + Phase 4 pattern subsection</name>
  <read_first>
    - Hub_All/CLAUDE.md TOÀN BỘ (đọc section 6 v3.0 progress table + Phase 3 pattern subsection style)
    - Hub_All/.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md (D-V3-Phase4-A1..D3 LOCKED + 6 metrics)
    - .planning/phases/04-cross-hub-data-sync/04-01..04-06-PLAN.md (deliverable scope mỗi plan)
  </read_first>
  <action>
    Sửa `Hub_All/CLAUDE.md` section 6:

    **A. v3.0 progress table** — Update row Phase 4:
    ```markdown
    | 4 — Cross-hub Data Sync | ✅ **DONE** | **6 plan** | **2026-05-22** | **SYNC-01..05 (5)** |
    ```

    Đổi `🚦 v3.0-a EXIT GATE` row — nếu user accept tiếp tục, có thể add note bên cạnh hoặc gộp progress.

    **B. Thêm subsection mới "Phase 4 Cross-hub Data Sync pattern (SYNC-01..05 — 2026-05-22)"** sau Phase 3 SSO pattern:

    ```markdown
    ### Phase 4 Cross-hub Data Sync pattern (SYNC-01..05 — 2026-05-22)

    6 plan đóng 5 REQ-ID Cross-hub Data Sync + 9 D-V3-Phase4 decision LOCKED + 6 Prometheus metric:

    - **Plan 04-01 SYNC-05 / D-V3-Phase4-A2+A4 — Per-hub Alembic 0005 sync_outbox + trigger:**
      Migration `api/migrations/versions/0005_sync_outbox_per_hub.py` (~250 LOC) tạo bảng
      `sync_outbox` (11 cột: id/op_type/chunk_id/document_id/payload/attempt_count/last_error/
      status/next_retry_at/created_at/processed_at + 2 CHECK constraint + 2 index) + Postgres
      function `enqueue_sync_outbox()` + 2 trigger AFTER INSERT/DELETE chunks + cột
      `documents.sync_status` enum (pending/syncing/synced/failed/partial). Skip-central
      runtime guard `if current_db == 'medinet_central': return` (sync_outbox per-hub-only).
      Per-hub Alembic env (Plan 01-03 carry forward) — `alembic -x hub=<name> upgrade head`.

    - **Plan 04-02 SYNC-01/03/04 / D-V3-Phase4-A5+C3+D2 — Settings 5 field + 3 validator + docker-compose env:**
      Settings field mới: `hub_id` (UUID khớp central.hubs.id) + `central_sync_dsn` (asyncpg pool tới central) +
      `checksum_hub_dsns_json` (central dict DSN tới N hub con) + `sync_batch_size=100` + `sync_poll_interval=5.0` +
      `sync_max_attempts=5` + `sync_backoff_seconds=[1,5,30,120]`. 3 model_validator hub con required HUB_ID +
      CENTRAL_SYNC_DSN; central optional CHECKSUM_HUB_DSNS_JSON (lazy connect). 1 length validator backoff =
      max_attempts-1. docker-compose 3 hub con env + override.yml.template FACTOR-04 placeholder extend.

    - **Plan 04-03 SYNC-01/02/05 / D-V3-Phase4-A1+A5+B1+B3 — app/sync module + 6 Prometheus collectors:**
      Module mới `api/app/sync/`: `keys.py` (re-export `stable_chunk_id` rag/flow.py + SQL constants
      CLAIM_PENDING_SQL FOR UPDATE SKIP LOCKED + PUSH_INSERT_CHUNK_SQL ON CONFLICT (id) DO UPDATE
      WHERE content_hash IS DISTINCT) + `models.py` (Pydantic SyncOutboxRow + ChunkPayload + SyncStatus +
      OpType enum) + `metrics.py` (6 Prometheus collector: SYNC_LAG_SECONDS histogram + SYNC_OUTBOX_PENDING
      gauge + SYNC_ATTEMPT_TOTAL counter + SYNC_DEAD_TOTAL counter + SYNC_COUNT_DRIFT gauge + SYNC_HASH_DRIFT
      counter + normalize_error_class helper) + `worker.py` (`sync_worker_loop` async + claim/push/mark
      helpers + exp backoff + dead path).

    - **Plan 04-04 SYNC-01/02/05 — Lifespan integration + rag/flow.py guard + 4 mock integration test:**
      `api/app/main.py` lifespan extend 2 step hub con: (a) init `central_sync_pool = await asyncpg.create_pool(CENTRAL_SYNC_DSN)`
      verify `SELECT current_database() == 'medinet_central'` fail-fast (T-04-04-01); (b) spawn
      `sync_worker_task = asyncio.create_task(sync_worker_loop(app))`. Graceful shutdown cancel +
      await timeout 10s + close pool TRƯỚC db_pool. `rag/flow.py::index_document` defensive guard skip
      nếu `doc_row['hub_id'] != settings.hub_id` (D-V3-Phase4-D2 enforce). 4 mock integration test
      (TestClient + AsyncMock asyncpg) + 2 live-DB test marked skipif (defer Phase 7 MIGRATE-05).

    - **Plan 04-05 SYNC-03 / D-V3-Phase4-D1+D3 — _search_cross_hub_impl refactor 1 SQL + central-only mount:**
      `services/search_service.py::_search_cross_hub_impl` thay fan-out `asyncio.gather(_search_one(hub_id))`
      bằng 1 SQL aggregated `_run_vector_query(hub_ids=hub_ids, all_hubs=False)` (WHERE hub_id = ANY($1::uuid[])
      ORDER BY vector <=> $embedding LIMIT $k). Public API `SearchService.search_cross_hub()` signature giữ
      NGUYÊN (backward compat M2 ask_service). `routers/search.py` tách 2 router: `search_router` universal
      local + `search_cross_hub_router` central-only (FACTOR-02 extend). `main.py` conditional mount cross-hub
      ở central block. `tests/integration/test_factor_hub_scoped.py` update endpoint matrix 9 central-only
      (was 8) + 4 test mới verify strip. HNSW iterative_scan=relaxed_order + ef_search=200 carry forward M2.

    - **Plan 04-06 SYNC-04 / D-V3-Phase4-C1+C2+C3 — Checksum scheduler + admin replay:**
      `api/app/observability/checksum_scheduler.py` mới (~150 LOC) — central lifespan asyncio task naive
      sleep loop (KHÔNG cần APScheduler dep). Daily 2AM COUNT(*) per hub con vs central → SYNC_COUNT_DRIFT
      gauge. Hourly TABLESAMPLE BERNOULLI(1) chunks last 1h → content_hash diff → SYNC_HASH_DRIFT counter
      (mismatch|missing). Lazy connect N asyncpg pool tới hub con qua Settings.checksum_hub_dsns. Per-hub
      error isolation (1 hub fail KHÔNG abort scheduler). Endpoint mới `POST /api/sync/replay` admin RBAC
      (require_role("admin") — Phase 3 SSO-04 carry forward) — reset dead rows trong sync_outbox hub con qua
      remote DSN (`UPDATE sync_outbox SET status='pending', attempt_count=0, last_error=NULL, next_retry_at=NULL
      WHERE status='dead' AND created_at >= since`). Mount conditional central-only (FACTOR-02 extend).

    - **Plan 04-07 closeout — docs + smoke checkpoint (file này note):**
      CLAUDE.md section 6 + STATE.md + REQUIREMENTS.md + README.md update. Task 5 smoke compose runtime
      SKIP pre-resolved per user decision — defer Phase 7 MIGRATE-05 full E2E. Evidence chain in-process
      unit + integration test cover semantic SYNC-01..05.

    **6 Prometheus metric mới (Phase 4 observability):**
    - `sync_lag_seconds{hub_id}` histogram — outbox.created_at → central INSERT processed_at
    - `sync_outbox_pending{hub_id}` gauge — số row pending hiện tại
    - `sync_attempt_total{hub_id, status=success|fail}` counter — cumulative push attempts
    - `sync_dead_total{hub_id, error_class=network|timeout|conflict|unknown}` counter — cumulative dead rows
    - `sync_count_drift{hub_id}` gauge — daily COUNT diff ratio
    - `sync_hash_drift{hub_id, drift_type=mismatch|missing}` counter — hourly hash sample diff

    **Backward compat (Phase 4 KHÔNG break M2):**
    - `SearchService.search_cross_hub(body, user)` public API signature unchanged — ask_service consumer giữ nguyên call.
    - Cross-hub endpoint URL `/api/search/cross-hub` giữ nguyên ở central — chỉ refactor implementation 1 SQL.
    - Hub con cross-hub endpoint strip → 404 (FACTOR-02 extend) — frontend M2 hardcode same-origin sẽ FAIL ở hub con cho tới Phase 5 PROXY-02 wire form action central.

    **R-V3-1 HIGH mitigation chain:**
    - Outbox INSERT atomic cùng transaction chunks INSERT (Plan 04-01 trigger).
    - Worker ON CONFLICT (id) DO UPDATE WHERE content_hash IS DISTINCT — idempotent retry KHÔNG dup (Plan 04-03 + B1).
    - Exp backoff [1, 5, 30, 120]s + max 5 attempts → dead status + Prometheus alert (Plan 04-03 + A5).
    - Daily COUNT drift + Hourly TABLESAMPLE hash sample (Plan 04-06 + C1) → SYNC_COUNT_DRIFT + SYNC_HASH_DRIFT metric.
    - Admin replay endpoint manual recovery (Plan 04-06 + C2).

    **E-V3-2 cross-hub p95 < 1.5s carry forward:**
    - HNSW iterative_scan=relaxed_order + ef_search=200 carry forward M2 Phase 6.
    - 1 SQL aggregated thay fan-out N asyncio task → giảm overhead orchestration (Plan 04-05 + D1).
    - Live measure defer Phase 7 MIGRATE-05 runtime smoke.

    Reference:
    - `.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md` — 9 D-V3-Phase4-A1..D3 LOCKED.
    - `.planning/phases/04-cross-hub-data-sync/04-{01..06}-PLAN.md` — implementation chi tiết 6 plan.
    - `.planning/phases/04-cross-hub-data-sync/04-{01..06}-SUMMARY.md` — deliverable + commit + test count per plan.
    ```

    **C. Footer update** "Cập nhật:" line cuối CLAUDE.md với date + phase 4 done note.
  </action>
  <verify>
    Markdown structure preserve — KHÔNG break 5 section gốc; YAML/code block markdown vẫn parse.
  </verify>
  <acceptance_criteria>
    - `grep -n "Phase 4 — Cross-hub Data Sync" Hub_All/CLAUDE.md` returns ≥ 2 lines (progress table + section header).
    - `grep -n "Plan 04-01" Hub_All/CLAUDE.md` returns ≥ 1 line.
    - `grep -n "Plan 04-06" Hub_All/CLAUDE.md` returns ≥ 1 line.
    - `grep -n "SYNC-01..05" Hub_All/CLAUDE.md` returns ≥ 1 line.
    - `grep -n "D-V3-Phase4" Hub_All/CLAUDE.md` returns ≥ 3 lines (3+ decision reference).
    - `grep -n "sync_lag_seconds" Hub_All/CLAUDE.md` returns ≥ 1 line.
    - `grep -n "outbox + worker" Hub_All/CLAUDE.md` returns ≥ 1 line.
    - Section count unchanged — `grep -n "^## " Hub_All/CLAUDE.md | wc -l` returns same number as before (5-6 sections).
    - Markdown lint (nếu có): `npx markdownlint Hub_All/CLAUDE.md 2>&1 | head -10` no critical errors.
  </acceptance_criteria>
  <done>CLAUDE.md section 6 Phase 4 DONE row + Phase 4 pattern subsection (6 plan detail + 6 metric + R-V3-1 mitigation + E-V3-2 carry forward) + footer changelog; structure preserve.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md update — frontmatter + Current Position + Phase 4 Planning + Results Summary + Next Action</name>
  <read_first>
    - Hub_All/.planning/STATE.md TOÀN BỘ (đọc Phase 3 closeout pattern Plan 03-05 — Current Position + Phase 3 Planning + Results Summary + Next Action structure)
    - .planning/phases/04-cross-hub-data-sync/04-*-PLAN.md (6 plan deliverable cho Results Summary table)
  </read_first>
  <action>
    Sửa `Hub_All/.planning/STATE.md`:

    **A. Frontmatter** — Update status + last_updated + progress:
    ```yaml
    status: "Phase 4 DONE 2026-05-22 ✅ — Cross-hub Data Sync SYNC-01..05 6 plan ship. Outbox + worker pattern (sync_outbox per-hub-con + Postgres trigger AFTER INSERT/DELETE chunks atomic) + idempotent push central ON CONFLICT (id) DO UPDATE WHERE content_hash IS DISTINCT + exp backoff [1,5,30,120]s max 5 attempts → dead + admin replay endpoint + checksum scheduler daily count + hourly TABLESAMPLE hash + 6 Prometheus metric + cross-hub search refactor 1 SQL aggregated (E-V3-2 p95 < 1.5s carry forward) + hub con strip /api/search/cross-hub (FACTOR-02 extend) + Settings 5 field mới + 3 validator boot fail-fast + docker-compose env wire. 6 plans / ~30 commits / 60+ unit test + 4+ integration test PASS in-process. v3.0-b progress: Phase 4 DONE (1/4 phase v3.0-b — 22/~32 plan ≈ 69%). Next: /gsd-discuss-phase 5 Reverse Proxy + Frontend Subpath (PROXY-01..04 + D-V3-06 D6 expire formally + GA-V3-C confirm 1 build detect prefix)."
    last_updated: "2026-05-22T<time>.000Z"
    progress:
      total_phases: 7
      completed_phases: 4
      total_plans: 22
      completed_plans: 22
      percent: 69
    ```

    **B. Current Position section** — Update Phase 4 done:
    ```markdown
    - **Phase:** 4 — Cross-hub Data Sync ✅ **DONE 2026-05-22 — SYNC-01..05 fully shipped (Wave 1 + Wave 2 parallel + Wave 3 + Wave 4 + Wave 5 + Wave 6 ✅)**
    - **Plan:** 6/6 plans complete (04-01..04-07; numbering jump 04-07 = closeout)
    - **Status:** Phase 4 closed SYNC-01..05 — [chi tiết similar Phase 3 status, 1-2 paragraph cover 9 D-V3-Phase4 LOCKED + outbox pattern + worker + checksum + replay + cross-hub refactor + 6 metric].
    - **Last activity:** 2026-05-22 — `/gsd-execute-phase 4` wave-based execution complete cho SYNC-01..05:
      - **04-01 DONE ✅** (Wave 1 BLOCKING): [summary]
      - **04-02 DONE ✅** (Wave 2 parallel): [summary]
      - **04-03 DONE ✅** (Wave 2 parallel): [summary]
      - **04-04 DONE ✅** (Wave 3): [summary]
      - **04-05 DONE ✅** (Wave 4): [summary]
      - **04-06 DONE ✅** (Wave 5): [summary]
      - **04-07 DONE ✅** (Wave 6 closeout): [summary]
    ```

    **C. Phase 4 Planning Summary table** — sau Phase 3 Planning Summary:
    ```markdown
    ## Phase 4 Planning Summary

    | Plan | Wave | Objective | Tasks | Files modified | REQ | Status |
    |------|------|-----------|-------|----------------|-----|--------|
    | 04-01 | 1 BLOCKING | Per-hub Alembic 0005 sync_outbox + trigger + documents.sync_status enum | 2 (auto + tdd) | `migrations/versions/0005_sync_outbox_per_hub.py` (new), `migrations/env.py`, `tests/unit/test_sync_outbox_migration.py` (new) | SYNC-05, SYNC-01, SYNC-02 | ✅ **DONE 2026-05-22** |
    | 04-02 | 2 parallel | Settings 5 field + 3 model_validator + docker-compose env wire | 2 (tdd + auto) | `app/config.py`, `tests/unit/test_config_phase4_fields.py` (new), `docker-compose.yml`, `docker-compose.override.yml.template` | SYNC-01, SYNC-03, SYNC-04 | ✅ **DONE 2026-05-22** |
    | 04-03 | 2 parallel | app/sync module (keys + models + metrics + worker) + 6 Prometheus collector + ~27 unit test | 3 (tdd × 3) | `app/sync/{__init__,keys,models,metrics,worker}.py` (new), `tests/unit/test_sync_{models,metrics,worker}.py` (new) | SYNC-01, SYNC-02, SYNC-05 | ✅ **DONE 2026-05-22** |
    | 04-04 | 3 | Lifespan integration central_sync_pool + sync_worker_task + rag/flow.py guard + 4 integration test | 3 (auto) | `app/main.py`, `app/rag/flow.py`, `tests/integration/test_sync_lifespan_integration.py` (new) | SYNC-01, SYNC-02, SYNC-05 | ✅ **DONE 2026-05-22** |
    | 04-05 | 4 | _search_cross_hub_impl refactor 1 SQL aggregated + tách 2 router central-only + endpoint matrix update | 3 (tdd + 2 auto) | `app/services/search_service.py`, `app/main.py`, `app/routers/search.py`, `tests/unit/test_search_cross_hub_refactor.py` (new), `tests/integration/test_factor_hub_scoped.py` | SYNC-03 | ✅ **DONE 2026-05-22** |
    | 04-06 | 5 | observability/checksum_scheduler.py + POST /api/sync/replay admin endpoint + central lifespan spawn | 2 (tdd × 2) | `app/observability/checksum_scheduler.py` (new), `app/routers/sync.py`, `app/main.py`, `tests/unit/test_checksum_scheduler.py` (new), `tests/unit/test_sync_replay_endpoint.py` (new) | SYNC-04 | ✅ **DONE 2026-05-22** |
    | 04-07 | 6 closeout | CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md + smoke checkpoint | 5 (4 docs + 1 checkpoint) | `Hub_All/CLAUDE.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `Hub_All/README.md` | SYNC-01..05 closeout | Ready (Wave 6 — closeout) |

    **Coverage:** 5/5 REQ (SYNC-01..05) covered ≥ 1 plan/REQ.

    **Critical path:** 04-01 ✅ (Wave 1 BLOCKING) → 04-02 ⊥ 04-03 (parallel Wave 2 file-disjoint) → 04-04 (Wave 3 depend 04-01..03) → 04-05 (Wave 4 depend 04-04) → 04-06 (Wave 5 depend 04-05) → 04-07 (Wave 6 closeout).

    **Auto-chain pause expected:** Plan 04-07 Task 5 (`checkpoint:human-action gate=blocking`) — smoke compose runtime central + 3 hub con. User resume signal: `skip smoke` (pre-resolved 2026-05-22 — defer Phase 7 MIGRATE-05 full E2E).
    ```

    Thêm Performance Metrics + Results Summary table tương tự Phase 3.

    **D. Phase 4 Results Summary section** (sau Phase 3 Results Summary):
    ```markdown
    ## Phase 4 Results Summary

    | Plan | Wave | Objective | Commits | Tests |
    |------|------|-----------|---------|-------|
    | 04-01 | 1 BLOCKING | Per-hub Alembic 0005 sync_outbox + trigger + documents.sync_status | [TBD execute] | unit ≥ 10 PASS |
    | 04-02 | 2 | Settings 5 field + 3 validator + docker-compose env | [TBD] | unit ≥ 12 PASS |
    | 04-03 | 2 | app/sync module 5 file + 6 Prometheus metric | [TBD] | unit ≥ 27 PASS |
    | 04-04 | 3 | Lifespan integration + rag/flow.py guard + 4 integration mock | [TBD] | unit + integration ≥ 4 PASS |
    | 04-05 | 4 | _search_cross_hub_impl refactor 1 SQL + central-only mount | [TBD] | unit ≥ 8 + integration ≥ 4 PASS |
    | 04-06 | 5 | Checksum scheduler + POST /api/sync/replay admin endpoint | [TBD] | unit ≥ 18 PASS |
    | 04-07 | 6 closeout | CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md + smoke SKIP | [TBD] | docs grep acceptance PASS |

    **Phase 4 deliverable summary:**
    - Outbox + worker pattern per-hub-con — sync_outbox table + Postgres trigger AFTER INSERT/DELETE chunks atomic + worker poll batch 100/5s + SKIP LOCKED concurrency-safe.
    - Idempotent push central — ON CONFLICT (id) DO UPDATE WHERE content_hash IS DISTINCT FROM EXCLUDED.content_hash (1 statement atomic, no race window).
    - Sync timing async decoupled — document.status=completed ngay sau cocoindex INSERT chunks; document.sync_status enum (pending/syncing/synced/failed/partial) Alembic migration.
    - Failure handling — exp backoff [1, 5, 30, 120]s + max 5 attempts → status='dead' + Prometheus alert; admin endpoint `POST /api/sync/replay { hub_id, since }` reset dead rows.
    - Checksum scheduler central lifespan task — daily 2AM COUNT(*) + hourly TABLESAMPLE BERNOULLI(1) content_hash diff per hub con DSN; 6 Prometheus metric infrastructure.
    - Cross-hub search refactor — 1 SQL aggregated `WHERE hub_id = ANY($1::uuid[]) ORDER BY vector <=> $embedding LIMIT $k` thay fan-out N asyncio task; HNSW iterative_scan=relaxed_order + ef_search=200 carry forward M2 Phase 6.
    - Hub con strip `/api/search/cross-hub` mount (FACTOR-02 extend) — 404 envelope D6.
    - Settings 5 field mới: hub_id + central_sync_dsn + checksum_hub_dsns_json + sync_batch_size + sync_poll_interval + sync_max_attempts + sync_backoff_seconds + 3 model_validator hub con required + 1 length validator backoff = max_attempts-1.
    - docker-compose env wire 3 hub con (HUB_ID + CENTRAL_SYNC_DSN) + central (CHECKSUM_HUB_DSNS_JSON) + override.yml.template FACTOR-04 placeholder extend.
    - 9 decision LOCKED D-V3-Phase4-A1..D3 consumed (outbox mechanism + sync_outbox schema + worker placement + trigger DDL + worker loop config + idempotent ON CONFLICT + sync timing async + event-typed insert/delete + checksum cadence + failure mode + checksum runner placement + cross-hub refactor in-place + hub_id UUID resolution + strip endpoint hub con).
    - ~40 STRIDE threat mitigation breakdown: Plan 04-01 (5: 3 mitigate + 2 accept) + Plan 04-02 (6: 5 mitigate + 1 accept) + Plan 04-03 (7: 6 mitigate + 1 accept) + Plan 04-04 (6: 5 mitigate + 1 accept) + Plan 04-05 (6: 5 mitigate + 1 accept) + Plan 04-06 (6: 5 mitigate + 1 accept) ≈ **36 threat addressed**.

    **v3.0-b progress: Phase 4 DONE (1/4 phase v3.0-b — 22/~32 plan ≈ 69%). Next: /gsd-discuss-phase 5 Reverse Proxy + Frontend Subpath (PROXY-01..04 — Caddy subpath routing + frontend prefix detect 1 build + D-V3-06 D6 expire formally + per-hub login branding; GA-V3-C confirm).**
    ```

    **E. Next Action section** — Update để promote Phase 5:
    ```markdown
    ## Next Action

    1. **(Recommended) `/gsd-discuss-phase 5`** — Reverse Proxy + Frontend Subpath (GA-V3-C chốt: 1 build detect prefix vs per-hub build matrix; D-V3-06 D6 expire formally; Caddy auto-TLS extend route `wiki.domain.com/<hub>/api/*` strip prefix; per-hub login branding tách `frontend/src/branding/<hub>/`).
    2. (Optional) `/gsd-code-review 4` — advisory code review trên ~30 commits Phase 4 (workflow.code_review gate), phủ 6 plan ship SYNC-01..05 + 9 decision LOCKED D-V3-Phase4-A1..D3 + 36 STRIDE threat + 6 Prometheus metric infrastructure.
    3. (Optional) `/gsd-verify-work 4` — manual UAT 5 SC nếu user muốn extra verify ngoài automated test (Plan 04-07 Task 5 smoke compose runtime SKIP pre-resolved, sẽ verify ở Phase 7 MIGRATE-05 runtime smoke E2E live).
    ```
  </action>
  <verify>
    Markdown structure preserve + YAML frontmatter parse OK.
  </verify>
  <acceptance_criteria>
    - `grep -n "completed_phases: 4" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - `grep -n "completed_plans: 22" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - `grep -n "percent: 69" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - `grep -n "Phase 4 Planning Summary" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - `grep -n "Phase 4 Results Summary" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - `grep -n "SYNC-01..05 closeout" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - `grep -n "Phase 4 — Cross-hub Data Sync ✅ \*\*DONE 2026-05-22" Hub_All/.planning/STATE.md` returns ≥ 1 line.
    - YAML frontmatter parse OK (Python yaml.safe_load test pass).
  </acceptance_criteria>
  <done>STATE.md frontmatter + Current Position + 2 new section (Phase 4 Planning Summary + Phase 4 Results Summary) + Next Action promote Phase 5; YAML parse OK.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md SYNC-01..05 mark [x] + Phase 4 closeout note</name>
  <read_first>
    - Hub_All/.planning/REQUIREMENTS.md TOÀN BỘ (Section SYNC — 5 REQ-ID + pattern SSO-01..04 mark [x] Plan 03-05)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md (9 decision reference cho note)
  </read_first>
  <action>
    Sửa `Hub_All/.planning/REQUIREMENTS.md` Section SYNC:

    **A. Mark 5 SYNC REQ-ID `[x]`:**
    ```markdown
    - [x] **SYNC-01** ✅ Phase 4: ... — **DONE 2026-05-22** (Plan 04-01..04 sync_outbox + trigger + worker + lifespan integration; Plan 04-01 per-hub Alembic 0005 + Postgres function enqueue_sync_outbox + 2 trigger AFTER INSERT/DELETE chunks atomic; Plan 04-02 Settings.hub_id + central_sync_dsn boot fail-fast; Plan 04-03 worker loop + ChunkPayload Pydantic + PUSH_INSERT_CHUNK_SQL; Plan 04-04 lifespan central_sync_pool init verify SELECT current_database() == 'medinet_central' fail-fast + sync_worker_task spawn).
    - [x] **SYNC-02** ✅ Phase 4: ... — **DONE 2026-05-22** (Plan 04-03 ON CONFLICT (id) DO UPDATE WHERE content_hash IS DISTINCT FROM EXCLUDED.content_hash — 1 statement atomic D-V3-Phase4-B1; chunk_id stable UUID5 re-export `stable_chunk_id` rag/flow.py qua sync.keys; content_hash filter tránh UPDATE no-op bảo vệ HNSW vector index disk write thừa).
    - [x] **SYNC-03** ✅ Phase 4: ... — **DONE 2026-05-22** (Plan 04-05 SearchService._search_cross_hub_impl refactor 1 SQL aggregated WHERE hub_id = ANY($1::uuid[]) ORDER BY vector <=> embedding LIMIT k thay fan-out asyncio.gather; public API SearchService.search_cross_hub() signature unchanged backward compat M2 ask_service; routers/search.py tách 2 router universal + central-only; main.py conditional mount; tests/integration/test_factor_hub_scoped.py update endpoint matrix 9 central-only; HNSW iterative_scan=relaxed_order + ef_search=200 carry forward M2 Phase 6; E-V3-2 p95 < 1.5s defer Phase 7 MIGRATE-05 runtime smoke).
    - [x] **SYNC-04** ✅ Phase 4: ... — **DONE 2026-05-22** (Plan 04-06 observability/checksum_scheduler.py central lifespan asyncio task — daily 2AM COUNT(*) per hub_id → SYNC_COUNT_DRIFT gauge + hourly TABLESAMPLE BERNOULLI(1) chunks created last 1h content_hash diff → SYNC_HASH_DRIFT counter mismatch|missing; naive asyncio.sleep loop KHÔNG cần APScheduler dep; lazy connect N asyncpg pool tới hub con qua Settings.checksum_hub_dsns; per-hub error isolation; POST /api/sync/replay admin RBAC endpoint reset dead rows; D-V3-Phase4-C1+C2+C3 LOCKED).
    - [x] **SYNC-05** ✅ Phase 4: ... — **DONE 2026-05-22** (Plan 04-01..04 outbox + worker mechanism — REJECT cocoindex target thứ 2 + Postgres logical replication; per-hub sync_outbox table + Postgres trigger AFTER INSERT/DELETE chunks atomic enqueue + in-process asyncio worker hub con lifespan poll batch 100/5s + SELECT FOR UPDATE SKIP LOCKED + exp backoff [1, 5, 30, 120]s + max 5 attempts → status='dead' + 6 Prometheus metric infrastructure; D-V3-Phase4-A1..A5 LOCKED).
    ```

    **B. Thêm NOTE Phase 4 closeout** sau SYNC-05 (pattern similar SSO-04 Phase 3 closeout note):
    ```markdown
      > **NOTE Phase 4 closeout (Plan 04-07 SYNC-01..05 — 2026-05-22):** 6 plan ship đầy đủ 5 REQ-ID:
      >
      > 1. Plan 04-01 — Per-hub Alembic 0005 sync_outbox + trigger (D-V3-Phase4-A2/A4)
      > 2. Plan 04-02 — Settings 5 field + 3 validator + docker-compose env (D-V3-Phase4-A5/C3/D2)
      > 3. Plan 04-03 — app/sync module 5 file + 6 Prometheus collector (D-V3-Phase4-A1/A5/B1/B3)
      > 4. Plan 04-04 — Lifespan integration central_sync_pool + sync_worker_task + rag/flow.py guard
      > 5. Plan 04-05 — _search_cross_hub_impl refactor 1 SQL + central-only mount (D-V3-Phase4-D1/D3)
      > 6. Plan 04-06 — Checksum scheduler central lifespan + admin POST /api/sync/replay (D-V3-Phase4-C1/C2/C3)
      > 7. Plan 04-07 — Closeout docs (CLAUDE.md + STATE.md + REQUIREMENTS.md + README.md) + smoke skip pre-resolved (defer Phase 7 MIGRATE-05)
      >
      > **Reference:** `.planning/phases/04-cross-hub-data-sync/04-{01..07}-SUMMARY.md` chi tiết deliverable.
      >
      > **Backward incompat:** Hub con `/api/search/cross-hub` strip → 404 envelope D6 NOT_FOUND (FACTOR-02 extend); M2 frontend hardcode same-origin sẽ FAIL ở hub con cho tới Phase 5 PROXY-02 wire base URL detect prefix. Acceptable dev v3.0-b.
      >
      > **v3.0-b progress:** Phase 4 DONE (1/4 phase v3.0-b — 22/~32 plan ≈ 69%). Next: /gsd-discuss-phase 5 Reverse Proxy + Frontend Subpath.
    ```
  </action>
  <verify>
    Section SYNC structure preserve + Traceability table cuối file intact.
  </verify>
  <acceptance_criteria>
    - `grep -n "\\[x\\] \\*\\*SYNC-01\\*\\*" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - `grep -n "\\[x\\] \\*\\*SYNC-02\\*\\*" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - `grep -n "\\[x\\] \\*\\*SYNC-03\\*\\*" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - `grep -n "\\[x\\] \\*\\*SYNC-04\\*\\*" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - `grep -n "\\[x\\] \\*\\*SYNC-05\\*\\*" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - `grep -n "NOTE Phase 4 closeout" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - `grep -n "D-V3-Phase4-A1..A5" Hub_All/.planning/REQUIREMENTS.md` returns ≥ 1 line.
    - Traceability table intact (REQ-ID → Phase mapping count unchanged).
  </acceptance_criteria>
  <done>SYNC-01..05 mark [x] với detailed inline note Phase 4 closeout + NOTE block 7-step plan listing; Traceability table preserve.</done>
</task>

<task type="auto">
  <name>Task 4: README.md — Cross-hub Sync Deploy Notes section mới</name>
  <read_first>
    - Hub_All/README.md TOÀN BỘ (đọc "SSO Backward Incompat (Phase 3 v3.0)" section pattern Plan 03-05 cho format reference)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md (env list mới Settings 5 field + 6 metric)
  </read_first>
  <action>
    Append `Hub_All/README.md` section mới SAU section "SSO Backward Incompat":

    ```markdown
    ## Cross-hub Sync Deploy Notes (Phase 4 v3.0)

    Phase 4 ship cross-hub data sync infrastructure — chunks + vector denormalized push từ hub con → central qua outbox + worker pattern (D-V3-02 LOCKED).

    ### Env vars mới (operator MUST set trước `docker compose up`)

    **Hub con (yte/duoc/hcns/dynamic FACTOR-04):**

    | Env | Required | Format | Note |
    |-----|----------|--------|------|
    | `HUB_YTE_ID` | ✅ | UUID4 | Khớp `medinet_central.hubs.id` row UUID — Phase 7 MIGRATE-01 sẽ centralize. v3.0-b operator export manual |
    | `HUB_DUOC_ID` | ✅ | UUID4 | Tương tự |
    | `HUB_HCNS_ID` | ✅ | UUID4 | Tương tự |
    | `CENTRAL_SYNC_DSN` | ✅ (hub con) | asyncpg DSN tới `medinet_central` | Hardcoded ở docker-compose 3 hub con; production deploy qua secrets backing |
    | `SYNC_BATCH_SIZE` | optional | int (default 100) | Worker claim batch size — operator tune sau observe metrics |
    | `SYNC_POLL_INTERVAL` | optional | float seconds (default 5.0) | Idle sleep khi outbox empty |
    | `SYNC_MAX_ATTEMPTS` | optional | int (default 5) | Mark dead sau N attempts |
    | `SYNC_BACKOFF_SECONDS` | optional | CSV "1,5,30,120" (length = MAX_ATTEMPTS-1) | Exp backoff retry seconds |

    **Central:**

    | Env | Required | Format | Note |
    |-----|----------|--------|------|
    | `CHECKSUM_HUB_DSNS_JSON` | optional | JSON dict `{"yte":"asyncpg DSN read-only","duoc":"..."}` | Central checksum scheduler connect tới N hub con; empty/None → scheduler no-op (deploy lần đầu CHƯA register hub con) |

    ### Export env shell example

    ```bash
    export HUB_YTE_ID="00000000-0000-0000-0000-000000000001"
    export HUB_DUOC_ID="00000000-0000-0000-0000-000000000002"
    export HUB_HCNS_ID="00000000-0000-0000-0000-000000000003"
    export CHECKSUM_HUB_DSNS_JSON='{"yte":"postgresql+asyncpg://medinet_ro:medinet@postgres:5432/medinet_hub_yte","duoc":"postgresql+asyncpg://medinet_ro:medinet@postgres:5432/medinet_hub_duoc","hcns":"postgresql+asyncpg://medinet_ro:medinet@postgres:5432/medinet_hub_hcns"}'
    docker compose up -d
    ```

    ### Per-hub Alembic migration

    Apply migration 0005 (sync_outbox per-hub):
    ```bash
    cd api
    make migrate-all  # apply 4 DB sequentially: central + yte + duoc + hcns
    # rev 0005 SKIP central runtime guard (sync_outbox per-hub-only)
    alembic -x hub=yte current  # expect rev "0005" (head)
    alembic current             # expect rev "0005" (central — guard skip nhưng vẫn ghi rev alembic_version table)
    ```

    ### Prometheus metrics mới

    Scrape `/metrics` endpoint central + hub con — 6 metric mới:
    - `sync_lag_seconds{hub_id}` (histogram) — outbox → central INSERT lag
    - `sync_outbox_pending{hub_id}` (gauge) — current pending count
    - `sync_attempt_total{hub_id, status}` (counter) — cumulative attempts (success/fail)
    - `sync_dead_total{hub_id, error_class}` (counter) — cumulative dead (network/timeout/conflict/unknown)
    - `sync_count_drift{hub_id}` (gauge) — daily ratio diff
    - `sync_hash_drift{hub_id, drift_type}` (counter) — hourly TABLESAMPLE diff (mismatch/missing)

    Recommended AlertManager rules (defer Phase 7 deploy guide):
    - `sync_count_drift > 0.01` sustained 7 days → STOP (E-V3-5 trigger)
    - `sync_hash_drift_total > 0` increase last 1h → Slack alert

    ### Admin replay endpoint

    ```bash
    # Replay dead rows trong sync_outbox hub yte since 2026-05-22 (manual recovery)
    curl -X POST https://central.medinet.vn/api/sync/replay \
      -H "Authorization: Bearer $ADMIN_JWT" \
      -H "Content-Type: application/json" \
      -d '{"hub_id":"yte","since":"2026-05-22T00:00:00Z"}'

    # Response D6:
    # {"success":true,"data":{"hub_id":"yte","replayed_count":42,"since":"2026-05-22T00:00:00+00:00"},"error":null,"meta":null}
    ```

    Endpoint chỉ available ở central (FACTOR-02 extend — hub con strip → 404 NOT_FOUND envelope). Require admin role (Phase 3 SSO-04 require_role("admin") dependency).

    ### Cross-hub search behavior change

    Hub con `POST /api/search/cross-hub` → **404 NOT_FOUND envelope** (FACTOR-02 extend Plan 04-05 D-V3-Phase4-D3). Frontend M2 hardcode same-origin sẽ FAIL ở hub con cho tới Phase 5 PROXY-02 wire base URL detect prefix.

    Central `POST /api/search/cross-hub` giữ behavior M2 (1 SQL aggregated thay fan-out — performance carry forward HNSW iterative_scan + ef_search=200). Public API signature unchanged.

    ### Rollback procedure

    Nếu sync drift > 1% sustained 7 days (E-V3-5 trigger):
    1. Stop worker: `docker compose stop python-api-yte python-api-duoc python-api-hcns`
    2. Verify outbox state: `psql -d medinet_hub_yte -c "SELECT status, COUNT(*) FROM sync_outbox GROUP BY status"`
    3. Replay dead: `POST /api/sync/replay { hub_id, since: <root-cause-fix-date> }`
    4. Resume worker: `docker compose start python-api-yte python-api-duoc python-api-hcns`
    5. Re-discuss GA-V3-D mechanism (xem `.planning/PROJECT.md` EXIT criteria E-V3-5).

    ### Reference

    - `.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md` — 9 D-V3-Phase4-A1..D3 LOCKED.
    - `.planning/phases/04-cross-hub-data-sync/04-{01..07}-PLAN.md` — 6 plan implementation chi tiết.
    - `.planning/phases/04-cross-hub-data-sync/04-{01..07}-SUMMARY.md` — deliverable + commit + test per plan.
    ```
  </action>
  <verify>
    README.md structure preserve — existing sections (Add a new hub, SSO Backward Incompat, Milestone status, ...) intact.
  </verify>
  <acceptance_criteria>
    - `grep -n "Cross-hub Sync Deploy Notes" Hub_All/README.md` returns ≥ 1 line.
    - `grep -n "HUB_YTE_ID" Hub_All/README.md` returns ≥ 1 line.
    - `grep -n "CHECKSUM_HUB_DSNS_JSON" Hub_All/README.md` returns ≥ 1 line.
    - `grep -n "SYNC_BATCH_SIZE" Hub_All/README.md` returns ≥ 1 line.
    - `grep -n "sync_lag_seconds" Hub_All/README.md` returns ≥ 1 line.
    - `grep -n "POST /api/sync/replay" Hub_All/README.md` returns ≥ 1 line.
    - `grep -n "Admin replay endpoint" Hub_All/README.md` returns ≥ 1 line.
    - Existing section "SSO Backward Incompat (Phase 3 v3.0)" intact.
    - Existing section "Add a new hub (dynamic registration — FACTOR-04 Plan 02-05)" intact.
  </acceptance_criteria>
  <done>README section mới "Cross-hub Sync Deploy Notes (Phase 4 v3.0)" với env list + Alembic + Prometheus + admin replay curl example + rollback procedure + reference; existing sections preserve.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5 (CHECKPOINT): Smoke compose runtime — Phase 4 sync E2E</name>
  <what-built>
    6 plan Phase 4 đã ship đầy đủ SYNC-01..05 + 9 D-V3-Phase4 decision + 6 Prometheus metric infrastructure:
    - Migration 0005 sync_outbox per-hub + trigger AFTER INSERT/DELETE chunks atomic
    - Settings 5 field mới + 3 validator boot fail-fast + docker-compose env wire
    - app/sync module (keys + models + metrics + worker) — outbox poll + push central + exp backoff + dead path
    - Lifespan integration hub con central_sync_pool + sync_worker_task + rag/flow.py guard
    - _search_cross_hub_impl refactor 1 SQL aggregated thay fan-out + tách central-only router mount
    - checksum_scheduler.py central lifespan + POST /api/sync/replay admin endpoint
    - 60+ unit test + 4+ integration test in-process PASS (mock asyncpg pool)
  </what-built>
  <how-to-verify>
    **Smoke compose runtime E2E (manual operator):**

    1. Export 3 dummy hub_id UUID + CHECKSUM_HUB_DSNS_JSON:
       ```bash
       export HUB_YTE_ID="00000000-0000-0000-0000-000000000001"
       export HUB_DUOC_ID="00000000-0000-0000-0000-000000000002"
       export HUB_HCNS_ID="00000000-0000-0000-0000-000000000003"
       export CHECKSUM_HUB_DSNS_JSON='{"yte":"postgresql+asyncpg://medinet:medinet@postgres:5432/medinet_hub_yte"}'
       ```

    2. Verify config render:
       ```bash
       cd Hub_All
       docker compose config --quiet  # expect exit 0
       docker compose config | grep -E "HUB_ID|CENTRAL_SYNC_DSN|CHECKSUM_HUB_DSNS_JSON" | wc -l  # expect ≥ 7
       ```

    3. Up 4 service (central + 3 hub con):
       ```bash
       docker compose up -d postgres redis
       sleep 5  # wait db ready
       cd api && make migrate-all  # apply 0005 per-hub
       cd .. && docker compose up -d python-api-central python-api-yte
       sleep 10
       ```

    4. Verify hub con boot success (no fail-fast):
       ```bash
       docker compose logs python-api-yte 2>&1 | grep -E "central_sync_pool_ready|sync_worker_task_started"
       # Expect 2 line match
       ```

    5. Verify central scheduler:
       ```bash
       docker compose logs python-api-central 2>&1 | grep "checksum_scheduler_task_started"
       # Expect 1 line match
       ```

    6. Verify Prometheus metrics:
       ```bash
       curl http://localhost:8181/metrics | grep -E "^sync_"  # hub con metrics
       curl http://localhost:8180/metrics | grep -E "^sync_count_drift|^sync_hash_drift"  # central metrics
       ```

    7. (Optional) End-to-end golden path: login central JWT → upload DOC hub yte → cocoindex flow ingest → trigger fire sync_outbox row → worker poll + push central → SELECT chunks WHERE hub_id='yte' ở central thấy chunks.

    8. Teardown:
       ```bash
       docker compose down -v
       ```

    **Acceptance criteria:**
    - All `docker compose config --quiet` exit 0
    - Hub con boot KHÔNG fail-fast (no `central_sync_pool_init_failed_fail_fast`)
    - Log evidence `sync_worker_task_started` + `central_sync_pool_ready` + `checksum_scheduler_task_started`
    - Prometheus endpoint expose ≥ 6 sync_* metric

    **Pre-resolved decision (per user pattern Plan 02-04 + 03-05):**
    Task 5 smoke compose runtime có thể SKIP pre-resolved nếu user accept:
    - Evidence chain: 60+ unit + 4+ integration test in-process PASS cover semantic SYNC-01..05.
    - `docker compose config --quiet` base PASS (Plan 04-02 Task 2 verify).
    - Smoke runtime full E2E defer Phase 7 MIGRATE-05 (3 hub + central golden path + JWT SSO live + cross-hub p95 < 1.5s measure live).

    Pattern follow Plan 02-04 + 03-05 SKIP rationale documented.
  </how-to-verify>
  <resume-signal>User type `skip smoke` (defer Phase 7 MIGRATE-05) hoặc `approved` (smoke PASS) hoặc `failed` (root cause cần debug).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation update → developer behavior | Inaccurate docs có thể cause regression future phase — verify cross-reference chính xác |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | Docs drift với implementation (env name typo) | mitigate | Acceptance grep verify env name match Settings field (HUB_ID + CENTRAL_SYNC_DSN + CHECKSUM_HUB_DSNS_JSON + SYNC_BATCH_SIZE etc.) |
| T-04-07-02 | Repudiation | STATE.md frontmatter progress số không phản ánh thực tế | mitigate | Acceptance check completed_phases=4 + completed_plans=22 + percent=69; cross-ref tests/unit pass count |
| T-04-07-03 | Info Disclosure | README example expose production credential | mitigate | Example dùng dummy UUID + medinet:medinet dev password; production deploy guide note (defer Phase 7) emphasize secrets backing |
| T-04-07-04 | DoS | Smoke runtime tốn tài nguyên dev workstation | accept | Task 5 checkpoint optional SKIP — defer Phase 7 MIGRATE-05 full E2E; in-process unit + integration test cover semantic |
| T-04-07-05 | Elevation | Operator paste production CHECKSUM_HUB_DSNS_JSON với role superuser | mitigate | README note recommend read-only role per hub — `medinet_ro:medinet` username convention; Phase 7 deploy guide formal |
| T-04-07-06 | Spoofing | README admin replay example expose JWT token format | accept | Example dùng `$ADMIN_JWT` placeholder — operator KHÔNG copy-paste literal token |
</threat_model>

<verification>
- 4 docs file update đầy đủ structure preserve.
- Acceptance grep PASS toàn bộ key strings.
- Markdown lint no critical errors.
- Smoke checkpoint pause expected — user resume `skip smoke` or `approved`.
</verification>

<success_criteria>
- CLAUDE.md section 6 update Phase 4 pattern subsection + progress table row.
- STATE.md frontmatter v3.0 progress + 2 new section (Phase 4 Planning + Results) + Next Action promote Phase 5.
- REQUIREMENTS.md SYNC-01..05 mark [x] + NOTE Phase 4 closeout 7-step plan list.
- README.md section mới Cross-hub Sync Deploy Notes (env list + Alembic + metrics + admin replay + rollback).
- Task 5 smoke checkpoint resolved per user signal.
- Git commits: 4 docs commits + 1 SUMMARY.md commit closing Phase 4.
- Phase 4 closed → `/gsd-discuss-phase 5` (PROXY-01..04 + GA-V3-C + D-V3-06 D6 expire).
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-hub-data-sync/04-07-SUMMARY.md` với Phase 4 tổng kết:
- 6 plan deliverable
- ~60+ unit + 4+ integration test PASS aggregate
- ~30 commits
- 36 STRIDE threat addressed
- 6 Prometheus metric infrastructure
- 9 D-V3-Phase4 decision LOCKED consumed
- 5 SYNC REQ-ID closed
- Next: `/gsd-discuss-phase 5`
</output>
