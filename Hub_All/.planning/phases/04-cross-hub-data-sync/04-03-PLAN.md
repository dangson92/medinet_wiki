---
phase: 04-cross-hub-data-sync
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - api/app/sync/__init__.py
  - api/app/sync/keys.py
  - api/app/sync/models.py
  - api/app/sync/metrics.py
  - api/app/sync/worker.py
  - api/tests/unit/test_sync_models.py
  - api/tests/unit/test_sync_metrics.py
  - api/tests/unit/test_sync_worker.py
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-05
tags:
  - sync
  - worker
  - asyncio
  - prometheus
  - outbox
user_setup: []

must_haves:
  truths:
    - "Module `api/app/sync/` tồn tại với 5 file: __init__.py + keys.py + models.py + metrics.py + worker.py"
    - "`sync.keys.stable_chunk_id(doc_id, idx) -> UUID` re-export từ `rag.flow` (KHÔNG duplicate logic)"
    - "Pydantic schema `SyncOutboxRow` parse JSONB payload từ trigger Plan 04-01 → dict typed cho worker push"
    - "ChunkPayload.content_hash field_validator decode hex string (từ trigger `encode(content_hash, 'hex')` Plan 04-01) thành bytes — BLOCKER 2 fix end-to-end serialization"
    - "6 Prometheus collector mới đăng ký module-level với label `hub_name` (NOT `hub_id` — W7 fix rõ semantic: Prometheus label là hub_name string)"
    - "`sync_worker_loop(app)` async function poll outbox batch 100/5s + SKIP LOCKED + exp backoff [1,5,30,120] + max 5 attempts → status='dead' + Prometheus metrics emit"
    - "Worker `_push_batch_to_central` thực thi INSERT ON CONFLICT (id) DO UPDATE WHERE chunks.content_hash IS DISTINCT FROM EXCLUDED.content_hash (D-V3-Phase4-B1) cho op_type=insert; DELETE FROM chunks WHERE id = ANY($1) cho op_type=delete (D-V3-Phase4-B3)"
    - "Worker `_update_document_sync_status` helper aggregate sync_outbox state per document → UPDATE `documents.sync_status` enum (synced|failed|partial|syncing) sau MỖI batch — D-V3-Phase4-B2 lifecycle truth delivered (BLOCKER 1 fix)"
    - "Unit test cover: happy path 100 row push success → status='processed'; retry path attempt 2/3/4/5 với backoff exp; dead path attempt 5 → status='dead' + sync_dead_total counter incr; concurrency SKIP LOCKED simulation; documents.sync_status lifecycle synced/failed/partial/syncing (4 test mới)"
  artifacts:
    - path: "api/app/sync/__init__.py"
      provides: "Package init + public re-export"
      contains: "sync_worker_loop"
    - path: "api/app/sync/keys.py"
      provides: "Re-export stable_chunk_id + helper outbox SQL constants + UPDATE_DOC_SYNC_STATUS_SQL aggregate"
      contains: "from app.rag.flow import stable_chunk_id"
      contains_alt: "UPDATE_DOC_SYNC_STATUS_SQL"
    - path: "api/app/sync/models.py"
      provides: "Pydantic schema SyncOutboxRow + SyncStatus enum + ChunkPayload schema (content_hash hex decode validator)"
      contains: "class SyncOutboxRow"
      contains_alt: "class SyncStatus"
    - path: "api/app/sync/metrics.py"
      provides: "6 Prometheus collector module-level với label `hub_name`"
      contains: "sync_lag_seconds"
      contains_alt: "sync_attempt_total"
    - path: "api/app/sync/worker.py"
      provides: "sync_worker_loop async + _push_batch_to_central + _claim_pending_batch + _mark_processed/_mark_failed/_mark_dead + _update_document_sync_status helper"
      contains: "async def sync_worker_loop"
      contains_alt: "ON CONFLICT (id) DO UPDATE"
      contains_extra: "_update_document_sync_status"
      min_lines: 280
  key_links:
    - from: "sync.worker.sync_worker_loop"
      to: "sync.metrics 6 Prometheus collectors"
      via: "increment counter / observe histogram / set gauge per outbox row processed (label hub_name)"
      pattern: "sync_attempt_total.labels"
    - from: "sync.worker._claim_pending_batch"
      to: "sync_outbox table"
      via: "SELECT ... FOR UPDATE SKIP LOCKED LIMIT $batch_size"
      pattern: "FOR UPDATE SKIP LOCKED"
    - from: "sync.worker._push_batch_to_central"
      to: "central asyncpg pool (Plan 04-04 wire)"
      via: "INSERT ON CONFLICT (id) DO UPDATE WHERE content_hash IS DISTINCT"
      pattern: "ON CONFLICT \\(id\\) DO UPDATE"
    - from: "sync.worker._update_document_sync_status"
      to: "documents.sync_status enum column (Plan 04-01)"
      via: "Aggregate sync_outbox state per document_id → UPDATE documents.sync_status synced|failed|partial|syncing"
      pattern: "UPDATE documents.*sync_status"
---

<objective>
Tạo module `api/app/sync/` mới với 5 file: __init__.py + keys.py + models.py + metrics.py + worker.py — implement D-V3-Phase4-A1/A3/A5 outbox worker logic + D-V3-Phase4-B1/B3 idempotent push semantic + **D-V3-Phase4-B2 documents.sync_status lifecycle UPDATE sau MỖI batch** (BLOCKER 1 fix). Plan 04-04 sẽ wire vào lifespan; Plan này CHỈ build module + unit test isolated (mock pools).

Purpose:
- **Wave 2 file-disjoint** với Plan 04-02 → parallel execution OK.
- **SYNC-05 mechanism + SYNC-01 push + SYNC-02 idempotent** core logic encapsulated trong worker.
- **D-V3-Phase4-B2 truth delivered** — worker UPDATE documents.sync_status aggregate state sau mỗi batch (KHÔNG để stay 'pending' vĩnh viễn — BLOCKER 1 fix).
- **BLOCKER 2 fix end-to-end** — ChunkPayload.content_hash hex string decode → bytes (khớp trigger `encode(.., 'hex')` Plan 04-01).
- **W7 Prometheus label semantic fix** — label `hub_name` (NOT `hub_id`) — Prometheus cardinality label là hub_name string (vd "yte"), KHÔNG UUID.

Output:
- `api/app/sync/__init__.py` (re-export public API)
- `api/app/sync/keys.py` (re-export `stable_chunk_id` + outbox SQL constants + UPDATE_DOC_SYNC_STATUS_SQL)
- `api/app/sync/models.py` (Pydantic SyncOutboxRow + SyncStatus enum + ChunkPayload với hex decode validator)
- `api/app/sync/metrics.py` (6 Prometheus collector, label `hub_name`)
- `api/app/sync/worker.py` (~280 LOC core loop + push helpers + _update_document_sync_status)
- `api/tests/unit/test_sync_models.py` (~100 LOC)
- `api/tests/unit/test_sync_metrics.py` (~60 LOC)
- `api/tests/unit/test_sync_worker.py` (~280 LOC mock asyncpg pool, 13+ test bao gồm 4 test sync_status lifecycle)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md
@Hub_All/.planning/STATE.md

# Reference Pydantic + Prometheus + async pattern
@Hub_All/api/app/rag/flow.py
@Hub_All/api/app/models/chunk.py
@Hub_All/api/app/observability/metrics.py
@Hub_All/api/app/auth/jwks.py

<interfaces>
<!-- stable_chunk_id từ rag.flow re-export (D-V3-Phase4-B1) -->

From api/app/rag/flow.py:
```python
CHUNK_ID_NAMESPACE = uuid.UUID("8f1a3c6e-0000-4000-a000-0000007a3d1c")

def stable_chunk_id(document_id: uuid.UUID, chunk_index: int) -> uuid.UUID:
    """Deterministic UUID5 — re-index cùng (doc_id, idx) → cùng chunk_id."""
    return uuid.uuid5(CHUNK_ID_NAMESPACE, f"{document_id}:{chunk_index}")
```

From CONTEXT.md D-V3-Phase4-A5: batch_size=100, poll_interval=5s, max_attempts=5, backoff=[1,5,30,120], SKIP LOCKED.

From CONTEXT.md D-V3-Phase4-B1: ON CONFLICT (id) DO UPDATE WHERE content_hash IS DISTINCT FROM EXCLUDED.content_hash.

From CONTEXT.md D-V3-Phase4-B2 (CRITICAL BLOCKER 1):
```
- Initial: documents.sync_status='pending' (Plan 04-01 migration default)
- Trigger INSERT chunks → cùng transaction UPDATE 'syncing' WHERE sync_status='pending' (Plan 04-01)
- Worker push success → THIS PLAN: UPDATE 'synced' WHEN no pending/processing outbox + no dead
- Worker dead → THIS PLAN: UPDATE 'failed' WHEN no pending/processing + all dead
- Worker mixed → THIS PLAN: UPDATE 'partial' WHEN no pending/processing + có cả processed + dead
- Worker active → THIS PLAN: UPDATE 'syncing' WHEN còn outbox pending/processing
```

From CONTEXT.md D-V3-Phase4-B3:
```
op_type enum 'insert'|'delete'
INSERT → INSERT ON CONFLICT (id) UPDATE
DELETE → DELETE FROM chunks WHERE id IN ($1)
```

From Plan 04-01 trigger payload shape (BLOCKER 2 end-to-end serialization):
```sql
-- INSERT branch:
jsonb_build_object(
    'id', NEW.id,
    'document_id', NEW.document_id,
    'hub_id', NEW.hub_id,
    'content', NEW.content,
    'content_hash', encode(NEW.content_hash, 'hex'),  -- hex string KHÔNG có \x prefix
    'vector', NEW.vector::float4[],  -- float4 array → jsonb number array
    ...
)
-- DELETE branch:
jsonb_build_object('id', OLD.id)  -- key 'id' (NOT 'chunk_id')
```

Phase 4 6 metrics mới (label `hub_name` — W7 fix):
```
sync_lag_seconds{hub_name}        — histogram (outbox enqueue → central processed_at)
sync_outbox_pending{hub_name}     — gauge (pending row count)
sync_attempt_total{hub_name, status=success|fail}  — counter
sync_dead_total{hub_name, error_class}             — counter
sync_count_drift{hub_name}        — gauge (ratio drift)
sync_hash_drift{hub_name, drift_type=mismatch|missing}  — counter
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sync.keys + sync.models — pure Pydantic + helpers + UPDATE_DOC_SYNC_STATUS_SQL</name>
  <read_first>
    - api/app/rag/flow.py (đọc lines 76-92 — stable_chunk_id + CHUNK_ID_NAMESPACE)
    - api/app/models/chunk.py (Chunk SQLAlchemy schema cho ChunkPayload Pydantic mirror)
    - api/app/auth/jwt.py (pattern Pydantic class struct)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md sections A5 + B1 + B2 + B3
    - api/migrations/versions/0005_sync_outbox_per_hub.py (Plan 04-01 trigger payload shape — confirm hex encode + vector cast + DELETE key 'id')
  </read_first>
  <behavior>
    - Test 1 (test_stable_chunk_id_reexport): `from app.sync.keys import stable_chunk_id` + `from app.rag.flow import stable_chunk_id as orig`; assert same function (identity OR same output).
    - Test 2 (test_sync_status_enum): `SyncStatus.PENDING == "pending"`, `PROCESSING`, `PROCESSED`, `DEAD`.
    - Test 3 (test_op_type_enum): `OpType.INSERT == "insert"`, `OpType.DELETE == "delete"`.
    - Test 4 (test_document_sync_status_enum): `DocumentSyncStatus.PENDING/SYNCING/SYNCED/FAILED/PARTIAL` match Plan 04-01 CHECK.
    - Test 5 (test_chunk_payload_parse_hex_content_hash): parse `{"content_hash": "ab" * 32, "vector": [0.1, 0.2], ...}` (64 char hex) → `ChunkPayload(content_hash=bytes.fromhex(...))` 32 bytes. **BLOCKER 2 fix verify** — trigger emit hex KHÔNG có `\x` prefix.
    - Test 6 (test_chunk_payload_parse_bytes_content_hash_passthrough): nếu test mock pass raw bytes (no hex encode), validator KHÔNG raise — passthrough bytes.
    - Test 7 (test_chunk_payload_invalid_hash_raises): pass `{"content_hash": 12345}` (int) → ValidationError.
    - Test 8 (test_chunk_payload_delete_shape_key_id): parse `{"id": "<uuid>"}` (NOT chunk_id) → ChunkPayload subset OK hoặc DeletePayload riêng.
    - Test 9 (test_sync_outbox_row_full): parse full outbox row dict (11 cột Plan 04-01) → `SyncOutboxRow`.
    - Test 10 (test_outbox_row_validates_op_type): `op_type="invalid"` raise ValidationError.
    - Test 11 (test_outbox_sql_constants): keys.py exports `CLAIM_PENDING_SQL` chứa "FOR UPDATE SKIP LOCKED"; `MARK_PROCESSED_SQL` chứa "status = 'processed'"; `MARK_DEAD_SQL` chứa "status = 'dead'"; `PUSH_INSERT_CHUNK_SQL` chứa "ON CONFLICT (id) DO UPDATE"; `UPDATE_DOC_SYNC_STATUS_SQL` chứa "UPDATE documents" + "sync_status".
    - Test 12 (test_update_doc_sql_aggregate_state): UPDATE_DOC_SYNC_STATUS_SQL CASE branches cover all 4 state — 'synced', 'failed', 'partial', 'syncing'.
  </behavior>
  <action>
    Tạo `api/app/sync/__init__.py`:
    ```python
    """Phase 4 sync module — outbox worker + metrics + idempotent push (SYNC-01..05)."""
    from app.sync.metrics import (
        SYNC_ATTEMPT_TOTAL, SYNC_COUNT_DRIFT, SYNC_DEAD_TOTAL,
        SYNC_HASH_DRIFT, SYNC_LAG_SECONDS, SYNC_OUTBOX_PENDING,
    )
    from app.sync.models import ChunkPayload, DocumentSyncStatus, OpType, SyncOutboxRow, SyncStatus
    from app.sync.worker import sync_worker_loop

    __all__ = [
        "ChunkPayload", "DocumentSyncStatus", "OpType",
        "SYNC_ATTEMPT_TOTAL", "SYNC_COUNT_DRIFT", "SYNC_DEAD_TOTAL",
        "SYNC_HASH_DRIFT", "SYNC_LAG_SECONDS", "SYNC_OUTBOX_PENDING",
        "SyncOutboxRow", "SyncStatus", "sync_worker_loop",
    ]
    ```

    Tạo `api/app/sync/keys.py`:
    ```python
    """Re-export stable_chunk_id + outbox SQL constants — D-V3-Phase4-A5 LOCKED queries."""
    from __future__ import annotations

    from app.rag.flow import CHUNK_ID_NAMESPACE, stable_chunk_id

    CLAIM_PENDING_SQL = """
        SELECT id, op_type, chunk_id, document_id, payload,
               attempt_count, last_error, status, next_retry_at,
               created_at, processed_at
        FROM sync_outbox
        WHERE status = 'pending'
          AND (next_retry_at IS NULL OR next_retry_at <= NOW())
        ORDER BY created_at
        LIMIT $1
        FOR UPDATE SKIP LOCKED
    """

    MARK_PROCESSING_SQL = """
        UPDATE sync_outbox
        SET status = 'processing', attempt_count = attempt_count + 1
        WHERE id = ANY($1::uuid[])
    """

    MARK_PROCESSED_SQL = """
        UPDATE sync_outbox
        SET status = 'processed', processed_at = NOW(), last_error = NULL
        WHERE id = ANY($1::uuid[])
    """

    MARK_FAILED_RETRY_SQL = """
        UPDATE sync_outbox
        SET status = 'pending',
            last_error = $2,
            next_retry_at = NOW() + ($3::int * INTERVAL '1 second')
        WHERE id = $1
    """

    MARK_DEAD_SQL = """
        UPDATE sync_outbox
        SET status = 'dead', last_error = $2, processed_at = NOW()
        WHERE id = ANY($1::uuid[])
    """

    PUSH_INSERT_CHUNK_SQL = """
        INSERT INTO chunks (id, document_id, hub_id, content, content_hash,
                            heading_path, page_start, page_end, vector, metadata, created_at)
        VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9::vector, $10::jsonb, COALESCE($11, NOW()))
        ON CONFLICT (id) DO UPDATE SET
            content = EXCLUDED.content,
            content_hash = EXCLUDED.content_hash,
            heading_path = EXCLUDED.heading_path,
            page_start = EXCLUDED.page_start,
            page_end = EXCLUDED.page_end,
            vector = EXCLUDED.vector,
            metadata = EXCLUDED.metadata
        WHERE chunks.content_hash IS DISTINCT FROM EXCLUDED.content_hash
    """

    PUSH_DELETE_CHUNK_SQL = """
        DELETE FROM chunks WHERE id = ANY($1::uuid[])
    """

    # BLOCKER 1 FIX — D-V3-Phase4-B2 lifecycle aggregate
    # CASE logic: NO còn outbox pending/processing → check dead:
    #   - dead + processed both exist → 'partial'
    #   - dead only (no processed) → 'failed'
    #   - no dead → 'synced'
    # Còn outbox pending/processing → 'syncing'
    UPDATE_DOC_SYNC_STATUS_SQL = """
        UPDATE documents
        SET sync_status = (
            CASE
                WHEN NOT EXISTS (
                    SELECT 1 FROM sync_outbox
                    WHERE document_id = documents.id
                      AND status IN ('pending', 'processing')
                ) THEN
                    CASE
                        WHEN EXISTS (
                            SELECT 1 FROM sync_outbox
                            WHERE document_id = documents.id AND status = 'dead'
                        ) THEN
                            CASE
                                WHEN EXISTS (
                                    SELECT 1 FROM sync_outbox
                                    WHERE document_id = documents.id AND status = 'processed'
                                ) THEN 'partial'
                                ELSE 'failed'
                            END
                        ELSE 'synced'
                    END
                ELSE 'syncing'
            END
        )
        WHERE id = ANY($1::uuid[])
        RETURNING id, sync_status
    """

    __all__ = [
        "CHUNK_ID_NAMESPACE",
        "CLAIM_PENDING_SQL",
        "MARK_DEAD_SQL",
        "MARK_FAILED_RETRY_SQL",
        "MARK_PROCESSED_SQL",
        "MARK_PROCESSING_SQL",
        "PUSH_DELETE_CHUNK_SQL",
        "PUSH_INSERT_CHUNK_SQL",
        "UPDATE_DOC_SYNC_STATUS_SQL",
        "stable_chunk_id",
    ]
    ```

    Tạo `api/app/sync/models.py` (ChunkPayload content_hash hex decode — BLOCKER 2 end-to-end):
    ```python
    """Pydantic schemas + enums cho sync_outbox + chunks push payload."""
    from __future__ import annotations

    import enum
    import uuid
    from datetime import datetime
    from typing import Any

    from pydantic import BaseModel, ConfigDict, Field, field_validator


    class SyncStatus(str, enum.Enum):
        PENDING = "pending"
        PROCESSING = "processing"
        PROCESSED = "processed"
        DEAD = "dead"


    class OpType(str, enum.Enum):
        INSERT = "insert"
        DELETE = "delete"


    class DocumentSyncStatus(str, enum.Enum):
        PENDING = "pending"
        SYNCING = "syncing"
        SYNCED = "synced"
        FAILED = "failed"
        PARTIAL = "partial"


    class ChunkPayload(BaseModel):
        """Parsed JSONB payload từ trigger Plan 04-01 INSERT branch.

        BLOCKER 2 end-to-end serialization fix:
        - Trigger Plan 04-01 emit content_hash qua `encode(NEW.content_hash, 'hex')` → hex string KHÔNG có '\\x' prefix.
        - Trigger Plan 04-01 emit vector qua `to_jsonb(NEW.vector::float4[])` → list[float].
        - field_validator `_decode_content_hash` mode='before' decode hex string → bytes (or passthrough bytes for direct test).
        """
        model_config = ConfigDict(extra="ignore")

        id: uuid.UUID
        document_id: uuid.UUID
        hub_id: uuid.UUID
        content: str
        content_hash: bytes  # decoded từ hex string của trigger payload
        heading_path: str | None = None
        page_start: int | None = None
        page_end: int | None = None
        vector: list[float] | None = None  # từ to_jsonb(NEW.vector::float4[]) → array float
        metadata: dict[str, Any] = Field(default_factory=dict)
        created_at: datetime | None = None

        @field_validator("content_hash", mode="before")
        @classmethod
        def _decode_content_hash(cls, v: Any) -> bytes:
            """Trigger Plan 04-01 dùng encode(content_hash, 'hex') — decode hex string → bytes.

            Hex string KHÔNG có '\\x' prefix (encode(.., 'hex') trả raw hex).
            Bytes passthrough cho direct test (mock outbox row).
            """
            if isinstance(v, bytes):
                return v
            if isinstance(v, str):
                # encode(.., 'hex') emits raw hex (no '\\x' prefix)
                hex_str = v[2:] if v.startswith("\\x") else v
                return bytes.fromhex(hex_str)
            raise ValueError(f"content_hash phải là bytes hoặc hex str, got {type(v).__name__}")


    class DeletePayload(BaseModel):
        """Parsed JSONB payload từ trigger Plan 04-01 DELETE branch — key 'id' (NOT 'chunk_id')."""
        id: uuid.UUID


    class SyncOutboxRow(BaseModel):
        """Pydantic mirror của 1 row sync_outbox (Plan 04-01 schema)."""
        model_config = ConfigDict(extra="ignore")

        id: uuid.UUID
        op_type: OpType
        chunk_id: uuid.UUID
        document_id: uuid.UUID | None = None
        payload: dict[str, Any]
        attempt_count: int = 0
        last_error: str | None = None
        status: SyncStatus = SyncStatus.PENDING
        next_retry_at: datetime | None = None
        created_at: datetime
        processed_at: datetime | None = None
    ```

    Tạo `api/tests/unit/test_sync_models.py` ≥ 12 test cover behavior 1-12.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_sync_models.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `api/app/sync/__init__.py`, `api/app/sync/keys.py`, `api/app/sync/models.py`.
    - `grep -n "from app.rag.flow import" api/app/sync/keys.py` returns ≥ 1 line.
    - `grep -n "FOR UPDATE SKIP LOCKED" api/app/sync/keys.py` returns ≥ 1 line.
    - `grep -n "ON CONFLICT (id) DO UPDATE" api/app/sync/keys.py` returns ≥ 1 line.
    - `grep -n "WHERE chunks.content_hash IS DISTINCT FROM EXCLUDED.content_hash" api/app/sync/keys.py` returns ≥ 1 line.
    - `grep -n "UPDATE_DOC_SYNC_STATUS_SQL" api/app/sync/keys.py` returns ≥ 1 line (BLOCKER 1).
    - `grep -n "'partial'" api/app/sync/keys.py` returns ≥ 1 line (CASE branch).
    - `grep -n "'syncing'" api/app/sync/keys.py` returns ≥ 1 line.
    - `grep -n "class SyncStatus" api/app/sync/models.py` returns ≥ 1 line.
    - `grep -n "class DocumentSyncStatus" api/app/sync/models.py` returns ≥ 1 line.
    - `grep -n "class ChunkPayload" api/app/sync/models.py` returns ≥ 1 line.
    - `grep -n "_decode_content_hash" api/app/sync/models.py` returns ≥ 1 line (BLOCKER 2).
    - `grep -n "bytes.fromhex" api/app/sync/models.py` returns ≥ 1 line.
    - `cd api && uv run pytest tests/unit/test_sync_models.py -v` PASS ≥ 12 tests.
    - `cd api && uv run ruff check app/sync/ tests/unit/test_sync_models.py` exit 0.
    - `cd api && uv run mypy --strict app/sync/` exit 0.
  </acceptance_criteria>
  <done>3 file module + UPDATE_DOC_SYNC_STATUS_SQL aggregate logic (BLOCKER 1) + ChunkPayload hex decode validator (BLOCKER 2) + 12+ unit test PASS; ruff + mypy --strict PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sync.metrics — 6 Prometheus collector module-level (label `hub_name`, W7 fix)</name>
  <read_first>
    - api/app/observability/metrics.py hoặc tương tự (đọc pattern Prometheus collector existing — Plan 10-02 HARD-02 search_latency_seconds)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md section "Prometheus metrics mới (6)"
  </read_first>
  <behavior>
    - Test 1 (test_sync_lag_seconds_histogram): metric Histogram label `hub_name`. Buckets (0.1, 0.5, 1, 5, 10, 30, 60, 120, 300, 600).
    - Test 2 (test_sync_outbox_pending_gauge): Gauge label `hub_name`. `SYNC_OUTBOX_PENDING.labels(hub_name="yte").set(42)` works.
    - Test 3 (test_sync_attempt_counter): Counter labels `hub_name`, `status`. `SYNC_ATTEMPT_TOTAL.labels(hub_name="yte", status="success").inc()`.
    - Test 4 (test_sync_dead_counter): Counter labels `hub_name`, `error_class`.
    - Test 5 (test_sync_count_drift_gauge): Gauge label `hub_name`.
    - Test 6 (test_sync_hash_drift_counter): Counter labels `hub_name`, `drift_type`.
    - Test 7 (test_no_duplicate_register): import twice → KHÔNG raise.
    - Test 8 (test_prometheus_textformat_render): `generate_latest()` chứa tất cả 6 metric name.
    - Test 9 (test_label_is_hub_name_not_hub_id): introspect 6 collector → all `labelnames` chứa `"hub_name"` (W7 fix — KHÔNG `"hub_id"`).
  </behavior>
  <action>
    Tạo `api/app/sync/metrics.py` với label `hub_name` (W7 fix — semantic rõ ràng: label values là hub_name string "yte"/"duoc"/..., KHÔNG UUID):

    ```python
    """Phase 4 Plan 04-03 — 6 Prometheus collectors for cross-hub sync observability.

    W7 fix (CR Iteration 1): label `hub_name` (NOT `hub_id`) — Prometheus label values là
    hub_name string ("yte"/"duoc"/"hcns"/dynamic), KHÔNG UUID. Tách rõ semantic với
    sync_outbox.chunk_id (UUID) + chunks.hub_id (UUID FK).

    Cardinality control (T-04-03-01 mitigation):
    - hub_name label = settings.hub_name (4-10 hub max → bounded).
    - error_class label normalize (network/timeout/conflict/unknown — 4 class).
    - drift_type label fixed (mismatch|missing).
    - status label fixed (success|fail).
    """
    from __future__ import annotations

    from prometheus_client import Counter, Gauge, Histogram

    SYNC_LAG_SECONDS = Histogram(
        "sync_lag_seconds",
        "Cross-hub sync latency: outbox.created_at → central INSERT processed_at",
        labelnames=("hub_name",),
        buckets=(0.1, 0.5, 1.0, 5.0, 10.0, 30.0, 60.0, 120.0, 300.0, 600.0),
    )

    SYNC_OUTBOX_PENDING = Gauge(
        "sync_outbox_pending",
        "Số outbox rows pending hiện tại theo hub_name (worker poll mỗi tick set)",
        labelnames=("hub_name",),
    )

    SYNC_ATTEMPT_TOTAL = Counter(
        "sync_attempt_total",
        "Cumulative push attempts (success/fail) — Plan 04-03 worker emit per row processed",
        labelnames=("hub_name", "status"),
    )

    SYNC_DEAD_TOTAL = Counter(
        "sync_dead_total",
        "Cumulative dead rows (attempt_count >= max_attempts) — per error class normalized",
        labelnames=("hub_name", "error_class"),
    )

    SYNC_COUNT_DRIFT = Gauge(
        "sync_count_drift",
        "COUNT(*) hub con vs central WHERE hub_id ratio (1.0 = perfect)",
        labelnames=("hub_name",),
    )

    SYNC_HASH_DRIFT = Counter(
        "sync_hash_drift",
        "Cumulative hash mismatches từ Hourly TABLESAMPLE BERNOULLI(1) verify — Plan 04-06",
        labelnames=("hub_name", "drift_type"),
    )


    def normalize_error_class(exc: BaseException) -> str:
        name = type(exc).__name__.lower()
        if "timeout" in name:
            return "timeout"
        if "connection" in name or "network" in name or "socket" in name:
            return "network"
        if "unique" in name or "conflict" in name or "integrity" in name:
            return "conflict"
        return "unknown"


    __all__ = [
        "SYNC_ATTEMPT_TOTAL", "SYNC_COUNT_DRIFT", "SYNC_DEAD_TOTAL",
        "SYNC_HASH_DRIFT", "SYNC_LAG_SECONDS", "SYNC_OUTBOX_PENDING",
        "normalize_error_class",
    ]
    ```

    Tạo `api/tests/unit/test_sync_metrics.py` ≥ 9 test cover behavior 1-9 (test 9 verify label `hub_name`):

    ```python
    def test_label_is_hub_name_not_hub_id():
        from app.sync.metrics import (
            SYNC_LAG_SECONDS, SYNC_OUTBOX_PENDING, SYNC_ATTEMPT_TOTAL,
            SYNC_DEAD_TOTAL, SYNC_COUNT_DRIFT, SYNC_HASH_DRIFT,
        )
        for m in [SYNC_LAG_SECONDS, SYNC_OUTBOX_PENDING, SYNC_ATTEMPT_TOTAL,
                  SYNC_DEAD_TOTAL, SYNC_COUNT_DRIFT, SYNC_HASH_DRIFT]:
            assert "hub_name" in m._labelnames, f"{m._name} missing hub_name label"
            assert "hub_id" not in m._labelnames, f"{m._name} has stale hub_id label"
    ```
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_sync_metrics.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - File `api/app/sync/metrics.py` exists ≥ 60 LOC.
    - `grep -n "sync_lag_seconds" api/app/sync/metrics.py` returns ≥ 1 line.
    - `grep -n "sync_outbox_pending" api/app/sync/metrics.py` returns ≥ 1 line.
    - `grep -n "sync_attempt_total" api/app/sync/metrics.py` returns ≥ 1 line.
    - `grep -n "sync_dead_total" api/app/sync/metrics.py` returns ≥ 1 line.
    - `grep -n "sync_count_drift" api/app/sync/metrics.py` returns ≥ 1 line.
    - `grep -n "sync_hash_drift" api/app/sync/metrics.py` returns ≥ 1 line.
    - `grep -c '"hub_name"' api/app/sync/metrics.py` returns ≥ 6 (6 labelnames tuple).
    - `grep -c '"hub_id"' api/app/sync/metrics.py` returns 0 (W7 fix — KHÔNG label hub_id).
    - `grep -n "def normalize_error_class" api/app/sync/metrics.py` returns ≥ 1 line.
    - `cd api && uv run pytest tests/unit/test_sync_metrics.py -v` PASS ≥ 9 tests.
    - `cd api && uv run ruff check app/sync/metrics.py tests/unit/test_sync_metrics.py` exit 0.
    - `cd api && uv run mypy --strict app/sync/metrics.py` exit 0.
  </acceptance_criteria>
  <done>6 Prometheus collector label `hub_name` (W7 fix) + normalize_error_class helper + 9+ unit test PASS; ruff + mypy PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: sync.worker — sync_worker_loop + claim + push happy paths + _update_document_sync_status helper (BLOCKER 1 fix)</name>
  <read_first>
    - api/app/sync/keys.py + models.py + metrics.py (Task 1-2 vừa tạo — confirm UPDATE_DOC_SYNC_STATUS_SQL exported)
    - api/app/auth/jwks.py (pattern asyncio task + refresh loop)
    - api/migrations/versions/0005_sync_outbox_per_hub.py (Plan 04-01 — confirm trigger payload shape, INSERT branch UPDATE documents.sync_status='syncing' đã có)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md sections A1/A3/A5 + B1/B2/B3
  </read_first>
  <behavior>
    - Test 1 (test_worker_skip_central): `settings.hub_name == "central"` → immediate return (no loop).
    - Test 2 (test_claim_pending_batch_empty): mock pool trả 0 row → sleep poll_interval + KHÔNG call _push_batch.
    - Test 3 (test_push_batch_insert_happy_path): mock 5 outbox row op_type='insert' → _push_inserts executes PUSH_INSERT_CHUNK_SQL × 5 → MARK_PROCESSED_SQL → SYNC_ATTEMPT_TOTAL.labels(hub_name=, status="success") += 5.
    - Test 4 (test_push_batch_delete_happy_path): mock 3 row op_type='delete' → PUSH_DELETE_CHUNK_SQL với 3 chunk_id ANY array → MARK_PROCESSED.
    - Test 5 (test_push_batch_mixed_op_types): mixed insert + delete trong cùng batch — split + push + mark processed.
    - Test 6 (test_update_document_sync_status_synced) **BLOCKER 1 fix**: 2 outbox row cùng document_id all processed (no pending/processing/dead) → `_update_document_sync_status(pool, [doc_id])` execute UPDATE_DOC_SYNC_STATUS_SQL → mock conn.fetch returns `[{"id": doc_id, "sync_status": "synced"}]`.
    - Test 7 (test_update_document_sync_status_partial) **BLOCKER 1 fix**: 1 processed + 1 dead cùng doc → `_update_document_sync_status` returns 'partial'.
    - Test 8 (test_update_document_sync_status_failed) **BLOCKER 1 fix**: all dead cùng doc → 'failed'.
    - Test 9 (test_update_document_sync_status_syncing) **BLOCKER 1 fix**: còn outbox pending/processing cùng doc → 'syncing'.
    - Test 10 (test_update_document_sync_status_empty_doc_ids_no_op) **BLOCKER 1 fix**: empty list → KHÔNG call conn.fetch.
    - Test 11 (test_worker_calls_update_doc_status_after_each_branch) **BLOCKER 1 fix**: mock _push_inserts + _push_deletes + _handle_failures + verify _update_document_sync_status called ≥ 3 lần với union document_ids của batch (insert branch + delete branch + failure branch).
  </behavior>
  <action>
    Tạo `api/app/sync/worker.py` với `_update_document_sync_status` helper + call sites trong loop:

    ```python
    """Phase 4 Plan 04-03 — sync_worker_loop async task (D-V3-Phase4-A1/A3/A5).

    BLOCKER 1 fix (CR Iteration 1): _update_document_sync_status helper aggregate
    sync_outbox state per document → UPDATE documents.sync_status enum SAU MỖI batch.
    Plan 04-01 trigger handle initial 'pending'→'syncing'; worker handle 'syncing'→
    'synced'|'failed'|'partial' transitions (D-V3-Phase4-B2 lifecycle complete).
    """
    from __future__ import annotations

    import asyncio
    import logging
    import uuid
    from typing import TYPE_CHECKING

    import asyncpg

    from app.sync.keys import (
        CLAIM_PENDING_SQL, MARK_DEAD_SQL, MARK_FAILED_RETRY_SQL,
        MARK_PROCESSED_SQL, MARK_PROCESSING_SQL,
        PUSH_DELETE_CHUNK_SQL, PUSH_INSERT_CHUNK_SQL,
        UPDATE_DOC_SYNC_STATUS_SQL,
    )
    from app.sync.metrics import (
        SYNC_ATTEMPT_TOTAL, SYNC_DEAD_TOTAL, SYNC_LAG_SECONDS,
        SYNC_OUTBOX_PENDING, normalize_error_class,
    )
    from app.sync.models import ChunkPayload, OpType, SyncOutboxRow

    if TYPE_CHECKING:
        from fastapi import FastAPI

    logger = logging.getLogger(__name__)

    LAST_ERROR_TRUNCATE = 1000


    async def sync_worker_loop(app: FastAPI) -> None:
        """Main worker loop — spawn ở hub con lifespan (Plan 04-04)."""
        from app.config import get_settings

        settings = get_settings()
        if settings.hub_name == "central":
            logger.info("sync_worker_skip_central")
            return

        local_pool: asyncpg.Pool = app.state.db_pool
        central_pool: asyncpg.Pool = app.state.central_sync_pool
        hub_name_label = settings.hub_name

        logger.info("sync_worker_start: hub=%s", hub_name_label)

        while True:
            try:
                batch = await _claim_pending_batch(local_pool, settings.sync_batch_size)
                if not batch:
                    SYNC_OUTBOX_PENDING.labels(hub_name=hub_name_label).set(0)
                    await asyncio.sleep(settings.sync_poll_interval)
                    continue

                SYNC_OUTBOX_PENDING.labels(hub_name=hub_name_label).set(len(batch))

                row_ids = [row.id for row in batch]
                async with local_pool.acquire() as conn:
                    await conn.execute(MARK_PROCESSING_SQL, row_ids)

                inserts = [r for r in batch if r.op_type == OpType.INSERT]
                deletes = [r for r in batch if r.op_type == OpType.DELETE]

                processed_ids: list[uuid.UUID] = []
                failed_rows: list[tuple[SyncOutboxRow, BaseException]] = []
                # BLOCKER 1 FIX: track document_ids đã touched ở batch này để UPDATE sync_status
                affected_document_ids: set[uuid.UUID] = set()

                if inserts:
                    try:
                        await _push_inserts(central_pool, inserts)
                        processed_ids.extend([r.id for r in inserts])
                        SYNC_ATTEMPT_TOTAL.labels(hub_name=hub_name_label, status="success").inc(len(inserts))
                        _observe_lag(inserts, hub_name_label)
                    except Exception as e:  # noqa: BLE001
                        for row in inserts:
                            failed_rows.append((row, e))
                        SYNC_ATTEMPT_TOTAL.labels(hub_name=hub_name_label, status="fail").inc(len(inserts))
                    affected_document_ids.update(
                        r.document_id for r in inserts if r.document_id is not None
                    )

                if deletes:
                    try:
                        await _push_deletes(central_pool, deletes)
                        processed_ids.extend([r.id for r in deletes])
                        SYNC_ATTEMPT_TOTAL.labels(hub_name=hub_name_label, status="success").inc(len(deletes))
                        _observe_lag(deletes, hub_name_label)
                    except Exception as e:  # noqa: BLE001
                        for row in deletes:
                            failed_rows.append((row, e))
                        SYNC_ATTEMPT_TOTAL.labels(hub_name=hub_name_label, status="fail").inc(len(deletes))
                    affected_document_ids.update(
                        r.document_id for r in deletes if r.document_id is not None
                    )

                if processed_ids:
                    async with local_pool.acquire() as conn:
                        await conn.execute(MARK_PROCESSED_SQL, processed_ids)

                if failed_rows:
                    await _handle_failures(
                        local_pool, failed_rows, settings.sync_max_attempts,
                        settings.sync_backoff_seconds, hub_name_label,
                    )

                # BLOCKER 1 FIX — D-V3-Phase4-B2 lifecycle UPDATE documents.sync_status
                # sau MỖI batch (sau khi MARK_PROCESSED + MARK_DEAD đã apply).
                if affected_document_ids:
                    await _update_document_sync_status(local_pool, list(affected_document_ids))

            except asyncio.CancelledError:
                logger.info("sync_worker_cancelled: hub=%s", hub_name_label)
                raise
            except Exception as e:  # noqa: BLE001
                logger.exception("sync_worker_loop_error: hub=%s err=%s", hub_name_label, e)
                await asyncio.sleep(settings.sync_poll_interval)


    async def _claim_pending_batch(local_pool, batch_size):
        async with local_pool.acquire() as conn:
            async with conn.transaction():
                records = await conn.fetch(CLAIM_PENDING_SQL, batch_size)
        return [SyncOutboxRow.model_validate(dict(r)) for r in records]


    async def _push_inserts(central_pool, rows):
        async with central_pool.acquire() as conn:
            async with conn.transaction():
                for row in rows:
                    payload = ChunkPayload.model_validate(row.payload)
                    await conn.execute(
                        PUSH_INSERT_CHUNK_SQL,
                        payload.id, payload.document_id, payload.hub_id,
                        payload.content, payload.content_hash,
                        payload.heading_path, payload.page_start, payload.page_end,
                        payload.vector, payload.metadata, payload.created_at,
                    )


    async def _push_deletes(central_pool, rows):
        chunk_ids = [row.chunk_id for row in rows]
        async with central_pool.acquire() as conn:
            await conn.execute(PUSH_DELETE_CHUNK_SQL, chunk_ids)


    async def _update_document_sync_status(
        local_pool: asyncpg.Pool,
        document_ids: list[uuid.UUID],
    ) -> None:
        """BLOCKER 1 FIX — D-V3-Phase4-B2 lifecycle aggregate update.

        Aggregate sync_outbox state per document → UPDATE documents.sync_status enum:
        - 'synced': all outbox rows processed, no dead/pending/processing
        - 'failed': all outbox rows dead (no processed), no pending/processing
        - 'partial': mixed processed + dead, no pending/processing
        - 'syncing': còn pending/processing rows

        Idempotent — re-call cùng document_ids sau batch khác → re-aggregate state mới.
        """
        if not document_ids:
            return
        async with local_pool.acquire() as conn:
            rows = await conn.fetch(UPDATE_DOC_SYNC_STATUS_SQL, list(document_ids))
            for row in rows:
                logger.info(
                    "document_sync_status_updated: doc_id=%s sync_status=%s",
                    str(row["id"]), row["sync_status"],
                )


    async def _handle_failures(local_pool, failed, max_attempts, backoff_seconds, hub_name_label):
        dead_ids = []
        dead_errors = {}
        async with local_pool.acquire() as conn:
            for row, exc in failed:
                err_msg = f"{type(exc).__name__}: {exc!s}"[:LAST_ERROR_TRUNCATE]
                if row.attempt_count >= max_attempts:
                    dead_ids.append(row.id)
                    err_class = normalize_error_class(exc)
                    dead_errors.setdefault(err_class, []).append(row.id)
                else:
                    backoff_idx = min(row.attempt_count - 1, len(backoff_seconds) - 1)
                    backoff = backoff_seconds[backoff_idx]
                    await conn.execute(MARK_FAILED_RETRY_SQL, row.id, err_msg, backoff)

            if dead_ids:
                last_err = f"{type(failed[-1][1]).__name__}: {failed[-1][1]!s}"[:LAST_ERROR_TRUNCATE]
                await conn.execute(MARK_DEAD_SQL, dead_ids, last_err)
                for err_class, ids in dead_errors.items():
                    SYNC_DEAD_TOTAL.labels(hub_name=hub_name_label, error_class=err_class).inc(len(ids))


    def _observe_lag(rows, hub_name_label):
        from datetime import datetime, timezone
        now = datetime.now(timezone.utc)
        for row in rows:
            if row.created_at is None:
                continue
            created = row.created_at if row.created_at.tzinfo else row.created_at.replace(tzinfo=timezone.utc)
            lag = (now - created).total_seconds()
            if lag >= 0:
                SYNC_LAG_SECONDS.labels(hub_name=hub_name_label).observe(lag)


    __all__ = ["sync_worker_loop"]
    ```

    Tạo phần Task 3a của `api/tests/unit/test_sync_worker.py` ≥ 11 test bao gồm 4 test mới cho `_update_document_sync_status` (test 6-9) + test affected_document_ids gather (test 11).
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_sync_worker.py -v -x -k "happy or skip_central or empty or update_document"</automated>
  </verify>
  <acceptance_criteria>
    - File `api/app/sync/worker.py` exists ≥ 280 LOC.
    - `grep -n "async def sync_worker_loop" api/app/sync/worker.py` returns ≥ 1 line.
    - `grep -n "async def _claim_pending_batch" api/app/sync/worker.py` returns ≥ 1 line.
    - `grep -n "async def _push_inserts" api/app/sync/worker.py` returns ≥ 1 line.
    - `grep -n "async def _push_deletes" api/app/sync/worker.py` returns ≥ 1 line.
    - `grep -n "async def _handle_failures" api/app/sync/worker.py` returns ≥ 1 line.
    - `grep -n "async def _update_document_sync_status" api/app/sync/worker.py` returns ≥ 1 line (BLOCKER 1).
    - `grep -c "_update_document_sync_status" api/app/sync/worker.py` returns ≥ 2 (definition + ≥ 1 call site in loop).
    - `grep -n "UPDATE_DOC_SYNC_STATUS_SQL" api/app/sync/worker.py` returns ≥ 1 line (import).
    - `grep -n "affected_document_ids" api/app/sync/worker.py` returns ≥ 2 lines (set init + update calls).
    - `grep -n "hub_name=hub_name_label" api/app/sync/worker.py` returns ≥ 3 lines (W7 — labels dùng hub_name).
    - `cd api && uv run pytest tests/unit/test_sync_worker.py -v -k "happy or skip_central or empty or update_document"` PASS ≥ 11 tests.
    - `cd api && uv run ruff check app/sync/worker.py tests/unit/test_sync_worker.py` exit 0.
    - `cd api && uv run mypy --strict app/sync/worker.py` exit 0.
  </acceptance_criteria>
  <done>worker.py 280+ LOC + `_update_document_sync_status` helper aggregate D-V3-Phase4-B2 (BLOCKER 1) + 11 unit test PASS happy paths + 4 test sync_status lifecycle (synced/failed/partial/syncing); W7 label hub_name; ruff + mypy PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: sync.worker retry + dead + backoff + cancel + lag tests (W5 split — focused complexity)</name>
  <read_first>
    - api/app/sync/worker.py (Task 3a vừa tạo — sync_worker_loop + _handle_failures + _observe_lag)
    - api/app/sync/metrics.py (Task 2 — SYNC_DEAD_TOTAL + SYNC_LAG_SECONDS + normalize_error_class)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md A5 backoff [1,5,30,120]
  </read_first>
  <behavior>
    - Test 1 (test_retry_on_network_error): mock central pool execute raise ConnectionRefusedError attempt 1 → mark_failed_retry với backoff=1s + last_error truncated + attempt_count incr; SYNC_ATTEMPT_TOTAL.labels(status="fail") += 1.
    - Test 2 (test_dead_after_max_attempts): row attempt_count=4 + raise error → attempt_count incr=5 + MARK_DEAD_SQL + SYNC_DEAD_TOTAL.labels(error_class="network") += 1.
    - Test 3 (test_backoff_progression_full): 4 row attempt 1→2→3→4 với expected backoffs [1, 5, 30, 120]s respectively.
    - Test 4 (test_dead_path_calls_update_document_sync_status): dead path qua _handle_failures → loop body call _update_document_sync_status với affected_document_ids include failed doc → expected 'failed' state.
    - Test 5 (test_graceful_shutdown_cancel): asyncio.create_task(sync_worker_loop) + task.cancel() + await → CancelledError caught + KHÔNG raise.
    - Test 6 (test_lag_seconds_histogram_observe): row created_at=T0, after push observe lag T1-T0 ≥ 0 vào SYNC_LAG_SECONDS.labels(hub_name).
    - Test 7 (test_error_class_normalize_used_for_dead_label): mock asyncio.TimeoutError attempt 5 → SYNC_DEAD_TOTAL.labels(error_class="timeout") incr.
  </behavior>
  <action>
    Bổ sung test cases vào `api/tests/unit/test_sync_worker.py` (cùng file Task 3a — split test cases theo behavior để giảm complexity per task).

    Test pattern (carry forward Task 3a fixture):
    ```python
    import asyncio
    import uuid
    from datetime import datetime, timezone
    from unittest.mock import AsyncMock, MagicMock
    import pytest

    from app.sync.models import OpType, SyncOutboxRow, SyncStatus
    from app.sync.worker import _handle_failures, _observe_lag, sync_worker_loop


    @pytest.mark.asyncio
    async def test_retry_on_network_error(mock_local_pool, mock_settings_yte):
        row = _make_outbox_row(attempt_count=1)
        failed = [(row, ConnectionRefusedError("central down"))]
        await _handle_failures(
            mock_local_pool, failed,
            max_attempts=5,
            backoff_seconds=[1, 5, 30, 120],
            hub_name_label="yte",
        )
        # Assert MARK_FAILED_RETRY_SQL called với backoff=1 (attempt_count=1 → backoff[0])
        # Verify mock conn.execute call args


    @pytest.mark.asyncio
    async def test_dead_after_max_attempts(mock_local_pool, mock_settings_yte):
        row = _make_outbox_row(attempt_count=5)  # >= max_attempts
        failed = [(row, ConnectionRefusedError("permanent"))]
        await _handle_failures(
            mock_local_pool, failed, max_attempts=5,
            backoff_seconds=[1, 5, 30, 120], hub_name_label="yte",
        )
        # Verify MARK_DEAD_SQL called + SYNC_DEAD_TOTAL incr với error_class="network"


    @pytest.mark.asyncio
    async def test_backoff_progression_full(mock_local_pool, mock_settings_yte):
        for attempt, expected_backoff in [(1, 1), (2, 5), (3, 30), (4, 120)]:
            row = _make_outbox_row(attempt_count=attempt)
            failed = [(row, ConnectionRefusedError("transient"))]
            await _handle_failures(
                mock_local_pool, failed, max_attempts=5,
                backoff_seconds=[1, 5, 30, 120], hub_name_label="yte",
            )
            # Assert latest conn.execute call's 3rd arg = expected_backoff


    def test_lag_seconds_histogram_observe():
        row = _make_outbox_row()
        # created_at = now - 5s → lag ≥ 5
        row.created_at = datetime.now(timezone.utc).replace(microsecond=0)
        _observe_lag([row], "yte")
        # Verify SYNC_LAG_SECONDS.labels(hub_name="yte") observed value


    @pytest.mark.asyncio
    async def test_graceful_shutdown_cancel(mock_settings_yte, mock_app_with_pools):
        task = asyncio.create_task(sync_worker_loop(mock_app_with_pools))
        await asyncio.sleep(0.1)
        task.cancel()
        with pytest.raises(asyncio.CancelledError):
            await task
    ```

    Cụ thể: Task 3b sẽ chia sẻ fixture `mock_local_pool` + `mock_app_with_pools` + `mock_settings_yte` với Task 3a — định nghĩa fixture ở `conftest.py` cấp tests/unit/ HOẶC ở đầu test_sync_worker.py.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_sync_worker.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "test_retry_on_network_error" api/tests/unit/test_sync_worker.py` returns ≥ 1 line.
    - `grep -n "test_dead_after_max_attempts" api/tests/unit/test_sync_worker.py` returns ≥ 1 line.
    - `grep -n "test_backoff_progression" api/tests/unit/test_sync_worker.py` returns ≥ 1 line.
    - `grep -n "test_graceful_shutdown" api/tests/unit/test_sync_worker.py` returns ≥ 1 line.
    - `grep -n "test_lag_seconds" api/tests/unit/test_sync_worker.py` returns ≥ 1 line.
    - `cd api && uv run pytest tests/unit/test_sync_worker.py -v` PASS ≥ 16 tests aggregate (Task 3a 11 + Task 3b 5).
    - Phase 1+2+3 regression: `cd api && uv run pytest tests/unit -m "not integration" -x --ignore=tests/unit/test_sync_worker.py 2>&1 | tail -5` PASS ≥ 280 tests.
  </acceptance_criteria>
  <done>5 test mới Task 3b retry/dead/backoff/cancel/lag PASS + aggregate test_sync_worker.py 16+ test PASS; regression 280+ PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hub con worker → central asyncpg pool | Credential-based DSN (CENTRAL_SYNC_DSN) — operator deploy role limited INSERT/UPDATE/DELETE chunks ONLY (T-04-06 mitigation) |
| sync_outbox row → ChunkPayload deserialize | Pydantic validation `extra=ignore` + content_hash hex decode validator — defense vs malformed JSONB (trigger Plan 04-01 controls source) |
| Prometheus label cardinality | hub_name ~10 max + error_class 4 fixed + drift_type 2 fixed + status 2 fixed = 240 series bounded (W7 — label hub_name semantic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Spoofing | Worker connect central với DSN giả mạo | mitigate | Plan 04-04 lifespan asyncpg pool initial query verify == 'medinet_central'; Plan 04-04 owns check |
| T-04-03-02 | Tampering | Outbox payload JSONB tampering qua direct INSERT bypass trigger | mitigate | Plan 04-01 trigger AFTER INSERT/DELETE ON chunks — outbox INSERT chỉ qua trigger function |
| T-04-03-03 | Repudiation | Worker push success/fail KHÔNG ghi audit | mitigate | SYNC_ATTEMPT_TOTAL counter + SYNC_DEAD_TOTAL counter + sync_outbox.last_error column trunc 1000 char |
| T-04-03-04 | Info Disclosure | Worker log full chunk.content lên stdout structlog | accept | logger chỉ log count + error class |
| T-04-03-05 | DoS | Worker tight loop khi outbox empty CPU 100% | mitigate | asyncio.sleep(poll_interval=5s) khi `not batch` branch |
| T-04-03-06 | DoS | Worker hold lock SELECT FOR UPDATE quá lâu → block ingest | mitigate | SKIP LOCKED + batch_size=100 + commit ngay sau MARK_PROCESSING_SQL |
| T-04-03-07 | Elevation | Worker compromise → DROP central tables qua central_pool | mitigate | Operator deploy CENTRAL_SYNC_DSN với role limited GRANT INSERT/UPDATE chunks ONLY |
| T-04-03-08 | Integrity | documents.sync_status stay 'pending'/'syncing' vĩnh viễn (no worker UPDATE) | mitigate | BLOCKER 1 FIX — `_update_document_sync_status` aggregate state SAU MỖI batch; D-V3-Phase4-B2 truth delivered; 4 unit test verify all 4 lifecycle state transition |
| T-04-03-09 | Integrity | Trigger payload content_hash format mismatch ChunkPayload parse | mitigate | BLOCKER 2 FIX — ChunkPayload.content_hash field_validator decode hex string → bytes; Plan 04-01 trigger emit hex qua encode(.., 'hex'); unit test test_chunk_payload_parse_hex_content_hash verify roundtrip |
</threat_model>

<verification>
- 5 file module `api/app/sync/` đầy đủ public + private helpers + UPDATE_DOC_SYNC_STATUS_SQL.
- Task 1+2+3a+3b: 12 + 9 + 11 + 5 = 37 unit test PASS (models + metrics + worker).
- BLOCKER 1 fix: 4 test verify documents.sync_status lifecycle (synced/failed/partial/syncing) + helper called ≥ 3 sites in loop.
- BLOCKER 2 fix: ChunkPayload hex decode validator + roundtrip test với trigger payload shape Plan 04-01.
- W5 fix: Task 3 split → 3a (happy + sync_status, 11 test) + 3b (retry/dead/backoff/cancel/lag, 5 test) — complexity bounded.
- W7 fix: Prometheus label `hub_name` (NOT `hub_id`) verified qua test_label_is_hub_name_not_hub_id.
- Phase 1+2+3 regression 280+ unit test PASS.
- ruff + mypy --strict PASS toàn bộ `app/sync/`.
- Integration test với 2 asyncpg pool runtime defer Plan 04-04.
</verification>

<success_criteria>
- `api/app/sync/{__init__.py, keys.py, models.py, metrics.py, worker.py}` đầy đủ.
- 37+ unit test PASS tách 3 file test.
- 6 Prometheus collector module-level (label `hub_name` — W7 fix).
- UPDATE_DOC_SYNC_STATUS_SQL aggregate logic + `_update_document_sync_status` helper called sau mỗi batch (BLOCKER 1 fix).
- ChunkPayload.content_hash hex decode validator (BLOCKER 2 end-to-end).
- worker.py implement đúng D-V3-Phase4-A5 + B1 + B2 lifecycle complete + B3 op split.
- Phase 1+2+3 regression PASS.
- Git commits: `feat(04-03): app/sync module keys + models + metrics + worker + _update_document_sync_status lifecycle (SYNC-01/02/05 D-V3-Phase4-A1/A5/B1/B2/B3)` + test commits.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-hub-data-sync/04-03-SUMMARY.md`.
</output>
</content>
</invoke>