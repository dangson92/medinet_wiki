---
phase: 04-cross-hub-data-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - api/app/config.py
  - api/tests/unit/test_config_phase4_fields.py
  - docker-compose.yml
  - docker-compose.override.yml.template
autonomous: true
requirements:
  - SYNC-01
  - SYNC-03
  - SYNC-04
tags:
  - settings
  - config
  - docker-compose
  - validator
user_setup: []

must_haves:
  truths:
    - "Hub con boot fail-fast nếu thiếu env HUB_ID hoặc CENTRAL_SYNC_DSN (per D-V3-Phase4-D2 + A5)"
    - "Central boot fail-fast nếu thiếu env CHECKSUM_HUB_DSNS_JSON sau khi có ≥ 1 hub con register (per D-V3-Phase4-C3)"
    - "Settings.sync_batch_size + sync_poll_interval + sync_max_attempts + sync_backoff_seconds đọc đúng từ env, default theo D-V3-Phase4-A5"
    - "Settings.hub_id resolve UUID4 string từ env HUB_ID, reject non-UUID format"
    - "docker-compose.yml 3 hub con block thêm env HUB_ID + CENTRAL_SYNC_DSN; central block thêm CHECKSUM_HUB_DSNS_JSON; KHÔNG break existing env CENTRAL_JWKS_URL + CENTRAL_URL Phase 3"
    - "docker-compose.override.yml.template (FACTOR-04) extend với placeholder {{HUB_ID}} + {{CENTRAL_SYNC_DSN}} cho dynamic hub registration"
  artifacts:
    - path: "api/app/config.py"
      provides: "5 Settings field mới + 3 model_validator boot fail-fast"
      contains: "hub_id"
      contains_alt: "central_sync_dsn"
      contains_extra: "checksum_hub_dsns_json"
    - path: "api/tests/unit/test_config_phase4_fields.py"
      provides: "Unit test 5 field mới + 3 validator + boundary cases"
      min_lines: 150
    - path: "docker-compose.yml"
      provides: "3 hub con env HUB_ID + CENTRAL_SYNC_DSN; central env CHECKSUM_HUB_DSNS_JSON"
      contains: "HUB_ID"
      contains_alt: "CENTRAL_SYNC_DSN"
    - path: "docker-compose.override.yml.template"
      provides: "FACTOR-04 dynamic hub template thêm placeholder {{HUB_ID}}"
      contains: "{{HUB_ID}}"
  key_links:
    - from: "Settings._enforce_hub_id_for_hub_con"
      to: "Settings.hub_id field"
      via: "model_validator(mode=after) raise ValueError if hub_name != central and not hub_id"
      pattern: "_enforce_hub_id_for_hub_con"
    - from: "Settings._enforce_central_sync_dsn_for_hub"
      to: "Settings.central_sync_dsn field"
      via: "model_validator hub con required"
      pattern: "_enforce_central_sync_dsn_for_hub"
    - from: "Settings._enforce_checksum_hub_dsns_for_central"
      to: "Settings.checksum_hub_dsns_json field"
      via: "model_validator central required (JSON dict[str, str])"
      pattern: "_enforce_checksum_hub_dsns_for_central"
---

<objective>
Add 5 Settings field mới + 3 model_validator boot fail-fast (carry forward Phase 1/3 pattern) + docker-compose env wire cho Phase 4 sync infrastructure. Đây là Wave 2 — file-disjoint với Plan 04-03 (chỉ touch `app/config.py` + docker-compose + tests/unit/), KHÔNG đụng `app/sync/`.

Purpose: Config-driven outbox worker (D-V3-Phase4-A5 LOCKED `batch_size=100` + `poll_interval=5s` + `max_attempts=5` + `backoff_seconds=[1,5,30,120]`) + per-hub UUID identity (D-V3-Phase4-D2 — operator deploy responsibility, fail-loud boot nếu thiếu) + central checksum runner DSNs (D-V3-Phase4-C3 dict env CHECKSUM_HUB_DSNS_JSON).

Output:
- `Settings.hub_id: str | None = None` + `_enforce_hub_id_for_hub_con` validator
- `Settings.central_sync_dsn: str | None = None` + `_enforce_central_sync_dsn_for_hub` validator
- `Settings.checksum_hub_dsns_json: str | None = None` + helper `checksum_hub_dsns: dict[str, str]` property + `_enforce_checksum_hub_dsns_for_central` validator
- `Settings.sync_batch_size: int = 100`
- `Settings.sync_poll_interval: float = 5.0`
- `Settings.sync_max_attempts: int = 5`
- `Settings.sync_backoff_seconds: list[int] = [1, 5, 30, 120]` (Annotated NoDecode CSV parse)
- docker-compose 3 hub con env + central env
- override.yml.template FACTOR-04 placeholder
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/04-cross-hub-data-sync/04-CONTEXT.md
@Hub_All/.planning/STATE.md

# Reference Settings pattern Phase 1 + 3
@Hub_All/api/app/config.py
@Hub_All/api/tests/unit/test_config_hub_name.py

# Reference docker-compose pattern Phase 2 + 3
@Hub_All/docker-compose.yml
@Hub_All/docker-compose.override.yml.template

<interfaces>
<!-- Phase 1/3 model_validator pattern carry forward -->

From api/app/config.py (existing validators):
```python
@model_validator(mode="after")
def _enforce_hub_dsn_match(self) -> Settings:
    """E-V3-3 enforce — hub_name phải khớp database name trong DSN."""
    ...

@model_validator(mode="after")
def _enforce_central_jwks_url_for_hub(self) -> Settings:
    """Phase 3 Plan 03-02 — Hub con required CENTRAL_JWKS_URL."""
    if self.hub_name != "central" and not self.central_jwks_url:
        raise ValueError(...)
    return self

@model_validator(mode="after")
def _enforce_central_url_for_hub(self) -> Settings:
    """Phase 3 Plan 03-04 — Hub con required CENTRAL_URL."""
    if self.hub_name != "central" and not self.central_url:
        raise ValueError(...)
    return self
```

From api/app/config.py (NoDecode CSV pattern existing):
```python
cors_allowed_origins: Annotated[list[str], NoDecode] = Field(default_factory=list)

@field_validator("cors_allowed_origins", mode="before")
@classmethod
def _parse_csv(cls, v: str | list[str]) -> list[str]:
    if isinstance(v, str):
        return [o.strip() for o in v.split(",") if o.strip()]
    return v
```

From CONTEXT.md D-V3-Phase4-A5:
```
sync_batch_size=100
sync_poll_interval=5 (seconds)
sync_max_attempts=5
sync_backoff_seconds=[1, 5, 30, 120]
SELECT FOR UPDATE SKIP LOCKED
```

From CONTEXT.md D-V3-Phase4-D2:
```
Settings.hub_id field mới (UUID, đọc từ env HUB_ID boot fail-loud cho hub con).
Operator deploy responsibility set HUB_ID khớp medinet_central.hubs.id row UUID.
```

From CONTEXT.md D-V3-Phase4-C3:
```
Settings.checksum_hub_dsns: dict[str, str] (read-only role per hub)
docker-compose env CHECKSUM_HUB_DSNS_JSON
Hub con KHÔNG spawn (lifespan conditional)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings 5 field mới + 3 model_validator + helper property</name>
  <read_first>
    - api/app/config.py (đọc toàn bộ — 388 LOC; cần hiểu pattern Field + model_validator + NoDecode)
    - api/tests/unit/test_config_hub_name.py (pattern unit test Settings boot)
    - api/tests/unit/test_jwks_cache.py (pattern test model_validator hub con required)
    - .planning/phases/04-cross-hub-data-sync/04-CONTEXT.md sections D-V3-Phase4-A5 + D2 + C3
  </read_first>
  <behavior>
    - Test 1: `Settings(hub_name="central", database_url="postgresql+asyncpg://u:p@h:5432/medinet_central", cocoindex_database_url="...", redis_url="...", ...) ` — central KHÔNG required hub_id / central_sync_dsn → boot OK.
    - Test 2: `Settings(hub_name="yte", database_url="...medinet_hub_yte", ...)` không set HUB_ID → `ValidationError` chứa text "hub_name='yte' (hub con) yêu cầu HUB_ID".
    - Test 3: `Settings(hub_name="yte", hub_id="not-a-uuid", ...)` → `ValidationError` chứa "hub_id invalid UUID".
    - Test 4: `Settings(hub_name="yte", hub_id="12345678-1234-1234-1234-123456789012", ...)` không set CENTRAL_SYNC_DSN → `ValidationError` chứa "hub_name='yte' (hub con) yêu cầu CENTRAL_SYNC_DSN".
    - Test 5: `Settings(hub_name="yte", hub_id="<uuid>", central_sync_dsn="postgresql+asyncpg://...medinet_central", central_jwks_url=..., central_url=..., ...)` → boot OK.
    - Test 6: `Settings(hub_name="central", ..., checksum_hub_dsns_json=None)` → boot OK (CHECKSUM_HUB_DSNS_JSON optional ở central, default empty dict — Settings cho phép central deploy lần đầu CHƯA có hub con).
    - Test 7: `Settings(hub_name="central", checksum_hub_dsns_json='{"yte": "postgresql://...medinet_hub_yte"}')` → `settings.checksum_hub_dsns == {"yte": "postgresql://...medinet_hub_yte"}` (property parse JSON).
    - Test 8: `Settings(hub_name="central", checksum_hub_dsns_json='not-json')` → `ValidationError` "CHECKSUM_HUB_DSNS_JSON invalid JSON".
    - Test 9: `Settings(hub_name="central", checksum_hub_dsns_json='["list", "not", "dict"]')` → `ValidationError` "CHECKSUM_HUB_DSNS_JSON phải là dict[str, str]".
    - Test 10: defaults D-V3-Phase4-A5: `settings.sync_batch_size == 100`, `sync_poll_interval == 5.0`, `sync_max_attempts == 5`, `sync_backoff_seconds == [1, 5, 30, 120]`.
    - Test 11: CSV parse `SYNC_BACKOFF_SECONDS="2,10,60,300"` env → `settings.sync_backoff_seconds == [2, 10, 60, 300]`.
    - Test 12: `sync_backoff_seconds` length phải MATCH `sync_max_attempts - 1` (4 backoff cho 5 attempts — attempt 1 KHÔNG backoff, attempts 2-5 dùng backoff[0..3]). Validator `_validate_backoff_length` raise nếu mismatch.
  </behavior>
  <action>
    Thêm 5 field + 3 validator + 1 property vào `api/app/config.py` (insert SAU `central_url` field, TRƯỚC `file_store_dir`):

    ```python
    # ──────────────────────────────────────────────────────────────────────
    # Phase 4 Plan 04-02 — Cross-hub Data Sync config (SYNC-01..05)
    # ──────────────────────────────────────────────────────────────────────

    # D-V3-Phase4-D2 — Hub con UUID identity từ medinet_central.hubs.id
    # Operator deploy responsibility: set HUB_ID khớp central.hubs row UUID.
    # docker-compose 3 hub con env HUB_ID=<uuid from migrate-data step>
    # Central KHÔNG cần hub_id (aggregator, KHÔNG own data).
    hub_id: str | None = None

    # D-V3-Phase4-A1 — Hub con outbox worker push central qua dedicated pool.
    # DSN trỏ medinet_central (CHỈ hub con cần; central KHÔNG self-push).
    # Credential-based auth — operator deploy với role hạn chế INSERT chunks
    # ON CONFLICT (T-04-06 mitigation — hub con KHÔNG DROP central tables).
    # docker-compose 3 hub con env CENTRAL_SYNC_DSN=postgresql+asyncpg://sync_user:...@postgres:5432/medinet_central
    central_sync_dsn: str | None = None

    # D-V3-Phase4-C3 — Central checksum scheduler connect read-only tới N hub con.
    # JSON dict {"hub_name": "asyncpg DSN read-only"} qua env CHECKSUM_HUB_DSNS_JSON.
    # Property `checksum_hub_dsns` parse + cache. Hub con KHÔNG dùng (validator skip).
    # Default None ở central cho phép deploy lần đầu CHƯA register hub con
    # (scheduler tick sẽ no-op khi dict empty — KHÔNG fail-loud).
    checksum_hub_dsns_json: str | None = None

    # D-V3-Phase4-A5 — Worker loop config (Recommended option 1 default).
    # Operator tune sau observe Prometheus metrics (Plan 04-03 + 04-06).
    sync_batch_size: int = 100
    sync_poll_interval: float = 5.0  # seconds
    sync_max_attempts: int = 5
    sync_backoff_seconds: Annotated[list[int], NoDecode] = Field(
        default_factory=lambda: [1, 5, 30, 120]
    )
    ```

    Thêm `@field_validator` cho sync_backoff_seconds (mode="before"):

    ```python
    @field_validator("sync_backoff_seconds", mode="before")
    @classmethod
    def _parse_backoff_csv(cls, v: str | list[int]) -> list[int]:
        """Parse CSV "1,5,30,120" → [1, 5, 30, 120]; cho phép pass list trực tiếp."""
        if isinstance(v, str):
            return [int(x.strip()) for x in v.split(",") if x.strip()]
        return v
    ```

    Thêm `@field_validator` cho hub_id (mode="after"):

    ```python
    @field_validator("hub_id", mode="after")
    @classmethod
    def _validate_hub_id_uuid(cls, v: str | None) -> str | None:
        """Validate hub_id format UUID4 string (D-V3-Phase4-D2)."""
        if v is None:
            return v
        import uuid as _uuid
        try:
            _uuid.UUID(v)
        except (ValueError, TypeError) as e:
            raise ValueError(
                f"hub_id invalid UUID format: {v!r} — must be UUID4 string "
                f"khớp medinet_central.hubs.id row (xem docker-compose env HUB_ID)."
            ) from e
        return v
    ```

    Thêm 3 `@model_validator(mode="after")`:

    ```python
    @model_validator(mode="after")
    def _enforce_hub_id_for_hub_con(self) -> Settings:
        """Phase 4 Plan 04-02 SYNC-01/D-V3-Phase4-D2 — Hub con required HUB_ID.

        Central (hub_name="central") aggregator KHÔNG own data → hub_id None OK.
        Hub con (yte/duoc/hcns/dynamic) PHẢI set HUB_ID UUID khớp central.hubs row
        cho chunks.hub_id INSERT (rag/flow.py ChunkRow.hub_id) + sync_outbox payload.

        Threat model:
        - T-04-02-01 Tampering — env thiếu HUB_ID → hub con boot OK nhưng cocoindex
          flow INSERT chunks với hub_id=NULL → constraint violation runtime. Fail-fast
          validator chống production silent bug.
        """
        if self.hub_name != "central" and not self.hub_id:
            raise ValueError(
                f"hub_name={self.hub_name!r} (hub con) yêu cầu HUB_ID env var "
                f"(UUID khớp medinet_central.hubs.id row). "
                f"Xem docker-compose.yml 3 hub con block — Plan 04-02 SYNC-01."
            )
        return self

    @model_validator(mode="after")
    def _enforce_central_sync_dsn_for_hub(self) -> Settings:
        """Phase 4 Plan 04-02 SYNC-01/D-V3-Phase4-A1 — Hub con required CENTRAL_SYNC_DSN.

        Worker (Plan 04-03) dùng dedicated asyncpg pool tới central để push chunks
        ON CONFLICT (chunk_id) DO UPDATE. Operator deploy với role limited INSERT
        chunks (KHÔNG DELETE other hub_id — T-04-06 Elevation mitigation).

        Threat model:
        - T-04-02-02 DoS — env thiếu CENTRAL_SYNC_DSN → worker spawn lifespan FAIL
          runtime + outbox accumulate indefinitely. Fail-fast validator chống deploy bug.
        """
        if self.hub_name != "central" and not self.central_sync_dsn:
            raise ValueError(
                f"hub_name={self.hub_name!r} (hub con) yêu cầu CENTRAL_SYNC_DSN "
                f"env var (postgresql+asyncpg://... trỏ medinet_central). "
                f"Xem docker-compose.yml 3 hub con block — Plan 04-02 SYNC-01."
            )
        return self

    @model_validator(mode="after")
    def _enforce_checksum_hub_dsns_for_central(self) -> Settings:
        """Phase 4 Plan 04-02 SYNC-04/D-V3-Phase4-C3 — Central CHECKSUM_HUB_DSNS_JSON.

        Optional ở central deploy lần đầu (CHƯA register hub con) — default None
        cho phép Settings(...) construct success. Validator CHỈ enforce format JSON
        nếu set, KHÔNG enforce required. Scheduler loop (Plan 04-06) no-op khi dict empty.

        Threat model:
        - T-04-02-03 Tampering — operator paste malformed JSON → boot fail thay vì
          silent scheduler crash mid-tick. Fail-fast validator JSON parse + dict[str,str] type check.
        """
        if self.checksum_hub_dsns_json is None:
            return self
        import json
        try:
            parsed = json.loads(self.checksum_hub_dsns_json)
        except json.JSONDecodeError as e:
            raise ValueError(
                f"CHECKSUM_HUB_DSNS_JSON invalid JSON: {e!s}. "
                f"Expected dict {{\"hub_name\": \"asyncpg DSN\"}} — Plan 04-02 SYNC-04."
            ) from e
        if not isinstance(parsed, dict):
            raise ValueError(
                f"CHECKSUM_HUB_DSNS_JSON phải là dict[str, str], "
                f"got {type(parsed).__name__!r} — Plan 04-02 SYNC-04 D-V3-Phase4-C3."
            )
        for k, v in parsed.items():
            if not isinstance(k, str) or not isinstance(v, str):
                raise ValueError(
                    f"CHECKSUM_HUB_DSNS_JSON entry {k!r}: {v!r} — keys + values "
                    f"phải là str (hub_name + asyncpg DSN). Plan 04-02 SYNC-04."
                )
        return self

    @model_validator(mode="after")
    def _validate_backoff_length(self) -> Settings:
        """Backoff array length phải = max_attempts - 1.

        D-V3-Phase4-A5: attempt 1 KHÔNG backoff (immediate first try), attempts 2-N
        dùng backoff[0..N-2]. max_attempts=5 → backoff_seconds length=4.

        Operator override env SYNC_MAX_ATTEMPTS=3 phải tương ứng SYNC_BACKOFF_SECONDS="1,5"
        (length 2). Mismatch → boot fail-loud thay vì IndexError runtime.
        """
        expected = self.sync_max_attempts - 1
        actual = len(self.sync_backoff_seconds)
        if actual != expected:
            raise ValueError(
                f"sync_backoff_seconds length mismatch: max_attempts={self.sync_max_attempts} "
                f"yêu cầu length={expected} (attempts 2..N), got {actual} ({self.sync_backoff_seconds!r}). "
                f"Plan 04-02 D-V3-Phase4-A5."
            )
        return self
    ```

    Thêm property cho central convenience:

    ```python
    @property
    def checksum_hub_dsns(self) -> dict[str, str]:
        """Parse checksum_hub_dsns_json → dict (empty nếu None — central deploy lần đầu)."""
        if self.checksum_hub_dsns_json is None:
            return {}
        import json
        result = json.loads(self.checksum_hub_dsns_json)
        return {str(k): str(v) for k, v in result.items()}
    ```
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/unit/test_config_phase4_fields.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hub_id: str | None = None" api/app/config.py` returns ≥ 1 line.
    - `grep -n "central_sync_dsn: str | None = None" api/app/config.py` returns ≥ 1 line.
    - `grep -n "checksum_hub_dsns_json: str | None = None" api/app/config.py` returns ≥ 1 line.
    - `grep -n "sync_batch_size: int = 100" api/app/config.py` returns ≥ 1 line.
    - `grep -n "sync_poll_interval: float = 5.0" api/app/config.py` returns ≥ 1 line.
    - `grep -n "sync_max_attempts: int = 5" api/app/config.py` returns ≥ 1 line.
    - `grep -n "sync_backoff_seconds" api/app/config.py` returns ≥ 2 lines (field + parse_backoff_csv).
    - `grep -n "_enforce_hub_id_for_hub_con" api/app/config.py` returns ≥ 1 line.
    - `grep -n "_enforce_central_sync_dsn_for_hub" api/app/config.py` returns ≥ 1 line.
    - `grep -n "_enforce_checksum_hub_dsns_for_central" api/app/config.py` returns ≥ 1 line.
    - `grep -n "_validate_backoff_length" api/app/config.py` returns ≥ 1 line.
    - `grep -n "def checksum_hub_dsns" api/app/config.py` returns ≥ 1 line (property).
    - `cd api && uv run pytest tests/unit/test_config_phase4_fields.py -v` PASS ≥ 12 tests.
    - Phase 1+3 regression: `cd api && uv run pytest tests/unit/test_config_hub_name.py tests/unit/test_config_hub_name_dynamic.py -v` exit 0 (KHÔNG break — env HUB_ID + CENTRAL_SYNC_DSN missing ở các test cũ vẫn pass do default None, CHỈ hub_name != central + missing mới fail).
    - `cd api && uv run ruff check app/config.py tests/unit/test_config_phase4_fields.py` exit 0.
    - `cd api && uv run mypy --strict app/config.py` exit 0.
  </acceptance_criteria>
  <done>5 field + 3 model_validator + 1 length validator + 1 property + ≥ 12 unit test PASS; Phase 1/3 regression giữ nguyên; ruff + mypy --strict PASS.</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose 3 hub con + central env wire + override.yml.template FACTOR-04</name>
  <read_first>
    - docker-compose.yml (đọc toàn bộ — confirm 4 service block hiện hữu: python-api-central + 3 hub con)
    - docker-compose.override.yml.template (FACTOR-04 dynamic hub template)
    - api/app/config.py (Task 1 vừa đổi — confirm env name HUB_ID + CENTRAL_SYNC_DSN + CHECKSUM_HUB_DSNS_JSON + SYNC_* match)
  </read_first>
  <action>
    **A. docker-compose.yml** — thêm env vào 3 hub con service block (python-api-yte/duoc/hcns):

    Mỗi hub con block hiện có structure (Phase 3 carry forward):
    ```yaml
    python-api-yte:
      <<: *api-template
      environment:
        HUB_NAME: yte
        DATABASE_URL: postgresql+asyncpg://medinet:medinet@postgres:5432/medinet_hub_yte
        CENTRAL_JWKS_URL: http://python-api-central:8080/.well-known/jwks.json
        CENTRAL_URL: http://python-api-central:8080
      ports:
        - "8181:8080"
    ```

    Thêm 2 env vào MỖI hub con block (yte/duoc/hcns):
    ```yaml
        HUB_ID: ${HUB_YTE_ID:?HUB_YTE_ID env var phải set khớp central.hubs.id UUID — Plan 04-02 SYNC-01}
        CENTRAL_SYNC_DSN: postgresql+asyncpg://medinet:medinet@postgres:5432/medinet_central
    ```

    Cho `python-api-duoc`:
    ```yaml
        HUB_ID: ${HUB_DUOC_ID:?HUB_DUOC_ID env var phải set khớp central.hubs.id UUID — Plan 04-02 SYNC-01}
        CENTRAL_SYNC_DSN: postgresql+asyncpg://medinet:medinet@postgres:5432/medinet_central
    ```

    Cho `python-api-hcns`:
    ```yaml
        HUB_ID: ${HUB_HCNS_ID:?HUB_HCNS_ID env var phải set khớp central.hubs.id UUID — Plan 04-02 SYNC-01}
        CENTRAL_SYNC_DSN: postgresql+asyncpg://medinet:medinet@postgres:5432/medinet_central
    ```

    Lý do `${HUB_*_ID:?error message}` syntax: docker-compose interpolation fail-loud nếu env shell `HUB_YTE_ID` không set (operator phải export trước `docker compose up`). UUID actual lấy từ Phase 7 MIGRATE-01 migrate data hoặc Phase 6 SETTINGS-04 hub_registry; v3.0-b initial deploy operator manual export.

    Thêm vào `python-api-central` block:
    ```yaml
        CHECKSUM_HUB_DSNS_JSON: ${CHECKSUM_HUB_DSNS_JSON:-}
    ```

    `${VAR:-}` syntax: default empty string nếu env không set (central CHƯA register hub con OK — checksum scheduler no-op).

    **B. docker-compose.override.yml.template** — Extend FACTOR-04 placeholder:

    Đọc file hiện tại, thêm 2 env vào template block:
    ```yaml
        HUB_ID: ${HUB_{{HUB_UPPER}}_ID:?HUB_{{HUB_UPPER}}_ID env var phải set khớp central.hubs.id UUID}
        CENTRAL_SYNC_DSN: postgresql+asyncpg://medinet:medinet@postgres:5432/medinet_central
    ```

    Nơi `{{HUB_UPPER}}` là placeholder sed substitute uppercase hub_name (FACTOR-04 hub-add.sh transform).

    **C. Verify post-write:**
    ```bash
    docker compose config --quiet  # exit 0
    docker compose config | grep -E "HUB_ID|CENTRAL_SYNC_DSN|CHECKSUM_HUB_DSNS_JSON" | wc -l
    # expect ≥ 7 line (3 HUB_ID + 3 CENTRAL_SYNC_DSN + 1 CHECKSUM_HUB_DSNS_JSON)
    ```

    Lưu ý: `docker compose config` interpolate env — nếu HUB_YTE_ID không export ở shell, command sẽ fail. Test workflow:
    ```bash
    export HUB_YTE_ID="00000000-0000-0000-0000-000000000001"
    export HUB_DUOC_ID="00000000-0000-0000-0000-000000000002"
    export HUB_HCNS_ID="00000000-0000-0000-0000-000000000003"
    docker compose config --quiet
    ```
  </action>
  <verify>
    <automated>cd Hub_All && (HUB_YTE_ID="00000000-0000-0000-0000-000000000001" HUB_DUOC_ID="00000000-0000-0000-0000-000000000002" HUB_HCNS_ID="00000000-0000-0000-0000-000000000003" docker compose config --quiet) && echo "config OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc "HUB_ID:" docker-compose.yml` returns ≥ 3 (3 hub con block).
    - `grep -nc "CENTRAL_SYNC_DSN:" docker-compose.yml` returns ≥ 3 (3 hub con block).
    - `grep -n "CHECKSUM_HUB_DSNS_JSON" docker-compose.yml` returns ≥ 1 line (central block).
    - `grep -n "HUB_YTE_ID" docker-compose.yml` returns ≥ 1 line.
    - `grep -n "HUB_DUOC_ID" docker-compose.yml` returns ≥ 1 line.
    - `grep -n "HUB_HCNS_ID" docker-compose.yml` returns ≥ 1 line.
    - `grep -n "{{HUB_UPPER}}_ID" docker-compose.override.yml.template` returns ≥ 1 line.
    - `grep -n "CENTRAL_SYNC_DSN" docker-compose.override.yml.template` returns ≥ 1 line.
    - With env vars exported (3 dummy UUID): `docker compose config --quiet` exit 0.
    - `docker compose config | grep HUB_ID | wc -l` ≥ 3.
    - Phase 2/3 regression: existing CENTRAL_JWKS_URL + CENTRAL_URL vẫn render OK.
  </acceptance_criteria>
  <done>3 hub con block thêm HUB_ID + CENTRAL_SYNC_DSN env; central block thêm CHECKSUM_HUB_DSNS_JSON; FACTOR-04 template extend placeholder; `docker compose config` PASS với 3 dummy UUID exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell env → Settings | Operator export HUB_YTE_ID + CENTRAL_SYNC_DSN → docker-compose interpolate → container env → pydantic-settings load |
| docker-compose.override.yml → container env | FACTOR-04 dynamic hub registration (Plan 02-05 carry forward) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | Hub con boot với HUB_ID giả mạo (operator paste UUID hub khác) | accept | Operator deploy responsibility; Phase 7 MIGRATE-01 + Phase 6 SETTINGS-04 hub_registry sẽ centralize source-of-truth. v3.0-b chấp nhận manual deploy gate |
| T-04-02-02 | Tampering | Env CHECKSUM_HUB_DSNS_JSON paste malformed JSON | mitigate | `_enforce_checksum_hub_dsns_for_central` validator parse + dict[str,str] type check; ValueError fail-fast boot |
| T-04-02-03 | Tampering | Env HUB_ID không phải UUID (vd "yte" string) | mitigate | `_validate_hub_id_uuid` field_validator parse uuid.UUID — ValueError fail-fast |
| T-04-02-04 | Info Disclosure | CENTRAL_SYNC_DSN chứa password plaintext trong docker-compose.yml | mitigate | docker-compose dev/test dùng dummy password `medinet:medinet`; production deploy qua secrets backing (Docker Swarm secrets / K8s Secret) — defer Phase 7 deploy guide |
| T-04-02-05 | DoS | sync_backoff_seconds length ≠ max_attempts - 1 → IndexError worker runtime | mitigate | `_validate_backoff_length` model_validator boot fail-fast với expected/actual diff message |
| T-04-02-06 | Elevation | Operator vô tình paste CENTRAL_SYNC_DSN với role superuser → hub con có quyền DROP TABLE central | mitigate | Plan 04-04 deploy doc note: production phải tạo role `sync_user` GRANT INSERT, UPDATE, DELETE ON chunks (theo hub_id) + SELECT ON hubs — defer Plan 04-06 admin doc + Phase 7 MIGRATE deploy |
</threat_model>

<verification>
- 5 Settings field mới + 3 model_validator + 1 length validator + 1 property.
- ≥ 12 unit test PASS test_config_phase4_fields.py.
- Phase 1+3 regression test_config_hub_name + test_config_hub_name_dynamic + test_jwks_cache PASS.
- docker-compose.yml + override.yml.template render OK với 3 dummy HUB_*_ID exported.
- ruff + mypy --strict PASS.
</verification>

<success_criteria>
- `api/app/config.py` add 5 field + 3 validator + 1 length validator + 1 property.
- `api/tests/unit/test_config_phase4_fields.py` ≥ 12 test PASS.
- `docker-compose.yml` 3 hub con + central block env wire đúng.
- `docker-compose.override.yml.template` FACTOR-04 placeholder extend.
- All Phase 1/3 regression PASS.
- Git commit: `feat(04-02): Settings 5 field mới + 3 validator + docker-compose env Phase 4 sync (SYNC-01/03/04 D-V3-Phase4-A5/C3/D2)`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-hub-data-sync/04-02-SUMMARY.md`.
</output>
