---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - api/app/main.py
  - api/app/auth/api_key.py
  - api/app/auth/__init__.py
  - api/app/services/documents_service.py
  - api/app/routers/documents.py
  - api/tests/integration/test_documents_list_delete.py
  - api/tests/integration/test_hub_isolation.py
  - api/tests/integration/test_rate_limit.py
autonomous: true
requirements: [HUB-02, AUX-02, AUX-03]
user_setup: []

must_haves:
  truths:
    - "main.py mount đủ 7 router (auth + documents + hubs/users/profile/api_keys/audit_logs)"
    - "main.py wire slowapi limiter (app.state.limiter + exception_handler RateLimitExceeded → envelope 429)"
    - "main.py wire HubIsolationError → 403 envelope handler"
    - "X-API-Key header auth — require_api_key verify key qua AES-GCM decrypt; key sai/revoked → 401"
    - "Editor Hub A KHÔNG thể DELETE document Hub B kể cả khi truyền explicit hub_id — 403"
    - "documents_service.delete() gọi verify_hub_access + enqueue_audit('security.hub_isolation_violation') tại điểm reject TRƯỚC khi raise"
    - "GET /api/audit-logs (rate-limited Plan 05-05) — spam vượt ngưỡng → 429 envelope"
  artifacts:
    - path: "api/app/main.py"
      provides: "Wiring đủ router + rate limiter + HubIsolationError handler"
      contains: "hubs_router"
    - path: "api/app/auth/api_key.py"
      provides: "require_api_key dependency — X-API-Key external auth"
      exports: ["require_api_key"]
    - path: "api/app/services/documents_service.py"
      provides: "delete() enforce hub isolation + audit emit tại reject point (HUB-02 / E4)"
      contains: "security.hub_isolation_violation"
    - path: "api/tests/integration/test_hub_isolation.py"
      provides: "E4 EXIT criteria — hub isolation critical test suite"
      contains: "security.hub_isolation_violation"
  key_links:
    - from: "api/app/main.py"
      to: "hubs_router/users_router/profile_router/api_keys_router/audit_logs_router"
      via: "include_router trong create_app"
      pattern: "include_router"
    - from: "api/app/services/documents_service.py"
      to: "verify_hub_access + enqueue_audit"
      via: "delete() mutation enforce hub isolation + audit security.hub_isolation_violation"
      pattern: "verify_hub_access"
    - from: "api/app/main.py"
      to: "slowapi limiter"
      via: "app.state.limiter + add_exception_handler"
      pattern: "RateLimitExceeded"
---

<objective>
Wave 4 — khép Phase 5: wiring + hub isolation enforcement + E4 EXIT criteria test. 3 việc:

1. **Mount router + wire rate limiter + HubIsolationError handler + X-API-Key** vào `main.py`:
   - `include_router` 5 router Phase 5: `hubs_router`, `users_router`, `profile_router`, `api_keys_router`, `audit_logs_router` (đã export sẵn ở `routers/__init__.py` qua Plan 05-03/04/05).
   - Wire slowapi (AUX-03): `app.state.limiter = limiter` + `app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)` (Plan 05-02 tạo module; Plan 05-05 đã decorate `GET /api/audit-logs` với `@limiter.limit` — plan này wire vào app để 429 thực sự kích hoạt).
   - `@app.exception_handler(HubIsolationError)` → 403 envelope.
   - X-API-Key auth (AUX-02): tạo `app/auth/api_key.py` — dependency `require_api_key`.

2. **Hub isolation enforce ở document DELETE mutation** (HUB-02 — EXIT criteria E4 — BLOCKER 1+2): `documents_service.delete()` (Phase 4) được retrofit để gọi `verify_hub_access` (Plan 05-02) + emit audit `security.hub_isolation_violation` TẠI điểm reject TRƯỚC khi raise. Đây là task `auto` SỞ HỮU enforcement design — KHÔNG để executor tự "đọc router rồi tự chọn endpoint". Endpoint mục tiêu CHỐT: `DELETE /api/documents/:doc_id` (ROADMAP SC2 nêu đích danh). Editor Hub A KHÔNG DELETE được document Hub B; admin bypass.

3. **test_hub_isolation.py** + **test_rate_limit.py** — E4 critical test suite + rate-limit envelope verify (hit endpoint THẬT `GET /api/audit-logs`).

NOTE: CRUD endpoint test (Hub/User/APIKey/Audit) ĐÃ thuộc Plan 05-03/04/05 — plan này KHÔNG lặp lại; chỉ lo wiring + hub-isolation mutation enforce + test.

Purpose: HUB-02 là điều kiện ship M2 (E4). Wave cuối — mọi router/service plan trước đã sẵn sàng.
Output: `main.py` wiring, `auth/api_key.py`, `documents_service.py` + `documents.py` hub-isolation enforce, `test_hub_isolation.py`, `test_rate_limit.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- main.py — api/app/main.py:
  create_app() dòng 262-270: `from app.auth import auth_router` + include_router(auth_router)
                            + `from app.routers import documents_router` + include_router(documents_router)
  HTTPException → envelope handler đã có (main.py:280-304 — http_exception_handler).
  Middleware order CONVENTIONS §4 — KHÔNG đổi. -->

<!-- Plan 05-02 output — api/app/middleware/rate_limit.py:
  limiter (slowapi Limiter), rate_limit_exceeded_handler (RateLimitExceeded → resp.too_many_requests).
  slowapi wiring chuẩn: app.state.limiter = limiter; app.add_exception_handler(RateLimitExceeded, handler).
  Module export qua app/middleware/__init__.py — `from app.middleware import limiter, rate_limit_exceeded_handler`.
  Plan 05-05 đã decorate GET /api/audit-logs với @limiter.limit — plan này CHỈ wire app.state.limiter + handler. -->

<!-- Plan 05-02 output — api/app/repositories/hub_isolation.py:
  verify_hub_access(*, role: str, user_hub_ids: list[str], resource_hub_id: str) -> None  — raise HubIsolationError
  HubIsolationError(message, *, resource_hub_id) — .resource_hub_id attribute
  get_current_user_with_hubs -> UserWithHubs (.user, .hub_ids) — auth/dependencies.py -->

<!-- Plan 05-01 output — api/app/services/audit_service.py:
  enqueue_audit(AuditEntry(action="security.hub_isolation_violation", ...)) — action có trong AUDIT_ACTIONS -->

<!-- Plan 05-03/04/05 output — api/app/routers/__init__.py ĐÃ export:
  documents_router, hubs_router, users_router, profile_router, api_keys_router, audit_logs_router -->

<!-- Plan 05-05 output — api/app/services/api_key_service.py:
  ApiKeyService(db).verify_key(plain_key: str) -> dict | None  — {id, permissions, allowed_hub_ids} nếu key hợp lệ -->

<!-- documents_service.delete HIỆN TẠI — documents_service.py:299-364:
  delete(document_id, *, deleted_by: UUID, request_id=None) -> bool
  SELECT hub_id, file_path FROM documents WHERE id=:id  → đã có hub_id resource sẵn (KHÔNG cần đọc từ payload).
  documents.py DELETE endpoint dòng 187-225 — hiện auth require_role("admin").
  test_documents_list_delete.py gọi qua HTTP endpoint (KHÔNG gọi service.delete trực tiếp) — kiểm tra trước khi sửa. -->

<!-- get_current_user / require_role — api/app/auth/dependencies.py. User .id .role. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 5 router + rate limiter + HubIsolationError handler + X-API-Key vào main.py</name>
  <files>api/app/main.py, api/app/auth/api_key.py, api/app/auth/__init__.py</files>
  <read_first>
    - api/app/main.py (create_app dòng 230-366 — include_router block, exception_handler, healthz)
    - api/app/middleware/rate_limit.py + api/app/middleware/__init__.py (Plan 05-02 — limiter, rate_limit_exceeded_handler)
    - api/app/routers/__init__.py (sau Plan 05-03/04/05 — exports 6 router)
    - api/app/repositories/hub_isolation.py (Plan 05-02 — HubIsolationError)
    - api/app/services/api_key_service.py (Plan 05-05 — ApiKeyService.verify_key)
    - api/app/auth/dependencies.py (analog dependency pattern; get_session)
    - api/app/auth/__init__.py (pattern re-export __all__)
  </read_first>
  <action>
**A) Mount 5 router Phase 5** — trong `create_app()`, sau `app.include_router(documents_router)` (dòng ~270):
```python
# Mount Phase 5 router (HUB-01..03, USER-01..03, AUX-01..02).
from app.routers import (
    api_keys_router,
    audit_logs_router,
    hubs_router,
    profile_router,
    users_router,
)
app.include_router(hubs_router)
app.include_router(users_router)
app.include_router(profile_router)
app.include_router(api_keys_router)
app.include_router(audit_logs_router)
```

**B) Wire slowapi rate limiter** (AUX-03) — sau mount router:
```python
# Rate limiter (Phase 5 AUX-03 — slowapi). Plan 05-02 tạo module; Plan 05-05
# decorate GET /api/audit-logs; wiring app.state.limiter + handler tại đây.
from slowapi.errors import RateLimitExceeded
from app.middleware import limiter, rate_limit_exceeded_handler
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)
```
KHÔNG `add_middleware(SlowAPIMiddleware)` — `@limiter.limit` decorator (Plan 05-05 đã áp `GET /api/audit-logs`; Phase 6 sẽ áp search/ask) + handler đủ render 429 envelope. Comment ghi: "GET /api/audit-logs đã decorate @limiter.limit (Plan 05-05 W4); auth/me KHÔNG decorate = KHÔNG limit (AUX-03)".

**C) HubIsolationError → 403 envelope handler** — sau `http_exception_handler` (dòng ~304):
```python
from app.repositories.hub_isolation import HubIsolationError

@app.exception_handler(HubIsolationError)
async def hub_isolation_handler(
    request: FastAPIRequest, exc: HubIsolationError
) -> StarletteJSONResponse:
    """HubIsolationError → 403 envelope (HUB-02 / E4).

    LƯU Ý: handler này CHỈ render envelope 403. Audit entry
    'security.hub_isolation_violation' do `documents_service.delete()`
    enqueue TẠI điểm reject (Task 2) — KHÔNG emit ở handler này.
    """
    body = {
        "success": False,
        "data": None,
        "error": {"code": "FORBIDDEN", "message": str(exc)},
        "meta": None,
    }
    return StarletteJSONResponse(content=body, status_code=403)
```
(`FastAPIRequest`, `StarletteJSONResponse` đã import ở main.py:36-37.)

**D) X-API-Key auth** (AUX-02 — "auth middleware accept header X-API-Key ngoài JWT"). Tạo file mới `api/app/auth/api_key.py`:
```python
"""X-API-Key auth dependency — AUX-02 external integration.

Cho phép request mang header `X-API-Key: mdk_...` truy cập API thay vì Bearer JWT.
Key verify qua ApiKeyService.verify_key (decrypt AES-GCM + match key_prefix).
Endpoint external (Phase 6/7 search/ask) opt-in Depends(require_api_key).
"""
from __future__ import annotations

from fastapi import Depends, Header, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_session
from app.services.api_key_service import ApiKeyService


async def require_api_key(
    x_api_key: str | None = Header(default=None, alias="X-API-Key"),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> dict:
    """Verify X-API-Key header → principal dict {id, permissions, allowed_hub_ids}.

    Raise 401 API_KEY_MISSING nếu thiếu header; API_KEY_INVALID nếu key sai/revoked.
    """
    if not x_api_key:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "API_KEY_MISSING", "message": "Thiếu header X-API-Key"},
        )
    principal = await ApiKeyService(db=db).verify_key(x_api_key)
    if principal is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "API_KEY_INVALID", "message": "API key không hợp lệ hoặc đã thu hồi"},
        )
    return principal
```
Cập nhật `api/app/auth/__init__.py`: thêm `from app.auth.api_key import require_api_key` + thêm `"require_api_key"` vào `__all__`.

Phase 5 chỉ TẠO dependency `require_api_key` — endpoint cụ thể opt-in thuộc Phase 6/7. Test Task 3 verify dependency hoạt động qua test-only route.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/main.py app/auth/api_key.py app/auth/__init__.py && uv run mypy --strict app/main.py app/auth/api_key.py && uv run python -c "from app.main import create_app; app=create_app(); paths={r.path for r in app.routes}; assert {'/api/hubs','/api/users','/api/profile','/api/api-keys','/api/audit-logs'} <= paths, sorted(paths); assert getattr(app.state,'limiter',None) is not None; from app.auth import require_api_key"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "hubs_router" api/app/main.py` + `users_router` + `profile_router` + `api_keys_router` + `audit_logs_router` — tất cả match
    - `grep "app.state.limiter" api/app/main.py` match; `grep "RateLimitExceeded" api/app/main.py` match
    - `grep "HubIsolationError" api/app/main.py` match (exception_handler đăng ký)
    - `api/app/auth/api_key.py` tồn tại, export `require_api_key`; `app/auth/__init__.py` `__all__` chứa `require_api_key`
    - `create_app()` mount đủ route `/api/hubs`, `/api/users`, `/api/profile`, `/api/api-keys`, `/api/audit-logs`
    - `ruff check` + `mypy --strict` exit 0; smoke test exit 0
  </acceptance_criteria>
  <done>main.py mount 5 router Phase 5 + slowapi limiter + HubIsolationError handler; auth/api_key.py require_api_key dependency tạo xong.</done>
</task>

<task type="auto">
  <name>Task 2: Hub isolation enforce + audit emit ở document DELETE mutation (HUB-02 / E4 — BLOCKER 1+2)</name>
  <files>api/app/services/documents_service.py, api/app/routers/documents.py, api/tests/integration/test_documents_list_delete.py</files>
  <read_first>
    - api/app/services/documents_service.py (delete() dòng 299-364 — SELECT hub_id đã có sẵn; điểm chèn enforcement)
    - api/app/routers/documents.py (DELETE endpoint dòng 187-225 — hiện require_role("admin"); cần đổi sang editor-eligible)
    - api/app/repositories/hub_isolation.py (Plan 05-02 — verify_hub_access, HubIsolationError)
    - api/app/auth/dependencies.py (Plan 05-02 — get_current_user_with_hubs; UserWithHubs .user .hub_ids)
    - api/app/services/audit_service.py (Plan 05-01 — enqueue_audit, AuditEntry — action security.hub_isolation_violation trong AUDIT_ACTIONS)
    - api/tests/integration/test_documents_list_delete.py (kiểm tra có gọi service.delete trực tiếp không — nếu chỉ hit HTTP endpoint thì KHÔNG cần sửa signature call)
  </read_first>
  <action>
**Mục tiêu HUB-02 + E4 (BLOCKER 1):** Retrofit hub isolation vào path DELETE document Phase 4 — endpoint `DELETE /api/documents/:doc_id` (ROADMAP SC2 nêu đích danh). Hub registry CRUD (Plan 05-03) là admin-only nên KHÔNG verify được hub isolation ở đó (admin bypass theo thiết kế). Mutation cross-hub thực sự verify được là document DELETE bởi **editor** — editor có role mutate nhưng bị giới hạn theo `hub_assignments`. Đây là endpoint editor-eligible, hub-scoped để E4/SC2 HTTP-verifiable.

**Sửa `api/app/services/documents_service.py` `delete()` — task auto SỞ HỮU enforcement design (BLOCKER 2):**
- Đổi signature: `async def delete(self, document_id: UUID, *, actor: User, actor_hub_ids: list[str], request_id: str | None = None) -> bool` (thay `deleted_by: UUID`).
- Import thêm: `from app.models.auth import User`, `from app.repositories.hub_isolation import HubIsolationError, verify_hub_access`, `from app.services.audit_service import AuditEntry, enqueue_audit`.
- Sau SELECT `hub_id, file_path FROM documents WHERE id=:id` (dòng ~318-326 — `hub_id` resource đọc TỪ DB, KHÔNG từ payload), TRƯỚC `DELETE FROM documents` — chèn khối enforcement. **File `documents_service.py` SỞ HỮU audit emit `security.hub_isolation_violation`** (service layer, KHÔNG phải main.py handler):
```python
try:
    verify_hub_access(
        role=actor.role,
        user_hub_ids=actor_hub_ids,
        resource_hub_id=str(hub_id),
    )
except HubIsolationError:
    # Audit emit SỞ HỮU ở service layer — tại điểm reject, TRƯỚC khi raise.
    # main.py exception_handler CHỈ render 403 envelope, KHÔNG emit audit.
    enqueue_audit(AuditEntry(
        action="security.hub_isolation_violation",
        user_id=str(actor.id),
        target_type="document",
        target_id=str(document_id),
        hub_id=str(hub_id),
        payload={"attempted_op": "delete", "resource_hub_id": str(hub_id), "actor_role": actor.role},
        request_id=request_id,
    ))
    raise  # re-raise — router bắt → 403 (hoặc main.py handler fallback)
```
- INSERT audit_logs `document.delete` (dòng 336-349 — đã có) — đổi param `deleted_by` → `actor.id`. GIỮ INSERT synchronous (KHÔNG đổi sang queue — preserve behavior Plan 04-05 đã test).
- `verify_hub_access` với `role="admin"` → pass (admin cross-hub DELETE — T-04-05-03 preserved).

**Sửa `api/app/routers/documents.py` DELETE endpoint** (`delete_by_id`) — đổi RBAC từ admin-only sang editor-eligible:
- Đổi auth: `user: User = Depends(require_role("admin"))` → `principal: UserWithHubs = Depends(get_current_user_with_hubs)`. (Endpoint giờ chấp nhận admin + editor — viewer reject riêng.)
- Thêm: viewer reject — `if principal.user.role == "viewer": return resp.forbidden(message="Viewer không được xoá document", code="FORBIDDEN")`.
- Gọi `service.delete(doc_uuid, actor=principal.user, actor_hub_ids=principal.hub_ids, request_id=request_id)`.
- Wrap call trong `try/except HubIsolationError` → `return resp.forbidden(message=str(e), code="FORBIDDEN")` (bắt tại router — rõ ràng, không phụ thuộc fallback handler).
- Import `from app.auth.dependencies import get_current_user_with_hubs, UserWithHubs` + `from app.repositories.hub_isolation import HubIsolationError`.

**Sửa `test_documents_list_delete.py` nếu cần**: kiểm tra test có gọi `service.delete(...)` trực tiếp không.
- Nếu test CHỈ hit HTTP `DELETE /api/documents/:id` (qua `auth_client.delete`) → KHÔNG cần sửa (signature service nội bộ).
- Nếu có call `service.delete(deleted_by=...)` trực tiếp → cập nhật sang `actor=` + `actor_hub_ids=`.
- Test Plan 04-05 `test_delete_happy_path_cascade` dùng `admin_token` hit endpoint — admin role → `verify_hub_access` pass → KHÔNG regress. `test_delete_viewer_403` — viewer → endpoint reject viewer trước (vẫn 403). Verify cả 2 vẫn PASS sau đổi.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/documents_service.py app/routers/documents.py tests/integration/test_documents_list_delete.py && uv run mypy --strict app/services/documents_service.py app/routers/documents.py && uv run pytest tests/integration/test_documents_list_delete.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "verify_hub_access" api/app/services/documents_service.py` match (trong delete())
    - `grep "security.hub_isolation_violation" api/app/services/documents_service.py` match — `enqueue_audit` được gọi TRƯỚC `raise` tại điểm reject (audit emit SỞ HỮU ở service layer, KHÔNG ở main.py handler)
    - `documents_service.delete` signature có param `actor` + `actor_hub_ids`
    - `documents.py` DELETE endpoint dùng `get_current_user_with_hubs` (KHÔNG còn `require_role("admin")` cho endpoint này — editor-eligible); reject viewer; bắt `HubIsolationError` → `resp.forbidden`
    - `pytest tests/integration/test_documents_list_delete.py` exit 0 (test Plan 04-05 KHÔNG regress)
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>documents_service.delete + DELETE endpoint enforce verify_hub_access; reject cross-hub → audit security.hub_isolation_violation (emit ở service layer) + 403; endpoint editor-eligible; admin bypass; Plan 04-05 test không regress.</done>
</task>

<task type="auto">
  <name>Task 3: test_hub_isolation.py (E4 MANDATORY) + test_rate_limit.py</name>
  <files>api/tests/integration/test_hub_isolation.py, api/tests/integration/test_rate_limit.py</files>
  <read_first>
    - api/tests/integration/test_documents_list_delete.py (analog — _create_hub, _upload, mock_cocoindex_app fixture, envelope assert)
    - api/tests/integration/conftest.py (fixture: app_with_auth, auth_client, admin_token, editor_token, viewer_token, _insert_user)
    - api/tests/integration/test_rbac_dependency.py (analog test-only route mount pattern — _spawn_rbac_app helper)
    - api/app/routers/documents.py (DELETE endpoint sau Task 2)
    - api/app/routers/audit_logs.py (Plan 05-05 — GET /api/audit-logs đã decorate @limiter.limit)
    - api/app/repositories/hub_isolation.py (Plan 05-02 — verify_hub_access logic)
    - api/app/auth/api_key.py (Task 1 — require_api_key)
    - api/app/config.py (Plan 05-01 — rate_limit_audit_logs_per_minute; có thể override env để test ngưỡng thấp)
  </read_first>
  <action>
**`api/tests/integration/test_hub_isolation.py`** — E4 EXIT criteria suite. MỌI test marker `@pytest.mark.critical` + `@pytest.mark.integration` + `@pytest.mark.asyncio` (CI gate `pytest -m critical` HARD-03). KHÔNG `pytest.skip`, KHÔNG `assert True # TODO` (CONVENTIONS §1 DON'T).

Reuse fixture conftest. Helper cần (copy/adapt từ test_documents_list_delete.py):
- `_create_hub(app_with_auth, slug)` — INSERT hub với cột sau Plan 05-01: `INSERT INTO hubs (id, slug, code, name, subdomain, status, is_active, created_at) VALUES (:id, :slug, :slug, 'h', :slug, 'active', TRUE, NOW())`. Return hub_id (UUID).
- `_assign_user_hub(user_id, hub_id)` — `INSERT INTO user_hubs (user_id, hub_id, assigned_at) VALUES (:uid, :hid, NOW())`.
- `_upload(client, token, hub_id, filename, content)` — copy POST /api/documents/upload helper. `mock_cocoindex_app` fixture copy từ test_documents_list_delete.py (mock app.state.cocoindex_app).

Setup mỗi test: tạo `hub_a` + `hub_b`; `editor_user` (conftest) assigned CHỈ `hub_a` qua `_assign_user_hub`; document upload vào `hub_b` qua admin.

Test cases (≥6):
1. `test_editor_hub_a_cannot_delete_doc_hub_b` — editor (assigned hub_a) `DELETE /api/documents/:doc_hub_b` → **403** `error.code=="FORBIDDEN"`; sau đó `SELECT COUNT(*) FROM documents WHERE id=:doc_hub_b` = 1 (document Hub B VẪN tồn tại — không bị xoá).
2. `test_hub_isolation_violation_audit_logged` — sau test reject (gọi DELETE bị 403) → `SELECT COUNT(*) FROM audit_logs WHERE action='security.hub_isolation_violation'` ≥ 1; row mới nhất có `user_id` = editor.id + `target_type='document'`. (LƯU Ý audit là async batch flush — test cần `await` một khoảng / gọi `flush_pending` hoặc poll tới khi entry xuất hiện; dùng pattern poll-with-timeout, KHÔNG sleep cứng.)
3. `test_admin_can_delete_doc_any_hub` — admin `DELETE /api/documents/:doc_hub_b` → **204** (admin bypass cross-hub — T-04-05-03).
4. `test_editor_can_delete_doc_own_hub` — editor (assigned hub_a) upload doc vào hub_a → `DELETE /api/documents/:doc_hub_a` → **204** (editor xoá document hub MÌNH OK).
5. `test_viewer_cannot_delete_any_doc` — viewer `DELETE /api/documents/:doc` → **403** FORBIDDEN (viewer read-only — reject trước verify_hub_access).
6. `test_verify_hub_access_unit` — gọi trực tiếp `verify_hub_access(role="editor", user_hub_ids=[str(hub_a)], resource_hub_id=str(hub_b))` → raise `HubIsolationError`; `verify_hub_access(role="admin", user_hub_ids=[], resource_hub_id=str(hub_b))` → KHÔNG raise (admin bypass).

Mỗi test docstring ghi liên kết E4 — "hub isolation bug = STOP, security review (PROJECT.md E4)".

**`api/tests/integration/test_rate_limit.py`** — `@pytest.mark.integration` + `@pytest.mark.asyncio`:

W4 — Phase 5 đã có endpoint THẬT rate-limited: `GET /api/audit-logs` (Plan 05-05 decorate `@limiter.limit`). Test hit endpoint thật này — KHÔNG dùng test-only route nữa.
- Set ngưỡng thấp để test nhanh: override env `RATE_LIMIT_AUDIT_LOGS_PER_MINUTE=3` (hoặc monkeypatch `get_settings`) TRƯỚC khi build app (`get_settings.cache_clear()`); rebuild `create_app()` để decorator đọc limit mới. Nếu khó override sau khi decorator đã bind → dùng helper spawn app với settings patched (pattern test_rbac_dependency.py `_spawn_*`).
1. `test_audit_logs_rate_limit_429_envelope` — admin token gọi `GET /api/audit-logs` 4 lần liên tiếp (ngưỡng 3/phút) → request thứ 4 trả **429**, body `{success:false, data:null, error:{code:"RATE_LIMIT_EXCEEDED", message:...}, meta:null}` — assert đủ 4 key envelope + `error.code`.
2. `test_audit_logs_under_limit_passes` — admin gọi `GET /api/audit-logs` 2 lần (dưới ngưỡng 3) → cả 2 trả 200.
3. `test_x_api_key_invalid_rejected` (`@pytest.mark.critical`) — test-only route `/test/apikey` decorate `Depends(require_api_key)` (mount qua test-only app helper `_spawn_*`): gửi `X-API-Key: bad-key` → **401** `error.code=="API_KEY_INVALID"`; gửi không header → 401 `API_KEY_MISSING`. (Verify dependency require_api_key — Task 1.)

Envelope assertion: `body=r.json(); assert set(body.keys())=={"success","data","error","meta"}; assert body["error"]["code"]=="..."`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py && uv run pytest tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py -x -q && uv run pytest -m critical -q</automated>
  </verify>
  <acceptance_criteria>
    - `api/tests/integration/test_hub_isolation.py` có ≥6 test, MỌI test marker `@pytest.mark.critical`
    - KHÔNG có `pytest.skip` hay `assert True  # TODO` trong test_hub_isolation.py (E4 KHÔNG skip)
    - `grep "security.hub_isolation_violation" tests/integration/test_hub_isolation.py` match
    - Có test admin bypass (204) + editor own-hub (204) + editor cross-hub reject (403) + audit logged
    - `test_rate_limit.py` có ≥3 test — hit endpoint THẬT `GET /api/audit-logs`: 429 envelope shape + under-limit pass + X-API-Key invalid 401
    - `pytest tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py` exit 0
    - `pytest -m critical` exit 0 (HARD-03 CI gate — toàn bộ critical Phase 3/4/5 PASS, không regress)
  </acceptance_criteria>
  <done>test_hub_isolation.py ≥6 critical PASS (E4 — cross-hub 403 + audit + admin bypass + editor own-hub); test_rate_limit.py 429 envelope trên GET /api/audit-logs thật + X-API-Key 401.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → DELETE /api/documents/:id | Editor mutation — hub_id resource đọc từ DB, KHÔNG từ client (E4 trust boundary chính) |
| X-API-Key header | External integration auth — key verify qua AES-GCM decrypt + match |
| client → GET /api/audit-logs | Spam request — slowapi @limiter.limit chặn theo user_id (JWT sub) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Elevation of Privilege | Editor Hub A xoá document Hub B (DATA LEAK cross-tenant — EXIT criteria E4) | mitigate | `verify_hub_access` trong `documents_service.delete` — so `resource_hub_id` (load từ DB row) với `actor_hub_ids` (từ user_hubs); reject → 403. test_hub_isolation.py ≥6 critical test mandatory; CI gate `pytest -m critical`. E4 — test fail = STOP |
| T-05-06-02 | Tampering | Client truyền explicit `hub_id` để bypass isolation | mitigate | `documents_service.delete` lấy `hub_id` từ `SELECT ... FROM documents WHERE id=:id` — KHÔNG từ payload; DELETE endpoint không nhận body hub_id |
| T-05-06-03 | Repudiation | Tấn công cross-hub không để lại dấu vết | mitigate | Reject → `enqueue_audit('security.hub_isolation_violation')` emit Ở SERVICE LAYER (documents_service.delete) tại điểm reject TRƯỚC raise, với `user_id` + `target_id` + `request_id`; test_hub_isolation_violation_audit_logged verify |
| T-05-06-04 | Spoofing | X-API-Key giả mạo truy cập API | mitigate | `require_api_key` verify qua `ApiKeyService.verify_key` — decrypt AES-GCM + match; key revoked (is_active=FALSE) reject; key sai → 401 API_KEY_INVALID; test_x_api_key_invalid_rejected (`@pytest.mark.critical`) |
| T-05-06-05 | Denial of Service | Endpoint bị spam làm sập API | mitigate | slowapi limiter wired (`app.state.limiter` + exception_handler); `GET /api/audit-logs` decorate `@limiter.limit` (Plan 05-05); test_audit_logs_rate_limit_429_envelope verify 429 envelope shape trên endpoint THẬT |
| T-05-06-06 | Elevation of Privilege | viewer xoá document | mitigate | DELETE endpoint reject `role=="viewer"` → 403 trước khi vào service; test_viewer_cannot_delete_any_doc verify |
| T-05-06-07 | Information Disclosure | Admin bypass hub filter bị lạm dụng | accept | Admin cross-hub theo thiết kế quản trị (T-04-05-03 precedent); admin account compromise ngoài scope Phase 5 |
</threat_model>

<verification>
- `cd Hub_All/api && uv run ruff check app tests && uv run mypy --strict app` clean
- `uv run python -c "from app.main import create_app; create_app()"` không raise
- `uv run pytest tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py -q` — PASS
- `uv run pytest -m critical -q` — toàn bộ critical Phase 3/4/5 PASS (HARD-03 CI gate)
- `uv run pytest tests/ -q` — không regress test Phase 2/3/4
- `uv run alembic upgrade head` → `alembic downgrade base` clean (sau migration 0003)
</verification>

<success_criteria>
- main.py mount đủ 7 router (2 cũ + 5 Phase 5) + wire slowapi limiter + HubIsolationError handler + X-API-Key dependency
- HUB-02 enforce: editor cross-hub DELETE → 403 + audit security.hub_isolation_violation (emit ở service layer); admin bypass; endpoint DELETE document editor-eligible (E4/SC2 HTTP-verifiable)
- E4 EXIT criteria — test_hub_isolation.py ≥6 critical test PASS; `pytest -m critical` toàn bộ PASS
- Rate limit verify 429 envelope trên endpoint THẬT GET /api/audit-logs; X-API-Key auth verify 401
- Không regress test Phase 3/4
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-06-SUMMARY.md`

LƯU Ý cuối phase: nếu test_hub_isolation.py phát hiện hub isolation chưa enforce đúng → đây là EXIT criteria E4 trigger. KHÔNG đánh dấu Phase 5 complete cho tới khi E4 test PASS. Nếu không fix được trong 7 ngày → STOP, security review (PROJECT.md E4).
</output>
</content>
