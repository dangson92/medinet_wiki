---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - api/app/routers/__init__.py
  - api/app/main.py
  - api/tests/integration/conftest.py
  - api/tests/integration/test_hub_crud.py
  - api/tests/integration/test_hub_isolation.py
  - api/tests/integration/test_user_crud.py
  - api/tests/integration/test_audit_logger.py
  - api/tests/integration/test_rate_limit.py
autonomous: true
requirements: [HUB-01, HUB-02, HUB-03, USER-01, USER-02, USER-03, AUX-01, AUX-02, AUX-03]
user_setup: []

must_haves:
  truths:
    - "5 router Phase 5 (hubs/users/profile/audit-logs/api-keys) mount vào app — endpoint reachable"
    - "slowapi rate limit wired — exception handler trả 429 envelope"
    - "Editor Hub A không thể PUT/PATCH resource Hub B → 403 + audit security.hub_isolation_violation (E4)"
    - "Viewer/editor không gọi được endpoint admin-only → 403"
    - "Audit logger 100 concurrent action → audit_logs đủ 100 row, không block request thread"
    - "pytest -m critical PASS — CI gate HARD-03"
  artifacts:
    - path: "api/tests/integration/test_hub_isolation.py"
      provides: "EXIT criteria E4 — hub isolation test mỗi mutation endpoint"
      contains: "security.hub_isolation_violation"
    - path: "api/app/main.py"
      provides: "5 router mounted + slowapi wired"
      contains: "hubs_router"
    - path: "api/app/routers/__init__.py"
      provides: "export 5 router Phase 5"
      exports: ["hubs_router", "users_router", "profile_router", "audit_logs_router", "api_keys_router"]
  key_links:
    - from: "api/app/main.py"
      to: "5 router Phase 5"
      via: "include_router"
      pattern: "include_router"
    - from: "api/tests/integration/test_hub_isolation.py"
      to: "audit_logs table"
      via: "verify entry security.hub_isolation_violation"
      pattern: "hub_isolation_violation"
---

<objective>
Hoàn tất Phase 5: (1) wiring 5 router + slowapi vào `main.py`, (2) bộ integration test critical-path — đặc biệt **hub isolation EXIT criteria E4** (điều kiện ship M2).

**Wiring:**
- `routers/__init__.py` export 5 router mới (hubs/users/profile/audit_logs/api_keys).
- `main.py` `include_router` 5 router; wire slowapi (`app.state.limiter` + `add_exception_handler(RateLimitExceeded, ...)`).
- Mount endpoint hub-scoped với hub isolation enforce — Plan 05-03/04/05 router hiện admin-only; bổ sung enforce isolation cho mutation endpoint nếu cần (xem Task 2).

**Integration test (CONVENTIONS §1 mandatory):**
- `test_hub_crud.py` — hub CRUD happy path + pagination cap 100.
- `test_hub_isolation.py` — **E4 MANDATORY**: editor Hub A KHÔNG mutate resource Hub B kể cả explicit `hub_id` payload → 403 + audit entry `security.hub_isolation_violation`.
- `test_user_crud.py` — user CRUD + viewer không gọi admin endpoint → 403 + profile self-scoped.
- `test_audit_logger.py` — 100 concurrent action → audit_logs đủ 100 row.
- `test_rate_limit.py` — vượt limit → 429 envelope; auth/me KHÔNG limit.

Purpose: Khép Phase 5 — E4 hub isolation test fail = critical bug, KHÔNG ship M2. HARD-03 CI gate `pytest -m critical` PASS.
Output: wiring + 5 test file + conftest fixtures mở rộng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- main.py wiring — main.py:262-270 (include_router pattern), :251-260 (middleware order) -->
from app.routers import documents_router  # đã có
app.include_router(documents_router)
<!-- slowapi wire — Plan 05-02 output app/middleware/rate_limit.py: limiter, rate_limit_exceeded_handler -->
from slowapi.errors import RateLimitExceeded
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)

<!-- routers __init__.py hiện tại — chỉ export documents_router -->

<!-- Hub isolation — Plan 05-02 output -->
from app.repositories.hub_isolation import verify_hub_access, HubIsolationError, hub_filter_clause
from app.auth.dependencies import get_current_user_with_hubs, UserWithHubs
<!-- enqueue_audit — Plan 05-01 -->
from app.services.audit_service import enqueue_audit, AuditEntry  # action "security.hub_isolation_violation"

<!-- Test fixture — conftest.py có: postgres_container, redis_container, app_with_auth, auth_client,
     admin_user/editor_user/viewer_user, admin_token/editor_token/viewer_token, _insert_user helper.
     app_with_auth TRUNCATE users/refresh_tokens/user_hubs CASCADE per-test (KHÔNG truncate hubs/documents/audit_logs).
     Test Phase 5 cần thêm TRUNCATE hubs/audit_logs/api_keys + fixture editor có hub_assignment cụ thể. -->

<!-- Test analog — test_documents_list_delete.py + test_rbac_dependency.py -->
@pytest.mark.critical + @pytest.mark.integration + @pytest.mark.asyncio
httpx AsyncClient; assert body["success"], body["data"], body["error"]["code"], body["meta"]["total"]

<!-- Hub isolation test pattern — PATTERNS.md / CONVENTIONS §1 -->
resp = await client.patch(f"/api/hubs/{hub_b_id}/status",
    headers={"Authorization": f"Bearer {editor_hub_a_token}"},
    json={"status": "inactive"})  # editor Hub A → hub B → 403
assert resp.status_code == 403
assert resp.json()["error"]["code"] == "FORBIDDEN"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring — routers/__init__.py + main.py (5 router + slowapi + hub isolation handler)</name>
  <files>api/app/routers/__init__.py, api/app/main.py</files>
  <read_first>
    - api/app/routers/__init__.py (hiện chỉ export documents_router)
    - api/app/main.py (dòng 262-270 include_router; 251-260 middleware; 280-304 HTTPException handler)
    - api/app/middleware/rate_limit.py (Plan 05-02 — limiter, rate_limit_exceeded_handler)
    - api/app/repositories/hub_isolation.py (Plan 05-02 — HubIsolationError)
  </read_first>
  <action>
**`api/app/routers/__init__.py`** — thêm import + export 5 router:
```python
from app.routers.documents import router as documents_router
from app.routers.hubs import router as hubs_router
from app.routers.users import router as users_router
from app.routers.profile import router as profile_router
from app.routers.audit_logs import router as audit_logs_router
from app.routers.api_keys import router as api_keys_router

__all__ = ["documents_router", "hubs_router", "users_router",
           "profile_router", "audit_logs_router", "api_keys_router"]
```

**`api/app/main.py`** `create_app()`:
- Sau `app.include_router(documents_router)` (dòng ~270), thêm:
```python
from app.routers import (
    hubs_router, users_router, profile_router,
    audit_logs_router, api_keys_router,
)
app.include_router(hubs_router)
app.include_router(users_router)
app.include_router(profile_router)
app.include_router(audit_logs_router)
app.include_router(api_keys_router)
```
- Wire slowapi (AUX-03) — sau include_router:
```python
# Rate limit (Phase 5 AUX-03) — slowapi Limiter + envelope 429 handler.
from slowapi.errors import RateLimitExceeded
from app.middleware import limiter, rate_limit_exceeded_handler
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)
```
- Thêm `@app.exception_handler(HubIsolationError)` (sau `http_exception_handler`):
```python
from app.repositories.hub_isolation import HubIsolationError

@app.exception_handler(HubIsolationError)
async def hub_isolation_handler(
    request: FastAPIRequest, exc: HubIsolationError
) -> StarletteJSONResponse:
    """HubIsolationError → 403 envelope (HUB-02 / E4)."""
    body = {
        "success": False, "data": None,
        "error": {"code": "FORBIDDEN", "message": str(exc)},
        "meta": None,
    }
    return StarletteJSONResponse(content=body, status_code=403)
```
- Cập nhật comment middleware order: ghi rõ "[Phase 5 AUX-03] rate_limit — slowapi dùng Limiter + exception_handler (KHÔNG add_middleware BaseHTTPMiddleware); enabled".
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/main.py app/routers/__init__.py && uv run mypy --strict app/main.py app/routers/__init__.py && uv run python -c "from app.main import create_app; app=create_app(); paths={r.path for r in app.routes}; assert '/api/hubs' in paths and '/api/users' in paths and '/api/profile' in paths and '/api/audit-logs' in paths and '/api/api-keys' in paths, sorted(paths)"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/routers/__init__.py` `__all__` chứa 6 router (documents + 5 mới)
    - `create_app()` mount đủ 5 prefix: `/api/hubs`, `/api/users`, `/api/profile`, `/api/audit-logs`, `/api/api-keys`
    - `grep "RateLimitExceeded" api/app/main.py` có match (slowapi exception handler wired)
    - `grep "HubIsolationError" api/app/main.py` có match (403 envelope handler)
    - `app.state.limiter` được set
    - `ruff check` + `mypy --strict` exit 0; route smoke test exit 0
  </acceptance_criteria>
  <done>5 router mount vào app; slowapi + HubIsolationError handler wired; mọi endpoint reachable.</done>
</task>

<task type="auto">
  <name>Task 2: conftest.py fixtures + test_hub_crud.py + test_user_crud.py</name>
  <files>api/tests/integration/conftest.py, api/tests/integration/test_hub_crud.py, api/tests/integration/test_user_crud.py</files>
  <read_first>
    - api/tests/integration/conftest.py (fixtures hiện có — postgres_container, app_with_auth, admin_token, _insert_user)
    - api/tests/integration/test_documents_list_delete.py (analog integration test — INSERT helper, envelope assert)
    - api/tests/integration/test_rbac_dependency.py (analog 403 assert)
    - api/app/routers/hubs.py, api/app/routers/users.py, api/app/routers/profile.py (endpoint shape)
  </read_first>
  <action>
**`api/tests/integration/conftest.py`** — mở rộng (APPEND, KHÔNG sửa fixture cũ):
- Trong fixture `app_with_auth`, mở rộng câu `TRUNCATE` thêm bảng Phase 5: `TRUNCATE TABLE users, refresh_tokens, user_hubs, hubs, audit_logs, api_keys, documents, chunks RESTART IDENTITY CASCADE` (đảm bảo test isolation per-test cho mọi bảng Phase 5 đụng). Nếu sửa câu TRUNCATE phá test cũ — kiểm tra: test Phase 2/3/4 vẫn TRUNCATE đủ; thêm bảng KHÔNG hại (RESTART IDENTITY CASCADE an toàn).
- Thêm helper async `_insert_hub(*, name, code, subdomain) -> str`: INSERT hubs (id uuid4, slug=code, name, code, subdomain, status='active', is_active=TRUE, created_at/updated_at NOW()) qua `get_engine()`; return hub_id string.
- Thêm helper async `_assign_user_hub(*, user_id, hub_id) -> None`: INSERT user_hubs (user_id, hub_id, assigned_at NOW()).
- Thêm fixture `editor_hub_a` — INSERT editor user + 1 hub A + assign; trả dict `{user_id, hub_a_id, token}`. Token: login qua auth_client (login issue JWT với hub_ids claim — nếu JWT không carry hub_ids, get_current_user_with_hubs query user_hubs nên vẫn đúng).
- Thêm fixture `hub_b` — INSERT 1 hub B (editor A KHÔNG được assign).

**`api/tests/integration/test_hub_crud.py`** — `@pytest.mark.integration @pytest.mark.asyncio` (1-2 test `@pytest.mark.critical`):
- `test_admin_create_and_get_hub`: admin POST `/api/hubs` `{name,code,subdomain}` → 201, body.data.id; GET `/api/hubs/{id}` → 200 khớp; assert response data KHÔNG có key `chroma_collection`/`db_host` (D-05).
- `test_list_hubs_pagination_cap`: tạo vài hub; GET `/api/hubs?per_page=500` → meta.per_page == 100 (cap).
- `test_hub_stats`: tạo hub → GET `/api/hubs/{id}/stats` → data có document_count/chunk_count/user_count (0 khi rỗng).
- `test_viewer_cannot_create_hub` `@pytest.mark.critical`: viewer POST `/api/hubs` → 403 `error.code == "FORBIDDEN"`.

**`api/tests/integration/test_user_crud.py`** — `@pytest.mark.integration @pytest.mark.asyncio`:
- `test_admin_create_user_with_hub`: cần 1 hub trước (fixture). admin POST `/api/users` `{email,name,password,hub_id,role}` → 201; body.data.user.email khớp; body.data.roles có entry hub_id.
- `test_viewer_cannot_list_users` `@pytest.mark.critical`: viewer GET `/api/users` → 403 FORBIDDEN.
- `test_profile_self_scoped`: editor GET `/api/profile` → 200 trả CHÍNH editor; PUT `/api/profile` `{name:"New"}` → 200; verify GET trả name mới.
- `test_change_password_wrong_old`: editor POST `/api/profile/password` `{old_password:"wrong",new_password:"NewPass123"}` → 400 `error.code == "INVALID_PASSWORD"`.
- Envelope assert: `body["success"]`, `body["data"]`, `body["error"]["code"]`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/conftest.py tests/integration/test_hub_crud.py tests/integration/test_user_crud.py && uv run pytest tests/integration/test_hub_crud.py tests/integration/test_user_crud.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` có helper `_insert_hub`, `_assign_user_hub` + fixture `editor_hub_a`, `hub_b`
    - `conftest.py` câu TRUNCATE bao gồm `hubs`, `audit_logs`, `api_keys`
    - `test_hub_crud.py` có test create/get, pagination cap 100, stats, viewer 403
    - `test_hub_crud.py` assert response hub KHÔNG có `chroma_collection`/`db_host` (D-05)
    - `test_user_crud.py` có test create user, viewer 403, profile self-scoped, wrong old password 400
    - `pytest tests/integration/test_hub_crud.py tests/integration/test_user_crud.py` exit 0 (tất cả PASS)
  </acceptance_criteria>
  <done>conftest fixtures hub/editor; test_hub_crud + test_user_crud PASS — CRUD happy + RBAC 403 + D-05 verify.</done>
</task>

<task type="auto">
  <name>Task 3: test_hub_isolation.py (E4 MANDATORY) + test_audit_logger.py + test_rate_limit.py</name>
  <files>api/tests/integration/test_hub_isolation.py, api/tests/integration/test_audit_logger.py, api/tests/integration/test_rate_limit.py</files>
  <read_first>
    - api/tests/integration/conftest.py (Task 2 — editor_hub_a, hub_b fixture)
    - api/tests/integration/test_rbac_dependency.py (analog 403 assert)
    - api/tests/integration/test_auth_refresh_race.py (analog concurrent asyncio.gather pattern)
    - api/app/repositories/hub_isolation.py (verify_hub_access — hành vi)
    - api/app/services/audit_service.py (enqueue_audit, AUDIT_ACTIONS, audit_flush_loop)
    - api/app/routers/hubs.py, api/app/routers/users.py (mutation endpoint)
    - Hub_All/.planning/CONVENTIONS.md (§1 DO block — hub isolation test)
  </read_first>
  <action>
**`api/tests/integration/test_hub_isolation.py`** — EXIT criteria E4, mọi test `@pytest.mark.critical @pytest.mark.integration @pytest.mark.asyncio`:

> LƯU Ý cho executor: hub CRUD endpoint (Plan 05-03) hiện admin-only — editor KHÔNG có quyền hub mutation bất kể hub. Hub isolation E4 verify chính xác trên **resource hub-scoped mà editor CÓ quyền role nhưng KHÔNG quyền hub** — endpoint phù hợp nhất là document mutation hoặc user-trong-hub. Test phải kiểm tra: editor được assign Hub A, thao tác resource thuộc Hub B → 403 + audit. Nếu Plan 05-03/04/05 chưa có endpoint editor-eligible hub-scoped, test verify ở 2 lớp: (a) helper `verify_hub_access` raise HubIsolationError cho cross-hub; (b) endpoint mutation hub-scoped trả 403. Executor đọc router thực tế để chọn endpoint editor-có-role; nếu mọi mutation đều admin-only thì E4 test verify qua document delete endpoint (Phase 4 documents router — editor role) HOẶC test trực tiếp `verify_hub_access` + audit enqueue. KHÔNG skip — E4 mandatory.

- `test_verify_hub_access_rejects_cross_hub`: gọi `verify_hub_access(role="editor", user_hub_ids=[hub_a], resource_hub_id=hub_b)` → raise `HubIsolationError`; cùng input role="admin" → KHÔNG raise.
- `test_editor_cannot_mutate_resource_other_hub`: editor Hub A gửi mutation request tới resource Hub B với payload chứa explicit `hub_id` = hub_a (cố ép) → 403 `error.code == "FORBIDDEN"`. Endpoint cụ thể: executor chọn endpoint hub-scoped editor-eligible (xem lưu ý trên).
- `test_hub_isolation_violation_logged`: sau khi mutation cross-hub bị reject → query `audit_logs WHERE action='security.hub_isolation_violation'` có ≥ 1 row với `user_id` = editor. (Nếu reject ở dependency-layer trước khi enqueue — executor đảm bảo handler `HubIsolationError` HOẶC service layer enqueue audit trước khi raise; cập nhật service/router để enqueue `security.hub_isolation_violation` khi verify_hub_access fail.)
- `test_admin_bypass_hub_filter`: admin truy resource bất kỳ hub → 200 (admin cross-hub theo thiết kế).

> Executor: nếu cần, bổ sung enqueue audit `security.hub_isolation_violation` vào điểm reject (service layer Plan 05-03/04/05 hoặc `main.py` HubIsolationError handler) — đây là phần wiring hợp lệ của Plan 05-06.

**`api/tests/integration/test_audit_logger.py`** — `@pytest.mark.integration @pytest.mark.asyncio` (1 test `@pytest.mark.critical`):
- `test_audit_batch_flush_100_entries`: enqueue 100 `AuditEntry` qua `enqueue_audit` (action hợp lệ, ví dụ `hub.create`); chờ `audit_flush_interval_seconds` + headroom (ví dụ `await asyncio.sleep(5)`); query `SELECT COUNT(*) FROM audit_logs` → == 100. Verify request_id distinct per entry (set request_id khác nhau).
- `test_enqueue_audit_non_blocking`: enqueue trong vòng lặp 100 lần đo thời gian — assert tổng thời gian enqueue < 100ms (non-blocking — put_nowait).

**`api/tests/integration/test_rate_limit.py`** — `@pytest.mark.integration @pytest.mark.asyncio`:
- `test_rate_limit_returns_429_envelope`: dùng 1 endpoint có decorate `@limiter.limit` (executor xác nhận endpoint nào; nếu Phase 5 chưa decorate endpoint nào — search/ask thuộc Phase 6 — thì test mức middleware: gọi endpoint có limit thấp tạm thời HOẶC verify `rate_limit_exceeded_handler` trả envelope shape qua unit-level. Ưu tiên: decorate `GET /api/audit-logs` hoặc `GET /api/users` với limit cao (ví dụ env override `rate_limit_search_per_minute` thấp cho test) → spam vượt → 429 `error.code == "RATE_LIMIT_EXCEEDED"`, body có keys `{success,data,error,meta}`).
- `test_auth_me_not_rate_limited`: spam `GET /api/auth/me` nhiều lần → KHÔNG có 429 (auth/me KHÔNG limit — AUX-03).

> Executor lưu ý: AUX-03 spec limit search/ask/upload — các endpoint đó thuộc Phase 6/4. Phase 5 verify cơ chế rate-limit hoạt động: chọn 1 endpoint Phase 5 (audit-logs/users) decorate `@limiter.limit` với limit từ settings để test có thể trigger 429. Decorate endpoint là phần wiring hợp lệ Plan 05-06. auth/me PHẢI giữ KHÔNG limit.

Mọi test dùng fixture conftest sẵn có; envelope assert `body["success"]`, `body["error"]["code"]`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/test_hub_isolation.py tests/integration/test_audit_logger.py tests/integration/test_rate_limit.py && uv run pytest tests/integration/test_hub_isolation.py tests/integration/test_audit_logger.py tests/integration/test_rate_limit.py -x -q && uv run pytest -m critical -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_hub_isolation.py` có ≥ 4 test, mọi test `@pytest.mark.critical`
    - `grep "security.hub_isolation_violation" tests/integration/test_hub_isolation.py` có match (E4 audit verify)
    - `grep "HubIsolationError" tests/integration/test_hub_isolation.py` có match
    - KHÔNG có `assert True  # TODO` hay `pytest.skip` trong test_hub_isolation.py (E4 KHÔNG skip — CONVENTIONS §1 DON'T)
    - `test_audit_logger.py` verify 100 entry → 100 row audit_logs
    - `test_rate_limit.py` verify 429 envelope + auth/me không limit
    - `pytest tests/integration/test_hub_isolation.py tests/integration/test_audit_logger.py tests/integration/test_rate_limit.py` exit 0
    - `pytest -m critical` exit 0 (HARD-03 CI gate — không regress test Phase 2/3/4)
  </acceptance_criteria>
  <done>test_hub_isolation E4 PASS (cross-hub 403 + audit); test_audit_logger 100-row; test_rate_limit 429 envelope; pytest -m critical PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor cross-hub mutation | Editor Hub A cố mutate resource Hub B — trust boundary chính của Phase 5 (E4) |
| client → rate-limited endpoint | Spam request — slowapi chặn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Elevation of Privilege | Editor Hub A mutate resource Hub B (data leak cross-tenant) — EXIT criteria E4 | mitigate | `verify_hub_access` reject cross-hub; `HubIsolationError` handler → 403 envelope; test_hub_isolation.py MANDATORY verify 403 + audit `security.hub_isolation_violation`; E4 — test fail = STOP, KHÔNG ship M2 |
| T-05-06-02 | Repudiation | Cross-hub mutation attempt không được ghi nhận | mitigate | reject path enqueue audit `security.hub_isolation_violation` với user_id + target — test_hub_isolation_violation_logged verify row tồn tại |
| T-05-06-03 | Denial of Service | Endpoint Phase 5 không có rate limit bị spam | mitigate | slowapi wired vào app; endpoint nhạy cảm decorate `@limiter.limit`; 429 envelope |
| T-05-06-04 | Tampering | Test isolation kém — data leak giữa test gây false PASS/FAIL | mitigate | conftest TRUNCATE hubs/users/audit_logs/api_keys RESTART IDENTITY CASCADE per-test |
| T-05-06-05 | Elevation of Privilege | auth/me bị rate-limit gây user lockout giả | accept→verify | test_auth_me_not_rate_limited xác nhận auth/me KHÔNG decorate limiter (AUX-03 spec) |
</threat_model>

<verification>
- `cd Hub_All/api && uv run ruff check app tests && uv run mypy --strict app` clean
- `uv run pytest tests/ -q` — toàn bộ test PASS, KHÔNG regress Phase 2/3/4
- `uv run pytest -m critical -q` — CI gate HARD-03 PASS
- `uv run python -c "from app.main import create_app; create_app()"` không raise
- E4 verify: `test_hub_isolation.py` PASS — editor cross-hub → 403 + audit entry
</verification>

<success_criteria>
- 5 router Phase 5 mount; slowapi + HubIsolationError handler wired
- test_hub_isolation.py PASS (E4 — cross-hub 403 + audit security.hub_isolation_violation), KHÔNG skip
- test_hub_crud / test_user_crud / test_audit_logger / test_rate_limit PASS
- `pytest -m critical` PASS — HARD-03 CI gate
- 9 REQ-ID Phase 5 verified qua test critical path
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-06-SUMMARY.md`

LƯU Ý cuối phase: nếu test_hub_isolation.py phát hiện hub isolation chưa enforce đúng → đây là EXIT criteria E4 trigger. KHÔNG đánh dấu Phase 5 complete cho tới khi E4 test PASS. Nếu không fix được trong 7 ngày → STOP, security review (PROJECT.md E4).
</output>
