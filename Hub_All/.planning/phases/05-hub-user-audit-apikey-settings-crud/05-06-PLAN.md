---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - api/app/routers/__init__.py
  - api/app/main.py
  - api/app/auth/api_key.py
  - api/app/auth/__init__.py
  - api/app/services/documents_service.py
  - api/app/routers/documents.py
  - api/tests/integration/conftest.py
  - api/tests/integration/test_documents_list_delete.py
  - api/tests/integration/test_hub_isolation.py
  - api/tests/integration/test_rate_limit.py
autonomous: true
requirements: [HUB-02, AUX-02, AUX-03]
user_setup: []

must_haves:
  truths:
    - "routers/__init__.py export 6 router; main.py mount đủ 7 router (auth + documents + 5 Phase 5)"
    - "main.py wire slowapi limiter (app.state.limiter + exception_handler RateLimitExceeded → envelope 429)"
    - "main.py wire HubIsolationError → 403 envelope handler"
    - "X-API-Key header auth — require_api_key verify key qua AES-GCM decrypt; key sai/revoked → 401"
    - "Editor Hub A KHÔNG thể DELETE document Hub B kể cả khi truyền explicit hub_id — 403"
    - "documents_service.delete() gọi verify_hub_access + enqueue_audit('security.hub_isolation_violation') tại điểm reject TRƯỚC khi raise"
    - "pytest -m critical PASS — CI gate HARD-03"
  artifacts:
    - path: "api/app/routers/__init__.py"
      provides: "export 6 router (documents + 5 Phase 5)"
      exports: ["hubs_router", "users_router", "profile_router", "audit_logs_router", "api_keys_router"]
    - path: "api/app/main.py"
      provides: "Wiring đủ router + rate limiter + HubIsolationError handler"
      contains: "hubs_router"
    - path: "api/app/auth/api_key.py"
      provides: "require_api_key dependency — X-API-Key external auth"
      exports: ["require_api_key"]
    - path: "api/app/services/documents_service.py"
      provides: "delete() enforce hub isolation + audit emit tại reject point (HUB-02 / E4)"
      contains: "security.hub_isolation_violation"
    - path: "api/tests/integration/test_hub_isolation.py"
      provides: "E4 EXIT criteria — hub isolation critical test suite"
      contains: "security.hub_isolation_violation"
  key_links:
    - from: "api/app/main.py"
      to: "hubs_router/users_router/profile_router/api_keys_router/audit_logs_router"
      via: "include_router trong create_app"
      pattern: "include_router"
    - from: "api/app/services/documents_service.py"
      to: "verify_hub_access + enqueue_audit"
      via: "delete() mutation enforce hub isolation + audit security.hub_isolation_violation"
      pattern: "verify_hub_access"
    - from: "api/app/main.py"
      to: "slowapi limiter"
      via: "app.state.limiter + add_exception_handler"
      pattern: "RateLimitExceeded"
---

<objective>
Wave 4 — khép Phase 5: wiring + hub isolation enforcement + E4 EXIT criteria test. 3 việc:

1. **Export router + mount + wire rate limiter + HubIsolationError handler + X-API-Key**:
   - `routers/__init__.py` export 5 router mới (Plan 05-03/04/05 tạo file `routers/hubs.py`...`routers/audit_logs.py` nhưng KHÔNG sửa `routers/__init__.py` — plan này export).
   - `main.py` `include_router` 5 router; wire slowapi (`app.state.limiter` + `add_exception_handler(RateLimitExceeded, ...)`); `@app.exception_handler(HubIsolationError)` → 403 envelope.
   - X-API-Key auth (AUX-02): tạo `app/auth/api_key.py` — dependency `require_api_key`.

2. **Hub isolation enforce ở document DELETE mutation** (HUB-02 — EXIT criteria E4): `documents_service.delete()` (Phase 4) retrofit gọi `verify_hub_access` (Plan 05-02) + emit audit `security.hub_isolation_violation` TẠI điểm reject TRƯỚC khi raise. Endpoint mục tiêu CHỐT: `DELETE /api/documents/:doc_id` (ROADMAP SC2 nêu đích danh — endpoint editor-eligible, hub-scoped). Editor Hub A KHÔNG DELETE được document Hub B; admin bypass.

3. **test_hub_isolation.py** + **test_rate_limit.py** — E4 critical test suite + rate-limit envelope verify.

NOTE: CRUD endpoint test (Hub/User/APIKey/Audit) ĐÃ thuộc Plan 05-03/04/05? — KHÔNG: Plan 05-03/04/05 CHỈ tạo router/service/schema, KHÔNG tạo test. CRUD integration test cũng thuộc plan này — nhưng để giữ plan ≤3 task + tránh phình, CRUD happy-path test gộp vào Task 3 cùng hub-isolation. E4 (hub isolation) là test critical bắt buộc; CRUD happy-path test mức smoke.

NOTE BLOCKER 1 (data contract): ApiKeyService method verify X-API-Key tên canonical = **`verify_key`** (Plan 05-05 là producer, khai báo + assert tên này). Plan này — `auth/api_key.py` `require_api_key` — gọi `ApiKeyService(db=db).verify_key(x_api_key)`. KHÔNG dùng `verify_plaintext` (tên cũ sai — đã sửa toàn bộ plan này theo BLOCKER 1).

Purpose: HUB-02 là điều kiện ship M2 (E4). Wave cuối — mọi router/service plan trước đã sẵn sàng.
Output: `routers/__init__.py`, `main.py`, `auth/api_key.py`, `documents_service.py` + `documents.py` hub-isolation, `conftest.py` fixtures, `test_hub_isolation.py`, `test_rate_limit.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- main.py — api/app/main.py:
  create_app() dòng 262-270: include_router(auth_router) + include_router(documents_router).
  http_exception_handler đã có (main.py:280-304). FastAPIRequest/StarletteJSONResponse import dòng 36-37.
  Middleware order CONVENTIONS §4 — KHÔNG đổi. -->

<!-- routers/__init__.py HIỆN TẠI chỉ export documents_router — Plan 05-03/04/05 KHÔNG sửa file này
     (files_modified của chúng chỉ liệt kê routers/hubs.py, routers/users.py, ... — KHÔNG __init__.py).
     → Plan 05-06 chịu trách nhiệm export 5 router mới. -->

<!-- Plan 05-02 output — api/app/middleware/rate_limit.py + middleware/__init__.py:
  limiter (slowapi Limiter), rate_limit_exceeded_handler (RateLimitExceeded → resp.too_many_requests).
  Export qua app/middleware/__init__.py — `from app.middleware import limiter, rate_limit_exceeded_handler`.
  slowapi wiring: app.state.limiter = limiter; app.add_exception_handler(RateLimitExceeded, handler).
  LƯU Ý: Phase 5 endpoint DUY NHẤT decorate @limiter.limit = GET /api/audit-logs (Plan 05-05 W4);
  search/ask Phase 6, upload Phase 4 — decoration defer.
  Test rate-limit dùng test-only route (KHÔNG sửa production main.py) — pattern test_rbac_dependency.py _spawn. -->

<!-- Plan 05-02 output — api/app/repositories/hub_isolation.py:
  verify_hub_access(*, role: str, user_hub_ids: list[str], resource_hub_id: str) -> None  — raise HubIsolationError
  HubIsolationError(message, *, resource_hub_id) — .resource_hub_id attribute
  get_current_user_with_hubs -> UserWithHubs (.user, .hub_ids) — auth/dependencies.py -->

<!-- Plan 05-01 output — api/app/services/audit_service.py:
  enqueue_audit(AuditEntry(action="security.hub_isolation_violation", ...)) — action trong AUDIT_ACTIONS
  flush_pending() — drain queue (test dùng để flush trước assert). -->

<!-- Plan 05-03/04/05 output — router file (KHÔNG export ở routers/__init__.py — plan này export):
  app/routers/hubs.py::router (/api/hubs), users.py::router (/api/users), profile.py::router (/api/profile),
  api_keys.py::router (/api/api-keys), audit_logs.py::router (/api/audit-logs). -->

<!-- Plan 05-05 output — api/app/services/api_key_service.py (DATA CONTRACT — BLOCKER 1):
  ApiKeyService(db=db).verify_key(plain_key: str) -> dict | None  — {id, permissions, allowed_hub_ids} nếu key hợp lệ.
  Tên method canonical = `verify_key` (KHÔNG `verify_plaintext`). Plan 05-05 là producer — khai báo + assert
  `hasattr(ApiKeyService, 'verify_key')`. auth/api_key.py + get_api_key_or_jwt đều gọi `verify_key`. -->

<!-- documents_service.delete HIỆN TẠI — documents_service.py:299-364:
  delete(document_id, *, deleted_by: UUID, request_id=None) -> bool
  SELECT hub_id, file_path FROM documents WHERE id=:id — hub_id resource đọc TỪ DB, KHÔNG payload.
  documents.py DELETE endpoint dòng 187-225 — hiện auth require_role("admin").
  test_documents_list_delete.py hit qua HTTP endpoint (KHÔNG gọi service.delete trực tiếp). -->

<!-- conftest.py — fixture có: postgres_container, redis_container, app_with_auth, auth_client,
     admin_user/editor_user/viewer_user, admin_token/editor_token/viewer_token, _insert_user helper.
     app_with_auth TRUNCATE users/refresh_tokens/user_hubs CASCADE per-test (dòng ~212-216) —
     KHÔNG truncate hubs/documents/audit_logs/api_keys → Plan 05-06 mở rộng. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routers/__init__.py export + main.py wire (5 router + slowapi + HubIsolationError) + auth/api_key.py</name>
  <files>api/app/routers/__init__.py, api/app/main.py, api/app/auth/api_key.py, api/app/auth/__init__.py</files>
  <read_first>
    - api/app/routers/__init__.py (hiện chỉ export documents_router)
    - api/app/main.py (create_app dòng 262-304 — include_router block, http_exception_handler; import dòng 36-37)
    - api/app/middleware/rate_limit.py + api/app/middleware/__init__.py (Plan 05-02 — limiter, rate_limit_exceeded_handler)
    - api/app/repositories/hub_isolation.py (Plan 05-02 — HubIsolationError)
    - api/app/services/api_key_service.py (Plan 05-05 — ApiKeyService.verify_key — tên method canonical, BLOCKER 1)
    - api/app/auth/dependencies.py + api/app/auth/__init__.py (pattern dependency + re-export __all__)
    - api/app/routers/hubs.py, users.py, profile.py, api_keys.py, audit_logs.py (Plan 05-03/04/05 — router export tên `router`)
  </read_first>
  <action>
**A) `api/app/routers/__init__.py`** — export 6 router (giữ documents_router):
```python
"""Routers package — APIRouter modular cho mỗi domain."""
from __future__ import annotations

from app.routers.api_keys import router as api_keys_router
from app.routers.audit_logs import router as audit_logs_router
from app.routers.documents import router as documents_router
from app.routers.hubs import router as hubs_router
from app.routers.profile import router as profile_router
from app.routers.users import router as users_router

__all__ = [
    "api_keys_router",
    "audit_logs_router",
    "documents_router",
    "hubs_router",
    "profile_router",
    "users_router",
]
```

**B) `api/app/main.py` `create_app()` — mount 5 router** — sau `app.include_router(documents_router)` (dòng ~270):
```python
# Mount Phase 5 router (HUB-01..03, USER-01..03, AUX-01..02).
from app.routers import (
    api_keys_router,
    audit_logs_router,
    hubs_router,
    profile_router,
    users_router,
)
app.include_router(hubs_router)
app.include_router(users_router)
app.include_router(profile_router)
app.include_router(api_keys_router)
app.include_router(audit_logs_router)
```

**C) Wire slowapi rate limiter** (AUX-03) — sau mount router:
```python
# Rate limiter (Phase 5 AUX-03 — slowapi). Plan 05-02 tạo module; wiring tại đây.
# Endpoint Phase 5 decorate @limiter.limit = GET /api/audit-logs (Plan 05-05 W4).
# search/ask 100/min Phase 6, upload 30/min Phase 4 — decoration defer.
# KHÔNG cần add_middleware — handler đủ render 429 envelope.
from slowapi.errors import RateLimitExceeded
from app.middleware import limiter, rate_limit_exceeded_handler
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)
```

**D) HubIsolationError → 403 envelope handler** — sau `http_exception_handler` (dòng ~304):
```python
from app.repositories.hub_isolation import HubIsolationError

@app.exception_handler(HubIsolationError)
async def hub_isolation_handler(
    request: FastAPIRequest, exc: HubIsolationError
) -> StarletteJSONResponse:
    """HubIsolationError → 403 envelope (HUB-02 / E4).

    Handler CHỈ render envelope 403. Audit 'security.hub_isolation_violation'
    do documents_service.delete() enqueue tại điểm reject (Task 2).
    """
    body = {
        "success": False,
        "data": None,
        "error": {"code": "FORBIDDEN", "message": str(exc)},
        "meta": None,
    }
    return StarletteJSONResponse(content=body, status_code=403)
```
(`FastAPIRequest`, `StarletteJSONResponse` đã import main.py:36-37.)

**E) X-API-Key auth** (AUX-02) — tạo `api/app/auth/api_key.py`:
```python
"""X-API-Key auth dependency — AUX-02 external integration.

Cho phép request mang header `X-API-Key: mdk_...` truy cập API thay vì Bearer JWT.
Key verify qua ApiKeyService.verify_key (decrypt AES-GCM + match key_prefix).
Endpoint external (Phase 6/7 search/ask) opt-in Depends(require_api_key).
"""
from __future__ import annotations

from fastapi import Depends, Header, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.session import get_session
from app.services.api_key_service import ApiKeyService


async def require_api_key(
    x_api_key: str | None = Header(default=None, alias="X-API-Key"),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> dict:
    """Verify X-API-Key header → principal dict {id, permissions, allowed_hub_ids}.

    Raise 401 API_KEY_MISSING nếu thiếu header; API_KEY_INVALID nếu key sai/revoked.
    """
    if not x_api_key:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "API_KEY_MISSING", "message": "Thiếu header X-API-Key"},
        )
    principal = await ApiKeyService(db=db).verify_key(x_api_key)
    if principal is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail={"code": "API_KEY_INVALID", "message": "API key không hợp lệ hoặc đã thu hồi"},
        )
    return principal
```
**BLOCKER 1 — tên method:** `require_api_key` PHẢI gọi `ApiKeyService(db=db).verify_key(x_api_key)` — tên canonical `verify_key` (Plan 05-05 producer). KHÔNG dùng `verify_plaintext` (tên sai — sẽ gây AttributeError runtime, test_x_api_key_invalid_rejected fail).

Cập nhật `api/app/auth/__init__.py`: thêm `from app.auth.api_key import require_api_key` + `"require_api_key"` vào `__all__`.

KHÔNG đổi middleware add order (CONVENTIONS §4).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/routers/__init__.py app/main.py app/auth/api_key.py app/auth/__init__.py && uv run mypy --strict app/routers/__init__.py app/main.py app/auth/api_key.py && uv run python -c "from app.main import create_app; app=create_app(); paths={r.path for r in app.routes}; assert {'/api/hubs','/api/users','/api/profile','/api/api-keys','/api/audit-logs'} <= paths, sorted(paths); assert getattr(app.state,'limiter',None) is not None; from app.auth import require_api_key" && uv run python -c "import inspect; from app.auth.api_key import require_api_key; src=inspect.getsource(require_api_key); assert 'verify_key' in src and 'verify_plaintext' not in src, 'BLOCKER 1: require_api_key phải gọi verify_key'"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/routers/__init__.py` `__all__` chứa 6 router (documents + hubs/users/profile/api_keys/audit_logs)
    - `create_app()` mount đủ route `/api/hubs`, `/api/users`, `/api/profile`, `/api/api-keys`, `/api/audit-logs`
    - `grep "app.state.limiter" api/app/main.py` match; `grep "RateLimitExceeded" api/app/main.py` match
    - `grep "HubIsolationError" api/app/main.py` match (exception_handler đăng ký)
    - `api/app/auth/api_key.py` tồn tại, export `require_api_key`; `app/auth/__init__.py` `__all__` chứa `require_api_key`
    - **BLOCKER 1:** `grep "verify_key" api/app/auth/api_key.py` có match; `grep "verify_plaintext" api/app/auth/api_key.py` KHÔNG có match
    - `ruff check` + `mypy --strict` exit 0; smoke test exit 0
  </acceptance_criteria>
  <done>routers/__init__.py export 6 router; main.py mount 5 router Phase 5 + slowapi limiter + HubIsolationError handler; auth/api_key.py require_api_key dependency gọi verify_key (BLOCKER 1).</done>
</task>

<task type="auto">
  <name>Task 2: Hub isolation enforce + audit emit ở document DELETE mutation (HUB-02 / E4)</name>
  <files>api/app/services/documents_service.py, api/app/routers/documents.py, api/tests/integration/test_documents_list_delete.py</files>
  <read_first>
    - api/app/services/documents_service.py (delete() dòng 299-364 — SELECT hub_id đã có sẵn; điểm chèn enforcement)
    - api/app/routers/documents.py (DELETE endpoint dòng 187-225 — hiện require_role("admin"))
    - api/app/repositories/hub_isolation.py (Plan 05-02 — verify_hub_access, HubIsolationError)
    - api/app/auth/dependencies.py (Plan 05-02 — get_current_user_with_hubs; UserWithHubs .user .hub_ids)
    - api/app/services/audit_service.py (Plan 05-01 — enqueue_audit, AuditEntry — action security.hub_isolation_violation trong AUDIT_ACTIONS)
    - api/tests/integration/test_documents_list_delete.py (kiểm tra có gọi service.delete trực tiếp không — nếu chỉ hit HTTP thì KHÔNG sửa signature call)
  </read_first>
  <action>
**Mục tiêu HUB-02 + E4:** Retrofit hub isolation vào path DELETE document Phase 4 — endpoint `DELETE /api/documents/:doc_id` (ROADMAP SC2). Hub registry CRUD (Plan 05-03) admin-only nên KHÔNG verify được hub isolation ở đó. Mutation cross-hub verify được là document DELETE bởi **editor** — editor có role mutate nhưng giới hạn theo `hub_assignments`.

**Sửa `api/app/services/documents_service.py` `delete()`:**
- Đổi signature: `async def delete(self, document_id: UUID, *, actor: User, actor_hub_ids: list[str], request_id: str | None = None) -> bool` (thay `deleted_by: UUID`).
- Import thêm: `from app.models.auth import User`, `from app.repositories.hub_isolation import HubIsolationError, verify_hub_access`, `from app.services.audit_service import AuditEntry, enqueue_audit`.
- Sau SELECT `hub_id, file_path FROM documents WHERE id=:id` (dòng ~318-326 — `hub_id` resource đọc TỪ DB, KHÔNG từ payload), TRƯỚC `DELETE FROM documents` — chèn khối enforcement. **Audit emit SỞ HỮU ở service layer** (KHÔNG ở main.py handler):
```python
try:
    verify_hub_access(
        role=actor.role,
        user_hub_ids=actor_hub_ids,
        resource_hub_id=str(hub_id),
    )
except HubIsolationError:
    enqueue_audit(AuditEntry(
        action="security.hub_isolation_violation",
        user_id=str(actor.id),
        target_type="document",
        target_id=str(document_id),
        hub_id=str(hub_id),
        payload={"attempted_op": "delete", "resource_hub_id": str(hub_id), "actor_role": actor.role},
        request_id=request_id,
    ))
    raise  # re-raise — router bắt → 403
```
- INSERT audit_logs `document.delete` (dòng 336-349 — đã có) — đổi param `deleted_by` → `actor.id`. GIỮ INSERT synchronous (preserve behavior Plan 04-05).
- `verify_hub_access` với `role="admin"` → pass (admin cross-hub DELETE — T-04-05-03 preserved).

**Sửa `api/app/routers/documents.py` DELETE endpoint** (`delete_by_id`):
- Đổi auth: `user: User = Depends(require_role("admin"))` → `principal: UserWithHubs = Depends(get_current_user_with_hubs)`. (Endpoint giờ chấp nhận admin + editor — viewer reject riêng.)
- Thêm: viewer reject — `if principal.user.role == "viewer": return resp.forbidden(message="Viewer không được xoá document", code="FORBIDDEN")`.
- Gọi `service.delete(doc_uuid, actor=principal.user, actor_hub_ids=principal.hub_ids, request_id=request_id)`.
- Wrap call trong `try/except HubIsolationError` → `return resp.forbidden(message=str(e), code="FORBIDDEN")`.
- Import `from app.auth.dependencies import get_current_user_with_hubs, UserWithHubs` + `from app.repositories.hub_isolation import HubIsolationError`.

**Sửa `test_documents_list_delete.py` nếu cần**: kiểm tra test có gọi `service.delete(...)` trực tiếp không.
- Nếu test CHỈ hit HTTP `DELETE /api/documents/:id` (qua `auth_client.delete`) → KHÔNG cần sửa signature.
- Nếu có call `service.delete(deleted_by=...)` trực tiếp → cập nhật sang `actor=` + `actor_hub_ids=`.
- Test Plan 04-05 `test_delete_happy_path_cascade` dùng `admin_token` → admin role → `verify_hub_access` pass → KHÔNG regress. `test_delete_viewer_403` — viewer → endpoint reject viewer trước (vẫn 403). Verify cả 2 PASS sau đổi.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/documents_service.py app/routers/documents.py tests/integration/test_documents_list_delete.py && uv run mypy --strict app/services/documents_service.py app/routers/documents.py && uv run pytest tests/integration/test_documents_list_delete.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "verify_hub_access" api/app/services/documents_service.py` match (trong delete())
    - `grep "security.hub_isolation_violation" api/app/services/documents_service.py` match — `enqueue_audit` gọi TRƯỚC `raise` tại điểm reject
    - `documents_service.delete` signature có param `actor` + `actor_hub_ids`
    - `documents.py` DELETE endpoint dùng `get_current_user_with_hubs` (KHÔNG còn `require_role("admin")` cho endpoint này); reject viewer; bắt `HubIsolationError` → `resp.forbidden`
    - `pytest tests/integration/test_documents_list_delete.py` exit 0 (test Plan 04-05 KHÔNG regress)
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>documents_service.delete + DELETE endpoint enforce verify_hub_access; reject cross-hub → audit security.hub_isolation_violation (service layer) + 403; endpoint editor-eligible; admin bypass; Plan 04-05 test không regress.</done>
</task>

<task type="auto">
  <name>Task 3: conftest fixtures + test_hub_isolation.py (E4 MANDATORY) + test_rate_limit.py</name>
  <files>api/tests/integration/conftest.py, api/tests/integration/test_hub_isolation.py, api/tests/integration/test_rate_limit.py</files>
  <read_first>
    - api/tests/integration/conftest.py (fixture hiện có; câu TRUNCATE app_with_auth dòng ~210-217)
    - api/tests/integration/test_documents_list_delete.py (analog — _create_hub dòng 53-68, _upload, mock_cocoindex_app fixture, envelope assert dòng 287-290)
    - api/tests/integration/test_rbac_dependency.py (analog — _spawn test-only app pattern, 403 assert)
    - api/app/routers/documents.py (DELETE endpoint sau Task 2)
    - api/app/repositories/hub_isolation.py (Plan 05-02 — verify_hub_access logic)
    - api/app/services/audit_service.py (Plan 05-01 — enqueue_audit, flush_pending, AuditEntry)
    - api/app/auth/api_key.py (Task 1 — require_api_key — gọi verify_key)
    - api/app/middleware/rate_limit.py (Plan 05-02 — limiter, rate_limit_exceeded_handler)
  </read_first>
  <action>
**`api/tests/integration/conftest.py`** — mở rộng (APPEND helper/fixture, sửa câu TRUNCATE):
- Mở rộng câu `TRUNCATE` trong `app_with_auth` (dòng ~212-216) thêm bảng Phase 5: `TRUNCATE TABLE users, refresh_tokens, user_hubs, hubs, audit_logs, api_keys, documents, chunks RESTART IDENTITY CASCADE`. (RESTART IDENTITY CASCADE an toàn — test Phase 2/3/4 vẫn truncate đủ.)
- Thêm helper async `_insert_hub(*, name: str, code: str, subdomain: str) -> str`: INSERT hubs qua `get_engine()` — cột sau Plan 05-01 migration 0003: `INSERT INTO hubs (id, slug, code, name, subdomain, description, status, is_active, created_at, updated_at) VALUES (:id, :slug, :code, :name, :subdomain, NULL, 'active', TRUE, NOW(), NOW())` với `id=uuid4()`, `slug=code`. Return hub_id string.
- Thêm helper async `_assign_user_hub(*, user_id: str, hub_id: str) -> None`: `INSERT INTO user_hubs (user_id, hub_id, assigned_at) VALUES (:uid, :hid, NOW())`.

**`api/tests/integration/test_hub_isolation.py`** — E4 EXIT criteria suite. MỌI test marker `@pytest.mark.critical` + `@pytest.mark.integration` + `@pytest.mark.asyncio` (CI gate `pytest -m critical` HARD-03). KHÔNG `pytest.skip`, KHÔNG `assert True # TODO` (CONVENTIONS §1 DON'T).

Reuse fixture conftest. Helper `_upload(client, token, hub_id, filename, content)` copy từ test_documents_list_delete.py; `mock_cocoindex_app` fixture copy (mock app.state.cocoindex_app). Dùng `_insert_hub`/`_assign_user_hub` từ conftest.

Setup mỗi test: tạo `hub_a` + `hub_b`; `editor_user` (conftest) assigned CHỈ `hub_a` qua `_assign_user_hub`; document upload vào `hub_b` qua admin.

Test cases (≥6):
1. `test_editor_hub_a_cannot_delete_doc_hub_b` — editor (assigned hub_a) `DELETE /api/documents/:doc_hub_b` → **403** `error.code=="FORBIDDEN"`; `SELECT COUNT(*) FROM documents WHERE id=:doc_hub_b` = 1 (document Hub B VẪN tồn tại).
2. `test_hub_isolation_violation_audit_logged` — sau test reject → audit là async batch flush: gọi `flush_pending()` HOẶC poll-with-timeout (KHÔNG sleep cứng) tới khi `SELECT COUNT(*) FROM audit_logs WHERE action='security.hub_isolation_violation'` ≥ 1; row mới nhất có `user_id` = editor.id + `target_type='document'`.
3. `test_admin_can_delete_doc_any_hub` — admin `DELETE /api/documents/:doc_hub_b` → **204** (admin bypass cross-hub — T-04-05-03).
4. `test_editor_can_delete_doc_own_hub` — editor (assigned hub_a) upload doc vào hub_a → `DELETE /api/documents/:doc_hub_a` → **204** (editor xoá document hub MÌNH OK).
5. `test_viewer_cannot_delete_any_doc` — viewer `DELETE /api/documents/:doc` → **403** FORBIDDEN (viewer read-only — reject trước verify_hub_access).
6. `test_verify_hub_access_unit` — gọi trực tiếp `verify_hub_access(role="editor", user_hub_ids=[str(hub_a)], resource_hub_id=str(hub_b))` → raise `HubIsolationError`; `verify_hub_access(role="admin", user_hub_ids=[], resource_hub_id=str(hub_b))` → KHÔNG raise (admin bypass).

Mỗi test docstring ghi liên kết E4 — "hub isolation bug = STOP, security review (PROJECT.md E4)".

**`api/tests/integration/test_rate_limit.py`** — `@pytest.mark.integration` + `@pytest.mark.asyncio`:

Phase 5 endpoint production decorate @limiter.limit = GET /api/audit-logs (Plan 05-05). Test verify CƠ CHẾ rate-limit qua test-only route — pattern `_spawn` test-only (test_rbac_dependency.py): helper build FastAPI app, mount route `/test/limited` decorate `@limiter.limit("3/minute")`, wire `app.state.limiter` + `add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)`. KHÔNG sửa production `main.py`. (Test-only route giữ deterministic — limit nhỏ 3/min; verify GET /api/audit-logs production decoration đã có ở Plan 05-05 acceptance grep.)
1. `test_rate_limit_returns_429_envelope` — gọi `/test/limited` 4 lần → request thứ 4 trả **429**, body `{success:false, data:null, error:{code:"RATE_LIMIT_EXCEEDED", message:...}, meta:null}` — assert đủ 4 key envelope.
2. `test_under_limit_passes` — gọi `/test/limited` 2 lần (dưới ngưỡng 3) → cả 2 trả 200.
3. `test_x_api_key_invalid_rejected` (`@pytest.mark.critical`) — test-only route `/test/apikey` decorate `Depends(require_api_key)` (mount qua test-only app helper): gửi `X-API-Key: bad-key` → **401** `error.code=="API_KEY_INVALID"`; gửi không header → 401 `API_KEY_MISSING`. (Verify dependency `require_api_key` — Task 1 — gọi `ApiKeyService.verify_key`. BLOCKER 1: test này fail nếu method tên sai → AttributeError.)
4. `test_auth_me_not_rate_limited` — spam `GET /api/auth/me` qua auth_client với admin_token nhiều lần → KHÔNG 429 (auth/me KHÔNG decorate limiter — AUX-03).

Envelope assertion: `body=r.json(); assert set(body.keys())=={"success","data","error","meta"}; assert body["error"]["code"]=="..."`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check tests/integration/conftest.py tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py && uv run pytest tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py -x -q && uv run pytest -m critical -q</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` có helper `_insert_hub`, `_assign_user_hub`; câu TRUNCATE bao gồm `hubs`, `audit_logs`, `api_keys`, `documents`, `chunks`
    - `api/tests/integration/test_hub_isolation.py` có ≥6 test, MỌI test marker `@pytest.mark.critical`
    - KHÔNG có `pytest.skip` hay `assert True  # TODO` trong test_hub_isolation.py (E4 KHÔNG skip)
    - `grep "security.hub_isolation_violation" tests/integration/test_hub_isolation.py` match
    - Có test editor cross-hub reject (403 + document vẫn tồn tại) + audit logged + admin bypass (204) + editor own-hub (204)
    - `test_rate_limit.py` có ≥4 test — 429 envelope shape + under-limit pass + X-API-Key invalid 401 + auth/me không limit
    - `test_x_api_key_invalid_rejected` PASS — chứng minh `require_api_key` gọi đúng `verify_key` (BLOCKER 1 — method tên sai → AttributeError → test fail)
    - `pytest tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py` exit 0
    - `pytest -m critical` exit 0 (HARD-03 CI gate — toàn bộ critical Phase 3/4/5 PASS, không regress)
  </acceptance_criteria>
  <done>conftest helper hub; test_hub_isolation.py ≥6 critical PASS (E4); test_rate_limit.py 429 envelope + X-API-Key 401 + auth/me không limit; pytest -m critical PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → DELETE /api/documents/:id | Editor mutation — hub_id resource đọc từ DB, KHÔNG từ client (E4 trust boundary chính) |
| X-API-Key header | External integration auth — key verify qua AES-GCM decrypt + match |
| client → rate-limited endpoint | Spam request — slowapi limiter chặn theo user_id |
| test isolation | Data leak giữa test gây false PASS/FAIL — TRUNCATE per-test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Elevation of Privilege | Editor Hub A xoá document Hub B (DATA LEAK cross-tenant — EXIT criteria E4) | mitigate | `verify_hub_access` trong `documents_service.delete` — so `resource_hub_id` (load từ DB row) với `actor_hub_ids` (từ user_hubs); reject → 403. test_hub_isolation.py ≥6 critical test mandatory; CI gate `pytest -m critical`. E4 — test fail = STOP |
| T-05-06-02 | Tampering | Client truyền explicit `hub_id` để bypass isolation | mitigate | `documents_service.delete` lấy `hub_id` từ `SELECT ... FROM documents WHERE id=:id` — KHÔNG từ payload; DELETE endpoint không nhận body hub_id |
| T-05-06-03 | Repudiation | Tấn công cross-hub không để lại dấu vết | mitigate | Reject → `enqueue_audit('security.hub_isolation_violation')` emit Ở SERVICE LAYER tại điểm reject TRƯỚC raise, với `user_id` + `target_id` + `request_id`; test_hub_isolation_violation_audit_logged verify |
| T-05-06-04 | Spoofing | X-API-Key giả mạo truy cập API | mitigate | `require_api_key` verify qua `ApiKeyService.verify_key` (BLOCKER 1 — tên method canonical) — decrypt AES-GCM + match; key revoked (is_active=FALSE) reject; key sai → 401; test_x_api_key_invalid_rejected (`@pytest.mark.critical`) |
| T-05-06-05 | Denial of Service | Endpoint bị spam làm sập API | mitigate | slowapi limiter wired (`app.state.limiter` + exception_handler); test_rate_limit_returns_429_envelope verify 429 envelope shape |
| T-05-06-06 | Elevation of Privilege | viewer xoá document | mitigate | DELETE endpoint reject `role=="viewer"` → 403 trước khi vào service; test_viewer_cannot_delete_any_doc verify |
| T-05-06-07 | Information Disclosure | Admin bypass hub filter bị lạm dụng | accept | Admin cross-hub theo thiết kế quản trị (T-04-05-03 precedent); admin account compromise ngoài scope Phase 5 |
</threat_model>

<verification>
- `cd Hub_All/api && uv run ruff check app tests && uv run mypy --strict app` clean
- `uv run python -c "from app.main import create_app; create_app()"` không raise
- `uv run pytest tests/integration/test_hub_isolation.py tests/integration/test_rate_limit.py -q` — PASS
- `uv run pytest -m critical -q` — toàn bộ critical Phase 3/4/5 PASS (HARD-03 CI gate)
- `uv run pytest tests/ -q` — không regress test Phase 2/3/4
- `uv run alembic upgrade head` → `uv run alembic downgrade base` clean (sau migration 0003)
- E4 verify: `test_hub_isolation.py` PASS — editor cross-hub → 403 + audit entry
- BLOCKER 1 verify: `grep verify_plaintext api/app/auth/api_key.py` KHÔNG match; require_api_key gọi `verify_key`
</verification>

<success_criteria>
- routers/__init__.py export 6 router; main.py mount đủ 7 router + wire slowapi limiter + HubIsolationError handler + X-API-Key dependency
- HUB-02 enforce: editor cross-hub DELETE → 403 + audit security.hub_isolation_violation (emit ở service layer); admin bypass; endpoint DELETE document editor-eligible
- E4 EXIT criteria — test_hub_isolation.py ≥6 critical test PASS; `pytest -m critical` toàn bộ PASS
- Rate limit verify 429 envelope; X-API-Key auth verify 401 (require_api_key gọi verify_key — BLOCKER 1 resolved)
- Không regress test Phase 3/4
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-06-SUMMARY.md`

LƯU Ý cuối phase: nếu test_hub_isolation.py phát hiện hub isolation chưa enforce đúng → đây là EXIT criteria E4 trigger. KHÔNG đánh dấu Phase 5 complete cho tới khi E4 test PASS. Nếu không fix được trong 7 ngày → STOP, security review (PROJECT.md E4).
</output>
</content>
