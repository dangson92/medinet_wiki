---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - api/app/schemas/hubs.py
  - api/app/services/hub_service.py
  - api/app/routers/hubs.py
autonomous: true
requirements: [HUB-01, HUB-03]
user_setup: []

must_haves:
  truths:
    - "Admin tạo hub mới qua POST /api/hubs → 201"
    - "GET /api/hubs trả list phân trang cap per_page ≤ 100"
    - "PUT /api/hubs/:id cập nhật hub (PUT không PATCH — D-07)"
    - "PATCH /api/hubs/:id/status đổi status active/inactive"
    - "GET /api/hubs/:id/stats trả counts documents/chunks/users từ Postgres aggregate"
    - "HubResponse KHÔNG có field chroma_collection/db_host/db_port/db_name/db_user (D-05)"
  artifacts:
    - path: "api/app/routers/hubs.py"
      provides: "Hub CRUD router — GET/POST list, GET/PUT/PATCH-status/stats by id"
      exports: ["router"]
    - path: "api/app/services/hub_service.py"
      provides: "HubService — create/get/list/update/update_status/stats"
      exports: ["HubService"]
    - path: "api/app/schemas/hubs.py"
      provides: "Pydantic v2 schemas CreateHubRequest/UpdateHubRequest/HubResponse/HubStats"
  key_links:
    - from: "api/app/routers/hubs.py"
      to: "HubService"
      via: "Depends(get_hub_service)"
      pattern: "HubService"
    - from: "api/app/services/hub_service.py"
      to: "audit_service.enqueue_audit"
      via: "hub.create / hub.update audit entry"
      pattern: "enqueue_audit"
---

<objective>
Giao Hub Registry CRUD (HUB-01) + hub stats (HUB-03) — router + service + schema theo layered architecture. Endpoint contract lấy từ `frontend/src/services/api.ts` (D-07 — frontend thắng REQUIREMENTS.md).

7 endpoint (theo D-07 + D-05 + D-06):
- `GET /api/hubs` — list, admin-only, pagination cap per_page ≤ 100
- `GET /api/hubs/:id` — get single, admin-only
- `POST /api/hubs` — create, admin-only → 201
- `PUT /api/hubs/:id` — update (PUT, KHÔNG PATCH — D-07), admin-only
- `PATCH /api/hubs/:id/status` — đổi status, admin-only
- `GET /api/hubs/:id/stats` — counts từ Postgres aggregate (HUB-03), admin-only
- `POST /api/hubs/:id/test-connection` — KHÔNG implement (D-06 — M2 dùng 1 Postgres chung)

D-05: `HubResponse` CHỈ trả `id, name, code, subdomain, description, status, created_at, updated_at`. KHÔNG `chroma_collection`, `db_host`, `db_port`, `db_name`, `db_user` (di sản Go multi-DB — drop hẳn).

Purpose: HUB-01 + HUB-03 — admin quản lý hub registry. Phụ thuộc Plan 05-01 (cột schema code/subdomain/status) + Plan 05-02 (enqueue_audit có sẵn).
Output: `schemas/hubs.py`, `services/hub_service.py`, `routers/hubs.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- Router analog — api/app/routers/documents.py (exact CRUD router) -->
APIRouter(prefix="/api/hubs", tags=["hubs"])
def get_hub_service(db: AsyncSession = Depends(get_session)) -> HubService: ...
admin-only: user: User = Depends(require_role("admin"))  # noqa: B008
return resp.created(data=...) / resp.ok / resp.paginated(items, page, per_page, total)
UUID validate: try: UUID(hub_id) except ValueError: return resp.bad_request(code="INVALID_HUB_ID")
not_found: obj is None -> resp.not_found(code="NOT_FOUND")

<!-- Service analog — api/app/services/documents_service.py -->
class HubService: def __init__(self, db: AsyncSession) -> None: self.db = db
INSERT/SELECT raw SQL qua text() parametrized; timestamp = SQL NOW() server-side
get_session() auto-commit khi handler không raise — service KHÔNG tự commit

<!-- D-07 contract — frontend api.ts -->
HubAPI fields (api.ts:392): id, name, code, subdomain, description?, status, created_at, updated_at
  (api.ts còn list db_host?/db_port/db_name?/db_user?/chroma_collection — D-05 OVERRIDE: Python KHÔNG trả các field này)
CreateHubRequest (api.ts:408): name, code, subdomain, description? (db_*/chroma_collection bỏ qua per D-05)
UpdateHubRequest (api.ts:421): name?, description? (db_* bỏ qua per D-05)
updateHubStatus body: { status: string }

<!-- HUB-03 stats aggregate SQL — PATTERNS.md -->
SELECT
  (SELECT COUNT(*) FROM documents WHERE hub_id = :hub_id) AS document_count,
  (SELECT COUNT(*) FROM chunks    WHERE hub_id = :hub_id) AS chunk_count,
  (SELECT COUNT(*) FROM user_hubs WHERE hub_id = :hub_id) AS user_count

<!-- audit — Plan 05-02 / 05-01 output -->
from app.services.audit_service import enqueue_audit, AuditEntry
enqueue_audit(AuditEntry(action="hub.create", user_id=..., target_type="hub", target_id=..., hub_id=..., payload=..., request_id=...))

<!-- hubs table cột sau Plan 05-01 migration 0003: id, slug, name, code, subdomain, description, status, is_active, created_at, updated_at.
     slug NOT NULL unique (Phase 2 baseline) — service.create phải set slug = code (lowercase) nếu không có giá trị riêng. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: schemas/hubs.py — Pydantic v2 schemas</name>
  <files>api/app/schemas/hubs.py</files>
  <read_first>
    - api/app/schemas/documents.py (analog Pydantic v2 — Literal enum, UUID-to-str, datetime)
    - Hub_All/frontend/src/services/api.ts (HubAPI ~392, CreateHubRequest ~408, UpdateHubRequest ~421)
    - Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md (D-05 — field drop list)
  </read_first>
  <action>
Tạo `api/app/schemas/hubs.py` (`from __future__ import annotations` đầu file):
- `HubStatus = Literal["active", "inactive"]`.
- `class CreateHubRequest(BaseModel)`: `name: str = Field(min_length=1, max_length=200)`, `code: str = Field(min_length=1, max_length=50)`, `subdomain: str = Field(min_length=1, max_length=100)`, `description: str | None = None`. (D-05 — KHÔNG `chroma_collection`/`db_*`.)
- `class UpdateHubRequest(BaseModel)`: `name: str | None = Field(default=None, max_length=200)`, `description: str | None = None`. (D-07 — PUT update chỉ name/description.)
- `class UpdateHubStatusRequest(BaseModel)`: `status: HubStatus`.
- `class HubResponse(BaseModel)`: `id: str`, `name: str`, `code: str`, `subdomain: str`, `description: str | None`, `status: HubStatus`, `created_at: datetime`, `updated_at: datetime | None`. (D-05 — CHỈ 8 field này.)
- `class HubStats(BaseModel)`: `hub_id: str`, `document_count: int`, `chunk_count: int`, `user_count: int`.
Import `datetime` từ `datetime`, `Literal` từ `typing`, `BaseModel`/`Field` từ `pydantic`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/schemas/hubs.py && uv run mypy --strict app/schemas/hubs.py && uv run python -c "from app.schemas.hubs import HubResponse; f=set(HubResponse.model_fields); assert not (f and {'chroma_collection','db_host','db_port'} and f & {'chroma_collection','db_host','db_port'}), f"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/schemas/hubs.py` tồn tại với 5 class: CreateHubRequest, UpdateHubRequest, UpdateHubStatusRequest, HubResponse, HubStats
    - `HubResponse.model_fields` KHÔNG chứa `chroma_collection`, `db_host`, `db_port`, `db_name`, `db_user` (D-05)
    - `HubResponse.model_fields` chứa đúng: id, name, code, subdomain, description, status, created_at, updated_at
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>schemas/hubs.py có 5 schema; HubResponse KHÔNG có field di sản Go.</done>
</task>

<task type="auto">
  <name>Task 2: services/hub_service.py — HubService CRUD + stats</name>
  <files>api/app/services/hub_service.py</files>
  <read_first>
    - api/app/services/documents_service.py (analog CRUD raw SQL — create/get/list pattern, dòng 71-297)
    - api/app/services/audit_service.py (Plan 05-01 output — enqueue_audit, AuditEntry)
    - api/app/schemas/hubs.py (Task 1 output)
    - api/app/models/hub.py (cột bảng hubs sau migration 0003)
  </read_first>
  <action>
Tạo `api/app/services/hub_service.py`:
- `from __future__ import annotations`, import `logging`, `Any`, `UUID`, `uuid4`, `text`, `AsyncSession`, schemas hubs, `enqueue_audit`/`AuditEntry`.
- `class HubService`: `__init__(self, db: AsyncSession) -> None: self.db = db`.
- `async def create(self, *, req: CreateHubRequest, created_by: UUID, request_id: str | None = None) -> HubResponse`:
  - `hub_id = uuid4()`. `slug` cột Phase 2 NOT NULL unique → set `slug = req.code.lower()`.
  - INSERT: `text("INSERT INTO hubs (id, slug, name, code, subdomain, description, status, is_active, created_at, updated_at) VALUES (:id, :slug, :name, :code, :subdomain, :desc, 'active', TRUE, NOW(), NOW())")`.
  - Bắt `IntegrityError` (unique code/slug) → raise `HubConflictError` (định nghĩa class trong file: `class HubConflictError(Exception)`).
  - Sau INSERT thành công: `enqueue_audit(AuditEntry(action="hub.create", user_id=str(created_by), target_type="hub", target_id=str(hub_id), hub_id=str(hub_id), payload={"code": req.code, "name": req.name}, request_id=request_id))`.
  - Return `HubResponse` build từ giá trị vừa INSERT (SELECT lại row để lấy created_at/updated_at, hoặc INSERT ... RETURNING — dùng RETURNING created_at, updated_at cho gọn).
- `async def get(self, hub_id: UUID) -> HubResponse | None`: `SELECT id, name, code, subdomain, description, status, created_at, updated_at FROM hubs WHERE id = :id` → fetchone → map HubResponse hoặc None.
- `async def list(self, *, page: int, per_page: int) -> tuple[list[HubResponse], int]`: `SELECT COUNT(*) FROM hubs` cho total; `SELECT ... FROM hubs ORDER BY created_at DESC LIMIT :limit OFFSET :offset`. (KHÔNG cap per_page ở service — router cap.)
- `async def update(self, *, hub_id: UUID, req: UpdateHubRequest, updated_by: UUID, request_id: str | None = None) -> HubResponse | None`:
  - Build SET clause động chỉ cho field không None (`name`, `description`); nếu cả 2 None → chỉ update `updated_at=NOW()`.
  - `UPDATE hubs SET ... updated_at=NOW() WHERE id = :id RETURNING ...`; nếu 0 row → return None.
  - `enqueue_audit(AuditEntry(action="hub.update", user_id=str(updated_by), target_type="hub", target_id=str(hub_id), hub_id=str(hub_id), payload={"changed": {k: v for k, v in req.model_dump(exclude_none=True).items()}}, request_id=request_id))`.
- `async def update_status(self, *, hub_id: UUID, status: str, updated_by: UUID, request_id: str | None = None) -> bool`:
  - `UPDATE hubs SET status = :status, updated_at=NOW() WHERE id = :id`; return `result.rowcount > 0`.
  - `enqueue_audit(AuditEntry(action="hub.update", ..., payload={"status": status}, ...))`.
- `async def stats(self, hub_id: UUID) -> HubStats | None`:
  - Trước tiên verify hub tồn tại: `SELECT 1 FROM hubs WHERE id = :id`; None → return None.
  - Aggregate query (xem interfaces): 3 subquery COUNT documents/chunks/user_hubs WHERE hub_id.
  - Return `HubStats(hub_id=str(hub_id), document_count=..., chunk_count=..., user_count=...)`.
- Timestamp luôn SQL `NOW()` — KHÔNG `datetime.utcnow()`.
- `logger = logging.getLogger(__name__)`; `IntegrityError` import `from sqlalchemy.exc import IntegrityError`.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/services/hub_service.py && uv run mypy --strict app/services/hub_service.py && uv run python -c "from app.services.hub_service import HubService, HubConflictError"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/services/hub_service.py` export `HubService`, `HubConflictError`
    - `HubService` có method: create, get, list, update, update_status, stats
    - `grep "enqueue_audit" api/app/services/hub_service.py` có ≥ 2 match (hub.create + hub.update)
    - `grep "NOW()" api/app/services/hub_service.py` có match; KHÔNG có `datetime.utcnow`
    - stats dùng aggregate query trên `documents`, `chunks`, `user_hubs` (grep cả 3 table)
    - `ruff check` + `mypy --strict` exit 0
  </acceptance_criteria>
  <done>HubService có 6 method CRUD+stats, raw SQL parametrized, audit non-blocking, stats từ Postgres aggregate.</done>
</task>

<task type="auto">
  <name>Task 3: routers/hubs.py — Hub CRUD router</name>
  <files>api/app/routers/hubs.py</files>
  <read_first>
    - api/app/routers/documents.py (analog router — get_X_service, require_role, UUID validate, resp.* helpers)
    - api/app/services/hub_service.py (Task 2 output)
    - api/app/schemas/hubs.py (Task 1 output)
    - api/app/pkg/response.py (resp.created/ok/paginated/bad_request/not_found/conflict)
    - Hub_All/frontend/src/services/api.ts (D-07 — verb/path: getHubs/getHub/createHub PUT updateHub PATCH status getHubStats)
  </read_first>
  <action>
Tạo `api/app/routers/hubs.py`:
- Imports như `documents.py:26-54` — `APIRouter`, `Depends`, `Request`, `status`, `JSONResponse`, `AsyncSession`, `require_role`, `get_session`, `User`, `resp`.
- `router = APIRouter(prefix="/api/hubs", tags=["hubs"])`.
- `def get_hub_service(db: AsyncSession = Depends(get_session)) -> HubService: return HubService(db=db)  # noqa: B008`.
- `@router.get("")` `list_hubs(page: int = 1, per_page: int = 20, user = Depends(require_role("admin")), service = Depends(get_hub_service))`:
  - `capped_per_page = max(1, min(per_page, 100))`, `capped_page = max(1, page)`.
  - `items, total = await service.list(page=capped_page, per_page=capped_per_page)`.
  - `return resp.paginated(items=[i.model_dump(mode="json") for i in items], page=capped_page, per_page=capped_per_page, total=total)`.
- `@router.post("")` `create_hub(req: CreateHubRequest, request: Request, user = Depends(require_role("admin")), service = Depends(get_hub_service))`:
  - `request_id = getattr(request.state, "request_id", None)`.
  - `try: result = await service.create(req=req, created_by=user.id, request_id=request_id)` `except HubConflictError as e: return resp.conflict(message=str(e), code="HUB_CODE_CONFLICT")`.
  - `return resp.created(data=result.model_dump(mode="json"))`.
- `@router.get("/{hub_id}")` `get_hub(hub_id: str, user = Depends(require_role("admin")), service = ...)`:
  - UUID validate → `resp.bad_request(code="INVALID_HUB_ID")`. `obj = await service.get(hub_uuid)`; None → `resp.not_found(code="NOT_FOUND")`. `return resp.ok(data=obj.model_dump(mode="json"))`.
- `@router.put("/{hub_id}")` `update_hub(hub_id: str, req: UpdateHubRequest, request: Request, user = Depends(require_role("admin")), service = ...)`:
  - UUID validate. `result = await service.update(hub_id=hub_uuid, req=req, updated_by=user.id, request_id=request_id)`; None → `resp.not_found(code="NOT_FOUND")`. `return resp.ok(data=result.model_dump(mode="json"))`.
- `@router.patch("/{hub_id}/status")` `update_hub_status(hub_id: str, req: UpdateHubStatusRequest, request: Request, user = Depends(require_role("admin")), service = ...)`:
  - UUID validate. `ok = await service.update_status(hub_id=hub_uuid, status=req.status, updated_by=user.id, request_id=request_id)`; not ok → `resp.not_found(code="NOT_FOUND")`. `return resp.ok(data={"message": "Cập nhật trạng thái hub thành công"})`.
- `@router.get("/{hub_id}/stats")` `get_hub_stats(hub_id: str, user = Depends(require_role("admin")), service = ...)`:
  - UUID validate. `s = await service.stats(hub_uuid)`; None → `resp.not_found(code="NOT_FOUND")`. `return resp.ok(data=s.model_dump(mode="json"))`.
- KHÔNG tạo endpoint `POST /{hub_id}/test-connection` (D-06).
- Mọi endpoint dùng `require_role("admin")` — hub CRUD admin-only. `# noqa: B008` cho Depends defaults.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/routers/hubs.py && uv run mypy --strict app/routers/hubs.py && uv run python -c "from app.routers.hubs import router; paths={(r.path, tuple(sorted(r.methods))) for r in router.routes}; print(paths); assert ('/api/hubs/{hub_id}', ('PUT',)) in paths; assert ('/api/hubs/{hub_id}/status', ('PATCH',)) in paths; assert not any('test-connection' in p[0] for p in paths)"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/routers/hubs.py` export `router` (APIRouter prefix `/api/hubs`)
    - Router có route: `GET /api/hubs`, `POST /api/hubs`, `GET /api/hubs/{hub_id}`, `PUT /api/hubs/{hub_id}`, `PATCH /api/hubs/{hub_id}/status`, `GET /api/hubs/{hub_id}/stats`
    - `grep "test-connection" api/app/routers/hubs.py` KHÔNG có match (D-06)
    - `grep -c "require_role(\"admin\")" api/app/routers/hubs.py` ≥ 6 (mọi endpoint admin-only)
    - Hub update dùng `@router.put` KHÔNG `@router.patch` (D-07)
    - `ruff check` + `mypy --strict` exit 0; route assertion smoke test exit 0
  </acceptance_criteria>
  <done>routers/hubs.py có 6 endpoint admin-only, PUT update, PATCH status, GET stats; KHÔNG test-connection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST/PUT /api/hubs | Request body name/code/subdomain — validate Pydantic; admin-only |
| client → GET stats | hub_id path param — UUID validate trước query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Elevation of Privilege | viewer/editor gọi POST/PUT/DELETE hub | mitigate | mọi endpoint `require_role("admin")` → 403 FORBIDDEN; hub CRUD là admin-only (HUB-01) |
| T-05-03-02 | Tampering | SQL injection qua name/code/subdomain | mitigate | raw SQL chỉ qua `text()` + named bind params (asyncpg parametrize) — KHÔNG f-string nội suy giá trị |
| T-05-03-03 | Information Disclosure | HubResponse leak field di sản Go (db_host/db_password) | mitigate | D-05 — HubResponse Pydantic schema CHỈ 8 field; cột db_* không tồn tại trong bảng hubs (Plan 05-01 KHÔNG thêm) |
| T-05-03-04 | Spoofing | Tạo 2 hub trùng code → ambiguity | mitigate | unique constraint `uq_hubs_code` (Plan 05-01) + IntegrityError → 409 HUB_CODE_CONFLICT |
| T-05-03-05 | Repudiation | Không truy được ai tạo/sửa hub | mitigate | enqueue_audit `hub.create`/`hub.update` với user_id + request_id + payload |
</threat_model>

<verification>
- `uv run ruff check app/routers/hubs.py app/services/hub_service.py app/schemas/hubs.py` clean
- `uv run mypy --strict` 3 file clean
- Route smoke test xác nhận đủ 6 path + KHÔNG test-connection
</verification>

<success_criteria>
- 6 endpoint Hub CRUD + stats, mọi endpoint admin-only
- HubResponse KHÔNG có field di sản Go (D-05)
- Hub update = PUT, status = PATCH riêng (D-07); KHÔNG test-connection (D-06)
- Stats từ Postgres aggregate; audit hub.create/hub.update enqueue non-blocking
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-03-SUMMARY.md`
</output>
