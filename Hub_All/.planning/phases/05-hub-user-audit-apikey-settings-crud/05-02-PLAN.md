---
phase: 05-hub-user-audit-apikey-settings-crud
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api/app/repositories/__init__.py
  - api/app/repositories/hub_isolation.py
  - api/app/auth/dependencies.py
  - api/app/auth/__init__.py
  - api/app/middleware/rate_limit.py
  - api/app/middleware/__init__.py
autonomous: true
requirements: [HUB-02, AUX-03]
user_setup: []

must_haves:
  truths:
    - "Editor của Hub A không thể mutate resource Hub B kể cả khi truyền explicit hub_id payload"
    - "Hub isolation enforce ở repository layer — WHERE hub_id IN (user.hub_assignments)"
    - "admin bypass hub filter; editor/viewer bắt buộc bị giới hạn theo hub_assignments"
    - "Rate limit middleware giới hạn search/ask 100/min, upload 30/min, KHÔNG limit auth/me"
    - "429 response dùng envelope {success,data,error,meta} code RATE_LIMIT_EXCEEDED"
  artifacts:
    - path: "api/app/repositories/hub_isolation.py"
      provides: "hub_filter_clause + verify_hub_access + HubIsolationError + get_current_user_with_hubs"
      exports: ["hub_filter_clause", "verify_hub_access", "HubIsolationError"]
    - path: "api/app/auth/dependencies.py"
      provides: "get_current_user_with_hubs dependency — User + hub_assignments"
      contains: "get_current_user_with_hubs"
    - path: "api/app/middleware/rate_limit.py"
      provides: "slowapi Limiter + envelope 429 handler"
      exports: ["limiter", "rate_limit_exceeded_handler"]
  key_links:
    - from: "api/app/repositories/hub_isolation.py"
      to: "user.hub_assignments từ JWT claims / user_hubs"
      via: "verify_hub_access raise HubIsolationError"
      pattern: "HubIsolationError"
    - from: "api/app/middleware/rate_limit.py"
      to: "app.pkg.response.too_many_requests"
      via: "custom RateLimitExceeded handler"
      pattern: "RATE_LIMIT_EXCEEDED"
---

<objective>
Xây 2 hạ tầng cross-cutting mà Plan 05-03/04/05 đều dùng:

1. **Hub isolation repository layer (HUB-02 — EXIT criteria E4, security-critical)** — `repositories/hub_isolation.py`: helper `hub_filter_clause()` sinh SQL fragment `WHERE hub_id IN (...)` từ `user.hub_assignments`; `verify_hub_access()` raise `HubIsolationError` khi resource's `hub_id` ∉ assignments; dependency `get_current_user_with_hubs` trả `User` + danh sách hub_id. **KHÔNG bao giờ tin `hub_id` trong request payload** — luôn lấy từ JWT/user_hubs. Khi reject → caller enqueue audit `security.hub_isolation_violation`.

2. **Rate limit middleware (AUX-03)** — `middleware/rate_limit.py`: slowapi `Limiter` (key_func = user_id từ JWT, fallback IP), backend Redis (`app.state.redis` đã có); custom handler map `RateLimitExceeded` → envelope 429. Limit: search/ask 100/min, upload 30/min, KHÔNG limit auth/me.

Purpose: HUB-02 là điều kiện ship M2 (E4 — hub isolation bug = STOP). Plan 05-03/04/05 import `hub_filter_clause`/`verify_hub_access`. Plan 05-06 test E4 dùng các helper này.
Output: `repositories/hub_isolation.py`, `dependencies.py` mở rộng, `middleware/rate_limit.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-CONTEXT.md
@Hub_All/.planning/phases/05-hub-user-audit-apikey-settings-crud/05-PATTERNS.md

<interfaces>
<!-- Auth dependency hiện có — api/app/auth/dependencies.py -->
get_current_user(token, jwt_mgr, redis, db) -> User
  — verify Bearer, blacklist check, load User ORM. KHÔNG kèm hub_assignments.
require_role(*roles) -> Callable — gate role, raise HTTPException 403 FORBIDDEN.
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/auth/login", auto_error=False)

<!-- JWT claims — auth/service.py:84-90 issue_token_pair(hub_ids=[...]) -->
JWT access token claim `hub_ids` chứa list hub_id string user được assign.
JWTManager.verify_token(token, expected_type="access") -> JWTClaims (có .sub, .jti, .hub_ids).
Nếu JWTClaims KHÔNG có field hub_ids — fallback query: select(UserHub.hub_id).where(UserHub.user_id == user.id) (auth/service.py:78-82 pattern).

<!-- Response envelope — api/app/pkg/response.py -->
resp.forbidden(message, code="FORBIDDEN") -> JSONResponse 403
resp.too_many_requests(message, code="RATE_LIMIT_EXCEEDED") -> JSONResponse 429

<!-- WHERE-clause builder analog — documents_service.py:244-260 -->
where_clauses: list[str] = []; params: dict[str,Any] = {}
where_sql = ("WHERE " + " AND ".join(where_clauses)) if where_clauses else ""

<!-- Middleware structural analog — api/app/middleware/request_id.py (BaseHTTPMiddleware) -->
<!-- Redis sẵn ở app.state.redis (main.py:84-89) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repositories/hub_isolation.py — filter clause + verify + dependency</name>
  <files>api/app/repositories/__init__.py, api/app/repositories/hub_isolation.py, api/app/auth/dependencies.py, api/app/auth/__init__.py</files>
  <read_first>
    - api/app/auth/dependencies.py (get_current_user — copy pattern để viết get_current_user_with_hubs)
    - api/app/auth/service.py (dòng 78-90 — pattern query UserHub + issue_token_pair hub_ids)
    - api/app/auth/jwt.py (JWTClaims — kiểm tra có field hub_ids không)
    - api/app/models/auth.py (UserHub join table — user_id, hub_id)
    - api/app/services/documents_service.py (dòng 244-260 — WHERE-clause builder analog)
    - api/app/pkg/response.py (resp.forbidden)
  </read_first>
  <behavior>
    - hub_filter_clause(admin role) → trả ("", {}) — admin bypass, query không thêm WHERE hub_id
    - hub_filter_clause(editor, hub_ids=[A,B]) → trả ("hub_id IN (:uh0, :uh1)", {"uh0":A,"uh1":B})
    - hub_filter_clause(editor, hub_ids=[]) → trả ("hub_id IN (NULL)", {}) hoặc clause luôn-false — KHÔNG để editor không-hub thấy mọi row
    - verify_hub_access(admin, resource_hub_id) → pass (return None) bất kể hub_id
    - verify_hub_access(editor hub A, resource_hub_id=hub B) → raise HubIsolationError
    - verify_hub_access(editor hub A, resource_hub_id=hub A) → pass
  </behavior>
  <action>
Tạo `api/app/repositories/__init__.py` (docstring ngắn + `from __future__ import annotations`).

Tạo `api/app/repositories/hub_isolation.py`:
- `from __future__ import annotations`.
- `class HubIsolationError(Exception)`: `__init__(self, message: str, *, resource_hub_id: str | None = None)` — lưu attribute để caller dùng cho audit payload.
- `def hub_filter_clause(*, role: str, hub_ids: list[str], param_prefix: str = "uh") -> tuple[str, dict[str, str]]`:
  - Nếu `role == "admin"` → `return "", {}` (admin bypass — admin quản trị cross-hub).
  - Nếu `hub_ids` rỗng → `return "hub_id IN (NULL)", {}` (luôn-false — editor/viewer chưa assign hub KHÔNG thấy row nào).
  - Ngược lại: `placeholders = ", ".join(f":{param_prefix}{i}" for i in range(len(hub_ids)))`; `clause = f"hub_id IN ({placeholders})"`; `params = {f"{param_prefix}{i}": h for i, h in enumerate(hub_ids)}`; return `clause, params`.
- `def verify_hub_access(*, role: str, user_hub_ids: list[str], resource_hub_id: str) -> None`:
  - Nếu `role == "admin"` → return None.
  - Nếu `resource_hub_id not in user_hub_ids` → `raise HubIsolationError(f"Resource thuộc hub {resource_hub_id} ngoài quyền truy cập", resource_hub_id=resource_hub_id)`.
  - Ngược lại return None.
- Docstring module: ghi rõ "Defense in depth 3 lớp: (1) repository hub_filter_clause inject WHERE; (2) service verify_hub_access trước mutation; (3) caller enqueue_audit('security.hub_isolation_violation') khi reject. KHÔNG BAO GIỜ tin hub_id trong request payload."

Mở rộng `api/app/auth/dependencies.py` — thêm dependency `get_current_user_with_hubs`:
```python
class UserWithHubs:
    """User ORM + danh sách hub_id được assign (HUB-02)."""
    def __init__(self, user: User, hub_ids: list[str]) -> None:
        self.user = user
        self.hub_ids = hub_ids

async def get_current_user_with_hubs(
    user: User = Depends(get_current_user),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> UserWithHubs:
    """User + hub_assignments từ user_hubs (HUB-02 isolation source).

    Lấy hub_ids từ DB user_hubs (KHÔNG tin payload). admin role vẫn trả
    hub_ids thực tế nhưng hub_filter_clause sẽ bypass cho admin.
    """
    from app.models.auth import UserHub
    stmt = select(UserHub.hub_id).where(UserHub.user_id == user.id)
    hub_ids = [str(h) for h in (await db.execute(stmt)).scalars().all()]
    return UserWithHubs(user=user, hub_ids=hub_ids)
```
(`select` + `UserHub` import — `select` đã import ở dependencies.py dòng 18; `AsyncSession`/`get_session` đã import.)

Cập nhật `api/app/auth/__init__.py`: re-export `get_current_user_with_hubs`, `UserWithHubs` (thêm vào `__all__`).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/repositories app/auth/dependencies.py app/auth/__init__.py && uv run mypy --strict app/repositories app/auth/dependencies.py && uv run python -c "from app.repositories.hub_isolation import hub_filter_clause, verify_hub_access, HubIsolationError; c,p=hub_filter_clause(role='editor',hub_ids=['a','b']); assert c=='hub_id IN (:uh0, :uh1)' and p=={'uh0':'a','uh1':'b'}; assert hub_filter_clause(role='admin',hub_ids=['x'])==('',{}); verify_hub_access(role='admin',user_hub_ids=[],resource_hub_id='z'); 
import pytest
try:
    verify_hub_access(role='editor',user_hub_ids=['a'],resource_hub_id='b'); raise AssertionError('should raise')
except HubIsolationError: pass"</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/repositories/hub_isolation.py` tồn tại, export `hub_filter_clause`, `verify_hub_access`, `HubIsolationError`
    - `hub_filter_clause(role='admin', ...)` trả `("", {})` (verify trong smoke test)
    - `hub_filter_clause(role='editor', hub_ids=[])` trả clause chứa `NULL` (editor không-hub thấy 0 row)
    - `verify_hub_access` raise `HubIsolationError` khi resource_hub_id ∉ user_hub_ids và role≠admin
    - `api/app/auth/dependencies.py` chứa `get_current_user_with_hubs` và class `UserWithHubs`
    - `api/app/auth/__init__.py` `__all__` chứa `get_current_user_with_hubs`
    - `ruff check` + `mypy --strict` exit 0; smoke test exit 0
  </acceptance_criteria>
  <done>hub_isolation.py có filter clause + verify; get_current_user_with_hubs dependency trả User + hub_ids từ user_hubs.</done>
</task>

<task type="auto">
  <name>Task 2: middleware/rate_limit.py — slowapi Limiter + envelope 429</name>
  <files>api/app/middleware/rate_limit.py, api/app/middleware/__init__.py</files>
  <read_first>
    - api/app/middleware/request_id.py (analog kết cấu middleware module)
    - api/app/middleware/__init__.py (cách export middleware)
    - api/app/pkg/response.py (resp.too_many_requests — code RATE_LIMIT_EXCEEDED)
    - api/app/main.py (dòng 239-260 — comment middleware order; app.state.redis dòng 84-89)
    - Hub_All/.planning/CONVENTIONS.md (§4 middleware order)
  </read_first>
  <action>
Kiểm tra `slowapi` có trong `pyproject.toml` dependencies chưa. Nếu chưa: `cd Hub_All/api && uv add slowapi` (slowapi là M2 stack pin cho rate limit theo CONTEXT — KHÔNG bump version dep khác).

Tạo `api/app/middleware/rate_limit.py`:
- `from __future__ import annotations`.
- Import `from slowapi import Limiter`, `from slowapi.errors import RateLimitExceeded`, `from slowapi.util import get_remote_address`.
- `def _rate_limit_key(request: Request) -> str`: lấy user_id từ JWT — đọc header `Authorization`, nếu có Bearer token thử decode lấy `sub` claim qua `request.app.state.jwt_manager.verify_token(token, expected_type="access")`; nếu fail/không có token → fallback `get_remote_address(request)`. Bọc decode trong try/except (KHÔNG raise — rate limit key fallback IP).
- `limiter = Limiter(key_func=_rate_limit_key, storage_uri=None, default_limits=[])` — `storage_uri` set runtime; mặc định in-memory, comment ghi "Redis backend qua storage_uri từ settings.redis_url — Plan 05-06 wiring đọc app.state". Để đơn giản M2: dùng `storage_uri` build từ `get_settings().redis_url` (slowapi hỗ trợ `redis://` URI trực tiếp) — Redis store để limit share giữa worker (CONTEXT Claude's Discretion → chọn Redis vì app.state.redis sẵn có).
- `async def rate_limit_exceeded_handler(request: Request, exc: RateLimitExceeded) -> JSONResponse`: trả `resp.too_many_requests(message=f"Vượt giới hạn request: {exc.detail}", code="RATE_LIMIT_EXCEEDED")`. Đây là envelope shape D6.
- Constant decorator-string helper: `SEARCH_LIMIT = f"{get_settings().rate_limit_search_per_minute}/minute"`, `UPLOAD_LIMIT = f"{get_settings().rate_limit_upload_per_minute}/minute"` — Plan 06/04 router dùng `@limiter.limit(SEARCH_LIMIT)`. KHÔNG limit auth/me (không decorate auth router).
- Docstring ghi: "AUX-03 — search/ask 100/min, upload 30/min, auth/me KHÔNG limit. slowapi dùng Limiter + decorator + exception_handler (KHÔNG BaseHTTPMiddleware). Wire vào main.py: app.state.limiter = limiter + app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler) — Plan 05-06 thực hiện wiring."

Cập nhật `api/app/middleware/__init__.py`: export `limiter`, `rate_limit_exceeded_handler` (thêm vào `__all__`).

Lưu ý: Plan này CHỈ tạo module rate_limit — wiring vào `main.py` (app.state.limiter + exception_handler + middleware) thuộc Plan 05-06 (để tránh xung đột file `main.py` với Plan 05-01 Task 3 cùng wave khác). Đảm bảo module import được độc lập.
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run ruff check app/middleware/rate_limit.py app/middleware/__init__.py && uv run mypy --strict app/middleware/rate_limit.py && uv run python -c "from app.middleware import limiter, rate_limit_exceeded_handler; from slowapi import Limiter; assert isinstance(limiter, Limiter)"</automated>
  </verify>
  <acceptance_criteria>
    - `slowapi` xuất hiện trong `Hub_All/api/pyproject.toml` dependencies
    - `api/app/middleware/rate_limit.py` tồn tại, export `limiter` (slowapi Limiter), `rate_limit_exceeded_handler`
    - `grep "RATE_LIMIT_EXCEEDED" api/app/middleware/rate_limit.py` có match
    - `rate_limit_exceeded_handler` trả về qua `resp.too_many_requests` (envelope shape)
    - `_rate_limit_key` có fallback IP khi không decode được JWT (try/except)
    - `ruff check` + `mypy --strict` exit 0; import smoke test exit 0
  </acceptance_criteria>
  <done>rate_limit.py có slowapi Limiter (key=user_id, fallback IP) + handler 429 envelope; export sẵn cho Plan 05-06 wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (hub_id payload) | Client gửi `hub_id` trong request body — UNTRUSTED, KHÔNG được dùng để authorize |
| JWT claims → hub_assignments | hub_ids lấy từ DB user_hubs (verified) — KHÔNG từ payload |
| client → rate limiter | Attacker spam request — limiter chặn theo user_id/IP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege | Editor Hub A truyền `hub_id="hub_a"` trong payload để mutate resource Hub B | mitigate | `verify_hub_access` so resource's hub_id THỰC TẾ (load từ DB) với `user_hub_ids` lấy từ `user_hubs` — KHÔNG tin payload. hub_filter_clause inject WHERE ở repository. EXIT criteria E4 — Plan 05-06 test mandatory |
| T-05-02-02 | Information Disclosure | hub_filter_clause với hub_ids rỗng vô tình trả mọi row | mitigate | hub_ids rỗng → `hub_id IN (NULL)` (luôn-false) — editor/viewer chưa assign hub thấy 0 row, KHÔNG phải mọi row |
| T-05-02-03 | Elevation of Privilege | admin bypass hub filter bị lạm dụng | accept | admin role theo thiết kế quản trị cross-hub (HUB-03 stats, USER-01 CRUD); admin-only endpoint đã gate `require_role("admin")` — rủi ro giới hạn ở tài khoản admin compromised (ngoài scope Phase 5) |
| T-05-02-04 | Denial of Service | Attacker spam search/ask/upload | mitigate | slowapi limiter — search/ask 100/min, upload 30/min theo user_id (fallback IP); 429 envelope; Redis store share giữa worker |
| T-05-02-05 | Spoofing | Rate-limit key dùng IP dễ bị NAT/proxy gộp | accept | M2 ưu tiên user_id key (JWT sub); IP fallback chỉ cho request chưa auth — auth/me KHÔNG limit nên ảnh hưởng nhỏ |
</threat_model>

<verification>
- `uv run ruff check app && uv run mypy --strict app/repositories app/middleware/rate_limit.py app/auth/dependencies.py` clean
- Smoke test hub_filter_clause + verify_hub_access logic pass
- `slowapi` resolvable trong môi trường uv
</verification>

<success_criteria>
- hub_isolation.py: hub_filter_clause (admin bypass, empty→false) + verify_hub_access (raise HubIsolationError)
- get_current_user_with_hubs dependency trả hub_ids từ user_hubs DB (KHÔNG payload)
- rate_limit.py: slowapi Limiter + handler 429 envelope; export sẵn cho wiring
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/05-hub-user-audit-apikey-settings-crud/05-02-SUMMARY.md`
</output>
