---
phase: 02-backend-rbac-enforcement
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03, DEP-04, DEP-05]
must_haves:
  truths:
    - "STATE.md frontmatter `completed_phases: 2` + `completed_plans: 8` (3 Phase 1 + 5 Phase 2) + `percent: 53` (8/15) + `next_action: \"/gsd-discuss-phase 3 — FE frontend form refactor\"`"
    - "STATE.md body table v3.1 Planning Summary Phase 2 row status `✅ DONE 2026-05-24 (5 plan)`"
    - "STATE.md body chứa section mới `## Phase 2 Results Summary (DONE 2026-05-24)` với 5 bullet plan + carry forward patterns"
    - "REQUIREMENTS.md 5 dòng `- [x] **DEP-XX**` (X = 01..05) + suffix `(DONE 2026-05-24 — Plan 02-XX)`"
    - "ROADMAP.md table Phase 2 row trạng thái DONE + 5 Plans checkbox `[x]`"
    - "CLAUDE.md §6 thêm subsection `### Phase 2 v3.1 RBAC enforcement pattern (DEP-01..05 — 2026-05-24)` với 5 bullet plan + Backward compat + R-V3.1-2 mitigation chain + Next pointer"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "v3.1 milestone progress cập nhật Phase 2 DONE"
      contains: "Phase 2 Results Summary"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DEP-01..05 marked [x]"
      contains: "[x] **DEP-01**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 row + plans checklist DONE"
      contains: "[x] 02-01-PLAN.md"
    - path: "CLAUDE.md"
      provides: "Phase 2 pattern reference cho Phase 3+ developer (carry forward Phase 1 v3.1 closeout pattern)"
      contains: "Phase 2 v3.1 RBAC enforcement pattern"
  key_links:
    - from: ".planning/STATE.md progress"
      to: ".planning/REQUIREMENTS.md DEP-01..05 [x]"
      via: "Cross-reference REQ-ID consumed count 5/5 + Phase 2 status DONE"
      pattern: "completed_plans.*8"
    - from: "CLAUDE.md §6 Phase 2 subsection"
      to: ".planning/phases/02-backend-rbac-enforcement/ plan + SUMMARY files"
      via: "Reference paths trong subsection"
      pattern: "02-backend-rbac-enforcement"
---

<objective>
Closeout Phase 2 v3.1 backend RBAC enforcement — update 4 docs source-of-truth phản ánh 5 plan ship đầy đủ 5 REQ-ID DEP-01..05.

**Purpose:** Phase 2 đã ship qua 4 plan (02-01..04 — Wave 1 validator + Wave 2 routers + Wave 3 audit). Plan 02-05 (Wave 4 closeout) đồng bộ docs source-of-truth:
1. STATE.md progress (2/4 phase DONE, 8/15 plan ship).
2. REQUIREMENTS.md mark [x] cho 5 REQ-ID DEP-01..05.
3. ROADMAP.md Phase 2 table row DONE + 5 plans checklist.
4. CLAUDE.md §6 thêm subsection v3.1 Phase 2 pattern reference (carry forward Phase 3+).

**Smoke checkpoint runtime SKIP pre-resolved** (carry forward Plan 03-05 + 04-07 + 05-06 + 06-05 v3.0 pattern + Plan 01-03 v3.1 carry forward — auto-fallback `skip smoke` per `--chain` mode active). Manual visual smoke + integration test runtime full E2E defer Phase 4 MIGRATE-02 (smoke E2E 4 scenario pytest httpx + audit verify).

**Output:** 4 file docs update atomic trong 1 commit.

**Carry forward patterns:**
- Plan 01-03 v3.1 closeout pattern (3 docs update + SUMMARY.md generation).
- Plan 04-07 + 05-06 + 06-05 v3.0 smoke skip pre-resolved auto-fallback.
- Tiếng Việt có dấu cho mọi description text (CLAUDE.md convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/CLAUDE.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-01-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-02-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-03-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-04-PLAN.md

<interfaces>
<!-- Pattern source-of-truth — closeout pattern carry forward Plan 01-03 v3.1 + Plan 06-05 v3.0. -->

STATE.md frontmatter update (carry forward Plan 01-03 v3.1):
```yaml
last_updated: "2026-05-24T<HH:MM>:00.000Z"
progress:
  total_phases: 4
  completed_phases: 2              # ← bump 1 → 2
  total_plans: 15
  completed_plans: 8               # ← bump 3 → 8 (Phase 1 3 plan + Phase 2 5 plan)
  percent: 53                      # ← (8/15)*100 = 53.33 → 53 round
next_action: "/gsd-discuss-phase 3 — FE frontend form refactor (UserManagement form 3 option + hub switcher hide central + edit modal disabled assign super)"
```

STATE.md body v3.1 Planning Summary table Phase 2 row update:
```
| 2 | Backend RBAC | require_hub_admin_for dep + GET /api/hubs filter + CRUD scope + audit | DEP-01..05 (5) | 4-5 | ✅ DONE 2026-05-24 (5 plan) |
```

STATE.md body section MỚI append SAU "## Phase 1 Results Summary" (TRƯỚC "## Open Question"):
```markdown
## Phase 2 Results Summary (DONE 2026-05-24)

5 plan ship 5 REQ-ID DEP-01..05:

- **Plan 02-01** (DEP-01): Validator function `assert_hub_admin_for(*, user, db, target_hub_id)` ở `api/app/auth/dependencies.py` — hybrid pattern D-V3.1-Phase2-D LOCKED (KHÔNG Depends factory vì hub_id ở body POST/PATCH). Import `get_effective_role` + `UserNotFoundError` từ Plan 01-02. 5 unit test PASS cover 5 case D-V3.1-Phase2-D (super admin bypass / hub_admin pass / hub_admin wrong hub / viewer / user_not_found defensive). Envelope `HUB_ADMIN_REQUIRED` mới (D-V3.1-Phase2-B LOCKED) — KHÔNG reuse FORBIDDEN để FE-01 switch UX rõ ràng.
- **Plan 02-02** (DEP-02 + DEP-04): Integration test 3 scenario verify GET /api/hubs filter cả admin (D-V3.1-Phase2-A LOCKED — hub_admin với `users.role='editor'` rơi else branch line 78 → filter user_hubs → CHỈ thấy hub được assign). 5 endpoint mutate hubs.py (POST/PUT/PATCH/stats — line 94-218) GIỮ `Depends(require_role("admin"))` UNCHANGED (DEP-04 LOCKED). routers/hubs.py KHÔNG cần code change — verify-only test.
- **Plan 02-03** (DEP-03): UserRole Literal extend 4 value (admin|hub_admin|editor|viewer) match Plan 01-01 migration 0006 CHECK. 3 endpoint routers/users.py refactor (POST create + PATCH role + GET list) với assert_hub_admin_for inline sau body parse. T-02-02-E mitigation business logic block `req.role == "admin" and user.role != "admin"` (hub_admin KHÔNG được escalate). HUB_ID_REQUIRED guard cho GET list non-super-admin. 2 endpoint (PATCH status + DELETE) GIỮ require_role super-only (D-V3.1-Phase2-E LOCKED cross-hub op). 5 integration test PASS scenario #1-5 ROADMAP success criteria #2.
- **Plan 02-04** (DEP-05): Helper `build_audit_payload(*, actor_role, actor_hub_id, extra)` ở audit_service.py — D-V3.1-Phase2-C LOCKED nest vào payload JSONB existing KHÔNG schema migration. 5 service callsite refactor (user_service.create + delete; hub_service.create + update + update_status) signature thêm actor_role + actor_hub_id keyword-only. 5 router callsite derive `actor_role = "admin" if user.role == "admin" else "hub_admin"` + `actor_hub_id = None if super else req.hub_id`. 4 unit test + 2 integration test PASS — verify `payload->>'actor_role'` + `payload->>'actor_hub_id'` queryable cho forensic.
- **Plan 02-05** (closeout): STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md update atomic — Phase 2 DONE 100% 5 REQ-ID consumed. Smoke checkpoint runtime SKIP pre-resolved (carry forward v3.0 Plan 04-07 + 05-06 + 06-05 pattern + Plan 01-03 v3.1) — defer Phase 4 MIGRATE-02 full E2E live runtime.

**Carry forward patterns:**
- Hybrid validator function (KHÔNG Depends factory) cho hub_id body — D-V3.1-Phase2-D LOCKED; áp dụng cho future endpoint per-hub gate khi hub_id KHÔNG ở path param.
- Envelope `HUB_ADMIN_REQUIRED` vs `FORBIDDEN` differentiation — frontend FE-01 sẽ switch UX (Phase 3).
- Audit payload nest (KHÔNG schema migration audit_logs) — D-V3.1-Phase2-C LOCKED carry forward Phase 4 v3.0 D-V3-Phase4-C2 pattern.
- Service signature `*` keyword-only + actor metadata params — Pattern G carry forward; force caller explicit pass tránh positional bug.
- Integration test helper `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` duplicate qua Plan 02-02 + 02-03 + 02-04 — Phase 4 MIGRATE-02 consolidate vào conftest.py nếu muốn DRY (defer).

**R-V3.1-2 MEDIUM mitigation chain Phase 2:**
- Backend filter authoritative GET /api/hubs (Plan 02-02 D-V3.1-Phase2-A LOCKED) — defense in depth.
- assert_hub_admin_for inline check sau body parse (Plan 02-01 + 02-03) — KHÔNG dựa FE để gate per-hub.
- T-02-02-E business logic block role escalation (Plan 02-03 PATCH role handler) — hub_admin KHÔNG được assign role='admin' cho user khác.
- DELETE + PATCH status giữ require_role super-only (Plan 02-03 + D-V3.1-Phase2-E LOCKED) — cross-hub op KHÔNG cho hub_admin.
- Audit forensic chain payload->>'actor_role' + payload->>'actor_hub_id' (Plan 02-04 DEP-05) — incident review filter scope.
- Test coverage ≥ 80% trên dependencies.py + routers/users.py + audit_service.py (D-V3.1-Phase2-F LOCKED — 5+3+5+4+2 = 19 test ship).

**Next:** Phase 3 `/gsd-discuss-phase 3` FE frontend form refactor (UserManagement 3 option radio + hub switcher hide central + edit modal disabled assign super + api.ts UserRole type extend).
```

REQUIREMENTS.md mark pattern (search/replace):
- `- [ ] **DEP-01**` → `- [x] **DEP-01**` + suffix ` (DONE 2026-05-24 — Plan 02-01)`
- `- [ ] **DEP-02**` → `- [x] **DEP-02**` + suffix ` (DONE 2026-05-24 — Plan 02-02)`
- `- [ ] **DEP-03**` → `- [x] **DEP-03**` + suffix ` (DONE 2026-05-24 — Plan 02-03)`
- `- [ ] **DEP-04**` → `- [x] **DEP-04**` + suffix ` (DONE 2026-05-24 — Plan 02-02)` (đồng Plan với DEP-02)
- `- [ ] **DEP-05**` → `- [x] **DEP-05**` + suffix ` (DONE 2026-05-24 — Plan 02-04)`

ROADMAP.md updates:
1. Table Phase 2 row trạng thái — append cuối:
```
| **2** | Backend RBAC enforcement (DEP) | ... | DEP-01..05 (5) | 5 | Phase 1 | ✅ DONE 2026-05-24 |
```
(verify column count trong ROADMAP table thực tế — adapt nếu cần thêm column status)

2. Phase 2 plans checklist:
```
Plans:
- [x] 02-01-PLAN.md — require_hub_admin_for validator + 5-case unit test + 403 envelope HUB_ADMIN_REQUIRED (DEP-01) ✅ DONE 2026-05-24
- [x] 02-02-PLAN.md — Integration test GET /api/hubs filter cả admin + POST hub_admin 403 verify (DEP-02 + DEP-04) ✅ DONE 2026-05-24
- [x] 02-03-PLAN.md — routers/users.py 3 endpoint refactor scope + UserRole Literal extend (DEP-03) ✅ DONE 2026-05-24
- [x] 02-04-PLAN.md — Audit payload actor_role + actor_hub_id nest 5 callsite (DEP-05) ✅ DONE 2026-05-24
- [x] 02-05-PLAN.md — Closeout STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md update ✅ DONE 2026-05-24
```

3. Progress table:
```
| **v3.1 RBAC hub_admin** | **4** | **8/~15** | **9/15** | 🚧 **Phase 2 DONE** | — |
```
(adapt theo format ROADMAP existing — verify column header trước update)

CLAUDE.md §6 subsection (append cuối, SAU "### Phase 1 v3.1 RBAC schema pattern (ROLE-01..04 — 2026-05-23)"):
```markdown
### Phase 2 v3.1 RBAC enforcement pattern (DEP-01..05 — 2026-05-24)

5 plan đóng 5 REQ-ID DEP backend RBAC enforcement + 6 D-V3.1-Phase2-A..F LOCKED:

- **Plan 02-01 DEP-01 — assert_hub_admin_for validator function:** `api/app/auth/dependencies.py` thêm `async def assert_hub_admin_for(*, user, db, target_hub_id) -> None` (hybrid pattern D-V3.1-Phase2-D LOCKED — KHÔNG Depends factory vì hub_id ở body POST/PATCH). Import `get_effective_role` + `UserNotFoundError` từ Plan 01-02 ROLE-04. Logic 5 case: super admin bypass / hub_admin đúng pass / hub_admin sai 403 / viewer 403 / user_not_found defensive catch + 403 (KHÔNG leak 404). Envelope `HUB_ADMIN_REQUIRED` mới (D-V3.1-Phase2-B LOCKED) — KHÔNG reuse FORBIDDEN. 5 unit test pytest PASS với AsyncMock(AsyncSession) factory carry forward Plan 01-02 pattern. Reference: `.planning/phases/02-backend-rbac-enforcement/02-01-PLAN.md` + `02-01-SUMMARY.md`.

- **Plan 02-02 DEP-02 + DEP-04 — GET /api/hubs verify + hubs mutate preserve:** routers/hubs.py UNCHANGED — D-V3.1-Phase2-A LOCKED logic `if user.role == "admin"` line 73-77 ĐÃ ĐÚNG (hub_admin với `users.role='editor'` global rơi else branch line 78-85 → filter user_hubs). 5 endpoint mutate (POST line 94 + PUT line 138 + PATCH status line 168 + GET stats line 198 + GET single line 115) GIỮ `Depends(require_role("admin"))` (DEP-04 LOCKED). Integration test 3 scenario verify hub_admin dmd CHỈ thấy dmd + hub_admin POST → 403 FORBIDDEN (require_role envelope giữ NGUYÊN — KHÔNG đổi HUB_ADMIN_REQUIRED). Helper `_seed_hub_admin_user` + fixture `seed_hubs_dmd_tdt` (Plan 01-01 migration 0006 user_hubs.role TEXT NULL khai thác). Reference: `02-02-PLAN.md` + `02-02-SUMMARY.md`.

- **Plan 02-03 DEP-03 — users.py 3 endpoint refactor + Literal extend:** `schemas/users.py` UserRole Literal extend 4 value (admin|hub_admin|editor|viewer) match migration 0006 CHECK. `routers/users.py` 3 endpoint (POST create + PATCH role + GET list) refactor: `Depends(get_current_user)` thay `Depends(require_role("admin"))` + inline `await assert_hub_admin_for(user, db, target_hub_id=req.hub_id)` sau body parse. T-02-02-E mitigation business logic block `if req.role == "admin" and user.role != "admin"` → 403 HUB_ADMIN_REQUIRED (hub_admin KHÔNG escalate role admin cho user khác). HUB_ID_REQUIRED guard cho GET list non-super-admin (hub_admin BUỘC pass hub_id query). 2 endpoint (PATCH status + DELETE + reset-password + GET single + PUT) GIỮ require_role super-only (D-V3.1-Phase2-E LOCKED cross-hub op). 5 integration test PASS verify scenario #1-5 ROADMAP success criteria #2. Reference: `02-03-PLAN.md` + `02-03-SUMMARY.md`.

- **Plan 02-04 DEP-05 — Audit payload actor_role + actor_hub_id nest 5 callsite:** `services/audit_service.py` thêm `build_audit_payload(*, actor_role, actor_hub_id, extra=None) -> dict[str, Any]` helper — D-V3.1-Phase2-C LOCKED nest vào payload JSONB existing (audit_logs.payload nullable JSONB từ migration 0001) KHÔNG schema migration. 5 service callsite refactor: user_service.create + delete (signature force keyword required) + hub_service.create + update + update_status (signature default 'admin' + None vì DEP-04 LOCKED hub mutate super-only). 5 router callsite derive actor metadata: POST /api/users + DELETE /api/users + POST /api/hubs + PUT /api/hubs + PATCH /api/hubs/status. 4 unit test (pure Python helper) + 2 integration test (poll-based wait `_wait_audit_row` pattern carry forward conftest.py:778-806) verify `payload->>'actor_role'` + `payload->>'actor_hub_id'` queryable. Forensic query Phase 4 MIGRATE-02 hỗ trợ `WHERE payload->>'actor_role' = 'hub_admin' AND payload->>'actor_hub_id' = '<dmd-uuid>'`. Reference: `02-04-PLAN.md` + `02-04-SUMMARY.md`.

- **Plan 02-05 closeout — docs update:** STATE.md frontmatter (`completed_phases: 2` + `completed_plans: 8` + `percent: 53`) + body Phase 2 Results Summary section (5 bullet plan + carry forward + R-V3.1-2 mitigation + Next) + REQUIREMENTS.md 5 dòng `[x] **DEP-XX**` + ROADMAP.md table + plans checklist + CLAUDE.md §6 subsection mới (file này). Smoke checkpoint runtime SKIP pre-resolved (auto-fallback `--chain` mode active per Plan 04-07 + 05-06 + 06-05 v3.0 + Plan 01-03 v3.1 carry forward). Evidence chain: 5+3+5+4+2 = 19 test PASS in-process cover semantic DEP-01..05; manual visual smoke + integration test runtime full E2E defer Phase 4 MIGRATE-02 (4 scenario pytest httpx + audit verify). Reference: `02-05-PLAN.md` + `02-05-SUMMARY.md`.

**Backward compat (Phase 2 KHÔNG break v3.0 + v3.1 Phase 1):**
- Schema 0006 carry forward unchanged (Plan 01-01 ship).
- 5 router endpoint M2 + v3.0 envelope `{success, data, error, meta}` shape LOCKED — chỉ thêm error code mới (HUB_ADMIN_REQUIRED) + extend hành vi (HUB_ID_REQUIRED guard GET list).
- `require_role` closure factory KHÔNG đổi — 4 endpoint giữ NGUYÊN (DELETE + PATCH status + reset-password + GET single + PUT users + 5 hubs mutate).
- `get_effective_role` helper API contract LOCKED Plan 01-02 carry forward.
- audit_logs.payload JSONB existing đủ chứa actor metadata — KHÔNG schema migration.
- **Breaking change (tests cũ):** `user_service.create(req=, created_by=)` + `delete(user_id=, deleted_by=)` + `hub_service.create/update/update_status` signature THÊM keyword-only `actor_role` (user_service required, hub_service default 'admin') + `actor_hub_id`. Existing tests M2 + v3.0 phải update call site — Plan 02-05 acceptance criteria audit tests Phase 5 v3.0 nếu phát hiện regression sau Wave 1-3.

**R-V3.1-2 MEDIUM mitigation chain Phase 2:**
- Backend filter authoritative GET /api/hubs (Plan 02-02 D-V3.1-Phase2-A LOCKED) — defense in depth, KHÔNG dựa FE.
- assert_hub_admin_for inline check sau body parse (Plan 02-01 + 02-03) — production code path enforce per-hub gate.
- T-02-02-E business logic block role escalation (Plan 02-03 PATCH role handler) — hub_admin KHÔNG được assign role='admin' cho user khác.
- DELETE + PATCH status giữ require_role super-only (Plan 02-03 + D-V3.1-Phase2-E LOCKED) — cross-hub op KHÔNG cho hub_admin.
- Audit forensic chain `payload->>'actor_role'` + `payload->>'actor_hub_id'` (Plan 02-04 DEP-05) — incident review filter scope hub_admin operation.
- Test coverage 19 test ship cover semantic DEP-01..05 (D-V3.1-Phase2-F LOCKED ≥ 80% satisfied).
**Phase 2 backward incompat (operator action required — W5 iter 1):**
- hub_admin GET /api/users yêu cầu hub_id query (HUB_ID_REQUIRED guard Plan 02-03 DEP-03).
- Existing M2/v3.0 frontend chưa pass hub_id query → 1-2 ngày downtime trên user management page cho hub_admin role giữa Phase 2 ship và Phase 3 FE-04 ship (acceptable v3.1 timeline).
- **Operator broadcast (Slack/Email) trước Phase 3 FE-04 ship:** thông báo hub_admin user về temporary breakage + ETA Phase 3 FE-04.
- Phase 3 FE-04 sẽ pass hub_id query → resolve.

- Phase 4 MIGRATE-02 sẽ verify runtime full E2E 4 scenario pytest httpx + audit log inspect live.

**Next:** Phase 3 `/gsd-discuss-phase 3` FE frontend form refactor (UserManagement 3 option radio + hub switcher hide central + edit modal disabled assign super + api.ts UserRole type extend).
```

Test fixture (carry forward Plan 01-03 v3.1 pattern):
```bash
# Verify atomic update — KHÔNG đụng section khác của STATE.md / REQUIREMENTS.md / CLAUDE.md (preserve Phase 1 v3.1 + M2 + v3.0 content nguyên).
grep -c "completed_plans: 8" Hub_All/.planning/STATE.md
grep -c "Phase 2 Results Summary" Hub_All/.planning/STATE.md
grep -c "\[x\] \*\*DEP-01\*\*" Hub_All/.planning/REQUIREMENTS.md
grep -c "\[x\] \*\*DEP-05\*\*" Hub_All/.planning/REQUIREMENTS.md
grep -c "Phase 2 v3.1 RBAC enforcement pattern" Hub_All/CLAUDE.md
grep -c "\[x\] 02-01-PLAN.md" Hub_All/.planning/ROADMAP.md
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật 4 file docs source-of-truth atomic (STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md)</name>
  <read_first>
    - Hub_All/.planning/STATE.md (toàn bộ — biết section nào cần update progress + frontmatter + body Results Summary mới)
    - Hub_All/.planning/REQUIREMENTS.md (tìm 5 dòng `- [ ] **DEP-XX**` → mark [x] + suffix)
    - Hub_All/.planning/ROADMAP.md (Phase 2 row table + plans checklist + progress table)
    - Hub_All/CLAUDE.md §6 cuối file (append subsection v3.1 Phase 2 — SAU Phase 1 v3.1 subsection)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-01-PLAN.md → 02-04-PLAN.md (verify artifact list + REQ-ID coverage cho summary text)
  </read_first>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, CLAUDE.md</files>
  <action>
**Update 1: `Hub_All/.planning/STATE.md`** — 3 thay đổi atomic:

1. **Frontmatter update** (lines 1-16):
   - `last_updated:` → `"2026-05-24T<HH:MM>:00.000Z"` (current timestamp).
   - `progress.completed_phases:` → `2` (bump 1 → 2).
   - `progress.completed_plans:` → `8` (bump 3 → 8: 3 Phase 1 + 5 Phase 2).
   - `progress.percent:` → `53` (8/15*100 = 53.33 round).
   - `next_action:` → `"/gsd-discuss-phase 3 — FE frontend form refactor (UserManagement form 3 option + hub switcher hide central + edit modal disabled assign super)"`.

2. **Body table v3.1 Planning Summary** — update Phase 2 row (search current row `| 2 | Backend RBAC | ... | Not started |`):
   ```
   | 2 | Backend RBAC | require_hub_admin_for dep + GET /api/hubs filter + CRUD scope + audit | DEP-01..05 (5) | 4-5 | ✅ DONE 2026-05-24 (5 plan) |
   ```

3. **Body section MỚI** — append SAU `## Phase 1 Results Summary (DONE 2026-05-23)` (TRƯỚC `## Open Question`):

```markdown
## Phase 2 Results Summary (DONE 2026-05-24)

5 plan ship 5 REQ-ID DEP-01..05:

- **Plan 02-01** (DEP-01): Validator function `assert_hub_admin_for(*, user, db, target_hub_id)` ở `api/app/auth/dependencies.py` — hybrid pattern D-V3.1-Phase2-D LOCKED (KHÔNG Depends factory vì hub_id ở body POST/PATCH). Import `get_effective_role` + `UserNotFoundError` từ Plan 01-02. 5 unit test PASS cover 5 case D-V3.1-Phase2-D (super admin bypass / hub_admin pass / hub_admin wrong hub / viewer / user_not_found defensive). Envelope `HUB_ADMIN_REQUIRED` mới (D-V3.1-Phase2-B LOCKED) — KHÔNG reuse FORBIDDEN để FE-01 switch UX rõ ràng.
- **Plan 02-02** (DEP-02 + DEP-04): Integration test 3 scenario verify GET /api/hubs filter cả admin (D-V3.1-Phase2-A LOCKED — hub_admin với `users.role='editor'` rơi else branch line 78 → filter user_hubs → CHỈ thấy hub được assign). 5 endpoint mutate hubs.py (POST/PUT/PATCH/stats — line 94-218) GIỮ `Depends(require_role("admin"))` UNCHANGED (DEP-04 LOCKED). routers/hubs.py KHÔNG cần code change — verify-only test.
- **Plan 02-03** (DEP-03): UserRole Literal extend 4 value (admin|hub_admin|editor|viewer) match Plan 01-01 migration 0006 CHECK. 3 endpoint routers/users.py refactor (POST create + PATCH role + GET list) với assert_hub_admin_for inline sau body parse. T-02-02-E mitigation business logic block `req.role == "admin" and user.role != "admin"` (hub_admin KHÔNG được escalate). HUB_ID_REQUIRED guard cho GET list non-super-admin. 2 endpoint (PATCH status + DELETE) GIỮ require_role super-only (D-V3.1-Phase2-E LOCKED cross-hub op). 5 integration test PASS scenario #1-5 ROADMAP success criteria #2.
- **Plan 02-04** (DEP-05): Helper `build_audit_payload(*, actor_role, actor_hub_id, extra)` ở audit_service.py — D-V3.1-Phase2-C LOCKED nest vào payload JSONB existing KHÔNG schema migration. 5 service callsite refactor (user_service.create + delete; hub_service.create + update + update_status) signature thêm actor_role + actor_hub_id keyword-only. 5 router callsite derive `actor_role = "admin" if user.role == "admin" else "hub_admin"` + `actor_hub_id = None if super else req.hub_id`. 4 unit test + 2 integration test PASS — verify `payload->>'actor_role'` + `payload->>'actor_hub_id'` queryable cho forensic.
- **Plan 02-05** (closeout): STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md update atomic — Phase 2 DONE 100% 5 REQ-ID consumed. Smoke checkpoint runtime SKIP pre-resolved (carry forward v3.0 Plan 04-07 + 05-06 + 06-05 pattern + Plan 01-03 v3.1) — defer Phase 4 MIGRATE-02 full E2E live runtime.

**Carry forward patterns:**
- Hybrid validator function (KHÔNG Depends factory) cho hub_id body — D-V3.1-Phase2-D LOCKED; áp dụng cho future endpoint per-hub gate khi hub_id KHÔNG ở path param.
- Envelope `HUB_ADMIN_REQUIRED` vs `FORBIDDEN` differentiation — frontend FE-01 sẽ switch UX (Phase 3).
- Audit payload nest (KHÔNG schema migration audit_logs) — D-V3.1-Phase2-C LOCKED carry forward Phase 4 v3.0 D-V3-Phase4-C2 pattern.
- Service signature `*` keyword-only + actor metadata params — Pattern G carry forward; force caller explicit pass tránh positional bug.
- Integration test helper `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` duplicate qua Plan 02-02 + 02-03 + 02-04 — Phase 4 MIGRATE-02 consolidate vào conftest.py nếu muốn DRY (defer).

**R-V3.1-2 MEDIUM mitigation chain Phase 2:**
- Backend filter authoritative GET /api/hubs (Plan 02-02 D-V3.1-Phase2-A LOCKED) — defense in depth.
- assert_hub_admin_for inline check sau body parse (Plan 02-01 + 02-03) — KHÔNG dựa FE để gate per-hub.
- T-02-02-E business logic block role escalation (Plan 02-03 PATCH role handler) — hub_admin KHÔNG được assign role='admin' cho user khác.
- DELETE + PATCH status giữ require_role super-only (Plan 02-03 + D-V3.1-Phase2-E LOCKED) — cross-hub op KHÔNG cho hub_admin.
- Audit forensic chain payload->>'actor_role' + payload->>'actor_hub_id' (Plan 02-04 DEP-05) — incident review filter scope.
- Test coverage 19 test ship (5+3+5+4+2) cover semantic DEP-01..05 — D-V3.1-Phase2-F LOCKED ≥ 80% satisfied.

**Phase 2 backward incompat (W5 iter 1 acknowledgement):**
- hub_admin gọi GET /api/users qua existing M2/v3.0 frontend (chưa pass hub_id query) sẽ thấy 400 HUB_ID_REQUIRED.
- Operator broadcast Slack/Email trước Phase 3 FE-04 ship: existing FE 1-2 ngày downtime trên user management page cho hub_admin role (acceptable v3.1 timeline).
- Phase 3 FE-04 sẽ pass hub_id query → resolve breakage.

**Next:** Phase 3 `/gsd-discuss-phase 3` FE frontend form refactor (UserManagement 3 option radio + hub switcher hide central + edit modal disabled assign super + api.ts UserRole type extend).
```

**Update 2: `Hub_All/.planning/REQUIREMENTS.md`** — mark 5 REQ-ID DEP-01..05:

Tìm 5 dòng (search/replace):
```markdown
- [ ] **DEP-01** Phase 2: Dependency mới `require_hub_admin_for(hub_id)`...
- [ ] **DEP-02** Phase 2: Refactor `GET /api/hubs`...
- [ ] **DEP-03** Phase 2: Endpoint `routers/users.py`...
- [ ] **DEP-04** Phase 2: Endpoint `routers/hubs.py`...
- [ ] **DEP-05** Phase 2: Audit log tag actor scope...
```

→ Đổi `[ ]` → `[x]` cho 5 dòng + append suffix `(DONE 2026-05-24 — Plan 02-XX)`:
```markdown
- [x] **DEP-01** Phase 2: Dependency mới `require_hub_admin_for(hub_id)`... (DONE 2026-05-24 — Plan 02-01)
- [x] **DEP-02** Phase 2: Refactor `GET /api/hubs`... (DONE 2026-05-24 — Plan 02-02)
- [x] **DEP-03** Phase 2: Endpoint `routers/users.py`... (DONE 2026-05-24 — Plan 02-03)
- [x] **DEP-04** Phase 2: Endpoint `routers/hubs.py`... (DONE 2026-05-24 — Plan 02-02)
- [x] **DEP-05** Phase 2: Audit log tag actor scope... (DONE 2026-05-24 — Plan 02-04)
```

**Update 3: `Hub_All/.planning/ROADMAP.md`** — 3 thay đổi:

1. **Table "Phases — v3.1" Phase 2 row** — append column status (verify format actual table; nếu table cũ chỉ 6 column, thêm status note inline):
   - Tìm row: `| **2** | Backend RBAC enforcement (DEP) | ... | DEP-01..05 (5) | 5 | Phase 1 |`
   - Update để indicate DONE (nếu format hiện tại KHÔNG có column status, append inline note hoặc thêm row mới sau format `## Phase Details > ### Phase 2`).

2. **Phase 2 plans checklist** (search section "### Phase 2 — Backend RBAC enforcement (DEP)" > "Plans:"):
   ```
   Plans:
   - [x] 02-01-PLAN.md — require_hub_admin_for validator + 5-case unit test + 403 envelope HUB_ADMIN_REQUIRED (DEP-01) ✅ DONE 2026-05-24
   - [x] 02-02-PLAN.md — Integration test GET /api/hubs filter cả admin + POST hub_admin 403 verify (DEP-02 + DEP-04) ✅ DONE 2026-05-24
   - [x] 02-03-PLAN.md — routers/users.py 3 endpoint refactor scope + UserRole Literal extend (DEP-03) ✅ DONE 2026-05-24
   - [x] 02-04-PLAN.md — Audit payload actor_role + actor_hub_id nest 5 callsite (DEP-05) ✅ DONE 2026-05-24
   - [x] 02-05-PLAN.md — Closeout STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md update ✅ DONE 2026-05-24
   ```

3. **Progress table** — update row v3.1:
   - Tìm row: `| **v3.1 RBAC hub_admin** | **4** | **3/~12** | **4/15** | 🚧 **Phase 1 DONE** | — |`
   - Update: `| **v3.1 RBAC hub_admin** | **4** | **8/~15** | **9/15** | 🚧 **Phase 2 DONE** | — |`

**Update 4: `Hub_All/CLAUDE.md`** — append cuối §6 subsection mới SAU "### Phase 1 v3.1 RBAC schema pattern (ROLE-01..04 — 2026-05-23)":

```markdown
### Phase 2 v3.1 RBAC enforcement pattern (DEP-01..05 — 2026-05-24)

5 plan đóng 5 REQ-ID DEP backend RBAC enforcement + 6 D-V3.1-Phase2-A..F LOCKED:

- **Plan 02-01 DEP-01 — assert_hub_admin_for validator function:** `api/app/auth/dependencies.py` thêm `async def assert_hub_admin_for(*, user, db, target_hub_id) -> None` (hybrid pattern D-V3.1-Phase2-D LOCKED — KHÔNG Depends factory vì hub_id ở body POST/PATCH). Import `get_effective_role` + `UserNotFoundError` từ Plan 01-02 ROLE-04. Logic 5 case: super admin bypass / hub_admin đúng pass / hub_admin sai 403 / viewer 403 / user_not_found defensive catch + 403 (KHÔNG leak 404). Envelope `HUB_ADMIN_REQUIRED` mới (D-V3.1-Phase2-B LOCKED) — KHÔNG reuse FORBIDDEN. 5 unit test pytest PASS với AsyncMock(AsyncSession) factory carry forward Plan 01-02 pattern. Reference: `.planning/phases/02-backend-rbac-enforcement/02-01-PLAN.md` + `02-01-SUMMARY.md`.

- **Plan 02-02 DEP-02 + DEP-04 — GET /api/hubs verify + hubs mutate preserve:** routers/hubs.py UNCHANGED — D-V3.1-Phase2-A LOCKED logic `if user.role == "admin"` line 73-77 ĐÃ ĐÚNG (hub_admin với `users.role='editor'` global rơi else branch line 78-85 → filter user_hubs). 5 endpoint mutate (POST line 94 + PUT line 138 + PATCH status line 168 + GET stats line 198 + GET single line 115) GIỮ `Depends(require_role("admin"))` (DEP-04 LOCKED). Integration test 3 scenario verify hub_admin dmd CHỈ thấy dmd + hub_admin POST → 403 FORBIDDEN (require_role envelope giữ NGUYÊN — KHÔNG đổi HUB_ADMIN_REQUIRED). Helper `_seed_hub_admin_user` + fixture `seed_hubs_dmd_tdt` (Plan 01-01 migration 0006 user_hubs.role TEXT NULL khai thác). Reference: `02-02-PLAN.md` + `02-02-SUMMARY.md`.

- **Plan 02-03 DEP-03 — users.py 3 endpoint refactor + Literal extend:** `schemas/users.py` UserRole Literal extend 4 value (admin|hub_admin|editor|viewer) match migration 0006 CHECK. `routers/users.py` 3 endpoint (POST create + PATCH role + GET list) refactor: `Depends(get_current_user)` thay `Depends(require_role("admin"))` + inline `await assert_hub_admin_for(user, db, target_hub_id=req.hub_id)` sau body parse. T-02-02-E mitigation business logic block `if req.role == "admin" and user.role != "admin"` → 403 HUB_ADMIN_REQUIRED (hub_admin KHÔNG escalate role admin cho user khác). HUB_ID_REQUIRED guard cho GET list non-super-admin (hub_admin BUỘC pass hub_id query). 4 endpoint (PATCH status + DELETE + reset-password + GET single + PUT) GIỮ require_role super-only (D-V3.1-Phase2-E LOCKED cross-hub op). 5 integration test PASS verify scenario #1-5 ROADMAP success criteria #2. Reference: `02-03-PLAN.md` + `02-03-SUMMARY.md`.

- **Plan 02-04 DEP-05 — Audit payload actor_role + actor_hub_id nest 5 callsite:** `services/audit_service.py` thêm `build_audit_payload(*, actor_role, actor_hub_id, extra=None) -> dict[str, Any]` helper — D-V3.1-Phase2-C LOCKED nest vào payload JSONB existing (audit_logs.payload nullable JSONB từ migration 0001) KHÔNG schema migration. 5 service callsite refactor: user_service.create + delete (signature force keyword required) + hub_service.create + update + update_status (signature default 'admin' + None vì DEP-04 LOCKED hub mutate super-only). 5 router callsite derive actor metadata: POST /api/users + DELETE /api/users + POST /api/hubs + PUT /api/hubs + PATCH /api/hubs/status. 4 unit test (pure Python helper) + 2 integration test (poll-based wait `_wait_audit_row` pattern carry forward conftest.py:778-806) verify `payload->>'actor_role'` + `payload->>'actor_hub_id'` queryable. Forensic query Phase 4 MIGRATE-02 hỗ trợ `WHERE payload->>'actor_role' = 'hub_admin' AND payload->>'actor_hub_id' = '<dmd-uuid>'`. Reference: `02-04-PLAN.md` + `02-04-SUMMARY.md`.

- **Plan 02-05 closeout — docs update:** STATE.md frontmatter (`completed_phases: 2` + `completed_plans: 8` + `percent: 53`) + body Phase 2 Results Summary section + REQUIREMENTS.md 5 dòng `[x] **DEP-XX**` + ROADMAP.md table + plans checklist + CLAUDE.md §6 subsection mới (file này). Smoke checkpoint runtime SKIP pre-resolved (auto-fallback `--chain` mode active per Plan 04-07 + 05-06 + 06-05 v3.0 + Plan 01-03 v3.1 carry forward). Evidence chain: 5+3+5+4+2 = 19 test PASS in-process cover semantic DEP-01..05; manual visual smoke + integration test runtime full E2E defer Phase 4 MIGRATE-02 (4 scenario pytest httpx + audit verify). Reference: `02-05-PLAN.md` + `02-05-SUMMARY.md`.

**Backward compat (Phase 2 KHÔNG break v3.0 + v3.1 Phase 1):**
- Schema 0006 carry forward unchanged (Plan 01-01 ship).
- 5 router endpoint M2 + v3.0 envelope `{success, data, error, meta}` shape LOCKED — chỉ thêm error code mới (HUB_ADMIN_REQUIRED) + extend hành vi (HUB_ID_REQUIRED guard GET list).
- `require_role` closure factory KHÔNG đổi — 4 endpoint users + 5 endpoint hubs giữ NGUYÊN.
- `get_effective_role` helper API contract LOCKED Plan 01-02 carry forward.
- audit_logs.payload JSONB existing đủ chứa actor metadata — KHÔNG schema migration.
- **Breaking change (tests cũ):** `user_service.create(req=, created_by=)` + `delete(user_id=, deleted_by=)` + `hub_service.create/update/update_status` signature THÊM keyword-only `actor_role` (user_service required, hub_service default 'admin') + `actor_hub_id`. Existing tests M2 + v3.0 phải update call site — nếu phát hiện regression sau Wave 4 closeout, escalate revision request.

**R-V3.1-2 MEDIUM mitigation chain Phase 2:**
- Backend filter authoritative GET /api/hubs (Plan 02-02 D-V3.1-Phase2-A LOCKED) — defense in depth, KHÔNG dựa FE.
- assert_hub_admin_for inline check sau body parse (Plan 02-01 + 02-03) — production code path enforce per-hub gate.
- T-02-02-E business logic block role escalation (Plan 02-03 PATCH role handler) — hub_admin KHÔNG được assign role='admin' cho user khác.
- DELETE + PATCH status giữ require_role super-only (Plan 02-03 + D-V3.1-Phase2-E LOCKED) — cross-hub op KHÔNG cho hub_admin.
- Audit forensic chain `payload->>'actor_role'` + `payload->>'actor_hub_id'` (Plan 02-04 DEP-05) — incident review filter scope hub_admin operation.
- Test coverage 19 test ship cover semantic DEP-01..05 (D-V3.1-Phase2-F LOCKED ≥ 80% satisfied).
- Phase 4 MIGRATE-02 sẽ verify runtime full E2E 4 scenario pytest httpx + audit log inspect live.

**Phase 2 backward incompat (operator action required — W5 iter 1):**
- hub_admin GET /api/users yêu cầu hub_id query (HUB_ID_REQUIRED guard Plan 02-03 DEP-03).
- Existing M2/v3.0 frontend chưa pass hub_id query → 1-2 ngày downtime trên user management page cho hub_admin role giữa Phase 2 ship và Phase 3 FE-04 ship (acceptable v3.1 timeline).
- **Operator broadcast (Slack/Email) trước Phase 3 FE-04 ship:** thông báo hub_admin user về temporary breakage + ETA Phase 3 FE-04.
- Phase 3 FE-04 sẽ pass hub_id query → resolve.

**Next:** Phase 3 `/gsd-discuss-phase 3` FE frontend form refactor (UserManagement 3 option radio + hub switcher hide central + edit modal disabled assign super + api.ts UserRole type extend).
```

**Lưu ý quan trọng:**
- 4 file update atomic trong 1 commit (Plan 02-05 closeout).
- KHÔNG đụng section khác của STATE.md / REQUIREMENTS.md / ROADMAP.md / CLAUDE.md (preserve M2 + v3.0 + v3.1 Phase 1 content nguyên).
- Tiếng Việt có dấu cho mọi description text (CLAUDE.md convention).
- Smoke checkpoint auto-skip per `--chain` mode active (carry forward Plan 04-07 v3.0 + Plan 01-03 v3.1 pattern).
- KHÔNG insert section MỚI lên trên các subsection v3.0 existing — append cuối CLAUDE.md §6 + STATE.md ngoài sections existing.
- Test fixture (verify) — last task acceptance criteria run grep verify sau update.
  </action>
  <verify>
    <automated>grep -c "completed_plans: 8" Hub_All/.planning/STATE.md && grep -c "Phase 2 Results Summary" Hub_All/.planning/STATE.md && grep -c "\\[x\\] \\*\\*DEP-01\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\\[x\\] \\*\\*DEP-02\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\\[x\\] \\*\\*DEP-03\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\\[x\\] \\*\\*DEP-04\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "\\[x\\] \\*\\*DEP-05\\*\\*" Hub_All/.planning/REQUIREMENTS.md && grep -c "Phase 2 v3.1 RBAC enforcement pattern" Hub_All/CLAUDE.md && grep -c "\\[x\\] 02-01-PLAN.md" Hub_All/.planning/ROADMAP.md && grep -c "\\[x\\] 02-05-PLAN.md" Hub_All/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/.planning/STATE.md` frontmatter chứa `completed_phases: 2` (bump 1 → 2).
    - `Hub_All/.planning/STATE.md` frontmatter chứa `completed_plans: 8` (bump 3 → 8).
    - `Hub_All/.planning/STATE.md` frontmatter chứa `percent: 53` (8/15 round).
    - `Hub_All/.planning/STATE.md` frontmatter chứa `next_action:` mention `/gsd-discuss-phase 3` HOẶC `FE frontend form refactor`.
    - `Hub_All/.planning/STATE.md` body chứa section `## Phase 2 Results Summary (DONE 2026-05-24)` với 5 bullet plan (Plan 02-01..05).
    - `Hub_All/.planning/STATE.md` body Phase 2 Results section chứa mention `D-V3.1-Phase2-A` + `D-V3.1-Phase2-B` + `D-V3.1-Phase2-C` + `D-V3.1-Phase2-D` + `D-V3.1-Phase2-E` + `D-V3.1-Phase2-F` (6 LOCKED decisions traceability).
    - `Hub_All/.planning/STATE.md` body Phase 2 Results section chứa `R-V3.1-2 MEDIUM mitigation chain Phase 2` heading.
    - `Hub_All/.planning/STATE.md` body table Phase 2 row có `✅ DONE 2026-05-24` HOẶC equivalent status indicator.
    - `Hub_All/.planning/REQUIREMENTS.md` có 5 dòng `- [x] **DEP-XX**` (X = 01, 02, 03, 04, 05).
    - `Hub_All/.planning/REQUIREMENTS.md` mỗi dòng DEP marked có suffix `(DONE 2026-05-24 — Plan 02-XX)`.
    - `Hub_All/.planning/ROADMAP.md` chứa `[x] 02-01-PLAN.md` + `[x] 02-02-PLAN.md` + `[x] 02-03-PLAN.md` + `[x] 02-04-PLAN.md` + `[x] 02-05-PLAN.md` (5 plan checklist marked).
    - `Hub_All/.planning/ROADMAP.md` progress table row v3.1 chứa `8/~15` HOẶC `Phase 2 DONE` (status indicator).
    - `Hub_All/CLAUDE.md` chứa subsection `### Phase 2 v3.1 RBAC enforcement pattern (DEP-01..05 — 2026-05-24)`.
    - `Hub_All/CLAUDE.md` subsection mới chứa 5 bullet plan (Plan 02-01..05) + Backward compat + R-V3.1-2 mitigation chain + Next pointer.
    - `Hub_All/CLAUDE.md` subsection mới ĐẶT SAU `### Phase 1 v3.1 RBAC schema pattern` (preserve order; KHÔNG xen vào giữa Phase 1).
    - 4 file update KHÔNG đụng section v3.0 / M2 existing (verify qua git diff CHỈ thấy thay đổi trong v3.1 sections).
    - W5 iter 1 acknowledgement: Hub_All/CLAUDE.md chứa string 'HUB_ID_REQUIRED' HOẶC 'operator broadcast' HOẶC 'W5 iter 1' (operator broadcast note).
    - W5 iter 1 acknowledgement: Hub_All/.planning/STATE.md chứa string 'HUB_ID_REQUIRED' HOẶC 'backward incompat' HOẶC 'W5 iter 1' (FE breakage acknowledgement).
  </acceptance_criteria>
  <done>
    4 file docs update atomic (STATE.md frontmatter + body Results section; REQUIREMENTS.md 5 [x]; ROADMAP.md 5 plan + progress; CLAUDE.md subsection mới); progress reflect Phase 2 DONE 8/15 plan; 5 REQ-ID DEP-01..05 marked [x]; pattern reference cho Phase 3 đầy đủ.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 02-05 closeout → 4 docs file | Local repo files — KHÔNG có trust boundary external; chỉ git commit history trace |
| Docs update → developer Phase 3 onward | Subsection v3.1 Phase 2 pattern reference cho future developer carry forward (Phase 3 FE refactor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Closeout docs xoá hoặc đè len section v3.0 / M2 existing | mitigate | Acceptance criteria verify atomic update CHỈ trong v3.1 sections (grep target subsection mới + KHÔNG remove existing markers Phase 1 v3.1 + v3.0 progress); git diff review trước commit. |
| T-02-05-02 | Repudiation | Docs update KHÔNG trace ai update khi nào | accept | Git commit history đủ trace — commit prefix `chore(02-05): closeout Phase 2 v3.1 DEP backend RBAC enforcement` + body tiếng Việt nói tại sao. |
| T-02-05-03 | Integrity | STATE.md + ROADMAP.md merge conflict với parallel plan future | accept | Plan 02-05 ở Wave 4 (sequential SAU 02-01..04 Wave 1-3 ship); KHÔNG có parallel writer cùng file ở Phase 2; Phase 3 sẽ touch STATE.md riêng (sequential). |
| T-02-05-04 | DoS | Smoke checkpoint chặn closeout vì runtime infra chưa sẵn | mitigate | Auto-fallback `skip smoke` per `--chain` mode active (Plan 04-07 + 05-06 + 06-05 v3.0 + Plan 01-03 v3.1 carry forward pattern); manual smoke defer Phase 4 MIGRATE-02 full E2E. |

</threat_model>

<verification>
- STATE.md frontmatter `completed_phases: 2` + `completed_plans: 8` + `percent: 53` + next_action update đúng.
- STATE.md body Phase 2 Results Summary section append đúng vị trí (sau Phase 1).
- REQUIREMENTS.md 5 dòng DEP-01..05 marked [x] + suffix Plan ID.
- ROADMAP.md Phase 2 plans checklist 5 marked + progress table row v3.1 update.
- CLAUDE.md §6 subsection mới append sau Phase 1 v3.1 subsection (preserve order chronological).
- 4 file update KHÔNG đụng section v3.0 / M2 existing (atomic scope).
- Smoke checkpoint auto-skip per --chain mode (KHÔNG block closeout).
- Phase 3 sẵn sàng start qua `/gsd-discuss-phase 3`.
</verification>

<success_criteria>
1. STATE.md progress phản ánh Phase 2 DONE 8/15 plan + 9/15 REQ-ID consumed.
2. REQUIREMENTS.md mark 5 REQ-ID DEP-01..05 [x] + Plan suffix.
3. ROADMAP.md table Phase 2 + 5 plans checklist DONE.
4. CLAUDE.md subsection v3.1 Phase 2 pattern reference đầy đủ + R-V3.1-2 mitigation + Next.
5. 4 file update atomic 1 commit — KHÔNG break section existing.
6. Phase 3 sẵn sàng start `/gsd-discuss-phase 3`.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-05-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 4 file docs update atomic — Phase 2 DONE 100% 5 REQ-ID DEP-01..05 consumed
- STATE.md frontmatter + body Results section + table row update
- REQUIREMENTS.md 5 dòng [x] + Plan suffix
- ROADMAP.md table + plans checklist + progress table
- CLAUDE.md subsection v3.1 Phase 2 pattern reference cho Phase 3+ developer
- Smoke checkpoint auto-skip per --chain mode (carry forward v3.0 + v3.1 Phase 1 pattern)
- Evidence chain: 19 test ship (5 unit assert_hub_admin_for + 3 integration hubs.py scope + 5 integration users.py CRUD + 4 unit audit payload + 2 integration audit metadata) cover semantic DEP-01..05
- Manual visual smoke runtime defer Phase 4 MIGRATE-02 full E2E 4 scenario pytest httpx + audit log inspect live
- Carry forward Plan 01-03 v3.1 closeout pattern (DSN injection lesson đã merge vào CLAUDE.md Phase 1 subsection — Phase 2 KHÔNG có pattern mới đáng note)
- Phase 3 sẵn sàng `/gsd-discuss-phase 3` FE frontend form refactor

Commit atomic: `chore(02-05): closeout Phase 2 v3.1 DEP backend RBAC enforcement` + body tiếng Việt nói tại sao (5 plan ship 5 REQ-ID DEP-01..05; 4 docs update atomic; smoke skip --chain mode; pattern reference cho Phase 3+ developer; Phase 2 DONE 100%).
</output>
</content>
</invoke>