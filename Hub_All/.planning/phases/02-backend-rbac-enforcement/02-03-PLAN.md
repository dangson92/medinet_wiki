---
phase: 02-backend-rbac-enforcement
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api/app/schemas/users.py
  - api/app/routers/users.py
  - api/tests/integration/test_dep_users_scope.py
autonomous: true
requirements: [DEP-03]
must_haves:
  truths:
    - "Pydantic schema `UserRole` Literal extend thêm `'hub_admin'` (4 value: admin|hub_admin|editor|viewer) — match Plan 01-01 migration 0006 CHECK constraint"
    - "4 endpoint `routers/users.py` refactor scope: POST create + GET list + PATCH role + DELETE — replace `Depends(require_role('admin'))` thành `Depends(get_current_user)` + inline `await assert_hub_admin_for(...)` cho 3 endpoint có hub_id trong body (POST create + PATCH role + GET list query); DELETE branch single-hub vs cross-hub (B1 iter 1 fix DEP-03 spec compliance: hub_admin xoá user thuộc CHỈ hub mình OK; user thuộc nhiều hub → super admin only); PATCH status giữ `require_role('admin')` super-only (D-V3.1-Phase2-E LOCKED giữ cross-hub op)"
    - "Sau khi merge, hub_admin dmd POST /api/users (hub_id=dmd) → 201; POST (hub_id=tdt) → 403 HUB_ADMIN_REQUIRED"
    - "Sau khi merge, hub_admin dmd PATCH /api/users/:id/role (hub_id=dmd) → 200; PATCH role với role='admin' transition → 403 HUB_ADMIN_REQUIRED (T-02-02-E block role escalation)"
    - "Sau khi merge, super admin POST /api/users any hub → 201 (bypass)"
    - "Sau khi merge, hub_admin dmd DELETE user thuộc CHỈ hub dmd → 200; hub_admin dmd DELETE user thuộc dmd+tdt (cross-hub) → 403 CROSS_HUB_USER_DELETE_DENIED; hub_admin dmd DELETE user thuộc CHỈ tdt → 403 HUB_ADMIN_REQUIRED (B1 iter 1 DEP-03 spec compliance)"
    - "Integration test 7 scenario PASS — verify success criteria #2 ROADMAP Phase 2 (5 cũ + 2 mới B1)"
  artifacts:
    - path: "api/app/schemas/users.py"
      provides: "UserRole Literal extend 'hub_admin' (4 value) — match migration 0006 CHECK"
      contains: 'UserRole = Literal["admin", "hub_admin", "editor", "viewer"]'
    - path: "api/app/routers/users.py"
      provides: "4 endpoint refactor (POST/PATCH role/GET list + DELETE branch single-hub vs cross-hub); 1 endpoint (PATCH status) giữ require_role('admin') super-only"
      contains: "CROSS_HUB_USER_DELETE_DENIED"
    - path: "api/tests/integration/test_dep_users_scope.py"
      provides: "Integration test 7 scenario — hub_admin dmd CRUD + super admin bypass + role escalation block + DELETE branch (single-hub OK, cross-hub denied, other-hub denied)"
      contains: "async def test_"
  key_links:
    - from: "routers/users.py::create_user"
      to: "assert_hub_admin_for(user, db, target_hub_id=req.hub_id)"
      via: "inline await sau body parse Pydantic CreateUserRequest"
      pattern: 'await assert_hub_admin_for\(user=user, db=db, target_hub_id=req.hub_id\)'
    - from: "routers/users.py::change_user_role"
      to: "assert_hub_admin_for + block role='admin' transition"
      via: "inline check role escalation TRƯỚC service.change_role"
      pattern: 'if req.role == "admin" and user.role != "admin"'
    - from: "routers/users.py::delete_user"
      to: "branch single-hub (assert_hub_admin_for) vs cross-hub (CROSS_HUB_USER_DELETE_DENIED)"
      via: "Query user_hubs membership target user TRƯỚC delete; super admin bypass; hub_admin chỉ pass nếu target user thuộc CHỈ 1 hub VÀ hub đó là hub mà caller hub_admin"
      pattern: "CROSS_HUB_USER_DELETE_DENIED"
    - from: "test_dep_users_scope.py"
      to: "POST/PATCH/DELETE /api/users + assert envelope HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED"
      via: "httpx.AsyncClient + Authorization Bearer + JSON body assertion"
      pattern: 'auth_client.post\(.*/api/users'
---

<objective>
DEP-03: Refactor 4 endpoint `routers/users.py` CRUD scope hub_admin — super admin pass; hub_admin check qua `assert_hub_admin_for(target_hub_id)` inline sau body parse. DELETE handler có branching đặc biệt theo B1 iter 1 fix (REQUIREMENTS.md DEP-03 spec compliance + D-V3.1-Phase2-E narrowing).

**Revision iter 1 — B1 fix (DELETE single-hub vs cross-hub branch):** REQUIREMENTS.md DEP-03 line 24 literal include `DELETE /api/users/:id` trong hub_admin scope. CONTEXT.md D-V3.1-Phase2-E narrows correctly: "user target có thể thuộc nhiều hub; hub_admin của hub A xoá user thuộc CHỈ hub A → OK; user thuộc cả hub A + hub B → CẦN super admin." DELETE endpoint MỚI refactor từ `require_role("admin")` sang `get_current_user` + branching logic:
1. Super admin → pass.
2. Hub_admin: query user_hubs target user → nếu thuộc CHỈ 1 hub VÀ hub đó = caller's hub_admin scope → pass.
3. Hub_admin: target thuộc nhiều hub → 403 envelope `CROSS_HUB_USER_DELETE_DENIED` (code MỚI).
4. Hub_admin: target thuộc 1 hub khác → 403 `HUB_ADMIN_REQUIRED` (assert_hub_admin_for raise).

**Purpose (DEP-03 + D-V3.1-Phase2-E + B1 iter 1 LOCKED):**

4 endpoint `users.py` (POST create line 90, GET list line 56, PATCH role line 158, DELETE line 208) hiện tại `Depends(require_role("admin"))` → hub_admin KHÔNG dùng được.

**Refactor strategy (CONTEXT.md §"D-V3.1-Phase2-D/E" + PATTERNS.md §"#3 routers/users.py" + B1 iter 1):**

| Endpoint | hub_id source | Refactor |
|----------|---------------|----------|
| POST /api/users (create) | `req.hub_id` body | `Depends(get_current_user)` + `await assert_hub_admin_for(user, db, target_hub_id=req.hub_id)` |
| GET /api/users (list) | `hub_id` query optional | `Depends(get_current_user)` + nếu hub_admin + `hub_id` query có → `await assert_hub_admin_for`; nếu KHÔNG hub_id query → 400 HUB_ID_REQUIRED (hub_admin BUỘC pass hub_id query) |
| PATCH /api/users/:id/role | `req.hub_id` body | `Depends(get_current_user)` + `await assert_hub_admin_for(user, db, target_hub_id=req.hub_id)` + **business logic block** `if req.role == "admin" and user.role != "admin"` → 403 (T-02-02-E mitigation) |
| **DELETE /api/users/:id (B1 iter 1)** | KHÔNG hub_id body | `Depends(get_current_user)` + branch: super admin pass; hub_admin query user_hubs target → 1 hub đúng scope pass / nhiều hub → 403 CROSS_HUB_USER_DELETE_DENIED / 1 hub khác → 403 HUB_ADMIN_REQUIRED |
| PATCH /api/users/:id/status | KHÔNG hub_id | GIỮ `Depends(require_role("admin"))` — D-V3.1-Phase2-E LOCKED cross-hub admin op super-only |
| POST /api/users/:id/reset-password | KHÔNG hub_id | GIỮ `Depends(require_role("admin"))` — KHÔNG trong scope DEP-03 (USER-02 M2 carry forward) |
| GET /api/users/:id | KHÔNG hub_id | GIỮ `Depends(require_role("admin"))` — đọc single user out of scope per-hub gate; defer |
| PUT /api/users/:id | KHÔNG hub_id | GIỮ `Depends(require_role("admin"))` — update profile fields KHÔNG có hub_id; defer Phase 3 nếu cần |

Pydantic `UserRole` Literal phải extend `'hub_admin'` để accept role transition + body validation.

**Output:** 3 file change — schemas/users.py 1 line extend Literal; routers/users.py 4 endpoint refactor + 4 endpoint giữ nguyên; test_dep_users_scope.py mới 7 test PASS (5 cũ + 2 mới B1).

**Carry forward patterns:**
- PATTERNS.md §"#3 routers/users.py" refactor template (lines 224-251).
- assert_hub_admin_for từ Plan 02-01 (BLOCKING dependency).
- `_seed_hub_admin_user` helper + `seed_hubs_dmd_tdt` fixture từ Plan 02-02 (DRY).
- Integration test pattern `test_dep_hubs_scope.py` Plan 02-02 (LifespanManager + auth_client).
- `tests/integration/conftest.py:264-284` per-test TRUNCATE (W1 isolation auto-handled bởi app_with_auth).
- W2: hubs table 10 cột reference migration 0001:80-106 + 0003:47-74.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-01-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-02-PLAN.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/routers/users.py
@Hub_All/api/app/schemas/users.py
@Hub_All/api/app/auth/dependencies.py
@Hub_All/api/app/services/user_service.py
@Hub_All/api/tests/integration/conftest.py
@Hub_All/api/migrations/versions/0001_initial_schema.py
@Hub_All/api/migrations/versions/0003_phase5_schema_reconcile.py

<interfaces>
<!-- Pattern source-of-truth — Executor dùng trực tiếp KHÔNG cần explore. -->

Pydantic schema hiện tại (schemas/users.py:20 — extend 4 value):
```python
# HIỆN TẠI (3 value M2 baseline):
UserRole = Literal["admin", "editor", "viewer"]

# PHASE 2 PLAN 02-03 (4 value match migration 0006):
UserRole = Literal["admin", "hub_admin", "editor", "viewer"]
```

POST /api/users handler hiện tại (routers/users.py:90-108):
```python
@router.post("")
async def create_user(
    req: CreateUserRequest,
    request: Request,
    user: User = Depends(require_role("admin")),  # noqa: B008  ← Phase 2 thay
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    request_id = getattr(request.state, "request_id", None)
    try:
        result = await service.create(
            req=req, created_by=user.id, request_id=request_id
        )
    except UserConflictError as e:
        return resp.conflict(message=str(e), code="EMAIL_CONFLICT")
    return resp.created(data=result.model_dump(mode="json"))
```

DELETE /api/users/:id handler SAU REFACTOR B1 iter 1 (Phase 2 Plan 02-03):
```python
@router.delete("/{user_id}")
async def delete_user(
    user_id: str,
    request: Request,
    user: User = Depends(get_current_user),  # noqa: B008  ← B1 iter 1 thay require_role
    db: AsyncSession = Depends(get_session),  # noqa: B008  ← B1 iter 1 NEW cho query user_hubs
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    """DELETE /api/users/:id — Phase 2 Plan 02-03 B1 iter 1 DEP-03 spec compliance.

    Branch:
    - Super admin (user.role='admin' global) → pass (cross-hub by design).
    - Hub_admin: query user_hubs membership target user; nếu target thuộc CHỈ 1
      hub VÀ hub đó = caller's hub_admin scope → pass (assert_hub_admin_for).
    - Hub_admin: target thuộc nhiều hub → 403 CROSS_HUB_USER_DELETE_DENIED.
    - Hub_admin: target thuộc 1 hub khác (KHÔNG phải scope mình) → 403 HUB_ADMIN_REQUIRED
      (qua assert_hub_admin_for raise).

    Self-delete + LastAdmin guard (M2 carry forward).
    """
    try:
        user_uuid = UUID(user_id)
    except ValueError:
        return resp.bad_request(
            message=f"user_id không hợp lệ: {user_id!r}",
            code="INVALID_USER_ID",
        )

    if user_uuid == user.id:
        return resp.forbidden(
            message=(
                "Không thể tự xoá tài khoản của chính mình. "
                "Nhờ admin khác xoá hoặc reset password."
            ),
            code="CANNOT_DELETE_SELF",
        )

    # B1 iter 1 — branch single-hub vs cross-hub (DEP-03 spec compliance).
    if user.role != "admin":
        # Hub_admin path — query user_hubs membership target user.
        membership_rows = (await db.execute(
            text(
                "SELECT hub_id FROM user_hubs WHERE user_id = :target_id"
            ),
            {"target_id": str(user_uuid)},
        )).scalars().all()
        target_hub_ids = [str(h) for h in membership_rows]

        if len(target_hub_ids) > 1:
            # Cross-hub user — chỉ super admin được xoá.
            return resp.forbidden(
                message=(
                    "Chỉ super admin được xoá user thuộc nhiều hub "
                    "(user target có membership ở "
                    f"{len(target_hub_ids)} hub)"
                ),
                code="CROSS_HUB_USER_DELETE_DENIED",
            )

        if len(target_hub_ids) == 1:
            # Single-hub user — hub_admin của hub đó được xoá.
            # assert_hub_admin_for raise 403 HUB_ADMIN_REQUIRED nếu caller
            # KHÔNG phải hub_admin của hub đó.
            await assert_hub_admin_for(
                user=user, db=db, target_hub_id=target_hub_ids[0]
            )
        # len == 0 — target user KHÔNG có hub membership. Hub_admin KHÔNG
        # được xoá (chỉ super admin có quyền xoá orphan user).
        elif len(target_hub_ids) == 0:
            return resp.forbidden(
                message=(
                    "Chỉ super admin được xoá user KHÔNG có hub membership "
                    "(orphan user)"
                ),
                code="HUB_ADMIN_REQUIRED",
            )

    request_id = getattr(request.state, "request_id", None)
    try:
        deleted = await service.delete(
            user_id=user_uuid,
            deleted_by=user.id,
            request_id=request_id,
        )
    except LastAdminError as e:
        return resp.conflict(message=str(e), code="LAST_ADMIN")
    if not deleted:
        return resp.not_found(
            message=f"User {user_id} không tồn tại", code="NOT_FOUND"
        )
    return resp.ok(data={"message": "Đã xoá user"})
```

POST /api/users handler SAU REFACTOR (Phase 2 Plan 02-03):
```python
@router.post("")
async def create_user(
    req: CreateUserRequest,
    request: Request,
    user: User = Depends(get_current_user),  # noqa: B008  ← THAY require_role
    db: AsyncSession = Depends(get_session),  # noqa: B008  ← THÊM cho assert_hub_admin_for
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    # Phase 2 Plan 02-03 DEP-03 — inline scope check sau body parse.
    await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)

    request_id = getattr(request.state, "request_id", None)
    try:
        result = await service.create(
            req=req, created_by=user.id, request_id=request_id
        )
    except UserConflictError as e:
        return resp.conflict(message=str(e), code="EMAIL_CONFLICT")
    return resp.created(data=result.model_dump(mode="json"))
```

PATCH /api/users/:id/role handler SAU REFACTOR (T-02-02-E mitigation):
```python
@router.patch("/{user_id}/role")
async def change_user_role(
    user_id: str,
    req: ChangeUserRoleRequest,
    user: User = Depends(get_current_user),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    # DEP-03 — scope check.
    await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)

    # T-02-02-E mitigation — hub_admin KHÔNG được escalate role='admin' cho user khác.
    if req.role == "admin" and user.role != "admin":
        return resp.forbidden(
            message="Hub_admin KHÔNG được gán role 'admin' (super admin) cho user khác",
            code="HUB_ADMIN_REQUIRED",
        )

    try:
        user_uuid = UUID(user_id)
    except ValueError:
        return resp.bad_request(
            message=f"user_id không hợp lệ: {user_id!r}",
            code="INVALID_USER_ID",
        )
    ok = await service.change_role(
        user_id=user_uuid, hub_id=req.hub_id, role=req.role
    )
    if not ok:
        return resp.not_found(
            message=f"User {user_id} không tồn tại", code="NOT_FOUND"
        )
    return resp.ok(data={"message": "Cập nhật vai trò thành công"})
```

GET /api/users (list) handler SAU REFACTOR:
```python
@router.get("")
async def list_users(
    hub_id: str | None = None,
    role: str | None = None,
    status: str | None = None,
    search: str | None = None,
    page: int = 1,
    per_page: int = 20,
    user: User = Depends(get_current_user),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    # DEP-03 — super admin bypass, hub_admin check + auto-filter.
    if user.role != "admin":
        # Hub_admin path — hub_id query REQUIRED + assert_hub_admin_for.
        if hub_id is None:
            return resp.bad_request(
                message=(
                    "hub_id query REQUIRED cho non-super-admin "
                    "(hub_admin chỉ list user trong hub mình)"
                ),
                code="HUB_ID_REQUIRED",
            )
        await assert_hub_admin_for(user=user, db=db, target_hub_id=hub_id)
    capped_per_page = max(1, min(per_page, 100))
    capped_page = max(1, page)
    items, total = await service.list(
        hub_id=hub_id, role=role, status=status, search=search,
        page=capped_page, per_page=capped_per_page,
    )
    return resp.paginated(
        items=[i.model_dump(mode="json") for i in items],
        page=capped_page, per_page=capped_per_page, total=total,
    )
```

Imports cần thêm ở routers/users.py top:
```python
from app.auth.dependencies import (
    assert_hub_admin_for,  # ← Phase 2 Plan 02-01 NEW
    get_current_user,      # ← Phase 2 Plan 02-03 NEW (replace require_role)
    get_redis,
    require_role,          # GIỮ — vẫn dùng cho PATCH status + reset-password + GET single + PUT
)
from sqlalchemy import text  # ← B1 iter 1 NEW cho query user_hubs membership
from sqlalchemy.ext.asyncio import AsyncSession  # ĐÃ có line 27
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Pydantic UserRole Literal + refactor 4 router endpoint (POST create + PATCH role + GET list + DELETE branch B1 iter 1) cho hub_admin scope (DEP-03 D-V3.1-Phase2-D/E)</name>
  <read_first>
    - Hub_All/api/app/schemas/users.py:20 (UserRole Literal hiện tại 3 value — extend 4)
    - Hub_All/api/app/routers/users.py:1-44 (import block — verify thêm assert_hub_admin_for + get_current_user + text)
    - Hub_All/api/app/routers/users.py:56-108 (GET list + POST create — refactor scope)
    - Hub_All/api/app/routers/users.py:158-181 (PATCH role — refactor + T-02-02-E block escalation)
    - Hub_All/api/app/routers/users.py:184-254 (PATCH status giữ require_role super-only; DELETE — B1 iter 1 refactor sang branch single-hub vs cross-hub)
    - Hub_All/api/app/auth/dependencies.py (verify Plan 02-01 đã ship `assert_hub_admin_for`)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md §"D-V3.1-Phase2-D/E + Test scenario reference + B1 iter 1 DELETE branch"
  </read_first>
  <behavior>
    - Test 1 (schemas Literal): `UserRole.__args__ == ("admin", "hub_admin", "editor", "viewer")` (Plan 01-01 migration 0006 match).
    - Test 2 (POST hub_admin dmd hub_id=dmd): hub_admin role=editor + user_hubs.role='hub_admin' dmd, POST /api/users body{role:'viewer',hub_id:dmd} → 201.
    - Test 3 (POST hub_admin dmd hub_id=tdt): hub_admin dmd POST /api/users body{role:'viewer',hub_id:tdt} → 403 + envelope code=HUB_ADMIN_REQUIRED.
    - Test 4 (PATCH role escalation block): hub_admin dmd PATCH /api/users/:id/role body{role:'admin',hub_id:dmd} → 403 + envelope code=HUB_ADMIN_REQUIRED (T-02-02-E).
    - Test 5 (DELETE single-hub-of-own pass): hub_admin dmd DELETE user-thuộc-CHỈ-dmd → 200 (B1 iter 1).
    - Test 6 (DELETE cross-hub denied): hub_admin dmd DELETE user-thuộc-dmd+tdt → 403 + envelope code=CROSS_HUB_USER_DELETE_DENIED (B1 iter 1 mới).
    - Test 7 (DELETE other-hub denied): hub_admin dmd DELETE user-thuộc-CHỈ-tdt → 403 + envelope code=HUB_ADMIN_REQUIRED (B1 iter 1 từ assert_hub_admin_for raise).
  </behavior>
  <files>api/app/schemas/users.py, api/app/routers/users.py</files>
  <action>
**STEP 1 — Extend UserRole Literal ở `api/app/schemas/users.py:20`:**

```python
# Phase 2 Plan 02-03 DEP-03 — extend 'hub_admin' match Plan 01-01 migration 0006
# CHECK constraint 4 value (admin|hub_admin|editor|viewer).
UserRole = Literal["admin", "hub_admin", "editor", "viewer"]
```

Đổi 1 dòng. Update docstring comment `# Role enum — match users.role CHECK constraint role_enum` → `# Role enum — 4 value match Plan 01-01 migration 0006 CHECK constraint (D-V3.1-01 LOCKED).`.

**STEP 2 — Update imports ở `api/app/routers/users.py:29` (line ~29):**

Existing:
```python
from app.auth.dependencies import get_redis, require_role
```

REPLACE bằng:
```python
from app.auth.dependencies import (
    assert_hub_admin_for,   # Phase 2 Plan 02-01 — DEP-01 validator function
    get_current_user,       # Phase 2 Plan 02-03 — replace require_role cho 4 endpoint
    get_redis,
    require_role,           # GIỮ — vẫn dùng cho PATCH status + reset-password + GET single + PUT
)
from sqlalchemy import text  # ← B1 iter 1 cho DELETE query user_hubs membership
```

Verify `from sqlalchemy.ext.asyncio import AsyncSession` đã có (line 27); KHÔNG cần thêm.

**STEP 3 — Refactor POST /api/users handler (line ~90-108):**

Replace toàn bộ function `create_user`:

```python
@router.post("")
async def create_user(
    req: CreateUserRequest,
    request: Request,
    user: User = Depends(get_current_user),  # noqa: B008 — Phase 2 thay require_role("admin")
    db: AsyncSession = Depends(get_session),  # noqa: B008 — Phase 2 cho assert_hub_admin_for
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    """POST /api/users — create user, scope per role (Phase 2 DEP-03 D-V3.1-Phase2-D LOCKED).

    Super admin → tạo user any hub. Hub_admin → CHỈ tạo user trong hub được gán
    quyền (assert_hub_admin_for gate). Email trùng → 409 EMAIL_CONFLICT (T-05-04-07).
    """
    # DEP-03 — inline scope check sau body parse (hybrid pattern D-V3.1-Phase2-D LOCKED).
    await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)

    request_id = getattr(request.state, "request_id", None)
    try:
        result = await service.create(
            req=req, created_by=user.id, request_id=request_id
        )
    except UserConflictError as e:
        return resp.conflict(message=str(e), code="EMAIL_CONFLICT")
    return resp.created(data=result.model_dump(mode="json"))
```

**STEP 4 — Refactor PATCH /api/users/:id/role handler (line ~158-181) + T-02-02-E mitigation:**

Replace toàn bộ function `change_user_role`:

```python
@router.patch("/{user_id}/role")
async def change_user_role(
    user_id: str,
    req: ChangeUserRoleRequest,
    user: User = Depends(get_current_user),  # noqa: B008 — Phase 2 thay require_role
    db: AsyncSession = Depends(get_session),  # noqa: B008
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    """PATCH /api/users/:id/role — đổi role + hub assignment (Phase 2 DEP-03).

    Super admin → đổi role any hub any user. Hub_admin → CHỈ đổi role trong hub
    được gán quyền + KHÔNG được escalate role='admin' (T-02-02-E mitigation).
    """
    # DEP-03 — scope check.
    await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)

    # T-02-02-E mitigation — hub_admin KHÔNG được gán role='admin' (super) cho user khác.
    if req.role == "admin" and user.role != "admin":
        return resp.forbidden(
            message=(
                "Hub_admin KHÔNG được gán role 'admin' (super admin toàn hệ thống) "
                "cho user khác — chỉ super admin mới có quyền này"
            ),
            code="HUB_ADMIN_REQUIRED",
        )

    try:
        user_uuid = UUID(user_id)
    except ValueError:
        return resp.bad_request(
            message=f"user_id không hợp lệ: {user_id!r}",
            code="INVALID_USER_ID",
        )
    ok = await service.change_role(
        user_id=user_uuid, hub_id=req.hub_id, role=req.role
    )
    if not ok:
        return resp.not_found(
            message=f"User {user_id} không tồn tại", code="NOT_FOUND"
        )
    return resp.ok(data={"message": "Cập nhật vai trò thành công"})
```

**STEP 5 — Refactor GET /api/users (list) handler (line ~56-87):**

Replace toàn bộ function `list_users`:

```python
@router.get("")
async def list_users(
    hub_id: str | None = None,
    role: str | None = None,
    status: str | None = None,
    search: str | None = None,
    page: int = 1,
    per_page: int = 20,
    user: User = Depends(get_current_user),  # noqa: B008 — Phase 2 thay require_role
    db: AsyncSession = Depends(get_session),  # noqa: B008
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    """GET /api/users — list phân trang + filter (Phase 2 DEP-03).

    Super admin → list any user (hub_id query optional). Hub_admin → REQUIRED
    hub_id query + assert_hub_admin_for gate (chỉ list user trong hub mình).

    Cap per_page ≤ 100 + page ≥ 1 (DoS — pagination cap).
    """
    # DEP-03 — super admin bypass, hub_admin REQUIRED hub_id + scope check.
    if user.role != "admin":
        if hub_id is None:
            return resp.bad_request(
                message=(
                    "hub_id query REQUIRED cho non-super-admin — "
                    "hub_admin chỉ list user trong hub được gán quyền"
                ),
                code="HUB_ID_REQUIRED",
            )
        await assert_hub_admin_for(user=user, db=db, target_hub_id=hub_id)

    capped_per_page = max(1, min(per_page, 100))
    capped_page = max(1, page)
    items, total = await service.list(
        hub_id=hub_id, role=role, status=status, search=search,
        page=capped_page, per_page=capped_per_page,
    )
    return resp.paginated(
        items=[i.model_dump(mode="json") for i in items],
        page=capped_page, per_page=capped_per_page, total=total,
    )
```

**STEP 6 — Refactor DELETE /api/users/:id handler (line ~208-254) — B1 iter 1 branch single-hub vs cross-hub:**

Replace toàn bộ function `delete_user`:

```python
@router.delete("/{user_id}")
async def delete_user(
    user_id: str,
    request: Request,
    user: User = Depends(get_current_user),  # noqa: B008 — B1 iter 1 thay require_role
    db: AsyncSession = Depends(get_session),  # noqa: B008 — B1 iter 1 cho query user_hubs
    service: UserService = Depends(get_user_service),  # noqa: B008
) -> JSONResponse:
    """DELETE /api/users/:id — Phase 2 Plan 02-03 B1 iter 1 DEP-03 spec compliance.

    Branch (D-V3.1-Phase2-E narrowed bởi B1 iter 1):
    - Super admin (user.role='admin' global) → pass (cross-hub by design).
    - Hub_admin: query user_hubs membership target user.
      * Target thuộc CHỈ 1 hub VÀ hub đó = caller's hub_admin scope
        (assert_hub_admin_for pass) → pass.
      * Target thuộc nhiều hub → 403 CROSS_HUB_USER_DELETE_DENIED.
      * Target thuộc 1 hub khác (KHÔNG phải scope mình)
        → 403 HUB_ADMIN_REQUIRED (qua assert_hub_admin_for raise).
      * Target KHÔNG có hub membership (orphan) → 403 HUB_ADMIN_REQUIRED
        (chỉ super admin được xoá orphan).

    Block:
    - Self-delete → 403 CANNOT_DELETE_SELF (self-DoS prevention).
    - Last active admin → 409 LAST_ADMIN.
    """
    try:
        user_uuid = UUID(user_id)
    except ValueError:
        return resp.bad_request(
            message=f"user_id không hợp lệ: {user_id!r}",
            code="INVALID_USER_ID",
        )

    if user_uuid == user.id:
        return resp.forbidden(
            message=(
                "Không thể tự xoá tài khoản của chính mình. "
                "Nhờ admin khác xoá hoặc reset password."
            ),
            code="CANNOT_DELETE_SELF",
        )

    # B1 iter 1 — branch single-hub vs cross-hub (DEP-03 spec compliance +
    # D-V3.1-Phase2-E narrowing).
    if user.role != "admin":
        # Hub_admin path — query user_hubs membership target user.
        result = await db.execute(
            text(
                "SELECT hub_id FROM user_hubs WHERE user_id = :target_id"
            ),
            {"target_id": str(user_uuid)},
        )
        target_hub_ids = [str(h) for h in result.scalars().all()]

        if len(target_hub_ids) > 1:
            # Cross-hub user — chỉ super admin được xoá.
            return resp.forbidden(
                message=(
                    "Chỉ super admin được xoá user thuộc nhiều hub "
                    f"(user target có membership ở {len(target_hub_ids)} hub)"
                ),
                code="CROSS_HUB_USER_DELETE_DENIED",
            )

        if len(target_hub_ids) == 1:
            # Single-hub user — hub_admin của hub đó được xoá.
            # assert_hub_admin_for raise 403 HUB_ADMIN_REQUIRED nếu caller
            # KHÔNG phải hub_admin của hub đó.
            await assert_hub_admin_for(
                user=user, db=db, target_hub_id=target_hub_ids[0]
            )
        else:
            # len == 0 — orphan user, chỉ super admin được xoá.
            return resp.forbidden(
                message=(
                    "Chỉ super admin được xoá user KHÔNG có hub membership "
                    "(orphan user)"
                ),
                code="HUB_ADMIN_REQUIRED",
            )

    request_id = getattr(request.state, "request_id", None)
    try:
        deleted = await service.delete(
            user_id=user_uuid,
            deleted_by=user.id,
            request_id=request_id,
        )
    except LastAdminError as e:
        return resp.conflict(message=str(e), code="LAST_ADMIN")
    if not deleted:
        return resp.not_found(
            message=f"User {user_id} không tồn tại", code="NOT_FOUND"
        )
    return resp.ok(data={"message": "Đã xoá user"})
```

**STEP 7 — GIỮ NGUYÊN 4 endpoint còn lại (D-V3.1-Phase2-E LOCKED + scope decision):**

KHÔNG sửa:
- `GET /api/users/:id` (line 111-131) — `Depends(require_role("admin"))` GIỮ NGUYÊN (read single out of scope DEP-03; defer Phase 3 nếu cần).
- `PUT /api/users/:id` (line 134-155) — `Depends(require_role("admin"))` GIỮ NGUYÊN (update profile fields không có hub_id; defer Phase 3 nếu hub_admin cần edit profile user trong hub mình).
- `PATCH /api/users/:id/status` (line 184-205) — `Depends(require_role("admin"))` GIỮ NGUYÊN (D-V3.1-Phase2-E cross-hub op super-only).
- `POST /api/users/:id/reset-password` (line 257-286) — `Depends(require_role("admin"))` GIỮ NGUYÊN (USER-02 M2 carry forward — KHÔNG trong scope DEP-03).

**Lưu ý quan trọng:**
- 4 endpoint refactor (POST create + PATCH role + GET list + DELETE B1 iter 1).
- Comments tiếng Việt có dấu cho mọi text mới (CLAUDE.md convention).
- T-02-02-E mitigation: PATCH role check `req.role == "admin" and user.role != "admin"` → BLOCK escalation; envelope code HUB_ADMIN_REQUIRED (KHÔNG FORBIDDEN — để frontend FE-01 phân biệt UX).
- GET list: hub_id query OPTIONAL cho super admin (M2 baseline), REQUIRED cho hub_admin (DEP-03 enforce). Envelope code `HUB_ID_REQUIRED` (400 bad_request).
- DELETE B1 iter 1: envelope code `CROSS_HUB_USER_DELETE_DENIED` MỚI cho cross-hub user case; `HUB_ADMIN_REQUIRED` reuse cho single-hub-other-scope + orphan case.
- KHÔNG đụng service layer (user_service.py) — signature giữ NGUYÊN; Plan 02-04 sẽ refactor signature thêm `actor_role` + `actor_hub_id` (DEP-05 audit).
  </action>
  <verify>
    <automated>grep -c "UserRole = Literal" Hub_All/api/app/schemas/users.py && grep -c "await assert_hub_admin_for" Hub_All/api/app/routers/users.py && grep -c "HUB_ADMIN_REQUIRED" Hub_All/api/app/routers/users.py && grep -c "CROSS_HUB_USER_DELETE_DENIED" Hub_All/api/app/routers/users.py && grep -c "from app.auth.dependencies import" Hub_All/api/app/routers/users.py && python -c "import ast; ast.parse(open('Hub_All/api/app/routers/users.py').read()); ast.parse(open('Hub_All/api/app/schemas/users.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/api/app/schemas/users.py` chứa exact string `UserRole = Literal["admin", "hub_admin", "editor", "viewer"]` (4 value).
    - `Hub_All/api/app/routers/users.py` chứa import `assert_hub_admin_for` + `get_current_user` từ `app.auth.dependencies`.
    - `Hub_All/api/app/routers/users.py` chứa import `from sqlalchemy import text` (B1 iter 1 cho DELETE query user_hubs).
    - `Hub_All/api/app/routers/users.py` chứa `await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)` ≥ 2 lần (POST create + PATCH role).
    - `Hub_All/api/app/routers/users.py` chứa `await assert_hub_admin_for(user=user, db=db, target_hub_id=hub_id)` ≥ 1 lần (GET list).
    - `Hub_All/api/app/routers/users.py` chứa `await assert_hub_admin_for(user=user, db=db, target_hub_id=target_hub_ids[0])` ≥ 1 lần (DELETE single-hub branch).
    - `Hub_All/api/app/routers/users.py` chứa `if req.role == "admin" and user.role != "admin":` (T-02-02-E mitigation PATCH role).
    - `Hub_All/api/app/routers/users.py` chứa string `"HUB_ADMIN_REQUIRED"` ≥ 2 lần (envelope code PATCH role escalation block + DELETE orphan).
    - `Hub_All/api/app/routers/users.py` chứa string `"CROSS_HUB_USER_DELETE_DENIED"` ≥ 1 lần (envelope code DELETE cross-hub B1 iter 1).
    - `Hub_All/api/app/routers/users.py` chứa string `"HUB_ID_REQUIRED"` ≥ 1 lần (envelope code GET list hub_admin).
    - `Hub_All/api/app/routers/users.py` chứa `Depends(get_current_user)` ≥ 4 lần (4 endpoint refactor).
    - `Hub_All/api/app/routers/users.py` chứa `Depends(require_role("admin"))` ≥ 3 lần (3 endpoint giữ nguyên: PATCH status + GET single + PUT + reset-password = 4; tối thiểu 3 sau refactor — DELETE đã refactor sang get_current_user theo B1 iter 1).
    - `Hub_All/api/app/routers/users.py` chứa comment hoặc docstring mention `D-V3.1-Phase2-D` HOẶC `D-V3.1-Phase2-E` HOẶC `DEP-03` HOẶC `T-02-02-E` HOẶC `B1 iter 1` (traceability).
    - `Hub_All/api/app/routers/users.py` chứa `SELECT hub_id FROM user_hubs WHERE user_id` (DELETE B1 iter 1 query membership).
    - `python -c "import ast; ast.parse(open('Hub_All/api/app/routers/users.py').read())"` exit 0 (syntax valid).
    - `python -c "import ast; ast.parse(open('Hub_All/api/app/schemas/users.py').read())"` exit 0.
    - `cd Hub_All/api && python -c "from app.schemas.users import UserRole; assert UserRole.__args__ == ('admin', 'hub_admin', 'editor', 'viewer'); print('OK 4 value')"` exit 0.
    - `cd Hub_All/api && python -c "from app.routers.users import router; print('OK router importable')"` exit 0 (verify import chain KHÔNG break).
  </acceptance_criteria>
  <done>
    schemas/users.py UserRole extend 4 value; routers/users.py 4 endpoint refactor (POST create + PATCH role + GET list + DELETE B1 iter 1 branch single-hub vs cross-hub) với assert_hub_admin_for inline + T-02-02-E block escalation + HUB_ID_REQUIRED guard + CROSS_HUB_USER_DELETE_DENIED envelope code mới; 4 endpoint còn lại giữ require_role super-only (D-V3.1-Phase2-E LOCKED narrowed bởi B1 iter 1 cho DELETE).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test test_dep_users_scope.py — 7 scenario verify DEP-03 acceptance (5 cũ + 2 mới B1 iter 1)</name>
  <read_first>
    - Hub_All/api/app/routers/users.py (state sau Task 1 — verify refactor đúng)
    - Hub_All/api/tests/integration/test_dep_hubs_scope.py (Plan 02-02 helper `_seed_hub_admin_user` + fixture `seed_hubs_dmd_tdt` — import DRY)
    - Hub_All/api/tests/integration/conftest.py (fixture auth_client + admin_token + GO_SEED_HASH)
    - Hub_All/api/tests/integration/conftest.py:264-284 (W1 verify — app_with_auth TRUNCATE 8 bảng per-test RESTART IDENTITY CASCADE)
    - Hub_All/api/migrations/versions/0001_initial_schema.py:80-106 (W2 — hubs base 7 cột)
    - Hub_All/api/migrations/versions/0003_phase5_schema_reconcile.py:47-74 (W2 — hubs ADD 3 cột; INSERT 9 cột valid)
    - Hub_All/api/tests/integration/test_rbac_dependency.py (LifespanManager pattern carry forward)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md §"Test scenario reference" + §"D-V3.1-Phase2-E B1 iter 1 narrowing" (7 case acceptance ROADMAP success criteria #2)
  </read_first>
  <behavior>
    - Test 1 (super_admin_create_any_hub): super admin POST /api/users body{hub_id=dmd,role=viewer} → 201.
    - Test 2 (hub_admin_create_own_hub_201): hub_admin dmd POST /api/users body{hub_id=dmd,role=viewer} → 201.
    - Test 3 (hub_admin_create_other_hub_403): hub_admin dmd POST /api/users body{hub_id=tdt,role=viewer} → 403 + envelope code=HUB_ADMIN_REQUIRED.
    - Test 4 (hub_admin_block_role_escalation): hub_admin dmd PATCH /api/users/:id/role body{hub_id=dmd,role='admin'} → 403 + envelope code=HUB_ADMIN_REQUIRED (T-02-02-E).
    - Test 5 (hub_admin_delete_single_hub_of_own_passes): hub_admin dmd DELETE user-thuộc-CHỈ-dmd → 200 (B1 iter 1).
    - Test 6 (hub_admin_delete_cross_hub_denied): hub_admin dmd DELETE user-thuộc-dmd+tdt → 403 + envelope code=CROSS_HUB_USER_DELETE_DENIED (B1 iter 1 mới).
    - Test 7 (hub_admin_delete_other_hub_denied): hub_admin dmd DELETE user-thuộc-CHỈ-tdt → 403 + envelope code=HUB_ADMIN_REQUIRED (B1 iter 1 từ assert_hub_admin_for raise).
  </behavior>
  <files>api/tests/integration/test_dep_users_scope.py</files>
  <action>
Tạo file `api/tests/integration/test_dep_users_scope.py` với 7 integration test.

```python
"""Integration test users.py CRUD scope — Phase 2 Plan 02-03 (DEP-03 + D-V3.1-Phase2-E + B1 iter 1).

7 scenario verify ROADMAP Phase 2 success criteria #2 (5 cũ + 2 mới B1 iter 1):
1. Super admin POST /api/users any hub → 201.
2. Hub_admin dmd POST /api/users (hub_id=dmd) → 201.
3. Hub_admin dmd POST /api/users (hub_id=tdt) → 403 HUB_ADMIN_REQUIRED.
4. Hub_admin dmd PATCH /api/users/:id/role escalation role='admin' → 403 (T-02-02-E).
5. Hub_admin dmd DELETE user-thuộc-CHỈ-dmd → 200 (B1 iter 1 single-hub pass).
6. Hub_admin dmd DELETE user-thuộc-dmd+tdt → 403 CROSS_HUB_USER_DELETE_DENIED (B1 iter 1 cross-hub mới).
7. Hub_admin dmd DELETE user-thuộc-CHỈ-tdt → 403 HUB_ADMIN_REQUIRED (B1 iter 1 other-hub deny).

W1 isolation note: `app_with_auth` fixture (conftest.py:264-284) đã TRUNCATE 8 bảng
per-test RESTART IDENTITY CASCADE — fixture seed dưới KHÔNG cần re-truncate.

W2 schema note: hubs table 10 cột (migration 0001:80-106 + 0003:47-74) — INSERT 9 cột
(bỏ updated_at server_default NOW()) là VALID.

W4 coverage note: chạy với --cov-fail-under=80 ở acceptance criteria.

W5 FE breakage note: Phase 2 ship trước Phase 3 FE-04 → hub_admin gọi GET /api/users
qua FE cũ (chưa pass hub_id query) sẽ thấy 400 HUB_ID_REQUIRED. Acceptable theo
Phase 2 → Phase 3 sequential dep; Plan 02-05 closeout sẽ note operator broadcast.

Carry forward patterns:
- `tests/integration/test_dep_hubs_scope.py` (Plan 02-02) — `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` fixture DRY.
- `tests/integration/conftest.py` — auth_client + admin_token + GO_SEED_HASH fixture.
- Plan 02-01 `assert_hub_admin_for` validator function (BLOCKING dep — Wave 1).
"""
from __future__ import annotations

import uuid as _uuid

import httpx
import pytest
from sqlalchemy import text


async def _seed_user_in_hubs(
    *, email: str, hub_ids: list[str], role: str = "viewer"
) -> str:
    """Seed user qua DB direct + assignment user_hubs cho 1+ hub.

    B1 iter 1: hỗ trợ multi-hub user (test #6 cross-hub denied case).

    Args:
        email: user email.
        hub_ids: list hub UUID str (≥ 1; 2+ cho cross-hub test).
        role: users.role global (default 'viewer').

    Returns:
        user_id str.
    """
    from app.db.session import get_engine
    from tests.integration.conftest import GO_SEED_HASH

    engine = get_engine()
    user_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO users (id, email, password_hash, full_name, role, "
                "phone, department, status, is_active, created_at, updated_at) "
                "VALUES (:id, :email, :hash, :name, :role, NULL, NULL, "
                "'active', TRUE, NOW(), NOW())"
            ),
            {
                "id": user_id, "email": email, "hash": GO_SEED_HASH,
                "name": f"Test {email}", "role": role,
            },
        )
        for hub_id in hub_ids:
            await conn.execute(
                text(
                    "INSERT INTO user_hubs (user_id, hub_id, assigned_at) "
                    "VALUES (:uid, :hid, NOW())"
                ),
                {"uid": user_id, "hid": hub_id},
            )
    return user_id


async def _seed_user_in_hub(*, email: str, hub_id: str, role: str = "viewer") -> str:
    """Shortcut single-hub variant (carry forward Plan 02-02 pattern)."""
    return await _seed_user_in_hubs(email=email, hub_ids=[hub_id], role=role)


async def _seed_hub_admin_user(*, email: str, hub_id: str) -> str:
    """Seed hub_admin user (users.role='editor' global + user_hubs.role='hub_admin').

    Carry forward Plan 02-02 helper — duplicate inline để Plan 02-03 self-contained.
    W1: app_with_auth TRUNCATE per-test → KHÔNG cần re-truncate.
    """
    from app.db.session import get_engine
    from tests.integration.conftest import GO_SEED_HASH

    engine = get_engine()
    user_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO users (id, email, password_hash, full_name, role, "
                "phone, department, status, is_active, created_at, updated_at) "
                "VALUES (:id, :email, :hash, :name, 'editor', NULL, NULL, "
                "'active', TRUE, NOW(), NOW())"
            ),
            {
                "id": user_id, "email": email, "hash": GO_SEED_HASH,
                "name": "Hub Admin Test",
            },
        )
        await conn.execute(
            text(
                "INSERT INTO user_hubs (user_id, hub_id, role, assigned_at) "
                "VALUES (:uid, :hid, 'hub_admin', NOW())"
            ),
            {"uid": user_id, "hid": hub_id},
        )
    return user_id


@pytest.fixture
async def seed_hubs_dmd_tdt() -> tuple[str, str]:
    """Tạo 2 hub dmd + tdt qua DB direct (carry forward Plan 02-02).

    W1: app_with_auth TRUNCATE per-test → KHÔNG cần re-truncate.
    W2: hubs 10 cột — INSERT 9 cột valid (bỏ updated_at).
    """
    from app.db.session import get_engine

    engine = get_engine()
    dmd_id = str(_uuid.uuid4())
    tdt_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO hubs (id, slug, name, code, subdomain, status, "
                "is_active, created_at, updated_at) "
                "VALUES (:id, :slug, :name, :code, :subdomain, 'active', TRUE, "
                "NOW(), NOW())"
            ),
            {"id": dmd_id, "slug": "dmd", "name": "Đỗ Minh Đường",
             "code": "dmd", "subdomain": "dmd"},
        )
        await conn.execute(
            text(
                "INSERT INTO hubs (id, slug, name, code, subdomain, status, "
                "is_active, created_at, updated_at) "
                "VALUES (:id, :slug, :name, :code, :subdomain, 'active', TRUE, "
                "NOW(), NOW())"
            ),
            {"id": tdt_id, "slug": "tdt", "name": "Thuốc Dân Tộc",
             "code": "tdt", "subdomain": "tdt"},
        )
    return dmd_id, tdt_id


async def _login(client: httpx.AsyncClient, email: str, password: str) -> str:
    """Login + return access_token (carry forward conftest pattern)."""
    r = await client.post(
        "/api/auth/login",
        json={"email": email, "password": password},
    )
    assert r.status_code == 200, r.text
    return r.json()["data"]["access_token"]


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_super_admin_create_user_any_hub_returns_201(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 1 ROADMAP success criteria #2:
    Super admin POST /api/users any hub → 201 (bypass assert_hub_admin_for)."""
    dmd_id, _ = seed_hubs_dmd_tdt

    r = await auth_client.post(
        "/api/users",
        headers={"Authorization": f"Bearer {admin_token}"},
        json={
            "email": f"super.created.{_uuid.uuid4()}@dmd.vn",
            "name": "User by super admin",
            "password": "Pass1234",
            "hub_id": dmd_id,
            "role": "viewer",
        },
    )

    assert r.status_code == 201, r.text


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_create_user_in_own_hub_returns_201(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 2: Hub_admin dmd POST /api/users (hub_id=dmd) → 201."""
    dmd_id, _ = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.create.own@dmd.vn", hub_id=dmd_id)
    token = await _login(auth_client, "hub.admin.create.own@dmd.vn", "Admin@123")

    r = await auth_client.post(
        "/api/users",
        headers={"Authorization": f"Bearer {token}"},
        json={
            "email": f"viewer.dmd.{_uuid.uuid4()}@dmd.vn",
            "name": "Viewer DMD",
            "password": "Pass1234",
            "hub_id": dmd_id,
            "role": "viewer",
        },
    )

    assert r.status_code == 201, r.text


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_create_user_in_other_hub_returns_403_hub_admin_required(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 3: Hub_admin dmd POST /api/users (hub_id=tdt) → 403 HUB_ADMIN_REQUIRED."""
    dmd_id, tdt_id = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.create.other@dmd.vn", hub_id=dmd_id)
    token = await _login(auth_client, "hub.admin.create.other@dmd.vn", "Admin@123")

    r = await auth_client.post(
        "/api/users",
        headers={"Authorization": f"Bearer {token}"},
        json={
            "email": f"viewer.tdt.{_uuid.uuid4()}@tdt.vn",
            "name": "Viewer TDT",
            "password": "Pass1234",
            "hub_id": tdt_id,
            "role": "viewer",
        },
    )

    assert r.status_code == 403, r.text
    body = r.json()
    assert body["success"] is False
    assert body["error"]["code"] == "HUB_ADMIN_REQUIRED"


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_patch_role_admin_escalation_blocked_403(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 4 T-02-02-E mitigation:
    Hub_admin dmd PATCH /api/users/:id/role body{role='admin', hub_id=dmd} → 403."""
    dmd_id, _ = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.escalate@dmd.vn", hub_id=dmd_id)
    target_user_id = await _seed_user_in_hub(
        email=f"target.escalate.{_uuid.uuid4()}@dmd.vn",
        hub_id=dmd_id,
        role="viewer",
    )
    token = await _login(auth_client, "hub.admin.escalate@dmd.vn", "Admin@123")

    r = await auth_client.patch(
        f"/api/users/{target_user_id}/role",
        headers={"Authorization": f"Bearer {token}"},
        json={"hub_id": dmd_id, "role": "admin"},  # ← escalation attempt
    )

    assert r.status_code == 403, r.text
    body = r.json()
    assert body["error"]["code"] == "HUB_ADMIN_REQUIRED", (
        f"T-02-02-E mitigation: escalation phải reject với HUB_ADMIN_REQUIRED, "
        f"got {body['error'].get('code')!r}"
    )


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_delete_single_hub_user_of_own_hub_passes_200(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 5 B1 iter 1 DEP-03 spec compliance:
    Hub_admin dmd DELETE user-thuộc-CHỈ-dmd → 200.

    DEP-03 line 24 spec literal include DELETE trong hub_admin scope.
    D-V3.1-Phase2-E narrowed: hub_admin xoá user thuộc CHỈ hub mình → OK.
    """
    dmd_id, _ = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.del.own@dmd.vn", hub_id=dmd_id)
    target_user_id = await _seed_user_in_hub(
        email=f"target.del.own.{_uuid.uuid4()}@dmd.vn",
        hub_id=dmd_id,
        role="viewer",
    )
    token = await _login(auth_client, "hub.admin.del.own@dmd.vn", "Admin@123")

    r = await auth_client.delete(
        f"/api/users/{target_user_id}",
        headers={"Authorization": f"Bearer {token}"},
    )

    assert r.status_code == 200, r.text


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_delete_cross_hub_user_returns_403_cross_hub_denied(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 6 B1 iter 1 mới:
    Hub_admin dmd DELETE user-thuộc-dmd+tdt → 403 CROSS_HUB_USER_DELETE_DENIED.

    D-V3.1-Phase2-E: user thuộc nhiều hub → cross-hub op → super admin only.
    Envelope code MỚI để phân biệt với HUB_ADMIN_REQUIRED (scope violation).
    """
    dmd_id, tdt_id = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.del.cross@dmd.vn", hub_id=dmd_id)
    target_user_id = await _seed_user_in_hubs(
        email=f"target.cross.{_uuid.uuid4()}@cross.vn",
        hub_ids=[dmd_id, tdt_id],  # ← multi-hub user
        role="viewer",
    )
    token = await _login(auth_client, "hub.admin.del.cross@dmd.vn", "Admin@123")

    r = await auth_client.delete(
        f"/api/users/{target_user_id}",
        headers={"Authorization": f"Bearer {token}"},
    )

    assert r.status_code == 403, r.text
    body = r.json()
    assert body["error"]["code"] == "CROSS_HUB_USER_DELETE_DENIED", (
        f"B1 iter 1: cross-hub DELETE phải reject với CROSS_HUB_USER_DELETE_DENIED, "
        f"got {body['error'].get('code')!r}"
    )


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_delete_user_in_other_hub_returns_403_hub_admin_required(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 7 B1 iter 1:
    Hub_admin dmd DELETE user-thuộc-CHỈ-tdt → 403 HUB_ADMIN_REQUIRED.

    Target user single-hub nhưng hub đó ≠ caller's scope → assert_hub_admin_for
    raise HUB_ADMIN_REQUIRED (KHÔNG CROSS_HUB_USER_DELETE_DENIED).
    """
    dmd_id, tdt_id = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.del.other@dmd.vn", hub_id=dmd_id)
    target_user_id = await _seed_user_in_hub(
        email=f"target.other.{_uuid.uuid4()}@tdt.vn",
        hub_id=tdt_id,  # ← user single-hub nhưng hub khác caller
        role="viewer",
    )
    token = await _login(auth_client, "hub.admin.del.other@dmd.vn", "Admin@123")

    r = await auth_client.delete(
        f"/api/users/{target_user_id}",
        headers={"Authorization": f"Bearer {token}"},
    )

    assert r.status_code == 403, r.text
    body = r.json()
    assert body["error"]["code"] == "HUB_ADMIN_REQUIRED", (
        f"B1 iter 1: other-hub single-user DELETE phải reject với HUB_ADMIN_REQUIRED "
        f"(qua assert_hub_admin_for), got {body['error'].get('code')!r}"
    )
```

**Lưu ý:**
- 7 test scenario CHÍNH XÁC theo CONTEXT.md §"Test scenario reference" + B1 iter 1 narrowing mapping success criteria #2 ROADMAP.
- `_seed_user_in_hubs` mới (multi-hub variant) + `_seed_user_in_hub` shortcut single-hub — DRY support cho B1 iter 1 cross-hub case (test #6).
- `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` duplicate từ Plan 02-02 — Plan 02-05 closeout có thể consolidate vào `conftest.py` (DRY future refactor).
- Test #5/#6/#7 verify B1 iter 1 envelope code differentiation: 200 (single-own) / 403 CROSS_HUB_USER_DELETE_DENIED (multi-hub) / 403 HUB_ADMIN_REQUIRED (single-other) — frontend FE-04 sẽ dùng differentiation cho UX.
- `_login` helper inline (verify signature match conftest.py existing — adapt nếu khác).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/integration/test_dep_users_scope.py -x --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_dep_users_scope.py` EXISTS.
    - File chứa 7 test function `async def test_` (7 scenario success criteria #2 + B1 iter 1).
    - File chứa `@pytest.mark.critical` + `@pytest.mark.integration` + `@pytest.mark.asyncio` ≥ 7 lần mỗi marker.
    - File chứa helper `async def _seed_hub_admin_user(` + `async def _seed_user_in_hub(` + `async def _seed_user_in_hubs(` (multi-hub variant B1 iter 1).
    - File chứa fixture `seed_hubs_dmd_tdt`.
    - File chứa assert `r.status_code == 201` ≥ 2 lần (test #1 + #2 — super admin + hub_admin own hub create OK).
    - File chứa assert `r.status_code == 200` ≥ 1 lần (test #5 — hub_admin DELETE single-own pass B1 iter 1).
    - File chứa assert `r.status_code == 403` ≥ 4 lần (test #3 + #4 + #6 + #7).
    - File chứa assert `body["error"]["code"] == "HUB_ADMIN_REQUIRED"` ≥ 3 lần (test #3 + #4 + #7).
    - File chứa assert `body["error"]["code"] == "CROSS_HUB_USER_DELETE_DENIED"` ≥ 1 lần (test #6 — B1 iter 1 mới).
    - File chứa JSON body `"role": "admin"` (test #4 escalation attempt).
    - File chứa `hub_ids=[dmd_id, tdt_id]` HOẶC equivalent multi-hub seed call (test #6 cross-hub).
    - File chứa comment hoặc docstring mention `T-02-02-E` HOẶC `D-V3.1-Phase2-E` HOẶC `DEP-03` HOẶC `B1 iter 1` (traceability).
    - File chứa comment hoặc docstring mention `W1` HOẶC `app_with_auth` HOẶC `TRUNCATE` (W1 isolation ack).
    - File chứa comment hoặc docstring mention `W5` HOẶC `FE breakage` HOẶC `HUB_ID_REQUIRED` (W5 FE breakage ack).
    - `python -c "import ast; ast.parse(open('Hub_All/api/tests/integration/test_dep_users_scope.py').read())"` exit 0.
    - `cd Hub_All/api && python -m pytest tests/integration/test_dep_users_scope.py -x --tb=no -q 2>&1 | grep -E "7 passed"` exit 0 (yêu cầu Postgres + Redis testcontainers).
    - `cd Hub_All/api && python -m pytest tests/integration/test_dep_users_scope.py tests/unit/test_require_hub_admin_for.py --cov=app.auth.dependencies --cov=app.routers.users --cov-fail-under=80 -q` exit 0 (W4 coverage measurement step).
  </acceptance_criteria>
  <done>
    test_dep_users_scope.py 7 test PASS — verify scenario #1-7 success criteria #2 ROADMAP + B1 iter 1 narrowing; envelope HUB_ADMIN_REQUIRED (3 endpoint refactor + DELETE other-hub) vs CROSS_HUB_USER_DELETE_DENIED (DELETE cross-hub mới) vs 200 (DELETE single-hub-own); T-02-02-E escalation block verify; W1/W2/W4/W5 acknowledged inline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → POST/PATCH/GET/DELETE /api/users | body JSON UNTRUSTED (req.hub_id, req.role); user object verified qua get_current_user (JWT chain trusted) |
| Refactored handler → assert_hub_admin_for | Plan 02-01 BLOCKING dep — Wave 1 ship trước Wave 2 (Plan 02-03 depends_on: [01]) |
| DELETE handler → user_hubs membership query | Target user_id từ path param UNTRUSTED; named bind param `:target_id` (T-01-02-01 mitigation Plan 01-02 carry forward) — KHÔNG concat raw SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Elevation | Hub_admin gán role='admin' cho user khác qua PATCH /api/users/:id/role (escalate cho user khác trở thành super admin cross-hub) | mitigate | PATCH role handler check `if req.role == "admin" and user.role != "admin"` → 403 HUB_ADMIN_REQUIRED (T-02-02-E mitigation D-V3.1-Phase2-D LOCKED). Test #4 verify. |
| T-02-03-02 | Information Disclosure | Hub_admin GET /api/users (list) thấy user hub khác | mitigate | GET list handler: nếu non-super-admin + hub_id None → 400 HUB_ID_REQUIRED; nếu có hub_id → assert_hub_admin_for check. Hub_admin BUỘC pass hub_id query thuộc hub mình. |
| T-02-03-03 | Tampering | Hub_admin tự sửa user_hubs.role thành 'hub_admin' qua API direct DB | accept | M2 baseline KHÔNG có API direct edit user_hubs.role (Phase 3 FE-03 sẽ ship API + check D-V3.1-Phase2-E: assign role hub khác cần super admin); production code phải qua Plan 02-04 PATCH /api/users/:id/role với business logic check. |
| T-02-03-04 | Elevation | Hub_admin DELETE user thuộc nhiều hub → cross-hub side effect | mitigate | B1 iter 1 — DELETE branch single-hub vs cross-hub: target user thuộc nhiều hub → 403 CROSS_HUB_USER_DELETE_DENIED (envelope code mới); target thuộc 1 hub khác → 403 HUB_ADMIN_REQUIRED (qua assert_hub_admin_for). Test #6 + #7 verify. |
| T-02-03-05 | Elevation | Hub_admin PATCH /api/users/:id/status user thuộc hub khác → cross-hub side effect | mitigate | D-V3.1-Phase2-E LOCKED: PATCH status GIỮ require_role("admin") super-only. KHÔNG refactor ở Plan 02-03. |
| T-02-03-06 | Information Disclosure | Pydantic Literal extend 'hub_admin' leak schema cho frontend chưa update | accept | Phase 3 FE-04 sẽ extend frontend type — backward compat (existing role values vẫn accept). |
| T-02-03-07 | Tampering | DELETE user_hubs membership query manipulated via SQL injection | mitigate | `text()` + named bind param `:target_id` (T-01-02-01 mitigation Plan 01-02 carry forward); KHÔNG concat raw SQL. |

</threat_model>

<verification>
- schemas/users.py UserRole 4 value (admin|hub_admin|editor|viewer) match migration 0006.
- routers/users.py 4 endpoint refactor (POST create + PATCH role + GET list + DELETE B1 iter 1) với assert_hub_admin_for inline + T-02-02-E block escalation + HUB_ID_REQUIRED guard + CROSS_HUB_USER_DELETE_DENIED envelope code mới.
- 4 endpoint còn lại GIỮ require_role super-only (D-V3.1-Phase2-E LOCKED narrowed bởi B1 iter 1 cho DELETE).
- 7 integration test PASS — verify scenario #1-7 success criteria #2 ROADMAP + B1 iter 1 narrowing.
- Envelope code phân biệt rõ: HUB_ADMIN_REQUIRED (3 endpoint refactor + DELETE other-hub) vs CROSS_HUB_USER_DELETE_DENIED (DELETE cross-hub mới) vs FORBIDDEN (PATCH status + GET single + PUT + reset-password giữ require_role).
- Import chain KHÔNG break — `from app.routers.users import router` importable.
- W4 coverage: `--cov=app.auth.dependencies --cov=app.routers.users --cov-fail-under=80` PASS.
- W5 acknowledged: Phase 2 → Phase 3 sequential dep, FE breakage acceptable cho hub_admin role 1-2 ngày.
</verification>

<success_criteria>
1. schemas/users.py UserRole Literal extend 4 value.
2. routers/users.py 4 endpoint refactor + 3 endpoint giữ nguyên.
3. T-02-02-E mitigation: role='admin' escalation block trong PATCH role handler.
4. HUB_ID_REQUIRED guard cho GET list non-super-admin (DEP-03 enforce).
5. B1 iter 1: DELETE branch single-hub (pass) vs cross-hub (CROSS_HUB_USER_DELETE_DENIED) vs other-hub (HUB_ADMIN_REQUIRED) — DEP-03 spec compliance.
6. 7 integration test PASS — testcontainers Postgres + Redis required.
7. Coverage ≥ 80% trên dependencies.py + routers/users.py (W4 measurement step explicit).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-03-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- UserRole Literal extend 4 value match migration 0006 (Plan 01-01 schema ready)
- 4 endpoint refactor (POST create + PATCH role + GET list + DELETE B1 iter 1) với assert_hub_admin_for inline
- B1 iter 1 fix: DELETE branch single-hub vs cross-hub (DEP-03 spec compliance) — envelope code CROSS_HUB_USER_DELETE_DENIED mới + HUB_ADMIN_REQUIRED reuse cho single-other + orphan
- T-02-02-E mitigation business logic block role='admin' escalation cho non-super-admin caller
- HUB_ID_REQUIRED guard cho GET list non-super-admin (DEP-03 enforce strict scope)
- 3 endpoint (PATCH status + GET single + PUT + reset-password) giữ require_role super-only (D-V3.1-Phase2-E LOCKED narrowed bởi B1 iter 1 cho DELETE)
- 7 integration test PASS — scenario #1-7 success criteria #2 ROADMAP + B1 iter 1
- Service layer (user_service.py) UNCHANGED ở plan này — Plan 02-04 sẽ refactor signature thêm actor_role + actor_hub_id (DEP-05 audit; bao gồm DELETE handler vì B1 iter 1 đã refactor DELETE từ require_role sang get_current_user)
- Helper `_seed_hub_admin_user` + `_seed_user_in_hubs` + `seed_hubs_dmd_tdt` duplicate từ Plan 02-02 — Plan 02-05 closeout consolidate vào conftest.py nếu cần
- W1 isolation auto-handled bởi app_with_auth TRUNCATE per-test (conftest.py:264-284)
- W2 schema reference hubs 10 cột (migration 0001:80-106 + 0003:47-74)
- W4 coverage measurement step explicit ở acceptance criteria
- W5 FE breakage acknowledged — Phase 3 FE-04 sequential dep, operator broadcast prepared trong Plan 02-05 closeout

**Temporary FE breakage (W5):** Non-super-admin users gọi `GET /api/users` qua existing M2/v3.0 frontend (chưa pass `hub_id` query) sẽ thấy 400 HUB_ID_REQUIRED. Acceptable per Phase 2 → Phase 3 sequential dep (FE-04 Phase 3 sẽ pass hub_id query). Plan 02-05 closeout sẽ update CLAUDE.md note operator broadcast Slack/Email trước Phase 3 FE-04 ship.

Commit atomic: `feat(02-03): users.py 4 endpoint refactor hub_admin scope + DELETE branch single-hub vs cross-hub (DEP-03 + B1 iter 1)` + body tiếng Việt nói tại sao (DEP-03 ship hub_admin CRUD scope; T-02-02-E mitigation block escalation; D-V3.1-Phase2-E narrowed bởi B1 iter 1 cho DELETE; CROSS_HUB_USER_DELETE_DENIED envelope code mới; 7 integration test scenario verify; W4 coverage ≥ 80% explicit).
</output>
</content>
</invoke>