---
phase: 02-backend-rbac-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/auth/dependencies.py
  - api/tests/unit/test_require_hub_admin_for.py
autonomous: true
requirements: [DEP-01]
must_haves:
  truths:
    - "Sau khi merge, validator function `assert_hub_admin_for(*, user, db, target_hub_id)` callable + async — KHÔNG raise khi (a) user.role='admin' hoặc (b) get_effective_role(user, hub)=='hub_admin'"
    - "Sau khi merge, gọi `assert_hub_admin_for` với user `role='editor'` + hub không có override → raise HTTPException 403 với detail['code']=='HUB_ADMIN_REQUIRED'"
    - "Sau khi merge, gọi `assert_hub_admin_for` với user_id KHÔNG tồn tại trong users → raise HTTPException 403 (catch UserNotFoundError defensive) với detail['code']=='HUB_ADMIN_REQUIRED'"
    - "5 unit test PASS — cover D-V3.1-Phase2-D 5 case + 1 defensive (super_admin bypass / hub_admin pass / hub_admin wrong_hub fail / viewer fail / user_not_found fail)"
    - "Phase 2 Plan 02-03 + 02-04 có thể import `from app.auth.dependencies import assert_hub_admin_for` (consumed by 5 router refactor + 3 service signature)"
  artifacts:
    - path: "api/app/auth/dependencies.py"
      provides: "Inline validator function `assert_hub_admin_for(*, user, db, target_hub_id) -> None` (hybrid pattern D-V3.1-Phase2-D LOCKED — pure function, KHÔNG FastAPI Depends factory vì hub_id ở body POST/PATCH)"
      contains: "async def assert_hub_admin_for"
    - path: "api/app/auth/dependencies.py"
      provides: "Import `from app.auth.role import UserNotFoundError, get_effective_role` (Phase 1 Plan 01-02 carry forward)"
      contains: "from app.auth.role import UserNotFoundError, get_effective_role"
    - path: "api/tests/unit/test_require_hub_admin_for.py"
      provides: "5 unit test cover 5 case D-V3.1-Phase2-D + AsyncMock(AsyncSession) pattern carry forward test_role_helper.py"
      contains: "async def test_"
  key_links:
    - from: "assert_hub_admin_for"
      to: "app.auth.role.get_effective_role"
      via: "await call truyền user.id + target_hub_id"
      pattern: "effective = await get_effective_role\\(db, user.id, target_hub_id\\)"
    - from: "assert_hub_admin_for"
      to: "HTTPException 403 envelope"
      via: "raise với detail dict {code, message}"
      pattern: "detail=\\{\"code\": \"HUB_ADMIN_REQUIRED\""
    - from: "test_require_hub_admin_for.py"
      to: "assert_hub_admin_for"
      via: "import + AsyncMock(AsyncSession) + pytest.raises(HTTPException)"
      pattern: "from app.auth.dependencies import assert_hub_admin_for"
---

<objective>
Tạo validator function `assert_hub_admin_for(*, user, db, target_hub_id)` ở `api/app/auth/dependencies.py` (BLOCKING dependency cho Wave 2 router refactor) + 5-case unit test với AsyncMock(AsyncSession).

**Purpose (DEP-01 + D-V3.1-Phase2-D LOCKED):** Phase 2 cần gate per-hub cho 5 endpoint users.py CRUD. Vì hub_id nằm trong BODY POST/PATCH (KHÔNG path param), pattern FastAPI Depends factory KHÔNG khả thi — phải dùng validator function gọi inline trong handler sau body parse (hybrid pattern). Plan 02-03 sẽ refactor 5 router endpoint inject `await assert_hub_admin_for(...)` sau body parse.

**Logic 5 case (D-V3.1-Phase2-D LOCKED):**
1. Super admin (`user.role == 'admin'`) → bypass (no DB call, return None).
2. Hub_admin của hub đúng (`get_effective_role(user.id, hub) == 'hub_admin'`) → return None.
3. Hub_admin của hub khác (effective ≠ 'hub_admin') → raise 403 HUB_ADMIN_REQUIRED.
4. Viewer / editor không phải hub_admin → raise 403 HUB_ADMIN_REQUIRED.
5. User không tồn tại trong users (UserNotFoundError defensive) → catch + raise 403 HUB_ADMIN_REQUIRED (KHÔNG leak 404).

**Output:** 2 file change — `dependencies.py` thêm validator function + import; `test_require_hub_admin_for.py` mới 5 test PASS.

**Carry forward patterns:**
- Closure factory `require_role(*roles)` lines 290-333 (envelope shape + detail dict {code, message}).
- `get_current_user_for_hub_access` Layer 3 SSO-04 lines 365-431 (defense-in-depth raise pattern).
- `test_role_helper.py` AsyncMock(AsyncSession) factory _make_session — pattern Plan 01-02 carry forward.
- `app/pkg/response.py:98-103` envelope D6 `{success, data, error, meta}` shape (code = `HUB_ADMIN_REQUIRED` mới — D-V3.1-Phase2-B LOCKED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/auth/dependencies.py
@Hub_All/api/app/auth/role.py
@Hub_All/api/tests/unit/test_role_helper.py

<interfaces>
<!-- Pattern source-of-truth từ dependencies.py + role.py. Executor dùng trực tiếp — KHÔNG cần explore. -->

Existing closure factory pattern (dependencies.py:290-333 — `require_role`):
```python
def require_role(
    *roles: str,
) -> Callable[[User], Awaitable[User]]:
    if not roles:
        raise ValueError("require_role cần ít nhất 1 role")
    allowed = set(roles)

    async def _dependency(user: User = Depends(get_current_user)) -> User:  # noqa: B008
        if user.role not in allowed:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail={
                    "code": "FORBIDDEN",
                    "message": f"Không đủ quyền — yêu cầu một trong {sorted(allowed)}",
                },
            )
        return user

    return _dependency
```

Existing inline-validator pattern (dependencies.py:401-431 — `get_current_user_for_hub_access` Layer 3):
```python
async def get_current_user_for_hub_access(
    request: Request,
    user: User = Depends(get_current_user),
) -> User:
    from app.config import get_settings
    settings = get_settings()
    if settings.hub_name == "central":
        return user
    claims = getattr(request.state, "jwt_claims", None)
    # ...
    if settings.hub_name not in claims.hub_ids:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={
                "code": "CROSS_HUB_ACCESS_DENIED",
                "message": f"Token KHÔNG có quyền truy cập hub {settings.hub_name!r} ...",
            },
        )
    return user
```

Helper API contract từ Plan 01-02 (api/app/auth/role.py):
```python
class UserNotFoundError(Exception): ...

async def get_effective_role(
    session: AsyncSession,
    user_id: UUID | str,
    hub_id: UUID | str,
) -> str:
    """Returns 'admin' | 'hub_admin' | 'editor' | 'viewer'.
    Raises UserNotFoundError nếu users row missing."""
```

AsyncMock(AsyncSession) factory pattern (test_role_helper.py:24-42):
```python
def _make_session(*fetchone_returns: object) -> AsyncMock:
    session = AsyncMock()
    result_mocks = []
    for fetchone_value in fetchone_returns:
        result = MagicMock()
        result.fetchone.return_value = fetchone_value
        result_mocks.append(result)
    session.execute = AsyncMock(side_effect=result_mocks)
    return session
```

Imports hiện có ở dependencies.py top (lines 1-30 — Phase 2 cần thêm 1 dòng import role):
```python
from fastapi import Depends, Header, HTTPException, Request, status
from sqlalchemy.ext.asyncio import AsyncSession
from app.models.auth import User
# Phase 2 thêm:
from app.auth.role import UserNotFoundError, get_effective_role
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm import + validator function `assert_hub_admin_for` ở dependencies.py (DEP-01 D-V3.1-Phase2-D LOCKED)</name>
  <read_first>
    - Hub_All/api/app/auth/dependencies.py:1-30 (import block hiện có — thêm 1 dòng `from app.auth.role import UserNotFoundError, get_effective_role`)
    - Hub_All/api/app/auth/dependencies.py:290-333 (require_role closure factory pattern — analog cho envelope shape + detail dict)
    - Hub_All/api/app/auth/dependencies.py:365-431 (get_current_user_for_hub_access Layer 3 SSO-04 — analog cho async inline check + raise)
    - Hub_All/api/app/auth/role.py (get_effective_role API contract + UserNotFoundError defensive exception — Plan 01-02 carry forward)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md §"D-V3.1-Phase2-B/D LOCKED" (envelope code + hybrid pattern)
  </read_first>
  <behavior>
    - Test 1 (super_admin_bypass): user.role='admin' → return None KHÔNG call get_effective_role (session.execute call_count == 0).
    - Test 2 (hub_admin_correct_hub): user.role='editor' + get_effective_role return 'hub_admin' → return None KHÔNG raise.
    - Test 3 (hub_admin_wrong_hub): user.role='editor' + get_effective_role return 'viewer' (no override) → raise HTTPException 403 detail['code']=='HUB_ADMIN_REQUIRED'.
    - Test 4 (viewer_not_hub_admin): user.role='viewer' + get_effective_role return 'viewer' → raise HTTPException 403 detail['code']=='HUB_ADMIN_REQUIRED'.
    - Test 5 (user_not_found_defensive): get_effective_role raise UserNotFoundError → catch + raise HTTPException 403 detail['code']=='HUB_ADMIN_REQUIRED' (KHÔNG leak 404 / 500).
  </behavior>
  <files>api/app/auth/dependencies.py</files>
  <action>
**STEP 1 — Thêm import** vào block imports đầu file (sau line ~25, sau `from app.models.auth import User`):

```python
# Phase 2 Plan 02-01 DEP-01 — get_effective_role helper từ Plan 01-02 ROLE-04.
from app.auth.role import UserNotFoundError, get_effective_role
```

**STEP 2 — Append validator function `assert_hub_admin_for` SAU `require_role` closure factory (NGAY SAU line 333, TRƯỚC `class UserWithHubs` line 336):**

```python
async def assert_hub_admin_for(
    *,
    user: User,
    db: AsyncSession,
    target_hub_id: str,
) -> None:
    """Phase 2 Plan 02-01 DEP-01 (D-V3.1-Phase2-D LOCKED) — Inline validator function.

    Pure async function (KHÔNG phải FastAPI dependency factory). Gọi INLINE
    trong handler SAU khi parse body — vì hub_id nằm trong BODY POST/PATCH
    (KHÔNG path param), pattern Depends factory `require_role(*roles)` không
    khả thi (FastAPI Depends inject TRƯỚC body parse).

    Logic 5 case (D-V3.1-Phase2-D LOCKED):
    1. Super admin (`user.role == 'admin'`) → bypass (no DB call, return None).
    2. Hub_admin của hub đúng (`get_effective_role == 'hub_admin'`) → return None.
    3. Hub_admin của hub khác (`get_effective_role` trả về role khác) → raise 403.
    4. Viewer / editor không phải hub_admin → raise 403.
    5. User KHÔNG tồn tại (UserNotFoundError) → catch + raise 403 (KHÔNG leak 404).

    Args:
        user: User ORM object (đã verify JWT qua `get_current_user`).
        db: AsyncSession đang mở (caller manage lifecycle qua Depends(get_session)).
        target_hub_id: hub_id ở body request (POST/PATCH) — UNTRUSTED user input,
            nhưng KHÔNG dùng làm SQL value trực tiếp (qua get_effective_role
            named bind params — T-01-02-01 mitigation Plan 01-02 carry forward).

    Returns:
        None nếu pass (super admin bypass HOẶC hub_admin của hub đúng).

    Raises:
        HTTPException(403): với envelope D6 `detail={"code": "HUB_ADMIN_REQUIRED",
            "message": "..."}` cho mọi case fail (D-V3.1-Phase2-B LOCKED).

    Usage (Plan 02-03 sẽ refactor 5 router endpoint):
        @router.post("")
        async def create_user(
            req: CreateUserRequest,
            request: Request,
            user: User = Depends(get_current_user),
            db: AsyncSession = Depends(get_session),
            service: UserService = Depends(get_user_service),
        ) -> JSONResponse:
            # DEP-03 — inline scope check sau body parse.
            await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)
            # ... rest of handler

    Threat model:
        - T-02-01-E Elevation: hub_admin tạo hub mới → KHÔNG mitigated ở dep này
          (DEP-04 require_role("admin") giữ NGUYÊN cho hubs.py mutate endpoints).
        - T-02-02-E Elevation: hub_admin gán role='admin' cho user khác → MỘT PHẦN
          mitigated ở dep này; Plan 02-03 sẽ add business logic block role transition
          'admin' khi caller KHÔNG phải super admin.
        - T-02-05-T Tampering: stale JWT hub_admin sau bị demote → mitigated bởi
          `get_effective_role` query DB live mỗi request (KHÔNG cache role trong JWT).
    """
    # CASE 1 — Super admin bypass (cross-hub by design, D-V3.1-01 LOCKED).
    if user.role == "admin":
        return

    # CASE 2-5 — Hub_admin gate qua get_effective_role per-hub override.
    try:
        effective = await get_effective_role(db, user.id, target_hub_id)
    except UserNotFoundError as e:
        # CASE 5 — Defensive: stale JWT user_id → reject như hub_admin fail.
        # KHÔNG leak existence của user_id (KHÔNG raise 404).
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={
                "code": "HUB_ADMIN_REQUIRED",
                "message": (
                    "Token user không tồn tại — vui lòng đăng nhập lại "
                    "(stale JWT defensive guard)."
                ),
            },
        ) from e

    if effective != "hub_admin":
        # CASE 3-4 — Hub_admin của hub khác / viewer / editor → reject.
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={
                "code": "HUB_ADMIN_REQUIRED",
                "message": (
                    f"Yêu cầu hub_admin của hub {target_hub_id!r} — "
                    f"effective role hiện tại: {effective!r}"
                ),
            },
        )
    # CASE 2 — hub_admin đúng → pass (return None implicit).
```

**Lưu ý quan trọng:**
- Function signature dùng `*` force keyword-only (Pattern G — carry forward Phase 1 stack pin). Plan 02-03 caller PHẢI gọi `await assert_hub_admin_for(user=..., db=..., target_hub_id=...)` explicit (KHÔNG positional bug).
- `target_hub_id: str` — Pydantic schema CreateUserRequest.hub_id là str (xem `api/app/schemas/users.py:37`); KHÔNG cần convert UUID.
- Return type `None` (pure assertion function — analog `assert` builtin nhưng raise HTTPException thay vì AssertionError).
- KHÔNG return User (KHÁC `require_role` closure trả về User) — caller đã có user object từ `Depends(get_current_user)`.
- Envelope `detail` dict shape `{"code": "HUB_ADMIN_REQUIRED", "message": "..."}` — exception_handler FastAPI sẽ convert sang JSONResponse envelope D6 `{success: false, data: null, error: {code, message}, meta: null}` (carry forward M2 + v3.0 LOCKED).
- `from e` exception chain — defensive UserNotFoundError → HTTPException 403; preserve traceback cho debug nhưng KHÔNG leak user_id existence.
  </action>
  <verify>
    <automated>grep -c "async def assert_hub_admin_for" Hub_All/api/app/auth/dependencies.py && grep -c "HUB_ADMIN_REQUIRED" Hub_All/api/app/auth/dependencies.py && grep -c "from app.auth.role import UserNotFoundError, get_effective_role" Hub_All/api/app/auth/dependencies.py && python -c "import ast; ast.parse(open('Hub_All/api/app/auth/dependencies.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/app/auth/dependencies.py` chứa `async def assert_hub_admin_for(`.
    - File chứa `from app.auth.role import UserNotFoundError, get_effective_role` (import line).
    - File chứa string `"HUB_ADMIN_REQUIRED"` ≥ 2 lần (case 3-4 raise + case 5 raise).
    - File chứa `effective = await get_effective_role(db, user.id, target_hub_id)` (helper call).
    - File chứa `if user.role == "admin":` followed by `return` (case 1 bypass).
    - File chứa `except UserNotFoundError as e:` (case 5 defensive catch).
    - File chứa `if effective != "hub_admin":` (case 3-4 reject branch).
    - File chứa `raise HTTPException` ≥ 2 lần MỚI (super admin bypass + 2 raise patterns).
    - File chứa `*,` force keyword-only signature ở `assert_hub_admin_for` (Pattern G — grep `def assert_hub_admin_for\(\s*\*` qua sed/grep multiline-tolerant).
    - File chứa `status_code=status.HTTP_403_FORBIDDEN` (FastAPI status enum).
    - File chứa comment hoặc docstring mention `D-V3.1-Phase2-D` (traceability tới CONTEXT.md LOCKED decision).
    - `python -c "import ast; ast.parse(open('Hub_All/api/app/auth/dependencies.py').read())"` exit 0 (syntax valid).
    - `cd Hub_All/api && python -c "from app.auth.dependencies import assert_hub_admin_for; import inspect; assert inspect.iscoroutinefunction(assert_hub_admin_for); print('OK async function importable')"` exit 0.
  </acceptance_criteria>
  <done>
    Function `assert_hub_admin_for` async + keyword-only signature có trong dependencies.py; import `get_effective_role` + `UserNotFoundError` từ role.py; envelope `HUB_ADMIN_REQUIRED` + super admin bypass + UserNotFoundError defensive catch; syntax Python hợp lệ; importable runtime.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo unit test test_require_hub_admin_for.py — 5 case D-V3.1-Phase2-D + AsyncMock(AsyncSession)</name>
  <read_first>
    - Hub_All/api/app/auth/dependencies.py (state sau Task 1 — verify assert_hub_admin_for signature + import)
    - Hub_All/api/tests/unit/test_role_helper.py:24-42 (`_make_session` AsyncMock factory pattern — carry forward Plan 01-02)
    - Hub_All/api/tests/unit/test_require_internal_auth.py (pattern `pytest.raises(HTTPException)` + `assert exc_info.value.detail["code"]`)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md §"#8 test_require_hub_admin_for.py" (5-case test template + Pattern C/D)
  </read_first>
  <behavior>
    - Test 1 (super_admin_bypass_no_db_call): user.role='admin' → assert_hub_admin_for return None KHÔNG raise; session.execute.call_count == 0 (case 1 bypass).
    - Test 2 (hub_admin_correct_hub_pass): user.role='editor' + get_effective_role return 'hub_admin' → pass (no raise); session.execute.call_count >= 1 (case 2).
    - Test 3 (hub_admin_wrong_hub_403): user.role='editor' + get_effective_role return 'viewer' (fallback global khi no override) → raise HTTPException 403 + detail['code']=='HUB_ADMIN_REQUIRED' (case 3).
    - Test 4 (viewer_403): user.role='viewer' + get_effective_role return 'viewer' → raise HTTPException 403 + detail['code']=='HUB_ADMIN_REQUIRED' (case 4).
    - Test 5 (user_not_found_403_not_404): patch get_effective_role raise UserNotFoundError → assert_hub_admin_for catch + raise HTTPException 403 (KHÔNG leak 404); detail['code']=='HUB_ADMIN_REQUIRED' (case 5 defensive).
  </behavior>
  <files>api/tests/unit/test_require_hub_admin_for.py</files>
  <action>
Tạo file `api/tests/unit/test_require_hub_admin_for.py` với 5 test case + helper factory.

```python
"""Unit test assert_hub_admin_for validator — Phase 2 Plan 02-01 DEP-01.

Pure Python test, KHÔNG cần Postgres/Redis runtime. Mock AsyncSession.execute
trả về MagicMock result với fetchone() side_effect — cover 5 case D-V3.1-Phase2-D
LOCKED.

Test cases (DEP-01 acceptance — 5 case D-V3.1-Phase2-D):
1. super_admin_bypass: user.role='admin' → pass (KHÔNG call get_effective_role).
2. hub_admin_correct_hub: user.role='editor' + get_effective_role='hub_admin' → pass.
3. hub_admin_wrong_hub: user.role='editor' + get_effective_role='viewer' (no override) → 403.
4. viewer_not_hub_admin: user.role='viewer' + get_effective_role='viewer' → 403.
5. user_not_found_defensive: patch get_effective_role raise UserNotFoundError → 403 (KHÔNG 404).

Carry forward patterns:
- `_make_session` AsyncMock factory — pattern `tests/unit/test_role_helper.py:24-42`.
- `pytest.raises(HTTPException)` + `assert exc_info.value.detail["code"]` — pattern
  `tests/unit/test_require_internal_auth.py` carry forward Phase 6 Plan 06-03.
- `SimpleNamespace` user fixture — pattern Plan 01-02 + nhẹ hơn full User ORM mock.
"""
from __future__ import annotations

from types import SimpleNamespace
from unittest.mock import AsyncMock, MagicMock, patch
from uuid import uuid4

import pytest
from fastapi import HTTPException

from app.auth.dependencies import assert_hub_admin_for
from app.auth.role import UserNotFoundError


def _make_session(*fetchone_returns: object) -> AsyncMock:
    """Build AsyncMock(AsyncSession) trả về sequence fetchone results.

    Carry forward pattern `tests/unit/test_role_helper.py:24-42`.

    Args:
        *fetchone_returns: Tuple sequence — mỗi item là row return cho lần
            execute() tiếp theo. None = row không tồn tại; tuple = row tồn tại.

    Example:
        >>> session = _make_session(('hub_admin',))  # 1 query trả row role='hub_admin'
        >>> session = _make_session(None, ('viewer',))  # 2 queries: 1 None, 2 row
    """
    session = AsyncMock()
    result_mocks = []
    for fetchone_value in fetchone_returns:
        result = MagicMock()
        result.fetchone.return_value = fetchone_value
        result_mocks.append(result)
    session.execute = AsyncMock(side_effect=result_mocks)
    return session


def _make_user(role: str, user_id: object = None) -> SimpleNamespace:
    """SimpleNamespace user fixture — KHÔNG cần full User ORM.

    Carry forward pattern `tests/unit/test_require_internal_auth.py` —
    SimpleNamespace duck-type đủ cho assert_hub_admin_for đọc `user.role` + `user.id`.
    """
    return SimpleNamespace(id=user_id or uuid4(), role=role)


@pytest.mark.asyncio
async def test_super_admin_bypass_returns_none_without_db_call() -> None:
    """Case 1 D-V3.1-Phase2-D: user.role='admin' → bypass, KHÔNG call get_effective_role.

    Verify session.execute KHÔNG được gọi (bypass trước query — performance
    + security guarantee).
    """
    user = _make_user("admin")
    session = AsyncMock()  # KHÔNG cần _make_session vì sẽ KHÔNG gọi execute.
    hub_id = str(uuid4())

    # Không raise → pass case 1.
    result = await assert_hub_admin_for(user=user, db=session, target_hub_id=hub_id)

    assert result is None, "assert_hub_admin_for phải return None khi pass"
    assert session.execute.call_count == 0, (
        "Super admin bypass: get_effective_role KHÔNG được gọi"
    )


@pytest.mark.asyncio
async def test_hub_admin_correct_hub_returns_none() -> None:
    """Case 2 D-V3.1-Phase2-D: user.role='editor' global + hub_admin per-hub → pass.

    STEP 1 (user_hubs.role override): query trả ('hub_admin',) → get_effective_role
    return 'hub_admin' → assert_hub_admin_for pass.
    """
    user = _make_user("editor")
    hub_id = str(uuid4())
    # Mock 1 query: user_hubs.role='hub_admin' → fetchone trả ('hub_admin',).
    session = _make_session(('hub_admin',))

    result = await assert_hub_admin_for(user=user, db=session, target_hub_id=hub_id)

    assert result is None, "assert_hub_admin_for phải return None khi hub_admin đúng"


@pytest.mark.asyncio
async def test_hub_admin_wrong_hub_raises_403_hub_admin_required() -> None:
    """Case 3 D-V3.1-Phase2-D: user.role='editor' + hub khác (no override) → 403.

    STEP 1: user_hubs.role NULL cho hub này → fallthrough STEP 2.
    STEP 2: users.role='editor' (KHÔNG hub_admin) → get_effective_role return 'editor'.
    → assert_hub_admin_for raise 403 HUB_ADMIN_REQUIRED.
    """
    user = _make_user("editor")
    hub_id = str(uuid4())
    # Mock 2 query: user_hubs row None (no override), users.role='editor'.
    session = _make_session(None, ('editor',))

    with pytest.raises(HTTPException) as exc_info:
        await assert_hub_admin_for(user=user, db=session, target_hub_id=hub_id)

    assert exc_info.value.status_code == 403, "Phải raise 403 (KHÔNG 401/404)"
    assert isinstance(exc_info.value.detail, dict), "detail phải là dict envelope"
    assert exc_info.value.detail["code"] == "HUB_ADMIN_REQUIRED", (
        f"Envelope code phải 'HUB_ADMIN_REQUIRED', got {exc_info.value.detail.get('code')!r}"
    )


@pytest.mark.asyncio
async def test_viewer_not_hub_admin_raises_403_hub_admin_required() -> None:
    """Case 4 D-V3.1-Phase2-D: user.role='viewer' global + no override → 403.

    STEP 1: user_hubs row None.
    STEP 2: users.role='viewer' → get_effective_role return 'viewer'.
    → assert_hub_admin_for raise 403 HUB_ADMIN_REQUIRED.
    """
    user = _make_user("viewer")
    hub_id = str(uuid4())
    session = _make_session(None, ('viewer',))

    with pytest.raises(HTTPException) as exc_info:
        await assert_hub_admin_for(user=user, db=session, target_hub_id=hub_id)

    assert exc_info.value.status_code == 403
    assert exc_info.value.detail["code"] == "HUB_ADMIN_REQUIRED"


@pytest.mark.asyncio
async def test_user_not_found_raises_403_not_404_defensive() -> None:
    """Case 5 D-V3.1-Phase2-D: get_effective_role raise UserNotFoundError → catch + 403.

    Defensive: stale JWT user_id (user vừa bị xoá nhưng JWT còn valid signature)
    → assert_hub_admin_for catch UserNotFoundError + raise 403 HUB_ADMIN_REQUIRED.
    KHÔNG raise 404 (leak user_id existence) hoặc 500 (internal error).

    Pattern dùng patch để force get_effective_role raise — KHÔNG mock session
    (vì sẽ KHÔNG được dùng — patch ở module level).
    """
    user = _make_user("editor")
    hub_id = str(uuid4())
    session = AsyncMock()  # KHÔNG quan trọng vì get_effective_role bị patch.

    with patch(
        "app.auth.dependencies.get_effective_role",
        new=AsyncMock(side_effect=UserNotFoundError(f"User {user.id!r} not found")),
    ):
        with pytest.raises(HTTPException) as exc_info:
            await assert_hub_admin_for(user=user, db=session, target_hub_id=hub_id)

    assert exc_info.value.status_code == 403, (
        "Defensive guard: KHÔNG raise 404 leak existence"
    )
    assert exc_info.value.detail["code"] == "HUB_ADMIN_REQUIRED"
    # Verify UserNotFoundError được chain (`raise ... from e`) — exception traceback preserve.
    assert isinstance(exc_info.value.__cause__, UserNotFoundError), (
        "Exception chain phải preserve UserNotFoundError cho debug traceback"
    )
```

**Lưu ý:**
- 5 test case CHÍNH XÁC theo D-V3.1-Phase2-D LOCKED (super admin / hub_admin pass / hub_admin wrong / viewer / user_not_found defensive).
- `@pytest.mark.asyncio` REQUIRED (pytest-asyncio đã cài Phase 3 carry forward).
- `_make_session` factory tái sử dụng pattern Plan 01-02 — không cần fixture conftest.py.
- `SimpleNamespace` thay full User ORM — duck-type đủ (`.role` + `.id`).
- Test 5 dùng `patch("app.auth.dependencies.get_effective_role", new=AsyncMock(...))` — patch ở module nơi import (NOT ở `app.auth.role`) — pattern Python mock standard.
- `exc_info.value.__cause__` verify exception chain — defensive UserNotFoundError preserve.
- KHÔNG import `from app.db.session` hoặc fixture DB — pure mock (T-01-03-03 mitigation tránh accidentally hit real DB).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/unit/test_require_hub_admin_for.py -x --tb=short 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/unit/test_require_hub_admin_for.py` EXISTS.
    - File chứa `from app.auth.dependencies import assert_hub_admin_for` (import target SUT).
    - File chứa `from app.auth.role import UserNotFoundError` (import defensive exception).
    - File chứa `@pytest.mark.asyncio` ≥ 5 lần (5 test cases).
    - File chứa `async def test_` ≥ 5 lần (5 test functions).
    - File chứa `def _make_session(` (helper factory carry forward Plan 01-02).
    - File chứa `pytest.raises(HTTPException)` ≥ 3 lần (case 3 + 4 + 5).
    - File chứa `"HUB_ADMIN_REQUIRED"` ≥ 3 lần (assert envelope code).
    - File chứa `status_code == 403` ≥ 3 lần.
    - File chứa `session.execute.call_count == 0` (test 1 super admin bypass guarantee).
    - File chứa `with patch(` (test 5 force UserNotFoundError).
    - File chứa `exc_info.value.__cause__` (test 5 exception chain verify).
    - `cd Hub_All/api && python -m pytest tests/unit/test_require_hub_admin_for.py -x` exit 0 (tất cả 5 test PASS).
    - `cd Hub_All/api && python -m pytest tests/unit/test_require_hub_admin_for.py --tb=no -q 2>&1 | grep -E "5 passed"` exit 0 (verify đủ 5 test PASS, không skip).
    - File KHÔNG chứa `from app.db.session` (pure mock, KHÔNG hit real DB — T-01-03-03 mitigation).
  </acceptance_criteria>
  <done>
    test_require_hub_admin_for.py có 5 test PASS pytest; mock AsyncSession + patch get_effective_role; cover 5 case D-V3.1-Phase2-D LOCKED; envelope HUB_ADMIN_REQUIRED verify; defensive UserNotFoundError chain verify.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI handler → assert_hub_admin_for | hub_id từ body POST/PATCH = UNTRUSTED input; user + db từ trusted Depends chain (get_current_user + get_session) |
| assert_hub_admin_for → get_effective_role | Trusted internal helper (Plan 01-02); KHÔNG dùng hub_id làm SQL value trực tiếp (named bind params T-01-02-01 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | Caller (handler) gọi assert_hub_admin_for với user object giả | mitigate | Caller phải pass `user = Depends(get_current_user)` (JWT verified upstream); KHÔNG có path bypass tại assert_hub_admin_for layer (lệ thuộc trusted dependency chain). |
| T-02-01-02 | Tampering (SQL injection) | hub_id body input → get_effective_role SQL execution | mitigate | get_effective_role dùng `sqlalchemy.text()` + named bind params `:user_id`/`:hub_id` (T-01-02-01 mitigation Plan 01-02 carry forward); assert_hub_admin_for KHÔNG concat hub_id vào SQL trực tiếp. |
| T-02-01-03 | Information Disclosure | Exception leak user_id tồn tại qua 404 | mitigate | UserNotFoundError → catch + raise HTTPException 403 HUB_ADMIN_REQUIRED (KHÔNG 404); message KHÔNG echo user_id raw (chỉ effective role string từ context). |
| T-02-01-04 | Elevation of Privilege | Super admin bypass có thể bị abuse nếu user.role='admin' bị stale | mitigate | M2 + v3.0 + v3.1 D-V3.1-01 LOCKED — `users.role='admin'` GIỮ semantic super-admin global; demoted admin sẽ có `users.role` != 'admin' (DB UPDATE qua admin CRUD); stale JWT cho super admin có hạn 15 min (M2 AUTH baseline carry forward). |
| T-02-01-05 | DoS | get_effective_role query DB mỗi request → tăng DB load | accept | Mỗi request 1-2 query (user_hubs + users); pgvector index user_hubs PK composite (user_id, hub_id) — query plan index-only scan. M2 baseline tolerate; nếu hot path → Phase 3 FE-04 có thể cache effective role ở context (defer v4.0). |

</threat_model>

<verification>
- File `dependencies.py` import `get_effective_role` + `UserNotFoundError` từ `app.auth.role` (Plan 01-02 carry forward).
- Function `assert_hub_admin_for` có signature keyword-only `*, user, db, target_hub_id` (Pattern G).
- 5 case D-V3.1-Phase2-D LOCKED implement đầy đủ: super admin bypass / hub_admin pass / hub_admin wrong / viewer / user_not_found defensive.
- Envelope HUB_ADMIN_REQUIRED dùng cho case 3-4-5 (D-V3.1-Phase2-B LOCKED).
- 5 unit test PASS pytest — mock AsyncSession + patch get_effective_role.
- File KHÔNG break Phase 1 import chain (`from app.auth.role import ...` đúng module Plan 01-02).
- Phase 2 Plan 02-03 + 02-04 có thể import `from app.auth.dependencies import assert_hub_admin_for` (BLOCKING dep cho Wave 2).
</verification>

<success_criteria>
1. dependencies.py thêm `assert_hub_admin_for` async function + import role.py helpers — syntax Python hợp lệ.
2. test_require_hub_admin_for.py 5 test PASS — cover 5 case D-V3.1-Phase2-D LOCKED.
3. Envelope HUB_ADMIN_REQUIRED dùng đúng 3 raise (case 3 + 4 + 5) — match D-V3.1-Phase2-B.
4. Caller pattern hybrid validator (KHÔNG Depends factory) — match D-V3.1-Phase2-D LOCKED cho hub_id body case.
5. Phase 2 Plan 02-03 + 02-04 blocking dependency ready (Wave 1 BLOCKING).
6. Coverage ≥ 80% trên function mới (D-V3.1-Phase2-F LOCKED — 5/5 case PASS đủ).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-01-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- assert_hub_admin_for validator function thêm vào dependencies.py + import role.py
- 5 unit test PASS — cover 5 case D-V3.1-Phase2-D LOCKED
- Envelope HUB_ADMIN_REQUIRED mới (D-V3.1-Phase2-B LOCKED) — KHÔNG đụng FORBIDDEN existing
- Hybrid pattern (validator function KHÔNG Depends factory) — phù hợp hub_id body POST/PATCH (D-V3.1-Phase2-D LOCKED)
- Wave 2 (Plan 02-02 + 02-03) + Wave 3 (Plan 02-04) consume `from app.auth.dependencies import assert_hub_admin_for`
- Test coverage 100% trên 5 case D-V3.1-Phase2-D — D-V3.1-Phase2-F ≥ 80% threshold satisfied

Commit atomic: `feat(02-01): assert_hub_admin_for validator + 5 unit test (DEP-01)` + body tiếng Việt nói tại sao (Phase 2 BLOCKING dependency cho 5 router users.py refactor + 3 service signature; hybrid pattern validator function thay Depends factory vì hub_id ở body; envelope HUB_ADMIN_REQUIRED mới phân biệt với FORBIDDEN cho frontend FE-01 switch UX).
</output>
</content>
</invoke>