---
phase: 02-backend-rbac-enforcement
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - api/app/services/audit_service.py
  - api/app/services/user_service.py
  - api/app/services/hub_service.py
  - api/app/routers/users.py
  - api/app/routers/hubs.py
  - api/tests/unit/test_audit_actor_scope.py
  - api/tests/integration/test_audit_actor_metadata.py
autonomous: true
requirements: [DEP-05]
must_haves:
  truths:
    - "audit_service.py có helper `build_audit_payload(*, actor_role, actor_hub_id, extra=None) -> dict[str, Any]` nest actor_role + actor_hub_id vào payload dict (D-V3.1-Phase2-C LOCKED — KHÔNG schema migration audit_logs)"
    - "audit_service.py `build_audit_payload` có docstring note (B7): mọi audit-emitter (user_service, hub_service, api_key_service) PHẢI dùng build_audit_payload để thread-through actor_role + actor_hub_id. KHÔNG construct payload dict raw"
    - "user_service.py create() + delete() signature thêm `actor_role: str` + `actor_hub_id: str | None` keyword-only params; 2 callsite enqueue_audit dùng build_audit_payload thay dict literal"
    - "hub_service.py create() + update() + update_status() signature thêm `actor_role` + `actor_hub_id` params (default 'admin' + None vì DEP-04 LOCKED hub mutate luôn super admin); 3 callsite enqueue_audit refactor"
    - "5 router callsite (POST /api/users + DELETE /api/users + POST /api/hubs + PUT /api/hubs + PATCH /api/hubs/status) derive actor_role + actor_hub_id; B5 iter 1 — POST + DELETE dùng patch-style insertion AFTER Plan 02-03 ship lines (KHÔNG full handler rewrite)"
    - "DELETE /api/users derive actor_role + actor_hub_id từ B1 iter 1 context: super admin → 'admin'+None; hub_admin → 'hub_admin'+target_hub_ids[0] (single-hub case sau Plan 02-03 ship validate)"
    - "Audit log row sau hub_admin POST /api/users: payload chứa `actor_role='hub_admin'` + `actor_hub_id='<dmd-uuid>'` + extra fields email + role"
    - "Audit log row sau super admin POST /api/users: payload chứa `actor_role='admin'` + `actor_hub_id` IS NULL"
    - "B7 iter 1 future-proof guard: api_key_service.py grep enqueue_audit count == 0 (verified) — acceptance criterion lock invariant cho khi nào Phase n+ thêm audit emit ở api_key_service phải bắt buộc refactor build_audit_payload"
    - "4 unit test PASS + 2 integration test PASS — verify success criteria #4 ROADMAP Phase 2"
  artifacts:
    - path: "api/app/services/audit_service.py"
      provides: "Helper function `build_audit_payload` nest actor scope vào payload dict + docstring B7 note (mandatory pattern cho audit emitter)"
      contains: "def build_audit_payload"
    - path: "api/app/services/user_service.py"
      provides: "create() + delete() signature thêm actor_role + actor_hub_id; 2 callsite enqueue_audit refactor build_audit_payload"
      contains: "build_audit_payload(actor_role=actor_role"
    - path: "api/app/services/hub_service.py"
      provides: "create() + update() + update_status() signature thêm actor_role + actor_hub_id; 3 callsite refactor"
      contains: "build_audit_payload(actor_role=actor_role"
    - path: "api/tests/unit/test_audit_actor_scope.py"
      provides: "4 unit test build_audit_payload (super admin / hub_admin / extra None / extra override)"
      contains: "def test_"
    - path: "api/tests/integration/test_audit_actor_metadata.py"
      provides: "2 integration test verify payload->>'actor_role' + payload->>'actor_hub_id' nested sau hub_admin + super admin POST /api/users"
      contains: "audit_logs"
  key_links:
    - from: "routers/users.py::create_user"
      to: "service.create(..., actor_role=..., actor_hub_id=...)"
      via: "B5 iter 1 patch-style — AFTER `await assert_hub_admin_for(...)` ship bởi Plan 02-03, INSERT 2 derive lines + UPDATE service.create call site kwargs"
      pattern: 'actor_role = "admin" if user.role == "admin" else "hub_admin"'
    - from: "routers/users.py::delete_user"
      to: "service.delete(..., actor_role=..., actor_hub_id=...)"
      via: "B5 iter 1 patch-style — AFTER Plan 02-03 B1 iter 1 branch (target_hub_ids derived), DERIVE actor từ user.role + (target_hub_ids[0] if hub_admin else None)"
      pattern: 'actor_role = "admin" if user.role == "admin" else "hub_admin"'
    - from: "services/user_service.py::create"
      to: "audit_logs.payload"
      via: "build_audit_payload(actor_role, actor_hub_id, extra={email, role}) → dict → enqueue_audit"
      pattern: 'payload=build_audit_payload\(actor_role=actor_role'
    - from: "integration test"
      to: "audit_logs.payload->>'actor_role'"
      via: "poll-based wait (audit_flush_loop background task) + SQL query JSONB extract"
      pattern: "payload->>'actor_role'"
---

<objective>
DEP-05: Tag audit log với actor scope — extend `AuditEntry.payload` thêm `actor_role` + `actor_hub_id` nest (D-V3.1-Phase2-C LOCKED — KHÔNG schema migration audit_logs). Mọi audit emit từ user_service / hub_service include 2 field này → forensic queries filter by scope qua `payload->>'actor_role'`.

**Revision iter 1 — B5 fix (patch-style POST + DELETE):** Plan 02-04 STEP 4 (POST create) và STEP 5 (DELETE) chuyển từ full handler rewrite sang patch-style insertion để tránh xung đột với Plan 02-03 ship lines. Read Plan 02-03 final state cho POST/DELETE handler shape (sau B1 iter 1 DELETE refactor).

**Revision iter 1 — B7 fix (api_key_service guard):** acceptance criterion lock invariant `grep -c 'enqueue_audit' app/services/api_key_service.py` == 0 — khi nào Phase n+ thêm audit emit ở api_key_service.py PHẢI bắt buộc refactor qua build_audit_payload. Đồng thời thêm docstring vào audit_service.py build_audit_payload nhấn mạnh mandatory pattern.

**Purpose (DEP-05 + D-V3.1-Phase2-C LOCKED):**

ROADMAP success criteria #4 Phase 2: "Audit log row `action='user.create'` chứa `actor_role='hub_admin' + actor_hub_id='<dmd-uuid>'` cho hub_admin operation; super admin → `actor_role='admin' + actor_hub_id=NULL`."

**Strategy (D-V3.1-Phase2-C LOCKED):**
- KHÔNG migration schema thêm column cho audit_logs (M2 baseline payload JSONB nullable đủ chứa).
- Helper `build_audit_payload(*, actor_role, actor_hub_id, extra) -> dict` ở audit_service.py — dedup pattern 5 callsite.
- 5 service callsite thêm params + 5 router callsite derive actor metadata pass xuống.
- B5 iter 1: POST + DELETE router patch-style (insert AFTER Plan 02-03 ship lines).
- B7 iter 1: api_key_service.py grep guard + audit_service.py docstring mandatory pattern note.

**Callsite refactor scope (5 total):**

| File | Function | Line | hub_admin caller? |
|------|----------|------|-------------------|
| user_service.py | create() | 129-202 | YES (POST hub_admin tạo user trong hub mình) |
| user_service.py | delete() | 408-490 | YES (B1 iter 1 — hub_admin DELETE user single-hub-of-own → actor='hub_admin') |
| hub_service.py | create() | 68-129 | NO (DEP-04 hub mutate super-only) — actor_role='admin' always |
| hub_service.py | update() | 210-258 | NO (DEP-04) — actor_role='admin' always |
| hub_service.py | update_status() | 260-302 | NO (DEP-04) — actor_role='admin' always |

**Note:** Hub_service callsite vẫn nhận `actor_role` + `actor_hub_id` params (consistency với user_service) NHƯNG default `'admin'` + `None` vì DEP-04 LOCKED hub mutate super-only. Router caller pass explicit từ `user.role` (sẽ luôn 'admin' vì require_role guard).

**B5 iter 1 DELETE router — context từ Plan 02-03 B1 iter 1:** DELETE handler sau Plan 02-03 dùng `get_current_user` thay `require_role`. Branch:
- Super admin → actor_role='admin', actor_hub_id=None.
- Hub_admin path → `target_hub_ids` đã derived bởi B1 iter 1 query user_hubs membership. Single-hub case (`len(target_hub_ids) == 1`) → actor_role='hub_admin', actor_hub_id=target_hub_ids[0]. (Cross-hub case / orphan case đã return 403 sớm — KHÔNG reach service.delete.)

**Output:** 5 file change service + router + 2 file test mới. DEP-05 deliver complete.

**Carry forward patterns:**
- PATTERNS.md §"#4 audit_service.py" + §"#5 user_service.py" + §"#6 hub_service.py" — refactor template chính xác.
- Pattern G — service signature `*` keyword-only.
- `_wait_usage_count` poll-based wait pattern (conftest.py:778-806) cho integration test audit verify.
- `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` từ Plan 02-02 + 02-03 — DRY.
- `tests/integration/conftest.py:264-284` per-test TRUNCATE (W1 isolation auto-handled bởi app_with_auth).
- W2: hubs table 10 cột reference migration 0001:80-106 + 0003:47-74.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-03-PLAN.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-03-SUMMARY.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/services/audit_service.py
@Hub_All/api/app/services/user_service.py
@Hub_All/api/app/services/hub_service.py
@Hub_All/api/app/services/api_key_service.py
@Hub_All/api/app/routers/users.py
@Hub_All/api/app/routers/hubs.py
@Hub_All/api/migrations/versions/0001_initial_schema.py
@Hub_All/api/migrations/versions/0003_phase5_schema_reconcile.py

<interfaces>
<!-- Pattern source-of-truth từ audit_service + user_service + hub_service. -->

audit_service.py hiện tại (line 51-94 — verify state):
```python
@dataclass
class AuditEntry:
    action: str
    user_id: str | None = None
    target_type: str | None = None
    target_id: str | None = None
    hub_id: str | None = None
    payload: dict[str, Any] | None = None
    request_id: str | None = None


def enqueue_audit(entry: AuditEntry) -> None:
    try:
        _get_queue().put_nowait(entry)
    except asyncio.QueueFull:
        logger.warning("audit_queue_full_dropped: action=%s", entry.action)
```

Phase 2 ADD helper function (AFTER AuditEntry dataclass, BEFORE _queue module-level — line ~67):
```python
def build_audit_payload(
    *,
    actor_role: str,
    actor_hub_id: str | None,
    extra: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Phase 2 Plan 02-04 DEP-05 (D-V3.1-Phase2-C LOCKED) — Nest actor scope vào payload.

    Helper convenience để dedup pattern 5 callsite enqueue_audit (user_service +
    hub_service). KHÔNG migration schema thêm cột cho audit_logs — payload JSONB
    đủ chứa metadata (audit_logs.payload nullable JSONB — migration 0001).

    **Pattern Phase 2 DEP-05 (B7 iter 1 MANDATORY):** mọi audit-emitter
    (user_service, hub_service, api_key_service) PHẢI dùng build_audit_payload
    để thread-through actor_role + actor_hub_id. KHÔNG construct payload dict raw.
    Khi nào api_key_service.py thêm audit emit (currently 0 enqueue_audit calls
    — Phase 2 DEP-05 chưa cover) → BẮT BUỘC refactor add actor_role + actor_hub_id
    qua helper này.

    Args:
        actor_role: 'admin' (super admin) | 'hub_admin' (per-hub).
        actor_hub_id: None nếu super admin (cross-hub op); hub_id cụ thể nếu hub_admin.
        extra: payload-specific data (email, role, code, name, ...) — merge vào dict.

    Returns:
        dict shape: {actor_role: str, actor_hub_id: str | None, **extra}

    Forensic query:
        SELECT * FROM audit_logs WHERE payload->>'actor_role' = 'hub_admin'
          AND payload->>'actor_hub_id' = '<dmd-uuid>';
    """
    base: dict[str, Any] = {
        "actor_role": actor_role,
        "actor_hub_id": actor_hub_id,
    }
    if extra:
        base.update(extra)
    return base
```

user_service.py create() callsite hiện tại (line 182-192):
```python
enqueue_audit(
    AuditEntry(
        action="user.create",
        user_id=str(created_by),
        target_type="user",
        target_id=str(user_id),
        hub_id=req.hub_id,
        payload={"email": req.email, "role": req.role},
        request_id=request_id,
    )
)
```

user_service.py create() Phase 2 REFACTOR (signature + callsite):
```python
async def create(
    self,
    *,
    req: CreateUserRequest,
    created_by: UUID,
    actor_role: str,                # ← Phase 2 thêm
    actor_hub_id: str | None,       # ← Phase 2 thêm
    request_id: str | None = None,
) -> UserWithRolesResponse:
    # ... INSERT lines 144-178 GIỮ NGUYÊN

    enqueue_audit(
        AuditEntry(
            action="user.create",
            user_id=str(created_by),
            target_type="user",
            target_id=str(user_id),
            hub_id=req.hub_id,
            payload=build_audit_payload(
                actor_role=actor_role,
                actor_hub_id=actor_hub_id,
                extra={"email": req.email, "role": req.role},
            ),
            request_id=request_id,
        )
    )
    # ... rest unchanged
```

routers/users.py POST create_user handler — B5 iter 1 patch-style AFTER Plan 02-03 ship:
- Plan 02-03 đã add line `await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)` ở line ~98 (sau body parse).
- B5 iter 1 patch-style: INSERT 2 derive lines + UPDATE service.create kwargs call site:

```python
# AFTER line `await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)`,
# INSERT 2 derive lines:
actor_role = "admin" if user.role == "admin" else "hub_admin"
actor_hub_id = None if user.role == "admin" else req.hub_id

# UPDATE call site `await service.create(req=req, created_by=user.id, request_id=request_id)`
# add 2 keyword args:
result = await service.create(
    req=req,
    created_by=user.id,
    actor_role=actor_role,          # ← Phase 2 Plan 02-04 B5 iter 1 INSERT
    actor_hub_id=actor_hub_id,      # ← Phase 2 Plan 02-04 B5 iter 1 INSERT
    request_id=request_id,
)
```

routers/users.py DELETE delete_user handler — B5 iter 1 patch-style AFTER Plan 02-03 B1 iter 1 ship:
- Plan 02-03 B1 iter 1 đã add DELETE branch query user_hubs → `target_hub_ids` list derived.
- Super admin path → KHÔNG có target_hub_ids (skip branch).
- Hub_admin single-hub path → `target_hub_ids[0]` = caller's scope (assert_hub_admin_for pass).
- B5 iter 1 patch-style: INSERT 2 derive lines BEFORE `service.delete(...)` call:

```python
# AFTER Plan 02-03 B1 iter 1 branch block (if user.role != "admin": ... query
# user_hubs ... assert_hub_admin_for or 403 early return), BEFORE
# `request_id = getattr(...)` ở line ~end of branch:

# Phase 2 Plan 02-04 DEP-05 B5 iter 1 — derive actor metadata cho audit payload.
# Super admin → ('admin', None). Hub_admin single-hub case → ('hub_admin', target_hub_ids[0]).
# (Cross-hub + orphan đã 403 sớm trong B1 iter 1 — KHÔNG reach đây.)
actor_role = "admin" if user.role == "admin" else "hub_admin"
actor_hub_id = (
    None if user.role == "admin"
    else target_hub_ids[0]  # hub_admin single-hub guaranteed by B1 iter 1
)

# UPDATE call site service.delete kwargs:
deleted = await service.delete(
    user_id=user_uuid,
    deleted_by=user.id,
    actor_role=actor_role,          # ← Phase 2 Plan 02-04 B5 iter 1 INSERT
    actor_hub_id=actor_hub_id,      # ← Phase 2 Plan 02-04 B5 iter 1 INSERT
    request_id=request_id,
)
```

Imports thêm ở 2 service file (user_service.py + hub_service.py):
```python
from app.services.audit_service import (
    AuditEntry,
    build_audit_payload,    # ← Phase 2 Plan 02-04 NEW
    enqueue_audit,
)
```

Poll-based wait pattern integration test audit (conftest.py:778-806 carry forward):
```python
async def _wait_audit_count(conn, action: str, expected: int, *, timeout_s: float = 2.0):
    import asyncio, time
    deadline = time.monotonic() + timeout_s
    last = -1
    while time.monotonic() < deadline:
        last = await conn.fetchval(
            "SELECT count(*) FROM audit_logs WHERE action = $1", action
        )
        if last >= expected:
            return int(last)
        await asyncio.sleep(0.05)
    raise AssertionError(f"audit_logs count={last}, kỳ vọng ≥ {expected}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm `build_audit_payload` helper + B7 docstring mandatory note + refactor 5 service callsite + 5 router callsite (B5 iter 1 patch-style cho POST + DELETE) (DEP-05 D-V3.1-Phase2-C)</name>
  <read_first>
    - Hub_All/api/app/services/audit_service.py:1-100 (AuditEntry dataclass + enqueue_audit pattern — thêm helper sau dataclass + B7 docstring mandatory note)
    - Hub_All/api/app/services/api_key_service.py (toàn bộ — B7 iter 1 verify enqueue_audit count == 0 hiện tại; KHÔNG modify file này, chỉ acceptance criterion guard)
    - Hub_All/api/app/services/user_service.py:129-202 (create() signature + callsite 182-192 refactor)
    - Hub_All/api/app/services/user_service.py:408-490 (delete() signature + callsite 465-478 refactor)
    - Hub_All/api/app/services/hub_service.py:68-129 (create() signature + callsite 113-122)
    - Hub_All/api/app/services/hub_service.py:210-258 (update() signature + callsite 246-256)
    - Hub_All/api/app/services/hub_service.py:260-302 (update_status() signature + callsite 285-295)
    - Hub_All/api/app/routers/users.py (POST create + DELETE handler — B5 iter 1 patch-style INSERT AFTER Plan 02-03 ship lines)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-03-SUMMARY.md (Plan 02-03 final state — DELETE handler có target_hub_ids derived bởi B1 iter 1)
    - Hub_All/api/app/routers/hubs.py (POST + PUT + PATCH status handler — thêm derive — full rewrite OK vì hubs handler chưa bị Plan 02-03 touch)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md §"#4-6" refactor template
  </read_first>
  <behavior>
    - Test 1 (build_audit_payload super admin): build_audit_payload(actor_role='admin', actor_hub_id=None, extra={'email':'x@y'}) → dict {actor_role:'admin', actor_hub_id:None, email:'x@y'}.
    - Test 2 (build_audit_payload hub_admin): build_audit_payload(actor_role='hub_admin', actor_hub_id='dmd-uuid', extra={'code':'dmd'}) → dict actor_role + actor_hub_id + code.
    - Test 3 (extra None): build_audit_payload(actor_role='admin', actor_hub_id=None, extra=None) → dict chỉ 2 key.
    - Test 4 (extra override): build_audit_payload(actor_role='admin', actor_hub_id=None, extra={'actor_role':'override'}) → extra override (document behavior).
  </behavior>
  <files>api/app/services/audit_service.py, api/app/services/user_service.py, api/app/services/hub_service.py, api/app/routers/users.py, api/app/routers/hubs.py</files>
  <action>
**STEP 1 — Thêm `build_audit_payload` helper + B7 docstring mandatory note vào `api/app/services/audit_service.py` (NGAY SAU AuditEntry dataclass line ~65, TRƯỚC `_queue` module-level line ~68):**

```python
def build_audit_payload(
    *,
    actor_role: str,
    actor_hub_id: str | None,
    extra: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Phase 2 Plan 02-04 DEP-05 (D-V3.1-Phase2-C LOCKED) — Nest actor scope vào payload.

    Helper convenience dedup pattern 5 callsite enqueue_audit (user_service +
    hub_service). KHÔNG migration schema thêm cột cho audit_logs — payload
    JSONB nullable đủ chứa metadata (migration 0001 baseline).

    **Pattern Phase 2 DEP-05 (B7 iter 1 MANDATORY):** mọi audit-emitter
    (user_service, hub_service, api_key_service) PHẢI dùng build_audit_payload
    để thread-through actor_role + actor_hub_id. KHÔNG construct payload dict
    raw. Khi nào api_key_service.py thêm audit emit (currently 0 enqueue_audit
    calls — Phase 2 DEP-05 chưa cover) → BẮT BUỘC refactor add actor_role +
    actor_hub_id qua helper này. Acceptance criterion lock invariant tại Plan
    02-04 acceptance:
        grep -c "enqueue_audit" app/services/api_key_service.py == 0

    Args:
        actor_role: 'admin' (super admin) | 'hub_admin' (per-hub).
        actor_hub_id: None nếu super admin (cross-hub op); hub_id cụ thể nếu hub_admin.
        extra: payload-specific data (email, role, code, name, ...) — merge vào dict.

    Returns:
        dict shape: {actor_role: str, actor_hub_id: str | None, **extra}

    Forensic query (D-V3.1-Phase2-C LOCKED):
        SELECT * FROM audit_logs WHERE payload->>'actor_role' = 'hub_admin'
          AND payload->>'actor_hub_id' = '<dmd-uuid>';

    Example:
        >>> build_audit_payload(
        ...     actor_role='hub_admin',
        ...     actor_hub_id='dmd-uuid',
        ...     extra={'email': 'new@dmd.vn', 'role': 'viewer'},
        ... )
        {'actor_role': 'hub_admin', 'actor_hub_id': 'dmd-uuid',
         'email': 'new@dmd.vn', 'role': 'viewer'}
    """
    base: dict[str, Any] = {
        "actor_role": actor_role,
        "actor_hub_id": actor_hub_id,
    }
    if extra:
        base.update(extra)
    return base
```

**STEP 2 — Refactor `api/app/services/user_service.py` (2 callsite: create + delete):**

Update import top:
```python
from app.services.audit_service import (
    AuditEntry,
    build_audit_payload,    # ← Phase 2 Plan 02-04 NEW
    enqueue_audit,
)
```

**user_service.create() signature update (line 129-135):**
```python
async def create(
    self,
    *,
    req: CreateUserRequest,
    created_by: UUID,
    actor_role: str,                # ← Phase 2 Plan 02-04 NEW
    actor_hub_id: str | None,       # ← Phase 2 Plan 02-04 NEW
    request_id: str | None = None,
) -> UserWithRolesResponse:
```

**user_service.create() callsite refactor (line 182-192) — REPLACE dict literal `{"email": req.email, "role": req.role}` thành build_audit_payload:**
```python
enqueue_audit(
    AuditEntry(
        action="user.create",
        user_id=str(created_by),
        target_type="user",
        target_id=str(user_id),
        hub_id=req.hub_id,
        payload=build_audit_payload(
            actor_role=actor_role,
            actor_hub_id=actor_hub_id,
            extra={"email": req.email, "role": req.role},
        ),
        request_id=request_id,
    )
)
```

**user_service.delete() signature update (line 408-414):**
```python
async def delete(
    self,
    *,
    user_id: UUID,
    deleted_by: UUID,
    actor_role: str,                # ← Phase 2 Plan 02-04 NEW
    actor_hub_id: str | None,       # ← Phase 2 Plan 02-04 NEW
    request_id: str | None = None,
) -> bool:
```

**user_service.delete() callsite refactor (line 465-478):**
```python
enqueue_audit(
    AuditEntry(
        action="user.delete",
        user_id=str(deleted_by),
        target_type="user",
        target_id=str(user_id),
        hub_id=None,
        payload=build_audit_payload(
            actor_role=actor_role,
            actor_hub_id=actor_hub_id,
            extra={
                "deleted_email": email_to_delete,
                "deleted_role": role_to_delete,
            },
        ),
        request_id=request_id,
    )
)
```

**STEP 3 — Refactor `api/app/services/hub_service.py` (3 callsite: create + update + update_status):**

Update import top (cùng pattern user_service).

**hub_service.create() signature update (line 68-74) — default 'admin' + None vì DEP-04 LOCKED:**
```python
async def create(
    self,
    *,
    req: CreateHubRequest,
    created_by: UUID,
    actor_role: str = "admin",          # ← Phase 2 Plan 02-04 — default 'admin' vì DEP-04
    actor_hub_id: str | None = None,    # ← Phase 2 Plan 02-04 — default None
    request_id: str | None = None,
) -> HubResponse:
```

**hub_service.create() callsite refactor (line 113-122):**
```python
enqueue_audit(
    AuditEntry(
        action="hub.create",
        user_id=str(created_by),
        target_type="hub",
        target_id=str(hub_id),
        hub_id=str(hub_id),
        payload=build_audit_payload(
            actor_role=actor_role,
            actor_hub_id=actor_hub_id,
            extra={"code": req.code, "name": req.name},
        ),
        request_id=request_id,
    )
)
```

**hub_service.update() signature update (line 210-217) — apply cùng pattern (default 'admin' + None):**
```python
async def update(
    self,
    *,
    hub_id: UUID,
    req: UpdateHubRequest,
    updated_by: UUID,
    actor_role: str = "admin",          # ← Phase 2 Plan 02-04
    actor_hub_id: str | None = None,    # ← Phase 2 Plan 02-04
    request_id: str | None = None,
) -> HubResponse | None:
```

**hub_service.update() callsite refactor (line 246-256):**
```python
enqueue_audit(
    AuditEntry(
        action="hub.update",
        user_id=str(updated_by),
        target_type="hub",
        target_id=str(hub_id),
        hub_id=str(hub_id),
        payload=build_audit_payload(
            actor_role=actor_role,
            actor_hub_id=actor_hub_id,
            extra={"changed": req.model_dump(exclude_none=True)},
        ),
        request_id=request_id,
    )
)
```

**hub_service.update_status() signature update (line 260-267):**
```python
async def update_status(
    self,
    *,
    hub_id: UUID,
    status: str,
    updated_by: UUID,
    actor_role: str = "admin",          # ← Phase 2 Plan 02-04
    actor_hub_id: str | None = None,    # ← Phase 2 Plan 02-04
    request_id: str | None = None,
) -> bool:
```

**hub_service.update_status() callsite refactor (line 285-295):**
```python
enqueue_audit(
    AuditEntry(
        action="hub.update",
        user_id=str(updated_by),
        target_type="hub",
        target_id=str(hub_id),
        hub_id=str(hub_id),
        payload=build_audit_payload(
            actor_role=actor_role,
            actor_hub_id=actor_hub_id,
            extra={"status": status},
        ),
        request_id=request_id,
    )
)
```

**STEP 4 — Refactor `api/app/routers/users.py` POST /api/users — B5 iter 1 patch-style INSERT AFTER Plan 02-03 ship:**

Plan 02-03 đã refactor POST `create_user` handler với `await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)` ở line ~98 (sau body parse).

**PATCH instruction (B5 iter 1):**
1. **AFTER** line chứa `await assert_hub_admin_for(user=user, db=db, target_hub_id=req.hub_id)` (added bởi Plan 02-03), **INSERT** 2 derive lines:

```python
    # Phase 2 Plan 02-04 DEP-05 B5 iter 1 — derive actor metadata cho audit payload nest.
    actor_role = "admin" if user.role == "admin" else "hub_admin"
    actor_hub_id = None if user.role == "admin" else req.hub_id
```

2. **UPDATE** call site `await service.create(req=req, created_by=user.id, request_id=request_id)` to add 2 keyword args:

```python
    result = await service.create(
        req=req,
        created_by=user.id,
        actor_role=actor_role,          # ← Phase 2 Plan 02-04 B5 iter 1
        actor_hub_id=actor_hub_id,      # ← Phase 2 Plan 02-04 B5 iter 1
        request_id=request_id,
    )
```

KHÔNG full handler rewrite — chỉ insert 2 derive lines + update kwargs.

**STEP 5 — Refactor `api/app/routers/users.py` DELETE /api/users/{user_id} — B5 iter 1 patch-style AFTER Plan 02-03 B1 iter 1 ship:**

Plan 02-03 B1 iter 1 đã refactor DELETE `delete_user` handler với:
- `Depends(get_current_user)` thay require_role.
- Branch `if user.role != "admin":` query user_hubs → `target_hub_ids` derived.
- Early return 403 cho cross-hub (CROSS_HUB_USER_DELETE_DENIED) + orphan (HUB_ADMIN_REQUIRED) cases.
- `await assert_hub_admin_for(user=user, db=db, target_hub_id=target_hub_ids[0])` cho single-hub case.

**PATCH instruction (B5 iter 1):**
1. **AFTER** Plan 02-03 B1 iter 1 branch block (end of `if user.role != "admin":` block, ngay TRƯỚC `request_id = getattr(request.state, "request_id", None)` ở line ~end of branch), **INSERT** 4 derive lines:

```python
    # Phase 2 Plan 02-04 DEP-05 B5 iter 1 — derive actor metadata cho audit payload.
    # Super admin → ('admin', None). Hub_admin single-hub (B1 iter 1 guaranteed) →
    # ('hub_admin', target_hub_ids[0]). Cross-hub + orphan đã 403 sớm.
    actor_role = "admin" if user.role == "admin" else "hub_admin"
    actor_hub_id = (
        None if user.role == "admin" else target_hub_ids[0]
    )
```

2. **UPDATE** call site `await service.delete(user_id=user_uuid, deleted_by=user.id, request_id=request_id)` to add 2 keyword args:

```python
    try:
        deleted = await service.delete(
            user_id=user_uuid,
            deleted_by=user.id,
            actor_role=actor_role,          # ← Phase 2 Plan 02-04 B5 iter 1
            actor_hub_id=actor_hub_id,      # ← Phase 2 Plan 02-04 B5 iter 1
            request_id=request_id,
        )
    except LastAdminError as e:
        return resp.conflict(message=str(e), code="LAST_ADMIN")
```

KHÔNG full handler rewrite — chỉ insert 4 derive lines + update kwargs.

**STEP 6 — Refactor `api/app/routers/hubs.py` (POST + PUT + PATCH status) — thêm derive actor (luôn 'admin' + None vì DEP-04 LOCKED). Hubs handler chưa bị Plan 02-03 touch → full kwargs update OK:**

**POST /api/hubs callsite extend:**
```python
result = await service.create(
    req=req,
    created_by=user.id,
    actor_role="admin",         # ← Phase 2 Plan 02-04 — DEP-04 LOCKED hub mutate super-only
    actor_hub_id=None,          # ← Phase 2 Plan 02-04
    request_id=request_id,
)
```

**PUT /api/hubs/{id} callsite extend (line 155-160):**
```python
result = await service.update(
    hub_id=hub_uuid,
    req=req,
    updated_by=user.id,
    actor_role="admin",         # ← Phase 2 Plan 02-04
    actor_hub_id=None,          # ← Phase 2 Plan 02-04
    request_id=request_id,
)
```

**PATCH /api/hubs/{id}/status callsite extend (line 185-190):**
```python
ok = await service.update_status(
    hub_id=hub_uuid,
    status=req.status,
    updated_by=user.id,
    actor_role="admin",         # ← Phase 2 Plan 02-04
    actor_hub_id=None,          # ← Phase 2 Plan 02-04
    request_id=request_id,
)
```

**Lưu ý quan trọng:**
- `build_audit_payload` accept `extra=None` (default) — caller có thể skip nếu KHÔNG extra fields.
- 5 service callsite enqueue_audit refactor — KHÔNG đụng logic INSERT (chỉ payload dict build).
- Hub service: signature default 'admin' + None vì DEP-04 LOCKED — router caller pass explicit (KHÔNG dùng default) cho traceability.
- User service: NO default (force keyword required) — router PHẢI pass actor_role/actor_hub_id derive từ user.role.
- Import `from app.services.audit_service import AuditEntry, build_audit_payload, enqueue_audit` (3 symbol) — verify import line không break.
- B5 iter 1: POST + DELETE router patch-style INSERT (KHÔNG full rewrite) — tránh xung đột với Plan 02-03 ship lines.
- B5 iter 1 DELETE: derive `actor_hub_id = target_hub_ids[0]` valid vì Plan 02-03 B1 iter 1 đã guarantee single-hub case khi reach service.delete (cross-hub + orphan đã 403 sớm).
- B7 iter 1: api_key_service.py grep guard enqueue_audit count == 0 — acceptance criterion lock invariant.
- Backward compat: existing tests Phase 5+ M2 + v3.0 dùng `await service.create(req=..., created_by=...)` WITHOUT actor params SẼ FAIL (TypeError missing required keyword args) — Plan 02-04 acceptance criteria PHẢI verify tests existing được update HOẶC sẽ FAIL → orchestrator escalation.
  </action>
  <verify>
    <automated>grep -c "def build_audit_payload" Hub_All/api/app/services/audit_service.py && grep -c "MANDATORY" Hub_All/api/app/services/audit_service.py && grep -c "build_audit_payload(actor_role=actor_role" Hub_All/api/app/services/user_service.py && grep -c "build_audit_payload(actor_role=actor_role" Hub_All/api/app/services/hub_service.py && grep -c "actor_role=actor_role" Hub_All/api/app/routers/users.py && grep -c "actor_role=\"admin\"" Hub_All/api/app/routers/hubs.py && grep -c "enqueue_audit" Hub_All/api/app/services/api_key_service.py && python -c "import ast; ast.parse(open('Hub_All/api/app/services/audit_service.py').read()); ast.parse(open('Hub_All/api/app/services/user_service.py').read()); ast.parse(open('Hub_All/api/app/services/hub_service.py').read()); ast.parse(open('Hub_All/api/app/routers/users.py').read()); ast.parse(open('Hub_All/api/app/routers/hubs.py').read()); print('syntax OK 5 files')"</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/api/app/services/audit_service.py` chứa `def build_audit_payload(` (helper function).
    - `Hub_All/api/app/services/audit_service.py` chứa `"actor_role": actor_role` + `"actor_hub_id": actor_hub_id` (base dict keys).
    - `Hub_All/api/app/services/audit_service.py` build_audit_payload docstring chứa `MANDATORY` HOẶC `B7` HOẶC `api_key_service` (B7 iter 1 mandatory pattern note).
    - `Hub_All/api/app/services/user_service.py` chứa `from app.services.audit_service import` mention `build_audit_payload` (import update).
    - `Hub_All/api/app/services/user_service.py` chứa `actor_role: str` AND `actor_hub_id: str | None` trong signature (2 location — create + delete).
    - `Hub_All/api/app/services/user_service.py` chứa `build_audit_payload(actor_role=actor_role` ≥ 2 lần (2 callsite create + delete).
    - `Hub_All/api/app/services/hub_service.py` chứa `actor_role: str = "admin"` (default vì DEP-04 LOCKED) ≥ 3 lần (3 signature: create + update + update_status).
    - `Hub_All/api/app/services/hub_service.py` chứa `build_audit_payload(actor_role=actor_role` ≥ 3 lần (3 callsite).
    - `Hub_All/api/app/routers/users.py` chứa `actor_role = "admin" if user.role == "admin" else "hub_admin"` ≥ 2 lần (POST create + DELETE derive — B5 iter 1).
    - `Hub_All/api/app/routers/users.py` chứa `actor_hub_id = None if user.role == "admin" else req.hub_id` ≥ 1 lần (POST create derive — B5 iter 1).
    - `Hub_All/api/app/routers/users.py` chứa `target_hub_ids[0]` trong DELETE handler context (B5 iter 1 patch-style — actor_hub_id derive cho hub_admin single-hub case).
    - `Hub_All/api/app/routers/users.py` chứa `actor_role=actor_role` + `actor_hub_id=actor_hub_id` pass xuống service.create (POST).
    - `Hub_All/api/app/routers/users.py` chứa `actor_role=actor_role` + `actor_hub_id=actor_hub_id` pass xuống service.delete (DELETE — B5 iter 1 derive thay vì hardcode "admin"+None).
    - `Hub_All/api/app/routers/hubs.py` chứa `actor_role="admin"` + `actor_hub_id=None` pass xuống service.create/update/update_status ≥ 3 lần (3 endpoint mutate; DEP-04 LOCKED hard-code OK).
    - File chứa comment hoặc docstring mention `DEP-05` HOẶC `D-V3.1-Phase2-C` HOẶC `Phase 2 Plan 02-04` HOẶC `B5 iter 1` HOẶC `B7 iter 1` (traceability) trong audit_service.py + user_service.py.
    - **B7 iter 1 future-proof guard:** `cd Hub_All/api && python -c "import subprocess; out = subprocess.check_output(['grep', '-c', 'enqueue_audit', 'app/services/api_key_service.py']).decode().strip(); count = int(out) if out.isdigit() else 0; assert count == 0, f'api_key_service.py có {count} enqueue_audit call — Phase 2 DEP-05 chưa cover, cần refactor add actor_role + actor_hub_id'"` exit 0.
    - `python -c "import ast; ast.parse(open('Hub_All/api/app/services/audit_service.py').read())"` exit 0 (syntax valid).
    - `cd Hub_All/api && python -c "from app.services.audit_service import build_audit_payload; result = build_audit_payload(actor_role='hub_admin', actor_hub_id='x', extra={'k':'v'}); assert result == {'actor_role':'hub_admin','actor_hub_id':'x','k':'v'}; print('OK')"` exit 0.
    - `cd Hub_All/api && python -c "from app.routers.users import router; from app.routers.hubs import router as hubs_router; print('OK 2 routers importable')"` exit 0 (verify import chain KHÔNG break).
  </acceptance_criteria>
  <done>
    audit_service.py + build_audit_payload helper + B7 docstring mandatory note; user_service.py + hub_service.py + 5 callsite refactor + signature thêm actor params; routers/users.py + routers/hubs.py + 5 caller derive actor_role/actor_hub_id (POST + DELETE patch-style B5 iter 1); B7 iter 1 future-proof guard: api_key_service.py grep enqueue_audit == 0 verified; backward compat: PHẢI rebuild existing tests dùng service.create signature.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo unit test test_audit_actor_scope.py (4 case) + integration test test_audit_actor_metadata.py (2 scenario poll-based wait)</name>
  <read_first>
    - Hub_All/api/app/services/audit_service.py (state sau Task 1 — verify build_audit_payload)
    - Hub_All/api/tests/integration/conftest.py:778-806 (`_wait_usage_count` poll-based wait pattern)
    - Hub_All/api/tests/integration/conftest.py:264-284 (W1 verify — app_with_auth TRUNCATE 8 bảng per-test RESTART IDENTITY CASCADE)
    - Hub_All/api/migrations/versions/0001_initial_schema.py:80-106 (W2 — hubs base 7 cột)
    - Hub_All/api/migrations/versions/0003_phase5_schema_reconcile.py:47-74 (W2 — hubs ADD 3 cột; INSERT 9 cột valid)
    - Hub_All/api/tests/integration/test_dep_users_scope.py (Plan 02-03 helpers `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` — import DRY)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md §"#9" + §"#10 + Pattern F"
  </read_first>
  <behavior>
    - Unit Test 1 (super admin nest): build_audit_payload('admin', None, {'email':'x'}) → assert dict shape 3 keys (actor_role + actor_hub_id None + email).
    - Unit Test 2 (hub_admin nest): build_audit_payload('hub_admin', 'dmd-uuid', {'role':'viewer'}) → assert actor_role + actor_hub_id + role.
    - Unit Test 3 (extra None): build_audit_payload('admin', None, None) → assert dict CHỈ 2 keys actor_role + actor_hub_id.
    - Unit Test 4 (extra override): build_audit_payload('admin', None, {'actor_role':'override'}) → assert extra override (document behavior — caller responsibility).
    - Integration Test 5 (hub_admin user.create audit verify): hub_admin dmd POST /api/users → poll audit_logs row action='user.create' → payload->>'actor_role' == 'hub_admin' + payload->>'actor_hub_id' == dmd_id.
    - Integration Test 6 (super admin user.create audit verify): super admin POST /api/users → poll audit_logs row → payload->>'actor_role' == 'admin' + payload->>'actor_hub_id' IS NULL.
  </behavior>
  <files>api/tests/unit/test_audit_actor_scope.py, api/tests/integration/test_audit_actor_metadata.py</files>
  <action>
**STEP 1 — Tạo unit test `api/tests/unit/test_audit_actor_scope.py`:**

```python
"""Unit test build_audit_payload — Phase 2 Plan 02-04 DEP-05 D-V3.1-Phase2-C LOCKED.

Pure Python test — KHÔNG cần Postgres/Redis. Verify shape dict output cho 4 case.
"""
from __future__ import annotations

from app.services.audit_service import build_audit_payload


def test_super_admin_nests_actor_role_admin_and_null_hub() -> None:
    """Case 1: super admin → actor_role='admin' + actor_hub_id=None + extra merged."""
    result = build_audit_payload(
        actor_role="admin",
        actor_hub_id=None,
        extra={"email": "x@y.com", "role": "viewer"},
    )
    assert result["actor_role"] == "admin"
    assert result["actor_hub_id"] is None
    assert result["email"] == "x@y.com"
    assert result["role"] == "viewer"
    assert len(result) == 4


def test_hub_admin_nests_actor_role_hub_admin_and_hub_id() -> None:
    """Case 2: hub_admin → actor_role='hub_admin' + actor_hub_id=<uuid> + extra."""
    hub_id = "dmd-uuid-12345"
    result = build_audit_payload(
        actor_role="hub_admin",
        actor_hub_id=hub_id,
        extra={"code": "dmd", "name": "Đỗ Minh Đường"},
    )
    assert result["actor_role"] == "hub_admin"
    assert result["actor_hub_id"] == hub_id
    assert result["code"] == "dmd"
    assert result["name"] == "Đỗ Minh Đường"


def test_extra_none_returns_only_actor_keys() -> None:
    """Case 3: extra=None → dict CHỈ 2 key actor (KHÔNG merge thêm)."""
    result = build_audit_payload(
        actor_role="admin", actor_hub_id=None, extra=None
    )
    assert set(result.keys()) == {"actor_role", "actor_hub_id"}
    assert result["actor_role"] == "admin"
    assert result["actor_hub_id"] is None


def test_extra_can_override_actor_keys_document_behavior() -> None:
    """Case 4 defensive: extra có actor_role/actor_hub_id sẽ OVERRIDE base.

    Document behavior — caller responsibility KHÔNG pass extra với actor keys.
    Helper KHÔNG enforce hard guard (PEP 20 simple > complex; caller bug guard
    qua code review thay vì runtime check).
    """
    result = build_audit_payload(
        actor_role="admin",
        actor_hub_id=None,
        extra={"actor_role": "override_value"},
    )
    assert result["actor_role"] == "override_value", (
        "Document behavior: extra override base (caller bug responsibility)."
    )
```

**STEP 2 — Tạo integration test `api/tests/integration/test_audit_actor_metadata.py`:**

```python
"""Integration test audit_logs payload actor metadata — Phase 2 Plan 02-04 DEP-05.

2 scenario verify ROADMAP success criteria #4:
1. Hub_admin dmd POST /api/users → audit_logs row action='user.create' payload chứa
   actor_role='hub_admin' + actor_hub_id='<dmd-uuid>'.
2. Super admin POST /api/users → audit_logs row payload chứa actor_role='admin'
   + actor_hub_id IS NULL.

W1 isolation note: `app_with_auth` fixture (conftest.py:264-284) đã TRUNCATE 8 bảng
per-test RESTART IDENTITY CASCADE — fixture seed dưới KHÔNG cần re-truncate.

W2 schema note: hubs table 10 cột (migration 0001:80-106 + 0003:47-74) — INSERT 9 cột
(bỏ updated_at server_default NOW()) là VALID.

Carry forward patterns:
- `_seed_hub_admin_user` + `seed_hubs_dmd_tdt` từ Plan 02-02 + 02-03.
- `_wait_audit_count` poll-based wait pattern conftest.py:778-806 (audit_flush_loop
  background task — KHÔNG sync, phải poll).
- asyncpg direct connect cho query JSONB payload->>'actor_role'.
"""
from __future__ import annotations

import asyncio
import json
import time
import uuid as _uuid

import httpx
import pytest
from sqlalchemy import text


# Reuse helpers từ Plan 02-03 — duplicate inline để Plan 02-04 self-contained.
async def _seed_hub_admin_user(*, email: str, hub_id: str) -> str:
    """W1: app_with_auth TRUNCATE per-test → KHÔNG cần re-truncate."""
    from app.db.session import get_engine
    from tests.integration.conftest import GO_SEED_HASH

    engine = get_engine()
    user_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO users (id, email, password_hash, full_name, role, "
                "phone, department, status, is_active, created_at, updated_at) "
                "VALUES (:id, :email, :hash, :name, 'editor', NULL, NULL, "
                "'active', TRUE, NOW(), NOW())"
            ),
            {"id": user_id, "email": email, "hash": GO_SEED_HASH,
             "name": "Hub Admin Audit Test"},
        )
        await conn.execute(
            text(
                "INSERT INTO user_hubs (user_id, hub_id, role, assigned_at) "
                "VALUES (:uid, :hid, 'hub_admin', NOW())"
            ),
            {"uid": user_id, "hid": hub_id},
        )
    return user_id


@pytest.fixture
async def seed_hubs_dmd_tdt() -> tuple[str, str]:
    """W1: TRUNCATE auto. W2: hubs 10 cột — INSERT 9 cột valid."""
    from app.db.session import get_engine

    engine = get_engine()
    dmd_id = str(_uuid.uuid4())
    tdt_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        for hub_id, slug, name, code in [
            (dmd_id, "dmd", "Đỗ Minh Đường", "dmd"),
            (tdt_id, "tdt", "Thuốc Dân Tộc", "tdt"),
        ]:
            await conn.execute(
                text(
                    "INSERT INTO hubs (id, slug, name, code, subdomain, status, "
                    "is_active, created_at, updated_at) "
                    "VALUES (:id, :slug, :name, :code, :sub, 'active', TRUE, "
                    "NOW(), NOW())"
                ),
                {"id": hub_id, "slug": slug, "name": name,
                 "code": code, "sub": slug},
            )
    return dmd_id, tdt_id


async def _wait_audit_row(
    target_email: str, *, action: str = "user.create", timeout_s: float = 3.0
) -> dict[str, object]:
    """Poll audit_logs cho row có payload->>'email' == target_email.

    audit_flush_loop background task batch flush (KHÔNG sync) — poll-based wait
    tránh flaky sleep (carry forward conftest.py:778-806 pattern).

    Returns:
        dict payload đã parse JSON.

    Raises:
        AssertionError nếu timeout.
    """
    from app.db.session import get_engine

    engine = get_engine()
    deadline = time.monotonic() + timeout_s
    while time.monotonic() < deadline:
        async with engine.begin() as conn:
            result = await conn.execute(
                text(
                    "SELECT payload FROM audit_logs "
                    "WHERE action = :action AND payload->>'email' = :email "
                    "ORDER BY created_at DESC LIMIT 1"
                ),
                {"action": action, "email": target_email},
            )
            row = result.fetchone()
            if row is not None:
                payload = row[0]
                if isinstance(payload, str):
                    payload = json.loads(payload)
                return payload
        await asyncio.sleep(0.05)
    raise AssertionError(
        f"audit_logs row action={action} email={target_email} KHÔNG xuất hiện "
        f"trong {timeout_s}s — audit_flush_loop có thể chưa flush"
    )


async def _login(client: httpx.AsyncClient, email: str, password: str) -> str:
    r = await client.post(
        "/api/auth/login",
        json={"email": email, "password": password},
    )
    assert r.status_code == 200, r.text
    return r.json()["data"]["access_token"]


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_user_create_audit_payload_nested_correctly(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 1 ROADMAP success criteria #4:
    Hub_admin dmd POST /api/users → audit_logs payload nest
    actor_role='hub_admin' + actor_hub_id=<dmd-uuid>.
    """
    dmd_id, _ = seed_hubs_dmd_tdt
    await _seed_hub_admin_user(email="hub.admin.audit@dmd.vn", hub_id=dmd_id)
    token = await _login(auth_client, "hub.admin.audit@dmd.vn", "Admin@123")

    target_email = f"audit.target.dmd.{_uuid.uuid4()}@dmd.vn"
    r = await auth_client.post(
        "/api/users",
        headers={"Authorization": f"Bearer {token}"},
        json={
            "email": target_email,
            "name": "Audit Target DMD",
            "password": "Pass1234",
            "hub_id": dmd_id,
            "role": "viewer",
        },
    )
    assert r.status_code == 201, r.text

    # Poll audit_logs cho row vừa INSERT (audit_flush_loop background batch).
    payload = await _wait_audit_row(target_email=target_email)

    assert payload.get("actor_role") == "hub_admin", (
        f"DEP-05: payload.actor_role phải = 'hub_admin', got {payload.get('actor_role')!r}"
    )
    assert payload.get("actor_hub_id") == dmd_id, (
        f"DEP-05: payload.actor_hub_id phải = '{dmd_id}', "
        f"got {payload.get('actor_hub_id')!r}"
    )
    assert payload.get("email") == target_email, "extra fields phải preserve"
    assert payload.get("role") == "viewer", "extra fields phải preserve"


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_super_admin_user_create_audit_payload_actor_admin_null_hub(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 2 ROADMAP success criteria #4:
    Super admin POST /api/users → audit_logs payload nest
    actor_role='admin' + actor_hub_id IS NULL.
    """
    dmd_id, _ = seed_hubs_dmd_tdt

    target_email = f"audit.target.super.{_uuid.uuid4()}@dmd.vn"
    r = await auth_client.post(
        "/api/users",
        headers={"Authorization": f"Bearer {admin_token}"},
        json={
            "email": target_email,
            "name": "Audit Target Super",
            "password": "Pass1234",
            "hub_id": dmd_id,
            "role": "viewer",
        },
    )
    assert r.status_code == 201, r.text

    payload = await _wait_audit_row(target_email=target_email)

    assert payload.get("actor_role") == "admin", (
        f"DEP-05: super admin payload.actor_role phải = 'admin', "
        f"got {payload.get('actor_role')!r}"
    )
    assert payload.get("actor_hub_id") is None, (
        f"DEP-05: super admin payload.actor_hub_id phải IS NULL, "
        f"got {payload.get('actor_hub_id')!r}"
    )
```

**Lưu ý:**
- Unit test 4 case CHÍNH XÁC theo behavior block — pure Python KHÔNG cần Postgres.
- Integration test 2 scenario verify payload->>'actor_role' + payload->>'actor_hub_id' (JSONB extract qua text() query).
- `_wait_audit_row` poll-based wait pattern carry forward conftest.py:778-806 — `audit_flush_loop` background task batch INSERT 128 entry HOẶC 2s interval → poll tối đa 3s đủ.
- 2 helper `_seed_hub_admin_user` + fixture `seed_hubs_dmd_tdt` duplicate từ Plan 02-03 — Plan 02-05 closeout consolidate (DRY future).
- `admin_token` fixture lấy từ conftest.py existing (verify signature match).
- payload có thể là `dict` (asyncpg auto-parse) HOẶC `str` (SQLAlchemy result) — defensive `if isinstance(payload, str): payload = json.loads(payload)`.
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/unit/test_audit_actor_scope.py -x --tb=short 2>&1 | tail -10 && python -m pytest tests/integration/test_audit_actor_metadata.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/unit/test_audit_actor_scope.py` EXISTS.
    - File chứa 4 test function `def test_` (4 case pure Python).
    - File chứa `from app.services.audit_service import build_audit_payload` (import target SUT).
    - File chứa 4 assert `result["actor_role"] ==` (4 case verify shape).
    - File `Hub_All/api/tests/integration/test_audit_actor_metadata.py` EXISTS.
    - File chứa 2 test function `async def test_` (2 scenario integration).
    - File chứa `@pytest.mark.critical` + `@pytest.mark.integration` + `@pytest.mark.asyncio` ≥ 2 lần.
    - File chứa helper `async def _wait_audit_row(` (poll-based wait pattern Pattern F).
    - File chứa `payload->>'email'` HOẶC `payload.get("actor_role")` (JSONB extract verify).
    - File chứa assert `payload.get("actor_role") == "hub_admin"` ≥ 1 lần (scenario 1).
    - File chứa assert `payload.get("actor_role") == "admin"` ≥ 1 lần (scenario 2).
    - File chứa assert `payload.get("actor_hub_id") is None` ≥ 1 lần (scenario 2 super admin).
    - File chứa assert `payload.get("actor_hub_id") == dmd_id` ≥ 1 lần (scenario 1 hub_admin).
    - File chứa comment hoặc docstring mention `W1` HOẶC `app_with_auth` HOẶC `TRUNCATE` (W1 isolation ack).
    - File chứa comment hoặc docstring mention `W2` HOẶC `10 cột` HOẶC `migration 0001` (W2 schema ack).
    - `cd Hub_All/api && python -m pytest tests/unit/test_audit_actor_scope.py --tb=no -q 2>&1 | grep -E "4 passed"` exit 0 (pure Python — không cần infra).
    - `cd Hub_All/api && python -m pytest tests/integration/test_audit_actor_metadata.py --tb=no -q 2>&1 | grep -E "2 passed"` exit 0 (yêu cầu testcontainers).
    - **W4 coverage measurement step:** `cd Hub_All/api && python -m pytest tests/unit/test_audit_actor_scope.py tests/integration/test_audit_payload_actor.py tests/integration/test_audit_actor_metadata.py --cov=app.services.audit_service --cov=app.services.user_service --cov=app.services.hub_service --cov-fail-under=80 -q` exit 0. (Note: test_audit_payload_actor.py = test_audit_actor_metadata.py alias if different name; adapt path.)
  </acceptance_criteria>
  <done>
    test_audit_actor_scope.py 4 unit test PASS pure Python; test_audit_actor_metadata.py 2 integration test PASS verify payload nest hub_admin + super admin scenario; poll-based wait Pattern F carry forward; W1/W2/W4 acknowledged inline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router caller → service.create/delete (actor params) | Router PHẢI derive đúng actor_role + actor_hub_id từ user.role + req.hub_id (POST) HOẶC target_hub_ids[0] (DELETE B1 iter 1); nếu sai → audit log integrity break (T-02-04-R) |
| Service.enqueue_audit → audit_logs.payload JSONB | payload dict serialize JSON qua audit_service._flush_batch (existing M2 logic) — KHÔNG đổi flush logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Repudiation | Hub_admin operation KHÔNG audit hoặc audit thiếu actor scope | mitigate | DEP-05 — 5 callsite (user_service + hub_service) enqueue_audit với build_audit_payload nest actor_role + actor_hub_id. Forensic query qua payload->>'actor_role' filter scope. |
| T-02-04-02 | Tampering | Router caller pass sai actor_role (vd hub_admin pass 'admin' lụi) | accept | Router code derive `actor_role = "admin" if user.role == "admin" else "hub_admin"` deterministic từ user.role DB (KHÔNG tin JWT payload field role — get_current_user load user từ DB). Code review qua acceptance criteria grep verify pattern. |
| T-02-04-03 | Information Disclosure | audit_logs.payload leak hub_id qua actor_hub_id field | accept | M2 audit_logs.hub_id column ĐÃ tồn tại (migration 0001) cho non-PII metadata; payload->>'actor_hub_id' duplicate cho query convenience. Access audit_logs API qua require_role("admin") guard carry forward Phase 5. |
| T-02-04-04 | DoS | build_audit_payload tăng latency request | accept | Helper pure Python dict construction — O(n) trên extra dict size; n=5-10 keys realistic. Profiler measure < 100µs typical. KHÔNG block enqueue async flow. |
| T-02-04-05 | Tampering | audit_flush_loop batch flush mất entry với actor_hub_id NULL bị treat thành missing | mitigate | _flush_batch (audit_service.py:97-120) dùng `json.dumps(e.payload) if e.payload else None` — None preserved như JSON null trong JSONB column. Postgres `payload->>'actor_hub_id' IS NULL` return TRUE đúng. Test scenario 2 integration verify. |
| T-02-04-06 | Repudiation | Phase n+ thêm audit emit ở api_key_service.py KHÔNG dùng build_audit_payload → actor scope missing | mitigate | B7 iter 1 — acceptance criterion lock invariant `grep -c "enqueue_audit" app/services/api_key_service.py == 0`. Khi nào Phase sau thêm emit → CI/test FAIL → buộc refactor qua build_audit_payload. audit_service.py docstring B7 MANDATORY note nhấn mạnh pattern. |

</threat_model>

<verification>
- audit_service.py + build_audit_payload helper + B7 docstring mandatory note deployed.
- 5 service callsite (user_service.create + delete; hub_service.create + update + update_status) refactor — signature thêm actor_role + actor_hub_id; callsite enqueue_audit dùng build_audit_payload.
- 5 router callsite (POST /api/users + DELETE /api/users + POST /api/hubs + PUT /api/hubs + PATCH /api/hubs/status) pass actor metadata xuống service.
- B5 iter 1 POST + DELETE router patch-style INSERT (KHÔNG full handler rewrite) — tránh xung đột với Plan 02-03 ship lines.
- B5 iter 1 DELETE actor_hub_id derive đúng từ target_hub_ids[0] (single-hub case Plan 02-03 B1 iter 1 guarantee).
- B7 iter 1 future-proof guard: api_key_service.py grep enqueue_audit count == 0 verified ở acceptance criterion.
- 4 unit test PASS pure Python.
- 2 integration test PASS — payload->>'actor_role' + payload->>'actor_hub_id' verify đúng.
- KHÔNG migration schema audit_logs (D-V3.1-Phase2-C LOCKED — payload JSONB existing đủ).
- Backward compat: existing tests dùng service.create signature CŨ sẽ FAIL TypeError (missing required keyword). Plan 02-04 acceptance verify orchestrator escalate nếu tests cũ break.
- W1 isolation auto-handled bởi app_with_auth TRUNCATE per-test (conftest.py:264-284).
- W2 schema reference hubs 10 cột (migration 0001 + 0003).
- W4 coverage measurement step explicit acceptance criterion (cov-fail-under=80).
</verification>

<success_criteria>
1. build_audit_payload helper triển khai đúng D-V3.1-Phase2-C LOCKED + B7 docstring mandatory note.
2. 5 service callsite refactor — actor_role + actor_hub_id nest vào payload.
3. 5 router callsite derive đúng actor metadata pass xuống service (POST + DELETE B5 iter 1 patch-style).
4. 4 unit test PASS — verify helper shape.
5. 2 integration test PASS — verify audit_logs payload nest đúng cho hub_admin + super admin scenario.
6. Forensic query `payload->>'actor_role' = 'hub_admin'` queryable (D-V3.1-Phase2-C deliverable).
7. B7 iter 1 future-proof guard PASS: api_key_service.py enqueue_audit count == 0.
8. W4 coverage ≥ 80% trên audit_service + user_service + hub_service.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-04-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- build_audit_payload helper thêm vào audit_service.py + B7 docstring mandatory pattern note (cho future api_key_service.py refactor invariant)
- 5 service callsite refactor (user_service create + delete; hub_service create + update + update_status) — signature thêm actor_role + actor_hub_id; callsite build_audit_payload thay dict literal
- 5 router callsite (POST/DELETE /api/users + POST/PUT/PATCH /api/hubs) pass actor metadata
- B5 iter 1: POST + DELETE router patch-style INSERT (KHÔNG full rewrite) — tránh xung đột với Plan 02-03 ship lines; DELETE actor_hub_id derive từ target_hub_ids[0] (Plan 02-03 B1 iter 1 single-hub case guarantee)
- B7 iter 1: api_key_service.py grep enqueue_audit count == 0 verified (acceptance criterion lock invariant)
- 4 unit test PASS (pure Python) + 2 integration test PASS (testcontainers)
- D-V3.1-Phase2-C LOCKED — KHÔNG migration schema audit_logs (payload JSONB existing đủ)
- Forensic query `payload->>'actor_role' = 'hub_admin' AND payload->>'actor_hub_id' = '<dmd-uuid>'` queryable
- W1 isolation auto-handled bởi app_with_auth TRUNCATE per-test (conftest.py:264-284)
- W2 schema reference hubs 10 cột (migration 0001:80-106 + 0003:47-74)
- W4 coverage measurement step explicit (--cov-fail-under=80 audit_service + user_service + hub_service)
- Backward compat note: existing tests dùng service.create signature cũ sẽ FAIL — Plan 02-05 closeout cần audit + update remaining test sites nếu phát hiện regression

Commit atomic: `feat(02-04): audit payload actor_role + actor_hub_id nest 5 callsite (DEP-05 + B5/B7 iter 1)` + body tiếng Việt nói tại sao (D-V3.1-Phase2-C LOCKED — nest payload JSONB KHÔNG schema migration; helper build_audit_payload dedup + B7 mandatory pattern docstring; 5 callsite uniform; B5 patch-style POST + DELETE router INSERT thay full rewrite; B7 future-proof guard api_key_service enqueue_audit count == 0; forensic query support hub_admin filter).
</output>
</content>
</invoke>