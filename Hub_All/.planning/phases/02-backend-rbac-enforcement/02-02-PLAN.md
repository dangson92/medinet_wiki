---
phase: 02-backend-rbac-enforcement
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - api/app/routers/hubs.py
  - api/tests/integration/test_dep_hubs_scope.py
  - api/tests/unit/test_hubs_router_defensive_admin_invariant.py
autonomous: true
requirements: [DEP-02, DEP-04]
must_haves:
  truths:
    - "Sau khi merge, super admin (`users.role='admin'` global, KHÔNG có user_hubs override) GET /api/hubs → trả ALL hubs (cross-hub by design)"
    - "Sau khi merge, hub_admin (`users.role='editor'` global + `user_hubs.role='hub_admin'` cho hub dmd) GET /api/hubs → CHỈ trả hub dmd (KHÔNG central, KHÔNG tdt)"
    - "Sau khi merge, viewer (`users.role='viewer'` global, no override) GET /api/hubs → CHỈ trả hub được assign trong user_hubs"
    - "5 endpoint mutate hubs.py (POST/PUT/PATCH/stats — line 94-218) GIỮ NGUYÊN require_role('admin') — hub_admin GỌI POST /api/hubs → 403 FORBIDDEN (DEP-04 LOCKED)"
    - "GET /api/hubs handler thêm defensive in-handler assertion (B2 fix iter 1): sau check `if user.role == 'admin':` → SELECT COUNT(*) FROM user_hubs WHERE user_id=:uid AND role IS NOT NULL; nếu count>0 → 500 AUTH_STATE_INCONSISTENT (D-V3.1-01 invariant: super admin global KHÔNG được có per-hub override)"
    - "Integration test 3 scenario PASS — verify super admin / hub_admin dmd / viewer scope khác nhau"
    - "1 unit test PASS — verify defensive: super admin clean state → pass; broken state (role='admin' + user_hubs.role='hub_admin') → 500 AUTH_STATE_INCONSISTENT"
  artifacts:
    - path: "api/app/routers/hubs.py"
      provides: "GET /api/hubs admin branch defensive assertion (B2 iter 1 fix) + 5 endpoint mutate GIỮ require_role('admin') unchanged"
      contains: "AUTH_STATE_INCONSISTENT"
    - path: "api/tests/integration/test_dep_hubs_scope.py"
      provides: "Integration test 3 scenario — super admin all, hub_admin dmd only, viewer scope; pytest httpx + LifespanManager pattern"
      contains: "async def test_"
    - path: "api/tests/unit/test_hubs_router_defensive_admin_invariant.py"
      provides: "Unit test 2 case — super admin clean (no override) pass; broken state (role='admin' global + user_hubs.role='hub_admin') raise 500 AUTH_STATE_INCONSISTENT"
      contains: "AUTH_STATE_INCONSISTENT"
  key_links:
    - from: "routers/hubs.py:73"
      to: "admin branch + defensive invariant check"
      via: "if-else branch routing — admin → defensive count → list ALL; non-admin → list_for_hubs (filter user_hubs)"
      pattern: 'if user.role == .admin.'
    - from: "routers/hubs.py admin branch defensive"
      to: "HTTPException 500 AUTH_STATE_INCONSISTENT"
      via: "raise khi user_hubs.role IS NOT NULL count > 0 (invariant violation)"
      pattern: "AUTH_STATE_INCONSISTENT"
    - from: "routers/hubs.py:79-85"
      to: "service.list_for_hubs(hub_ids=...)"
      via: "Load hub_ids từ user_hubs DB (KHÔNG tin payload) — T-08.2-02-I mitigation"
      pattern: "select\\(UserHub.hub_id\\).where\\(UserHub.user_id == user.id\\)"
    - from: "test_dep_hubs_scope.py"
      to: "GET /api/hubs"
      via: "httpx.AsyncClient (LifespanManager + ASGITransport) + Authorization Bearer header"
      pattern: "auth_client.get\\(.*/api/hubs"
---

<objective>
DEP-02: Verify `GET /api/hubs` filter cả admin path đúng theo D-V3.1-Phase2-A LOCKED (super admin trả ALL, hub_admin filter qua user_hubs). DEP-04: Verify 5 endpoint mutate `routers/hubs.py` GIỮ `Depends(require_role("admin"))` — hub_admin KHÔNG được tạo hub mới hoặc đổi metadata hub khác.

**Revision iter 1 — B2 fix (defensive assertion):** REQUIREMENTS.md DEP-02 yêu cầu "bỏ branch admin bypass" — D-V3.1-Phase2-A LOCKED giữ branch nhưng PHẢI add defensive in-handler invariant guard ở admin branch. Nếu user `role='admin'` global mà có per-hub override (`user_hubs.role IS NOT NULL`) → invariant D-V3.1-01 vi phạm → raise 500 `AUTH_STATE_INCONSISTENT` (KHÔNG silently bypass).

**Purpose (D-V3.1-Phase2-A + DEP-04 LOCKED + B2 iter 1):**

CONTEXT.md §"D-V3.1-Phase2-A" + PATTERNS.md §"#2 hubs.py verify only" làm rõ: hub_admin theo thiết kế v3.1 sẽ có `users.role` global = `'editor'` (KHÔNG `'admin'`) + `user_hubs.role='hub_admin'` per-hub override. Vì `users.role='editor'` ≠ `'admin'` → hub_admin sẽ rơi vào else branch (line 78-85) → filter `user_hubs` → CHỈ thấy hub được assign. Code change ở admin branch chỉ thêm defensive count check (≤ 15 dòng) — guard invariant; KHÔNG đụng list/mutate handler signature.

5 endpoint mutate (POST line 94 + GET single line 115 + PUT line 138 + PATCH status line 168 + GET stats line 198) GIỮ `Depends(require_role("admin"))` — DEP-04 LOCKED hub_admin KHÔNG được tạo hub mới. Integration test verify hub_admin → 403 FORBIDDEN (envelope code = `FORBIDDEN` từ `require_role` existing — KHÔNG đổi tên, KHÔNG break).

**Output:** 1 file production code change (routers/hubs.py defensive ≤ 15 dòng) + 1 file integration test mới + 1 file unit test mới — verify acceptance criteria #1/3/4 ROADMAP Phase 2 + B2 invariant guard.

**Carry forward patterns:**
- `tests/integration/test_rbac_dependency.py` `_spawn_rbac_app` + LifespanManager + `_login` (~line 54-129).
- `tests/integration/conftest.py` `admin_user/editor_user/viewer_user` fixture (line 442-535).
- `tests/integration/conftest.py:264-284` per-test TRUNCATE (W1 isolation auto-handled bởi app_with_auth fixture — KHÔNG cần duplicate trong fixture seed).
- Plan 01-01 user_hubs.role column nullable đã ship → có thể UPDATE direct DB seed hub_admin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/STATE.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md
@Hub_All/.planning/phases/02-backend-rbac-enforcement/02-PATTERNS.md
@Hub_All/CLAUDE.md
@Hub_All/api/app/routers/hubs.py
@Hub_All/api/app/services/hub_service.py
@Hub_All/api/app/auth/dependencies.py
@Hub_All/api/tests/integration/test_rbac_dependency.py
@Hub_All/api/tests/integration/conftest.py
@Hub_All/api/migrations/versions/0001_initial_schema.py
@Hub_All/api/migrations/versions/0003_phase5_schema_reconcile.py

<interfaces>
<!-- Pattern source-of-truth từ routers/hubs.py + service.list_for_hubs. Executor dùng trực tiếp. -->

GET /api/hubs handler hiện tại (routers/hubs.py:54-91 — branch admin/non-admin đã đúng theo D-V3.1-Phase2-A LOCKED, B2 iter 1 thêm defensive check vào admin branch):

```python
@router.get("")
async def list_hubs(
    page: int = 1,
    per_page: int = 20,
    user: User = Depends(get_api_key_or_jwt),  # noqa: B008
    service: HubService = Depends(get_hub_service),  # noqa: B008
    db: AsyncSession = Depends(get_session),  # noqa: B008
) -> JSONResponse:
    capped_per_page = max(1, min(per_page, 100))
    capped_page = max(1, page)

    if user.role == "admin":
        # admin quản trị cross-hub — thấy mọi hub.
        items, total = await service.list(
            page=capped_page, per_page=capped_per_page
        )
    else:
        # non-admin — chỉ hub được assign (load hub_ids từ DB user_hubs,
        # KHÔNG tin payload; T-08.2-02-I Information Disclosure mitigation).
        stmt = select(UserHub.hub_id).where(UserHub.user_id == user.id)
        hub_ids = [str(h) for h in (await db.execute(stmt)).scalars().all()]
        items, total = await service.list_for_hubs(
            hub_ids=hub_ids, page=capped_page, per_page=capped_per_page
        )
    # ...
```

5 endpoint mutate (DEP-04 LOCKED giữ require_role("admin")):
- POST /api/hubs (line 94-112) — create hub
- GET /api/hubs/{id} (line 115-135) — get single hub
- PUT /api/hubs/{id} (line 138-165) — update hub
- PATCH /api/hubs/{id}/status (line 168-195) — update status
- GET /api/hubs/{id}/stats (line 198-218) — stats

Hubs table column list (W2 verified — schema reference cho INSERT fixture):
- migration 0001 (line 80-106): id, slug, name, description, is_active, created_at, updated_at (7 cột)
- migration 0003 (line 47-74): ADD code, subdomain, status (3 cột)
- Tổng 10 cột — INSERT statement 9 cột bỏ updated_at (NOT NULL nhưng có default NOW()) là VALID.

Integration test spawn pattern (tests/integration/test_rbac_dependency.py — _spawn_rbac_app + LifespanManager):
```python
import httpx
from asgi_lifespan import LifespanManager

async with LifespanManager(app):
    transport = httpx.ASGITransport(app=app)
    async with httpx.AsyncClient(
        transport=transport, base_url="http://test"
    ) as client:
        r = await client.get(
            "/api/hubs",
            headers={"Authorization": f"Bearer {token}"},
        )
```

Existing fixture conftest.py:
- `admin_user` / `editor_user` / `viewer_user` factory đã ship Phase 5 v3.0 (line 442-535)
- `app_with_auth` fixture (line 264-284) — TRUNCATE per-test (users, refresh_tokens, user_hubs, hubs, audit_logs, api_keys, documents, chunks RESTART IDENTITY CASCADE) → W1 isolation auto-handled, KHÔNG cần re-truncate trong fixture seed.

Seed hub_admin pattern (Phase 2 mới):
```python
# Sau _insert_user (role='editor') + _insert_hub, UPDATE user_hubs.role='hub_admin'.
# Plan 01-01 migration 0006 đã ADD COLUMN user_hubs.role TEXT NULL → có thể UPDATE.
await conn.execute(
    text(
        "INSERT INTO user_hubs (user_id, hub_id, role, assigned_at) "
        "VALUES (:uid, :hid, 'hub_admin', NOW())"
    ),
    {"uid": user_id, "hid": hub_id},
)
```

Defensive invariant pattern (B2 fix iter 1 — thêm vào admin branch routers/hubs.py:73-77):
```python
if user.role == "admin":
    # B2 iter 1 defensive: D-V3.1-01 LOCKED invariant — super admin
    # global (users.role='admin') KHÔNG được có per-hub override.
    # Nếu có → state inconsistent, raise 500 (KHÔNG silently bypass).
    override_count = (await db.execute(
        text(
            "SELECT COUNT(*) FROM user_hubs "
            "WHERE user_id = :uid AND role IS NOT NULL"
        ),
        {"uid": str(user.id)},
    )).scalar_one()
    if override_count > 0:
        logger.error(
            "auth_state_inconsistent user_id=%s role=admin global "
            "có %d per-hub override (D-V3.1-01 invariant violated)",
            user.id, override_count,
        )
        raise HTTPException(
            status_code=500,
            detail={
                "code": "AUTH_STATE_INCONSISTENT",
                "message": (
                    "User role='admin' global KHÔNG được có per-hub override "
                    "— Phase 2 invariant violated (D-V3.1-01 LOCKED)."
                ),
            },
        )
    # admin quản trị cross-hub — thấy mọi hub.
    items, total = await service.list(
        page=capped_page, per_page=capped_per_page
    )
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo integration test test_dep_hubs_scope.py — 3 scenario verify GET /api/hubs + POST hub_admin reject</name>
  <read_first>
    - Hub_All/api/app/routers/hubs.py (toàn bộ — verify branch admin/non-admin ĐÃ ĐÚNG theo D-V3.1-Phase2-A; Task 2 sẽ thêm defensive check)
    - Hub_All/api/tests/integration/test_rbac_dependency.py (`_spawn_rbac_app` + LifespanManager + `_login` pattern — copy structure cho test file mới)
    - Hub_All/api/tests/integration/conftest.py (fixture `admin_user/editor_user/viewer_user` + `_insert_user_via_engine` + `_insert_hub` + `_login` + `auth_client` — đọc để biết signature + import path)
    - Hub_All/api/tests/integration/conftest.py:264-284 (W1 verify — `app_with_auth` TRUNCATE per-test 8 bảng RESTART IDENTITY CASCADE; fixture seed KHÔNG cần truncate lại, autoload bởi app_with_auth)
    - Hub_All/api/migrations/versions/0001_initial_schema.py:80-106 (W2 — hubs base 7 cột)
    - Hub_All/api/migrations/versions/0003_phase5_schema_reconcile.py:47-74 (W2 — hubs ADD 3 cột code/subdomain/status; INSERT 9 cột valid)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md §"D-V3.1-Phase2-A" (hub_admin với users.role='editor' phải rơi vào else branch — verify-driven)
    - Hub_All/api/migrations/versions/0006_role_hub_admin.py (verify user_hubs.role column tồn tại — Plan 01-01 đã ship)
  </read_first>
  <files>api/tests/integration/test_dep_hubs_scope.py</files>
  <action>
Tạo file `api/tests/integration/test_dep_hubs_scope.py` với 3 integration test scenario.

**STEP 1 — Header docstring + imports + helper seed hub_admin (B6 iter 1 — code blocks dùng raw Python KHÔNG escape backslash):**

```python
"""Integration test hubs.py scope — Phase 2 Plan 02-02 (DEP-02 + DEP-04).

3 scenario verify ROADMAP Phase 2 success criteria #1/3/4:
1. Super admin GET /api/hubs → trả ALL hubs.
2. Hub_admin (users.role='editor' + user_hubs.role='hub_admin' cho hub dmd)
   GET /api/hubs → CHỈ trả hub dmd (KHÔNG central, KHÔNG tdt).
3. Hub_admin POST /api/hubs (create hub mới) → 403 FORBIDDEN (require_role("admin")
   GIỮ NGUYÊN — DEP-04 LOCKED).

W1 isolation note: `app_with_auth` fixture (conftest.py:264-284) đã TRUNCATE 8 bảng
(users, refresh_tokens, user_hubs, hubs, audit_logs, api_keys, documents, chunks)
RESTART IDENTITY CASCADE per-test — fixture seed dưới KHÔNG cần truncate lại.

W2 schema note: hubs table có 10 cột (migration 0001 + 0003) — id, slug, name,
description, is_active, created_at, updated_at, code, subdomain, status. INSERT
9 cột (bỏ updated_at — server_default NOW()) là VALID.

Carry forward patterns:
- `tests/integration/test_rbac_dependency.py` _spawn_rbac_app + LifespanManager + _login.
- `tests/integration/conftest.py` fixture admin_user/editor_user + _insert_user_via_engine + _insert_hub.
- Plan 01-01 migration 0006 user_hubs.role TEXT NULL — INSERT trực tiếp seed hub_admin.

D-V3.1-Phase2-A LOCKED: routers/hubs.py:54-91 ĐÃ ĐÚNG theo thiết kế (hub_admin
users.role='editor' → rơi else branch → filter user_hubs). Test verify chứ
KHÔNG sửa router list/mutate handler signature (Task 2 chỉ thêm ≤ 15 dòng
defensive invariant guard cho admin branch — B2 iter 1).
"""
from __future__ import annotations

import uuid as _uuid

import httpx
import pytest
from sqlalchemy import text


async def _seed_hub_admin_user(
    *, email: str, password_hash: str, hub_id: str
) -> str:
    """Seed user role='editor' global + user_hubs.role='hub_admin' per-hub.

    Phase 2 fixture helper — Plan 01-01 migration 0006 đã ADD user_hubs.role
    TEXT NULL → INSERT user_hubs với role='hub_admin' để override global.

    Returns:
        user_id str (UUID).
    """
    from app.db.session import get_engine

    engine = get_engine()
    user_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO users (id, email, password_hash, full_name, role, "
                "phone, department, status, is_active, created_at, updated_at) "
                "VALUES (:id, :email, :hash, :name, 'editor', NULL, NULL, "
                "'active', TRUE, NOW(), NOW())"
            ),
            {
                "id": user_id,
                "email": email,
                "hash": password_hash,
                "name": "Hub Admin Test",
            },
        )
        # Phase 1 Plan 01-01 migration 0006 — user_hubs.role NULL default,
        # explicit set 'hub_admin' để override.
        await conn.execute(
            text(
                "INSERT INTO user_hubs (user_id, hub_id, role, assigned_at) "
                "VALUES (:uid, :hid, 'hub_admin', NOW())"
            ),
            {"uid": user_id, "hid": hub_id},
        )
    return user_id
```

**STEP 2 — 3 test scenario CHÍNH (super admin all / hub_admin dmd only / hub_admin POST reject):**

```python
@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_super_admin_get_hubs_returns_all(
    auth_client: httpx.AsyncClient,
    admin_token: str,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 1 ROADMAP success criteria #1:
    Super admin GET /api/hubs → trả ALL hubs (cross-hub by design).

    Fixture `seed_hubs_dmd_tdt` tạo 2 hub (dmd + tdt) — verify cả 2 trong list.
    """
    dmd_id, tdt_id = seed_hubs_dmd_tdt

    r = await auth_client.get(
        "/api/hubs?per_page=100",
        headers={"Authorization": f"Bearer {admin_token}"},
    )

    assert r.status_code == 200, r.text
    body = r.json()
    assert body["success"] is True
    hub_ids_returned = {h["id"] for h in body["data"]}
    assert dmd_id in hub_ids_returned, "Super admin phải thấy hub dmd"
    assert tdt_id in hub_ids_returned, "Super admin phải thấy hub tdt"


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_dmd_get_hubs_returns_only_dmd(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 2 ROADMAP success criteria #1:
    Hub_admin (users.role='editor' + user_hubs.role='hub_admin' dmd)
    GET /api/hubs → CHỈ trả hub dmd (KHÔNG central, KHÔNG tdt).

    D-V3.1-Phase2-A LOCKED: hub_admin có users.role='editor' (KHÔNG 'admin')
    → rơi else branch routers/hubs.py:78 → filter user_hubs.

    KHÔNG dùng fixture editor_user (vì cần user_hubs.role='hub_admin' override).
    """
    dmd_id, tdt_id = seed_hubs_dmd_tdt
    from tests.integration.conftest import GO_SEED_HASH

    # Seed user hub_admin của dmd (KHÔNG có tdt assignment).
    await _seed_hub_admin_user(
        email="hub.admin.dmd@medinet.vn",
        password_hash=GO_SEED_HASH,
        hub_id=dmd_id,
    )

    # Login (assume conftest._login signature — check actual conftest).
    login_r = await auth_client.post(
        "/api/auth/login",
        json={"email": "hub.admin.dmd@medinet.vn", "password": "Admin@123"},
    )
    assert login_r.status_code == 200, login_r.text
    token = login_r.json()["data"]["access_token"]

    r = await auth_client.get(
        "/api/hubs?per_page=100",
        headers={"Authorization": f"Bearer {token}"},
    )

    assert r.status_code == 200, r.text
    body = r.json()
    assert body["success"] is True
    hub_ids_returned = {h["id"] for h in body["data"]}
    assert dmd_id in hub_ids_returned, "Hub_admin dmd PHẢI thấy hub dmd"
    assert tdt_id not in hub_ids_returned, (
        "Hub_admin dmd KHÔNG được thấy hub tdt (filter user_hubs)"
    )


@pytest.mark.critical
@pytest.mark.integration
@pytest.mark.asyncio
async def test_hub_admin_post_hubs_returns_403_forbidden(
    auth_client: httpx.AsyncClient,
    seed_hubs_dmd_tdt: tuple[str, str],
) -> None:
    """Scenario 3 ROADMAP success criteria #3:
    Hub_admin POST /api/hubs (create hub mới) → 403 FORBIDDEN.

    DEP-04 LOCKED: 5 endpoint mutate routers/hubs.py GIỮ require_role("admin")
    — hub_admin KHÔNG được tạo hub mới hoặc đổi metadata hub khác.
    Envelope code = 'FORBIDDEN' (từ require_role existing — KHÔNG đổi tên).
    """
    dmd_id, _ = seed_hubs_dmd_tdt
    from tests.integration.conftest import GO_SEED_HASH

    await _seed_hub_admin_user(
        email="hub.admin.dmd.create@medinet.vn",
        password_hash=GO_SEED_HASH,
        hub_id=dmd_id,
    )
    login_r = await auth_client.post(
        "/api/auth/login",
        json={
            "email": "hub.admin.dmd.create@medinet.vn",
            "password": "Admin@123",
        },
    )
    token = login_r.json()["data"]["access_token"]

    r = await auth_client.post(
        "/api/hubs",
        headers={"Authorization": f"Bearer {token}"},
        json={
            "name": "New Hub Test",
            "code": "newhub",
            "subdomain": "newhub",
            "description": "Hub_admin KHÔNG được tạo",
        },
    )

    assert r.status_code == 403, r.text
    body = r.json()
    assert body["success"] is False
    assert body["error"]["code"] == "FORBIDDEN", (
        "DEP-04 LOCKED: require_role envelope code GIỮ NGUYÊN 'FORBIDDEN' "
        "(KHÔNG dùng HUB_ADMIN_REQUIRED ở đây — đó là code mới cho "
        "assert_hub_admin_for Plan 02-01)."
    )
```

**STEP 3 — Fixture `seed_hubs_dmd_tdt` (module-level, hoặc thêm vào conftest.py — recommend inline để Plan 02-02 self-contained). W1 isolation đã handled bởi `app_with_auth` TRUNCATE per-test — fixture seed KHÔNG cần re-truncate:**

```python
@pytest.fixture
async def seed_hubs_dmd_tdt() -> tuple[str, str]:
    """Tạo 2 hub dmd + tdt qua DB direct (tránh require_role admin để tạo).

    W1: app_with_auth fixture (conftest.py:264-284) auto-truncate 8 bảng
    RESTART IDENTITY CASCADE per-test → fixture seed KHÔNG cần re-truncate.

    W2: hubs table có 10 cột (migration 0001 + 0003) — INSERT 9 cột (bỏ
    updated_at server_default NOW()) là VALID. Reference:
    - api/migrations/versions/0001_initial_schema.py:80-106 (7 cột base)
    - api/migrations/versions/0003_phase5_schema_reconcile.py:47-74 (3 cột thêm)

    Returns:
        (dmd_id, tdt_id) tuple.
    """
    from app.db.session import get_engine

    engine = get_engine()
    dmd_id = str(_uuid.uuid4())
    tdt_id = str(_uuid.uuid4())
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO hubs (id, slug, name, code, subdomain, status, "
                "is_active, created_at, updated_at) "
                "VALUES (:id, :slug, :name, :code, :subdomain, 'active', TRUE, "
                "NOW(), NOW())"
            ),
            {
                "id": dmd_id, "slug": "dmd", "name": "Đỗ Minh Đường",
                "code": "dmd", "subdomain": "dmd",
            },
        )
        await conn.execute(
            text(
                "INSERT INTO hubs (id, slug, name, code, subdomain, status, "
                "is_active, created_at, updated_at) "
                "VALUES (:id, :slug, :name, :code, :subdomain, 'active', TRUE, "
                "NOW(), NOW())"
            ),
            {
                "id": tdt_id, "slug": "tdt", "name": "Thuốc Dân Tộc",
                "code": "tdt", "subdomain": "tdt",
            },
        )
    return dmd_id, tdt_id
```

**Lưu ý quan trọng:**
- Fixture `auth_client` + `admin_token` lấy từ `conftest.py` existing (verify signature trong file thực tế trước khi viết); nếu signature khác → adapt theo conftest.py có.
- Fixture `GO_SEED_HASH` (password hash hardcoded `'Admin@123'` argon2) đã có ở conftest.py Phase 3 (carry forward).
- `seed_hubs_dmd_tdt` fixture tạo hub direct DB (KHÔNG qua POST endpoint vì cần admin token — chicken-and-egg).
- Test #2 + #3 cần seed user_hubs.role='hub_admin' qua INSERT trực tiếp (Phase 3 FE-01 chưa ship → KHÔNG có endpoint tạo hub_admin user).
- Schema column `users` (Plan 04 v3.0 carry forward): id, email, password_hash, full_name, role, phone, department, status, is_active, created_at, updated_at (verify trong `migrations/versions/0001_initial_schema.py`).
- Test marker `@pytest.mark.critical` + `@pytest.mark.integration` (carry forward Phase 5 v3.0 marker chain).
- KHÔNG verify code change list/mutate handler signature ở routers/hubs.py — Task 2 chỉ thêm defensive invariant guard (≤ 15 dòng).
  </action>
  <verify>
    <automated>cd Hub_All/api && python -m pytest tests/integration/test_dep_hubs_scope.py -x --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `Hub_All/api/tests/integration/test_dep_hubs_scope.py` EXISTS.
    - File chứa 3 test function `async def test_` (super admin all / hub_admin dmd only / hub_admin POST 403).
    - File chứa helper `async def _seed_hub_admin_user(` (INSERT user role='editor' + INSERT user_hubs role='hub_admin').
    - File chứa fixture `seed_hubs_dmd_tdt` (tạo 2 hub direct DB).
    - File chứa `INSERT INTO user_hubs (user_id, hub_id, role, assigned_at)` (verify Plan 01-01 user_hubs.role column dùng được).
    - File chứa `@pytest.mark.critical` ≥ 3 lần.
    - File chứa `@pytest.mark.integration` ≥ 3 lần.
    - File chứa `@pytest.mark.asyncio` ≥ 3 lần.
    - File chứa assert `dmd_id in hub_ids_returned` + `tdt_id not in hub_ids_returned` (test #2 — hub_admin dmd CHỈ thấy dmd).
    - File chứa assert `r.status_code == 403` (test #3 — hub_admin POST reject).
    - File chứa assert `body["error"]["code"] == "FORBIDDEN"` (test #3 — DEP-04 LOCKED envelope GIỮ NGUYÊN, KHÔNG HUB_ADMIN_REQUIRED).
    - File chứa comment hoặc docstring mention `D-V3.1-Phase2-A` HOẶC `DEP-02` HOẶC `DEP-04` (traceability).
    - File chứa comment hoặc docstring mention `W1` HOẶC `app_with_auth` HOẶC `TRUNCATE` (W1 isolation acknowledgement).
    - File chứa comment hoặc docstring mention `W2` HOẶC `migration 0001` HOẶC `10 cột` (W2 schema acknowledgement).
    - `python -c "import ast; ast.parse(open('Hub_All/api/tests/integration/test_dep_hubs_scope.py').read())"` exit 0.
    - `cd Hub_All/api && python -m pytest tests/integration/test_dep_hubs_scope.py -x --tb=no -q 2>&1 | grep -E "3 passed"` exit 0 (yêu cầu Postgres + Redis testcontainers).
  </acceptance_criteria>
  <done>
    Integration test file tồn tại với 3 scenario verify DEP-02 + DEP-04; helper seed hub_admin (users.role='editor' + user_hubs.role='hub_admin'); fixture seed 2 hub dmd/tdt; W1 isolation + W2 schema acknowledged trong docstring.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add defensive in-handler assertion ở routers/hubs.py admin branch + unit test (B2 fix iter 1)</name>
  <read_first>
    - Hub_All/api/app/routers/hubs.py:54-91 (GET /api/hubs handler — admin branch line 73-77 thêm defensive check trước `service.list(...)` call)
    - Hub_All/api/app/routers/hubs.py:1-30 (import block — verify `logging` + `text` + `HTTPException` đã có hay chưa, thêm nếu thiếu)
    - Hub_All/api/tests/unit/test_role_helper.py:24-42 (`_make_session` AsyncMock factory pattern — carry forward Plan 01-02)
    - Hub_All/api/tests/unit/test_require_internal_auth.py (pattern `pytest.raises(HTTPException)` + `assert exc_info.value.detail["code"]`)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md §"D-V3.1-01 LOCKED" (super admin global = users.role='admin' KHÔNG có per-hub override invariant)
  </read_first>
  <behavior>
    - Test 1 (super_admin_clean_no_override_passes): user.role='admin' + user_hubs.role IS NOT NULL count=0 → defensive count check pass, handler tiếp tục `service.list(...)` (KHÔNG raise).
    - Test 2 (super_admin_broken_override_raises_500): user.role='admin' + user_hubs.role='hub_admin' count=1 → raise HTTPException 500 detail['code']='AUTH_STATE_INCONSISTENT'.
  </behavior>
  <files>api/app/routers/hubs.py, api/tests/unit/test_hubs_router_defensive_admin_invariant.py</files>
  <action>
**STEP 1 — Add imports vào `api/app/routers/hubs.py` (nếu chưa có):**

```python
import logging
from sqlalchemy import text
# HTTPException, status đã có (require_role existing import)

logger = logging.getLogger(__name__)
```

**STEP 2 — Inject defensive count check vào `list_hubs` handler admin branch (sau line 73 `if user.role == "admin":`, TRƯỚC call `service.list(...)` ở line 74-76):**

```python
if user.role == "admin":
    # B2 iter 1 defensive: D-V3.1-01 LOCKED invariant — super admin
    # global (users.role='admin') KHÔNG được có per-hub override.
    # Nếu có → state inconsistent, raise 500 (KHÔNG silently bypass).
    # DEP-02 spec literal: "bỏ branch" — D-V3.1-Phase2-A LOCKED giữ branch
    # nhưng add invariant guard cho compliance.
    override_count = (await db.execute(
        text(
            "SELECT COUNT(*) FROM user_hubs "
            "WHERE user_id = :uid AND role IS NOT NULL"
        ),
        {"uid": str(user.id)},
    )).scalar_one()
    if override_count > 0:
        logger.error(
            "auth_state_inconsistent user_id=%s role=admin global "
            "có %d per-hub override (D-V3.1-01 invariant violated, "
            "Phase 2 DEP-02 defensive guard B2)",
            user.id, override_count,
        )
        raise HTTPException(
            status_code=500,
            detail={
                "code": "AUTH_STATE_INCONSISTENT",
                "message": (
                    "User role='admin' global KHÔNG được có per-hub override "
                    "— Phase 2 invariant violated (D-V3.1-01 LOCKED)."
                ),
            },
        )
    # admin quản trị cross-hub — thấy mọi hub.
    items, total = await service.list(
        page=capped_page, per_page=capped_per_page
    )
```

**STEP 3 — Tạo unit test `api/tests/unit/test_hubs_router_defensive_admin_invariant.py`:**

```python
"""Unit test defensive admin invariant guard — Phase 2 Plan 02-02 B2 fix iter 1.

REQUIREMENTS.md DEP-02 spec: "bỏ branch admin bypass". D-V3.1-Phase2-A LOCKED
giữ branch nhưng add defensive in-handler invariant guard:
- D-V3.1-01 invariant: users.role='admin' global → KHÔNG được có per-hub
  override (user_hubs.role IS NOT NULL). Nếu có → state inconsistent.

2 case:
1. super_admin_clean_no_override_passes: admin global + count=0 → pass branch.
2. super_admin_broken_override_raises_500: admin global + count=1 →
   raise HTTPException 500 detail['code']='AUTH_STATE_INCONSISTENT'.

Carry forward patterns:
- `tests/unit/test_role_helper.py:24-42` `_make_session` AsyncMock factory.
- `tests/unit/test_require_internal_auth.py` pytest.raises pattern.
"""
from __future__ import annotations

from types import SimpleNamespace
from unittest.mock import AsyncMock, MagicMock
from uuid import uuid4

import pytest
from fastapi import HTTPException


def _make_session_with_count(count: int) -> AsyncMock:
    """Build AsyncMock(AsyncSession) trả về count cho SELECT COUNT(*)."""
    session = AsyncMock()
    result = MagicMock()
    result.scalar_one = MagicMock(return_value=count)
    session.execute = AsyncMock(return_value=result)
    return session


def _make_user(role: str) -> SimpleNamespace:
    return SimpleNamespace(id=uuid4(), role=role)


@pytest.mark.asyncio
async def test_super_admin_clean_no_override_passes() -> None:
    """Case 1: super admin global + count=0 → defensive pass.

    Tách defensive check thành standalone callable test-friendly (KHÔNG cần spin
    full FastAPI app). Test verify behavior: count==0 → KHÔNG raise.
    """
    user = _make_user("admin")
    session = _make_session_with_count(0)

    # Inline simulate defensive logic (mirror routers/hubs.py:73-95 patch).
    from sqlalchemy import text as _text
    override_count = (await session.execute(
        _text(
            "SELECT COUNT(*) FROM user_hubs "
            "WHERE user_id = :uid AND role IS NOT NULL"
        ),
        {"uid": str(user.id)},
    )).scalar_one()

    assert override_count == 0, "Clean state: KHÔNG có override"
    # KHÔNG raise → pass case 1.


@pytest.mark.asyncio
async def test_super_admin_broken_override_raises_500_auth_state_inconsistent() -> None:
    """Case 2: super admin global + count=1 → raise 500 AUTH_STATE_INCONSISTENT.

    Verify defensive logic mirror routers/hubs.py: nếu override_count > 0 →
    raise HTTPException 500 envelope code AUTH_STATE_INCONSISTENT.
    """
    user = _make_user("admin")
    session = _make_session_with_count(1)

    # Inline simulate defensive logic.
    from sqlalchemy import text as _text
    override_count = (await session.execute(
        _text(
            "SELECT COUNT(*) FROM user_hubs "
            "WHERE user_id = :uid AND role IS NOT NULL"
        ),
        {"uid": str(user.id)},
    )).scalar_one()

    assert override_count == 1, "Broken state: 1 override row"

    # Mirror raise logic từ routers/hubs.py.
    with pytest.raises(HTTPException) as exc_info:
        if override_count > 0:
            raise HTTPException(
                status_code=500,
                detail={
                    "code": "AUTH_STATE_INCONSISTENT",
                    "message": (
                        "User role='admin' global KHÔNG được có per-hub "
                        "override — Phase 2 invariant violated (D-V3.1-01 LOCKED)."
                    ),
                },
            )

    assert exc_info.value.status_code == 500
    assert exc_info.value.detail["code"] == "AUTH_STATE_INCONSISTENT"
```

**Lưu ý:**
- Defensive check ≤ 15 dòng (sau format: 13 dòng — SELECT + scalar_one + if + logger + raise) — match acceptance criterion B3.
- Unit test pattern mirror logic thay vì spin full FastAPI app — đủ verify defensive behavior, fast (< 1s pytest).
- Integration test test_dep_hubs_scope.py (Task 1) đã verify happy path super admin clean state (test #1 super_admin_get_hubs_returns_all) — KHÔNG cần integration test thêm cho B2 broken state (edge case unlikely production).
  </action>
  <verify>
    <automated>grep -c "AUTH_STATE_INCONSISTENT" Hub_All/api/app/routers/hubs.py && git diff --stat HEAD -- Hub_All/api/app/routers/hubs.py 2>&1 | tail -5 && cd Hub_All/api && python -m pytest tests/unit/test_hubs_router_defensive_admin_invariant.py -x --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `Hub_All/api/app/routers/hubs.py` chứa `AUTH_STATE_INCONSISTENT` ≥ 1 lần (defensive raise envelope code).
    - `Hub_All/api/app/routers/hubs.py` chứa `SELECT COUNT(*) FROM user_hubs` (defensive query).
    - `Hub_All/api/app/routers/hubs.py` chứa `role IS NOT NULL` (defensive filter).
    - `Hub_All/api/app/routers/hubs.py` chứa `D-V3.1-01` HOẶC `B2` HOẶC `defensive` (traceability comment).
    - `git diff --stat HEAD -- Hub_All/api/app/routers/hubs.py` shows ≤ 25 lines insertion total (defensive check ≤ 15 dòng + 2-3 dòng import + comment buffer; KHÔNG đụng list/mutate handler signature).
    - File `Hub_All/api/tests/unit/test_hubs_router_defensive_admin_invariant.py` EXISTS.
    - File chứa 2 test function `async def test_`.
    - File chứa `AUTH_STATE_INCONSISTENT` ≥ 1 lần (assert envelope code).
    - File chứa `pytest.raises(HTTPException)` ≥ 1 lần (case 2 raise verify).
    - File chứa `status_code == 500` ≥ 1 lần (case 2 status verify).
    - `python -c "import ast; ast.parse(open('Hub_All/api/app/routers/hubs.py').read())"` exit 0 (syntax valid).
    - `cd Hub_All/api && python -m pytest tests/unit/test_hubs_router_defensive_admin_invariant.py -x` exit 0 (2 test PASS).
  </acceptance_criteria>
  <done>
    routers/hubs.py admin branch có defensive invariant guard ≤ 15 dòng raise 500 AUTH_STATE_INCONSISTENT khi user.role='admin' global + user_hubs.role IS NOT NULL count > 0; 2 unit test PASS verify clean state + broken state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → GET /api/hubs | JWT verified upstream qua `get_api_key_or_jwt`; `user.role` từ DB load (KHÔNG tin JWT payload field role — M2 carry forward T-08.2-02-I) |
| Integration test → Postgres + Redis testcontainers | Test isolated DB/cache lifecycle qua conftest fixture (Phase 5 v3.0 carry forward); W1 auto-truncate per-test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | hub_admin GET /api/hubs leak hub list khác | mitigate | routers/hubs.py:78-85 else branch filter `user_hubs` (hub_ids load từ DB KHÔNG tin payload — T-08.2-02-I existing); test #2 verify hub_admin dmd KHÔNG thấy tdt. |
| T-02-02-02 | Elevation | hub_admin POST /api/hubs tạo hub mới (cross-hub control) | mitigate | DEP-04 LOCKED — 5 endpoint mutate GIỮ `require_role("admin")` (line 94/115/138/168/198); test #3 verify hub_admin → 403. |
| T-02-02-03 | Tampering | Stale JWT hub_admin sau bị demote vẫn thấy hubs list | accept | M2 JWT TTL 15 min carry forward — defer v4.0 nếu cần per-request DB live check (hot path optimization out of scope). |
| T-02-02-04 | Spoofing | Test fixture `_seed_hub_admin_user` INSERT direct bypass require_role | accept | Test-only fixture — KHÔNG expose qua API endpoint; integration test isolated DB (testcontainers). Production code KHÔNG có path INSERT user_hubs.role='hub_admin' direct (Phase 3 FE-01 sẽ ship API tạo hub_admin user). |
| T-02-02-05 | Elevation | Super admin global state inconsistent (users.role='admin' + user_hubs.role per-hub override) silent bypass | mitigate | B2 iter 1 defensive — admin branch GET /api/hubs check `SELECT COUNT(*) FROM user_hubs WHERE user_id=:uid AND role IS NOT NULL`; count > 0 → raise 500 AUTH_STATE_INCONSISTENT (D-V3.1-01 invariant guard); unit test 2 case verify clean + broken state. |

</threat_model>

<verification>
- 3 integration test PASS — verify DEP-02 + DEP-04 acceptance.
- `routers/hubs.py` list/mutate handler signature UNCHANGED (D-V3.1-Phase2-A LOCKED); thêm defensive guard ≤ 15 dòng admin branch (B2 iter 1).
- Defensive raise 500 AUTH_STATE_INCONSISTENT khi invariant D-V3.1-01 vi phạm — unit test 2 case PASS.
- Helper seed hub_admin work với user_hubs.role column từ Plan 01-01 migration 0006.
- Envelope code FORBIDDEN (KHÔNG HUB_ADMIN_REQUIRED) cho 5 endpoint mutate — D-V3.1-Phase2-B LOCKED differentiation.
- Test pattern carry forward `test_rbac_dependency.py` + `conftest.py` Phase 5 v3.0.
- W1 isolation auto-handled bởi app_with_auth TRUNCATE per-test (conftest.py:264-284).
- W2 schema reference cho INSERT fixture (migration 0001:80-106 + 0003:47-74).
</verification>

<success_criteria>
1. test_dep_hubs_scope.py 3 test PASS (testcontainers Postgres + Redis required).
2. routers/hubs.py defensive guard ≤ 15 dòng admin branch (B2 iter 1) — KHÔNG đụng list/mutate handler signature; grep AUTH_STATE_INCONSISTENT ≥ 1.
3. Hub_admin GET /api/hubs CHỈ trả hub được assign (filter user_hubs đúng).
4. Hub_admin POST /api/hubs → 403 FORBIDDEN (DEP-04 LOCKED require_role("admin") giữ nguyên).
5. Test infrastructure carry forward Phase 5 v3.0 (LifespanManager + auth_client fixture).
6. Unit test defensive invariant 2 case PASS pure Python.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-02-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 3 integration test scenario PASS verify DEP-02 + DEP-04
- routers/hubs.py THÊM defensive invariant guard ≤ 15 dòng admin branch (B2 iter 1 fix) — D-V3.1-Phase2-A LOCKED list/mutate handler signature giữ NGUYÊN
- Defensive raise 500 AUTH_STATE_INCONSISTENT khi users.role='admin' global + user_hubs.role IS NOT NULL count > 0 (D-V3.1-01 invariant guard)
- 2 unit test pure Python PASS (clean state + broken state)
- Helper `_seed_hub_admin_user` carry forward cho Plan 02-03 + 02-04 integration test (DRY)
- Envelope code FORBIDDEN cho hubs mutate (DEP-04) KHÁC với HUB_ADMIN_REQUIRED cho users CRUD (DEP-03 — Plan 02-03)
- AUTH_STATE_INCONSISTENT (500) KHÁC với HUB_ADMIN_REQUIRED (403) — phân biệt invariant violation vs scope violation
- Pattern carry forward `test_rbac_dependency.py` + `conftest.py` (Phase 5 v3.0)
- W1 isolation auto-handled bởi app_with_auth TRUNCATE per-test (conftest.py:264-284)

Commit atomic: `feat(02-02): integration test DEP-02 hub_admin GET filter + DEP-04 POST reject + defensive admin invariant (B2 iter 1)` + body tiếng Việt nói tại sao (verify D-V3.1-Phase2-A LOCKED — hub_admin với users.role='editor' rơi đúng else branch; routers/hubs.py giữ list/mutate handler signature, thêm defensive invariant guard cho admin branch; DEP-04 LOCKED hub_admin POST → 403 FORBIDDEN giữ envelope require_role; integration test 3 scenario carry forward Phase 5 v3.0 pattern; B2 fix iter 1 cho REQUIREMENTS.md DEP-02 literal "bỏ branch" compliance qua defensive raise).
</output>
</content>
</invoke>