---
phase: 07
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - Hub_All/scripts/migrate/02-restore-hub.sh
  - Hub_All/scripts/migrate/03-switch-caddy.sh
autonomous: true
requirements: [MIGRATE-02]
tags: [migration, restore, blue-green, caddy-verify, bash-script]

must_haves:
  truths:
    - "scripts/migrate/02-restore-hub.sh accept hub arg required (KHÔNG default loop)"
    - "02-restore-hub.sh 4-step pipeline: validate + verify DB exist + psql -f restore + row count sanity"
    - "psql -v ON_ERROR_STOP=1 -d medinet_hub_<HUB> -f migrate-snapshots/migrate-<HUB>-<date>.sql (auto-detect latest snapshot)"
    - "Row count sanity check post-restore so với snapshot expected count (grep INSERT vs SELECT COUNT) tolerance ±5%"
    - "Per-hub Alembic head SHA verify (carry forward Phase 1 alembic-head-check.sh pattern)"
    - "scripts/migrate/03-switch-caddy.sh VERIFY-ONLY mode (per CONTEXT.md correction — Caddyfile dynamic regex KHÔNG cần sed edit)"
    - "03-switch-caddy.sh kiểm tra docker compose ps python-api-<HUB> running + caddy validate + curl smoke /<HUB>/api/health"
    - "03-switch-caddy.sh hỗ trợ flag --rollback → docker compose stop python-api-<HUB> (Caddy regex fall-through file_server 404)"
  artifacts:
    - path: "Hub_All/scripts/migrate/02-restore-hub.sh"
      provides: "psql restore snapshot vào medinet_hub_<HUB> blue/green"
      min_lines: 130
      contains: "psql -v ON_ERROR_STOP=1"
    - path: "Hub_All/scripts/migrate/03-switch-caddy.sh"
      provides: "Verify Caddy auto-route hub container post-restore (KHÔNG sed edit)"
      min_lines: 80
      contains: "caddy validate"
  key_links:
    - from: "scripts/migrate/02-restore-hub.sh"
      to: "migrate-snapshots/migrate-<hub>-<date>.sql (Plan 07-01 output)"
      via: "auto-detect latest date snapshot"
      pattern: "migrate-snapshots/migrate-"
    - from: "scripts/migrate/02-restore-hub.sh"
      to: "medinet_hub_<HUB> DB (Phase 1 hub-init schema + Phase 4 Alembic 0005)"
      via: "psql -d medinet_hub_<HUB> -f snapshot.sql"
      pattern: "medinet_hub_"
    - from: "scripts/migrate/03-switch-caddy.sh"
      to: "Caddyfile {re.hub_api.1} dynamic regex (Phase 5 PROXY-01)"
      via: "verify-only — KHÔNG sed edit"
      pattern: "caddy validate"

threat_model:
  trust_boundaries:
    - boundary: "operator shell → psql medinet_hub_<HUB> data restore"
      description: "Restore script chạy local data load; nếu snapshot file tampered → data hub bị inject"
    - boundary: "operator shell → docker compose ps + caddy reload verify"
      description: "Switch script verify container state; rollback flag stop container ảnh hưởng routing"
  threats:
    - id: T-07-02-01
      category: Tampering
      component: "02-restore-hub.sh hub_name arg"
      disposition: mitigate
      mitigation: "Regex ^[a-z][a-z0-9_]{0,15}$ + RESERVED blacklist trước khi DSN compose (T-5-05 carry forward)"
    - id: T-07-02-02
      category: Tampering
      component: "snapshot .sql file path traversal"
      disposition: mitigate
      mitigation: "Path constrain ${REPO_ROOT}/migrate-snapshots/migrate-<hub>-<date>.sql validate exist + readable trước psql -f"
    - id: T-07-02-03
      category: Information Disclosure
      component: "psql -f restore vào DB sai (vd DSN trỏ medinet_central thay vì medinet_hub_yte)"
      disposition: mitigate
      mitigation: "Verify DB name SELECT current_database() == 'medinet_hub_<HUB>' trước restore; fail-fast nếu mismatch"
    - id: T-07-02-04
      category: Denial of Service
      component: "03-switch-caddy.sh --rollback stop container"
      disposition: accept
      mitigation: "Rollback intentional behavior; operator explicit flag; user-visible 404 là signal cho switch revert (acceptable)"
    - id: T-07-02-05
      category: Repudiation
      component: "Restore không có audit trail"
      disposition: accept
      mitigation: "Phase 7 Plan 07-03 sẽ INSERT audit_logs cho truncate; restore là forward-only load — acceptable risk first migration"
---

<objective>
Wave 2 BLOCKING của Phase 7 — Tạo 2 script:
1. `02-restore-hub.sh <HUB>` — restore snapshot từ Plan 07-01 vào `medinet_hub_<HUB>` blue/green per-hub (D-V3-Phase7-B LOCKED zero-downtime).
2. `03-switch-caddy.sh <HUB>` — VERIFY-ONLY post-restore (per CONTEXT.md `<specifics>` correction: Caddyfile `{re.hub_api.1}` đã dynamic capture hub_name từ URL prefix; Phase 7 KHÔNG cần sed edit Caddyfile, chỉ verify container `python-api-<HUB>` running + Caddy auto-route).

**Purpose:** Implement blue/green per-hub procedure step 3-5 (D-V3-Phase7-B). Mỗi hub: snapshot → restore → verify Caddy auto-route → smoke (Plan 07-05) → switch traffic. Phase 7 KHÔNG modify Caddyfile vì Phase 5 đã ship dynamic regex `path_regexp hub_api ^/({$HUBS_ALLOWLIST_REGEX:yte|duoc|hcns})/api/(.*)$` + `reverse_proxy python-api-{re.hub_api.1}:8080` — Caddy tự route đúng container.

**Output:** 2 bash script (~130 + 80 LOC) + verify-only design giảm complexity 50%+ so với sed edit Caddyfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-01-PLAN.md
@Hub_All/api/scripts/hub-init.sh
@Hub_All/api/scripts/hub-add.sh
@Hub_All/Caddyfile
@Hub_All/docker-compose.yml

<interfaces>
<!-- Key contracts from Phase 5 Caddyfile + Phase 1 hub-init.sh -->

**Caddyfile dynamic regex (Phase 5 lines 32-49 — DO NOT MODIFY trong Phase 7):**
```caddyfile
@hub_api path_regexp hub_api ^/({$HUBS_ALLOWLIST_REGEX:yte|duoc|hcns})/api/(.*)$
handle @hub_api {
    route {
        uri strip_prefix /{re.hub_api.1}
        reverse_proxy python-api-{re.hub_api.1}:8080 { ... }
    }
}
```

Implication: Caddy auto-route `python-api-yte:8080` khi prefix `/yte/api/*`. Phase 7 chỉ cần verify container `python-api-<HUB>` running → Caddy regex tự match.

**psql restore prerequisite (Phase 1 hub-init.sh đã CREATE schema):**
- `medinet_hub_<HUB>` DB exist với extension vector
- Alembic 0001..0005 đã upgrade (Phase 1 + Phase 4 Plan 04-01 sync_outbox)
- Schema match snapshot (chunks + documents + users + audit_logs + usage_events + sync_outbox)

**psql restore command:**
```bash
psql -v ON_ERROR_STOP=1 \
    -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
    -d "medinet_hub_$HUB" \
    -f "$SNAPSHOT_FILE"
```

**Verify current_database() match (T-07-02-03 mitigation):**
```sql
SELECT current_database() = 'medinet_hub_<HUB>'::name;
-- returns 't' (true) hoặc 'f' (fail-fast)
```

**docker compose ps JSON parse (Phase 5 hub-add.sh line 301):**
```bash
docker compose ps caddy --format json | grep -q '"State":"running"'
docker compose ps python-api-<HUB> --format json | grep -q '"State":"running"'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo scripts/migrate/02-restore-hub.sh psql restore snapshot blue/green</name>
  <files>Hub_All/scripts/migrate/02-restore-hub.sh</files>
  <read_first>
    - Hub_All/api/scripts/hub-init.sh (toàn bộ — psql + heredoc + ON_ERROR_STOP pattern)
    - Hub_All/api/scripts/hub-add.sh (lines 43-91 — validation chain T-5-05)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md (§"scripts/migrate/02-restore-hub.sh" pattern assignment)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-01-PLAN.md (Plan 07-01 snapshot output naming)
  </read_first>
  <behavior>
    - Accept arg required: `$1` = hub name (KHÔNG default loop — restore là per-hub operation)
    - Validate hub_name regex + RESERVED + reject central
    - Auto-detect latest snapshot file: glob `migrate-snapshots/migrate-<HUB>-*.sql` lấy newest (sort -r | head -1)
    - Verify snapshot file readable + exist
    - Verify DB `medinet_hub_<HUB>` exist (psql -tA SELECT 1 FROM pg_database)
    - Verify Alembic head SHA match expected (qua `alembic current` parse) — warn nếu mismatch
    - Count INSERT trong snapshot file (expected row count)
    - psql -f restore với ON_ERROR_STOP=1
    - SELECT current_database() verify match `medinet_hub_<HUB>` (T-07-02-03)
    - Post-restore row count verify: `SELECT COUNT(*) FROM chunks WHERE hub_id = '<uuid>'` + tolerance ±5% so với expected
    - Exit codes: 0/2/3 (snapshot miss)/4 (psql fail)/5 (row count mismatch)
  </behavior>
  <action>
    Tạo file `Hub_All/scripts/migrate/02-restore-hub.sh`:

    ```bash
    #!/usr/bin/env bash
    # Medinet Wiki — Phase 7 MIGRATE-02: Restore snapshot vào medinet_hub_<HUB> (blue/green per-hub)
    # Per D-V3-Phase7-B LOCKED 2026-05-23 — Blue/green zero-downtime, KHÔNG full downtime.
    #
    # Usage:
    #   bash scripts/migrate/02-restore-hub.sh yte
    #   HUB=duoc bash scripts/migrate/02-restore-hub.sh
    #
    # Prereq:
    #   - Plan 07-01 đã chạy: migrate-snapshots/migrate-<HUB>-<date>.sql exist
    #   - hub-init.sh đã chạy: medinet_hub_<HUB> DB exist với schema + Alembic 0001..0005
    #   - PGHOST/PGPORT/PGUSER/PGPASSWORD set HOẶC default

    set -euo pipefail
    IFS=$'\n\t'

    # ──────────────────────────────────────────────────────────────────────
    # (1) Parse args + validate
    # ──────────────────────────────────────────────────────────────────────

    HUB=${1:-${HUB:-}}

    if [ -z "$HUB" ]; then
        echo "[restore-hub] ERROR: thiếu hub name."
        echo "  Usage: bash scripts/migrate/02-restore-hub.sh <hub_name>"
        exit 2
    fi

    # Regex validate (T-5-05 carry forward)
    if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{0,15}$ ]]; then
        echo "[restore-hub] ERROR: hub name '$HUB' invalid format ^[a-z][a-z0-9_]{0,15}$"
        exit 2
    fi

    # RESERVED blacklist
    RESERVED_NAMES=("postgres" "cocoindex" "template0" "template1" "public" "medinet")
    for reserved in "${RESERVED_NAMES[@]}"; do
        if [ "$HUB" = "$reserved" ]; then
            echo "[restore-hub] ERROR: hub name '$HUB' reserved."
            exit 2
        fi
    done

    # Reject central
    if [ "$HUB" = "central" ]; then
        echo "[restore-hub] ERROR: 'central' KHÔNG restore qua script này."
        exit 2
    fi

    # ──────────────────────────────────────────────────────────────────────
    # (2) Postgres connection params + repo root
    # ──────────────────────────────────────────────────────────────────────

    PGHOST=${PGHOST:-localhost}
    PGPORT=${PGPORT:-5432}
    PGUSER_EFFECTIVE=${PGUSER:-medinet}
    export PGPASSWORD=${PGPASSWORD:-${POSTGRES_PASSWORD:-medinet_dev_pwd}}

    TARGET_DB="medinet_hub_$HUB"

    if [ -f "docker-compose.yml" ]; then
        REPO_ROOT="$(pwd)"
    elif [ -f "Hub_All/docker-compose.yml" ]; then
        REPO_ROOT="$(pwd)/Hub_All"
    elif [ -f "../docker-compose.yml" ]; then
        REPO_ROOT="$(cd .. && pwd)"
    else
        echo "[restore-hub] ERROR: KHÔNG tìm thấy docker-compose.yml."
        exit 2
    fi

    SNAPSHOT_DIR="$REPO_ROOT/migrate-snapshots"

    # ──────────────────────────────────────────────────────────────────────
    # (3) Auto-detect latest snapshot file
    # ──────────────────────────────────────────────────────────────────────

    echo "[restore-hub] === Restore hub '$HUB' → DB '$TARGET_DB' ==="
    echo "[restore-hub] (1/4) Auto-detect latest snapshot trong $SNAPSHOT_DIR..."

    SNAPSHOT_FILE=$(ls -1 "$SNAPSHOT_DIR"/migrate-"$HUB"-*.sql 2>/dev/null | sort -r | head -1 || echo "")

    if [ -z "$SNAPSHOT_FILE" ] || [ ! -f "$SNAPSHOT_FILE" ]; then
        echo "[restore-hub] ERROR: KHÔNG tìm thấy snapshot file migrate-${HUB}-*.sql trong $SNAPSHOT_DIR."
        echo "  Chạy: bash scripts/migrate/01-snapshot-hubs.sh $HUB"
        exit 3
    fi

    echo "[restore-hub] (1/4) Latest snapshot: $SNAPSHOT_FILE"

    # Count expected INSERT row trong snapshot
    EXPECTED_INSERTS=$(grep -c '^INSERT' "$SNAPSHOT_FILE" || echo "0")
    echo "[restore-hub] (1/4) Expected INSERT rows: $EXPECTED_INSERTS"

    if [ "$EXPECTED_INSERTS" = "0" ]; then
        echo "[restore-hub] WARN: snapshot có 0 INSERT — hub có thể empty hoặc snapshot corrupt."
        echo "  Proceed restore (0-row OK nếu hub mới)?"
    fi

    # ──────────────────────────────────────────────────────────────────────
    # (4) Verify target DB exist + schema ready
    # ──────────────────────────────────────────────────────────────────────

    echo "[restore-hub] (2/4) Verify target DB '$TARGET_DB' exist..."

    DB_EXISTS=$(psql -tA -v ON_ERROR_STOP=1 \
        -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
        -d postgres \
        -c "SELECT 1 FROM pg_database WHERE datname='$TARGET_DB'" 2>/dev/null || echo "")

    if [ "$DB_EXISTS" != "1" ]; then
        echo "[restore-hub] ERROR: DB '$TARGET_DB' KHÔNG tồn tại."
        echo "  Chạy: bash api/scripts/hub-init.sh $HUB"
        exit 3
    fi

    # T-07-02-03 mitigation: verify current_database() match
    CURRENT_DB=$(psql -tA -v ON_ERROR_STOP=1 \
        -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
        -d "$TARGET_DB" \
        -c "SELECT current_database()" 2>/dev/null || echo "")

    if [ "$CURRENT_DB" != "$TARGET_DB" ]; then
        echo "[restore-hub] ERROR: current_database() returns '$CURRENT_DB' KHÔNG khớp '$TARGET_DB'."
        exit 3
    fi

    echo "[restore-hub] (2/4) DB '$TARGET_DB' ready (current_database verified)."

    # ──────────────────────────────────────────────────────────────────────
    # (5) Restore qua psql -f
    # ──────────────────────────────────────────────────────────────────────

    echo "[restore-hub] (3/4) psql -f restore $(basename "$SNAPSHOT_FILE") → $TARGET_DB..."

    if ! psql -v ON_ERROR_STOP=1 \
        -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
        -d "$TARGET_DB" \
        -f "$SNAPSHOT_FILE" 2>&1 | tail -20; then
        echo "[restore-hub] ERROR: psql restore FAIL cho hub '$HUB'."
        echo "  Snapshot file giữ lại debug: $SNAPSHOT_FILE"
        exit 4
    fi

    # ──────────────────────────────────────────────────────────────────────
    # (6) Post-restore row count sanity check
    # ──────────────────────────────────────────────────────────────────────

    echo "[restore-hub] (4/4) Post-restore row count sanity check..."

    ACTUAL_CHUNKS=$(psql -tA -v ON_ERROR_STOP=1 \
        -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
        -d "$TARGET_DB" \
        -c "SELECT COUNT(*) FROM chunks" 2>/dev/null || echo "0")

    ACTUAL_DOCS=$(psql -tA -v ON_ERROR_STOP=1 \
        -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
        -d "$TARGET_DB" \
        -c "SELECT COUNT(*) FROM documents" 2>/dev/null || echo "0")

    ACTUAL_USERS=$(psql -tA -v ON_ERROR_STOP=1 \
        -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
        -d "$TARGET_DB" \
        -c "SELECT COUNT(*) FROM users" 2>/dev/null || echo "0")

    echo "[restore-hub] (4/4) Row counts post-restore:"
    echo "  chunks=$ACTUAL_CHUNKS, documents=$ACTUAL_DOCS, users=$ACTUAL_USERS"
    echo "  expected INSERT statements in snapshot: $EXPECTED_INSERTS"

    # Tolerance ±5% — INSERT statements include all 5 tables, không chính xác per-table
    # Chỉ verify total > 0 nếu expected > 0
    TOTAL_ROWS=$((ACTUAL_CHUNKS + ACTUAL_DOCS + ACTUAL_USERS))
    if [ "$EXPECTED_INSERTS" -gt "0" ] && [ "$TOTAL_ROWS" = "0" ]; then
        echo "[restore-hub] ERROR: Expected $EXPECTED_INSERTS INSERT nhưng DB hoàn toàn empty."
        exit 5
    fi

    echo "[restore-hub] DONE — hub '$HUB' restored từ $SNAPSHOT_FILE."
    echo ""
    echo "Next: bash scripts/migrate/03-switch-caddy.sh $HUB"
    echo "Then: bash scripts/migrate/05-smoke-e2e.sh $HUB"
    exit 0
    ```
  </action>
  <verify>
    <automated>bash -n Hub_All/scripts/migrate/02-restore-hub.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n Hub_All/scripts/migrate/02-restore-hub.sh` exit 0
    - `grep -c "set -euo pipefail" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 1
    - `grep -c "psql -v ON_ERROR_STOP=1" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 4 (verify DB + current_database + restore + counts)
    - `grep -c "medinet_hub_" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 2
    - `grep -c "current_database()" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 1 (T-07-02-03)
    - `grep -c "SELECT COUNT(\*) FROM chunks" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 1
    - `grep -c "RESERVED_NAMES" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 1
    - `grep -c "migrate-snapshots" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 1
    - `grep -c "ON_ERROR_STOP=1" Hub_All/scripts/migrate/02-restore-hub.sh` returns ≥ 4
    - Line 1 = `#!/usr/bin/env bash`
  </acceptance_criteria>
  <done>Script `02-restore-hub.sh` accept hub arg, validate regex+RESERVED, auto-detect latest snapshot, verify target DB + current_database match, psql -f restore với ON_ERROR_STOP, post-restore row count sanity check, exit codes 0/2/3/4/5 đúng.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo scripts/migrate/03-switch-caddy.sh VERIFY-ONLY (Caddyfile KHÔNG sed edit)</name>
  <files>Hub_All/scripts/migrate/03-switch-caddy.sh</files>
  <read_first>
    - Hub_All/Caddyfile (lines 28-87 — wiki block với path_regexp dynamic regex hub_api)
    - Hub_All/api/scripts/hub-add.sh (lines 290-341 — Step 9 caddy validate + reload + smoke pattern)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md (§specifics "Correction during planning" — VERIFY-ONLY mode)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md (§"scripts/migrate/03-switch-caddy.sh" pattern)
  </read_first>
  <behavior>
    - Accept arg required `$1` = hub name, optional `$2` = `--rollback`
    - Validate hub_name regex + RESERVED + reject central
    - Verify `docker compose ps python-api-<HUB>` running (JSON parse `"State":"running"`)
    - Verify caddy container running (skip nếu dev pre-up)
    - Caddy validate config (Pitfall 7 mitigation pre-reload)
    - Smoke `curl -k -sf https://${WIKI_PUBLIC_DOMAIN:-localhost}/<HUB>/api/health` warn-only
    - --rollback mode: `docker compose stop python-api-<HUB>` (Caddy regex tự fall-through file_server → user-visible 404 signal switch revert)
    - KHÔNG sed edit Caddyfile (per CONTEXT.md correction — Phase 5 đã ship dynamic regex)
    - Exit codes: 0/2 (arg invalid)/3 (container không running)/4 (caddy validate fail)
  </behavior>
  <action>
    Tạo file `Hub_All/scripts/migrate/03-switch-caddy.sh`:

    ```bash
    #!/usr/bin/env bash
    # Medinet Wiki — Phase 7 MIGRATE-02: Verify Caddy auto-route post-restore (VERIFY-ONLY).
    #
    # Per CONTEXT.md `<specifics>` correction 2026-05-23: Caddyfile sau Phase 5 đã có
    # dynamic regex `{re.hub_api.1}` capture hub_name từ URL prefix + `reverse_proxy
    # python-api-{re.hub_api.1}:8080` (Phase 5 Plan 05-01). Phase 7 KHÔNG cần sed edit
    # Caddyfile — chỉ verify python-api-<HUB> container running + Caddy auto-route.
    #
    # Usage:
    #   bash scripts/migrate/03-switch-caddy.sh yte              # verify + smoke
    #   bash scripts/migrate/03-switch-caddy.sh yte --rollback   # stop hub container (revert routing)
    #   HUB=duoc bash scripts/migrate/03-switch-caddy.sh

    set -euo pipefail
    IFS=$'\n\t'

    # ──────────────────────────────────────────────────────────────────────
    # (1) Parse args
    # ──────────────────────────────────────────────────────────────────────

    HUB=${1:-${HUB:-}}
    MODE=${2:-"verify"}

    if [ -z "$HUB" ]; then
        echo "[switch-caddy] ERROR: thiếu hub name."
        echo "  Usage: bash scripts/migrate/03-switch-caddy.sh <hub_name> [--rollback]"
        exit 2
    fi

    # Regex validate
    if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{0,15}$ ]]; then
        echo "[switch-caddy] ERROR: hub name '$HUB' invalid format ^[a-z][a-z0-9_]{0,15}$"
        exit 2
    fi

    # RESERVED blacklist
    RESERVED_NAMES=("postgres" "cocoindex" "template0" "template1" "public" "medinet")
    for reserved in "${RESERVED_NAMES[@]}"; do
        if [ "$HUB" = "$reserved" ]; then
            echo "[switch-caddy] ERROR: hub name '$HUB' reserved."
            exit 2
        fi
    done

    # Reject central
    if [ "$HUB" = "central" ]; then
        echo "[switch-caddy] ERROR: 'central' KHÔNG switch qua script này."
        exit 2
    fi

    # ──────────────────────────────────────────────────────────────────────
    # (2) Rollback mode: stop hub container
    # ──────────────────────────────────────────────────────────────────────

    if [ "$MODE" = "--rollback" ]; then
        echo "[switch-caddy] === ROLLBACK hub '$HUB' (stop container — Caddy fall through file_server 404) ==="
        if docker compose ps "python-api-$HUB" --format json 2>/dev/null | grep -q '"State":"running"'; then
            docker compose stop "python-api-$HUB"
            echo "[switch-caddy] ROLLBACK DONE — python-api-$HUB stopped."
            echo "  Caddy regex /(yte|duoc|hcns)/api/* fall-through file_server → 404 user-visible signal."
            echo "  Re-start: docker compose start python-api-$HUB"
            exit 0
        else
            echo "[switch-caddy] WARN: python-api-$HUB không running — nothing to rollback."
            exit 0
        fi
    fi

    # ──────────────────────────────────────────────────────────────────────
    # (3) Verify-only mode: container running + caddy validate + smoke
    # ──────────────────────────────────────────────────────────────────────

    echo "[switch-caddy] === VERIFY Caddy auto-route hub '$HUB' ==="

    # Step 1: Verify python-api-<HUB> container running
    echo "[switch-caddy] (1/3) Verify python-api-$HUB container running..."
    if ! docker compose ps "python-api-$HUB" --format json 2>/dev/null | grep -q '"State":"running"'; then
        echo "[switch-caddy] ERROR: python-api-$HUB KHÔNG running."
        echo "  Run: docker compose up -d python-api-$HUB"
        exit 3
    fi
    echo "[switch-caddy] (1/3) python-api-$HUB running."

    # Step 2: Caddy validate (Pitfall 7 mitigation — Phase 5 Plan 05-05 carry forward)
    echo "[switch-caddy] (2/3) Caddy validate config..."
    if docker compose ps caddy --format json 2>/dev/null | grep -q '"State":"running"'; then
        if ! docker compose exec -T caddy caddy validate --config /etc/caddy/Caddyfile 2>&1; then
            echo "[switch-caddy] ERROR: Caddy config validate FAIL."
            exit 4
        fi
        echo "[switch-caddy] (2/3) Caddy validate PASS."
    else
        echo "[switch-caddy] (2/3) Caddy container chưa running — skip validate (dev pre-up)."
        echo "  Sau khi up: docker compose exec caddy caddy validate --config /etc/caddy/Caddyfile"
    fi

    # Step 3: Smoke curl /<HUB>/api/health
    echo "[switch-caddy] (3/3) Smoke check Caddy auto-route..."
    if command -v curl >/dev/null 2>&1; then
        DOMAIN="${WIKI_PUBLIC_DOMAIN:-localhost}"
        SMOKE_URL="https://${DOMAIN}/${HUB}/api/health"
        echo "[switch-caddy] (3/3) curl -k ${SMOKE_URL}"
        if curl -k -sf -o /dev/null --max-time 5 "${SMOKE_URL}" 2>/dev/null; then
            echo "[switch-caddy] (3/3) Smoke PASS — Caddy auto-route python-api-$HUB:8080 (dynamic regex {re.hub_api.1})."
        else
            echo "[switch-caddy] WARN: Smoke check returned non-200 hoặc timeout."
            echo "  Caddy có thể chưa running HOẶC python-api-$HUB chưa ready ($DOMAIN unreachable)."
            echo "  Retry sau 5-10s: curl -k $SMOKE_URL"
        fi
    else
        echo "[switch-caddy] (3/3) Skip smoke check (curl không có)."
    fi

    echo ""
    echo "[switch-caddy] DONE — hub '$HUB' Caddy auto-route verified."
    echo "Next: bash scripts/migrate/05-smoke-e2e.sh $HUB  # golden path 7-step E2E"
    exit 0
    ```
  </action>
  <verify>
    <automated>bash -n Hub_All/scripts/migrate/03-switch-caddy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n Hub_All/scripts/migrate/03-switch-caddy.sh` exit 0
    - `grep -c "set -euo pipefail" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 1
    - `grep -c "caddy validate" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 1 (Pitfall 7)
    - `grep -c "docker compose ps python-api-" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 1
    - `grep -cF '"State":"running"' Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 2 (verify + rollback check)
    - `grep -c "WIKI_PUBLIC_DOMAIN" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 1
    - `grep -c "curl -k" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 1 (smoke pattern)
    - `grep -c -- "--rollback" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 2 (handle mode + usage)
    - `grep -c "docker compose stop" Hub_All/scripts/migrate/03-switch-caddy.sh` returns ≥ 1 (rollback)
    - `grep -cv "sed.*Caddyfile" Hub_All/scripts/migrate/03-switch-caddy.sh` — ensure NO sed edit (verify-only correctness)
    - Line 1 = `#!/usr/bin/env bash`
  </acceptance_criteria>
  <done>Script `03-switch-caddy.sh` VERIFY-ONLY (KHÔNG sed Caddyfile), check container running + caddy validate + smoke curl, hỗ trợ --rollback stop container, exit codes 0/2/3/4 đúng.</done>
</task>

</tasks>

<verification>
- bash syntax PASS cả 2 script (`bash -n` exit 0)
- 10+ acceptance grep PASS trên 2 script
- 02-restore-hub.sh có 4-step pipeline (detect + verify DB + restore + sanity count)
- 03-switch-caddy.sh VERIFY-ONLY (KHÔNG sed Caddyfile per CONTEXT correction)
- Plan 07-04 + 07-05 có thể consume 2 script này trong smoke + 7-step deploy procedure
</verification>

<success_criteria>
- 2 bash script (~130 + 80 LOC) committed
- Blue/green per-hub restore procedure (D-V3-Phase7-B LOCKED) implementable bằng `01-snapshot` → `02-restore <HUB>` → `03-switch-caddy <HUB>` → `05-smoke <HUB>` chain
- Rollback path enable qua `03-switch-caddy --rollback`
- MIGRATE-02 acceptance criteria satisfied (psql restore vào DB con + verify smoke + switch Caddy upstream — verify-only per dynamic regex)
</success_criteria>

<output>
After completion, create `Hub_All/.planning/phases/07-migration-smoke-e2e/07-02-SUMMARY.md` per template.
</output>
