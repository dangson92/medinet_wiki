---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Hub_All/scripts/migrate/01-snapshot-hubs.sh
  - Hub_All/migrate-snapshots/.gitignore
  - Hub_All/migrate-snapshots/README.md
autonomous: true
requirements: [MIGRATE-01]
tags: [migration, pg-dump, snapshot, bash-script, wave-1-blocking]

must_haves:
  truths:
    - "scripts/migrate/01-snapshot-hubs.sh bash strict mode (set -euo pipefail) + IFS=$'\\n\\t'"
    - "Script accept 1 hub arg HOẶC empty = loop 3 hub default (yte/duoc/hcns)"
    - "Resolve hub_id UUID từ medinet_central.hubs WHERE hub_name=$HUB qua psql -tA"
    - "pg_dump --data-only --no-owner --no-acl --column-inserts với 5 --table flag + --where hub_id output migrate-snapshots/migrate-<hub>-<YYYY-MM-DD>.sql"
    - "Hub_name regex validate ^[a-z][a-z0-9_]{0,15}$ + RESERVED blacklist 6 name (T-5-05 carry forward) + reject central"
    - "Row count sanity check post-dump qua grep -c '^INSERT' + warn nếu 0"
    - "migrate-snapshots/.gitignore exclude *.sql + *.sql.gpg (KHÔNG commit production data PII)"
    - "migrate-snapshots/README.md document 30-day retention + recovery procedure"
  artifacts:
    - path: "Hub_All/scripts/migrate/01-snapshot-hubs.sh"
      provides: "pg_dump snapshot 3 hub_id từ medinet_central"
      min_lines: 120
      contains: "pg_dump --data-only"
    - path: "Hub_All/migrate-snapshots/.gitignore"
      provides: "Exclude .sql snapshot khỏi git (privacy)"
      contains: "*.sql"
    - path: "Hub_All/migrate-snapshots/README.md"
      provides: "30-day retention policy + recovery procedure"
      contains: "find migrate-snapshots/"
  key_links:
    - from: "scripts/migrate/01-snapshot-hubs.sh"
      to: "medinet_central.hubs table"
      via: "psql -tA resolve hub_id UUID"
      pattern: "SELECT id FROM hubs WHERE hub_name"
    - from: "scripts/migrate/01-snapshot-hubs.sh"
      to: "migrate-snapshots/ output directory"
      via: "pg_dump > file path"
      pattern: "migrate-snapshots/migrate-"
    - from: "scripts/migrate/01-snapshot-hubs.sh"
      to: "hub-init.sh regex pattern"
      via: "T-5-05 carry forward bash strict mode"
      pattern: "\\^\\[a-z\\]\\[a-z0-9_\\]"

threat_model:
  trust_boundaries:
    - boundary: "operator shell → pg_dump → migrate-snapshots/ filesystem"
      description: "Operator chạy script với superuser DB access; output chứa PII (users + audit_logs + usage_events)"
    - boundary: "operator shell arg → psql medinet_central hub_id resolution"
      description: "psql -c arg injection risk nếu hub_name không validate"
  threats:
    - id: T-07-01-01
      category: Tampering
      component: "01-snapshot-hubs.sh hub_name arg"
      disposition: mitigate
      mitigation: "Regex ^[a-z][a-z0-9_]{0,15}$ + RESERVED blacklist + reject central trước khi pass vào psql/pg_dump (T-5-05 carry forward hub-add.sh)"
    - id: T-07-01-02
      category: Information Disclosure
      component: "migrate-snapshots/*.sql output files"
      disposition: mitigate
      mitigation: ".gitignore *.sql + README explicit warn 'KHÔNG commit production data' + operator local-only retention 30-day"
    - id: T-07-01-03
      category: Denial of Service
      component: "pg_dump on medinet_central (50k+ chunks per hub)"
      disposition: accept
      mitigation: "Operator runs off-peak; pg_dump --where filter giảm row scope; ~5-10 min per hub acceptable"
    - id: T-07-01-04
      category: Repudiation
      component: "Snapshot file naming convention"
      disposition: mitigate
      mitigation: "Filename pattern migrate-<hub>-<YYYY-MM-DD>.sql immutable timestamp; pg_dump header tự ghi version + DB + timestamp"
---

<objective>
Wave 1 BLOCKING của Phase 7 — Tạo script `scripts/migrate/01-snapshot-hubs.sh` snapshot 3 hub (yte/duoc/hcns) từ `medinet_central` (v2.0 legacy DB) qua `pg_dump --data-only --where="hub_id = '<uuid>'"` thành file `migrate-<hub>-<YYYY-MM-DD>.sql` lưu `migrate-snapshots/`. Tạo `.gitignore` exclude `.sql` khỏi git + `README.md` doc 30-day retention.

**Purpose:** Đầu vào cho toàn bộ migration pipeline (D-V3-Phase7-A LOCKED — `pg_dump --where` option a, REJECT cocoindex replay). Mọi script downstream (`02-restore`, `04-truncate`) phụ thuộc snapshot consistent-at-time-T. Foundation BLOCKING vì 4 plan sau cần output này.

**Output:** 1 bash script (~150-180 LOC) + 1 .gitignore + 1 README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md
@Hub_All/api/scripts/hub-init.sh
@Hub_All/api/scripts/hub-add.sh
@Hub_All/docker-compose.yml

<interfaces>
<!-- Key SQL/bash contracts script này dùng — extracted from codebase analog -->

**Postgres connection params (carry forward hub-init.sh lines 47-48):**
```bash
PGHOST=${PGHOST:-localhost}
PGPORT=${PGPORT:-5432}
PGUSER_EFFECTIVE=${PGUSER:-medinet}
export PGPASSWORD=${PGPASSWORD:-${POSTGRES_PASSWORD:-medinet_dev_pwd}}
SOURCE_DB="medinet_central"
```

**medinet_central.hubs schema (resolve hub_id UUID — Phase 4 carry forward):**
```sql
-- checksum_scheduler.py lines 102-110 pattern
SELECT id FROM hubs WHERE hub_name = '<hub>' LIMIT 1;
-- returns UUID4 stable identity matching HUB_*_ID env wire Phase 4 Plan 04-02
```

**5 tables to snapshot per hub (CONTEXT.md domain item 1):**
- `chunks` (50k rows/hub, vector(1536) ~50MB)
- `documents` (~500 rows/hub)
- `users` (~50 rows/hub, PII)
- `audit_logs` (~5k rows/hub, PII)
- `usage_events` (~10k rows/hub, PII)

**pg_dump flags (CONTEXT.md `<decisions>` Claude's Discretion):**
- `--data-only` — skip schema (Alembic 0001..0005 đã upgrade)
- `--no-owner --no-acl` — skip ownership/GRANT
- `--column-inserts` — cross-PG-version safe
- `--table=<name>` × 5 explicit
- `--where="hub_id = '<uuid>'"` — row-level filter per hub
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo scripts/migrate/01-snapshot-hubs.sh pg_dump snapshot 3 hub</name>
  <files>Hub_All/scripts/migrate/01-snapshot-hubs.sh</files>
  <read_first>
    - Hub_All/api/scripts/hub-init.sh (lines 31-50 — bash strict mode + regex validate pattern)
    - Hub_All/api/scripts/hub-add.sh (lines 43-91 — validation chain T-5-05 mitigation)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md (§"scripts/migrate/01-snapshot-hubs.sh" pattern assignment)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md (§decisions D-V3-Phase7-A LOCKED)
    - Hub_All/docker-compose.yml (lines 44-61 postgres params)
  </read_first>
  <behavior>
    - Bash strict mode `set -euo pipefail` + `IFS=$'\n\t'` dòng đầu sau shebang
    - Accept `$1` = single hub HOẶC empty = loop 3 hub default
    - Validate hub_name regex `^[a-z][a-z0-9_]{0,15}$` (copy verbatim hub-add.sh line 63)
    - RESERVED_NAMES blacklist 6 name (sync hub-add.sh line 75)
    - Reject `central` explicit (KHÔNG snapshot central qua script này)
    - Resolve hub_id UUID qua psql -tA `SELECT id FROM hubs WHERE hub_name='$HUB'`
    - Exit code 3 nếu hub_id không resolve
    - Tạo dir `migrate-snapshots/` nếu chưa có (mkdir -p)
    - Output file: `migrate-snapshots/migrate-<hub>-$(date +%Y-%m-%d).sql`
    - Idempotent skip nếu file đã tồn tại
    - pg_dump 5 --table flag + --where filter hub_id
    - Sanity check `grep -c '^INSERT' "$OUT_FILE"` log row count + warn nếu 0
    - Exit codes: 0/2 (arg invalid)/3 (hub_id miss)/4 (pg_dump fail)
  </behavior>
  <action>
    Tạo file `Hub_All/scripts/migrate/01-snapshot-hubs.sh` (writer agent: COPY VERBATIM nội dung dưới, KHÔNG paraphrase):

    ```bash
    #!/usr/bin/env bash
    # Medinet Wiki — Phase 7 MIGRATE-01: Snapshot data per hub_id từ medinet_central
    # → migrate-snapshots/migrate-<hub>-<YYYY-MM-DD>.sql
    #
    # Per D-V3-Phase7-A LOCKED 2026-05-23 — pg_dump --where (option a, REJECT
    # cocoindex replay): speed ~5-10 min/hub + vector preserve + schema match.
    #
    # Usage:
    #   bash scripts/migrate/01-snapshot-hubs.sh                # loop 3 hub default
    #   bash scripts/migrate/01-snapshot-hubs.sh yte            # single hub
    #   HUB=duoc bash scripts/migrate/01-snapshot-hubs.sh
    #
    # Output: migrate-snapshots/migrate-<hub>-<date>.sql (gitignored — privacy PII).
    # Retention: 30 days (xem migrate-snapshots/README.md cron command).
    #
    # Prereq:
    #   - Postgres container running (docker compose up -d postgres)
    #   - PGHOST/PGPORT/PGUSER/PGPASSWORD set HOẶC default medinet/medinet_dev_pwd
    #   - medinet_central.hubs có row hub_name match arg + id UUID stable

    set -euo pipefail
    IFS=$'\n\t'

    # ──────────────────────────────────────────────────────────────────────
    # (1) Parse args
    # ──────────────────────────────────────────────────────────────────────

    HUB_ARG=${1:-${HUB:-}}

    if [ -z "$HUB_ARG" ]; then
        HUBS_TO_SNAPSHOT=("yte" "duoc" "hcns")
        echo "[snapshot-hubs] Default loop 3 hub: ${HUBS_TO_SNAPSHOT[*]}"
    else
        HUBS_TO_SNAPSHOT=("$HUB_ARG")
    fi

    validate_hub_name() {
        local HUB="$1"

        # Regex validate (sync hub-init.sh + hub-add.sh + Settings Plan 02-05 FACTOR-04)
        if ! [[ "$HUB" =~ ^[a-z][a-z0-9_]{0,15}$ ]]; then
            echo "[snapshot-hubs] ERROR: hub name '$HUB' invalid format."
            echo "  Pattern required: ^[a-z][a-z0-9_]{0,15}$"
            return 2
        fi

        # RESERVED blacklist (sync RESERVED_HUB_NAMES app/config.py)
        local RESERVED_NAMES=("postgres" "cocoindex" "template0" "template1" "public" "medinet")
        for reserved in "${RESERVED_NAMES[@]}"; do
            if [ "$HUB" = "$reserved" ]; then
                echo "[snapshot-hubs] ERROR: hub name '$HUB' reserved (Postgres system DB / role)."
                return 2
            fi
        done

        # Reject central (aggregator — KHÔNG snapshot central qua script này)
        if [ "$HUB" = "central" ]; then
            echo "[snapshot-hubs] ERROR: 'central' KHÔNG snapshot qua script này."
            echo "  Phase 7 chỉ snapshot 3 hub con; central truncate qua 04-truncate-central.sh."
            return 2
        fi

        return 0
    }

    # ──────────────────────────────────────────────────────────────────────
    # (2) Postgres connection params (carry forward docker-compose.yml)
    # ──────────────────────────────────────────────────────────────────────

    PGHOST=${PGHOST:-localhost}
    PGPORT=${PGPORT:-5432}
    PGUSER_EFFECTIVE=${PGUSER:-medinet}
    export PGPASSWORD=${PGPASSWORD:-${POSTGRES_PASSWORD:-medinet_dev_pwd}}

    SOURCE_DB="medinet_central"

    # ──────────────────────────────────────────────────────────────────────
    # (3) Determine repo root + create output dir
    # ──────────────────────────────────────────────────────────────────────

    if [ -f "docker-compose.yml" ]; then
        REPO_ROOT="$(pwd)"
    elif [ -f "Hub_All/docker-compose.yml" ]; then
        REPO_ROOT="$(pwd)/Hub_All"
    elif [ -f "../docker-compose.yml" ]; then
        REPO_ROOT="$(cd .. && pwd)"
    else
        echo "[snapshot-hubs] ERROR: KHÔNG tìm thấy docker-compose.yml."
        echo "  Chạy từ repo root hoặc Hub_All/ directory."
        exit 2
    fi

    OUT_DIR="$REPO_ROOT/migrate-snapshots"
    mkdir -p "$OUT_DIR"

    DATE_TAG=$(date +%Y-%m-%d)

    # ──────────────────────────────────────────────────────────────────────
    # (4) Snapshot one hub function
    # ──────────────────────────────────────────────────────────────────────

    snapshot_one_hub() {
        local HUB="$1"

        echo ""
        echo "[snapshot-hubs] === Snapshot hub '$HUB' (date=$DATE_TAG) ==="

        if ! validate_hub_name "$HUB"; then
            return 2
        fi

        # Resolve hub_id UUID
        echo "[snapshot-hubs] (1/3) Resolve hub_id UUID từ $SOURCE_DB.hubs..."
        local HUB_ID
        HUB_ID=$(psql -tA -v ON_ERROR_STOP=1 \
            -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
            -d "$SOURCE_DB" \
            -c "SELECT id FROM hubs WHERE hub_name='$HUB' LIMIT 1" 2>/dev/null || echo "")

        if [ -z "$HUB_ID" ]; then
            echo "[snapshot-hubs] ERROR: KHÔNG tìm thấy hub_id cho hub_name='$HUB' trong $SOURCE_DB.hubs."
            echo "  Kiểm tra: psql -d $SOURCE_DB -c \"SELECT id, hub_name FROM hubs;\""
            return 3
        fi

        echo "[snapshot-hubs] (1/3) hub_id='$HUB_ID'"

        local OUT_FILE="$OUT_DIR/migrate-${HUB}-${DATE_TAG}.sql"

        # Idempotent skip
        if [ -f "$OUT_FILE" ]; then
            echo "[snapshot-hubs] (2/3) $OUT_FILE đã tồn tại — skip (xóa explicit để re-snapshot)."
            return 0
        fi

        # pg_dump (D-V3-Phase7-A LOCKED)
        echo "[snapshot-hubs] (2/3) pg_dump --data-only --where=\"hub_id='$HUB_ID'\" 5 table..."
        if ! pg_dump \
            -h "$PGHOST" -p "$PGPORT" -U "$PGUSER_EFFECTIVE" \
            --data-only \
            --no-owner \
            --no-acl \
            --column-inserts \
            --table=chunks \
            --table=documents \
            --table=users \
            --table=audit_logs \
            --table=usage_events \
            --where="hub_id = '$HUB_ID'" \
            "$SOURCE_DB" > "$OUT_FILE" 2>&1; then
            echo "[snapshot-hubs] ERROR: pg_dump FAIL cho hub '$HUB'."
            rm -f "$OUT_FILE"
            return 4
        fi

        # Sanity check
        echo "[snapshot-hubs] (3/3) Sanity check row count..."
        local ROW_COUNT
        ROW_COUNT=$(grep -c '^INSERT' "$OUT_FILE" || echo "0")
        local FILE_SIZE
        FILE_SIZE=$(wc -c < "$OUT_FILE" | tr -d ' ')

        if [ "$ROW_COUNT" = "0" ]; then
            echo "[snapshot-hubs] WARN: hub '$HUB' có 0 INSERT row — hub có thể empty hoặc hub_id resolve sai."
            echo "         File giữ lại debug: $OUT_FILE (size $FILE_SIZE bytes)"
        else
            echo "[snapshot-hubs] (3/3) PASS — $ROW_COUNT INSERT statements, file size $FILE_SIZE bytes."
        fi

        echo "[snapshot-hubs] DONE hub '$HUB' → $OUT_FILE"
        return 0
    }

    # ──────────────────────────────────────────────────────────────────────
    # (5) Execute sequential (v3.0 first migration ưu tiên debug)
    # ──────────────────────────────────────────────────────────────────────

    FAIL_COUNT=0
    for HUB in "${HUBS_TO_SNAPSHOT[@]}"; do
        if ! snapshot_one_hub "$HUB"; then
            FAIL_COUNT=$((FAIL_COUNT + 1))
        fi
    done

    echo ""
    echo "[snapshot-hubs] === Summary ==="
    echo "[snapshot-hubs] Total: ${#HUBS_TO_SNAPSHOT[@]} hub. Failed: $FAIL_COUNT."

    if [ "$FAIL_COUNT" -gt 0 ]; then
        exit 1
    fi

    echo "[snapshot-hubs] Next: bash scripts/migrate/02-restore-hub.sh <hub>"
    exit 0
    ```

    Đảm bảo file có shebang `#!/usr/bin/env bash` ở dòng đầu tiên (line 1).
  </action>
  <verify>
    <automated>bash -n Hub_All/scripts/migrate/01-snapshot-hubs.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n Hub_All/scripts/migrate/01-snapshot-hubs.sh` exit 0 (syntax OK)
    - `grep -c "set -euo pipefail" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1
    - `grep -c "pg_dump --data-only" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1
    - `grep -cF "^[a-z][a-z0-9_]" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1 (regex pattern present)
    - `grep -c "RESERVED_NAMES" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1 (T-5-05 mitigation)
    - `grep -c "migrate-snapshots/migrate-" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1
    - `grep -c "SELECT id FROM hubs" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1
    - `grep -cF '"central"' Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 1 (reject central explicit)
    - `grep -c "snapshot_one_hub" Hub_All/scripts/migrate/01-snapshot-hubs.sh` returns ≥ 2 (def + call)
    - Line 1 = `#!/usr/bin/env bash` (`head -1 Hub_All/scripts/migrate/01-snapshot-hubs.sh` matches)
  </acceptance_criteria>
  <done>Script `01-snapshot-hubs.sh` được tạo, syntax PASS, validate regex + RESERVED + reject central, resolve hub_id qua psql, pg_dump 5 table với --where, sanity check post-dump, exit codes 0/2/3/4 đúng semantic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo migrate-snapshots/.gitignore + README.md retention policy</name>
  <files>Hub_All/migrate-snapshots/.gitignore, Hub_All/migrate-snapshots/README.md</files>
  <read_first>
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md (§domain item 1 retention 30-day; §code_context snapshot encryption defer)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md (§"Audit log INSERT" privacy context)
  </read_first>
  <action>
    **File 1 — `Hub_All/migrate-snapshots/.gitignore`** (copy verbatim):

    ```gitignore
    # Phase 7 MIGRATE-01 (D-V3-Phase7-A LOCKED 2026-05-23)
    # Loại trừ snapshot pg_dump khỏi git — chứa PII (users + audit_logs + usage_events).
    # KHÔNG commit production data vào repo public/private.
    *.sql
    *.sql.gpg
    *.dump

    # Giữ README.md + .gitignore (tracked)
    !.gitignore
    !README.md
    ```

    **File 2 — `Hub_All/migrate-snapshots/README.md`** (copy verbatim):

    ```markdown
    # migrate-snapshots/ — Phase 7 MIGRATE-01 snapshot directory

    **Created:** 2026-05-23 (Phase 7 Plan 07-01)
    **Purpose:** Lưu file `pg_dump` snapshot per-hub từ `medinet_central` legacy DB sang `medinet_hub_<name>` (D-V3-Phase7-A LOCKED).

    ## File naming convention

    `migrate-<hub>-<YYYY-MM-DD>.sql`

    Ví dụ:
    - `migrate-yte-2026-05-23.sql`
    - `migrate-duoc-2026-05-23.sql`
    - `migrate-hcns-2026-05-23.sql`

    Date tag enables `find -mtime +30 -delete` automation.

    ## Generate snapshot

    ```bash
    # Loop 3 hub default
    bash scripts/migrate/01-snapshot-hubs.sh

    # Single hub
    bash scripts/migrate/01-snapshot-hubs.sh yte
    ```

    Output file size kỳ vọng: ~50-200 MB/hub (50k chunks × vector(1536) × float4 ~6 KB/row).

    ## 30-day retention policy

    Snapshot file là **operator-local backup** cho rollback per-hub blue/green (Phase 7 Plan 07-02 D-V3-Phase7-B procedure). Giữ tối thiểu 30 ngày để enable rollback nếu post-migration smoke fail.

    ### Manual cleanup

    ```bash
    # Liệt kê file > 30 ngày
    find migrate-snapshots/ -name "*.sql" -mtime +30 -print

    # Xóa file > 30 ngày
    find migrate-snapshots/ -name "*.sql" -mtime +30 -delete
    ```

    ### Cron automation (optional)

    ```cron
    # Daily 4AM cleanup file > 30 ngày
    0 4 * * * find /path/to/Hub_All/migrate-snapshots/ -name "*.sql" -mtime +30 -delete
    ```

    ## Privacy + security

    - **KHÔNG commit `.sql` vào git** — `.gitignore` enforce. Nội dung file chứa PII:
      - `users`: email, username, password_hash
      - `audit_logs`: user actions + timestamps
      - `usage_events`: usage analytics per-user
      - `documents`: file metadata (titles, hashes)
    - **Operator local-only**: KHÔNG sync lên cloud storage không encrypted. Nếu cần backup remote, encrypt qua `gpg --symmetric` trước upload (defer v4.0).
    - **File permission**: `chmod 600 migrate-*.sql` sau `pg_dump` nếu deploy multi-user host (default umask 644 readable).

    ## Recovery procedure (rollback Phase 7 migration)

    Nếu post-migration smoke fail per-hub (Plan 07-05 `05-smoke-e2e.sh` exit 1):

    ```bash
    # 1. Switch Caddy upstream về central (Plan 07-02 03-switch-caddy.sh --rollback)
    bash scripts/migrate/03-switch-caddy.sh yte --rollback

    # 2. Drop hub con DB + re-create từ snapshot
    psql -U medinet -d postgres -c "DROP DATABASE medinet_hub_yte;"
    bash api/scripts/hub-init.sh yte  # CREATE DB + alembic upgrade head
    psql -U medinet -d medinet_hub_yte -f migrate-snapshots/migrate-yte-<date>.sql

    # 3. Re-run smoke
    bash scripts/migrate/05-smoke-e2e.sh yte
    ```

    Snapshot file MUST PERSIST 30 ngày tối thiểu để enable rollback path.

    ## Reference

    - `scripts/migrate/01-snapshot-hubs.sh` — Generator script.
    - `.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md` §domain item 1 — Snapshot rationale.
    - `.planning/phases/07-migration-smoke-e2e/07-01-PLAN.md` — Plan này.
    - D-V3-Phase7-A LOCKED 2026-05-23 — `pg_dump --where` option a.
    ```
  </action>
  <verify>
    <automated>test -f Hub_All/migrate-snapshots/.gitignore && test -f Hub_All/migrate-snapshots/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Hub_All/migrate-snapshots/.gitignore` exit 0
    - `test -f Hub_All/migrate-snapshots/README.md` exit 0
    - `grep -cF "*.sql" Hub_All/migrate-snapshots/.gitignore` returns ≥ 1
    - `grep -cF "!.gitignore" Hub_All/migrate-snapshots/.gitignore` returns ≥ 1
    - `grep -cF "!README.md" Hub_All/migrate-snapshots/.gitignore` returns ≥ 1
    - `grep -c "find migrate-snapshots/" Hub_All/migrate-snapshots/README.md` returns ≥ 1
    - `grep -ci "30 ngày\|30 days" Hub_All/migrate-snapshots/README.md` returns ≥ 1
    - `grep -ci "privacy\|PII" Hub_All/migrate-snapshots/README.md` returns ≥ 1
    - `grep -c "migrate-yte-2026-05-23.sql" Hub_All/migrate-snapshots/README.md` returns ≥ 1
  </acceptance_criteria>
  <done>migrate-snapshots/ directory có .gitignore exclude *.sql + README.md document 30-day retention + privacy warn + recovery rollback procedure.</done>
</task>

</tasks>

<verification>
- bash syntax PASS (`bash -n` exit 0)
- 9 acceptance grep PASS trên `01-snapshot-hubs.sh`
- `.gitignore` exclude .sql + track README.md/.gitignore
- README.md có 30-day retention + privacy + recovery sections
- Plan 07-02 có thể consume output file `migrate-snapshots/migrate-<hub>-<date>.sql` cho restore
</verification>

<success_criteria>
- 1 bash script (~150-180 LOC) + 1 .gitignore + 1 README.md committed
- Script syntactically valid + idempotent (skip nếu file đã tồn tại)
- Foundation BLOCKING Plan 07-02 + Plan 07-03 đọc snapshot file output
- MIGRATE-01 acceptance criteria satisfied (pg_dump per hub_id thành công + file output đúng naming)
</success_criteria>

<output>
After completion, create `Hub_All/.planning/phases/07-migration-smoke-e2e/07-01-SUMMARY.md` per template — list deliverable + commit count + acceptance grep PASS + script LOC + ready handoff to Plan 07-02.
</output>
