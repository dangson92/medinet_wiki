---
phase: 07
plan: 04
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - Hub_All/mcp_service/mcp_app/config.py
  - Hub_All/docker-compose.yml
  - Hub_All/scripts/migrate/06-mcp-smoke.md
autonomous: true
requirements: [MIGRATE-04]
tags: [migration, mcp-repoint, oauth, runbook, regression-test]

must_haves:
  truths:
    - "mcp_service/mcp_app/config.py Settings.api_base_url default đổi từ 'http://localhost:8180' → 'http://python-api-central:8080' (D-V3-Phase7-C LOCKED MCP re-point central, KHÔNG fan-out N hub)"
    - "docker-compose.yml mcp_service env block: MCP_API_BASE_URL: http://python-api-central:8080 — verify-only (Phase 2 đã set đúng từ Plan 02-02, Phase 7 update comment 're-confirm' → 'CONFIRMED Phase 7 MIGRATE-04')"
    - "scripts/migrate/06-mcp-smoke.md runbook 5-step manual checklist (Inspector OAuth + search_wiki yte + ask_wiki cross-hub + citation verify + 135/135 regression)"
    - "mcp_service/tests/ 135 test PASS regression mandatory pre-deploy (Phase 8.3 v2.0 carry forward unchanged)"
    - "OAuth flow Phase 8.3 carry forward UNCHANGED — mcp_service KHÔNG đụng api_key_service AES-GCM AUX-02 (per CONTEXT.md carry forward)"
    - "field_validator _validate_base_url unchanged — chỉ thay default value (KHÔNG đụng validation logic SSRF mitigation T-08.2-01-T)"
    - "Env var name MCP_API_BASE_URL giữ NGUYÊN (carry forward — operator chỉ override .env value, KHÔNG đụng config.py field name)"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/config.py"
      provides: "Settings.api_base_url default re-point central"
      contains: "http://python-api-central:8080"
    - path: "Hub_All/docker-compose.yml"
      provides: "mcp_service env MCP_API_BASE_URL verify-only comment update"
      contains: "Phase 7 MIGRATE-04"
    - path: "Hub_All/scripts/migrate/06-mcp-smoke.md"
      provides: "5-step Inspector OAuth smoke runbook"
      min_lines: 80
      contains: "Claude Inspector"
  key_links:
    - from: "mcp_service/mcp_app/config.py::Settings.api_base_url"
      to: "python-api-central:8080 (D-V3-02 LOCKED aggregator)"
      via: "HTTP request từ MCP service tới central"
      pattern: "python-api-central"
    - from: "docker-compose.yml mcp_service env"
      to: "Settings env_prefix MCP_ → MCP_API_BASE_URL"
      via: "pydantic-settings BaseSettings env override"
      pattern: "MCP_API_BASE_URL"
    - from: "scripts/migrate/06-mcp-smoke.md"
      to: "mcp_service OAuth flow Phase 8.3 carry forward"
      via: "wiki.medinet.vn/mcp/auth/v2/authorize endpoint"
      pattern: "Inspector OAuth"

threat_model:
  trust_boundaries:
    - boundary: "mcp_service → python-api-central HTTP (internal docker network)"
      description: "MCP service forward query+JWT tới central; SSRF risk nếu api_base_url mismatch scheme"
    - boundary: "Claude Inspector → mcp_service OAuth public HTTPS"
      description: "Public boundary Phase 8.3 carry forward; CORS + token verify mandatory"
  threats:
    - id: T-07-04-01
      category: Information Disclosure
      component: "api_base_url misconfigured SSRF"
      disposition: mitigate
      mitigation: "field_validator _validate_base_url Phase 8.3 carry forward — scheme http/https + host required (T-08.2-01-T mitigation unchanged)"
    - id: T-07-04-02
      category: Spoofing
      component: "Claude Inspector OAuth token forgery"
      disposition: mitigate
      mitigation: "Phase 8.3 OAuth 2.0 + JWT verify carry forward UNCHANGED + CRIT-01 CORS split metadata vs sensitive (Plan 10-04 v2.0)"
    - id: T-07-04-03
      category: Repudiation
      component: "MCP tool call audit"
      disposition: accept
      mitigation: "M2 audit_logs Phase 5 carry forward — MCP tool call qua central /api/search + /api/ask đã có audit_logs INSERT per request (M2 AUX); KHÔNG cần thêm Phase 7"
    - id: T-07-04-04
      category: Denial of Service
      component: "MCP fan-out N hub (rejected per D-V3-Phase7-C)"
      disposition: mitigate
      mitigation: "D-V3-Phase7-C LOCKED — MCP gọi central aggregate (1 request per tool call), KHÔNG fan-out N hub con. Cross-hub search 1 SQL aggregated Phase 4 Plan 04-05 carry forward."
---

<objective>
Wave 3 của Phase 7 (parallel-able với Plan 07-03 — file-disjoint) — Re-point `mcp_service` từ M2 monolith (`http://localhost:8180`) sang v3.0 central (`http://python-api-central:8080`) per D-V3-Phase7-C LOCKED (central aggregate, KHÔNG fan-out N hub). Tạo runbook manual `06-mcp-smoke.md` 5-step Inspector OAuth verify post-deploy.

**Purpose:** MIGRATE-04 acceptance criteria — MCP tools `search_wiki(hub_id?)` + `ask_wiki(hub_id?)` GIỮ NGUYÊN signature (`hub_id` optional cho cross-hub), OAuth flow Phase 8.3 v2.0 carry forward UNCHANGED, 135/135 mcp_service/tests/ regression PASS mandatory pre-deploy. Cross-hub search 1 SQL aggregated Phase 4 Plan 04-05 carry forward — MCP chỉ cần đổi 1-line default api_base_url.

**Output:** 1-line config.py modify + verify-only docker-compose.yml comment update + 1 runbook markdown ~120 LOC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md
@Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md
@Hub_All/mcp_service/mcp_app/config.py
@Hub_All/docker-compose.yml
@Hub_All/README.md

<interfaces>
<!-- Current state extracted từ config.py + docker-compose.yml -->

**mcp_service/mcp_app/config.py current default (line 34):**
```python
# Base URL của API Service (KHÔNG kèm /api).
api_base_url: str = "http://localhost:8180"
```

**Phase 7 MIGRATE-04 target (1-line change):**
```python
# Base URL của API Service v3.0 — central aggregator (D-V3-02 LOCKED, KHÔNG fan-out N hub).
# D-V3-Phase7-C LOCKED 2026-05-23 — MCP re-point central (carry forward Phase 2 docker-compose env wire).
api_base_url: str = "http://python-api-central:8080"
```

**docker-compose.yml mcp_service env (line 267 — Phase 2 đã set ĐÚNG):**
```yaml
# Phase 2 v3.0 (D-V3-Phase2-C): re-point từ python-api (M2) → python-api-central.
# MCP service gọi central cho cross-hub aggregate (LOCKED D-V3-02 — KHÔNG fan-out
# N hub con). Phase 7 MIGRATE-04 re-confirm contract.
MCP_API_BASE_URL: http://python-api-central:8080
```

Phase 7 update comment "re-confirm" → "CONFIRMED" (Phase 7 verify), KHÔNG đổi env value (đã đúng).

**field_validator _validate_base_url (config.py lines 106-119 — DO NOT MODIFY):**
```python
@field_validator("api_base_url", mode="after")
@classmethod
def _validate_base_url(cls, value: str) -> str:
    stripped = value.rstrip("/")
    parsed = urlparse(stripped)
    if parsed.scheme not in ("http", "https"):
        raise ValueError(...)
    if not parsed.netloc:
        raise ValueError("MCP_API_BASE_URL thiếu host")
    return stripped
```

Validator logic giữ nguyên — SSRF mitigation T-08.2-01-T carry forward.

**MCP regression test:**
```bash
cd Hub_All/mcp_service && uv run pytest -q
# expected: 135 passed (Phase 8.3 v2.0 baseline)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update mcp_service/mcp_app/config.py Settings.api_base_url default + docker-compose.yml comment</name>
  <files>Hub_All/mcp_service/mcp_app/config.py, Hub_All/docker-compose.yml</files>
  <read_first>
    - Hub_All/mcp_service/mcp_app/config.py (toàn bộ — extract default + validator pattern unchanged)
    - Hub_All/docker-compose.yml (lines 252-279 mcp_service block — env wire đã đúng từ Phase 2)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md (§"mcp_service/mcp_app/config.py" + §"docker-compose.yml" pattern)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md (§decisions D-V3-Phase7-C LOCKED MCP re-point)
  </read_first>
  <action>
    **File 1 — `Hub_All/mcp_service/mcp_app/config.py`** (line 33-34 modify):

    Tìm dòng:
    ```python
        # Base URL của API Service (KHÔNG kèm /api).
        api_base_url: str = "http://localhost:8180"
    ```

    Replace bằng:
    ```python
        # Base URL của API Service v3.0 — central aggregator (D-V3-02 LOCKED, KHÔNG fan-out N hub).
        # D-V3-Phase7-C LOCKED 2026-05-23 — MCP re-point central (carry forward Phase 2 docker-compose env wire).
        # MCP tools search_wiki + ask_wiki forward tới central; cross-hub search 1 SQL aggregated
        # (Phase 4 Plan 04-05 D-V3-Phase4-D1). Validator _validate_base_url enforce scheme http/https
        # + host required (T-08.2-01-T SSRF mitigation carry forward Phase 8.3 v2.0).
        api_base_url: str = "http://python-api-central:8080"
    ```

    KHÔNG đụng `field_validator _validate_base_url` (lines 106-119) — giữ nguyên.

    **File 2 — `Hub_All/docker-compose.yml`** (lines 264-267 comment update — env value đã đúng):

    Tìm block:
    ```yaml
        environment:
          # Phase 2 v3.0 (D-V3-Phase2-C): re-point từ python-api (M2) → python-api-central.
          # MCP service gọi central cho cross-hub aggregate (LOCKED D-V3-02 — KHÔNG fan-out
          # N hub con). Phase 7 MIGRATE-04 re-confirm contract.
          MCP_API_BASE_URL: http://python-api-central:8080
    ```

    Replace bằng:
    ```yaml
        environment:
          # Phase 2 v3.0 (D-V3-Phase2-C): re-point từ python-api (M2) → python-api-central.
          # Phase 7 v3.0 MIGRATE-04 (D-V3-Phase7-C LOCKED 2026-05-23): CONFIRMED contract.
          # MCP service gọi central cho cross-hub aggregate (LOCKED D-V3-02 — KHÔNG fan-out
          # N hub con). Cross-hub search 1 SQL aggregated Phase 4 Plan 04-05 carry forward.
          MCP_API_BASE_URL: http://python-api-central:8080
    ```

    KHÔNG đụng env value — env đã đúng từ Phase 2.
  </action>
  <verify>
    <automated>
    grep -c "http://python-api-central:8080" Hub_All/mcp_service/mcp_app/config.py
    grep -c "D-V3-Phase7-C" Hub_All/mcp_service/mcp_app/config.py
    grep -c "Phase 7 v3.0 MIGRATE-04" Hub_All/docker-compose.yml
    grep -c "_validate_base_url" Hub_All/mcp_service/mcp_app/config.py
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'api_base_url: str = "http://python-api-central:8080"' Hub_All/mcp_service/mcp_app/config.py` returns ≥ 1 (default updated)
    - `grep -c "D-V3-Phase7-C" Hub_All/mcp_service/mcp_app/config.py` returns ≥ 1 (decision reference)
    - `grep -c "Phase 4 Plan 04-05" Hub_All/mcp_service/mcp_app/config.py` returns ≥ 1 (carry forward note)
    - `grep -c "_validate_base_url" Hub_All/mcp_service/mcp_app/config.py` returns ≥ 1 (validator unchanged)
    - `grep -c 'api_base_url: str = "http://localhost:8180"' Hub_All/mcp_service/mcp_app/config.py` returns 0 (old default REMOVED)
    - `grep -c "Phase 7 v3.0 MIGRATE-04" Hub_All/docker-compose.yml` returns ≥ 1 (comment update)
    - `grep -c "CONFIRMED contract" Hub_All/docker-compose.yml` returns ≥ 1
    - `grep -c "MCP_API_BASE_URL: http://python-api-central:8080" Hub_All/docker-compose.yml` returns ≥ 1 (env value unchanged)
    - `docker compose config --quiet -f Hub_All/docker-compose.yml` exit 0 (YAML valid) — chạy với SETTINGS_PROXY_SECRET=x*32 HUB_*_ID set env (xem 07-CONTEXT prereq) hoặc skip nếu mock env không có
  </acceptance_criteria>
  <done>config.py default api_base_url đổi sang python-api-central:8080 + 4-line comment Phase 7 decision reference + validator unchanged. docker-compose.yml comment updated 're-confirm' → 'CONFIRMED Phase 7 MIGRATE-04' + env value unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo scripts/migrate/06-mcp-smoke.md runbook Inspector OAuth 5-step</name>
  <files>Hub_All/scripts/migrate/06-mcp-smoke.md</files>
  <read_first>
    - Hub_All/README.md (§"v3.0 Auth SSO deployment notes" — analog runbook structure)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-PATTERNS.md (§"scripts/migrate/06-mcp-smoke.md" 5-step template)
    - Hub_All/.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md (§domain item 6 MCP smoke Inspector runbook scope)
  </read_first>
  <action>
    Tạo file `Hub_All/scripts/migrate/06-mcp-smoke.md`:

    ```markdown
    # MCP Smoke Runbook (Phase 7 v3.0 MIGRATE-04)

    **Created:** 2026-05-23 (Phase 7 Plan 07-04)
    **Decision:** D-V3-Phase7-C LOCKED — MCP re-point central aggregate (KHÔNG fan-out N hub).
    **Carry forward:** Phase 8.3 v2.0 OAuth flow UNCHANGED + 135/135 mcp_service/tests/ regression baseline.

    Manual checklist 5-step verify MCP service hoạt động sau Phase 7 re-point `api_base_url = http://python-api-central:8080`. Operator chạy sau khi `docker compose up -d mcp_service`.

    ## Pre-deploy verify (mandatory)

    Trước khi smoke, verify regression baseline:

    ```bash
    cd Hub_All/mcp_service
    uv run pytest -q
    # Expected: 135 passed (Phase 8.3 v2.0 baseline)
    # Nếu FAIL → block deploy, fix trước.
    ```

    Verify container running:

    ```bash
    cd Hub_All
    docker compose ps mcp_service
    # Expected: State=running

    docker compose logs --tail=50 mcp_service
    # Expected: KHÔNG có boot error; thấy "Application startup complete"
    ```

    Verify env wire central re-point:

    ```bash
    docker compose exec mcp_service env | grep MCP_API_BASE_URL
    # Expected: MCP_API_BASE_URL=http://python-api-central:8080
    ```

    ## Smoke checklist 5-step

    ### Step 1 — Connect Claude Inspector via OAuth

    Browser tới Inspector URL (https://inspector.modelcontextprotocol.io HOẶC `http://localhost:6274` dev):

    1. Inspector "Add Server" → MCP URL: `https://wiki.medinet.vn/mcp/` (prod) HOẶC `http://localhost:8190/` (dev native).
    2. Click "Authorize" → Inspector redirect tới `wiki.medinet.vn/mcp/auth/v2/authorize` (OAuth flow Phase 8.3).
    3. Verify discovery metadata response 200:
       ```bash
       curl -k https://wiki.medinet.vn/.well-known/oauth-authorization-server | jq '.'
       # Expected: {issuer, authorization_endpoint, token_endpoint, registration_endpoint, ...}
       ```
    4. Complete OAuth dance: enter test credentials (admin@medinet.vn / dev password) → consent screen → redirect Inspector với code → token exchange.
    5. Inspector connection state: **Connected** (green indicator).

    **FAIL signal:** Discovery 404 / token exchange 401 / CORS preflight reject → check Caddy reverse proxy `/.well-known/*` + `/mcp/*` route (Phase 5 PROXY-01 carry forward).

    ### Step 2 — Call `search_wiki(query="vaccin", hub_id="yte")` single-hub

    Inspector tools tab → invoke `search_wiki`:

    ```json
    {
      "query": "vaccin",
      "hub_id": "yte"
    }
    ```

    Expected response envelope D6:
    ```json
    {
      "success": true,
      "data": {
        "results": [
          {
            "chunk_id": "...",
            "content": "Vaccin (vaccine) là chế phẩm sinh học...",
            "score": 0.85,
            "hub_id": "yte",
            "document_id": "..."
          }
        ]
      },
      "error": null,
      "meta": {"hub_id": "yte"}
    }
    ```

    **Verify:** Tất cả chunks chứa `hub_id="yte"` only (KHÔNG cross-hub leak).

    **FAIL signal:** 403 CROSS_HUB_ACCESS_DENIED → JWT user thiếu `hub_ids=["yte"]` claim (Phase 3 Plan 03-03 hub_ids REQUIRED). 500 → central upstream down hoặc DB connection fail.

    ### Step 3 — Call `ask_wiki(query="dược ngoại")` cross-hub no hub_id

    Inspector tools tab → invoke `ask_wiki`:

    ```json
    {
      "query": "dược ngoại"
    }
    ```

    Expected response:
    ```json
    {
      "success": true,
      "data": {
        "answer": "Theo tài liệu [1], dược ngoại là... [2] đề cập...",
        "citations": [
          {"id": 1, "chunk_id": "...", "hub_id": "duoc", "content": "...", "document_id": "..."},
          {"id": 2, "chunk_id": "...", "hub_id": "yte", "content": "..."}
        ]
      }
    }
    ```

    **Verify:**
    - Cross-hub aggregation: citations từ MULTIPLE hub_id (intersection user.hub_ids ∩ all hubs = central aggregate Phase 4 Plan 04-05).
    - Citation `[N]` xuất hiện trong `data.answer` text (M2 LLM citation Phase 7 v2.0 carry forward).
    - Latency p95 < 1.5s (E-V3-2 — đo qua Inspector network tab).

    **FAIL signal:** citations chỉ 1 hub → cross-hub search aggregate fail (Phase 4 Plan 04-05 D-V3-Phase4-D1 broke). Empty answer → LLM provider down hoặc rag_config sai (Phase 6 SETTINGS-01 cache stale).

    ### Step 4 — Verify citation `[N]` resolve

    Click `[1]` link trong Inspector response → expect popup hoặc detail view document:

    1. Document title displayed.
    2. Document hub_id match citation `hub_id` field.
    3. Chunk content highlight trong document body (carry forward M2 Phase 7 v2.0).

    **Verify:** `GET /api/documents/<id>` envelope D6 success + data.id match citation chunk_id parent document.

    **FAIL signal:** 404 document → chunk_id orphan (sync drift R-V3-1 — chạy `POST /api/sync/replay` Phase 4 Plan 04-06 recover). 403 → hub access enforce (Phase 3 SSO-04 E4 3-layer).

    ### Step 5 — Verify Prometheus metrics post-smoke

    ```bash
    curl http://localhost:8180/metrics | grep -E "apikey_verify_total|cross_hub_search_latency"
    # Expected:
    #   apikey_verify_total{hub_name="central",result="cached"} > 0  (Phase 6 SETTINGS-03 cache hit)
    #   cross_hub_search_latency_seconds histogram populated (Phase 4 Plan 04-05)

    curl http://localhost:8180/metrics | grep -E "sync_lag_seconds|sync_count_drift"
    # Expected:
    #   sync_lag_seconds{hub_name=...} < 30 (E-V3-4)
    #   sync_count_drift{hub_name=...} < 0.01 (< 1% drift, R-V3-1)
    ```

    **FAIL signal:** sync_lag > 30s → hub con worker stale (Phase 4 Plan 04-04 lifespan). count_drift > 1% → checksum scheduler alert (Phase 4 Plan 04-06).

    ## Rollback procedure

    Nếu Step 1-5 FAIL critical (vd: OAuth flow break / fan-out N hub instead aggregate):

    ```bash
    # 1. Stop mcp_service
    docker compose stop mcp_service

    # 2. Revert config.py + docker-compose.yml comment changes
    git revert <commit-sha-phase7-mcp-repoint>

    # 3. Re-build + restart
    docker compose up -d --build mcp_service

    # 4. Re-run regression
    cd mcp_service && uv run pytest -q
    # Expected: 135 passed

    # 5. Manual verify Inspector connect lại
    ```

    M2 monolith `http://localhost:8180` legacy default reachable (port 8180 = python-api-central host map, container alias `python-api-central:8080`).

    ## Reference

    - `Hub_All/mcp_service/mcp_app/config.py` — Settings.api_base_url default re-point central.
    - `Hub_All/docker-compose.yml` lines 264-271 — env wire MCP_API_BASE_URL.
    - `.planning/phases/07-migration-smoke-e2e/07-CONTEXT.md` §decisions D-V3-Phase7-C LOCKED.
    - `.planning/phases/07-migration-smoke-e2e/07-04-PLAN.md` — Plan này.
    - Phase 8.3 v2.0 archive (`.planning/milestones/v2.0-archive/`) — OAuth 9-plan ship carry forward UNCHANGED.
    - Phase 4 Plan 04-05 — Cross-hub search 1 SQL aggregated D-V3-Phase4-D1 LOCKED.
    ```
  </action>
  <verify>
    <automated>test -f Hub_All/scripts/migrate/06-mcp-smoke.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f Hub_All/scripts/migrate/06-mcp-smoke.md` exit 0
    - `grep -c "Step 1\|Step 2\|Step 3\|Step 4\|Step 5" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 5
    - `grep -c "Inspector" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 3 (OAuth flow + tools + browser)
    - `grep -c "search_wiki" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1
    - `grep -c "ask_wiki" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1
    - `grep -c "135 passed" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1 (regression mandatory)
    - `grep -c "D-V3-Phase7-C" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1
    - `grep -c "MCP_API_BASE_URL" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1
    - `grep -c "Rollback" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1 (rollback procedure section)
    - `grep -c "cross_hub_search_latency\|apikey_verify_total\|sync_lag_seconds" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 2 (Prometheus assertion)
    - `grep -c "envelope D6\|envelope" Hub_All/scripts/migrate/06-mcp-smoke.md` returns ≥ 1
  </acceptance_criteria>
  <done>06-mcp-smoke.md có 5-step manual checklist (Inspector OAuth + search_wiki single-hub + ask_wiki cross-hub + citation resolve + Prometheus assert) + pre-deploy regression mandatory + rollback procedure + reference links.</done>
</task>

</tasks>

<verification>
- config.py default api_base_url đổi sang python-api-central:8080 + decision comment
- docker-compose.yml comment updated 're-confirm' → 'CONFIRMED'
- _validate_base_url validator unchanged (T-08.2-01-T mitigation carry forward)
- 06-mcp-smoke.md 5-step runbook + pre-deploy 135-test regression + rollback procedure
- 10+ acceptance grep PASS
</verification>

<success_criteria>
- 2 file modify (config.py + docker-compose.yml) — small surface area, 1-line value change + comment update
- 1 file mới (06-mcp-smoke.md runbook ~150 LOC)
- MIGRATE-04 acceptance criteria satisfied (mcp_service re-point central + 135/135 test PASS + smoke Inspector OAuth PASS)
- Phase 8.3 OAuth flow UNCHANGED (carry forward) + cross-hub aggregate 1 SQL Phase 4 carry forward
</success_criteria>

<output>
After completion, create `Hub_All/.planning/phases/07-migration-smoke-e2e/07-04-SUMMARY.md` per template.
</output>
