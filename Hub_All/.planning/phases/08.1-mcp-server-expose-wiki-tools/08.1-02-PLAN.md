---
phase: "08.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "08.1-01"
files_modified:
  - Hub_All/api/app/mcp/server.py
  - Hub_All/api/app/main.py
autonomous: true
requirements:
  - MCP-01
  - MCP-02

must_haves:
  truths:
    - "FastMCP instance 'mcp' tồn tại trong server.py với 3 tool đăng ký"
    - "search_wiki: nhận query + hub_id? + top_k? → route sang SearchService.search hoặc search_cross_hub"
    - "ask_wiki: nhận query + hub_id? → route sang AskService.ask hoặc ask_cross_hub + log usage_events non-blocking"
    - "list_hubs: trả danh sách hub user được phép (admin = tất cả active, viewer/editor = user_hubs)"
    - "set_pool()/  _get_pool() singleton module-level để tool handler truy cập db_pool không qua FastAPI Depends"
    - "main.py dùng combine_lifespans(lifespan, _mcp_app.lifespan) — KHÔNG chỉ app.mount() đơn thuần"
    - "MCP ASGI sub-app mount tại /mcp với mcp.http_app(path='/', stateless_http=True)"
    - "set_pool() được gọi trong lifespan sau khi app.state.db_pool được gán"
    - "Endpoint /mcp phản hồi HTTP 200 với valid JSON-RPC response cho tools/list"
  artifacts:
    - path: "Hub_All/api/app/mcp/server.py"
      provides: "FastMCP instance + 3 tool handler + module-level singleton pool"
      exports:
        - mcp
        - set_pool
    - path: "Hub_All/api/app/main.py"
      provides: "combine_lifespans + app.mount('/mcp', _mcp_app)"
      contains: "combine_lifespans"
  key_links:
    - from: "Hub_All/api/app/mcp/server.py"
      to: "Hub_All/api/app/services/ask_service.AskService"
      via: "AskService(pool=pool, redis=_get_redis())"
      pattern: "AskService\\(pool=_get_pool\\(\\)"
    - from: "Hub_All/api/app/mcp/server.py"
      to: "Hub_All/api/app/services/usage_service.log_usage_event"
      via: "asyncio.create_task(log_usage_event(...))"
      pattern: "asyncio\\.create_task.*log_usage_event"
    - from: "Hub_All/api/app/main.py"
      to: "Hub_All/api/app/mcp/server.set_pool"
      via: "mcp_set_pool(app.state.db_pool, app.state.redis)"
      pattern: "mcp_set_pool"
    - from: "Hub_All/api/app/main.py"
      to: "fastmcp.utilities.lifespan.combine_lifespans"
      via: "combine_lifespans(lifespan, _mcp_app.lifespan)"
      pattern: "combine_lifespans"
---

<objective>
Định nghĩa 3 tool MCP (search_wiki, ask_wiki, list_hubs) trong server.py và wire
MCP sub-app vào FastAPI main.py với lifespan composition đúng.

Purpose: Đây là core implementation của Phase 8.1 — sau plan này, AI client
(Claude Code, Cursor) có thể kết nối tới /mcp và gọi tool. Landmine lifespan
(combine_lifespans) PHẢI được xử lý đúng — sai sẽ gây runtime error
"Task group is not initialized" khi client gọi.

Output: app/mcp/server.py + main.py modified
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-CONTEXT.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-01-SUMMARY.md

<interfaces>
<!-- Contracts từ Plan 01 và codebase hiện hữu -->

Từ Hub_All/api/app/mcp/schemas.py (tạo ở Plan 01):
```python
class CitationItem(BaseModel): chunk_id, document_name, hub_name, snippet, score
class AskAnswer(BaseModel): answer: str, citations: list[CitationItem]
class SearchResultItem(BaseModel): chunk_id, content, score, document_name, hub_name, hub_id
class SearchResult(BaseModel): results: list[SearchResultItem], total: int
class HubItem(BaseModel): id, name, description
class HubList(BaseModel): hubs: list[HubItem]
```

Từ Hub_All/api/app/mcp/auth.py (tạo ở Plan 01):
```python
async def authenticate_mcp_request(pool: asyncpg.Pool) -> UserWithHubs: ...
# Raise ValueError nếu auth fail → caller map sang ToolError
```

Từ Hub_All/api/app/services/ask_service.py:
```python
class AskService:
    def __init__(self, *, pool: Any, redis: Any = None) -> None: ...
    async def ask(self, *, body: AskRequest, user: Any) -> AskOutcome: ...
    async def ask_cross_hub(self, *, body: AskRequest, user: Any) -> AskOutcome: ...

class AskOutcome:
    response: AskResponse  # response.answer, response.citations (list[Citation])
    usage: UsageRecord     # usage.model, usage.prompt_tokens, usage.completion_tokens, usage.total_tokens, usage.cost_usd
    chunks: list[_AskChunk]

# Citation có các field: chunk_id, document_name, hub_name, content_snippet, score
```

Từ Hub_All/api/app/services/search_service.py:
```python
class SearchService:
    def __init__(self, *, pool: Any, redis: Any = None) -> None: ...
    async def search(self, *, body: SearchRequest, user: Any) -> dict: ...
    async def search_cross_hub(self, *, body: SearchRequest, user: Any) -> dict: ...
# Cả hai trả dict với key "results": list[dict]
# Mỗi result dict: {"id": str, "hub_id": str, "title": str, "hub_name": str, "snippet": str, "content": str|None, "score": float}
```

Từ Hub_All/api/app/schemas/search.py:
```python
class SearchRequest(BaseModel):
    query: str
    hub_ids: list[str] | None = None
    top_k: int = 10
```

Từ Hub_All/api/app/schemas/ask.py:
```python
class AskRequest(BaseModel):
    query: str
    hub_id: str | None = None
    hub_ids: list[str] | None = None
    top_k: int | None = None
```

Từ Hub_All/api/app/services/usage_service.py:
```python
# Signature log_usage_event:
async def log_usage_event(
    pool: asyncpg.Pool,
    user_id: str | None,
    hub_id: str | None,
    model: str,
    prompt_tokens: int | None,
    completion_tokens: int | None,
    total_tokens: int | None,
    cost_usd: float | None,
    request_id: str | None,
) -> None: ...
```

Từ Hub_All/api/app/repositories/hub_isolation.py:
```python
class HubIsolationError(Exception): ...
```

Từ Hub_All/api/app/main.py (create_app hiện tại — lines 316–323):
```python
def create_app() -> FastAPI:
    settings = get_settings()
    app = FastAPI(
        title="Medinet Wiki API",
        version="0.1.0",
        lifespan=lifespan,  # ← THAY bằng combine_lifespans(lifespan, _mcp_app.lifespan)
    )
    # ...
    # Cuối create_app, sau tất cả include_router, THÊM:
    # app.mount("/mcp", _mcp_app)
```

Từ Hub_All/api/app/auth/dependencies.py:
```python
class UserWithHubs:
    user: User   # user.id, user.role (str "admin"|"editor"|"viewer")
    hub_ids: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo mcp/server.py — FastMCP instance + module-level pool singleton + 3 tool</name>
  <files>Hub_All/api/app/mcp/server.py</files>
  <read_first>
    - Hub_All/api/app/routers/ask.py — đọc toàn bộ (analog exact): cấu trúc pool guard, service call, error handling, usage log non-blocking (lines 39–180)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md — Pattern 4 (module singleton pool), Pattern 5 (ask_wiki complete), Ví dụ 1 (list_hubs), Ví dụ 2 (search_wiki)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md — section server.py (mcp tool structure)
    - Hub_All/api/app/mcp/auth.py — xem signature authenticate_mcp_request (Plan 01)
    - Hub_All/api/app/mcp/schemas.py — xem tất cả output models (Plan 01)
    - Hub_All/api/app/services/search_service.py — đọc search() và search_cross_hub() để biết exact field tên trong result dict (id/hub_id/title/hub_name/snippet/content/score)
    - Hub_All/api/app/services/ask_service.py — đọc AskOutcome và cách truy cập response.citations[N].document_name v.v.
  </read_first>
  <behavior>
    - Module-level singleton: _pool, _redis; set_pool(pool, redis); _get_pool() guard None
    - FastMCP instance: mcp = FastMCP("Medinet Wiki MCP Server")
    - tool list_hubs(): auth → query hubs từ asyncpg pool; admin = tất cả active; khác = user_hubs join; trả HubList
    - tool search_wiki(query, hub_id=None, top_k=10): auth → clamp top_k [1,20]; hub_id có → SearchService.search; không có → search_cross_hub; map result dict sang SearchResultItem; trả SearchResult
    - tool ask_wiki(query, hub_id=None): auth → hub_id có → AskService.ask; không có → ask_cross_hub với hub_ids=None; asyncio.create_task(log_usage_event(...)) non-blocking; map citations sang CitationItem; trả AskAnswer
    - Mọi tool: ValueError từ authenticate → ToolError; HubIsolationError → ToolError "HUB_ACCESS_DENIED"; AskError → ToolError "LLM lỗi"; Exception → ToolError "Lỗi nội bộ" (KHÔNG leak stack trace)
    - top_k clamp: max(1, min(top_k, 20)) trong search_wiki
    - Verify import path FastMCP TRƯỚC KHI viết: thử "from mcp.fastmcp import FastMCP" nếu fail thử "from fastmcp import FastMCP" (RESEARCH.md Open Question 1); dùng path nào thành công
  </behavior>
  <action>
Tạo Hub_All/api/app/mcp/server.py. Analog: app/routers/ask.py (cấu trúc tổng thể) + RESEARCH.md Pattern 4+5.

BƯỚC 0 — Xác nhận import path FastMCP:
```bash
cd Hub_All/api && uv pip install "mcp>=1.9.4,<2" --quiet 2>/dev/null || true
python -c "from mcp.fastmcp import FastMCP; print('mcp.fastmcp OK')" 2>/dev/null || \
python -c "from fastmcp import FastMCP; print('fastmcp OK')"
```
Dùng path nào thành công. Ghi lại trong comment đầu file và trong SUMMARY.md sau.

Cấu trúc file (viết đầy đủ — không placeholder):

```python
"""MCP server — Phase 8.1 (MCP-01, MCP-02).

FastMCP Streamable HTTP server expose 3 tool read-only cho AI client:
- list_hubs   — danh sách Hub user được phép (D-07)
- search_wiki — vector semantic search (D-07, D-08)
- ask_wiki    — RAG ask + citation + usage log (D-07, D-08, D-11, D-14)

Transport: Streamable HTTP, stateless_http=True (D-01)
Mount: /mcp trong FastAPI app chính (D-02)
Auth: X-API-Key qua get_http_headers → authenticate_mcp_request (D-05/D-06)
Service: gọi trực tiếp SearchService/AskService — KHÔNG self-call HTTP (D-04)
Pool: module-level singleton set_pool/  _get_pool (RESEARCH.md Pattern 4)
"""
from __future__ import annotations

import asyncio
import logging
from typing import Any

import asyncpg
# BƯỚC 0: xác nhận import path sau khi install mcp>=1.9.4
from mcp.fastmcp import FastMCP  # hoặc: from fastmcp import FastMCP
from fastmcp.exceptions import ToolError

from app.mcp.auth import authenticate_mcp_request
from app.mcp.schemas import (
    AskAnswer,
    CitationItem,
    HubItem,
    HubList,
    SearchResult,
    SearchResultItem,
)
from app.repositories.hub_isolation import HubIsolationError
from app.services.ask_service import AskError, AskService
from app.services.usage_service import log_usage_event

logger = logging.getLogger(__name__)

# ---------------------------------------------------------------------------
# Module-level pool singleton (RESEARCH.md Pattern 4)
# Không có FastAPI Request trong MCP tool → không dùng request.app.state
# ---------------------------------------------------------------------------
_pool: asyncpg.Pool | None = None
_redis: Any = None


def set_pool(pool: asyncpg.Pool, redis: Any = None) -> None:
    """Gọi từ lifespan FastAPI sau khi app.state.db_pool init.

    PHẢI gọi trước bất kỳ tool call nào — nếu quên → _get_pool() raise RuntimeError.
    """
    global _pool, _redis
    _pool = pool
    _redis = redis
    logger.info("mcp_pool_set")


def _get_pool() -> asyncpg.Pool:
    """Guard pool None → RuntimeError (tương tự ask.py line 74)."""
    if _pool is None:
        raise RuntimeError("db_pool chưa sẵn sàng — MCP không khả dụng")
    return _pool


def _get_redis() -> Any:
    """Trả redis client (có thể None — SearchService/AskService xử lý redis=None)."""
    return _redis


# ---------------------------------------------------------------------------
# FastMCP instance
# ---------------------------------------------------------------------------
mcp = FastMCP("Medinet Wiki MCP Server")


# ---------------------------------------------------------------------------
# Tool: list_hubs
# ---------------------------------------------------------------------------
@mcp.tool
async def list_hubs() -> HubList:
    """Liệt kê các Hub Medinet Wiki mà API key có quyền truy cập.

    Dùng hub_id trả về để filter trong search_wiki / ask_wiki.

    Returns:
        hubs: Danh sách Hub (id, name, description) mà user của API key được phép.
    """
    pool = _get_pool()
    try:
        user = await authenticate_mcp_request(pool)
    except ValueError as e:
        raise ToolError(str(e))

    async with pool.acquire() as conn:
        if user.user.role == "admin":
            rows = await conn.fetch(
                "SELECT id, name, description FROM hubs WHERE status = 'active' ORDER BY name"
            )
        else:
            rows = await conn.fetch(
                "SELECT h.id, h.name, h.description "
                "FROM hubs h "
                "JOIN user_hubs uh ON h.id = uh.hub_id "
                "WHERE uh.user_id = $1 AND h.status = 'active' "
                "ORDER BY h.name",
                user.user.id,
            )

    return HubList(
        hubs=[
            HubItem(id=str(r["id"]), name=r["name"], description=r["description"])
            for r in rows
        ]
    )


# ---------------------------------------------------------------------------
# Tool: search_wiki
# ---------------------------------------------------------------------------
@mcp.tool
async def search_wiki(
    query: str,
    hub_id: str | None = None,
    top_k: int = 10,
) -> SearchResult:
    """Tìm kiếm tri thức Medinet Wiki bằng vector semantic search.

    Args:
        query: Câu truy vấn tìm kiếm (tiếng Việt hoặc tiếng Anh).
        hub_id: ID Hub cụ thể (tuỳ chọn). Để trống = cross-hub tất cả Hub được phép.
        top_k: Số chunk trả về (mặc định 10, tối đa 20).

    Returns:
        results: Danh sách chunk phù hợp (content, score, document_name, hub_name).
        total: Tổng số kết quả trả về.
    """
    pool = _get_pool()
    try:
        user = await authenticate_mcp_request(pool)
    except ValueError as e:
        raise ToolError(str(e))

    from app.schemas.search import SearchRequest
    from app.services.search_service import SearchService

    svc = SearchService(pool=pool, redis=_get_redis())
    top_k = max(1, min(top_k, 20))  # clamp [1, 20]

    try:
        if hub_id:
            # Single-hub: truyền hub_ids=[hub_id] → SearchService.search enforce isolation
            result = await svc.search(
                body=SearchRequest(query=query, hub_ids=[hub_id], top_k=top_k),
                user=user,
            )
        else:
            # Cross-hub: hub_ids=None → search mọi hub user được phép (D-08)
            result = await svc.search_cross_hub(
                body=SearchRequest(query=query, hub_ids=None, top_k=top_k),
                user=user,
            )
    except HubIsolationError as e:
        raise ToolError(f"HUB_ACCESS_DENIED: {e}")
    except Exception as e:  # noqa: BLE001
        logger.error("search_wiki_error: %s", e)
        raise ToolError(f"Search lỗi: {e}")

    items = [
        SearchResultItem(
            chunk_id=str(r["id"]),
            content=r.get("snippet") or r.get("content") or "",
            score=float(r["score"]),
            document_name=r.get("title") or "",
            hub_name=r.get("hub_name") or "",
            hub_id=str(r["hub_id"]),
        )
        for r in result.get("results", [])
    ]
    return SearchResult(results=items, total=len(items))


# ---------------------------------------------------------------------------
# Tool: ask_wiki
# ---------------------------------------------------------------------------
@mcp.tool
async def ask_wiki(
    query: str,
    hub_id: str | None = None,
) -> AskAnswer:
    """Hỏi-đáp RAG từ Medinet Wiki. Trả câu trả lời + danh sách trích dẫn.

    Args:
        query: Câu hỏi bằng tiếng Việt hoặc tiếng Anh.
        hub_id: ID Hub cụ thể (tuỳ chọn). Để trống = hỏi cross-hub mọi Hub được phép.

    Returns:
        answer: Câu trả lời với marker [1], [2]... chỉ nguồn trích dẫn.
        citations: Danh sách nguồn tương ứng với mỗi marker [N].
    """
    pool = _get_pool()
    try:
        user = await authenticate_mcp_request(pool)
    except ValueError as e:
        raise ToolError(str(e))

    from app.schemas.ask import AskRequest
    from app.services.ask_service import AskService

    ask_svc = AskService(pool=pool, redis=_get_redis())
    body = AskRequest(query=query, hub_id=hub_id)
    cross_hub = hub_id is None

    try:
        if cross_hub:
            # Cross-hub: hub_ids=None → AskService dùng tất cả hub user được phép
            outcome = await ask_svc.ask_cross_hub(body=body, user=user)
        else:
            # Single-hub: hub_id đã set trong body
            outcome = await ask_svc.ask(body=body, user=user)
    except AskError as e:
        raise ToolError(f"LLM lỗi: {e}")
    except HubIsolationError as e:
        raise ToolError(f"HUB_ACCESS_DENIED: {e}")
    except Exception as e:  # noqa: BLE001
        logger.error("ask_wiki_error: %s", e)
        raise ToolError(f"Lỗi nội bộ: {e}")

    # D-14: log usage_events non-blocking
    # asyncio.create_task (không có BackgroundTasks trong MCP tool — tham chiếu
    # ask.py dùng background_tasks.add_task; MCP dùng asyncio.create_task tương đương)
    asyncio.create_task(  # noqa: RUF006
        log_usage_event(
            pool,
            user_id=str(user.user.id),
            hub_id=hub_id,
            model=outcome.usage.model,
            prompt_tokens=outcome.usage.prompt_tokens,
            completion_tokens=outcome.usage.completion_tokens,
            total_tokens=outcome.usage.total_tokens,
            cost_usd=outcome.usage.cost_usd,
            request_id=None,
        )
    )

    return AskAnswer(
        answer=outcome.response.answer,
        citations=[
            CitationItem(
                chunk_id=c.chunk_id,
                document_name=c.document_name,
                hub_name=c.hub_name,
                snippet=c.content_snippet,
                score=c.score,
            )
            for c in outcome.response.citations
        ],
    )
```

LƯU Ý: Nếu import path "from mcp.fastmcp import FastMCP" không hoạt động sau khi
install mcp==1.9.4, thử "from fastmcp import FastMCP". Ghi lại path thực tế vào
SUMMARY.md. Tương tự cho "from fastmcp.exceptions import ToolError" và
"from fastmcp.server.dependencies import get_http_headers" (trong auth.py).
  </action>
  <acceptance_criteria>
    - grep "set_pool" Hub_All/api/app/mcp/server.py → xuất hiện (function định nghĩa)
    - grep "_get_pool" Hub_All/api/app/mcp/server.py → xuất hiện (guard None)
    - grep "@mcp.tool" Hub_All/api/app/mcp/server.py | wc -l → 3 (3 tool đăng ký)
    - grep "asyncio.create_task" Hub_All/api/app/mcp/server.py → xuất hiện trong ask_wiki (D-14 non-blocking)
    - grep "HUB_ACCESS_DENIED" Hub_All/api/app/mcp/server.py → xuất hiện (D-13 isolation error)
    - python -c "from app.mcp.server import mcp, set_pool; print('server OK')" từ Hub_All/api/
  </acceptance_criteria>
  <verify>
    <automated>cd Hub_All/api && grep -c "@mcp.tool" app/mcp/server.py && grep "asyncio.create_task" app/mcp/server.py && python -c "from app.mcp.server import mcp, set_pool; print('server 3 tools OK')"</automated>
  </verify>
  <done>
    - server.py import được; mcp instance + set_pool export được
    - 3 tool: list_hubs, search_wiki, ask_wiki đăng ký qua @mcp.tool
    - Pool singleton set_pool/_get_pool có mặt
    - ask_wiki có asyncio.create_task(log_usage_event(...)) (D-14)
    - Mọi tool có try/except ValueError → ToolError cho auth fail
    - HubIsolationError → ToolError "HUB_ACCESS_DENIED" trong search_wiki + ask_wiki
  </done>
</task>

<task type="auto">
  <name>Task 2: Sửa main.py — combine_lifespans + set_pool + app.mount('/mcp')</name>
  <files>Hub_All/api/app/main.py</files>
  <read_first>
    - Hub_All/api/app/main.py — đọc toàn bộ: xác định CHÍNH XÁC dòng create_app() (line ~316), FastAPI(..., lifespan=lifespan) (line ~319), lifespan startup bước 1 (asyncpg pool init lines 71–78), vị trí sau tất cả include_router để thêm app.mount
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md — Pattern 1 (lifespan composition, CRITICAL), Pattern 4 (set_pool call từ lifespan), Cạm bẫy 1 (thiếu combine_lifespans → runtime error), Cạm bẫy 2 (double path prefix), Cạm bẫy 4 (circular import)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md — section main.py (self-modify pattern, lines thêm vào)
  </read_first>
  <action>
Sửa Hub_All/api/app/main.py theo 3 bước sau. ĐỌC file trước, sau đó Edit đúng vị trí.

BƯỚC A — Thêm import ở TOP của file (sau các import hiện hữu, trước def _to_asyncpg_dsn):
```python
# Phase 8.1 MCP — import trước create_app để tránh circular import
from app.mcp.server import set_pool as mcp_set_pool
```

BƯỚC B — Thêm mcp_set_pool call vào TRONG lifespan(), ngay sau bước 1 (db_pool init, sau line
"app.state.db_ready = True"):
```python
    # Phase 8.1 MCP — inject pool vào module singleton (Pattern 4)
    if app.state.db_pool is not None:
        mcp_set_pool(app.state.db_pool, app.state.redis)
        logger.info("mcp_pool_injected")
```
Lưu ý: app.state.redis có thể chưa init ở thời điểm này (bước 2 chưa chạy) →
để redis=None là OK vì bước 2 (redis init) chạy sau; SearchService/AskService
xử lý redis=None. Hoặc thêm sau bước 2 redis init để inject cả redis.

THỰC TẾ nên đặt SAU bước 2 (redis init), trước bước 2.5 (rag_config), để inject cả redis:
```python
    # Phase 8.1 MCP — inject pool + redis vào module singleton sau bước 2
    if app.state.db_pool is not None:
        mcp_set_pool(app.state.db_pool, app.state.redis)
        logger.info("mcp_pool_injected")
```

BƯỚC C — Sửa create_app() tại FastAPI(...) constructor:

TRƯỚC (khoảng line 319):
```python
    app = FastAPI(
        title="Medinet Wiki API",
        version="0.1.0",
        lifespan=lifespan,
    )
```

SAU — thêm 4 dòng TRƯỚC FastAPI(...) + sửa lifespan argument:
```python
    # Phase 8.1 MCP — tạo ASGI sub-app trước FastAPI init (thứ tự khai báo quan trọng)
    # CRITICAL: path="/" KHÔNG phải path="/mcp" — tránh double-prefix (Cạm bẫy 2)
    # stateless_http=True — mỗi request fresh context, header X-API-Key luôn đúng (Cạm bẫy 3)
    from fastmcp.utilities.lifespan import combine_lifespans  # noqa: PLC0415
    from app.mcp.server import mcp as _mcp_server  # noqa: PLC0415

    _mcp_app = _mcp_server.http_app(path="/", stateless_http=True)

    app = FastAPI(
        title="Medinet Wiki API",
        version="0.1.0",
        # CRITICAL: combine_lifespans BẮT BUỘC — chỉ app.mount() sẽ gây
        # "Task group is not initialized" khi client gọi (Cạm bẫy 1 / RESEARCH.md Pattern 1)
        lifespan=combine_lifespans(lifespan, _mcp_app.lifespan),
    )
```

BƯỚC D — Mount MCP ASGI sub-app ở CUỐI create_app(), sau tất cả include_router + exception_handler
(sau ai_chat_router và slowapi wiring, trước return app nếu có):
```python
    # Phase 8.1 MCP — mount ASGI sub-app tại /mcp (D-02)
    app.mount("/mcp", _mcp_app)
```

SAU KHI SỬA — verify không có import ở top-level của module làm circular:
- Đảm bảo "from app.mcp.server import mcp as _mcp_server" NẰM TRONG create_app() (local import),
  KHÔNG ở top-level module — tránh circular import khi server.py import từ app modules
- "from app.mcp.server import set_pool as mcp_set_pool" OK ở top-level (chỉ import hàm, không gọi)

Nếu sợ circular import, move import "from app.mcp.server import set_pool as mcp_set_pool" vào
trong lifespan() thay vì top-level.
  </action>
  <acceptance_criteria>
    - grep "combine_lifespans" Hub_All/api/app/main.py → xuất hiện
    - grep "mcp_set_pool" Hub_All/api/app/main.py → xuất hiện ít nhất 1 lần
    - grep 'app.mount.*"/mcp"' Hub_All/api/app/main.py → xuất hiện
    - grep 'stateless_http=True' Hub_All/api/app/main.py → xuất hiện
    - python -c "from app.main import create_app; app = create_app(); print('create_app OK')" từ Hub_All/api/ (không exception)
    - grep '"lifespan=lifespan"' Hub_All/api/app/main.py → KHÔNG xuất hiện (phải đã thay bằng combine_lifespans)
  </acceptance_criteria>
  <verify>
    <automated>cd Hub_All/api && grep "combine_lifespans" app/main.py && grep "mcp_set_pool" app/main.py && grep 'app.mount.*"/mcp"' app/main.py && COCOINDEX_SKIP_SETUP=1 python -c "from app.main import create_app; app = create_app(); print('create_app with MCP OK')"</automated>
  </verify>
  <done>
    - main.py có combine_lifespans(lifespan, _mcp_app.lifespan) thay vì lifespan=lifespan
    - main.py có mcp_set_pool(app.state.db_pool, app.state.redis) trong lifespan sau bước redis init
    - main.py có app.mount("/mcp", _mcp_app) ở cuối create_app()
    - COCOINDEX_SKIP_SETUP=1 python -c "from app.main import create_app; app = create_app()" — không exception
    - Tất cả router hiện tại vẫn mount (KHÔNG xóa bất kỳ include_router nào)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Ranh giới | Mô tả |
|-----------|-------|
| AI Client → HTTP POST /mcp | MCP JSON-RPC request từ external client; header X-API-Key đi qua đây |
| MCP tool → SearchService/AskService | Service layer nhận UserWithHubs — hub isolation enforce ở đây |
| MCP tool → usage_events | asyncio.create_task write — non-blocking, best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-02-S | Spoofing | Mỗi tool handler — auth check đầu tool | mitigate | authenticate_mcp_request() gọi đầu MỖI tool; KHÔNG trust tool params; KHÔNG cache user giữa request (stateless_http=True) |
| T-08.1-02-E | Elevation of Privilege | search_wiki hub_id param | mitigate | D-13: hub_id ngoài phạm vi → HubIsolationError từ SearchService → ToolError "HUB_ACCESS_DENIED"; KHÔNG để client escalate qua param |
| T-08.1-02-E2 | Elevation of Privilege | list_hubs admin check | mitigate | admin query tất cả hub; non-admin query user_hubs join — KHÔNG tin role từ client param |
| T-08.1-02-P | Prompt Injection | ask_wiki query param | mitigate | anti-injection system prompt đã có ở AskService (Phase 7 ASK-02); MCP tool không thêm logic mới — tái dùng service layer |
| T-08.1-02-D | Denial of Service | Tool call không rate limit | accept | slowapi không áp dụng cho ASGI sub-app; M2 internal use → chấp nhận; Redis counter trong authenticate_mcp_request defer v4.0 hardening (Claude's Discretion) |
| T-08.1-02-I | Information Disclosure | Exception → ToolError message | mitigate | catch-all Exception → ToolError("Lỗi nội bộ: {e}") — không expose stack trace; FastMCP mask_error_details option khả dụng nếu cần |
| T-08.1-02-L | Lifespan Race | set_pool gọi trễ | mitigate | set_pool() trong lifespan sau redis init; combine_lifespans đảm bảo lifespan cha chạy TRƯỚC lifespan MCP; _get_pool() guard RuntimeError nếu pool chưa có |
</threat_model>

<verification>
Sau khi hoàn thành Plan 02:

```bash
cd Hub_All/api

# 1. Server import được
python -c "from app.mcp.server import mcp, set_pool; print('server OK')"

# 2. Đủ 3 tool
python -c "
from app.mcp.server import mcp
import asyncio
async def check():
    tools = await mcp.get_tools()
    names = set(tools.keys())
    assert 'list_hubs' in names, 'list_hubs missing'
    assert 'search_wiki' in names, 'search_wiki missing'
    assert 'ask_wiki' in names, 'ask_wiki missing'
    print(f'tools OK: {names}')
asyncio.run(check())
"

# 3. main.py có combine_lifespans
grep "combine_lifespans" app/main.py

# 4. main.py mount /mcp
grep 'app.mount.*"/mcp"' app/main.py

# 5. create_app không crash (cocoindex skip mode)
COCOINDEX_SKIP_SETUP=1 python -c "from app.main import create_app; app = create_app(); print('create_app OK')"

# 6. Ruff lint
uv run ruff check app/mcp/server.py app/main.py
```
</verification>

<success_criteria>
1. app/mcp/server.py: 3 tool (list_hubs, search_wiki, ask_wiki) đăng ký qua @mcp.tool; set_pool/  _get_pool singleton; ask_wiki có asyncio.create_task(log_usage_event(...))
2. app/main.py: combine_lifespans(lifespan, _mcp_app.lifespan) thay lifespan=lifespan; mcp_set_pool() trong lifespan; app.mount("/mcp", _mcp_app) ở cuối create_app()
3. COCOINDEX_SKIP_SETUP=1 python -c "from app.main import create_app; app = create_app()" không exception
4. python -c "from app.mcp.server import mcp, set_pool" không exception
5. 3 tool name verify qua mcp.get_tools(): list_hubs, search_wiki, ask_wiki
6. ruff check app/mcp/ app/main.py — 0 lỗi
</success_criteria>

<output>
Sau khi hoàn thành Plan 02, tạo file:
`.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-02-SUMMARY.md`

Ghi rõ:
- Import path FastMCP thực tế đã dùng (mcp.fastmcp vs fastmcp)
- Import path combine_lifespans thực tế (fastmcp.utilities.lifespan vs mcp.*)
- Import path ToolError thực tế
- Bất kỳ deviation nào trong field name khi map result dict → SearchResultItem
- Kết quả verify commands
</output>
