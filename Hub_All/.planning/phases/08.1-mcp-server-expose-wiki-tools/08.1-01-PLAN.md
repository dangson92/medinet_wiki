---
phase: "08.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Hub_All/api/pyproject.toml
  - Hub_All/api/app/mcp/__init__.py
  - Hub_All/api/app/mcp/schemas.py
  - Hub_All/api/app/mcp/auth.py
autonomous: true
requirements:
  - MCP-01
  - MCP-02

must_haves:
  truths:
    - "Package mcp==1.9.4 được khai báo trong pyproject.toml và có thể import"
    - "Module app.mcp tồn tại dưới dạng package Python hợp lệ"
    - "Pydantic output schemas (SearchResult, AskAnswer, HubList) export đúng từ mcp/schemas.py"
    - "authenticate_mcp_request() đọc X-API-Key → verify → dựng UserWithHubs thủ công (không Depends)"
    - "await db.commit() được gọi sau ApiKeyService.verify_key (MEMORY: project_fastapi_bgtask_commit)"
  artifacts:
    - path: "Hub_All/api/pyproject.toml"
      provides: "khai báo mcp>=1.9.4,<2 trong [project].dependencies"
      contains: "mcp>=1.9.4,<2"
    - path: "Hub_All/api/app/mcp/__init__.py"
      provides: "Package init module"
      min_lines: 1
    - path: "Hub_All/api/app/mcp/schemas.py"
      provides: "Pydantic output models cho 3 tool"
      exports:
        - CitationItem
        - AskAnswer
        - SearchResultItem
        - SearchResult
        - HubItem
        - HubList
    - path: "Hub_All/api/app/mcp/auth.py"
      provides: "authenticate_mcp_request() → UserWithHubs"
      exports:
        - authenticate_mcp_request
  key_links:
    - from: "Hub_All/api/app/mcp/auth.py"
      to: "app.services.api_key_service.ApiKeyService.verify_key"
      via: "AsyncSession thủ công (không FastAPI Depends)"
      pattern: "ApiKeyService\\(db=db\\)\\.verify_key"
    - from: "Hub_All/api/app/mcp/auth.py"
      to: "app.auth.dependencies.UserWithHubs"
      via: "import trực tiếp + khởi tạo thủ công"
      pattern: "UserWithHubs\\(user=user, hub_ids=hub_ids\\)"
    - from: "Hub_All/api/app/mcp/auth.py"
      to: "fastmcp.server.dependencies.get_http_headers"
      via: "get_http_headers(include={'x-api-key'})"
      pattern: "get_http_headers"
---

<objective>
Thiết lập foundation cho MCP module: khai báo dependency mcp==1.9.4, tạo package
app/mcp/ với Pydantic output schemas cho 3 tool và hàm xác thực API key tái dùng
ApiKeyService từ Phase 5.

Purpose: Wave 1 phải hoàn tất trước khi Wave 2 có thể định nghĩa tool handler —
schemas và auth là contracts mà server.py và tests sẽ build against. KHÔNG viết
tool logic ở plan này.

Output: pyproject.toml cập nhật + app/mcp/__init__.py + app/mcp/schemas.py +
app/mcp/auth.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/ROADMAP.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-CONTEXT.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md
@Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md

<interfaces>
<!-- Các type/class codebase hiện hữu mà plan này sẽ tái dùng. Executor KHÔNG cần tìm kiếm thêm. -->

Từ Hub_All/api/app/auth/dependencies.py:
```python
class UserWithHubs:
    def __init__(self, user: User, hub_ids: list[str]) -> None:
        self.user = user
        self.hub_ids = hub_ids
```

Từ Hub_All/api/app/services/api_key_service.py (tên method canonical):
```python
# ApiKeyService(db=db).verify_key(x_api_key) → dict | None
# dict có field "id" là api_keys.id UUID
# verify_key ghi last_used_at = NOW() → PHẢI await db.commit() sau khi gọi
```

Từ Hub_All/api/app/db/session.py (dùng để tạo AsyncSession thủ công):
```python
# from app.db.session import get_engine
# engine = get_engine()
# async with AsyncSession(engine) as db: ...
```

Từ Hub_All/api/app/models/auth.py:
```python
# class User: id, email, full_name, role, is_active
# class UserHub: user_id, hub_id
```

Từ Hub_All/api/app/schemas/ask.py:
```python
class AskRequest(BaseModel):
    query: str
    hub_id: str | None = None
    hub_ids: list[str] | None = None
    top_k: int | None = None

class Citation(BaseModel):
    number: int; marker: str; chunk_id: str; document_id: str
    hub_id: str; document_name: str; hub_name: str; score: float
    content_snippet: str

class AskResponse(BaseModel):
    answer: str; citations: list[Citation]; model: str; query_time_ms: int
```

Từ Hub_All/api/app/services/ask_service.py:
```python
class AskOutcome:
    response: AskResponse
    usage: UsageRecord
    chunks: list[_AskChunk]

class UsageRecord:
    user_id: str | None; hub_id: str | None; model: str
    prompt_tokens: int | None; completion_tokens: int | None
    total_tokens: int | None; cost_usd: float | None; request_id: str | None
```

Từ Hub_All/api/app/services/search_service.py (kết quả trả của search):
```python
# search() / search_cross_hub() → dict với key "results": list[dict]
# Mỗi result dict: {"id": str, "hub_id": str, "title": str, "hub_name": str,
#                   "snippet": str, "content": str, "score": float}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm dependency mcp vào pyproject.toml + tạo package init</name>
  <files>Hub_All/api/pyproject.toml, Hub_All/api/app/mcp/__init__.py</files>
  <read_first>
    - Hub_All/api/pyproject.toml — đọc section [project].dependencies để thêm đúng vị trí (sau "slowapi>=0.1.9")
    - Hub_All/api/app/routers/__init__.py — xem mẫu package init để sao chép convention
  </read_first>
  <behavior>
    - pyproject.toml: thêm "mcp>=1.9.4,<2" vào [project].dependencies, sau dòng "slowapi>=0.1.9"
    - mcp/__init__.py: rỗng hoặc chỉ "from __future__ import annotations" — không logic nghiệp vụ
  </behavior>
  <action>
Mở Hub_All/api/pyproject.toml. Trong section [project].dependencies, sau dòng
`"slowapi>=0.1.9",`, thêm dòng mới:
```toml
"mcp>=1.9.4,<2",
```

KHÔNG dùng "==1.9.4" exact pin cho mcp (khác với fastapi/asyncpg là stack core);
upper bound <2 tránh breaking change major version (nhất quán pattern litellm>=1.82,<2).

Tạo file Hub_All/api/app/mcp/__init__.py với nội dung:
```python
from __future__ import annotations
```

Nếu cần re-export sau (Wave 2 server.py), sẽ thêm vào — hiện tại giữ tối giản.
  </action>
  <verify>
    <automated>cd Hub_All/api && grep "mcp>=1.9.4" pyproject.toml</automated>
  </verify>
  <done>pyproject.toml chứa "mcp>=1.9.4,<2"; file mcp/__init__.py tồn tại</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tạo mcp/schemas.py — Pydantic output models cho 3 tool</name>
  <files>Hub_All/api/app/mcp/schemas.py</files>
  <read_first>
    - Hub_All/api/app/schemas/ask.py — xem AskResponse + Citation để biết field nào cần map sang CitationItem
    - Hub_All/api/app/schemas/search.py — xem SearchResultItem shape hiện tại (để biết field search service trả)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md — xem Pattern 3 (structured output)
  </read_first>
  <behavior>
    - CitationItem có: chunk_id str, document_name str, hub_name str, snippet str, score float
    - AskAnswer có: answer str (giữ nguyên marker [N]), citations list[CitationItem]
    - SearchResultItem có: chunk_id str, content str, score float, document_name str, hub_name str, hub_id str
    - SearchResult có: results list[SearchResultItem], total int
    - HubItem có: id str, name str, description str | None
    - HubList có: hubs list[HubItem]
    - Tất cả kế thừa pydantic.BaseModel, đầu file có "from __future__ import annotations"
  </behavior>
  <action>
Tạo Hub_All/api/app/mcp/schemas.py. Tham chiếu analog: api/app/schemas/ask.py (AskResponse + Citation).

```python
"""MCP output schemas — Phase 8.1 (MCP-02).

Pydantic models định nghĩa structured output cho 3 tool MCP:
- `AskAnswer` — output tool ask_wiki (D-11: answer giữ marker [N] + citations structured)
- `SearchResult` — output tool search_wiki
- `HubList` — output tool list_hubs

FastMCP tự generate `structuredContent` + `outputSchema` từ return type Pydantic model
(RESEARCH.md Pattern 3 — KHÔNG viết JSON Schema tay).
"""
from __future__ import annotations

from pydantic import BaseModel


class CitationItem(BaseModel):
    """1 trích dẫn trong câu trả lời ask_wiki (D-11)."""

    chunk_id: str
    document_name: str
    hub_name: str
    snippet: str
    score: float


class AskAnswer(BaseModel):
    """Output tool ask_wiki (D-11).

    `answer` giữ nguyên marker [N] — LLM client đọc trực tiếp.
    `citations` structured để client attribute nguồn.
    """

    answer: str
    citations: list[CitationItem]


class SearchResultItem(BaseModel):
    """1 chunk kết quả tìm kiếm từ search_wiki."""

    chunk_id: str
    content: str
    score: float
    document_name: str
    hub_name: str
    hub_id: str


class SearchResult(BaseModel):
    """Output tool search_wiki."""

    results: list[SearchResultItem]
    total: int


class HubItem(BaseModel):
    """1 Hub mà API key có quyền truy cập."""

    id: str
    name: str
    description: str | None = None


class HubList(BaseModel):
    """Output tool list_hubs (D-07)."""

    hubs: list[HubItem]
```
  </action>
  <verify>
    <automated>cd Hub_All/api && python -c "from app.mcp.schemas import AskAnswer, SearchResult, HubList, CitationItem, SearchResultItem, HubItem; print('schemas OK')"</automated>
  </verify>
  <done>Tất cả 6 class export được từ app.mcp.schemas; file ruff/mypy clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tạo mcp/auth.py — authenticate_mcp_request() xác thực X-API-Key</name>
  <files>Hub_All/api/app/mcp/auth.py</files>
  <read_first>
    - Hub_All/api/app/auth/dependencies.py — đọc get_api_key_or_jwt (lines 222–288) và UserWithHubs (lines 193–219): đây là analog EXACT — copy luồng verify_key + load created_by + load User + load hub_ids, thay HTTPException → ValueError, KHÔNG có FastAPI Depends
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-RESEARCH.md — đọc Pattern 2 (get_http_headers + authenticate_mcp_request) và Cạm bẫy 6 (commit sau verify_key)
    - Hub_All/.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-PATTERNS.md — đọc section mcp/auth.py (analog dependencies.py lines 222–288)
  </read_first>
  <behavior>
    - get_http_headers(include={"x-api-key"}) → lấy header; thiếu key → raise ValueError("MCP_UNAUTHORIZED: thiếu header X-API-Key")
    - Tạo AsyncSession thủ công qua get_engine() (KHÔNG Depends(get_session))
    - ApiKeyService(db=db).verify_key(api_key) → None nếu không hợp lệ/đã thu hồi → raise ValueError
    - Query "SELECT created_by FROM api_keys WHERE id = :id" để lấy user_id
    - Load User từ ORM SELECT WHERE is_active IS TRUE → None → raise ValueError
    - Load hub_ids: SELECT UserHub.hub_id WHERE user_id = user.id → list[str]
    - await db.commit() TRƯỚC KHI kết thúc AsyncSession context (MEMORY: project_fastapi_bgtask_commit — verify_key ghi last_used_at=NOW(), PHẢI commit)
    - Return UserWithHubs(user=user, hub_ids=hub_ids)
    - Hàm nhận pool: asyncpg.Pool (reserve param cho tương lai — chưa dùng trực tiếp, nhưng giữ signature nhất quán với RESEARCH.md)
  </behavior>
  <action>
Tạo Hub_All/api/app/mcp/auth.py. Analog: app/auth/dependencies.py get_api_key_or_jwt (lines 222–288).
Điểm khác biệt THEN CHỐT so với analog:
1. Không có FastAPI DI → không Header(...), không Depends(get_session) — tạo AsyncSession thủ công
2. HTTPException → ValueError (không có FastAPI error context trong MCP tool)
3. await db.commit() bắt buộc (MEMORY: project_fastapi_bgtask_commit)
4. get_http_headers từ fastmcp.server.dependencies (không từ FastAPI Header)

```python
"""MCP authentication — Phase 8.1 (MCP-01, D-05/D-06/D-12).

`authenticate_mcp_request()` — xác thực MCP client qua header X-API-Key.

Tái dùng luồng `get_api_key_or_jwt` (app/auth/dependencies.py lines 222–288)
nhưng KHÔNG có FastAPI DI context — 3 khác biệt then chốt:
1. Header đọc qua `get_http_headers(include={'x-api-key'})` từ fastmcp (KHÔNG Header(...) FastAPI)
2. Lỗi auth → raise ValueError (KHÔNG HTTPException — không có FastAPI error context trong MCP tool)
3. await db.commit() bắt buộc sau verify_key (MEMORY: project_fastapi_bgtask_commit —
   ApiKeyService.verify_key ghi last_used_at=NOW(), PHẢI commit trước session close)

Caller (tool handler) bắt ValueError → raise ToolError (KHÔNG để tool chạy nếu auth fail — D-06).
"""
from __future__ import annotations

import asyncpg
from fastmcp.server.dependencies import get_http_headers
from sqlalchemy import select, text
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.dependencies import UserWithHubs
from app.db.session import get_engine
from app.models.auth import User, UserHub
from app.services.api_key_service import ApiKeyService


async def authenticate_mcp_request(pool: asyncpg.Pool) -> UserWithHubs:  # noqa: ARG001
    """Xác thực MCP client qua header X-API-Key → dựng UserWithHubs.

    Gọi từ đầu mỗi tool handler. Raise ValueError nếu auth fail → tool handler
    map sang ToolError → client nhận MCP error (isError: true).

    Args:
        pool: asyncpg.Pool (reserve param — chưa dùng trực tiếp; giữ nhất quán
              với RESEARCH.md Pattern 4 để sau này inject qua _get_pool()).

    Returns:
        UserWithHubs với user ORM + danh sách hub_id từ user_hubs.

    Raises:
        ValueError: nếu header thiếu, key không hợp lệ/thu hồi, user bị vô hiệu.
    """
    # Bước 1: đọc X-API-Key từ HTTP header (fastmcp stateless mode — header luôn fresh)
    headers = get_http_headers(include={"x-api-key"})
    api_key = headers.get("x-api-key")
    if not api_key:
        raise ValueError("MCP_UNAUTHORIZED: thiếu header X-API-Key")

    # Bước 2: verify qua ApiKeyService (SQLAlchemy session thủ công — không có Depends)
    engine = get_engine()
    async with AsyncSession(engine) as db:
        svc = ApiKeyService(db=db)
        principal = await svc.verify_key(api_key)
        if principal is None:
            raise ValueError("MCP_UNAUTHORIZED: API key không hợp lệ hoặc đã thu hồi")

        # Bước 3: load created_by user_id từ api_keys table (giống dependencies.py lines 255–269)
        key_row = (
            await db.execute(
                text("SELECT created_by FROM api_keys WHERE id = :id"),
                {"id": principal["id"]},
            )
        ).fetchone()
        if key_row is None or key_row[0] is None:
            raise ValueError("MCP_UNAUTHORIZED: API key không gắn user hợp lệ")

        # Bước 4: load User ORM — phải is_active=True
        stmt = select(User).where(User.id == key_row[0], User.is_active.is_(True))
        user = (await db.execute(stmt)).scalar_one_or_none()
        if user is None:
            raise ValueError("MCP_UNAUTHORIZED: User gắn API key đã bị vô hiệu hoá")

        # Bước 5: load hub_ids từ user_hubs (giống get_current_user_with_hubs dependencies.py lines 215–218)
        stmt_hubs = select(UserHub.hub_id).where(UserHub.user_id == user.id)
        hub_ids = [str(h) for h in (await db.execute(stmt_hubs)).scalars().all()]

        # CRITICAL: commit TRƯỚC khi AsyncSession close
        # ApiKeyService.verify_key ghi last_used_at=NOW() → nếu không commit → bị rollback
        # MEMORY: project_fastapi_bgtask_commit.md — commit SAU background writes
        await db.commit()

    return UserWithHubs(user=user, hub_ids=hub_ids)
```
  </action>
  <acceptance_criteria>
    - grep "await db.commit()" Hub_All/api/app/mcp/auth.py → xuất hiện (CRITICAL: MEMORY pattern)
    - grep "get_http_headers" Hub_All/api/app/mcp/auth.py → xuất hiện (không phải FastAPI Header)
    - grep "raise ValueError" Hub_All/api/app/mcp/auth.py → xuất hiện ít nhất 4 lần (4 nhánh auth fail)
    - grep "HTTPException" Hub_All/api/app/mcp/auth.py → KHÔNG xuất hiện (dùng ValueError)
    - python -c "from app.mcp.auth import authenticate_mcp_request; print('import OK')" chạy được từ Hub_All/api/
  </acceptance_criteria>
  <verify>
    <automated>cd Hub_All/api && grep -c "raise ValueError" app/mcp/auth.py && grep "await db.commit" app/mcp/auth.py && python -c "from app.mcp.auth import authenticate_mcp_request; print('auth OK')"</automated>
  </verify>
  <done>
    - authenticate_mcp_request import được từ app.mcp.auth
    - Hàm có 4 nhánh ValueError (thiếu header, key không hợp lệ, key không gắn user, user bị vô hiệu)
    - await db.commit() có mặt sau verify_key + hub_ids load
    - KHÔNG có HTTPException trong file
    - mypy --strict không báo lỗi (ignore_missing_imports = true cho fastmcp)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Ranh giới | Mô tả |
|-----------|-------|
| Không có ở Plan 01 | Plan này chỉ tạo schemas và auth module — chưa có transport hay endpoint |
| `X-API-Key` header → `ApiKeyService.verify_key` | Header đến từ MCP client qua HTTP; phải verify trước khi trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01-S | Spoofing | `authenticate_mcp_request` header check | mitigate | Thiếu header hoặc key sai → ValueError ngay bước đầu; KHÔNG để tool chạy (D-06) |
| T-08.1-01-S2 | Spoofing | `ApiKeyService.verify_key` key đã thu hồi | mitigate | `verify_key` check `is_active=TRUE` trong DB; trả None → ValueError |
| T-08.1-01-E | Elevation | `created_by` user load với `is_active.is_(True)` | mitigate | User bị vô hiệu hoá → ValueError; KHÔNG trả UserWithHubs cho user inactive |
| T-08.1-01-I | Information Disclosure | Stack trace trong ValueError message | accept | ValueError message tường minh nhưng KHÔNG có stack trace; caller map sang ToolError |
</threat_model>

<verification>
Sau khi hoàn thành Plan 01:

```bash
cd Hub_All/api

# 1. Dependency khai báo đúng
grep "mcp>=1.9.4,<2" pyproject.toml

# 2. Package structure tồn tại
python -c "import app.mcp; print('package OK')"

# 3. Schemas import được
python -c "from app.mcp.schemas import AskAnswer, SearchResult, HubList, CitationItem, SearchResultItem, HubItem; print('schemas 6/6 OK')"

# 4. Auth module import được
python -c "from app.mcp.auth import authenticate_mcp_request; print('auth OK')"

# 5. CRITICAL: commit pattern có mặt trong auth.py
grep "await db.commit" app/mcp/auth.py

# 6. CRITICAL: không có HTTPException trong auth.py (dùng ValueError)
grep "HTTPException" app/mcp/auth.py && echo "FAIL: HTTPException found" || echo "OK: no HTTPException"

# 7. Ruff lint
uv run ruff check app/mcp/
```
</verification>

<success_criteria>
1. pyproject.toml có "mcp>=1.9.4,<2" trong [project].dependencies
2. app/mcp/__init__.py tồn tại — package hợp lệ
3. app/mcp/schemas.py export 6 class: CitationItem, AskAnswer, SearchResultItem, SearchResult, HubItem, HubList
4. app/mcp/auth.py: authenticate_mcp_request() đọc X-API-Key qua get_http_headers, verify qua ApiKeyService, await db.commit() sau writes, return UserWithHubs, raise ValueError (không HTTPException) khi fail
5. ruff check app/mcp/ — 0 lỗi (mypy ignore_missing_imports cho fastmcp)
</success_criteria>

<output>
Sau khi hoàn thành Plan 01, tạo file:
`.planning/phases/08.1-mcp-server-expose-wiki-tools/08.1-01-SUMMARY.md`

Ghi rõ:
- Các file đã tạo/sửa
- Quyết định kỹ thuật đã thực hiện (import path fastmcp, commit pattern)
- Bất kỳ deviation nào so với plan (ví dụ: import path fastmcp khác với dự kiến)
- Kết quả verify commands
</output>
