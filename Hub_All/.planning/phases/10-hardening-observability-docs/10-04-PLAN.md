---
phase: 10-hardening-observability-docs
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - Hub_All/mcp_service/mcp_app/server.py
  - Hub_All/mcp_service/mcp_app/config.py
  - Hub_All/mcp_service/tests/test_cors_split_policy.py
  - Hub_All/mcp_service/tests/test_path_prefix_wrapper.py
autonomous: true
requirements: [HARD-02]
must_haves:
  truths:
    - "Endpoint metadata (/.well-known/oauth-authorization-server[/mcp] + /.well-known/oauth-protected-resource[/mcp] + /.well-known/openid-configuration) trả CORS Access-Control-Allow-Origin: * (RFC 8414 §3.1)"
    - "Endpoint nhạy cảm (/token, /authorize, /revoke, /register, /mcp) trả CORS chỉ với origin trong whitelist (claude.ai + inspector.modelcontextprotocol.io + localhost dev)"
    - "Browser POST /token từ origin lạ → preflight FAIL hoặc Access-Control-Allow-Origin KHÔNG match → request blocked"
    - "Không còn double-add CORS header trong path-prefix mode (CRIT-02 đã đóng từ HEAD 282abaa nhưng test mới enforce regression)"
  artifacts:
    - path: "Hub_All/mcp_service/mcp_app/server.py"
      provides: "_add_metadata_cors_middleware + _add_sensitive_cors_middleware (2 hàm thay 1 _add_cors_middleware)"
      contains: "_add_metadata_cors_middleware\\|_add_sensitive_cors_middleware"
    - path: "Hub_All/mcp_service/mcp_app/config.py"
      provides: "Setting mcp_oauth_sensitive_allowed_origins list[str] default whitelist"
      contains: "mcp_oauth_sensitive_allowed_origins"
    - path: "Hub_All/mcp_service/tests/test_cors_split_policy.py"
      provides: "Test 2 CORS policy tách biệt (metadata * vs sensitive whitelist)"
  key_links:
    - from: "build_asgi_app() path-prefix mode"
      to: "_add_metadata_cors_middleware(wrapper) + _add_sensitive_cors_middleware(inner)"
      via: "2 instance CORSMiddleware riêng (Starlette không support per-route CORS native)"
      pattern: "_add_(metadata|sensitive)_cors_middleware"
    - from: "build_asgi_app() subdomain mode"
      to: "_add_sensitive_cors_middleware(inner)"
      via: "subdomain deploy = OAuth endpoint cùng host, dùng whitelist sensitive"
      pattern: "_add_sensitive_cors_middleware\\(inner\\)"
---

<objective>
Đóng CRIT-01 (Phase 8.3 audit 2026-05-21 defer): tách `_add_cors_middleware` hiện
tại (allow_origins=["*"] cho TẤT CẢ route gồm /token + /authorize + /revoke) thành
**2 CORS policy độc lập**:

1. **Metadata-only policy** (`allow_origins=["*"]`): áp dụng cho 5 route metadata
   `/.well-known/oauth-authorization-server[+suffix]`, `/.well-known/oauth-protected-resource[+suffix]`,
   `/.well-known/openid-configuration`. RFC 8414 §3.1 / RFC 9728 §3.1 cho phép
   wildcard origin vì metadata endpoint public hoàn toàn, không có credential.

2. **Sensitive policy** (`allow_origins=[whitelist]`): áp dụng cho `/token`,
   `/authorize`, `/revoke`, `/register`, và transport `/mcp` + `/mcp/*`. Whitelist
   default:
   - `https://claude.ai`
   - `https://inspector.modelcontextprotocol.io`
   - `http://localhost:6274` (MCP Inspector dev)
   - `http://127.0.0.1:6274`

   Defer expose qua config (`MCP_OAUTH_SENSITIVE_ALLOWED_ORIGINS` env var
   comma-separated) để ops thêm origin custom mà không sửa code.

**Purpose:** Khai thác audit ghi nhận: malicious browser extension hoặc tab
compromised có thể gọi `/token` + transport tools từ origin bất kỳ chỉ cần biết
Bearer token (vd qua XSS app khác). Phòng vệ duy nhất hiện tại là token nằm trong
memory client OAuth — Claude/Inspector đều giữ client-side, có thể leak qua XSS.
Tách CORS giảm bề mặt khai thác xuống còn 4 origin whitelist.

**Phase 8.3 audit hint:** "Cần 2 instance CORSMiddleware riêng (Starlette không
support per-route CORS native — dùng 2 Starlette sub-app)" — KHÔNG đúng hoàn toàn:
Starlette CORSMiddleware add ở app level, **NHƯNG** path-prefix mode đã có sẵn
2-level structure (`wrapper` outer + `inner` Mount). Có thể add CORS khác nhau lên
mỗi level. Subdomain mode (root) phức tạp hơn — chỉ có inner duy nhất, cần dùng
**custom middleware chọn origin theo path** thay vì 2 CORSMiddleware Starlette.

**Output:**
- `mcp_service/mcp_app/server.py` — thay 1 hàm `_add_cors_middleware` thành 2 hàm
  + cập nhật 2 call site (subdomain mode + path-prefix mode).
- `mcp_service/mcp_app/config.py` — thêm setting `mcp_oauth_sensitive_allowed_origins`
  list[str] default 4 origin whitelist (đọc env comma-separated).
- 1 test file mới `test_cors_split_policy.py` 6 case (metadata * + sensitive
  whitelist allowed + sensitive whitelist rejected + path mode + subdomain mode).
- Cập nhật `test_path_prefix_wrapper.py` (nếu test hiện hữu assert
  `Access-Control-Allow-Origin: *` cho /token → đổi sang assert whitelist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/phases/08.3-mcp-oauth-deploy-public-https/08.3-MCP-AUDIT-2026-05-21.md
@Hub_All/mcp_service/mcp_app/server.py
@Hub_All/mcp_service/mcp_app/config.py

<interfaces>
<!-- Function hiện tại Plan 10-04 sẽ thay/tách. -->

Hub_All/mcp_service/mcp_app/server.py (line 820 hiện tại):
```python
def _add_cors_middleware(app: Any) -> None:
    """Add Starlette CORSMiddleware lên `app` với cấu hình public OAuth AS."""
    from starlette.middleware.cors import CORSMiddleware
    app.add_middleware(
        CORSMiddleware,
        allow_origins=["*"],   # ← CRIT-01: quá lỏng cho /token + /authorize + /revoke
        allow_methods=["*"],
        allow_headers=["*"],
        expose_headers=["Mcp-Session-Id", "WWW-Authenticate"],
        max_age=86400,
    )
```

Call sites:
- Line 676: `_add_cors_middleware(inner)` (subdomain mode)
- Line 767: `_add_cors_middleware(wrapper)` (path-prefix mode — wrapper, KHÔNG inner)

Note CRIT-02 đã đóng: line 607 cũ `_add_cors_middleware(inner)` cho path-prefix
mode đã xoá (HEAD 282abaa). Plan 10-04 KHÔNG đụng layout này.

Hub_All/mcp_service/mcp_app/config.py — pattern thêm setting:
```python
class Settings(BaseSettings):
    # ... existing
    oauth_issuer_url: str = "http://localhost:8190"
    # NEW Plan 10-04:
    mcp_oauth_sensitive_allowed_origins: list[str] = Field(
        default_factory=lambda: [
            "https://claude.ai",
            "https://inspector.modelcontextprotocol.io",
            "http://localhost:6274",
            "http://127.0.0.1:6274",
        ],
    )
    # Pydantic-settings: env `MCP_OAUTH_SENSITIVE_ALLOWED_ORIGINS=https://a,http://b`
    # parse comma-separated qua field_validator.
```
</interfaces>

<approach_decision>
**Hai phương án triển khai:**

**A. 2 CORSMiddleware Starlette riêng cho path-prefix mode + custom middleware
chọn-theo-path cho subdomain mode** (hint audit):
- Path-prefix: add `_add_metadata_cors_middleware(wrapper)` + `_add_sensitive_cors_middleware(inner)` — Starlette CORSMiddleware tự match request → 2 instance độc lập.
- Subdomain: Mount metadata routes + transport vào cùng app (inner). Cần custom
  middleware đọc `request.url.path`, chọn origin list theo path, build header
  Access-Control-Allow-Origin tay.

**B. 1 custom middleware lookup path → policy (chính xác hơn nhưng nhiều code):**
- Mọi mode dùng 1 middleware `MultiPolicyCORSMiddleware(app, metadata_paths, metadata_origins, sensitive_origins)` tự route theo path.
- Code path khớp `/.well-known/*` → wildcard; còn lại → whitelist.
- Test deterministic, KHÔNG phụ thuộc layout 2-level.

**Quyết định: A (path-prefix tận dụng 2-level) cho production deploy chính + B
(custom middleware) cho subdomain mode** vì subdomain chỉ có 1 app level.

Đơn giản hơn nữa: **B cho cả 2 mode** — viết 1 custom `MultiPolicyCORSMiddleware`
xài chung. Test deterministic + ít branching hơn. Plan 10-04 chọn B.
</approach_decision>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo MultiPolicyCORSMiddleware + setting + đổi 2 call site + test 6 case</name>
  <files>
    Hub_All/mcp_service/mcp_app/config.py (thêm setting whitelist)
    Hub_All/mcp_service/mcp_app/server.py (thay _add_cors_middleware → MultiPolicyCORSMiddleware + 2 call site)
    Hub_All/mcp_service/tests/test_cors_split_policy.py (mới — 6 case)
    Hub_All/mcp_service/tests/test_path_prefix_wrapper.py (patch nếu assert old behavior)
  </files>
  <behavior>
    Test 1 (metadata wildcard): GET `/.well-known/oauth-authorization-server` với header `Origin: https://random-attacker.com` → 200 + `Access-Control-Allow-Origin: *` (RFC 8414 §3.1 metadata wildcard).
    Test 2 (metadata wildcard mode path-prefix): GET `/.well-known/oauth-authorization-server/mcp` với Origin malicious → 200 + ACAO `*`.
    Test 3 (sensitive whitelist allowed): OPTIONS /token với Origin `https://claude.ai` + Access-Control-Request-Method: POST → 200 + `Access-Control-Allow-Origin: https://claude.ai` (KHÔNG wildcard, KHÔNG có credentials=true vì OAuth dùng Authorization Bearer header, KHÔNG dùng cookie).
    Test 4 (sensitive whitelist rejected): OPTIONS /token với Origin `https://evil.com` → response KHÔNG có header `Access-Control-Allow-Origin` (Starlette CORSMiddleware behavior khi origin không match là KHÔNG echo header — browser block CORS).
    Test 5 (path-prefix mode /mcp/token): tạo wrapper với MCP_PATH_PREFIX=mcp, OPTIONS /mcp/token với Origin claude.ai → 200 + ACAO `https://claude.ai`.
    Test 6 (transport /mcp endpoint sensitive): OPTIONS /mcp (transport stream HTTP) với Origin localhost:6274 → 200 + ACAO `http://localhost:6274`.
  </behavior>
  <action>
    **A. `mcp_service/mcp_app/config.py` — thêm setting:**
    ```python
    from pydantic import Field, field_validator
    from pydantic_settings import BaseSettings

    class Settings(BaseSettings):
        # ... existing fields (oauth_issuer_url, api_base_url, etc.)

        # CRIT-01 fix: 2 CORS policy split — metadata (*) vs sensitive (whitelist).
        # Sensitive whitelist cover Claude web + MCP Inspector + dev localhost.
        # Ops thêm origin qua env MCP_OAUTH_SENSITIVE_ALLOWED_ORIGINS comma-sep.
        mcp_oauth_sensitive_allowed_origins: list[str] = Field(
            default_factory=lambda: [
                "https://claude.ai",
                "https://inspector.modelcontextprotocol.io",
                "http://localhost:6274",
                "http://127.0.0.1:6274",
            ],
        )

        @field_validator("mcp_oauth_sensitive_allowed_origins", mode="before")
        @classmethod
        def _split_comma(cls, v):
            if isinstance(v, str):
                return [s.strip() for s in v.split(",") if s.strip()]
            return v
    ```

    **B. `mcp_service/mcp_app/server.py` — thay `_add_cors_middleware`:**

    1. Định nghĩa constant set path metadata + sensitive:
       ```python
       # Path metadata (wildcard origin OK per RFC 8414 §3.1 / RFC 9728 §3.1).
       # Suffix path (vd /mcp) thêm động ở build_asgi_app (issuer path component).
       _METADATA_PATHS_PREFIX = (
           "/.well-known/oauth-authorization-server",
           "/.well-known/oauth-protected-resource",
           "/.well-known/openid-configuration",
       )
       # Path sensitive (whitelist origin). Cover root variant + prefix variant.
       _SENSITIVE_PATHS = frozenset({
           "/token", "/authorize", "/revoke", "/register",
           "/mcp/token", "/mcp/authorize", "/mcp/revoke", "/mcp/register",
           "/mcp",  # transport endpoint root
       })

       def _is_metadata_path(path: str) -> bool:
           """Check path khớp prefix metadata (cover cả suffix /mcp)."""
           return any(path.startswith(p) for p in _METADATA_PATHS_PREFIX)

       def _is_sensitive_path(path: str) -> bool:
           """Check path là sensitive OAuth endpoint hoặc transport /mcp."""
           if path in _SENSITIVE_PATHS:
               return True
           # /mcp/<anything> = transport stream, ngoài 4 endpoint OAuth.
           return path == "/mcp" or path.startswith("/mcp/")
       ```

    2. Tạo `class _MultiPolicyCORSMiddleware`:
       ```python
       class _MultiPolicyCORSMiddleware:
           """ASGI middleware tách CORS policy theo path.

           - Metadata path (`/.well-known/*`) → wildcard origin (`*`).
           - Sensitive path (`/token`, `/authorize`, `/revoke`, `/register`, `/mcp[/*]`)
             → whitelist origin từ settings.mcp_oauth_sensitive_allowed_origins.
           - Path khác (vd `/login`) → KHÔNG inject CORS header (Starlette
             redirect_slashes / 404 default behavior — không có origin matters).

           Defense in depth chống CRIT-01 (audit 2026-05-21): malicious browser
           extension/tab compromised KHÔNG gọi được /token từ origin lạ.
           """

           def __init__(self, app: Any, sensitive_origins: list[str]) -> None:
               self._app = app
               self._sensitive_origins = frozenset(sensitive_origins)

           async def __call__(self, scope, receive, send):
               if scope["type"] != "http":
                   await self._app(scope, receive, send)
                   return
               path = scope.get("path", "")
               request_origin = self._extract_origin(scope.get("headers", []))

               if scope.get("method") == "OPTIONS":
                   # Preflight — handle full response trong middleware (KHÔNG forward
                   # vì Starlette router KHÔNG có default OPTIONS handler).
                   response = self._build_preflight(path, request_origin)
                   await self._send_response(response, send)
                   return

               # Forward request, intercept response để inject CORS header.
               async def _send(message):
                   if message["type"] == "http.response.start":
                       headers = list(message.get("headers", []))
                       cors_headers = self._build_cors_headers(path, request_origin)
                       for name, value in cors_headers.items():
                           headers.append((name.encode("ascii"), value.encode("ascii")))
                       message = dict(message, headers=headers)
                   await send(message)
               await self._app(scope, receive, _send)

           def _extract_origin(self, headers: list[tuple[bytes, bytes]]) -> str | None:
               for name, value in headers:
                   if name.lower() == b"origin":
                       return value.decode("latin-1")
               return None

           def _build_cors_headers(self, path: str, origin: str | None) -> dict:
               if _is_metadata_path(path):
                   return {
                       "access-control-allow-origin": "*",
                       "access-control-expose-headers": "Mcp-Session-Id, WWW-Authenticate",
                   }
               if _is_sensitive_path(path) and origin and origin in self._sensitive_origins:
                   return {
                       "access-control-allow-origin": origin,
                       "vary": "Origin",
                       "access-control-expose-headers": "Mcp-Session-Id, WWW-Authenticate",
                   }
               return {}

           def _build_preflight(self, path: str, origin: str | None) -> dict:
               cors = self._build_cors_headers(path, origin)
               if not cors:
                   # Origin không match whitelist → response empty preflight, browser block.
                   return {"status": 200, "headers": []}
               cors["access-control-allow-methods"] = "GET, POST, OPTIONS, DELETE"
               cors["access-control-allow-headers"] = "Authorization, Content-Type, Mcp-Session-Id, X-API-Key"
               cors["access-control-max-age"] = "86400"
               return {"status": 200, "headers": list(cors.items())}

           async def _send_response(self, response: dict, send):
               headers = [(k.encode("ascii"), v.encode("ascii")) for k, v in response["headers"]]
               await send({"type": "http.response.start", "status": response["status"], "headers": headers})
               await send({"type": "http.response.body", "body": b"", "more_body": False})

           def __getattr__(self, name):
               return getattr(self._app, name)
       ```

    3. **Xoá** hàm `_add_cors_middleware` cũ (line ~820). **Thay** 2 call site:
       - Line ~676 (subdomain mode):
         ```python
         if not prefix:
             # Subdomain mode: 1 level inner — wrap MultiPolicyCORSMiddleware.
             settings = get_settings()
             inner = _MultiPolicyCORSMiddleware(inner, settings.mcp_oauth_sensitive_allowed_origins)
             # ... existing _BasicAuthFormShim wrap
             return _BasicAuthFormShim(inner)
         ```
       - Line ~767 (path-prefix mode wrapper):
         ```python
         settings = get_settings()
         wrapper = _MultiPolicyCORSMiddleware(wrapper, settings.mcp_oauth_sensitive_allowed_origins)
         ```
         Lưu ý: `_MultiPolicyCORSMiddleware` là ASGI wrapper (giống `_BasicAuthFormShim`),
         KHÔNG phải Starlette `add_middleware`. Wrap thay vì add → đảm bảo
         exécution order TRƯỚC inner SDK router.

    **C. Test `tests/test_cors_split_policy.py` (mới):**
    - Boot 2 mode app (subdomain + path-prefix) qua fixture `monkeypatch.setenv("MCP_PATH_PREFIX", ...)`.
    - 6 case như behavior block trên.
    - Dùng `starlette.testclient.TestClient` synchronous (KHÔNG cần async — request preflight đơn giản).
    - Assert presence/absence + value của header `access-control-allow-origin` qua `response.headers.get("access-control-allow-origin")`.

    **D. `tests/test_path_prefix_wrapper.py` patch:** grep test cũ xem có assert
    `Access-Control-Allow-Origin: *` cho /token / /authorize không. Nếu có → đổi
    sang assert whitelist behavior (origin claude.ai). Nếu test cũ chỉ test
    metadata + transport (chưa test /token) → KHÔNG đụng.

    **E. Regression:**
    - `cd Hub_All/mcp_service && uv run pytest -v` toàn suite mcp_service PASS.
    - `cd Hub_All/api && uv run pytest -m critical -q` không regression (Plan 10-04 KHÔNG đụng api/).
    - Manual: docker compose up + curl OPTIONS /token với Origin malicious → response không có ACAO header (verify CRIT-01 đóng).

    **F. Documentation:** Thêm comment block 5 dòng đầu hàm `_MultiPolicyCORSMiddleware` cite audit `08.3-MCP-AUDIT-2026-05-21.md` CRIT-01 + ngày fix.
  </action>
  <verify>
    <automated>cd Hub_All/mcp_service && uv run pytest tests/test_cors_split_policy.py tests/test_path_prefix_wrapper.py -v && uv run pytest -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    6 test PASS file mới + toàn suite mcp_service không regression; ruff + mypy --strict PASS các file sửa; `_add_cors_middleware` cũ đã xoá hoàn toàn; 2 call site mới dùng `_MultiPolicyCORSMiddleware`; setting `mcp_oauth_sensitive_allowed_origins` parse env comma-separated OK; manual curl OPTIONS /token với Origin malicious không có ACAO header.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser tab (origin lạ) → MCP /token | CRIT-01 fix: chặn tại CORS middleware — origin không trong whitelist → KHÔNG có ACAO header → browser block request |
| Cross-origin server-to-server fetch (KHÔNG Origin header) | Pass-through — middleware không inject CORS header khi không có Origin (server fetch không cần CORS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | Malicious tab/extension gọi /token từ origin lạ | mitigate | CORS sensitive whitelist (claude.ai + inspector.modelcontextprotocol.io + localhost dev) — origin không match → KHÔNG echo ACAO → browser block |
| T-10-04-02 | Spoofing | Malicious server fake Origin header trong server-to-server fetch | accept | Server fetch KHÔNG bị browser SOP enforce — CORS chỉ relevant cho browser client. Origin header malicious chỉ ảnh hưởng CORS layer; auth/PKCE/client_id mismatch (CRIT-03 đã đóng) là defense thật |
| T-10-04-03 | Tampering | Ops thêm origin malicious vào whitelist | accept | Setting `mcp_oauth_sensitive_allowed_origins` env-driven — ops trách nhiệm. Document trong DEPLOY.md (Plan 10-05) hướng dẫn whitelist tối thiểu |
| T-10-04-04 | Denial of Service | Preflight flood để probe whitelist | accept | Rate limit Caddy/Cloudflare (Phase 8.3 đã có) — defer hardening v4.0 |
</threat_model>

<verification>
- 6 test PASS file mới `test_cors_split_policy.py`
- Toàn suite mcp_service KHÔNG regression
- Manual: curl OPTIONS /token với Origin lạ → KHÔNG có ACAO header
- Metadata endpoint vẫn trả ACAO `*` (RFC 8414 §3.1 compliance)
- `ruff` + `mypy --strict` PASS
</verification>

<success_criteria>
1. `_add_cors_middleware` cũ đã xoá. Thay bằng `_MultiPolicyCORSMiddleware` ASGI wrapper.
2. Setting `mcp_oauth_sensitive_allowed_origins` default 4 origin whitelist + env override comma-separated.
3. Metadata path (5 route /.well-known/*) → ACAO `*` (RFC 8414 §3.1 + 9728 §3.1).
4. Sensitive path (/token, /authorize, /revoke, /register, /mcp[/*]) → ACAO echo whitelist match origin, KHÔNG match → KHÔNG echo.
5. 6 test critical case PASS — cover cả 2 mode (subdomain + path-prefix).
6. Suite mcp_service không regression sau fix.
7. CRIT-01 (Phase 8.3 audit) closed — document mapping trong SUMMARY.
</success_criteria>

<output>
Tạo `10-04-SUMMARY.md` ghi:
- Số test PASS (6/6 mới + suite tổng X/Y)
- Mapping CRIT-01 audit → fix (cite line audit file + commit hash)
- Sample curl output manual verify (3 case: metadata `*`, sensitive whitelist match, sensitive whitelist reject)
- Decision approach B (custom middleware) thay vì 2 Starlette CORSMiddleware (lý do: subdomain mode chỉ 1 level)
- Deviation nếu có (vd test cũ phải patch — list rõ)
</output>
