---
phase: 10-hardening-observability-docs
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - Hub_All/api/tests/integration/test_critical_path_coverage.py
  - Hub_All/api/tests/integration/conftest_hardening.py
autonomous: true
requirements: [HARD-03]
must_haves:
  truths:
    - "5 integration test critical path PASS trên testcontainers Postgres pgvector pg16 + Redis 7"
    - "Coverage critical path ≥50% — đo bằng pytest-cov trên module {auth, hubs, users, documents, search, ask}"
    - "Test chạy ổn định trên CI GitHub Actions (Plan 10-06) — không flaky"
    - "Mỗi test marker @pytest.mark.critical + @pytest.mark.integration"
    - "Test KHÔNG mock Postgres/Redis (real testcontainers per Plan 04-06/05-06 pattern)"
  artifacts:
    - path: "Hub_All/api/tests/integration/test_critical_path_coverage.py"
      provides: "5 test critical path: auth happy + hub isolation + ingest VN filename + search hub filter + ask citation parsing"
      contains: "@pytest.mark.critical"
    - path: "Hub_All/api/tests/integration/conftest_hardening.py"
      provides: "Shared fixture coverage report (nếu cần custom override conftest Phase 3)"
  key_links:
    - from: "test_critical_path_coverage.py"
      to: "app_with_auth fixture"
      via: "pytest fixture reuse"
      pattern: "app_with_auth"
    - from: "test_critical_path_coverage.py"
      to: "5 module {auth/hubs/users/documents/search/ask}"
      via: "HTTP request qua AsyncClient → coverage instrument tự đo"
      pattern: "POST /api/(auth|hubs|users|documents|search|ask)"
---

<objective>
Tổng hợp 5 integration test critical path đáp ứng HARD-03 (≥50% coverage critical
path module + CI gate). Tận dụng tối đa test đã có ở các phase trước (Plan 04-06
test_ingest_e2e, Plan 05-06 test_hub_isolation, Plan 06-04 test_search_hub_isolation,
Plan 07-05 test ask integration, Plan 09-05 test_eval_pipeline) — Plan 10-03 chỉ
THÊM test còn THIẾU + gom thành 1 file critical-path-suite hợp nhất + đo coverage.

**Purpose:** HARD-03 yêu cầu "integration test suite ≥50% critical path coverage,
pytest + testcontainers Postgres + Redis, CI gate". Phase 3-9 đã có nhiều test
critical riêng từng module — Plan 10-03 KHÔNG viết lại mà:
1. Audit test critical hiện hữu (grep `@pytest.mark.critical` toàn `tests/`)
2. Map vào 5 nhóm HARD-03: auth happy + hub isolation + ingest VN filename +
   search hub filter + ask citation parsing
3. Bổ sung test còn THIẾU vào `test_critical_path_coverage.py` mới
4. Cấu hình `pytest-cov` đo coverage chỉ trên 6 module critical path
5. CI gate (Plan 10-06): `pytest -m critical -m integration --cov=app.auth --cov=app.routers --cov=app.services --cov-fail-under=50`

**Output:**
- `tests/integration/test_critical_path_coverage.py` — 5 test (1 mỗi nhóm) ĐẢM BẢO
  coverage gate ≥50%, ưu tiên test mới cho gap nếu có (KHÔNG duplicate test
  đã có ở phase trước).
- `tests/integration/conftest_hardening.py` — fixture chia sẻ (nếu cần — nhiều khả
  năng reuse `conftest.py` Phase 3 + `conftest_eval.py` Phase 9, file mới chỉ
  thêm fixture override nếu thiếu).
- `pyproject.toml` patch `[tool.pytest.ini_options]` để gate coverage qua CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/.planning/PROJECT.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/CONVENTIONS.md
@Hub_All/api/pyproject.toml
@Hub_All/api/tests/conftest.py

<interfaces>
<!-- Fixture pattern tái dùng. Plan 10-03 KHÔNG viết lại — chỉ tham chiếu. -->

Hub_All/api/tests/conftest.py (Phase 3-5):
- `app_with_auth` — async fixture boot full app + testcontainers Postgres + Redis
  + TRUNCATE CASCADE per test + audit_service.reset_queue + alembic upgrade head.
  Đây là fixture LẶP qua >1 test → cần `COCOINDEX_SKIP_SETUP=1` cho test không
  cần ingest flow, hoặc `_cocoindex_env` session-scoped cho test cần cocoindex
  (pattern test_ingest_e2e.py).

Hub_All/api/tests/integration/test_eval_pipeline.py (Plan 09-05 mới nhất):
- Đã có 3 test critical mới (upload+search pipeline + scanned PDF + hub isolation).
- Pattern shadow conftest: override `app_with_auth` per file + `_cocoindex_env`
  session-scoped DEF-05-01.

Test critical hiện hữu (grep `@pytest.mark.critical` trên tests/):
- Phase 3: test_login_envelope, test_jwt_compat, test_rbac, test_refresh_race, test_pkcs8
- Phase 4: test_e2e_upload_docx_to_chunks_completed, test_e2e_pdf_scanned_failed_unsupported, test_e2e_content_hash_incremental_dedup
- Phase 5: test_hub_isolation (6 test E4)
- Phase 6: test_search_hub_isolation (6 test E4)
- Phase 7: 11 critical test (citation + anti-injection prompt + cross-hub + hot-swap + 10 ask)
- Phase 8: 2 test critical (golden path API + VN filename UTF-8)
- Phase 9: 3 test smoke regression (upload+search pipeline + scanned + hub-no-leak)

Tổng: ~37 test critical hiện hữu — Plan 10-03 KHÔNG cần viết thêm để đạt ≥50%.
File `test_critical_path_coverage.py` đóng vai PRIMARY GATE — gom 5 test acceptance
crisp (mỗi REQ HARD-03 acceptance line 1 test mapping) thay vì rải rác 37 test
phải scroll qua nhiều phase.
</interfaces>

<scope_clarification>
HARD-03 acceptance line:
> "Integration test suite ≥50% critical path coverage (pytest + testcontainers
> Postgres + Redis chạy CI GitHub Actions PASS). Test cho auth happy + hub
> isolation + ingest VN filename + search hub filter + ask citation parsing."

→ 5 nhóm test, **mỗi nhóm 1 acceptance test crisp**. KHÔNG phải viết test mới
cho mỗi tính năng (Phase 3-9 đã làm). Plan 10-03 LÀ test mới CHỈ để có 1 file
duy nhất `test_critical_path_coverage.py` Phase 10 cite được trong DEPLOY.md +
README (HARD-04) làm "đây là smoke test coverage gate, chạy `pytest -m critical
tests/integration/test_critical_path_coverage.py` để verify M2 production-ready".

Mỗi test trong file = 1 acceptance test cấp suite (smaller scope than full Phase
test):
- T1 auth happy: POST /login → 200 + envelope + JWT decode OK
- T2 hub isolation: editor Hub A PATCH doc Hub B → 403
- T3 ingest VN: POST /upload "Khám bệnh đa khoa.docx" → status=completed +
  filename UTF-8 preserved
- T4 search hub filter: viewer Hub A POST /api/search hub_ids=[B] → 0 result
  hoặc 403
- T5 ask citation: POST /api/ask mock LLM trả "ans [1] [2]" → citations[0].
  chunk_id khớp chunks[0]
</scope_clarification>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viết 5 acceptance test critical path + pytest-cov dep + coverage gate</name>
  <files>
    Hub_All/api/tests/integration/test_critical_path_coverage.py (mới)
    Hub_All/api/tests/integration/conftest_hardening.py (mới — minimal)
    Hub_All/api/pyproject.toml (đã có pytest-cov>=5,<6 — verify)
  </files>
  <behavior>
    Test 1 (auth happy): POST /api/auth/login với admin seed → 200 + envelope `{success:true, data:{access_token, refresh_token, user}, error:null}` + JWT decode được bằng JWTManager + user.role == "admin".
    Test 2 (hub isolation): seed 2 hub + 2 doc + editor user assigned hub-A → login editor → DELETE /api/documents/<doc_hub_b_id> → 403 FORBIDDEN + audit_logs có entry `action='security.hub_isolation_violation'` + doc Hub B vẫn tồn tại.
    Test 3 (ingest VN): login admin → POST /api/documents/upload với file giả lập tên "Khám bệnh đa khoa.docx" (PHẢI dùng filename byte UTF-8 trong multipart) → reconcile race (Plan 04-08) → poll status='completed' chunk_count>0 → GET /api/documents/<id> trả `filename` đúng UTF-8 (so byte với original — KHÔNG mojibake).
    Test 4 (search hub filter): viewer Hub A login → POST /api/search body `{query, hub_ids:[hub_b_id], top_k:5}` → KHÔNG nhận chunk Hub B (test SC2 phase 6 — viewer intersect hub_ids ∩ user_hubs = empty → 0 result hoặc 403). Test assert `results == []` HOẶC status 403 — chấp nhận cả hai (D-06 Phase 6 SEARCH-03 defense in depth).
    Test 5 (ask citation): mock litellm.acompletion trả "Theo tài liệu, A là B [1]. Còn C [2]." → POST /api/ask với mock chunks Phase 7 fixture → response `citations[0].chunk_id == chunks[0].id` + `citations[1].chunk_id == chunks[1].id`.
  </behavior>
  <action>
    **A. Audit fixture hiện hữu:**
    - Đọc `tests/conftest.py` xác định `app_with_auth` signature + dependencies.
    - Đọc `tests/integration/conftest_eval.py` (Plan 09-05) xem có fixture nào tái dùng được (admin_token, eval_hub_seeded, _deterministic_embedding).
    - Quyết định: tái dùng `app_with_auth` Phase 3 + thêm 2 fixture mới ở `conftest_hardening.py`:
      - `seeded_two_hubs_and_editor` — seed 2 hub (A, B), 2 doc (1/hub), 1 editor user assigned hub-A → trả tuple (hub_a_id, hub_b_id, doc_a_id, doc_b_id, editor_token).
      - `mock_litellm_citation_response` — monkeypatch `litellm.acompletion` trả response mock với marker `[1] [2]` để Test 5 deterministic (KHÔNG cần OpenAI API key).

    **B. `tests/integration/conftest_hardening.py`:**
    ```python
    """Fixture cho Plan 10-03 critical-path acceptance test.

    Tái dùng tối đa fixture conftest.py + conftest_eval.py. CHỈ thêm fixture
    đặc thù 5 acceptance test.
    """
    import pytest
    import pytest_asyncio
    # ... (impl 2 fixture trên — pattern khớp conftest_eval.py Plan 09-05)
    ```

    **C. `tests/integration/test_critical_path_coverage.py`:**
    1. Marker: `pytestmark = [pytest.mark.critical, pytest.mark.integration, pytest.mark.asyncio]` cho mọi test.
    2. 5 test function độc lập (KHÔNG share state — mỗi test self-contained, TRUNCATE CASCADE giữa các test do `app_with_auth` lifespan).
    3. Mỗi test có docstring trỏ về REQ-ID + Phase tham chiếu:
       ```python
       async def test_critical_auth_happy_path(app_with_auth, admin_seed_user):
           """HARD-03 acceptance: AUTH-01 happy path login.

           Smoke gate cho M2 production-ready — đầy đủ contract envelope + JWT
           decode. Test chi tiết: Phase 3 test_login_envelope (12 case).
           """
           ...
       ```
    4. Test 3 (VN filename) — dùng helper:
       ```python
       vn_filename = "Khám bệnh đa khoa.docx"
       files = {"file": (vn_filename, file_bytes, "application/vnd.openxmlformats-officedocument.wordprocessingml.document")}
       # multipart httpx encoding UTF-8 đúng spec RFC 7578
       ```
       Verify byte-identical: `assert response_json["data"]["filename"] == vn_filename`.

    5. Test 5 (ask citation) — monkeypatch litellm:
       ```python
       async def _mock_acompletion(*args, **kwargs):
           return {
               "choices": [{"message": {"content": "Câu trả lời mock [1] [2]."}}],
               "usage": {"prompt_tokens": 100, "completion_tokens": 20, "total_tokens": 120},
               "model": "gpt-4o-mini",
           }
       monkeypatch.setattr("litellm.acompletion", _mock_acompletion)
       ```

    **D. `pyproject.toml`** — verify `pytest-cov>=5,<6` đã có trong `dev` deps (đã có). KHÔNG cần thêm — chỉ verify.

    **E. Coverage CLI gate (sẽ wire CI Plan 10-06):**
    Doc trong SUMMARY: lệnh chạy local verify gate:
    ```bash
    cd Hub_All/api && uv run pytest -m critical tests/integration/test_critical_path_coverage.py \
        --cov=app.auth --cov=app.routers.auth \
        --cov=app.routers.hubs --cov=app.routers.documents \
        --cov=app.routers.search --cov=app.routers.ask \
        --cov=app.services.documents_service --cov=app.services.search_service \
        --cov=app.services.ask_service --cov=app.repositories.hub_isolation \
        --cov-report=term-missing --cov-fail-under=50
    ```

    **Coverage scope CHỈ critical path** (KHÔNG bao toàn app — defer v4.0). Lý do:
    - HARD-03 nói "≥50% critical path" — không phải ≥50% toàn codebase
    - Module list cố định trong CLI args (10 module) — set rõ ràng dễ verify
    - Coverage tổng codebase có thể thấp hơn nhưng critical path đảm bảo

    **F. Lint + regression:**
    - `ruff check tests/integration/test_critical_path_coverage.py tests/integration/conftest_hardening.py` PASS.
    - `pytest -m critical -m integration tests/integration/test_critical_path_coverage.py -v` 5/5 PASS.
    - Toàn suite `pytest -m critical` Phase 3-9 KHÔNG regression.

    **G. KHÔNG đụng test cũ:** Plan 10-03 thêm 5 test mới. Test critical Phase 3-9 hiện hữu giữ nguyên (chúng là detail-level test còn 5 test mới là suite-level acceptance gate).
  </action>
  <verify>
    <automated>cd Hub_All/api && uv run pytest -m critical -m integration tests/integration/test_critical_path_coverage.py -v && uv run pytest -m critical -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    5 acceptance test PASS; toàn bộ `pytest -m critical` KHÔNG regression; coverage CLI lệnh ở section E chạy thành công với `--cov-fail-under=50` PASS (≥50% critical path coverage); ruff + mypy --strict PASS các file mới; lệnh CLI coverage ghi vào SUMMARY để Plan 10-06 CI dùng.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Real Postgres testcontainer | Test seed admin + viewer + editor → verify RBAC + hub isolation enforce repo layer |
| Test → mocked litellm | Test 5 mock LLM response để deterministic citation parsing — KHÔNG dùng OpenAI key thật |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | Test seed editor escalate sang admin role | mitigate | Fixture `seeded_two_hubs_and_editor` ép `role='editor'` + assert role TRƯỚC khi run test body. Hub isolation enforce repo layer (Phase 5 E4) |
| T-10-03-02 | Information Disclosure | Test log chứa raw password (seed admin) | mitigate | Fixture seed password qua `hash_password` Argon2 — KHÔNG log raw + KHÔNG commit fixture file có password plaintext |
| T-10-03-03 | Repudiation | Coverage gate bị bypass | mitigate | CI gate (Plan 10-06) require `--cov-fail-under=50` — exit code != 0 nếu fail. Plan 10-06 wire vào GitHub Actions workflow |
</threat_model>

<verification>
- 5 acceptance test PASS
- `pytest -m critical` toàn suite KHÔNG regression
- Coverage CLI `--cov-fail-under=50` PASS với module list ở section E
- `ruff check` + `mypy --strict` PASS các file mới
</verification>

<success_criteria>
1. `tests/integration/test_critical_path_coverage.py` tồn tại với 5 test critical (1 mỗi nhóm HARD-03 acceptance line).
2. 5 test PASS trên testcontainers Postgres pgvector pg16 + Redis 7.
3. Coverage gate `--cov-fail-under=50` PASS trên 10 module critical path liệt kê.
4. Toàn bộ `pytest -m critical` Phase 3-9 KHÔNG regression.
5. Mỗi test có docstring rõ HARD-03 mapping + tham chiếu Phase detail test.
6. Vietnamese filename test (Test 3) preserve byte-identical UTF-8 (so với P15 mitigation Phase 8).
</success_criteria>

<output>
Tạo `10-03-SUMMARY.md` ghi:
- Số test PASS (5/5) + coverage % thực đo
- Lệnh coverage CLI đã verify local (paste vào SUMMARY để Plan 10-06 CI tham chiếu)
- Module list coverage scope (10 module — set cứng cho gate)
- Mapping 5 test → 5 acceptance line HARD-03
- Decision nếu có (vd dùng mock litellm cho Test 5 thay vì OpenAI key thật)
- Deviation nếu có
</output>
