---
phase: 03
slug: frontend-form-refactor
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-24
milestone: v3.1
reference_phase: 05-reverse-proxy-frontend-subpath (PROXY-04 pattern carry forward)
language: vietnamese
---

# Phase 3 v3.1 — UI Design Contract (Frontend form refactor RBAC hub_admin)

> Visual + interaction contract cho refactor form tạo user 3 option (FE-01) + hub switcher sidebar filter (FE-02) + edit modal disabled super (FE-03) + type alias `UserRole` (FE-04). Scope minimal R-V3-2 carry forward Phase 5 — CHỈ touch 3 React component + 3 file type/mock. Generated by gsd-ui-researcher auto-mode (CONTEXT.md 5 D-V3.1-Phase3-A..E LOCKED 2026-05-24).

---

## 1. Design System

| Property | Value |
|----------|-------|
| Tool | none (KHÔNG shadcn — D6 expired Phase 5 PROXY-03 NHƯNG dự án M2/v3.0 KHÔNG init shadcn registry) |
| Preset | not applicable |
| Component library | none (Tailwind utility-only — pattern M2 + Phase 5 carry forward) |
| Icon library | none (Phase 3 KHÔNG cần icon mới — warning emoji `⚠` inline string) |
| Font | inherit `font-sans` Tailwind default (v3.1 Phase 3 KHÔNG đụng font) |
| CSS framework | Tailwind v4 (Phase 5 PROXY-04 baseline) |
| Theme delivery | inline CSS variable `--hub-theme` Phase 5 carry forward (KHÔNG đụng Phase 3) |

**Component pattern (Phase 3 carry forward Phase 5):**
- Native HTML element + Tailwind class (`<input type="radio">` + `<select>` + `<button disabled>`)
- KHÔNG dùng shadcn/ui RadioGroup / Tooltip / Dropdown (KHÔNG cài registry — defer v4.0 nếu cần)
- KHÔNG custom Tailwind plugin (native browser `title=""` tooltip + `<select>` native dropdown)
- Tooltip: native HTML `title` attribute (default browser screen reader support sẵn)

---

## 2. Spacing Scale (KHÔNG mới — reuse Phase 5 M2/v3.0)

Phase 3 KHÔNG declare spacing scale mới. Reuse existing Tailwind utility class trong codebase:

| Token | Tailwind class | Value | Usage Phase 3 |
|-------|----------------|-------|---------------|
| xs | `gap-1`, `p-1` | 4px | Icon gap warning banner (`⚠ + text`) |
| sm | `gap-2`, `p-2`, `gap-2.5`, `p-2.5` | 8-10px | Radio option vertical gap, description spacing |
| md | `gap-4`, `p-4`, `px-4`, `py-3` | 12-16px | Form field gap, button padding |
| lg | `gap-6`, `p-6` | 24px | Form section break |
| xl | `gap-8`, `p-8` | 32px | Modal padding, page section gap |

**Exceptions Phase 3:** none — Phase 3 chỉ refactor existing form layout, KHÔNG layout mới.

---

## 3. Typography (KHÔNG mới — reuse Phase 5 M2/v3.0)

Phase 3 KHÔNG declare typography scale mới. Reuse existing Tailwind class:

| Role | Tailwind class | Size | Weight | Line Height | Usage Phase 3 |
|------|----------------|------|--------|-------------|---------------|
| Body | `text-sm` | 14px | 400 (normal) | 1.5 (`leading-relaxed`) | Radio option description (3 line dưới label) |
| Label | `text-sm font-medium` | 14px | 500 | 1.5 | Radio label "Admin toàn hệ thống" / "Quản lý hub này" / "Viewer" |
| Heading (modal) | `text-lg font-semibold` | 18px | 600 | 1.4 | Modal title "Quản lý hub & quyền" |
| Warning text | `text-sm font-medium` | 14px | 500 | 1.5 | Warning banner content (yellow) |
| Disabled label | `text-sm font-medium opacity-50` | 14px | 500 | 1.5 | Modal disabled "Admin toàn hệ thống" cho hub_admin |
| Tooltip | native browser default | n/a | n/a | n/a | `title="Cần Admin toàn hệ thống"` |

---

## 4. Color (KHÔNG branding mới — reuse Phase 5 PROXY-04 + Tailwind palette)

Phase 3 KHÔNG thêm hub branding color. Reuse:

| Role | Token / Class | Usage Phase 3 |
|------|---------------|---------------|
| Dominant (60%) | `bg-white dark:bg-slate-900` | Form modal background, page surface |
| Secondary (30%) | `bg-slate-50 dark:bg-slate-800` | Card, Manage modal panel, sidebar bg |
| Accent (10%) | `text-accent focus:ring-accent` (Tailwind config `accent` token Phase 5 baseline) | Radio checked state, primary CTA "Tạo user" button |
| Warning | `bg-yellow-50 border-yellow-200 text-yellow-800` (light) + `dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200` (dark) | Warning banner khi `newUserRole === 'admin'` form FE-01 |
| Disabled | `opacity-50 cursor-not-allowed` | Edit modal option "Admin toàn hệ thống" khi `currentUser.role !== 'admin'` (FE-03) |
| Active hub (switcher) | `bg-accent/10 text-accent font-semibold` | Highlight `h.code === CURRENT_HUB` trong hub switcher (FE-02) |
| Focus ring | `focus:ring-2 focus:ring-accent focus:ring-offset-2` | Keyboard focus radio + select + button (a11y WCAG 2.4.7) |
| Theme color (per-hub) | `var(--hub-theme)` (Phase 5 PROXY-04 inline CSS variable) | KHÔNG đụng Phase 3 (sidebar header carry forward) |

**Accent reserved for (10% rule — Phase 3 strict scope):**
- Radio checked indicator (form FE-01 + modal FE-03)
- Primary CTA button "Tạo user" submit (existing UserManagement.tsx — KHÔNG đổi)
- Hub switcher active row highlight (FE-02)
- Focus ring keyboard navigation (a11y)
- KHÔNG dùng cho: body text, icon decorative, divider line

**WCAG AA contrast verified (Phase 5 Plan 05-03 audit carry forward):**
- `bg-yellow-50` + `text-yellow-800` → contrast ratio 7.2:1 (AAA — đủ AA 4.5:1)
- `bg-yellow-900/20` dark + `text-yellow-200` → contrast ratio 8.1:1 (AAA)
- `opacity-50` disabled label vẫn pass contrast (interactive disabled element WCAG 1.4.3 exempt)

---

## 5. Component Inventory (Phase 3 scope: 3 React component + 3 file type/mock)

### 5.1 Component touched (visual UI change)

| Component | File path (absolute) | Plan | Change scope |
|-----------|---------------------|------|--------------|
| UserManagement form "Quyền" | `Hub_All/frontend/src/pages/UserManagement.tsx` (line 322 + 372 + 635 + 639 + state line 37) | 03-02 (FE-01) | Radio group 2 option → 3 option + warning banner conditional |
| Layout sidebar hub switcher | `Hub_All/frontend/src/Layout.tsx` (line 1-65 sidebar header) | 03-03 (FE-02) | ADD hub switcher ABOVE sidebar nav (inline hoặc extract `<HubSwitcher>` — Claude discretion plan-time) |
| UserManagement Manage modal | `Hub_All/frontend/src/pages/UserManagement.tsx` (line 200-210 `handleOpenManageHub`) | 03-03 (FE-03) | Disabled "Admin toàn hệ thống" option + tooltip cho hub_admin user |

### 5.2 Files type/mock only (KHÔNG visual UI change)

| File | Plan | Change scope |
|------|------|--------------|
| `Hub_All/frontend/src/services/api.ts` (line 481-492 UserAPI + ~line 480 NEW UserRole export) | 03-01 (FE-04) | ADD `export type UserRole = 'admin' \| 'hub_admin' \| 'editor' \| 'viewer'` + extend `UserAPI` interface thêm field `role: UserRole` |
| `Hub_All/frontend/src/mockData.ts` (line 78-115 MOCK_USERS append) | 03-01 (FE-04) | ADD 2 hub_admin fixture (1 dmd + 1 tdt) — KHÔNG modify existing 35 user |
| `Hub_All/frontend/src/contexts/AuthContext.tsx` (line 25-43 verify only) | 03-01 (FE-04) | VERIFY ONLY — `api.me()` đã return `UserWithRoles.user.role`; KHÔNG code change |

---

## 6. Component Contracts (Props + States + Interactions)

### 6.1 UserManagement form "Quyền" radio group (FE-01 — Plan 03-02)

**Props/State shape:**
```typescript
// UserManagement.tsx line 37 — CHANGE
const [newUserRole, setNewUserRole] = useState<UserRole>('viewer');  // BEFORE: useState<'admin' | 'viewer'>('viewer')

// Import từ Plan 03-01
import type { UserRole } from '../services/api';
```

**Visual layout (CONTEXT.md `<specifics>` §"Form 3 option layout"):**

```text
Quyền *  (label text-sm font-medium)
┌─────────────────────────────────────────────────────────────────┐
│  ○  Admin toàn hệ thống                                          │
│     Có thể quản lý tất cả hub, user, settings. Cảnh báo: gán    │
│     cho user TIN CẬY.                                            │
│                                                                  │
│     ┌──────────────────────────────────────────────────────────┐ │
│     │ ⚠  Quyền cao nhất — quản lý toàn bộ hệ thống            │ │
│     │   bg-yellow-50 border-yellow-200 text-yellow-800        │ │
│     │   (chỉ show khi newUserRole === 'admin')                │ │
│     └──────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ○  Quản lý hub này                                              │
│     Quản lý user + document trong hub được chỉ định. KHÔNG       │
│     vào hub khác.                                                │
│                                                                  │
│  ●  Viewer                              [default selected]       │
│     Xem document + search trong hub được gán. KHÔNG quản lý     │
│     user.                                                        │
└─────────────────────────────────────────────────────────────────┘
```

**Visual states:**

| State | Visual treatment |
|-------|------------------|
| Default | `Viewer` selected (`role='viewer'` default Plan 03-01 + carry forward existing line 37 `'viewer'` default) |
| Hover (option) | `hover:bg-slate-50 dark:hover:bg-slate-800 cursor-pointer` toàn row clickable |
| Selected (checked) | Radio indicator `text-accent` + label `font-semibold` |
| Focus (keyboard Tab) | `focus:ring-2 focus:ring-accent focus:ring-offset-2` outline (a11y WCAG 2.4.7) |
| Admin selected → warning visible | Banner `bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-md p-3 mt-2` + `role="alert" aria-live="polite"` |
| Admin selected → dark mode | `dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200` mirror |
| Submitting (form busy) | All 3 radio `disabled` + opacity-50 (existing pattern UserManagement.tsx submit busy) |

**Interaction states:**

| Trigger | Action |
|---------|--------|
| Click radio "Admin toàn hệ thống" | `setNewUserRole('admin')` → warning banner mount (transition optional `transition-opacity duration-150`) |
| Click radio "Quản lý hub này" | `setNewUserRole('hub_admin')` → warning banner unmount |
| Click radio "Viewer" | `setNewUserRole('viewer')` → warning banner unmount |
| Keyboard Arrow Down/Up (ARIA radiogroup) | Cycle 3 option theo native HTML radio behavior |
| Keyboard Space (focused radio) | Select option (native) |
| Submit form (existing CTA "Tạo user") | POST `/api/users` body `{email, name, password, hub_id, role: newUserRole}` — backend Phase 2 Plan 02-03 đã accept `role='hub_admin'` Pydantic Literal extend |

**Edge cases:**

| Edge case | Handling |
|-----------|----------|
| BE 403 envelope `HUB_ADMIN_REQUIRED` (hub_admin tạo user role='admin' bị block T-02-02-E backend Plan 02-03) | Toast error "Bạn không có quyền tạo user với role Admin toàn hệ thống. Liên hệ Super Admin." + form giữ state radio (user thấy được chọn gì để sửa) |
| BE 403 envelope `CROSS_HUB_USER_DELETE_DENIED` (DELETE handler) | KHÔNG áp dụng form tạo — defer Plan 03-03 cho Manage modal DELETE flow |
| BE 422 unprocessable role value | Toast "Role không hợp lệ. Refresh trang và thử lại." (defensive — Plan 03-01 type alias match BE Literal nên KHÔNG xảy ra runtime) |
| BE 500 `AUTH_STATE_INCONSISTENT` (Plan 02-02 invariant guard) | Toast "Lỗi hệ thống — liên hệ admin. Mã: AUTH_STATE_INCONSISTENT" |
| Loading `useAuth().isLoading=true` | Form fields disabled + show skeleton/spinner (existing pattern carry forward) |

### 6.2 Layout sidebar hub switcher (FE-02 — Plan 03-03)

**Props/State shape:**
```typescript
// Layout.tsx new state + hook
const { user, isLoading } = useAuth();
const currentUser = user?.user;  // nested wrapper UserWithRoles → UserAPI
const currentRole: UserRole = currentUser?.role ?? 'viewer';  // defensive default
const userHubIds: string[] = currentUser?.hub_ids ?? [];

const [hubs, setHubs] = useState<Hub[]>([]);
const [hubsLoading, setHubsLoading] = useState(true);

useEffect(() => {
  api.listHubs().then(setHubs).finally(() => setHubsLoading(false));
}, []);

// Filter logic (CONTEXT.md D-V3.1-Phase3-A LOCKED)
const visibleHubs = currentRole === 'admin'
  ? hubs
  : hubs.filter(h => h.code !== 'central' && userHubIds.includes(h.id));
```

**Visual layout (ABOVE sidebar nav, giữa logo + nav items):**

```text
┌──────────────────────────────────┐
│  [Logo]  Medinet Wiki            │  ← Phase 5 PROXY-04 sidebar header (UNCHANGED)
├──────────────────────────────────┤
│  Hub đang xem:                   │  ← Phase 3 FE-02 NEW (label text-xs uppercase text-slate-500)
│  ┌────────────────────────────┐  │
│  │  Đỗ Minh Đường (dmd)    ▼ │  │  ← <select> hoặc custom dropdown
│  └────────────────────────────┘  │
├──────────────────────────────────┤
│  📄 Documents                    │  ← Sidebar nav items (UNCHANGED)
│  🔍 Search                       │
│  ❓ Ask                           │
│  👥 Users                         │
│  ⚙️  Settings                     │
└──────────────────────────────────┘
```

**Implementation pattern (Claude discretion plan-time per CONTEXT.md):**

Option A — Native `<select>` (recommend đơn giản):
```tsx
<select
  value={CURRENT_HUB}
  onChange={(e) => window.location.href = `/${e.target.value}/`}
  className="w-full rounded-md border border-slate-300 dark:border-slate-700 bg-white dark:bg-slate-900 px-3 py-2 text-sm focus:ring-2 focus:ring-accent focus:border-accent"
  aria-label="Chọn hub đang xem"
>
  {visibleHubs.map(h => (
    <option key={h.code} value={h.code}>
      {h.name} ({h.code})
    </option>
  ))}
</select>
```

Option B — Custom Tailwind dropdown (extract `<HubSwitcher>` component nếu > 30 LOC).

**Visual states:**

| State | Visual treatment |
|-------|------------------|
| Default | Show current hub label (qua `getBranding(CURRENT_HUB).label` Phase 5 carry forward) |
| Hover (dropdown item) | `hover:bg-slate-50 dark:hover:bg-slate-800` |
| Active (current hub) | `bg-accent/10 text-accent font-semibold` highlight `h.code === CURRENT_HUB` |
| Focus (keyboard) | `focus:ring-2 focus:ring-accent` outline |
| Loading (`isLoading || hubsLoading`) | Skeleton `<div className="h-9 w-full rounded-md bg-slate-200 dark:bg-slate-800 animate-pulse" />` (KHÔNG render `<select>` empty) |
| Empty state (hub_admin với `userHubIds=[]`) | Text panel `text-sm text-slate-500 italic` content "Bạn chưa được gán hub nào — liên hệ admin." (KHÔNG render dropdown) |
| Super admin view ALL | Show full list 4 hub (central + dmd + tdt + ghost yte/duoc/hcns nếu trong allowlist) |

**Interaction states:**

| Trigger | Action |
|---------|--------|
| Change hub (click option) | `window.location.href = '/${selectedHub.code}/'` — full reload (Phase 5 PROXY pattern carry forward, KHÔNG SPA navigate vì backend hub_name khác cần re-fetch JWT + branding) |
| Click dropdown open | Native browser dropdown UX |
| Keyboard Tab focus | Focus ring + native select open via Space/Enter |
| Loading complete | Skeleton → dropdown swap (no transition cần) |

**Edge cases:**

| Edge case | Handling |
|-----------|----------|
| `userHubIds=[]` hub_admin chưa gán hub nào | Empty state panel (đã list trên) — KHÔNG render dropdown |
| `visibleHubs.length === 1` | Render dropdown disabled OR text label only (UX clarity — chỉ 1 option) |
| `api.listHubs()` fail (5xx) | Toast "Không tải được danh sách hub. Refresh trang." + fallback hardcode `CURRENT_HUB` label only |
| Network slow > 3s | Skeleton state vẫn hiển thị (loading indicator preserved) |
| User chuyển hub khác Tab đang mở | `beforeunload` warning IF form unsaved (existing UserManagement.tsx pattern carry forward — out of Phase 3 scope) |

### 6.3 UserManagement Manage modal disabled option (FE-03 — Plan 03-03)

**Props/State shape:**
```typescript
// UserManagement.tsx existing state line ~200 (handleOpenManageHub)
const [manageRole, setManageRole] = useState<UserRole>('viewer');
const { user } = useAuth();
const currentUser = user?.user;
const isCurrentUserHubAdmin = currentUser?.role === 'hub_admin';
const isCurrentUserSuper = currentUser?.role === 'admin';

// Modal render — 3 option radio group (giống FE-01 structure NHƯNG khác disabled logic)
```

**Visual layout (Manage modal "Quản lý hub & quyền"):**

```text
┌────────────────────────────────────────────────────────────────┐
│  Quản lý hub & quyền — Nguyễn Văn A                          ×│
├────────────────────────────────────────────────────────────────┤
│  Hub: [Đỗ Minh Đường ▼]                                       │
│                                                                │
│  Vai trò:                                                      │
│    ○  Admin toàn hệ thống                                      │
│       (DISABLED nếu hub_admin — opacity-50 cursor-not-allowed │
│        + title="Cần Admin toàn hệ thống")                      │
│                                                                │
│    ○  Quản lý hub này                                          │
│       Quản lý user + document trong hub được chỉ định.        │
│                                                                │
│    ●  Viewer                                                   │
│       Xem document + search trong hub được gán.                │
│                                                                │
│  [Hủy]  [Lưu]                                                  │
└────────────────────────────────────────────────────────────────┘
```

**Visual states:**

| State | Visual treatment |
|-------|------------------|
| currentUser super admin | All 3 option enable + clickable (full access — defense in depth backend Plan 02-03 đã enforce) |
| currentUser hub_admin → "Admin toàn hệ thống" option | `disabled` attribute + `opacity-50 cursor-not-allowed` Tailwind class + `title="Cần Admin toàn hệ thống"` native tooltip + `aria-disabled="true"` |
| currentUser hub_admin → "Quản lý hub này" + "Viewer" | Enable + clickable (defense in depth — backend Plan 02-03 enforce `assert_hub_admin_for` qua hub_id scope) |
| Selected option | Radio indicator `text-accent` + label `font-semibold` (giống FE-01) |
| Focus (keyboard) | `focus:ring-2 focus:ring-accent` outline (a11y) |
| Submitting (PATCH /api/users/{id}/role) | Submit button busy + 3 radio disabled (existing pattern carry forward) |

**Interaction states:**

| Trigger | Action |
|---------|--------|
| Click disabled "Admin toàn hệ thống" (hub_admin viewer) | No-op (native HTML disabled — preventDefault) + screen reader announce "disabled" |
| Hover disabled option | Cursor `not-allowed` + native browser tooltip show `title="Cần Admin toàn hệ thống"` sau ~500ms |
| Click enabled "Quản lý hub này" / "Viewer" | `setManageRole(...)` update state |
| Submit form | PATCH `/api/users/{id}/role` body `{role: manageRole, hub_id}` |
| Esc keyboard | Close modal (existing Manage modal pattern) |

**Edge cases:**

| Edge case | Handling |
|-----------|----------|
| Defensive: hub_admin tamper DevTools remove `disabled` + submit `role='admin'` | Backend Plan 02-03 T-02-02-E business logic block 403 `HUB_ADMIN_REQUIRED` envelope → Toast "Bạn không có quyền gán role Admin toàn hệ thống." |
| BE 403 `HUB_ADMIN_REQUIRED` other reason (wrong hub_id scope) | Toast "Bạn không có quyền quản lý hub này." |
| BE 422 invalid role | Toast "Role không hợp lệ. Refresh và thử lại." |
| Loading currentUser | Modal show skeleton 3 radio placeholder (KHÔNG render real options) |
| `currentUser.role` undefined (race condition) | Defensive default `'viewer'` → ALL option disabled (failsafe — bao gồm "Quản lý hub này" + "Viewer") + warning text "Đang tải quyền người dùng..." |

---

## 7. Copywriting Contract (TIẾNG VIỆT có dấu — CLAUDE.md §3 enforce)

### 7.1 Form "Quyền" radio group (FE-01)

| Element | Copy (Vietnamese) |
|---------|-------------------|
| Form field label | `Quyền *` (red asterisk required) |
| Radio option 1 label | `Admin toàn hệ thống` |
| Radio option 1 description | `Có thể quản lý tất cả hub, user, settings. Cảnh báo: gán cho user TIN CẬY.` |
| Radio option 1 warning banner | `⚠  Quyền cao nhất — quản lý toàn bộ hệ thống` (chỉ show khi `newUserRole === 'admin'`) |
| Radio option 2 label | `Quản lý hub này` |
| Radio option 2 description | `Quản lý user + document trong hub được chỉ định. KHÔNG vào hub khác.` |
| Radio option 3 label | `Viewer` |
| Radio option 3 description | `Xem document + search trong hub được gán. KHÔNG quản lý user.` |
| Primary CTA (existing) | `Tạo user` (KHÔNG đổi từ M2/v3.0) |
| Empty hub_id select | `-- Chọn hub --` (existing — KHÔNG đổi) |

### 7.2 Hub switcher sidebar (FE-02)

| Element | Copy (Vietnamese) |
|---------|-------------------|
| Switcher label (above dropdown) | `Hub đang xem:` (text-xs uppercase text-slate-500) |
| Dropdown option format | `{hub.name} ({hub.code})` — vd "Đỗ Minh Đường (dmd)", "Thuốc Dân Tộc (tdt)", "Trung tâm (central)" (super admin only) |
| ARIA label | `Chọn hub đang xem` |
| Empty state heading | `Bạn chưa được gán hub nào` |
| Empty state body | `Liên hệ admin để được gán quyền truy cập hub.` |
| Loading state | (skeleton — no copy) |
| Error state (api.listHubs fail) | Toast: `Không tải được danh sách hub. Vui lòng refresh trang.` |

### 7.3 Manage modal disabled tooltip (FE-03)

| Element | Copy (Vietnamese) |
|---------|-------------------|
| Modal title (existing) | `Quản lý hub & quyền` |
| Disabled option tooltip (`title` attribute) | `Cần Admin toàn hệ thống` |
| Aria description disabled | `Tùy chọn này yêu cầu quyền Admin toàn hệ thống.` (linked via `aria-describedby`) |
| Submit CTA (existing) | `Lưu` |
| Cancel CTA (existing) | `Hủy` |
| Loading currentUser fallback | `Đang tải quyền người dùng...` |

### 7.4 Error messages (4 BE envelope từ Phase 2 — Plan 03-02/03 handle)

| Error code | Toast copy (Vietnamese) |
|-----------|-------------------------|
| `HUB_ADMIN_REQUIRED` (POST /api/users create) | `Bạn không có quyền tạo user với role Admin toàn hệ thống. Liên hệ Super Admin.` |
| `HUB_ADMIN_REQUIRED` (PATCH /api/users/{id}/role) | `Bạn không có quyền gán role này. Liên hệ Super Admin.` |
| `HUB_ID_REQUIRED` (GET /api/users) | `Vui lòng chọn hub trước khi xem danh sách user.` |
| `CROSS_HUB_USER_DELETE_DENIED` (DELETE /api/users) | `Không thể xóa user thuộc nhiều hub. Liên hệ Super Admin để xử lý.` |
| `AUTH_STATE_INCONSISTENT` (Plan 02-02 invariant) | `Lỗi hệ thống xác thực — liên hệ admin. Mã: AUTH_STATE_INCONSISTENT` |
| `FORBIDDEN` (require_role generic) | `Bạn không có quyền thực hiện thao tác này.` |
| Network timeout | `Mất kết nối tới máy chủ. Vui lòng thử lại.` |
| 500 generic | `Đã xảy ra lỗi. Vui lòng thử lại hoặc liên hệ admin.` |

### 7.5 Destructive confirmation (KHÔNG có Phase 3)

Phase 3 KHÔNG thêm destructive action mới. Existing DELETE user flow giữ NGUYÊN (UserManagement.tsx existing confirm dialog M2/v3.0 carry forward — Plan 03-03 chỉ thêm error envelope handle cho `CROSS_HUB_USER_DELETE_DENIED`).

---

## 8. Accessibility Contract (WCAG AA + ARIA + keyboard + screen reader)

### 8.1 Radio group (FE-01 form + FE-03 modal)

**HTML structure:**
```tsx
<fieldset role="radiogroup" aria-labelledby="role-group-label">
  <legend id="role-group-label" className="text-sm font-medium">Quyền *</legend>

  <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50">
    <input
      type="radio"
      name="role"
      value="admin"
      checked={newUserRole === 'admin'}
      onChange={() => setNewUserRole('admin')}
      aria-describedby="role-admin-desc"
      className="mt-1 text-accent focus:ring-accent"
    />
    <div>
      <div className="text-sm font-medium">Admin toàn hệ thống</div>
      <div id="role-admin-desc" className="text-sm text-slate-600 leading-relaxed">
        Có thể quản lý tất cả hub, user, settings. Cảnh báo: gán cho user TIN CẬY.
      </div>
    </div>
  </label>

  {/* ... 2 option còn lại */}

  {newUserRole === 'admin' && (
    <div role="alert" aria-live="polite" className="bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-md p-3 mt-2 text-sm font-medium dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200">
      ⚠  Quyền cao nhất — quản lý toàn bộ hệ thống
    </div>
  )}
</fieldset>
```

**Requirements:**
- `<fieldset role="radiogroup">` + `<legend id="...">` + `aria-labelledby="..."` link
- Mỗi `<input type="radio">` + `aria-describedby="role-{value}-desc"` link tới description text
- Warning banner: `role="alert"` + `aria-live="polite"` (announce KHI user chọn "Admin toàn hệ thống")
- Disabled option (FE-03 modal): `disabled` HTML attribute + `aria-disabled="true"` + `aria-describedby="role-admin-tooltip-desc"` (link tới hidden text giải thích) + native `title="Cần Admin toàn hệ thống"` tooltip

### 8.2 Hub switcher (FE-02)

**HTML structure:**
```tsx
<div className="px-4 py-3 border-b border-slate-200 dark:border-slate-800">
  <label htmlFor="hub-switcher" className="block text-xs uppercase text-slate-500 font-medium mb-1">
    Hub đang xem:
  </label>
  <select
    id="hub-switcher"
    value={CURRENT_HUB}
    onChange={handleHubChange}
    aria-label="Chọn hub đang xem"
    className="w-full rounded-md border border-slate-300 dark:border-slate-700 bg-white dark:bg-slate-900 px-3 py-2 text-sm focus:ring-2 focus:ring-accent focus:border-accent"
  >
    {visibleHubs.map(h => (
      <option key={h.code} value={h.code}>{h.name} ({h.code})</option>
    ))}
  </select>
</div>
```

**Requirements:**
- `<label htmlFor="hub-switcher">` + `<select id="hub-switcher">` link
- `aria-label="Chọn hub đang xem"` cho screen reader
- Empty state: `role="status"` + `aria-live="polite"` cho text "Bạn chưa được gán hub nào"
- Loading skeleton: `aria-busy="true"` + `aria-label="Đang tải danh sách hub"`

### 8.3 Keyboard navigation

| Action | Expected behavior |
|--------|-------------------|
| Tab vào form/modal | Focus chuyển tuần tự qua input fields + radio group (1 stop cho group) |
| Tab trong radio group | Focus skip qua group (chỉ 1 stop ở selected option — native HTML behavior) |
| Arrow Up/Down trong radio group | Cycle 3 option theo native HTML radio behavior |
| Space (focused radio) | Select option |
| Tab trong hub switcher | Focus vào `<select>` |
| Enter / Space (select focused) | Open dropdown native |
| Arrow keys trong dropdown open | Navigate options |
| Esc (modal open) | Close modal |
| Esc (dropdown open) | Close dropdown |

### 8.4 Screen reader support

| Element | Screen reader announces |
|---------|------------------------|
| Radio group focus | "Quyền, nhóm nút radio, 3 lựa chọn" |
| Radio "Admin toàn hệ thống" focus | "Admin toàn hệ thống, nút radio, chưa chọn. Có thể quản lý tất cả hub, user, settings. Cảnh báo: gán cho user TIN CẬY." |
| Radio "Admin" selected | "Admin toàn hệ thống, nút radio, đã chọn" + announce warning banner: "Cảnh báo: Quyền cao nhất — quản lý toàn bộ hệ thống" |
| Disabled radio (FE-03 modal) | "Admin toàn hệ thống, nút radio, không khả dụng. Cần Admin toàn hệ thống." (từ `title` + `aria-disabled`) |
| Hub switcher focus | "Chọn hub đang xem, hộp danh sách thả xuống. Đỗ Minh Đường (dmd), 1 trong 2" |
| Empty hub state | "Bạn chưa được gán hub nào. Liên hệ admin để được gán quyền truy cập hub." |

### 8.5 WCAG AA contrast (Tailwind palette verified Phase 5 Plan 05-03 carry forward)

| Pair | Light mode ratio | Dark mode ratio | WCAG |
|------|------------------|-----------------|------|
| `bg-yellow-50` + `text-yellow-800` | 7.2:1 | n/a | AAA pass |
| `bg-yellow-900/20` + `text-yellow-200` | n/a | 8.1:1 | AAA pass |
| `bg-accent/10` + `text-accent` | ≥ 4.5:1 (Phase 5 audit verified) | ≥ 4.5:1 | AA pass |
| `bg-white` + `text-slate-600` (description) | 9.7:1 | n/a | AAA pass |
| `bg-slate-900` + `text-slate-300` (dark description) | n/a | 8.2:1 | AAA pass |
| `opacity-50` disabled label | exempt (WCAG 1.4.3) | exempt | n/a |

### 8.6 Focus indicator (WCAG 2.4.7)

- Tất cả interactive element (radio, select, button, link) phải có visible focus ring: `focus:ring-2 focus:ring-accent focus:ring-offset-2`
- Disabled element KHÔNG focus được (native HTML `disabled` skip Tab order)
- Custom dropdown (nếu Option B) phải implement `:focus-visible` đầy đủ

---

## 9. Test Scenarios (vitest pattern Phase 5 carry forward — Plan 03-04)

Plan 03-04 closeout implement 3 vitest test file mới (`frontend/src/__tests__/` + `frontend/src/pages/__tests__/`). Pattern carry forward `frontend/src/__tests__/Layout.spec.tsx` Phase 5 baseline.

### 9.1 Test file inventory (Plan 03-04 deliverable)

| Test file | Component covered | Scenario count (estimate) |
|-----------|------------------|--------------------------|
| `frontend/src/pages/__tests__/UserManagement.form-3-option.spec.tsx` | FE-01 form refactor | 3-4 test |
| `frontend/src/__tests__/Layout.hub-switcher.spec.tsx` | FE-02 hub switcher | 3-4 test |
| `frontend/src/pages/__tests__/UserManagement.modal-disabled.spec.tsx` | FE-03 modal disabled | 2-3 test |

### 9.2 Test scenarios (semantic coverage CONTEXT.md `<decisions>` D-V3.1-Phase3-C carry forward)

**Test 1 (FE-01 form 3 option):**
- Render form → 3 radio option visible với label đúng tiếng Việt
- Click "Admin toàn hệ thống" → warning banner mount với content `⚠ Quyền cao nhất`
- Click "Viewer" → warning banner unmount
- Submit form với `role='hub_admin'` → POST `/api/users` body include `role: 'hub_admin'`
- BE 403 `HUB_ADMIN_REQUIRED` mock → toast message hiển thị tiếng Việt đúng

**Test 2 (FE-02 hub switcher filter):**
- Mock super admin (`currentUser.role='admin'`) → switcher show ALL hubs (4 hub including central)
- Mock hub_admin dmd (`currentUser.role='hub_admin', hub_ids=['dmd-uuid']`) → switcher show ONLY dmd (KHÔNG central + KHÔNG tdt)
- Mock viewer với `hub_ids=[]` → empty state "Bạn chưa được gán hub nào"
- Change hub → `window.location.href` set `/dmd/` (full reload)

**Test 3 (FE-03 modal disabled):**
- Open Manage modal là super admin → "Admin toàn hệ thống" option ENABLE
- Open Manage modal là hub_admin → "Admin toàn hệ thống" option DISABLED + `title="Cần Admin toàn hệ thống"` + `aria-disabled="true"`
- Click disabled option → no state change (preventDefault native)
- Submit modal với `role='hub_admin'` → PATCH `/api/users/{id}/role` body include `role: 'hub_admin'`

**Test 4 (FE-04 type alias — KHÔNG vitest cần — TypeScript compile-time check):**
- `UserRole` type alias compile-time check qua `tsc --noEmit` (existing CI gate)
- `UserAPI.role: UserRole` field consume verify qua `api.me().then(u => u.user.role satisfies UserRole)` (TypeScript inference)

### 9.3 Mock fixtures (Plan 03-01 mockData.ts append)

```typescript
// frontend/src/mockData.ts — APPEND TO MOCK_USERS array (KHÔNG modify existing 35)
// v3.1 Phase 3 fixture — RBAC hub_admin scope (project_real_hubs_deployment: dmd + tdt)
{
  id: 'u-hadmin-dmd',
  name: 'Nguyễn Hub Admin DMD',
  email: 'hadmin.dmd@medinet.vn',
  role: 'hub_admin',
  hubId: MOCK_HUBS.find(h => h.code === 'dmd')?.id ?? '',
  createdAt: '01/05/2026',
  lastLogin: '1 giờ trước',
  status: 'active',
},
{
  id: 'u-hadmin-tdt',
  name: 'Trần Hub Admin TDT',
  email: 'hadmin.tdt@medinet.vn',
  role: 'hub_admin',
  hubId: MOCK_HUBS.find(h => h.code === 'tdt')?.id ?? '',
  createdAt: '01/05/2026',
  lastLogin: '30 phút trước',
  status: 'active',
},
```

---

## 10. Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (KHÔNG init shadcn — pattern M2/v3.0 carry forward) |
| Third-party | none | not applicable |

**Vetting gate:** N/A — Phase 3 KHÔNG dùng external component/registry. Tất cả code Tailwind utility-only + native HTML.

---

## 11. Scope Boundary (R-V3-2 carry forward Phase 5)

### 11.1 IN scope Phase 3 (visual change)

- `pages/UserManagement.tsx` form radio group refactor (FE-01)
- `pages/UserManagement.tsx` Manage modal disabled option (FE-03)
- `Layout.tsx` sidebar hub switcher ADD (FE-02)

### 11.2 IN scope Phase 3 (type/mock only — KHÔNG visual)

- `services/api.ts` `UserRole` export + `UserAPI.role` field (FE-04)
- `mockData.ts` 2 hub_admin fixture (FE-04)
- `contexts/AuthContext.tsx` VERIFY ONLY (KHÔNG code change — FE-04)

### 11.3 OUT scope Phase 3 (defer v3.1 Phase 4 hoặc v4.0)

- `Login.tsx` — KHÔNG touch (Phase 5 PROXY-04 carry forward locked scope)
- `Dashboard.tsx` / `Documents.tsx` / `Search.tsx` / `Ask.tsx` / `Settings.tsx` / 11 trang nội dung — KHÔNG touch (R-V3-2 mitigation)
- Storybook scaffolding — KHÔNG (project KHÔNG dùng Storybook)
- Figma exports — KHÔNG (project KHÔNG có Figma)
- New SVG/PNG asset — KHÔNG (warning emoji `⚠` inline string)
- Full Tailwind config new (color/spacing/typography) — KHÔNG (reuse Phase 5)
- shadcn init / registry — KHÔNG (defer v4.0 nếu cần)
- Tooltip plugin (radix / floating-ui) — KHÔNG (native `title=""` đủ Phase 3)
- Custom dropdown library (downshift, react-select) — KHÔNG (native `<select>` đủ Phase 3)
- i18n EN/VN switch — KHÔNG (defer v4.0 i18n framework)
- Form validation library (formik / react-hook-form) — KHÔNG (reuse pattern M2 basic HTML required + server envelope)
- Multi-role 1 user trong cùng hub — KHÔNG (schema 0006 mỗi user_hubs row 1 role; defer v4.0)
- Per-resource ACL granular — KHÔNG (defer v4.0)
- Visual regression smoke 4 hub × 11 trang — KHÔNG (defer v4.0 ops handover)

---

## 12. Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (TIẾNG VIỆT có dấu đầy đủ 4 nhóm — radio + tooltip + error envelope + hub switcher)
- [ ] Dimension 2 Visuals: PASS (component contracts định nghĩa rõ props/state/visual states/interaction states/edge cases cho 3 component)
- [ ] Dimension 3 Color: PASS (KHÔNG branding mới — reuse Phase 5 PROXY-04 + Tailwind palette + 60/30/10 rule + accent reserved list)
- [ ] Dimension 4 Typography: PASS (KHÔNG scale mới — reuse Tailwind `text-sm` / `text-lg` / `font-medium` / `font-semibold` từ M2/v3.0)
- [ ] Dimension 5 Spacing: PASS (KHÔNG scale mới — reuse `gap-2.5` / `p-2.5` / `px-4` / `py-3` existing)
- [ ] Dimension 6 Registry Safety: PASS (KHÔNG external registry — Tailwind utility + native HTML only)

**Approval:** pending (chờ gsd-ui-checker validate)

---

## 13. References

### Upstream artifacts (consumed by this UI-SPEC)

- `Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md` — 5 D-V3.1-Phase3-A..E LOCKED 2026-05-24 (auto-mode generate)
- `Hub_All/.planning/ROADMAP.md` — v3.1 Phase 3 success criteria + 3 gray area GA-FE-A/B/C
- `Hub_All/.planning/REQUIREMENTS.md` — FE-01..04 (4 REQ-ID) + DEP-01..05 backend contract Phase 2 carry forward
- `Hub_All/.planning/STATE.md` — v3.1 progress (Phase 1 + Phase 2 DONE; Phase 3 next action)

### Reference pattern (carry forward)

- `Hub_All/.planning/phases/05-reverse-proxy-frontend-subpath/05-UI-SPEC.md` — Phase 5 PROXY-04 visual contract baseline (4 hub branding + theme color CSS variable + Login state machine + Layout sidebar minimal scope R-V3-2)
- `Hub_All/CLAUDE.md` §3 — Ngôn ngữ enforce tiếng Việt + D6 EXPIRED Phase 5 PROXY-03
- `Hub_All/CLAUDE.md` §6 Phase 5 — sidebar minimal scope precedent (chỉ Login.tsx + Layout.tsx sidebar header touch — Dashboard/Documents/Search/11 trang giữ NGUYÊN)

### Frontend source files (audit 2026-05-24 — Plan 03-01/02/03 touch)

- `Hub_All/frontend/src/pages/UserManagement.tsx` — form line 322/372/635/639 (2 radio admin/viewer) + Manage modal line 200-210 + state line 37
- `Hub_All/frontend/src/Layout.tsx` — sidebar header line 1-65 + `getBranding(CURRENT_HUB)` line 34 + `useAuth()` line 28
- `Hub_All/frontend/src/services/api.ts` — `UserAPI` interface line 481-492 (THIẾU `role`) + `UserWithRoles` line 476-479 + `api.me()` line 168 + `CURRENT_HUB` line 46 + `HUB_CONFIG.allowlist` line 23-28
- `Hub_All/frontend/src/contexts/AuthContext.tsx` — `refreshUser()` line 25-43 + `user: UserWithRoles | null` state
- `Hub_All/frontend/src/mockData.ts` — `MOCK_USERS` line 78-115 (35 user existing)
- `Hub_All/frontend/src/branding/index.ts` — Phase 5 branding registry (Plan 03-03 KHÔNG đụng)
- `Hub_All/frontend/vitest.config.ts` — vitest infra Phase 5 carry forward (Plan 03-04 add 3 test file)

### Backend contract (Phase 2 ship — FE consume)

- `Hub_All/api/app/schemas/users.py` — `UserRole = Literal["admin", "hub_admin", "editor", "viewer"]` (FE Plan 03-01 mirror)
- `Hub_All/api/app/routers/users.py` — 4 endpoint scope + error envelope code (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + AUTH_STATE_INCONSISTENT)
- `Hub_All/api/app/auth/dependencies.py` — `assert_hub_admin_for` validator Plan 02-01 ship

### Memory references

- `memory/project_rbac_hub_admin_gap.md` — v3.1 milestone trigger 2026-05-23
- `memory/project_real_hubs_deployment.md` — 2 hub thật DB (dmd Đỗ Minh Đường + tdt Thuốc Dân Tộc) cho mockData fixture

---

*Phase: 03-frontend-form-refactor*
*UI-SPEC generated: 2026-05-24 via gsd-ui-researcher auto-mode (CONTEXT.md 5 D-V3.1-Phase3-A..E LOCKED + Phase 5 PROXY-04 pattern carry forward)*
*Language: Vietnamese (CLAUDE.md §3 enforce)*
