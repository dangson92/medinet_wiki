---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api.ts
  - frontend/src/mockData.ts
  - frontend/src/types.ts
autonomous: true
requirements: [FE-04]
tags: [frontend, typescript, rbac, mock-fixture, type-alias]
must_haves:
  truths:
    - "Module frontend/src/services/api.ts export type alias UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer' (mirror BE Pydantic Literal Plan 02-03 + Phase 1 migration 0006 CHECK constraint)"
    - "UserAPI interface (services/api.ts) chứa field role: UserRole bắt buộc (BE đã trả qua /api/auth/me Phase 2 — type-only change KHÔNG runtime fetch)"
    - "types.ts User.role type extend từ 'admin' | 'viewer' → 'admin' | 'hub_admin' | 'editor' | 'viewer' để mockData hub_admin fixture tương thích"
    - "mockData.ts MOCK_HUBS có entry tdt (Thuốc Dân Tộc) — ADD mới vì hiện thiếu (audit 2026-05-24: chỉ có tamdao/dmd/hcns/test/pkdkqt/bvyhct)"
    - "mockData.ts MOCK_USERS có 2 hub_admin fixture mới (1 dmd + 1 tdt) với role='hub_admin' + hubId lookup qua MOCK_HUBS.find(h => h.code === ...)?.id"
    - "AuthContext.tsx VERIFY ONLY — useAuth().user?.user?.role TypeScript accessible sau Plan 03-01 UserAPI.role extend (KHÔNG code change)"
    - "TypeScript build npx tsc --noEmit PASS clean (zero error)"
  artifacts:
    - path: "frontend/src/services/api.ts"
      provides: "UserRole type alias export + UserAPI.role field — type contract foundation cho Plan 03-02 + 03-03"
      contains: "export type UserRole"
    - path: "frontend/src/mockData.ts"
      provides: "MOCK_HUBS tdt entry + 2 hub_admin user fixture (dmd + tdt)"
      contains: "role: 'hub_admin'"
    - path: "frontend/src/types.ts"
      provides: "User.role extend 4 value tương thích hub_admin fixture"
      contains: "'hub_admin'"
  key_links:
    - from: "frontend/src/services/api.ts UserAPI.role"
      to: "frontend/src/contexts/AuthContext.tsx useAuth().user?.user?.role"
      via: "Nested wrapper UserWithRoles → UserAPI consume path"
      pattern: "user\\?\\.user\\?\\.role"
    - from: "frontend/src/mockData.ts hub_admin fixture hubId"
      to: "frontend/src/mockData.ts MOCK_HUBS.find(h => h.code === 'dmd'|'tdt')?.id"
      via: "Lookup helper avoid hardcode UUID drift"
      pattern: "MOCK_HUBS\\.find"
---

<objective>
Plan 03-01 v3.1 Phase 3 FE-04 — Wave 1 BLOCKING foundation: type alias `UserRole` + UserAPI.role field extend + types.ts User.role extend + mockData.ts 2 hub_admin fixture + MOCK_HUBS tdt entry mới.

**Purpose:** Phase 3 cần single source-of-truth type alias `UserRole` 4 value mirror BE Phase 2 Pydantic Literal (Plan 02-03) + Phase 1 migration 0006 CHECK constraint. Drift 1 chỗ → POST `role='hub_admin'` BE 422 reject HOẶC FE KHÔNG hiển thị role mới. Plan 03-02 + 03-03 BLOCKED chờ Plan 03-01 ship type contract + fixture.

**Output:**
- `frontend/src/services/api.ts` — `export type UserRole` + extend `UserAPI` interface thêm `role: UserRole` field.
- `frontend/src/types.ts` — `User.role` extend 4 value (đồng bộ với UserRole BE-level).
- `frontend/src/mockData.ts` — ADD MOCK_HUBS entry `tdt` (Thuốc Dân Tộc) + APPEND 2 hub_admin user fixture vào cuối MOCK_USERS array.
- `frontend/src/contexts/AuthContext.tsx` — VERIFY ONLY (KHÔNG code change — BE đã trả role qua api.me() Phase 2).

**Pattern decisions Claude discretion (per PATTERNS.md):**
- **types.ts User.role extend** — REQUIRED vì TypeScript strict mode reject fixture `role: 'hub_admin'` nếu type vẫn `'admin' | 'viewer'`.
- **MOCK_HUBS tdt entry** — REQUIRED (audit 2026-05-24 KHÔNG có `code: 'tdt'`). ADD entry mới `{id: '7', code: 'tdt', ...}` SAU bvyhct (id=6).
- **userHubIds derivation** — Option A LOCKED: Plan 03-03 derive từ `roles: RoleAPI[]` đã có sẵn (less invasive — KHÔNG extend UserAPI.hub_ids).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md
@Hub_All/frontend/src/services/api.ts
@Hub_All/frontend/src/mockData.ts
@Hub_All/frontend/src/types.ts
@Hub_All/frontend/src/contexts/AuthContext.tsx

<interfaces>
<!-- BE Pydantic Literal Plan 02-03 ship — FE Plan 03-01 mirror chính xác -->

From Hub_All/api/app/schemas/users.py (Plan 02-03 DEP-03 ship):
```python
UserRole = Literal["admin", "hub_admin", "editor", "viewer"]
```

From Hub_All/frontend/src/services/api.ts (line 476-492 — UserWithRoles + UserAPI hiện tại THIẾU `role`):
```typescript
export interface UserWithRoles {
  user: UserAPI;
  roles: RoleAPI[];
}

export interface UserAPI {
  id: string;
  email: string;
  name: string;
  phone?: string;
  department?: string;
  avatar_url?: string;
  status: string;
  failed_login_count: number;
  created_at: string;
  updated_at: string;
  // Plan 03-01 ADD: role: UserRole;
}

export interface RoleAPI {
  user_id: string;
  hub_id: string;
  role: string;
}
```

From Hub_All/frontend/src/types.ts (line 14-23 — User.role hiện chỉ 2 value):
```typescript
export interface User {
  id: string;
  name: string;
  email: string;
  role: 'admin' | 'viewer';  // Plan 03-01 EXTEND: 'admin' | 'hub_admin' | 'editor' | 'viewer'
  hubId: string;
  createdAt: string;
  lastLogin: string;
  status: 'active' | 'disabled';
}
```

From Hub_All/frontend/src/mockData.ts (line 3-76 — MOCK_HUBS hiện tại; THIẾU code='tdt'):
- id=1 → tamdao (Tâm Đạo Y Quán)
- id=2 → dmd (Đỗ Minh Đường) ✅
- id=3 → hcns (HCNS)
- id=4 → test (Test Hub)
- id=5 → pkdkqt (Phòng khám Đa khoa Quốc tế)
- id=6 → bvyhct (Bệnh viện Y học Cổ truyền)
- **THIẾU code='tdt' (Thuốc Dân Tộc)** — Plan 03-01 ADD entry mới id='7'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend api.ts (UserRole export + UserAPI.role field) + extend types.ts (User.role 4 value)</name>
  <read_first>
    - Hub_All/frontend/src/services/api.ts (TOÀN FILE — biết chính xác vị trí UserAPI line 481-492 + nơi chèn UserRole export line ~480 trước UserAPI declare)
    - Hub_All/frontend/src/types.ts (TOÀN FILE — biết User interface line 14-23 hiện tại 2 value)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "1. frontend/src/services/api.ts" — concrete code excerpt UserRole export pattern)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §5.2 (Files type/mock only — FE-04 scope)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md decisions D-V3.1-Phase3-B + D-V3.1-Phase3-D
    - Hub_All/api/app/schemas/users.py (verify BE Pydantic UserRole Literal 4 value match)
  </read_first>
  <behavior>
    - Test 1 (TypeScript compile): `cd frontend && npx tsc --noEmit` exits 0 sau khi UserRole export + UserAPI.role thêm + User.role extend.
    - Test 2 (Grep export): grep cho `export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer'` trong api.ts == 1.
    - Test 3 (Grep UserAPI extend): UserAPI interface chứa `role: UserRole;` (grep `role: UserRole` >= 1 trong file).
    - Test 4 (Grep types.ts): grep `'admin' | 'hub_admin' | 'editor' | 'viewer'` trong types.ts >= 1.
    - Test 5 (Import alias): UserAPI declare PHẢI ở SAU UserRole declare (readability — UserRole đứng trước).
  </behavior>
  <action>
**Bước 1 — `Hub_All/frontend/src/services/api.ts`** ADD `UserRole` export + extend `UserAPI`:

1. ADD ngay TRƯỚC `export interface UserAPI {` (hiện ở line 481 — verify lại line number qua Read):
```typescript
// Plan 03-01 v3.1 Phase 3 FE-04 — UserRole literal union mirror BE Pydantic Literal
// Source: .planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md D-V3.1-Phase2-A LOCKED
//         api/app/schemas/users.py:UserRole = Literal["admin","hub_admin","editor","viewer"]
//         .planning/phases/01-rbac-schema-migration/01-01-PLAN.md migration 0006 CHECK constraint
//         .planning/phases/03-frontend-form-refactor/03-CONTEXT.md D-V3.1-Phase3-D LOCKED
//
// CRITICAL: Drift between FE alias + BE Literal + Phase 1 CHECK constraint → 422 reject runtime.
// Centralize export tại đây (api.ts) — consumer import qua `import type { UserRole } from '../services/api'`.
export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer';

```

2. UPDATE `UserAPI` interface ADD field `role: UserRole;`:
```typescript
export interface UserAPI {
  id: string;
  email: string;
  name: string;
  phone?: string;
  department?: string;
  avatar_url?: string;
  status: string;
  role: UserRole;  // Plan 03-01 v3.1 Phase 3 FE-04 — BE Phase 2 ship qua /api/auth/me (D-V3.1-Phase3-B LOCKED)
  failed_login_count: number;
  created_at: string;
  updated_at: string;
}
```

**Bước 2 — `Hub_All/frontend/src/types.ts`** EXTEND `User.role`:

Tìm line 18 `role: 'admin' | 'viewer';` → REPLACE bằng:
```typescript
  role: 'admin' | 'hub_admin' | 'editor' | 'viewer';  // Plan 03-01 v3.1 Phase 3 FE-04 — extend match UserRole (api.ts)
```

KHÔNG đổi tên field, KHÔNG xoá comment khác trong types.ts, KHÔNG đụng các interface khác (Hub/SyncBatch/SyncPage/AuditLogEntry/APIKey/RAGDocument).

**Bước 3 — verify TypeScript build clean:**

```bash
cd Hub_All/frontend && npx tsc --noEmit
```

PHẢI exit 0. Nếu fail, audit chỗ nào consume `User.role` hoặc `UserAPI` mà bị strict reject — FE Plan 03-02/03 sẽ refactor; tại Plan 03-01 KHÔNG fix consumer (chỉ extend type — Plan 03-02/03 sẽ consume UserRole).
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && cd ../.. && grep -c "export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer'" Hub_All/frontend/src/services/api.ts && grep -c "role: UserRole" Hub_All/frontend/src/services/api.ts && grep -c "'admin' | 'hub_admin' | 'editor' | 'viewer'" Hub_All/frontend/src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0 (TypeScript build clean — KHÔNG regression).
    - grep `export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer'` trong Hub_All/frontend/src/services/api.ts == 1.
    - grep `role: UserRole` trong Hub_All/frontend/src/services/api.ts >= 1 (UserAPI.role field added).
    - grep `'admin' | 'hub_admin' | 'editor' | 'viewer'` trong Hub_All/frontend/src/types.ts >= 1 (User.role extended).
    - grep `FE-04` trong Hub_All/frontend/src/services/api.ts >= 1 (comment trace decision ID).
    - grep `D-V3.1-Phase3-D` trong Hub_All/frontend/src/services/api.ts >= 1 (trace decision LOCKED).
    - UserRole declare đặt TRƯỚC UserAPI declare (readability — verify qua line number).
    - KHÔNG đổi tên `UserAPI` interface, KHÔNG xoá field hiện có (additive only).
    - KHÔNG đụng `UserWithRoles` interface, `RoleAPI` interface, `HubAPI` interface trong api.ts.
    - KHÔNG đụng các interface khác trong types.ts (Hub/SyncBatch/SyncPage/AuditLogEntry/APIKey/RAGDocument).
  </acceptance_criteria>
  <done>
    api.ts có `export type UserRole` mới + `UserAPI.role: UserRole` field; types.ts `User.role` extend 4 value; TypeScript build PASS clean; Plan 03-02 + 03-03 có thể import `UserRole` type và mockData hub_admin fixture compile được.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append MOCK_HUBS tdt entry + 2 hub_admin user fixture vào mockData.ts</name>
  <read_first>
    - Hub_All/frontend/src/mockData.ts (TOÀN FILE line 1-115 — biết MOCK_HUBS array structure + MOCK_USERS array structure + 35 user existing schema)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "2. frontend/src/mockData.ts" — concrete append pattern)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §9.3 (Mock fixtures)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md decisions D-V3.1-Phase3-C
    - Hub_All/frontend/src/types.ts (verify User interface đã extend role 4 value sau Task 1)
  </read_first>
  <behavior>
    - Test 1: grep `code: 'tdt'` trong Hub_All/frontend/src/mockData.ts == 1 (MOCK_HUBS có entry tdt).
    - Test 2: grep `role: 'hub_admin'` trong Hub_All/frontend/src/mockData.ts == 2 (đúng 2 user hub_admin — 1 dmd + 1 tdt).
    - Test 3: grep `u-hadmin-dmd` trong Hub_All/frontend/src/mockData.ts == 1.
    - Test 4: grep `u-hadmin-tdt` trong Hub_All/frontend/src/mockData.ts == 1.
    - Test 5: TypeScript build clean `cd frontend && npx tsc --noEmit` exits 0 sau khi append fixture.
  </behavior>
  <action>
**Bước 1 — ADD MOCK_HUBS entry `tdt`** SAU entry `bvyhct` (id=6, kết thúc line ~75), TRƯỚC `];` đóng MOCK_HUBS array:

```typescript
  {
    id: '7',
    name: 'Thuốc Dân Tộc',
    code: 'tdt',
    subdomain: 'wiki.medinet.vn/tdt',
    pages: 178,
    users: 22,
    lastUpdate: '4 giờ trước',
    status: 'active',
    pendingSync: 1,
    createdAt: '2025-01-20',
  },
];
```

**Bước 2 — APPEND 2 hub_admin fixture vào MOCK_USERS array** SAU row `u35` (kết thúc line ~113), TRƯỚC `];` đóng MOCK_USERS array:

```typescript
  { id: 'u35', name: 'Âu Thị KK', email: 'atkk@medinet.vn', role: 'viewer', hubId: '2', createdAt: '20/03/2025', lastLogin: '7 giờ trước', status: 'active' },
  // v3.1 Phase 3 FE-04 fixture — RBAC hub_admin scope (D-V3.1-Phase3-C LOCKED)
  // Source: .planning/phases/03-frontend-form-refactor/03-CONTEXT.md D-V3.1-Phase3-C
  //         memory/project_real_hubs_deployment.md (dmd Đỗ Minh Đường + tdt Thuốc Dân Tộc 2 hub thật trong DB)
  { id: 'u-hadmin-dmd', name: 'Nguyễn Hub Admin DMD', email: 'hadmin.dmd@medinet.vn', role: 'hub_admin', hubId: MOCK_HUBS.find(h => h.code === 'dmd')?.id ?? '', createdAt: '01/05/2026', lastLogin: '1 giờ trước', status: 'active' },
  { id: 'u-hadmin-tdt', name: 'Trần Hub Admin TDT', email: 'hadmin.tdt@medinet.vn', role: 'hub_admin', hubId: MOCK_HUBS.find(h => h.code === 'tdt')?.id ?? '', createdAt: '01/05/2026', lastLogin: '30 phút trước', status: 'active' },
];
```

**Lưu ý:**
- `MOCK_HUBS.find(h => h.code === 'dmd')?.id` resolve runtime = `'2'`.
- `MOCK_HUBS.find(h => h.code === 'tdt')?.id` resolve runtime = `'7'` (entry mới ADD ở Bước 1).
- Nullish coalescing `?? ''` defensive — nếu tdt KHÔNG có hub fixture vẫn compile.

**Bước 3 — verify TypeScript build clean:**

```bash
cd Hub_All/frontend && npx tsc --noEmit
```

PHẢI exit 0. Sau Task 1 User.role extend 4 value, fixture `role: 'hub_admin'` compile OK.

KHÔNG đụng 35 user existing MOCK_USERS (chỉ APPEND 2 row mới sau u35). KHÔNG đổi tên MOCK_HUBS, MOCK_USERS, MOCK_SYNC_BATCHES, hoặc các export array khác trong mockData.ts.
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && cd ../.. && grep -c "code: 'tdt'" Hub_All/frontend/src/mockData.ts && grep -c "role: 'hub_admin'" Hub_All/frontend/src/mockData.ts && grep -c "u-hadmin-dmd" Hub_All/frontend/src/mockData.ts && grep -c "u-hadmin-tdt" Hub_All/frontend/src/mockData.ts && grep -c "Thuốc Dân Tộc" Hub_All/frontend/src/mockData.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0 (TypeScript build clean sau append fixture).
    - grep `code: 'tdt'` trong Hub_All/frontend/src/mockData.ts == 1 (MOCK_HUBS có entry tdt).
    - grep `Thuốc Dân Tộc` trong Hub_All/frontend/src/mockData.ts == 1 (Vietnamese name correct — copywriting Vietnamese enforce).
    - grep `role: 'hub_admin'` trong Hub_All/frontend/src/mockData.ts == 2 (đúng 2 hub_admin fixture).
    - grep `u-hadmin-dmd` trong Hub_All/frontend/src/mockData.ts == 1.
    - grep `u-hadmin-tdt` trong Hub_All/frontend/src/mockData.ts == 1.
    - grep `hadmin.dmd@medinet.vn` trong Hub_All/frontend/src/mockData.ts == 1.
    - grep `hadmin.tdt@medinet.vn` trong Hub_All/frontend/src/mockData.ts == 1.
    - grep `MOCK_HUBS.find(h => h.code === 'dmd')` trong Hub_All/frontend/src/mockData.ts >= 1 (UUID lookup pattern).
    - grep `MOCK_HUBS.find(h => h.code === 'tdt')` trong Hub_All/frontend/src/mockData.ts >= 1.
    - grep `FE-04 fixture` trong Hub_All/frontend/src/mockData.ts >= 1 (comment trace decision ID).
    - KHÔNG xoá hoặc đổi 35 user existing trong MOCK_USERS.
    - KHÔNG xoá hoặc đổi 6 hub existing trong MOCK_HUBS (tamdao/dmd/hcns/test/pkdkqt/bvyhct giữ nguyên).
    - KHÔNG đụng MOCK_SYNC_BATCHES, MOCK_AUDIT_LOG, MOCK_API_KEYS, MOCK_RAG_DOCUMENTS arrays.
  </acceptance_criteria>
  <done>
    mockData.ts có thêm MOCK_HUBS entry `tdt` + 2 hub_admin user fixture (u-hadmin-dmd + u-hadmin-tdt); TypeScript build PASS; Plan 03-04 vitest test (Wave 3) có fixture role mới để mock context cho 3 test file.
  </done>
</task>

<task type="auto">
  <name>Task 3: VERIFY ONLY AuthContext.tsx + TypeScript build full clean (Plan 03-01 closeout integrity)</name>
  <read_first>
    - Hub_All/frontend/src/contexts/AuthContext.tsx (TOÀN FILE line 1-85 — verify `useAuth().user?.user?.role` TypeScript accessible sau Task 1 + 2)
    - Hub_All/frontend/src/services/api.ts (verify UserAPI.role + UserRole export sau Task 1)
    - Hub_All/frontend/src/mockData.ts (verify hub_admin fixture sau Task 2)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "3. frontend/src/contexts/AuthContext.tsx" — verify-only checklist)
  </read_first>
  <behavior>
    - Test 1: KHÔNG có file modify cho AuthContext.tsx — `git diff Hub_All/frontend/src/contexts/AuthContext.tsx` trống.
    - Test 2: TypeScript build full project clean — `cd frontend && npx tsc --noEmit` exits 0.
    - Test 3: Lint clean — `cd frontend && npm run lint` exits 0 (catch import/unused warning).
    - Test 4: Vitest baseline regression KHÔNG break — `cd frontend && npm run test` (Phase 5 baseline test PASS — Layout.spec.tsx + api.spec.ts + Login.spec.tsx + branding/__tests__/registry.spec.ts + App.spec.tsx).
  </behavior>
  <action>
**Bước 1 — VERIFY AuthContext.tsx KHÔNG modify:**

Read file `Hub_All/frontend/src/contexts/AuthContext.tsx` toàn bộ. Confirm:
- `AuthState.user: UserWithRoles | null` — UNCHANGED.
- `refreshUser()` call `api.me()` — UNCHANGED (BE Phase 2 đã trả role qua /api/auth/me).
- `useAuth()` hook export — UNCHANGED.

KHÔNG ghi modification nào lên AuthContext.tsx. Plan 03-01 chỉ extend type api.ts → consumer `useAuth().user?.user?.role` truy cập được runtime mà KHÔNG code change ở AuthContext.

**Bước 2 — TypeScript build full clean:**

```bash
cd Hub_All/frontend && npx tsc --noEmit
```

PHẢI exit 0. Nếu fail → audit error message, fix tại file gốc (api.ts hoặc types.ts) hoặc revert. KHÔNG modify file consumer (Plan 03-02/03 scope).

**Bước 3 — ESLint clean:**

```bash
cd Hub_All/frontend && npm run lint
```

PHẢI exit 0. Catch unused import / no-unused-vars / type-import enforce (existing project lint config).

**Bước 4 — Vitest baseline regression KHÔNG break:**

```bash
cd Hub_All/frontend && npm run test -- --run
```

PHẢI exit 0. 5 test file Phase 5 baseline (Layout.spec.tsx + api.spec.ts + Login.spec.tsx + branding registry.spec.ts + App.spec.tsx) PASS regression — Plan 03-01 chỉ extend type alias + mock fixture KHÔNG đụng runtime behavior nên KHÔNG nên break test cũ.

Nếu test cũ fail vì mock type strict reject (vd Login.spec.tsx mock `useAuth()` thiếu field `role`), document failure trong SUMMARY.md + escalate Plan 03-04 (test file refactor scope).

**Bước 5 — Verify Plan 03-01 completion qua git diff:**

```bash
git diff --stat Hub_All/frontend/src/services/api.ts Hub_All/frontend/src/mockData.ts Hub_All/frontend/src/types.ts
```

Expected: 3 file modified (api.ts + mockData.ts + types.ts) + 0 file modified ngoài 3 file đó.

```bash
git diff --stat Hub_All/frontend/src/contexts/AuthContext.tsx
```

Expected: trống (KHÔNG modification).
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && npm run lint && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0 (TypeScript build clean — Plan 03-01 closeout integrity).
    - `cd Hub_All/frontend && npm run lint` exits 0 (ESLint clean).
    - `cd Hub_All/frontend && npm run test -- --run` exits 0 (Vitest 5 baseline test file PASS).
    - `git diff --stat Hub_All/frontend/src/contexts/AuthContext.tsx` output trống (KHÔNG modify file).
    - `git diff --stat Hub_All/frontend/src/` chỉ list 3 file: services/api.ts + mockData.ts + types.ts (KHÔNG có file khác bị accidental modify).
    - Nếu Vitest có test fail do mock type strict (mock useAuth() thiếu role field), document failure trong SUMMARY.md + flag Plan 03-04 sẽ refactor mock helper (out of scope Plan 03-01).
  </acceptance_criteria>
  <done>
    AuthContext.tsx UNCHANGED (verify-only checklist pass); TypeScript + ESLint + Vitest baseline PASS clean; Plan 03-01 ship đầy đủ — Plan 03-02 + 03-03 unblocked (có UserRole import + hub_admin fixture).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE TypeScript alias ↔ BE Pydantic Literal | Type-only contract giữa FE + BE — runtime nếu drift → 422 UnprocessableEntity (BE reject) hoặc FE KHÔNG render role mới. Trust boundary mềm (compile-time check tsc + runtime fallback envelope). |
| mockData.ts dev fixture ↔ prod runtime | mockData CHỈ dev mode (per memory `project_no_smtp_v4` + Vite `import.meta.env.DEV`) — fixture KHÔNG bundle vào production build. Trust boundary stable (Vite tree-shake KHÔNG include mockData vào main bundle). |
| frontend types ↔ existing M2/v3.0 consumer | Plan 03-01 EXTEND type `User.role` từ 2 → 4 value — backward compat additive only (TypeScript widening accept 4 value bao 2 value cũ); existing consumer giữ NGUYÊN. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FE-01-01 | Tampering | Hub_admin DevTools edit FE allowlist client-side → bypass hub switcher filter (defense in depth) | mitigate | Backend Caddy regex (Phase 5 T-5-02 carry forward) + RBAC GET /api/hubs filter (Phase 2 DEP-02 carry forward) là trust boundary. FE alias chỉ UX layer. KHÔNG fix tại Plan 03-01 vì FE type alias chỉ định nghĩa shape; backend authoritative. Severity: LOW. |
| T-03-FE-01-02 | Information Disclosure | mockData.ts dev fixture email pattern (`hadmin.dmd@medinet.vn`) leak naming convention | accept | Email pattern public knowledge (org structure transparent — Medinet domain). Vite tree-shake mockData KHÔNG bundle prod (`import.meta.env.DEV` guard pattern). Severity: VERY LOW (dev-only). |
| T-03-FE-01-03 | Tampering | Existing consumer M2/v3.0 reject hub_admin role value (User type widening break callsite) | mitigate | Plan 03-01 chỉ EXTEND type (additive widening — TypeScript accept superset). Plan 03-02 + 03-03 sẽ refactor 3 consumer touch (UserManagement form + Manage modal + Layout). Existing 11 trang React M2/v3.0 KHÔNG touch User.role qua mockData (chỉ qua UserAPI BE). Severity: LOW. |
| T-03-FE-01-04 | Spoofing | mockData hub_admin fixture với email pattern dễ đoán cho dev/test → leak prod login attempt | mitigate | Plan 03-01 fixture pattern `hadmin.<hub>@medinet.vn` — KHÔNG seed vào prod DB (defer Phase 4 MIGRATE-02 real user seed). mockData CHỈ dev mode. Severity: VERY LOW (dev-only). |
</threat_model>

<verification>
- `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
- `cd Hub_All/frontend && npm run lint` exits 0.
- `cd Hub_All/frontend && npm run test -- --run` exits 0 (5 baseline test PASS — Layout/Login/api/registry/App).
- api.ts có `export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer'` + UserAPI.role field.
- types.ts User.role extend 4 value.
- mockData.ts có MOCK_HUBS tdt entry + 2 hub_admin fixture (u-hadmin-dmd + u-hadmin-tdt).
- AuthContext.tsx KHÔNG modify (git diff trống).
- KHÔNG đụng các file khác trong frontend/src/ ngoài 3 file listed.
</verification>

<success_criteria>
1. `UserRole` type alias export ở api.ts mirror BE Pydantic Literal 4 value (admin|hub_admin|editor|viewer).
2. `UserAPI.role: UserRole` field extend — `useAuth().user?.user?.role` TypeScript accessible.
3. `User.role` (types.ts) extend 4 value tương thích hub_admin fixture.
4. MOCK_HUBS có entry `tdt` (Thuốc Dân Tộc) — ADD mới id=7.
5. MOCK_USERS có 2 hub_admin user fixture (u-hadmin-dmd hubId='2' + u-hadmin-tdt hubId='7').
6. TypeScript build + ESLint + Vitest baseline regression PASS clean.
7. AuthContext.tsx UNCHANGED (verify-only).
8. Plan 03-02 + 03-03 unblocked (có UserRole import + hub_admin fixture sẵn sàng consume).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/03-frontend-form-refactor/03-01-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 3 file frontend modify atomic (api.ts UserRole + UserAPI.role; types.ts User.role extend; mockData.ts MOCK_HUBS tdt + 2 hub_admin fixture)
- AuthContext.tsx VERIFY ONLY (KHÔNG modify — BE Phase 2 đã trả role qua /api/auth/me)
- TypeScript + ESLint + Vitest baseline PASS clean (3 verify command)
- Decisions implemented: D-V3.1-Phase3-B (UserAPI.role extend) + D-V3.1-Phase3-C (mockData 2 hub_admin fixture) + D-V3.1-Phase3-D (UserRole type alias centralize) + D-V3.1-Phase3-E Wave 1 BLOCKING
- Discovery findings:
  - MOCK_HUBS THIẾU code='tdt' → ADD entry mới id=7 (decision plan-time per PATTERNS.md)
  - User.role types.ts chỉ 2 value → EXTEND 4 value để fixture compile (decision plan-time)
  - userHubIds derivation Option A LOCKED (derive từ `roles: RoleAPI[]` đã có sẵn — Plan 03-03 consume) — KHÔNG extend UserAPI.hub_ids
- Plan 03-02 + 03-03 ready unblock (Wave 2 parallel-able trên 2 file độc lập NHƯNG cùng UserManagement.tsx → sequential per discretion note)

Commit atomic: `feat(03-01): FE-04 UserRole type alias + UserAPI.role extend + mockData hub_admin fixture` + body tiếng Việt nói tại sao (Wave 1 BLOCKING foundation Phase 3 v3.1; FE alias mirror BE Pydantic Literal 4 value; mockData 2 hub_admin fixture + MOCK_HUBS tdt entry cho dev test + vitest pattern Plan 03-04 carry forward).
</output>
</content>
</invoke>