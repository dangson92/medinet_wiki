---
phase: 03-frontend-form-refactor
mapped: 2026-05-24
status: Ready for planning
source: CONTEXT.md (5 D-V3.1-Phase3-A..E LOCKED) + UI-SPEC.md §5-§9 + codebase audit Hub_All/frontend/src/
language: vietnamese
---

# Phase 3 v3.1: Frontend form refactor — Pattern Map

> Pattern map cho 4 plan Wave (Plan 03-01 type/mock + Plan 03-02 form 3-option + Plan 03-03 hub switcher + edit modal disabled + Plan 03-04 closeout vitest). Mỗi file Phase 3 touch → analog file thật trong `Hub_All/frontend/src/` + concrete code excerpt (line numbers). Output cho gsd-planner Step 8 với `read_first` field accurate. Generated by gsd-phase-pattern-mapper auto-mode (CONTEXT.md 5 decision LOCKED + UI-SPEC.md 13 section + Phase 5 PROXY-04 carry forward).

**Mapped:** 2026-05-24
**Files analyzed:** 6 file (3 visual + 3 type/mock — UI-SPEC §5.1+§5.2) + 3 test file mới (Plan 03-04)
**Analogs found:** 9/9 (100% coverage — KHÔNG zero-coverage)

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality | Wave |
|-------------------|------|-----------|----------------|---------------|------|
| `frontend/src/services/api.ts` (MODIFY ~line 480 + 481-492) | type-export + interface-extend | type-only (compile-time) | `frontend/src/services/api.ts:12-28` (HubConfigRuntime interface + KNOWN_HUBS readonly export) | exact (same file pattern) | Wave 1 |
| `frontend/src/mockData.ts` (MODIFY append after line 115) | fixture | static-data | `frontend/src/mockData.ts:78-115` (35 MOCK_USERS schema) | exact (same file, same shape) | Wave 1 |
| `frontend/src/contexts/AuthContext.tsx` (VERIFY ONLY) | context-provider + hook | request-response | `frontend/src/contexts/AuthContext.tsx:25-43` (refreshUser + UserWithRoles consume) | exact (file là analog của chính nó — KHÔNG code change) | Wave 1 |
| `frontend/src/pages/UserManagement.tsx` (MODIFY line 37 + 633-642) — FE-01 form | page-component + form-handler | request-response + state-mgmt | `frontend/src/pages/UserManagement.tsx:633-642` (radio group existing 2 option) | exact (same file, same component) | Wave 2 |
| `frontend/src/pages/UserManagement.tsx` (MODIFY line 905-1000+ Manage modal) — FE-03 disabled | modal-component + conditional-render | state-mgmt + auth-gate | `frontend/src/pages/UserManagement.tsx:941-980` (Manage modal button-style radio existing) | exact (same file, same modal) | Wave 2 |
| `frontend/src/Layout.tsx` (MODIFY ABOVE line 217 sidebar nav) — FE-02 hub switcher | layout-component + auth-context-consumer | request-response (api.getHubs) | `frontend/src/Layout.tsx:166-215` (sidebar header section) + `frontend/src/services/api.ts:172-174` (getHubs) | exact (same file sibling section) | Wave 2 |
| `frontend/src/pages/__tests__/UserManagement.form-3-option.spec.tsx` (CREATE) | unit-test | jsdom + vi.mock | `frontend/src/__tests__/Layout.spec.tsx:10-50` (renderLayoutWithCurrentHub helper + vi.doMock AuthContext pattern) | role-match (RTL render + mock context) | Wave 3 |
| `frontend/src/__tests__/Layout.hub-switcher.spec.tsx` (CREATE) | unit-test | jsdom + vi.mock | `frontend/src/__tests__/Layout.spec.tsx` (toàn file — pattern Layout render với hub config + mock auth) | exact (same file, extend cùng pattern) | Wave 3 |
| `frontend/src/pages/__tests__/UserManagement.manage-modal-disabled.spec.tsx` (CREATE) | unit-test | jsdom + vi.mock | `frontend/src/pages/__tests__/Login.spec.tsx:46-108` (renderLoginAtUrl + branching state machine test) | role-match (page render + mock auth + branch by user.role) | Wave 3 |

---

## Pattern Assignments

### Wave 1 (Plan 03-01 BLOCKING — type + mock + verify foundation)

#### 1. `frontend/src/services/api.ts` (MODIFY — FE-04 type alias + UserAPI extend)

**Analog:** `frontend/src/services/api.ts:12-28` (HubConfigRuntime interface declare + readonly union export — Phase 5 PROXY-02 pattern carry forward)

**Why analog:** Phase 5 đã ship pattern `export const KNOWN_HUBS: readonly string[]` (line 28) + interface declare nested type + comment block reference decision LOCKED → Plan 03-01 mirror cùng style cho `UserRole` literal union + `UserAPI.role` field extend.

**Imports pattern** (line 1-8 — comment block reference):
```typescript
// Phase 5 D-V3-Phase5-B1 LOCKED — 1-build prefix detect (replaces M2 absolute origin hardcode)
// Source: .planning/phases/05-reverse-proxy-frontend-subpath/05-CONTEXT.md §"Implementation Decisions B1"
//         .planning/phases/05-reverse-proxy-frontend-subpath/05-RESEARCH.md Pattern 2 + Pitfall 4
//
// T-5-02 (Tampering hub allowlist client-side): ...
// ... KHÔNG dùng FE allowlist làm trust boundary.
```

**Type alias + interface extend pattern** (line 12-28):
```typescript
// Runtime hub config — Caddy/backend inject via <script>window.__HUB_CONFIG__=...</script>
// HOẶC fallback hardcode 3 hub initial Phase 5 (Phase 6 SETTINGS-04 sync DB-driven hub_registry).
interface HubConfigRuntime {
  allowlist: readonly string[];
  current?: string; // optional — chỉ set nếu backend render dynamic
}

declare global {
  interface Window {
    __HUB_CONFIG__?: HubConfigRuntime;
  }
}

const HUB_CONFIG: HubConfigRuntime = (typeof window !== 'undefined' && window.__HUB_CONFIG__) ?? {
  allowlist: ['yte', 'duoc', 'hcns'] as const,
};
```

**Existing `UserAPI` interface** (line 481-492 — Plan 03-01 sẽ extend thêm `role: UserRole`):
```typescript
export interface UserAPI {
  id: string;
  email: string;
  name: string;
  phone?: string;
  department?: string;
  avatar_url?: string;
  status: string;
  failed_login_count: number;
  created_at: string;
  updated_at: string;
}
// Plan 03-01 ADD: role: UserRole;  ← (BE Phase 2 đã trả qua /api/auth/me)
```

**Existing nested wrapper** (line 476-479) — Plan 03-02/03 consumer truy cập qua `user?.user?.role`:
```typescript
export interface UserWithRoles {
  user: UserAPI;
  roles: RoleAPI[];
}
```

**Plan 03-01 ADD pattern (BEFORE line 481 — sát UserAPI declare):**
```typescript
// Plan 03-01 v3.1 Phase 3 FE-04 — UserRole literal union mirror BE Pydantic Literal
// Source: .planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md D-V3.1-Phase2-A LOCKED
//         api/app/schemas/users.py:UserRole = Literal["admin","hub_admin","editor","viewer"]
//         .planning/phases/01-rbac-schema-migration/01-01-PLAN.md migration 0006 CHECK constraint
//
// CRITICAL: Drift between FE alias + BE Literal + Phase 1 CHECK constraint → 422 reject runtime.
// Centralize export tại đây (api.ts) — consumer import qua `import type { UserRole } from '../services/api'`.
export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer';
```

---

#### 2. `frontend/src/mockData.ts` (MODIFY append after line 115 — FE-04 fixture)

**Analog:** `frontend/src/mockData.ts:78-115` (35 MOCK_USERS existing schema 8 field `{id, name, email, role, hubId, createdAt, lastLogin, status}`)

**Why analog:** Plan 03-01 APPEND 2 row cùng shape — KHÔNG modify existing 35; chỉ add 2 hub_admin fixture (1 dmd + 1 tdt — memory `project_real_hubs_deployment` 2 hub thật).

**Schema pattern** (line 78-79 — first 2 row reference shape):
```typescript
export const MOCK_USERS: User[] = [
  { id: 'u1', name: 'Nguyễn Văn A', email: 'nva@medinet.vn', role: 'admin', hubId: '1', createdAt: '01/01/2025', lastLogin: '2 giờ trước', status: 'active' },
  { id: 'u2', name: 'Trần Thị B', email: 'ttb@medinet.vn', role: 'viewer', hubId: '1', createdAt: '15/02/2025', lastLogin: 'Chưa đăng nhập', status: 'active' },
  // ... 33 user khác — toàn role 'admin' hoặc 'viewer' (KHÔNG có hub_admin / editor)
```

**Hub UUID lookup pattern** (sử dụng MOCK_HUBS line 3-76 — `MOCK_HUBS.find(h => h.code === 'dmd')?.id`):
- `MOCK_HUBS[1]` line 16-27: `{ id: '2', name: 'Đỗ Minh Đường', code: 'dmd', ... }` — DMD hub id = `'2'`
- `MOCK_HUBS[2]` line 28-39: `{ id: '3', name: 'HCNS', code: 'hcns', ... }` — note: chưa có `tdt` trong MOCK_HUBS hiện tại; Plan 03-01 cần verify hub code 'tdt' tồn tại HOẶC fallback dùng `code === 'tamdao'` (id '1') hoặc tạo `MOCK_HUBS` entry `tdt` mới

**Plan 03-01 APPEND pattern (AFTER line 114 — sau row u35, TRƯỚC `];` line 114):**
```typescript
  { id: 'u35', name: 'Âu Thị KK', email: 'atkk@medinet.vn', role: 'viewer', hubId: '2', createdAt: '20/03/2025', lastLogin: '7 giờ trước', status: 'active' },
  // v3.1 Phase 3 fixture — RBAC hub_admin scope
  // Source: .planning/phases/03-frontend-form-refactor/03-CONTEXT.md D-V3.1-Phase3-C LOCKED
  //         memory/project_real_hubs_deployment.md (dmd Đỗ Minh Đường + tdt Thuốc Dân Tộc 2 hub thật trong DB)
  { id: 'u-hadmin-dmd', name: 'Nguyễn Hub Admin DMD', email: 'hadmin.dmd@medinet.vn', role: 'hub_admin', hubId: MOCK_HUBS.find(h => h.code === 'dmd')?.id ?? '', createdAt: '01/05/2026', lastLogin: '1 giờ trước', status: 'active' },
  { id: 'u-hadmin-tdt', name: 'Trần Hub Admin TDT', email: 'hadmin.tdt@medinet.vn', role: 'hub_admin', hubId: MOCK_HUBS.find(h => h.code === 'tdt')?.id ?? '', createdAt: '01/05/2026', lastLogin: '30 phút trước', status: 'active' },
];
```

**Cảnh báo Plan 03-01:** Verify `types.ts` `User.role` type field hiện đang chấp nhận literal nào. Nếu hiện chỉ `'admin' | 'viewer'` → cần extend `User.role: UserRole` HOẶC tách 2 type (BE-level `UserAPI.role: UserRole` Plan 03-01 vs mockData level `User.role: string`). Decision-time Claude discretion plan.

---

#### 3. `frontend/src/contexts/AuthContext.tsx` (VERIFY ONLY — FE-04 KHÔNG code change)

**Analog:** `frontend/src/contexts/AuthContext.tsx:25-43` (toàn file là analog chính nó — Plan 03-01 chỉ verify path `user?.user?.role` accessible sau api.ts UserAPI extend).

**State shape** (line 4-8 — Plan 03-02/03 consume):
```typescript
interface AuthState {
  user: UserWithRoles | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}
```

**refreshUser pattern** (line 25-43 — Plan 03-01 KHÔNG touch; BE Phase 2 đã trả `role` qua `api.me()`):
```typescript
const refreshUser = useCallback(async () => {
  const token = localStorage.getItem('access_token');
  if (!token) {
    setState({ user: null, isAuthenticated: false, isLoading: false });
    return;
  }
  try {
    const res = await api.me();
    if (res.success && res.data) {
      setState({ user: res.data, isAuthenticated: true, isLoading: false });
    } else {
      localStorage.removeItem('access_token');
      localStorage.removeItem('refresh_token');
      setState({ user: null, isAuthenticated: false, isLoading: false });
    }
  } catch {
    setState({ user: null, isAuthenticated: false, isLoading: false });
  }
}, []);
```

**Hook export** (line 80-84 — Plan 03-02/03 consumer pattern):
```typescript
export function useAuth() {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error('useAuth must be used within AuthProvider');
  return ctx;
}
```

**Verify-only checklist Plan 03-01:**
- [ ] `useAuth().user?.user?.role` TypeScript type-check OK sau Plan 03-01 UserAPI.role field add (path nested `UserWithRoles → UserAPI`).
- [ ] `tsc --noEmit` clean (existing CI gate).
- [ ] KHÔNG modify file source.

---

### Wave 2 (Plan 03-02 + Plan 03-03 parallel — file độc lập)

#### 4. `frontend/src/pages/UserManagement.tsx` form FE-01 (MODIFY line 37 + line 633-642 — Plan 03-02)

**Analog:** `frontend/src/pages/UserManagement.tsx:633-642` (radio group existing 2 option flex layout, Tailwind `text-accent focus:ring-accent`)

**Why analog:** Same file same component — Plan 03-02 chỉ mở rộng 2 → 3 radio option + ADD warning banner conditional. KHÔNG dùng shadcn RadioGroup (project KHÔNG init shadcn registry).

**Existing state hook** (line 37 — Plan 03-02 CHANGE type literal → `UserRole`):
```typescript
const [newUserRole, setNewUserRole] = useState<'admin' | 'viewer'>('viewer');
// Plan 03-02 CHANGE: useState<UserRole>('viewer')
```

**Imports pattern** (line 1-8 — Plan 03-02 ADD `import type { UserRole } from '../services/api';`):
```typescript
import React, { useState, useEffect, useCallback } from 'react';
import { api, HubAPI, UserWithRolesAPI } from '../services/api';
import { User } from '../types';
import { cn, getHubUrl } from '../lib/utils';
import { Search, Plus, Shield, UserX, UserCheck, X, CheckCircle2, MoreVertical, Loader2, Building2, Info, KeyRound, Copy, Check, AlertTriangle, Trash2 } from 'lucide-react';
import { motion, AnimatePresence } from 'motion/react';
import Pagination from '../components/Pagination';
import { useAuth } from '../contexts/AuthContext';
```

**Existing radio group pattern** (line 631-643 — Plan 03-02 REPLACE 2 → 3 option):
```tsx
<div className="space-y-1.5">
  <label className="text-sm font-medium text-slate-600 dark:text-slate-300">Quyền <span className="text-[11px] font-normal text-slate-400">(áp cho tất cả hub được gán)</span></label>
  <div className="flex gap-4">
    <label className="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="role" value="admin" checked={newUserRole === 'admin'} onChange={() => setNewUserRole('admin')} className="text-accent focus:ring-accent" />
      <span className="text-sm text-slate-700 dark:text-slate-200">Admin Hub</span>
    </label>
    <label className="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="role" value="viewer" checked={newUserRole === 'viewer'} onChange={() => setNewUserRole('viewer')} className="text-accent focus:ring-accent" />
      <span className="text-sm text-slate-700 dark:text-slate-200">Viewer</span>
    </label>
  </div>
</div>
```

**Plan 03-02 REPLACE pattern (per UI-SPEC §6.1 + §7.1 + §8.1):**
```tsx
<div className="space-y-1.5">
  <fieldset role="radiogroup" aria-labelledby="role-group-label">
    <legend id="role-group-label" className="text-sm font-medium text-slate-600 dark:text-slate-300">
      Quyền <span className="text-danger">*</span>
    </legend>

    {/* Option 1: Admin toàn hệ thống */}
    <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50 dark:hover:bg-slate-800 rounded-md">
      <input
        type="radio"
        name="role"
        value="admin"
        checked={newUserRole === 'admin'}
        onChange={() => setNewUserRole('admin')}
        aria-describedby="role-admin-desc"
        className="mt-1 text-accent focus:ring-accent"
      />
      <div>
        <div className="text-sm font-medium text-slate-700 dark:text-slate-200">Admin toàn hệ thống</div>
        <div id="role-admin-desc" className="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
          Có thể quản lý tất cả hub, user, settings. Cảnh báo: gán cho user TIN CẬY.
        </div>
      </div>
    </label>

    {/* Option 2: Quản lý hub này (hub_admin) */}
    <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50 dark:hover:bg-slate-800 rounded-md">
      <input
        type="radio"
        name="role"
        value="hub_admin"
        checked={newUserRole === 'hub_admin'}
        onChange={() => setNewUserRole('hub_admin')}
        aria-describedby="role-hubadmin-desc"
        className="mt-1 text-accent focus:ring-accent"
      />
      <div>
        <div className="text-sm font-medium text-slate-700 dark:text-slate-200">Quản lý hub này</div>
        <div id="role-hubadmin-desc" className="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
          Quản lý user + document trong hub được chỉ định. KHÔNG vào hub khác.
        </div>
      </div>
    </label>

    {/* Option 3: Viewer (default) */}
    <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50 dark:hover:bg-slate-800 rounded-md">
      <input
        type="radio"
        name="role"
        value="viewer"
        checked={newUserRole === 'viewer'}
        onChange={() => setNewUserRole('viewer')}
        aria-describedby="role-viewer-desc"
        className="mt-1 text-accent focus:ring-accent"
      />
      <div>
        <div className="text-sm font-medium text-slate-700 dark:text-slate-200">Viewer</div>
        <div id="role-viewer-desc" className="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
          Xem document + search trong hub được gán. KHÔNG quản lý user.
        </div>
      </div>
    </label>

    {/* Warning banner — conditional render khi admin selected */}
    {newUserRole === 'admin' && (
      <div
        role="alert"
        aria-live="polite"
        className="bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-md p-3 mt-2 text-sm font-medium dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200"
      >
        ⚠  Quyền cao nhất — quản lý toàn bộ hệ thống
      </div>
    )}
  </fieldset>
</div>
```

**Reference conditional render analog** (Phase 5 Login.tsx state machine 4 branch — `frontend/src/pages/Login.tsx` per UI-SPEC §6.2 chip render `{returnHub && ...}` pattern — Plan 03-02 mirror chỉ render warning banner khi `newUserRole === 'admin'`).

**Error envelope handle pattern (per UI-SPEC §7.4 — Plan 03-02 ADD switch ở `handleCreateUser`):**

Existing error pattern (line 374-375):
```typescript
if (!createRes.success || !createRes.data) {
  setAddError(createRes.error?.message ?? 'Tạo user thất bại.');
  return;
}
```

Plan 03-02 ADD error.code switch (BEFORE existing `.message` fallback):
```typescript
if (!createRes.success || !createRes.data) {
  const code = createRes.error?.code;
  let msg = createRes.error?.message ?? 'Tạo user thất bại.';
  if (code === 'HUB_ADMIN_REQUIRED') {
    msg = 'Bạn không có quyền tạo user với role Admin toàn hệ thống. Liên hệ Super Admin.';
  } else if (code === 'HUB_ID_REQUIRED') {
    msg = 'Vui lòng chọn hub trước khi xem danh sách user.';
  } else if (code === 'AUTH_STATE_INCONSISTENT') {
    msg = 'Lỗi hệ thống xác thực — liên hệ admin. Mã: AUTH_STATE_INCONSISTENT';
  }
  setAddError(msg);
  return;
}
```

---

#### 5. `frontend/src/pages/UserManagement.tsx` Manage modal FE-03 (MODIFY line 941-980 — Plan 03-03)

**Analog:** `frontend/src/pages/UserManagement.tsx:941-980` (button-style radio existing 2 option Manage modal "Quyền")

**Why analog:** Same file same modal — Plan 03-03 mở rộng 2 → 3 option + ADD disabled state cho "Admin toàn hệ thống" khi `currentUser.role !== 'admin'`. KHÁC FE-01 form pattern (button-style thay radio HTML — UI-SPEC §6.3 carry forward existing button visual).

**Existing button-style role selector** (line 941-980 — Plan 03-03 REPLACE expand 3 option + disabled):
```tsx
<div className="space-y-3">
  <label className="text-sm font-medium text-slate-600 dark:text-slate-300">
    Quyền <span className="text-[11px] font-normal text-slate-400">(áp cho tất cả hub user thuộc về)</span>
  </label>
  <div className="grid grid-cols-1 gap-2">
    <button
      onClick={() => setManageRole('admin')}
      className={cn(
        'flex items-center justify-between p-3 rounded-xl border transition-all text-left',
        manageRole === 'admin'
          ? 'border-brand-indigo bg-brand-indigo/[0.03] dark:bg-brand-indigo/[0.08] ring-1 ring-brand-indigo'
          : 'border-slate-200 dark:border-slate-700 hover:border-slate-300 dark:hover:border-slate-600 bg-white dark:bg-slate-800',
      )}
    >
      <div>
        <p className="text-sm font-bold text-slate-900 dark:text-white">Admin</p>
        <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Toàn quyền quản lý Hub, User và Tài liệu.</p>
      </div>
      {manageRole === 'admin' && <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white"><CheckCircle2 size={12} /></div>}
    </button>
    {/* + viewer button line 961-980 (cùng pattern) */}
```

**Existing state hook** (line 44 — Plan 03-03 CHANGE type literal → `UserRole`):
```typescript
const [manageRole, setManageRole] = useState<'admin' | 'viewer'>('viewer');
// Plan 03-03 CHANGE: useState<UserRole>('viewer')
```

**Plan 03-03 REPLACE pattern (per UI-SPEC §6.3 — currentUser-aware disabled cho hub_admin):**
```tsx
{(() => {
  const currentRole: UserRole = (currentUser?.user?.role as UserRole) ?? 'viewer';
  const isCurrentSuper = currentRole === 'admin';
  const isCurrentHubAdmin = currentRole === 'hub_admin';

  return (
    <div className="space-y-3">
      <label className="text-sm font-medium text-slate-600 dark:text-slate-300">
        Quyền <span className="text-[11px] font-normal text-slate-400">(áp cho tất cả hub user thuộc về)</span>
      </label>
      <div className="grid grid-cols-1 gap-2">
        {/* Option 1: Admin toàn hệ thống — DISABLED nếu hub_admin */}
        <button
          onClick={() => isCurrentSuper && setManageRole('admin')}
          disabled={!isCurrentSuper}
          aria-disabled={!isCurrentSuper}
          title={!isCurrentSuper ? 'Cần Admin toàn hệ thống' : undefined}
          className={cn(
            'flex items-center justify-between p-3 rounded-xl border transition-all text-left',
            manageRole === 'admin'
              ? 'border-brand-indigo bg-brand-indigo/[0.03] dark:bg-brand-indigo/[0.08] ring-1 ring-brand-indigo'
              : 'border-slate-200 dark:border-slate-700 hover:border-slate-300 dark:hover:border-slate-600 bg-white dark:bg-slate-800',
            !isCurrentSuper && 'opacity-50 cursor-not-allowed hover:border-slate-200 dark:hover:border-slate-700'
          )}
        >
          <div>
            <p className="text-sm font-bold text-slate-900 dark:text-white">Admin toàn hệ thống</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Toàn quyền quản lý Hub, User và Tài liệu.</p>
          </div>
          {manageRole === 'admin' && <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white"><CheckCircle2 size={12} /></div>}
        </button>

        {/* Option 2: Quản lý hub này (hub_admin) — luôn enable */}
        <button onClick={() => setManageRole('hub_admin')} className={cn(/* ... cùng pattern enabled */)}>
          <div>
            <p className="text-sm font-bold text-slate-900 dark:text-white">Quản lý hub này</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Quản lý user + document trong hub được chỉ định.</p>
          </div>
          {manageRole === 'hub_admin' && <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white"><CheckCircle2 size={12} /></div>}
        </button>

        {/* Option 3: Viewer */}
        <button onClick={() => setManageRole('viewer')} className={cn(/* ... cùng pattern enabled */)}>
          <div>
            <p className="text-sm font-bold text-slate-900 dark:text-white">Viewer</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Xem document + search trong hub được gán.</p>
          </div>
          {manageRole === 'viewer' && <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white"><CheckCircle2 size={12} /></div>}
        </button>
      </div>

      {/* a11y: hidden helper text linked via aria-describedby (UI-SPEC §8.1) */}
      {!isCurrentSuper && (
        <p id="role-admin-tooltip-desc" className="sr-only">
          Tùy chọn này yêu cầu quyền Admin toàn hệ thống.
        </p>
      )}
    </div>
  );
})()}
```

**Existing currentUser consume** (line 11 — Plan 03-03 reuse):
```typescript
const { user: currentUser } = useAuth();
```

**Error envelope handle pattern Manage modal (per UI-SPEC §7.4 — Plan 03-03 ADD switch ở `handleSubmitManageHub` line 192-218):**

Existing error pattern (line 207-209):
```typescript
const failed = results.filter(r => !r.success);
if (failed.length > 0) {
  setManageError(`${failed.length}/${results.length} cập nhật thất bại.`);
}
```

Plan 03-03 ADD error.code derive (failed[0].error.code switch):
```typescript
const failed = results.filter(r => !r.success);
if (failed.length > 0) {
  const code = failed[0].error?.code;
  let msg = `${failed.length}/${results.length} cập nhật thất bại.`;
  if (code === 'HUB_ADMIN_REQUIRED') {
    msg = 'Bạn không có quyền gán role này. Liên hệ Super Admin.';
  } else if (code === 'CROSS_HUB_USER_DELETE_DENIED') {
    msg = 'Không thể xóa user thuộc nhiều hub. Liên hệ Super Admin để xử lý.';
  }
  setManageError(msg);
}
```

---

#### 6. `frontend/src/Layout.tsx` hub switcher FE-02 (MODIFY ADD ABOVE line 217 — Plan 03-03)

**Analog:** `frontend/src/Layout.tsx:166-215` (sidebar header section — logo + branding title + collapse button) + `frontend/src/services/api.ts:172-174` (`api.getHubs()`)

**Why analog:** Hub switcher cần inject vào sidebar BETWEEN header (line 166-215) + nav items (line 217+). Pattern Phase 5 PROXY-04 sidebar branding logo carry forward — chỉ touch sidebar header area (UI-SPEC §11.1 minimal scope R-V3-2).

**Imports pattern** (line 1-30 — Plan 03-03 ADD `import type { UserRole, HubAPI } from './services/api';`):
```typescript
import React, { useState, useEffect } from 'react';
import { LayoutDashboard, Search, Users, Database, RefreshCw, History, Key, Zap, LogOut, Bell, ChevronLeft, ChevronRight, Menu, X, Search as SearchIcon, BookOpen, Settings as SettingsIcon, Sun, Moon } from 'lucide-react';
import { Link, useLocation, useNavigate, Outlet } from 'react-router-dom';
import { cn } from './lib/utils';
import { motion, AnimatePresence } from 'motion/react';
import GeminiAssistant from './components/GeminiAssistant';
import { useTheme } from './contexts/ThemeContext';
import { useAuth } from './contexts/AuthContext';
import { getBranding, getContrastTextColor } from './branding';
import { CURRENT_HUB } from './services/api';
// Plan 03-03 ADD: import { api } from './services/api';
//                 import type { UserRole, HubAPI } from './services/api';
```

**Existing useAuth + currentHub branding** (line 89, 30-35):
```typescript
const { user, logout } = useAuth();
// ...
const HUB_BRANDING = getBranding(CURRENT_HUB);
const HUB_CONTRAST = getContrastTextColor(HUB_BRANDING.themeColor);
```

**Sidebar header pattern existing** (line 166-215 — Plan 03-03 KHÔNG đụng, chỉ INSERT block sau line 215 + TRƯỚC `<nav>` line 217):
```tsx
<div className="h-16 flex items-center px-6 border-b border-slate-200/50 dark:border-slate-700/50">
  {(!collapsed || isMobileMenuOpen) && (
    <motion.div initial={{ opacity: 0, x: -10 }} animate={{ opacity: 1, x: 0 }} className="flex items-center gap-2">
      {/* Phase 5 PROXY-04 — sidebar header logo + title từ HUB_BRANDING (D-V3-Phase5-D2 LOCKED) */}
      <div className="w-8 h-8 rounded-xl flex items-center justify-center shadow-lg shrink-0" style={{ backgroundColor: HUB_BRANDING.themeColor }}>
        <img src={HUB_BRANDING.logo} alt={HUB_BRANDING.title} ... />
      </div>
      <span className="font-bold text-slate-900 dark:text-white tracking-tight text-lg truncate max-w-[160px]">
        {HUB_BRANDING.title}
      </span>
    </motion.div>
  )}
  {/* ... collapse button */}
</div>

{/* Plan 03-03 INSERT HERE — ABOVE <nav> */}

<nav className={cn("flex-1 p-4 space-y-1", ...)}>
```

**`api.getHubs()` existing** (line 172-174 — Plan 03-03 reuse `api.getHubs()`):
```typescript
async getHubs() {
  return this.request<HubAPI[]>('GET', '/api/hubs');
}
```

**Plan 03-03 INSERT pattern (per UI-SPEC §6.2 + §8.2 — Layout.tsx ABOVE line 217 `<nav>`):**
```tsx
{/* Plan 03-03 v3.1 Phase 3 FE-02 — Hub switcher sidebar */}
{/* Source: .planning/phases/03-frontend-form-refactor/03-CONTEXT.md D-V3.1-Phase3-A LOCKED (hardcode 'central' slug)
            .planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §6.2 + §8.2 */}
{(!collapsed || isMobileMenuOpen) && (
  <HubSwitcher />
)}
```

Và component `HubSwitcher` inline trong Layout.tsx (Claude discretion plan-time per CONTEXT.md `<decisions>` — extract `<HubSwitcher>` riêng nếu logic > 30 LOC):
```tsx
function HubSwitcher() {
  const { user, isLoading } = useAuth();
  const currentUser = user?.user;
  const currentRole: UserRole = (currentUser?.role as UserRole) ?? 'viewer';
  // hub_ids field MỚI từ Phase 2 BE — Plan 03-01 đã extend UserAPI; assume present qua nested wrapper
  const userHubIds: string[] = ((currentUser as unknown) as { hub_ids?: string[] })?.hub_ids ?? [];

  const [hubs, setHubs] = useState<HubAPI[]>([]);
  const [hubsLoading, setHubsLoading] = useState(true);

  useEffect(() => {
    api.getHubs().then((res) => {
      if (res.success && res.data) setHubs(res.data);
    }).finally(() => setHubsLoading(false));
  }, []);

  // Filter logic (CONTEXT.md D-V3.1-Phase3-A LOCKED slug 'central' hardcode)
  const visibleHubs = currentRole === 'admin'
    ? hubs
    : hubs.filter(h => h.code !== 'central' && userHubIds.includes(h.id));

  if (isLoading || hubsLoading) {
    return (
      <div className="px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50" aria-busy="true" aria-label="Đang tải danh sách hub">
        <div className="h-9 w-full rounded-md bg-slate-200 dark:bg-slate-800 animate-pulse" />
      </div>
    );
  }

  if (visibleHubs.length === 0) {
    return (
      <div role="status" aria-live="polite" className="px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50 text-sm text-slate-500 italic">
        Bạn chưa được gán hub nào — liên hệ admin.
      </div>
    );
  }

  return (
    <div className="px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50">
      <label htmlFor="hub-switcher" className="block text-xs uppercase text-slate-500 font-medium mb-1">
        Hub đang xem:
      </label>
      <select
        id="hub-switcher"
        value={CURRENT_HUB}
        onChange={(e) => { window.location.href = `/${e.target.value}/`; }}
        aria-label="Chọn hub đang xem"
        className="w-full rounded-md border border-slate-300 dark:border-slate-700 bg-white dark:bg-slate-900 px-3 py-2 text-sm focus:ring-2 focus:ring-accent focus:border-accent"
      >
        {visibleHubs.map(h => (
          <option key={h.code} value={h.code}>{h.name} ({h.code})</option>
        ))}
      </select>
    </div>
  );
}
```

**Cảnh báo Plan 03-03:** `UserAPI.hub_ids` field hiện tại CHƯA có (chỉ có `roles: RoleAPI[]` với `roles[i].hub_id`). Decision-time Claude discretion:
- **Option A:** Derive `userHubIds` từ `user?.roles?.map(r => r.hub_id) ?? []` (KHÔNG cần extend UserAPI thêm).
- **Option B:** Plan 03-01 extend UserAPI thêm `hub_ids: string[]` mirror BE Phase 2 nếu BE trả via `/api/auth/me`.

Recommend Option A (less invasive — UserWithRoles.roles đã có sẵn từ M2).

---

### Wave 3 (Plan 03-04 closeout — 3 test file mới + docs)

#### 7. `frontend/src/pages/__tests__/UserManagement.form-3-option.spec.tsx` (CREATE — FE-01 test)

**Analog:** `frontend/src/__tests__/Layout.spec.tsx:10-50` (renderLayoutWithCurrentHub helper + `vi.doMock` AuthContext pattern) + `frontend/src/pages/__tests__/Login.spec.tsx:46-108` (page render với mock auth + branching state)

**Why analog:** Page-level component test cần render `<UserManagement />` + mock `useAuth()` + mock `api.createUser()` + assert radio render + warning banner toggle. Layout.spec.tsx + Login.spec.tsx đã ship `vi.doMock` pattern cho AuthContext + ThemeContext.

**Test infrastructure (Phase 5 baseline — KHÔNG đụng):**
- `vitest.config.ts` (line 9-28) jsdom env + globals + include `src/**/__tests__/**/*.{ts,tsx,spec.ts,spec.tsx}`
- `src/test-setup.ts` (line 3-10) `@testing-library/jest-dom/vitest` + `cleanup()` afterEach

**Helper render pattern** (carry forward Layout.spec.tsx line 10-50):
```typescript
import React from 'react';
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';

async function renderUserManagement(opts: {
  currentRole?: 'admin' | 'hub_admin' | 'viewer';
}) {
  vi.resetModules();

  vi.doMock('../../contexts/AuthContext', () => ({
    useAuth: () => ({
      isAuthenticated: true,
      user: { user: { id: 'u1', name: 'Test', email: 't@medinet.vn', role: opts.currentRole ?? 'admin' }, roles: [] },
      logout: vi.fn(),
      isLoading: false,
      login: vi.fn(),
      refreshUser: vi.fn(),
    }),
    AuthProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  }));

  vi.doMock('../../services/api', () => ({
    api: {
      getHubs: vi.fn().mockResolvedValue({ success: true, data: [{ id: '1', code: 'dmd', name: 'Đỗ Minh Đường', status: 'active' }] }),
      listUsers: vi.fn().mockResolvedValue({ success: true, data: [], meta: { page: 1, per_page: 10, total: 0, total_pages: 1 } }),
      createUser: vi.fn().mockResolvedValue({ success: true, data: { id: 'new-user-id' } }),
      changeUserRole: vi.fn().mockResolvedValue({ success: true }),
    },
  }));

  const { default: UserManagement } = await import('../UserManagement');
  return render(
    <MemoryRouter>
      <UserManagement />
    </MemoryRouter>
  );
}
```

**Test scenarios (UI-SPEC §9.2 Test 1):**
```typescript
describe('Phase 3 v3.1 FE-01 — UserManagement form 3 option', () => {
  beforeEach(() => {
    vi.resetModules();
  });

  it('renders 3 radio option với label tiếng Việt đúng', async () => {
    await renderUserManagement({});
    // ... open Add modal first → assert 3 label visible
    expect(screen.queryByText(/Admin toàn hệ thống/)).toBeInTheDocument();
    expect(screen.queryByText(/Quản lý hub này/)).toBeInTheDocument();
    expect(screen.queryByText(/^Viewer$/)).toBeInTheDocument();
  });

  it('click "Admin toàn hệ thống" → warning banner mount', async () => {
    await renderUserManagement({});
    // ... open modal + click radio admin
    fireEvent.click(screen.getByLabelText(/Admin toàn hệ thống/));
    expect(screen.queryByText(/⚠.*Quyền cao nhất/)).toBeInTheDocument();
  });

  it('submit với role=hub_admin → POST /api/users body.role=hub_admin', async () => {
    const { container } = await renderUserManagement({});
    // ... fill form + click hub_admin radio + submit
    // Assert: api.createUser được call với body.role === 'hub_admin'
  });

  it('BE 403 HUB_ADMIN_REQUIRED → toast tiếng Việt', async () => {
    // Mock api.createUser reject với envelope HUB_ADMIN_REQUIRED
    // Assert: error message "Bạn không có quyền tạo user với role Admin toàn hệ thống. Liên hệ Super Admin."
  });
});
```

---

#### 8. `frontend/src/__tests__/Layout.hub-switcher.spec.tsx` (CREATE — FE-02 test)

**Analog:** `frontend/src/__tests__/Layout.spec.tsx` (toàn file — pattern Layout render với mock AuthContext + ThemeContext + GeminiAssistant + assert sidebar element).

**Why analog:** Same file naming convention + extend Layout test scope. Plan 03-04 có thể CHỌN extend existing Layout.spec.tsx HOẶC tách file riêng (Claude discretion plan-time).

**Test pattern (extend Layout.spec.tsx helper line 10-50 — add hub_ids + role param):**
```typescript
async function renderLayoutWithUser(opts: {
  currentRole: 'admin' | 'hub_admin' | 'viewer';
  userHubIds: string[];
  hubsList?: Array<{ id: string; code: string; name: string }>;
}) {
  vi.resetModules();

  vi.doMock('../contexts/AuthContext', () => ({
    useAuth: () => ({
      isAuthenticated: true,
      user: {
        user: { id: 'u1', name: 'Test', email: 't@medinet.vn', role: opts.currentRole },
        roles: opts.userHubIds.map(hid => ({ user_id: 'u1', hub_id: hid, role: opts.currentRole })),
      },
      logout: vi.fn(),
      isLoading: false,
      login: vi.fn(),
      refreshUser: vi.fn(),
    }),
    AuthProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  }));

  vi.doMock('../contexts/ThemeContext', () => ({
    useTheme: () => ({ theme: 'light', toggleTheme: vi.fn() }),
    ThemeProvider: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  }));

  vi.doMock('../components/GeminiAssistant', () => ({ default: () => null }));

  vi.doMock('../services/api', () => ({
    api: {
      getHubs: vi.fn().mockResolvedValue({
        success: true,
        data: opts.hubsList ?? [
          { id: 'c', code: 'central', name: 'Trung tâm', status: 'active' },
          { id: '1', code: 'dmd', name: 'Đỗ Minh Đường', status: 'active' },
          { id: '2', code: 'tdt', name: 'Thuốc Dân Tộc', status: 'active' },
        ],
      }),
    },
    CURRENT_HUB: 'central',
  }));

  const { default: Layout } = await import('../Layout');
  return render(<MemoryRouter><Layout /></MemoryRouter>);
}
```

**Test scenarios (UI-SPEC §9.2 Test 2):**
```typescript
describe('Phase 3 v3.1 FE-02 — Layout hub switcher filter', () => {
  it('super admin → switcher show ALL hubs (4 hub including central)', async () => {
    await renderLayoutWithUser({ currentRole: 'admin', userHubIds: [] });
    await waitFor(() => {
      expect(screen.queryByLabelText(/Chọn hub đang xem/)).toBeInTheDocument();
      expect(screen.queryByText(/Trung tâm.*central/)).toBeInTheDocument();
      expect(screen.queryByText(/Đỗ Minh Đường.*dmd/)).toBeInTheDocument();
    });
  });

  it('hub_admin dmd → switcher show ONLY dmd (filter central + tdt)', async () => {
    await renderLayoutWithUser({ currentRole: 'hub_admin', userHubIds: ['1'] });
    await waitFor(() => {
      expect(screen.queryByText(/Đỗ Minh Đường.*dmd/)).toBeInTheDocument();
      expect(screen.queryByText(/Trung tâm.*central/)).not.toBeInTheDocument();
      expect(screen.queryByText(/Thuốc Dân Tộc.*tdt/)).not.toBeInTheDocument();
    });
  });

  it('viewer với userHubIds=[] → empty state "Bạn chưa được gán hub nào"', async () => {
    await renderLayoutWithUser({ currentRole: 'viewer', userHubIds: [] });
    await waitFor(() => {
      expect(screen.queryByText(/Bạn chưa được gán hub nào/)).toBeInTheDocument();
    });
  });
});
```

---

#### 9. `frontend/src/pages/__tests__/UserManagement.manage-modal-disabled.spec.tsx` (CREATE — FE-03 test)

**Analog:** `frontend/src/pages/__tests__/Login.spec.tsx:46-108` (page render với branching state machine — analog conditional render disabled state)

**Why analog:** Login.spec.tsx demonstrate render page component + assert UI state khác nhau theo branch (return param valid/invalid). FE-03 test cần render Manage modal + assert "Admin toàn hệ thống" option disabled state khác nhau theo `currentUser.role`.

**Test scenarios (UI-SPEC §9.2 Test 3):**
```typescript
describe('Phase 3 v3.1 FE-03 — UserManagement Manage modal disabled option', () => {
  it('super admin currentUser → "Admin toàn hệ thống" option ENABLED', async () => {
    await renderUserManagement({ currentRole: 'admin' });
    // Open Manage modal cho user khác
    // ... click "Quản lý hub" action button
    const adminBtn = await screen.findByText(/Admin toàn hệ thống/);
    expect(adminBtn.closest('button')).not.toBeDisabled();
  });

  it('hub_admin currentUser → "Admin toàn hệ thống" DISABLED + title="Cần Admin toàn hệ thống"', async () => {
    await renderUserManagement({ currentRole: 'hub_admin' });
    // ... open Manage modal
    const adminBtn = await screen.findByText(/Admin toàn hệ thống/);
    const button = adminBtn.closest('button')!;
    expect(button).toBeDisabled();
    expect(button).toHaveAttribute('title', 'Cần Admin toàn hệ thống');
    expect(button).toHaveAttribute('aria-disabled', 'true');
  });

  it('hub_admin click disabled "Admin" → no state change (manageRole unchanged)', async () => {
    await renderUserManagement({ currentRole: 'hub_admin' });
    // ... open Manage modal (initial manageRole='viewer')
    const adminBtn = await screen.findByText(/Admin toàn hệ thống/);
    fireEvent.click(adminBtn.closest('button')!);
    // Assert: viewer option vẫn highlighted (manageRole still 'viewer')
  });
});
```

---

#### 10. Closeout docs (Plan 03-04 — STATE.md + REQUIREMENTS.md + ROADMAP.md + CLAUDE.md §6)

**Analog:** `.planning/phases/02-backend-rbac-enforcement/02-05-PLAN.md` + `02-05-SUMMARY.md` (Phase 2 closeout pattern carry forward — Plan 02-05 đã ship 2026-05-24)

**Why analog:** Plan 02-05 Phase 2 ship closeout 2026-05-24 — pattern fresh + 4 file docs update (STATE.md frontmatter + body Results Summary + REQUIREMENTS.md 5 dòng `[x] **DEP-XX**` + ROADMAP.md table + CLAUDE.md §6 subsection mới).

**Pattern closeout (Plan 02-05 line reference — Plan 03-04 mirror):**
1. **STATE.md frontmatter:** `completed_phases: 3` + `completed_plans: 12` + `percent: 80` (3/3 v3.1 phase done × ~4 plan avg = 12)
2. **STATE.md body Results Summary section:** Phase 3 v3.1 — 4 plan ship + 4 REQ-ID consumed (FE-01..04) + evidence chain test count
3. **REQUIREMENTS.md:** 4 dòng `[x] **FE-01** ... ✅ Plan 03-02` + `[x] **FE-02** ... ✅ Plan 03-03` + `[x] **FE-03** ... ✅ Plan 03-03` + `[x] **FE-04** ... ✅ Plan 03-01`
4. **ROADMAP.md:** Phase 3 row status DONE + plans checklist
5. **CLAUDE.md §6:** Phase 3 v3.1 subsection mới (sau Phase 2 v3.1 subsection cuối file) + pattern carry forward summary

Smoke checkpoint runtime SKIP pre-resolved (auto-fallback `--chain` mode active per Plan 04-07 + 05-06 + 06-05 v3.0 + Plan 01-03 + 02-05 v3.1 carry forward).

---

## Shared Patterns (cross-cutting concerns)

### Authentication / Hook Access

**Source:** `frontend/src/contexts/AuthContext.tsx:80-84` (useAuth hook export)
**Apply to:** Plan 03-02 (UserManagement.tsx form FE-01 currentUser super-admin check) + Plan 03-03 (UserManagement.tsx Manage modal FE-03 + Layout.tsx hub switcher FE-02)

```typescript
import { useAuth } from '../contexts/AuthContext';
// ...
const { user, isLoading } = useAuth();
const currentUser = user?.user;  // nested wrapper UserWithRoles → UserAPI
const currentRole: UserRole = (currentUser?.role as UserRole) ?? 'viewer';  // defensive default
```

### Error Envelope Handling (Phase 2 BE Plan 02-01..05 4 envelope code mới)

**Source:** `frontend/src/pages/UserManagement.tsx:374-376` (existing `setAddError(createRes.error?.message ...)` pattern)
**Apply to:** Plan 03-02 (handleCreateUser FE-01 form error) + Plan 03-03 (handleSubmitManageHub FE-03 modal error)

```typescript
const code = res.error?.code;
let msg = res.error?.message ?? 'Thao tác thất bại.';
switch (code) {
  case 'HUB_ADMIN_REQUIRED': msg = 'Bạn không có quyền tạo user với role Admin toàn hệ thống. Liên hệ Super Admin.'; break;
  case 'HUB_ID_REQUIRED': msg = 'Vui lòng chọn hub trước khi xem danh sách user.'; break;
  case 'CROSS_HUB_USER_DELETE_DENIED': msg = 'Không thể xóa user thuộc nhiều hub. Liên hệ Super Admin để xử lý.'; break;
  case 'AUTH_STATE_INCONSISTENT': msg = 'Lỗi hệ thống xác thực — liên hệ admin. Mã: AUTH_STATE_INCONSISTENT'; break;
  case 'FORBIDDEN': msg = 'Bạn không có quyền thực hiện thao tác này.'; break;
}
setAddError(msg);
```

### Tailwind Class Tokens

**Source:** `frontend/src/pages/UserManagement.tsx:635, 639` (radio Tailwind class) + Phase 5 UI-SPEC §4 (warning banner colors)
**Apply to:** Plan 03-02 (3 radio option + warning banner) + Plan 03-03 (3 button-style + disabled state + hub switcher dropdown)

| Component | Tailwind class |
|-----------|----------------|
| Radio input | `text-accent focus:ring-accent` |
| Radio focus ring | `focus:ring-2 focus:ring-accent focus:ring-offset-2` |
| Warning banner (FE-01) | `bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-md p-3 mt-2 dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200` |
| Disabled button (FE-03) | `opacity-50 cursor-not-allowed` |
| Active hub switcher row (FE-02) | `bg-accent/10 text-accent font-semibold` |
| Sidebar block (FE-02 wrapper) | `px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50` |
| Loading skeleton (FE-02) | `h-9 w-full rounded-md bg-slate-200 dark:bg-slate-800 animate-pulse` |

### Accessibility (WCAG AA + ARIA)

**Source:** UI-SPEC.md §8 (4-section a11y contract) + branding/registry.spec.ts pattern carry forward
**Apply to:** All 3 Wave 2 components (FE-01 form + FE-02 hub switcher + FE-03 modal)

| Element | ARIA attribute |
|---------|----------------|
| Radio group container | `role="radiogroup"` + `aria-labelledby` |
| Radio input | `aria-describedby="role-{value}-desc"` |
| Warning banner | `role="alert" aria-live="polite"` |
| Disabled option | `disabled` + `aria-disabled="true"` + native `title="..."` tooltip |
| Hub switcher select | `aria-label="Chọn hub đang xem"` + `<label htmlFor>` |
| Empty state | `role="status" aria-live="polite"` |
| Loading skeleton | `aria-busy="true" aria-label="Đang tải..."` |
| Hidden helper text | `<p id="..." className="sr-only">` linked via `aria-describedby` |

### vitest Mock Pattern (Plan 03-04 Wave 3 test setup)

**Source:** `frontend/src/__tests__/Layout.spec.tsx:10-50` (renderLayoutWithCurrentHub helper)
**Apply to:** All 3 test file mới Plan 03-04

```typescript
async function renderXxx(opts: { /* test-specific */ }) {
  vi.resetModules();
  Object.defineProperty(window, 'location', { value: { ...window.location, /* override */ }, writable: true });

  vi.doMock('../contexts/AuthContext', () => ({
    useAuth: () => ({ /* mock state */ }),
    AuthProvider: ({ children }) => <>{children}</>,
  }));

  vi.doMock('../services/api', () => ({ api: { /* mock methods */ } }));

  const { default: Component } = await import('../Component');
  return render(<MemoryRouter><Component /></MemoryRouter>);
}
```

---

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| (none) | — | — | Toàn bộ 9 file Phase 3 touch (6 source + 3 test mới) match ≥ 1 analog trong `Hub_All/frontend/src/`. **Zero-coverage = 0.** |

**Coverage verification:**
- 6/6 source file (3 visual + 3 type/mock) match analog cùng file HOẶC sibling file (UserManagement.tsx form/modal là analog của chính nó; api.ts + mockData.ts + AuthContext.tsx self-analog; Layout.tsx sidebar header pattern Phase 5 carry forward).
- 3/3 test file match `Layout.spec.tsx` + `Login.spec.tsx` + `api.spec.ts` Phase 5 baseline vitest infrastructure.

---

## Metadata

**Analog search scope:**
- `Hub_All/frontend/src/services/api.ts` (819 LOC)
- `Hub_All/frontend/src/pages/UserManagement.tsx` (1061 LOC)
- `Hub_All/frontend/src/Layout.tsx` (377 LOC)
- `Hub_All/frontend/src/contexts/AuthContext.tsx` (85 LOC)
- `Hub_All/frontend/src/mockData.ts` (line 1-115 audited)
- `Hub_All/frontend/src/__tests__/Layout.spec.tsx` (79 LOC — Phase 5 PROXY-04 test analog)
- `Hub_All/frontend/src/__tests__/App.spec.tsx` (Phase 5 baseline — scope smoke)
- `Hub_All/frontend/src/pages/__tests__/Login.spec.tsx` (109 LOC — Phase 5 state machine test analog)
- `Hub_All/frontend/src/services/__tests__/api.spec.ts` (237 LOC — Phase 5 prefix detect test analog)
- `Hub_All/frontend/src/branding/__tests__/registry.spec.ts` (105 LOC — Phase 5 registry test analog)
- `Hub_All/frontend/vitest.config.ts` + `frontend/src/test-setup.ts` (Phase 5 Wave 0 infra)

**Files scanned:** 11 file `Hub_All/frontend/src/` + 2 file vitest config = 13 file total

**Pattern extraction date:** 2026-05-24

**Wave grouping (D-V3.1-Phase3-E LOCKED):**
- **Wave 1 BLOCKING** — Plan 03-01 (api.ts UserRole export + UserAPI.role extend + mockData.ts 2 fixture + AuthContext.tsx verify-only)
- **Wave 2 parallel** — Plan 03-02 (UserManagement.tsx form FE-01 line 37 + 633-642) **|** Plan 03-03 (UserManagement.tsx Manage modal FE-03 line 941-980 + Layout.tsx hub switcher FE-02 ABOVE line 217)
- **Wave 3 BLOCKING** — Plan 03-04 closeout (3 test file mới + 4 docs update)

**Critical path:** Wave 1 (1 plan) → Wave 2 (2 plan parallel) → Wave 3 (1 plan) = 4 plan total (match ROADMAP v3.1 estimate "3-4 plan").

---

## References

### Upstream artifacts (consumed by this PATTERNS.md)

- `Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md` — 5 D-V3.1-Phase3-A..E LOCKED 2026-05-24 (auto-mode generate)
- `Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md` — 13 section UI design contract 2026-05-24 (Component Inventory + Component Contracts + Copywriting + a11y + Test Scenarios)

### Carry forward pattern (Phase 5 v3.0 + Phase 2 v3.1)

- `Hub_All/.planning/phases/05-reverse-proxy-frontend-subpath/05-02-PLAN.md` — vitest Wave 0 install + prefix detect pattern (api.ts module-level export const + literal union)
- `Hub_All/.planning/phases/05-reverse-proxy-frontend-subpath/05-04-PLAN.md` — Login.tsx 4 state machine + Layout.tsx sidebar branding (minimal scope R-V3-2 — chỉ touch sidebar header)
- `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-05-PLAN.md` — Closeout pattern 4 docs update (STATE + REQUIREMENTS + ROADMAP + CLAUDE.md §6)
- `Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md` — 4 error envelope mới (HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT) — Plan 03-02/03 error.code switch consume

### Frontend source file audit 2026-05-24 (per CONTEXT.md `<canonical_refs>`)

- `Hub_All/frontend/src/services/api.ts` line 1-46 (prefix detect + KNOWN_HUBS export — Phase 5) + line 167-194 (api.me + api.getHubs) + line 467-498 (UserWithRoles + UserAPI + RoleAPI interface) + line 500-514 (HubAPI interface)
- `Hub_All/frontend/src/pages/UserManagement.tsx` line 1-50 (imports + state hook) + line 152-220 (handleOpenManageHub + handleSubmitManageHub) + line 300-420 (handleCreateUser) + line 600-705 (Add modal form) + line 905-1000 (Manage modal)
- `Hub_All/frontend/src/Layout.tsx` line 1-95 (imports + branding + useAuth) + line 165-245 (sidebar header + nav)
- `Hub_All/frontend/src/contexts/AuthContext.tsx` line 1-85 (toàn file — Plan 03-01 verify only)
- `Hub_All/frontend/src/mockData.ts` line 1-115 (MOCK_HUBS + MOCK_USERS schema)
- `Hub_All/frontend/src/__tests__/Layout.spec.tsx` line 10-79 (vi.doMock helper pattern)
- `Hub_All/frontend/src/pages/__tests__/Login.spec.tsx` line 46-108 (renderLoginAtUrl branching state test)
- `Hub_All/frontend/src/services/__tests__/api.spec.ts` line 36-148 (prefix detect + crossHubSearch absolute path test)
- `Hub_All/frontend/src/branding/__tests__/registry.spec.ts` line 14-105 (4 hub branding + WCAG contrast + fallback test)
- `Hub_All/frontend/vitest.config.ts` line 9-28 (jsdom env + include glob)
- `Hub_All/frontend/src/test-setup.ts` line 3-10 (jest-dom matchers + cleanup)

### Memory references (user-level cross-session)

- `memory/project_real_hubs_deployment.md` — 2 hub thật DB (dmd Đỗ Minh Đường + tdt Thuốc Dân Tộc) cho mockData fixture Plan 03-01
- `memory/project_rbac_hub_admin_gap.md` — v3.1 milestone trigger 2026-05-23 (Plan 03-01 UserRole literal mirror BE)

---

*Phase: 03-frontend-form-refactor*
*PATTERNS.md generated: 2026-05-24 via gsd-phase-pattern-mapper auto-mode (CONTEXT.md 5 D-V3.1-Phase3-A..E LOCKED + UI-SPEC.md 13 section + Phase 5 PROXY-04 vitest infrastructure carry forward + Phase 2 v3.1 DEP-01..05 error envelope contract)*
*Language: Vietnamese (CLAUDE.md §3 enforce)*
