---
phase: 03
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/pages/UserManagement.tsx
autonomous: true
requirements: [FE-01]
tags: [frontend, react, form, radio-group, warning-banner, error-envelope, accessibility]
must_haves:
  truths:
    - "UserManagement.tsx Add User form chứa 3 radio option: 'Admin toàn hệ thống' (admin) + 'Quản lý hub này' (hub_admin) + 'Viewer' (viewer) — KHÔNG còn label 'Admin Hub' mơ hồ"
    - "newUserRole state typed UserRole (4 value) thay 'admin' | 'viewer' (line 37 hiện tại)"
    - "Warning banner màu yellow (bg-yellow-50/dark:bg-yellow-900/20) hiển thị chỉ khi newUserRole === 'admin' với content '⚠ Quyền cao nhất — quản lý toàn bộ hệ thống'"
    - "Mỗi radio option có description text tiếng Việt theo UI-SPEC §7.1 + ARIA aria-describedby link tới description"
    - "Submit form POST /api/users body chứa role: newUserRole — backend Phase 2 Plan 02-03 đã accept role='hub_admin' qua Pydantic Literal extend"
    - "Error envelope handle 4 BE code mới Phase 2 (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN) qua switch error.code với toast tiếng Việt đúng UI-SPEC §7.4"
    - "Form 3 radio dùng <fieldset role='radiogroup'> + <legend> + aria-labelledby + native HTML radio (KHÔNG shadcn) — WCAG 2.4.7 focus ring"
    - "TypeScript build + ESLint + Vitest baseline PASS clean"
  artifacts:
    - path: "frontend/src/pages/UserManagement.tsx"
      provides: "Form 3 option radio + warning banner + error envelope switch — FE-01 visual + UX + a11y contract complete"
      contains: "Quản lý hub này"
  key_links:
    - from: "UserManagement.tsx form line 37 newUserRole state"
      to: "UserManagement.tsx handleCreateUser POST /api/users body.role"
      via: "Form submit handler propagate state vào request body"
      pattern: "role: newUserRole"
    - from: "UserManagement.tsx error.code switch"
      to: "Phase 2 BE envelope HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT"
      via: "Toast message tiếng Việt đúng UI-SPEC §7.4"
      pattern: "HUB_ADMIN_REQUIRED"
    - from: "UserManagement.tsx fieldset role='radiogroup'"
      to: "ARIA legend + aria-describedby links"
      via: "Screen reader announce label + description + warning banner role='alert'"
      pattern: "role=\"radiogroup\""
---

<objective>
Plan 03-02 v3.1 Phase 3 FE-01 — Wave 2 (sequential SAU Plan 03-03 vì cùng file UserManagement.tsx Manage modal Plan 03-03 cũng touch — Wave assignment thực tế sequential): UserManagement.tsx Add User form radio group refactor 2 → 3 option (Admin toàn hệ thống / Quản lý hub này / Viewer) + warning banner conditional + error envelope handle 4 code mới Phase 2.

**Purpose:** Phase 2 backend đã accept `role='hub_admin'` Pydantic Literal extend (Plan 02-03 DEP-03). UserManagement form hiện chỉ 2 radio option `admin | viewer` (line 633-642) với label "Admin Hub" mơ hồ. Plan 03-02 refactor form 3 option rõ ràng + warning banner cho Admin toàn hệ thống + handle 4 envelope mới (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + AUTH_STATE_INCONSISTENT).

**Output:**
- `frontend/src/pages/UserManagement.tsx` — line 37 state type change (UserRole) + line 633-642 radio group REPLACE (3 option + warning banner) + handleCreateUser error.code switch ADD + import UserRole type.

**Scope:**
- Add User form section ONLY (line 322-705 estimate vùng modal Add).
- KHÔNG đụng Manage modal (Plan 03-03 sẽ refactor).
- KHÔNG đụng list table, search, pagination, delete confirm dialog (existing M2/v3.0 carry forward).

**Pattern decisions Claude discretion:**
- Pattern carry forward — radio HTML native (KHÔNG shadcn RadioGroup — project KHÔNG init registry).
- Warning banner conditional render qua React `{newUserRole === 'admin' && <div>...</div>}` (KHÔNG transition library cần — defer v4.0).
- Error envelope switch case `error.code` BEFORE generic `error.message` fallback (defensive — match Phase 2 envelope shape).
- Tooltip native `title=""` attribute (KHÔNG dùng plugin — defer v4.0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-01-SUMMARY.md
@Hub_All/frontend/src/pages/UserManagement.tsx
@Hub_All/frontend/src/services/api.ts

<interfaces>
<!-- Plan 03-01 ship type contract — Plan 03-02 import UserRole -->

From Hub_All/frontend/src/services/api.ts (Plan 03-01 ship):
```typescript
export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer';

export interface UserAPI {
  // ... existing fields
  role: UserRole;  // Plan 03-01 add
  // ...
}
```

From Hub_All/frontend/src/pages/UserManagement.tsx (state line 37 hiện tại):
```typescript
const [newUserRole, setNewUserRole] = useState<'admin' | 'viewer'>('viewer');
// Plan 03-02 CHANGE: useState<UserRole>('viewer')
```

From Hub_All/frontend/src/pages/UserManagement.tsx (radio group line 633-642 hiện tại — Plan 03-02 REPLACE):
```tsx
<div className="space-y-1.5">
  <label className="text-sm font-medium text-slate-600 dark:text-slate-300">Quyền <span className="text-[11px] font-normal text-slate-400">(áp cho tất cả hub được gán)</span></label>
  <div className="flex gap-4">
    <label className="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="role" value="admin" checked={newUserRole === 'admin'} onChange={() => setNewUserRole('admin')} className="text-accent focus:ring-accent" />
      <span className="text-sm text-slate-700 dark:text-slate-200">Admin Hub</span>
    </label>
    <label className="flex items-center gap-2 cursor-pointer">
      <input type="radio" name="role" value="viewer" checked={newUserRole === 'viewer'} onChange={() => setNewUserRole('viewer')} className="text-accent focus:ring-accent" />
      <span className="text-sm text-slate-700 dark:text-slate-200">Viewer</span>
    </label>
  </div>
</div>
```

From Hub_All/frontend/src/pages/UserManagement.tsx (error handle line 374-376 hiện tại — Plan 03-02 ADD switch trước fallback):
```typescript
if (!createRes.success || !createRes.data) {
  setAddError(createRes.error?.message ?? 'Tạo user thất bại.');
  return;
}
```

BE envelope shape (Phase 2 v3.1 Plan 02-01..04 ship — Plan 03-02 consume):
- HUB_ADMIN_REQUIRED — hub_admin tạo user role='admin' (T-02-02-E business logic block)
- HUB_ID_REQUIRED — hub_admin GET /api/users thiếu hub_id query
- CROSS_HUB_USER_DELETE_DENIED — DELETE handler 3-branch B1 iter 1 (KHÔNG áp dụng form create — defer Plan 03-03)
- AUTH_STATE_INCONSISTENT — Plan 02-02 invariant guard
- FORBIDDEN — require_role generic fallback
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Add User form radio group 2 → 3 option + warning banner + state type extend</name>
  <read_first>
    - Hub_All/frontend/src/pages/UserManagement.tsx (TOÀN FILE — biết state line 37, Add modal form line ~322-705, handleCreateUser logic line ~370-400, imports line 1-15)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "4. UserManagement.tsx form FE-01" — concrete REPLACE pattern + analog line 633-642)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §6.1 (visual layout 3 option + warning banner) + §7.1 (copywriting exact tiếng Việt) + §8.1 (a11y radio group ARIA)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md decisions D-V3.1-Phase3-D (UserRole alias) + `<specifics>` form 3 option layout
    - Hub_All/frontend/src/services/api.ts (line ~480 UserRole export after Plan 03-01)
  </read_first>
  <behavior>
    - Test 1 (Grep import UserRole): grep `import type { UserRole }` HOẶC `import type {.*UserRole.*}` trong UserManagement.tsx >= 1.
    - Test 2 (Grep state extend): grep `useState<UserRole>` trong UserManagement.tsx >= 1 (line 37 area).
    - Test 3 (Grep 3 radio): grep `<input type="radio"` trong UserManagement.tsx >= 3 (3 option mới) HOẶC grep `value="hub_admin"` >= 1 (option mới hub_admin).
    - Test 4 (Grep copywriting): grep `Admin toàn hệ thống` trong UserManagement.tsx >= 1 + grep `Quản lý hub này` >= 1 (exact UI-SPEC §7.1 — KHÔNG paraphrase).
    - Test 5 (Grep warning banner): grep `bg-yellow-50` HOẶC `dark:bg-yellow-900/20` trong UserManagement.tsx >= 1.
    - Test 6 (Grep warning copy): grep `Quyền cao nhất` trong UserManagement.tsx >= 1 (exact warning banner copy UI-SPEC §7.1).
    - Test 7 (Grep ARIA): grep `role="radiogroup"` trong UserManagement.tsx >= 1 + grep `aria-describedby` >= 1.
    - Test 8 (TypeScript build): `cd frontend && npx tsc --noEmit` exits 0.
  </behavior>
  <action>
**Bước 1 — ADD import UserRole** (top of UserManagement.tsx line 1-15):

Tìm dòng import api types hiện tại:
```typescript
import { api, HubAPI, UserWithRolesAPI } from '../services/api';
```

REPLACE bằng:
```typescript
import { api, HubAPI, UserWithRolesAPI } from '../services/api';
import type { UserRole } from '../services/api';
```

HOẶC merge inline:
```typescript
import { api, HubAPI, UserWithRolesAPI } from '../services/api';
import type { UserRole } from '../services/api';
```

(Pick whichever đúng project ESLint convention — `type` import separate là standard).

**Bước 2 — CHANGE state type** (line 37 area):

Tìm:
```typescript
const [newUserRole, setNewUserRole] = useState<'admin' | 'viewer'>('viewer');
```

REPLACE bằng:
```typescript
const [newUserRole, setNewUserRole] = useState<UserRole>('viewer');
```

KHÔNG đổi giá trị default `'viewer'` (Plan 03-02 acceptance — default carry forward).

**Bước 3 — REPLACE radio group block** (line 631-643 area — Add User modal form):

REPLACE toàn bộ `<div className="space-y-1.5">` chứa 2 radio cũ bằng:

```tsx
<div className="space-y-1.5">
  <fieldset role="radiogroup" aria-labelledby="role-group-label" className="space-y-2">
    <legend id="role-group-label" className="text-sm font-medium text-slate-600 dark:text-slate-300">
      Quyền <span className="text-danger">*</span>
    </legend>

    {/* Option 1: Admin toàn hệ thống */}
    <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50 dark:hover:bg-slate-800 rounded-md">
      <input
        type="radio"
        name="role"
        value="admin"
        checked={newUserRole === 'admin'}
        onChange={() => setNewUserRole('admin')}
        aria-describedby="role-admin-desc"
        className="mt-1 text-accent focus:ring-accent"
      />
      <div>
        <div className="text-sm font-medium text-slate-700 dark:text-slate-200">Admin toàn hệ thống</div>
        <div id="role-admin-desc" className="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
          Có thể quản lý tất cả hub, user, settings. Cảnh báo: gán cho user TIN CẬY.
        </div>
      </div>
    </label>

    {/* Option 2: Quản lý hub này (hub_admin) */}
    <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50 dark:hover:bg-slate-800 rounded-md">
      <input
        type="radio"
        name="role"
        value="hub_admin"
        checked={newUserRole === 'hub_admin'}
        onChange={() => setNewUserRole('hub_admin')}
        aria-describedby="role-hubadmin-desc"
        className="mt-1 text-accent focus:ring-accent"
      />
      <div>
        <div className="text-sm font-medium text-slate-700 dark:text-slate-200">Quản lý hub này</div>
        <div id="role-hubadmin-desc" className="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
          Quản lý user + document trong hub được chỉ định. KHÔNG vào hub khác.
        </div>
      </div>
    </label>

    {/* Option 3: Viewer (default) */}
    <label className="flex items-start gap-2.5 p-2.5 cursor-pointer hover:bg-slate-50 dark:hover:bg-slate-800 rounded-md">
      <input
        type="radio"
        name="role"
        value="viewer"
        checked={newUserRole === 'viewer'}
        onChange={() => setNewUserRole('viewer')}
        aria-describedby="role-viewer-desc"
        className="mt-1 text-accent focus:ring-accent"
      />
      <div>
        <div className="text-sm font-medium text-slate-700 dark:text-slate-200">Viewer</div>
        <div id="role-viewer-desc" className="text-sm text-slate-600 dark:text-slate-400 leading-relaxed">
          Xem document + search trong hub được gán. KHÔNG quản lý user.
        </div>
      </div>
    </label>

    {/* Warning banner — conditional render khi admin selected */}
    {newUserRole === 'admin' && (
      <div
        role="alert"
        aria-live="polite"
        className="bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-md p-3 mt-2 text-sm font-medium dark:bg-yellow-900/20 dark:border-yellow-800 dark:text-yellow-200"
      >
        ⚠  Quyền cao nhất — quản lý toàn bộ hệ thống
      </div>
    )}
  </fieldset>
</div>
```

**Bước 4 — verify TypeScript build:**

```bash
cd Hub_All/frontend && npx tsc --noEmit
```

PHẢI exit 0. Nếu fail vì `UserRole` type narrow strict, audit import line 1-15.

**Bước 5 — verify visual layout qua dev preview (optional manual smoke):**

```bash
cd Hub_All/frontend && npm run dev
```

Mở http://localhost:5173 → Users → Add User modal → verify 3 radio visible + click admin → warning banner mount + click viewer → warning unmount. **Manual smoke optional — Plan 03-04 vitest tự động verify scenario này (Wave 3).**

KHÔNG đổi:
- Tên state `newUserRole` (preserve handleCreateUser line 370+ consume).
- Default value `'viewer'`.
- Outer `<div className="space-y-1.5">` wrapper (preserve form section spacing).
- Form submit handler `handleCreateUser` (Task 2 sẽ extend error switch).
- Field hub_id select dropdown (existing).
- Field email/name/password inputs (existing).
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && cd ../.. && grep -c "import type { UserRole }" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "useState<UserRole>" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c 'value="hub_admin"' Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "Admin toàn hệ thống" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "Quản lý hub này" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "bg-yellow-50" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "Quyền cao nhất" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c 'role="radiogroup"' Hub_All/frontend/src/pages/UserManagement.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
    - grep `import type { UserRole }` trong UserManagement.tsx >= 1 (type-only import per ESLint convention).
    - grep `useState<UserRole>` trong UserManagement.tsx >= 1 (state type extended).
    - grep `value="hub_admin"` trong UserManagement.tsx >= 1 (option mới hub_admin radio).
    - grep `Admin toàn hệ thống` trong UserManagement.tsx >= 1 (exact label tiếng Việt UI-SPEC §7.1).
    - grep `Quản lý hub này` trong UserManagement.tsx >= 1 (exact label tiếng Việt).
    - grep `^.*Viewer.*$` trong UserManagement.tsx có >= 1 occurrence là option 3 label (giữ tên Viewer).
    - grep `bg-yellow-50` trong UserManagement.tsx >= 1 (warning banner light mode color).
    - grep `dark:bg-yellow-900/20` trong UserManagement.tsx >= 1 (warning banner dark mode color).
    - grep `Quyền cao nhất` trong UserManagement.tsx >= 1 (exact warning banner copy UI-SPEC §7.1).
    - grep `role="radiogroup"` trong UserManagement.tsx >= 1 (ARIA fieldset role).
    - grep `aria-describedby` trong UserManagement.tsx >= 3 (3 radio link description).
    - grep `aria-live="polite"` trong UserManagement.tsx >= 1 (warning banner announce).
    - grep `Có thể quản lý tất cả hub, user, settings` trong UserManagement.tsx >= 1 (exact description option 1).
    - grep `Quản lý user . document trong hub được chỉ định` trong UserManagement.tsx >= 1 (exact description option 2).
    - grep `Xem document . search trong hub được gán` trong UserManagement.tsx >= 1 (exact description option 3).
    - KHÔNG còn label `Admin Hub` (search/grep `>Admin Hub<` == 0).
    - KHÔNG đụng các state khác (manageRole, hubs, users, search, pagination — preserve existing).
  </acceptance_criteria>
  <done>
    Form 3 radio option ship với copywriting tiếng Việt đúng UI-SPEC §7.1 + warning banner conditional + ARIA full (role/aria-describedby/aria-live) + TypeScript build PASS; default state `'viewer'` preserve; handleCreateUser submit body chứa role mới (Task 2 sẽ extend error envelope handle).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handleCreateUser error.code switch ADD cho 4 envelope mới Phase 2</name>
  <read_first>
    - Hub_All/frontend/src/pages/UserManagement.tsx (handleCreateUser function — line ~370-410 estimate; biết error path hiện tại `setAddError(createRes.error?.message ...)`)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "Error Envelope Handling" — shared cross-cutting pattern + switch case template)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §7.4 (Error messages copy tiếng Việt exact 4 envelope)
    - Hub_All/.planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md (envelope spec HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN body shape)
  </read_first>
  <behavior>
    - Test 1 (Grep switch): grep `case 'HUB_ADMIN_REQUIRED'` trong UserManagement.tsx >= 1.
    - Test 2 (Grep HUB_ID_REQUIRED): grep `HUB_ID_REQUIRED` trong UserManagement.tsx >= 1.
    - Test 3 (Grep AUTH_STATE_INCONSISTENT): grep `AUTH_STATE_INCONSISTENT` trong UserManagement.tsx >= 1.
    - Test 4 (Grep error.code access): grep `error\\?\\.code` HOẶC `\\.error\\?\\.code` trong UserManagement.tsx >= 1.
    - Test 5 (Grep toast Vietnamese exact): grep `Bạn không có quyền tạo user với role Admin toàn hệ thống` trong UserManagement.tsx >= 1 (exact UI-SPEC §7.4).
    - Test 6 (TypeScript build): `cd frontend && npx tsc --noEmit` exits 0.
  </behavior>
  <action>
**Bước 1 — ADD error.code switch trong handleCreateUser** (BEFORE existing `setAddError(... ?.message ...)` fallback):

Tìm block error handle hiện tại (line ~374):
```typescript
if (!createRes.success || !createRes.data) {
  setAddError(createRes.error?.message ?? 'Tạo user thất bại.');
  return;
}
```

REPLACE bằng:
```typescript
if (!createRes.success || !createRes.data) {
  const code = createRes.error?.code;
  let msg = createRes.error?.message ?? 'Tạo user thất bại.';
  // Plan 03-02 v3.1 Phase 3 FE-01 — switch BE envelope code Phase 2 v3.1 (Plan 02-01..04 ship)
  // Source: .planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md envelope spec
  //         .planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §7.4 toast copy tiếng Việt
  switch (code) {
    case 'HUB_ADMIN_REQUIRED':
      msg = 'Bạn không có quyền tạo user với role Admin toàn hệ thống. Liên hệ Super Admin.';
      break;
    case 'HUB_ID_REQUIRED':
      msg = 'Vui lòng chọn hub trước khi tạo user.';
      break;
    case 'AUTH_STATE_INCONSISTENT':
      msg = 'Lỗi hệ thống xác thực — liên hệ admin. Mã: AUTH_STATE_INCONSISTENT';
      break;
    case 'FORBIDDEN':
      msg = 'Bạn không có quyền thực hiện thao tác này.';
      break;
  }
  setAddError(msg);
  return;
}
```

**Lưu ý:**
- `CROSS_HUB_USER_DELETE_DENIED` KHÔNG áp dụng form create (chỉ DELETE handler — defer Plan 03-03 Manage modal section).
- Generic fallback `msg = createRes.error?.message ?? 'Tạo user thất bại.'` PRESERVE (BE message trả về nếu code KHÔNG match 4 case mới — defensive).
- Comment trace decision source — Phase 2 envelope spec + UI-SPEC §7.4.

**Bước 2 — verify TypeScript build clean:**

```bash
cd Hub_All/frontend && npx tsc --noEmit
```

PHẢI exit 0.

**Bước 3 — verify Vitest baseline regression:**

```bash
cd Hub_All/frontend && npm run test -- --run
```

PHẢI exit 0. 5 baseline test file Phase 5 carry forward.

KHÔNG đụng:
- API call `api.createUser(...)` signature (body shape giữ — backend Phase 2 Plan 02-03 đã accept role='hub_admin').
- `setAddError` state setter (preserve).
- Validation pre-submit (`if (!newUserEmail || !newUserPassword) ...` etc — existing).
- handleEditUser, handleDeleteUser, handleOpenManageHub khác (Plan 03-03 scope).
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && npm run test -- --run && cd ../.. && grep -c "case 'HUB_ADMIN_REQUIRED'" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "HUB_ID_REQUIRED" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "AUTH_STATE_INCONSISTENT" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "Bạn không có quyền tạo user với role Admin toàn hệ thống" Hub_All/frontend/src/pages/UserManagement.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
    - `cd Hub_All/frontend && npm run test -- --run` exits 0 (Vitest 5 baseline PASS regression).
    - grep `case 'HUB_ADMIN_REQUIRED'` trong UserManagement.tsx >= 1.
    - grep `HUB_ID_REQUIRED` trong UserManagement.tsx >= 1.
    - grep `AUTH_STATE_INCONSISTENT` trong UserManagement.tsx >= 1.
    - grep `case 'FORBIDDEN'` trong UserManagement.tsx >= 1 HOẶC `'FORBIDDEN'` literal trong switch context.
    - grep `Bạn không có quyền tạo user với role Admin toàn hệ thống` trong UserManagement.tsx >= 1 (exact UI-SPEC §7.4 toast copy).
    - grep `Vui lòng chọn hub trước khi tạo user` trong UserManagement.tsx >= 1 (exact toast HUB_ID_REQUIRED).
    - grep `Lỗi hệ thống xác thực` trong UserManagement.tsx >= 1 (exact toast AUTH_STATE_INCONSISTENT).
    - grep `Bạn không có quyền thực hiện thao tác này` trong UserManagement.tsx >= 1 (exact toast FORBIDDEN).
    - Generic fallback `createRes.error?.message ?? 'Tạo user thất bại.'` PRESERVE (grep `Tạo user thất bại` vẫn còn >= 1).
    - Comment trace `FE-01` HOẶC `Plan 03-02` trong UserManagement.tsx error switch block >= 1.
    - KHÔNG đụng `api.createUser` signature (body shape preserve).
    - KHÔNG đụng pre-submit validation logic.
    - ESLint clean (`cd frontend && npm run lint` exits 0 — defer task verify nếu cần).
  </acceptance_criteria>
  <done>
    handleCreateUser error.code switch ADD đầy đủ 4 BE envelope mới Phase 2 (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN) với toast tiếng Việt exact UI-SPEC §7.4; generic fallback preserve; TypeScript + Vitest baseline PASS; FE-01 form refactor complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE form input ↔ BE POST /api/users | User input chuyển từ form (newUserRole, hub_id, email, name, password) qua HTTPS → BE validate Pydantic + assert_hub_admin_for + T-02-02-E role escalation block. BE authoritative trust boundary. |
| FE error envelope display ↔ BE envelope code | BE response shape `{success, data, error, meta}` với error.code Phase 2 (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN). FE switch consume + display toast tiếng Việt. Trust boundary trust BE envelope shape stable (M2 + v3.0 LOCKED). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FE-02-01 | Tampering | Hub_admin DevTools edit form state `newUserRole = 'admin'` + submit → bypass UI 3 option visible | mitigate | Backend Plan 02-03 T-02-02-E business logic block `if req.role == "admin" and user.role != "admin"` → 403 HUB_ADMIN_REQUIRED envelope. FE switch consume envelope.code → display toast tiếng Việt UI-SPEC §7.4. Defense in depth — BE authoritative. Severity: LOW. |
| T-03-FE-02-02 | Information Disclosure | Warning banner màu yellow + label "Admin toàn hệ thống" leak system structure cho non-admin user | accept | Warning label public knowledge (org structure transparent — Admin role visible across UI cùng làm việc M2/v3.0). Severity: VERY LOW (acceptable). |
| T-03-FE-02-03 | XSS via user name input | Hub_admin tạo user với name chứa `<script>` → render UserManagement table → XSS attack | mitigate | React auto-escape JSX (existing M2 pattern); KHÔNG dùng `dangerouslySetInnerHTML`; UserManagement.tsx table render qua `{user.name}` JSX escape default. Severity: LOW (React default). |
| T-03-FE-02-04 | Repudiation | BE envelope code KHÔNG match 4 case → FE fallback message generic → user KHÔNG biết exact lý do reject | mitigate | Fallback `error?.message ?? 'Tạo user thất bại.'` preserve BE message (BE Phase 2 ship message tiếng Việt cho 4 envelope mới). FE chỉ override message khi code match (defensive). Severity: VERY LOW. |
| T-03-FE-02-05 | Tampering | Hub_admin chỉnh form validation client-side (vd disable required) → submit empty form | mitigate | BE Pydantic validate body shape (email + hub_id + role + password length) → 422 envelope reject. FE pre-submit `if (!newUserEmail || !newUserPassword) ...` validation chỉ UX layer. Severity: LOW (BE authoritative). |
</threat_model>

<verification>
- `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
- `cd Hub_All/frontend && npm run lint` exits 0.
- `cd Hub_All/frontend && npm run test -- --run` exits 0 (5 baseline test PASS).
- UserManagement.tsx import `UserRole` type + state `useState<UserRole>` + 3 radio option (admin/hub_admin/viewer) + warning banner conditional `{newUserRole === 'admin' && ...}` + handleCreateUser switch 4 envelope code.
- Copywriting tiếng Việt exact UI-SPEC §7.1 + §7.4 (KHÔNG paraphrase).
- ARIA radiogroup/aria-describedby/aria-live đầy đủ.
- KHÔNG đụng list table, search, pagination, Manage modal (Plan 03-03 scope), delete confirm dialog.
</verification>

<success_criteria>
1. Form 3 radio option visible với copywriting tiếng Việt exact + description + warning banner conditional.
2. State type extend UserRole (4 value).
3. Error envelope handle 4 BE code mới Phase 2 (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN) với toast tiếng Việt exact UI-SPEC §7.4.
4. ARIA full (radiogroup/aria-describedby/aria-live/role=alert).
5. TypeScript + ESLint + Vitest baseline PASS.
6. FE-01 ROADMAP success criteria #1 satisfied (form 3 radio + warning banner + body POST role correct).
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/03-frontend-form-refactor/03-02-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 1 file UserManagement.tsx modify (state line 37 + radio group line 633-642 REPLACE + handleCreateUser error switch ADD + import UserRole type)
- 3 radio option + warning banner ship per UI-SPEC §6.1 + §7.1 + §8.1
- 4 BE envelope code switch handle (HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN)
- Plan 03-01 ship UserRole import + Plan 03-01 mockData hub_admin fixture chuẩn bị cho Plan 03-04 vitest test
- ARIA full WCAG AA (radiogroup/aria-describedby/aria-live)
- TypeScript + Vitest baseline PASS regression
- FE-01 ROADMAP success criteria #1 satisfied

Commit atomic: `feat(03-02): FE-01 UserManagement form 3 option radio + warning banner + error envelope switch` + body tiếng Việt nói tại sao (Phase 2 backend Plan 02-03 đã accept role='hub_admin' Pydantic Literal; FE refactor form 2→3 option rõ ràng + warning Admin toàn hệ thống + handle 4 envelope mới HUB_ADMIN_REQUIRED/HUB_ID_REQUIRED/AUTH_STATE_INCONSISTENT/FORBIDDEN; copywriting tiếng Việt exact UI-SPEC §7.1+§7.4; ARIA full WCAG AA).
</output>
</content>
