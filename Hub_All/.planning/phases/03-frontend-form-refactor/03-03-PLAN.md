---
phase: 03
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - frontend/src/pages/UserManagement.tsx
  - frontend/src/Layout.tsx
autonomous: true
requirements: [FE-02, FE-03]
tags: [frontend, react, hub-switcher, manage-modal, rbac-ui, accessibility, defense-in-depth]
must_haves:
  truths:
    - "Layout.tsx sidebar có hub switcher ABOVE nav (giữa logo + nav items) — render qua native <select> hoặc inline component HubSwitcher (Claude discretion <80 LOC inline / >=80 LOC extract riêng)"
    - "Hub switcher filter logic: currentUser.role === 'admin' ? allHubs : hubs.filter(h => h.code !== 'central' && userHubIds.includes(h.id)) — D-V3.1-Phase3-A hardcode 'central' slug LOCKED"
    - "userHubIds derive từ user?.roles?.map(r => r.hub_id) ?? [] (Option A LOCKED per PATTERNS.md — KHÔNG extend UserAPI.hub_ids)"
    - "Hub switcher onChange → window.location.href = `/${selectedHub.code}/` (full reload, Phase 5 PROXY pattern carry forward, KHÔNG SPA navigate vì cần re-fetch BE hub_name khác)"
    - "Empty state khi visibleHubs.length === 0: text 'Bạn chưa được gán hub nào — liên hệ admin.' với role='status' aria-live='polite'"
    - "Loading state khi isLoading || hubsLoading: skeleton aria-busy='true'"
    - "Active hub highlight compare h.code === CURRENT_HUB (existing services/api.ts:46 CURRENT_HUB const)"
    - "UserManagement.tsx Manage modal 3 option Admin/Quản lý hub/Viewer — 'Admin toàn hệ thống' option DISABLED khi currentUser.role !== 'admin' với tooltip title='Cần Admin toàn hệ thống' + aria-disabled='true' + Tailwind opacity-50 cursor-not-allowed"
    - "manageRole state type extend UserRole (4 value)"
    - "Manage modal submit (handleSubmitManageHub) error switch handle HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED envelope với toast tiếng Việt exact UI-SPEC §7.4"
    - "TypeScript + ESLint + Vitest baseline PASS clean"
  artifacts:
    - path: "frontend/src/Layout.tsx"
      provides: "Sidebar hub switcher dropdown filter — FE-02 visual + a11y contract"
      contains: "Hub đang xem"
    - path: "frontend/src/pages/UserManagement.tsx"
      provides: "Manage modal 3 option + disabled state Admin cho hub_admin — FE-03 visual + UX defense in depth"
      contains: "Cần Admin toàn hệ thống"
  key_links:
    - from: "Layout.tsx HubSwitcher visibleHubs filter"
      to: "user.roles[].hub_id derive userHubIds"
      via: "Option A — derive từ RoleAPI[] existing M2"
      pattern: "roles\\?\\.map\\(r => r\\.hub_id\\)"
    - from: "Layout.tsx HubSwitcher onChange"
      to: "window.location.href = `/${code}/`"
      via: "Full reload navigation Phase 5 PROXY pattern"
      pattern: "window\\.location\\.href"
    - from: "UserManagement.tsx Manage modal disabled button"
      to: "currentUser.role !== 'admin' check"
      via: "Defense in depth UX layer (BE Plan 02-03 T-02-02-E business logic block)"
      pattern: "currentRole !== 'admin'"
---

<objective>
Plan 03-03 v3.1 Phase 3 FE-02 + FE-03 — Wave 2 (sequential SAU Plan 03-02 vì cùng file UserManagement.tsx Manage modal section, parallel-able với Layout.tsx sidebar): hub switcher filter (FE-02) + Manage modal disabled option Admin cho hub_admin (FE-03) + error envelope switch ADD cho Manage submit.

**Purpose:**
- **FE-02:** Sidebar hub switcher ADD cho non-super-admin chỉ thấy hub được gán + ẩn central. Defense in depth — backend Plan 02-02 GET /api/hubs đã filter authoritative; FE chỉ UX layer.
- **FE-03:** Manage modal đóng UI loophole — UI hub_admin KHÔNG thấy option "Admin toàn hệ thống" để assign cho user khác. Backend Plan 02-03 T-02-02-E đã block role escalation business logic; FE Plan 03-03 add UX defense (tooltip + disabled visual).

**Output:**
- `frontend/src/Layout.tsx` — ADD imports (UserRole, HubAPI, api) + ADD `<HubSwitcher>` component inline (hoặc extract `frontend/src/components/HubSwitcher.tsx` nếu logic >= 80 LOC) + render block ABOVE sidebar nav (line ~217 area).
- `frontend/src/pages/UserManagement.tsx` — line ~44 state type extend (UserRole) + line ~941-980 Manage modal 3 option block REPLACE + disabled state cho admin option + handleSubmitManageHub error switch ADD.

**Pattern decisions Claude discretion (per CONTEXT.md `<decisions>`):**
- **HubSwitcher inline vs extract:** DEFAULT inline trong Layout.tsx. Nếu logic >= 80 LOC (skeleton + empty state + dropdown + filter + onChange) thì extract `frontend/src/components/HubSwitcher.tsx`. Plan-time estimate ~60 LOC inline OK (UI-SPEC §6.2 native `<select>` recommend).
- **userHubIds derivation Option A LOCKED:** Derive từ `user?.roles?.map(r => r.hub_id) ?? []`. RoleAPI[] existing M2 — KHÔNG extend UserAPI thêm `hub_ids: string[]` field (less invasive).
- **Manage modal disabled UX:** Native `disabled` attribute + Tailwind `opacity-50 cursor-not-allowed` + native browser `title=""` tooltip (KHÔNG dùng Tailwind plugin Radix — defer v4.0).
- **Error envelope handle Manage modal:** Switch case HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED (Plan 03-02 đã handle HUB_ADMIN_REQUIRED + HUB_ID_REQUIRED + AUTH_STATE_INCONSISTENT + FORBIDDEN cho form create; Plan 03-03 handle Manage modal context).

**Wave note:** Plan 03-03 touch CÙNG FILE UserManagement.tsx với Plan 03-02 → sequential execution sau Plan 03-02 ship (KHÔNG parallel — avoid file conflict). Layout.tsx independent file → có thể parallel với UserManagement.tsx Manage modal section trong Plan 03-03 (1 plan, 2 task song song execute).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Hub_All/CLAUDE.md
@Hub_All/.planning/STATE.md
@Hub_All/.planning/REQUIREMENTS.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-01-SUMMARY.md
@Hub_All/.planning/phases/03-frontend-form-refactor/03-02-SUMMARY.md
@Hub_All/frontend/src/Layout.tsx
@Hub_All/frontend/src/pages/UserManagement.tsx
@Hub_All/frontend/src/services/api.ts
@Hub_All/frontend/src/contexts/AuthContext.tsx

<interfaces>
<!-- Plan 03-01 ship type contract + Plan 03-02 ship form FE-01 — Plan 03-03 consume -->

From Hub_All/frontend/src/services/api.ts (Plan 03-01 ship):
```typescript
export type UserRole = 'admin' | 'hub_admin' | 'editor' | 'viewer';

export interface UserAPI {
  // ... existing fields
  role: UserRole;  // Plan 03-01 add
}

export interface RoleAPI {
  user_id: string;
  hub_id: string;
  role: string;
}

export interface HubAPI {
  id: string;
  name: string;
  code: string;
  // ...
}

export const CURRENT_HUB: string = PREFIX ?? 'central';  // line 46 Phase 5 PROXY-02 carry forward
```

From Hub_All/frontend/src/Layout.tsx (sidebar header line 1-65 + line 89 useAuth + line 217 nav):
```typescript
import { useAuth } from './contexts/AuthContext';
import { getBranding, getContrastTextColor } from './branding';
import { CURRENT_HUB } from './services/api';

const { user, logout } = useAuth();
const HUB_BRANDING = getBranding(CURRENT_HUB);
```

From Hub_All/frontend/src/pages/UserManagement.tsx (line 44 manageRole state + line 11 useAuth + Manage modal line ~941-980):
```typescript
const { user: currentUser } = useAuth();  // line ~11
const [manageRole, setManageRole] = useState<'admin' | 'viewer'>('viewer');  // line ~44
// Plan 03-03 CHANGE: useState<UserRole>('viewer')

// Manage modal existing button-style radio (line 941-980 — Plan 03-03 REPLACE 2 → 3 option + disabled)
<button onClick={() => setManageRole('admin')} className={...}>
  <p>Admin</p>
  <p>Toàn quyền quản lý Hub, User và Tài liệu.</p>
</button>
<button onClick={() => setManageRole('viewer')} className={...}>...</button>
```

From Hub_All/frontend/src/pages/UserManagement.tsx (handleSubmitManageHub line ~192-218 error path — Plan 03-03 ADD switch):
```typescript
const failed = results.filter(r => !r.success);
if (failed.length > 0) {
  setManageError(`${failed.length}/${results.length} cập nhật thất bại.`);
}
```

BE envelope shape (Plan 02-03 ship — Manage modal PATCH /api/users/{id}/role consume):
- HUB_ADMIN_REQUIRED (T-02-02-E business logic block role escalation)
- CROSS_HUB_USER_DELETE_DENIED (DELETE handler — Plan 03-03 Manage modal có DELETE flow? defer)
- FORBIDDEN (require_role generic)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layout.tsx ADD HubSwitcher component inline ABOVE sidebar nav (FE-02)</name>
  <read_first>
    - Hub_All/frontend/src/Layout.tsx (TOÀN FILE — biết imports line 1-30, useAuth line 89, sidebar header line 166-215, nav block line 217+, getBranding usage)
    - Hub_All/frontend/src/contexts/AuthContext.tsx (verify useAuth shape: `user: UserWithRoles | null` + `isLoading: boolean`)
    - Hub_All/frontend/src/services/api.ts (verify api.getHubs() method line ~172-174 + HubAPI interface line ~500-514 + CURRENT_HUB const line 46)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "6. frontend/src/Layout.tsx hub switcher FE-02" — concrete inline component pattern + Option A userHubIds derive)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §6.2 (visual layout + states + interaction) + §7.2 (copywriting) + §8.2 (a11y)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md decisions D-V3.1-Phase3-A (hardcode central slug) + D-V3.1-Phase3-B (currentUser source)
  </read_first>
  <behavior>
    - Test 1 (Grep imports): grep `import type { UserRole, HubAPI }` HOẶC `import type {.*UserRole.*}` + `HubAPI` trong Layout.tsx >= 1; grep `import { api }` (đã có hoặc thêm) trong Layout.tsx >= 1.
    - Test 2 (Grep HubSwitcher render): grep `Hub đang xem` trong Layout.tsx >= 1 (copywriting UI-SPEC §7.2).
    - Test 3 (Grep filter logic): grep `h.code !== 'central'` HOẶC `h\\.code\\s*!==\\s*['\"]central['\"]` trong Layout.tsx >= 1 (D-V3.1-Phase3-A LOCKED).
    - Test 4 (Grep navigate): grep `window.location.href = ` HOẶC `\\/\\$\\{.*code.*\\}\\/` trong Layout.tsx Hub switcher block >= 1 (full reload pattern).
    - Test 5 (Grep ARIA): grep `aria-label="Chọn hub đang xem"` HOẶC `aria-busy=` HOẶC `role="status"` trong Layout.tsx >= 1.
    - Test 6 (Grep empty state): grep `Bạn chưa được gán hub nào` trong Layout.tsx >= 1 (copywriting exact).
    - Test 7 (Grep Option A roles derive): grep `roles\\?\\.map\\(r => r\\.hub_id\\)` trong Layout.tsx >= 1 (Option A LOCKED).
    - Test 8 (TypeScript build): `cd frontend && npx tsc --noEmit` exits 0.
  </behavior>
  <action>
**Bước 1 — ADD imports** (top of Layout.tsx line 1-30):

Tìm dòng import services/api hiện tại:
```typescript
import { CURRENT_HUB } from './services/api';
```

REPLACE bằng (merge import — `api` + `CURRENT_HUB` cùng module):
```typescript
import { api, CURRENT_HUB } from './services/api';
import type { UserRole, HubAPI } from './services/api';
```

Nếu `api` đã được import ở chỗ khác trong Layout.tsx, KHÔNG duplicate (verify qua grep trước).

ADD `useEffect` + `useState` từ react nếu CHƯA có (đã có `useState` qua line 1 — verify).

**Bước 2 — ADD HubSwitcher component inline** TRƯỚC main `Layout` function declaration (đặt sau imports + trước `const Layout = ...` function — vùng module-level functions):

```tsx
// Plan 03-03 v3.1 Phase 3 FE-02 — Hub switcher sidebar component
// Source: .planning/phases/03-frontend-form-refactor/03-CONTEXT.md D-V3.1-Phase3-A LOCKED (hardcode 'central' slug)
//         .planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §6.2 + §7.2 + §8.2
//         .planning/phases/03-frontend-form-refactor/03-PATTERNS.md Option A userHubIds derive (roles[].hub_id)
function HubSwitcher() {
  const { user, isLoading } = useAuth();
  const currentUser = user?.user;
  const currentRole: UserRole = (currentUser?.role as UserRole) ?? 'viewer';
  // Option A LOCKED — derive userHubIds từ roles: RoleAPI[] (existing M2/v3.0)
  const userHubIds: string[] = user?.roles?.map((r) => r.hub_id) ?? [];

  const [hubs, setHubs] = React.useState<HubAPI[]>([]);
  const [hubsLoading, setHubsLoading] = React.useState(true);

  React.useEffect(() => {
    let mounted = true;
    api.getHubs().then((res) => {
      if (mounted && res.success && res.data) {
        setHubs(res.data);
      }
    }).finally(() => {
      if (mounted) setHubsLoading(false);
    });
    return () => { mounted = false; };
  }, []);

  // Filter logic — D-V3.1-Phase3-A LOCKED: hardcode 'central' slug + Plan 02-02 backend filter authoritative
  const visibleHubs = currentRole === 'admin'
    ? hubs
    : hubs.filter((h) => h.code !== 'central' && userHubIds.includes(h.id));

  if (isLoading || hubsLoading) {
    return (
      <div
        className="px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50"
        aria-busy="true"
        aria-label="Đang tải danh sách hub"
      >
        <div className="h-9 w-full rounded-md bg-slate-200 dark:bg-slate-800 animate-pulse" />
      </div>
    );
  }

  if (visibleHubs.length === 0) {
    return (
      <div
        role="status"
        aria-live="polite"
        className="px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50 text-sm text-slate-500 italic"
      >
        Bạn chưa được gán hub nào — liên hệ admin.
      </div>
    );
  }

  return (
    <div className="px-4 py-3 border-b border-slate-200/50 dark:border-slate-700/50">
      <label
        htmlFor="hub-switcher"
        className="block text-xs uppercase text-slate-500 font-medium mb-1"
      >
        Hub đang xem:
      </label>
      <select
        id="hub-switcher"
        value={CURRENT_HUB}
        onChange={(e) => { window.location.href = `/${e.target.value}/`; }}
        aria-label="Chọn hub đang xem"
        className="w-full rounded-md border border-slate-300 dark:border-slate-700 bg-white dark:bg-slate-900 px-3 py-2 text-sm focus:ring-2 focus:ring-accent focus:border-accent"
      >
        {visibleHubs.map((h) => (
          <option key={h.code} value={h.code}>
            {h.name} ({h.code})
          </option>
        ))}
      </select>
    </div>
  );
}
```

**Lưu ý import React:**
- Nếu Layout.tsx hiện tại `import React, { useState, useEffect } from 'react'` thì `React.useState`/`React.useEffect` OK HOẶC dùng `useState`/`useEffect` trực tiếp (cùng module).
- Verify qua grep import line top of file → adapt accordingly.

**Bước 3 — INSERT `<HubSwitcher />` render** ABOVE sidebar nav (line ~217 area — `<nav className={cn("flex-1 p-4 space-y-1", ...)}>`):

Tìm khu vực sau sidebar header (line ~215 — `</div>` đóng sidebar header với `border-b`) + TRƯỚC `<nav className="flex-1 p-4 space-y-1">`:

```tsx
        </div>
        {/* Plan 03-03 v3.1 Phase 3 FE-02 — Hub switcher sidebar */}
        {(!collapsed || isMobileMenuOpen) && <HubSwitcher />}
        <nav className={cn("flex-1 p-4 space-y-1", ...)}>
```

Condition `(!collapsed || isMobileMenuOpen)` reuse pattern existing Layout.tsx sidebar header line ~170 (chỉ render khi sidebar expanded). Hub switcher hide khi sidebar collapsed (desktop) — match UX existing logo+title.

**Bước 4 — verify TypeScript build:**

```bash
cd Hub_All/frontend && npx tsc --noEmit
```

PHẢI exit 0.

**Bước 5 — Estimate LOC + decision extract:**

Count LOC của `function HubSwitcher() { ... }` block. Nếu < 80 LOC → keep inline. Nếu >= 80 LOC → extract to `frontend/src/components/HubSwitcher.tsx` (separate file) + import vào Layout.tsx. Plan-time estimate ~60 LOC inline (UI-SPEC native `<select>` approach) → expect inline OK.

KHÔNG đụng:
- Logo + branding header (line 166-215 — Phase 5 PROXY-04 carry forward).
- Nav items list (line 217+ existing pattern).
- Logout button, theme toggle, notification bell (existing).
- Mobile menu state, collapsed state logic (existing).
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && cd ../.. && grep -c "import type { UserRole, HubAPI }" Hub_All/frontend/src/Layout.tsx && grep -c "Hub đang xem" Hub_All/frontend/src/Layout.tsx && grep -c "h.code !== 'central'" Hub_All/frontend/src/Layout.tsx && grep -c "window.location.href" Hub_All/frontend/src/Layout.tsx && grep -c 'aria-label="Chọn hub đang xem"' Hub_All/frontend/src/Layout.tsx && grep -c "Bạn chưa được gán hub nào" Hub_All/frontend/src/Layout.tsx && grep -c "roles\\?\\.map" Hub_All/frontend/src/Layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
    - grep `import type { UserRole, HubAPI }` trong Layout.tsx >= 1 (type-only imports).
    - grep `import.*api.*from './services/api'` HOẶC `api,` trong import block Layout.tsx >= 1 (api runtime).
    - grep `function HubSwitcher` trong Layout.tsx >= 1 (component declared).
    - grep `Hub đang xem` trong Layout.tsx >= 1 (label exact UI-SPEC §7.2).
    - grep `h.code !== 'central'` trong Layout.tsx >= 1 (D-V3.1-Phase3-A hardcode slug).
    - grep `window.location.href = \`/\${` HOẶC `window.location.href =` trong Layout.tsx Hub switcher context >= 1.
    - grep `aria-label="Chọn hub đang xem"` trong Layout.tsx >= 1 (a11y ARIA).
    - grep `Bạn chưa được gán hub nào` trong Layout.tsx >= 1 (empty state copy exact).
    - grep `roles\\?\\.map` trong Layout.tsx >= 1 (Option A userHubIds derive).
    - grep `aria-busy="true"` HOẶC `role="status"` trong Layout.tsx >= 1 (loading + empty state a11y).
    - grep `api.getHubs()` trong Layout.tsx >= 1 (fetch hub list).
    - grep `value={CURRENT_HUB}` trong Layout.tsx >= 1 (active hub).
    - grep `D-V3.1-Phase3-A` HOẶC `FE-02` trong Layout.tsx comment >= 1 (trace decision).
    - LOC count function HubSwitcher (grep -c `^` từ `function HubSwitcher` đến `^}` đầu tiên) <= 80 (inline OK). Nếu > 80, extract `frontend/src/components/HubSwitcher.tsx` (acceptance update: file mới + import vào Layout.tsx).
    - KHÔNG đụng logo + branding header (line 166-215 — Phase 5 PROXY-04).
    - KHÔNG đụng nav items list logic.
    - KHÔNG đụng logout / theme toggle / notification bell.
  </acceptance_criteria>
  <done>
    Layout.tsx có HubSwitcher inline component ship + render ABOVE sidebar nav (visible khi sidebar expanded) + filter logic D-V3.1-Phase3-A LOCKED + Option A userHubIds derive + loading/empty state + ARIA + copywriting exact UI-SPEC §7.2; TypeScript PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserManagement.tsx Manage modal 3 option + disabled state Admin cho hub_admin (FE-03)</name>
  <read_first>
    - Hub_All/frontend/src/pages/UserManagement.tsx (Manage modal block — line ~941-980; manageRole state line ~44; handleSubmitManageHub line ~192-218; currentUser useAuth line ~11)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-PATTERNS.md (section "5. UserManagement.tsx Manage modal FE-03" — concrete REPLACE pattern + button-style disabled UX)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §6.3 (visual layout disabled) + §7.3 (copywriting tooltip + ARIA) + §7.4 (error envelope toast) + §8.1 (a11y disabled)
    - Hub_All/.planning/phases/03-frontend-form-refactor/03-CONTEXT.md decisions D-V3.1-Phase3-B (currentUser.role consume) + Manage modal disabled logic
  </read_first>
  <behavior>
    - Test 1 (Grep state extend): grep `manageRole.*UserRole` HOẶC `useState<UserRole>` (second occurrence — Plan 03-02 đã có 1) trong UserManagement.tsx >= 2 (newUserRole + manageRole đều UserRole).
    - Test 2 (Grep 3 option Manage modal): grep `Admin toàn hệ thống` trong UserManagement.tsx >= 2 (1 ở Add modal form Plan 03-02 + 1 ở Manage modal Plan 03-03).
    - Test 3 (Grep tooltip exact): grep `Cần Admin toàn hệ thống` trong UserManagement.tsx >= 1 (tooltip copy UI-SPEC §7.3).
    - Test 4 (Grep disabled logic): grep `disabled=` HOẶC `aria-disabled` trong Manage modal section UserManagement.tsx >= 1.
    - Test 5 (Grep currentRole check): grep `currentRole !== 'admin'` HOẶC `currentRole === 'admin'` HOẶC `isCurrentSuper` trong UserManagement.tsx >= 1.
    - Test 6 (Grep error switch CROSS_HUB): grep `CROSS_HUB_USER_DELETE_DENIED` trong UserManagement.tsx >= 1 (Manage modal error switch).
    - Test 7 (Grep error switch HUB_ADMIN_REQUIRED Manage context): grep `Bạn không có quyền gán role này` trong UserManagement.tsx >= 1 (exact toast UI-SPEC §7.4 Manage context).
    - Test 8 (TypeScript build): `cd frontend && npx tsc --noEmit` exits 0.
  </behavior>
  <action>
**Bước 1 — CHANGE manageRole state type** (line ~44):

Tìm:
```typescript
const [manageRole, setManageRole] = useState<'admin' | 'viewer'>('viewer');
```

REPLACE bằng:
```typescript
const [manageRole, setManageRole] = useState<UserRole>('viewer');
```

(UserRole đã import ở Plan 03-02 Task 1 — reuse import line top of file.)

**Bước 2 — REPLACE Manage modal role selector block** (line ~941-980 — 2 button cũ thay 3 button + disabled):

Tìm `<div className="space-y-3">` chứa 2 button cũ ("Admin" + "Viewer" — line 941-980 area) REPLACE TOÀN BỘ bằng:

```tsx
{(() => {
  const currentRole: UserRole = (currentUser?.user?.role as UserRole) ?? 'viewer';
  const isCurrentSuper = currentRole === 'admin';

  return (
    <div className="space-y-3">
      <label className="text-sm font-medium text-slate-600 dark:text-slate-300">
        Quyền <span className="text-[11px] font-normal text-slate-400">(áp cho tất cả hub user thuộc về)</span>
      </label>
      <div className="grid grid-cols-1 gap-2">
        {/* Option 1: Admin toàn hệ thống — DISABLED nếu hub_admin (Plan 03-03 FE-03 defense in depth) */}
        <button
          onClick={() => isCurrentSuper && setManageRole('admin')}
          disabled={!isCurrentSuper}
          aria-disabled={!isCurrentSuper}
          title={!isCurrentSuper ? 'Cần Admin toàn hệ thống' : undefined}
          aria-describedby={!isCurrentSuper ? 'manage-admin-tooltip-desc' : undefined}
          className={cn(
            'flex items-center justify-between p-3 rounded-xl border transition-all text-left',
            manageRole === 'admin'
              ? 'border-brand-indigo bg-brand-indigo/[0.03] dark:bg-brand-indigo/[0.08] ring-1 ring-brand-indigo'
              : 'border-slate-200 dark:border-slate-700 hover:border-slate-300 dark:hover:border-slate-600 bg-white dark:bg-slate-800',
            !isCurrentSuper && 'opacity-50 cursor-not-allowed hover:border-slate-200 dark:hover:border-slate-700'
          )}
        >
          <div>
            <p className="text-sm font-bold text-slate-900 dark:text-white">Admin toàn hệ thống</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Toàn quyền quản lý Hub, User và Tài liệu.</p>
          </div>
          {manageRole === 'admin' && (
            <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white">
              <CheckCircle2 size={12} />
            </div>
          )}
        </button>

        {/* Option 2: Quản lý hub này (hub_admin) — luôn enable */}
        <button
          onClick={() => setManageRole('hub_admin')}
          className={cn(
            'flex items-center justify-between p-3 rounded-xl border transition-all text-left',
            manageRole === 'hub_admin'
              ? 'border-brand-indigo bg-brand-indigo/[0.03] dark:bg-brand-indigo/[0.08] ring-1 ring-brand-indigo'
              : 'border-slate-200 dark:border-slate-700 hover:border-slate-300 dark:hover:border-slate-600 bg-white dark:bg-slate-800'
          )}
        >
          <div>
            <p className="text-sm font-bold text-slate-900 dark:text-white">Quản lý hub này</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Quản lý user + document trong hub được chỉ định.</p>
          </div>
          {manageRole === 'hub_admin' && (
            <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white">
              <CheckCircle2 size={12} />
            </div>
          )}
        </button>

        {/* Option 3: Viewer */}
        <button
          onClick={() => setManageRole('viewer')}
          className={cn(
            'flex items-center justify-between p-3 rounded-xl border transition-all text-left',
            manageRole === 'viewer'
              ? 'border-brand-indigo bg-brand-indigo/[0.03] dark:bg-brand-indigo/[0.08] ring-1 ring-brand-indigo'
              : 'border-slate-200 dark:border-slate-700 hover:border-slate-300 dark:hover:border-slate-600 bg-white dark:bg-slate-800'
          )}
        >
          <div>
            <p className="text-sm font-bold text-slate-900 dark:text-white">Viewer</p>
            <p className="text-xs text-slate-500 dark:text-slate-400 mt-0.5">Xem document + search trong hub được gán.</p>
          </div>
          {manageRole === 'viewer' && (
            <div className="w-5 h-5 rounded-full bg-brand-indigo flex items-center justify-center text-white">
              <CheckCircle2 size={12} />
            </div>
          )}
        </button>
      </div>

      {/* a11y: hidden helper text linked via aria-describedby (UI-SPEC §8.1) */}
      {!isCurrentSuper && (
        <p id="manage-admin-tooltip-desc" className="sr-only">
          Tùy chọn này yêu cầu quyền Admin toàn hệ thống.
        </p>
      )}
    </div>
  );
})()}
```

**Bước 3 — ADD error.code switch trong handleSubmitManageHub** (line ~207-209 area):

Tìm:
```typescript
const failed = results.filter(r => !r.success);
if (failed.length > 0) {
  setManageError(`${failed.length}/${results.length} cập nhật thất bại.`);
}
```

REPLACE bằng:
```typescript
const failed = results.filter(r => !r.success);
if (failed.length > 0) {
  const code = failed[0].error?.code;
  let msg = `${failed.length}/${results.length} cập nhật thất bại.`;
  // Plan 03-03 v3.1 Phase 3 FE-03 — switch BE envelope code Phase 2 v3.1 Manage modal context
  // Source: .planning/phases/02-backend-rbac-enforcement/02-CONTEXT.md envelope spec
  //         .planning/phases/03-frontend-form-refactor/03-UI-SPEC.md §7.4 toast copy tiếng Việt
  switch (code) {
    case 'HUB_ADMIN_REQUIRED':
      msg = 'Bạn không có quyền gán role này. Liên hệ Super Admin.';
      break;
    case 'CROSS_HUB_USER_DELETE_DENIED':
      msg = 'Không thể xóa user thuộc nhiều hub. Liên hệ Super Admin để xử lý.';
      break;
    case 'FORBIDDEN':
      msg = 'Bạn không có quyền thực hiện thao tác này.';
      break;
  }
  setManageError(msg);
}
```

**Bước 4 — verify TypeScript build + Vitest baseline:**

```bash
cd Hub_All/frontend && npx tsc --noEmit && npm run test -- --run
```

PHẢI exit 0.

KHÔNG đụng:
- Manage modal hub select dropdown (existing).
- Manage modal close/save button logic (existing).
- handleOpenManageHub function (existing).
- Add User form section (Plan 03-02 đã refactor).
- List table, search, pagination (existing).
  </action>
  <verify>
    <automated>cd Hub_All/frontend && npx tsc --noEmit && npm run test -- --run && cd ../.. && grep -c "Cần Admin toàn hệ thống" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "CROSS_HUB_USER_DELETE_DENIED" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "Bạn không có quyền gán role này" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "isCurrentSuper" Hub_All/frontend/src/pages/UserManagement.tsx && grep -c "manage-admin-tooltip-desc" Hub_All/frontend/src/pages/UserManagement.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
    - `cd Hub_All/frontend && npm run test -- --run` exits 0 (Vitest 5 baseline regression PASS).
    - grep `useState<UserRole>` trong UserManagement.tsx >= 2 (newUserRole Plan 03-02 + manageRole Plan 03-03).
    - grep `Admin toàn hệ thống` trong UserManagement.tsx >= 2 (Add modal Plan 03-02 + Manage modal Plan 03-03).
    - grep `Cần Admin toàn hệ thống` trong UserManagement.tsx >= 1 (tooltip exact UI-SPEC §7.3).
    - grep `Quản lý hub này` trong UserManagement.tsx >= 2 (Add + Manage cả 2 modal có option hub_admin).
    - grep `disabled={!isCurrentSuper}` HOẶC `disabled=` trong Manage modal section >= 1 (defense in depth UX).
    - grep `aria-disabled` trong UserManagement.tsx >= 1 (a11y).
    - grep `opacity-50 cursor-not-allowed` trong UserManagement.tsx >= 1 (Tailwind disabled visual).
    - grep `isCurrentSuper` trong UserManagement.tsx >= 1 (currentRole check derive).
    - grep `manage-admin-tooltip-desc` trong UserManagement.tsx >= 1 (sr-only ARIA hidden helper text).
    - grep `Tùy chọn này yêu cầu quyền Admin toàn hệ thống` trong UserManagement.tsx >= 1 (sr-only copy exact UI-SPEC §7.3).
    - grep `CROSS_HUB_USER_DELETE_DENIED` trong UserManagement.tsx >= 1 (Manage modal error switch).
    - grep `case 'HUB_ADMIN_REQUIRED'` trong UserManagement.tsx >= 2 (form Plan 03-02 + Manage modal Plan 03-03 đều handle).
    - grep `Bạn không có quyền gán role này` trong UserManagement.tsx >= 1 (exact toast UI-SPEC §7.4 Manage context — KHÁC form context "tạo user").
    - grep `Không thể xóa user thuộc nhiều hub` trong UserManagement.tsx >= 1 (exact toast CROSS_HUB_USER_DELETE_DENIED).
    - grep `FE-03` HOẶC `Plan 03-03` comment trong UserManagement.tsx >= 1 (trace decision).
    - KHÔNG đụng Add User form section (Plan 03-02 ship).
    - KHÔNG đụng Manage modal hub select dropdown.
    - KHÔNG đụng handleOpenManageHub, handleDeleteUser, handleEditUser khác.
  </acceptance_criteria>
  <done>
    Manage modal 3 option ship với "Admin toàn hệ thống" disabled cho hub_admin + tooltip "Cần Admin toàn hệ thống" + ARIA full (aria-disabled + aria-describedby sr-only); manageRole state UserRole 4 value; handleSubmitManageHub error switch handle 3 envelope (HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + FORBIDDEN); TypeScript + Vitest baseline PASS; FE-03 ROADMAP success criteria #3 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE hub switcher filter ↔ BE GET /api/hubs filter | FE filter UX layer; BE Plan 02-02 GET /api/hubs filter authoritative (defense in depth). Trust boundary BE-side. |
| FE Manage modal disabled UX ↔ BE PATCH /api/users/{id}/role business logic | FE disabled UX layer; BE Plan 02-03 T-02-02-E business logic block role escalation (`if req.role == "admin" and user.role != "admin"` → 403). Trust boundary BE-side. |
| FE userHubIds derive (Option A) ↔ JWT claim hub_ids | userHubIds derive từ `user.roles[].hub_id` (RoleAPI[] payload từ /api/auth/me). Stale FE state — refresh fetch /api/auth/me; backend Layer 3 JWT hub_ids check (v3.0 SSO-04 carry forward) authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-FE-03-01 | Tampering | Hub_admin DevTools edit `currentRole = 'admin'` React state + click disabled Admin option → submit PATCH /api/users/{id}/role với role='admin' | mitigate | Backend Plan 02-03 T-02-02-E business logic block `req.role == "admin" and user.role != "admin"` → 403 HUB_ADMIN_REQUIRED envelope. Manage modal Task 2 error switch consume envelope.code → display toast tiếng Việt "Bạn không có quyền gán role này. Liên hệ Super Admin." Defense in depth — BE authoritative. Severity: LOW. |
| T-03-FE-03-02 | Tampering | Hub_admin DevTools edit hub switcher `visibleHubs` → bypass filter + click central hub redirect | mitigate | Backend Caddy regex (Phase 5 T-5-02 carry forward) + JWT hub_ids check Layer 3 (v3.0 SSO-04) → hub_admin POST/GET endpoint hub khác → 403 CROSS_HUB_ACCESS_DENIED. FE filter chỉ UX layer. Full reload `window.location.href = '/dmd/'` → BE re-fetch with new hub_name → fail-loud nếu KHÔNG có quyền. Severity: LOW. |
| T-03-FE-03-03 | Information Disclosure | Hub switcher dropdown leak names hub_admin chưa được gán (vd `<option>Đỗ Minh Đường</option>` cho viewer khác) | mitigate | Filter logic `userHubIds.includes(h.id)` exclude hub KHÔNG gán. Backend GET /api/hubs Plan 02-02 filter authoritative `WHERE user_id` join → viewer/hub_admin KHÔNG thấy hub khác trong response. FE filter là double check. Severity: VERY LOW. |
| T-03-FE-03-04 | XSS via hub.name | BE response GET /api/hubs trả hub.name chứa `<script>` → render `<option>{h.name}</option>` JSX → XSS | mitigate | React auto-escape JSX text content (existing M2 pattern). hub.name input validated khi tạo hub (BE Pydantic + Phase 5 hub regex). Severity: LOW. |
| T-03-FE-03-05 | DoS | Layout.tsx mỗi mount call `api.getHubs()` → BE rate limit hit (4 hub × 5 tab open × N user) | mitigate | api.getHubs() Phase 2 ship (Plan 02-02 verify-only) — endpoint đã có rate limit M2 + Phase 6 Redis cache TTL 300s. Plan 03-03 KHÔNG add new fetch — reuse existing. Severity: VERY LOW. |
| T-03-FE-03-06 | Spoofing | DevTools edit `currentUser.role = 'admin'` → all 3 option Manage modal enable | mitigate | FE Manage modal UX layer; BE Plan 02-03 T-02-02-E business logic block role escalation 403 envelope. JWT signature verify Layer 1 (Phase 1 SSO carry forward) → stale/tampered token reject. Severity: LOW. |
</threat_model>

<verification>
- `cd Hub_All/frontend && npx tsc --noEmit` exits 0.
- `cd Hub_All/frontend && npm run lint` exits 0.
- `cd Hub_All/frontend && npm run test -- --run` exits 0 (5 baseline test PASS — Layout/Login/api/registry/App + Plan 03-02 test nếu có).
- Layout.tsx có HubSwitcher inline component + render ABOVE sidebar nav với filter logic D-V3.1-Phase3-A LOCKED + Option A userHubIds + loading/empty state + ARIA + copywriting exact UI-SPEC §7.2.
- UserManagement.tsx Manage modal 3 option (Admin/Quản lý hub/Viewer) với "Admin toàn hệ thống" disabled cho hub_admin + tooltip + ARIA + handleSubmitManageHub error switch 3 envelope.
- KHÔNG đụng Add User form (Plan 03-02 scope), list table, search, pagination, logo/branding header, nav items, logout/theme/notification.
</verification>

<success_criteria>
1. Sidebar hub switcher ABOVE nav (visible khi sidebar expanded) filter central + chỉ show hub được gán cho non-super-admin.
2. Manage modal 3 option với Admin toàn hệ thống disabled + tooltip "Cần Admin toàn hệ thống" cho hub_admin.
3. userHubIds derive từ `user?.roles?.map(r => r.hub_id) ?? []` (Option A LOCKED).
4. Error envelope handle HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + FORBIDDEN ở Manage modal submit.
5. ARIA full (aria-label/aria-busy/role=status/aria-disabled/aria-describedby sr-only).
6. Copywriting tiếng Việt exact UI-SPEC §7.2 + §7.3 + §7.4.
7. TypeScript + ESLint + Vitest baseline PASS.
8. FE-02 + FE-03 ROADMAP success criteria #2 + #3 satisfied.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `Hub_All/.planning/phases/03-frontend-form-refactor/03-03-SUMMARY.md` (tiếng Việt có dấu) ghi nhận:
- 2 file frontend modify atomic (Layout.tsx HubSwitcher inline ADD + UserManagement.tsx Manage modal 3 option REPLACE + handleSubmitManageHub error switch ADD + manageRole state extend UserRole)
- FE-02 hub switcher filter D-V3.1-Phase3-A LOCKED hardcode 'central' slug + Option A userHubIds derive
- FE-03 Manage modal disabled UX defense in depth (BE Plan 02-03 T-02-02-E authoritative)
- HubSwitcher inline ~60 LOC keep inline (KHÔNG extract — meets <80 LOC threshold per Claude discretion)
- 3 BE envelope code switch handle Manage modal (HUB_ADMIN_REQUIRED + CROSS_HUB_USER_DELETE_DENIED + FORBIDDEN) — DIFFERENT toast text từ Plan 03-02 form context (Manage modal nói "gán role" thay "tạo user")
- ARIA full WCAG AA (aria-label/aria-busy/role=status/aria-disabled/aria-describedby sr-only helper text)
- TypeScript + Vitest baseline PASS regression
- FE-02 + FE-03 ROADMAP success criteria #2 + #3 satisfied
- Plan 03-04 Wave 3 unblocked (3 vitest test file mới — form 3 option + hub switcher filter + Manage modal disabled scenario)

Commit atomic: `feat(03-03): FE-02 hub switcher + FE-03 Manage modal disabled Admin cho hub_admin` + body tiếng Việt nói tại sao (D-V3.1-Phase3-A LOCKED hardcode 'central' slug filter; Option A userHubIds derive từ roles[].hub_id less invasive; defense in depth Manage modal disabled UX layer + BE Plan 02-03 T-02-02-E authoritative; HubSwitcher inline <80 LOC; ARIA full WCAG AA; copywriting tiếng Việt exact UI-SPEC §7.2+§7.3+§7.4).
</output>
</content>
